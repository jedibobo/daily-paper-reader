---
title: Microarchitectural Co-Optimization for Sustained Throughput of RISC-V Multi-Lane Chaining Vector Processors
title_zh: 面向 RISC-V 多车道链接向量处理器持续吞吐量的微架构协同优化
authors: "Weiying Wang, Zhiwei Zhang"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22314v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 旨在持续吞吐量的微架构协同优化
tldr: 本研究针对RISC-V向量处理器Ara在多流水线链接执行中的性能瓶颈，提出了一套微架构协同优化方案。通过建立理想执行模型，深入分析并解决了存储端数据供应、控制端依赖管理及操作数传递冲突等关键效率问题。在不增加硬件资源的前提下，优化后的Ara-Opt实现了1.33倍的几何平均加速比，显著提升了流式和高吞吐量负载的性能，使其更接近理论性能上限。
selection_source: fresh_fetch
motivation: 现代RISC-V向量处理器因微架构效率低下，导致其实际持续吞吐量往往远低于理论性能上限。
method: 通过建立理想的多流水线链接执行模型识别瓶颈，并针对存储访问、指令发射控制和操作数传递路径实施了协同优化。
result: "在不改变硬件资源约束的情况下，Ara-Opt实现了1.33倍的几何平均加速比，并将多个核心算子的性能差距缩小了59.3%至93.7%。"
conclusion: 微架构层面的协同优化能有效回收因设计缺陷损失的吞吐量，使向量处理器在现有资源约束下发挥出接近极限的性能。
---

## 摘要
现代 RISC 向量处理器依靠多车道并行与链接（chaining）的协同作用来实现高持续吞吐量，然而由于微架构效率低下，其实际性能往往大幅低于理论性能极限。在这项工作中，我们以开源 RVV 处理器 Ara 为目标平台，分析了其持续吞吐量损失的来源，并据此对设计进行了优化。我们首先建立了一个理想的多车道链接执行模型，作为向量后端理想稳态推进的微架构参考。基于该模型，我们将 Ara 的关键瓶颈归因于三条关键执行路径上的低效：存储侧的数据供应与事务发布低效、控制侧由保守的依赖管理和发射控制引起的低效，以及操作数传递过程中由访问冲突和结果传播开销引起的低效。为了解决这些瓶颈，我们提出了一套协调的微架构优化方案。实验结果表明，在不增加原始内存带宽或改变主处理器配置的情况下，Ara-Opt 相比基准 Ara 实现了 1.33 倍的几何平均加速。在基于 Roofline 模型的归一化下，几何平均差距弥补率（gap-closed ratio）达到 12.2%。特别是 scal、axpy、ger 和 gemm 分别实现了约 2.41 倍、1.60 倍、1.52 倍和 1.42 倍的加速，对应的差距弥补率分别为 93.7%、88.9%、78.3% 和 59.3%。这些结果表明，所提出的方法在硬件资源约束基本不变的情况下，能够有效恢复 Ara 因微架构低效而损失的持续吞吐能力，并使常规流式处理和高吞吐量工作负载的实现点显著逼近理论性能极限。

## Abstract
Modern RISC vector processors rely on the synergy of multi-lane parallelism and chaining to achieve high sustained throughput, yet their achieved performance often falls substantially short of the theoretical performance bound due to microarchitectural inefficiencies. In this work, we take the open-source RVV processor Ara as the target platform and analyze the sources of its sustained-throughput loss and optimize the design accordingly. We first establish an ideal multi-lane chaining execution model as a microarchitectural reference for the ideal steady-state progression of the vector backend. Based on this model, we attribute Ara's key bottlenecks to inefficiencies along three critical execution paths: memory-side inefficiencies in data supply and transaction issuance, control-side inefficiencies caused by conservative dependence management and issue control, and operand-delivery inefficiencies caused by access conflicts and result-propagation overhead. To address these bottlenecks, we propose a coordinated set of microarchitectural optimizations. Experimental results show that, without increasing raw memory bandwidth or changing the main processor configuration, Ara-Opt achieves a geometric-mean speedup of 1.33x over baseline Ara. Under roofline-based normalization, the geometric-mean gap-closed ratio reaches 12.2%. In particular, scal, axpy, ger, and gemm achieve speedups of approximately 2.41x, 1.60x, 1.52x, and 1.42x, with corresponding gap-closed ratios of 93.7%, 88.9%, 78.3%, and 59.3%, respectively. These results show that the proposed method can effectively recover sustained-throughput capability lost to microarchitectural inefficiencies in Ara under essentially unchanged hardware resource constraints, and move the implementation points of regular streaming and high-throughput workloads significantly closer to the theoretical performance bound.

---

## 论文详细总结（自动生成）

### 论文总结：面向 RISC-V 多车道链接向量处理器持续吞吐量的微架构协同优化

#### 1. 核心问题与整体含义
*   **研究动机**：现代 RISC-V 向量处理器（如 Ara）虽然通过多车道并行和链接（Chaining）技术理论上能提供极高吞吐量，但在实际运行中，受限于微架构效率，其实际性能往往远低于理论上限（Roofline 边界）。
*   **核心问题**：在不增加原始内存带宽和计算资源（车道数）的前提下，如何识别并消除导致持续吞吐量损失的微架构瓶颈，使处理器性能逼近理论极限。

#### 2. 方法论
论文提出了一套针对执行路径的协同优化方案，核心步骤包括：
*   **建立理想模型**：构建“理想多车道链接执行模型”，作为衡量性能损失的基准参考。
*   **瓶颈诊断**：将性能损失归纳为三条路径的低效：
    1.  **存储侧**：数据供应不连续，事务组织效率低。
    2.  **控制侧**：依赖管理过于保守（如 WAR 依赖释放过晚），发射控制存在冗余气泡。
    3.  **操作数传递侧**：向量寄存器堆（VRF）访问冲突，结果传播路径过长。
*   **关键技术细节**：
    *   **解耦存储前端与 Next-VL 预取**：引入描述符驱动的地址生成，通过 AXI ID 区分需求与预取请求，实现跨向量长度（VL）的主动数据供应。
    *   **早期读依赖释放**：一旦操作数读取完成即释放读依赖，无需等待整条指令结束，从而提前后续写指令的执行。
    *   **多源转发与双源操作数队列**：建立多路旁路转发网络，使结果可绕过 VRF 直接进入操作数队列，缩短传播延迟并减轻 VRF 访问压力。

#### 3. 实验设计
*   **目标平台**：基于开源 RVV 处理器 Ara 的 RTL 实现（配置为 VLEN=1024, DLEN=256）。
*   **Benchmark**：选取了 11 个具有代表性的向量内核，涵盖：
    *   **流式负载**：`scal`, `axpy`。
    *   **BLAS 库函数**：`dotp`, `gemv`, `symv`, `ger`, `trsm`, `syrk`, `gemm`。
    *   **不规则/混合负载**：`spmv`, `dwt`。
*   **对比方法**：以原始基准 Ara 处理器作为 Baseline，对比优化后的 Ara-Opt。

#### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练算力，因为该研究属于硬件架构设计与仿真。实验基于周期精确（Cycle-accurate）的 RTL 模型进行仿真测量。
*   **硬件约束**：实验严格限制在不增加内存带宽、不改变 AXI 接口宽度（128-bit）和车道配置的条件下进行。

#### 5. 实验数量与充分性
*   **实验规模**：针对 11 种算子进行了性能对比，并对 `scal` 和 `gemm` 进行了问题规模（向量长度、矩阵大小）的灵敏度分析。
*   **充分性评价**：实验设计较为充分。作者不仅提供了加速比数据，还利用 Roofline 模型进行了归一化分析，并深入统计了“仅存储暴露比例”、“车道利用率”和“VRF 冲突率”等底层指标，客观地解释了性能提升的来源。

#### 6. 主要结论与发现
*   **性能提升**：Ara-Opt 实现了 **1.33倍** 的几何平均加速比。
*   **差距弥补**：在流式算子（如 `scal`）上，性能差距弥补率高达 **93.7%**；在计算密集型算子（如 `gemm`）上达到 **59.3%**。
*   **瓶颈转移**：研究发现，对于规约类算子（如 `dotp`, `gemv`），性能提升有限（仅约 5%），原因在于其瓶颈在于指令链尾部的串行规约延迟，而非前端供应或依赖管理。

#### 7. 优点
*   **系统性强**：从存储、控制、数据三条路径进行协同优化，而非单一点的改进。
*   **资源效率高**：在“零成本”（不增加带宽和核心规模）的情况下实现了显著的性能回收。
*   **分析深入**：通过理想模型量化了微架构低效的具体损失，为后续向量处理器设计提供了清晰的指导。

#### 8. 不足与局限
*   **算子局限性**：对规约类（Reduction）和复杂访存模式的负载优化效果不明显，这反映了当前架构在处理非线性数据流时的固有局限。
*   **硬件开销未详述**：虽然未增加主资源，但引入预取缓冲、转发网络和复杂的控制逻辑会增加额外的逻辑门数（Area）和功耗（Power），文中未给出具体的面积和功耗开销评估。
*   **应用场景**：主要针对长向量、规则数据流场景，对于短向量或高度分支化的代码提升可能有限。

（完）
