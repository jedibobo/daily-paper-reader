---
title: Hardware-Software Co-Design for Event-Driven SNN Deployment on Low-Cost Neuromorphic FPGAs
title_zh: 低成本类脑 FPGA 上事件驱动 SNN 部署的软硬件协同设计
authors: "Jiwoon Lee, Souvik Chakraborty, Syed Bahauddin Alam, Cheolsoo Park"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22179v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于在FPGA上部署SNN的软硬件协同设计框架
tldr: 本研究针对低成本FPGA上脉冲神经网络（SNN）部署中软硬件脱节的问题，提出了一种软硬件协同设计框架。该框架实现了从PyTorch定义的SNN到事件驱动FPGA执行的确定性部署，通过统一的导出工件确保软件仿真与硬件运行的一致性。在MNIST数据集上的实验表明，该方案在保持高精度的同时，显著降低了推理延迟和功耗，为软件定义的SNN模型提供了一条直接且可复现的硬件落地路径。
selection_source: fresh_fetch
motivation: 现有的SNN工作流在硬件优先实现与软件优先框架之间存在断层，导致PyTorch定义的模型难以直接且一致地部署到低成本FPGA硬件上。
method: 提出一种语义保持的协同设计框架，利用包含权重、阈值及解码元数据的统一工件，实现从软件定义到硬件执行的无缝转换。
result: "在80MHz的FPGA上实现MNIST分类，准确率达87.40%且与软件参考完全一致，推理延迟仅为0.1375微秒/图像，动态能耗为31.6纳焦/图像。"
conclusion: 该框架证明了低成本事件驱动FPGA能够为软件定义的SNN模型提供高效、确定且可复现的硬件部署方案。
---

## 摘要
低成本 FPGA 平台可以扩大神经形态系统研究的受众，但目前的脉冲神经网络 (SNN) 工作流在硬件优先实现（难以与 PyTorch 风格的开发集成）和软件优先框架（通常止步于仿真或 GPU 执行）之间仍然存在分歧。本文提出了一种语义保持的软硬件协同设计框架，用于将 PyTorch 定义的 SNN 确定性地部署到事件驱动的 FPGA 执行中。一个单一的导出产物包含了权重、阈值、连接描述符以及分组的首脉冲时间 (TTFS) 解码元数据，从软件定义阶段一直传递到板卡执行阶段，并被软件参考模型和板卡运行时原封不动地复用。在 80 MHz 布线设计中实现的 10 类 MNIST TTFS 分类器达到了 87.40% 的准确率，并在全部 10,000 张测试图像上与软件参考模型的结果完全匹配。可编程逻辑路径实现了 0.1375 μs/图像的推理延迟和 31.6 nJ/图像的估计动态能耗，同时通过与匹配的 GPU 和 CPU 基准进行范围感知的对比，明确区分了仅加速器和系统级的测量指标。这些结果表明，低成本事件驱动 FPGA 硬件可以为软件定义的 SNN 模型提供一条直接且可复现的从软件到板卡的部署路径。

## Abstract
Low-cost FPGA platforms can broaden access to neuromorphic systems research, but current spiking neural network (SNN) workflows remain divided between hardware-first implementations, which are difficult to integrate with PyTorch-style development, and software-first frameworks, which often stop at simulation or GPU execution. This paper presents a semantics-preserving hardware-software co-design framework for the deterministic deployment of PyTorch-defined SNNs to event-driven FPGA execution. A single exported artifact carries weights, thresholds, connectivity descriptors, and grouped time-to-first-spike (TTFS) decoding metadata from software definition to board execution and is reused unchanged by both the software reference and the board runtime. A 10-class MNIST TTFS classifier implemented in the routed 80 MHz design achieves 87.40\% accuracy and matches the software reference on all 10,000 test images. The programmable-logic path delivers a service latency of 0.1375 μs/image and an estimated dynamic energy of 31.6 nJ/image, while scope-aware comparisons with matched GPU and CPU baselines keep accelerator-only and system-level measurements distinct. These results show that low-cost event-driven FPGA hardware can provide a direct and reproducible software-to-board path for software-defined SNN models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个针对低成本 FPGA 平台的脉冲神经网络（SNN）软硬件协同设计框架，旨在解决 SNN 在软件仿真与硬件部署之间存在的断层问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 SNN 开发流存在“软硬分裂”现象。硬件优先的方法（如 HDL 实现）效率高但难以与 PyTorch 等主流软件框架集成；软件优先的方法（如 SpikingJelly）虽然易于建模，但往往止步于 GPU 仿真，缺乏向低成本硬件迁移的直接路径。
*   **研究动机**：
    *   降低神经形态计算的研究门槛，使研究人员能在廉价的商品化 FPGA（如 PYNQ-Z2）上运行软件定义的 SNN。
    *   实现“语义保持”的部署，确保硬件执行结果与软件仿真结果在位级（Bit-level）上完全一致。
    *   建立清晰的测量规范，区分加速器核心性能与系统级开销。

### 2. 方法论
*   **核心思想**：采用**单一导出工件（Single Deployment Artifact）**作为软硬件的共同真理源。该工件包含权重、阈值、连接描述符和分组首脉冲时间（TTFS）解码元数据。
*   **关键技术细节**：
    *   **PyTorch 协同接口**：模型构建遵循 `nn.Module` 风格，通过自定义的 `deploy.export` 自动生成硬件可识别的配置。
    *   **硬件架构**：在 FPGA 上实现了 AXI 总线驱动的事件路由、神经元处理阵列（16 组，每组 128 个神经元）、连接表和分组 TTFS 解码器。
    *   **TTFS 解码逻辑**：采用首脉冲时间编码，通过硬件计数器捕获输出脉冲的时间戳，并根据预设的分组规则直接在硬件端完成分类预测。
    *   **确定性执行**：通过严格的定时控制和参数映射，确保硬件逻辑完全复现软件定义的数学行为。

### 3. 实验设计
*   **数据集**：使用标准的 **MNIST** 手写数字识别数据集（10,000 张测试图像）。
*   **Benchmark（基准）**：
    *   **软件参考**：基于 NumPy 的 FP32 和 INT8 实现。
    *   **GPU 基准**：使用 NVIDIA RTX 3080 运行相同的分类器内核。
    *   **CPU 基准**：通用 CPU 上的密集矩阵运算实现。
*   **对比维度**：准确率（Accuracy）、推理延迟（Latency）、吞吐量（Throughput）以及动态能耗（Energy）。

### 4. 资源与算力
*   **硬件平台**：Xilinx Zynq-7020 SoC (PYNQ-Z2 板卡)，运行频率为 **80 MHz**。
*   **资源占用**：使用了 14,552 个 LUT（27.35%）和 140 个 BRAM 块（100%，是主要瓶颈）。
*   **算力对比**：
    *   **GPU**：使用了一块 RTX 3080 用于获取对比基准。
    *   **训练过程**：论文重点在于“部署”，未详细说明训练时长，但指出模型是在 PyTorch 环境下预训练后导出的。

### 5. 实验数量与充分性
*   **实验规模**：对 MNIST 全部的 10,000 张测试图进行了验证。
*   **一致性测试**：进行了 5 次重复实验，共 50,000 次图像推理，硬件预测与软件参考实现了 **100% 匹配**。
*   **鲁棒性实验**：进行了输入脉冲丢失（Spike Drop）实验，测试了从 0% 到 75% 丢失率下的准确率变化。
*   **充分性评价**：实验设计较为客观，特别是通过“全测试集匹配”证明了部署的确定性，这比单纯对比平均准确率更具说服力。

### 6. 主要结论与发现
*   **性能卓越**：FPGA 的 PL（可编程逻辑）端推理延迟仅为 **0.1375 μs/图像**，比 GPU INT8 内核快 1.79 倍，比 CPU 快 488 倍。
*   **极低能耗**：估计的动态能耗为 **31.6 nJ/图像**，比 GPU 动态能耗低约 933 倍。
*   **高可靠性**：在 80 MHz 时钟下，硬件准确率（87.40%）与软件参考完全一致，证明了框架的语义保持能力。
*   **系统瓶颈**：研究发现系统级延迟（含 DMA 传输和软件预处理）远高于加速器核心延迟（4.25 ms vs 0.1375 μs），强调了未来优化主机通信的重要性。

### 7. 优点
*   **易用性**：提供了 PyTorch 风格的接口，降低了 SNN 硬件部署的门槛。
*   **确定性**：通过单一工件确保了软硬件行为的高度一致，方便调试和复现。
*   **测量严谨**：明确区分了“加速器延迟”和“系统延迟”，避免了神经形态计算论文中常见的指标模糊问题。
*   **开源贡献**：提供了公开的代码仓库。

### 8. 不足与局限
*   **模型复杂度有限**：目前仅验证了线性 TTFS 分类器，尚未扩展到深层卷积 SNN（ConvSNN）。
*   **存储瓶颈**：受限于 Zynq-7020 的 BRAM 容量，无法直接部署大规模稠密网络。
*   **能耗测量**：能耗数据基于 Xilinx 工具的仿真估计，而非物理电流表实测，可能存在一定偏差。
*   **应用场景**：目前仅针对 MNIST 这种相对简单的任务，对于更复杂的时序数据集（如 DVS 事件相机数据）尚未验证。

（完）
