---
title: "SPAC: Automating FPGA-based Network Switches with Protocol Adaptive Customization"
title_zh: SPAC：通过协议自适应定制实现基于 FPGA 的网络交换机自动化
authors: "Guoyu Li, Yang Cao, Lucas H L Ng, Alexander Charlton, Qianzhou Wang, Will Punter, Philippos Papaphilippou, Ce Guo, Hongxiang Fan, Wayne Luk, Saman Amarasinghe, Ajay Brahmakshatriya"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21881v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对FPGA交换机的协议-架构协同设计
tldr: SPAC是一个自动化生成FPGA网络交换机的框架，旨在解决不同应用（如低延迟传感器网络或高吞吐数据中心）对定制化协议和流量模式的需求。它通过领域特定语言（DSL）、模块化HLS组件库和设计空间搜索（DSE）引擎，实现了协议与架构的协同优化。实验表明，SPAC能显著降低资源消耗并提升延迟性能，为特定工作负载提供帕累托最优设计。
selection_source: fresh_fetch
motivation: 传统固定架构交换机难以满足新兴应用对特定协议和流量模式在低延迟或高吞吐方面的差异化需求。
method: 引入包含DSL、模块化HLS组件库和追踪感知DSE引擎的统一工作流，实现协议与交换机微架构的协同定制与自动化生成。
result: "实验显示SPAC将LUT和BRAM资源消耗分别降低了55%和53%，同时在多种任务中实现了7.8%至38.4%的延迟缩减。"
conclusion: SPAC证明了通过针对特定工作负载进行协议自适应定制，可以显著优化FPGA网络交换机的资源效率与性能表现。
---

## 摘要
随着新兴应用对网络需求的差异化，延迟敏感型服务要求极低的逻辑延迟，而超大规模训练和集合通信则需要持续的线速吞吐量以进行同步批量传输。这种差异化产生了对针对特定协议和特定应用流量模式定制的网络交换机的迫切需求。本文提出了 SPAC（交换机与协议自适应定制），这是一种自动化生成基于 FPGA 的网络交换机的新方法，该方法针对自定义协议和特定应用流量模式进行了协同优化。SPAC 引入了一个统一的工作流，包括用于协议-架构协同设计的领域特定语言 (DSL)、基于 HLS 的模块化自适应交换机组件库，以及一个感知追踪（trace-aware）的设计空间探索 (DSE) 引擎。通过提供多保真度仿真栈，SPAC 能够在部署前快速识别帕累托最优设计。我们在从延迟敏感型传感器和高频交易 (HFT) 网络到超大规模数据中心架构的一系列真实场景中，展示了 SPAC 领域特定自适应的有效性。实验结果表明，通过针对特定工作负载定制微架构和协议，SPAC 生成的设计分别减少了 55% 的 LUT 和 53% 的 BRAM 使用量。与固定架构的同类产品相比，SPAC 在各种任务中实现了 7.8% 到 38.4% 的延迟降低，同时保持了合理的资源消耗和丢包率。

## Abstract
With network requirements diverging across emerging applications, latency-critical services demand minimal logic delay, while hyperscale training and collectives require sustained line-rate throughput for synchronized bulk transfers. This divergence creates an urgent need for custom network switches tailored to specialized protocols and application-specific traffic patterns. This paper presents SPAC (Switch and Protocol Adaptive Customization), a novel approach that automates the generation of FPGA-based network switches co-optimized for custom protocols and application-specific traffic patterns. SPAC introduces a unified workflow with a domain-specific language (DSL) for protocol-architecture co-design, a library of modular HLS-based adaptive switch components, and a trace-aware Design Space Exploration (DSE) engine. By providing a multi-fidelity simulation stack, SPAC enables rapid identification of Pareto-optimal designs prior to deployment. We demonstrate the efficacy of the domain-specific adaptation of SPAC across a spectrum of real-world scenarios, spanning from latency-sensitive sensor and HFT networks to hyperscale datacenter fabrics. Experimental results show that by tailoring the micro-architecture and protocol to the specific workload, SPAC-generated designs reduce LUT and BRAM usage by 55% and 53%, respectively. Compared to fixed-architecture counterparts, SPAC delivers latency reductions ranging from 7.8% to 38.4% across various tasks while maintaining adequate resource consumption and packet drop rate.

---

## 论文详细总结（自动生成）

### 论文总结：SPAC — 自动化 FPGA 网络交换机协议自适应定制框架

#### 1. 核心问题与整体含义（研究动机和背景）
现代网络应用需求呈现两极分化：高频交易（HFT）等实时系统追求极低延迟，而超大规模 AI 训练则需要高吞吐量的同步批量传输。传统的固定架构交换机（如基于 PISA 架构的 P4 交换机）或通用协议（如 Ethernet/IP）由于架构僵化和协议冗余，无法针对特定流量模式进行最优适配。
**核心问题**在于：如何打破协议与硬件架构的紧耦合，实现针对特定应用（如传感器网络、数据中心、AI 训练）的“协议-架构协同设计”自动化生成。

#### 2. 方法论
SPAC 提出了一个从协议定义到线速硬件生成的端到端工作流，包含三个核心组件：
*   **SPAC DSL（领域特定语言）：** 允许用户定义自定义报文结构、进行语义绑定（如指定路由键）并配置架构策略。它作为“单一事实来源”，自动生成软件驱动、HLS 解析库和仿真模型。
*   **可配置交换机模板（HLS-based）：** 采用模块化设计，包含解析器、自定义内核接口、转发表（FullLUT 或 Multi-Bank Hash）、VOQ 缓存（N*N 或共享模式）和调度器（RR、iSLIP、EDRRM）。各模块通过 Meta+Data 流接口解耦。
*   **设计空间探索（DSE）引擎：** 采用**渐进式约束满足策略**（算法 1）：
    1.  **静态剪枝：** 根据流量特征（如最小包长）排除无法满足线速处理的架构。
    2.  **粗粒度分析：** 利用统计代理模型进行无限缓存仿真，评估延迟分布。
    3.  **统计调优：** 根据目标丢包率确定最优 BRAM 缓存大小，并进行硬件资源校验。
*   **多保真度仿真：** 结合了基于 ns-3 的硬件感知仿真（支持自定义协议适配）和轻量级统计代理模型，确保在综合前快速识别帕累托最优设计。

#### 3. 实验设计
*   **应用场景/数据集：**
    *   **HFT：** 高频交易，低延迟、高突发。
    *   **RL (All-Reduce)：** 分布式强化学习，具有明显的 Incast（多对一）流量。
    *   **DataCenter：** 基于阿里巴巴集群追踪（Alibaba Cluster Trace）的微服务依赖模拟。
    *   **Industry：** 医疗废物焚烧炉的 SCADA 工业控制流量。
    *   **Underwater：** 水下机器人通信，极小载荷（2字节）。
*   **Benchmark（基准方法）：**
    *   **SPAC Ethernet：** 作为通用基准（Ethernet + iSLIP + N*N VOQ）。
    *   **学术界先进设计：** GCQ、SMiSLIP、Hipernetch（主要对比调度器和交换矩阵性能）。
    *   **P4 编译器：** VitisNetP4、P4-to-FPGA、P4THLS（对比解析和基础转发逻辑）。

#### 4. 资源与算力
*   **硬件平台：** AMD Alveo U45N 网络加速卡（FPGA 型号：xcu26），宿主机配备 AMD EPYC 9335 32 核处理器。
*   **开发工具：** Vitis HLS 2023.2，目标时钟频率 350MHz。
*   **算力说明：** 论文未详细记录 DSE 搜索的具体耗时，但提到统计代理模型能在秒级处理流量追踪，显著快于周期精确仿真。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **资源与性能对比：** 对比了不同 port 数量（2-16口）下的 LUT、FF、BRAM 消耗及吞吐量。
*   **可扩展性分析：** 测试了不同架构组合在不同端口规模下的延迟趋势。
*   **领域特定适配实验：** 针对 5 种真实场景进行了 DSE 优化前后的对比。
*   **充分性评价：** 实验覆盖了从底层硬件资源到上层应用延迟的完整链路，对比对象既包括传统 RTL 手写设计也包括现代 P4 框架，实验设计较为客观、公平。

#### 6 主要结论与发现
*   **定制化优势显著：** 针对特定工作负载定制后，LUT 和 BRAM 资源分别减少了 **55%** 和 **53%**。
*   **延迟大幅降低：** 相比固定架构基准，SPAC 生成的设计在不同任务中实现了 **7.8% 至 38.4%** 的延迟缩减。
*   **架构敏感性：** 实验发现小包延迟敏感场景（如 HFT）偏好 RR 调度和 FullLookup 表；而高突发场景（如 RL）则更适合 EDRRM 调度。
*   **协议压缩价值：** 在水下机器人等场景，将协议头从 14B 压缩至 2B 可显著提升有效吞吐量并简化硬件逻辑。

#### 7. 优点
*   **端到端自动化：** 解决了从协议 DSL 到 FPGA RTL 生成的复杂映射问题，极大降低了开发门槛。
*   **协议-架构协同：** 不仅仅是解析协议，还能根据流量特征自动选择最匹配的交换机微架构（如调度算法和缓存策略）。
*   **多保真度仿真：** 引入统计代理模型加速 DSE，解决了大规模网络仿真耗时过长的问题。
*   **开源贡献：** 项目代码已开源，具有较高的学术和工程参考价值。

#### 8. 不足与局限
*   **HLS 的非确定性：** 论文提到 HLS 综合结果存在波动，可能导致 DSE 预测与最终硬件表现有细微偏差。
*   **硬件平台单一：** 实验主要集中在 AMD/Xilinx FPGA 上，未探讨在 Intel FPGA 或其他可编程 ASIC 上的迁移性。
*   **复杂逻辑支持：** 虽然提供了自定义内核接口，但对于极其复杂的有状态处理（Stateful Processing）在 DSE 中的性能建模仍有待深入探讨。
*   **应用限制：** 主要针对特定领域的专用网络，对于需要大规模通用互操作性的场景，定制协议的优势会受限。

（完）
