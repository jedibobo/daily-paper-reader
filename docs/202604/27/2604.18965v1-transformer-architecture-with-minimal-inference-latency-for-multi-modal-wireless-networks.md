---
title: Transformer Architecture with Minimal Inference Latency for Multi-Modal Wireless Networks
title_zh: 面向多模态无线网络的极低推理延迟 Transformer 架构
authors: "Minsu Kim, Walid Saad, Kui Wang, Zongdian Li, Tao Yu, Kei Sakaguchi"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.18965v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 无线网络中极低的推理延迟和内存占用
tldr: 针对下一代无线网络中多模态Transformer推理延迟高、内存占用大的问题，本文提出一种轻量化推理框架。通过模态特定分词器、令牌路由机制及可训练的保留率，该框架能动态筛选重要令牌并优化计算量。实验表明，在保持精度的前提下，该方法显著降低了波束赋形任务的延迟与资源消耗，并成功应用于实时切换预测。
selection_source: fresh_fetch
motivation: 现有Transformer架构在处理多模态无线通信任务时，因注意力机制的二次复杂度导致推理延迟过高，难以应对动态变化的通信环境。
method: 提出一种基于令牌筛选的推理框架，利用模态特定分词器统一嵌入维度，并通过令牌路由器和可训练保留率在目标计算量下动态处理关键信息。
result: "在DeepSense 6G任务中，推理延迟降低86.2%，内存和计算量分别减少35%和80%，且精度损失微乎其微。"
conclusion: 该框架有效解决了多模态Transformer在无线网络中的部署瓶颈，为实时、低延迟的智能通信任务提供了可行方案。
---

## 摘要
下一代无线网络预计将利用多模态数据源，在具备情境感知能力的情况下执行各种无线通信任务，如波束赋形和遮挡预测。为此，多模态 Transformer 已成为一种有效的工具，然而，现有的基于 Transformer 的方法在处理多模态数据时面临高推理延迟和巨大内存占用的问题。因此，这些现有方案无法处理需要快速推理以追踪包含移动车辆和遮挡物的动态变化环境的无线通信任务。一个主要的瓶颈在于对注意力机制的依赖，其复杂度随令牌（token）数量呈二次方增长。因此，本文设计了一种新型、快速的多模态 Transformer 推理框架，通过仅处理重要令牌来实际支持无线通信任务。为此，本文构建了一个优化问题，旨在保持任务准确性的同时，在目标 FLOPs 约束下为给定的无线通信任务寻找最优令牌数量。为了解决这一问题，首先设计了特定模态的分词器，将每个模态投影到相同的嵌入维度。随后，引入了一个令牌路由来学习每个令牌的重要性，并仅处理重要令牌。接着，引入了一个可训练的保留率（keep ratio），以学习在目标 FLOPs 下每层应处理的令牌数量。仿真结果表明，在 DeepSense 6G 波束赋形任务中，我们可以在准确率损失微乎其微的情况下，分别将推理延迟、GPU 内存和 FLOPs 降低 86.2%、35% 和 80%。为了验证实际部署的可行性，利用真实测试平台开发了一个多模态切换数据集。在该数据集上的仿真结果显示，所提出的框架能够在遮挡发生前主动发起切换。

## Abstract
Next-generation wireless networks are expected to leverage multi-modal data sources to execute various wireless communication tasks such as beamforming and blockage prediction with situational-awareness. To do so, multi-modal transformers emerged as an effective tool, however, existing transformer-based approaches suffer from high inference latency and large memory footprints when processing multi-modal data. Hence, such existing solutions cannot handle wireless communication tasks that require fast inference to track a dynamically changing environment with moving vehicles and blockages. One major bottleneck is the reliance on attention mechanisms whose complexity grows quadratically with respect to the number of tokens. Hence, in this paper, a novel, fast multi-modal transformer inference framework is designed to practically support wireless communication tasks by processing only important tokens. To this end, an optimization problem is formulated to find the optimal number of tokens under a target FLOPs for a given wireless communication task while maintaining the task accuracy. To solve this problem, modality-specific tokenizers are first designed to project each modality into the same embedding dimension. Then, a token router is introduced to learn the importance of each token and process only important tokens. Subsequently, a trainable keep ratio is introduced to learn how many tokens to process for each layer under the target FLOPs. Simulation results show that, on DeepSense 6G beamforming tasks, we can reduce the inference latency, GPU memory, and FLOPs by 86.2% 35%, and 80%, respectively, with negligible accuracy loss. To validate the feasibility for real-world deployments, a multi-modal handover dataset is developed using a real-world testbed. Emulation results on the developed dataset show that the proposed framework can proactively initiate handover before blockage.

---

## 论文详细总结（自动生成）

这篇论文针对下一代无线网络（6G）中多模态 Transformer 模型的推理效率问题提出了创新方案。以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 6G 赋能的车辆到基础设施（V2I）场景中，Transformer 模型虽能融合多模态数据（如摄像头、LiDAR、雷达、GPS）实现高精度的波束赋形和遮挡预测，但其核心的自注意力机制具有**二次方计算复杂度**。
*   **研究动机**：高推理延迟和巨大的内存占用导致模型无法实时追踪动态变化的无线环境（如高速移动的车辆和突发遮挡）。如果推理时间超过了信道相干时间，预测结果将失效，导致通信质量大幅下降。

### 2. 论文提出的方法论
论文提出了一种**面向极低推理延迟的多模态 Transformer 框架**，核心思想是“只处理最重要的令牌（Tokens）”：
*   **模态特定分词器（Modality-specific Tokenizers）**：针对不同模态设计轻量化分词器（如图像/LiDAR 使用残差 CNN，雷达/GPS 使用线性层），将异构数据投影到统一的嵌入维度，缩小模态间隙。
*   **令牌路由器（Token Router）**：在每个 Transformer 块中引入轻量级路由器，通过两层线性层预测每个令牌的重要性得分，仅选择前 $K$ 个最重要的令牌进入后续计算，其余令牌直接跳过（Bypass）。
*   **可训练的保留率（Trainable Keep Ratio）**：引入连续参数 $r_l$ 来控制每层处理令牌的比例。
*   **端到端优化流程**：为了解决 Top-K 操作不可微的问题，论文通过将 $r_l$ 映射到相邻的两个整数，并对两次前向传播的结果进行线性插值，从而构建梯度路径。最终通过任务损失函数和 FLOPs 约束损失（MSE Loss）共同优化模型参数、路由器和保留率。

### 3. 实验设计
*   **数据集/场景**：
    1.  **DeepSense 6G (Scenarios 31-34)**：基于真实世界的波束赋形数据集。
    2.  **CARLA-MATLAB 仿真数据集**：模拟多车场景下的波束预测。
    3.  **自建真实世界切换数据集**：在东京科学大学校园内采集，包含 60GHz WiGig 信号、LiDAR 和图像，用于验证主动切换任务。
*   **对比方法（Benchmarks）**：
    *   **Baseline [MHA]**：处理所有令牌的标准多头注意力 Transformer。
    *   **ToMe (Token Merging)**：通过合并相似令牌来减枝的方法。
    *   **MoD (Mixture-of-Depths)**：针对 NLP 任务设计的启发式令牌路由方法。

### 4. 资源与算力
*   **GPU 型号**：
    *   主要仿真使用 **NVIDIA A30**。
    *   边缘推理性能评估使用 **NVIDIA T4**（模拟边缘侧受限算力）。
    *   训练时长分析使用 **NVIDIA H100**。
*   **训练细节**：Batch size 为 16，训练 30 个 Epoch，使用 ADAM 优化器，学习率为 0.0001。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同性质的数据集上进行了测试，涵盖了波束赋形和链路切换两个关键任务。
*   **消融实验**：针对分词器、令牌路由器和可训练保留率分别进行了消融测试，验证了各组件对精度的贡献。
*   **充分性评价**：实验设计较为充分，不仅对比了精度，还详细分析了延迟、内存占用和 FLOPs。通过 9 折交叉验证和多次随机种子取平均值，保证了结果的客观性和公平性。

### 6. 主要结论与发现
*   **性能提升**：在 DeepSense 6G 任务中，当目标保留率为 30% 时，**推理延迟降低了 86.2%，内存占用减少了 35%，FLOPs 降低了 80%**，而 Top-1 准确率损失微乎其微（仅约 0.12%）。
*   **层间差异**：实验发现，Transformer 的前几层通常需要保留更多的令牌以提取特征，而深层可以大幅削减令牌数量。
*   **实时性优势**：在主动切换实验中，该框架由于推理速度快，能在遮挡发生前完成切换；而基准模型因延迟过高，在完成推理时信号已受损。

### 7. 优点与亮点
*   **端到端可微优化**：通过插值法解决了 Top-K 离散选择的梯度回传问题，实现了保留率的自动化学习，优于手动设置。
*   **多模态深度融合**：不仅考虑了计算效率，还通过分词器设计解决了异构数据的对齐问题。
*   **软硬件结合验证**：不仅有理论仿真，还通过真实测试床采集数据并模拟边缘 GPU 环境，具有很强的工程参考价值。

### 8. 不足与局限
*   **自建数据集规模较小**：自建的切换数据集仅包含约 800 个样本和 10 个场景，可能存在过拟合风险，需在更复杂的交通环境中进一步验证。
*   **超参数敏感性**：FLOPs 约束系数 $\lambda$ 和目标保留率 $\gamma'$ 仍需手动预设，未来若能实现根据硬件状态动态调整这些参数会更具鲁棒性。
*   **泛化性挑战**：论文提到未来需研究在未见过的无线环境、载波频率和遮挡类型下的鲁棒性。

（完）
