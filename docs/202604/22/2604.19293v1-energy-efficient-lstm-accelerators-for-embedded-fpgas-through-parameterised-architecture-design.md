---
title: Energy Efficient LSTM Accelerators for Embedded FPGAs through Parameterised Architecture Design
title_zh: 通过参数化架构设计实现嵌入式 FPGA 的高效能 LSTM 加速器
authors: "Chao Qian, Tianheng Ling, Gregor Schiele"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19293v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 针对FPGA的可参数化硬件加速器设计
tldr: 本研究针对资源受限的嵌入式FPGA，提出了一种高效的LSTM硬件加速器设计。通过参数化架构，用户可根据需求优化DSP使用和激活函数实现。该设计在提升执行速度的同时显著降低了功耗，实现了11.89 GOP/s/W的能效比，为嵌入式设备上的实时时间序列分析提供了高性能、低能耗的解决方案。
selection_source: fresh_fetch
motivation: 旨在解决嵌入式FPGA在处理本地传感器数据流时，LSTM模型计算资源受限且能耗较高的问题。
method: 提出一种可参数化配置的硬件加速器架构，支持对DSP资源利用和激活函数实现进行灵活优化。
result: 在实时推理中达到了11.89 GOP/s/W的能效比，处理速度高达每秒32873个样本。
conclusion: 该参数化设计为资源受限的嵌入式环境提供了一种兼具灵活性与高能效的LSTM加速方案。
---

## 摘要
长短期记忆网络（LSTMs）是一种至关重要的深度学习技术，适用于在嵌入式设备的本地传感器数据流上执行设备端时间序列分析。在本文中，我们提出了一种专门针对资源受限的嵌入式现场可编程门阵列（FPGAs）进行优化的新型 LSTM 硬件加速器设计。与相关工作相比，我们的设计提高了执行速度并降低了能耗。此外，它可以通过多种优化参数（如 DSP 的使用或激活函数的实现）适应不同的应用场景。我们介绍了关键的设计决策并评估了性能。我们的加速器在实时推理中达到了 11.89 GOP/s/W 的能效，处理速度为 32873 样本/秒。

## Abstract
Long Short-term Memory Networks (LSTMs) are a vital Deep Learning technique suitable for performing on-device time series analysis on local sensor data streams of embedded devices. In this paper, we propose a new hardware accelerator design for LSTMs specially optimised for resource-scarce embedded Field Programmable Gate Arrays (FPGAs). Our design improves the execution speed and reduces energy consumption compared to related work. Moreover, it can be adapted to different situations using a number of optimisation parameters, such as the usage of DSPs or the implementation of activation functions. We present our key design decisions and evaluate the performance. Our accelerator achieves an energy efficiency of 11.89 GOP/s/W during a real-time inference with 32873 samples/s.

---

## 论文详细总结（自动生成）

这篇论文针对资源受限的嵌入式 FPGA，提出了一种参数化、高能效的 LSTM（长短期记忆网络）硬件加速器架构。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在嵌入式边缘设备（如 IoT 传感器）上运行 LSTM 模型面临计算资源（LUT、DSP、内存）匮乏和功耗限制的双重挑战。
*   **研究动机**：
    *   微控制器（MCU）处理速度慢，GPU 功耗过高。
    *   现有的 FPGA 加速器研究多集中在云端或中端设备，针对极低资源嵌入式 FPGA（如 Spartan-7）的设计较少。
    *   现有方案往往过度依赖 DSP 资源，导致模型规模受限，且时钟频率较低，影响了能效比。

### 2. 方法论
论文提出了一种基于模板的 RTL 设计，核心思想是通过参数化配置平衡资源利用与性能。
*   **8位定点量化**：采用 (4, 8) 定点格式（4位小数，总宽8位），在保持精度的同时，允许使用 LUT 代替 DSP 实现乘法器。
*   **激活函数优化**：
    *   使用分段线性的 **HardTanh** 和 **HardSigmoid*** 替代复杂的指数运算。
    *   针对 HardSigmoid* 提供了三种实现模式：**算术移位法**（省资源但延迟高）、**1对1查找表**（速度快）、**步进查找表**（针对特定位宽优化的折中方案）。
*   **5级流水线 ALU**：设计了专门的乘累加（MAC）流水线，将数据加载、乘法、累加、舍入等步骤拆分，显著提升了最大工作频率。
*   **参数化架构**：支持配置隐藏层大小、输入维度、ALU 资源类型（DSP 或 LUT）、权重存储类型（BRAM 或 LUTRAM）等，使设计能适配不同的 FPGA 芯片。

### 3. 实验设计
*   **数据集/场景**：使用 **PeMS-4W** 数据集进行单步交通速度预测（时间序列分析）。
*   **硬件平台**：Xilinx Spartan-7 XC7S15（极小规模 FPGA）。
*   **Benchmark 与对比方法**：
    *   对比了 **Qian et al. (2022/2023)** 的前期工作。
    *   对比了 **Chen et al. (2021)** 的 Eciton 加速器（基于 iCE40 FPGA）。
    *   对比了 **Hasib-Al-Rashid et al. (2020)** 的研究。
*   **评估指标**：资源利用率（LUT/DSP/BRAM）、最大时钟频率、吞吐量（GOP/s）、功耗（mW）及能效比（GOP/s/W）。

### 4. 资源与算力
*   **硬件资源**：实验主要在 **Xilinx Spartan-7 XC7S15** 上完成，该芯片仅有 8000 个 LUT 单元和 20 个 DSP 切片。
*   **算力表现**：
    *   最高时钟频率达到 **204 MHz**。
    *   吞吐量达到 **0.740 GOP/s**。
    *   推理速度为 **32873 样本/秒**。
*   **训练细节**：使用 ElasticAI-Creator 工具进行**量化感知训练（QAT）**，而非简单的后量化，以确保 8 位精度下的模型效果。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了**资源伸缩性实验**：测试了隐藏层维度从 20 到 200 变化时对 BRAM、DSP 和 LUT 的占用情况。
    *   进行了**消融/对比实验**：对比了不同激活函数实现方式对延迟和面积的影响，以及流水线设计对频率的提升。
    *   进行了**硬件验证**：不仅使用 Xilinx Power Estimator 进行仿真，还在真实硬件上测量了功耗。
*   **充分性评价**：实验设计较为充分，涵盖了从逻辑合成、时序分析到功耗测量的全流程，且对比了同类研究中的最新成果，结果具有说服力。

### 6. 主要结论与发现
*   **高能效**：在真实硬件上实现了 **11.89 GOP/s/W** 的能效比，比前人工作提升了约 2.33 倍。
*   **资源优化**：通过优化，LUT 利用率平均降低 29.62%，LUTRAM 降低 33.33%。
*   **可扩展性**：在极小的 Spartan-7 芯片上，该架构可支持多达 5 层 LSTM（每层隐藏层大小为 60），打破了以往设计只能支持 1-2 层的限制。
*   **频率突破**：流水线设计使频率从 100MHz 提升至 204MHz，吞吐量翻倍。

### 7. 优点
*   **灵活性极高**：参数化设计允许开发者在“节省 DSP”和“追求高性能”之间灵活切换。
*   **针对性强**：专门为资源极度受限的嵌入式 FPGA 优化，填补了该领域的空白。
*   **软硬协同**：结合了量化感知训练和硬件流水线优化，在低比特下仍保持了较高的预测精度（MSE 降低了 78%）。

### 8. 不足与局限
*   **应用场景单一**：实验仅基于交通预测数据集，未在语音识别或更复杂的多维传感器任务上验证其泛化性。
*   **模型规模上限**：虽然支持多层，但受限于嵌入式 FPGA 本身的 BRAM 容量，无法处理超大规模的隐藏层（如 >200）。
*   **量化限制**：目前固定为 8 位定点，对于某些对精度极度敏感的任务，可能需要更灵活的位宽支持。

（完）
