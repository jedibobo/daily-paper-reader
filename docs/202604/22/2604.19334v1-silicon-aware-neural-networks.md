---
title: Silicon Aware Neural Networks
title_zh: 硅感知神经网络
authors: "Sebastian Fieldhouse, Kea-Tiong Tang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19334v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 针对定制硅片的面积和功耗优化的硅感知神经网络
tldr: "本研究探讨了可微分逻辑门网络（DLGN）在定制硅芯片上的实现。通过将训练好的离散逻辑门模型直接映射为CMOS标准单元库的门级网表，实现了神经网络与硬件底层的深度融合。研究提出了一种新型损失函数，可根据标准单元面积优化电路的面积和功耗。在SkyWater 130nm工艺下的仿真表明，该方法在保持97% MNIST分类准确率的同时，实现了极高的推理速度和低功耗，为高效硬件加速提供了新路径。"
selection_source: fresh_fetch
motivation: 旨在解决深度学习模型在传统硬件上运行效率受限的问题，通过将逻辑门网络直接映射到定制硅芯片来提升性能。
method: 提出一种将DLGN转换为门级网表的方法，并引入基于标准单元面积的损失函数来协同优化电路的物理特性。
result: "在130nm工艺仿真中，该电路以83.88mW的功耗实现了每秒4180万次的MNIST分类，准确率达97%。"
conclusion: 该研究证明了硅感知神经架构搜索在实现超高性能、低功耗专用集成电路（ASIC）方面的巨大潜力。
---

## 摘要
机器学习领域的近期研究表明，深度学习可以训练由离散逻辑门函数组成的神经网络，从而在 CPU、GPU 和 FPGA 平台上以极高的速度执行简单的图像分类任务。由于这些可微分逻辑门网络（DLGN）由离散逻辑门构成，它们自然适用于定制硅片的实现。在本研究中，我们提出了一种通过将训练好的模型转换为门级网表，将 DLGN 一一映射到数字 CMOS 标准单元库的方法。我们还提出了一种新型损失函数，使 DLGN 能够根据目标标准单元库中标准单元的面积，通过最小化每个神经元的预期面积，来优化所得电路的面积并间接降低功耗。最后，我们还首次展示了 DLGN 作为硅电路的仿真实现，在 SkyWater 130nm 工艺中，使用 Cadence 标准单元库将 DLGN 布局为定制硬宏，并进行了布局后功耗分析。我们发现，该定制宏在 MNIST 数据集上能以 97% 的准确率每秒执行 4180 万次分类，功耗为 83.88 mW。

## Abstract
Recent work in the machine learning literature has demonstrated that deep learning can train neural networks made of discrete logic gate functions to perform simple image classification tasks at very high speeds on CPU, GPU and FPGA platforms. By virtue of being formed by discrete logic gates, these Differentiable Logic Gate Networks (DLGNs) lend themselves naturally to implementation in custom silicon - in this work we present a method to map DLGNs in a one-to-one fashion to a digital CMOS standard cell library by converting the trained model to a gate-level netlist. We also propose a novel loss function whereby the DLGN can optimize the area, and indirectly power consumption, of the resulting circuit by minimizing the expected area per neuron based on the area of the standard cells in the target standard cell library. Finally, we also show for the first time an implementation of a DLGN as a silicon circuit in simulation, performing layout of a DLGN in the SkyWater 130nm process as a custom hard macro using a Cadence standard cell library and performing post-layout power analysis. We find that our custom macro can perform classification on MNIST with 97% accuracy 41.8 million times a second at a power consumption of 83.88 mW.

---

## 论文详细总结（自动生成）

这篇论文《Silicon Aware Neural Networks》（硅感知神经网络）探讨了如何将可微分逻辑门网络（DLGN）直接映射到定制硅芯片（ASIC）上，并提出了一种在训练阶段优化电路面积和功耗的方法。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：深度学习依赖连续可微函数（如矩阵乘法），而数字电路底层是离散的逻辑运算。传统的量化和近似方法在将模型部署到硬件时会产生效率损失。
*   **研究背景**：前人提出的 DLGN 允许使用逻辑门（如 AND, OR, XOR）构建网络，并在 CPU/FPGA 上实现了极速推理。
*   **研究动机**：目前 DLGN 尚未直接实现在定制硅片（ASIC）上。作者旨在通过“硅感知”训练，使神经网络在学习分类任务的同时，自动优化其在标准单元库（Standard Cell Library）中的物理实现（面积和功耗）。

### 2. 方法论
*   **核心思想**：将逻辑门的选择过程转化为可微的概率分布，并将硬件标准单元的物理参数（面积）引入损失函数。
*   **关键技术细节**：
    *   **逻辑门松弛**：将 16 种可能的 2 输入逻辑门函数松弛为连续函数，并使用 Softmax 为每个节点分配概率分布，使其在训练中可微。
    *   **标准单元映射**：将 16 种逻辑功能映射到 SkyWater 130nm 工艺的标准单元（如 AND2X1, XOR2X1 等）。对于没有直接对应单元的功能，使用组合单元并累加面积。
    *   **面积感知损失函数**：
        *   计算每个神经元的预期面积：$E[area_n] = \sum p_{n,i} \cdot A_i$（其中 $p$ 是逻辑门概率，$A$ 是对应单元面积）。
        *   总损失函数：$L_{total} = L_{CE} + \delta \cdot L_{area}$。通过调节超参数 $\delta$，在准确率和电路面积之间取得平衡。
    *   **架构调整**：为了适应 130nm 工艺有限的布线资源，将原有的“宽而浅”架构改为“窄而深”（18 层，每层 4000 神经元）。

### 3. 实验设计
*   **数据集**：MNIST（手写数字识别）和 CIFAR-10（物体识别）。
*   **Benchmark（基准）**：
    *   **内部对比**：不带面积优化损失函数的基准 DLGN 模型。
    *   **外部对比**：与其他硬件加速器（如 FPGA 实现、65nm/40nm/28nm CMOS 上的 Tsetlin Machine 或 CNN 加速器）进行性能对比。
*   **评估指标**：分类准确率、平均神经元面积、总面积、延迟（Latency）、功耗（Power）和每帧推理能量（Energy per Inference）。

### 4. 资源与算力
*   **软件工具**：使用了 Cadence 的标准单元库、Xcelium（门级仿真）和 Innovus（布局布线）。
*   **算力说明**：文中**未明确说明**训练模型所使用的具体 GPU 型号、数量或训练时长。实验重点在于布局布线后的物理仿真结果。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对超参数 $\delta$ 进行了消融实验（取值从 $10^{-5}$ 到 $0.1$），验证了其对准确率和面积的影响。
    *   在 MNIST 和 CIFAR-10 两个经典数据集上验证了方法的通用性。
*   **充分性评价**：实验设计较为客观，涵盖了从算法训练到物理版图生成的全流程。通过引入 16nm 工艺的等效缩放计算，增强了与现代先进工艺节点的对比公平性。但由于缺乏实际流片（Tapeout）测试，结果仍停留在仿真阶段。

### 6. 主要结论与发现
*   **面积优化显著**：引入面积损失函数后，MNIST 模型的平均神经元面积从 $9.38 \mu m^2$ 降至 $6.11 \mu m^2$（下降约 35%），而准确率仅轻微下降（从 98.04% 降至 97.66%）。
*   **极高性能**：在 130nm 工艺下，MNIST 识别电路的吞吐量达到 **4180 万次/秒**，功耗仅为 **83.88 mW**。
*   **能效优势**：单次推理能量为 2.0 nJ（等效 16nm 下约为 69 pJ），在延迟和能效上优于许多传统的混合信号或数字 CNN 加速器。

### 7. 优点
*   **软硬协同设计**：首次将标准单元库的物理面积信息直接反馈到神经网络的训练循环中。
*   **端到端流程**：实现了从高层描述到门级网表，再到物理版图（Hard Macro）的完整自动化映射。
*   **开源兼容**：基于开源的 SkyWater 130nm PDK，具有良好的可复现性和学术参考价值。

### 8. 不足与局限
*   **布线瓶颈**：在较老工艺（130nm）下，由于金属层较少（1P5M），大规模逻辑门网络的布线非常困难，限制了模型规模。
*   **任务复杂度有限**：目前仅在 MNIST 和 CIFAR-10 等简单任务上验证，对于更复杂的视觉任务（如 ImageNet），逻辑门网络的表达能力和可扩展性尚待证明。
*   **仿真局限**：所有功耗和延迟数据均来自布局后仿真，未经实际芯片测试，可能存在寄生参数估计偏差。

（完）
