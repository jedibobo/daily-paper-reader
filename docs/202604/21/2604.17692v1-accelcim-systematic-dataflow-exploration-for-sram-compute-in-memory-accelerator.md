---
title: "AccelCIM: Systematic Dataflow Exploration for SRAM Compute-in-Memory Accelerator"
title_zh: AccelCIM：SRAM 存内计算加速器的系统性数据流探索
authors: "Chenhao Xue, Yukun Wang, An Guo, Yuhui Shi, Jinwei Zhou, Xiping Dong, Yihan Yin, Yuanpeng Zhang, Tianyu Jia, Wei Gao, Qiang Wu, Xin Si, Jun Yang, Guangyu Sun"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17692v1"
tags: ["query:hwnas"]
score: 9.0
evidence: SRAM 存内计算加速器的系统化数据流探索
tldr: 本文针对SRAM存内计算（CIM）加速器在处理大型深度神经网络时面临的存储容量限制及数据迁移开销问题，提出了AccelCIM框架。该框架系统地构建了涵盖CIM宏单元配置与阵列组织的设计空间，并结合周期精确仿真与布局后PPA分析进行严谨评估。通过在LLM应用上的探索，为高效CIM加速器设计提供了科学的指导原则。
selection_source: fresh_fetch
motivation: 现有CIM研究往往忽略了有限片上容量导致的大模型数据迁移开销，缺乏系统性的数据流设计探索。
method: 提出AccelCIM框架，通过定义涵盖宏单元到阵列层级的系统化设计空间，并采用周期精确仿真与布局后分析进行评估。
result: 通过对代表性LLM应用的广泛探索，揭示了CIM加速器在性能、功耗和面积之间的复杂权衡。
conclusion: AccelCIM为SRAM存内计算加速器的系统化设计与优化提供了有效的评估工具和实践见解。
---

## 摘要
基于 SRAM 的存内计算（CIM）为深度神经网络（DNN）加速器提供了高计算密度和能效，但其有限的容量导致大型 DNN 模型面临显著的片上/片外数据搬运开销。现有的 CIM 加速器研究通常假设 DNN 模型可以完全存储在片上，使得高效的数据流设计在很大程度上尚未得到充分探索。本文介绍了 AccelCIM，这是一个针对 SRAM CIM 加速器的系统性数据流探索框架，旨在解决先前研究的两个关键局限性：（1）它构建了一个涵盖 CIM 宏单元配置和宏单元阵列组织的系统性数据流设计空间；（2）它引入了基于周期精确架构仿真和布局后 PPA 分析的严谨设计评估。我们进行了广泛的设计空间探索，并将 AccelCIM 应用于代表性的大语言模型（LLM）应用，为 CIM 加速器的规范化设计提供了实用见解。

## Abstract
SRAM-based compute-in-memory (CIM) offers high computational density and energy efficiency for deep neural network (DNN) accelerators, but its limited capacity causes on/off-chip data movement overhead for large DNN models. Existing CIM accelerator studies typically assume that DNN models fit entirely on-chip, leaving efficient dataflow design largely untapped. This paper introduces AccelCIM, a systematic dataflow exploration framework for SRAM CIM accelerator, which addresses two key limitations of prior work. (1) It formulates a systematic dataflow design space spanning CIM macro configurations and macro-array organizations. (2) It introduces rigorous design evaluation using cycle-accurate architectural simulation and post-layout PPA analysis. We conduct an extensive design space exploration and apply AccelCIM to representative LLM applications, providing practical insights for the principled design of CIM accelerators.

---

## 论文详细总结（自动生成）

### AccelCIM：SRAM 存内计算加速器的系统性数据流探索

#### 1. 论文的核心问题与整体含义
*   **研究背景**：基于 SRAM 的存内计算（CIM）技术因其高计算密度和能效，成为加速深度神经网络（DNN）的热门方案。
*   **核心问题**：
    1.  **容量限制**：SRAM CIM 的片上容量有限，而现代 DNN（如 LLM）模型巨大，无法完全装入片上，导致严重的片外数据搬运开销。
    2.  **设计空间缺失**：现有研究多假设模型可完全驻留片上，忽略了权重交换（Weight Swapping）对数据流设计的影响。
    3.  **评估不准确**：传统框架多采用简化的解析模型，忽略了物理布局（Layout）带来的面积和布线开销。
*   **整体含义**：本文提出了 **AccelCIM** 框架，通过系统化探索 CIM 宏单元配置与阵列组织，结合周期精确仿真与布局后 PPA（性能、功耗、面积）分析，为大模型时代的 CIM 加速器设计提供科学指导。

#### 2. 论文提出的方法论
*   **系统化设计空间**：
    *   **宏单元层级（Macro）**：包括累加长度（AL）、局部存储长度（LSL）、并行通道（PC）、流水线级数（PL）以及是否支持**计算-I/O 重叠（OL）**。
    *   **阵列层级（Array）**：包括阵列行列尺寸（BR, BC）、数据流策略（权重驻留 WS、输出驻留 OS）以及互连拓扑（广播 Broadcast、脉动阵列 Systolic）。
*   **核心技术细节**：
    *   **计算-I/O 重叠（OL）**：通过增加硬件资源（如额外的字线驱动和位线），允许在进行当前行计算的同时更新下一行权重。论文给出了周期计算公式：$T_{ol} = LSL \times \max(T_s, T_c)$，其中 $T_s$ 为更新周期，$T_c$ 为计算周期。
    *   **自动布局生成器**：集成开源 CIM 编译器与 Cadence EDA 工具链（Genus/Innovus），自动生成 RTL 并完成 28nm 工艺下的布局布线，以获取真实的物理指标。
*   **算法流程**：采用贝叶斯优化（Bayesian Optimization）在庞大的设计空间中自动搜索 Pareto 最优解。

#### 3. 实验设计
*   **应用场景**：针对大语言模型（LLM）推理任务，包括 Qwen3-0.6B、LLaMA-3-8B/70B 和 GPT-3 175B。
*   **Benchmark**：
    *   对比了传统的 **WS-Broadcast**（现有研究最常用）与其他组合（WS-Systolic, OS-Broadcast, OS-Systolic）。
    *   对比了 **CIM-MXU** 等前沿学术工作。
*   **对比维度**：在相同的计算能力（如 20 TOPS）下，对比不同数据流在延迟、功耗和面积上的 Pareto 前沿。

#### 4. 资源与算力
*   **工艺节点**：所有物理设计均基于 **28nm** 工艺。
*   **EDA 工具**：Cadence Genus（综合）、Cadence Innovus（布局布线）。
*   **算力说明**：文中未明确提及运行 EDA 工具和模拟器的具体 GPU/CPU 硬件型号及训练时长。由于该研究侧重于硬件架构探索而非深度学习模型训练，其算力消耗主要集中在 EDA 流程的仿真与物理实现上。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了广泛的设计空间探索，生成了数千个设计点的 PPA 数据。
    *   针对 LLM 推理进行了多模型、多规模（从 0.6B 到 175B 参数）的案例研究。
    *   包含了针对阵列规模、宏单元容量、计算-I/O 重叠功能的专项消融实验。
*   **充分性与客观性**：实验涵盖了从电路级到系统级的关键变量，且使用了布局后（Post-layout）数据而非纯理论推导，评估结果较为客观、真实。

#### 6. 论文的主要结论与发现
1.  **互连效率**：脉动阵列（Systolic）在面积效率上显著优于广播（Broadcast）结构，因为广播结构在大规模阵列中会消耗极高的布线资源。
2.  **宏单元尺寸**：中等规模的 CIM 宏单元在系统级表现最佳，平衡了内部计算效率与外部集成开销。
3.  **OL 功能的权衡**：支持计算-I/O 重叠（OL）虽然能隐藏权重更新延迟，但会使能效降低 25-35%，仅在带宽受限的特定场景下具有面积效率优势。
4.  **数据流偏好**：边缘场景（小模型）倾向于 OS-Systolic-OL 以降低延迟；云端大规模场景（如 GPT-3）则倾向于 WS-Systolic-NOL 以节省面积和功耗。

#### 7. 优点
*   **全栈评估**：打通了从底层电路参数到顶层应用性能的评估链路。
*   **物理感知**：引入了真实的布局布线开销，修正了以往研究中对面积和频率的乐观估计。
*   **针对性强**：专门针对 LLM 时代“模型大、片上存储小”的痛点进行数据流优化。

#### 8. 不足与局限
*   **工艺单一**：实验仅基于 28nm 工艺，未讨论在更先进工艺（如 FinFET 节点）下互连延迟与逻辑功耗占比的变化。
*   **存储层次简化**：假设核心缓冲区（Core Buffer）带宽无限，未深入探讨片外 HBM/DDR 接口带宽受限时的系统瓶颈。
*   **稀疏性未涉及**：未考虑模型稀疏性（Sparsity）对 CIM 计算效率和数据流调度的影响。

（完）
