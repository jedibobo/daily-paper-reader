---
title: "WaveTune: Wave-aware Bilinear Modeling for Efficient GPU Kernel Auto-tuning"
title_zh: WaveTune：面向高效 GPU 算子自动调优的波感知双线性建模
authors: "Kaixuan Zhang, Chutong Ding, Shiyou Qian, Luping Wang, Jian Cao, Guangtao Xue, Cheng Huang, Guodong Yang, Liping Zhang"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10187v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 提升计算利用率的GPU内核自动调优
tldr: WaveTune是一个针对GPU算子自动调优的框架，旨在解决大语言模型推理中GEMM等算子性能受运行时参数影响且调优开销大的问题。它通过波次感知的双线性模型和稀疏采样策略，在极低开销下实现了接近最优的参数配置。该方法在多种GPU架构上显著提升了算子性能和端到端推理速度，消除了配置延迟与执行效率之间的权衡。
selection_source: fresh_fetch
motivation: 现有的GPU算子调优方法在性能最优性和运行时效率之间存在矛盾，难以在保证低延迟的同时获得最优参数配置。
method: 引入波次感知的双线性模型进行延迟预测，并结合配置空间分解、稀疏采样和双表检索机制来降低运行时开销。
result: 在多种GPU架构上实现了高达1.83倍的算子加速，并将决策开销较穷举搜索降低了五个数量级。
conclusion: WaveTune有效消除了配置延迟与执行性能之间的权衡，为高性能LLM推理提供了一种高效且鲁棒的算子调优方案。
---

## 摘要
大语言模型（LLM）的快速普及使得 GPU 推理效率成为日益关键的系统关注点。LLM 工作负载的运行时间主要由基于分块（tile-based）的算子决定，特别是通用矩阵乘法（GEMM）。尽管这些算子已经过高度优化，但其性能仍对大量的运行时参数（如分块大小和流水线阶段数）非常敏感。这些参数与硬件资源之间的相互作用导致了非凸的优化空间。现有的参数配置方法——包括基于搜索的自动调优、启发式规则和学习型代价模型——在性能最优性与运行时效率之间面临着根本性的权衡。在本文中，我们提出了 WaveTune，一个用于运行时算子自动调优的波感知框架。首先，我们引入了一种统一映射方法来处理输入的多样性，并分解配置空间以管理高维性。其次，我们开发了一个解析式的波感知双线性模型，能够准确预测算子延迟。第三，我们设计了一种基于波结构的稀疏采样方案和轻量级双表检索机制，以最小化运行时开销。因此，WaveTune 实现了对 GPU 算子精确且高效的运行时配置。在三种代表性算子和五种 GPU 架构上，WaveTune 始终能获得接近最优的算子性能，实现高达 1.83 倍的算子级加速和高达 1.33 倍的端到端首字延迟（TTFT）降低，同时与穷举搜索相比，将运行时决策开销降低了五个数量级。这些结果表明，WaveTune 有效消除了配置延迟与执行最优性之间的传统权衡，为高性能 LLM 推理提供了一种实用且稳健的解决方案。

## Abstract
The rapid adoption of Large Language Models (LLMs) has made GPU inference efficiency an increasingly critical system concern. The runtime of LLM workloads is largely dominated by tile-based kernels, particularly General Matrix Multiplications (GEMMs). Although these kernels are highly optimized, their performance remains sensitive to a large space of runtime parameters, such as tile sizes and pipeline stages. The interaction between these parameters and hardware resources leads to a non-convex optimization landscape. Existing approaches to parameter configuration -- including search-based auto-tuning, heuristic rules, and learned cost models -- face a fundamental trade-off between performance optimality and runtime efficiency.   In this paper, we present WaveTune, a wave-aware framework for runtime kernel auto-tuning. First, we introduce a unified mapping method to handle input diversity and decompose the configuration space to manage high dimensionality. Second, we develop an analytical wave-aware bilinear model that accurately predicts kernel latency. Third, we design a sparse sampling scheme based on wave structures and a lightweight dual-table retrieval mechanism to minimize runtime overhead. As a result, WaveTune enables precise and efficient runtime configuration for GPU kernels. Across three representative kernels and five GPU architectures, WaveTune consistently achieves near-optimal kernel performance, delivering up to 1.83x kernel-level speedup and up to 1.33x end-to-end TTFT reduction, while reducing runtime decision overhead by five orders of magnitude compared to exhaustive search. These results demonstrate that WaveTune effectively eliminates the traditional trade-off between configuration latency and execution optimality, providing a practical and robust solution for high-performance LLM inference.

---

## 论文详细总结（自动生成）

### WaveTune: 面向高效 GPU 算子自动调优的波感知双线性建模 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的广泛应用，GPU 推理效率成为核心瓶颈。LLM 的运行时间主要消耗在基于分块（tile-based）的算子（如 GEMM 和 FlashAttention）上。这些算子的性能对运行时参数（如分块大小、流水线阶段、Warp 数量等）高度敏感。
*   **现有挑战**：现有的配置策略存在“性能-开销”权衡。**穷举搜索**虽准确但在线开销巨大；**启发式规则**开销低但缺乏鲁棒性，常导致性能损失；**机器学习代价模型**（如 GBDT）虽然能泛化，但其推理延迟（毫秒级）往往超过了算子本身的执行时间。
*   **核心动机**：利用 GPU 执行的物理特性（波次调度机制），构建一个既能接近搜索最优性能，又能实现微秒级决策开销的轻量级解析模型。

#### 2. 方法论：核心思想与关键技术
WaveTune 的核心思想是：GPU 算子延迟并非不可预测的黑盒，而是遵循**波次感知的物理结构**。
*   **物理观察**：算子延迟在跨越“波次（Wave）”边界时呈现离散的阶梯状，而在波次内部则呈现近似线性的增长。
*   **关键技术细节**：
    1.  **统一映射与分解**：将复杂的逻辑输入（如 Batch Size, Seq Len）映射为统一的物理坐标：**网格大小（G）**和**循环次数（L）**。同时将配置空间分解为 **Macro（宏观分块）**和 **Micro（微观执行优化）**参数。
    2.  **波感知双线性模型**：在每个波次区间内，使用双线性公式预测延迟：$T(G, L) \approx \alpha GL + \beta G + \gamma L + \delta$。其中 $GL$ 项捕捉空间与时间的耦合，$G$ 和 $L$ 项捕捉边际开销。
    3.  **稀疏结构化采样**：无需全量测试，仅在每个波次区间内进行少量采样即可拟合模型系数。
    4.  **两阶段运行时决策**：
        *   **Stage I**：通过查表获取模型系数，计算所有 Macro 配置的预测延迟，选出最优结构。
        *   **Stage II**：根据最优结构，通过邻近锚点检索（Proximal Retrieval）确定 Micro 配置。

#### 3. 实验设计
*   **算子场景**：Dense GEMM（DeepGEMM 库）、Grouped GEMM（MoE 场景）、FlashAttention。
*   **硬件平台**：涵盖 NVIDIA（A100, H20, B200）和 AMD（MI308X, MI355X）两大厂商的五种主流架构。
*   **对比方法（Benchmarks）**：
    *   **Default Heuristic**：框架自带的启发式规则（如 SGLang, vLLM 的默认配置）。
    *   **GBDT-based Cost Model**：基于 XGBoost 的代价模型。
    *   **Decision Tree Dispatch**：直接预测配置索引的决策树。
    *   **Brute-Force Oracle**：离线穷举搜索得到的最优值（性能上限）。
*   **端到端测试**：集成到 SGLang 推理引擎中，测试 Qwen3-30B 模型在不同序列长度下的首字延迟（TTFT）。

#### 4. 资源与算力
*   **硬件使用**：使用了 A100、H20、B200、MI308X 和 MI355X 等高性能数据中心 GPU。
*   **训练/采样时长**：
    *   **WaveTune 采样**：每个“硬件-算子”对仅需 **0.5 到 2 个 GPU 小时**。
    *   **Oracle 搜索**：为了获得对比基准，穷举搜索耗费了 **10 到 600 个 GPU 小时**。
*   **模型大小**：WaveTune 的系数表仅约 **0.09 MB**，远小于 XGBoost（38.78 MB）。

#### 5. 实验数量与充分性
*   **实验规模**：测试集包含数万个样本（Dense GEMM 26K, FlashAttention 2.8K, Grouped GEMM 7.1K）。
*   **消融实验**：对比了纯线性模型（Linear）和纯阶梯模型（Step），验证了“波次感知+双线性”结合的必要性。
*   **鲁棒性验证**：通过外推（Extrapolation）实验验证了模型在超出采样范围（大 Wave 数）时的预测稳定性。
*   **客观性**：实验覆盖了不同厂商、不同算子类型以及端到端系统集成，对比基准选择合理，能够客观反映该方法在实际生产环境中的增益。

#### 6. 主要结论与发现
*   **性能提升**：WaveTune 在算子级实现了高达 **1.83倍** 的加速，端到端 TTFT 降低高达 **1.33倍**。
*   **开销极低**：运行时决策开销仅为 **5-6 微秒**，比穷举搜索低 5 个数量级，比 GBDT 模型快约 300-500 倍。
*   **接近最优**：在所有测试场景中，WaveTune 的性能始终紧贴 Oracle（最优搜索结果），且未出现学习型模型常见的性能回退（Regression）。
*   **跨平台一致性**：该物理启发式模型在 NVIDIA 和 AMD 架构上表现出高度的一致性和通用性。

#### 7. 优点
*   **物理启发式建模**：不同于纯数据驱动的黑盒模型，WaveTune 捕捉了 GPU 调度的本质特征，模型更简单、更鲁棒。
*   **极高的运行效率**：微秒级开销使其能够直接嵌入到推理引擎的临界路径中，不阻塞异步执行。
*   **采样效率高**：仅需极少的离线采样即可覆盖庞大的配置空间。
*   **消除权衡**：成功解决了“配置延迟”与“执行最优性”之间的长期矛盾。

#### 8. 不足与局限
*   **MoE 算子的简化**：在处理 Grouped GEMM 时，由于无法预知运行时的 Token 分布，采用了均匀分布假设，导致其与 Oracle 之间仍存在一定性能差距。
*   **算子覆盖范围**：目前主要针对基于分块（tile-based）的计算密集型算子，对于非分块或极小规模的算子（如 Element-wise 操作）增益有限。
*   **硬件依赖性**：虽然模型结构通用，但每个新硬件架构仍需进行一次短时间的离线采样以获取系数。

（完）
