---
title: "COEVO: Co-Evolutionary Framework for Joint Functional Correctness and PPA Optimization in LLM-Based RTL Generation"
title_zh: COEVO：基于大语言模型 RTL 生成中功能正确性与 PPA 联合优化的协同进化框架
authors: "Heng Ping, Peiyu Zhang, Shixuan Li, Wei Yang, Anzhe Cheng, Shukai Duan, Xiaole Zhang, Paul Bogdan"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15001v1"
tags: ["query:hwnas"]
score: 9.0
evidence: RTL 生成中的功能正确性与 PPA 联合优化
tldr: "针对LLM生成RTL代码时功能正确性与PPA优化脱节的问题，本文提出COEVO协同进化框架。该框架打破了先正确后优化的传统范式，将正确性视为连续优化维度，通过自适应退火门控保留具有架构潜力的部分正确代码，并利用四维帕累托非支配排序平衡面积、延迟和功耗。实验表明，COEVO在VerilogEval 2.0等基准上显著提升了Pass@1率，并在多数设计中实现了最优PPA。"
selection_source: fresh_fetch
motivation: 现有RTL生成方法将功能正确性与PPA优化解耦，导致具有架构潜力的部分正确代码被系统性丢弃，且无法有效处理多目标性能权衡。
method: 提出协同进化框架，通过细粒度评分、自适应退火门控和四维帕累托非支配排序，在单一进化循环中联合优化正确性与PPA指标。
result: "COEVO在多个基准测试中刷新了Pass@1记录，并在49个RTLLM设计中的43个上达到了最佳PPA表现。"
conclusion: 该研究证明了统一正确性与性能维度的协同进化策略在提升LLM生成硬件描述语言质量方面的优越性。
---

## 摘要
基于大语言模型（LLM）的 RTL 代码生成方法日益关注功能正确性和 PPA 质量，但现有方法普遍将这两个目标解耦，仅在完全实现正确性后才优化 PPA。无论是通过顺序多智能体流水线、带有二值正确性门控的进化搜索，还是分层奖励依赖，部分正确但在架构上具有潜力的候选代码都会被系统性地丢弃。此外，现有方法将多目标 PPA 空间简化为单一的标量适应度，掩盖了面积、延迟和功耗之间的权衡。为了解决这些局限性，我们提出了 COEVO，这是一个在单一进化循环中统一正确性和 PPA 优化的协同进化框架。COEVO 将正确性表述为与面积、延迟和功耗并列的连续协同优化维度，并通过增强的测试平台提供细粒度评分和详细的诊断反馈。具有退火机制的自适应正确性门控允许在 PPA 方面具有潜力但部分正确的候选代码引导搜索，从而获得联合最优解。为了保留完整的 PPA 权衡结构，COEVO 采用了基于四维 Pareto 的非支配排序及可配置的层内排序，取代了标量适应度，且无需手动调整权重。在 VerilogEval 2.0 和 RTLLM 2.0 上的评估结果显示，COEVO 使用 GPT-5.4-mini 实现了 97.5% 和 94.5% 的 Pass@1，在四种 LLM 骨干网络上均超越了所有智能体基准，并在 49 个可综合的 RTLLM 设计中的 43 个上获得了最佳 PPA。

## Abstract
LLM-based RTL code generation methods increasingly target both functional correctness and PPA quality, yet existing approaches universally decouple the two objectives, optimizing PPA only after correctness is fully achieved. Whether through sequential multi-agent pipelines, evolutionary search with binary correctness gates, or hierarchical reward dependencies, partially correct but architecturally promising candidates are systematically discarded. Moreover, existing methods reduce the multi-objective PPA space to a single scalar fitness, obscuring the trade-offs among area, delay, and power. To address these limitations, we propose COEVO, a co-evolutionary framework that unifies correctness and PPA optimization within a single evolutionary loop. COEVO formulates correctness as a continuous co-optimization dimension alongside area, delay, and power, enabled by an enhanced testbench that provides fine-grained scoring and detailed diagnostic feedback. An adaptive correctness gate with annealing allows PPA-promising but partially correct candidates to guide the search toward jointly optimal solutions. To preserve the full PPA trade-off structure, COEVO employs four-dimensional Pareto-based non-dominated sorting with configurable intra-level sorting, replacing scalar fitness without manual weight tuning. Evaluated on VerilogEval 2.0 and RTLLM 2.0, COEVO achieves 97.5\% and 94.5\% Pass@1 with GPT-5.4-mini, surpassing all agentic baselines across four LLM backbones, while attaining the best PPA on 43 out of 49 synthesizable RTLLM designs.

---

## 论文详细总结（自动生成）

这是一份关于论文《COEVO: Co-Evolutionary Framework for Joint Functional Correctness and PPA Optimization in LLM-Based RTL Generation》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在生成 Verilog 代码方面取得了进展，但现有的方法在处理**功能正确性**与**PPA（面积、延迟、功耗）优化**时存在严重的脱节。
*   **核心痛点**：
    1.  **解耦优化**：现有方法通常先追求 100% 正确，再进行 PPA 优化。这导致许多虽然功能有微小瑕疵但硬件架构设计极其优秀的候选代码被系统性丢弃。
    2.  **标量化陷阱**：现有研究常将 PPA 多个目标通过加权求和简化为单一标量，掩盖了面积、延迟和功耗之间的复杂权衡，且需要繁琐的手动调参。
*   **研究目标**：提出一个协同进化框架，在单一循环中同时进化代码的正确性和硬件性能。

### 2. 方法论：COEVO 框架
*   **核心思想**：将功能正确性视为一个**连续的优化维度**（而非二值门控），与 PPA 指标共同构成一个四维优化空间。
*   **关键技术细节**：
    *   **增强测试平台（Enhanced Testbench）**：通过 LLM 生成覆盖边界条件的测试向量，计算细粒度的正确性得分（$c \in [0, 1]$），并提供详细的诊断反馈。
    *   **自适应退火门控（Adaptive Correctness Gate）**：引入随进化代数增加而提高的正确性阈值。初期允许“部分正确但 PPA 优秀”的个体生存，作为进化的“垫脚石”；后期收敛至完全正确。
    *   **4D Pareto 非支配排序**：采用 NSGA-II 算法的思想，在（正确性、面积、延迟、功耗）四个维度上进行非支配排序，保留 Pareto 前沿的候选解，避免了手动设置权重。
    *   **进化算子与自适应选择**：定义了针对正确性（Fix, Simplify）、PPA（Optimize, Restructure, Explore）及两者兼顾（PPA-aware Fix, Architecture Fusion）的七种算子，并利用 **UCB-Softmax** 算法根据历史表现动态调整算子调用概率。
    *   **综合修复（SynthRepair）**：针对无法通过逻辑综合的 Offspring，利用综合工具的错误日志进行针对性修复。

### 3. 实验设计
*   **数据集/场景**：
    *   **VerilogEval 2.0**：包含 156 个设计任务，侧重组合逻辑和时序逻辑。
    *   **RTLLM 2.0**：包含 50 个更复杂的硬件模块（算术、控制、数据通路）。
*   **对比基准（Baselines）**：
    *   **智能体方法**：VeriOpt, VeriAgent, VerilogCoder, VeriMoA, REvolution, EvolVE 等。
    *   **训练/微调方法**：RTLCoder, OriGen, VeriRL, CodeV-R1 等。
*   **LLM 骨干网络**：GPT-4o-mini, GPT-4.1-mini, GPT-5-mini, GPT-5.4-mini 以及开源的 Qwen2.5-Coder-7B。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。
*   **工具链**：实验使用了 OpenAI 的 API 进行代码生成，并调用了开源 EDA 工具链（**Yosys** 进行逻辑综合，**OpenSTA** 进行静态时序分析，**NanGate 45nm** 标准库）来获取 PPA 数据。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 种不同的 LLM 骨干上进行了全量对比。
    *   针对 RTLLM 2.0 的 49 个可综合设计进行了逐一的 PPA 性能对比。
    *   **消融实验**：设计了 5 组消融实验（移除 4D 排序、增强测试平台、联合算子、自适应门控、多架构初始化），验证了各组件的必要性。
*   **客观性**：采用了 Pass@k（k=1, 5, 10）的标准无偏估计指标，并对基准测试中存在的歧义进行了修正，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **正确性突破**：COEVO 在所有 LLM 骨干上均刷新了记录。使用 GPT-5.4-mini 时，在 VerilogEval 2.0 上达到 **97.5% Pass@1**，在 RTLLM 2.0 上达到 **94.5% Pass@1**。
*   **PPA 优势**：在 49 个 RTLLM 设计中，COEVO 在 **43 个**设计上取得了最佳的综合 PPA 表现，显著优于参考设计和其他进化算法。
*   **超越微调**：使用 Qwen2.5-Coder-7B 的 COEVO（纯推理端优化）性能超过了经过强化学习微调的 CodeV-R1 模型。

### 7. 优点与亮点
*   **范式创新**：打破了“先正确后优化”的传统顺序，通过协同进化保留了具有架构潜力的中间解。
*   **多目标平衡**：引入 4D Pareto 排序，能够同时处理面积、延迟和功耗的权衡，无需人工干预权重。
*   **自适应性**：退火门控机制巧妙地平衡了进化初期的探索（Exploration）和后期的开发（Exploitation）。

### 8. 不足与局限
*   **计算开销**：由于每一代都需要调用逻辑综合工具（Yosys）和时序分析工具，且涉及多次 LLM 调用，其时间成本和 API 费用可能较高。
*   **EDA 工具限制**：实验依赖于开源 EDA 工具，其结果与商业工具（如 DC, Vivado）可能存在相关性偏差。
*   **规模扩展性**：实验主要集中在模块级 RTL 生成，对于包含数百万门电路的超大规模 SoC 级设计的适用性尚待验证。

（完）
