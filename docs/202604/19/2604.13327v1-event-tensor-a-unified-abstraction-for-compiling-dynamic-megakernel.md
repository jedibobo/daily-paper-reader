---
title: "Event Tensor: A Unified Abstraction for Compiling Dynamic Megakernel"
title_zh: Event Tensor：一种用于编译动态巨核的统一抽象
authors: "Hongyi Jin, Bohan Hou, Guanjie Wang, Ruihang Lai, Jinqi Chen, Zihao Ye, Yaxing Cai, Yixin Dong, Xinhao Cheng, Zhihao Zhang, Yilong Zhao, Yingyi Huang, Lijie Yang, Jinchen Jiang, Gabriele Oliaro, Jianan Ji, Xupeng Miao, Vinod Grover, Todd C. Mowry, Zhihao Jia, Tianqi Chen"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13327v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 高性能算子融合的编译器抽象
tldr: 针对LLM推理等GPU负载中内核启动开销大且难以处理动态性的问题，本文提出Event Tensor统一编译器抽象。该方法通过编码分块任务间的依赖关系，实现了对动态形状和数据依赖计算的原生支持。基于此构建的ETC编译器能生成高性能持久化内核，在显著降低预热开销的同时，实现了领先的推理延迟性能。
selection_source: fresh_fetch
motivation: 现有的Megakernel技术在处理大语言模型推理中的动态形状和数据依赖计算时存在局限，导致内核启动开销高且并行度不足。
method: 提出Event Tensor抽象来统一描述分块任务间的依赖，并利用ETC编译器进行静态与动态调度转换以生成高效的持久化内核。
result: 实验表明ETC在LLM推理服务中达到了最先进的延迟水平，并显著减少了系统的预热开销。
conclusion: Event Tensor为动态Megakernel提供了一种强大的编译抽象，有效提升了复杂动态GPU负载的执行效率。
---

## 摘要
现代 GPU 工作负载，特别是大语言模型（LLM）推理，受限于算子启动开销和粗粒度同步，这限制了核间并行性。近期的巨核（Megakernel）技术通过将多个算子融合进单个持久化内核中，以消除启动间隙并挖掘核间并行性，但在处理实际工作负载中的动态形状和数据依赖计算时面临挑战。我们提出了 Event Tensor，一种用于动态巨核的统一编译器抽象。Event Tensor 编码了分块任务之间的依赖关系，并为形状和数据依赖的动态性提供了原生支持。基于该抽象，我们的 Event Tensor 编译器（ETC）应用静态和动态调度变换来生成高性能的持久化内核。评估结果表明，ETC 在显著降低系统预热开销的同时，实现了领先的 LLM 服务延迟。

## Abstract
Modern GPU workloads, especially large language model (LLM) inference, suffer from kernel launch overheads and coarse synchronization that limit inter-kernel parallelism. Recent megakernel techniques fuse multiple operators into a single persistent kernel to eliminate launch gaps and expose inter-kernel parallelism, but struggle to handle dynamic shapes and data-dependent computation in real workloads. We present Event Tensor, a unified compiler abstraction for dynamic megakernels. Event Tensor encodes dependencies between tiled tasks, and enables first-class support for both shape and data-dependent dynamism. Built atop this abstraction, our Event Tensor Compiler (ETC) applies static and dynamic scheduling transformations to generate high-performance persistent kernels. Evaluations show that ETC achieves state-of-the-art LLM serving latency while significantly reducing system warmup overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Event Tensor: A Unified Abstraction for Compiling Dynamic Megakernel》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 GPU 工作负载（尤其是 LLM 推理）面临两大瓶颈：一是**内核启动开销**（Kernel Launch Overhead），在小算子密集的推理任务中，启动延迟往往超过执行时间；二是**粗粒度同步**，传统的算子边界强制执行全局同步，限制了核间并行性。
*   **研究动机**：现有的“巨核”（Megakernel）技术虽能通过算子融合缓解上述问题，但难以处理**动态形状**（如连续批处理导致的 Batch Size 变化）和**数据依赖计算**（如 MoE 架构中的动态路由）。此外，手动编写巨核代码极其复杂且难以维护。

### 2. 方法论
*   **核心思想**：提出 **Event Tensor** 抽象，将同步原语（Event）提升为编译器中的“一等公民”张量。它通过编码分块任务（Tiled Tasks）之间的依赖关系，统一了对动态性和并行性的处理。
*   **关键技术细节**：
    *   **Event Tensor 抽象**：一个多维事件数组，每个元素代表一组任务的完成状态。它支持符号形状（Symbolic Shapes），允许在不重新编译的情况下处理动态输入。
    *   **数据依赖支持**：通过索引表达式将任务坐标映射到事件坐标。例如在 MoE 中，利用运行时计算的路由张量动态更新事件计数器并触发后续任务。
    *   **ETC 编译器变换**：
        *   **静态调度**：预先为每个 SM 分配任务队列，通过 `notify()` 和 `wait()` 实现极低开销的同步。
        *   **动态调度**：使用片上（On-GPU）轻量级调度器，动态分发就绪任务，实现更好的负载均衡。
    *   **最小化运行时**：将调度逻辑直接编译进持久化内核（Persistent Kernel），避免了运行时物化任务图的开销。

### 3. 实验设计
*   **测试场景**：
    1.  **通信与计算融合**：GEMM + Reduce-Scatter, All-Gather + GEMM。
    2.  **MoE 算子**：完整的 MoE 层处理（含动态路由）。
    3.  **端到端服务**：Qwen3-30B-A3B (MoE) 和 Qwen3-32B (Dense) 的解码阶段。
*   **Benchmark 与对比方法**：
    *   **工业级系统**：vLLM, SGLang（均开启了 CUDA Graph 和 torch.compile）。
    *   **高性能库/编译器**：cuBLAS, NCCL, Triton Distributed, FlashInfer, cuBLASMp。
*   **评估指标**：推理延迟（TPOT）、系统预热时间（Warmup Time）、原始内核执行时间。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA B200 GPU**，通过 NVLink 连接。
*   **软件环境**：Ubuntu 24.04, PyTorch 2.8.0, CUDA 13.0。
*   **训练/编译时长**：论文侧重于推理和编译优化，提到 Qwen3-32B 的离线编译时间约为 107 秒。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从单算子融合到全模型端到端的测试，包括了单卡和多卡张量并行（TP=4）的配置。
*   **充分性**：实验对比了当前最强的工业级基准（vLLM/SGLang），并针对静态与动态调度进行了消融分析。
*   **客观性**：不仅展示了优势，也客观指出了在某些 TP 配置下受限于 CPU 调度开销，性能仅与基准持平的情况。

### 6. 主要结论与发现
*   **性能提升**：在 Batch Size 为 1 的低延迟场景下，ETC 相比 vLLM 实现了高达 **1.48x** 的加速。
*   **预热优化**：由于支持 AOT（提前编译）动态形状，ETC 将系统预热时间从 vLLM 的 123 秒缩短至 **35 秒**（约 **3.5x** 提升），彻底消除了运行时 CUDA Graph 捕获的负担。
*   **调度权衡**：静态调度适合规律性强的负载（如 Dense 模型），而动态调度在处理不规则负载（如 MoE）时通过负载均衡展现出更优性能。

### 7. 优点
*   **高度抽象化**：将复杂的巨核同步逻辑简化为张量操作，极大降低了开发门槛。
*   **原生支持动态性**：解决了巨核技术在动态形状和 MoE 路由上的长期痛点。
*   **真正的 AOT 编译**：避免了生产环境中频繁重新捕获 CUDA Graph 的复杂性和开销。

### 8. 不足与局限
*   **调度器竞争**：动态调度采用全局集中式队列，在极大规模集群扩展时可能存在原子操作竞争。
*   **算子调优依赖**：生成的 GEMM 分块代码在某些特定配置下可能略逊于高度手工优化的 cuBLAS。
*   **应用范围**：目前主要针对 LLM 推理的解码阶段（Latency-sensitive），对于吞吐量导向的大 Batch Prefill 阶段提升相对有限。

（完）
