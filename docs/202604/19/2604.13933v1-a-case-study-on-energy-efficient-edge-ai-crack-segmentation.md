---
title: A Case Study on Energy-Efficient Edge AI Crack Segmentation
title_zh: 关于高能效边缘人工智能裂缝分割的案例研究
authors: "Matthias Tschope, Mohamed Moursi, Vladimir Rybalkin, Bo Zhou, Norbert Wehn, Paul Lukowicz"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13933v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 电池供电设备上用于基础设施监测的能效边缘AI
tldr: 本研究针对无人机等边缘设备在基础设施裂缝检测中的高能效需求，提出了一套结合知识蒸馏（KD）和训练后量化（PTQ）的优化方案。通过在多种边缘平台部署并设计定制化的FPGA硬件架构，显著提升了裂缝分割模型的推理速度和能效比。实验表明，该方法在CrackVision12K数据集上不仅刷新了精度记录，还在FPGA上实现了极高的吞吐量和能效，为实时、低功耗的边缘AI监测提供了有效参考。
selection_source: fresh_fetch
motivation: 边缘设备在进行裂缝分割时面临内存、计算能力及电池寿命的严格限制，且现有的高性能Transformer模型难以直接高效部署。
method: 采用知识蒸馏提升基础模型性能，利用训练后量化进行模型压缩，并针对FPGA设计了定制化硬件架构以优化能效。
result: "在CrackVision12K数据集上达到71.92%的mIoU，FPGA实现达到了398 FPS的推理速度和204.99 Frames/J的超高能效。"
conclusion: 结合模型压缩技术与定制化硬件加速，可以有效解决边缘AI在复杂监测任务中的精度与能效平衡难题。
---

## 摘要
边缘设备上的裂缝分割可以支持持续的基础设施监测与维护，从而有助于保障公共安全。此外，利用无人机（UAV）进行自主基础设施监测可以降低巡检风险，因为操作人员不再需要进入危险区域。边缘处理通过消除离线处理所需的高分辨率图像存储需求降低了巡检成本，并减轻了向云服务器传输流媒体的安全风险和带宽需求。由于边缘设备的内存和计算能力有限，边缘推理面临挑战，这可能会影响准确性和延迟。此外，电池供电的设备受到严格的功耗和能量限制。这些限制共同约束了可部署在传感器附近的模型大小和计算复杂度。近年来，Transformer 在包括语义分割在内的多种应用中取得了最先进的准确率。然而，基于 Transformer 的模型通常体积庞大且计算密集，使得高效的边缘部署变得困难。为解决这一问题，我们首先应用知识蒸馏（KD）来增强基础模型的性能。随后，我们使用训练后量化（PTQ）进一步压缩模型。此外，我们还考虑了这些模型在多个边缘平台上的部署。为了最大化能效，我们在 FPGA 上为这些模型设计并实现了一种定制硬件架构。实验结果表明，知识蒸馏（KD）提升了所有测试的 U-Net 变体的性能。在评估的平台中，所选的 FPGA 实现达到了 398 FPS 的帧率和 204.99 Frames/J 的能效，同时保持了 69.42% 的平均交并比（mIoU）。此外，我们表现最好的模型达到了 71.92% 的 mIoU，比之前在 CrackVision12K 数据集上报告的结果高出 8.82 个百分点（pps）。

## Abstract
Crack segmentation on edge devices can support continuous infrastructure monitoring and maintenance and thereby help to preserve public safety. Furthermore, autonomous infrastructure monitoring by using Unmanned Aerial Vehicles (UAVs) can reduce inspection risks, as human operators no longer need to enter hazardous areas. Edge processing reduces the cost of inspection by eliminating the need for high resolution image storage for offline processing and mitigates the security risks and bandwidth requirements of streaming to cloud servers. Edge inference is difficult due to the limited memory and computational capabilities of edge devices, which can affect both accuracy and latency. Furthermore, battery-powered devices are subject to strict power and energy constraints. Together, these limitations impose restrictions on the model size and computational complexity that can be deployed close to the sensor. In recent years, Transformers have achieved state-of-the-art accuracy in a variety of applications, including semantic segmentation. However, Transformer-based models are typically large and computationally intensive, making efficient edge deployment difficult. To address this, we first apply knowledge distillation to enhance the performance of the base models. We then use PTQ to compress the models further. Additionally, we consider the deployment of these models across multiple edge platforms. To maximize energy efficiency, we design and implement a custom hardware architecture for the models on an FPGA. Our results show that Knowledge Distillation (KD) improves all tested U-Net variants. Among the evaluated platforms, the selected FPGA implementation achieves 398 FPS at 204.99 Frames/J while maintaining a mean IoU of 69.42%. In addition, our best model reaches 71.92% mean IoU, which is 8.82 percentage points (pps) higher than the previously reported result on the CrackVision12K dataset.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Case Study on Energy-Efficient Edge AI Crack Segmentation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**基础设施（如桥梁、隧道、高层建筑）的自动化监测**。传统人工巡检风险高且效率低，而利用无人机（UAV）搭载边缘AI进行实时裂缝分割是理想方案。
*   **核心挑战**：边缘设备（如无人机载计算机）面临严苛的内存、算力及电池寿命限制。
*   **技术矛盾**：目前性能最优的 Transformer 模型过于庞大，难以在边缘端实时运行；而轻量级的 U-Net 虽然速度快，但在复杂裂缝识别上的精度不足。
*   **研究目标**：通过模型压缩与定制硬件加速，在保证分割精度的前提下，实现极高的能效比和处理速度。

### 2. 方法论：核心思想与关键技术
研究者提出了一套从算法优化到硬件定制的垂直整合方案：
*   **通道缩放（Channel Scaling）**：以原始 U-Net 为基础，通过缩放因子 $c \in \{2, 4, 8, 16, 32\}$ 构建了一系列不同复杂度的变体，参数量跨越三个数量级。
*   **知识蒸馏（Knowledge Distillation, KD）**：
    *   **教师模型**：采用高容量的 **SegFormer-B5**。
    *   **学生模型**：缩放后的轻量化 U-Net。
    *   **损失函数**：结合了软目标损失（KL 散度，温度系数 $T$）和硬目标损失（加权交叉熵 + Dice Loss）。
*   **训练后量化（PTQ）**：将模型从 FP32 压缩至 INT8 和 INT4，以适配边缘加速器。
*   **FPGA 定制架构**：
    *   针对 U-Net 的**跳跃连接（Skip Connections）**设计了混合存储架构。由于跳跃连接需要缓存高分辨率特征图，极度消耗片上资源，研究者通过 AXI 接口将其卸载至片外内存，并利用片上缓冲（Buffers）平衡带宽，从而支持更大规模的模型部署。

### 3. 实验设计
*   **数据集**：使用 **CrackVision12K**，包含 12,000 张 256x256 分辨率的图像，是目前该领域标准化的基准数据集。
*   **对比基准（Benchmark）**：
    *   **算法对比**：对比了原始 U-Net、Hybrid-Segmentor（该数据集之前的 SOTA）以及 SegFormer-B5。
    *   **平台对比**：涵盖了四类典型边缘平台：
        1.  **CPU**: Raspberry Pi 5 (Cortex-A76)。
        2.  **GPU**: NVIDIA Jetson Orin Nano (使用 TensorRT)。
        3.  **NPU**: Coral Edge TPU (使用 TFLite)。
        4.  **FPGA**: AMD Zynq UltraScale+ ZU19EG (定制 HLS 架构)。
*   **评价指标**：平均交并比 (mIoU)、加权交并比 (wIoU)、吞吐量 (FPS)、功耗 (W) 及动态能效比 (Frames/J)。

### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器，训练周期最高 150 epochs，包含早停机制。
*   **超参数搜索**：采用了随机搜索与进化算法结合的方式优化学习率、权重衰减、KD 混合因子 $\alpha$ 等。
*   **算力说明**：文中**未明确说明**具体用于训练的服务器 GPU 型号（如 A100 或 RTX 4090），但详细列出了推理端所有边缘设备的型号及配置。

### 5. 实验数量与充分性
*   **实验规模**：针对 5 种不同规模的 U-Net 变体，分别在有/无 KD、不同量化精度（FP32/INT8/INT4）以及 4 种硬件平台上进行了交叉测试。
*   **客观性**：所有模型训练均重复 **5 次随机种子**并取平均值和标准差，确保了结果的统计学意义。
*   **公平性**：在对比不同硬件平台时，考虑了数据传输延迟和格式转换开销，并区分了静态功耗与动态功耗，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **KD 的显著作用**：知识蒸馏大幅提升了小模型的性能。例如，Base 4 模型在 KD 引导下，mIoU 提升了约 8 个百分点，缩小了与大模型的差距。
*   **精度突破**：其最佳模型在 CrackVision12K 上达到了 **71.92% mIoU**，比之前的记录高出 **8.82 pps**。
*   **FPGA 性能领先**：在单批次（Batch Size = 1）推理中，FPGA 实现表现最优。Base 4 变体在 FPGA 上达到了 **398 FPS** 的吞吐量和 **204.99 Frames/J** 的超高能效，远超 Jetson 和 Coral 平台。
*   **量化权衡**：INT8 量化对精度影响极小，甚至因正则化效应略有提升；但 INT4 量化在 PTQ 模式下会导致精度大幅下降。

### 7. 优点（亮点）
*   **全栈式优化**：不仅在算法层（KD）和模型层（缩放）做文章，还深入到硬件架构层（FPGA 存储优化），提供了完整的工程参考。
*   **硬件架构创新**：提出的“跳跃连接片外卸载”方案有效解决了 FPGA 部署 U-Net 时片上 BRAM 资源不足的痛点。
*   **多平台详尽对比**：为开发者在不同边缘硬件（CPU/GPU/NPU/FPGA）之间进行权衡提供了宝贵的实测数据。

### 8. 不足与局限
*   **量化技术单一**：仅使用了训练后量化（PTQ）。对于 INT4 精度大幅下降的问题，未尝试量化感知训练（QAT），这限制了模型进一步压缩的潜力。
*   **场景覆盖有限**：实验仅基于 CrackVision12K 静态数据集，尚未在真实的无人机飞行环境（伴随运动模糊、光照剧烈变化等）中进行实地验证。
*   **FPGA 开发门槛**：虽然性能优异，但定制 HLS 架构的开发成本远高于直接使用 TensorRT 或 TFLite 的通用平台。

（完）
