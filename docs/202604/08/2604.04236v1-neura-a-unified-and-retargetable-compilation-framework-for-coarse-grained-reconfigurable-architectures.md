---
title: "NEURA: A Unified and Retargetable Compilation Framework for Coarse-Grained Reconfigurable Architectures"
title_zh: NEURA：一种用于粗粒度可重构架构的统一且可重定向的编译框架
authors: "Shangkun Li, Jinming Ge, Diyuan Tao, Zeyu Li, Jiawei Liang, Linfeng Du, Jiang Xu, Wei Zhang, Cheng Tan"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04236v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 粗粒度可重构架构的编译框架
tldr: NEURA是一个针对粗粒度可重构架构（CGRA）的统一且可重定向的编译框架。针对CGRA难以高效处理控制流的问题，NEURA提出了一种基于谓词类型系统的纯数据流中间表示（IR），将控制上下文嵌入数据中，从而将复杂控制流展平为统一的数据流图。该框架实现了内核表示与硬件的解耦，支持多种执行模型，在性能和通用性上显著优于现有方案。
selection_source: fresh_fetch
motivation: 传统的CGRA在处理循环和分支等控制流时，由于控制流与并行数据驱动架构不匹配，导致效率低下或缺乏通用性。
method: 提出一种基于谓词类型系统的纯数据流中间表示（IR），通过将控制信息作为数据的内在属性，将复杂控制流系统地展平为统一的数据流图。
result: 在高性能CGRA上，NEURA相比现有SOTA方案实现了2.20倍的内核加速和2.71倍的应用加速，且在低功耗架构上也具有竞争力。
conclusion: NEURA通过解耦内核表示与硬件架构，为不同执行模型的CGRA提供了一种高效、通用且可重定向的编译解决方案。
---

## 摘要
粗粒度可重构架构 (CGRA) 是一种极具前景且多功能的加速器平台，在专用加速器的性能与效率以及软件可编程性之间实现了平衡。然而，由于控制流（如循环、分支）与并行的、数据驱动的 CGRA 结构在根本上不兼容，加速内核中的控制流严重阻碍了其潜力的充分发挥。先前解决 CGRA 内核加速中这种不匹配问题的策略要么效率低下（以牺牲性能换取通用性），要么由于难以适配不同的执行模型而缺乏通用性。因此，一种用于高效 CGRA 内核加速的通用且统一的解决方案仍然难以实现。本文介绍了 NEURA，这是一个统一且可重定向的编译框架，系统地解决了 CGRA 中的控制-数据流不匹配问题。NEURA 的核心创新是一种基于谓词类型系统（predicated type system）构建的新型纯数据流中间表示 (IR)。在该 IR 中，控制上下文作为谓词嵌入到每个数据中，使控制成为数据的内在属性。这种机制使 NEURA 能够系统地将复杂的控制流展平为单一的统一数据流图。这种统一的表示形式将内核表示与硬件解耦，使 NEURA 能够重定向到具有不同执行模型和微架构特征的各种 CGRA。当针对高性能时空 CGRA 时，与最先进的 (SOTA) 高性能基准相比，NEURA 在内核基准测试中实现了 2.20 倍的加速，在实际应用中实现了高达 2.71 倍的几何平均加速。当重定向到仅空间（spatial-only）CGRA 时，它还提供了一个与 SOTA 低功耗 CGRA 相比具有竞争力的解决方案。NEURA 已开源，可在 https://github.com/coredac/neura 获取。

## Abstract
Coarse-Grained Reconfigurable Architectures (CGRAs) are a promising and versatile accelerator platform, offering a balance between the performance and efficiency of specialized accelerators and the software programmability. However, their full potential is severely hindered by control flow in accelerated kernels, as the control flow (e.g., loops, branches) is fundamentally incompatible with the parallel, data-driven CGRA fabric. Prior strategies to resolve this mismatch in CGRA kernel acceleration are either inefficient, sacrificing performance for generality, or lack generality due to the difficulty of adapting them across different execution models. Thus, a general and unified solution for efficient CGRA kernel acceleration remains elusive.   This paper introduces NEURA, a unified and retargetable compilation framework that systematically resolves the control-dataflow mismatch in CGRAs. NEURA's core innovation is a novel, pure dataflow intermediate representation (IR) built on a predicated type system. In this IR, control contexts are embedded as a predicate within each data, making control an intrinsic property of data. This mechanism enables NEURA to systematically flatten complex control flow into a single unified dataflow graph. This unified representation decouples kernel representation from hardware, empowering NEURA to retarget diverse CGRAs with different execution models and microarchitectural features. When targeted to a high-performance spatio-temporal CGRA, NEURA delivers a 2.20x speedup on kernel benchmarks and up to 2.71x geometric mean speedup on real-world applications over state-of-the-art (SOTA) high-performance baselines. It also provides a competitive solution against the SOTA low-power CGRA when retargeted to a spatial-only CGRA. NEURA is open-source and available at https://github.com/coredac/neura.

---

## 论文详细总结（自动生成）

这是一份关于论文《NEURA: A Unified and Retargetable Compilation Framework for Coarse-Grained Reconfigurable Architectures》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：粗粒度可重构架构（CGRA）在处理内核中的**控制流**（如嵌套循环、分支）时存在严重的“控制-数据流语义鸿沟”。
*   **研究动机**：
    *   CGRA本质上是并行的、数据驱动的硬件，而传统的控制流（CFG）是顺序的、控制驱动的。
    *   **现有方案的缺陷**：
        1.  **CDFG策略**：依赖外部控制器（如CPU）管理控制流，导致执行序列化，产生大量通信和重构开销。
        2.  **转向控制（Steering Control）策略**：难以适配时空（Spatio-Temporal）执行模型，且布线复杂。
        3.  **有限谓词策略**：无法处理分层谓词（如嵌套循环中的分支），最终仍需退回到序列化执行。
*   **目标**：建立一个统一、可重定向的编译框架，将复杂的控制流系统地展平为纯数据流表示。

### 2. 方法论
*   **核心思想**：提出一种**谓词类型系统（Predicated Type System）**，将控制上下文作为“谓词”嵌入到每个数据中。数据不再只是单纯的数值，而是 `(data, predicate)` 元组，使控制成为数据的内在属性。
*   **关键技术细节**：
    *   **NEURA Dataflow IR**：一种纯数据流中间表示。通过 `grant_predicate`（赋予谓词）、`phi`（数据流合并）和 `grant_once`（初始触发）等操作，将控制依赖转化为数据依赖。
    *   **分层谓词处理**：通过谓词位的逻辑运算（如AND），天然支持嵌套循环和复杂分支的逻辑表达。
    *   **系统化展平流程**：
        1.  **规范化（Canonicalization）**：通过 `live-in` 分析，确保所有跨基本块的数据依赖在CFG边缘显式传递。
        2.  **数据谓词化**：将所有标准类型提升为谓词类型。
        3.  **展平（Flattening）**：利用 `reserve` 和 `ctrl_mov` 操作处理循环回边，在维持SSA（静态单赋值）形式的同时消除所有分支指令。
*   **硬件接口**：仅需对现有CGRA ISA进行极小修改（增加3个专用指令，并在FU中增加简单的谓词逻辑门）。

### 3. 实验设计
*   **数据集/场景**：
    *   **内核基准测试**：涵盖机器学习（conv, relu）、线性代数（gemm, mvt）、信号处理（fft, adpcm）和图算法（bfs, dijkstra）等4个领域的14个典型内核。
    *   **实际应用**：2层图卷积网络（GCN）和LU分解（LU Decomposition）。
*   **对比方法（Baselines）**：
    *   **Marionette**（代表CDFG策略/外部控制）。
    *   **RipTide**（代表转向控制策略/低功耗空间模型）。
    *   **ICED**（代表有限谓词策略）。
*   **评估架构**：构建了高性能时空架构（NEURA-ST）和低功耗仅空间架构（NEURA-SO）两个原型。

### 4. 资源与算力
*   **硬件实现**：使用 RTL 实现原型，并在 **TSMC 22nm ULL 工艺**下进行综合。
*   **算力/工具**：
    *   使用 **Synopsys Design Compiler** 进行面积和频率分析（目标频率 800MHz）。
    *   编译器基于 **MLIR** 框架开发（约1.5万行C++代码）。
    *   **未提及 GPU 训练算力**：由于本文属于编译器与架构设计领域，不涉及深度学习模型的训练，主要算力消耗在于电路综合与周期精确仿真。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对14个内核和2个大型应用进行了完整的性能、IPC和面积效率对比。
    *   进行了**消融实验**，验证了硬件无关优化（如常量折叠）和硬件相关优化（如循环流化）的贡献。
    *   进行了**可扩展性分析**，测试了从 4x4 到 6x6 阵列规模下的性能增益。
*   **充分性评价**：实验设计较为全面，不仅对比了不同策略的SOTA框架，还跨越了两种主流的CGRA执行模型（时空 vs 仅空间），证明了框架的通用性（Retargetability）。

### 6. 主要结论
*   **性能提升**：在高性能架构上，NEURA 相比 SOTA 基准实现了 **2.20倍** 的内核加速，在实际应用中实现高达 **2.71倍** 的几何平均加速。
*   **面积效率**：NEURA-ST 的单位面积性能（Perf/Area）比 Marionette 高出 **6.40倍**。
*   **硬件开销极低**：引入谓词支持带来的面积增加仅为 **1.39% - 1.89%**。
*   **通用性**：同一套 IR 能够同时适配高性能时空模型和低功耗空间模型，且在低功耗场景下性能优于 RipTide。

### 7. 优点
*   **理论完备性**：通过谓词类型系统彻底解决了嵌套控制流的展平问题，并给出了形式化语义和算法证明。
*   **架构解耦**：IR 不绑定特定硬件，使得编译器能够灵活支持多种 CGRA 微架构。
*   **工程落地**：基于成熟的 MLIR 基础设施，易于集成到现有的 LLVM 编译生态中，且已开源。

### 8. 不足与局限
*   **映射算法依赖**：性能高度依赖于底层的 Mapper（映射器）寻找最优 II（启动间隔）的能力，论文主要使用了启发式映射，未探讨极大规模阵列下的映射效率。
*   **加速粒度选择**：虽然论文提到可以手动选择加速粒度（从内层循环到整个内核），但如何**自动**确定最优的加速边界仍是一个待解决的挑战。
*   **内存瓶颈**：虽然解决了控制流问题，但对于访存密集型应用，性能仍受限于 CGRA 的内存带宽，这一点在纯计算优化中难以完全消除。

（完）
