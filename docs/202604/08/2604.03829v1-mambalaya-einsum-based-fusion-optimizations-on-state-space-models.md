---
title: "Mambalaya: Einsum-Based Fusion Optimizations on State-Space Models"
title_zh: Mambalaya：基于 Einsum 的状态空间模型融合优化
authors: "Toluwanimi O. Odemuyiwa, John D. Owens, Joel S. Emer, Michael Pellauer"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03829v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对状态空间模型的可重构加速器与算子融合优化
tldr: 针对Mamba模型因复杂依赖关系和非线性计算导致的硬件运行效率低下及高内存访问开销问题，本文提出Mambalaya加速器。该方案利用Einsum级联抽象对Mamba计算结构建模，通过扩展Einsum框架系统探索算子融合机会，显著减少了片外访存流量，在Prefill和Generation阶段均实现了显著的性能提升。
selection_source: fresh_fetch
motivation: Mamba复杂的依赖图导致全算子链融合困难，产生了巨大的算子间内存访问开销。
method: 利用Einsum级联抽象表征Mamba结构，并基于扩展Einsum框架系统地探索和实施算子融合映射。
result: 在Prefill和Generation阶段分别比MARCA快4.9倍和1.9倍，且在Prefill场景下优于现有的内存感知融合加速器。
conclusion: Mambalaya通过基于Einsum的融合优化策略，有效克服了状态空间模型在现代硬件上的内存瓶颈。
---

## 摘要
Mamba 是一种新兴的复杂工作负载，具有各种短程和远程依赖关系、非线性以及逐元素计算，在现代硬件上无法以接近峰值的速度运行。具体而言，Mamba 复杂的依赖图使得跨其完整算子级联的融合变得困难，从而留下了大量的算子间内存流量。为了应对这些挑战，我们提出了 Mambalaya，这是一种新型的可重构加速器，利用融合技术来克服 Mamba 的局限性。我们使用最近提出的 Einsum 级联（cascade-of-Einsums）抽象来表征 Mamba 的完整计算结构，然后应用扩展的 Einsum 框架系统地探索 Einsum 间的融合机会。这种基于原则的方法产生了一系列融合映射，减少了片外 Einsum 间的流量。这些映射由底层的 Mambalaya 架构支持。与 MARCA 相比，Mambalaya 在预填充（prefill）阶段实现了 4.9 倍的层性能加速，在生成（generation）阶段实现了 1.9 倍的加速。在以预填充为主的场景中，与最近的一种针对 Mamba 的细粒度、内存感知融合加速器相比，它实现了高达 1.5 倍的加速。

## Abstract
Mamba is an emerging, complex workload with various short-range and long-range dependencies, nonlinearities, and elementwise computations that are unable to run at near-peak speeds on modern hardware. Specifically, Mamba's complex dependency graph makes fusion across its full operator cascade difficult, leaving substantial inter-operator memory traffic on the table. To address these challenges, we propose Mambalaya, a novel reconfigurable accelerator that leverages fusion to overcome the limitations of Mamba. We use the recently proposed cascade-of-Einsums abstraction to characterize Mamba's full computational structure, then apply the extended Einsum framework to systematically explore inter-Einsum fusion opportunities. This principled approach yields a series of fusion mappings that reduce off-chip inter-Einsum traffic. These mappings are supported by the underlying Mambalaya architecture. Mambalaya achieves a layer performance speedup of 4.9$\times$ for prefill and 1.9$\times$ for generation over MARCA. In prefill-dominated scenarios, it achieves up to 1.5$\times$ over a recent fine-grained, memory-aware fusion accelerator for Mamba.

---

## 论文详细总结（自动生成）

这是一份关于论文《Mambalaya: Einsum-Based Fusion Optimizations on State-Space Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型的发展，状态空间模型（SSM）如 Mamba 因其在线性时间内处理长序列的能力而备受关注。然而，与结构相对简单的 Transformer 不同，Mamba 包含复杂的算子依赖图、大量的非线性函数和逐元素计算。
*   **研究动机**：现有的硬件加速器在运行 Mamba 时，由于算子间存在海量的中间数据交换（DRAM 流量），导致计算利用率远低于峰值。
*   **核心问题**：如何通过系统性的算子融合（Fusion）减少访存开销，并设计匹配的硬件架构来提升 Mamba 的执行效率。

### 2. 方法论
论文提出了一套从算法抽象到硬件实现的完整方案：
*   **Einsum 级联抽象**：利用扩展的爱因斯坦求和（Einsum）框架将 Mamba 的 24 个算子形式化。引入“代际秩（Generational Ranks）”来表达 SSM 的递归特性，以及用户自定义操作来表达非线性激活。
*   **融合分类法（Taxonomy）**：定义了四种融合类型：
    1.  **秩同构融合 (RI)**：迭代空间完全一致。
    2.  **秩子集融合 (RSb)**：上游是下游的超集（如规约操作）。
    3.  **秩超集融合 (RSp)**：下游是上游的超集（如广播操作）。
    4.  **秩不相交融合 (RD)**：双方各有独特维度。
*   **贪婪缝合算法 (Greedy Stitching)**：通过分析张量索引，自动将多个 Einsum 组合成融合组，确保中间结果留在片上。
*   **Mambalaya 架构**：一种可重构加速器。其核心是一个 2D PE 阵列，支持两种模式：
    *   **2D 模式**：用于高效执行 GEMM（矩阵乘法）。
    *   **1D 模式**：将阵列重构为长向量处理器，专门处理低计算强度的逐元素和非线性算子，避免计算资源闲置。

### 3. 实验设计
*   **数据集/场景**：测试了 Mamba-370m 和 Mamba-2.8b 两种模型规模；涵盖了从短上下文长生成到长上下文短生成（如文档摘要）的多种推理场景。
*   **Benchmark**：
    *   **MARCA**：此前最先进的 Mamba 定制加速器。
    *   **Geens et al.**：一种最近提出的细粒度内存感知融合方案。
    *   **Best Unfused**：假设具有完美片内缓存但未进行算子融合的理想基准。
*   **评估工具**：使用 Timeloop 建模工具进行性能、功耗和访存流量的仿真。

### 4. 资源与算力
*   **硬件配置**：Mambalaya 模拟了类 NVIDIA H100 的规格（1.75 GHz 频率，2039 GB/s 内存带宽，32MB 全局缓存）。
*   **算力规模**：包含 65,536 个主 PE 和 256 个专门的 1D PE。
*   **训练/仿真时长**：文中未提及具体仿真耗时，主要侧重于推理阶段的周期精确度建模。

### 5. 实验数量与充分性
论文进行了多维度的实验：
*   对比了四种不同深度的融合策略（RI, RI+RSb, RI+RSb+RSp, Fully Fused）。
*   分析了预填充（Prefill）和生成（Decode）两个阶段的 Roofline 利用率。
*   测试了不同序列长度（最高达 $2^{20}$）下的性能表现。
*   **评价**：实验设计较为充分，通过分解算子流量（Inter- vs Intra-Einsum）客观地展示了融合带来的收益，对比基准也选择了该领域最新的研究成果。

### 6. 主要结论与发现
*   **性能提升**：在预填充阶段，Mambalaya 比 MARCA 快 **4.9 倍**；在生成阶段快 **1.9 倍**。
*   **访存优化**：融合策略将算子间的内存流量减少了 **4 倍至 34 倍**。
*   **利用率**：通过全融合策略，原本受限于内存带宽的 SSM 核心区域在预填充阶段达到了接近 100% 的计算利用率。
*   **发现**：虽然全融合在长序列预填充中表现最优，但在单 Token 生成阶段，由于计算规模太小，过度融合导致的额外局部流量反而可能成为瓶颈，此时简单的 RI 融合效果更好。

### 7. 优点
*   **理论系统化**：首次将复杂的 Mamba 算子链通过 Einsum 理论进行了严谨的分类和自动化融合分析。
*   **架构灵活性**：Mambalaya 的 1D/2D 可重构设计巧妙地解决了 Mamba 中“重计算（GEMM）”与“轻计算（逐元素）”算子并存的资源分配难题。
*   **通用性**：提出的融合分类法不仅适用于 Mamba，也可推广至其他复杂的张量代数工作负载。

### 8. 不足与局限
*   **仿真局限**：所有结果均基于 Timeloop 仿真，缺乏实际流片或 FPGA 原型的实测数据，可能忽略了复杂的布线延迟和物理约束。
*   **多 pass 限制**：对于某些需要多次扫描输入的算子（如某些归一化操作），目前的融合策略仍无法完全消除重复加载。
*   **应用范围**：主要针对推理优化，未深入讨论该架构在 Mamba 模型训练阶段的适用性。

（完）
