---
title: "LOCALUT: Harnessing Capacity-Computation Tradeoffs for LUT-Based Inference in DRAM-PIM"
title_zh: LOCALUT：在 DRAM-PIM 中利用容量-计算权衡实现基于 LUT 的推理
authors: "Junguk Hong, Changmin Shin, Sukjin Kim, Si Ung Noh, Taehee Kwon, Seongyeon Park, Hanjun Kim, Youngsok Kim, Jinho Lee"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04523v1"
tags: ["query:hwnas"]
score: 9.0
evidence: DRAM-PIM中基于查找表推理的软硬件权衡
tldr: 本研究针对DRAM-PIM架构中逻辑密度受限的问题，提出了LOCALUT框架，利用查找表（LUT）替代传统算术逻辑进行低比特量化DNN推理。通过将多个乘累加（MAC）操作打包进单个LUT，实现了存储容量与计算性能的权衡。LOCALUT引入了LUT规范化、重排序LUT及切片流式传输技术，显著降低了冗余并优化了数据复用。在UPMEM真实系统上的评估显示，该方案在多种模型下实现了1.82倍的几何平均加速。
selection_source: fresh_fetch
motivation: DRAM-PIM架构面临逻辑密度有限但存储带宽充足的挑战，需要一种能将存储容量转化为计算能力的推理机制。
method: 提出了LOCALUT设计，通过LUT规范化消除冗余、利用重排序LUT映射权重，并采用切片流式传输策略优化DRAM缓存层级的数据复用。
result: 在基于UPMEM的真实硬件系统上，LOCALUT在不同数值精度和DNN模型下实现了1.82倍的几何平均加速。
conclusion: LOCALUT证明了在PIM设计中通过LUT实现容量与计算权衡的可行性，为低逻辑密度的PIM架构提供了高效的DNN推理路径。
---

## 摘要
查找表（LUT）作为一种将输入操作数映射到预计算结果的替代计算机制，最近受到了广泛关注，它消除了对算术逻辑的需求。LUT 不仅降低了逻辑复杂度，而且能够自然地支持多种数值精度，而无需为每种位宽设计独立的电路——这是量化深度神经网络（DNN）中一个日益重要的特性。这在存内计算（PIM）中创造了一种有利的权衡：可以利用存储容量代替逻辑来提高计算吞吐量，这与提供高带宽和充足内存但逻辑密度有限的 DRAM-PIM 架构非常契合。在这项工作中，我们探索了基于 LUT 的 PIM 设计中的这种容量-计算权衡，通过将多个乘累加（MAC）操作打包到单个 LUT 查找中，以存储容量换取性能。基于这一见解，我们提出了 LOCALUT，这是一种基于 PIM 的设计，利用操作打包的 LUT 实现高效的低比特量化 DNN 推理。首先，我们观察到这些 LUT 包含大量冗余，并引入了 LUT 规范化（canonicalization），通过消除重复条目来减小 LUT 大小。其次，我们提出了重排序 LUT（reordering LUT），这是一种轻量级的辅助 LUT，通过简单的 LUT 查找将权重向量重新映射为 LUT 规范化所需的规范形式。第三，我们提出了 LUT 切片流式传输（LUT slice streaming），这是一种利用 DRAM-缓冲区层级结构的新颖执行策略，它仅将相关的 LUT 列流式传输到缓冲区，并在多个权重向量之间重用。在基于 UPMEM 设备的真实系统上进行的评估表明，在各种数值精度和 DNN 模型中，我们实现了 1.82 倍的几何平均加速。我们相信 LOCALUT 为针对基于 LUT 的 DNN 推理而定制的可扩展、低逻辑 PIM 设计开辟了道路。我们的 LOCALUT 实现可在 https://github.com/AIS-SNU/LoCaLUT 获取。

## Abstract
Lookup tables (LUTs) have recently gained attention as an alternative compute mechanism that maps input operands to precomputed results, eliminating the need for arithmetic logic. LUTs not only reduce logic complexity, but also naturally support diverse numerical precisions without requiring separate circuits for each bitwidth-an increasingly important feature in quantized DNNs. This creates a favorable tradeoff in PIM: memory capacity can be used in place of logic to increase computational throughput, aligning well with DRAM-PIM architectures that offer high bandwidth and easily available memory but limited logic density. In this work, we explore this capacity-computation tradeoff in LUT-based PIM designs, where memory capacity is traded for performance by packing multiple MAC operations into a single LUT lookup. Building on this insight, we propose LOCALUT, a PIM-based design for efficient low-bit quantized DNN inference using operation-packed LUTs. First, we observe that these LUTs contain extensive redundancy and introduce LUT canonicalization, which eliminates duplicate entries to reduce LUT size. Second, we propose reordering LUT, a lightweight auxiliary LUT that remaps weight vectors to their canonical form required by LUT canonicalization with a simple LUT lookup. Third, we propose LUT slice streaming, a novel execution strategy that exploits the DRAM-buffer hierarchy by streaming only relevant LUT columns into the buffer and reusing them across multiple weight vectors. Evaluated on a real system based on UPMEM devices, we demonstrate a geometric mean speedup of 1.82x across various numeric precisions and DNN models. We believe LOCALUT opens a path toward scalable, low-logic PIM designs tailored for LUT-based DNN inference. Our implementation of LOCALUT is available at https://github.com/AIS-SNU/LoCaLUT.

---

## 论文详细总结（自动生成）

这是一份关于论文《LOCALUT: Harnessing Capacity–Computation Tradeoffs for LUT-Based Inference in DRAM-PIM》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：DRAM-PIM（存内计算）架构虽然解决了“内存墙”问题，但面临“逻辑墙”挑战。DRAM制造工艺追求高密度存储，导致其逻辑电路面积效率低、速度慢。现有的PIM芯片（如UPMEM、HBM-PIM）仅能集成少量的固定精度算术单元，难以高效支持现代DNN中多样化的低比特量化格式。
*   **研究动机**：查找表（LUT）可以将复杂的算术运算转化为简单的内存查找。由于DRAM拥有巨大的存储容量，研究者提出利用“容量换计算”的思路，通过在LUT中打包多个操作来提升吞吐量。
*   **整体含义**：本文提出了 **LOCALUT**，一套针对DRAM-PIM优化的、基于LUT的低比特DNN推理框架，旨在通过优化LUT的存储与访问机制，在逻辑受限的PIM芯片上实现高性能推理。

### 2. 方法论
LOCALUT的核心思想是利用DRAM的大容量来存储预计算的乘累加（MAC）结果，并引入以下关键技术：
*   **操作打包 LUT (Operation-Packed LUT)**：将 $p$ 个乘法操作的结果打包进一个LUT条目。例如，给定权重向量和激活向量，直接查找其内积结果。
*   **LUT 规范化 (LUT Canonicalization)**：观察到 $a \cdot x + b \cdot y$ 与 $b \cdot y + a \cdot x$ 结果相同，通过对输入激活进行排序，消除LUT中的排列冗余，大幅缩减LUT体积（在 $p=7$ 时可缩减 611倍）。
*   **重排序 LUT (Reordering LUT)**：为了避免PIM中微弱的CPU核心在运行时进行复杂的排序操作，引入一个辅助LUT，通过一次查找直接将原始权重映射为规范化后的索引。
*   **LUT 切片流式传输 (LUT Slice Streaming)**：针对PIM内部SRAM缓冲区（Local Buffer）容量有限的问题，将大型LUT存储在DRAM Bank中，仅将当前计算所需的“列切片”流式传输到缓冲区并进行多次复用。
*   **性能模型**：提供了一个一阶解析模型，用于根据矩阵维度和硬件参数自动选择最优的打包程度 $p^*$。

### 3. 实验设计
*   **硬件平台**：包含 16 个 UPMEM DIMM（共 2048 个处理单元/PE）的真实服务器系统。
*   **数据集/场景**：
    *   **语言模型**：BERT-base, OPT-125M（使用 GLUE 基准测试：QNLI, QQP, STS-B, SST-2）。
    *   **视觉模型**：ViT-Base（使用 ImageNet 数据集）。
*   **对比方法 (Benchmarks)**：
    *   **Naive PIM**：使用UPMEM原生的int8乘法器。
    *   **LTC (LUT Tensor Core)**：一种先进的基于位串行的LUT设计。
    *   **PIM-DL / LUT-DLA**：基于乘积量化（PQ）的SOTA方法。
*   **量化配置**：W1A3, W1A4, W2A2, W4A4（W=权重，A=激活）。

### 4. 资源与算力
*   **硬件资源**：
    *   **主机**：Intel Xeon Gold 5215 CPU。
    *   **PIM设备**：32 Rank 的 UPMEM PIM-DRAM，每个 Bank 拥有 64MB DRAM 和 64KB SRAM 缓冲区。
*   **算力说明**：实验在真实硬件上运行，而非纯模拟。此外，作者还使用 **Ramulator 2.0** 模拟器评估了该方法在类 HBM-PIM 架构上的潜力。
*   **训练时长**：未提及（本文聚焦于推理加速，不涉及模型训练）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从算子级（GEMM）到端到端模型（BERT, OPT, ViT）的全面评估。
*   **维度丰富**：进行了针对矩阵维度（M, K, N）、打包程度（$p$）、切片数量（$k$）的灵敏度分析，并对比了能量消耗和模型精度。
*   **充分性**：实验对比了真实硬件基准与多种SOTA算法，并验证了在不同量化比特下的表现，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **性能提升**：相比于 LTC，LOCALUT 实现了 **1.82倍** 的几何平均加速；相比于原生 PIM 乘法实现，加速比更高。
*   **精度保持**：在极低比特（如W1A3）下，LOCALUT 在保持与现有量化技术相当精度的同时，显著优于基于 PQ 的方法。
*   **位宽敏感性**：比特数越低，LUT 越小，可实现的打包程度 $p$ 越高，加速效果越明显。
*   **架构通用性**：该方法不仅适用于 UPMEM，在模拟的类 HBM-PIM 架构上也实现了约 2.04倍 的加速。

### 7. 优点
*   **软硬协同优化**：通过规范化和重排序 LUT，巧妙地解决了 PIM 逻辑能力弱的问题。
*   **真实系统验证**：在拥有 2048 个处理单元的真实 PIM 系统上完成评估，具有极高的工程参考价值。
*   **灵活性**：LUT 机制使其能自然支持各种数值格式（包括浮点 FP4/FP8），无需修改硬件电路。

### 8. 不足与局限
*   **容量开销**：虽然有规范化技术，但随着打包程度 $p$ 或位宽增加，LUT 占用的 DRAM 空间仍会呈指数级增长（最高占用 64MB/Bank）。
*   **位宽限制**：当位宽超过 4-bit 时，LUT 的规模将变得不可接受，性能优势会迅速减弱。
*   **主机依赖**：量化、排序索引准备以及非线性层（如 Softmax）仍需依赖主机 CPU 处理，这可能成为整体系统的瓶颈。

（完）
