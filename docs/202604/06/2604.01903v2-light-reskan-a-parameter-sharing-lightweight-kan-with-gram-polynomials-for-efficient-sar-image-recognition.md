---
title: "Light-ResKAN: A Parameter-Sharing Lightweight KAN with Gram Polynomials for Efficient SAR Image Recognition"
title_zh: Light-ResKAN：一种基于 Gram 多项式的参数共享轻量级 KAN，用于高效 SAR 图像识别
authors: "Pan Yi, Weijie Li, Xiaodong Chen, Jiehua Zhang, Li Liu, Yongxiang Liu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01903v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于资源受限边缘设备SAR图像识别的轻量化模型
tldr: 针对合成孔径雷达（SAR）图像识别在边缘设备部署中面临的计算资源受限问题，本文提出了一种轻量化模型 Light-ResKAN。该模型通过将 ResNet 中的卷积替换为基于 Gram 多项式的 KAN 卷积，并引入参数共享策略，在保持高精度的同时大幅降低了参数量和计算量。在 MSTAR 等多个数据集上的实验证明，该模型在显著减少 FLOPs 的同时实现了优异的识别性能，为边缘端 SAR 图像识别提供了高效解决方案。
selection_source: fresh_fetch
motivation: 现有的 SAR 图像识别模型难以在资源受限的边缘设备上平衡高精度特征提取与低计算需求。
method: 结合 KAN 网络与 ResNet 架构，采用 Gram 多项式作为激活函数，并实施通道级参数共享策略以实现轻量化。
result: 在 MSTAR 等数据集上取得高准确率，且相比 VGG16，FLOPs 降低了 82.90 倍，参数量减少了 163.78 倍。
conclusion: Light-ResKAN 成功实现了 SAR 图像识别的高效化与轻量化，为边缘端实时监测提供了理想的深度学习方案。
---

## 摘要
合成孔径雷达 (SAR) 图像识别对于灾害监测、军事侦察和海洋观测至关重要。然而，庞大的 SAR 图像尺寸阻碍了深度学习在资源受限的边缘设备上的部署，且现有的轻量级模型难以在保持高精度特征提取与降低计算需求之间取得平衡。新兴的 Kolmogorov-Arnold 网络 (KAN) 通过将固定激活函数替换为可学习的激活函数来增强拟合能力，从而减少了参数量和计算量。受 KAN 的启发，我们提出了 Light-ResKAN，旨在实现精度与效率之间更好的平衡。首先，Light-ResKAN 通过将卷积替换为 KAN 卷积来改进 ResNet，从而实现对 SAR 图像的自适应特征提取。其次，我们采用 Gram 多项式作为激活函数，这非常适合 SAR 数据，能够捕捉复杂的非线性关系。第三，我们采用了一种参数共享策略：每个卷积核在通道间共享参数，在保留独特特征的同时减少了参数量和浮点运算量 (FLOPs)。我们的模型在 MSTAR、FUSAR-Ship 和 SAR-ACD 数据集上分别达到了 99.09%、93.01% 和 97.26% 的准确率。在调整为 $1024 \times 1024$ 分辨率的 MSTAR 数据集上的实验表明，与 VGG16 相比，我们的模型将 FLOPs 降低了 82.90 倍，参数量减少了 163.78 倍。本研究为边缘 SAR 图像识别提供了一种高效的解决方案。

## Abstract
Synthetic Aperture Radar (SAR) image recognition is vital for disaster monitoring, military reconnaissance, and ocean observation. However, large SAR image sizes hinder deep learning deployment on resource-constrained edge devices, and existing lightweight models struggle to balance high-precision feature extraction with low computational requirements. The emerging Kolmogorov-Arnold Network (KAN) enhances fitting by replacing fixed activations with learnable ones, reducing parameters and computation. Inspired by KAN, we propose Light-ResKAN to achieve a better balance between precision and efficiency. First, Light-ResKAN modifies ResNet by replacing convolutions with KAN convolutions, enabling adaptive feature extraction for SAR images. Second, we use Gram Polynomials as activations, which are well-suited for SAR data to capture complex non-linear relationships. Third, we employ a parameter-sharing strategy: each kernel shares parameters per channel, preserving unique features while reducing parameters and FLOPs. Our model achieves 99.09%, 93.01%, and 97.26% accuracy on MSTAR, FUSAR-Ship, and SAR-ACD datasets, respectively. Experiments on MSTAR resized to $1024 \times 1024$ show that compared to VGG16, our model reduces FLOPs by $82.90 \times$ and parameters by $163.78 \times$. This work establishes an efficient solution for edge SAR image recognition.

---

## 论文详细总结（自动生成）

以下是对论文《Light-ResKAN: A Parameter-Sharing Lightweight KAN with Gram Polynomials for Efficient SAR Image Recognition》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：合成孔径雷达（SAR）图像识别在边缘设备（如无人机、卫星）部署时面临巨大挑战。SAR 图像通常具有极大的像素尺寸和复杂的相干斑噪声，而现有的深度学习模型（如 CNN 和 Transformer）往往参数量巨大、计算开销高，且在轻量化过程中容易损失高频散射特征，难以平衡识别精度与计算效率。
*   **研究背景**：Kolmogorov-Arnold Network (KAN) 作为一种新兴架构，通过可学习的激活函数代替传统神经元的固定激活函数，理论上能以极少的参数实现强大的非线性拟合。本文旨在利用 KAN 的优势，针对 SAR 图像特性设计一种超轻量且高精度的识别模型。

### 2. 论文提出的方法论
*   **核心思想**：将残差网络（ResNet）架构与 KAN 卷积相结合，并引入 **Gram 多项式** 作为激活函数，同时通过**通道级参数共享策略**极大地压缩模型规模。
*   **关键技术细节**：
    *   **KAN 卷积改进**：将 ResNet 中的标准卷积替换为 KAN 卷积。不同于传统卷积的线性加权，KAN 卷积在连接处使用可学习的非线性函数。
    *   **Gram 多项式激活**：选用 Gram 多项式作为 KAN 的基函数。相比 Spline 函数，Gram 多项式能更好地捕捉 SAR 图像的复杂散射特征和高动态范围，且具有更好的数值稳定性。其通过三项递归公式定义，并包含可学习的递归系数。
    *   **通道级参数共享**：这是实现轻量化的核心。在同一个通道内，卷积核的所有空间位置共享同一套激活函数参数。这种设计类似于深度可分离卷积的演变，显著降低了参数量和 FLOPs。
    *   **Bottleneck ResKAGN 结构**：设计了包含 $1\times1$ 和 $3\times3$ KAN 卷积的瓶颈结构，进一步优化特征提取效率并缓解梯度消失。

### 3. 实验设计
*   **数据集**：
    1.  **MSTAR**：地面军事目标识别（10类）。
    2.  **FUSAR-Ship**：高分辨率舰船目标识别（10类）。
    3.  **SAR-ACD**：民用飞机目标识别（6类）。
*   **Benchmark 与对比方法**：
    *   **经典 CNN**：AlexNet, VGG16, ResNet18, ResNet50。
    *   **现代/轻量化 CNN**：ConvNeXt, MACN, Cross-HL, ASANet。
    *   **KAN 变体**：SpectralKAN, WaveKAN, 以及基础版的 ResKAGNet。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA RTX 5090** GPU。
*   **训练配置**：采用 AdamW 优化器，学习率为 $5 \times 10^{-4}$，Dropout 为 0.1，Batch Size 为 16。
*   **训练时长**：每个模型在每个数据集上独立训练 **200 个 Epoch**。文中未明确给出总训练小时数，但提供了推理阶段的测试耗时（Test time）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括：
    *   **主干对比实验**：在三个不同领域的 SAR 数据集上验证精度、参数量和 FLOPs。
    *   **消融实验**：验证了 KAN 卷积、Gram 多项式、Bottleneck 结构和参数共享策略各自的贡献。
    *   **抗噪实验**：引入不同强度的 Gamma 分布乘性噪声，验证模型的鲁棒性。
    *   **可视化实验**：通过 t-SNE 展现特征聚类效果。
    *   **扩展性实验**：将图像缩放至 $1024 \times 1024$ 验证大尺寸下的计算优势。
    *   **小样本实验**：测试在 5-shot, 10-shot, 20-shot 下的性能。
*   **充分性评价**：实验设计非常全面且客观。通过跨数据集验证和极端条件（噪声、大尺寸、小样本）测试，充分证明了模型在不同 SAR 应用场景下的泛化能力和效率优势。

### 6. 主要结论与发现
*   **极致的轻量化**：在 $1024 \times 1024$ 的大图输入下，Light-ResKAN 相比 VGG16，**FLOPs 降低了 82.90 倍，参数量减少了 163.78 倍**。
*   **高精度表现**：在 MSTAR、FUSAR-Ship 和 SAR-ACD 上分别达到 **99.09%、93.01% 和 97.26%** 的准确率，均优于或持平于对比的重型模型。
*   **强鲁棒性**：在噪声干扰下，Light-ResKAN 的精度下降幅度远小于传统 CNN，证明 Gram 多项式能有效抑制相干斑噪声。
*   **正则化效应**：参数共享机制在小样本场景下起到了结构正则化的作用，有效防止了过拟合。

### 7. 优点
*   **理论创新与应用结合**：首次将 KAN 架构针对 SAR 图像特性（散射、噪声）进行了深度定制。
*   **效率平衡极佳**：在极低的参数量（约 0.82M）下实现了 SOTA 级别的精度，非常适合边缘计算。
*   **数学解释性强**：利用 Gram 多项式的正交性和函数逼近理论，提升了模型处理非线性散射特征的能力。

### 8. 不足与局限
*   **推理延迟问题**：尽管 FLOPs 极低，但实验显示其**实际测试耗时（Test time）高于部分传统模型**。这是因为 KAN 算子涉及复杂的多项式展开，且目前主流深度学习框架（如 PyTorch）缺乏针对 KAN 的底层 CUDA 核融合优化，导致内存访问成本（MAC）较高。
*   **小样本局限**：虽然优于基础 KAN，但在极极端的小样本情况下，其表现仍略逊于具有强平移不变性先验的传统残差网络（如 ResNet18）。
*   **硬件适配**：目前仍依赖通用 GPU，尚未在真正的嵌入式 FPGA 或 DSP 平台上进行端到端部署验证。

（完）
