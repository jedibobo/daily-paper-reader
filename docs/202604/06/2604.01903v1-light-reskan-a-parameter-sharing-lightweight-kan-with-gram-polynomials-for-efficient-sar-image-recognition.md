---
title: "Light-ResKAN: A Parameter-Sharing Lightweight KAN with Gram Polynomials for Efficient SAR Image Recognition"
title_zh: Light-ResKAN：一种基于 Gram 多项式的参数共享轻量级 KAN，用于高效 SAR 图像识别
authors: "Pan Yi, Weijie Li, Xiaodong Chen, Jiehua Zhang, Li Liu, Yongxiang Liu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01903v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于资源受限边缘设备高效SAR图像识别的轻量化KAN
tldr: 针对合成孔径雷达（SAR）图像识别在边缘设备部署中面临的计算资源受限问题，本文提出了一种轻量化模型 Light-ResKAN。该模型结合了 Kolmogorov-Arnold 网络（KAN）的优势，通过将 ResNet 中的卷积替换为 KAN 卷积，并采用 Gram 多项式作为激活函数，配合参数共享策略，在保持高精度的同时大幅降低了参数量和计算量，为高效 SAR 图像识别提供了新方案。
selection_source: fresh_fetch
motivation: 现有的深度学习模型在处理大尺寸 SAR 图像时，难以在边缘设备上平衡高精度特征提取与低计算资源消耗。
method: 提出 Light-ResKAN 架构，利用 Gram 多项式增强非线性拟合能力，并通过通道级参数共享策略显著减少模型参数和计算量。
result: 在多个 SAR 数据集上取得优异精度，且相比 VGG16，其 FLOPs 降低了 82.90 倍，参数量减少了 163.78 倍。
conclusion: Light-ResKAN 成功实现了 SAR 图像识别的高精度与轻量化，是适用于资源受限边缘设备的有效解决方案。
---

## 摘要
合成孔径雷达（SAR）图像识别对于灾害监测、军事侦察和海洋观测至关重要。然而，庞大的 SAR 图像尺寸阻碍了深度学习在资源受限的边缘设备上的部署，且现有的轻量级模型难以在保持高精度特征提取的同时满足低计算需求。新兴的 Kolmogorov-Arnold 网络（KAN）通过将固定激活函数替换为可学习的激活函数来增强拟合能力，从而减少了参数量和计算量。受 KAN 的启发，我们提出了 Light-ResKAN，以在精度和效率之间取得更好的平衡。首先，Light-ResKAN 通过将卷积替换为 KAN 卷积来改进 ResNet，从而实现对 SAR 图像的自适应特征提取。其次，我们采用 Gram 多项式作为激活函数，这非常适合 SAR 数据，能够捕捉复杂的非线性关系。第三，我们采用了一种参数共享策略：每个卷积核在每个通道上共享参数，在保留独特特征的同时减少了参数量和浮点运算量（FLOPs）。我们的模型在 MSTAR、FUSAR-Ship 和 SAR-ACD 数据集上分别达到了 99.09%、93.01% 和 97.26% 的准确率。在调整为 $1024 \times 1024$ 分辨率的 MSTAR 数据集上的实验表明，与 VGG16 相比，我们的模型将 FLOPs 降低了 82.90 倍，参数量减少了 163.78 倍。这项工作为边缘 SAR 图像识别提供了一种高效的解决方案。

## Abstract
Synthetic Aperture Radar (SAR) image recognition is vital for disaster monitoring, military reconnaissance, and ocean observation. However, large SAR image sizes hinder deep learning deployment on resource-constrained edge devices, and existing lightweight models struggle to balance high-precision feature extraction with low computational requirements. The emerging Kolmogorov-Arnold Network (KAN) enhances fitting by replacing fixed activations with learnable ones, reducing parameters and computation. Inspired by KAN, we propose Light-ResKAN to achieve a better balance between precision and efficiency. First, Light-ResKAN modifies ResNet by replacing convolutions with KAN convolutions, enabling adaptive feature extraction for SAR images. Second, we use Gram Polynomials as activations, which are well-suited for SAR data to capture complex non-linear relationships. Third, we employ a parameter-sharing strategy: each kernel shares parameters per channel, preserving unique features while reducing parameters and FLOPs. Our model achieves 99.09%, 93.01%, and 97.26% accuracy on MSTAR, FUSAR-Ship, and SAR-ACD datasets, respectively. Experiments on MSTAR resized to $1024 \times 1024$ show that compared to VGG16, our model reduces FLOPs by $82.90 \times$ and parameters by $163.78 \times$. This work establishes an efficient solution for edge SAR image recognition.

---

## 论文详细总结（自动生成）

这是一份关于论文《Light-ResKAN: A Parameter-Sharing Lightweight KAN with Gram Polynomials for Efficient SAR Image Recognition》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何在**资源受限的边缘设备**（如无人机、卫星）上实现**高效且高精度的合成孔径雷达（SAR）图像识别**。
*   **研究背景**：SAR 图像具有全天候成像能力，但其独特的成像机制导致图像尺寸巨大、存在严重的相干斑噪声（Speckle Noise）及几何畸变。
*   **动机**：传统的深度学习模型（如 CNN、Transformer）参数量大、计算开销高，且在压缩过程中容易丢失 SAR 图像的高频散射特征。新兴的 Kolmogorov-Arnold 网络（KAN）理论上具有极高的参数效率，但直接应用在 SAR 领域仍存在计算冗余和对复杂散射特征建模不足的问题。

### 2. 论文提出的方法论
论文提出了 **Light-ResKAN** 架构，其核心思想是将残差学习与 KAN 卷积相结合，并通过多项式优化和参数共享实现极致轻量化。
*   **KAN 卷积改进**：将 ResNet 中的标准线性卷积替换为基于 KAN 的非线性卷积。每个卷积核不再是固定权重，而是可学习的激活函数。
*   **Gram 多项式激活函数**：放弃了原始 KAN 中的 B-spline，改用 **Gram 多项式**。其优势在于通过三项递归关系构建正交基，能更有效地捕捉 SAR 数据的复杂非线性关系和纹理特征，且具有更好的数值稳定性。
*   **通道级参数共享策略**：这是实现轻量化的关键。在同一个通道内，卷积核的所有空间位置共享同一套激活函数参数。这类似于深度可分离卷积的思路，但作用于非线性激活函数，极大地降低了参数量和 FLOPs。
*   **Bottleneck ResKAGN 模块**：设计了两种瓶颈结构（ResKAGN 1 和 2），通过 $1\times1$ 卷积进行通道压缩和扩张，进一步优化计算效率。

### 3. 实验设计
*   **数据集**：
    1.  **MSTAR**：地面军事目标识别（10类）。
    2.  **FUSAR-Ship**：高分辨率海面舰船识别（10类）。
    3.  **SAR-ACD**：民用飞机目标识别（6类）。
*   **Benchmark 与对比方法**：
    *   **经典 CNN**：AlexNet, VGG16, ResNet18/50。
    *   **现代/轻量化 CNN**：ConvNeXt, MACN, Cross-HL, ASANet。
    *   **KAN 相关变体**：SpectralKAN, WaveKAN, 以及基础版 ResKAGNet。
*   **评估指标**：准确率（Accuracy）、参数量（Pars.）、模型大小（Size）、浮点运算量（FLOPs）、推理时间（Test time）。

### 4. 资源与算力
*   **硬件平台**：使用单张 **NVIDIA RTX 5090** GPU。
*   **训练配置**：使用 AdamW 优化器，学习率为 $5 \times 10^{-4}$，Dropout 为 0.1，Batch Size 为 16。
*   **训练时长**：每个模型在每个数据集上独立训练 **200 个 Epoch**。文中未明确给出具体的总训练小时数，但提供了每批次的推理延迟（ms）。

### 5. 实验数量与充分性
实验设计非常充分且具有多维度客观性：
*   **多场景验证**：在三个不同类型的 SAR 数据集上进行了测试，验证了泛化性。
*   **消融实验**：分别验证了 KAN 卷积、Gram 多项式、Bottleneck 结构和参数共享策略的有效性，并对比了 5 种不同的基函数（Spline, Chebyshev, Legendre 等）。
*   **鲁棒性测试**：引入了不同强度的 **Gamma 分布乘性噪声** 实验，模拟真实 SAR 环境。
*   **扩展性实验**：将 MSTAR 图像上采样至 $1024 \times 1024$，验证模型在大尺寸图像下的计算优势。
*   **少样本实验**：测试了在 5-shot, 10-shot, 20-shot 条件下的表现。
*   **可视化分析**：通过 t-SNE 聚类图直观展示了特征提取的判别力。

### 6. 主要结论与发现
*   **极致的效率平衡**：在 MSTAR 上达到 99.09% 准确率，参数量仅 0.82M。相比 VGG16，参数量减少了 **163.78 倍**，FLOPs 降低了 **82.90 倍**。
*   **抗噪性强**：在强噪声干扰下，Light-ResKAN 的准确率下降幅度远小于传统 CNN 和其他 KAN 变体，证明了 Gram 多项式在噪声抑制方面的潜力。
*   **大尺寸图像优势**：在 $1024 \times 1024$ 高分辨率输入下，Light-ResKAN 的 FLOPs 仅为 3.87G，而 VGG16 和 ConvNeXt 均超过 320G。
*   **正则化效应**：参数共享机制在少样本场景下起到了结构正则化的作用，缓解了原始 KAN 容易过拟合的问题。

### 7. 优点（亮点）
*   **理论与实践结合**：首次将 KAN 架构针对 SAR 图像特性（相干斑、高动态范围）进行了底层算子级的定制化改进。
*   **参数共享创新**：通过通道内参数共享，解决了 KAN 卷积在空间维度上参数冗余的痛点。
*   **数学严谨性**：选用 Gram 多项式而非经验性的激活函数，利用其正交性和递归特性提升了特征表达的稳定性。

### 8. 不足与局限
*   **推理延迟（Test Time）较高**：尽管 FLOPs 极低，但实际推理速度（ms）慢于部分经典 CNN。论文坦诚指出这是由于当前深度学习框架（如 PyTorch）缺乏对 KAN 算子的底层硬件优化（如 CUDA 内核融合），导致内存访问成本（MAC）较高。
*   **少样本精度非最优**：在极少样本（如 5-shot）下，其精度虽优于基础 KAN，但仍略逊于具有强归纳偏置的传统残差网络（如 ResNet18）。
*   **应用限制**：目前主要验证了分类任务，尚未在目标检测或语义分割等更复杂的 SAR 任务中进行大规模验证。

（完）
