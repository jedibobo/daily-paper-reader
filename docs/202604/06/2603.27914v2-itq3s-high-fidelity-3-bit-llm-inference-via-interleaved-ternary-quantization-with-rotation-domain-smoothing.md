---
title: "ITQ3_S: High-Fidelity 3-bit LLM Inference via Interleaved Ternary Quantization with Rotation-Domain Smoothing"
title_zh: ITQ3_S：通过旋转域平滑的交错三值量化实现高保真 3 比特大语言模型推理
authors: Edward J. Yoon
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27914v2"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对硬件优化的3比特量化及CUDA共享内存优化
tldr: 针对大语言模型（LLM）3位量化中离群值导致的精度损失问题，本文提出ITQ3_S。该方法结合TurboQuant技术，通过快速沃尔什-阿达玛变换（FWHT）在旋转域平滑权重分布，并设计了将逆变换融合进CUDA内核的高效算子。ITQ3_S在保持接近FP16精度的同时，在RTX 5090上实现了超过4位量化1.5倍的吞吐量，为消费级显卡上的高保真LLM推理提供了新方案。
selection_source: fresh_fetch
motivation: 传统3位量化受权重分布重尾和通道间离群值影响导致精度下降，且现有旋转域方法缺乏高效的算子实现。
method: 利用FWHT预旋转权重空间以消除离群值影响，并将256点逆变换融合至CUDA共享内存加载阶段，实现与IQ3_S格式协同设计的统一内核。
result: "在RTX 5090上，ITQ3_S的困惑度指标接近FP16，且推理吞吐量比主流4位量化方案提升了50%以上。"
conclusion: ITQ3_S通过数学上的旋转域平滑与底层内核的深度优化，为消费级硬件上的高保真3位LLM推理提供了实用且高效的解决方案。
---

## 摘要
我们提出了 ITQ3_S（专门的交错三值量化），这是一种针对大语言模型（LLM）的新型 3 比特权重量化格式，它集成了 TurboQuant (TQ)——一种基于快速沃尔什-哈达玛变换（FWHT）的旋转域策略。传统的 3 比特方法由于权重的重尾分布和通道间的离群值而面临精度损失。ITQ3_S 在量化前通过 FWHT 对权重空间进行预旋转，将离群值的能量分散到整个向量中，并诱导出一种适合均匀三值编码的近高斯分布。我们推导出了一个严谨的反量化过程，将 256 点逆 FWHT 融合到 CUDA 共享内存加载阶段，确保重建误差仅受限于三值量化网格，而不会因变换逆转产生额外误差。对于任何权重向量 $\mathbf{w} \in \mathbb{R}^{256}$，重建结果满足 $\|\hat{\mathbf{w}} - \mathbf{w}\|_2 \leq ε_q$，严格小于未利用旋转诱导分布归一化的均匀 3 比特基准模型。TurboQuant 缺乏原生的 CUDA 算子，导致无法直接部署；简单地将 TQ 与现有的权重量化器组合会引入域不匹配误差，这些误差会在层间累积，导致模型质量降至标准 3 比特基准以下。ITQ3_S 通过将 FWHT 旋转和量化算子协同设计为基于 IQ3_S 权重格式的统一流水线解决了这一问题，并将逆变换融合到 CUDA MMQ 算子中。实验表明，在 NVIDIA RTX 5090 (Blackwell) 上，ITQ3_S 实现了与 FP16 相当的困惑度（perplexity），同时通过优化的 DP4A 和 Tensor Core 调度，其吞吐量超过了 4 比特替代方案的 1.5 倍。我们的结果证明 ITQ3_S 是一种在消费级硬件上实现高保真 LLM 部署的实用且具有数学基础的解决方案。

## Abstract
We present ITQ3_S (Interleaved Ternary Quantization -- Specialized), a novel 3-bit weight quantization format for LLMs integrating TurboQuant (TQ), a rotation-domain strategy based on the Fast Walsh-Hadamard Transform (FWHT). Conventional 3-bit methods suffer precision loss from heavy-tailed weight distributions and inter-channel outliers. ITQ3_S pre-rotates the weight space via FWHT before quantization, spreading outlier energy across the vector and inducing a near-Gaussian distribution amenable to uniform ternary coding.   We derive a rigorous dequantization procedure fusing a 256-point Inverse FWHT into the CUDA shared-memory loading stage, ensuring reconstruction error is bounded exclusively by the ternary quantization grid with no additional error from the transform inversion. For any weight vector $\mathbf{w} \in \mathbb{R}^{256}$, the reconstruction satisfies $\|\hat{\mathbf{w}} - \mathbf{w}\|_2 \leq ε_q$, strictly smaller than uniform 3-bit baselines that do not exploit rotation-induced distribution normalization.   TurboQuant lacks a native CUDA kernel, precluding direct deployment; naively composing TQ with existing weight quantizers introduces domain mismatch errors that accumulate across layers, degrading quality below standard 3-bit baselines. ITQ3_S resolves this by co-designing the FWHT rotation and quantization kernel as a unified pipeline grounded in the IQ3_S weight format, with the inverse transform fused into the CUDA MMQ kernel.   Empirically, on the NVIDIA RTX 5090 (Blackwell), ITQ3_S achieves perplexity competitive with FP16 while delivering throughput exceeding 1.5x that of 4-bit alternatives via optimized DP4A and Tensor Core scheduling. Our results establish ITQ3_S as a practical, mathematically grounded solution for high-fidelity LLM deployment on consumer hardware.

---

## 论文详细总结（自动生成）

### ITQ3_S：通过旋转域平滑实现高保真 3 比特大语言模型推理

#### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）参数量巨大，在消费级 GPU 上部署面临严重的显存瓶颈。虽然量化技术可以压缩模型，但 **3 比特（3-bit）量化**通常被视为 LLM 逻辑能力的“崩溃点”，会导致严重的困惑度（Perplexity）上升和推理能力丧失。
*   **研究动机**：
    *   **权重分布不均**：Transformer 权重存在重尾分布和极端的离群值（Outliers），导致均匀量化在覆盖动态范围时浪费了大量精度。
    *   **现有方案缺陷**：现有的旋转量化方法（如 QuIP#）虽然理论效果好，但缺乏高效的 CUDA 内核实现，或者在权重和 KV 缓存之间存在域不匹配，导致误差累积。
    *   **硬件适配**：缺乏针对最新消费级硬件（如 RTX 5090）深度优化的 3 比特量化算子。

#### 2. 方法论
*   **核心思想**：利用**快速沃尔什-阿达玛变换（FWHT）**在量化前对权重进行确定性旋转，将离群值的能量均匀分散到整个向量空间，使权重分布趋于近高斯分布，从而适配均匀的三值编码。
*   **关键技术细节**：
    *   **旋转域平滑**：在离线量化阶段，对每 256 个权重组成的块执行 FWHT。根据中心极限定理，变换后的条目趋向于 $N(0, \sigma^2)$ 分布，显著降低了离群值的影响。
    *   **交错三值编码（Interleaved Ternary Coding）**：将权重限制为 $\{-α, 0, +α\}$，通过交错存储方式将 3 比特值打包进 32 比特字中，优化了 DP4A 指令的吞吐量。
    *   **TurboQuant 融合内核**：这是本文的核心工程创新。在 CUDA 的 MMQ（矩阵-矩阵量化乘法）内核中，将 **256 点逆 FWHT** 直接融合进共享内存（Shared Memory）加载阶段。
    *   **数学保证**：证明了重建误差仅受三值量化网格限制，逆变换过程本身不引入额外误差（等距映射性质）。

#### 3. 实验设计
*   **模型范围**：LLaMA-3 (8B, 70B), Mistral 7B v0.3, Qwen2.5 32B。
*   **数据集**：WikiText-2 和 C4 困惑度评估。
*   **对比基准（Benchmarks）**：
    *   **精度对比**：FP16（基准）、Q8_0、Q4_K_M、IQ4_XS、IQ3_S（llama.cpp 原生 3-bit）、QuIP#-3bit。
    *   **性能对比**：Token 生成速度（Decode）和预填充速度（Prefill）。

#### 4. 资源与算力
*   **硬件环境**：主要在 **NVIDIA RTX 5090 (Blackwell 架构)** 上进行测试，利用了其 32GB GDDR7 显存和 192KB 共享内存的特性。
*   **训练/量化耗时**：由于 ITQ3_S 属于**训练后量化（PTQ）**方案，且 FWHT 是确定性变换，量化过程极快（算法复杂度 $O(n \log n)$）。文中未提及具体的总计算时长，但强调了其离线处理的简便性。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 70B 不同规模的主流模型，并在两个标准数据集上进行了测试。
*   **消融实验**：针对 FWHT 的块大小（32 到 512）进行了详细消融，验证了 256 点变换是质量与开销的最佳平衡点。
*   **客观性**：对比了目前社区最先进的量化格式（如 QuIP# 和 IQ3_S），实验设计较为全面，涵盖了精度、显存占用和推理速度三个核心维度。

#### 6. 主要结论与发现
*   **精度突破**：ITQ3_S 将 3 比特量化与 FP16 之间的困惑度差距缩小了 **57%**（相比于 IQ3_S 基准）。
*   **性能优异**：在 RTX 5090 上，ITQ3_S 的吞吐量超过了 4 比特方案的 **1.5 倍**，且预填充速度因交错布局优化而显著提升。
*   **单卡运行 70B**：ITQ3_S 使得 70B 规模的模型（约 27.3 GiB）能够完整装入单张 32GB 显存的消费级显卡，并保留足够的 KV 缓存空间（约 16K 上下文）。

#### 7. 优点
*   **理论与工程结合**：不仅有严谨的分布平滑理论证明，还提供了深度优化的 CUDA 内核实现。
*   **确定性**：不同于 QuIP# 需要存储随机种子，ITQ3_S 使用固定 FWHT，简化了部署流程。
*   **硬件感知设计**：充分利用了 Blackwell 架构的共享内存和位操作特性，实现了极低的计算开销（IFWHT 仅增加 2.1% 的计算开销）。

#### 8. 不足与局限
*   **激活量化缺失**：目前仅针对权重量化，未涉及激活量化，在长文本推理时带宽压力仍存。
*   **非 2 的幂次限制**：对于隐藏层维度不是 256 倍数的模型，需要额外的填充（Padding）策略，可能影响极小部分模型的效率。
*   **训练感知缺失**：目前仅限于 PTQ，如果结合量化感知训练（QAT），精度可能进一步提升，但本文未探讨。

（完）
