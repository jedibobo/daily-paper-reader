---
title: "TwinMixing: A Shuffle-Aware Feature Interaction Model for Multi-Task Segmentation"
title_zh: TwinMixing：一种用于多任务分割的混洗感知特征交互模型
authors: "Minh-Khoi Do, Huy Che, Dinh-Duy Phan, Duc-Khai Lam, Duc-Lung Vu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28233v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 在低成本硬件上实现自动驾驶分割的实时性能
tldr: 针对自动驾驶中可行驶区域和车道线分割任务，本文提出轻量化多任务模型TwinMixing。该模型采用共享编码器和任务特定解码器架构，通过高效金字塔混合（EPM）模块增强多尺度特征提取，并利用双分支上采样（DBU）块实现精细的特征重建。在BDD100K数据集上，该模型以极低的参数量（0.43M）和计算量实现了优异的分割精度，在保持实时性的同时显著提升了低成本硬件上的感知性能。
selection_source: fresh_fetch
motivation: 旨在解决自动驾驶中多任务分割模型在低成本硬件上难以兼顾高精度与实时性的挑战。
method: 提出包含高效金字塔混合模块（EPM）的共享编码器和具有双分支上采样结构（DBU）的任务特定解码器。
result: "在BDD100K数据集上，Base版本仅需0.43M参数即可达到92.0%的可行驶区域mIoU和32.3%的车道线IoU。"
conclusion: TwinMixing通过紧凑的模块化设计，在计算效率和分割精度之间取得了极佳平衡，非常适合嵌入式实时部署。
---

## 摘要
准确且高效的感知对于自动驾驶至关重要，其中可行驶区域分割和车道线分割等分割任务为运动规划与控制提供了关键线索。然而，在低成本硬件上保持实时性能的同时实现高分割精度仍然是一个具有挑战性的问题。为了解决这一问题，我们提出了 TwinMixing，这是一种专门为可行驶区域和车道线分割设计的轻量级多任务分割模型。所提出的网络采用共享编码器和任务特定解码器的架构，实现了特征共享与任务专业化。在编码器内部，我们提出了一个高效金字塔混合（EPM）模块，该模块通过结合分组卷积、深度可分离空洞卷积和通道混洗（channel shuffle）操作来增强多尺度特征提取，在最小化计算成本的同时有效地扩大了感受野。每个解码器采用双分支上采样（DBU）块，该块由基于可学习转置卷积的精细分支和基于无参数双线性插值的粗粒度分支组成，实现了细节丰富且空间一致的特征重建。在 BDD100K 数据集上的广泛实验验证了 TwinMixing 在 tiny、base 和 large 三种配置下的有效性。其中，base 配置在精度和计算效率之间取得了最佳平衡，仅凭 0.43M 参数和 3.95 GFLOPs 的计算量，在可行驶区域分割上达到了 92.0% 的 mIoU，在车道线分割上达到了 32.3% 的 IoU。此外，如图 1 所示，TwinMixing 在相同任务上始终优于现有的分割模型。得益于其紧凑且模块化的设计，TwinMixing 在自动驾驶和嵌入式感知系统的实时部署中展现出强大的潜力。源代码：https://github.com/Jun0se7en/TwinMixing。

## Abstract
Accurate and efficient perception is essential for autonomous driving, where segmentation tasks such as drivable-area and lane segmentation provide critical cues for motion planning and control. However, achieving high segmentation accuracy while maintaining real-time performance on low-cost hardware remains a challenging problem. To address this issue, we introduce TwinMixing, a lightweight multi-task segmentation model designed explicitly for drivable-area and lane segmentation. The proposed network features a shared encoder and task-specific decoders, enabling both feature sharing and task specialization. Within the encoder, we propose an Efficient Pyramid Mixing (EPM) module that enhances multi-scale feature extraction through a combination of grouped convolutions, depthwise dilated convolutions and channel shuffle operations, effectively expanding the receptive field while minimizing computational cost. Each decoder adopts a Dual-Branch Upsampling (DBU) Block composed of a learnable transposed convolution-based Fine detailed branch and a parameter-free bilinear interpolation-based Coarse grained branch, achieving detailed yet spatially consistent feature reconstruction. Extensive experiments on the BDD100K dataset validate the effectiveness of TwinMixing across three configurations - tiny, base, and large. Among them, the base configuration achieves the best trade-off between accuracy and computational efficiency, reaching 92.0% mIoU for drivable-area segmentation and 32.3% IoU for lane segmentation with only 0.43M parameters and 3.95 GFLOPs. Moreover, TwinMixing consistently outperforms existing segmentation models on the same tasks, as illustrated in Fig. 1. Thanks to its compact and modular design, TwinMixing demonstrates strong potential for real-time deployment in autonomous driving and embedded perception systems. The source code: https://github.com/Jun0se7en/TwinMixing.

---

## 论文详细总结（自动生成）

这是一份关于论文《TwinMixing: A Shuffle-Aware Feature Interaction Model for Multi-Task Segmentation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的嵌入式硬件（如自动驾驶车载系统）上，同时实现高精度的“可行驶区域”和“车道线”分割，并保持实时运行性能。
*   **研究背景**：传统的语义分割模型（如DeepLab、SegFormer）计算量巨大，难以在低成本硬件上实时运行。现有的轻量化模型虽然使用了空洞卷积扩大感受野，但往往缺乏有效的通道间交互，且在特征重建（上采样）阶段容易产生棋盘效应或边界模糊。

### 2. 方法论：核心思想与关键技术
TwinMixing 采用“共享编码器 + 任务特定解码器”的架构，核心创新点包括：
*   **高效金字塔混合模块 (EPM)**：
    *   **核心思想**：遵循“压缩-拆分-变换-合并”原则。
    *   **技术细节**：首先通过 1x1 分组卷积降低维度，然后拆分为多个并行分支，每个分支使用不同扩张率（Dilation Rate）的**深度可分离空洞卷积 (DD-Conv)** 来捕获多尺度上下文。
    *   **混洗机制**：引入 **Channel Shuffle（通道混洗）** 操作，打破分组卷积带来的通道间信息孤岛，增强特征交互。
*   **双分支上采样块 (DBU)**：
    *   **精细分支 (Fine Branch)**：使用可学习的转置卷积（Deconvolution）并结合跳跃连接（Skip Connection），负责恢复丢失的几何细节和边界。
    *   **粗粒度分支 (Coarse Branch)**：使用无参数的双线性插值，提供空间平滑性和结构稳定性，避免转置卷积产生的棋盘伪影。
    *   **融合**：通过逐元素相加融合两路特征。
*   **损失函数**：采用 Focal Loss（解决类别不平衡）和 Tversky Loss（优化细长车道线结构）的混合损失。

### 3. 实验设计
*   **数据集**：主要在 **BDD100K**（包含10万张图像，涵盖多种天气和场景）上进行训练和验证。
*   **泛化性测试**：在 Cityscapes 和 ACDC 数据集上进行了跨域验证。
*   **对比方法 (Benchmarks)**：
    *   通用分割模型：DeepLabV3+、SegFormer。
    *   自动驾驶多任务模型：YOLOP、TwinLiteNet、TwinLiteNet+、DFFM、IALaneNet。
*   **评价指标**：mIoU（可行驶区域）、Accuracy/IoU（车道线）、Params（参数量）、FLOPs（计算量）、FPS（帧率）。

### 4. 资源与算力消耗
*   **训练硬件**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：Base 版本每轮（epoch）训练时间约为 **1070.2秒**（由于计算图较复杂，训练耗时比前作 TwinLiteNet 略长）。
*   **部署测试**：在 NVIDIA Jetson 系列（AGX Orin, Xavier, Orin Nano, TX2）上进行了推理延迟测试。
*   **模型规模**：
    *   Tiny: 0.10M 参数, 1.08 GFLOPs。
    *   Base: 0.43M 参数, 3.95 GFLOPs。
    *   Large: 1.50M 参数, 14.25 GFLOPs。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的定量对比（Table 3）和定性可视化（Fig 9, 10）。
*   **消融实验**：针对 DBU 模块的有效性、EPM 模块的扩张率敏感性、分组卷积的组数（Group Size）以及多任务 vs 单任务的性能差异进行了多组实验。
*   **部署实验**：包含了 FP16 和 INT8（量化感知训练 QAT）的量化实验，验证了模型在低精度下的稳定性。
*   **评估客观性**：实验覆盖了白天、黑夜、雨雪、隧道等多种极端工况，对比基准涵盖了近年来的主流轻量化模型，实验设计较为客观、公平。

### 6. 主要结论
*   **性能领先**：TwinMixing Base 在 BDD100K 上达到了 92.4% mIoU（可行驶区域）和 33.2% IoU（车道线），在参数量仅为 0.43M 的情况下，性能优于许多参数量大得多的模型（如 IALaneNet）。
*   **效率极高**：Tiny 版本在 RTX 4090 上可达 1302 FPS，在嵌入式设备 Orin Nano 上也能保持约 27ms 的低延迟。
*   **多任务增益**：多任务模型相比两个独立的单任务模型，在参数量减半的情况下，精度几乎持平甚至略有提升。

### 7. 优点与亮点
*   **极致轻量化**：参数量控制在 1M 以下，非常适合内存受限的嵌入式设备。
*   **混洗感知设计**：通过 Channel Shuffle 解决了深度可分离卷积特征交互弱的问题，提升了特征表达能力。
*   **解码器稳定性**：DBU 模块结合了可学习上采样和线性插值，兼顾了边界锐度和空间平滑度。
*   **部署友好**：提供了从 Tiny 到 Large 的灵活配置，且支持 INT8 量化。

### 8. 不足与局限
*   **极端环境挑战**：在雨天、雪天和隧道等极低能见度或强反光场景下，车道线分割的 IoU 仍有明显下降（如隧道场景降至 28.9%）。
*   **超参数依赖**：EPM 模块中的扩张率和分组数等超参数仍需人工调优，未引入自动搜索（NAS）机制。
*   **训练开销**：虽然推理很快，但由于多分支结构和频繁的内存访问（Shuffle 操作），其训练时的计算图开销比简单的卷积网络更高。
*   **任务局限**：目前仅专注于分割，未整合目标检测（如车辆、行人检测）等其他自动驾驶关键任务。

（完）
