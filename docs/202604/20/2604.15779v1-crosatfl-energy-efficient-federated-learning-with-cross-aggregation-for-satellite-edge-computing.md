---
title: "CroSatFL: Energy-Efficient Federated Learning with Cross-Aggregation for Satellite Edge Computing"
title_zh: CroSatFL：面向卫星边缘计算的具有交叉聚合功能的高能效联邦学习
authors: "Nan Yang, Bahman Javadi, Rodrigo Neves Calheiros, David Boland, Philip Leong"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15779v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 用于卫星边缘计算的在轨分层联邦学习
tldr: 针对低轨卫星边缘计算中能源受限、连接动态及硬件异构等挑战，本文提出CroSatFL框架。该框架通过将联邦学习迭代过程完全置于轨道上，利用激光星间链路进行聚合，仅在初始和结束阶段与地面站通信。结合StarMask聚类、Skip-One落后者缓解及随机k交叉聚合机制，在保证模型精度的同时，显著降低了地面通信开销和系统总能耗，实现了高效可持续的在轨训练。
selection_source: fresh_fetch
motivation: 传统卫星联邦学习过度依赖高能耗且带宽受限的地面站链路，且难以应对卫星硬件异构和动态拓扑带来的能耗挑战。
method: 提出一种分层在轨联邦学习框架，利用激光星间链路进行通信，并集成StarMask聚类、Skip-One落后者跳过及随机k交叉聚合机制。
result: 实验表明，该方法将地面站通信次数减少了两个数量级，地面传输能耗降低约6倍，同时保持了优异的分类精度和收敛速度。
conclusion: CroSatFL通过优化在轨资源调度与通信拓扑，为能源受限的卫星边缘计算提供了一种高效、低能耗的联邦学习解决方案。
---

## 摘要
低地球轨道（LEO）巨型星座将云边连续体扩展到了太空，从而实现了卫星边缘计算。然而，由于动态的星间连接、异构的星载计算硬件以及严格的功率预算，该环境下的联邦学习（FL）受到根本性的能源限制。我们提出了 CroSatFL，这是一种可持续的在轨分层联邦学习框架，它在保持真实 LEO 动态环境下强大训练性能的同时，降低了计算和通信的端到端能耗。CroSatFL 通过在轨道上执行所有本地训练和中间聚合，使地面站（GS）脱离了迭代循环，仅需两个地面站通信阶段：一个用于初始化，另一个用于最终模型收集。这大幅减少了对带宽受限且高能耗的地面站链路的重复使用，并将迭代交换转移到了激光星间链路（LISL）。CroSatFL 集成了三种能耗感知机制：StarMask 构建 LISL 可行的集群，使数据量与异构 CPU/GPU 能力相匹配；Skip-One 通过在每个集群中最多跳过一个慢速客户端来缓解瞬时掉队者问题，从而在保持长期公平性的同时降低轮次能耗和延迟；random-k 交叉聚合实现了轻量级的拓扑感知跨集群混合，且不会延长轮次时长。通过在真实的 Walker-Delta 星座上使用端到端能耗模型，我们证明了相对于以地面站为中心和在轨的基准方案，CroSatFL 将地面站通信次数减少了两个数量级以上，地面站传输能耗降低了约 6 倍，同时实现了具有竞争力的准确率和更快的收敛速度。

## Abstract
Low Earth Orbit (LEO) mega-constellations extend the cloud-to-edge continuum into space, enabling satellite edge computing. However, Federated Learning (FL) in this environment is fundamentally energy-constrained due to dynamic inter-satellite connectivity, heterogeneous onboard computing hardware, and strict power budgets. We propose CroSatFL, a sustainable on-orbit hierarchical FL framework that reduces end-to-end energy across computation and communication while maintaining strong training performance under realistic LEO dynamics. CroSatFL keeps the ground station (GS) off the iterative loop by performing all local training and intermediate aggregations on orbit, requiring only two GS communication phases: one for initialization and one for final model collection. This sharply reduces repeated use of bandwidth-limited and energy-expensive GS links and shifts iterative exchanges to laser inter-satellite links (LISLs). CroSatFL integrates three energy-aware mechanisms: StarMask forms LISL-feasible clusters that align data volume with heterogeneous CPU/GPU capability, Skip-One mitigates transient stragglers by skipping at most one slow client per cluster to lower round energy and latency while preserving long-term fairness, and random-k cross-aggregation enables lightweight topology-aware cross-cluster mixing without extending round duration. Using an end-to-end energy model with a realistic Walker-Delta constellation, we show that CroSatFL reduces GS communication count by over two orders of magnitude and GS transmission energy by about 6x relative to GS-centric and on-orbit baselines, while achieving competitive accuracy and faster convergence.

---

## 论文详细总结（自动生成）

这是一份关于论文《CroSatFL: Energy-Efficient Federated Learning with Cross-Aggregation for Satellite Edge Computing》的结构化总结：

### 1. 核心问题与研究背景
随着低轨（LEO）巨型星座的发展，卫星边缘计算成为可能。然而，在空间环境中部署联邦学习（FL）面临三大挑战：
*   **能源受限：** 卫星电力预算紧张，且传统的地面站（GS）通信能耗极高。
*   **连接动态：** 卫星间链路（LISL）随轨道运动不断变化，且地面站可见窗口短促且不连续。
*   **硬件异构：** 星载计算资源差异大（如CPU与GPU混杂），导致严重的“掉队者”问题，拖慢同步训练进度。
**研究动机：** 现有的卫星FL方案大多依赖地面站进行全局聚合，导致昂贵的通信开销和严重的同步延迟。本文旨在设计一种完全在轨、能效高且能应对硬件异构的层次化联邦学习框架。

### 2. 方法论：CroSatFL 核心架构
CroSatFL 的核心思想是**“地面站脱离迭代循环”**，仅在初始化和最终模型收集时使用地面站，中间过程全部通过激光星间链路（LISL）在轨完成。其包含三个关键技术：

*   **StarMask（基于强化学习的聚类）：**
    *   利用强化学习（RL）和动作掩码（Action Masking）技术，根据卫星的数据量、硬件类型、能耗和星间链路可行性进行自动聚类。
    *   **目标：** 最小化集群内的计算时间差异和总能耗，同时平衡各集群间的数据份额。
*   **Skip-One（掉队者缓解机制）：**
    *   在每个集群内部，主卫星根据实时效用评分（考虑延迟减少、能耗节省和长期公平性），允许每轮最多跳过一个瞬时掉队卫星。
    *   **目的：** 避免单个慢速节点拖累整个集群的同步速度，同时通过冷却计数器确保所有卫星长期参与。
*   **Random-k 交叉聚合（跨集群混合）：**
    *   集群主卫星利用间歇性的跨轨道面 LISL，随机选择 $k$ 个可达的邻居集群进行模型交换和加权平均。
    *   **效果：** 实现了类似“流言算法（Gossip）”的共识传播，无需全局同步即可维持模型的一致性。

### 3. 实验设计
*   **场景设置：** 模拟了一个包含 720 颗卫星的 Walker-Delta 星座（Starlink 规模），其中 50% 为 CPU 卫星，50% 为 GPU 卫星。
*   **数据集：** MNIST、CIFAR-10、EuroSAT（遥感影像数据集），涵盖 IID（独立同分布）和 Non-IID（非独立同分布）设置。
*   **基准方法（Benchmarks）：**
    *   **FedSyn：** 标准同步联邦学习。
    *   **FedLEO：** 针对 LEO 优化的地面中心化方案。
    *   **FELLO：** 基于聚类和边缘选择的在轨方案。
    *   **FedSCS：** 能耗感知的客户端选择方案。
    *   **FedOrbit：** 采用低精度算术的能效优化方案。

### 4. 资源与算力
*   **硬件环境：** 实验在配备 **NVIDIA RTX 5090 GPU** 的服务器上运行。
*   **软件框架：** 使用 **Flower** 联邦学习框架结合 PyTorch 实现。
*   **数据来源：** 卫星计算能力参数参考了 2023 年 **Spiral Blue Space Edge One** 真实的在轨测试轨迹数据。
*   **训练时长：** 论文展示了长达 40 轮的训练过程，并在实验结果中以“天”为单位对比了端到端训练时间。

### 5. 实验数量与充分性
*   **实验规模：** 论文在三个不同复杂度的图像数据集上进行了测试，并对比了 5 种主流基准算法。
*   **维度覆盖：** 实验不仅评估了模型准确率（收敛性），还深入分析了总能耗（kJ）、训练时间（天）、地面站通信次数、等待时间等关键指标。
*   **消融/异构分析：** 专门针对不同硬件比例（全CPU、混合、全GPU）进行了对比实验，验证了 Skip-One 机制的鲁棒性。
*   **评价：** 实验设计较为充分，使用了真实的卫星硬件参数和轨道动力学模拟，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **极高的通信效率：** CroSatFL 将地面站通信次数减少了 **2 个数量级以上**（从 3200 次降至 18 次）。
*   **显著的节能效果：** 地面传输能耗降低了约 **6 倍**，总能耗在达到相同精度时远低于基准方法。
*   **训练加速：** 通过减少对地面站窗口的依赖和缓解掉队者问题，端到端训练时间从数十天缩短至数天（例如在 EuroSAT 上从 20+ 天缩短至约 5 天）。
*   **精度保持：** 在 IID 和 Non-IID 环境下，其准确率均能匹配甚至超过现有的最强基准。

### 7. 优点与亮点
*   **完全在轨化：** 彻底摆脱了地面站作为同步瓶颈的限制，非常适合未来超大规模星座。
*   **硬件感知：** 充分考虑了 CPU/GPU 异构对 FL 的影响，StarMask 和 Skip-One 的结合有效解决了系统层面的瓶颈。
*   **真实性高：** 结合了真实的星载硬件测试数据和 Walker-Delta 轨道模型，而非纯理论假设。

### 8. 不足与局限
*   **客户端规模限制：** 虽然模拟了 720 颗卫星，但实际参与 FL 训练的客户端仅选择了 40 颗，对于更大规模并发参与的情况（如数百颗卫星同时训练）探讨不足。
*   **RL 训练开销：** 论文未详细说明 StarMask 强化学习模型本身的训练成本及其在星载资源受限环境下的部署开销。
*   **容错性探讨：** 尽管有 Skip-One 机制，但对于卫星彻底失效或链路长期中断等极端空间环境下的容错性缺乏深入讨论。

（完）
