---
title: "AdaVFM: Adaptive Vision Foundation Models for Edge Intelligence via LLM-Guided Execution"
title_zh: AdaVFM：通过 LLM 引导执行实现边缘智能的自适应视觉基础模型
authors: "Yiwei Zhao, Yi Zheng, Huapeng Su, Jieyu Lin, Stefano Ambrogio, Cijo Jose, Michaël Ramamonjisoa, Patrick Labatut, Barbara De Salvo, Chiao Liu, Phillip B. Gibbons, Ziyun Li"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15622v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 集成NAS以实现边缘设备上的高效端侧推理
tldr: 针对边缘设备部署视觉基础模型（VFM）面临的功耗和延迟挑战，本文提出AdaVFM框架。该框架通过神经架构搜索（NAS）在VFM中嵌入轻量级子网，并利用云端多模态大模型（LLM）作为智能体，根据场景上下文和任务复杂度动态调整运行时计算量。AdaVFM在保持高精度的同时显著降低了计算开销，为边缘智能提供了高效的自适应推理方案。
selection_source: fresh_fetch
motivation: 视觉基础模型在边缘端部署受限于功耗和延迟，且模型规模缩减对性能的影响随任务复杂度而异，需要动态适配策略。
method: 通过神经架构搜索构建支持子网切换的VFM骨干网络，并引入云端多模态大模型作为上下文感知代理来引导运行时的模型执行。
result: "在零样本分类和开放词汇分割任务中，AdaVFM在同等精度下最高可减少77.9%的计算量，或在同等规模下显著提升准确率。"
conclusion: AdaVFM证明了通过LLM引导的自适应执行能够有效平衡视觉基础模型的推理效率与任务精度，是实现边缘智能的关键技术。
---

## 摘要
语言对齐的视觉基础模型（VFMs）为全天候上下文 AI 提供了通用的视觉理解能力，但其在边缘设备上的部署受到严格的延迟和功耗限制。我们提出了 AdaVFM，这是一个用于语言对齐 VFM 高效端侧推理的自适应框架，可根据场景上下文和任务复杂度动态调整计算量。我们的核心见解是，在视觉应用中，模型规模缩减对性能的影响具有任务依赖性，这促使了运行时自适应执行策略的产生。AdaVFM 将神经架构搜索（NAS）集成到语言对齐的 VFM 主干网络中，以实现运行时的轻量级子网执行。部署在云端的多模态大语言模型（LLM）通过上下文感知代理实现运行时控制。这种协同作用使得模型能够在各种条件下进行高效适配，同时保持强大的准确性。在零样本分类和开放词汇分割上的广泛实验表明，AdaVFM 实现了最先进的准确率-效率权衡，在 IN1K 上的 acc@1 提升高达 7.9%，在 ADE20K 上的 mIoU 提升高达 5.2%（相比同等规模的最优 VFM 模型）。对于准确率相近的模型，AdaVFM 进一步将平均 FLOPs 降低了高达 77.9%。

## Abstract
Language-aligned vision foundation models (VFMs) enable versatile visual understanding for always-on contextual AI, but their deployment on edge devices is hindered by strict latency and power constraints. We present AdaVFM, an adaptive framework for efficient on-device inference of language-aligned VFMs that dynamically adjusts computation based on scene context and task complexity. Our key insight is that the effect of model size reduction on performance is task-dependent in vision applications, motivating a runtime-adaptive execution strategy. AdaVFM integrates neural architecture search (NAS) into the language-aligned VFM backbone to enable lightweight subnet execution during runtime. A multimodal large language model (LLM) deployed on the cloud enables runtime control with a context-aware agent. This synergy allows efficient model adaptation under diverse conditions while maintaining strong accuracy. Extensive experiments on zero-shot classification and open-vocabulary segmentation demonstrate that AdaVFM achieves state-of-the-art accuracy-efficiency trade-offs, surpassing prior baselines by up to $7.9\%$ in acc@1 on IN1K and $5.2\%$ mIoU on ADE20K over the best models of comparable VFM sizes. For models with similar accuracy, AdaVFM further reduces average FLOPs by up to $77.9\%$.

---

## 论文详细总结（自动生成）

这是一份关于论文《AdaVFM: Adaptive Vision Foundation Models for Edge Intelligence via LLM-Guided Execution》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在计算资源、内存和功耗受限的边缘设备（如智能眼镜、手机）上，高效部署强大的语言对齐视觉基础模型（VFM）。
*   **研究动机**：
    *   **性能与任务的非线性关系**：作者发现模型缩减对性能的影响具有“任务依赖性”。简单任务（如识别宠物）在小模型上表现良好，而复杂任务（如150类语义分割）则需要大模型。
    *   **边缘部署瓶颈**：现有的VFM（如CLIP、DINOv2）体积巨大，直接剪枝会导致精度大幅下降，且无法适应动态变化的场景需求。
    *   **全天候AI需求**：智能穿戴设备需要实时感知，但持续将数据流传回云端在功耗和延迟上不可行。

### 2. 方法论
AdaVFM 提出了一个云端协作的自适应执行框架，核心包含以下三个关键技术：
*   **NAS 集成的自适应骨干网络**：
    *   采用 **Once-for-All (OFA) NAS** 策略，构建了一个基于 **ConvNeXt** 的超网（Supernet）。
    *   **两阶段训练**：第一阶段从 DINOv2 ViT-G/14 蒸馏知识到 ConvNeXt 超网；第二阶段进行视觉-文本对齐（CLIP 风格训练）。
    *   该超网支持在运行时动态提取不同深度和宽度的子网（Subnet），以匹配不同的计算预算。
*   **LLM 引导的运行时代理（Cloud Agent）**：
    *   **场景感知**：云端多模态 LLM 以低频率（如每隔几分钟）接收图像，识别当前场景（如“驾驶中”或“办公室内”）。
    *   **语义过滤**：LLM 根据场景过滤掉不可能出现的类别（如办公室内过滤掉“轮船”），减少开放词汇任务的噪声。
    *   **子网选择算法**：根据场景复杂度和硬件模式参数（$\alpha$），算法自动选择能满足精度要求的最小子网。
*   **算法流程**：LLM 代理生成文本嵌入和执行方案 -> 边缘端 Vision Encoder 切换至对应子网 -> 实时进行视觉-文本对比推理。

### 3. 实验设计
*   **数据集**：
    *   **零样本分类**：ImageNet-1K (IN1K), Food-101, DTD, Oxford Pets。
    *   **开放词汇分割**：ADE20K。
    *   **预训练数据**：LVD-142M（蒸馏用），MetaCLIP 数据集（对齐用）。
*   **Benchmark 与对比方法**：
    *   对比了 CLIP, OpenCLIP, Waffle, LaFTer, NoLA, 以及最先进的 DINO.txt。
    *   针对不同 LLM（Llama 3.2/4, GPT-5, Gemini 2.5）作为代理的效果进行了对比。
*   **硬件评估**：在真实的 **ARM Ethos-U55 NPU**（7nm 工艺测试芯片）上测量了实际延迟和能耗。

### 4. 资源与算力
*   **训练细节**：
    *   骨干网络蒸馏：625k 次迭代，Batch Size 4096。
    *   视觉-文本对齐：100k 次迭代，Batch Size 4096。
*   **算力说明**：文中**未明确说明**具体使用的 GPU 型号和数量，但从其训练迭代次数和 Batch Size 来看，需要大规模 GPU 集群（如 H100 或 A100 阵列）支持。
*   **推理算力**：边缘端运行在 560 MHz 的 NPU 上，提供 >128 GOP/s 的算力。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从分类到分割的多种视觉任务，并进行了详尽的消融实验。
*   **充分性**：
    *   **多维度评估**：不仅报告了 FLOPs 和参数量，还报告了真实硬件上的延迟（ms）和能耗（mJ）。
    *   **客观性**：通过 WordNet 层次结构定义任务难度，并对比了多种主流 LLM 代理的效果，实验设计较为严谨、客观。
    *   **公平性**：在对比基准模型时，考虑了模型规模的差异，并展示了 AdaVFM 在不同精度/效率权衡点上的优越性。

### 6. 主要结论与发现
*   **性能提升**：在 IN1K 上比同类模型准确率高出 **7.9%**，在 ADE20K 上 mIoU 提升 **5.2%**。
*   **效率极高**：在达到相同精度的情况下，AdaVFM 平均可减少 **77.9%** 的 FLOPs。
*   **自适应价值**：LLM 引导的语义过滤对小模型提升尤为显著，证明了“云端智慧引导边缘执行”模式的有效性。
*   **硬件友好**：ConvNeXt 结构在边缘 NPU 上的效率远高于传统的 ViT 结构。

### 7. 优点（亮点）
*   **协同设计**：巧妙地结合了 NAS 的结构灵活性和 LLM 的语义理解能力，解决了 VFM 难以落地的痛点。
*   **动态适配**：不再使用单一模型应对所有场景，而是根据任务难度“量体裁衣”。
*   **真实落地参考**：在真实 NPU 芯片上进行测试，数据对工业界具有极高的参考价值。
*   **低频云端调用**：通过低频调用 LLM 降低了对网络和云端成本的依赖，同时保证了端侧的实时性。

### 8. 不足与局限
*   **云端依赖性**：虽然是低频调用，但系统仍需网络连接。在完全断网环境下，自适应能力会受限。
*   **场景切换延迟**：当用户快速切换场景时，云端 LLM 的推理和网络回传可能存在滞后，导致子网切换不及时。
*   **训练成本**：超网的蒸馏和对齐训练过程极其耗费算力，普通研究者难以复现。
*   **LLM 偏差风险**：如果云端 LLM 对场景理解错误（如将办公室误判为街道），会导致错误的子网选择和类别过滤，从而引发严重的推理错误。

（完）
