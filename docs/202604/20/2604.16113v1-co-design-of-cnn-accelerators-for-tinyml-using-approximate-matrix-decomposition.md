---
title: Co-Design of CNN Accelerators for TinyML using Approximate Matrix Decomposition
title_zh: 基于近似矩阵分解的 TinyML CNN 加速器协同设计
authors: "José Juan Hernández Morales, Georgios Mentzos, Frank Hannig, Konstantinos Balaskas, Georgios Zervakis, Jörg Henkel, Jürgen Teich"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16113v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 资源受限下TinyML的CNN加速器协同设计
tldr: 针对TinyML在资源受限及数据隐私场景下的推理延迟问题，本文提出一种基于近似矩阵分解的CNN加速器协同设计框架。该框架利用遗传算法探索矩阵分解方案，将复杂的乘法运算转化为硬件友好的移位和加法，且无需对预训练模型进行重新训练或微调。实验表明，该方法在FPGA平台上能显著提升推理速度并保持较高的模型精度。
selection_source: fresh_fetch
motivation: TinyML应用面临严苛的资源约束和数据隐私限制，传统的延迟优化方法往往依赖训练数据或导致显著的精度损失。
method: 采用遗传算法驱动的近似矩阵分解技术，将CNN中的乘法器替换为无乘法器的移位加法结构，并针对FPGA进行硬件协同优化。
result: "在TinyML基准测试中，该框架相比传统的脉动阵列加速器实现了平均33%的延迟改进，且平均精度损失仅为1.3%。"
conclusion: 本研究为受限环境下的TinyML部署提供了一种无需重训的高效优化方案，平衡了推理性能、资源消耗与模型精度。
---

## 摘要
微型机器学习（TinyML）领域代表了在严格资源限制下向本地和设备端推理的范式转变。TinyML 的主要目标是在严格的资源、能耗和延迟约束下，将智能集成到微型、低成本设备中。然而，这些设备极度受限的资源特性可能导致推理执行时间增加，这在延迟敏感型应用中可能产生不利影响。同时，TinyML 应用通常涉及敏感数据。因此，当数据不可获取、受版权保护或具有敏感性时，依赖训练样本的延迟优化方法是不可行的，这凸显了对无需访问训练数据集且能直接应用于预训练模型的优化方法的迫切需求。采用移位和加法等更具硬件效率的操作来替代昂贵的乘法，已被提出作为降低推理延迟的有效方法。然而，训练后二的幂（Po2）方法较为稀缺，且在许多情况下会导致不可接受的精度损失。在这项工作中，我们提出了一个框架，将近似矩阵分解应用于给定的 CNN，以便在严格约束下优化硬件实现，且无需任何重训练或微调步骤。该遗传算法驱动的框架探索了不同的矩阵分解以及由此产生的针对 FPGA 目标的无乘法器 CNN 加速器设计。对不同 TinyML 基准测试的全面评估表明，该框架在生成满足严格精度和资源约束的延迟优化实现方面具有显著成效，与典型的基于脉动阵列的 FPGA 加速器相比，实现了平均 33% 的延迟改进，而平均精度损失仅为 1.3%。

## Abstract
The paradigm shift towards local and on-device inference under stringent resource constraints is represented by the tiny machine learning (TinyML) domain. The primary goal of \gls{tml} is to integrate intelligence into tiny, low-cost devices under strict resource, energy, and latency constraints. However, the ultra-resource-constrained nature of these devices can lead to increased inference execution time, which can be detrimental in latency critical applications. At the same time, TinyML applications are often associated with sensitive data. As such, latency optimization approaches that rely on training samples are infeasible when such data is unavailable, proprietary, or sensitive, highlighting a pressing need for optimization approaches that do not require access to the training dataset and can be applied directly to pre-trained models. Replacing costly multiplications with more hardware-efficient operations, such as shifts and additions, has been proposed as an effective method for reducing inference latency. However, post-training power-of-two (Po2) approaches are scarce and, in many cases, lead to unacceptable accuracy loss.   In this work, we propose a framework that applies approximate matrix decomposition to a given CNN in order to optimize hardware implementations subject to strict constraints and without any need of re-training or fine-tuning steps. The genetic algorithm-driven framework explores different matrix decompositions and resulting multiplier-less CNN accelerator designs for FPGA targets. A comprehensive evaluation of different TinyML benchmarks demonstrates our framework's efficacy in generating latency-optimized implementations that satisfy strict accuracy and resource constraints, achieving an average 33% latency improvement with an average accuracy loss of 1.3% compared to typical systolic array-based FPGA accelerators.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对 TinyML 场景的 CNN 加速器协同设计框架，通过近似矩阵分解实现无乘法器推理。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：TinyML 设备（如微控制器和小型 FPGA）资源极度受限，传统的卷积神经网络（CNN）推理中大量的乘法运算（MAC）导致高延迟和高能耗。
*   **研究动机**：
    *   **硬件瓶颈**：乘法器在硬件资源（如 FPGA 的 LUT）消耗上远高于移位和加法操作。
    *   **数据隐私与限制**：现有的优化方法（如量化感知训练）通常需要访问原始训练数据集进行重训练或微调，但在许多实际应用中，数据是敏感、私有或不可获取的。
    *   **硬件不敏感**：现有的“二的幂”（Po2）量化方法往往只关注精度，忽略了底层硬件的实际资源约束。
*   **整体含义**：本文旨在开发一种**无需重训练**、**硬件感知**且**无乘法器**的协同设计框架，在保证精度的前提下，最大限度降低 TinyML 设备的推理延迟。

### 2. 方法论
*   **核心思想**：利用**近似权重矩阵分解（WMD）**将 CNN 的权重矩阵转化为仅包含“二的幂”元素的稀疏矩阵组合，从而将复杂的乘法转换为硬件友好的移位和加法。
*   **关键技术细节**：
    1.  **近似矩阵分解（WMD）**：将权重矩阵 $W$ 分解为一系列稀疏矩阵 $F$ 的乘积。每个 $F$ 矩阵的非零元素被限制为 $2^Z$ 形式，且每行仅包含 $E$ 个非零元素。
    2.  **可编程无乘法器加速器**：设计了一种基于脉动阵列（Systolic Array）的硬件架构，其处理单元（PE）由移位器和加法树组成，完全取代了传统的 MAC 单元。
    3.  **协同设计框架**：采用**遗传算法（NSGA-II）**在多维设计空间中搜索最优参数（如分解层数 $P$、移位范围 $Z$、非零元素个数 $E$ 等）。
    4.  **硬件感知建模**：框架内置了针对 FPGA 的资源（LUT、FF、BRAM）和延迟分析模型，确保搜索到的方案符合硬件约束。

### 3. 实验设计
*   **数据集与场景**：
    *   **CIFAR-10**：用于图像分类（ResNet 模型）。
    *   **VWW (Visual Wake Words)**：用于视觉唤醒（MobileNetV1 模型）。
    *   **Speech Commands**：用于语音命令识别（DS-CNN 模型）。
*   **Benchmark**：采用了行业标准的 **MLPerfTiny** 基准测试套件。
*   **对比方法**：
    *   传统的基于 8 位 MAC 单元的脉动阵列加速器。
    *   不同位宽（4-8 bit）的训练后量化（PTQ）方法。
    *   最先进的训练后 Po2 加速方案（如 ShiftCNN）。
*   **目标平台**：Xilinx Artix-7 XC7A100T FPGA。

### 4. 资源与算力
*   **训练算力**：论文未明确说明用于模型预训练的 GPU 型号，因为该方法属于**训练后（Post-training）**优化，核心计算在于遗传算法的搜索过程。
*   **搜索算力**：遗传算法配置为 250 个种群，运行 20 代。
*   **硬件实现**：使用 Vivado 2023.1 进行综合、布局布线和功耗估计。实验中详细记录了 FPGA 的 LUT、FF、BRAM 使用率及频率（约 110-125 MHz）。

### 5. 实验数量与充分性
*   **实验规模**：针对三种不同类型的 CNN 架构（ResNet, MobileNet, DS-CNN）进行了完整的 Pareto 优化搜索。
*   **充分性评价**：实验设计较为充分。作者不仅对比了精度和延迟，还深入分析了吞吐量（GOPS）、功耗（mW）和能效（GOPS/W）。通过与不同位宽的 PTQ 进行对比，证明了该方法在相同精度损失下具有更好的硬件性能。
*   **客观性**：使用了标准的 MLPerfTiny 模型和公开数据集，对比基准（Baseline）设置合理，实验结果具有说服力。

### 6. 主要结论与发现
*   **性能提升**：与传统的 8 位脉动阵列相比，该框架实现的加速器在平均精度损失仅为 **1.3%** 的情况下，实现了平均 **33%** 的延迟改进。
*   **吞吐量优势**：由于移位加法单元比乘法器更节省资源，相同芯片面积下可以部署更多的计算单元，从而使平均吞吐量提升了 **41%**。
*   **能效比**：在 DS-CNN 模型上，该方法相比 8 位基准方案实现了 **18.67%** 的能量节省。
*   **超越 SOTA**：在相同精度约束下，其吞吐量比 ShiftCNN 高出约 **2.4 倍**。

### 7. 优点
*   **无需数据重训**：极大地保护了数据隐私，并降低了模型部署的门槛和计算成本。
*   **硬件与算法协同**：不是孤立地优化算法，而是将硬件资源约束直接引入搜索目标，确保了方案的可落地性。
*   **灵活性高**：通过遗传算法生成的 Pareto 前沿，用户可以根据具体应用的延迟或精度偏好选择最合适的配置。

### 8. 不足与局限
*   **存储开销**：虽然节省了计算资源（LUT），但由于采用了深度流水线和复杂的 PE 结构，**BRAM 和寄存器（FF）的消耗有所增加**。
*   **搜索空间限制**：为了控制搜索复杂度，作者将部分参数设为全局共享（Hard parameters），这可能限制了每一层进行极致优化的潜力。
*   **平台局限性**：目前主要针对 FPGA 平台进行了验证，在 ASIC 或通用 MCU 上的表现（尤其是移位操作在不同架构下的效率）尚需进一步探讨。

（完）
