---
title: "MemExplorer: Navigating the Heterogeneous Memory Design Space for Agentic Inference NPUs"
title_zh: MemExplorer：探索面向智能体推理 NPU 的异构存储设计空间
authors: "Haoran Wu, Zeyu Cao, Yao Lai, Binglei Lou, Jiayi Nie, Can Xiao, Timi Adeniran, Przemyslaw Forys, Kauser Johar, Catriona Wright, Junyi Liu, Kai Shi, Nicholas D. Lane, Rika Antonova, Jianyi Cheng, Timothy Jones, Aaron Zhao, Robert Mullins"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16007v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对NPU的内存带宽优化
tldr: 针对Agentic LLM负载中Prefill和Decode阶段对内存的不同需求，本文提出MemExplorer。这是一个异构NPU内存系统综合器，能统一建模多种存储技术并自动优化NPU架构参数。实验显示，该工具设计的系统相比H100在Prefill和Decode场景下分别实现了3.23倍和2.72倍的能效提升。
selection_source: fresh_fetch
motivation: 智能体LLM负载的预填充和解码阶段对内存需求各异，且现有异构存储设计空间巨大且缺乏有效探索工具。
method: 提出MemExplorer综合器，通过统一抽象建模多种存储技术，自动确定异构内存系统与NPU设计参数，以平衡多设备系统的吞吐量与功耗。
result: 在Agentic负载下，MemExplorer设计的系统比H100在预填充阶段能效高3.23倍，在解码阶段能效高2.72倍。
conclusion: MemExplorer能有效导航复杂的异构内存设计空间，为下一代针对Agentic推理的NPU提供高效的架构方案。
---

## 摘要
新兴的智能体大语言模型（LLM）工作负载正推动对存储容量和带宽需求的快速增长，且推理的不同阶段（如预填充 prefill 和解码 decode）提出了截然不同的要求。业界正通过将异构加速器组合成单一的互连系统来应对这一挑战，例如 NVIDIA 的 Vera Rubin 平台，其中每个设备都拥有各自的存储架构。这种异构性因可用存储技术的不断扩展而变得更加复杂：高密度片上 SRAM、HBM、LPDDR、GDDR 以及高带宽闪存（HBF）等新兴选项，每种技术在容量、带宽和功耗之间提供不同的权衡。为下一代推理加速器确定合适的存储架构需要探索庞大且快速演进的设计空间，而工作负载特性、NPU 设计维度与存储系统设计之间的相互作用在很大程度上仍未得到充分研究。为了应对这一挑战，我们提出了 MemExplorer，这是一种用于异构 NPU 系统的存储系统综合器。MemExplorer 提供了一个统一的抽象，用于对跨不同层级（如片上和片外）的多样化存储技术进行建模，并自动确定高效的异构存储系统以及 NPU 设计选择（如矩阵引擎大小），以平衡多设备 NPU 系统中预填充和解码设备之间的吞吐量和功耗。实验结果表明，在针对智能体工作负载的相同功耗预算下，MemExplorer 在仅预填充设置下的能效比基准 NPU 高出 2.3 倍，比 H100 高出 3.23 倍。在解码设置的等效性能目标下，其能效分别比基准 NPU 和 H100 高出 1.93 倍和 2.72 倍。

## Abstract
Emerging agentic LLM workloads are driving rapidly growing demand on both memory capacity and bandwidth, with different phases of inference (e.g., prefill and decode) imposing distinct requirements. Industry is responding by composing heterogeneous accelerators into single interconnected systems, as exemplified by NVIDIA's Vera Rubin platform, where each device brings its own memory architecture.   This heterogeneity is further compounded by a widening landscape of available memory technologies: high-density on-chip SRAM, HBM, LPDDR, GDDR, and emerging options such as high-bandwidth flash (HBF), each offering different capacity, bandwidth, and power trade-offs.   Identifying the right memory architecture for next-generation inference accelerators requires navigating a vast and rapidly evolving design space, in which the interplay between workload characteristics, NPU design dimensions, and memory system design remains largely underexplored.   To address this challenge, we present MemExplorer, a new memory system synthesizer for heterogeneous NPU systems. MemExplorer provides a unified abstraction for modeling diverse memory technologies across different hierarchy levels (e.g., on-chip and off-chip) and automatically determines an efficient heterogeneous memory system together with NPU design choices (e.g., matrix engine size) to balance throughput and power between prefilling and decoding devices in a multi-device NPU system.   Experimental results show that, under the same power budget for agentic workloads, MemExplorer achieves up to 2.3x higher energy efficiency than the baseline NPU and 3.23x higher than H100 in the prefill-only setting. Under equivalent performance targets in the decode setting, it further delivers up to 1.93x and 2.72x higher power efficiency over the baseline NPU and H100, respectively.

---

## 论文详细总结（自动生成）

### 论文总结：MemExplorer — 探索面向智能体推理 NPU 的异构存储设计空间

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：新兴的“智能体”（Agentic）LLM 工作负载（如自动编程、网页操作智能体）具有极长的上下文（数百万 token）和不可预测的交互模式。这导致内存需求激增（仅 KV Cache 就可能超过 500GB），且推理的两个阶段需求迥异：**预填充（Prefill）**阶段是计算密集型，需要极高带宽；**解码（Decode）**阶段是访存密集型，需要海量容量。
*   **核心问题**：现有的单一存储架构（如仅 HBM）难以同时满足带宽、容量和功耗的平衡。虽然工业界开始转向异构系统（如 NVIDIA 的 Rubin 平台），但如何系统性地在庞大的异构存储技术（SRAM、HBM、LPDDR、HBF 等）和 NPU 架构之间进行协同设计，仍是一个未被充分探索的难题。

#### 2. 论文提出的方法论
*   **核心思想**：提出 **MemExplorer**，一个端到端的存储系统综合框架，通过统一的抽象层建模多种存储技术，并利用多目标优化算法在复杂的硬件-软件空间中搜索最优配置。
*   **关键技术细节**：
    *   **统一存储抽象**：将不同技术（HBM3E/4、LPDDR5X/6、GDDR6/7、3D 堆叠 SRAM、高带宽闪存 HBF）统一建模为延迟、容量、带宽、物理岸线（Shoreline）约束及功耗系数。
    *   **分层分析模型**：建立多级存储模型，支持双缓冲（Double Buffering）传输建模，精确计算数据在各级存储间移动的停顿时间和有效带宽。
    *   **协同设计空间**：涵盖计算配置（PE 阵列维度、向量长度）、存储配置（各级存储类型与堆叠数）、量化精度（MX 格式）以及软件策略（数据流策略、片上存储优先级、带宽分配优先级）。
    *   **搜索算法**：采用**多目标贝叶斯优化（MOBO）**，利用高斯过程（GP）作为代理模型，并通过期望超体积改进（EHVI）采集函数，在有限的评估预算内（如 100 步）快速逼近帕累托前沿（Pareto Frontier）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **智能体任务**：BFCL（函数调用评测）、OSWorld（真实计算机环境操作）。
    *   **模型**：Llama-3.3-70B、Qwen3-32B、LLaDA-8B（扩散语言模型）、Qwen3.5-397B（稀疏 MoE 模型）。
*   **Benchmark 与对比方法**：
    *   **基准硬件**：NVIDIA A100 (80GB)、H100 (80GB)。
    *   **基准 NPU**：基于 PLENA 架构的固定存储配置 NPU。
    *   **对比算法**：在设计空间探索中对比了随机搜索、NSGA-II 遗传算法和 MO-TPE。

#### 4. 资源与算力
*   **实验平台**：基准测试在 Ubuntu 22.04 系统上运行，使用 CUDA 12.8 和 vLLM 0.10 框架。
*   **算力资源**：使用了 4 台 A100 和 4 台 H100 GPU 进行性能对标。
*   **仿真开销**：MemExplorer 的分析模型运行极快（单次评估约 24ms），相比周期精确的模拟器（PLENA emulator，单次约 4.15 分钟）提速显著，从而支持大规模搜索。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 100 步的 DSE 搜索实验，并展示了超体积收敛曲线。
    *   针对软件策略、存储层次结构、量化精度分别进行了详细的**消融实验**。
    *   涵盖了自回归、扩散模型、MoE 等多种主流架构。
*   **充分性评价**：实验设计较为充分，通过跨验证（Cross-validation）确保了分析模型与周期精确模拟器之间的一致性（误差约 10%），且对比了多种优化算法，结果具有客观性。

#### 6. 论文的主要结论与发现
*   **阶段差异化需求**：预填充阶段最受益于大容量、高带宽的 **3D 堆叠片上 SRAM**；解码阶段则更依赖于**大容量、低带宽的片外存储**（如 LPDDR 或 HBF），因为容量增加能显著提升 Batch Size 从而提高吞吐。
*   **能效提升**：在相同功耗预算（700W）下，MemExplorer 优化的系统在预填充阶段的能效比 H100 高 **3.23 倍**；在解码阶段，能效比 H100 高 **2.72 倍**。
*   **新兴技术潜力**：高带宽闪存（HBF）在处理超大规模 MoE 模型时表现出色，能以较低成本存储不常访问的专家权重。

#### 7. 优点
*   **前瞻性**：首次针对智能体工作负载的长上下文特性，系统性地探讨了异构存储的协同设计。
*   **建模全面**：不仅考虑了带宽和容量，还引入了物理岸线（Shoreline）和热设计功耗（TDP）等实际硬件约束。
*   **高效搜索**：将复杂的架构设计转化为多目标优化问题，并证明了贝叶斯优化在这一领域的优越性。

#### 8. 不足与局限
*   **通信建模缺失**：目前尚未建模多设备系统中的芯片间（C2C）互连开销，这在实际的大规模分布式推理中至关重要。
*   **精度一致性假设**：目前搜索空间中预填充和解码阶段使用相同的量化精度，未探索跨阶段的混合精度优化。
*   **软件栈简化**：对编译器优化和算子融合的建模较为抽象，可能无法完全捕捉实际软件运行时的细微开销。

（完）
