---
title: Exploration of Energy and Throughput Tradeoffs for Dataflow Networks
title_zh: 数据流网络中能耗与吞吐量权衡的探索
authors: "Abrarul Karim, Joachim Falk, Jürgen Teich"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29367v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 数据流网络的能量和吞吐量权衡
tldr: 本研究探讨了数据流网络中动态功耗管理带来的能耗与吞吐量权衡问题。针对硬件执行器在空闲休眠时产生的延迟，论文提出了线性规划（LP）模型确定最大吞吐量，以及混合整数线性规划（MILP）模型在满足吞吐量要求下最小化能耗。此外，提出“Hop and Skip”多目标探索策略以高效寻找帕累托最优解，并通过实验验证了其在优化能效与性能平衡方面的有效性。
selection_source: fresh_fetch
motivation: 动态功耗管理虽能节能，但其开关延迟会降低数据流网络的吞吐量，需解决性能与能耗之间的冲突。
method: 提出了LP和MILP数学模型进行调度优化，并开发了名为“Hop and Skip”的策略来探索能耗与吞吐量的帕累托前沿。
result: 实验证明该方法能显著减少设计空间探索时间，并能准确找到不同功耗策略下的最优性能权衡点。
conclusion: 该研究为自供电数据流网络提供了一套系统化的优化框架，能在满足吞吐量约束的同时实现显著的节能。
---

## 摘要
在数据流网络中引入时钟门控和电源门控等动态电源管理策略，已被证明在空闲时段应用时能显著节省能耗。然而，由于关断和唤醒延迟，这些策略也可能导致吞吐量下降。对于需要保证吞吐量的信号处理系统而言，这种吞吐量下降可能尤为不利。作为解决方案，本文首先提出了一种线性规划公式，用于为给定的所谓“自供电数据流网络”寻找周期性最大吞吐量调度，在该网络中，硬件实现的执行器（actors）在未被使能触发时允许进入睡眠状态。根据允许哪些执行器关断电源，可以获得吞吐量与节能之间的权衡。作为第二项贡献，我们提出了一种混合整数线性规划公式，通过识别在空闲阶段允许关断和不允许关断的执行器集合，来确定满足给定吞吐量且使每个周期总能耗最小化的周期性调度。最后，作为第三项贡献，我们提出了一种名为“Hop and Skip”的多目标设计空间探索策略，以高效地探索能耗与吞吐量解决方案的帕累托前沿。对一组现有基准测试和随机生成图的实验评估表明，与暴力搜索相比，该策略显著缩短了探索时间。最后，本文详细阐述了一个真实案例研究，并报告了相关数据流网络在以下三种情况下的可实现节能和吞吐量：(a) 所有执行器始终处于活动状态，(b) 所有执行器均为自供电，以及 (c) 由所提出的设计空间探索策略找到的所有最优能耗与吞吐量权衡点。

## Abstract
The introduction of dynamic power management strategies such as clock gating and power gating in dataflow networks has been shown to provide significant energy savings when applied during idle times. However, these strategies can also degrade throughput due to shutdown and wake-up delays. Such throughput degradations might be particularly detrimental to signal processing systems that require a guaranteed throughput. As a solution, this paper first contributes a linear-program formulation for finding a periodic maximal-throughput schedule of a given so-called self-powering dataflow network where actors, realized in hardware, are allowed to go to sleep whenever not being enabled to fire. Depending on which actors are allowed to power down, tradeoffs between throughput and energy savings can be obtained. As a second contribution, we propose a mixed-integer-linear-program formulation to determine a periodic schedule that satisfies a given throughput while minimizing the overall energy per period by identifying a respective set of actors that is allowed to power down in phases of idleness and which rather not. Finally, as a third contribution, we propose a multi-objective design-space exploration strategy called "Hop and Skip" to efficiently explore the Pareto front of energy and throughput solutions. Experimental evaluations on a set of existing benchmarks and randomly generated graphs witness significant exploration time reductions over a brute-force sweep. Finally, a real-world case study is elaborated, and we report on achievable energy savings and throughputs of the related dataflow network where (a) all actors are always-active, (b) all actors are self-powered, and (c) all optimal energy and throughput tradeoff points as found by the proposed design-space exploration strategy.

---

## 论文详细总结（自动生成）

以下是对论文《Exploration of Energy and Throughput Tradeoffs for Dataflow Networks》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证数据流网络（Dataflow Networks）吞吐量的同时，通过动态功耗管理（如时钟门控、电源门控）最大化节能。
*   **研究动机**：现代IoT和信号处理应用对能效要求极高。虽然让硬件执行器（Actors）在空闲时进入睡眠模式（自供电模式）可以节能，但唤醒和关断延迟会降低系统吞吐量。研究者需要一种方法来决定哪些执行器应保持常开（Always-Active），哪些应进入自供电（Self-Powered）模式，以在性能和功耗之间取得最优平衡。

### 2. 方法论
论文提出了三个核心技术贡献：
*   **最大吞吐量分析（LP模型）**：建立线性规划（LP）模型，在给定执行器配置（即已知哪些执行器自供电）的情况下，计算系统能达到的最小周期 $P$（即最大吞吐量）。该模型考虑了数据依赖、执行时间以及自供电带来的额外延迟。
*   **最小能耗调度（MILP模型）**：建立混合整数线性规划（MILP）模型。在给定目标吞吐量（周期 $P$）的前提下，将执行器的运行模式设为二值决策变量，通过求解模型找到使总能耗最低的执行器配置方案。
*   **“Hop and Skip”（H&S）探索策略**：这是一种高效的多目标设计空间探索（DSE）算法。
    *   **Hop（跳跃）**：利用 MILP 在当前周期下寻找能耗最低的配置。
    *   **Skip（跳过）**：利用 LP 计算该配置下能达到的极限最小周期，从而直接跳过中间冗余的周期搜索点。
    *   该策略旨在比暴力搜索更快地逼近帕累托前沿（Pareto Front）。

### 3. 实验设计
*   **数据集与场景**：
    *   **真实案例研究**：声学回声消除（AEC）网络，使用 28nm 工艺下的真实硬件参数（功耗、延迟、执行时间）。
    *   **SDF3 基准测试集**：包括 H.263 编码器、调制解调器（Modem）、MP3 解码器、采样率转换器（Samplerate）和卫星接收机（Satellite）等。
    *   **随机图实验**：使用 SDF3 工具生成了 100 个随机数据流图，以验证算法的普适性。
*   **对比方法（Benchmark）**：
    *   **Decision Variable Sweep**：对所有可能的执行器配置进行穷举（$2^{|A|}$ 复杂度）。
    *   **Period Sweep**：在最小和最大周期之间进行步进式穷举搜索。
    *   **对比指标**：探索时间（CPU Time）和超体积比（Hypervolume Ratio，用于衡量帕累托前沿的质量）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Core i7-14700K** 处理器的机器上运行。
*   **算力说明**：该研究属于 EDA（电子设计自动化）和数学优化范畴，主要依赖 CPU 进行 LP 和 MILP 求解，未涉及 GPU 训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 组经典的真实应用基准测试和 100 组随机生成的复杂图。
*   **充分性评价**：实验设计非常充分。通过与穷举搜索对比，证明了 H&S 策略在 98% 以上的情况下能准确找到真实的帕累托最优解。使用了超体积（Hypervolume）这一标准多目标优化指标，客观地评价了不同策略的搜索质量。

### 6. 主要结论与发现
*   **节能效果显著**：在 AEC 案例中，混合模式（部分执行器自供电）在不损失任何吞吐量的情况下可节能 20%；若允许 17% 的吞吐量损失，节能可达 55%。
*   **探索效率极高**：提出的 H&S 策略比传统的周期扫描（Period Sweep）快 **29.95 倍**（平均值），在某些大型网络中加速比高达 **2300 倍**。
*   **帕累托前沿准确性**：H&S 策略在极短的时间内找到了与穷举搜索几乎一致的最优解集。

### 7. 优点
*   **理论与实践结合**：不仅有严谨的数学模型（LP/MILP），还结合了 28nm 芯片设计的真实物理参数。
*   **算法创新**：H&S 策略巧妙地利用了 LP 和 MILP 的对偶特性，大幅缩减了高维设计空间的搜索时间。
*   **灵活性**：支持对单个执行器进行细粒度的功耗模式定制。

### 8. 不足与局限
*   **模型限制**：目前主要针对有权图（Marked Graphs）。虽然可以将同步数据流图（SDF）展开为有权图，但对于极其复杂的动态数据流图，展开后的节点数爆炸可能会增加求解器的负担。
*   **延迟假设**：实验假设唤醒和关断延迟是固定的，但在某些复杂的 SoC 环境中，这些延迟可能受电源网络波动等动态因素影响。
*   **整数周期限制**：在某些对比实验中假设周期为整数，这可能忽略了某些理论上的有理数最优解（尽管 H&S 策略本身支持有理数）。

（完）
