---
title: A Multi-Sensor Fusion Parking Barrier System with Lightweight Vision on Edge
title_zh: 一种基于边缘端轻量化视觉的多传感器融合停车道闸系统
authors: "Yuwen Zhu, Feiyang Qi, Zhengzhe Xiang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29126v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对边缘设备的轻量化视觉与结构剪枝
tldr: 本研究针对智能停车场景中检测精度、边缘实时性与低功耗的平衡难题，提出一种基于深度学习与多传感器融合的智能车位锁系统。系统采用树莓派5作为边缘节点，通过剪枝优化YOLOv3-tiny模型实现轻量化视觉检测，并结合红外、惯性传感器构建非对称融合状态机。该方案在保证高精度的同时，显著降低了功耗，为边缘侧端到端停车管理提供了高效、稳健的工程化解决方案。
selection_source: fresh_fetch
motivation: 旨在解决智能停车场景中高精度检测、边缘侧实时响应与低功耗运行难以同时兼顾的挑战。
method: 在边缘侧部署剪枝优化的YOLOv3-tiny模型，并结合红外与惯性传感器设计了“红外触发-视觉确认-惯性回退”的多模态融合决策机制。
result: "实验显示模型mAP最高达98.2%，推理延迟低于850ms，且系统平均功耗从4.02W降至1.02W，节能约74%。"
conclusion: 该系统通过轻量化视觉与多传感器融合技术，在边缘侧成功实现了高精度、低功耗且具备强鲁棒性的智能停车管理。
---

## 摘要
针对在停车场景中同时满足检测精度、边缘实时性能、低功耗运行以及端到端业务联动的挑战，本文提出了一种基于深度学习和多传感器融合的智能停车道闸系统。该系统采用由边缘感知节点层、云端业务服务层和前端管理应用层组成的三层协同架构。在边缘端，树莓派 5（Raspberry Pi 5）集成了摄像头、红外测距传感器、MPU6050 姿态传感器和 LoRa 模块，用于车位状态感知和本地决策。在算法层面，对 YOLOv3-tiny 进行了针对单类检测的结构化剪枝，将模型权重压缩至约 33 MB。在决策层面，设计了一种非对称红外-视觉-惯性融合状态机，采用“红外触发-视觉确认-惯性回退”机制，以增强在夜间、遮挡和冲击干扰下的鲁棒性。实验结果表明，经过 5000 多次训练迭代后，mAP@0.5 达到 96.5%-98.2%。在树莓派 5 上，416x416 分辨率下的单帧推理延迟为 600-850 ms，满足空闲（5 s）和占用（10 s）的轮询需求。平均功耗从 4.02 W 降至 1.02 W，实现了约 74% 的节能。联调测试进一步验证了该方案在检测精度、响应及时性、能效和工程可部署性方面的优势。

## Abstract
To address the challenges of simultaneously satisfying detection accuracy, edge real-time performance, low-power operation, and end-to-end business linkage in parking scenarios, this paper proposes an intelligent parking barrier system based on deep learning and multi-sensor fusion. The system adopts a three-layer collaborative architecture comprising an edge sensing node layer, a cloud business service layer, and a front-end management application layer. On the edge side, a Raspberry Pi 5 integrates a camera, infrared ranging sensor, MPU6050 attitude sensor, and LoRa module for parking-state sensing and local decision-making. At the algorithmic level, YOLOv3-tiny is structurally pruned for single-class detection, compressing model weights to approximately 33 MB. At the decision level, an asymmetric infrared-vision-inertial fusion state machine is designed, employing an "infrared trigger - visual confirmation - inertial fallback" mechanism to enhance robustness under nighttime, occlusion, and impact disturbances. Experimental results show that after over 5000 training iterations, mAP@0.5 reaches 96.5%-98.2%. On Raspberry Pi 5, single-frame inference latency at 416x416 resolution is 600-850 ms, meeting polling requirements of 5 s (idle) and 10 s (occupied). Average power consumption decreases from 4.02 W to 1.02 W, achieving approximately 74% energy savings. Joint debugging tests further validate the solution's advantages in detection accuracy, response timeliness, energy efficiency, and engineering deployability.

---

## 论文详细总结（自动生成）

以下是对论文《A Multi-Sensor Fusion Parking Barrier System with Lightweight Vision on Edge》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在计算资源受限的边缘设备上，同时实现高精度的车位占用检测、实时的边缘响应、极低的运行功耗以及端到端的业务系统联动。
*   **研究背景**：随着城市停车压力增大，传统的人工巡检、地磁感应或单一光电传感器方案存在效率低、维度单一、误报率高且难以与计费系统深度集成的问题。
*   **动机**：纯视觉方案在夜间或遮挡下不稳定且功耗高，而纯红外方案存在非单调死区。因此，需要一种结合轻量化深度学习与多模态传感器融合的系统级解决方案。

### 2. 方法论：核心思想与技术细节
*   **三层协同架构**：
    *   **边缘感知层**：以树莓派 5 为核心，集成摄像头、红外测距、MPU6050 惯性传感器和 LoRa 通信模块。
    *   **云端服务层**：基于 Spring Boot 开发，负责多车位管理、计费、设备心跳监控及告警处理。
    *   **前端应用层**：基于 Vue3+Vite，实现车位地图渲染、状态可视化及分级告警显示。
*   **轻量化视觉算法**：
    *   针对停车场景仅需识别“车辆”单类目标的特点，对 **YOLOv3-tiny** 进行结构化剪枝。
    *   将检测头输出通道数缩减至 18，模型权重压缩至约 **33 MB**，利用 OpenCV DNN 库在 CPU 上进行 NEON 指令集优化推理。
*   **非对称融合状态机**：
    *   **红外触发**：低功耗红外传感器持续监测，当距离 < 80cm 时唤醒视觉。
    *   **视觉确认**：YOLO 模型进行目标确认，输出置信度。
    *   **惯性回退**：若视觉受限但 MPU6050 检测到冲击或倾斜（>25°），则触发异常告警或保守判定。
*   **能效策略**：采用“事件触发 + 周期校验”机制，空闲时 5s 轮询，占用时 10s 轮询，大幅降低无效计算。

### 3. 实验设计
*   **数据集**：使用 `car_dataset_1class` 公开数据集，涵盖轿车、SUV 等多种车型及不同光照、角度背景。
*   **对比基准（Benchmark）**：
    *   算法对比：标准 YOLOv3-tiny 与剪枝后的单类模型。
    *   模态对比：纯视觉方案、视觉+红外触发方案、视觉+红外+惯性全融合方案。
*   **评估指标**：mAP@0.5、推理延迟（ms）、系统功耗（W）、状态判定准确率。

### 4. 资源与算力
*   **训练端**：文中未明确说明训练所用的具体 GPU 型号，但提到进行了 5000 次以上的迭代训练。
*   **部署端（边缘）**：使用 **Raspberry Pi 5**（2.4 GHz Cortex-A76 CPU），无独立 GPU，完全依赖 CPU 进行推理。
*   **模型体积**：压缩后仅为 33 MB，适合嵌入式内存环境。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 5000+ 次训练迭代，给出了详细的 Loss 下降曲线和 mAP 增长曲线。
    *   进行了**消融实验**，量化了红外触发机制和惯性回退机制对功耗和鲁棒性的具体贡献。
    *   进行了**端到端联调测试**，验证了从感知到云端显示的全链路稳定性。
*   **充分性评价**：实验设计较为全面，不仅关注算法指标（mAP 达 96.5%-98.2%），还深入探讨了工程实践中的功耗（降低 74%）和延迟（600-850ms），具有较强的客观性和工程参考价值。

### 6. 主要结论与发现
*   **性能平衡**：剪枝后的 YOLOv3-tiny 在树莓派 5 上实现了精度与速度的平衡，单帧推理 600-850ms 足以满足停车场景的准实时需求。
*   **鲁棒性提升**：多传感器融合机制有效解决了夜间弱光、近距离红外死区及设备被撞击导致的误判问题。
*   **节能显著**：通过分级功耗状态切换，系统平均功耗从 4.02 W 降至 1.02 W，节能约 74%，极大延长了设备寿命并降低了散热压力。

### 7. 优点
*   **工程落地性强**：不只是纯算法研究，而是提供了一套包含硬件、算法、通信、后端及前端的完整闭环方案。
*   **针对性优化**：通过单类目标剪枝和非对称融合逻辑，精准解决了边缘端算力不足与环境干扰的痛点。
*   **低功耗设计**：分级唤醒机制是该系统在实际部署中的核心亮点。

### 8. 不足与局限
*   **环境覆盖有限**：实验主要基于现有数据集，对于极端天气（如暴雪、大雾）或极高密度的遮挡场景缺乏长期的实地测试数据。
*   **硬件依赖**：虽然实现了轻量化，但 600ms+ 的延迟在树莓派 5 上仍显吃力，若推广至更低成本的 MCU（如 ESP32）可能无法运行。
*   **通信瓶颈**：LoRa 虽然传输距离远，但在大规模集群部署时，其带宽限制可能会影响多节点并发上报的实时性。

（完）
