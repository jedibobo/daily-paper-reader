---
title: "G-ICSO-NAS: Shifting Gears between Gradient and Swarm for Robust Neural Architecture Search"
title_zh: G-ICSO-NAS：在梯度与群体算法间切换以实现鲁棒的神经架构搜索
authors: "Xingbang Du, Enzhi Zhang, Rui Zhong, Yang Cao, Masaharu Munetomo"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00703v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 鲁棒且高效的神经架构搜索混合框架
tldr: "针对神经架构搜索（NAS）中进化算法成本高和梯度算法易崩溃的问题，本文提出G-ICSO-NAS混合框架。该方法通过三阶段策略（预热、混合协同优化、稳定性搜索），结合改进的竞争群优化算法（ICSO）的全局导航能力与梯度下降的高效性。在CIFAR-10上仅需0.15 GPU天即可达到97.46%的精度，在多个数据集和基准测试中均表现出卓越的性能与鲁棒性。"
selection_source: fresh_fetch
motivation: 旨在解决进化算法计算成本过高以及梯度搜索方法容易陷入局部最优和性能崩溃的局限性。
method: 提出一种三阶段优化策略，利用改进的竞争群优化算法（ICSO）进行架构探索，并结合梯度下降同步更新超网络权重。
result: "在CIFAR-10上以极低的计算成本（0.15 GPU-Days）实现了97.46%的准确率，并在ImageNet和NAS-Bench-201上达到SOTA水平。"
conclusion: G-ICSO-NAS通过协同群智能与梯度优化，成功平衡了搜索效率与架构质量，为鲁棒且高效的NAS提供了新方案。
---

## 摘要
神经架构搜索（NAS）已成为自动机器学习中的关键技术。基于进化算法（EA）的方法展现出优越的搜索质量，但面临极高的计算成本；而像 DARTS 这样的基于梯度的方法虽然效率很高，但容易出现过早收敛和性能崩溃。为了弥补这一差距，我们提出了 G-ICSO-NAS，这是一个实现三阶段优化策略的混合框架。预热阶段（Warm-up Phase）通过可微方法预训练超网络权重（$w$），同时保持架构参数（$α$）冻结。探索阶段（Exploration Phase）采用混合协同优化机制：具有多样性感知适应度的改进竞争群优化器（ICSO）在架构空间中导航以更新 $α$，同时梯度下降同步更新 $w$。稳定阶段（Stability Phase）采用带有早停机制的细粒度梯度搜索，以收敛至最优架构。通过将 ICSO 的全局导航能力与可微方法的效率相结合，G-ICSO-NAS 以极低的成本实现了卓越的性能。在 DARTS 搜索空间中，该方法在 CIFAR-10 上达到了 97.46% 的准确率，而计算预算仅为 0.15 GPU-天。该方法还表现出强大的迁移潜力，在 CIFAR-100 和 ImageNet 上分别录得 83.1% 和 75.02% 的准确率。此外，在 NAS-Bench-201 基准测试中，G-ICSO-NAS 在所有评估数据集上均展现出了最先进的结果。

## Abstract
Neural Architecture Search (NAS) has become a pivotal technique in automated machine learning. Evolutionary Algorithm (EA)-based methods demonstrate superior search quality but suffer from prohibitive computational costs, while gradient-based approaches like DARTS offer high efficiency but are prone to premature convergence and performance collapse. To bridge this gap, we propose G-ICSO-NAS, a hybrid framework implementing a three-stage optimization strategy. The Warm-up Phase pre-trains supernet weights ($w$) via differentiable methods while architecture parameters ($α$) remain frozen. The Exploration Phase adopts a hybrid co-optimization mechanism: an Improved Competitive Swarm Optimizer (ICSO) with diversity-aware fitness navigates the architecture space to update $α$, while gradient descent concurrently updates $w$. The Stability Phase employs fine-grained gradient-based search with early stopping to converge to the optimal architecture. By synergizing ICSO's global navigation capability with differentiable methods' efficiency, G-ICSO-NAS achieves remarkable performance with minimal cost. In the context of the DARTS search space, an accuracy of 97.46\% is achieved on CIFAR-10 with a computational budget of just 0.15 GPU-Days. The method also exhibits strong transfer potential, recording accuracies of 83.1\% (CIFAR-100) and 75.02\% (ImageNet). Furthermore, regarding the NAS-Bench-201 benchmark, G-ICSO-NAS is shown to deliver state-of-the-art results across all evaluated datasets.

---

## 论文详细总结（自动生成）

以下是对论文《G-ICSO-NAS: Shifting Gears between Gradient and Swarm for Robust Neural Architecture Search》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：神经架构搜索（NAS）在效率与鲁棒性之间存在权衡。
    *   **进化算法（EA）**：搜索质量高、鲁棒性强，但计算成本极高（通常需数千GPU小时）。
    *   **梯度算法（如DARTS）**：效率高，但存在“性能崩溃”现象（过度倾向于无参数操作如Skip-connect）且易陷入局部最优。
*   **研究背景**：旨在结合群体智能（全局探索能力）与梯度下降（局部优化效率），提出一种既能防止崩溃又能保持极低计算成本的混合NAS框架。

### 2. 方法论：G-ICSO-NAS 三阶段策略
该方法通过“换挡”机制在不同阶段切换优化引擎：
*   **第一阶段：预热（Warm-up）**
    *   固定架构参数 $\alpha$，仅通过梯度下降预训练超网络权重 $w$。目的是为后续搜索提供一个稳定的权重初始化环境。
*   **第二阶段：探索（Exploration）—— 核心创新**
    *   **双螺旋更新**：架构参数 $\alpha$ 由**改进竞争群优化器（ICSO）**更新（仅需前向传播），而权重 $w$ 同步通过梯度下降更新。
    *   **多维适应度函数**：ICSO 不仅考虑验证集损失（$f_{base}$），还引入了**群体多样性**（防止个体过于趋同）和**操作多样性**（通过信息熵惩罚过度集中的操作，防止崩溃）的惩罚项。
    *   **三元竞争机制**：将粒子分为胜者、中等者和败者，胜者直接保留，败者向胜者和全局最优学习，增强全局导航能力。
*   **第三阶段：稳定（Stability）**
    *   回归梯度搜索，但使用极小的学习率进行微调。
    *   **自适应终止**：引入 **Hoeffding 不等式**进行收敛性测试，当架构参数的变化量低于统计阈值时自动停止，节省算力。

### 3. 实验设计与 Benchmark
*   **数据集**：CIFAR-10（搜索与评估）、CIFAR-100（迁移评估）、ImageNet（大规模评估）。
*   **搜索空间**：DARTS 搜索空间（Cell-based）以及 NAS-Bench-201（固定基准空间）。
*   **对比方法**：
    *   **强化学习/进化类**：ENAS, AmoebaNet, NSGA-Net。
    *   **梯度类**：DARTS (1st/2nd), SNAS, GDAS, PC-DARTS, DrNAS 等。
    *   **基准类**：NAS-Bench-201 上的各种 SOTA 算法。

### 4. 资源与算力
*   **硬件环境**：单台工作站，配备 **NVIDIA RTX A4000 GPU** (16GB VRAM)。
*   **搜索成本**：在 DARTS 搜索空间下，完成 CIFAR-10 的搜索仅需 **0.15 GPU-Days**。
*   **训练细节**：预热 5 epoch，ICSO 种群规模 60，每 epoch 迭代 8 代。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从小型数据集（CIFAR）到大型数据集（ImageNet）的完整测试，并在 NAS-Bench-201 上进行了 4 个随机种子的独立重复实验。
*   **消融实验**：针对多样性适应度函数、稳定阶段、预热阶段分别进行了消融测试，验证了各组件对防止性能崩溃和提高收敛稳定性的必要性。
*   **客观性**：通过在 NAS-Bench-201 这一标准化基准上对比，排除了训练技巧对结果的干扰，证明了搜索算法本身的优越性。

### 6. 主要结论与发现
*   **高性能与低成本**：在 CIFAR-10 上达到 97.46% 的准确率，成本仅为 DARTS 的十分之一左右。
*   **强大的迁移能力**：在 CIFAR-10 上搜索到的架构，在 ImageNet 上达到了 75.02% (Top-1) 的优异表现。
*   **解决崩溃问题**：通过引入操作多样性惩罚，成功抑制了 Skip-connect 的过度生长，解决了 DARTS 常见的鲁棒性问题。
*   **基准测试领先**：在 NAS-Bench-201 的所有评估指标上均达到了 SOTA 水平。

### 7. 优点与亮点
*   **混合优化范式**：巧妙地将 ICSO 的全局搜索能力与梯度的局部精修能力结合，实现了“换挡”式的优化。
*   **多样性感知**：适应度函数的设计考虑了操作分布熵，从根本上通过优化目标缓解了结构崩溃。
*   **搜索效率极高**：0.15 GPU-days 的成本使其非常适合资源受限的实际应用场景。
*   **数学严谨性**：引入 Hoeffding 不等式作为早停准则，增加了算法的自动化程度和理论依据。

### 8. 不足与局限
*   **搜索空间限制**：实验主要集中在 Cell-based 搜索空间，尚未在 Transformer 或更宏观的架构空间（如全网络搜索）中验证。
*   **超参数敏感性**：多样性权重（$\lambda_{swarm}, \lambda_{op}$）和稳定阶段阈值可能需要针对不同任务进行微调。
*   **硬件依赖**：虽然算力需求低，但实验环境基于特定的 A4000 显卡，在不同架构硬件上的加速效果未做详细对比。

（完）
