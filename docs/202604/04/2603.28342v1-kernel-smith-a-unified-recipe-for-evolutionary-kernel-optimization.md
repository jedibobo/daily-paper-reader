---
title: "Kernel-Smith: A Unified Recipe for Evolutionary Kernel Optimization"
title_zh: Kernel-Smith：一种统一的演化算子优化方案
authors: "He Du, Qiming Ge, Jiakai Hu, Aijun Yang, Zheng Cai, Zixian Huang, Sheng Yuan, Qinxiu Cheng, Xinchen Xie, Yicheng Chen, Yining Li, Jiaxing Xie, Huanan Dong, Yaguang Wu, Xiangjun Huang, Jian Yang, Hui Wang, Bowen Zhou, Bowen Li, Qipeng Guo, Kai Chen"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28342v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 结合编译反馈的进化算子与内核生成
tldr: Kernel-Smith是一个用于高性能GPU算子生成的统一框架，通过结合评估驱动的进化智能体与面向进化的后训练方案，显著提升了内核优化效率。该框架利用执行反馈（编译、正确性、加速比）进行迭代搜索，并将进化轨迹转化为强化学习信号，使模型成为强大的局部改进器。实验表明，Kernel-Smith在KernelBench上超越了Gemini和Claude等顶尖模型，并成功适配NVIDIA和MetaX等异构平台，其成果已应用于SGLang等生产系统。
selection_source: fresh_fetch
motivation: 针对高性能GPU算子开发难度大、LLM单次生成质量不稳定以及跨硬件平台适配难等挑战。
method: 采用进化搜索智能体结合结构化执行反馈，并通过后训练将长程进化轨迹转化为步进式监督与强化学习信号。
result: Kernel-Smith-235B在KernelBench上取得SOTA，且30B版本在国产GPU上超越了DeepSeek-V3等大规模模型。
conclusion: 该研究验证了LLM驱动的进化优化在异构硬件上的通用性，并展示了从基准测试到生产环境部署的闭环能力。
---

## 摘要
我们提出了 Kernel-Smith，这是一个用于高性能 GPU 内核与算子生成的框架，它结合了稳定的评估驱动型演化智能体与面向演化的后训练方案。在智能体方面，Kernel-Smith 维护着一个可执行候选程序的种群，并利用由高性能且多样化的程序组成的存档，结合关于编译、正确性和加速比的结构化执行反馈，对这些程序进行迭代改进。为了确保搜索的可靠性，我们为 NVIDIA GPU 上的 Triton 和沐曦（MetaX）GPU 上的 Maca 构建了特定后端的评估服务。在训练方面，我们通过保留保持正确性且高增益的修订，将长程演化轨迹转化为以步骤为中心的监督和强化学习信号，从而使模型在演化循环中被优化为一个强大的局部改进器，而非一次性生成器。在统一的演化协议下，Kernel-Smith-235B-RL 在使用 Nvidia Triton 后端的 KernelBench 上实现了最先进的综合性能，获得了最佳的平均加速比，并超越了包括 Gemini-3.0-pro 和 Claude-4.6-opus 在内的前沿商业模型。我们进一步在沐曦 MACA 后端验证了该框架，其中 Kernel-Smith-MACA-30B 超越了 DeepSeek-V3.2-think 和 Qwen3-235B-2507-think 等大规模同类模型，凸显了其在异构平台间无缝适配的潜力。除了基准测试结果外，该工作流还为包括 SGLang 和 LMDeploy 在内的生产系统做出了上游贡献，证明了大语言模型（LLM）驱动的算子优化可以从受控评估转化为实际部署。

## Abstract
We present Kernel-Smith, a framework for high-performance GPU kernel and operator generation that combines a stable evaluation-driven evolutionary agent with an evolution-oriented post-training recipe. On the agent side, Kernel-Smith maintains a population of executable candidates and iteratively improves them using an archive of top-performing and diverse programs together with structured execution feedback on compilation, correctness, and speedup. To make this search reliable, we build backend-specific evaluation services for Triton on NVIDIA GPUs and Maca on MetaX GPUs. On the training side, we convert long-horizon evolution trajectories into step-centric supervision and reinforcement learning signals by retaining correctness-preserving, high-gain revisions, so that the model is optimized as a strong local improver inside the evolutionary loop rather than as a one-shot generator. Under a unified evolutionary protocol, Kernel-Smith-235B-RL achieves state-of-the-art overall performance on KernelBench with Nvidia Triton backend, attaining the best average speedup ratio and outperforming frontier proprietary models including Gemini-3.0-pro and Claude-4.6-opus. We further validate the framework on the MetaX MACA backend, where our Kernel-Smith-MACA-30B surpasses large-scale counterparts such as DeepSeek-V3.2-think and Qwen3-235B-2507-think, highlighting potential for seamless adaptation across heterogeneous platforms. Beyond benchmark results, the same workflow produces upstream contributions to production systems including SGLang and LMDeploy, demonstrating that LLM-driven kernel optimization can transfer from controlled evaluation to practical deployment.

---

## 论文详细总结（自动生成）

### 论文总结：Kernel-Smith —— 统一的演化算子优化方案

#### 1. 核心问题与研究动机
*   **核心问题**：高性能 GPU 算子（Kernel）开发门槛极高，现有基于大语言模型（LLM）的生成方法通常局限于“单次生成（One-shot）”，难以在复杂的搜索空间（如算子融合、分块策略、重写方向）中进行持续、可靠的迭代优化。
*   **研究动机**：
    *   **性能与正确性的权衡**：高性能算子不仅要正确，还需针对特定硬件深度优化。
    *   **搜索效率低下**：现有的多轮对话（Multi-turn）智能体容易陷入局部最优，缺乏探索多样性。
    *   **数据稀缺**：高质量、经过人工优化的 Triton/CUDA 代码极少，限制了监督微调的效果。

#### 2. 方法论
Kernel-Smith 结合了**演化智能体（Evolutionary Agent）**与**面向演化的后训练（Evolution-oriented Post-training）**：
*   **演化智能体框架**：
    *   **种群管理**：维护一个可执行程序的种群，采用类似 MAP-Elites 的机制，保留高性能且多样化的候选代码。
    *   **结构化反馈循环**：评估后端（针对 NVIDIA Triton 和沐曦 MACA）提供编译状态、数值正确性、加速比及硬件元数据的详细反馈，引导模型进行下一轮变异。
    *   **稳定性优化**：通过预热执行、多次测量剔除异常值、引入 CUDAGraph 等手段将测量误差控制在 1% 以内。
*   **训练方案（从“生成器”转变为“改进器”）**：
    *   **数据合成**：从 GitHub 挖掘 5.9 万个 PyTorch 模块，利用教师模型生成演化轨迹。
    *   **步骤中心化监督微调（SFT）**：将长程轨迹拆解为单步改进，仅保留正确且有性能增益的样本（约 20 万条）。
    *   **强化学习（RL）**：采用 GRPO 算法，以加速比为奖励信号。核心策略是选择“最佳步骤（Best-step）”进行训练，避免模型产生“偷懒优化”或简单记忆。

#### 3. 实验设计
*   **数据集/场景**：
    *   **KernelBench**：标准的 GPU 算子生成基准测试，分为 Easy、Medium、Hard 三个难度。
    *   **MACA Benchmark**：针对国产沐曦 GPU 构建的 45 个常用算子测试集。
    *   **真实世界应用**：在 SGLang、LMDeploy 生产环境及 DeepSeek Engram 研究代码中进行实测。
*   **对比方法**：
    *   **开源模型**：Qwen3 (235B/397B)、DeepSeek-V3.2-Speciale、Kimi-K2.5、MiniMax-M2.5。
    *   **闭源模型**：Gemini-3.0-pro、Claude-4.6-opus。

#### 4. 资源与算力
*   **训练算力**：论文**未明确说明**训练 Kernel-Smith-235B 和 30B 模型所使用的具体 GPU 数量及总训练时长。但提到基础模型使用了 Intern-S1-Pro，且 SFT 阶段采用了 64k 的上下文长度。
*   **推理/评估算力**：实验评估在 **NVIDIA H200** GPU 上完成，每个算子独立运行 100 次以确保数据稳定。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从标准 Benchmark 到异构硬件适配，再到生产系统 PR（拉取请求）的全流程。
*   **消融与对比**：对比了不同演化步数（最高 40 步）下的性能增长曲线（图 1），展示了模型在测试时计算量（Test-time compute）增加时的收益。
*   **客观性**：引入了“作弊检测（Hacking Detection）”机制，防止模型通过直接调用 PyTorch 原生算子来伪造加速效果，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **SOTA 性能**：Kernel-Smith-235B-RL 在 KernelBench 上取得了最佳平均加速比（3.70x），在 Medium 难度下显著超越 Claude-4.6-opus。
*   **异构适配能力**：30B 版本的模型在沐曦 MACA 后端表现优异，证明了该框架在不同硬件架构间的通用性。
*   **生产价值**：成功为 SGLang 和 LMDeploy 贡献了合并后的上游代码，在实际推理任务中带来了 0.1%~3.0% 的端到端性能提升。

#### 7. 优点（亮点）
*   **从“生成”到“演化”的范式转变**：不追求一次性写对，而是训练模型成为一个能够根据反馈不断调优的“算子工程师”。
*   **闭环评估系统**：解决了 GPU 测量噪声问题，提供了可靠的强化学习奖励信号。
*   **跨平台通用性**：统一的 Recipe 能够快速迁移至国产芯片等异构后端。

#### 8. 不足与局限
*   **推理成本高昂**：为了达到最佳性能，需要进行多达 40 轮的演化搜索，这在实时性要求高的场景下难以应用。
*   **对初始代码的依赖**：虽然有演化机制，但如果初始翻译（PyTorch 到 Triton）完全失败，后续优化可能难以开展。
*   **算力细节缺失**：未披露训练资源消耗，使得其他研究者难以评估其复现成本。

（完）
