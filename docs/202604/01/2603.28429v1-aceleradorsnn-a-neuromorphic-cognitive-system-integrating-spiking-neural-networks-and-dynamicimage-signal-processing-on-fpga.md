---
title: "AceleradorSNN: A Neuromorphic Cognitive System Integrating Spiking Neural Networks and DynamicImage Signal Processing on FPGA"
title_zh: AceleradorSNN：一种在 FPGA 上集成脉冲神经网络与动态图像信号处理的类脑认知系统
authors: "Daniel Gutierrez, Ruben Martinez, Leyre Arnedo, Antonio Cuesta, Soukaina El Hamry"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28429v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在FPGA上集成基于SNN的NPU和可重构ISP以实现高效能检测
tldr: 针对自动驾驶和机器人领域对高速、低功耗目标检测的需求，本文提出了AceleradorSNN系统。该系统在FPGA上实现，集成了处理动态视觉传感器（DVS）异步数据的脉冲神经网络（SNN）处理器，以及用于RGB摄像头的动态可重构认知图像信号处理器（ISP）。这种异构架构结合了神经形态计算与传统视觉处理的优势，显著提升了实时感知性能。
selection_source: fresh_fetch
motivation: 传统卷积神经网络在自动驾驶等实时场景中面临高延迟和高功耗的局限，难以高效处理高速异步视觉数据。
method: 开发了一种集成SNN神经形态处理单元（NPU）和动态可重构认知ISP的FPGA架构，支持DVS和RGB双模输入。
result: 成功实现了基于代理梯度训练的SNN骨干网络，并在FPGA上完成了实时流式ISP架构的硬件验证。
conclusion: AceleradorSNN通过融合SNN与动态ISP，为自主系统提供了一种高效、低延迟的第三代人工智能认知解决方案。
---

## 摘要
自主系统（如高级驾驶辅助系统 (ADAS)、无人机 (UAV) 和工业 4.0 机器人）对高速、低延迟和高能效目标检测的需求，暴露了传统卷积神经网络 (CNN) 的局限性。为了应对这些挑战，我们开发了 AceleradorSNN，这是一种第三代人工智能认知系统。该架构集成了一个基于脉冲神经网络 (SNN) 的类脑处理单元 (NPU) 以处理来自动态视觉传感器 (DVS) 的异步数据，以及一个用于 RGB 摄像头的动态可重构认知图像信号处理器 (ISP)。本文详细介绍了这两个 IP 核的面向硬件的设计、基于代理梯度训练的 SNN 骨干网络的评估，以及在现场可编程门阵列 (FPGA) 上实现的实时流式 ISP 架构。

## Abstract
The demand for high-speed, low-latency, and energy-efficient object detection in autonomous systems -- such as advanced driver-assistance systems (ADAS), unmanned aerial vehicles (UAVs), and Industry 4.0 robotics -- has exposed the limitations of traditional Convolutional Neural Networks (CNNs). To address these challenges, we have developed AceleradorSNN, a third-generation artificial intelligence cognitive system. This architecture integrates a Neuromorphic Processing Unit (NPU) based on Spiking Neural Networks (SNNs) to process asynchronous data from Dynamic Vision Sensors (DVS), alongside a dynamically reconfigurable Cognitive Image Signal Processor (ISP) for RGB cameras. This paper details the hardware-oriented design of both IP cores, the evaluation of surrogate-gradienttrained SNN backbones, and the real-time streaming ISP architecture implemented on Field-Programmable Gate Arrays (FPGA).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AceleradorSNN** 的类脑认知系统，旨在通过 FPGA 硬件实现脉冲神经网络（SNN）与动态图像信号处理（ISP）的深度集成，以解决自动驾驶和工业机器人中的高能效感知问题。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：传统卷积神经网络（CNN）在处理高速运动和复杂光照场景时，面临计算开销大、延迟高、功耗高以及 RGB 传感器动态范围有限的问题。
*   **研究动机**：
    *   **传感器局限性**：标准 RGB 相机在高速运动下会产生模糊，且难以应对剧烈的光照变化；事件相机（DVS）虽然响应快、动态范围高，但缺乏纹理和颜色信息。
    *   **计算效率**：传统的逐帧处理方式在嵌入式边缘设备上效率低下。
    *   **系统协同**：探索如何结合 DVS 的超快响应能力和 RGB 的高分辨率特征，构建一个低功耗、高可靠的实时感知系统。

### 2. 论文提出的方法论
该系统由两个核心硬件 IP 核组成，形成一个“认知闭环”：
*   **神经形态处理单元 (NPU)**：
    *   **核心思想**：使用 SNN 处理 DVS 产生的异步事件流。
    *   **关键技术**：
        *   **事件编码**：将异步事件 $(t, x, y, p)$ 在固定时间窗口内聚合，转换为时空体素网格（Voxel Grid）。
        *   **神经元模型**：采用漏整流集成触发（LIF）模型，通过衰减项模拟电荷流失。
        *   **训练方法**：由于脉冲触发不可导，采用**代理梯度（Surrogate Gradients）**结合随时间反向传播（BPTT）算法进行训练。
*   **认知图像信号处理器 (ISP)**：
    *   **核心思想**：受 NPU 控制的动态可重构流水线。
    *   **算法流程**：包含动态坏点校正（DPC）、自动白平衡（AWB）、去马赛克（Demosaicing）、非局部均值（NLM）去噪、伽马校正及色彩空间转换。
    *   **协同机制**：NPU 检测到目标或光照异常后，实时生成指令调整 ISP 参数（如曝光、增益、去噪强度），优化 RGB 输出。

### 3. 实验设计
*   **数据集**：使用了 Prophesee 的 **GEN1 汽车检测数据集**（大规模事件相机数据集）。
*   **对比骨干网络 (Backbones)**：
    *   **Spiking-VGG**：层次化特征提取。
    *   **Spiking-DenseNet**：特征复用，防止梯度消失。
    *   **Spiking-MobileNet**：利用深度可分离卷积减少参数。
    *   **Spiking YOLO**：将 YOLO 架构转换为脉冲域，用于高性能目标检测。
*   **评估指标**：平均精度（AP @ IoU 0.50）和网络稀疏度（Sparsity）。

### 4. 资源与算力
*   **硬件平台**：系统在 **FPGA** 上进行了综合与验证，并为未来的 ASIC 制造做了映射准备。
*   **算力说明**：文中**未明确提及**具体的训练硬件（如 GPU 型号）或训练时长。它强调的是硬件实现（HDL 编写）和推理时的低功耗特性。

### 5. 实验数量与充分性
*   **实验规模**：对比了四种主流深度学习架构的脉冲版本在 GEN1 数据集上的表现。
*   **充分性评价**：
    *   在 SNN 骨干网络的选择上做了较为充分的横向对比。
    *   **局限性**：实验主要集中在 NPU 的检测精度上，对于“认知 ISP”动态调整后对整体系统精度提升的具体量化数据（例如：开启 NPU 反馈后 RGB 检测率提升了多少）描述较少。

### 6. 论文的主要结论与发现
*   **性能最优**：**Spiking YOLO** 在 GEN1 数据集上取得了最高的精度（AP 为 **0.4726**），在准确性与计算成本之间达到了最佳平衡。
*   **能效最优**：**Spiking-MobileNet** 表现出最高的网络稀疏度（**48.08%**），意味着近一半的神经元保持静默，非常适合低功耗边缘设备。
*   **架构可行性**：证明了在 FPGA 上实现全流水线、无帧缓存（Line-buffer based）的流式 ISP 是可行的，能极大地减少硬件资源占用。

### 7. 优点
*   **异构融合**：创新性地将 SNN 的事件驱动特性与传统 ISP 的高分辨率处理结合，互补了两种传感器的短板。
*   **硬件友好**：ISP 设计完全基于 HDL，采用 AXI4-Stream 协议，无需外部内存缓存整帧图像，显著降低了延迟和面积。
*   **高成熟度**：系统达到了 TRL 6/7（技术就绪水平），具备实际工业应用潜力。

### 8. 不足与局限
*   **量化评估缺失**：缺乏 ISP 动态调整功能的定量消融实验，难以直观判断“认知反馈”对复杂环境下的具体贡献。
*   **应用限制**：SNN 的训练依然依赖代理梯度和反向传播，这在算法转化和量化过程中可能存在精度损失。
*   **文档篇幅**：作为一篇短论文（3页），部分技术细节（如 NPU 与 ISP 同步的具体时序逻辑）描述较为简略。

（完）
