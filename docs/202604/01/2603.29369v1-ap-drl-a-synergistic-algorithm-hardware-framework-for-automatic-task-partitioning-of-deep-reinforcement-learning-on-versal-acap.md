---
title: "AP-DRL: A Synergistic Algorithm-Hardware Framework for Automatic Task Partitioning of Deep Reinforcement Learning on Versal ACAP"
title_zh: AP-DRL：一种用于在 Versal ACAP 上实现深度强化学习自动任务划分的算法-硬件协同框架
authors: "Enlai Li, Zhe Lin, Sharad Sinha, Wei Zhang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29369v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对Versal ACAP的深度强化学习算法-硬件协同框架
tldr: 针对深度强化学习（DRL）训练中计算强度差异大和量化误差显著的问题，本文提出AP-DRL框架。该框架利用AMD Versal ACAP的异构架构（CPU、FPGA、AIE），通过瓶颈分析、基于整数线性规划（ILP）的任务划分模型以及硬件感知的混合精度优化（FP32/FP16/BF16），实现了DRL训练的自动任务分配与加速。实验表明，该方法在保证收敛性的同时，显著提升了训练速度。
selection_source: fresh_fetch
motivation: DRL训练中算子计算强度差异大且动态范围广，导致传统硬件选择和量化方案难以兼顾效率与精度。
method: 提出一种基于ILP的任务划分模型和硬件感知的混合精度算法，将不同特性的算子自动映射至Versal ACAP的最优计算单元。
result: 实验显示AP-DRL相比纯可编程逻辑加速比达4.17倍，相比纯AI引擎加速比达3.82倍，且不影响训练收敛。
conclusion: AP-DRL通过软硬件协同优化，有效解决了异构平台上DRL训练的自动任务分配与精度损失挑战。
---

## 摘要
深度强化学习在多个领域取得了显著成功。然而，训练与推理过程之间的紧密耦合使得加速 DRL 训练成为 DRL 优化的一个核心挑战。阻碍高效 DRL 训练的两个关键问题在于：(1) 不同 DRL 算法之间、甚至同一算法内部不同算子之间的计算强度存在显著差异，这使得硬件平台的选择变得复杂；(2) DRL 较宽的动态范围在使用传统的 FP16+FP32 混合精度量化时，可能会导致显著的奖励误差。现有工作主要集中在针对特定计算单元加速 DRL 或优化推理阶段的量化，而我们提出了 AP-DRL 来应对上述挑战。AP-DRL 是一个自动任务划分框架，它利用 AMD Versal ACAP 的异构架构（集成了 CPU、FPGA 和 AI 引擎），通过智能硬件感知优化来加速 DRL 训练。我们的方法首先对不同 DRL 工作负载下的 CPU、FPGA 和 AIE 性能进行瓶颈分析，从而为 AP-DRL 的组件间任务划分和量化优化提供设计原则。该框架随后通过基于设计空间探索的性能分析和基于整数线性规划（ILP）的划分模型来解决平台选择挑战，根据算子的计算特性将其匹配到最优计算单元。针对量化挑战，AP-DRL 利用 Versal ACAP 对这些精度格式的原生支持，采用一种硬件感知算法来协调 FP32 (CPU)、FP16 (FPGA/DSP) 和 BF16 (AI 引擎) 操作。综合实验表明，AP-DRL 在保持训练收敛的同时，相比可编程逻辑基准可实现高达 4.17 倍的加速，相比 AI 引擎基准可实现高达 3.82 倍的加速。

## Abstract
Deep reinforcement learning has demonstrated remarkable success across various domains. However, the tight coupling between training and inference processes makes accelerating DRL training an essential challenge for DRL optimization. Two key issues hinder efficient DRL training: (1) the significant variation in computational intensity across different DRL algorithms and even among operations within the same algorithm complicates hardware platform selection, while (2) DRL's wide dynamic range could lead to substantial reward errors with conventional FP16+FP32 mixed-precision quantization. While existing work has primarily focused on accelerating DRL for specific computing units or optimizing inference-stage quantization, we propose AP-DRL to address the above challenges.   AP-DRL is an automatic task partitioning framework that harnesses the heterogeneous architecture of AMD Versal ACAP (integrating CPUs, FPGAs, and AI Engines) to accelerate DRL training through intelligent hardware-aware optimization. Our approach begins with bottleneck analysis of CPU, FPGA, and AIE performance across diverse DRL workloads, informing the design principles for AP-DRL's inter-component task partitioning and quantization optimization. The framework then addresses the challenge of platform selection through design space exploration-based profiling and ILP-based partitioning models that match operations to optimal computing units based on their computational characteristics. For the quantization challenge, AP-DRL employs a hardware-aware algorithm coordinating FP32 (CPU), FP16 (FPGA/DSP), and BF16 (AI Engine) operations by leveraging Versal ACAP's native support for these precision formats. Comprehensive experiments indicate that AP-DRL can achieve speedup of up to 4.17$\times$ over programmable logic and up to 3.82$\times$ over AI Engine baselines while maintaining training convergence.

---

## 论文详细总结（自动生成）

以下是对论文《AP-DRL: A Synergistic Algorithm-Hardware Framework for Automatic Task Partitioning of Deep Reinforcement Learning on Versal ACAP》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对深度强化学习（DRL）训练加速中的两个核心痛点展开研究：
*   **硬件平台选择难题**：DRL 算法内部算子计算强度差异巨大。例如，小 Batch 训练在 FPGA 上效率更高，而复杂的 CNN 算子在 GPU 或 AI 引擎（AIE）上表现更好。单一硬件平台难以兼顾所有算子的最优执行。
*   **量化精度损失**：DRL 的数据动态范围比监督学习更广，传统的 FP16+FP32 混合精度量化容易引入显著的奖励误差，导致训练不收敛。
*   **研究目标**：开发一个自动化的算法-硬件协同框架（AP-DRL），利用 AMD Versal ACAP 的异构架构（CPU+FPGA+AIE），实现任务的最优划分与硬件感知的混合精度训练。

### 2. 方法论：核心思想与技术细节
AP-DRL 框架分为**静态阶段**和**动态阶段**：
*   **瓶颈分析与性能建模**：首先量化分析 CPU (PS)、FPGA (PL) 和 AIE 在不同计算强度（FLOPs）下的表现。发现 AIE 在高 FLOPs 下占优但启动开销大，PL 在低 FLOPs 下更灵活。
*   **基于 ILP 的自动任务划分**：
    *   将 DRL 训练过程建模为有向无环图（DAG），以“层”为粒度进行划分。
    *   使用**整数线性规划（ILP）**模型，在满足硬件资源约束和算子依赖的前提下，最小化总执行时间。
    *   非矩阵运算（如 ReLU）通常分配给 PL，矩阵运算（MM）根据计算强度在 PL 和 AIE 之间动态分配。
*   **硬件感知的混合精度算法**：
    *   **BF16 优势利用**：在 AIE 上使用 BF16 格式，因其指数位与 FP32 相同，能有效保留 DRL 的动态范围，无需复杂的损失缩放（Loss Scaling）。
    *   **协同量化流程**：PS 负责 FP32 运算，PL 负责 FP16（利用 DSP），AIE 负责 BF16。
    *   **主权重备份与同步**：在 PL 执行 FP16 时使用主权重备份和动态损失缩放，确保数值稳定性，并与 AIE 的 BF16 进行同步。

### 3. 实验设计
*   **实验场景与数据集**：涵盖了 Atari 游戏（Breakout, MsPacman）、OpenAI Gym（CartPole, LunarLanderContinuous, MountainCarContinuous）以及 MuJoCo（InvertedPendulum）。
*   **覆盖算法**：DQN, DDPG, A2C, PPO。
*   **Benchmark（对比方法）**：
    1.  **AIE-Only**：仅在 AI 引擎上运行（使用 CHARM 优化）。
    2.  **FIXAR**：最先进的 CPU-FPGA 异构 DRL 加速框架（基于定点量化）。
    3.  **FP32 基准**：用于验证量化后的收敛性和奖励误差。

### 4. 资源与算力
*   **硬件平台**：AMD Versal ACAP **VEK280**（AI Edge 系列）。
*   **核心资源**：双核 Cortex-A72 CPU、304 个 AIE-ML 块、1312 个 DSP 引擎、520.7K LUT。
*   **运行参数**：PL 时钟频率为 245 MHz，AIE 时钟频率为 1 GHz。
*   **实现方式**：实验主要通过 **Vitis 2023.2 硬件仿真（Hardware Emulation）** 完成，并使用 Vitis Analyzer 进行性能剖析。文中未提及具体训练时长，但提供了单步训练的执行时间对比。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 种不同的环境-算法组合进行了测试，每种组合涵盖了不同的 Batch Size（从 16 到 4096 不等）。
*   **充分性**：实验设计较为全面，包含了：
    *   **收敛性实验**：展示了 5 个独立随机种子下的平均奖励曲线，证明了量化后的有效性。
    *   **吞吐量与延迟实验**：对比了不同 Batch Size 下的加速比。
    *   **消融分析**：分析了不同网络规模下量化带来的性能增益。
*   **客观性**：对比了现有的 SOTA 异构框架（FIXAR），并考虑了通信和初始化开销，评价较为客观。

### 6. 主要结论与发现
*   **加速效果显著**：AP-DRL 相比纯 PL 实现（FIXAR）实现了高达 **4.17×** 的加速，相比纯 AIE 实现实现了 **3.82×** 的加速。
*   **量化精度达标**：在所有测试环境中，AP-DRL 的奖励误差仅在 **1.12% 至 4.81%** 之间，且所有模型均能正常收敛。
*   **任务划分规律**：随着 Batch Size 和 FLOPs 的增加，ILP 模型会自动将更多层分配给 AIE，以利用其高频计算能力；而在低 FLOPs 场景下，PL 因启动开销小而更受青睐。

### 7. 优点
*   **异构资源最大化利用**：不同于以往只关注单一加速器的研究，该框架真正实现了 PS、PL、AIE 三者的协同。
*   **量化策略聪明**：敏锐捕捉到 BF16 在 DRL 训练中保持动态范围的天然优势，解决了 DRL 量化难收敛的痛点。
*   **自动化程度高**：通过 ILP 自动完成任务映射，降低了开发者在复杂异构芯片上的开发门槛。

### 8. 不足与局限
*   **仿真与实测差距**：主要实验数据基于硬件仿真（Hardware Emulation），虽然 Vitis 仿真精度较高，但文中缺乏实际板卡的功耗（Power）和真实热管理数据。
*   **同步开销**：在极低 FLOPs 的网络中，主权重同步的开销（约 22%）会抵消部分量化带来的性能增益。
*   **环境步骤未加速**：目前框架主要关注“训练”阶段的加速，对于某些环境模拟（Environment Step）成为瓶颈的 DRL 任务，整体端到端加速比可能会受限。

（完）
