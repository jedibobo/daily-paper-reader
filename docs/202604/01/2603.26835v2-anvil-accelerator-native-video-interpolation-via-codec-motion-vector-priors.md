---
title: "ANVIL: Accelerator-Native Video Interpolation via Codec Motion Vector Priors"
title_zh: ANVIL：通过编解码器运动矢量先验实现的加速器原生视频插帧
authors: Shibo Liu
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26835v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对移动 NPU 的加速器原生设计，移除了硬件不友好的算子
tldr: 针对移动端NPU上实时视频插帧面临的算子支持差、量化失效和内存受限等挑战，本文提出ANVIL。该方法通过复用H.264/AVC解码器的运动矢量进行预对齐，消除了复杂的学习光流和空间采样算子。ANVIL采用纯卷积网络进行残差细化，在Snapdragon 8 Gen 3上实现了12.8ms的1080p推理速度，且支持8位整数精度，为移动端实时视频插帧提供了高效的硬件原生解决方案。
selection_source: fresh_fetch
motivation: 传统基于光流的插帧方法在移动端NPU上存在算子不兼容、8位量化精度崩溃及内存带宽受限等部署障碍。
method: 复用H.264/AVC解码器的运动矢量预对齐输入帧，并利用以卷积为主的轻量化网络进行残差细化，从而避开复杂的流计算。
result: 在骁龙8 Gen 3设备上，ANVIL实现了12.8ms的1080p 8位整数推理，且在30分钟连续播放中保持了28.4ms的端到端中值延迟。
conclusion: 通过利用编解码器先验信息并优化算子结构，可以在移动端硬件上实现高性能、低功耗且量化友好的实时视频插帧。
---

## 摘要
在移动端神经网络处理器（NPU）上实现 30 到 60 fps 的实时视频插帧，要求每一帧的合成时间在 33.3 毫秒以内。我们展示了主流的基于光流的视频插帧在移动端 NPU 上面临三个结构性的部署障碍：空间采样算子超出了帧时间预算或缺乏硬件支持；迭代光流细化在 8 位整数训练后量化下会失效；以及访存受限算子在推理图中占据主导地位。ANVIL 通过复用 H.264/AVC 解码器中的运动矢量来预对齐输入帧，从而解决了这些障碍，并从加速器图中移除了学习到的光流、空间采样和迭代累加。剩余的残差由一个以卷积为主的网络进行细化，该网络几乎完全由计算受限算子组成。在骁龙 8 Gen 3 设备上，ANVIL 在 8 位整数精度下实现了 12.8 毫秒的 1080p 推理；一个开源 Android 播放器在 30 分钟连续播放中保持了 28.4 毫秒的中值端到端延迟。逐算子因果分析表明，循环光流状态上的量化累加是迭代方法中整数量化失败的关键机制。当前设计针对具有解码器暴露运动矢量的 H.264/AVC 播放。

## Abstract
Real-time 30-to-60 fps video frame interpolation on mobile neural processing units (NPUs) requires each synthesized frame within 33.3 ms. We show that mainstream flow-based video frame interpolation faces three structural deployment barriers on mobile NPUs: spatial sampling operators exceed the frame budget or lack hardware support, iterative flow refinement collapses under 8-bit integer post-training quantization, and memory-bound operators dominate the inference graph. ANVIL addresses these barriers by reusing motion vectors from the H.264/AVC decoder to prealign input frames, removing learned optical flow, spatial sampling, and iterative accumulation from the accelerator graph. The remaining residual is refined by a convolution-dominated network composed almost entirely of compute-bound operators. On a Snapdragon 8 Gen 3 device, ANVIL achieves 12.8 ms 1080p inference at 8-bit integer precision; an open-source Android player sustains 28.4 ms median end-to-end latency over 30-minute continuous playback. Per-operator causal analysis identifies quantized accumulation on recurrent flow states as a key mechanism behind integer quantization failure in iterative methods. The current design targets H.264/AVC playback with decoder-exposed motion vectors.

---

## 论文详细总结（自动生成）

以下是对论文《ANVIL: Accelerator-Native Video Interpolation via Codec Motion Vector Priors》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文聚焦于在**移动端 NPU（神经网络处理器）**上实现实时 1080p 视频插帧（VFI）的部署难题。
*   **研究动机**：现有的主流插帧算法（如 RIFE, IFRNet, AMT）虽然效果好，但在移动端部署面临三大障碍：
    1.  **算子不兼容**：核心的 `grid_sample`（空间采样）算子在 NPU 上延迟极高或根本不支持。
    2.  **量化崩溃**：为了性能必须使用 INT8 量化，但迭代式光流细化结构在量化后精度会严重下降。
    3.  **访存受限**：推理图中充斥着大量非卷积算子，导致无法充分利用 NPU 的计算算力。
*   **核心目标**：设计一种“加速器原生”的架构，在满足 33.3ms（60fps）时间预算的前提下，保持 1080p 高清画质。

### 2. 论文提出的方法论：ANVIL
ANVIL 的核心思想是将插帧任务分解为**编解码器端预对齐**和 **NPU 端残差细化**两个阶段：
*   **核心思想**：复用 H.264 视频解码过程中已有的**运动矢量（Motion Vectors, MVs）**作为零成本的运动先验，替代昂贵的深度学习光流计算。
*   **关键技术细节**：
    1.  **MV 提取与平滑**：从 H.264 P 帧提取块级 MV，通过中值滤波和高斯模糊进行空间平滑，生成稠密光流场。
    2.  **三处理器流水线**：
        *   **CPU**：负责 MV 的稠密化和下采样（约 2.9ms）。
        *   **GPU (Vulkan)**：负责平滑处理和图像变形（Warping），生成预对齐帧（约 3.7ms）。
        *   **NPU (HTP)**：运行一个纯卷积的残差网络（UNet-v3b），预测预对齐帧与真实帧之间的残差（约 12.8ms）。
    3.  **NPU 友好型架构**：网络仅包含 3x3 卷积、ReLU、加法跳跃连接和转置卷积。通过**BN 融合**和**加法连接替代拼接（Concat）**，最大化计算受限算子的比例。

### 3. 实验设计
*   **数据集**：使用 **Vimeo90K** 进行训练和基础评估；使用 **Xiph 1080p** 测试模型在高清真实场景下的泛化能力。
*   **Benchmark 与对比方法**：
    *   **对比算法**：RIFE (HDv3), IFRNet, AMT-S, EMA-VFI 等 9 种主流方法。
    *   **评估指标**：PSNR, SSIM（客观质量）；LPIPS（感知质量）；INT8 推理延迟；端到端播放稳定性。
*   **硬件平台**：骁龙 8 Gen 2/3 (Qualcomm) 和 天玑 9300/9400+ (MediaTek)。

### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 16，采用混合精度（bfloat16）训练。
*   **算力说明**：文中提到了训练时的超参数设置，但**未明确说明具体使用的 GPU 型号和总训练时长**。
*   **部署算力**：详细记录了在移动端骁龙 8 Gen 3 上的 HTP（Hexagon Tensor Processor）利用情况，1080p INT8 推理仅需 12.8ms。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨厂商（高通、联发科）的算子兼容性基准测试（涉及 17 种算子）。
    *   对 54,623 个连续帧对进行了长达 30 分钟的端到端播放压力测试。
    *   针对量化失效问题进行了逐算子的因果分析。
*   **充分性与公平性**：实验设计非常充分且具有高度的工程实践价值。作者不仅对比了算法精度，还深入分析了硬件底层的瓶颈。通过在相同量化协议下对比 RIFE 和 IFRNet，证明了 ANVIL 在硬件部署上的优越性，实验过程客观公平。

### 6. 论文的主要结论与发现
1.  **量化失效机制**：发现迭代式光流方法在 INT8 量化下崩溃的主因是**循环状态上的加法算子（Add）放大了量化误差**。
2.  **部署瓶颈**：VFI 的瓶颈不在于参数量或 FLOPs，而在于算子与加速器的不匹配（如 `grid_sample` 的访存开销）。
3.  **性能表现**：ANVIL-S 在骁龙 8 Gen 3 上实现了 12.8ms 的 1080p 推理，是唯一能在该设备上稳定实现 1080p 60fps 播放的深度学习方案。
4.  **质量权衡**：虽然 ANVIL 的感知质量（LPIPS）略逊于基于复杂光流的方法，但在移动端部署的实用性上具有压倒性优势。

### 7. 优点
*   **硬件原生设计**：不盲目追求 SOTA 精度，而是从硬件限制出发反向设计算法，极具工业落地价值。
*   **系统级协同**：巧妙利用了 CPU、GPU 和 NPU 的异构计算能力，并复用了编解码器的现有数据。
*   **深度诊断**：对量化崩溃原因的逐算子分析为后续移动端模型设计提供了重要参考。
*   **开源贡献**：提供了完整的训练代码、Android 播放器 Demo 和量化复现脚本。

### 8. 不足与局限
*   **编解码器依赖**：目前高度依赖 H.264 的运动矢量提取，对于 HEVC (H.265) 或 AV1 的支持尚需完善（受限于移动端解码 API 的开放程度）。
*   **感知质量差距**：由于采用了残差预测而非子像素级光流变形，在处理复杂纹理和随机运动时，画面可能显得过于平滑，LPIPS 指标较差。
*   **B 帧限制**：在处理包含 B 帧的非受控视频流时，由于 MV 引用关系复杂，插帧覆盖率会下降（约 50% 的帧可插）。
*   **功耗挑战**：软件解码提取 MV 会带来额外的 CPU 功耗负担（30 分钟耗电约 16%）。

（完）
