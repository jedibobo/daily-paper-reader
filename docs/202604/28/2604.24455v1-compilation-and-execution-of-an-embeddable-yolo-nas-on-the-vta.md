---
title: Compilation and Execution of an Embeddable YOLO-NAS on the VTA
title_zh: 在 VTA 上编译与执行可嵌入式 YOLO-NAS
authors: "Anthony Faure-Gignoux, Kevin Delmas, Adrien Gauffriau, Claire Pagetti"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24455v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 在 VTA 上编译 YOLO-NAS 并实现自动化编译链
tldr: 扩展 VTA 编译链以支持 YOLO-NAS 在 FPGA 加速器上的部署，实现自动化编译与执行。
selection_source: fresh_fetch
motivation: 在 VTA 上编译 YOLO-NAS 并实现自动化编译链。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
在基于 FPGA 的加速器上部署复杂的卷积神经网络 (CNN) 是航空等安全关键领域的一个充满前景的发展方向。在之前的工作中，我们探索了通用张量加速器 (VTA)，并展示了其在航空电子应用中的适用性。为此，我们开发了一个初步的独立编译器，其设计考虑了认证需求。然而，该编译器仍存在一些局限性，本文克服了这些局限性。本文的贡献在于扩展并实现了 VTA 编译链的全自动化，从而支持完整的 CNN 编译以及更大规模的 CNN（其参数无法完全存放在片上存储器中）。通过成功编译并模拟执行 YOLO-NAS 目标检测模型，证明了该方法的有效性。

## Abstract
Deploying complex Convolutional Neural Networks (CNNs) on FPGA-based accelerators is a promising way forward for safety-critical domains such as aeronautics. In a previous work, we have explored the Versatile Tensor Accelerator (VTA) and showed its suitability for avionic applications. For that, we developed an initial stand-alone compiler designed with certification in mind. However, this compiler still suffers from some limitations that are overcome in this paper. The contributions consist in extending and fully automating the VTA compilation chain to allow complete CNN compilation and support larger CNNs (which parameters do not fit in the on-chip memory). The effectiveness is demonstrated by the successful compilation and simulated execution of a YOLO-NAS object detection model.

---

## 论文详细总结（自动生成）

以下是对论文《Compilation and Execution of an Embeddable YOLO-NAS on the VTA》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在航空航天等安全关键领域，将机器学习（ML）集成到嵌入式系统中具有重要意义，但 ML 模型巨大的计算需求与严格的硬件限制（如功耗、尺寸）之间存在矛盾。
*   **核心问题**：现有的开源 FPGA 加速器 VTA（Versatile Tensor Accelerator）虽然具有潜力，但其原始编译链依赖于 TVM，不符合航空软件认证标准（如 DO-178C）。此外，之前的独立编译器无法处理超过片上 SRAM 容量的大型神经网络层。
*   **整体含义**：本文旨在开发并形式化一个全自动、可认证的独立 VTA 编译器，使其能够处理如 YOLO-NAS 这样的大型工业级 CNN 模型，并解决内存溢出和层间自动化衔接问题。

### 2. 论文提出的方法论
*   **核心思想**：通过形式化 VTA 的原子操作，引入中间表示（VTA IR），并开发自动化的矩阵分区（Matrix Partitioning）策略，实现从 ONNX 模型到 VTA 二进制指令的端到端转换。
*   **关键技术细节**：
    *   **形式化定义**：对 `LOAD`、`STORE`、`GEMM` 和 `ALU` 操作进行了数学定义，明确了数据在 DRAM 与 SRAM 之间的块状（Block-based）传输与计算逻辑。
    *   **VTA IR**：使用 JSON 格式定义中间表示，描述矩阵维度、数据路径、加载逻辑及计算算子序列。
    *   **矩阵分区策略**：针对大型矩阵设计了四种启发式分区策略（Strategy 1-4），将无法一次性存入 SRAM 的运算分解为多个可执行的离线任务（Offloads）。
    *   **CPU-VTA 协同**：CPU 负责执行 VTA 不支持的浮点运算（如重缩放）、层间数据重塑（Reshape）以及调度 VTA 指令流。
*   **算法流程**：ONNX 解析 -> 拓扑排序 -> 转换为矩阵域（im2row） -> 生成 VTA IR -> 矩阵分区 -> 生成 VTA 二进制指令与 CPU 调度参数。

### 3. 实验设计
*   **数据集/场景**：主要针对目标检测任务，使用了量化后的 **YOLO-NAS** 模型（包含 146 个算子）。
*   **Benchmark**：
    *   **LeNet-5**：用于初步验证自动化流程。
    *   **ONNX Runtime**：作为结果正确性的初步对比。
    *   **Numpy 参考实现**：作为位精确（Bit-accurate）验证的最终基准。
*   **对比维度**：对比了四种不同的矩阵分区策略对指令数量的影响，以及编译后模型与原始 ONNX 模型的内存占用差异。

### 4. 资源与算力
*   **硬件平台**：目标硬件为 FPGA（如 UltraScale+），但论文中的实验主要在 **功能性 C++ 模拟器** 和 **CHISEL 周期精确模拟器** 上完成。
*   **算力说明**：文中未提及具体的 GPU 训练算力，因为研究重点在于**推理阶段的编译与执行**。编译 108 个 VTA IR 层大约耗时 2 分钟，生成了约 1080 万条指令。

### 5. 实验数量与充分性
*   **实验规模**：对 YOLO-NAS 的 108 个 VTA 兼容算子进行了全量编译测试，并对四种分区策略在不同矩阵形状下的表现进行了消融分析。
*   **充分性评价**：实验较为充分。作者不仅验证了大型模型的端到端执行，还通过位对比（Bit-wise comparison）证明了计算的准确性。通过不同形状矩阵的案例研究，客观展示了各分区策略的优劣。

### 6. 论文的主要结论与发现
*   **自动化可行性**：成功实现了工业级模型 YOLO-NAS 在 VTA 上的全自动编译与模拟执行。
*   **策略敏感性**：矩阵分区策略的选择对生成的指令数量有显著影响（最高可达数倍差异），且最优策略高度依赖于矩阵的形状（行/列比例）。
*   **精度一致性**：模拟器输出与 Numpy 数学参考实现达到了位精确的一致，证明了编译逻辑的正确性。

### 7. 优点
*   **可认证性**：脱离了复杂的 TVM 框架，采用独立编译器设计，更符合安全关键领域的认证需求。
*   **开源贡献**：扩展后的工具链已开源，促进了 VTA 生态的发展。
*   **处理大规模模型**：通过自动分区解决了硬件 SRAM 限制问题，使 VTA 能够运行超出其物理内存容量的层。

### 8. 不足与局限
*   **内存开销**：由于 VTA 硬件限制，偏置（Bias）向量被扩展为矩阵存储，导致内存占用显著增加（例如 YOLO-NAS 的偏置占用从 33KB 激增至 297MB）。
*   **CPU 瓶颈**：大量非量化算子（如重缩放、重塑）仍需在 CPU 上运行，导致模拟执行中 CPU 处理时间占主导。
*   **性能评估局限**：目前的评估主要基于指令计数，而非真实的硬件执行周期。指令数最少并不一定代表延迟最低，需进一步结合周期精确模拟。
*   **硬件验证缺失**：论文主要基于模拟器，尚未在真实的 UltraScale+ FPGA 硬件上进行大规模性能实测。

（完）
