---
title: "QNAS: A Neural Architecture Search Framework for Accurate and Efficient Quantum Neural Networks"
title_zh: QNAS：一种用于构建准确且高效量子神经网络的神经架构搜索框架
authors: "Kooshan Maleki, Alberto Marchisio, Muhammad Shafique"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07013v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 量子网络的硬件感知神经架构搜索
tldr: QNAS是一个针对混合量子-经典神经网络（HQNN）设计的神经架构搜索框架，旨在解决NISQ硬件上的准确性与部署效率平衡问题。该框架通过训练共享参数的SuperCircuit，并利用NSGA-II算法同时优化验证误差、运行时间成本及电路切割开销。实验表明，QNAS能自动发现高效的架构，在MNIST等数据集上实现了高精度与低资源消耗的平衡，为量子神经网络的实际部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有的量子架构搜索方法往往忽略了电路切割带来的指数级开销，且难以在有限的量子比特资源下平衡模型精度与硬件部署效率。
method: 提出QNAS框架，通过SuperCircuit共享参数，并采用多目标遗传算法NSGA-II联合优化验证误差、运行时间代理指标和电路切割子电路数量。
result: "在MNIST、Fashion-MNIST和Iris基准测试中，QNAS发现了在精度、效率和切割开销之间具有良好权衡的Pareto前沿架构，最高实现了100%的验证准确率。"
conclusion: QNAS能够自动识别关键设计要素（如嵌入方式和纠缠模式），为在当前受限的量子硬件上设计高效且准确的量子神经网络提供了有效指导。
---

## 摘要
设计既准确又能在 NISQ 硬件上部署的量子神经网络 (QNN) 具有挑战性。手工设计的 ansatz 必须平衡表达能力、可训练性和资源消耗，而有限的量子比特通常需要进行电路切割 (circuit cutting)。现有的量子架构搜索方法主要优化准确率，仅启发式地控制量子资源，且大多忽略了电路切割带来的指数级开销。我们提出了 QNAS，这是一个为混合量子-经典神经网络 (HQNN) 统一了硬件感知评估、多目标优化和切割开销感知的神经架构搜索框架。QNAS 训练一个共享参数的超电路 (SuperCircuit)，并使用 NSGA-II 联合优化三个目标：(i) 验证误差，(ii) 衡量实际运行评估时间的运行成本代理指标，以及 (iii) 在目标量子比特预算下估计的子电路数量。QNAS 在经过几个 epoch 的训练后评估候选 HQNN，并发现清晰的帕累托前沿 (Pareto fronts)，揭示了准确率、效率和切割开销之间的权衡。在 MNIST、Fashion-MNIST 和 Iris 基准测试中，我们观察到嵌入类型和 CNOT 模式选择显著影响准确率和效率，其中 angle-y 嵌入和稀疏纠缠模式在图像数据集上优于其他配置，而振幅嵌入 (amplitude embedding) 在表格数据 (Iris) 上表现出色。在 MNIST 上，最佳架构通过紧凑的 8 量子比特、2 层电路实现了 97.16% 的测试准确率；在更具挑战性的 Fashion-MNIST 上，通过 5 量子比特、2 层电路实现了 87.38% 的准确率；在 Iris 上，通过 4 量子比特、2 层电路实现了 100% 的验证准确率。QNAS 在搜索过程中自动呈现这些设计见解，引导从业者构建在准确率、资源效率和当前硬件上的实际可部署性之间取得平衡的架构。

## Abstract
Designing quantum neural networks (QNNs) that are both accurate and deployable on NISQ hardware is challenging. Handcrafted ansatze must balance expressivity, trainability, and resource use, while limited qubits often necessitate circuit cutting. Existing quantum architecture search methods primarily optimize accuracy while only heuristically controlling quantum and mostly ignore the exponential overhead of circuit cutting. We introduce QNAS, a neural architecture search framework that unifies hardware aware evaluation, multi objective optimization, and cutting overhead awareness for hybrid quantum classical neural networks (HQNNs). QNAS trains a shared parameter SuperCircuit and uses NSGA-II to optimize three objectives jointly: (i) validation error, (ii) a runtime cost proxy measuring wall clock evaluation time, and (iii) the estimated number of subcircuits under a target qubit budget. QNAS evaluates candidate HQNNs under a few epochs of training and discovers clear Pareto fronts that reveal tradeoffs between accuracy, efficiency, and cutting overhead. Across MNIST, Fashion-MNIST, and Iris benchmarks, we observe that embedding type and CNOT mode selection significantly impact both accuracy and efficiency, with angle-y embedding and sparse entangling patterns outperforming other configurations on image datasets, and amplitude embedding excelling on tabular data (Iris). On MNIST, the best architecture achieves 97.16% test accuracy with a compact 8 qubit, 2 layer circuit; on the more challenging Fashion-MNIST, 87.38% with a 5 qubit, 2 layer circuit; and on Iris, 100% validation accuracy with a 4 qubit, 2 layer circuit. QNAS surfaces these design insights automatically during search, guiding practitioners toward architectures that balance accuracy, resource efficiency, and practical deployability on current hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《QNAS: A Neural Architecture Search Framework for Accurate and Efficient Quantum Neural Networks》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在近期的量子计算（NISQ）时代，设计既准确又能在实际硬件上部署的量子神经网络（QNN）面临巨大挑战。
*   **研究动机**：手动设计量子电路（Ansatz）耗时耗力，且容易遇到梯度消失（贫瘠高原）等问题。
*   **核心痛点**：现有的量子架构搜索（QAS）方法大多只关注准确率，而忽略了硬件限制。特别是当电路规模超过硬件量子比特数时，必须使用**电路切割（Circuit Cutting）**技术，但这会带来指数级的计算开销。
*   **论文目标**：提出一个名为 **QNAS** 的框架，旨在自动搜索出在准确率、运行效率和电路切割开销之间取得最佳平衡的混合量子-经典神经网络（HQNN）。

### 2. 方法论
QNAS 结合了硬件感知评估、多目标优化和切割开销感知技术：
*   **核心思想**：利用“一次性（One-shot）”超电路（SuperCircuit）共享参数，避免为每个候选架构从头训练，并使用多目标遗传算法（NSGA-II）进行搜索。
*   **搜索空间**：涵盖了嵌入类型（Angle-X/Y/Z, Amplitude）、量子比特数（2-8）、电路深度（1-4）、纠缠范围、CNOT 模式（全纠缠、奇数/偶数纠缠、无纠缠）以及学习率。
*   **多目标优化函数**：
    1.  **$F_1$ (验证误差)**：$1 - \text{Accuracy}_{val}$。
    2.  **$F_2$ (运行成本代理)**：GPU 同步后的每样本平均墙钟时间。
    3.  **$F_3$ (切割开销)**：在目标量子比特预算（$Q_{target}$）下，估计所需的子电路数量。
*   **算法流程**：首先通过少量 epoch 训练超电路以快速评估候选者，利用 NSGA-II 筛选出帕累托前沿（Pareto Front）上的最优架构，最后对选定架构进行全量训练。

### 3. 实验设计
*   **数据集**：
    *   **MNIST**：经典手写数字识别（10类）。
    *   **Fashion-MNIST**：更具挑战性的衣物分类（10类）。
    *   **Iris**：小规模表格数据分类（3类）。
*   **基准测试（Benchmark）**：
    *   对比了“包含 $F_3$（切割开销）优化”与“不包含 $F_3$ 优化”的搜索结果。
    *   分析了不同嵌入方式和纠缠模式对性能的影响。
*   **硬件模拟**：使用 Qiskit 的 `fake_melbourne` 等噪声模型后端进行转译（Transpilation）分析，评估实际硬件上的门数量和深度。

### 4. 资源与算力
*   **算力平台**：使用了支持 GPU 加速的量子模拟后端（`lightning.gpu`）。
*   **训练时长**：
    *   **MNIST**：总搜索时间约 **71.19 小时**（6代，72个候选架构）。
    *   **Fashion-MNIST**：总搜索时间约 **65.51 小时**。
    *   **Iris**：总搜索时间约 **0.6 小时**。
*   **具体硬件型号**：文中未明确指出具体的 GPU 型号（如 A100 或 H100），但提到了使用 GPU 同步时间作为成本度量。

### 5. 实验数量与充分性
*   **实验规模**：针对三个不同难度的任务进行了完整的搜索流程，总计评估了超过 250 个候选架构。
*   **充分性验证**：
    *   **相关性分析**：专门做了一组实验证明“训练 2 个 epoch 的准确率”与“最终准确率”之间存在强相关性（相关系数 > 0.9），验证了快速评估的有效性。
    *   **消融对比**：通过对比实验证明，加入 $F_3$ 目标能显著降低 62.8% 的切割开销，同时保持准确率。
*   **客观性**：实验涵盖了图像和表格两种数据模态，并考虑了噪声模型和转译开销，具有较强的客观性。

### 6. 主要结论与发现
*   **设计见解**：对于图像数据，**Angle-Y 嵌入**配合**稀疏纠缠模式**（如 none-odd）表现最优；而对于表格数据，**振幅嵌入（Amplitude Embedding）**更具优势。
*   **性能表现**：
    *   MNIST：8比特2层电路实现 **97.16%** 测试准确率。
    *   Fashion-MNIST：5比特2层电路实现 **87.38%** 准确率。
    *   Iris：4比特2层电路实现 **100%** 验证准确率。
*   **效率提升**：QNAS 能够自动避开“贫瘠高原”区域（如全纠缠模式在图像任务中表现极差），并发现那些在有限量子比特硬件上易于部署的紧凑架构。

### 7. 优点
*   **部署导向**：首次在 QAS 中显式地将“电路切割开销”作为优化目标，解决了大规模 QNN 在小规模硬件上运行的实际痛点。
*   **多目标平衡**：通过帕累托前沿提供了多种选择，用户可以根据实际需求在“高精度”和“低延迟”之间做权衡。
*   **开源贡献**：提供了公开的代码仓库，便于社区复现和进一步研究。

### 8. 不足与局限
*   **硬件验证缺失**：虽然使用了带噪声的模拟器（Fake Backends），但尚未在真实的量子处理器（QPU）上进行最终测试。
*   **任务范围**：目前仅限于监督学习分类任务，未涉及生成模型或强化学习等领域。
*   **搜索策略限制**：虽然使用了 NSGA-II，但对于极大规模的搜索空间，进化算法的收敛速度可能仍是一个瓶颈。

（完）
