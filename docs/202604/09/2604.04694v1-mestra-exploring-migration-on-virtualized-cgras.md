---
title: "Mestra: Exploring Migration on Virtualized CGRAs"
title_zh: Mestra：在虚拟化 CGRA 上探索迁移技术
authors: "Agamemnon Kyriazis, Panagiotis Miliadis, Dimitris Theodoropoulos, Nectarios Koziris, Dionisios Pnevmatikatos"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04694v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 高效利用CGRA阵列及动态调度
tldr: 针对现代CGRA在单应用下利用率低及多租户环境下碎片化严重的问题，本文提出了Mestra系统。该系统支持动态调度与资源分配，并引入实时内核迁移机制以解决碎片化。实验表明，Mestra显著提升了工作负载完成时间并降低了尾延迟，且硬件开销极低，为高效利用大规模CGRA资源提供了有效方案。
selection_source: carryover_cache
motivation: 现有的CGRA在多租户环境下由于内核乱序完成导致严重的织物碎片化，且缺乏有效的实时迁移机制。
method: 提出了Mestra端到端系统，通过支持动态资源分配和有状态的内核实时迁移来实现虚拟化和去碎片化。
result: "空间共享使工作负载完成时间缩短了高达70.48%，实时迁移将尾延迟降低了29.60%，而硬件开销仅为0.13%。"
conclusion: 多租户技术与实时迁移机制是提升CGRA利用率的关键，能以极小的硬件代价显著优化碎片化环境下的系统性能。
---

## 摘要
随着现代粗粒度可重构架构（CGRA）规模的扩大，单个应用程序对可用结构的有效利用变得越来越困难。现有的 CGRA 映射器要么无法充分利用可用结构，要么依赖于适应性有限的僵化静态代码转换。多租户 CGRA 已成为提高硬件利用率的一种极具前景的解决方案，但目前的尝试未能解决结构碎片化和实时迁移等关键挑战。为了填补这一空白，我们提出了 Mestra，这是一个用于 CGRA 多租户的端到端系统，支持共享环境中的动态调度和资源分配。Mestra 通过支持无状态和有状态的实时内核迁移作为去碎片化机制，解决了由内核无序完成导致的结构碎片化问题。我们在 Alveo-U280 数据中心级 FPGA 卡上评估了我们的解决方案，并报告了面积、频率和功耗。性能评估使用了 PolyBench 基准测试套件中的例程以及源自常见机器学习算子的内核。结果表明，跨多个用户的可用结构空间共享可将工作负载的完工时间（makespan）缩短高达 70.48%，而实时内核迁移可将碎片化布局上的尾延迟降低高达 29.60%。虚拟化和有状态迁移所需的定制紧耦合控制器和回读路径在每个区域引入了 0.13% 的 LUT 开销。我们的评估表明，多租户对于高效的 CGRA 利用至关重要，而实时内核迁移可以通过以极低的硬件成本回收碎片空间来进一步提高性能。

## Abstract
As modern Coarse Grain Reconfigurable Arrays (CGRAs) grow in size, efficient utilization of the available fabric by a single application becomes increasingly difficult. Existing CGRA mappers either fail to utilize the available fabric or rely on rigid static code transformations with limited adaptability. Multi-tenant CGRAs have emerged as a promising solution to increase hardware utilization, but current attempts fail to address key challenges such as fabric fragmentation and live migration. To address this gap, we present Mestra, an end-to-end system for CGRA multi-tenancy that supports dynamic scheduling and resource allocation in a shared environment. Mestra addresses fabric fragmentation caused by kernels completing out of order by supporting both stateless and stateful live kernel migration as a de-fragmentation mechanism. We assess our solution on an Alveo-U280 data-center-grade FPGA card, reporting area, frequency, and power. Performance is evaluated using routines from the PolyBench benchmark suite and kernels derived from common machine learning operators. Results show that spatial sharing of the available fabric across multiple users improves workload makespan by up to 70.48%, while live kernel migration reduces tail latency on fragmented layouts by up to 29.60%. The custom tightly coupled controller and read-back paths required for virtualization and stateful migration introduce a LUT cost of 0.13% per region. Our evaluation reveals that multi-tenancy is important for efficient CGRA utilization, and live kernel migration can further improve performance by recovering fragmented space with minimal hardware cost.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Mestra** 的系统，旨在解决现代粗粒度可重构架构（CGRA）在多租户环境下的资源利用率和碎片化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着 CGRA 规模不断扩大，单个应用程序往往难以填满整个硬件阵列（利用率低）。虽然引入多租户（Multi-tenancy）共享机制可以提高利用率，但由于不同任务的执行时间不一，动态分配和释放资源会导致硬件织物出现“碎片化”——即虽然总剩余资源足够，但物理上不连续，导致大任务无法加载。
*   **研究背景**：现有的 CGRA 编译器和映射器多针对静态环境，缺乏对运行时动态资源管理和“去碎片化”机制（如实时迁移）的支持。

### 2. 方法论
Mestra 是一个支持虚拟化和动态调度的端到端系统，其核心技术包括：
*   **虚拟化区域（vCGRA Regions）**：将 CGRA 划分为同构的逻辑区域，支持多个区域合并以运行大型内核。
*   **紧耦合控制器**：每个区域配备一个控制器，通过标准接口（FFA-RF）处理 `CONFIGURE`（配置）、`EXECUTE`（执行）、`HALT`（挂起）和 `SNAPSHOT`（快照）等指令，减少主机通信延迟。
*   **实时内核迁移机制**：
    *   **无状态迁移（Stateless）**：挂起内核并在新位置重启。优点是简单，缺点是丢失已完成的进度（需重新计算）。
    *   **有状态迁移（Stateful）**：通过读取 PE 内部寄存器和存储器状态生成快照，并在新区域恢复。这是本文的核心创新点，确保了执行的连续性和正确性。
*   **管理程序（Hypervisor）**：在主机端运行，负责动态资源分配。当检测到碎片化阻碍任务放置时，触发基于贪心压缩算法的去碎片化流程。

### 3. 实验设计
*   **硬件平台**：在 Xilinx Alveo-U280 数据中心级 FPGA 卡上实现。
*   **基准测试（Benchmarks）**：
    *   **PolyBench 集合**：包括 GEMM、2MM、MVT、Covariance 等。
    *   **机器学习算子**：ReLU、SAXPY 等。
*   **对比方法**：
    *   **Monolithic（单任务模式）**：传统的独占式执行。
    *   **Tiled（多租户模式）**：支持空间共享但不支持迁移。
    *   **Stateless/Stateful Migration**：在 Tiled 基础上增加迁移机制。

### 4. 资源与算力
*   **硬件参数**：实验系统配置为 4×4 的区域网格，共 240 个处理单元（PEs）。
*   **运行频率**：150 MHz。
*   **功耗**：整片功耗约为 3.3 W。
*   **开销**：虚拟化和迁移逻辑的硬件成本极低，每个区域仅增加 0.13% 的 LUT 资源。
*   **软件环境**：使用 Python 编写的模型级模拟器进行大规模碎片化场景模拟。

### 5. 实验数量与充分性
*   **实验规模**：通过硬件仿真评估了包含 64 个随机任务的工作负载；利用 Python 模拟器生成了大量高碎片化强度的测试用例。
*   **充分性**：实验涵盖了性能指标（Makespan、周转时间、尾延迟）、硬件面积开销、功耗以及迁移次数与性能增益的相关性分析。实验设计较为客观，通过对比不同阈值下的迁移策略，验证了有状态迁移的优越性。

### 6. 主要结论与发现
*   **性能提升**：空间共享（多租户）相比单任务模式，可将工作负载完成时间（Makespan）缩短高达 **70.48%**。
*   **去碎片化效果**：在严重碎片化的布局中，有状态实时迁移将 95% 尾延迟（Tail Latency）降低了 **29.60%**。
*   **迁移策略**：有状态迁移虽然单次成本略高于无状态迁移，但由于保留了计算进度，在整体系统效率上远优于无状态模式。
*   **相关性**：研究发现迁移次数与性能增益之间存在正相关，但并非线性，迁移的“质量”（时机和对象选择）比“数量”更重要。

### 7. 优点（亮点）
*   **端到端实现**：不仅有理论模型，还在真实 FPGA 硬件上实现了完整的控制路径和回读路径。
*   **极低开销**：证明了在 CGRA 上实现复杂的有状态迁移仅需极少的额外硬件资源（0.13% LUT）。
*   **灵活性**：支持区域合并，能够适应不同形状和大小的内核需求。

### 8. 不足与局限
*   **内存瓶颈**：实验观察到多内核并行执行时会产生内存带宽竞争，导致单个内核执行时间变长（虽然整体吞吐量提升了）。
*   **调度算法简单**：目前采用的是贪心压缩算法，可能不是最优的迁移策略。
*   **基准测试范围**：虽然涵盖了 PolyBench 和 ML 算子，但对于更复杂、非规则数据流的应用支持尚待验证。
*   **扩展性限制**：目前的区域合并仅支持矩形形状，限制了某些非规则映射的放置。

（完）
