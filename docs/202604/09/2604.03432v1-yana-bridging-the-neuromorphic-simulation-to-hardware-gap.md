---
title: "YANA: Bridging the Neuromorphic Simulation-to-Hardware Gap"
title_zh: YANA：弥合神经形态模拟与硬件之间的鸿沟
authors: "Brian Pachideh, Sven Nitzsche, Moritz Neher, Jann Krausse, Carmen Weigelt, Klaus Knobloch, Victor Pazmino Betancourt, Juergen Becker"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03432v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于FPGA的SNN加速器及软硬件协同设计框架
tldr: 针对脉冲神经网络（SNN）在仿真与硬件部署之间存在的鸿沟，本文提出了YANA，一个基于FPGA的开源数字SNN加速器。YANA采用五级事件驱动流水线，充分利用时空稀疏性并支持任意网络拓扑。通过在AMD Kria平台上的验证，该框架实现了高效的神经元更新和推理时间与稀疏性的近线性缩放，为SNN的训练、优化和部署提供了端到端解决方案。
selection_source: fresh_fetch
motivation: 现有神经形态硬件的匮乏阻碍了SNN算法创新和软硬件协同设计，导致仿真与实际硬件部署之间存在巨大差距。
method: 提出了一种基于FPGA的五级事件驱动处理流水线，利用查找表实现高效神经元更新，并通过点对点连接支持任意SNN拓扑。
result: 在SHD数据集上展示了推理时间随空间和时间稀疏性近线性缩放的能力，且在低资源占用下支持大量突触和神经元。
conclusion: YANA作为一个开源框架，通过集成神经形态中间表示（NIR），为弥合SNN仿真与硬件之间的差距提供了可扩展且易用的工具。
---

## 摘要
脉冲神经网络 (SNN) 在需要严格功耗约束下实时处理时间稀疏数据流的应用中，相比传统人工神经网络 (ANN) 具有显著优势——这一概念被称为“神经形态优势”。然而，神经形态硬件的有限可用性造成了巨大的模拟与硬件之间的鸿沟，阻碍了算法创新、软硬件协同设计以及成熟开源生态系统的发展。为了应对这一挑战，我们推出了 YANA (Yet Another Neuromorphic Accelerator)，这是一款基于 FPGA 的数字 SNN 加速器，旨在通过为神经形态计算提供易于获取的软硬件框架来弥合这一鸿沟。YANA 实现了五级事件驱动处理流水线，在通过点对点神经元连接支持任意 SNN 拓扑的同时，充分利用了时间与空间稀疏性。该架构具有输入预处理方案，可在无缓冲区溢出风险的情况下保持每个周期处理一个事件的稳定速度，并利用查找表进行漏电流计算，实现了硬件高效的事件驱动神经元更新。我们通过在 Spiking Heidelberg Digits 数据集上的实验展示了 YANA 的稀疏性利用能力，结果表明推理时间随空间和时间稀疏度水平呈近线性缩放。在易于获取的 AMD Kria KR260 平台上部署时，单个 YANA 内核使用 740 个 LUT、918 个寄存器、7 个 BRAM 和 24 个 URAM，支持多达 $2^{17}$ 个突触和 $2^{10}$ 个神经元。我们将 YANA 框架作为开源项目发布，提供了一套用于训练、优化和部署 SNN 的端到端解决方案，并通过神经形态中间表示 (NIR) 与现有的神经形态计算工具集成。

## Abstract
Spiking Neural Networks (SNNs) promise significant advantages over conventional Artificial Neural Networks (ANNs) for applications requiring real-time processing of temporally sparse data streams under strict power constraints -- a concept known as the Neuromorphic Advantage. However, the limited availability of neuromorphic hardware creates a substantial simulation-to-hardware gap that impedes algorithmic innovation, hardware-software co-design, and the development of mature open-source ecosystems. To address this challenge, we introduce Yet Another Neuromorphic Accelerator (YANA), an FPGA-based digital SNN accelerator designed to bridge this gap by providing an accessible hardware and software framework for neuromorphic computing. YANA implements a five-stage, event-driven processing pipeline that fully exploits temporal and spatial sparsity while supporting arbitrary SNN topologies through point-to-point neuron connections. The architecture features an input preprocessing scheme that maintains steady event processing at one event per cycle without buffer overflow risks, and implements hardware-efficient event-driven neuron updates using lookup tables for leak calculations. We demonstrate YANA's sparsity exploitation capabilities through experiments on the Spiking Heidelberg Digits dataset, showing near-linear scaling of inference time with both spatial and temporal sparsity levels. Deployed on the accessible AMD Kria KR260 platform, a single YANA core utilizes 740 LUTs, 918 registers, 7 BRAMS and 24 URAMs, supporting up to $2^{17}$ synapses and $2^{10}$ neurons. We release the YANA framework as an open-source project, providing an end-to-end solution for training, optimizing, and deploying SNNs that integrates with existing neuromorphic computing tools through the Neuromorphic Intermediate Representation (NIR).

---

## 论文详细总结（自动生成）

这是一份关于论文《YANA: Bridging the Neuromorphic Simulation-to-Hardware Gap》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：脉冲神经网络（SNN）虽然在理论上具有低功耗和实时处理的“神经形态优势”，但由于缺乏易于获取的硬件平台，导致了**“模拟到硬件的鸿沟”**（Simulation-to-Hardware Gap）。
*   **研究动机**：
    *   现有的神经形态芯片（如 Loihi）大多不对公众开放，限制了算法创新。
    *   现有的 FPGA 加速器多采用时钟驱动（Clock-driven）而非事件驱动（Event-driven），且往往仅支持顺序层级结构，无法处理复杂的递归或任意拓扑的 SNN 图。
    *   缺乏一个集训练、优化到部署于一体的开源端到端生态系统。

### 2. 方法论：YANA 架构与软件框架
*   **核心思想**：设计一个基于 FPGA 的数字 SNN 模拟器，采用全事件驱动的处理流水线，支持任意点对点连接，并提供完整的开源软件栈。
*   **关键技术细节**：
    *   **五级事件驱动流水线**：包括输入（RX）、突触（Synapse）、神经元（Neuron）、轴突（Axon）和输出（TX）五个阶段。系统仅在事件到达时处理，否则保持空闲。
    *   **输入预处理方案**：通过预处理将输入事件直接转化为神经元的权重和（Weight Sums），避免了原始事件的缓冲，实现了每周期处理一个事件的稳定吞吐量。
    *   **硬件高效的神经元更新**：采用前向欧拉法（Forward-Euler）实现 LIF 神经元模型。为了节省计算资源，利用**查找表（LUT）**预计算漏电流（Leak）相关的指数项，支持延迟更新（即仅在神经元接收到脉冲时才计算其状态变化）。
    *   **任意拓扑支持**：通过点对点连接表实现，每个事件包包含目标核心、目标神经元和目标突触的地址，支持权重重用和任意剪枝。
*   **软件框架**：
    *   **YANA train**：基于 Norse 库，支持硬件感知的量化训练。
    *   **YANA deploy**：利用神经形态中间表示（NIR）将模型映射到硬件内存布局。
    *   **YANA runtime**：在 SoC 的处理系统（PS）上运行，通过 PYNQ 库控制 FPGA 逻辑。

### 3. 实验设计
*   **数据集**：使用 **Spiking Heidelberg Digits (SHD)** 语音数据集。
*   **实验场景**：
    *   **稀疏性缩放实验**：通过均匀丢弃输入事件（增加时间稀疏性）和对权重进行剪枝（增加空间稀疏性），观察推理延迟的变化。
*   **对比基准**：主要对比不同稀疏度下的推理时间，验证架构对稀疏性的利用效率。

### 4. 资源与算力
*   **硬件平台**：AMD Kria KR260 机器人开发套件（搭载 Zynq UltraScale+ SoC FPGA）。
*   **资源占用（单个 YANA 内核）**：
    *   740 个 LUT，918 个寄存器。
    *   7 个 BRAM 和 24 个 URAM。
    *   支持多达 $2^{17}$（约 13.1 万）个突触和 $2^{10}$（1024）个神经元。
*   **算力说明**：实验中硬件时钟频率为 100 MHz。文中未详细列出训练阶段的 GPU 算力，重点在于硬件部署后的推理性能。

### 5. 实验数量与充分性
*   **实验规模**：针对空间稀疏性（0.0 到 0.9）和时间稀疏性（0.85 到 0.96）进行了多组交叉实验，每组实验取 20 个样本的平均推理时间。
*   **充分性评价**：实验充分验证了 YANA 架构的核心特性——**推理时间随稀疏性近线性缩放**。虽然实验主要集中在 SHD 数据集上，但对于验证“事件驱动架构优势”这一目的而言是客观且公平的。

### 6. 主要结论与发现
*   **稀疏性红利**：YANA 能够有效利用 SNN 的时空稀疏性。在 SHD 任务中，推理时间从 16ms（低稀疏度）缩短至 1ms（高稀疏度），远快于数据集本身的实时时长（500ms-1000ms）。
*   **性能缩放**：推理延迟与非零权重数量和输入脉冲数量呈近线性关系，证明了事件驱动流水线的有效性。
*   **开源贡献**：成功构建并发布了一个能够与现有神经形态生态系统（如 NIR）互操作的开源工具链。

### 7. 优点
*   **灵活性高**：支持点对点连接，能够部署任意结构的 SNN，不局限于全连接层。
*   **资源效率**：通过时间复用逻辑和 LUT 优化，在极低的 FPGA 资源占用下支持大规模突触。
*   **易用性**：集成了 NIR 中间表示，使得从主流深度学习框架（如 PyTorch）到硬件的迁移变得简单。

### 8. 不足与局限
*   **层类型支持有限**：目前主要支持全连接层，尚未完全实现卷积层等结构化连接的优化映射。
*   **功耗验证缺失**：虽然理论上事件驱动更省电，但文中未给出在 FPGA 上的实际功耗测量数据（如电流、电压监测）。
*   **多核扩展性**：目前的实验主要基于单核部署，虽然设计上支持多核，但尚未展示大规模多核互联（NoC）下的性能表现。

（完）
