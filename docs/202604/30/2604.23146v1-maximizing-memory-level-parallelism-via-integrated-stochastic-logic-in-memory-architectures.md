---
title: Maximizing Memory-Level Parallelism via Integrated Stochastic Logic-in-Memory Architectures
title_zh: 通过集成随机存内逻辑架构最大化存储级并行性
authors: "Farzad Razi, Mehran Moghadam, Sercan Aygun, M. Hassan Najafi, Marc Riedel"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23146v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对数据密集型工作负载的存内逻辑架构
tldr: 针对现代高性能架构中数据迁移带来的延迟和能耗挑战，本文提出一种基于磁隧道结（MTJ）的并行内存随机计算（SC）架构。该架构利用MTJ器件的固有随机性实现二进制到概率比特流的并行转换，无需外部随机数生成电路。通过在存储阵列中集成并行随机算术单元，实现了高效的算术与超越函数运算。该设计将存储、比特流生成与计算统一于内存织物中，显著提升了内存级并行度并最小化了数据迁移。
selection_source: fresh_fetch
motivation: 传统冯·诺依曼架构在处理数据密集型任务时面临严重的内存带宽瓶颈和高昂的数据迁移开销。
method: 提出一种集成逻辑在内存（LIM）功能的MTJ架构，利用器件特性实现端到端的随机计算流水线。
result: 实现了无需外部随机数生成器的全并行比特流转换，并能高效执行复杂的算术和超越函数运算。
conclusion: 该架构通过紧耦合存储与计算，在降低硬件复杂度的同时极大化了内存级并行度，是解决数据移动问题的有效方案。
---

## 摘要
随着单核性能提升放缓以及高度数据密集型工作负载的兴起，当今的高性能架构日益受到数据移动延迟和能耗开销的限制。存内架构已成为传统冯·诺依曼系统的补充方案，通过缓解内存带宽瓶颈、利用大规模并发性以及减少内存与处理单元之间过多的数据移动来发挥作用。本研究提出了一种并行存内随机计算（SC）架构，该架构在增强了存内逻辑（LIM）功能的磁隧道结（MTJ）存储器中实现了端到端计算流水线。通过利用 MTJ 器件固有的随机性和读写特性，所提出的架构能够将二进制操作数全并行且确定性地转换为概率比特流，从而消除了对高能耗外部随机数生成电路的需求。这些比特流由直接集成在存储阵列中的并行随机算术单元处理，以极低的硬件复杂度和固有的噪声容限高效实现核心算术和超越函数。生成的随机输出既可以作为未来随机处理的输入重复使用，也可以利用并行累加机制转换回二进制形式并存储在 MTJ 存储器中。通过将数据存储、比特流生成和计算紧密集成在统一的存内织构中，所设计的方案在最大化存储级并行性的同时，显著减少了数据移动。

## Abstract
Today's high-performance architectures are increasingly constrained by data movement latency and energy overhead, as the slowdown of single-core performance scaling coincides with the rise of highly data-intensive workloads. In-memory architectures have emerged as a complementary solution to conventional von Neumann systems by alleviating memory bandwidth bottlenecks, exploiting massive concurrency, and mitigating excessive data movement between memory and processing units. This study proposes a parallel in-memory stochastic computing (SC) architecture that implements an end-to-end computation pipeline within Magnetic Tunnel Junction (MTJ)-based memory augmented with logic-in-memory (LIM) capabilities. By leveraging the inherent stochasticity and write-read characteristics of MTJ devices, the proposed architecture enables a fully parallel and deterministic conversion of binary operands into probabilistic bit-streams, eliminating the need for energy-intensive external random number generation circuitry. These bit-streams are processed by parallel stochastic arithmetic units integrated directly within the memory arrays to efficiently implement core arithmetic and transcendental functions with minimal hardware complexity and inherent noise tolerance. The resulting stochastic outputs can be either reused as an input of future stochastic processing or converted back to binary form using parallel accumulation mechanisms and stored in the MTJ memory. By tightly integrating data storage, bit-stream generation, and computation within a unified in-memory fabric, the proposed design maximizes memory-level parallelism while substantially minimizing data movement.

---

## 论文详细总结（自动生成）

这是一份关于论文《Maximizing Memory-Level Parallelism via Integrated Stochastic Logic-in-Memory Architectures》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代高性能计算架构面临“冯·诺依曼瓶颈”，即处理单元与存储器之间频繁的数据移动导致了严重的延迟和能耗开销。
*   **研究背景**：
    *   **存内计算（IMC）**：通过在存储阵列内执行计算来减少数据迁移，但复杂的二进制逻辑在内存中实现成本极高。
    *   **随机计算（SC）**：将数值表示为概率比特流，仅需极简单的逻辑门（如AND门实现乘法）即可完成运算，且具有天然的容错性。
    *   **传统SC的局限**：传统的SC通常是**串行**处理的，处理 $N$ 位比特流需要 $N$ 个时钟周期，导致吞吐量低、延迟大。
*   **研究动机**：利用磁隧道结（MTJ）存储器的非易失性和逻辑功能，结合SC的简洁性，通过**空间并行化**（将时间序列转为空间向量）来彻底解决SC的延迟问题。

### 2. 论文提出的方法论
核心思想是构建一个基于MTJ的端到端并行存内随机计算流水线，主要包含以下关键技术：
*   **确定性并行比特流生成**：利用MTJ的读写特性和低差异序列（Low-discrepancy sequences）映射机制，在存储阵列内直接将二进制操作数转换为并行“比特束”（Bit-bundles），无需高能耗的外部随机数生成器（RNG）。
*   **并行随机计算单元（SCU）**：
    *   将传统的“时间串行”计算转变为“空间并行”计算。
    *   利用逻辑在内存（LIM）结构，通过控制字线和感测放大器（SA），在单周期内对整行存储单元执行逻辑运算（如AND, OR, XOR）。
    *   算术运算（乘法、加法等）的复杂度从 $O(N)$ 降至 $O(1)$。
*   **超越函数库实现**：通过截断的麦克劳林级数（Maclaurin-series）展开，将复杂的超越函数（sin, cos, exp, tanh, sigmoid等）分解为纯组合逻辑链，实现在存内的高效并行评估。
*   **并行SC到二进制转换**：采用基于XOR和AND逻辑的并行累加机制，将计算结果快速转换回二进制格式并存回MTJ。

### 3. 实验设计
*   **实验场景**：
    1.  **算术与超越函数评估**：测试基本运算及8种复杂超越函数的精度与性能。
    2.  **容错性测试**：在0%到30%的注入噪声水平下观察计算精度。
    3.  **案例研究（PIM-PIM）**：针对256x256图像进行色调映射（Tone-mapping）处理。
*   **Benchmark（对比方法）**：
    *   **串行SC实现**：传统的逐位处理随机计算。
    *   **SOTA IMC架构**：包括基于ReRAM的分布式算术架构、基于SRAM的CRAM、基于Memristor的MAGIC逻辑、以及基于DRAM的CORDIC架构等。
*   **工具链**：使用 MATLAB 进行算法级精度模拟；使用 HSPICE 结合 PTM FinFET 模型和 MTJ 器件模型进行电路级仿真。

### 4. 资源与算力
*   **算力说明**：论文未提及使用 GPU 或大规模集群训练，因为该研究属于**硬件架构与电路设计**领域。
*   **仿真环境**：主要依赖于电路仿真软件（HSPICE）和数学建模工具（MATLAB）。电路参数基于 0.9V 供电电压和混合 CMOS-磁性技术模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 100,000 次蒙特卡洛模拟以确保精度分析的统计意义。
    *   覆盖了从基础算术到复杂非线性函数的全谱系测试。
    *   对比了 10 余种不同的存内计算最前沿方案。
*   **充分性评价**：实验设计非常充分。不仅涵盖了底层的电路参数（延迟、功耗、PDP），还包括了高层的应用案例（图像处理），并深入探讨了在极端噪声环境下的鲁棒性，论证过程客观且具有说服力。

### 6. 论文的主要结论与发现
*   **性能飞跃**：相比串行SC实现，该架构实现了 **22x 至 64x** 的加速。
*   **延迟优势**：相比现有的存内计算方法，计算延迟降低了**三个数量级**以上（从数千周期缩短至 1-3 个周期）。
*   **高容错性**：在高达 **30%** 的噪声水平下，SC 架构的精度损失远小于传统二进制架构，表现出极强的鲁棒性。
*   **能效比**：虽然并行执行增加了瞬时功耗，但由于执行周期大幅缩短，其能量延迟积（PDP）在处理复杂函数时具有显著优势。

### 7. 优点（亮点）
*   **空间并行化创新**：成功解决了随机计算长期以来的延迟瓶颈，使其具备了与主流架构竞争的实时处理能力。
*   **无需外部RNG**：利用MTJ固有特性生成比特流，极大地节省了面积和功耗。
*   **通用性强**：提出的超越函数库基于模块化设计，仅需调整系数即可适配多种数学模型。
*   **端到端集成**：实现了从存储、转换、计算到写回的全流程存内闭环。

### 8. 不足与局限
*   **硬件开销权衡**：空间并行化是以增加感测放大器（SA）和外围电路面积为代价的，在大规模部署时需考虑面积开销。
*   **运算类型限制**：该架构主要针对组合逻辑运算，对于需要复杂状态反馈或迭代的运算（如SC除法）支持有限。
*   **技术成熟度**：基于MTJ的存内逻辑仍处于研究和原型阶段，距离大规模商业化生产（如CMOS工艺的成熟度）仍有距离。
*   **精度限制**：尽管可以通过增加比特流长度提高精度，但在极高精度需求场景下，SC的效率可能仍不如传统高位宽二进制运算。

（完）
