---
title: "FusionCIM: Accelerating LLM Inference with Fusion-Driven Computing-in-Memory Architecture"
title_zh: FusionCIM：利用融合驱动的存内计算架构加速大语言模型推理
authors: "Zihao Xuan, Jia Chen, Yewen Li, Wei Xuan, Hegan Chen, Xiao Huo, Fengbin Tu"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25317v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 算子融合驱动的存内计算加速器架构
tldr: 针对大语言模型（LLM）推理效率和可扩展性挑战，本文提出FusionCIM架构。该架构通过算子融合驱动，创新性地结合了基于内积和外积的混合存算一体（CIM）流水线，并设计了QO固定数据流以增强片上数据复用。此外，引入模式感知的在线Softmax机制优化非线性融合。在LLaMA-3上的实验表明，FusionCIM显著提升了能效和推理速度，为高效LLM硬件加速提供了新方案。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型推理过程中算子间频繁的数据传输和内存访问导致的效率瓶颈。
method: 提出了混合CIM流水线架构、QO固定数据流以及模式感知的在线Softmax机制，实现了高效的算子融合与数据复用。
result: 在LLaMA-3模型上，FusionCIM相比现有先进存算一体设计实现了3.86倍的节能和1.98倍的加速，系统能效达到29.4 TOPS/W。
conclusion: FusionCIM通过创新的算子融合架构设计，显著优化了LLM推理的能效和性能，展示了存算一体技术在处理大规模模型时的巨大潜力。
---

## 摘要
本文提出了 FusionCIM，一种算子融合驱动的存内计算（CIM）加速器架构，用于实现高效且可扩展的大语言模型（LLM）推理。该架构具有三项关键创新：(1) 一种混合 CIM 流水线架构，将 QKT 计算映射到基于内积的 CIM (IP-CIM)，将 PV 聚合映射到基于外积的 CIM (OP-CIM)，以实现高效的矩阵乘法融合；(2) 一种 QO 固定（QO-stationary）数据流，消除了转置融合下 CIM 中重复的 KV 加载以及缓存中的 K 矩阵访问，显著提高了片上数据复用率；(3) 一种模式感知的在线 Softmax 机制，利用注意力分数的分布规律来减少非线性融合的指数重缩放开销。在 LLaMA-3 模型上的实验结果表明，与之前的先进（SOTA）基于 CIM 的设计相比，FusionCIM 实现了高达 3.86 倍的节能和 1.98 倍的加速，系统级能效达到 29.4 TOPS/W。

## Abstract
In this paper, we propose FusionCIM, an operator-fusion-driven compute-in-memory (CIM) accelerator architecture for efficient and scalable LLM inference, with three key innovations: (1) a hybrid CIM pipeline architecture that maps QKT computation on inner-product-based CIM (IP-CIM) and PV aggregation on outer-product-based CIM (OP-CIM) for efficient matrix multiplications fusion; (2) a QO-stationary dataflow that eliminates repeated KV loading in CIM and K-matrix access in buffer under transpose fusion, significantly improving data reuse on chip; and (3) a pattern-aware online-softmax mechanism that exploits distribution regularities of attention scores to reduce exponential rescaling overhead for non-linear fusion. Experimental results on LLaMA-3 model show that FusionCIM achieves up to 3.86x energy saving, and 1.98x speedup compared with prior SOTA CIM-based designs with 29.4 TOPS/W energy efficiency at the system level.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FusionCIM** 的存内计算（CIM）加速器架构，旨在通过算子融合驱动的设计，解决大语言模型（LLM）推理中的内存瓶颈和计算开销问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：LLM 推理（尤其是长文本场景）面临巨大的计算和访存压力。虽然存内计算（CIM）能减少权重读取开销，但在 LLM 任务中仍面临三大挑战。
*   **核心挑战**：
    1.  **激活值访存高**：随着上下文长度增加，Score 矩阵等激活值的访存成为主要瓶颈。
    2.  **KV 缓存重复加载与转置开销**：现有 CIM 架构通常将 KV 矩阵作为静态权重，导致在不同 Query 块下需要重复加载 KV，且 K 矩阵的转置需要额外的缓冲空间和周期。
    3.  **Online Softmax 开销**：为了融合算子，Online Softmax 需要频繁进行指数重缩放（Rescaling），增加了非线性单元的负担。

### 2. 方法论
FusionCIM 提出了三项核心创新技术：
*   **混合 CIM 流水线架构（Hybrid CIM Pipeline）**：
    *   **IP-CIM（内积型）**：用于计算 $QK^T$（Score 矩阵），Query 矩阵固定在阵列中。
    *   **OP-CIM（外积型）**：用于计算 $PV$（Context 聚合），输出矩阵 $O$ 固定在阵列中。
    *   这种混合设计实现了矩阵乘法、转置和非线性算子的深度融合。
*   **QO 固定数据流（QO-Stationary Dataflow）**：
    *   将 Query (Q) 和输出 (O) 保持在 CIM 宏内不动，而让 Key (K) 和 Value (V) 以流式输入。
    *   **优势**：消除了 KV 的重复加载，并实现了“原位”K 矩阵转置，无需额外硬件。
*   **模式感知的在线 Softmax（Pattern-Aware Online-Softmax）**：
    *   **核心思想**：利用注意力分数（Attention Score）的最大值通常出现在对角线附近的特性。
    *   **算法流程**：调度器从对角线位置开始计算 $QK^T$，从而更早地识别出行最大值（Row Maxima），大幅减少后续的指数重缩放操作次数。

### 3. 实验设计
*   **数据集/模型**：主要使用 **LLaMA-3-8B** 模型进行评估，同时在 Softmax 分析中对比了使用 ALiBi 编码的 BERT 模型。
*   **场景**：涵盖了从 128 到 4096 的不同序列长度（Sequence Length）。
*   **Benchmark（对比方法）**：
    *   **Baseline 1**：传统数字 CIM 架构（带转置缓冲器）。
    *   **Baseline 2**：类似 TranCIM 的架构（采用 KV 固定数据流）。
    *   **SOTA 对比**：与 P3ViT 和 TranCIM 等先进 CIM 加速器进行性能指标对比。

### 4. 资源与算力
*   **硬件规格**：基于 **TSMC 28nm** 工艺模拟。
*   **架构参数**：包含 16 个混合引擎（HE），主频 **400MHz**。每个 HE 包含 128x128 的 IP-CIM 和 OP-CIM 宏，以及 128 个并行的 FP16 Softmax 处理单元。
*   **算力表现**：系统级能效达到 **29.4 TOPS/W**（INT8），面积效率为 **2.03 TOPS/mm²**。
*   **训练/仿真时长**：文中未提及具体仿真耗时，但明确了功率和面积数据来源于成熟的 CAD 工具（如 Synopsys DC, PrimeTime PX）和 Cacti 7.0。

### 5. 实验数量与充分性
*   **实验覆盖面**：
    *   对比了不同序列长度下的延迟（Latency）和能耗（Energy）。
    *   进行了访存分析（Off-chip vs On-chip）。
    *   针对 QO 固定数据流进行了消融实验（分解了 KV 加载、转置访问、Psum 访问的减少量）。
    *   针对不同模型、Prompt、层和头进行了 Softmax 重缩放次数的统计。
*   **充分性评价**：实验设计较为全面，从系统级指标到微架构级的访存细节均有覆盖，对比基准选择合理，能够客观反映所提技术的改进效果。

### 6. 主要结论
*   **性能提升**：相比 Baseline 1，FusionCIM 实现了最高 **1.98 倍** 的加速。
*   **能效优化**：相比 Baseline 1，实现了最高 **3.86 倍** 的节能。
*   **访存减少**：片外访存减少了 46.2%~58.9%，片上访存减少了 64.3%~71.0%。
*   **Softmax 优化**：在 LLaMA-3 上减少了约 60% 的重缩放操作，在 ALiBi 编码模型上甚至减少了 99%。

### 7. 优点
*   **架构创新**：巧妙地结合了内积和外积两种 CIM 模式，解决了 LLM 注意力机制中不同阶段的计算特性需求。
*   **数据流优化**：QO 固定数据流极大地提高了数据复用率，解决了 CIM 写入功耗高、延迟大的痛点。
*   **软硬协同**：利用注意力分数的空间分布规律（对角线特性）来优化硬件调度，是一个非常实用的跨层优化思路。

### 8. 不足与局限
*   **工艺依赖性**：实验基于 28nm 模拟，虽然具有代表性，但在更先进工艺（如 7nm/5nm）下的表现及漏电流影响未讨论。
*   **模型局限性**：虽然 LLaMA-3 具有代表性，但对于非 Transformer 架构（如线性注意力机制或 Mamba 等）的适用性未予讨论。
*   **精度损失**：虽然提到了动态部分和量化，但未详细给出在极端量化（如 INT4）下对 LLM 最终生成质量（如困惑度 PPL）的具体影响分析。

（完）
