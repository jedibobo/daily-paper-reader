---
title: Hardware-Efficient Softmax and Layer Normalization with Guaranteed Normalization for Edge Devices
title_zh: 面向边缘设备的具有归一化保证的硬件高效 Softmax 和层归一化
authors: "Dawon Choi, Hana Kim, Ji-Hoon Kim"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23647v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 边缘设备的高效硬件非GEMM运算
tldr: 针对Transformer模型中Softmax和LayerNorm等非GEMM操作在边缘设备上硬件成本高的问题，本文提出了一种保证归一化特性的硬件高效近似方案。该方法在保持归一化约束（如Softmax概率和为1）的同时，显著降低了硬件面积。实验表明，该设计在NLP任务中保持了极高的精度，且相比现有技术在面积上实现了高达11至14倍的缩减，非常适合边缘生成式AI应用。
selection_source: fresh_fetch
motivation: 现有的非GEMM近似方法多针对排序任务，无法满足边缘NLP和生成式AI对归一化精度的严格要求。
method: 提出了一种在保证归一化特性（Softmax和为1，LayerNorm标准差为1）的前提下，利用硬件高效近似算法实现的架构。
result: 在Samsung 28nm工艺下，Softmax和LayerNorm的面积分别缩减至942和1199平方微米，且在GLUE和SQuAD等任务上几乎无精度损失。
conclusion: 该设计通过极小的硬件开销实现了高性能的归一化操作，为边缘设备部署复杂的Transformer模型提供了高效方案。
---

## 摘要
在 Transformer 模型中，非 GEMM（非通用矩阵乘法）操作——特别是 Softmax 和层归一化 (LayerNorm)——由于其非线性特性，往往占据了主要的硬件成本。为了解决这一问题，以往的近似研究主要针对面向排名的任务，这对于分类任务是可以接受的。然而，边缘自然语言处理 (NLP) 应用和边缘生成式 AI 很大程度上是基于面向分数的任务进行评估的，因此具有归一化保证的非 GEMM 操作至关重要。我们提出了一种面向边缘设备的、具有归一化保证的硬件高效 Softmax 和 LayerNorm。我们的设计采用了硬件高效的近似方法，同时保留了归一化特性（Softmax: $\sum p = 1$，LayerNorm: $σ= 1$）。我们的架构使用 Verilog HDL 描述，并采用三星 28nm CMOS 工艺进行综合。在精度评估中，我们实现了高精度且性能下降极小：GLUE +0.07%，SQuAD -0.01%，困惑度 -0.09%。实现结果表明，我们的架构非常小巧：Softmax 为 $942\,μm^2$，LayerNorm 为 $1199\,μm^2$。与现有技术相比，我们的面积分别减少了高达 11 倍和 14 倍。

## Abstract
In Transformer models, non-GEMM (non-General Matrix Multiplication) operations -- especially Softmax and Layer Normalization (LayerNorm) -- often dominate hardware cost due to their nonlinear nature. To address this, previous approximation studies mainly target rank-oriented tasks, which is acceptable for classification. However, edge Natural Language Processing (NLP) applications and edge generative AI are largely evaluated based on score-oriented tasks, so normalization-guaranteed non-GEMM operations are essential. We propose a hardware-efficient Softmax and LayerNorm with Guaranteed Normalization for Edge devices. Our design employs hardware-efficient approximation methods while preserving the normalization (Softmax: $\sum p = 1$, LayerNorm: $σ= 1$). Our architecture is described in Verilog HDL and synthesized using the Samsung 28nm CMOS process. In accuracy evaluation, we achieve high accuracy with minimal degradation: GLUE +0.07%, SQuAD -0.01%, perplexity -0.09%. Implementation results show that our architecture is small: $942\,μm^2$ for Softmax, $1199\,μm^2$ for LayerNorm. Compared to the state of the art, we achieve up to 11x and 14x reduction in area, respectively.

---

## 论文详细总结（自动生成）

这篇论文针对 Transformer 模型在边缘设备上部署时，非 GEMM（非通用矩阵乘法）操作（如 Softmax 和 LayerNorm）硬件开销过大的问题，提出了一种具有“归一化保证”的高效硬件架构。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 Transformer 硬件加速中，Softmax 和 LayerNorm 等非线性操作是主要的瓶颈。现有的近似方法（如基于 log 域或 base-2 的近似）虽然减少了面积，但往往只保证“排名（Rank）”正确，而忽略了“数值归一化（Normalization）”的精确度。
*   **研究动机**：对于文本分类等“面向排名”的任务，归一化误差影响较小；但对于问答（SQuAD）和生成式任务（困惑度评估）等“面向分数”的任务，归一化误差（如 Softmax 输出和不等于 1，或 LayerNorm 标准差不等于 1）会导致严重的精度下降。因此，边缘设备需要一种既节省硬件资源又能保证归一化特性的方案。

### 2. 核心方法论
论文提出了两种硬件友好的算法及其对应的电路架构：

*   **无乘法器/除法器 Softmax (Mul-/Div-free Softmax)**：
    *   **指数运算近似**：将输入分解为粗略项（Coarse）和残差项（Residual），利用两个小型查找表（LUT）进行因子分解计算（$e^{-q} = e^{-R \cdot frac} \cdot e^{-rem}$），从而避免复杂的指数电路。
    *   **定点除法器 (FxP Div)**：采用基于二进制移位-减法算法的自定义定点除法器，通过缩放因子重新调整输出，强制保证所有概率之和为 1。
*   **牛顿法层归一化 (Newton LayerNorm)**：
    *   **倒数平方根近似**：使用牛顿迭代法代替传统的平方根和除法。
    *   **关键技术**：引入前导一检测器（LOD）来生成初始猜测值，仅需 2 个周期即可完成高精度估计。通过重构公式，将除法转换为乘法，确保输出的标准差 $\sigma$ 严格趋近于 1。

### 3. 实验设计
*   **软件精度评估**：
    *   **数据集**：GLUE 综合基准（分类）、SQuAD v1.1（问答）、WikiText（语言模型困惑度）。
    *   **模型**：BERT-base（用于 GLUE 和 SQuAD）、GPT-Neo 1.3B（用于困惑度评估）。
    *   **基准 (Benchmark)**：以 FP32 全精度模型为基准，对比了其他几种典型的近似方法（如 Softermax 等）。
*   **硬件实现评估**：
    *   **工艺**：三星 28nm CMOS 工艺。
    *   **对比对象**：对比了近年来发表在 SCIS'24、TCAS-I/II 等顶会/顶刊上的 4 种先进硬件架构。

### 4. 资源与算力
*   **硬件参数**：设计运行频率为 250 MHz（最高可达 1.0 GHz）。
*   **算力/训练说明**：论文未详细说明模型训练所使用的 GPU 型号和时长，因为其研究重点在于**推理阶段**的硬件架构设计。精度评估是在 PyTorch 环境下通过模拟定点运算完成的。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了 NLP 领域最主流的三类任务（分类、问答、生成），实验设计较为全面。
*   **消融与对比**：通过误差分布图（Fig. 5）展示了归一化误差的分布，证明了其方法在数值上无限接近 FP32 基准。
*   **公平性**：硬件对比采用了相同的工艺节点（部分进行了等效换算）和主流评价指标（面积、延迟、精度），对比具有客观性。

### 6. 主要结论与发现
*   **精度保持**：在 GLUE 上精度甚至提升了 0.07%，在 SQuAD 上仅下降 0.01%，在困惑度上仅下降 0.09%。相比之下，不保证归一化的传统近似方法在困惑度上会导致高达 13.68% 的损失。
*   **硬件极简**：Softmax 面积仅为 $942\,\mu m^2$，LayerNorm 面积为 $1199\,\mu m^2$。
*   **面积优势**：与现有最先进技术相比，Softmax 面积缩减了 11 倍，LayerNorm 面积缩减了 14 倍。

### 7. 优点与亮点
*   **归一化保证**：首次在极低功耗硬件设计中强调并实现了“归一化保证”，解决了生成式 AI 在边缘端部署的精度痛点。
*   **架构精巧**：通过 LUT 分解和牛顿法重构，巧妙避开了昂贵的除法和开方算子，实现了极高的面积效率。
*   **通用性强**：该方案不仅适用于 BERT，也证明了在 GPT 等自回归模型中的有效性。

### 8. 不足与局限
*   **延迟权衡**：为了保证归一化精度，其延迟为 $N+1$ 个周期，虽然在可接受范围内，但相比于某些极端的单周期非归一化近似方案，速度略有牺牲。
*   **精度上限**：虽然在 28nm 下表现优异，但在更低比特（如 INT4）量化环境下的鲁棒性尚未在文中深入探讨。
*   **应用范围**：主要针对 NLP 任务，未展示在视觉 Transformer (ViT) 等其他领域的效果。

（完）
