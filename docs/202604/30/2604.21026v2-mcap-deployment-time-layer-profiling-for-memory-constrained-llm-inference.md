---
title: "MCAP: Deployment-Time Layer Profiling for Memory-Constrained LLM Inference"
title_zh: MCAP：面向内存受限大语言模型推理的部署时层级分析
authors: Anurita Das
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.21026v2"
tags: ["query:hwnas"]
score: 7.0
evidence: 异构硬件上受内存限制的大模型推理动态放置
tldr: 针对大语言模型在异构硬件部署时面临的内存限制问题，本文提出MCAP（蒙特卡洛激活分析）。这是一种在加载时评估每层重要性的方法，支持根据目标设备的内存预算动态决定权重的精度（如W4A8或W4A16）和存储位置（GPU、内存或SSD）。该方法无需修改权重即可让模型在极低内存环境下运行。实验表明，其NVE系统在NVIDIA T4上的解码吞吐量比llama-cpp Q4_0高出1.5-1.8倍，显著提升了受限资源下的推理效率。
selection_source: fresh_fetch
motivation: 解决大语言模型在异构硬件上部署时受限于内存而非计算能力的问题。
method: 提出MCAP方法，通过在部署时进行每层重要性评估，指导权重的精度分配和多级存储放置。
result: 在NVIDIA T4上，NVE系统实现了比llama-cpp Q4_0高1.5-1.8倍的解码吞吐量，并支持在极低内存下运行。
conclusion: MCAP提供了一种灵活且高效的部署方案，使单一权重集能适应多种内存预算，提升了LLM的推理性能和可移植性。
---

## 摘要
在异构硬件上部署大语言模型通常受限于内存而非计算能力。我们引入了 MCAP（蒙特卡洛激活分析），这是一种加载时的逐层重要性评估器，能够在目标设备上实现动态精度和内存放置决策。MCAP 生成轻量级的逐层信号，用于驱动精度调度（如 W4A8 与 W4A16）和驻留层级（GPU、RAM、SSD），从而允许同一组权重在不同的内存预算下运行。我们的系统 NVE 在 NVIDIA T4 上的解码吞吐量比 llama-cpp Q4_0 高出 1.5-1.8 倍，并使模型能够在无需修改权重的情况下，在以前无法实现的内存环境下运行。

## Abstract
Deploying large language models to heterogeneous hardware is often constrained by memory, not compute. We introduce MCAP (Monte Carlo Activation Profiling), a load-time per-layer importance estimator that enables dynamic precision and memory placement decisions on the target device. MCAP produces a lightweight per-layer signal that drives both precision dispatch (W4A8 vs. W4A16) and residency tier (GPU, RAM, SSD), allowing a single set of weights to operate across diverse memory budgets. Our system, NVE, achieves 1.5-1.8x higher decode throughput than llama-cpp Q4_0 on NVIDIA T4 and enables models to run in memory regimes previously infeasible without modifying weights.