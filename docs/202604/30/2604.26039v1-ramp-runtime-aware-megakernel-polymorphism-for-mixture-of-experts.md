---
title: "RaMP: Runtime-Aware Megakernel Polymorphism for Mixture-of-Experts"
title_zh: RaMP：面向混合专家模型（MoE）的运行时感知巨核多态性
authors: "Vyom Sharma, Debajyoti Datta"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26039v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 通过运行时感知调度最大化内核吞吐量
tldr: RaMP是一个针对混合专家模型（MoE）推理的运行时感知调度框架。针对现有系统仅依赖批大小而忽略专家路由分布导致的性能损失，RaMP通过硬件性能区域分析和四参数波次成本模型，根据实时专家直方图动态选择最优内核配置。该框架具有内核无关性，在多种硬件架构上实现了显著的吞吐量提升，大幅优化了端到端推理性能。
selection_source: fresh_fetch
motivation: "现有MoE推理系统仅依据批大小进行调度，忽略了专家路由分布对内核性能的影响，导致10-70%的潜在吞吐量未能实现。"
method: 利用硬件常量进行性能区域分析，并结合四参数波次成本模型，从运行时专家分布中实时选择最优的多态内核配置。
result: 在vLLM中相比Triton实现1.30倍加速，相比DeepGEMM加速1.41倍，且内核调度开销极低，模型拟合仅需10-24分钟。
conclusion: RaMP通过感知运行时的路由分布并结合多态内核优化，为MoE推理提供了一种高效、通用且易于集成的性能提升方案。
---

## 摘要
混合专家模型（MoE）推理的最佳算子配置取决于批大小（batch size）和专家路由分布，然而现有的生产系统仅依据批大小进行调度，导致 10-70% 的算子吞吐量未能得到利用。我们提出了 RaMP，一个路由感知的调度框架。通过性能区域分析，仅凭硬件常量即可推导出各项优化的生效时机，并成功预测了包括 3 种未知架构在内的全部 8 种测试架构。一个四参数波次成本模型根据运行时专家直方图选择最快配置，与穷举搜索相比，其平均遗憾值（mean regret）仅为 0.93%，且每个模型仅需 10-24 分钟的一次性性能分析即可完成拟合。由于该模型仅依赖于 CTA 网格几何结构，因此具有算子无关性：在无需修改源码的情况下，将其应用于 Alpha-MoE 可获得 1.14 倍的性能提升。配合共同设计的、提供 134-268 种多态配置的 CuTe DSL 算子，RaMP 相比静态调度实现了 1.22 倍的算子加速；在 vLLM 服务中，相比 Triton 实现了 1.30 倍的端到端加速，相比 DeepGEMM 提升 1.41 倍，相比 FlashInfer CUTLASS 提升 1.13 倍。

## Abstract
The optimal kernel configuration for Mixture-of-Experts (MoE) inference depends on both batch size and the expert routing distribution, yet production systems dispatch from batch size alone, leaving 10-70% of kernel throughput unrealized. We present RaMP, a routing-aware dispatch framework. A performance-region analysis derives, from hardware constants alone, when each optimization helps, correctly predicting all 8 tested architectures, including 3 unseen. A four-parameter wave cost model selects the fastest configuration from the runtime expert histogram, achieving 0.93% mean regret versus exhaustive search, fitted from just 10-24 minutes of one-time profiling per model. Because the model depends only on CTA grid geometry, it is kernel-agnostic: applied to Alpha-MoE, it delivers 1.14x with no source modification. Paired with a co-designed CuTe DSL kernel exposing 134-268 polymorphic configurations, RaMP delivers 1.22x kernel speedup over static dispatch and 1.30x end-to-end speedup in vLLM serving over Triton, 1.41x over DeepGEMM, and 1.13x over FlashInfer CUTLASS.

---

## 论文详细总结（自动生成）

### RaMP：面向混合专家模型（MoE）的运行时感知巨核多态性论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：混合专家模型（MoE）推理的算子（Kernel）性能不仅受批大小（Batch Size）影响，还高度依赖于**专家路由分布（Routing Distribution）**。
*   **研究动机**：
    *   **现状**：现有的主流推理框架（如 vLLM, DeepGEMM, FlashInfer）仅根据批大小进行静态调度，忽略了路由分布。
    *   **痛点**：在实际生产中，专家路由通常是极度倾斜的（仅 8-14% 的专家处于活跃状态）。静态调度会导致严重的计算资源浪费（如 Padding 过多或波次利用率低），使得 10-70% 的潜在性能未能实现。
    *   **目标**：开发一个能够感知运行时路由分布、动态选择最优算子配置的调度框架。

#### 2. 方法论
RaMP 框架由三个核心组件组成：
*   **性能区域分析（Performance-Region Analysis）**：
    *   利用四个基于硬件常量的变量来划分性能区域：计算密度 ($\rho$)、L2 缓存压力 ($\lambda$)、波次利用率 ($\omega$) 和 K 维度归约深度 ($\kappa$)。
    *   这些变量能准确预测何时开启特定优化（如 Group M 变换、Split-K 归约等）。
*   **四参数波次成本模型（Wave Cost Model）**：
    *   公式：$T(c) = a(c) + b(c) \frac{g}{SM} + c(c)g + d(c) \log(g+1)$。
    *   其中 $g$ 是 CTA 网格大小，$SM$ 是硬件流处理器数量。四个参数分别代表：启动开销 ($a$)、波次调度成本 ($b$)、单 CTA 内存流量成本 ($c$) 和子波次非线性效应 ($d$)。
    *   该模型仅需 25 个性能采样点即可通过最小二乘法（OLS）拟合，拟合时间仅需 10-24 分钟。
*   **多态算子与运行时调度**：
    *   **算子设计**：使用 CuTe DSL 编写，支持 134-268 种多态配置（涵盖不同分块大小、流水线深度等）。
    *   **运行时流程**：在推理时，通过一个极轻量级的 Triton 算子（开销 <38 $\mu$s）统计专家直方图，代入成本模型，实时选出最优配置。

#### 3. 实验设计
*   **测试模型**：涵盖 8 种主流 MoE 架构，包括 OLMoE-1B-7B、Qwen3、DeepSeek-V3 (EP8/TP8)、Mixtral-8x7B，以及 3 个完全未见的测试模型（Phi-3.5-MoE、Jamba-1.5、DBRX）。
*   **对比基准（Benchmarks）**：
    *   **vLLM Triton FP8**：生产环境默认配置。
    *   **DeepGEMM**：DeepSeek 开发的高性能 FP8 算子。
    *   **FlashInfer (CUTLASS)**：集成自 CUTLASS 的优化算子。
    *   **Alpha-MoE**：具有 JIT 调优功能的 C++ 算子。
*   **评估指标**：算子吞吐量、平均遗憾值（Mean Regret）、端到端 TPOT（每 Token 延迟）和 TTFT（首 Token 延迟）。

#### 4. 资源与算力
*   **硬件平台**：单台 NVIDIA **H200 SXM** GPU（132 个 SM，4.8 TB/s HBM3e 显存）。
*   **训练/拟合成本**：
    *   无需大规模训练。
    *   每个模型的一次性性能分析（Profiling）仅需 **10-24 分钟**。
    *   运行时调度开销极低，摊销到每层 MoE 仅约 **2.5 $\mu$s**。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了超过 20 万个真实推理任务中的路由观测值。
    *   对 8 种不同规模和结构的架构进行了验证。
    *   进行了消融实验，验证了成本模型从 2 参数到 4 参数的改进效果。
*   **充分性与公平性**：实验涵盖了已知模型和完全未见的“黑盒”模型，证明了方法的通用性。对比实验均在相同硬件和相同算子二进制文件下进行，仅改变调度策略，确保了对比的客观性。

#### 6. 主要结论与发现
*   **性能提升**：RaMP 在算子层面比静态调度快 **1.22 倍**；在 vLLM 端到端服务中，相比 Triton 提升 **1.30 倍**，相比 DeepGEMM 提升 **1.41 倍**。
*   **模型精度**：成本模型的预测遗憾值仅为 **0.93%**，几乎等同于穷举搜索的效果。
*   **通用性**：该框架具有**算子无关性**。在不修改源码的情况下，将 RaMP 应用于第三方 Alpha-MoE 算子，使其性能提升了 1.14 倍。

#### 7. 优点
*   **物理建模**：成本模型基于硬件物理特性（波次、内存流量），而非纯黑盒机器学习，具有极强的解释性和跨架构迁移能力。
*   **低开销**：拟合时间短，运行时调度延迟几乎可以忽略不计。
*   **多态性**：通过丰富的配置空间（Polymorphism）充分挖掘了 Hopper 架构（如 TMA, WGMMA）的潜力。

#### 8. 不足与局限
*   **硬件局限**：目前主要针对 NVIDIA Hopper 架构（H100/H200）优化，扩展到 Blackwell 或其他架构需要重新定义硬件常量。
*   **单卡范围**：研究重点在于单 GPU 内核优化，未深入探讨多卡专家并行（Expert Parallelism）下的通信调度。
*   **极端场景**：在批大小 $S=1$ 或路由绝对均匀的情况下，RaMP 的优势会收敛至与静态调度持平。

（完）
