---
title: Feasible-First Exploration for Constrained ML Deployment Optimization in Crash-Prone Hierarchical Search Spaces
title_zh: 易崩溃层级搜索空间中受限机器学习部署优化的可行性优先探索
authors: Christian Lysenstøen
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.25073v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在延迟和内存约束下对模型族、量化和运行时后端进行联合优化
tldr: 本研究针对机器学习模型部署中复杂的层级搜索空间及严苛的资源约束，提出了一种名为“热预算退火”（TBA）的探索优先优化方法。该方法通过先显式探索可行区域再热启动 TPE 优化器，并结合超时中止与子空间黑名单机制，有效解决了传统黑盒优化器在面对频繁崩溃或无效配置时效率低下的问题。此外，研究还推出了 DeployBench 基准测试集。实验证明，该方法在多种 GPU 硬件上显著提升了受限环境下的模型发现效率，减少了无效评估开销。
selection_source: fresh_fetch
motivation: 传统的黑盒优化器在具有高崩溃率和严苛约束的层级化 ML 部署搜索空间中，容易浪费大量预算在无效或不具信息量的试验上。
method: 提出 TBA 流程，采用“可行性优先”的探索策略映射有效区域，并引入试验超时和子空间黑名单机制以增强对硬件故障的鲁棒性。
result: 在 DeployBench 及真实 GPU 环境（如 H100、A100）的视觉模型部署中，该方法相比冷启动 TPE 显著提高了模型发现率并降低了预算浪费。
conclusion: 在充满挑战的部署空间中，将优化分解为显式的可行性探索与模型引导的开发阶段，是实现高效约束优化的关键。
---

## 摘要
在生产约束下部署机器学习模型需要对模型族、量化方案、运行时后端和推理服务配置进行联合优化。这产生了一个层级混合变量搜索空间，其中许多配置是无效的：评估可能会崩溃、超出内存限制或违反延迟约束。当有效配置较为常见时，标准的黑盒优化器（如树状 Parzen 估计器 TPE 和受限贝叶斯优化）是有效的，但在恶劣的部署空间中，它们可能会在有限的评估预算中将大部分耗费在无效或无信息的试验上。本文研究了这一情形，并探讨了是否应将优化分解为显式的探索阶段和随后的模型引导开发阶段。我们提出了热预算退火（Thermal Budget Annealing, TBA），这是一种可行性优先的探索程序，在热启动 TPE 之前映射有效且可行的区域。该方法包含针对恶劣硬件环境的两种鲁棒性机制：试验超时（提前中止明显不可行的评估）和子空间黑名单（在重复失败后暂时抑制类别子空间）。我们还引入了 DeployBench，这是一个用于部署优化的基准测试套件，具有层级结构、隐藏崩溃区、硬约束和不等的评估成本。在合成基准测试以及跨五个 GPU 目标（NVIDIA H100、A100、RTX 5080、L4 和 T4）的五个预训练视觉模型的真实 GPU 部署中，所提出的混合方法在严格约束下改进了模型族的发现，同时相比冷启动 TPE 减少了预算浪费。

## Abstract
Deploying machine learning models under production constraints requires joint optimization over model family, quantization scheme, runtime backend, and serving configuration. This induces a hierarchical mixed-variable search space in which many configurations are invalid: evaluations may crash, exceed memory limits, or violate latency constraints. Standard black-box optimizers such as Tree-structured Parzen Estimators (TPE) and constrained Bayesian optimization are effective when valid configurations are common, but they can spend a large fraction of a small evaluation budget on invalid or uninformative trials in hostile deployment spaces. This paper studies that regime and asks whether optimization should be decomposed into an explicit exploration stage followed by model-guided exploitation. We propose Thermal Budget Annealing (TBA), a feasible-first exploration procedure that maps valid and feasible regions before warm-starting TPE. The method includes two robustness mechanisms for hostile hardware: trial timeouts that abort clearly infeasible evaluations early, and subspace blacklisting that temporarily suppresses categorical subspaces after repeated failures. We also introduce DeployBench, a benchmark suite for deployment optimization with hierarchical structure, hidden crash zones, hard constraints, and unequal evaluation costs. On synthetic benchmarks and real GPU deployment with five pre-trained vision models across five GPU targets (NVIDIA H100, A100, RTX 5080, L4, and T4), the proposed hybrid improves model-family discovery under tight constraints while reducing wasted budget relative to cold-start TPE.

---

## 论文详细总结（自动生成）

这是一份关于论文《Feasible-First Exploration for Constrained ML Deployment Optimization in Crash-Prone Hierarchical Search Spaces》的结构化分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：在实际部署机器学习模型时，需要在模型族、量化方案、运行时后端和批处理大小等多个维度进行联合优化。然而，这类搜索空间具有**层级性**（某些参数依赖于前置选择）且**极易崩溃**（30%-80% 的配置可能因内存溢出、编译器错误或硬件不兼容而失败）。
*   **研究动机**：传统的黑盒优化器（如 TPE）在有效配置较多时表现良好，但在“高崩溃率”空间中，它们往往会过早地陷入局部最优（即“过早开发”），因为初始的随机采样可能只发现了少数几个能运行的模型族，导致后续搜索被限制在狭窄的子空间内，错失全局最优解。

### 2. 方法论
论文提出了 **TBA → TPE** 混合优化框架，将过程分为两个阶段：
*   **阶段 1：热预算退火（TBA）**
    *   **核心思想**：采用一种“可行性优先”的模拟退火算法。在未找到可行解前，目标是最小化约束违反程度；找到后，转向优化目标函数。
    *   **层级邻域提议**：根据预算进度动态调整“结构性移动”（改变模型族/后端）和“数值性移动”（调整参数）的概率。
    *   **鲁棒性机制**：
        *   **试验超时（Trial Timeouts）**：如果初步测量的延迟超过约束阈值的 5 倍，则立即中止评估，节省时间。
        *   **子空间黑名单（Subspace Blacklisting）**：如果某个类别值（如特定的量化模式）连续失败 3 次，则暂时屏蔽该子空间。
*   **阶段 2：热启动 TPE**
    *   当满足特定条件（如至少 5 个可行观测和 3 个失败观测）时，将 TBA 的所有历史数据注入 Optuna 的 TPE 优化器中，利用模型引导的搜索进行精细化开发。

### 3. 实验设计
*   **基准测试集（DeployBench）**：
    *   **合成任务**：Crashy Branin（约 65% 无效率）和层级 Rosenbrock。
    *   **真实部署任务**：基于 ImageNette 数据集，涵盖 5 种视觉模型（ResNet, MobileNet, ViT 等）、3 种后端（Eager, Compile, ONNX）、3 种量化模式及多种批处理大小。
*   **对比方法**：随机搜索（Random Search）、原生 Optuna TPE、纯 TBA（无 TPE 阶段）。
*   **测试场景**：边缘端严苛约束（低延迟、小内存）和服务器吞吐量优化。

### 4. 资源与算力
*   **硬件平台**：
    *   主要实验：NVIDIA RTX 5080 Laptop GPU (16GB)。
    *   多硬件泛化实验：通过 Google Colab 使用了 NVIDIA H100 (80GB)、A100 (40GB)、L4 (24GB) 和 T4 (16GB)。
*   **算力消耗**：
    *   合成基准测试：约 15 分钟。
    *   RTX 5080 部署实验：约 120 分钟。
    *   多硬件实验：约 6 小时。
    *   **总计**：约 8.5 小时。

### 5. 实验数量与充分性
*   **实验规模**：主要实验使用了 10 个随机种子，多硬件实验使用了 5 个随机种子。
*   **充分性评价**：
    *   **多维度验证**：涵盖了从合成函数到真实硬件的跨度，并在 5 种不同性能等级的 GPU 上进行了验证，实验设计较为全面。
    *   **消融实验**：对 TBA 的各个组件（如重启策略、快回机制、自适应概率）进行了消融分析，证明了各部分的有效性。
    *   **统计显著性**：在 RTX 5080 上使用 10 个种子提供了较好的统计支持，但在 T4/L4 等硬件上仅用 5 个种子，部分 1-2 次发现率的差异可能存在随机性。

### 6. 主要结论与发现
*   **模型发现率提升**：在 RTX 5080 上，混合方法在 8/10 的种子中发现了最优模型族（vit_tiny），而 TPE 仅为 3/10。在 H100 上，混合方法达到了 5/5 的完美发现率。
*   **减少预算浪费**：混合方法显著降低了无效试验（崩溃或违反约束）的比例。例如在 H100 上，其浪费比例仅为 28%，远低于随机搜索的 63%。
*   **探索质量决定成败**：实验证明，在资源受限且易崩溃的空间中，早期的探索质量直接决定了后期开发阶段是否有优质的候选模型可用。

### 7. 优点
*   **实用性强**：直接针对工业界部署中常见的“环境恶劣”（频繁崩溃、层级依赖）问题提出解决方案。
*   **鲁棒性设计**：引入的超时中止和黑名单机制非常符合系统工程实践，能有效处理“有毒”的搜索子空间。
*   **开源贡献**：提供了 DeployBench 基准测试集，填补了 HPO 领域缺乏高崩溃率部署场景的空白。

### 8. 不足与局限
*   **硬件局限性**：实验仅限于 NVIDIA GPU，未测试国产芯片、移动端 NPU 或 CPU 部署环境，后端行为可能有所不同。
*   **领域局限性**：目前仅聚焦于图像分类任务，未涵盖 NLP（如 LLM 推理）或多模型流水线等更复杂的部署场景。
*   **性能上限**：混合方法的平均准确率提升并不巨大（与随机搜索接近），其核心优势更多体现在“发现稳定性”和“预算效率”上，而非绝对性能的突破。

（完）
