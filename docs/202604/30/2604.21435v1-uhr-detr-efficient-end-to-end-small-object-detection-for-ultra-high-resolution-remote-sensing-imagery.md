---
title: "UHR-DETR: Efficient End-to-End Small Object Detection for Ultra-High-Resolution Remote Sensing Imagery"
title_zh: UHR-DETR：面向超高分辨率遥感图像的高效端到端小目标检测
authors: "Jingfang Li, Haoran Zhu, Wen Yang, Jinrui Zhang, Fang Xu, Haijian Zhang, Gui-Song Xia"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21435v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 解决内存瓶颈的高效遥感目标检测
tldr: 针对超高分辨率遥感图像中小目标检测面临的内存瓶颈与上下文丢失难题，本文提出UHR-DETR。该模型通过覆盖最大化稀疏编码器动态分配计算资源，并利用全局-局部解耦解码器融合宏观场景与微观细节。在有限硬件条件下，UHR-DETR实现了端到端的高效检测，在提升精度的同时大幅加快了推理速度，为大规模遥感场景监测提供了高效方案。
selection_source: fresh_fetch
motivation: 旨在解决超高分辨率图像检测中保留高分辨率导致的内存瓶颈与下采样或切片导致的上下文丢失之间的矛盾。
method: 提出包含覆盖最大化稀疏编码器和全局-局部解耦解码器的端到端Transformer架构，实现高效的资源分配与特征融合。
result: "在STAR数据集上相比滑动窗口基准提升了2.8% mAP，推理速度提高10倍，且能在单张24GB显存显卡上运行。"
conclusion: UHR-DETR证明了通过稀疏计算与解耦解码可以打破超高分辨率图像检测的计算瓶颈，实现精度与效率的双重突破。
---

## 摘要
超高分辨率（UHR）图像已成为现代遥感不可或缺的一部分，提供了前所未有的空间覆盖范围。然而，在如此广阔的场景中检测小目标面临着一个关键困境：为保留小目标而维持原始分辨率会导致难以承受的内存瓶颈。相反，传统的折中方案（如图像下采样或切片裁剪）要么抹除了小目标，要么破坏了上下文信息。为了打破这一困境，我们提出了 UHR-DETR，一种专为 UHR 图像设计的高效端到端 Transformer 检测器。首先，我们引入了覆盖最大化稀疏编码器（Coverage-Maximizing Sparse Encoder），该编码器将有限的计算资源动态分配给包含丰富信息的高分辨率区域，从而在最小化空间冗余的同时确保最大的目标覆盖率。其次，我们设计了一个全局-局部解耦解码器（Global-Local Decoupled Decoder）。通过整合宏观场景感知与微观目标细节，该模块解决了语义歧义并防止了场景碎片化。在 UHR 图像数据集（如 STAR 和 SODA-A）上的广泛实验证明了 UHR-DETR 在严格硬件限制下（例如单张 24GB RTX 3090）的优越性。与 STAR 数据集上的标准滑动窗口基准相比，它在实现 2.8% mAP 提升的同时，提供了 10 倍的推理加速。我们的代码和模型将在 https://github.com/Li-JingFang/UHR-DETR 发布。

## Abstract
Ultra-High-Resolution (UHR) imagery has become essential for modern remote sensing, offering unprecedented spatial coverage. However, detecting small objects in such vast scenes presents a critical dilemma: retaining the original resolution for small objects causes prohibitive memory bottlenecks. Conversely, conventional compromises like image downsampling or patch cropping either erase small objects or destroy context. To break this dilemma, we propose UHR-DETR, an efficient end-to-end transformer-based detector designed for UHR imagery. First, we introduce a Coverage-Maximizing Sparse Encoder that dynamically allocates finite computational resources to informative high-resolution regions, ensuring maximum object coverage with minimal spatial redundancy. Second, we design a Global-Local Decoupled Decoder. By integrating macroscopic scene awareness with microscopic object details, this module resolves semantic ambiguities and prevents scene fragmentation. Extensive experiments on the UHR imagery datasets (e.g., STAR and SODA-A) demonstrate the superiority of UHR-DETR under strict hardware constraints (e.g., a single 24GB RTX 3090). It achieves a 2.8\% mAP improvement while delivering a 10$\times$ inference speedup compared to standard sliding-window baselines on the STAR dataset. Our codes and models will be available at https://github.com/Li-JingFang/UHR-DETR.

---

## 论文详细总结（自动生成）

这是一份关于论文《UHR-DETR: Efficient End-to-End Small Object Detection for Ultra-High-Resolution Remote Sensing Imagery》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着遥感技术发展，超高分辨率（UHR）图像（如 $8192 \times 8192$ 像素以上）成为主流。
*   **核心困境（感知悖论）**：
    1.  **分辨率与效率的冲突**：检测小目标需要保留高分辨率特征，但这会导致显存爆炸（OOM）和推理极慢。
    2.  **上下文与细节的冲突**：传统的滑动窗口（切片）法会割裂宏观地理背景（如电厂整体结构），而下采样则会抹除微观目标细节（如太阳能电池板）。
*   **研究动机**：开发一种既能处理超大规模输入、又能保留全局上下文、且对硬件友好的端到端检测器。

### 2. 论文提出的方法论
UHR-DETR 采用了**全局-局部解耦**的架构，核心包含两个创新模块：

*   **覆盖最大化稀疏编码器 (Coverage-Maximizing Sparse Encoder)**：
    *   **全局分布估计**：利用轻量级骨干网（如 ResNet-18）处理下采样后的全图，生成“增益图”（Gain Map），预测目标在空间上的分布密度。
    *   **局部峰值边缘损失 (LPM Loss)**：通过辅助损失函数强化增益图的局部峰值，使模型能更精准地锁定目标聚集区。
    *   **动态稀疏切片路由**：将切片选择建模为“几何集合覆盖问题”。提出**迭代软减法贪婪算法 (ISSGA)**，在保证覆盖最多目标的同时，通过软抑制机制减少切片间的冗余重叠，仅提取最有价值的 $K$ 个高分辨率切片。
*   **全局-局部解耦解码器 (Global-Local Decoupled Decoder)**：
    *   **全局宏观交互**：Object Queries 首先与全局低分辨率特征进行交叉注意力计算，获取宏观场景先验。
    *   **局部微观交互**：Queries 根据空间位置投影到对应的高分辨率切片特征中，利用变形注意力（MSDA）捕捉精细细节。这种设计确保了模型在“看清”小目标的同时，依然“记得”它在全图中的位置。

### 3. 实验设计
*   **数据集**：
    *   **STAR**：真实的卫星图像数据集，标准化为 $8192 \times 8192$ 分辨率。
    *   **SODA-A**：大规模小目标检测基准，通过 2 倍上采样模拟 $9600 \times 9600$ 的 UHR 场景。
*   **对比方法 (Baselines)**：
    *   **滑动窗口类**：Faster R-CNN, FCOS, YOLOX, RT-DETR。
    *   **下采样/缩放类**：Fovea。
    *   **动态路由/切片选择类**：SparseFormer, CEASC, SPDet, GigaDet。

### 4. 资源与算力
*   **硬件环境**：实验主要在**单张 NVIDIA RTX 3090 (24GB)** 上完成，这证明了该方法对消费级显卡的友好性。
*   **训练细节**：使用 AdamW 优化器，训练 12 个 epoch，Batch size 为 1。
*   **推理效率**：在处理 $8192 \times 8192$ 图像时，推理延迟约为 0.357s，显存占用约 3.13GB。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流 UHR 数据集上进行了全量对比，并针对核心组件（ISSGA, LPM, GCA）进行了详尽的消融实验。
*   **维度覆盖**：涵盖了切片数量 ($K$)、切片大小、损失函数超参数 ($\Delta$)、切片选择策略（硬抑制 vs 软减法）等多个维度的敏感性分析。
*   **公平性**：对比实验涵盖了从传统 CNN 到最新 Transformer 的多种架构，且在相同的硬件约束下测试了推理速度和显存，实验设计客观且具有说服力。

### 6. 论文的主要结论与发现
*   **性能突破**：在 STAR 数据集上，UHR-DETR 比滑动窗口版 RT-DETR 的 mAP 提升了 2.8%，同时推理速度快了 **10 倍**。
*   **显存优势**：标准检测器在处理 UHR 图像时极易 OOM，而 UHR-DETR 通过稀疏采样将显存占用控制在极低水平。
*   **上下文的重要性**：消融实验证实，引入全局交叉注意力（GCA）能显著提升小目标的检测精度（$AP_S$ 提升约 2.1%），证明了宏观背景对消除语义歧义的关键作用。

### 7. 优点（亮点）
*   **端到端设计**：摆脱了传统切片法复杂的后处理（如重复框合并），实现了从原始大图到检测结果的直接映射。
*   **数学建模严谨**：将切片选择转化为集合覆盖问题，并用 ISSGA 算法高效求解，比简单的 NMS 过滤更具理论支撑。
*   **资源高效**：成功解决了“大图检测必须依赖高性能计算集群”的痛点，具有极强的工业应用潜力。

### 8. 不足与局限
*   **极端分布下的退化**：论文坦诚，如果目标在全图中呈**均匀密集分布**（而非局部聚集），稀疏采样的优势会减弱，计算量将逼近滑动窗口法。
*   **类别偏差**：在 SODA-A 这种模拟（上采样）的数据集上，性能提升幅度不如在 STAR 这种原生 UHR 数据集上明显，说明模型对图像质量和分布特征有一定依赖。
*   **超参数依赖**：切片数量 $K$ 和增益图的阈值仍需根据具体场景手动预设，缺乏完全自适应的动态调整机制。

（完）
