---
title: "SatReg: Regression-based Neural Architecture Search for Lightweight Satellite Image Segmentation"
title_zh: SatReg：面向轻量级卫星图像分割的基于回归的神经架构搜索
authors: "Edward Humes, Tinoosh Mohsenin"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10306v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 针对边缘平台卫星图像分割的硬件感知NAS
tldr: 针对卫星图像分割在边缘设备上的低延迟和低功耗需求，本文提出SatReg框架。该框架以CM-UNet为基础，通过将搜索空间缩减至两个核心宽度变量，并利用回归代理模型预测模型精度与硬件开销。结合知识蒸馏技术，SatReg能在不进行穷举搜索的情况下，快速为NVIDIA Jetson等边缘平台筛选出性能与功耗平衡的最优轻量化架构，为未来空间边缘计算提供了高效的NAS方案。
selection_source: fresh_fetch
motivation: 卫星遥感任务正向星上边缘处理转型，亟需在严格的延迟和功耗约束下实现高效的图像分割模型部署。
method: 提出一种基于回归的硬件感知调优框架，通过对少量样本构建低阶代理模型并结合知识蒸馏来优化轻量化CNN-Mamba模型。
result: 实验表明该方法能有效预测mIoU、延迟和功耗，从而在目标平台上快速选出近乎最优的架构配置而无需穷举搜索。
conclusion: SatReg证明了缩减空间的回归策略是适配未来空间边缘系统、平衡分割精度与硬件成本的实用且高效的方案。
---

## 摘要
随着地球观测任务向星载和边缘处理转移，遥感分割模型必须在严格的延迟和能耗限制下运行。我们提出了 SatReg，这是一个针对边缘平台轻量级遥感分割的基于回归的硬件感知调优框架。以 CM-UNet 作为教师架构，我们将搜索空间缩减为两个主要的宽度相关变量，在 NVIDIA Jetson Orin Nano 上对一小组学生模型进行性能分析，并针对 mIoU、延迟和功耗拟合低阶代理模型。利用知识蒸馏来高效训练采样的学生模型。学习到的代理模型能够为部署目标快速选择近乎最优的架构设置，而无需进行穷举搜索。结果表明，所选变量对任务准确度和硬件成本的影响各不相同，这使得缩减空间的回归成为将 CNN-Mamba 混合分割模型适配到未来空间边缘系统的一种实用策略。

## Abstract
As Earth-observation workloads move toward onboard and edge processing, remote-sensing segmentation models must operate under tight latency and energy constraints. We present SatReg, a regression-based hardware-aware tuning framework for lightweight remote-sensing segmentation on edge platforms. Using CM-UNet as the teacher architecture, we reduce the search space to two dominant width-related variables, profile a small set of student models on an NVIDIA Jetson Orin Nano, and fit low-order surrogate models for mIoU, latency, and power. Knowledge distillation is used to efficiently train the sampled students. The learned surrogates enable fast selection of near-optimal architecture settings for deployment targets without exhaustive search. Results show that the selected variables affect task accuracy and hardware cost differently, making reduced-space regression a practical strategy for adapting hybrid CNN-Mamba segmentation models to future space-edge systems.

---

## 论文详细总结（自动生成）

以下是对论文《SatReg: Regression-based Neural Architecture Search for Lightweight Satellite Image Segmentation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限的卫星边缘计算平台（如小卫星）上，部署高精度的遥感图像分割模型。
*   **研究动机**：
    *   **星载处理需求**：遥感任务正从地面处理转向星载边缘处理，以减少下行链路压力并提高实时性。
    *   **硬件约束**：卫星平台对功耗、内存和延迟有严格限制。
    *   **模型复杂性**：现代混合架构（如 CNN-Mamba）虽然精度高，但计算开销大，且传统的神经架构搜索（NAS）在边缘设备上进行穷举搜索的成本过高。

### 2. 方法论
SatReg 提出了一种基于回归的硬件感知调优框架，核心步骤如下：
*   **缩减搜索空间**：将复杂的架构搜索简化为两个关键的宽度变量：
    1.  **$b$ (Encoder Width)**：控制编码器、中间融合模块及解码器外层通道的宽度。
    2.  **$h$ (Decoder Bottleneck Width)**：控制解码器中 CSMamba 模块的内部瓶颈宽度。
*   **代理模型构建（Surrogate Modeling）**：
    *   **延迟与功耗**：使用二次多项式回归模型预测不同 $(b, h)$ 组合下的硬件开销。
    *   **精度 (mIoU)**：采用有理函数形式的回归模型来拟合分割精度。
*   **知识蒸馏 (Knowledge Distillation)**：由于改变通道宽度导致无法直接使用预训练权重，采用通道级知识蒸馏（CWD）技术，以原始 CM-UNet 为教师模型训练缩减后的学生模型。
*   **多目标优化**：通过一个归一化的目标函数平衡 mIoU、延迟和功耗，在连续空间内寻找最优解，并映射回离散的架构配置。

### 3. 实验设计
*   **数据集**：
    *   **LoveDA**：土地覆盖分类数据集。
    *   **ISPRS Vaihingen**：城市场景语义标注数据集。
*   **基准模型 (Benchmark)**：原始的 **CM-UNet**（一种结合了 ResNet 编码器和 Mamba 解码器的混合架构）。
*   **对比对象**：在讨论中提及了 RS3Mamba 和 UNetMamba 等其他 Mamba 类遥感模型，以验证 SatReg 选定模型的竞争力。
*   **测试环境**：所有推理性能分析和部署测量均在 **NVIDIA Jetson Orin Nano Super** 边缘平台上完成。

### 4. 资源与算力
*   **硬件设备**：使用了 NVIDIA Jetson Orin Nano Super（MAXN 模式）。
*   **训练细节**：
    *   每个数据集仅采样了 **16 组** 不同的架构配置进行训练和性能分析。
    *   输入图像尺寸：训练时为 512×512，部署评估时为 1024×1024。
*   **算力说明**：文中未明确给出总训练时长（GPU hours），但强调了通过回归模型大幅减少了需要实际训练的模型数量（仅 16 个样本），这比传统 NAS 节省了大量算力。

### 5. 实验数量与充分性
*   **实验规模**：针对两个主流遥感数据集进行了完整的流程验证。
*   **数据可靠性**：延迟和功耗数据取 100 次推理的平均值；mIoU 取两个独立训练随机种子的平均值。
*   **拟合优度**：代理模型的 $R^2$ 值大多在 97% 以上，证明了回归模型在缩减空间内的预测是非常准确的。
*   **充分性评价**：实验设计较为客观，涵盖了精度、延迟、功耗、能量和吞吐量（FPS）等多个维度，足以支撑其“高效调优”的论点。

### 6. 主要结论与发现
*   **性能提升**：相比基准 CM-UNet，SatReg 选择的架构在保持大部分精度的前提下，**延迟降低了 32.9%–38.6%**，**能耗降低了 43.3%–51.0%**。
*   **吞吐量**：在 LoveDA 上从 5.60 FPS 提升至 9.11 FPS。
*   **变量敏感性**：发现 **$b$（编码器宽度）对精度影响更大**，而 **$h$（解码器瓶颈宽度）对延迟和功耗更敏感**。因此，优化策略倾向于保留编码器容量而压缩解码器瓶颈。

### 7. 优点
*   **高效性**：通过将搜索空间缩减至二维并使用回归模型，极大地降低了硬件感知 NAS 的搜索成本。
*   **实用性**：直接在目标边缘硬件上进行 Profile，确保了优化结果的真实部署效果。
*   **针对性强**：专门针对新兴的 CNN-Mamba 混合架构进行了优化，解决了该类模型在边缘端部署的痛点。

### 8. 不足与局限
*   **搜索空间受限**：仅考虑了两个宽度变量，未涉及深度（层数）或拓扑结构的改变，可能错过更优的架构。
*   **硬件单一性**：实验仅在 Jetson Orin Nano 上完成，不同硬件架构（如 FPGA 或其他专用加速器）上的回归趋势可能不同。
*   **精度损失**：虽然性能大幅提升，但 mIoU 仍有约 3-4% 的下降，在某些对精度要求极高的任务中可能需要权衡。
*   **量化缺失**：实验主要基于 FP32，未探讨 INT8 或 FP16 量化对回归模型和最终性能的影响。

（完）
