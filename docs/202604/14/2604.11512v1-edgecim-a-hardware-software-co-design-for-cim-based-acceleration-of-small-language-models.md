---
title: "EdgeCIM: A Hardware-Software Co-Design for CIM-Based Acceleration of Small Language Models"
title_zh: EdgeCIM：一种用于基于存内计算的小语言模型加速的软硬件协同设计
authors: "Jinane Bazzi, Mariam Rakka, Fadi Kurdahi, Mohammed E. Fouda, Ahmed Eltawil"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11512v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 基于存内计算的小语言模型加速软硬件协同设计
tldr: EdgeCIM是一个针对边缘端小语言模型（SLM）推理的软硬件协同设计框架，旨在解决自回归解码阶段因内存受限导致的低效能问题。该框架核心采用65nm存内计算（CIM）宏，并结合分块映射策略优化流水线并行度，显著降低了DRAM带宽压力。实验表明，EdgeCIM在多款主流SLM上展现出卓越的吞吐量和能效比，为实现实时、低功耗的边缘端AI推理提供了高效的硬件解决方案。
selection_source: fresh_fetch
motivation: 边缘设备在运行小语言模型时，自回归解码阶段的GEMV操作受限于内存带宽，导致现有加速器利用率低且能耗极高。
method: 采用65nm存内计算宏结合分块映射策略，通过软硬件协同设计平衡流水线阶段并最大化计算并行度。
result: 在LLaMA3.2-1B上，EdgeCIM的吞吐量和能效比分别是NVIDIA Orin Nano的7.3倍和49.59倍，平均推理速度达336.42 tokens/s。
conclusion: EdgeCIM通过存内计算技术有效克服了边缘端SLM推理的性能瓶颈，是实现实时、高能效边缘规模模型推理的有力方案。
---

## 摘要
在笔记本电脑、智能手机和嵌入式平台等边缘设备上部署小语言模型（SLM）的需求日益增长，这暴露了现有加速器在基础效率方面的不足。虽然 GPU 能够高效处理预填充（prefill）工作负载，但自回归解码阶段主要由 GEMV 操作主导，这些操作本质上受限于内存带宽，导致边缘端的利用率低下且能耗极高。在这项工作中，我们提出了 EdgeCIM，这是一个软硬件协同设计框架，重新思考了针对端到端仅解码器（decoder-only）推理的加速器设计。其核心是一个采用 65nm 工艺实现的存内计算（CIM）宏单元，并结合了基于分块（tile-based）的映射策略，该策略能够平衡流水线阶段，在最大化并行度的同时缓解 DRAM 带宽瓶颈。我们的模拟器支持对参数量高达 4B 的 SLM 进行设计空间探索，从而在延迟和能耗方面确定帕累托最优配置。与 NVIDIA Orin Nano 相比，EdgeCIM 在 LLaMA3.2-1B 上实现了高达 7.3 倍的吞吐量提升和 49.59 倍的能效提升；在 LLaMA3.2-3B 上，其吞吐量比高通 SA8255P 高出 9.95 倍。在 TinyLLaMA-1.1B、LLaMA3.2 (1B, 3B)、Phi-3.5-mini-3.8B、Qwen2.5 (0.5B, 1.5B, 3B)、SmolLM2-1.7B、SmolLM3-3B 以及 Qwen3 (0.6B, 1.7B, 4B) 上的广泛基准测试表明，在 INT4 精度下，我们的加速器平均达到了 336.42 tokens/s 的吞吐量和 173.02 tokens/J 的能效。这些结果证明 EdgeCIM 是实现实时、高能效边缘规模 SLM 推理的一种极具竞争力的解决方案。

## Abstract
The growing demand for deploying Small Language Models (SLMs) on edge devices, including laptops, smartphones, and embedded platforms, has exposed fundamental inefficiencies in existing accelerators. While GPUs handle prefill workloads efficiently, the autoregressive decoding phase is dominated by GEMV operations that are inherently memory-bound, resulting in poor utilization and prohibitive energy costs at the edge. In this work, we present EdgeCIM, a hardware-software co-design framework that rethinks accelerator design for end-to-end decoder-only inference. At its core is a CIM macro, implemented in 65nm, coupled with a tile-based mapping strategy that balances pipeline stages, maximizing parallelism while alleviating DRAM bandwidth bottlenecks. Our simulator enables design space exploration of SLMs up to 4B parameters, identifying Pareto-optimal configurations in terms of latency and energy. Compared to an NVIDIA Orin Nano, EdgeCIM achieves up to 7.3x higher throughput and 49.59x better energy efficiency on LLaMA3.2-1B, and delivers 9.95x higher throughput than Qualcomm SA8255P on LLaMA3.2-3B. Extensive benchmarks on TinyLLaMA-1.1B, LLaMA3.2 (1B, 3B), Phi-3.5-mini-3.8B, Qwen2.5 (0.5B, 1.5B, 3B), SmolLM2-1.7B, SmolLM3-3B, and Qwen3 (0.6B, 1.7B, 4B) reveal that our accelerator, under INT4 precision, achieves on average 336.42 tokens/s and 173.02 tokens/J. These results establish EdgeCIM as a compelling solution towards real-time, energy-efficient edge-scale SLM inference.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **EdgeCIM** 的软硬件协同设计框架，旨在通过存内计算（CIM）技术解决小语言模型（SLM）在边缘设备上推理效率低下的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着生成式AI向边缘端（手机、笔记本、嵌入式设备）迁移，小语言模型（SLM，如参数量在4B以下模型）的需求激增。
*   **核心痛点**：SLM的推理分为“预填充（Prefill）”和“解码（Decoding）”两个阶段。现有加速器（如GPU/NPU）在处理解码阶段时面临严重挑战。解码阶段由**GEMV（矩阵-向量乘法）**主导，具有极低的算术强度，属于典型的**内存受限（Memory-bound）**任务，导致硬件利用率极低且能耗巨大。
*   **研究目标**：设计一种端到端的存内计算加速方案，专门优化自回归解码阶段的吞吐量和能效比。

### 2. 方法论：核心思想与关键技术
EdgeCIM 采用了软硬件协同设计的思路，包含以下核心组件：
*   **硬件架构（CIM Macro）**：
    *   采用 **65nm SRAM 基数字存内计算（DCIM）** 宏单元。
    *   **分层架构**：芯片由多个集群（Cluster）组成，每个集群包含多个分块（Tile），每个分块由 16×16 的处理单元（PE）阵列组成。
    *   支持 **INT4 和 INT8** 精度重构，通过位串行（Bit-serial）输入实现硬件复用。
*   **软件映射策略（Fine-grained Tiling）**：
    *   **Active-Tile 机制**：为了平衡计算与 DRAM 带宽，框架仅激活部分 Tile 进行计算，同时利用其余 Tile 预取下一组权重，实现计算与数据传输的高度重叠。
    *   **端到端映射**：不仅优化 Attention 机制，还对投影（Projection）、线性层（Linear）和前馈网络（FFN）进行了全流程的流水线映射。
*   **设计空间探索（DSE）**：
    *   使用**遗传算法（GA）**在约 310 万种硬件配置中搜索最优解。
    *   **目标函数**：最小化 $Latency^\alpha \times Energy^{(1-\alpha)}$，通过调节 $\alpha$ 平衡延迟与能耗。

### 3. 实验设计
*   **测试模型（SLMs）**：涵盖了主流的轻量级模型，包括 LLaMA3.2 (1B, 3B)、TinyLLaMA-1.1B、Phi-3.5-mini、Qwen2.5 (0.5B-3B)、SmolLM2/3 以及 Qwen3 系列。
*   **对比基准（Benchmarks）**：
    *   **边缘 GPU**：NVIDIA Jetson Orin Nano, Orin Nano Super, AGX Orin。
    *   **移动端 NPU/SoC**：高通 SA8255P, Snapdragon X Elite, Snapdragon 8 Elite。
    *   **学术界 SOTA**：X-Former, TranCIM, ReTransformer 等存内计算加速器。
*   **实验场景**：固定 Batch Size = 1（典型的边缘端交互场景），设置 128 个预填充 token 和 128 个生成 token。

### 4. 资源与算力说明
*   **算力实现**：该研究主要基于**分析模拟器（Analytical Simulator）**。
*   **硬件建模**：使用 **CACTI 6.0** 评估 65nm 工艺下的缓存延迟、能耗和面积；使用 **HSPICE** 对计算组件进行电路级仿真。
*   **训练/运行资源**：论文未涉及模型训练，重点在于推理加速器的性能评估。模拟器在 Python 环境下运行遗传算法进行配置搜索。

### 5. 实验数量与充分性
*   **实验规模**：针对 11 种不同的 SLM 模型进行了基准测试，涵盖了从 0.5B 到 4B 的参数范围。
*   **消融与探索**：进行了详细的设计空间探索（DSE），展示了不同 $\alpha$ 值下的帕累托前沿（Pareto Front），并分析了序列长度对能效的影响。
*   **客观性**：对比对象包含了目前市面上最先进的商用边缘计算芯片（如骁龙 8 Elite），实验数据具有较高的参考价值和说服力。

### 6. 主要结论与发现
*   **性能飞跃**：在 LLaMA3.2-1B 模型上，EdgeCIM 的吞吐量是 NVIDIA Orin Nano 的 **7.3 倍**，能效比提升了 **49.59 倍**。
*   **对比 NPU**：在 LLaMA3.2-3B 上，其吞吐量比高通 SA8255P 高出 **9.95 倍**。
*   **平均表现**：在 INT4 精度下，EdgeCIM 平均可达到 **336.42 tokens/s** 的吞吐量和 **173.02 tokens/J** 的能效。
*   **带宽瓶颈确认**：实验证实 4096 位的总线宽度在解码阶段经常处于饱和状态，证明了解码阶段确实是带宽受限。

### 7. 优点与亮点
*   **端到端优化**：不同于以往只关注 Attention 算子的研究，EdgeCIM 考虑了 Transformer 解码器的所有层，提供了完整的系统级方案。
*   **软硬协同**：通过 Active-Tile 策略巧妙地解决了 DRAM 带宽与 CIM 计算速度不匹配的问题。
*   **高度灵活**：DSE 框架允许根据不同的应用需求（侧重延迟或侧重能耗）定制最优硬件配置。

### 8. 不足与局限
*   **仿真局限**：目前成果基于模拟器和电路仿真，尚未有实际流片（Tape-out）的测试数据，实际芯片中的非理想因素（如工艺偏差、热效应）可能影响最终表现。
*   **Batch Size 限制**：研究专注于 Batch Size = 1，虽然符合单用户边缘场景，但在多任务并发或服务器端边缘节点的适用性有待验证。
*   **工艺节点**：采用 65nm 工艺建模，虽然在 CIM 研究中常见，但相比商用 NPU 采用的 3nm/4nm 先进工艺，其面积效率在实际对比中可能存在偏差。

（完）
