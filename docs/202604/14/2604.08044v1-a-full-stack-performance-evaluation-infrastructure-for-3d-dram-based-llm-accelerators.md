---
title: A Full-Stack Performance Evaluation Infrastructure for 3D-DRAM-based LLM Accelerators
title_zh: 一种面向基于 3D-DRAM 的大语言模型加速器的全栈性能评估基础设施
authors: "Cong Li, Chenhao Xue, Yi Ren, Xiping Dong, Yu Cheng, Yinbo Hu, Fujun Bai, Yixin Guo, Xiping Jiang, Qiang Wu, Zhi Yang, Zhe Cheng, Yuan Xie, Guangyu Sun"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08044v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于3D-DRAM硬件加速器的全栈性能评估
tldr: 针对大语言模型（LLM）推理中解码阶段的内存瓶颈，3D-DRAM技术展现出巨大潜力，但目前缺乏通用的开源评估工具。本文推出ATLAS，这是首个经过真实芯片验证的3D-DRAM LLM加速器全栈仿真框架。ATLAS通过统一的架构抽象和编程原语，支持多种推理场景的性能建模，并在验证中表现出极高的准确性，为3D加速器的设计空间探索提供了有力支持。
selection_source: fresh_fetch
motivation: 现有的3D-DRAM加速器评估工具大多为闭源且针对特定场景定制，缺乏通用的全栈建模方法来支持多样化的LLM推理研究。
method: 开发了名为ATLAS的仿真框架，基于商用3D-DRAM技术构建，并引入了统一的系统架构抽象与编程原语以实现灵活的性能评估。
result: "ATLAS在真实芯片验证中实现了小于8.57%的仿真误差和超过97%的性能相关性，并成功揭示了3D存储系统与微架构设计的关键权衡。"
conclusion: ATLAS作为首个开源且经过硅验证的3D-DRAM加速器仿真基础设施，将有效推动未来高性能LLM硬件架构的研究与开发。
---

## 摘要
大语言模型（LLM）在解码过程中表现出内存密集型特征，使其成为 LLM 推理的关键瓶颈。为了加速解码执行，基于混合键合（hybrid-bonding）的 3D-DRAM 已被应用于 LLM 加速器中。尽管这种新兴技术相比现有硬件提供了显著的性能提升，但目前的 3D-DRAM 加速器（3D-Accelerators）仍依赖于闭源评估工具，限制了公众获取性能分析方法的途径。此外，现有设计针对特定场景进行了高度定制，缺乏针对不同应用场景的通用且可复用的 3D 加速器全栈建模。为了填补这一基础性空白，我们提出了 ATLAS，这是首个经过芯片验证的基于 3D-DRAM 的架构级 LLM 加速器模拟框架。ATLAS 基于商业部署的多层 3D-DRAM 技术，为 3D 加速器系统架构和编程原语引入了统一抽象，以支持任意 LLM 推理场景。实片验证表明，ATLAS 的模拟误差小于等于 8.57%，与实测性能的相关性达到 97.26%-99.96%。通过使用 ATLAS 进行设计空间探索，我们展示了其指导架构设计的能力，并总结了针对不同场景下 3D-DRAM 存储系统和 3D 加速器微架构的关键见解。ATLAS 将在论文发表后开源，从而推动 3D 加速器的进一步研究。

## Abstract
Large language models (LLMs) exhibit memory-intensive behavior during decoding, making it a key bottleneck in LLM inference. To accelerate decoding execution, hybrid-bonding-based 3D-DRAM has been adopted in LLM accelerators. While this emerging technology provides strong performance gains over existing hardware, current 3D-DRAM accelerators (3D-Accelerators) rely on closed-source evaluation tools, limiting access to publicly available performance analysis methods. Moreover, existing designs are highly customized for specific scenarios, lacking a general and reusable full-stack modeling for 3D-Accelerators across diverse usecases.   To bridge this fundamental gap, we present ATLAS, the first silicon-proven Architectural Three-dimesional-DRAM-based LLM Accelerator Simulation framework. Built on commercially deployed multi-layer 3D-DRAM technology, ATLAS introduces unified abstractions for both 3D-Accelerator system architecture and programming primitives to support arbitrary LLM inference scenarios. Validation against real silicon shows that ATLAS achieves $\le$8.57% simulation error and 97.26-99.96\% correlation with measured performance. Through design space exploration with ATLAS, we demonstrate its ability to guide architecture design and distill key takeaways for both 3D-DRAM memory system and 3D-Accelerator microarchitecture across scenarios. ATLAS will be open-sourced upon publication, enabling further research on 3D-Accelerators.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ATLAS** 的全栈性能评估基础设施，专门用于基于 3D-DRAM（三维堆叠动态随机存取存储器）的大语言模型（LLM）加速器。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心瓶颈**：LLM 推理分为预填充（Prefill）和解码（Decoding）阶段。解码阶段具有极高的内存访问需求，传统的 HBM 或 LPDDR 存储系统难以匹配计算单元的吞吐量，成为主要瓶颈。
*   **技术背景**：基于混合键合（Hybrid-bonding）的 3D-DRAM 技术通过极高的 I/O 密度和极短的数据路径，提供了远超传统存储的带宽并降低了能耗。
*   **研究空白**：尽管 3D-DRAM 加速器（3D-Accelerators）潜力巨大，但现有研究存在三大缺陷：
    1.  缺乏通用的硬件架构建模（多为特定场景定制）。
    2.  缺乏统一的软件编程抽象和原语。
    3.  缺乏开源且经过真实芯片验证（Silicon-proven）的性能评估工具。
*   **论文意义**：ATLAS 是首个填补上述空白的开源全栈仿真框架，旨在指导未来 3D 加速器的设计空间探索（DSE）。

### 2. 方法论：核心思想与关键技术
ATLAS 建立了一套从硬件抽象到软件编程再到循环级仿真的完整体系：
*   **硬件架构抽象**：
    *   **3D-DRAM 模型**：将物理库（Physical Bank）组织为逻辑库（Logical Bank），支持多层堆叠和灵活的通道（Channel）交织。
    *   **加速器模板**：采用分布式多核架构，每个核心直接耦合 3D-DRAM 分区，核心间通过片上网络（NoC）通信，形成非一致性内存访问（NUMA）体系。
*   **编程模型与原语**：
    *   **层次化模型**：核心内执行采用 SPMD（单程序多数据）模型，核心间通信采用 MPMD（多程序多数据）模型。
    *   **编程原语**：提供 `tensor`（内存声明）、`copy`（数据搬运）、`gemm`（矩阵运算）以及 `split_gemm/split_attention`（算子切分）等 Python 接口。
*   **仿真框架流程**：
    1.  **热分析器**：基于 HotSpot-7.0，考虑 3D 堆叠的热效应，动态调整频率以满足散热约束。
    2.  **算子解析与分块探索**：自动将算子映射到硬件分块（Tiling），优化 DRAM 行缓存利用率。
    3.  **循环级仿真**：集成并扩展了 Ramulator2（存储）和 BookSim2（网络），实现周期精确的性能模拟。

### 3. 实验设计
*   **验证场景**：使用工业界合作伙伴提供的真实 3D-DRAM 测试芯片参数进行保真度验证。
*   **测试负载（Benchmarks）**：
    *   **云端场景**：OPT-66B、LLaMA3-70B、Mixtral-8x22B、Qwen3-235B。
    *   **边缘场景**：OPT、LLaMA3-8B、PaLM-8B。
*   **对比方法（Baselines）**：
    *   **硬件对比**：NVIDIA H200 GPU（4nm + HBM3E）。
    *   **架构对比**：Stratum（云端 3D 加速器前作）、H2-LLM（边缘 3D 加速器前作）。
*   **评估指标**：仿真误差、吞吐量（TFLOPS）、带宽利用率、能效比、温度。

### 4. 资源与算力
*   **仿真环境**：论文未详细列出运行仿真框架本身的宿主机配置，但提到仿真逻辑由约 5.4K 行 C++ 代码实现。
*   **目标硬件参数**：
    *   **云端**：7nm 工艺，16 核心，16TB/s 3D-DRAM 带宽，峰值功耗约 242W。
    *   **边缘**：28nm 工艺，16 核心，LPDDR5 接口。
*   **算力说明**：实验主要关注推理性能模拟，不涉及模型训练，因此未提及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **保真度验证**：涵盖了 2304 组 DRAM 访问案例和 712 组计算/通信案例。
    *   **设计空间探索（DSE）**：对通道交织、I/O 引脚组织、逻辑行大小、带宽分配、SRAM 大小、矩阵-向量计算比例、NoC 带宽等多个维度进行了详尽的消融实验。
*   **充分性评价**：实验设计非常全面，不仅验证了框架的准确性，还通过跨场景（云与边缘）的 DSE 证明了框架的通用性。对比 H200 等顶级商用硬件，增强了结果的说服力。

### 6. 主要结论与发现
*   **仿真精度**：ATLAS 的模拟误差 $\le$ 8.57%，与真实硬件性能的相关性高达 97.26%-99.96%。
*   **性能提升**：ATLAS 导出的优化设计在云端场景下比 H200 GPU 实现最高 3.64 倍加速，能效提升 6.66 倍；比前作 Stratum 性能提升 1.42 倍。
*   **关键见解（Takeaways）**：
    *   **存储组织**：通道交织粒度需平衡行局部性与通道并行性；增加逻辑行大小可显著提升带宽利用率。
    *   **架构平衡**：过高的带宽会挤占计算面积并引发热失控；矩阵与向量计算单元的比例（如 32:1）对 Attention 算子至关重要。
    *   **边缘特性**：边缘负载更受益于细粒度的交织和更多的存储通道。

### 7. 优点
*   **硅验证的可靠性**：与真实芯片数据对比，解决了架构研究中常见的“空中楼阁”问题。
*   **全栈覆盖**：从底层的热力学建模到上层的编程原语，提供了端到端的评估能力。
*   **开源贡献**：ATLAS 的开源将极大地降低学术界研究 3D-DRAM 加速器的门槛。
*   **热感知设计**：在 3D 堆叠芯片中引入热约束分析，使设计更具工程可行性。

### 8. 不足与局限
*   **算子覆盖**：虽然声称支持任意算子，但实验重点几乎完全集中在 Transformer 结构的 LLM 上，对于其他新兴模型（如 SSM/Mamba）的适配性讨论较少。
*   **制造假设**：仿真依赖于特定的工业制造参数（如混合键合的间距、热导率等），若制造工艺发生重大变革，部分模型参数需重新校准。
*   **应用限制**：目前主要针对推理（解码阶段）优化，未深入探讨 3D-DRAM 在模型训练或长文本预填充阶段的潜在挑战。

（完）
