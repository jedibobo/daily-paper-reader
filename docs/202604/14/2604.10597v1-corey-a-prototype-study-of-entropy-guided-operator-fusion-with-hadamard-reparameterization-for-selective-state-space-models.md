---
title: "COREY: A Prototype Study of Entropy-Guided Operator Fusion with Hadamard Reparameterization for Selective State Space Models"
title_zh: COREY：针对选择性状态空间模型的熵引导算子融合与 Hadamard 重参数化的原型研究
authors: "Bo Ma, Jinsong Wu, Hongjiang Wei, Weiqi Yan"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10597v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对SSM模型的内存感知算子融合与运行时调度
tldr: 针对Mamba等状态空间模型（SSM）在长文本推理中受限于内存带宽的问题，本文提出了COREY原型框架。该框架结合了内存感知算子融合与Hadamard特征重参数化技术，利用激活熵作为运行时调度指标来动态优化融合边界和分块大小。通过将Hadamard变换融入线性投影，有效缓解了激活值的重尾分布问题。实验表明，COREY在降低延迟、提升吞吐量和减少DRAM访问方面表现优异，为SSM的高效部署提供了新思路。
selection_source: fresh_fetch
motivation: 状态空间模型在推理时因算子碎片化和中间张量反复读写导致严重的内存带宽瓶颈。
method: 结合基于激活熵引导的动态算子融合策略与Hadamard重参数化技术，优化内存访问并平滑激活值分布。
result: 在重尾激活分布的SSM实验中，COREY显著降低了代理延迟，提升了吞吐量并减少了DRAM流量。
conclusion: 通过熵引导的融合与重参数化协同优化，可以有效提升选择性状态空间模型在硬件上的执行效率。
---

## 摘要
以 Mamba 系列为代表的状态空间模型 (SSMs) 提供了线性时间的序列建模能力，在长上下文推理方面极具吸引力。然而，由于选择性状态更新通常被分解为碎片化的算子，并伴随重复的中间张量实例化，实际部署仍受限于内存带宽。我们提出了 COREY，这是一个结合了内存感知算子融合与基于 Hadamard 的特征重参数化的原型框架。利用固定宽度直方图估计的激活熵被用作运行时调度统计量，以确定融合边界并选择分块大小。为了正则化重尾激活，我们将归一化的 Hadamard 变换吸收到线性投影中，在保持功能等效的同时降低了峰值坐标集中度。在针对重尾 SSM 激活的受控原型研究中，相比于未融合和固定深度的基准模型，COREY 一致地降低了代理延迟，提高了吞吐量，并减少了 DRAM 流量。低比特结果仅通过手工构建的稳定性代理进行报告，旨在作为诊断证据，而非针对检查点级别的质量声明。代码仓库：https://github.com/mabo1215/COREY_Transformer.git。

## Abstract
State Space Models (SSMs), represented by the Mamba family, provide linear-time sequence modeling and are attractive for long-context inference. Yet practical deployments remain memory-bandwidth limited because selective state updates are often decomposed into fragmented kernels with repeated intermediate tensor materialization. We present COREY, a prototype framework that combines memory-aware operator fusion with Hadamard-based feature reparameterization. Activation entropy, estimated with fixed-width histograms, is used as a runtime scheduling statistic to place fusion boundaries and choose tile sizes. To regularize heavy-tailed activations, we absorb normalized Hadamard transforms into linear projections, preserving functional equivalence while reducing peak-coordinate concentration. In a controlled prototype study over heavy-tailed SSM activations, COREY consistently reduces proxy latency, improves throughput, and lowers DRAM traffic relative to unfused and fixed-depth baselines. Low-bit results are reported only through a hand-crafted stability proxy and are intended as diagnostic evidence rather than checkpoint-level quality claims. Code repository: https://github.com/mabo1215/COREY_Transformer.git.

---

## 论文详细总结（自动生成）

### 论文总结：COREY —— 针对选择性状态空间模型的熵引导算子融合与 Hadamard 重参数化研究

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然以 Mamba 为代表的选择性状态空间模型（SSM）在理论上具有线性时间复杂度 $O(L)$，但在实际 GPU 部署中，其性能往往受限于**内存带宽**而非计算能力。这是因为 SSM 的状态更新被分解为多个碎片化的算子，导致中间张量频繁地在显存（DRAM）和片上内存之间往返。
*   **核心问题**：如何减少中间张量的实例化开销，并处理 SSM 内部激活值中存在的“重尾分布”（离群值），这些离群值会降低低比特量化的精度并限制算子融合的深度。
*   **整体含义**：论文提出了 **COREY** 框架，试图通过“分布感知”的系统优化，将激活值的统计特性（熵）与硬件层面的算子融合调度相结合。

#### 2. 方法论：核心思想与关键技术
COREY 的核心思想是利用**激活熵**作为运行时信号，动态指导算子融合边界和分块（Tiling）策略，并结合 **Hadamard 重参数化**来平滑激活分布。
*   **熵引导的融合策略**：
    *   使用固定宽度的直方图实时估计激活值的 Shannon 熵。
    *   **融合评分公式**：$S(R) = \alpha eH(R) + \beta fAI(R) - \gamma fM(R)$。其中 $eH$ 是归一化熵，$fAI$ 是算术强度，$fM$ 是内存流量估计。
    *   当评分超过阈值 $\tau$ 且满足硬件资源约束（寄存器、共享内存）时，执行算子融合。
*   **Hadamard 重参数化**：
    *   在投影层 $y = Wx$ 中插入 Hadamard 变换矩阵 $H$：$y = (WH^\top)(Hx)$。
    *   利用 Hadamard 变换的旋转特性，将集中的离群值能量分散到各个通道，从而提高激活熵，扩大安全融合的深度。
*   **熵-分块协同设计**：
    *   高熵区域（分布均匀）：使用较大的分块以提高数据复用率。
    *   低熵区域（分布集中）：使用较小的分块以减轻寄存器压力并防止数值不稳定。

#### 3. 实验设计
*   **实验场景**：分为“受控原型研究”和“检查点级别验证”两个阶段。
*   **数据集**：
    *   **原型研究**：使用具有重尾特性的合成激活数据。
    *   **检查点验证**：使用预训练的 Mamba 模型（370M, 1.4B, 2.8B），在 LongBench（长文本理解）、WikiText-103 和 PG19 数据集上进行测试。
*   **Benchmark 与对比方法**：
    *   **No Fusion**：算子逐个执行，中间结果全部写回显存。
    *   **Static Fusion**：固定大小（如 3 个算子）的贪婪融合，不考虑熵信号。
    *   **COREY (Ours)**：基于熵引导的动态融合。
    *   **外部基准**：对比了 Pythia-410M 模型。

#### 4. 资源与算力
*   **本地环境**：Intel Core i9-13900K CPU, 64GB RAM, **NVIDIA RTX 3070** (8GB)。
*   **远程环境**：4 张 **NVIDIA RTX 3090** (24GB) GPU，用于验证跨硬件的可重复性和多 GPU 并行推理。
*   **软件栈**：Python 3.11, PyTorch 2.3, Triton 3.6.0 (在 WSL2 环境下)。
*   **训练/推理时长**：文中未明确给出总训练时长，但详细记录了不同序列长度下的推理延迟（ms/token）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 1k 到 64k 的多种序列长度分段。
    *   进行了针对阈值 $\tau$、分块策略、量化比特位（FP16, W8A8, W4A8）的消融实验。
    *   每个数据点通常基于 5 次重复实验的平均值。
*   **充分性评价**：实验设计较为系统，涵盖了性能（吞吐量、延迟）、资源（DRAM 流量）和质量（量化稳定性代理）三个维度。然而，由于目前仍处于“原型研究”阶段，缺乏在大规模真实任务上的端到端 Triton 内核性能实测，实验的充分性主要体现在理论验证和模拟成本模型上。

#### 6. 主要结论与发现
*   **性能提升**：在超长序列（64k）下，COREY 相比未融合基准显著提升了吞吐量，并减少了约 26% 的 DRAM 流量。
*   **分布平滑**：Hadamard 重参数化确实提高了激活熵，并有效降低了离群值对低比特量化稳定性的负面影响。
*   **序列长度相关性**：随着序列长度增加，内存瓶颈愈发明显，COREY 带来的加速比也随之扩大。
*   **硬件无关性**：在 RTX 3070 和 3090 上的测试结果一致，证明了该调度逻辑的通用性。

#### 7. 优点（亮点）
*   **创新视角**：首次提出将“激活熵”这一统计学指标直接引入算子融合的运行时调度决策中。
*   **协同优化**：将算法层面的分布平滑（Hadamard）与系统层面的内存优化（算子融合）紧密结合，形成闭环。
*   **理论支撑**：论文提供了关于熵增长、融合深度界限和量化稳定性的数学证明，增强了方法的说服力。

#### 8. 不足与局限
*   **原型阶段限制**：目前的延迟和吞吐量数据多基于 Python 层的模拟成本模型，而非生产级的 Triton 内核实测，实际硬件加速效果可能受内核启动开销影响。
*   **任务覆盖较窄**：LongBench 测试仅覆盖了 4 个子任务，且每个任务仅抽取了 20 个样本，样本量较小。
*   **调优成本**：熵阈值 $\tau$ 和评分权重 ($\alpha, \beta, \gamma$) 可能需要针对不同模型进行手动微调，缺乏完全自动化的超参数搜索。
*   **量化评估**：低比特实验使用的是“诊断代理指标”，而非真实的检查点微调后的准确率，参考价值有限。

（完）
