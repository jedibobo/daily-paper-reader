---
title: "DRIFT: Harnessing Inherent Fault Tolerance for Efficient and Reliable Diffusion Model Inference"
title_zh: DRIFT：利用固有容错性实现高效可靠的扩散模型推理
authors: "Jinqi Wen, Tong Xie, Runsheng Wang, Meng Li"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09073v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对能效扩散模型推理的算法-架构协同优化
tldr: 针对扩散模型推理的高能耗和高延迟问题，本文提出DRIFT框架。该框架通过深入分析模型的固有容错性，结合细粒度的弹性感知DVFS策略，对敏感模块和时间步进行重点保护。同时，引入基于回滚的算法级容错机制（ABFT）来纠正关键错误，并优化内存布局。实验表明，DRIFT在保持生成质量的同时，能显著降低能耗或提升推理速度。
selection_source: fresh_fetch
motivation: 现有的DVFS技术在提升扩散模型推理效率时，往往因忽视模型固有的容错特性而导致效率受限或生成质量下降。
method: 提出一种结合细粒度弹性感知DVFS策略、回滚式算法容错机制以及内存布局优化的软硬件协同优化框架。
result: "在多种模型和数据集上，DRIFT实现了平均36%的节能或1.7倍的推理加速，且不损失生成质量。"
conclusion: 利用扩散模型的固有容错性进行针对性优化，是实现高效且可靠的生成式AI推理的有效途径。
---

## 摘要
尽管扩散模型在视觉生成任务中表现优异，但其部署一直受困于高能耗和高推理延迟。动态电压频率调节（DVFS）为挖掘底层加速器的潜力提供了一种极具前景的解决方案。然而，现有方法往往由于忽视了扩散模型固有的容错性，导致效率提升有限或输出质量下降。因此，在本文中，我们提出了 DRIFT，这是一个新颖的算法-架构协同优化框架，旨在利用容错性实现高效且可靠的扩散模型推理。我们首先对代表性扩散模型进行了全面的韧性分析。基于这些观察结果，我们引入了一种细粒度的韧性感知 DVFS 策略，该策略选择性地保护对错误敏感的网络块和时间步；此外，我们还提出了一种基于回滚算法的容错（ABFT）机制，通过回退到之前的时间步来适应性地仅纠正关键错误。我们进一步优化了卸载间隔并重新组织了数据布局，以减少内存开销。在不同模型和数据集上的实验表明，在保持生成质量的同时，DRIFT 通过电压欠标定平均可实现 36% 的节能，或者通过超频实现 1.7 倍的加速。

## Abstract
Diffusion model deployment has been suffering from high energy consumption and inference latency despite its superior performance in visual generation tasks. Dynamic voltage and frequency scaling (DVFS) offers a promising solution to exploit the potential of the underlying accelerators. However, existing approaches often lead to either limited efficiency gains or degraded output quality because they overlook the inherent fault tolerance of the diffusion model. Therefore, in this paper, we propose DRIFT, a novel algorithmarchitecture co-optimization framework that harnesses the fault tolerance for efficient and reliable diffusion model inference. We first perform a comprehensive resilience analysis on representative diffusion models. Building on these observations, we introduce a fine-grained, resilience-aware DVFS strategy that selectively protects error-sensitive network blocks and timesteps, and a rollback algorithm-based fault tolerance (ABFT) mechanism that adaptively corrects only critical errors by reverting to previous timesteps. We further optimize offloading intervals and reorganize data layouts to reduce memory overhead. Experiments across diverse models and datasets show that DRIFT can achieve on average 36% energy savings through voltage underscaling or 1.7x speedup via overclocking while maintaining generation quality.

---

## 论文详细总结（自动生成）

### DRIFT：利用固有容错性实现高效可靠的扩散模型推理 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
扩散模型（Diffusion Models）在图像生成领域表现卓越，但其推理过程涉及数十至数百次迭代，导致**高能耗**和**高延迟**，限制了在边缘设备上的部署。
*   **研究动机**：动态电压频率调节（DVFS）是降低功耗和提升速度的常用手段，但激进的 DVFS（如欠压或超频）会导致硬件产生定时错误（Timing Errors），表现为计算过程中的随机比特翻转，从而严重破坏生成图像的质量。
*   **核心问题**：如何在不牺牲生成质量的前提下，利用扩散模型自身的容错特性，通过激进的 DVFS 提升推理效率？

#### 2. 方法论：核心思想与关键技术
论文提出了 **DRIFT** 框架，通过算法与架构的协同设计，包含以下四个关键环节：
*   **韧性特征分析（Resilience Characterization）**：
    *   **比特级**：高位比特翻转对质量影响巨大，低位比特影响微弱。
    *   **时间步级**：早期去噪步骤（构建全局结构）比后期步骤（细化纹理）更敏感。
    *   **模块级**：网络的前几层（Transformer Block）和嵌入层（Embedding）对错误最敏感。
    *   **自纠错能力**：观察到扩散过程具有天然的“平滑”能力，能部分抵消中间步骤产生的微小偏差。
*   **细粒度韧性感知 DVFS**：
    *   对“错误敏感”的模块和时间步采用标准电压/频率（保护质量）。
    *   对“错误韧性”的高占比计算任务采用激进的 DVFS（降低电压或提高频率）。
*   **回滚式算法容错机制（Rollback-ABFT）**：
    *   **检测**：利用校验和（Checksum）检测大误差。
    *   **纠正**：不同于传统的重计算（开销大），DRIFT 利用扩散模型相邻时间步激活值的相似性，直接用前一时间步的对应值替换当前错误值（即“回滚”）。
*   **内存访问优化**：
    *   **间隔检查点**：每隔 $n$ 步存储一次检查点以减少 DRAM 带宽压力。
    *   **数据布局重排**：将 Tile 数据重新打包为连续布局，提高 DRAM 行激活效率。

#### 3. 实验设计
*   **数据集/场景**：ImageNet（无条件生成）、COCO val2017（条件生成）、DrawBench（复杂文本生成）。
*   **模型（Benchmark）**：DiT-XL512（基于 Transformer）、PixArt-alpha、Stable Diffusion v1.5（基于 UNet）。
*   **对比方法**：
    *   **基准线**：标准电压频率运行。
    *   **可靠性增强方法**：ThUnderVolt（跳过故障计算）、ApproxABFT（异常值置零）。
    *   **恢复效率方法**：DMR（双模块冗余）、Stat ABFT（基于统计的重计算）。

#### 4. 资源与算力
*   **硬件仿真**：使用商业 **14nm PDK** 工艺库进行综合。
*   **工具链**：Synopsys PrimeTime 和 HSPICE 用于定时分析；PyTorch Hook 机制用于错误注入仿真；**SCALE-Sim** 用于周期级架构仿真。
*   **算力说明**：文中未明确提及模型训练所需的 GPU 资源（因为侧重于推理阶段的硬件加速器设计），但详细列出了加速器配置（64 个脉动阵列、HBM2 内存、2GHz 标称频率）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了四种主流模型与数据集组合，进行了大规模的错误注入实验。
*   **消融实验**：针对 DVFS 策略、Rollback-ABFT 机制、数据重排效果分别进行了验证。
*   **设计空间探索**：研究了 ABFT 阈值、检查点间隔 $n$、脉动阵列尺寸对性能的影响。
*   **充分性评价**：实验设计非常充分，不仅关注了客观指标（CLIP, ImageReward, LPIPS, FID），还展示了定性的视觉结果，并验证了与现有加速算法（如 TaylorSeer）的兼容性，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **效率提升**：在保持图像质量几乎不变的前提下，DRIFT 通过欠压实现了平均 **36% 的节能**，或通过超频实现了 **1.7 倍的推理加速**。
*   **容错阈值**：通过 Rollback-ABFT，模型对比特错误率（BER）的容忍度从 $10^{-8}$ 提升到了 $3 \times 10^{-3}$。
*   **内存开销**：经过优化的内存管理使额外的 DRAM 访问开销控制在 3% 以内。

#### 7. 优点与亮点
*   **视角独特**：首次深入分析了扩散模型在时间步和模块层面的异构韧性，并发现了其自纠错行为。
*   **协同优化**：巧妙地将算法层的“时间相似性”与硬件层的“DVFS 定时错误”结合，用低成本的“回滚”代替高成本的“重计算”。
*   **通用性强**：方案不依赖于特定的模型架构（Transformer 或 UNet 均适用），且与现有的剪枝、量化、缓存等加速技术正交。

#### 8. 不足与局限
*   **硬件依赖**：该方案需要特定的硬件支持（如 ABFT 包装电路、LDO/ADPLL 调节模块），对于现有的通用 GPU/CPU 部署可能难以直接应用。
*   **极端情况风险**：虽然假设成对的大误差抵消概率极低，但在极端恶劣的硬件环境下，ABFT 的检测能力可能存在理论上限。
*   **应用限制**：主要针对计算密集型的算子（如 GEMM），对于非线性算子或内存受限型算子的错误处理讨论较少。

（完）
