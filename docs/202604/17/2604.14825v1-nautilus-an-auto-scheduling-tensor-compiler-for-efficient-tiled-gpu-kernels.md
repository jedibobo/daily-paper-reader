---
title: "Nautilus: An Auto-Scheduling Tensor Compiler for Efficient Tiled GPU Kernels"
title_zh: Nautilus：一种用于高效分块 GPU 算子的自动调度张量编译器
authors: "Yifan Zhao, Yuchen Yang, Matei Budiu, Sasa Misailovic"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14825v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于GPU算子优化的自动调度张量编译器
tldr: Nautilus 是一款新型自动调度张量编译器，旨在实现从数学描述到高效 GPU 核函数的全自动优化。它采用逐级降低的设计，将高层代数优化与底层分块优化结合，通过创新的自动调度器探索复杂的优化空间。Nautilus 是首个能从数学定义自动生成类似 FlashAttention-3 高性能核函数的编译器，在 GH200 和 RTX 5090 上性能显著优于现有 SOTA 编译器和手动优化的 cuDNN。
selection_source: fresh_fetch
motivation: 现有的张量编译器难以在保持程序结构的同时，自动处理高层代数变换与底层分块优化之间的复杂权衡。
method: 提出一种逐级降低的架构和新型自动调度器，支持全局变换并能自动发现最优优化序列，从而生成高效的分块 GPU 核函数。
result: "在 GH200 和 RTX 5090 上分别比现有 SOTA 编译器提升高达 23% 和 42% 的吞吐量，且在长序列配置下达到或超过手动编写的 cuDNN 性能。"
conclusion: Nautilus 证明了通过端到端的自动调度，编译器可以从纯数学描述中自动发现最先进的注意力机制优化算法，大幅减轻了开发者的负担。
---

## 摘要
我们提出了 Nautilus，这是一种新型张量编译器，旨在实现从数学描述到算子优化的全自动化。Nautilus 将张量算子的高级代数规范编译为高效的分块 GPU 算子。Nautilus 的逐级降低（successive lowering）设计允许在单个端到端系统中共同应用高级优化、表达式重写和分块优化。Nautilus 提出了一种新型自动调度器，它在发现高级优化序列的同时，保留了分块优化器所需的规则程序结构。Nautilus 的自动调度器捕捉了高级优化中复杂的交互和权衡，包括诸如高级归约融合（advanced reduction fusion）之类的激进全局变换。Nautilus 是首个能够从类数学的注意力机制描述出发，并自动发现类似 FlashAttention-3 算子的端到端张量编译器，从而将全部优化负担从程序员转移到了编译器。在 NVIDIA GH200 和 RTX 5090 GPU 上的 5 个 Transformer 模型和 150 个评估配置中，Nautilus 在 GH200 上的吞吐量比最先进的编译器高出多达 23%，在 RTX 5090 上高出多达 42%，同时在许多长序列配置中达到或超过了手动编写的 cuDNN 算子。

## Abstract
We present Nautilus, a novel tensor compiler that moves toward fully automated math-to-kernel optimization. Nautilus compiles a high-level algebraic specification of tensor operators into efficient tiled GPU kernels. Nautilus's successive lowering design allows high-level optimizations, expression rewrites, and tile optimizations to be jointly applied in a single end-to-end system. Nautilus presents a novel auto-scheduler that discovers sequences of high-level optimizations, while preserving the regular program structure needed by tile optimizers. Nautilus's auto-scheduler captures complex interactions and trade-offs in the high-level optimizations, including aggressive global transformations like advanced reduction fusion. Nautilus is the first end-to-end tensor compiler capable of starting from a math-like description of attention and automatically discovering FlashAttention-3-like kernels, offloading the entire burden of optimization from the programmer to the compiler. Across five transformer-based models and 150 evaluation configurations on NVIDIA GH200 and RTX 5090 GPUs, Nautilus achieves up to 23% higher throughput than state-of-the-art compilers on GH200 and up to 42% on RTX 5090, while matching or exceeding manually written cuDNN kernels on many long-sequence configurations.

---

## 论文详细总结（自动生成）

这是一份关于论文《Nautilus: An Auto-Scheduling Tensor Compiler for Efficient Tiled GPU Kernels》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何弥合高层数学规范（如 PyTorch/JAX 代码）与底层高效 GPU 核函数（如 CUDA/CUTLASS）之间的性能鸿沟。
*   **研究动机**：
    *   **手动优化难**：编写高性能算子（如 FlashAttention）需要极高的硬件和算法专业知识，且难以随架构迭代移植。
    *   **现有编译器局限**：传统的调度编译器（如 TVM）搜索空间过大且难以处理复杂的全局变换；而分块编译器（如 Triton）仍需程序员手动控制高层优化（如算子融合）。
    *   **目标**：实现“数学到核函数”（Math-to-Kernel）的全自动优化，让编译器能从简单的数学描述中自动发现类似 FlashAttention-3 的复杂优化策略。

### 2. 方法论
Nautilus 采用了一种**逐级降低（Successive Lowering）**的架构，核心包含以下关键技术：
*   **三层 IR 抽象**：
    1.  **Scalar IR（标量 IR）**：用于高层调度，通过“块图”（Block Graph）捕捉数据依赖和循环结构，便于进行算子融合。
    2.  **VR-Tile IR（虚拟寄存器分块 IR）**：**本文创新点**。将分块表达为局部变量而非内存访问，便于进行代数表达式重写（如利用 $e^x = 2^{x \cdot \log_2 e}$ 等硬件友好变换）。
    3.  **MA-Tile IR（内存访问分块 IR）**：暴露显式的分块数据移动，对接底层分块优化器。
*   **新型自动调度器（Auto-Scheduler）**：
    *   **高级归约融合（Advanced Reduction Fusion）**：引入“滚动更新”（Rolling Update）技术，能够自动融合具有复杂依赖的归约循环（如 Softmax 与 MatMul 的融合）。
    *   **双层分块（Bi-level Tiling）**：将循环拆分为外层（映射到 GPU 块）和内层（保持规则结构以适配分块优化器）。
    *   **正则化与后端选择**：自动调整程序结构以适配不同的底层后端（Triton, Tawa, TileLang），并根据性能动态选择。
*   **自动调优（Auto-Tuner）**：基于 MetaSchedule 框架，对分块大小、流水线阶段、线程映射等参数进行实测调优。

### 3. 实验设计
*   **模型与算子**：评估了 5 个主流模型（GLM-4, Llama 2, Qwen 2, Qwen 3, ViT），涵盖全局注意力、因果注意力（Causal）和分组查询注意力（GQA）。
*   **场景配置**：包括 Prefill（预填充）和 Decoding（解码）阶段，序列长度从 1k 到 32k，支持 FP16 和 FP8 精度。
*   **对比基准（Benchmarks）**：
    *   **编译器**：Triton, Tawa, TileLang, Helion, FlexAttention (PyTorch), TVM。
    *   **手动优化库**：cuDNN, FlashAttention-2, FlashInfer, ThunderKittens, Tri-Dao CUTLASS。

### 4. 资源与算力
*   **硬件平台**：
    1.  **NVIDIA GH200** (Grace Hopper, Hopper 架构)。
    2.  **NVIDIA RTX 5090** (Blackwell 架构)。
*   **算力消耗**：
    *   **调优预算**：每个配置运行 256 次实测采样。
    *   **耗时**：自动调度搜索通常在 1 分钟内完成；自动调优（编译加运行）耗时从几秒到 10 分钟不等。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **150 组评估配置**。
*   **充分性**：实验覆盖了从老牌 Hopper 到最新 Blackwell 架构的跨度，对比了多达 12 种 SOTA 基准方法。
*   **客观性**：除了性能（吞吐量/延迟），论文还专门在附录中测试了**数值稳定性**（与 FP64 基准对比 RMS 误差），证明了其表达式重写优化的正确性。

### 6. 主要结论
*   **性能领先**：在 GH200 上比现有最强编译器提升达 **23%**，在 RTX 5090 上提升达 **42%**。
*   **比肩手动优化**：在长序列配置下，Nautilus 自动生成的核函数性能达到甚至超过了高度优化的 **cuDNN** 和 **FlashAttention-2**。
*   **自动化能力**：Nautilus 是首个能从纯数学描述中自动推导出类似 FlashAttention-3 优化逻辑的端到端系统。

### 7. 优点
*   **全自动流程**：真正实现了从数学公式到高性能代码的自动化，极大降低了 AI 算子开发的门槛。
*   **IR 设计巧妙**：VR-Tile IR 解决了在分块级别进行代数优化的难题。
*   **后端通用性**：不绑定单一后端，能同时利用 Triton、TileLang 等多种工具的优势。
*   **硬件前瞻性**：在尚未普及的 RTX 5090 (Blackwell) 上展现了卓越的自适应能力。

### 8. 不足与局限
*   **搜索时间**：虽然比 Mirage 等超优化器快，但每个算子 10 分钟的调优时间在动态编译场景下仍显略长。
*   **算子覆盖**：目前主要集中在注意力机制及其变体，对于非规则或非线性程度极高的算子支持情况尚不明确。
*   **依赖底层后端**：Nautilus 的最终性能仍受限于底层分块编译器（如 Triton）的生成质量，若后端编译器在特定架构上表现不佳，Nautilus 也会受限。

（完）
