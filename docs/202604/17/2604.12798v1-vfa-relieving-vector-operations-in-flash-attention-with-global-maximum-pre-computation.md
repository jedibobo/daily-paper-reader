---
title: "VFA: Relieving Vector Operations in Flash Attention with Global Maximum Pre-computation"
title_zh: VFA：通过全局最大值预计算缓解 Flash Attention 中的向量操作
authors: "Yupeng Sun, Yanzhao Li, Zhiqiang Zou, Bai Du, Zhiyuan Zhang, Hui Dong, Gaoyige Fan, Hui Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12798v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 减少注意力机制内核中矢量受限延迟的硬件友好方法
tldr: FlashAttention 虽然解决了内存问题，但其在线 Softmax 中的向量操作（如 rowmax 规约和重缩放）在高性能硬件上逐渐成为瓶颈。本文提出 VFA，通过预计算全局最大值的近似值来初始化运行最大值，并结合键块重排序（优先处理 Sink 和局部块）来尽早稳定最大值，从而减少后续的规约和重缩放开销。实验表明，VFA 在保持精度的同时显著提升了推理速度，在特定硬件配置下可实现数倍加速。
selection_source: fresh_fetch
motivation: 随着张量核心吞吐量接近极限，FlashAttention 中在线 Softmax 的非矩阵乘法向量操作（如规约和重缩放）成为了主要的性能瓶颈。
method: 提出 VFA 方法，通过键块摘要初始化最大值并重排序块遍历顺序，在最大值稳定后冻结更新以消除重复的向量计算。
result: 在 MMLU 和 MATH500 等基准测试中，VFA 和 VSA 在不损失性能的前提下，相比基准模型实现了近 2 倍甚至最高 6 倍的加速。
conclusion: VFA 有效缓解了在线 Softmax 的向量计算瓶颈，是优化现代加速器上注意力机制性能的一种硬件友好型方案。
---

## 摘要
FlashAttention 风格的在线 Softmax 通过在片上内存中流式传输评分分块，并维护运行最大值和归一化因子，实现了具有线性内存复杂度的精确注意力计算。然而，随着注意力算子在现代加速器上接近张量核心/立方核心的峰值吞吐量，在线 Softmax 的非矩阵乘法组件——特别是每个分块的行最大值和行和归约以及重缩放链——可能会受到向量或 SIMD 限制，并主导延迟。本文重新审视了 FlashAttention，并提出了向量缓解 Flash Attention（VFA），这是一种硬件友好的方法，在保留在线 Softmax 结构的同时，减少了由行最大值驱动的运行最大值更新。VFA 通过键块表示的廉价近似来初始化运行最大值，重新排序键块遍历以优先处理高影响力的汇聚块和局部块，并冻结剩余块的最大值以避免重复的归约和重缩放。我们进一步将 VFA 与块稀疏跳过方法（如 BLASST）集成，形成向量缓解稀疏注意力（VSA），从而同时减少块数量和每个块的开销。值得注意的是，VFA 和 VSA 完全避免了 FA4.0 更新阶段中使用的条件重缩放操作。在包括 MMLU 和 MATH500 在内的基准测试上的广泛评估，结合注意力统计数据，验证了我们的设计：(i) 汇聚和局部重排序能尽早稳定运行最大值；(ii) 简单的 Q 和 K 块摘要由于块内异质性而失败；(iii) 当最大值出现在中间块时，需要进行 m 初始化。总体而言，VFA 和 VSA 在不损失性能的情况下，有效地缓解了在线 Softmax 的归约瓶颈。与 C16V32 基准相比，C8V32、C4V32 和 C4V16 在现代硬件上达到向量瓶颈时实现了近两倍的加速。随着即将到来的架构改进，C4V16 将通过增强指数计算能力提供六倍的加速。

## Abstract
FlashAttention-style online softmax enables exact attention computation with linear memory by streaming score tiles through on-chip memory and maintaining a running maximum and normalizer. However, as attention kernels approach peak tensor-core/cube-core throughput on modern accelerators, non-matmul components of online softmax -- especially per-tile rowmax and rowsum reductions and rescale chains -- can become vector or SIMD limited and dominate latency. This paper revisits FlashAttention and proposes Vector Relieved Flash Attention (VFA), a hardware-friendly method that reduces rowmax-driven updates of the running maximum while retaining the online-softmax structure. VFA initializes the running maximum via a cheap approximation from key-block representations, reorders key-block traversal to prioritize high-impact sink and local blocks, and freezes the maximum for remaining blocks to avoid repeated reductions and rescaling. We further integrate VFA with block-sparse skipping methods such as BLASST to form Vector Relieved Sparse Attention (VSA), which reduces both block count and per-block overhead. Notably, VFA and VSA completely avoid the conditional rescale operation in the update stage used in FA4.0. Extensive evaluations on benchmarks including MMLU and MATH500, together with attention statistics, verify our design: (i) sink and local reordering stabilizes the running maximum early; (ii) simple Q and K block summaries fail due to intra-block heterogeneity; (iii) m-initialization is required when maxima appear in middle blocks. Overall, VFA and VSA efficiently alleviate online-softmax reduction bottlenecks without performance loss. Compared to the C16V32 baseline, C8V32, C4V32 and C4V16 achieve nearly two times speedup on modern hardware while hitting the vector bottleneck. With upcoming architecture improvements, C4V16 will deliver six times speedup by enhancing exponent capacity.

---

## 论文详细总结（自动生成）

这是一份关于论文《VFA: Relieving Vector Operations in Flash Attention with Global Maximum Pre-computation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在现代硬件加速器上，随着张量核心（Tensor Core）吞吐量的不断提升，FlashAttention（FA）中的**非矩阵乘法（non-matmul）操作**（如在线 Softmax 中的行最大值 `rowmax` 规约、行和 `rowsum` 规约以及重缩放 `rescale` 链）逐渐成为性能瓶颈。这些操作受限于向量/SIMD 单元的带宽和计算能力，导致算子无法达到理论峰值。
*   **研究背景**：FlashAttention 通过分块和在线统计更新解决了内存带宽问题，但频繁的统计更新（每个分块都要更新最大值并重缩放已有的累加结果）引入了大量的向量指令开销。

### 2. 核心方法论
论文提出了 **VFA (Vector Relieved Flash Attention)**，其核心思想是通过减少运行最大值（running maximum）的更新频率来缓解向量计算压力。主要包含三个关键技术步骤：
1.  **快速 m-初始化 (m-initialization)**：在进入主循环前，利用预计算的键块（Key-block）摘要（使用 `sabsmax` 算子，即保留符号的绝对值最大值）与查询向量 $Q$ 进行廉价的点积，获得每行运行最大值的一个高质量初始近似。
2.  **汇聚+局部块重排序 (Sink + Local Reordering)**：改变键块的遍历顺序，优先处理“汇聚块”（序列开头的 Sink tokens）和“局部块”（当前 Query 附近的块）。经验表明，注意力权重的最大值通常出现在这些区域。
3.  **最大值冻结 (Max-freezing)**：在处理完上述关键块后，如果运行最大值已趋于稳定，则在后续剩余块的计算中冻结该最大值。这样可以跳过每块的 `rowmax` 计算和复杂的 `rescale` 乘法链，仅进行指数累加。
*   **扩展方法 VSA**：将 VFA 与动态块稀疏跳过方法（如 BLASST）结合，既减少了处理的块数，又降低了每个块的向量计算开销。

### 3. 实验设计
*   **模型对象**：Qwen3-30B-A3B、Qwen3-8B、Llama3-8B。
*   **数据集/任务**：
    *   数学推理：MATH500。
    *   综合知识：MMLU（子集如抽象代数、计算机安全等）。
    *   代码生成：HumanEval。
    *   中文评估：CMMLU（大学数学、哲学）。
*   **基准方法 (Benchmark)**：主要对比 FlashAttention-2 (FA2)。
*   **对比维度**：下游任务准确率（Accuracy/Pass@1）和算子层面的延迟分解。

### 4. 资源与算力
*   **硬件环境**：论文提到了基于“最新架构”（Latest Architecture）的评估，使用了不同的计算/向量比例配置（如 C16V32, C8V32, C4V16 等，其中 C 代表张量核心能力，V 代表向量核心能力）。
*   **具体算力说明**：文中**未明确说明**具体的 GPU 型号（如 H100 或 A100）或具体的训练/推理总时长，而是侧重于算子层面的模拟和实测延迟比例分析。从作者隶属华为技术有限公司来看，实验可能基于昇腾（Ascend）系列硬件或类似的自研架构。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流模型系列上进行了 7 类任务的准确率测试。
*   **消融实验**：非常充分。作者分别验证了 m-初始化、`sabsmax` 表示法、重排序策略以及行级/块级近似对准确率的影响。
*   **统计分析**：提供了关于运行最大值稳定位置、注意力热图、块相似度以及范数变异性的深入数值分析，从底层数据分布解释了方法生效的原因。
*   **客观性**：实验涵盖了中英文、代码和数学等多种场景，证明了该近似方法在不同分布下的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：在当前硬件配置下，VFA 相比基准可实现约 **2 倍**的加速；在未来优化了指数运算能力的架构上，加速比有望达到 **6 倍**。
*   **精度保持**：VFA 在所有测试任务上均能保持与原始 FlashAttention 相当的准确率，平均波动极小（±0.001 左右）。
*   **统计特性**：实验证实注意力最大值确实在 Sink 和 Local 块处极快稳定，这为“最大值冻结”策略提供了理论支撑。
*   **互补性**：VFA 与 FA4.0 的指数近似技术以及 BLASST 的稀疏跳过技术是互补的，可以叠加使用。

### 7. 优点（亮点）
*   **硬件感知设计**：精准捕捉到了 Tensor Core 进化过程中 Vector Core 成为瓶颈的趋势。
*   **算法简洁性**：不需要修改模型权重，也不需要复杂的离线训练，是一种即插即用的内核级优化。
*   **理论与实践结合**：通过详尽的注意力统计数据（如最大值稳定位置分布）支撑了算法设计的合理性。

### 8. 不足与局限
*   **特定模式依赖**：VFA 依赖于 Sink 和 Local 块包含最大值的经验假设。对于某些注意力分布极其分散或具有特殊长程依赖的任务，该假设可能失效，导致精度下降。
*   **初始化开销**：虽然 m-初始化是廉价的，但在极短序列或小模型上，预计算键块摘要的开销可能会抵消掉向量计算节省的时间。
*   **硬件依赖性**：加速效果高度依赖于硬件中 Tensor Core 与 Vector Core 的吞吐比例，在向量单元非常强大的旧架构上收益可能不明显。

（完）
