Title: Breaking the KV Cache Bottleneck: Fan Duality Model Achieves O(1) Decode Memory with Superior Associative Recall

URL Source: https://arxiv.org/pdf/2604.07716v2

Published Time: Tue, 14 Apr 2026 00:40:16 GMT

Number of Pages: 6

Markdown Content:
# Breaking the KV Cache Bottleneck: Fan Duality Model Achieves O(1) Decode Memory with Superior Associative Recall 

Yasong Fan 

Independent Researcher 

rnzfjau@uoem.edu.gr 

## Abstract 

We present FDM (Fan Duality Model), a linear sequence architecture that resolves the fundamental tension between memory efficiency and associative recall in sequence modeling. FDM separates sequence processing into two components: a wave component (recurrent scan via phase-preserving Givens rotations) that compresses long-range patterns into a fixed-size complex hidden state, and a 

particle component (local-global cache) that retrieves specific tokens via learned associative addressing with W +K=272 slots independent of sequence length 

N . This yields strictly O(1) decode memory : 867 MB fixed across all prompt lengths 128–8,192 tokens, versus Transformer’s 853–4,247 MB ( 4.9× reduction at 

N =8 ,192 ). Beyond the architecture, we discover that jointly training the wave and particle components leads to suboptimal convergence. We propose Freeze-Scan , a two-phase training strategy that freezes the recurrent scan and optimizes the cache jointly with embeddings, achieving PPL = 64.9 on WikiText-103 in 44K steps—a 

7.5 × improvement over full fine-tuning (PPL = 487). On Multi-Query Associative Recall (MQAR), FDM achieves 0.966 accuracy, surpassing Transformer (0.606) by 59.5%, while pure scan without cache scores only 0.011, confirming the necessity of the particle component. Finally, we introduce Holographic Reference Beam Decoding , interpreting the complex hidden state ht as a holographic plate encoding the entire temporal history. Using the current input xt as a reference beam to modulate ht reduces PPL by up to 2.13 points (PPL = 62.79) with a 4-head orthogonal reference beam using only 1.3M additional parameters, providing empirical support for the holographic interpretation. 

Code and pretrained weights : https://github.com/YasongFan/FDM 

## 1 Introduction 

The Transformer [Vaswani et al., 2017] dominates sequence modeling, yet its O(N 2) attention mechanism imposes a fundamental bottleneck: at N =8 ,192 , a 137M-parameter model requires 4,247 MB for KV cache alone. Linear recurrent models [Gu and Dao, 2023, Peng et al., 2023] address memory cost but introduce the wave-particle deadlock [Fan, 2026]: no single linear operator can simultaneously preserve all historical context (wave-like, norm-preserving) and selectively forget irrelevant content (particle-like, dissipative). 

Proposition 1 (Wave-Particle Deadlock) . No linear operator A ∈ CD×D can simultaneously satisfy: (a) ∥Ah ∥ = ∥h∥ for all h; (b) ∃ h∗: ∥Ah ∗∥ ≪ ∥ h∗∥.

> Preprint.
> arXiv:2604.07716v2 [cs.LG] 11 Apr 2026

FDM resolves this by explicit separation: a wave component handles norm-preserving propagation; a particle component handles selective retrieval. We further discover that jointly training these components leads to a gradient sink problem—the scan parameters dominate gradient flow, starving the cache. Freeze-Scan decouples optimization and yields 7.5× better convergence. 

Contributions. 

1. FDM architecture : O(1) decode memory (867 MB fixed, W =256 , K=16 ), MQAR accu-racy 0.966 vs. Transformer 0.606. 2. Freeze-Scan : PPL 487 →64.9 ( 7.5×), crosses PPL = 100 at 17K steps ( 14 × training effi-ciency). 3. Holographic Reference Beam Decoding : 4-head orthogonal modulation reduces PPL by 2.13 points with 1.3M params. 4. Empirical analysis : holographic information concentrates in layer 0 ( −1.36 PPL), with rapidly diminishing returns in deeper layers, consistent with AdS/CFT boundary theory. 

## 2 Related Work 

Linear sequence models. S4 [Gu et al., 2022], Mamba [Gu and Dao, 2023], Mamba-2 [Dao and Gu, 2024], RWKV [Peng et al., 2023], and RetNet [Sun et al., 2023] achieve O(1) inference memory via recurrent state compression. FDM extends this family with explicit wave-particle separation and Freeze-Scan training. 

Associative memory. Based [Arora et al., 2024] introduces the MQAR benchmark and shows that linear models struggle with in-context recall. Induction heads [Olsson et al., 2022] implement associative recall in Transformers; we show Freeze-Scan induces analogous mechanisms in linear RNNs. 

KV cache compression. H2O [Zhang et al., 2023] and StreamingLLM [Xiao et al., 2024] compress KV caches post-hoc. Our local-global cache selects tokens end-to-end via learned seff , not post-hoc heuristics. Longformer [Beltagy et al., 2020] and BigBird [Zaheer et al., 2020] combine local windows with global tokens in full attention; we implement this in a linear RNN with O( W +K)memory. 

## 3 Method 

3.1 The Fan Operator (Wave Component) 

The core recurrent update is the Fan Operator :

ht = (1 − pt) ⊙ R(θt) · ht−1 + pt ⊙ (Wr xt + i W ixt) (1) where ht ∈ CD , R(θt) is a phase rotation via nG Givens rotations satisfying ∥R(θ)h∥ = ∥h∥, and 

pt ∈ (0 , 0.5] is the measurement probability :

seff (t) = βt · log( t+2) + Wpos [sin( πt/T ), cos( πt/T )] ⊤ (2) 

pt = σ(seff (t) + μ) · 0.5 + ε (3) The log( t+2) term implements renormalization group running : early tokens have low measurement rates (stronger wave coherence), while later positions allow more information injection. We use 

σ(·) · 0.5 + ε (smooth scaling) rather than hard clamping to maintain gradient flow throughout. 

Two-pass scan (Born approximation). 

h(1)  

> t

= Fan (pt, θ t, W r xt, W ixt) (4) 

δt = GivensH (h(1)  

> t

, x t) − h(1)  

> t

, g = σ(∆ gate ) (5) 

h(2)  

> t

= Fan (pt, θ t, W r xt + gδ t, W ixt) (6) Both passes share all weights; the second uses the first as a perturbative correction (Born approxima-tion in quantum scattering). 23.2 Local-Global Cache (Particle Component) 

mask (i, j ) = local (i, j ) ∨ global (i, j ) (7) local (i, j ) = 1[max(0 , i −W ) ≤ j < i ], W =256 (8) global (i, j ) = 1[j ∈ TopK ({seff (k) : k < i }, K )] , K=16 (9) Since W =256 and K=16 are constants independent of N , the cache maintains exactly W +K=272 

slots. Decode memory is O( W +K) = O(1) w.r.t. N .3.3 Freeze-Scan Training 

Let Φwave = {Wθ , W r , W i, W β , Givens , ∆gate , CausalConv } (14.9M params) and Φcache = all remain-ing parameters (122.1M params). 

Phase 1 (Wave Convergence): standard full-parameter training. 

Phase 2 (Cache Specialization): freeze Φwave , optimize Φcache at lr =10 −4.When Φwave is frozen, gradients flow exclusively through embed → cache → lm_head , forcing the cache to develop induction head mechanisms [Olsson et al., 2022]. 

3.4 Holographic Reference Beam Decoding 

We interpret ht as a holographic plate : a fixed-dimensional complex state encoding the entire temporal history via phase interference. Direct projection lm_head (ht) is analogous to illuminating a hologram with incoherent light—only noise is recovered. The current input xt serves as a reference beam : modulating ht with xt selectively reconstructs temporally relevant information. 

Single-head (phase modulation): 

hdecoded = ht ⊙ (1 + tanh( Wref xt)) (10) Zero-initialization of Wref ensures hdecoded = ht at initialization, preserving base model performance. 

Multi-head orthogonal reference beam: 

hdecoded = ht ⊙ 1

H

> H

X

> i=1

(1 + tanh( Wref ,i xt)) (11) 

Lorth = λ X

> i̸=j

∥Wref ,i W ⊤ 

> ref ,j

∥2 

> F

(12) The orthogonality penalty forces each head to extract information from a distinct semantic direction, analogous to multi-wavelength holographic reconstruction. 

Layer-wise holographic analysis: We train one reference beam per layer sequentially (layer 0 first), freezing each after convergence to prevent gradient interference. This reveals the layer-wise distribution of holographic information. 

## 4 Experiments 

4.1 Setup 

All experiments use a 137M FDM ( d=576 , 12 layers, K=16 , W =256 ) and 122M Transformer baseline, trained on WikiText-103 [Merity et al., 2017] (121M tokens), AdamW, sequence length 1024, batch size 8. 

4.2 Language Modeling 

Freeze-Scan reduces PPL from 487 to 64.9 ( 7.5× improvement). Adding 4-head orthogonal reference beam decoding (1.3M additional parameters, 5K fine-tuning steps) further reduces PPL to 62.79 

(−2.13 points). 3Table 1: WikiText-103 validation perplexity. Model Params Steps Val PPL ↓

Transformer 122M 44K 36.3 FDM — Full fine-tuning 137M 44K 487 FDM — Freeze-Scan 137M 10K 148.5 FDM — Freeze-Scan 137M 44K 64.9 

FDM + Holographic Decoding (4-head) 138.3M 44K+5K 62.79 4.3 Holographic Decoding Analysis 

Table 2: Holographic reference beam ablation (layer-wise and multi-head). Configuration Params Steps Val PPL ∆PPL FDM baseline — — 64.92 —Single-head (layer 0 only) +0.3M 5K 63.10 −1.82 

Sequential layerwise (layers 0–11) +4M 24K 63.34 −1.58 

4-head orthogonal (layer 0) +1.3M 5K 62.79 −2.13 

8-head orthogonal (layer 0) +2.7M 5K ≈62.8 ≈− 2.1

Key finding: holographic information concentrates in layer 0. Layer-wise sequential training reveals a striking pattern:              

> Layer 012345–11
> ∆PPL −1.36 −0.14 −0.05 −0.02 −0.01 ≈0

Over 90% of holographically recoverable information resides in layer 0. This is consistent with AdS/CFT boundary theory [Maldacena, 1999]: complex information lives on the boundary (shallowest layer), while deeper layers represent increasingly “collapsed” particle-like states. 

4.4 Multi-Query Associative Recall 

Table 3: MQAR accuracy (Easy: seq=64, 8 KV pairs). Model Easy Medium (seq=128) Transformer 0.606 0.238 FDM ( K=0 , scan only) 0.011 0.011 FDM ( K=16 ) 0.966 0.064 FDM with K=16 achieves 0.966, surpassing Transformer by 59.5%. The near-zero score of pure scan ( K=0 ) confirms that precise associative recall requires the particle component. 

4.5 Inference Efficiency 

FDM decode memory is fixed at 867 MB across all prompt lengths (4.9× reduction at 

N =8 ,192 ). FDM decode speed degrades only 15% (22.1 →18.7 tok/s) versus Transformer’s 83% (149.5 →25.7 tok/s). 

4.6 Downstream Benchmarks 

FDM matches or slightly exceeds Transformer on WinoGrande (50.8% vs. 49.3%), which requires cross-sentence coreference resolution—consistent with FDM’s superior associative recall. The gap on LAMBADA and HellaSwag is consistent with the PPL difference and is expected to close with larger training data. 4Table 4: Decode memory (MB) and speed (tok/s) vs. prompt length. Prompt Decode Memory (MB) Decode Speed (tok/s) TF FDM TF FDM 128 853 867 149.5 22.1 1,024 1,210 867 133.1 21.4 4,096 2,462 867 55.1 20.2 8,192 4,247 867 25.7 18.7 Table 5: Zero-shot downstream benchmark accuracy. Model LAMBADA HellaSwag WinoGrande Random Transformer (36.3 PPL) 6.0% 30.3% 49.3% 25%/50% FDM (64.9 PPL) 1.0% 28.3% 50.8% —

4.7 Complementary Strengths 

Table 6: FDM and Transformer occupy complementary operating regimes. Property Transformer FDM Language modeling PPL 36.3 64.9 MQAR accuracy ( K=16 ) 0.606 0.966 

Decode memory @ N =8 K 4,247 MB 867 MB 

Decode speed consistency Poor ( 6× degr.) Good ( 15% )

Training efficiency Standard 14 × faster 

WinoGrande 49.3% 50.8% 

## 5 Discussion 

Why does Freeze-Scan work? Freezing Φwave eliminates the gradient sink effect, forcing gradients through the cache circuit alone. This mirrors the formation of induction heads [Olsson et al., 2022]: the cache learns to match current queries against historical patterns. The dramatic PPL improvement (7.5×) suggests the cache was severely undertrained during joint optimization. 

Holographic interpretation. The concentration of holographic information in layer 0 ( > 90% )supports the interpretation of ht as a genuine holographic encoding: the first layer performs maximum phase interference (wave regime), while subsequent layers progressively collapse toward particle states. The 4-head orthogonal decoder ( −2.13 PPL) extracts more information than single-head (−1.82 PPL) by reading different “wavelengths” of the hologram simultaneously. 

Limitations. FDM’s PPL (64.9) exceeds Transformer’s (36.3) due to training data scale (121M vs. billions of tokens used by competitive SSMs). Prefill at N =16 ,384 requires chunked prefill (not yet implemented). Decode speed (22 tok/s) is below Transformer peak due to Python-level loop; a fused kernel would close this gap. 

## 6 Conclusion 

We presented FDM with O(1) decode memory, Freeze-Scan training, and Holographic Reference Beam Decoding. Key results: 867 MB fixed decode memory ( 4.9× vs. Transformer at N =8 ,192 ), 0.966 MQAR accuracy ( +59 .5% vs. Transformer), 7.5× training efficiency improvement, and −2.13 

PPL from holographic decoding with only 1.3M additional parameters. The holographic analysis reveals that FDM’s complex hidden state encodes information analogously to a physical hologram, with the first layer acting as the primary holographic boundary. 5Acknowledgements. Core technology subject to Chinese patent No. 2026104740169 (filed 2026-04-11), with priority from No. 2026104567714 (filed 2026-04-08). 

## References 

Simran Arora et al. Simple linear attention language models balance the recall-throughput tradeoff. In ICML , 2024. Iz Beltagy, Matthew E Peters, and Arman Cohan. Longformer: The long-document transformer. 2020. Tri Dao and Albert Gu. Transformers are SSMs: Generalized models and efficient algorithms. In 

ICML , 2024. Yasong Fan. MIPT-SSM: Scaling language models with O(1) inference cache via phase transitions. 

arXiv , 2026. Albert Gu and Tri Dao. Mamba: Linear-time sequence modeling with selective state spaces. 

arXiv:2312.00752 , 2023. Albert Gu, Karan Goel, and Christopher Ré. Efficiently modeling long sequences with structured state spaces. In ICLR , 2022. Juan Maldacena. The large-N limit of superconformal field theories and supergravity. International journal of theoretical physics , 38:1113–1133, 1999. Stephen Merity et al. Pointer sentinel mixture models. In ICLR , 2017. Catherine Olsson et al. In-context learning and induction heads. Transformer Circuits Thread , 2022. Bo Peng et al. RWKV: Reinventing RNNs for the transformer era. In EMNLP , 2023. Yutao Sun et al. Retentive network: A successor to transformer. arXiv:2307.08621 , 2023. Ashish Vaswani, Noam Shazeer, Niki Parmar, et al. Attention is all you need. In NeurIPS , 2017. Guangxuan Xiao et al. Efficient streaming language models with attention sinks. In ICLR , 2024. Manzil Zaheer et al. Big Bird: Transformers for longer sequences. In NeurIPS , 2020. Zhenyu Zhang et al. H2O: Heavy-hitter oracle for efficient generative inference. In NeurIPS , 2023. 6