---
title: "ATLAAS: Automatic Tensor-Level Abstraction of Accelerator Semantics"
title_zh: ATLAAS：加速器语义的自动张量级抽象
authors: "Ruijie Gao, Haoran Jin, Jirong Yang, Nathaniel Bleier"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13523v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 从加速器语义自动生成编译器后端
tldr: ATLAAS是首个基于MLIR的端到端流水线，旨在自动将硬件RTL提取的底层语义提升为张量级ISA规范。针对加速器设计中ISA和编译器后端缺失的问题，它通过8阶段语义提升流程，从位级LLVM IR中恢复MAC运算、多维缓存和数据布局等高层结构。该工具能自动生成软件栈，在Gemmini和VTA加速器上验证了其有效性，显著简化了从硬件设计到编译器生成的流程。
selection_source: fresh_fetch
motivation: 现有的张量加速器设计往往缺乏完善的ISA和编译器支持，且手动编写张量级ISA规范过程繁琐且依赖专家经验。
method: 提出一种包含8个阶段的MLIR流水线，通过语义提升技术从位级IR中自动恢复张量结构、饱和语义及缓冲区组织，并生成可用于自动构建软件栈的规范。
result: "在Gemmini上实现了高达92.9%的代码压缩率并发现了文档缺失的硬件特性，同时在VTA上证明了无需针对性修改即可实现通用的语义提升。"
conclusion: ATLAAS实现了从RTL到高性能编译器后端的自动化路径，显著降低了加速器评估和软件栈开发的门槛。
---

## 摘要
许多张量加速器设计已被提出，但大多数缺乏完善的指令集架构（ISA）文档和编译器后端，导致评估仅限于少数算子。最近的研究表明，给定张量级 ISA 规范，可以自动生成包括编译器后端在内的完整软件栈，但编写此类规范仍是一个依赖专家的手动过程。我们提出了 ATLAAS，这是第一个基于 MLIR 的端到端流水线，可将从 RTL 提取的加速器语义提升为张量 ISA 规范。ATLAAS 从先前架构级模型提取生成的位级 LLVM IR 出发，应用了一个 8 阶段的语义提升流水线，逐步恢复高层张量结构——包括 MAC 习语、饱和语义、多维缓冲区组织和数据布局转换——并输出规范，从而能够通过 ACT 生态系统立即实现自动软件栈生成。我们在 Gemmini 脉动阵列加速器上评估了 ATLAAS，该流水线在处理单元上将位级 MLIR 压缩了高达 92.9%，在控制器模块上压缩了 24-34%。ATLAAS 发现了手写参考规范中遗漏的硬件特性，并通过 Z3 SMT 等价性证明验证了其正确性。通用性在 TVM 的 VTA 处理器上得到了证实，同样的流水线在无需针对特定加速器进行修改的情况下，提升了所有四个数据通路模块，实现了从 RTL 到具有性能竞争力的编译器后端的自动化路径。

## Abstract
Numerous tensor accelerator designs have been proposed, yet most lack well-documented ISAs and compiler backends, limiting evaluation to a handful of operators. Recent work has shown that given a tensor-level ISA specification, complete software stacks including compiler backends can be automatically generated--but writing such specifications remains a manual, expert-driven process.   We present ATLAAS, the first end-to-end MLIR-based pipeline that lifts RTL-extracted accelerator semantics to tensor ISA specifications. Starting from bit-level LLVM IR produced by prior architecture-level model extraction, ATLAAS applies an 8-pass semantic lifting pipeline that progressively recovers high-level tensor structure--MAC idioms, saturation semantics, multi-dimensional buffer organizations, and data layout transformations--emitting specifications that immediately enable automatic software stack generation through the ACT ecosystem.   We evaluate ATLAAS on the Gemmini systolic-array accelerator, where the pipeline collapses bit-level MLIR by up to 92.9% on processing elements and 24-34% on controller modules. ATLAAS discovers hardware features omitted from the hand-written reference, with correctness validated via Z3 SMT equivalence proofs. Generality is confirmed on TVM's VTA processor, where the same pipeline lifts all four datapath modules without accelerator-specific changes, enabling an automated path from RTL to a performance-competitive compiler backend.

---

## 论文详细总结（自动生成）

这是一份关于论文《ATLAAS: Automatic Tensor-Level Abstraction of Accelerator Semantics》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管新型张量加速器（Tensor Accelerator）层出不穷，但大多数设计缺乏完善的指令集架构（ISA）文档和编译器后端支持。
*   **研究背景**：现有的自动化工具（如 ACT 生态系统）虽然能根据张量级 ISA 规范自动生成编译器，但编写这些规范（如 TAIDL）仍需专家手动完成，既耗时又易出错。
*   **整体含义**：ATLAAS 旨在填补“底层硬件 RTL”与“高层张量编译器”之间的鸿沟。它是首个能够自动从硬件 RTL 代码中提取并提升（Lift）出高层张量语义规范的端到端流水线，实现了从硬件设计到软件栈生成的全自动化。

### 2. 方法论
ATLAAS 采用基于 MLIR 的三阶段流水线：
*   **阶段 1：RTL 到 MLIR 提取**：利用符号展开技术（基于改进的 autoGenILA），将 Verilog RTL 转换为位级（Bit-level）的 MLIR 函数。该阶段保留了条件更新（scf.if）和内存访问（memref），并保留了原始 RTL 信号名称作为元数据。
*   **阶段 2：语义提升（核心技术）**：通过一个 **8 阶段的 MLIR 优化 pass** 逐步抽象语义：
    *   **规范化**：折叠冗余的位操作和符号扩展链。
    *   **习语检测**：识别乘加运算（MAC）、固定点饱和（Clamp）等硬件常用模式。
    *   **循环重建**：将离散的 MAC 操作链恢复为结构化的循环（scf.for），并进一步提升为线性代数算子（linalg.dot）。
    *   **元数据发射**：推断内存缓冲区角色（如 DRAM 或 Scratchpad）和网格维度。
*   **阶段 3：TAIDL 汇编**：将提升后的 MLIR 映射为 XLA-HLO 风格的语义，生成最终的 TAIDL 规范，供下游编译器生成工具使用。

### 3. 实验设计
*   **目标加速器**：
    1.  **Gemmini**：伯克利开发的脉动阵列加速器（16×16, INT8）。
    2.  **VTA**：TVM 的通用张量加速器。
*   **Benchmark 与场景**：
    *   **代码压缩率**：对比提升前后 MLIR 的行数。
    *   **正确性验证**：使用 **Z3 SMT 求解器** 证明提取的语义与原始 RTL 在位级上等价。
    *   **端到端性能**：将生成的规范输入 ACT 工具链生成编译器后端，在 **Spike 模拟器**上运行 ResNet-50、MobileNet、MLP 和 Transformer Linear 等模型。
*   **对比方法**：手写的 Gemmini 内核（gemmini-rocc-tests）以及手写的 TAIDL 参考规范。

### 4. 资源与算力
*   论文中**未明确说明**具体的训练算力（如 GPU 型号或时长），因为该研究属于 EDA（电子设计自动化）和编译器领域，主要涉及符号执行、SMT 求解和编译器变换，而非深度学习模型训练。实验主要在 Spike 模拟器和 Z3 求解器环境下完成。

### 5. 实验数量与充分性
*   **实验规模**：处理了 156 个 MLIR 文件，涉及超过 120 万行位级 IR。
*   **覆盖范围**：涵盖了计算密集型模块（PE）和复杂的控制模块（DMA 控制器、状态机）。
*   **充分性**：
    *   通过对 Gemmini 和 VTA 两个截然不同的架构进行测试，证明了方法的通用性。
    *   进行了端到端的全流程验证（从 RTL 到最终模型执行结果）。
    *   使用了形式化验证（Z3）确保语义提取的 100% 准确性，实验设计客观且严谨。

### 6. 主要结论与发现
*   **高效压缩**：在计算模块上实现了高达 **92.9%** 的代码行数缩减（从 686 行缩减至 17 行核心计算代码）。
*   **发现隐藏特性**：ATLAAS 自动发现了手写文档中遗漏的硬件特性，如多银行 DMA 配置、池化引擎语义和硬件 im2col 支持。
*   **性能竞争力**：自动生成的编译器后端在 ResNet-50 等负载上，比手写内核实现了 **1.014 倍**的几何平均加速比。
*   **通用性**：同一套流水线无需修改即可适用于不同的加速器架构。

### 7. 优点
*   **全自动化**：消除了加速器开发中最耗时的人工编写 ISA 规范环节。
*   **形式化保证**：利用 SMT 证明确保了提取语义的绝对正确性，避免了人工文档的歧义。
*   **深度洞察**：能够反向工程出硬件中未记录的优化细节，有助于硬件调试和软硬件协同设计。
*   **基于标准**：构建在 MLIR 之上，易于集成到现有的编译器基础设施中。

### 8. 不足与局限
*   **RTL 依赖性**：该方法前提是必须拥有可综合的 RTL 代码，对于闭源硬件无法直接应用。
*   **控制逻辑冗余**：虽然计算模块压缩率极高，但对于复杂的 FSM（状态机）和地址计算逻辑，仍保留了约 65-75% 的原始代码，存在进一步抽象的空间。
*   **习语覆盖**：目前的语义提升主要针对常见的张量习语（如 MAC、卷积、池化），如果硬件采用了极其特殊的非标准运算，可能需要扩展新的提升规则。

（完）
