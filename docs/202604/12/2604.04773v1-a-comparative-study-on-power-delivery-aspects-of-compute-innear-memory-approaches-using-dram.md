---
title: A comparative study on power delivery aspects of compute-in/near-memory approaches using DRAM
title_zh: 基于 DRAM 的存内/近存计算方法供电特性的比较研究
authors: "Siddhartha Raman Sundara Raman, Siyuan Ma, Lizy Kurian John"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04773v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 存内计算中供电与硬件挑战的综述
tldr: 本文针对基于DRAM的存内计算（PIM）技术带来的供电网络（PDN）挑战进行了深入研究。PIM虽能缓解存储墙问题，但其多行激活和大规模并行执行会产生非传统的电流需求，导致电压降和热点问题。文章提出了一个涵盖时间和空间维度的统一分类法，分析了不同PIM技术的供电压力，并探讨了通过内存调度和电源管理等策略进行优化的方向，为构建可靠的PIM系统提供了指导。
selection_source: fresh_fetch
motivation: 存内计算改变了DRAM的电流需求模式，传统的供电网络难以应对其带来的电压波动和热管理挑战。
method: 提出了一种统一的分类法，从时间和空间维度刻画PIM引起的电流行为，并对比分析了多种主流PIM技术对供电网络的影响。
result: 研究发现突发激活和大规模并行执行会导致严重的电压跌落、IR压降和局部热点，威胁系统稳定性。
conclusion: 实现可扩展且可靠的DRAM存内计算系统，必须采用感知供电网络的设计方法，并结合架构与电路级的缓解策略。
---

## 摘要
存内计算 (PIM) 通过在存储器内部执行计算来缓解“存储墙”问题，从而减少数据迁移并提高能效。基于 DRAM 的 PIM 因其高密度、成熟的制造生态系统以及与现有系统的兼容性而备受关注。近期的研究利用了 DRAM 层级的多个级别（包括子阵列、Bank 和 3D 堆叠结构），通过多行激活、行缓存操作和近 Bank 计算单元等机制来实现存内计算。然而，这些方法引入了非传统的电流需求模式，对供电网络 (PDN) 提出了挑战。本文调研了基于 DRAM 的 PIM 系统中的 PDN 挑战，并提出了一种统一的分类法，从时间（突发与持续）和空间（局部与分布式）维度刻画了 PIM 引起的电流行为。利用该框架，我们分析了代表性的 PIM 技术如何通过突发激活、多行并发和大规模并行执行给 PDN 带来压力，从而导致电压跌落、IR 压降和热点问题。我们进一步讨论了利用现有架构和电路级机制的 DRAM 特定缓解策略，包括时序约束、存储控制器调度、数据放置以及 Bank 和 Vault 级的电源管理。本综述强调了 PDN 感知设计对于构建可扩展且可靠的基于 DRAM 的 PIM 系统的重要性，并概述了未来的关键研究方向。

## Abstract
Compute-in-memory (PIM) mitigates the memory wall by performing computation within memory, reducing data movement and improving energy efficiency. DRAM-based PIM is particularly attractive due to its high density, mature manufacturing ecosystem, and compatibility with existing systems. Recent works exploit multiple levels of the DRAM hierarchy - including subarrays, banks, and 3D-stacked organizations - to enable in-memory computation using mechanisms such as multi-row activation, row-buffer operations, and near-bank compute units. However, these approaches introduce non-traditional current demand patterns that challenge the power delivery network (PDN).   This paper surveys PDN challenges in DRAM-based PIM systems and proposes a unified taxonomy that characterizes PIM-induced current behavior along temporal (burst vs. sustained) and spatial (localized vs. distributed) dimensions. Using this framework, we analyze how representative PIM techniques stress the PDN through bursty activations, multi-row concurrency, and large-scale parallel execution, leading to voltage droop, IR drop, and thermal hotspots.   We further discuss DRAM-specific mitigation strategies leveraging existing architectural and circuit-level mechanisms, including timing constraints, memory controller scheduling, data placement, and bank- and vault-level power management. This survey highlights the importance of PDN-aware design for scalable and reliable DRAM-based PIM systems and outlines key future research directions.

---

## 论文详细总结（自动生成）

这篇论文对基于 DRAM 的存内计算（PIM）技术在供电网络（PDN）方面的挑战进行了系统性的综述与比较研究。以下是详细的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的冯·诺依曼架构面临“存储墙”瓶颈，数据迁移耗能巨大。基于 DRAM 的存内计算（PIM）通过在存储器内部执行计算来解决此问题。然而，PIM 改变了 DRAM 原有的受限访问模式，引入了高并发、大批量的电流需求，这对现有的**供电网络（PDN）**造成了严峻挑战，可能导致电压跌落（Voltage Droop）、IR 压降和热点问题，进而影响计算的正确性和系统稳定性。
*   **研究动机**：现有的 PIM 研究多关注架构性能提升，往往假设供电模型是理想化的。本文旨在填补这一空白，系统分析不同层级的 PIM 技术对 PDN 的具体影响。

### 2. 论文提出的方法论
论文提出了一种**统一的 PDN 分类法（Unified Taxonomy）**，从两个维度刻画 PIM 引起的电流行为：
*   **时间维度（Temporal）**：
    *   **突发型（Burst）**：如 RowClone 和 AMBIT，在极短时间内产生巨大的瞬态电流（di/dt 压力）。
    *   **持续型（Sustained）**：如 DRISA 或近 Bank 计算单元，在计算执行期间产生长时间的稳定电流需求。
*   **空间维度（Spatial）**：
    *   **局部型（Localized）**：电流压力集中在特定的子阵列（Subarray）或字线驱动器。
    *   **分布式（Distributed）**：电流压力分布在多个 Bank、Rank 甚至 3D 堆叠的多个层级。
*   **技术层级分析**：
    *   **子阵列级（Subarray-level）**：利用多行激活（TRA）实现位运算，主要产生局部突发电流。
    *   **Bank 级（Bank-level）**：在 Bank 周边添加逻辑（如 Newton、HBM-PIM），产生分布式且部分持续的电流。
    *   **3D 堆叠级（3D-stacked）**：利用 HMC/HBM 的逻辑层进行计算，产生复杂的垂直和水平分布式电流。

### 3. 实验设计
*   **研究性质**：本文是一篇**综述与比较研究**，并非基于单一新算法的实验论文。
*   **对比对象（Benchmarks）**：论文选取了近年来具有代表性的 DRAM-PIM 技术进行对比分析，包括：
    *   **子阵列级**：AMBIT、DRISA、RowClone。
    *   **Bank 级**：Newton、HBM-PIM。
    *   **3D 级**：Neurocube、Tetris、iPIM。
*   **分析场景**：分析了这些技术在执行矩阵向量乘法、批量位运算和数据复制等任务时对 PDN 的压力。

### 4. 资源与算力
*   **算力说明**：由于本文属于综述和理论分析类论文，**文中未提及具体的 GPU 型号、训练时长或算力消耗**。其结论主要基于对现有文献的技术参数和 DRAM 物理特性的逻辑推理与定性分析。

### 5. 实验数量与充分性
*   **实验充分性**：论文通过表格（Table I & II）对 8 种主流 PIM 方案进行了分类对比。虽然没有提供新的仿真数据，但其引用的参考文献（共 49 篇）涵盖了从电路级到系统级的广泛研究。
*   **客观性**：论文从 DRAM 的物理约束（如 tRRD、tFAW 时序参数）出发，客观地评价了不同 PIM 方案在 PDN 方面的可行性。

### 6. 论文的主要结论与发现
*   **PDN 瓶颈转移**：随着 PIM 从子阵列级向 3D 堆叠级演进，PDN 的瓶颈从局部字线驱动器转移到了全局供电轨和 TSV（硅通孔）。
*   **并发风险**：多行激活和多 Bank 并发虽然提升了吞吐量，但会引起严重的瞬态电压波动，必须通过修改存储控制器调度策略来缓解。
*   **缓解策略**：提出了一系列 DRAM 特定的缓解方案，如利用 tFAW（四激活窗口）约束来限制峰值电流、采用感知子阵列的调度算法、以及在 3D 结构中进行 Vault 级的负载均衡。

### 7. 优点
*   **视角独特**：首次系统性地从供电网络（PDN）这一底层物理限制角度审视 PIM 技术，具有很强的工程指导意义。
*   **分类清晰**：提出的时空二维分类法为未来评估 PIM 系统的可靠性提供了一个标准框架。
*   **层级全面**：覆盖了从 2D DRAM 到 3D 堆叠内存的所有主流 PIM 架构。

### 8. 不足与局限
*   **缺乏定量仿真**：论文主要停留在定性分析层面，没有提供具体的电压跌落数值或热力学仿真曲线。
*   **应用限制**：讨论主要集中在基于 DRAM 的 PIM，对于 RRAM、PCM 等新兴非易失性存储器的 PDN 挑战涉及较少。
*   **缺乏具体电路实现**：虽然提到了缓解策略，但未给出具体的电路级改进设计（如新型去耦电容布局）。

（完）
