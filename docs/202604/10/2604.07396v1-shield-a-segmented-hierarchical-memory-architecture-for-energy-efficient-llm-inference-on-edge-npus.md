---
title: "SHIELD: A Segmented Hierarchical Memory Architecture for Energy-Efficient LLM Inference on Edge NPUs"
title_zh: SHIELD：一种用于边缘端 NPU 高能效大语言模型推理的分段式分层存储架构
authors: "Jintao Zhang, Xuanyao Fong"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07396v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对边缘NPU的生命周期感知内存架构
tldr: 针对边缘端NPU在LLM推理中受限于片上内存及eDRAM高刷新功耗的问题，本文提出SHIELD架构。该架构利用BF16激活值的生命周期和位级敏感度，通过分离符号/指数位与尾数位，对瞬态QO激活值禁用尾数刷新，对持久KV缓存放宽刷新频率，在保证模型精度的前提下显著降低了刷新能耗。
selection_source: fresh_fetch
motivation: 边缘NPU使用eDRAM存储激活值时面临巨大的周期性刷新能耗，且现有研究忽视了瞬态激活值的容错特性。
method: 提出一种生命周期感知的分段eDRAM架构，将BF16数据的符号/指数位与尾数位隔离存储，并根据数据类型实施差异化的刷新策略。
result: "在多个LLM推理场景下，SHIELD相比标准刷新基准降低了35%的eDRAM刷新能耗，且在主流基准测试中保持了模型精度。"
conclusion: 通过结合激活值的时域特性与位级敏感度，SHIELD为边缘端低功耗LLM推理提供了一种高效的片上存储优化方案。
---

## 摘要
边缘端神经网络处理器（NPU）上的大语言模型（LLM）推理从根本上受限于有限的片上存储容量。尽管高密度嵌入式动态随机存取存储器（eDRAM）在存储激活工作空间方面具有吸引力，但其周期性刷新会消耗大量能量。先前的研究主要集中在减少片外流量或优化持久性键值（KV）缓存的刷新，而瞬态且具有容错能力的查询和注意力输出（QO）激活在很大程度上被忽视了。我们提出了 SHIELD，一种生命周期感知的分段式 eDRAM 架构，该架构共同利用了 bfloat16 (BF16) 激活中的时间驻留特性和位级敏感性。SHIELD 将符号位和指数位字段与尾数位分离，对瞬态 QO 尾数禁用刷新，并对持久性 KV 尾数应用松弛刷新。在多种 LLM 和推理场景下，与标准刷新基准相比，SHIELD 将 eDRAM 刷新能耗降低了 35%，同时在 WikiText-2、PIQA 和 ARC-Easy 数据集上保持了准确率。

## Abstract
Large Language Model (LLM) inference on edge Neural Processing Units (NPUs) is fundamentally constrained by limited on-chip memory capacity. Although high-density embedded DRAM (eDRAM) is attractive for storing activation workspaces, its periodic refresh consumes substantial energy. Prior work has primarily focused on reducing off-chip traffic or optimizing refresh for persistent Key-Value (KV) caches, while transient and error-resilient Query and Attention Output (QO) activations are largely overlooked. We propose SHIELD, a lifecycle-aware segmented eDRAM architecture that jointly exploits temporal residency and bit-level sensitivity in bfloat16 (BF16) activations. SHIELD isolates the sign and exponent fields from the mantissa, disables refresh for transient QO mantissas, and applies relaxed refresh to persistent KV mantissas. Across multiple LLMs and inference scenarios, SHIELD reduces eDRAM refresh energy by 35% relative to a standard-refresh baseline while preserving accuracy on WikiText-2, PIQA, and ARC-Easy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SHIELD** 的新型存储架构，旨在解决边缘端神经网络处理器（NPU）在运行大语言模型（LLM）时面临的内存容量与功耗瓶颈。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：边缘端 NPU 的片上 SRAM 容量有限（通常仅 1-8MB），难以容纳 LLM 推理产生的海量中间激活值。虽然高密度的 **eDRAM** 是理想替代方案，但其**周期性刷新（Refresh）**会消耗大量能量。
*   **研究背景**：
    *   现有研究（如 Kelle）主要关注持久性的 **KV Cache** 刷新优化，但忽视了瞬态的 **QO（Query 和 Attention Output）** 激活值。
    *   QO 激活值虽然生命周期短，但在推理过程中占据了巨大的空间（如 Qwen3-8B 需要约 32MB），其刷新能耗不容忽视。
*   **核心动机**：利用激活值在**生命周期（驻留时间）**和**数值敏感度（位级容错性）**上的差异，实现更精细的能耗优化。

### 2. 核心方法论
SHIELD 架构的核心思想是**生命周期感知的位级分段存储**。
*   **数据分段映射**：针对 BF16 格式的激活值，将其拆分为两部分：
    1.  **关键字段（符号位 + 8位指数位）**：对误差极度敏感，存储在**标准刷新银行（Standard Refresh Banks）**。
    2.  **容错字段（7位尾数位）**：根据生命周期进一步细分。
*   **分层刷新策略**：
    1.  **瞬态 QO 尾数**：通过分析发现 QO 的生命周期通常小于 1.5ms，而 eDRAM 在此时间内的自然电荷保持能力足以维持较低误码率。因此，对 QO 尾数采用**无刷新（Refresh-less）**策略。
    2.  **持久 KV 尾数**：KV Cache 需要跨 Token 长期驻留，但尾数位对误差有一定容忍度（BER 阈值 $10^{-4}$）。因此，采用**松弛刷新（Relaxed Refresh）**策略，将刷新间隔从标准的 $45\mu s$ 延长至 $1216\mu s$。
*   **硬件实现**：设计了一个分段存储控制器，根据激活值的类型（QO 或 KV）和位段，将数据自动路由到具有不同刷新频率的 eDRAM 银行中。

### 3. 实验设计
*   **评估模型**：涵盖了 Qwen3 (1.7B, 4B, 8B)、Mistral-7B 和 Llama-3-8B 等主流开源模型。
*   **测试任务/数据集**：
    *   **语言建模**：WikiText-2（评估困惑度 PPL）。
    *   **常识推理**：PIQA、ARC-Easy（评估准确率 Accuracy）。
*   **推理场景**：
    *   **Summary**（预填充主导）。
    *   **Translation**（平衡型）。
    *   **Storytelling**（解码主导，长上下文）。
*   **对比基准（Benchmark）**：
    *   **Baseline**：标准 eDRAM 刷新策略。
    *   **Kelle**：仅针对 KV Cache 进行刷新的现有先进方法。

### 4. 资源与算力
*   **硬件设备**：论文提到使用单张 **NVIDIA H100 GPU** 进行激活值生命周期的系统性分析（Profiling）。
*   **模拟工具**：使用 **DESTINY** 工具进行 eDRAM 阵列的漏电和功耗建模；使用 3T-eDRAM 单元模型进行误码率（BER）表征。
*   **训练/推理时长**：文中未明确说明具体的实验总时长，但由于是推理侧的架构研究，重点在于前向传播的故障注入模拟。

### 5. 实验数量与充分性
*   **实验规模**：论文对 5 种不同规模和家族的模型进行了测试，并在 3 个不同的下游任务数据集上验证了鲁棒性。
*   **消融与对比**：对比了不同推理阶段（Prefill vs. Decode）的能效表现，并与最新的相关工作（Kelle）进行了直接对比。
*   **充分性评价**：实验设计较为全面，不仅验证了能效提升，还通过故障注入实验严谨地确定了不同位段的 BER 容忍阈值，证明了该架构在大幅节能的同时不会导致模型崩溃。

### 6. 主要结论与发现
*   **能效提升**：相比标准刷新基准，SHIELD 平均降低了 **35%** 的 eDRAM 刷新能耗。
*   **性能保持**：在所有测试模型和任务中，SHIELD 引起的精度损失几乎为零（$\Delta \approx 0$），优于更激进的刷新消除策略。
*   **场景优势**：在预填充（Prefill）阶段和解码（Decode）早期，由于 QO 激活值占比更高，SHIELD 的节能优势比仅优化 KV Cache 的方法更为显著。

### 7. 优点
*   **协同设计**：巧妙地结合了硬件架构（分段存储）与软件特性（LLM 激活值的容错性与生命周期）。
*   **粒度更细**：突破了以往只关注 KV Cache 的局限，首次深入探讨了瞬态 QO 激活值的优化潜力。
*   **高鲁棒性**：通过位级隔离，确保了指数位等关键数据的绝对安全，从而在大幅放宽尾数位刷新时仍能保持模型稳定性。

### 8. 不足与局限
*   **格式局限**：目前主要针对 **BF16** 格式设计，对于量化格式（如 INT8, FP4/FP8）或其他浮点格式（如 FP16）的适用性及位级敏感度尚未深入探讨。
*   **硬件开销**：虽然降低了刷新功耗，但分段存储控制器和位拆分/重组（Bit-shuffling）可能会带来额外的电路复杂度和微小的延迟开销，文中对此量化不足。
*   **应用范围**：主要针对边缘端 NPU 场景，对于拥有大容量 HBM 的云端 GPU 意义相对有限。

（完）
