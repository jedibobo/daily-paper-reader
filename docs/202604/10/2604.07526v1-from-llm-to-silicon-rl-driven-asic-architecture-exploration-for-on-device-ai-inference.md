---
title: "From LLM to Silicon: RL-Driven ASIC Architecture Exploration for On-Device AI Inference"
title_zh: 从大语言模型到芯片：强化学习驱动的端侧 AI 推理 ASIC 架构探索
authors: "Ravindra Ganti, Steve Xu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07526v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 强化学习驱动的编译器联合优化ASIC架构与算子映射
tldr: 本研究提出一种强化学习驱动的编译器，用于在3nm至28nm工艺下协同优化ASIC架构、存储层次和工作负载划分。通过将设计空间建模为包含混合动作的马尔可夫决策过程，并利用带有混合专家门控的SAC算法，实现了对网格拓扑、微架构及算子放置的联合探索。该方法在Llama 3.1和SmolVLM上验证了其在高性能和低功耗模式下的卓越表现，无需人工干预即可自动适配多种工艺节点。
selection_source: fresh_fetch
motivation: 旨在解决端侧AI推理中，跨不同工艺节点进行ASIC架构、存储和负载划分协同优化的巨大设计空间挑战。
method: 提出一种基于SAC算法和MoE门控的强化学习框架，将硬件参数与软件调度统一建模为马尔可夫决策过程进行联合搜索。
result: 在3nm工艺下实现了Llama 3.1的高速推理，并使SmolVLM在各节点功耗均低于13mW，成功适配了7个不同的工艺节点。
conclusion: 强化学习驱动的协同设计能有效自动化ASIC架构探索，在提升AI推理性能的同时显著降低功耗与人工调优成本。
---

## 摘要
我们提出了一种强化学习驱动的编译器，可针对 3nm 至 28nm 工艺下的 AI 推理，协同优化 ASIC 架构、存储层次结构和工作负载划分。设计空间被建模为一个具有离散-连续混合动作和统一功耗-性能-面积（PPA）目标的马尔可夫决策过程（MDP）。采用带有混合专家（MoE）门控机制的软 Actor-Critic（SAC）算法，探索了 Mesh 拓扑、单核微架构和算子放置的联合空间。我们在两个工作负载上进行了验证：Llama 3.1 8B FP16（高性能模式，3nm 下每秒 29809 个 token）和 SmolVLM（低功耗模式，在所有工艺节点下功耗均低于 13 mW，频率为 10 MHz）。在 7 个工艺节点上，强化学习算法能够自动适配 Mesh 尺寸和每个 Tile 的配置，包括异构的 FETCH、VLEN 和内存分配，无需针对特定节点进行手动调优。

## Abstract
We present an RL-driven compiler that jointly optimizes ASIC architecture, memory hierarchy, and workload partitioning for AI inference across 3nm to 28nm. The design space is formulated as a single Markov Decision Process with mixed discrete-continuous actions and a unified Power-Performance-Area (PPA) objective. Soft Actor-Critic (SAC) with Mixture-of-Experts gating explores the joint space of mesh topology, per-core microarchitecture, and operator placement. We validate on two workloads, Llama 3.1 8B FP16 (high-performance mode, 29809 tokens per second at 3nm) and SmolVLM (low-power mode, less than 13 mW at all nodes, 10 MHz). Across 7 process nodes, the RL automatically adapts mesh sizes and per-tile configurations, including heterogeneous FETCH, VLEN, and memory allocation without node-specific manual retuning.

---

## 论文详细总结（自动生成）

这篇论文由 XgenSilicon 公司的研究人员撰写，提出了一种利用强化学习（RL）驱动的编译器，旨在自动化探索和优化用于端侧 AI 推理的 ASIC 架构。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 ASIC 设计流程高度依赖人工编写 RTL 代码和验证，耗时数月。现有的自动化方法（如 AutoTVM 或某些 RL 布局工具）通常孤立地优化单个组件（如仅优化算子映射或仅优化芯片布局），缺乏从模型输入到硅片输出的全栈协同优化。
*   **研究动机**：随着大语言模型（LLM）向端侧设备迁移，如何在 3nm 到 28nm 的不同工艺节点下，快速找到硬件架构（Mesh 拓扑、微架构参数）与软件策略（存储层次、算子划分）的最优组合，成为提升 PPA（功耗、性能、面积）的关键。

### 2. 方法论
*   **核心思想**：将硬件-软件协同优化建模为一个**马尔可夫决策过程（MDP）**，利用强化学习在广阔的设计空间中搜索最优配置。
*   **关键技术细节**：
    *   **状态空间（State）**：包含 73 维特征（SAC 算法使用其中的 52 维），涵盖工作负载特征（指令数、访存强度）、当前硬件配置、负载分布、PPA 观测值及 LLM 特定参数（KV 缓存策略等）。
    *   **动作空间（Action）**：30 维混合动作空间。包括离散动作（Mesh 尺寸增量）和连续动作（指令发射宽度 FETCH、向量长度 VLEN、各类存储大小、NoC 宽度等）。
    *   **算法流程**：采用**带混合专家（MoE）门控的软 Actor-Critic（SAC）算法**。
        *   **MoE 策略**：针对 LLM 不同阶段（如 Attention 和 MLP）的特性，通过门控网络路由到不同的专家策略。
        *   **代理模型（Surrogate Model）**：构建基于工艺节点参数的解析模型，快速估算 PPA，减少昂贵的完整仿真次数。
        *   **MPC 优化**：在推理阶段使用模型预测控制（MPC）对连续参数进行微调。
    *   **异构 Tile 配置**：RL 代理会根据每个计算核心（TCC）承载的具体算子需求，自动推导异构的内存分配和向量宽度，而非简单的统一配置。

### 3. 实验设计
*   **测试模型/场景**：
    1.  **Llama 3.1 8B (FP16)**：代表高性能模式，侧重吞吐量。
    2.  **SmolVLM**：代表低功耗模式，侧重极低功耗下的视觉-语言推理。
*   **工艺节点**：覆盖 3nm, 5nm, 7nm, 10nm, 14nm, 22nm, 28nm 共 7 个节点。
*   **对比基准（Benchmark）**：
    *   **搜索策略对比**：将 SAC 与随机搜索（Random Search）和网格搜索（Grid Search）进行对比。
    *   **行业对比**：将估算的性能与 NVIDIA H200/B200、Groq、SambaNova、Taalas 等已知推理平台进行对比（注：本文数据为编译器估算值）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练 RL 代理所使用的具体 GPU 型号或数量。
*   **训练时长**：提到了每个工艺节点的优化预算约为 4,613 个 Episode（回合）。在 3nm 节点下，搜索过程在约 4,600 个回合内收敛。

### 5. 实验数量与充分性
*   **实验规模**：针对 2 个模型在 7 个工艺节点上进行了全量搜索，并进行了 PPA 敏感度分析、缩放定律（Scaling Laws）拟合以及搜索策略的消融实验。
*   **充分性评价**：实验覆盖了从先进（3nm）到成熟（28nm）的工艺跨度，且涵盖了高性能和低功耗两种极端需求，具有较好的代表性。但由于 RL 具有随机性，论文**未提供多随机种子（Multi-seed）的统计方差分析**，这在学术严谨性上略显不足。

### 6. 主要结论与发现
*   **性能表现**：在 3nm 工艺下，Llama 3.1 8B 可达到 29,809 tok/s 的吞吐量（高性能模式）；SmolVLM 在所有节点下的功耗均控制在 13mW 以下（低功耗模式）。
*   **工艺缩放**：性能随工艺节点呈幂律缩放。3nm 相比 28nm，吞吐量提升了 47.85 倍，面积缩小了 5.47 倍。
*   **协同效应**：联合优化（Joint Optimization）显著优于孤立优化。SAC 搜索到的配置在相同预算下，吞吐量比随机搜索高出 3.5 倍。
*   **架构优势**：通过片上 ROM 存储权重（无 HBM），消除了约 150W 的存储子系统功耗，是实现高能效的关键。

### 7. 优点
*   **端到端自动化**：实现了从 ONNX 模型到 RTL 生成的全流程自动化，无需人工干预。
*   **异构性探索**：能够自动为不同的计算核心分配不同的硬件资源（如 WMEM 大小），实现了极高的资源利用率。
*   **跨节点迁移**：一套 RL 框架即可适配多种工艺节点，展现了极强的泛化能力。

### 8. 不足与局限
*   **模型覆盖有限**：仅验证了 Transformer 架构（LLM 和 VLM），对于 CNN、扩散模型或 MoE 架构的适用性尚未证实。
*   **拓扑限制**：目前仅支持 2D Mesh 拓扑，不支持环形、环面（Torus）或芯片组（Chiplet）等更复杂的互连结构。
*   **数据偏差风险**：PPA 结果主要基于解析模型和编译器估算，而非真实的硅片实测数据，可能存在模型误差。
*   **统计严谨性**：缺乏重复实验的置信区间报告，难以评估 RL 搜索结果的稳定性。

（完）
