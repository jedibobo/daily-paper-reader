---
title: "FILCO: Flexible Composing Architecture with Real-Time Reconfigurability for DNN Acceleration"
title_zh: FILCO：具有实时可重构性的 DNN 加速灵活组合架构
authors: "Xingzhen Chen, Jinming Zhuang, Zhuoping Yang, Shixin Ji, Sarah Schultz, Zheng Dong, Weisong Shi, Peipei Zhou"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07523v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于DNN加速的灵活组合架构，以实现最佳存储和计算效率
tldr: 针对异构计算平台中深度神经网络（DNN）多样化负载导致的硬件资源效率低下问题，本文提出了FILCO架构。FILCO支持实时重构，可灵活组合为统一或多个独立的加速器，以实现存储与计算资源的最优匹配。配合包含两阶段设计空间搜索（DSE）的分析模型框架，FILCO在AMD Versal VCK190平台上实现了显著的性能提升，相比现有工作在吞吐量和硬件效率上提高了1.3至5倍。
selection_source: fresh_fetch
motivation: 现有的专用架构或覆盖层架构在处理多样化DNN负载时，存在硬件与负载不匹配及灵活性粒度受限导致的资源效率低下问题。
method: 提出一种支持实时重构的灵活组合架构FILCO，并配套提供包含两阶段设计空间搜索（DSE）的分析模型框架。
result: 在7nm AMD Versal VCK190开发板上的评估显示，FILCO在多种负载下实现了1.3倍至5倍的吞吐量和硬件效率提升。
conclusion: FILCO通过高灵活性的实时重构能力，有效解决了异构计算中多样化DNN任务的资源匹配难题，显著提升了硬件利用率。
---

## 摘要
随着深度神经网络（DNN）赋能应用的发展，在异构计算平台上针对多样化工作负载实现高硬件资源效率并非易事。先前的研究讨论了专用架构以实现最大的资源效率。然而，在各种多样化的工作负载中，硬件与工作负载之间始终存在不匹配。其他研究讨论了可以为不同工作负载动态切换数据流的覆盖（overlay）架构。然而，这些工作仍受限于灵活性粒度，并导致大量的资源效率低下。为了解决这个问题，我们提出了一种灵活组合架构 FILCO，它可以高效匹配多样化的工作负载，以实现最优的存储和计算资源效率。FILCO 可以实时重构，并灵活地组合成一个统一的或多个独立的加速器。我们还提出了 FILCO 框架，包括一个带有两阶段设计空间探索（DSE）的分析模型，可以达到最优设计点。我们还在 7nm AMD Versal VCK190 开发板上评估了 FILCO 框架。与先前的研究相比，我们的设计在各种多样化的工作负载上可以实现 1.3 倍至 5 倍的吞吐量和硬件效率。

## Abstract
With the development of deep neural network (DNN) enabled applications, achieving high hardware resource efficiency on diverse workloads is non-trivial in heterogeneous computing platforms. Prior works discuss dedicated architectures to achieve maximal resource efficiency. However, a mismatch between hardware and workloads always exists in various diverse workloads. Other works discuss overlay architecture that can dynamically switch dataflow for different workloads. However, these works are still limited by flexibility granularity and induce much resource inefficiency.   To solve this problem, we propose a flexible composing architecture, FILCO, that can efficiently match diverse workloads to achieve the optimal storage and computation resource efficiency. FILCO can be reconfigured in real-time and flexibly composed into a unified or multiple independent accelerators. We also propose the FILCO framework, including an analytical model with a two-stage DSE that can achieve the optimal design point. We also evaluate the FILCO framework on the 7nm AMD Versal VCK190 board. Compared with prior works, our design can achieve 1.3x - 5x throughput and hardware efficiency on various diverse workloads.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FILCO** 的灵活组合架构，旨在解决异构计算平台上深度神经网络（DNN）加速时，硬件资源与多样化工作负载之间不匹配的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 DNN 加速器通常采用针对特定模型优化的固定数据流。然而，实际应用（如自动驾驶）涉及多种类型的网络（如 MLP、Transformer、PointNet），其矩阵维度和计算/通信模式差异巨大。
*   **研究动机**：
    *   **静态架构的局限**：固定尺寸的片上缓存和计算块在处理小尺寸或异形矩阵时，必须进行大量数据填充（Padding），导致严重的计算和带宽浪费。
    *   **现有覆盖层（Overlay）架构的不足**：虽然支持动态切换，但灵活性粒度仍较粗，无法在运行时灵活调整计算块大小和内存视图。
    *   **目标**：设计一种能够实时重构、灵活组合计算与存储资源的架构，以实现对多样化负载的最优匹配。

### 2. 方法论
FILCO 包含硬件架构设计和配套的自动化设计空间搜索（DSE）框架：
*   **硬件架构（FILCO Architecture）**：
    *   **灵活计算并行度（Flexible Parallelism）**：在 AI 引擎（AIE）内核中采用动态循环边界。通过指令配置原子操作（如 2x8x8）的嵌套循环次数，使计算块大小能实时匹配矩阵形状，消除无效计算。
    *   **灵活片上内存视图（Flexible Memory Views, FMV）**：将片上缓存设计为 1D 寻址的灵活内存单元（FMU）。通过指令解码，FMU 可以被视为不同维度的矩阵，避免了因形状不匹配导致的填充开销。
    *   **灵活内存功能（Flexible Memory Functionality, FMF）**：FMU 的功能（存储权重、激活值或结果）在运行时可调，打破了静态分配的限制，最大化了存储资源利用率。
*   **算法流程（FILCO Framework）**：
    *   **两阶段 DSE**：第一阶段通过暴力搜索确定每层的最优运行时参数；第二阶段通过调度优化器确定全局执行时间线。
    *   **MILP 与 GA 求解**：提出混合整数线性规划（MILP）模型以保证小规模任务的最优性；针对大规模任务，提出基于遗传算法（GA）的启发式搜索以缩短搜索时间。

### 3. 实验设计
*   **硬件平台**：7nm AMD Versal VCK190 开发板。
*   **测试负载**：
    *   **多样化矩阵乘法（MM）**：设计了一系列具有不同序列长度、头数、维度和 MLP 比例的 Transformer 算子。
    *   **真实模型**：BERT 系列模型（从 BERT-32 到 BERT-512），涵盖了不同规模的序列长度。
*   **对比方法（Benchmark）**：
    *   **CHARM**：代表单体/静态多加速器组合架构。
    *   **RSN**：代表现有的流式网络覆盖层架构。

### 4. 资源与算力
*   **硬件规格**：使用 AMD Versal VCK190。
    *   **AI Engine (AIE)**：运行频率为 1GHz。
    *   **可编程逻辑 (PL)**：运行频率为 150MHz。
*   **编译工具**：AMD/Xilinx Vitis 2023.1。
*   **求解器**：使用 CPLEX 求解 MILP 问题，使用 Pymoo 运行遗传算法。
*   **训练/搜索时长**：论文提到 GA 算法能在 10 分钟内为复杂任务找到良好设计点，而 MILP 在一小时内可能无法求解大规模问题。

### 5. 实验数量与充分性
*   **实验设置**：
    *   进行了单 AIE 内核效率测试。
    *   针对 6 组不同复杂度和多样性的 MM 负载进行了吞吐量对比。
    *   针对 5 种不同规模的 BERT 模型进行了端到端性能评估。
    *   进行了消融实验，分别验证了灵活并行度（FP）、灵活内存功能（FMF）和灵活内存视图（FMV）的贡献。
*   **充分性评价**：实验设计较为全面，不仅涵盖了合成的极端负载，也包含了真实的工业级模型。对比基准（CHARM 和 RSN）具有代表性，实验结果客观地展示了 FILCO 在处理“非规整”负载时的优势。

### 6. 主要结论与发现
*   **性能提升**：相比 CHARM 和 RSN，FILCO 在多样化负载下实现了 **1.3x 至 5x** 的吞吐量和硬件效率提升。
*   **鲁棒性**：FILCO 在矩阵操作数变化超过 6 倍的情况下，仍能保持峰值性能的 95% 以上，而静态方法在小负载下效率骤降。
*   **DSE 效率**：提出的 GA 启发式搜索在处理大规模层数（如 50 层以上）时，比 MILP 具有更好的扩展性和实用性。

### 7. 优点
*   **极高的灵活性**：通过指令驱动的 1D 内存寻址和动态循环边界，实现了真正意义上的实时重构。
*   **资源利用率高**：有效解决了异构计算中常见的“数据填充”浪费问题，特别是在处理小尺寸或长条形矩阵时优势明显。
*   **软硬协同**：提供了从模型输入到二进制文件生成的完整自动化框架。

### 8. 不足与局限
*   **指令开销**：虽然论文提到指令解码，但频繁的实时重构可能会带来一定的控制平面延迟，论文对这部分开销的量化分析较少。
*   **算子覆盖面**：目前主要集中在矩阵乘法（MM）及其变体上，对于非线性算子或其他复杂数据流（如稀疏计算）的适配性尚未深入讨论。
*   **硬件依赖**：该架构高度依赖 AMD Versal 的 AIE 特性，将其迁移到其他 FPGA 或 ASIC 平台可能需要重新设计控制逻辑。

（完）
