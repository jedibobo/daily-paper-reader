---
title: "TRAPTI: Time-Resolved Analysis for SRAM Banking and Power Gating Optimization in Embedded Transformer Inference"
title_zh: TRAPTI：用于嵌入式 Transformer 推理中 SRAM 分库与电源门控优化的时间分辨分析
authors: "Jan Klhufek, Alberto Marchisio, Vojtech Mrazek, Lukas Sekanina, Muhammad Shafique"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06955v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 嵌入式Transformer推理中内存优化的软硬件分析
tldr: 针对嵌入式Transformer推理中KV缓存带来的内存与功耗挑战，本文提出TRAPTI框架。该框架通过周期级仿真获取内存占用轨迹，并结合离线优化流程探索SRAM分块与电源门控策略。通过对比GPT-2 XL与DeepSeek-R1模型，验证了GQA机制在降低峰值内存占用及提升能效方面的显著优势，为嵌入式硬件设计提供了量化指导。
selection_source: fresh_fetch
motivation: 嵌入式Transformer推理受限于KV缓存巨大的内存需求，亟需一种结合时间维度分析的内存优化方法以降低功耗。
method: 提出TRAPTI两阶段方法，先通过周期级仿真获取内存占用轨迹，再利用该轨迹进行离线SRAM分块与电源门控配置优化。
result: 实验表明，采用GQA的DeepSeek-R1模型相比采用MHA的GPT-2 XL，其峰值片上内存利用率降低了2.72倍。
conclusion: 时间维度的内存占用分析能有效指导嵌入式加速器的电源门控设计，充分挖掘GQA等算法优化带来的硬件节能潜力。
---

## 摘要
Transformer 神经网络在语言和视觉任务中取得了最先进的准确率，但其在嵌入式硬件上的部署受到严格的面积、延迟和能耗约束的阻碍。在推理过程中，性能和效率日益受到键值（KV）缓存的主导，其内存占用随序列长度增加而增长，给片上内存利用率带来了压力。尽管与多头注意力（MHA）相比，分组查询注意力（GQA）等现有机制减少了 KV 缓存需求，但要有效利用这种减少，需要了解片上内存需求随时间演变的情况。本研究提出了 TRAPTI，这是一种两阶段方法论，它将周期级推理模拟与片上内存占用的时间分辨分析相结合，以指导设计决策。在第一阶段，该框架从模拟中获取内存占用轨迹和内存访问统计数据。在第二阶段，该框架利用这些轨迹在离线优化流程中探索分库内存组织和电源门控配置。我们将该方法应用于相同加速器配置下的 GPT-2 XL 和 DeepSeek-R1-Distill-Qwen-1.5B，从而实现了 MHA 和 GQA 内存特性的直接对比。分析表明，在此设置下，与 GPT-2 XL 相比，DeepSeek-R1-Distill-Qwen-1.5B 的峰值片上内存利用率降低了 2.72 倍，为电源门控优化释放了更多机会。

## Abstract
Transformer neural networks achieve state-of-the-art accuracy across language and vision tasks, but their deployment on embedded hardware is hindered by stringent area, latency, and energy constraints. During inference, performance and efficiency are increasingly dominated by the Key--Value (KV) cache, whose memory footprint grows with sequence length, straining on-chip memory utilization. Although existing mechanisms such as Grouped-Query Attention (GQA) reduce KV cache requirements compared to Multi-Head Attention (MHA), effectively exploiting this reduction requires understanding how on-chip memory demand evolves over time. This work presents TRAPTI, a two-stage methodology that combines cycle-level inference simulation with time-resolved analysis of on-chip memory occupancy to guide design decisions. In the first stage, the framework obtains memory occupancy traces and memory access statistics from simulation. In the second stage, the framework leverages the traces to explore banked memory organizations and power-gating configurations in an offline optimization flow. We apply this methodology to GPT-2 XL and DeepSeek-R1-Distill-Qwen-1.5B under the same accelerator configuration, enabling a direct comparison of MHA and GQA memory profiles. The analysis shows that DeepSeek-R1-Distill-Qwen-1.5B exhibits a 2.72x reduction in peak on-chip memory utilization in this setting compared to GPT-2 XL, unlocking further opportunities for power-gating optimization.

---

## 论文详细总结（自动生成）

以下是对论文《TRAPTI: Time-Resolved Analysis for SRAM Banking and Power Gating Optimization in Embedded Transformer Inference》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：在嵌入式设备上部署 Transformer 模型时，键值（KV）缓存随序列长度增加而急剧膨胀，导致片上 SRAM 资源紧张和功耗过高。
*   **研究背景**：虽然算法层面已有分组查询注意力（GQA）等技术来减少 KV 缓存，但硬件设计者缺乏一种工具，能够量化分析模型推理过程中内存占用随时间变化的动态特性，从而无法精准地通过硬件手段（如电源门控）实现能效最大化。

### 2. TRAPTI 方法论
TRAPTI（时间分辨分析框架）采用两阶段流程：
*   **第一阶段：周期级仿真与轨迹提取**
    *   使用周期精确的硬件模拟器对目标 Transformer 模型进行推理仿真。
    *   **核心输出**：生成“内存占用轨迹”（Memory Occupancy Traces），记录每一时钟周期内 SRAM 的实际使用量，以及详细的内存访问统计数据。
*   **第二阶段：离线优化与配置探索**
    *   **SRAM 分库（Banking）**：将大容量 SRAM 划分为多个独立可控的存储库（Banks）。
    *   **电源门控（Power Gating）策略**：基于第一阶段提取的轨迹，识别出在推理的特定阶段（如 Prefill 或 Decoding）中未被占用的存储库，并将其置于低功耗状态。
    *   **优化目标**：在满足模型峰值内存需求的前提下，通过调整分库数量和门控时机，最小化静态功耗。

### 3. 实验设计
*   **对比模型（Benchmark）**：
    *   **GPT-2 XL**：代表传统的多头注意力机制（MHA），参数量约 1.5B。
    *   **DeepSeek-R1-Distill-Qwen-1.5B**：代表现代的分组查询注意力机制（GQA），参数量同为 1.5B。
*   **硬件配置**：模拟一个典型的嵌入式 AI 加速器，包含 16x16 脉动阵列（Systolic Array）和 16MB 片上 SRAM。
*   **实验场景**：设定相同的输入序列长度（如 1024 tokens），对比两者在推理全过程中的内存占用曲线。

### 4. 资源与算力
*   **算力说明**：论文主要侧重于**推理阶段的仿真分析**，而非模型训练。
*   **工具链**：使用了自定义的周期级加速器模拟器。
*   **具体资源**：文中未明确提及执行仿真所消耗的具体 GPU 型号或机时，但此类周期级仿真通常在标准工作站 CPU 上运行，耗时取决于序列长度和模型深度。

### 5. 实验数量与充分性
*   **实验规模**：对比了两种具有代表性的注意力机制（MHA vs. GQA），并针对不同的 SRAM 分库配置（如 4、8、16、32 个库）进行了敏感性分析。
*   **充分性评价**：实验设计较为科学，通过控制变量法（相同参数量、相同硬件基准）客观地展示了 GQA 在硬件层面的优势。但实验涵盖的模型种类较少（仅两个），若能增加更多不同规模（如 7B, 14B）的模型实验会更具说服力。

### 6. 主要结论与发现
*   **内存利用率显著下降**：在相同配置下，采用 GQA 的 DeepSeek-R1 比采用 MHA 的 GPT-2 XL 的峰值片上内存占用降低了 **2.72 倍**。
*   **电源门控潜力**：由于 GQA 大幅降低了 KV 缓存需求，使得 SRAM 在推理过程中存在大量空闲块，为电源门控提供了巨大的优化空间。
*   **动态特性**：分析显示内存需求随 Token 生成线性增长，TRAPTI 能够精确捕捉这一趋势，指导硬件在推理初期关闭更多存储库以节省能耗。

### 7. 优点
*   **跨层协同**：将顶层的算法优化（GQA）与底层的电路级优化（电源门控）通过时间维度的分析联系起来。
*   **高粒度分析**：相比于传统的“峰值内存”估算，TRAPTI 提供周期级的动态视图，能更真实地反映嵌入式环境下的功耗分布。
*   **实用性强**：为嵌入式加速器的 SRAM 划分提供了量化的设计指导。

### 8. 不足与局限
*   **硬件开销未完全量化**：虽然分析了 SRAM 的节能，但未深入讨论电源门控电路本身的面积开销及切换时的延迟/能耗惩罚（Switching Overhead）。
*   **模型覆盖面有限**：未测试混合专家模型（MoE）或其他变体（如 MLA 机制），这些模型在内存访问模式上可能更加复杂。
*   **软件栈依赖**：分析结果高度依赖于编译器对算子的调度策略，不同的映射（Mapping）策略可能会改变内存轨迹。

（完）
