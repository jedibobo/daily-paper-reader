---
title: Critical Patch-Aware Sparse Prompting with Decoupled Training for Continual Learning on the Edge
title_zh: 面向边缘侧持续学习的解耦训练关键补丁感知稀疏提示
authors: "Wonseon Lim, Jaesung Lee, Dae-Won Kim"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07399v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对边缘设备训练时的内存占用和计算成本
tldr: 针对边缘设备上持续学习面临的内存和计算资源限制，本文提出CPS-Prompt框架。该框架通过关键补丁采样（CPS）实现任务感知的令牌缩减，并结合解耦的提示词与分类器训练（DPCT）来降低反向传播开销。实验证明，在保持与SOTA相当的准确率的同时，该方法在峰值内存、训练时间和能效方面比基准模型提升了约1.6倍，显著增强了边缘端的训练效率。
selection_source: fresh_fetch
motivation: 现有的基于提示词的持续学习方法主要关注推理性能，忽视了边缘设备在在线训练时的内存和计算开销。
method: 提出了关键补丁采样（CPS）以减少输入令牌数量，并采用解耦训练策略（DPCT）来优化反向传播过程。
result: 在多个基准测试和真实硬件上，CPS-Prompt在内存和能效上比CODA-Prompt提升了1.6倍，且准确率损失极小。
conclusion: CPS-Prompt成功平衡了边缘设备持续学习的准确性与训练效率，为资源受限环境下的模型自适应提供了高效方案。
---

## 摘要
边缘设备上的持续学习（CL）不仅需要高准确率，还需要极高的训练效率，以支持在严格的内存和计算约束下的设备端自适应。虽然基于提示的持续学习（PCL）具有参数效率且能实现极具竞争力的准确率，但以往的研究主要集中在准确率或推理性能上，往往忽略了设备端训练的内存和计算开销。在本文中，我们提出了 CPS-Prompt，这是一个关键补丁感知稀疏提示框架，通过集成用于任务感知令牌（token）缩减的关键补丁采样（CPS）和用于降低反向传播开销的解耦提示与分类器训练（DPCT），明确针对训练时的内存占用和计算成本进行优化。在三个公共基准测试和真实边缘硬件上的实验表明，CPS-Prompt 在峰值内存、训练时间和能源效率方面比平衡的 CODA-Prompt 基准提升了约 1.6 倍，同时平均准确率与最先进的 C-Prompt 差距在 2% 以内，并与 CODA-Prompt 的准确率保持相当。代码可在 https://github.com/laymond1/cps-prompt 获取。

## Abstract
Continual learning (CL) on edge devices requires not only high accuracy but also training-time efficiency to support on-device adaptation under strict memory and computational constraints. While prompt-based continual learning (PCL) is parameter-efficient and achieves competitive accuracy, prior work has focused mainly on accuracy or inference-time performance, often overlooking the memory and computational costs of on-device training. In this paper, we propose CPS-Prompt, a critical patch-aware sparse prompting framework that explicitly targets training-time memory usage and computational cost by integrating critical patch sampling (CPS) for task-aware token reduction and decoupled prompt and classifier training (DPCT) to reduce backpropagation overhead. Experiments on three public benchmarks and real edge hardware show that CPS-Prompt improves peak memory, training time, and energy efficiency by about 1.6x over the balanced CODA-Prompt baseline, while maintaining accuracy within 2% of the state-of-the-art C-Prompt on average and remaining competitive with CODA-Prompt in accuracy. The code is available at https://github.com/laymond1/cps-prompt.

---

## 论文详细总结（自动生成）

这是一份关于论文《Critical Patch-Aware Sparse Prompting with Decoupled Training for Continual Learning on the Edge》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
本文针对**边缘设备（如机器人、无人机、智能手机）上的持续学习（Continual Learning, CL）**面临的资源瓶颈问题。
*   **研究动机**：现有的基于提示（Prompt-based）的持续学习方法（PCL）虽然参数效率高，但主要关注推理性能或准确率，忽视了**设备端训练（On-device Training）**时的内存占用和计算开销。
*   **核心挑战**：在边缘侧进行模型自适应时，反向传播产生的中间激活值极易超出受限的显存/内存，导致训练失败或能耗过高。

### 2. 方法论：核心思想与关键技术
论文提出了 **CPS-Prompt** 框架，通过两个核心模块在保证准确率的同时降低训练负担：
*   **关键补丁采样（Critical Patch Sampling, CPS）**：
    *   利用 PCL 结构中固有的“冻结查询编码器（Frozen Query Encoder）”生成的信号。
    *   **核心逻辑**：提取最后一层 Transformer Block 的类注意力（Class-to-patch Attention）和值向量范数（Value Norm），计算每个补丁的重要性得分 $s_j = A_{cls,j} \cdot \|V_j\|_2$。
    *   **随机采样**：基于温度系数 $\tau$ 的 Softmax 分布进行多项式采样，选择 $k$ 个关键补丁进入后续训练，从而大幅减少激活值的存储。
*   **解耦提示与分类器训练（Decoupled Prompt and Classifier Training, DPCT）**：
    *   **阶段一（提示训练）**：使用 CPS 筛选的**稀疏补丁**联合优化提示参数和分类器，学习任务自适应特征。
    *   **阶段二（分类器对齐）**：冻结提示参数，使用**全量补丁**仅微调分类器。
    *   **目的**：解决训练（稀疏）与推理（全量）之间的表征不匹配问题，并因阶段二无需更新提示参数而进一步降低了反向传播的计算开销。

### 3. 实验设计
*   **数据集**：CIFAR-100、ImageNet-R、CUB-200（均划分为 10 个任务）。
*   **Benchmark（对比方法）**：
    *   **传统 CL 方法**：LwF、ER。
    *   **SOTA PCL 方法**：L2P、DualPrompt、CODA-Prompt、C-Prompt、OS-Prompt。
    *   **令牌缩减方法**：ToMe（令牌合并）、PatchDropout（随机丢弃）。
*   **评估指标**：平均准确率（ACC）、遗忘率（FGT）、峰值内存占用、单任务训练时间、单任务能耗。

### 4. 资源与算力
*   **训练算力**：实验在 **NVIDIA RTX 4090 GPU** 上进行模型训练和准确率评估。
*   **边缘验证**：效率指标（内存、时间、能效）在真实的边缘硬件 **NVIDIA Jetson Orin Nano** 上测得。
*   **模型骨干**：使用预训练的 **ViT-Tiny/16**。
*   **训练细节**：Batch size 为 16，根据数据集不同训练 20 或 50 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准数据集上进行了完整测试，所有结果均为 **10 次独立运行的平均值**，并进行了 **p < 0.05 的配对 t 检验**以验证统计显著性。
*   **消融实验**：详细探讨了 CPS 与 DPCT 的各自贡献、采样温度 $\tau$、训练阶段比例 $\lambda$ 以及“随机采样 vs. 确定性 Top-k 采样”的效果对比。
*   **充分性评价**：实验设计客观、公平，不仅对比了准确率，还深入分析了边缘设备最关心的资源消耗曲线，论证了方法在资源受限环境下的优越性。

### 6. 论文的主要结论与发现
*   **效率提升**：CPS-Prompt 在峰值内存、训练时间和能源效率上比基准方法 CODA-Prompt 提升了约 **1.6 倍**。
*   **准确率保持**：在大幅减枝的情况下，准确率与最先进的 C-Prompt 相比差距仅在 **2% 以内**，且优于其他令牌缩减技术（如 ToMe 和 PatchDropout）。
*   **采样策略**：带有温度控制的随机采样比确定性的 Top-k 采样具有更好的泛化能力，能帮助模型在稀疏输入下学习到更稳健的特征。

### 7. 优点
*   **针对性强**：切中边缘侧持续学习“训练难”的痛点，而非仅仅关注推理。
*   **任务感知**：相比随机丢弃补丁，CPS 利用查询编码器的注意力机制实现了“任务相关”的智能缩减。
*   **训练策略巧妙**：DPCT 成功缓解了稀疏训练带来的表征偏移，同时通过冻结参数进一步节省了算力。

### 8. 不足与局限
*   **超参数敏感性**：温度 $\tau$ 和阶段比例 $\lambda$ 在不同数据集上需要微调以达到最优平衡。
*   **架构依赖**：目前主要基于 ViT 架构，对于非 Transformer 结构或其他类型的 Prompt 机制（如视觉编码器之外的提示）的适用性尚未讨论。
*   **两阶段开销**：虽然 CPS 降低了第二阶段的负担，但 PCL 本身固有的两阶段前向传播（Query + Prompt）在极度受限的设备上仍存在基础开销。

（完）
