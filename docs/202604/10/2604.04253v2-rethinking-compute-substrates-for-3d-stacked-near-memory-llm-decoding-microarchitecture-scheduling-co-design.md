---
title: "Rethinking Compute Substrates for 3D-Stacked Near-Memory LLM Decoding: Microarchitecture-Scheduling Co-Design"
title_zh: 重新思考3D堆叠近存LLM解码的计算基底：微架构-调度协同设计
authors: "Chenyang Ai, Yixing Zhang, Haoran Wu, Yudong Pan, Lechuan Zhao, Wenhui OU"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04253v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 近存LLM解码的微架构与调度协同设计
tldr: 针对大语言模型（LLM）推理中解码阶段的带宽瓶颈，本文重新审视了3D堆叠近存计算（NMP）的计算架构。由于NMP极高的带宽使部分算子转为计算受限，且逻辑层面积受限，研究者提出了一种将可重构脉动阵列与向量核心统一的微架构，并配合多核调度框架。该设计在保持低面积开销的同时，显著提升了处理多样化算子的效率，在Dense和MoE模型上均表现出优异的性能和能效。
selection_source: fresh_fetch
motivation: 3D堆叠近存计算提供的高带宽使LLM解码算子从带宽受限转为计算受限，而现有NMP设计在有限面积下难以兼顾计算效率与算子多样性。
method: 提出一种将面积高效的脉动阵列与向量核心深度融合的微架构，支持形状与数据流的可重构性，并辅以多核调度框架。
result: 实验表明，该设计相比Stratum在Dense和MoE模型上平均实现了2.91倍的加速和2.40倍的能效提升。
conclusion: 通过微架构与调度的协同设计，3D堆叠NMP能有效解决LLM解码的计算瓶颈，是实现高性能、高能效推理的关键。
---

## 摘要
大语言模型（LLM）解码是主要的推理瓶颈，因为其较低的算术强度使得性能对内存带宽高度敏感。3D堆叠近存处理（NMP）提供了比传统片外接口高得多的本地内存带宽，使其成为加速解码的极具前景的基底。然而，我们的分析表明，这种带宽优势也使得3D堆叠NMP上的许多解码算子重新回到了计算受限的状态。在逻辑晶圆有限的面积预算下，计算基底本身的设计因此成为了首要挑战。因此，我们重新思考了以往3D堆叠NMP设计的计算微架构。首先，我们将以往基于MAC树的计算单元替换为面积效率更高的脉动阵列，并进一步观察到解码算子表现出显著的形状多样性，这使得脉动阵列形状和数据流的可重构性对于维持高利用率至关重要。基于这一洞察，我们继续挖掘两个关键机会：高本地内存带宽减少了对大型片上缓冲区的需求，而原本用于处理辅助张量计算的现有向量核心已经提供了脉动阵列细粒度灵活性所需的大部分控制逻辑和多端口缓冲，这使我们能够以极高的面积效率统一这两种结构。基于这些见解，我们提出了首个专为3D堆叠NMP LLM解码定制的计算微架构，旨在明确满足低面积成本、高带宽运行和细粒度可重构性的共同需求。我们还提出了一个多核调度框架。与Stratum相比，我们的设计在稠密模型和MoE模型上平均实现了2.91倍的加速和2.40倍的能效提升。

## Abstract
Large language model (LLM) decoding is a major inference bottleneck because its low arithmetic intensity makes performance highly sensitive to memory bandwidth. 3D-stacked near-memory processing (NMP) provides substantially higher local memory bandwidth than conventional off-chip interfaces, making it a promising substrate for decode acceleration. However, our analysis shows that this bandwidth advantage also shifts many decode operators on 3D-stacked NMP back into the compute-bound regime. Under the tight area budget of the logic die, the design of the compute substrate itself therefore becomes a first-order challenge. Therefore, we rethink the compute microarchitecture of prior 3D-stacked NMP designs. First, we replace prior MAC tree-based compute units with a more area-efficient systolic array, and we further observe that decode operators exhibit substantial shape diversity, making reconfigurability in both systolic array shape and dataflow essential for sustaining high utilization. Building on this insight, we continue to exploit two key opportunities: the high local memory bandwidth reduces the need for large on-chip buffers, and the existing vector core, originally designed to handle auxiliary tensor computations, already provides much of the control logic and multi-ported buffering required for fine-grained flexibility for systolic array, allowing us to unify the two structures in a highly area-efficient manner. Based on these insights, we present the first compute microarchitecture tailored to 3D-stacked NMP LLM decoding, explicitly designed to satisfy the joint requirements of low area cost, high-bandwidth operation, and fine-grained reconfigurability. We further propose an multi-core scheduling framework. Compared with Stratum, our design achieves an average 2.91x speedup and 2.40x higher energy efficiency across both dense and MoE models.

---

## 论文详细总结（自动生成）

这篇论文名为《Rethinking Compute Substrates for 3D-Stacked Near-Memory LLM Decoding: Microarchitecture–Scheduling Co-Design》，提出了一种名为 **SNAKE** 的架构。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：大语言模型（LLM）的推理分为预填充（Prefill）和解码（Decode）阶段。解码阶段由于算术强度极低，受限于内存带宽。3D堆叠近存处理（NMP）通过在逻辑层（Logic Die）上集成计算单元，利用TSV等技术提供极高的本地带宽，被视为加速解码的理想方案。
*   **核心问题**：
    1.  **瓶颈转移**：3D NMP提供的高带宽使得原本受内存限制的解码算子转变为“计算受限”（Compute-bound），而逻辑层面积预算极其有限，无法简单通过堆砌计算单元解决。
    2.  **算子多样性**：解码算子（如不同Batch Size下的投影层）形状差异大，传统的固定形状计算阵列利用率低。
    3.  **架构冗余**：现有的MAC树结构面积效率低，且向量核心与张量核心分离，造成了面积浪费。

### 2. 论文提出的方法论
*   **核心思想**：通过“微架构-调度协同设计”，在极小的面积内实现高密度、可重构的计算基底。
*   **关键技术细节**：
    *   **计算单元替换**：使用面积效率更高的脉动阵列（Systolic Array, SA）替代传统的MAC树结构（RTL实现显示SA面积效率高出8.23倍）。
    *   **SNAKE-like 映射**：提出一种蛇形映射机制，通过物理PE阵列内的可重构路径，将固定的物理阵列（如64x64）逻辑上重构为不同形状（如8x512或16x256），以匹配解码算子的形状。
    *   **资源重分配**：利用3D NMP的高带宽特性，缩减了用于掩盖延迟的大型SRAM缓冲区，将节省的面积分配给更多的计算PE。
    *   **统一计算基底**：将向量核心（处理非线性算子）与脉动阵列深度融合，共享多端口缓冲和控制逻辑，减少冗余。
    *   **多核调度框架**：针对多处理单元（PU），设计了四种调度模式：空间划分（IS-S, OS-S）和时空划分（IS-ST, OS-ST），根据算子特征动态选择最优路径。

### 3. 实验设计
*   **数据集/场景**：涵盖了主流的Dense和MoE模型，包括 OPT 66B, LLaMA3 70B, Mixtral 8x22B, Qwen3 30B-A3B, 以及 DeepSeek 236B。
*   **Benchmark（基准）**：
    *   **Stratum**：最先进的3D堆叠NMP设计（基于MAC树）。
    *   **NVIDIA H100 GPU**：作为纯片外内存方案的对比。
    *   **固定形状SA**：48x48（近正方形）和8x288（长条形）的固定脉动阵列。
*   **对比维度**：端到端延迟（E2E）、Token间延迟（TBT）、能效比、面积效率、热力学表现。

### 4. 资源与算力
*   **硬件实现/模拟**：
    *   使用 **SystemVerilog** 进行了RTL实现，并在 **7nm ASAP7** 预测性工艺库下进行综合。
    *   性能模拟基于 **Scale-Sim v3**（周期精确模拟器）并集成了 **Ramulator** 内存模型。
    *   **GPU算力**：在端到端服务评估中，使用 NVIDIA H100 作为 Prefill 引擎，解码部分则在模拟的 NMP 架构上运行。
*   **训练/运行时间**：文中未提及具体的模拟运行总时长，但详细列出了逻辑层的频率（800MHz）和热功耗预算（约62W）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对5种不同规模和结构的LLM进行了全面测试。
    *   进行了详细的消融实验，包括：不同Batch Size下的性能波动、不同调度模式的效率对比、面积与缓存的权衡分析。
    *   包含热力学模拟（使用HotSpot 6.0），确保设计在3D堆叠环境下的可行性。
*   **公平性**：实验在相同的面积预算（Area-normalized）和相同的内存带宽（24 TB/s）假设下对比不同架构，具有较高的客观性。

### 6. 论文的主要结论与发现
*   **性能提升**：相比 Stratum，SNAKE 在稠密和MoE模型上平均实现 **2.91倍** 的加速。
*   **能效优化**：平均能效提升 **2.40倍**。
*   **面积效率**：通过架构融合和SA应用，计算面积效率比MAC树方案提升了 **4.00倍**。
*   **调度发现**：对于MoE模型，时空划分（ST模式）比单纯的空间划分更有效；而对于Dense模型，IS-S模式占据主导。

### 7. 优点
*   **协同设计视角**：不仅关注硬件微架构，还深入探讨了算子层面的调度策略，解决了NMP中“带宽过剩、计算不足”的新矛盾。
*   **极高的面积效率**：在逻辑层面积极其受限的3D堆叠环境下，通过SNAKE映射和资源复用，极大压榨了硬件性能。
*   **灵活性**：成功解决了脉动阵列在处理多样化形状算子时利用率低的顽疾。

### 8. 不足与局限
*   **频率限制**：由于引入了额外的可重构路由逻辑，其目标频率（800MHz）略低于固定形状的基准（1GHz），虽然性能最终胜出，但增加了时序收敛的难度。
*   **软件栈依赖**：该方案依赖于一个能够进行轻量级搜索的编译器/调度器来选择最优模式，增加了软件层的复杂性。
*   **工艺假设**：基于7nm预测性PDK和HBM3/4的假设，实际落地可能受限于3D封装（如Hybrid Bonding）的良率和成本。

（完）
