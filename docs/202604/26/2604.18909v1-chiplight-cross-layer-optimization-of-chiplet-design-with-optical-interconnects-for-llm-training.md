---
title: "ChipLight: Cross-Layer Optimization of Chiplet Design with Optical Interconnects for LLM Training"
title_zh: ChipLight：面向大语言模型训练的集成光互连芯粒设计跨层优化
authors: "Kangbo Bai, Zhantong Zhu, Yifan Ding, Tianyu Jia"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18909v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对大模型训练的芯粒架构与光互连跨层优化
tldr: ChipLight针对大规模LLM训练中的通信瓶颈，提出了一种结合芯粒（Chiplet）和光互连（OI）技术的跨层多目标优化方法。该方法通过协同优化芯粒架构、并行训练策略和光网络拓扑，并结合黑盒与白盒搜索算法进行设计空间探索，显著提升了训练集群的效率，为未来高性能计算集群设计提供了重要参考。
selection_source: fresh_fetch
motivation: 大规模LLM训练中设备间通信成为性能瓶颈，需结合芯粒和光互连技术以突破单封装及跨封装的带宽限制。
method: 提出ChipLight框架，通过抽象架构模型协同优化芯粒设计、并行策略与光网络拓扑，并采用混合优化算法进行设计空间探索。
result: 实验结果表明，ChipLight显著提高了LLM训练效率，并为未来训练集群的开发提供了有价值的设计洞察。
conclusion: 跨层协同优化芯粒与光互连技术是解决大规模分布式训练通信挑战的有效途径。
---

## 摘要
在大规模分布式大语言模型（LLM）训练中，设备间的通信已成为关键的性能瓶颈。芯粒（Chiplet）技术可以将多个裸片（die）集成到单个封装中，通过更高的带宽提升节点性能。同时，光互连（OI）技术提供长距离、高带宽的链路，使其非常适合横向扩展（scale-out）网络。这两种技术的结合具有克服封装内和封装间通信瓶颈的潜力。在这项工作中，我们提出了 ChipLight，一种针对利用芯粒和光互连的训练集群的跨层多目标设计与优化方法。我们首先为这类复杂集群抽象出一个架构模型，协同优化芯粒架构、训练并行策略以及光互连网络拓扑。基于该模型，我们结合黑盒与白盒方法定制了设计空间探索流程。实验结果评估表明，ChipLight 显著提升了训练效率，并为未来训练集群的开发提供了宝贵的设计见解。

## Abstract
In large-scale distributed LLM training, communication between devices becomes the key performance bottleneck. Chiplet technology can integrate multiple dies into a package to scale-up node performance with higher bandwidth. Meanwhile, optical interconnect (OI) technology offers long-reach, high-bandwidth links, making it well suited for scale-out networks. The combination of these two technologies has the potential to overcome communication bottlenecks within and across packages. In this work, we present ChipLight, a cross-layer multi-objective design and optimization method for training clusters leveraging chiplet and OI. We first abstract an architecture model for such complex clusters, co-optimizing chiplet architecture, training parallel strategy, and OI network topology. Based on such models, we tailor the design space exploration flow by combining both black-box and white-box methodologies. Evaluated by our experimental results, ChipLight achieves significantly improved training efficiency and provides valuable design insights for the development of future training clusters.

---

## 论文详细总结（自动生成）

这篇论文提出了 **ChipLight**，一个针对大语言模型（LLM）训练集群的跨层设计与优化框架，旨在通过结合**芯粒（Chiplet）**技术和**光互连（Optical Interconnect, OI）**技术来突破分布式训练中的通信瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着 LLM 参数量和上下文长度的激增，分布式训练中的设备间通信成为主要的性能瓶颈（即“通信墙”）。
*   **研究背景**：
    *   传统的 GPU 集群依赖 NVLink（封装内/节点内）和 InfiniBand（节点间）进行扩展，但节点间带宽（约 60GB/s）远低于节点内带宽，导致严重的带宽失配。
    *   **芯粒技术**（如多芯片模块 MCM）能显著提升封装内带宽，但若节点间仍使用低速电互连，芯粒带来的优势会被抵消。
    *   **光互连技术**（如共封装光学 CPO 和光电路交换 OCS）提供了高带宽、长距离传输的可能性。
*   **整体含义**：论文旨在探索如何协同设计芯粒架构、网络拓扑和并行策略，以充分发挥芯粒与光互连结合的潜力。

### 2. 方法论：核心思想与技术细节
ChipLight 采用了一种**跨层多目标优化方法**，主要包含以下核心组件：
*   **抽象模型**：
    *   **芯粒模型**：定义了 MCM 封装内的逻辑芯粒数量（$x \times y$）、内存芯粒数量（$m$）以及光 I/O 端口数（$o$）。
    *   **网络模型**：采用基于“Rail”的多维模型，将复杂的物理拓扑抽象为多个维度，支持通过 OCS 进行逻辑重构。
*   **核心思想：动态链路复用（Dynamic Link Reuse）**：
    *   基于对训练流量的观测，发现上下文并行（CP）和专家并行（EP）的流量在时间上是交替出现的。
    *   ChipLight 通过 OCS 在不同训练阶段重新配置链路，使同一组光纤在不同时间服务于不同的并行模式，从而提高带宽利用率。
*   **优化流程（嵌套搜索）**：
    *   **外层搜索（白盒）**：利用启发式规划器（Planner）分析仿真日志，识别硬件瓶颈（如计算、内存或通信），动态调整芯粒架构参数。
    *   **内层搜索（黑盒）**：针对给定的硬件配置，利用概率随机森林（PRF）等算法搜索最优的并行策略和网络拓扑映射。

### 3. 实验设计
*   **目标模型**：Qwen3-235B-A22B（混合专家模型 MoE），上下文长度为 10k。
*   **仿真工具**：基于修改后的 **ASTRA-sim** 模拟器。
*   **对比基准（Benchmarks）**：
    1.  **H100 GPU 集群**：使用 NVLink 和 InfiniBand（IB）的传统方案。
    2.  **Chiplet + IB**：仅使用芯粒技术，但保留电互连网络。
    3.  **RailX**：最近提出的结合芯粒与光互连的先进网络架构方案。
*   **评估指标**：训练吞吐量（Throughput）、成本（Cost）、帕累托前沿（Pareto frontier）。

### 4. 资源与算力
*   **硬件规格**：逻辑芯粒参数参考 NVIDIA H100，内存参考 HBM3，光互连参数参考最新的 CPO 技术指标。
*   **集群规模**：模拟了总计算能力高达 $16 \times 10^6$ TFLOPS 的集群（约合上千个高性能计算节点）。
*   **算力说明**：论文未提及执行优化算法本身消耗的具体算力时长，主要侧重于通过仿真评估不同架构设计下的训练效率。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   吞吐量随集群规模扩展的趋势分析。
    *   不同 MCM 规模（4 到 64 个芯粒）对成本和性能的影响。
    *   逻辑芯粒面积缩减（Yield 提升）与性能的权衡实验。
    *   内存带宽与光端口数量的消融实验。
*   **充分性**：实验涵盖了从单芯片设计到大规模网络拓扑的多个维度，对比了工业界主流方案和学术界最新成果，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **性能提升**：优化后的 ChipLight 集群相比传统 GPU 集群实现了 **19.58 倍**的吞吐量提升；在相同成本下，比 RailX 方案性能高出 **41%**。
*   **技术协同**：单纯依靠芯粒技术无法突破通信墙，必须结合光互连才能显著提升系统可扩展性。
*   **动态复用的价值**：如果不使用动态链路复用技术，系统吞吐量会下降 30%。
*   **设计洞察**：
    *   大规模芯粒集成能显著降低集群总成本。
    *   芯粒架构下的逻辑芯粒相比传统 GPU 需要更多的 HBM 堆栈（建议 14 个左右）以匹配带宽。
    *   当前光互连的瓶颈不在于 CPO 的带宽密度，而在于昂贵的 OCS 交换机成本。

### 7. 优点
*   **跨层协同**：打破了硬件设计、网络工程和软件并行策略之间的壁垒，实现了全局最优。
*   **时空特性利用**：敏锐捕捉到 LLM 训练流量在空间上的稀疏性和时间上的交替性，提出的动态链路复用极具创新性。
*   **高效搜索**：结合黑盒采样与基于日志的白盒启发式规划，显著提高了复杂设计空间的探索效率。

### 8. 不足与局限
*   **仿真依赖**：所有结论均基于模拟器得出，缺乏真实硬件原型的验证（考虑到光互连和大规模芯粒集成的落地难度，这在目前是普遍现象）。
*   **OCS 切换延迟**：虽然文中提到 OCS 毫秒级的切换延迟在训练阶段间是可以接受的，但在更复杂的动态负载下，重构开销可能会被放大。
*   **成本模型偏差**：成本评估基于现有文献的估算模型，未来芯粒制造和光电集成技术的良率变化可能会改变成本帕累托曲线。

（完）
