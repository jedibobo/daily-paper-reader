---
title: "AQPIM: Breaking the PIM Capacity Wall for LLMs with In-Memory Activation Quantization"
title_zh: AQPIM：通过内存内激活量化突破大语言模型的 PIM 容量墙
authors: "Kosuke Matsushima, Yasuyuki Okoshi, Masato Motomura, Daichi Fujiki"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18137v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对存内计算优化的激活量化，提升带宽和计算效率
tldr: AQPIM针对大语言模型在存内计算（PIM）架构中面临的KV缓存容量瓶颈，提出了一种存内激活量化框架。该框架利用乘积量化（PQ）技术在内存中直接对激活值进行压缩和计算，并通过算法优化解决了精度挑战。AQPIM显著降低了内存占用和计算开销，大幅减少了GPU-CPU通信延迟，相比现有SOTA PIM方案实现了3.4倍的加速，有效提升了长文本推理效率。
selection_source: carryover_cache
motivation: 传统PIM架构难以应对长文本场景下庞大的KV缓存需求，且现有量化方法未能充分利用PIM的高内部带宽特性。
method: 提出基于乘积量化（PQ）的存内激活量化框架，通过在内存中直接进行数据压缩与计算来优化LLM的注意力机制。
result: "AQPIM大幅减少了占据解码延迟90%以上的GPU-CPU通信开销，并比现有最先进的PIM方法实现了3.4倍的性能提升。"
conclusion: 通过将激活量化与存内计算深度结合，AQPIM成功突破了PIM的容量限制，为高效LLM推理提供了新途径。
---

## 摘要
存内计算 (PIM) 架构为数据密集型机器学习中的内存瓶颈提供了极具前景的解决方案，但往往忽略了激活值内存占用不断增加的挑战。传统的 PIM 方法难以应对基于 Transformer 的模型在长上下文场景下产生的海量 KV 缓存，这些缓存经常超出 PIM 有限的内存容量，而稀疏注意力等技术又可能与 PIM 对数据局部性的需求相冲突。现有的 PIM 方法和量化手段通常不足以或不适合利用激活值的独特特征。本研究发现，针对 PIM 专门设计的激活量化有机会提升带宽和计算效率。我们探索了基于聚类的矢量量化方法，该方法与激活值特征以及 PIM 的内部带宽能力高度契合。在此基础上，我们提出了 AQPIM，这是一种基于乘积量化 (PQ) 的新型 PIM 感知激活量化框架，并针对现代大语言模型 (LLMs) 进行了优化。通过直接在内存中执行量化，AQPIM 利用了 PIM 的高内部带宽，并实现了在压缩数据上的直接计算，显著降低了注意力计算的内存占用和计算开销。AQPIM 通过引入多项算法优化解决了 PQ 的精度挑战。评估结果表明，AQPIM 实现了显著的性能提升，大幅减少了占解码延迟 90%~98.5% 的 GPU-CPU 通信，且相比最先进的 (SOTA) PIM 方法实现了 3.4 倍的加速。

## 速览
**TLDR**：针对大语言模型在长文本场景下KV缓存占用过高、超出存内计算（PIM）容量限制的问题，本文提出AQPIM框架。该框架利用PIM的高内部带宽，在内存中直接进行基于乘积量化（PQ）的激活值量化，并支持在压缩数据上直接计算。通过引入算法优化解决精度挑战，AQPIM显著降低了内存占用和计算开销，相比现有SOTA PIM方案实现3.4倍加速，并大幅减少了GPU-CPU通信延迟。 \
**Motivation**：传统PIM架构难以应对长文本LLM产生的海量KV缓存，且现有量化方法未能有效结合PIM的内部带宽优势。 \
**Method**：提出一种基于乘积量化（PQ）的存内激活量化框架，通过在内存中直接执行量化和压缩数据计算来优化效率。 \
**Result**：AQPIM显著减少了占据解码延迟90%以上的通信开销，并相比现有最先进的PIM方法实现了3.4倍的性能提升。 \
**Conclusion**：AQPIM通过存内量化技术有效突破了PIM的容量瓶颈，为长上下文LLM的高效推理提供了高性能的解决方案。

---

## Abstract
Processing-in-Memory (PIM) architectures offer a promising solution to the memory bottlenecks in data-intensive machine learning, yet often overlook the growing challenge of activation memory footprint. Conventional PIM approaches struggle with massive KV cache sizes generated in long-context scenarios by Transformer-based models, frequently exceeding PIM's limited memory capacity, while techniques like sparse attention can conflict with PIM's need for data locality. Existing PIM approaches and quantization methods are often insufficient or poorly suited for leveraging the unique characteristics of activations. This work identifies an opportunity for PIM-specialized activation quantization to enhance bandwidth and compute efficiency.   We explore clustering-based vector quantization approaches, which align well with activation characteristics and PIM's internal bandwidth capabilities. Building on this, we introduce AQPIM, a novel PIM-aware activation quantization framework based on Product Quantization (PQ), optimizing it for modern Large Language Models (LLMs). By performing quantization directly within memory, AQPIM leverages PIM's high internal bandwidth and enables direct computation on compressed data, significantly reducing both memory footprint and computational overhead for attention computation. AQPIM addresses PQ's accuracy challenges by introducing several algorithmic optimizations. Evaluations demonstrate that AQPIM achieves significant performance improvements, drastically reducing of GPU-CPU communication that can account for 90$\sim$98.5\% of decoding latency, together with 3.4$\times$ speedup over a SOTA PIM approach.

---

## 论文详细总结（自动生成）

这是一份关于论文《AQPIM: Breaking the PIM Capacity Wall for LLMs with In-Memory Activation Quantization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在处理长文本推理时，KV Cache（键值缓存）的内存占用呈线性增长，形成了“容量墙”。
*   **研究背景**：
    *   **PIM（存内计算）的局限**：虽然 PIM 能缓解带宽瓶颈，但其内存密度低，难以容纳数百 GB 的 KV Cache。
    *   **现有方案的冲突**：传统的稀疏注意力（Sparse Attention）或卸载（Offloading）技术产生的随机访问模式与 PIM 的数据局部性需求相冲突；而常规量化方法在 PIM 中实现时，会因复杂的硬件逻辑（如支持不同精度的 ALU）导致巨大的面积开销。
*   **研究动机**：利用 PIM 极高的内部带宽，在内存中直接进行复杂的在线聚类量化，从而在不增加硬件复杂度的前提下压缩数据并加速计算。

### 2. 核心方法论
AQPIM 提出了一个算法与硬件协同设计的框架，核心思想是**基于乘积量化（Product Quantization, PQ）的存内激活压缩与计算**：
*   **在线聚类量化**：利用 PIM 内部带宽在预填充（Prefilling）阶段并行执行 k-means 聚类。通过将向量分解为子向量，降低量化误差。
*   **直接在压缩数据上计算**：将注意力机制中的 GEMV（矩阵-向量乘法）转换为查找表（LUT）操作。查询向量先与质心计算内积，随后通过索引直接累加结果，无需解压缩。
*   **页面感知窗口聚类（Page-aware Windowed Clustering）**：算法确保查找表数据驻留在单个 DRAM 行（Row Buffer）内。
*   **硬件协同设计**：
    *   **行内间接寻址（Intra-Row Indirection）**：在 Bank 级处理单元（BankPE）中添加极小的逻辑，支持快速的行缓冲命中查找，解决随机访问延迟。
    *   **异构 PE 布局**：BankPE 负责简单的 FP16 乘加运算，BufferPE（位于缓冲芯片）负责复杂的除法、指数（Softmax）等运算。
*   **算法优化**：
    *   **重要性加权聚类**：根据注意力分数对 Token 加权，确保关键 Token 的量化精度更高。
    *   **通道排序（Channel Sorting）**：在离线阶段根据余弦相似度重新排列通道，增强子向量内部的相关性。

### 3. 实验设计
*   **模型**：Mistral-7B-Instruct-v0.2 (32K 上下文) 和 Llama-3.2-3B-Instruct (128K 上下文)。
*   **数据集**：LongBench 评测集，涵盖单/多文档问答、摘要、代码补全等任务（如 NarrativeQA, HotpotQA, GovReport 等）。
*   **对比方法（Baselines）**：
    *   **算法类**：SnapKV（稀疏注意力）、PQCache（基于卸载的 PQ）、SKVQ（滑动窗口量化）。
    *   **架构类**：传统 GPU+HBM 系统、AttAcc!（SOTA PIM 加速器）。

### 4. 资源与算力
*   **硬件模拟**：实验基于定制化的 GPU-PIM 模拟器（基于 Ramulator 2.0 和 AttAcc! 模拟器修改）。
*   **算力配置**：模拟环境配置为 NVIDIA H100 GPU 核心，搭配 4 个 16GB 的 HBM3-PIM 模块。
*   **主机 CPU**：Intel Xeon Platinum 8480+。
*   **训练/运行时间**：文中未明确提及模拟运行的具体物理时长，但详细记录了推理过程中的周期（Cycles）和延迟（Latency）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流开源模型上进行了全量 LongBench 任务测试。
    *   进行了详细的消融实验，验证了“加权聚类”和“通道排序”对精度的贡献。
    *   测试了不同压缩率（子向量数量 $m$ 和质心数量 $K$）下的精度与性能权衡。
*   **充分性评价**：实验设计较为全面，不仅对比了算法精度，还深入分析了硬件层面的能量效率、面积开销和解码步长延迟，论证了方案的客观性。

### 6. 主要结论
*   **性能提升**：相比 SOTA PIM 方案（AttAcc!），AQPIM 实现了 **3.4 倍** 的加速；相比传统 GPU 卸载方案，加速比更高。
*   **通信优化**：大幅减少了 GPU 与 CPU 之间的通信开销，该开销在长文本解码中通常占总延迟的 90%~98.5%。
*   **容量突破**：在保持高精度的前提下，实现了约 **80%** 的 KV Cache 内存减量。
*   **能效比**：相比 GPU 基准，能量效率提升了高达 **14.29 倍**。

### 7. 优点与亮点
*   **协同设计精妙**：巧妙地将 PIM 的“高内部带宽、低逻辑密度”特性与 PQ 算法的“高带宽需求、低计算复杂度”特性结合。
*   **硬件开销极低**：通过行内间接寻址设计，仅增加了 0.43% 的 BankPE 面积，避免了在内存芯片上集成复杂 ALU。
*   **无需解压计算**：直接在压缩域进行注意力计算，这在 PIM 研究中是一个显著的效率突破。

### 8. 不足与局限
*   **模拟局限性**：所有性能数据均来自模拟器而非真实硅片，实际 DRAM 制造中的工艺偏差可能影响行内寻址的定时（Timing）。
*   **算法依赖性**：该方案高度依赖于 Transformer 的注意力机制结构，对于非 Transformer 架构（如线性注意力或状态空间模型 SSM）的适用性未讨论。
*   **在线聚类开销**：虽然文中提到聚类开销可被预填充阶段掩盖，但在极短输入或极高吞吐需求下，在线计算质心的延迟可能成为新的瓶颈。

（完）
