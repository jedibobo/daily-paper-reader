---
title: "HYPERHEURIST: A Simulated Annealing-Based Control Framework for LLM-Driven Code Generation in Optimized Hardware Design"
title_zh: HYPERHEURIST：一种用于优化硬件设计中大语言模型驱动代码生成的模拟退火控制框架
authors: "Shiva Ahir, Prajna Bhat, Alex Doboli"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15642v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 利用LLM生成RTL并进行PPA优化与硬件设计控制
tldr: 本研究针对大语言模型在生成RTL硬件设计时难以兼顾功能正确性与PPA（功耗、性能、面积）优化的问题，提出了HYPERHEURIST框架。该框架利用模拟退火算法控制LLM生成过程，将生成的代码视为中间候选，通过先验证功能正确性、后优化PPA的分阶段策略，在八个基准测试中实现了比单次生成更稳定且可重复的硬件设计优化。
selection_source: carryover_cache
motivation: 解决大语言模型在单次生成硬件代码时，难以同时满足功能正确性和功耗、性能、面积最优要求的问题。
method: 提出一种基于模拟退火的控制框架，将LLM生成的RTL作为中间候选，通过编译仿真过滤后进行启发式PPA优化。
result: 在八个RTL基准测试上的评估显示，该框架比传统的单次LLM生成方法具有更强的稳定性和更优的优化重复性。
conclusion: 结合模拟退火与LLM的分阶段搜索策略，能有效提升自动化硬件设计的质量，是实现高性能RTL生成的有效途径。
---

## 摘要
大语言模型（LLMs）在生成寄存器传输级（RTL）硬件设计方面展现出显著的进展，这主要归功于它们能够快速提出多种架构实现方案。然而，单次（single-shot）LLM 生成难以稳定地产生既能保证功能正确又具备高能效的设计。本文提出了 HYPERHEURIST，这是一种基于模拟退火的控制框架，它将 LLM 生成的 RTL 视为中间候选方案而非最终设计。该系统不仅关注功能正确性，还致力于功耗-性能-面积（PPA）的优化。在第一阶段，通过编译、结构检查和仿真对 RTL 候选方案进行筛选，以识别功能有效的设计。PPA 优化仅针对已通过编译和仿真的 RTL 设计进行。在八个 RTL 基准测试上的评估结果表明，与单次生成的 RTL 相比，这种分阶段方法能够产生更稳定且可重复的优化行为。

## Abstract
Large Language Models (LLMs) have shown promising progress for generating Register Transfer Level (RTL) hardware designs, largely because they can rapidly propose alternative architectural realizations. However, single-shot LLM generation struggles to consistently produce designs that are both functionally correct and power-efficient. This paper proposes HYPERHEURIST, a simulated annealing-based control framework that treats LLM-generated RTL as intermediate candidates rather than final designs. The suggested system not only focuses on functionality correctness but also on Power-Performance-Area (PPA) optimization. In the first phase, RTL candidates are filtered through compilation, structural checks, and simulation to identify functionally valid designs. PPA optimization is restricted to RTL designs that have already passed compilation and simulation. Evaluated across eight RTL benchmarks, this staged approach yields more stable and repeatable optimization behavior than single-pass LLM-generated RTL.

---

## 论文详细总结（自动生成）

以下是对论文《HYPERHEURIST: A Simulated Annealing-Based Control Framework for LLM-Driven Code Generation in Optimized Hardware Design》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在生成寄存器传输级（RTL）硬件代码方面表现出潜力，但面临两大挑战：一是“单次生成（Single-shot）”难以保证功能正确性；二是难以在满足约束的同时优化功耗、性能和面积（PPA）。
*   **核心问题**：现有的LLM硬件生成方法往往将功能修复与性能优化混为一谈，导致搜索过程不稳定、易陷入局部最优或产生无效设计。
*   **研究意义**：本文提出了 **HYPERHEURIST** 框架，旨在通过模拟退火（SA）算法控制LLM的生成过程，将RTL生成视为一个迭代优化的启发式过程，从而系统性地解决功能正确性与PPA优化之间的矛盾。

### 2. 论文提出的方法论
*   **核心思想**：采用**分阶段解耦策略**。将设计流程分为“功能正确性发现”和“PPA约束优化”两个独立但协调的阶段。
*   **关键技术细节**：
    *   **多角色提示词管道**：定义了四种LLM角色：生成器（Generator）、保守变异器（Conservative Mutator）、激进变异器（Aggressive Mutator）和评论员（Critique）。
    *   **两阶段流程**：
        *   **阶段I（正确性驱动）**：利用Synopsys VCS仿真反馈，通过SA算法最大化正确性得分，筛选出功能完备的RTL种子。
        *   **阶段II（PPA驱动）**：在保持功能正确的前提下，利用Synopsys Design Compiler（DC）的综合反馈，通过SA算法最小化PPA代价函数。
    *   **算法流程**：统一使用模拟退火算法。根据工具反馈（编译错误、仿真失败或PPA数据）计算能量函数（Reward），并依据Metropolis准则决定是否接受新的代码变异，以跳出局部最优。

### 3. 实验设计
*   **数据集/场景**：选取了来自 **RTLLLM** 基准测试集的8个典型RTL设计任务，包括：`serial2parallel_8`（串并转换）、`alu4`（算术逻辑单元）、`counter_0_12`（计数器）、`traffic_light`（交通灯控制）、`freq_div`（分频器）等。
*   **Benchmark与对比方法**：
    *   **基准方法**：直接使用 GPT-4.0 和 GPT-3.5（配合Self-planning）的单次生成结果（引用自RTLLLM论文数据）。
    *   **对比维度**：语法正确性、结构正确性、逻辑正确性以及综合后的面积（Area）、功耗（Power）和时序（Timing/Slack）。

### 4. 资源与算力
*   **EDA工具**：使用了 Synopsys VCS (W-2024.09) 进行仿真，Synopsys Design Compiler (V-2023.12) 进行逻辑综合（基于90nm工艺库）。
*   **LLM模型**：实验主要基于 GPT-4.0 接口进行启发式生成与变异。
*   **算力说明**：文中**未明确说明**具体的GPU型号、数量或LLM调用的总Token数/时长，主要侧重于算法框架在EDA工具链中的集成与迭代逻辑。

### 5. 实验数量与充分性
*   **实验规模**：针对8个基准测试，每个方法运行了5次独立试验以评估成功率和稳定性。
*   **充分性评价**：
    *   **优点**：涵盖了从简单组合逻辑到复杂时序逻辑的多种硬件模块；引入了真实的工业级EDA工具反馈，实验结果具有较高的客观性。
    *   **局限**：样本量（8个设计）在自动化设计领域尚属中等规模，对于超大规模集成电路（VLSI）的适用性尚未验证。

### 6. 论文的主要结论与发现
*   **解耦的有效性**：先确保功能正确再优化PPA的策略显著提升了搜索效率，避免了在无效设计上浪费综合算力。
*   **性能提升**：相比基准方法，HYPERHEURIST 在结构正确性上实现了最高 **70%** 的相对提升。
*   **PPA优化**：在多个任务（如 `johnson_counter` 和 `mux2_sync`）中，该框架生成的代码在面积和功耗上均优于LLM直接生成的结果，且能保持正的时序余量（Positive Slack）。

### 7. 优点
*   **系统性框架**：将模拟退火与多角色LLM结合，提供了一种比简单Prompt Engineering更严谨的控制机制。
*   **闭环反馈**：实现了真正的“工具在环（Tool-in-the-loop）”，利用编译器和综合器的真实报错/报告指导代码修复。
*   **稳定性**：通过SA算法的概率接受机制，有效缓解了LLM生成代码时常见的随机性和不稳定性。

### 8. 不足与局限
*   **复杂逻辑失效**：在处理高度耦合的状态机（如 `traffic_light`）时，由于仿真日志提供的诊断信息有限，框架难以通过简单的变异修复深层的逻辑错误。
*   **时序依赖限制**：对于具有严格多周期时序依赖的设计，目前的启发式修复策略表现不佳。
*   **计算开销**：虽然文中未详述，但多次调用LLM API结合EDA工具综合，其时间成本和API调用成本显著高于单次生成。

（完）
