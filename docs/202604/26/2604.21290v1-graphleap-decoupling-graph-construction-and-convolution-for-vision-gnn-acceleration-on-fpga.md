---
title: "GraphLeap: Decoupling Graph Construction and Convolution for Vision GNN Acceleration on FPGA"
title_zh: GraphLeap：用于 FPGA 上视觉 GNN 加速的图构建与卷积解耦方案
authors: "Anvitha Ramachandran, Dhruv Parikh, Viktor Prasanna"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21290v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 通过解耦构建与卷积在FPGA上加速视觉GNN
tldr: 视觉图神经网络（ViG）因每层需动态构建kNN图而面临巨大的计算瓶颈。本文提出GraphLeap，通过解耦图构建与特征更新，利用“前向看一层”的策略实现两者的并行处理。基于此，开发了首个端到端FPGA加速器，通过流式流水线设计显著提升了推理速度，且仅需微调即可保持精度，为实时ViG推理提供了高效方案。
selection_source: fresh_fetch
motivation: 视觉图神经网络中每层动态构建kNN图的过程占据了绝大部分计算时间，且与特征更新存在串行依赖，严重限制了推理效率。
method: 提出GraphLeap架构，通过使用上一层特征构建当前层图的解耦策略，实现图构建与消息传递的并行流水线化。
result: 在Alveo U280 FPGA上，GraphLeap相比CPU和GPU分别实现了最高95.7倍和8.5倍的加速。
conclusion: 该研究证明了通过算法与硬件协同设计，可以有效克服ViG的计算瓶颈，实现高性能的实时视觉图神经网络推理。
---

## 摘要
视觉图神经网络 (ViG) 将图像表示为补丁标记 (patch tokens) 的图，从而实现自适应的、特征驱动的邻域。与具有固定网格偏置的 CNN 或具有全局标记交互的 Vision Transformer 不同，ViG 依赖于动态图卷积：在每一层，通过对当前补丁特征进行 k-最近邻 (kNN) 搜索来构建依赖于特征的图，随后进行消息传递。这种逐层图构建是主要的瓶颈，在 CPU 和 GPU 上消耗了 50% 到 95% 的图卷积时间，其复杂度随补丁数量 $N$ 呈 $O(N^2)$ 增长，并在图构建与特征更新之间产生了顺序依赖关系。我们引入了 GraphLeap，这是一种简单的重构方法，通过跨层解耦图构建与特征更新来消除这种依赖。GraphLeap 使用前一层特征构建的图来执行第 $\ell$ 层的特征更新，同时利用当前层特征为第 $\ell+1$ 层构建图。这种单层前瞻 (one-layer-lookahead) 图构建实现了图构建与消息传递的并发执行。尽管使用前一层特征可能会导致轻微的精度下降，但只需几个轮次 (epochs) 的轻量级微调就足以恢复原始精度。基于 GraphLeap，我们提出了首个用于视觉 GNN 的端到端 FPGA 加速器。我们的流式、层流水线设计将 kNN 图构建引擎与特征更新引擎重叠，利用了节点级和通道级并行性，并实现了高效的片上数据流，无需显式的边特征实例化。在 Alveo U280 FPGA 上对各向同性 (isotropic) 和金字塔型 (pyramidal) ViG 模型进行的评估表明，GraphLeap 相比 CPU 基准实现了高达 95.7 倍的加速，相比 GPU 基准实现了 8.5 倍的加速，证明了实时视觉 GNN 推理的可行性。

## Abstract
Vision Graph Neural Networks (ViGs) represent an image as a graph of patch tokens, enabling adaptive, feature-driven neighborhoods. Unlike CNNs with fixed grid biases or Vision Transformers with global token interactions, ViGs rely on dynamic graph convolution: at each layer, a feature-dependent graph is built via k-nearest-neighbor (kNN) search on current patch features, followed by message passing. This per-layer graph construction is the main bottleneck, consuming 50--95\% of graph convolution time on CPUs and GPUs, scaling as $O(N^2)$ with the number of patches $N$, and creating a sequential dependency between graph construction and feature updates.   We introduce GraphLeap, a simple reformulation that removes this dependency by decoupling graph construction from feature update across layers. GraphLeap performs the feature update at layer $\ell$ using a graph built from the previous layer's features, while simultaneously using the current layer's features to construct the graph for layer $\ell+1$. This one-layer-lookahead graph construction enables concurrent graph construction and message passing. Although using prior-layer features can introduce minor accuracy degradation, lightweight fine-tuning for a few epochs is sufficient to recover the original accuracy. Building on GraphLeap, we present the first end-to-end FPGA accelerator for Vision GNNs. Our streaming, layer-pipelined design overlaps a kNN graph construction engine with a feature update engine, exploits node- and channel-level parallelism, and enables efficient on-chip dataflow without explicit edge-feature materialization. Evaluated on isotropic and pyramidal ViG models on an Alveo U280 FPGA, GraphLeap achieves up to $95.7\times$ speedup over CPU and $8.5\times$ speedup over GPU baselines, demonstrating the feasibility of real-time Vision GNN inference.

---

## 论文详细总结（自动生成）

以下是对论文《GraphLeap: Decoupling Graph Construction and Convolution for Vision GNN Acceleration on FPGA》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：视觉图神经网络（ViG）通过将图像块视为图节点，能够捕捉自适应的、特征驱动的局部关系，性能优于传统 CNN 和 ViT。然而，ViG 的核心瓶颈在于**动态图构建**：每一层都需要根据当前特征重新进行 k-最近邻（kNN）搜索。
*   **核心问题**：
    1.  **计算开销大**：图构建占据了 50%–95% 的推理时间。
    2.  **顺序依赖**：在标准 ViG 中，必须先完成图构建才能进行卷积，导致严重的串行延迟。
    3.  **硬件支持缺失**：现有加速器多针对静态图或规则网格，缺乏对动态图像图构建的端到端支持。

### 2. 论文提出的方法论
*   **核心思想（GraphLeap 重构）**：提出“单层前瞻”（One-layer Lookahead）机制。在计算第 $\ell$ 层的特征更新时，使用基于第 $\ell-1$ 层特征构建的图；同时，利用第 $\ell$ 层的特征并行构建第 $\ell+1$ 层所需的图。通过这种解耦，消除了层内的顺序等待。
*   **关键技术细节**：
    *   **硬件架构**：设计了首个端到端 FPGA 加速器，包含两个核心引擎：
        *   **图构建引擎 (GCE)**：采用分块距离计算和最小堆排序，实时生成 kNN 索引流。
        *   **特征更新引擎 (FUE)**：包含采集模块（处理不规则内存访问）、图卷积模块（利用脉动阵列进行矩阵运算）和前馈网络模块。
    *   **流式处理**：不显式存储 $O(NK)$ 复杂度的边特征，而是通过 FIFO 缓冲区直接流式传输邻居索引和特征，极大地节省了片上内存。
    *   **精度恢复**：由于使用了“过期”一层的图，初始精度会有所下降。论文通过轻量级的微调（Fine-tuning）来让模型适应这种解耦结构。

### 3. 实验设计
*   **数据集**：ImageNet-1K（包含 128 万张训练图，5 万张验证图）。
*   **模型变体**：涵盖了各向同性（Isotropic: Ti, S, B）和金字塔型（Pyramidal: Py-Ti, Py-S, Py-M, Py-B）的 ViG 全系列模型。
*   **Benchmark（对比基准）**：
    *   **CPU**：AMD EPYC 7763（64 核，多线程执行）。
    *   **GPU**：NVIDIA RTX A5000（Ampere 架构）。
    *   **SOTA 加速器**：与针对 ViT 的 DRViT 和 UbiMoE 加速器进行了对比。

### 4. 资源与算力
*   **FPGA 平台**：AMD-Xilinx Alveo U280。
*   **资源利用率**：使用了 78.5% 的 DSP（7088 个）、67.5% 的 LUT 和 89.6% 的 URAM。
*   **训练/微调算力**：
    *   使用 AdamW 优化器和余弦调度器。
    *   **微调时长**：在 ImageNet-1K 上微调 30 个 epoch。
    *   **硬件频率**：FPGA 运行频率为 300 MHz。

### 5. 实验数量与充分性
*   **实验规模**：针对 7 种不同规模和结构的 ViG 模型进行了测试，涵盖了 224x224 和 448x448 两种分辨率。
*   **充分性评价**：实验设计较为充分。不仅对比了端到端延迟，还详细分析了重构前后的精度变化（Top-1 Accuracy），并提供了与现有最先进（SOTA）视觉加速器的横向对比。实验涵盖了从轻量级到中等规模的模型，证明了方案的通用性。

### 6. 论文的主要结论与发现
*   **性能飞跃**：在 FPGA 上，GraphLeap 相比 CPU 实现了最高 **95.7倍** 的加速，相比 GPU 实现了最高 **8.5倍** 的加速。
*   **延迟优势**：对于 ViG-B 模型，端到端延迟仅为 2.77ms，比 SOTA 的 ViT 加速器快约 5 倍。
*   **精度保持**：通过微调，GraphLeap 能够将精度恢复到原始 ViG 的 1.4% 以内（例如 ViG-Py-B 达到 82.34%）。
*   **瓶颈消除**：成功将图构建的 $O(N^2)$ 瓶颈通过流水线并行化掩盖。

### 7. 优点
*   **算法-硬件协同设计**：不是单纯地加速现有算法，而是通过改变算法依赖关系（解耦）来适配硬件并行性。
*   **高效的数据流**：避免了昂贵的边特征实例化，解决了 FPGA 片上内存受限的问题。
*   **端到端实现**：涵盖了从图像输入、卷积干（Stem）、图构建到最终分类头的全流程加速。

### 8. 不足与局限
*   **微调依赖**：该方法并非“即插即用”，部署前需要对预训练模型进行额外的微调以弥补精度损失。
*   **硬件资源限制**：由于使用了大量的 DSP 和 URAM，在资源较少的嵌入式 FPGA 上可能难以部署大型模型。
*   **分辨率固定**：硬件设计针对特定分辨率（224/448）进行了优化，对于任意尺寸输入的灵活性可能受限。
*   **精度微降**：尽管经过微调，部分模型的精度仍略低于原始串行版本的 ViG。

（完）
