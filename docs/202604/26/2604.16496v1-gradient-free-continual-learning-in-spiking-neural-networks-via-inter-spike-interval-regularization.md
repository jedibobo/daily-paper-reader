---
title: Gradient-Free Continual Learning in Spiking Neural Networks via Inter-Spike Interval Regularization
title_zh: 基于脉冲间隔正则化的脉冲神经网络无梯度持续学习
authors: "Samrendra Roy, Kazuma Kobayashi, Souvik Chakraborty, Sajedul Talukder, Syed Bahauddin Alam"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.16496v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对缺乏反向传播支持的类脑硬件的无梯度度量
tldr: 针对神经形态硬件缺乏反向传播支持的问题，本文提出了一种名为ISI-CV的无梯度脉冲神经网络（SNN）持续学习方法。该方法利用脉冲间隔（ISI）的变异系数（CV）作为突触重要性度量，保护发放规律的神经元以保留旧任务知识。在Split-MNIST等多个基准测试中，ISI-CV实现了极低甚至零遗忘，特别是在处理真实DVS数据时优于传统的基于梯度的正则化方法，为低功耗边缘设备的持续学习提供了高效方案。
selection_source: carryover_cache
motivation: 现有的持续学习方法依赖梯度计算，难以在缺乏反向传播支持的神经形态硬件上直接部署。
method: 提出基于脉冲间隔变异系数（ISI-CV）的无梯度重要性度量，通过保护发放规律的神经元来防止旧知识被覆盖。
result: 在多个基准测试中实现了接近零的遗忘率，并在N-MNIST神经形态数据集上显著优于传统的基于梯度的正则化方法。
conclusion: ISI-CV为神经形态芯片提供了一种仅需整数运算和脉冲计数的硬件友好型持续学习方案，在处理异步事件数据时表现出更强的鲁棒性。
---

## 摘要
持续学习是指按顺序获取新任务而不遗忘先前知识的能力，这对于在从核数字孪生监测到电网边缘故障检测的动态现实环境中部署神经网络至关重要。现有的突触重要性方法，如弹性权重巩固（EWC）和突触智能（SI），依赖于梯度计算，这使得它们与缺乏反向传播支持的类脑硬件不兼容。我们提出了 ISI-CV，这是首个用于脉冲神经网络（SNN）持续学习的无梯度突触重要性度量，它源自脉冲间隔（ISI）的变异系数（CV）。放电规律（低 CV）的神经元编码了稳定的、与任务相关的特征，并受到保护以防被覆盖；放电不规律的神经元则被允许自由调整。ISI-CV 仅需要脉冲时间计数器和整数运算，这些在所有类脑芯片上都是原生支持的。我们在四个难度递增的基准数据集上进行了评估：Split-MNIST、Permuted-MNIST、Split-FashionMNIST，以及使用真实动态视觉传感器（DVS）事件数据的 Split-N-MNIST。在三个随机种子实验中，ISI-CV 在 Split-MNIST 和 Split-FashionMNIST 上实现了零遗忘（AF = 0.000 +/- 0.000），在 Permuted-MNIST 上实现了接近零遗忘（AF = 0.001 +/- 0.000），并在真实类脑 DVS 数据上获得了最高的准确率和最低的遗忘率（AA = 0.820 +/- 0.012, AF = 0.221 +/- 0.014）。在 N-MNIST 上，基于梯度的方法会产生不可靠的重要性估计，其表现甚至不如无正则化的情况；而 ISI-CV 通过设计规避了这一失效问题。

## Abstract
Continual learning, the ability to acquire new tasks sequentially without forgetting prior knowledge, is essential for deploying neural networks in dynamic real-world environments, from nuclear digital twin monitoring to grid-edge fault detection. Existing synaptic importance methods, such as Elastic Weight Consolidation (EWC) and Synaptic Intelligence (SI), rely on gradient computation, making them incompatible with neuromorphic hardware that lacks backpropagation support. We propose ISI-CV, the first gradient-free synaptic importance metric for SNN continual learning, derived from the Coefficient of Variation (CV) of Inter-Spike Intervals (ISIs). Neurons that fire regularly (low CV) encode stable, task-relevant features and are protected from overwriting; neurons with irregular firing are permitted to adapt freely. ISI-CV requires only spike time counters and integer arithmetic, all of which are native to every neuromorphic chip. We evaluate on four benchmarks of increasing difficulty: Split-MNIST, Permuted-MNIST, Split-FashionMNIST, and Split-N-MNIST using real Dynamic Vision Sensor (DVS) event data. Across three seeds, ISI-CV achieves zero forgetting (AF = 0.000 +/- 0.000) on Split-MNIST and Split-FashionMNIST, near-zero forgetting on Permuted-MNIST (AF = 0.001 +/- 0.000), and the highest accuracy with the lowest forgetting on real neuromorphic DVS data (AA = 0.820 +/- 0.012, AF = 0.221 +/- 0.014). On N-MNIST, gradient-based methods produce unreliable importance estimates and perform worse than no regularization; ISI-CV avoids this failure by design.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ISI-CV** 的创新方法，旨在解决脉冲神经网络（SNN）在类脑（Neuromorphic）硬件上进行持续学习时的梯度依赖问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的持续学习（Continual Learning）算法（如 EWC, SI）高度依赖**反向传播和梯度计算**来评估突触重要性。然而，主流的类脑芯片（如 Intel Loihi, IBM TrueNorth）原生支持异步脉冲通信，却往往缺乏对复杂浮点梯度运算的支持。
*   **研究动机**：为了让边缘设备（如核电站数字孪生监测、电网故障检测）能够在不连接云端的情况下，在类脑硬件上实现“边用边学”且不遗忘旧知识，迫切需要一种**无梯度（Gradient-Free）**的突触重要性度量方法。

### 2. 核心方法论：ISI-CV 正则化
*   **核心思想**：借鉴神经科学中“脉冲间隔变异系数（CV of ISI）”的概念。研究发现，放电极其规律（低 CV，像时钟一样）的神经元通常编码了稳定的、关键的任务特征；而放电不规律（高 CV）的神经元则处于未承诺或噪声状态。
*   **关键技术流程**：
    1.  **训练阶段**：使用标准代理梯度法训练当前任务 $k$。
    2.  **重要性评估（无梯度）**：训练完成后，进行一次推理（Forward Pass），记录隐藏层神经元的脉冲时间序列。
    3.  **计算 ISI-CV**：计算相邻脉冲之间的时间间隔（ISI），求其标准差与均值的比值（CV）。
    4.  **分配权重 $\Omega$**：重要性分数 $\Omega$ 与 CV 成反比。低 CV 的神经元获得高重要性权重，其对应的权重在学习新任务 $k+1$ 时会被严格保护（通过 L2 正则化惩罚项）。
*   **硬件友好性**：该计算仅涉及脉冲计数、减法和基本的均值/方差运算，完全可以在类脑芯片上利用整数算术实现。

### 3. 实验设计
*   **数据集**：
    *   **Split-MNIST**：将手写数字分为5个任务（每个任务2个数字）。
    *   **Permuted-MNIST**：对像素进行随机排列，产生输入统计特性冲突的任务。
    *   **Split-FashionMNIST**：视觉复杂度更高的服装分类。
    *   **Split-N-MNIST**：**核心实验**，使用真实的动态视觉传感器（DVS）拍摄的神经形态事件数据。
*   **对比基准（Benchmarks）**：
    *   **No Reg**：无正则化的顺序微调（产生灾难性遗忘）。
    *   **EWC (Elastic Weight Consolidation)**：基于 Fisher 信息矩阵的梯度方法。
    *   **SI (Synaptic Intelligence)**：基于路径积分的梯度方法。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3060 (12 GB)** GPU 的服务器上完成。
*   **软件框架**：基于 SpikingJelly 框架，使用 FP16 混合精度训练。
*   **算力细节**：论文未明确给出总训练时长，但提到每个任务训练 10 个 Epoch，Batch Size 为 128。由于 ISI-CV 仅需一次推理即可完成重要性估计，其计算开销远低于需要反向传播的 EWC 和 SI。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单图像到复杂神经形态事件数据的四个基准测试。
*   **充分性**：每个实验均运行了 **3 个不同的随机种子**并报告了均值和标准差。
*   **客观性**：针对正则化强度参数 $\lambda$ 进行了详细的参数敏感性分析（Pareto 前沿分析），证明了 ISI-CV 在不同超参数下的鲁棒性。实验设计对比了经典的梯度方法，揭示了梯度方法在 SNN 上的失效模式，具有较强的说服力。

### 6. 主要结论与发现
*   **零遗忘达成**：在 Split-MNIST 和 Split-FashionMNIST 上，ISI-CV 实现了 **0.000 的遗忘率（AF）**。
*   **梯度方法的失效**：在处理真实的 N-MNIST 神经形态数据时，EWC 和 SI 的表现甚至不如“无正则化”，因为 SNN 的代理梯度在复杂时序数据中产生的 Fisher 估计不可靠；而 ISI-CV 凭借脉冲时序统计特性，获得了最高的准确率和最低的遗忘率。
*   **稳定性-塑性平衡**：ISI-CV 提供了一个可解释的超参数 $\lambda$，允许在保护旧知识（稳定性）和学习新知识（塑性）之间平滑切换。

### 7. 优点与亮点
*   **首创性**：提出了首个专门针对 SNN 持续学习的无梯度重要性度量指标。
*   **硬件原生兼容**：完全避开了类脑芯片难以实现的浮点梯度运算，仅需脉冲计数器。
*   **鲁棒性强**：在真实 DVS 传感器数据上表现优异，证明了其在处理时序噪声和复杂动力学方面的潜力。

### 8. 不足与局限
*   **任务增量限制**：目前实验主要基于“任务已知（Task-Incremental）”设置，即推理时知道当前属于哪个任务，尚未深入探讨任务标识未知的类增量（Class-Incremental）场景。
*   **数据集规模**：虽然涵盖了 DVS 数据，但实验仍集中在 MNIST 级别的中小型数据集上，尚未在 ImageNet 级别或更复杂的视觉场景下验证。
*   **架构限制**：主要针对全连接层和简单的卷积前端进行正则化，对于极深层架构的适用性有待进一步研究。

（完）
