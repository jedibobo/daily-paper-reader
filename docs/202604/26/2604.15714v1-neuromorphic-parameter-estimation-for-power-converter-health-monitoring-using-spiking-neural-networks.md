---
title: Neuromorphic Parameter Estimation for Power Converter Health Monitoring Using Spiking Neural Networks
title_zh: 基于脉冲神经网络的功率变换器健康监测神经形态参数估计
authors: "Hyeongmeen Baik, Hamed Poursiami, Maryam Parsa, Jinia Roy"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15714v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 在类脑硬件上使用脉冲神经网络实现亚毫瓦级边缘推理以降低能耗
tldr: 针对电源转换器健康监测对超低功耗边缘推理的需求，本文提出一种基于脉冲神经网络（SNN）的神经形态参数估计方法。该方法通过三层LIF网络结合可微分ODE求解器，实现了物理一致性训练与高效的时间处理。在同步降压转换器基准测试中，该方案显著降低了参数估计误差，且功耗预计比传统硬件低270倍，支持全天候退化跟踪与故障检测。
selection_source: carryover_cache
motivation: 传统的物理信息神经网络功耗过高，无法满足电源转换器全天候、亚毫瓦级的边缘健康监测需求。
method: 采用三层LIF脉冲神经网络进行参数估计，并利用可微分ODE求解器将物理约束与脉冲循环解耦，实现物理一致性训练。
result: "在电磁干扰环境下将电阻估计误差降至10.2%，实现93%的脉冲稀疏度，且能效预计提升约270倍。"
conclusion: 该架构证明了SNN在低功耗、高精度的工业设备健康监测及实时故障检测任务中的巨大潜力。
---

## 摘要
常开型变换器健康监测需要亚毫瓦级的边缘推理，这是基于 GPU 的物理信息神经网络难以达到的领域。本研究将脉冲时间处理与物理约束分离：采用三层漏电积分发放（LIF）脉冲神经网络（SNN）估计无源元件参数，同时利用可微常微分方程（ODE）求解器通过将 ODE 物理损失与展开的脉冲循环解耦，实现物理一致性训练。在受电磁干扰（EMI）影响的同步降压变换器基准测试中，与前馈基准模型相比，SNN 将集总电阻误差从 25.8% 降低至 10.2%（处于无源元件 ±10% 的制造公差范围内），且在神经形态硬件上的预计能耗降低了约 270 倍。持久的膜电位状态进一步实现了退化跟踪，并通过突发故障时 5.5 个百分点的脉冲率跳变实现事件驱动的故障检测。凭借 93% 的脉冲稀疏度，该架构适用于在 Intel Loihi 2 或 BrainChip Akida 上进行常开式部署。

## Abstract
Always-on converter health monitoring demands sub-mW edge inference, a regime inaccessible to GPU-based physics-informed neural networks. This work separates spiking temporal processing from physics enforcement: a three-layer leaky integrate-and-fire SNN estimates passive component parameters while a differentiable ODE solver provides physics-consistent training by decoupling the ODE physics loss from the unrolled spiking loop. On an EMI-corrupted synchronous buck converter benchmark, the SNN reduces lumped resistance error from $25.8\%$ to $10.2\%$ versus a feedforward baseline, within the $\pm 10\%$ manufacturing tolerance of passive components, at a projected ${\sim}270\times$ energy reduction on neuromorphic hardware. Persistent membrane states further enable degradation tracking and event-driven fault detection via a $+5.5$ percentage-point spike-rate jump at abrupt faults. With $93\%$ spike sparsity, the architecture is suited for always-on deployment on Intel Loihi 2 or BrainChip Akida.

---

## 论文详细总结（自动生成）

### 论文结构化总结：基于脉冲神经网络的功率变换器健康监测神经形态参数估计

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：功率变换器（如DC-DC降压变换器）的数字孪生和健康监测需要实时估计关键物理参数（电感 $L$、电容 $C$、电阻 $R_s$）。
*   **研究动机**：
    *   **功耗瓶颈**：传统的物理信息神经网络（PINN）依赖GPU或云端加速器，功耗高，无法满足边缘侧“全天候（Always-on）”且亚毫瓦（sub-mW）级的监测需求。
    *   **环境干扰**：变换器运行中存在严重的电磁干扰（EMI），传统前馈网络难以从噪声波形中准确提取微弱的退化信号（尤其是 $R_s$）。
    *   **技术挑战**：直接在脉冲神经网络（SNN）的展开循环中通过自动微分计算ODE残差会导致计算量巨大且数值不稳定。

#### 2. 论文提出的方法论
核心思想是将**脉冲时间处理**与**物理约束执行**分离，构建 **SNN+ODE** 混合架构：
*   **SNN 估计器**：采用三层漏电积分发放（LIF）神经元网络。它逐时间步处理输入的电流 $i_L$ 和电压 $V_o$ 噪声波形。LIF神经元的膜电位具有时间积分特性，能从瞬态动力学中提取参数特征。
*   **可微 ODE 求解器**：SNN 的输出作为物理参数（$L, C, R_s$）输入到 ODE 求解器中。该求解器利用四阶龙格-库塔法（RK4）重建预测波形。
*   **解耦训练机制**：物理损失（重建波形与测量波形的均方误差）通过 ODE 求解器反向传播到 SNN 权重。这种设计避免了直接对 SNN 的脉冲展开循环进行复杂的物理求导，同时保证了估计结果符合物理规律。
*   **持久膜电位**：在连续监测周期中保留神经元膜状态，利用脉冲率的变化作为事件驱动的故障报警信号。

#### 3. 实验设计
*   **实验场景**：同步降压变换器（Synchronous Buck Converter）基准测试，模拟受结构化 EMI 噪声（高斯背景噪声 + 开关边缘尖峰）干扰的波形。
*   **对比基准（Benchmark）**：前馈神经网络（FF）+ ODE 架构。
*   **评估指标**：参数估计误差（$L, C, R_s$ 的百分比误差）、重建波形准确度、脉冲稀疏度、推理能耗。
*   **监测任务**：
    1.  静态参数识别。
    2.  多参数同时退化跟踪（50个监测快照）。
    3.  突发故障检测（$R_s$ 突变）。

#### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，余弦退火学习率，训练 3000 到 6000 个 Epoch。使用 `snnTorch` 和 `torchdiffeq` 库。
*   **算力说明**：文中**未明确提及**训练所用的具体 GPU 型号或训练时长。
*   **推理能效预测**：基于已发表的硬件数据进行估算。SNN 在神经形态芯片（如 Intel Loihi 2 或 BrainChip Akida）上的推理能耗约为 **3.3 μJ**，而 ARM Cortex-M 微控制器上的前馈网络推理能耗约为 **881 μJ**。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对单一工作点进行了 3000 Epoch 的收敛实验。
    *   针对 200 个随机参数组合生成的波形进行了多条件训练。
    *   进行了 50 个周期的退化模拟实验。
*   **充分性评价**：实验设计较为全面，涵盖了从静态识别到动态跟踪及故障预警的完整流程。对比实验（SNN vs. FF）在相同输入和损失函数下进行，具有客观性。但目前实验主要基于**仿真数据**，缺乏实际硬件电路采集的真实数据验证。

#### 6. 主要结论与发现
*   **精度提升**：在 EMI 噪声环境下，SNN 对集总电阻 $R_s$ 的估计误差仅为 **10.2%**，远优于前馈网络的 **25.8%**。
*   **极高能效**：SNN 实现了 **93%** 的脉冲稀疏度，预计推理能耗比传统边缘处理器降低约 **270 倍**。
*   **故障预警**：利用持久膜电位，当发生突发故障时，脉冲发放率会瞬间跳变 **5.5 个百分点**，提供了一种无需额外阈值逻辑的天然报警机制。
*   **物理一致性**：通过解耦架构，SNN 能够学习到符合电路物理特性的参数，而非仅仅进行曲线拟合。

#### 7. 优点
*   **架构创新**：成功解决了 SNN 与物理信息学习结合时的梯度不稳定问题。
*   **时间特征提取**：利用 LIF 神经元的记忆效应，比前馈网络更擅长处理含有时间相关噪声（如 EMI）的电力电子波形。
*   **边缘友好**：极低的功耗使其能够集成在变换器的辅助电源中，实现真正的全天候就地监测。

#### 8. 不足与局限
*   **参数敏感性**：电感 $L$ 的估计误差（约 16%）仍相对较高，受初始瞬态噪声影响较大。
*   **训练复杂性**：SNN 训练存在代理梯度噪声，需要精细的检查点选择（Best-checkpoint selection）。
*   **实测缺失**：论文中的能耗和性能提升主要基于仿真和硬件参数推算，尚未在真实的神经形态硬件（如 Loihi 2 实物）上完成闭环实测。
*   **应用限制**：目前仅针对降压变换器的平均模型进行了验证，对于更复杂的拓扑或非线性更强的系统，其泛化能力有待验证。

（完）
