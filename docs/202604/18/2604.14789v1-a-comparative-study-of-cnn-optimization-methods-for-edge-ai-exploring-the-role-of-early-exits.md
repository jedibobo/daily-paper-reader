---
title: "A Comparative Study of CNN Optimization Methods for Edge AI: Exploring the Role of Early Exits"
title_zh: 边缘 AI 卷积神经网络优化方法的比较研究：探索早期退出的作用
authors: "Nekane Fernandez, Ivan Valdes, Steven Van Vaerenbergh, Idoia de la Iglesia, Julen Arratibel"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14789v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在真实边缘设备上平衡准确率、延迟和资源约束
tldr: 本研究针对边缘设备部署深度神经网络的挑战，对比分析了静态压缩（剪枝、量化）与动态早退（Early-Exit）机制的性能。通过在真实硬件上使用ONNX推理流水线进行评估，发现静态方法能稳定减少内存占用，而动态方法可实现输入自适应的计算节省。研究证明，结合两者能显著降低推理延迟和内存消耗，且精度损失极小，为边缘AI优化提供了新路径。
selection_source: fresh_fetch
motivation: 旨在解决边缘设备上深度学习模型在精度、延迟和资源约束之间的平衡问题，填补静态与动态优化方法在真实硬件上对比研究的空白。
method: 在真实边缘设备上，利用基于ONNX的推理流水线，对静态压缩技术与动态早退机制进行了统一的部署导向型对比评估。
result: 静态方法在减少内存占用方面表现稳定，而早退机制提供了输入自适应的计算节省，两者结合可同时显著优化延迟与内存指标。
conclusion: 结合静态压缩与动态早退机制是优化边缘AI部署的高效策略，能够突破单一优化手段的局限，实现更优的资源利用与性能平衡。
---

## 摘要
在边缘设备上部署深度神经网络，需要在实际执行条件下平衡准确率、延迟和资源约束。为了使模型符合这些约束，出现了两大类策略：静态压缩技术（如剪枝和量化），通过永久性减小模型尺寸来发挥作用；以及动态方法（如早期退出机制），在运行时自适应调整计算成本。虽然这两类方法在各自领域都得到了广泛研究，但很少在物理硬件的相同条件下进行对比。本文提出了一种面向部署的静态压缩与动态早期退出机制的统一比较，并在真实边缘设备上使用基于 ONNX 的推理流水线进行了评估。研究结果表明，静态和动态技术在边缘部署中提供了根本不同的权衡。虽然剪枝和量化能带来稳定的内存占用减少，但早期退出机制实现了静态方法无法比拟的输入自适应计算节省。两者的结合被证明非常有效，能在准确率损失极小的情况下同时降低推理延迟和内存占用，扩展了边缘端处理能力的边界。

## Abstract
Deploying deep neural networks on edge devices requires balancing accuracy, latency, and resource constraints under realistic execution conditions. To fit models within these constraints, two broad strategies have emerged: static compression techniques such as pruning and quantization, which permanently reduce model size, and dynamic approaches such as early-exit mechanisms, which adapt computational cost at runtime. While both families are widely studied in isolation, they are rarely compared under identical conditions on physical hardware. This paper presents a unified deployment-oriented comparison of static compression and dynamic early-exit mechanisms, evaluated on real edge devices using ONNX based inference pipelines. Our results show that static and dynamic techniques offer fundamentally different trade-offs for edge deployment. While pruning and quantization deliver consistent memory footprint reduction, early-exit mechanisms enable input-adaptive computation savings that static methods cannot match. Their combination proves highly effective, simultaneously reducing inference latency and memory usage with minimal accuracy loss, expanding what is achievable at the edge.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Comparative Study of CNN Optimization Methods for Edge AI: Exploring the Role of Early Exits》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的边缘设备（AIoT）上平衡深度神经网络（CNN）的准确率、推理延迟和资源消耗。
*   **研究动机**：
    *   目前存在两类主流优化策略：**静态压缩**（如剪枝、量化）和**动态网络**（如早期退出 Early-Exit）。
    *   现有研究通常孤立地评估这些技术，缺乏在相同硬件条件、相同推理引擎（如 ONNX）下的统一对比。
    *   开发者缺乏明确的指导，不知道在特定边缘场景下应选择哪种优化组合。

### 2. 方法论
论文对比并整合了三种关键技术，所有模型均转换为 **ONNX** 格式进行部署：
*   **结构化剪枝 (Structured Pruning)**：
    *   基于 L1 范数衡量重要性，永久移除不重要的滤波器或通道。
    *   引入“通道对齐（Channel Rounding）”，确保通道数为 16 或 32 的倍数，以适配 GPU 的内存对齐优化。
*   **量化 (Quantization)**：
    *   对比了**动态量化 (DQ)** 和 **静态训练后量化 (PTQ)**（均针对 INT8）。
    *   针对敏感模型（如 EfficientNet-B2）采用“层选择性量化”，仅量化卷积层以保护精度。
*   **早期退出机制 (Early Exits)**：
    *   **核心思想**：在网络中间层插入轻量级分类分支。推理时，若中间分支的预测置信度（基于 Softmax 熵计算）超过阈值 $T$，则提前终止计算。
    *   **训练策略**：采用两阶段训练，冻结主干网络（Backbone），仅训练新增的退出分支，确保对比的公平性。
    *   **部署实现**：由于 ONNX 对动态控制流支持有限，论文采用“模型切分”策略，将模型拆分为多个子图顺序执行。

### 3. 实验设计
*   **数据集**：ImageNet100。
*   **模型架构 (Benchmark)**：
    *   大型模型：ResNet-152。
    *   平衡型模型：EfficientNet-B2。
    *   轻量化模型：MobileNet-V2、ShuffleNet-V2。
*   **对比方法**：BASE（基准）、PRUNE（剪枝）、PTQ/DQ（量化）、EE（早期退出）、PTQ-EE/DQ-EE（组合优化）。
*   **评估指标**：准确率、标签忠实度（Label Loyalty）、总/平均延迟、CPU/GPU 利用率、RAM 占用、磁盘模型大小。

### 4. 资源与算力
*   **硬件平台**：
    *   **NVIDIA Jetson AGX Orin** (64GB RAM, 高性能边缘端)。
    *   **NVIDIA Jetson Orin Nano** (8GB RAM, 中端边缘端)。
    *   **Raspberry Pi 5** (8GB RAM, 仅 CPU 推理)。
*   **算力细节**：
    *   推理引擎使用 **ONNX Runtime**。
    *   GPU 加速使用 CUDA 12.2。
    *   论文**未明确说明**训练这些模型所花费的总机时（如 GPU 小时数），但详细列出了各边缘设备的硬件规格。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同规模的架构、3 种异构硬件平台、多种剪枝率和置信度阈值。
*   **充分性**：实验设计非常全面。不仅关注准确率，还深入分析了 RAM 占用和 CPU/GPU 负载的动态变化。
*   **客观性**：通过在真实物理硬件（而非模拟器）上运行，并使用统一的 ONNX 框架，确保了结果的工程参考价值。

### 6. 主要结论与发现
*   **量化是静态压缩的首选**：PTQ/DQ 能提供 3.5x-4x 的模型压缩比，且在 ResNet 和 MobileNet 上精度损失极小。
*   **早期退出的动态优势**：在大型模型（ResNet-152）上，EE 实现了最高的推理加速（达 2.24x），且能根据输入样本的难度自适应调整计算量。
*   **架构敏感性**：
    *   剪枝对大型冗余模型有效，但对已高度优化的轻量级模型（如 MobileNet）效果较差。
    *   EfficientNet 对量化极其敏感，DQ 会导致精度崩溃，必须使用选择性 PTQ。
*   **协同效应 (Transformative Synergy)**：**PTQ + Early Exit** 的组合表现最强。它既能享受量化带来的内存缩减，又能通过早退降低延迟，甚至在某些配置下能修复量化带来的精度损失。

### 7. 优点
*   **部署导向**：不只是理论研究，而是解决了 ONNX 部署早退机制的实际工程问题（模型切分）。
*   **跨范式对比**：打破了“只对比同类技术”的局限，直接对比了静态与动态两类本质不同的优化路径。
*   **多维度度量**：引入了“忠实度（Loyalty）”指标，评估优化后模型决策与原模型的一致性。

### 8. 不足与局限
*   **模型覆盖**：实验仅限于 CNN，未涉及当前流行的 Vision Transformers (ViT) 或轻量级 LLM。
*   **数据集局限**：使用的是 ImageNet100 而非完整的 ImageNet-1K，实验结论在极大规模分类任务上的泛化性有待验证。
*   **训练成本**：虽然早退机制在推理时省电，但训练多个分支会增加训练阶段的算力开销，文中对此提及较少。
*   **量化方法**：未探讨量化感知训练 (QAT)，仅限于训练后量化 (PTQ/DQ)。

（完）
