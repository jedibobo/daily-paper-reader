---
title: "Prism: Symbolic Superoptimization of Tensor Programs"
title_zh: Prism：张量程序的符号化超级优化
authors: "Mengdi Wu, Xiaoyu Jiang, Oded Padon, Zhihao Jia"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15272v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 考虑硬件约束和算子语义的符号超优化
tldr: Prism是首个针对张量程序的符号超级优化器，旨在解决现代机器学习工作负载优化中的扩展性难题。它引入了sGraph符号化分层表示，通过两级搜索策略（构建符号图族与具体参数实例化）来紧凑编码并探索庞大的程序空间。Prism利用符号推理、代数恒等式和硬件约束进行高效剪枝，并结合e-graph重写进行等价性验证。实验表明，Prism在LLM任务上显著提升了执行性能并大幅缩短了优化耗时。
selection_source: fresh_fetch
motivation: 传统超级优化器在处理复杂的现代张量程序时，面临搜索空间巨大且难以兼顾严谨性与扩展性的挑战。
method: 采用sGraph符号表示法和两级搜索架构，结合符号推理剪枝、e-graph重写验证及自动调优实例化技术。
result: 在LLM工作负载中，Prism比现有超级优化器提速达2.2倍，比编译器提速4.9倍，同时优化时间减少了3.4倍。
conclusion: Prism通过符号化方法弥合了穷举搜索的严谨性与大规模模型优化需求之间的鸿沟，是高效张量程序优化的新范式。
---

## 摘要
本文介绍了 Prism，这是首个针对张量程序的符号化超级优化器。其核心思想是 sGraph，这是一种符号化的层级表示，通过对部分执行参数进行符号化表示，紧凑地编码了大规模的张量程序类别。Prism 将优化组织为两级搜索：首先构建代表程序族的符号图，然后将其实例化为具体的实现。这种形式化方法利用算子语义、代数恒等式和硬件约束进行符号推理，从而能够对搜索空间中可证明的次优区域进行结构化剪枝。我们开发了高效符号图生成、基于 e-graph 重写的等价性验证以及通过自动调优进行参数实例化的技术。这些组件共同使 Prism 能够将穷举搜索的严谨性与现代机器学习工作负载所需的扩展性结合起来。在五个常用的 LLM 工作负载上的评估表明，Prism 相比于最优的超级优化器实现了高达 2.2 倍的加速，相比于最优的基于编译器的方案实现了高达 4.9 倍的加速，同时将端到端优化时间缩短了高达 3.4 倍。

## Abstract
This paper presents Prism, the first symbolic superoptimizer for tensor programs. The key idea is sGraph, a symbolic, hierarchical representation that compactly encodes large classes of tensor programs by symbolically representing some execution parameters. Prism organizes optimization as a two-level search: it constructs symbolic graphs that represent families of programs, and then instantiates them into concrete implementations. This formulation enables structured pruning of provably suboptimal regions of the search space using symbolic reasoning over operator semantics, algebraic identities, and hardware constraints.   We develop techniques for efficient symbolic graph generation, equivalence verification via e-graph rewriting, and parameter instantiation through auto-tuning. Together, these components allow Prism to bridge the rigor of exhaustive search with the scalability required for modern ML workloads. Evaluation on five commonly used LLM workloads shows that Prism achieves up to $2.2\times$ speedup over best superoptimizers and $4.9\times$ over best compiler-based approaches, while reducing end-to-end optimization time by up to $3.4\times$.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Prism**，它是首个针对张量程序的符号化超级优化器（Symbolic Superoptimizer）。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：现代机器学习（ML）模型在 GPU 上的高效执行至关重要。目前的优化方法主要分为两类：一是基于专家设计的启发式规则（如 TVM、TensorRT），但难以覆盖复杂的组合优化空间；二是超级优化（Superoptimization），通过穷举（如 TASO、Mirage）或随机采样（如 AlphaEvolve）搜索最优算子融合和调度方案。
*   **核心问题**：现有的超级优化器面临**扩展性瓶颈**。穷举搜索在处理大规模或深层嵌套程序时会产生组合爆炸；而基于采样的搜索（如 LLM 引导）缺乏结构化保证，且搜索效率不稳定。
*   **论文目标**：通过引入符号化推理，在保持搜索严谨性的同时，解决现代 ML 工作负载优化的扩展性难题。

### 2. 方法论：核心思想与关键技术
Prism 的核心是将优化过程组织为**两级搜索**，并引入了 **sGraph** 符号表示法。
*   **sGraph（符号图）**：不同于传统方法固定执行参数（如网格维度、线程块大小、张量到线程的映射），sGraph 将这些参数表示为符号变量。一个 sGraph 可以代表一族具有相同计算结构但并行策略不同的程序。
*   **符号化并行参数与映射**：
    *   使用符号整数变量表示并行维度（Grid/Block Dim）。
    *   使用布尔变量编码张量映射（imap, fmap, omap），定义数据如何切分或复制。
*   **关键流程**：
    1.  **sGraph 生成**：通过“符号维度匹配”和“表达式引导剪枝”技术，在不确定具体参数的情况下，过滤掉物理上不可行或明显次优的图结构。
    2.  **映射实例化**：为符号变量分配具体的映射方案，并利用 **e-graph（等价图）重写**技术进行功能等价性验证。
    3.  **等价性验证**：定义了约 70 条代数公理（如矩阵乘法结合律、并行算子交换律），在符号层面证明候选程序与原始程序等价。
    4.  **参数实例化（自动调优）**：对验证通过的符号图，通过随机采样和 GPU 实际性能分析（Profiling）来确定最优的硬件执行参数。

### 3. 实验设计
*   **场景与工作负载**：选择了 5 种现代大语言模型（LLM）中常见的计算模式：
    *   **RMSNorm**（归一化与线性层融合）
    *   **RMSNorm-MLP**（带门控的 MLP 融合）
    *   **SwiGLU**（门控激活函数）
    *   **Attention**（组查询注意力 GQA 解码）
    *   **QK-Attention**（带查询-键归一化的注意力机制）
*   **基准测试（Benchmark）**：
    *   **PyTorch Eager**：标准执行模式。
    *   **PyTorch Compiled**：使用 `torch.compile` (Triton 后端)。
    *   **TVM (Ansor)**：高性能自动调度编译器。
    *   **Mirage**：目前最先进的非符号化超级优化器。

### 4. 资源与算力
*   **搜索算力**：使用了 2 台 Intel Xeon Platinum 8275CL CPU（共 48 核心、96 线程）进行符号搜索和验证。
*   **执行与分析算力**：使用 **NVIDIA A100 GPU** 进行内核编译、性能分析和最终性能测试。
*   **软件实现**：核心搜索逻辑由 C++ 编写，等价性验证使用 Rust 的 `egg` 库实现。

### 5. 实验数量与充分性
*   **实验规模**：针对 5 种工作负载，每种设置了 2 种不同的张量规模（共 10 组核心对比实验）。
*   **消融实验**：专门针对符号映射（imap/fmap/omap）对搜索时间的影响进行了消融分析。
*   **充分性评价**：实验设计较为充分。不仅对比了内核执行速度，还对比了端到端的优化耗时（Search Time），并分析了发现的唯一图结构数量（Graph Diversity）。对比对象涵盖了从主流编译器到最前沿超级优化器的完整频谱，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：Prism 发现的内核比目前最好的超级优化器（Mirage）快达 **2.2 倍**，比传统编译器（TVM/PyTorch）快达 **4.9 倍**。
*   **搜索效率**：在复杂任务（如 RMSNorm-MLP）中，Prism 将优化时间缩短了 **3.4 倍**。在 Mirage 因空间过大而超时的情况下，Prism 依然能高效完成搜索。
*   **发现能力**：由于符号化表示覆盖了更广的并行策略空间，Prism 能够发现人类专家和启发式工具难以触及的复杂并行方案（如在 Attention 中发现更多独特的并行切分方式）。

### 7. 优点
*   **首创性**：首次将符号执行与超级优化结合应用于张量程序，解决了搜索空间爆炸问题。
*   **严谨性**：通过 e-graph 和代数公理进行验证，保证了优化过程的正确性，而非仅仅依赖随机测试。
*   **解耦设计**：将“计算结构搜索”与“具体硬件参数调优”解耦，使得一次搜索结果可以复用于不同的硬件配置。

### 8. 不足与局限
*   **公理完备性**：虽然定义的公理足以覆盖大多数优化，但论文承认公理集并非理论上的“完备集”，可能遗漏某些极端的等价变换。
*   **调优策略**：在最后的参数实例化阶段，目前仅采用了简单的随机采样，若结合更先进的贝叶斯优化或强化学习，性能可能进一步提升。
*   **初始开销**：对于非常简单的算子，符号搜索和编译所有候选模板的固定开销可能高于简单的启发式方法。

（完）
