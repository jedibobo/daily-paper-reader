---
title: Robust Reasoning and Learning with Brain-Inspired Representations under Hardware-Induced Nonlinearities
title_zh: 硬件诱导非线性下基于类脑表示的鲁棒推理与学习
authors: "William Youngwoo Chung, Hamza Errahmouni Barkam, Tamoghno Das, Mohsen Imani"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12079v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对存算一体架构的硬件感知优化
tldr: 针对存内计算（CIM）硬件中存在的非线性失真和可靠性问题，本文提出了一种基于超维计算（HDC）的硬件感知优化框架。该框架通过将编码过程建模为优化问题，最小化理想核与硬件约束核之间的差异，并采用端到端校准策略。实验表明，该方法显著提升了QuantHD和RelHD在严苛硬件环境下的分类与推理准确率，为构建高效、鲁棒的类脑智能系统提供了新方案。
selection_source: fresh_fetch
motivation: 存内计算架构虽能提升能效，但半导体器件的非线性失真和不稳定性严重影响了传统机器学习算法的可靠性。
method: 提出一种硬件感知的HDC优化框架，通过最小化Frobenius范数来补偿非理想相似度计算，并结合联合优化策略进行端到端超向量校准。
result: "在严重硬件扰动下，QuantHD准确率提升至84%（比原始版本高48%），RelHD在Cora数据集上的准确率提升了5.4倍。"
conclusion: 该框架在保留HDC鲁棒性与符号推理特性的同时，有效解决了CIM硬件的非线性挑战，支持可扩展且高能效的智能系统。
---

## 摘要
传统机器学习依赖于高精度算术和近乎理想的硬件假设，而这正日益受到激进缩减尺寸的半导体器件波动性的挑战。存内计算（CIM）架构缓解了数据传输瓶颈并提高了能效，但同时也引入了非线性失真和可靠性问题。我们通过一种基于超维计算（HDC）的硬件感知优化框架来解决这些问题，系统地补偿了 CIM 中非理想的相似度计算。我们的方法将编码建模为一个优化问题，通过最小化理想核与其受硬件约束的对应项之间的 Frobenius 范数，并采用联合优化策略对超向量表示进行端到端校准。实验结果表明，当我们的方法应用于 QuantHD 时，在严重的硬件诱导扰动下实现了 84% 的准确率，相比相同条件下的原始 QuantHD 提升了 48%。此外，我们的优化对于依赖精确变量绑定以实现可解释推理的基于图的 HDC 至关重要。我们的框架在 Cora 数据集上保持了 RelHD 的准确率，在非线性环境下相比原始 RelHD 实现了 5.4 倍的准确率提升。通过保留 HDC 的鲁棒性和符号特性，我们的解决方案使可扩展、高能效的智能系统能够在新兴的 CIM 硬件上进行分类和推理。

## Abstract
Traditional machine learning depends on high-precision arithmetic and near-ideal hardware assumptions, which is increasingly challenged by variability in aggressively scaled semiconductor devices. Compute-in-memory (CIM) architectures alleviate data-movement bottlenecks and improve energy efficiency yet introduce nonlinear distortions and reliability concerns. We address these issues with a hardware-aware optimization framework based on Hyperdimensional Computing (HDC), systematically compensating for non-ideal similarity computations in CIM. Our approach formulates encoding as an optimization problem, minimizing the Frobenius norm between an ideal kernel and its hardware-constrained counterpart, and employs a joint optimization strategy for end-to-end calibration of hypervector representations. Experimental results demonstrate that our method when applied to QuantHD achieves 84\% accuracy under severe hardware-induced perturbations, a 48\% increase over naive QuantHD under the same conditions. Additionally, our optimization is vital for graph-based HDC reliant on precise variable-binding for interpretable reasoning. Our framework preserves the accuracy of RelHD on the Cora dataset, achieving a 5.4$\times$ accuracy improvement over naive RelHD under nonlinear environments. By preserving HDC's robustness and symbolic properties, our solution enables scalable, energy-efficient intelligent systems capable of classification and reasoning on emerging CIM hardware.

---

## 论文详细总结（自动生成）

这篇论文针对存内计算（CIM）硬件环境下的非线性失真问题，提出了一种硬件感知的超维计算（HDC）优化框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：传统机器学习依赖高精度算术和理想硬件假设。随着半导体工艺微缩，硬件的波动性、噪声和非线性失真日益严重。
*   **核心挑战**：存内计算（CIM）架构（如基于 FeFET 的架构）虽然能显著降低数据迁移能耗，但其模拟计算特性引入了严重的非线性失真。这种失真会破坏超维计算（HDC）中关键的“绑定（Binding）”和“相似度搜索”操作，导致模型在处理复杂推理和图学习任务时精度大幅下降。
*   **研究目的**：开发一种硬件感知的优化框架，使 HDC 模型能够在存在硬件非线性的情况下，依然保持鲁棒的分类能力和可解释的符号推理能力。

### 2. 方法论
*   **核心思想**：将超维编码过程建模为一个优化问题，通过“联合优化策略”同时校准编码模块和关联搜索模块，补偿硬件诱导的相似度偏差。
*   **关键技术细节**：
    *   **硬件失真建模**：定义硬件失真函数 $f$（模拟存储时的量化、饱和等）和硬件相似度函数 $\Psi$（模拟非理想的点积或汉明距离计算）。
    *   **核优化（Kernel Optimization）**：通过最小化理想核（如 RBF 核）与硬件约束核之间的 Frobenius 范数，调整超向量表示。
    *   **联合损失函数**：设计了包含任务损失（$L_{task}$）、相似度损失（$L_{sim}$）和正则化项（$R(\theta)$）的综合目标函数：
        $$\min_{\theta} L_{task}(\theta) + \alpha L_{sim}(\theta) + \beta R(\theta)$$
    *   **可学习编码器**：使用小型神经网络作为参数化编码器 $\phi_\theta$，而非传统的固定随机映射，以适应特定的硬件噪声分布。

### 3. 实验设计
*   **实验场景与任务**：
    1.  **核近似（Kernel Approximation）**：评估框架在非线性环境下模拟目标核形状的能力。
    2.  **HDC 分类**：在 **ISOLET**（语音识别）和 **FMNIST**（图像分类）数据集上测试。
    3.  **图重建与推理**：在 **Cora** 数据集（引文网络）上测试节点分类和图结构重建。
*   **对比基准（Benchmarks）**：
    *   **Naive QuantHD**：未优化的量化 HDC 模型。
    *   **Naive RelHD/GrapHD**：未优化的图 HDC 模型。
    *   **理想环境模型**：在无噪声 GPU 环境下运行的模型作为上限参考。
*   **硬件模拟**：使用指数、对数和双曲正切（Tanh）函数模拟 CIM 硬件的非线性。

### 4. 资源与算力
*   **硬件环境**：论文提到使用了 Python 和 PyTorch 库实现。
*   **算力说明**：文中对比了 FeFET 加速器相对于 NVIDIA GPU（如 1080Ti, 3090Ti）的能效提升（最高达 59287x），但**未明确给出**其优化框架本身在训练阶段的具体算力消耗、GPU 数量或训练时长。由于 HDC 编码器通常较小，推测其训练开销远低于深度神经网络。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了合成数据集和三个标准真实数据集（ISOLET, FMNIST, Cora）。
*   **充分性评价**：
    *   每组实验重复 10 次并取平均值，具有统计说服力。
    *   涵盖了从基础核近似到复杂图推理的多个维度。
    *   **客观性**：通过展示相似度分布图（Figure 6）深入解释了优化前后的失效机理，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **分类性能提升**：在严重硬件扰动下，优化后的 QuantHD 在 ISOLET 上达到 84% 的准确率，比原始版本提升了 **48%**。
*   **图推理突破**：在非线性环境下，原始 RelHD 性能几乎崩溃（65% 质量损失），而优化后的框架将损失降至 **6%**，实现了 **5.4 倍** 的准确率提升。
*   **鲁棒性机理**：优化框架成功修复了“关系绑定”失效问题，使节点超向量在噪声环境下仍能保持良好的正交性/区分度。

### 7. 优点与亮点
*   **软硬协同**：不要求硬件达到理想线性度，而是通过算法端的“预失真”补偿来适应不完美的硬件，降低了对新兴存储器件（如 FeFET）的工艺要求。
*   **保留符号特性**：在解决噪声问题的同时，保留了 HDC 的可解释性和符号推理能力，这对 AGI 研究具有重要意义。
*   **通用性**：该框架可无缝集成到现有的各种 HDC 变体（如 QuantHD, RelHD）中。

### 8. 不足与局限
*   **硬件模型简化**：实验中使用的非线性函数（Tanh, Exp 等）是数学抽象，虽然具有代表性，但与真实物理器件（如 RRAM 或 FeFET 的循环波动）的复杂噪声特性可能仍有偏差。
*   **训练开销**：引入可学习的编码器和联合优化过程会增加离线训练阶段的复杂度和时间，论文未对此额外开销进行量化分析。
*   **大规模图扩展性**：实验主要集中在 Cora 等中小型图数据集上，对于超大规模图结构的适应性尚待验证。

（完）
