---
title: "OptINC: Optical In-Network-Computing for Scalable Distributed Learning"
title_zh: OptINC：面向可扩展分布式学习的光学网络内计算
authors: "Sijie Fei, Grace Li Zhang, Bing Li, Ulf Schlichtmann"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28290v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 用于分布式学习的光学网络内计算
tldr: 针对大规模分布式学习中通信开销巨大的问题，本文提出OptINC架构，利用光互连中的马赫-曾德尔干涉仪（MZI）将梯度聚合和量化等计算任务卸载到光网络中。通过构建光神经网络（ONN）并结合硬件感知训练算法，该方案在保持模型精度的同时，有效消除了传统Ring All-reduce算法带来的通信瓶颈，显著提升了分布式训练的扩展性。
selection_source: carryover_cache
motivation: 传统分布式学习算法在服务器间产生沉重的通信负担，限制了大规模模型训练的效率。
method: 提出一种基于MZI的光在网计算架构，通过光域预处理和硬件感知训练算法，在光互连中实现梯度聚合与量化。
result: 在ResNet50和LLaMA等任务上的实验表明，该方案在达到与基准相当的训练精度的同时，消除了通信开销。
conclusion: OptINC通过将计算集成到光通信层，为解决大规模分布式学习的通信瓶颈提供了一种高效且可扩展的硬件方案。
---

## 摘要
分布式学习广泛应用于在大型数据集上训练大模型，通过将模型或数据集的部分分布到多个设备上，并聚合计算结果以进行后续计算或参数更新。现有的分布式学习通信算法（如 ring all-reduce）会导致服务器之间产生沉重的通信开销。由于大规模系统中的通信采用光纤，我们提出了一种光学网络内计算（OptINC）架构，将服务器中的计算任务卸载到光学互连网络中。为了在光域执行梯度平均和量化，我们将马赫-曾德尔干涉仪（MZI）等光学器件集成到互连网络中。这种实质上的光学神经网络（ONN）可以有效减少现有分布式训练方案中的通信开销。为了降低训练该神经网络的数据集复杂度，本文还提出了一种在光域实现的预处理算法。通过使用酉矩阵和对角矩阵近似光学神经网络的权重矩阵，降低了硬件成本，同时利用所提出的硬件感知训练算法保持了准确性。该方案在真实的分布式学习任务上进行了评估，包括 CIFAR-100 上的 ResNet50 和 Wikipedia-1B 上的基于 LLaMA 的网络。实验结果表明，在上述两种情况下，所提框架均能达到与 ring all-reduce 基准相当的训练准确率，同时消除了通信开销。

## Abstract
Distributed learning is widely used for training large models on large datasets by distributing parts of the model or dataset across multiple devices and aggregating the computed results for subsequent computations or parameter updates. Existing communication algorithms for distributed learning such as ring all-reduce result in heavy communication overhead between servers. Since communication in large-scale systems uses optical fibers, we propose an Optical In-Network-Computing (OptINC) architecture to offload the computation in servers onto the optical interconnects. To execute gradient averaging and quantization in the optical domain, we incorporate optical devices such as Mach-Zehnder-Interferometers (MZIs) into the interconnects. Such a de facto optical neural network (ONN) can effectively reduce the communication overhead in existing distributed training solutions. To reduce dataset complexity for training this neural network, a preprocessing algorithm implemented in the optical domain is also proposed. Hardware cost is lowered by approximating the weight matrices of the optical neural network with unitary and diagonal matrices, while the accuracy is maintained by a proposed hardware-aware training algorithm. The proposed solution was evaluated on real distributed learning tasks, including ResNet50 on CIFAR-100, and a LLaMA-based network on Wikipedia-1B. In both cases, the proposed framework can achieve comparable training accuracy to the ring all-reduce baseline, while eliminating communication overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《OptINC: Optical In-Network-Computing for Scalable Distributed Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着深度学习模型（如LLM）规模的剧增，单机训练已无法满足需求，分布式学习成为主流。然而，传统的通信算法（如 **Ring All-reduce**）在服务器间产生巨大的通信开销（在 $N$ 个服务器中，相对开销接近 100%），成为训练瓶颈。现有的电域网络内计算（INC）虽能缓解延迟，但存在光电转换（O-E-O）能耗高、数据缓冲导致丢包等问题。
**本文核心目标**：提出一种名为 **OptINC** 的光学网络内计算架构，利用光互连硬件直接在光域完成梯度的聚合（平均）与量化，从而彻底消除额外的通信轮次，实现高带宽、低延迟的分布式训练。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将光互连网络视为一个“事实上的光学神经网络（ONN）”。通过马赫-曾德尔干涉仪（MZI）阵列实现线性权重运算，结合非线性光学激活函数，在信号传输过程中直接输出计算结果。
*   **关键技术细节**：
    *   **信号编码**：将 $B$ 位梯度切分为 2 位段，映射为 **PAM4（4电平脉冲幅度调制）** 光信号进行传输。
    *   **架构组成**：包含**预处理单元（P）**（降低数据复杂度，将输入规模从指数级降至线性）、**ONN计算单元（$f_\theta$）**（执行平均与量化）和**拆分单元（T）**（广播结果）。
    *   **硬件优化（矩阵近似）**：为了减少 MZI 数量，将权重矩阵分解并近似为“一个对角矩阵 + 一个酉矩阵”，相比标准 SVD 分解可节省约 50% 的硬件面积。
    *   **硬件感知训练**：采用两阶段损失函数。第一阶段针对原始输出做加权均方误差（MSE）训练；第二阶段针对重构后的梯度进行微调，以补偿硬件近似带来的精度损失。
    *   **可扩展性**：提出级联拓扑结构，通过多级 OptINC 单元支持更多服务器（如 $N^2$ 台），并设计了专门的算法解决多级量化带来的累积误差。

### 3. 实验设计
*   **数据集与场景**：
    *   **CV任务**：在 CIFAR-100 数据集上训练 **ResNet50**。
    *   **NLP任务**：在 Wikipedia-1B 数据集上训练基于 **LLaMA** 的网络（8层，384隐藏维度）。
*   **Benchmark（基准）**：传统的 **Ring All-reduce** 算法（在服务器端进行精确梯度聚合）。
*   **对比维度**：训练准确率（Accuracy）、损失值（Loss）、通信数据量、硬件面积开销以及系统延迟。

### 4. 资源与算力
*   **训练算力**：使用 **NVIDIA A100 Tensor Core GPU** 在 PyTorch 环境下训练 OptINC 所需的光学神经网络模型。
*   **仿真参数**：延迟评估基于 **NVIDIA H100 GPU**（60 TFLOPs 算力，0.6 利用率）和 800 Gb/s 带宽的光收发器规格进行建模。
*   **训练时长**：文中未明确给出 ONN 本身的训练时长，但提到 ResNet50 训练了 300 epochs，LLaMA 训练了 50,000 steps。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8位/16位梯度宽度、4/8/16 个服务器等多种组合场景。
*   **消融实验**：针对矩阵近似层数的选择做了详细对比，分析了面积节省与精度误差之间的权衡（Table II）。
*   **充分性评价**：实验设计较为全面，不仅验证了 ONN 本身的计算准确性（达到 100%），还通过注入模拟误差验证了其在真实大规模模型训练中的鲁棒性。对比了不同模型架构（CNN vs Transformer），证明了方案的通用性。

### 6. 主要结论与发现
*   **精度保持**：OptINC 在 ResNet50 和 LLaMA 任务上均达到了与 Ring All-reduce 基准相当的精度（ResNet50 精度下降仅 0.55%，LLaMA 损失增加仅 0.02）。
*   **通信消除**：完全消除了 Ring All-reduce 中 $N-2/N$ 的额外通信开销。
*   **延迟优化**：在 ResNet50 任务中总延迟降低超过 **25%**，在 LLaMA 任务中降低约 **17%**。
*   **硬件效率**：通过矩阵近似技术，硬件面积（MZI 数量）减少了 **50.7% - 60.7%**。

### 7. 优点
*   **架构创新**：首次探索了在光路交换（OCS）中利用 ONN 进行在网计算，避免了昂贵的光电转换。
*   **硬件友好**：提出的矩阵近似和硬件感知训练算法，在大幅降低物理器件成本的同时保证了计算的可靠性。
*   **解决量化痛点**：通过修改训练数据集，巧妙解决了级联架构中多级量化导致的精度漂移问题。

### 8. 不足与局限
*   **物理层非理想性**：目前的评估主要基于仿真和理想模型，尚未深入讨论光器件的噪声、热漂移及相位误差对大规模计算的实际影响。
*   **灵活性限制**：ONN 的结构与服务器数量 $N$ 耦合，当集群规模变动时，可能需要重新设计或训练 ONN 权重。
*   **应用范围**：目前主要针对数据并行中的梯度聚合，对于更复杂的模型并行或非线性通信模式的适用性有待进一步研究。

（完）
