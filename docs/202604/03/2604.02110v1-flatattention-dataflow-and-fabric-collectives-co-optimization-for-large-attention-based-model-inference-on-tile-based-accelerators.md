---
title: "FlatAttention: Dataflow and Fabric Collectives Co-Optimization for Large Attention-Based Model Inference on Tile-Based Accelerators"
title_zh: FlatAttention：基于瓦片结构加速器的大型注意力机制模型推理的数据流与织构集合通信协同优化
authors: "Chi Zhang, Luca Colagrande, Renzo Andri, Luca Benini"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02110v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 加速器的数据流与架构协同优化
tldr: 针对大模型（如MoE）推理中Attention计算占比增加的问题，本文提出FlatAttention，一种专为瓦片式（Tile-based）加速器设计的优化数据流。该方法通过将集合通信原语集成到片上网络中，显著减少了高带宽内存（HBM）访问，在多种Attention变体上实现了极高的硬件利用率，相比FlashAttention-3和GH200架构在性能和带宽利用率上均有显著提升。
selection_source: fresh_fetch
motivation: 在大模型推理中，Attention计算占比日益增加，且传统架构面临严重的HBM访问瓶颈，亟需针对新型瓦片式加速器优化数据流。
method: 提出FlatAttention数据流，利用片上网络集成的集合通信原语优化瓦片间数据交换，从而最小化昂贵的HBM访问。
result: "实验显示其利用率高达92.3%，比FlashAttention-3快4.1倍，并在DeepSeek-v3推理中实现1.9倍的吞吐量提升和1.4倍的延迟降低。"
conclusion: FlatAttention通过数据流与片上通信的协同优化，为晶圆级加速器上的大规模模型高效推理提供了极具竞争力的解决方案。
---

## 摘要
在混合专家（MoE）模型的推理过程中，注意力机制在总计算量中所占的比例日益增大，使得高效加速变得至关重要。新兴的用于大模型推理的领域专用加速器正转向芯片级和晶圆级基于瓦片（Tile-based）的架构。瓦片包含大型矩阵和向量引擎，并通过片上互连进行连接，支持瓦片间通信以缓解瓦片到主存的流量瓶颈。因此，数据流管理对于实现高利用率至关重要。我们提出了 FlatAttention，一种适用于基于瓦片加速器的现代注意力变体的数据流。FlatAttention 通过利用集成在片上网络织构中的集合原语，最大限度地减少了昂贵的高带宽内存（HBM）访问，实现了高达 92.3% 的利用率，相比 FlashAttention-3 提速 4.1 倍，并降低了 16 倍的 HBM 流量。在峰值性能与 NVIDIA GH200 相当的 32x32 瓦片配置下，FlatAttention 可推广至多种注意力变体，在计算受限的注意力机制上实现了平均 86% 的利用率，在内存受限的注意力机制上实现了 78% 的 HBM 带宽利用率，相比 GH200 上的注意力实现平均提速 1.9 倍。最后，我们在晶圆级多芯片系统上评估了使用 FlatAttention 的端到端 DeepSeek-v3 FP8 解码，尽管其峰值系统性能比现有最先进方案低 1.5 倍，但仍实现了 1.9 倍的系统吞吐量提升和 1.4 倍的单用户 Token 输出延迟降低。

## Abstract
Attention accounts for an increasingly dominant fraction of total computation during inference for mixture-of-experts (MoE) models, making efficient acceleration critical. Emerging domain-specific accelerators for large model inference are shifting toward chip-scale and wafer-scale tile-based architectures. Tiles contain large matrix and vector engines and are connected through on-chip interconnects, which support tile-to-tile traffic to reduce the tile-to-main-memory traffic bottleneck. Hence, dataflow management is crucial to achieve high utilization. We propose FlatAttention, a dataflow for modern attention variants on tile-based accelerators. FlatAttention minimizes expensive high-bandwidth memory (HBM) accesses by exploiting collective primitives integrated into the on-chip network fabric, achieving up to 92.3% utilization, 4.1x speedup over FlashAttention-3, and 16x lower HBM traffic. On a 32x32 tile configuration with peak performance comparable to NVIDIA GH200, FlatAttention generalizes across multiple attention variants, achieving an average of 86% utilization for compute-bound attentions and 78% HBM bandwidth utilization for memory-bound ones, resulting in an average 1.9x speedup over attention implementations on GH200. Finally, we evaluate end-to-end DeepSeek-v3 FP8 decoding with FlatAttention on a wafer-scale multi-die system, achieving a 1.9x improvement in system throughput and a 1.4x reduction in per-user token output latency, despite operating with 1.5x lower peak system performance compared to the state-of-the-art solution.

---

## 论文详细总结（自动生成）

### FlatAttention：基于瓦片结构加速器的大型注意力机制模型推理的数据流与织构集合通信协同优化

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向混合专家（MoE）架构（如 DeepSeek-v3）演进，前馈网络（FFN）的计算开销显著降低，使得**注意力机制（Attention）成为推理过程中的主要瓶颈**（在 DeepSeek-v3 解码阶段占总计算量的 71%）。现有的 GPU 方案（如 FlashAttention-3、FlashMLA）在 NVIDIA GH200 等硬件上仍存在 26%-64% 的性能缺口。
论文的核心问题在于：**如何在瓦片式（Tile-based）和晶圆级（Wafer-scale）加速器上，通过协同优化数据流与片上网络（NoC）的集合通信原语，最大限度地减少高带宽内存（HBM）访问并提升硬件利用率。**

#### 2. 方法论：核心思想与关键技术
论文提出了 **FlatAttention**，其核心思想是将多个计算瓦片视为一个统一的计算单元（Group），利用聚合的 L1 缓存来存储更大的数据块，从而降低 HBM 的 I/O 复杂度。
*   **数据流重定义（Flattening）：** 不同于 FlashAttention 将工作负载分配给独立瓦片，FlatAttention 在瓦片组内进行二次分块。通过增加参与计算的瓦片数量，变相增大了可用的快速本地存储，使 HBM 访问量理论上可降低数倍。
*   **织构集合通信（Fabric Collectives）：** 在 NoC 硬件层面集成多播（Multicast）和归约（Reduction）原语。相比传统的软件模拟通信，硬件支持的多播和归约速度分别提升了约 30 倍和 67 倍。
*   **异步调度优化：** 采用双缓冲和异步执行机制，使矩阵引擎（GEMM）的计算与向量引擎（Softmax）的处理以及 DMA 的数据搬运完全重叠，确保矩阵引擎始终处于高负载状态。
*   **通用性适配：** 针对 MHA、GQA 和 MLA（多头潜在注意力）进行了泛化。特别是针对 MLA，利用“权重吸收（Weight-absorption）”技巧将其转化为类似 MQA 的形式，以便高效映射。

#### 3. 实验设计
*   **实验平台：** 使用基于事件的模拟器 GVSoC，并针对 RTL 级实现的 RedMulE 矩阵引擎和 FlooNoC 进行了周期精确的校准。
*   **对比基准（Benchmark）：** 
    *   **硬件对比：** NVIDIA GH200 GPU（峰值性能 988 TFLOPS, 带宽 4 TB/s）。
    *   **算法对比：** FlashAttention-2/3、FlashMLA。
*   **测试场景：** 
    *   微观测试：不同序列长度（512 到 4096）和头维度（64, 128）下的 Prefill 和 Decode 阶段。
    *   宏观测试：在 64 芯片组成的晶圆级系统上运行 DeepSeek-v3-671B 的端到端解码。

#### 4. 资源与算力
*   **模拟配置：** 模拟了一个 32x32 瓦片的加速器，FP16 峰值性能为 988 TFLOPS，运行频率在 965MHz 至 1.9GHz 之间。
*   **系统规模：** 端到端评估使用了 64 个此类加速器组成的晶圆级多芯片系统，每个芯片配备 128GB HBM4。
*   **GPU 参考：** 实验对比了 96 块 NVIDIA H800 组成的集群性能数据。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
1.  **校准实验：** 验证模拟器与 RTL 之间的一致性（误差在 0.17% - 12% 之间）。
2.  **消融实验：** 探讨了不同瓦片组规模（4x4 到 32x32）对利用率的影响，识别了“过度扁平化（Over-flattening）”导致的性能下降点。
3.  **变体覆盖：** 覆盖了 MHA、GQA、MLA 等主流注意力变体。
4.  **并行策略探索：** 对比了专家并行（EP）、流水线并行（PP）及其混合模式。
**评价：** 实验设计非常充分，从底层硬件原语到顶层系统吞吐量均有详尽数据，对比基准选择了目前工业界最强的 GH200 和 DeepSeek 官方内核，具有很高的说服力。

#### 6. 主要结论与发现
*   **性能飞跃：** 在相同硬件配置下，FlatAttention 比 FlashAttention-3 提速 4.1 倍，HBM 流量减少 16 倍。
*   **高利用率：** 在计算受限场景下实现 86% 的平均利用率，在内存受限场景下实现 78% 的带宽利用率。
*   **端到端优势：** 在 DeepSeek-v3-671B 推理中，尽管系统峰值性能比 H800 集群低 1.5 倍，但吞吐量提升了 1.9 倍，单用户延迟（TPOT）降低了 1.4 倍。
*   **硬件协同的重要性：** 证明了在 NoC 中集成硬件级集合通信原语是打破注意力机制瓶颈的关键。

#### 7. 优点
*   **软硬协同深度优化：** 不仅仅是算法改进，还深入到了 NoC 架构的指令级配合。
*   **前瞻性强：** 针对晶圆级加速器和大模型（DeepSeek-v3）的最新趋势进行了针对性设计。
*   **数学推导严谨：** 提供了 MLA 权重吸收的详细数学证明，使其数据流具有普适性。

#### 8. 不足与局限
*   **硬件依赖性：** 该方法高度依赖具有特定硬件集合通信支持的 NoC 架构，在传统 GPU 或缺乏此类原语的加速器上难以直接应用。
*   **短序列挑战：** 在极短序列（如 S=512）下，由于计算量不足以掩盖通信和同步开销，会出现利用率下降的“过度扁平化”问题。
*   **互连拓扑限制：** 实验主要基于 2D Mesh 拓扑，对于更大规模的晶圆级集成，复杂的流量拥塞模型可能还需进一步验证。

（完）
