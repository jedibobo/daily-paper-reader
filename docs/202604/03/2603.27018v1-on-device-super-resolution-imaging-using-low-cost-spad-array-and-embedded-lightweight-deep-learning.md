---
title: On-Device Super Resolution Imaging Using Low-Cost SPAD Array and Embedded Lightweight Deep Learning
title_zh: 基于低成本 SPAD 阵列与嵌入式轻量级深度学习的设备端超分辨率成像
authors: "Zhenya Zang, Xingda Li, David Day Uei Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.27018v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 用于微控制器端侧超分辨率的轻量级深度学习
tldr: 本研究针对消费级低分辨率（48x32）单光子雪崩二极管（SPAD）阵列，提出了一种轻量级超分辨率（LiteSR）神经网络。该方案通过在Arduino微控制器上部署压缩后的深度学习模型，实现了将低分辨率深度和强度图像实时提升至256x256甚至512x512的高分辨率视频流。实验证明该系统在合成和真实场景下均具有高保真度和鲁棒性，为低成本SPAD传感器的高清成像提供了可扩展的嵌入式解决方案。
selection_source: fresh_fetch
motivation: 旨在解决消费级SPAD传感器空间分辨率极低的问题，以满足高性能成像对高分辨率深度和强度图像的需求。
method: 开发并压缩了一种轻量级超分辨率神经网络（LiteSR），并将其集成到Arduino微控制器中实现端到端实时处理。
result: 该系统成功实现了从48x32到最高512x512分辨率的提升，在合成与真实室内外数据集中均表现出优异的重建质量和抗噪能力。
conclusion: 这种LiteSR嵌入式系统协同设计为提升低成本SPAD阵列的成像性能提供了一种高效、经济且可扩展的实时解决方案。
---

## 摘要
本研究提出了一种轻量级超分辨率（LiteSR）神经网络，用于处理由空间分辨率为 48x32 的消费级单光子雪崩二极管（SPAD）阵列获取的深度和强度图像。该框架可重建尺寸为 256x256 的高分辨率（HR）图像。性能评估采用了合成数据集和真实数据集。大量的定量指标证明了该方法在合成数据集上的高重建保真度，而室内外真实测量实验进一步证实了所提方法的鲁棒性。此外，SPAD 传感器与 Arduino UNO Q 微控制器相连，后者接收低分辨率（LR）深度和强度图像，并将其输入压缩后的预训练深度学习（DL）模型，从而实现实时超分辨率视频流。除了 256x256 的设置外，研究还评估了一系列目标高分辨率，以确定 LiteSR 可达到的最大放大分辨率（512x512），包括输入受噪声干扰的低分辨率图像场景。所提出的 LiteSR 嵌入式系统协同设计提供了一种可扩展且具有成本效益的解决方案，能够提升现有消费级 SPAD 阵列的空间分辨率，以满足高分辨率成像需求。

## Abstract
This work presents a lightweight super-resolution (LiteSR) neural network for depth and intensity images acquired from a consumer-grade single-photon avalanche diode (SPAD) array with a 48x32 spatial resolution. The proposed framework reconstructs high-resolution (HR) images of size 256x256. Both synthetic and real datasets are used for performance evaluation. Extensive quantitative metrics demonstrate high reconstruction fidelity on synthetic datasets, while experiments on real indoor and outdoor measurements further confirm the robustness of the proposed approach. Moreover, the SPAD sensor is interfaced with an Arduino UNO Q microcontroller, which receives low-resolution (LR) depth and intensity images and feeds them into a compressed, pre-trained deep learning (DL) model, enabling real-time SR video streaming. In addition to the 256x256 setting, a range of target HR resolutions is evaluated to determine the maximum achievable upscaling resolution (512x512) with LiteSR, including scenarios with noise-corrupted LR inputs. The proposed LiteSR-embedded system co-design provides a scalable, cost-effective solution to enhance the spatial resolution of current consumer-grade SPAD arrays to meet HR imaging requirements.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LiteSR** 的轻量级超分辨率（SR）方案，专门针对消费级低分辨率单光子雪崩二极管（SPAD）阵列设计，并成功在嵌入式微控制器上实现了实时部署。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：消费级 SPAD 传感器（如 TMF8829）虽然具有高灵敏度和飞行时间（ToF）测量能力，但其空间分辨率极低（仅 48×32 像素），严重限制了其在深度感知和场景理解中的应用。
*   **研究动机**：
    *   现有的 SPAD 超分辨率方法大多依赖外部高分辨率（HR）引导图像（如 RGB 相机或 iToF 相机）进行融合，增加了系统复杂性和成本。
    *   现有的非融合方法往往针对更低分辨率（8×8）且难以处理精细纹理。
    *   缺乏能在资源受限的嵌入式设备（如微控制器）上实时运行的轻量级联合（深度+强度）超分辨率模型。

### 2. 方法论
*   **核心思想**：提出一种双分支轻量级深度学习模型（LiteSR），在不依赖外部相机的情况下，同时对 SPAD 输出的深度图和强度图进行 4 倍至 8 倍的超分辨率重建。
*   **关键技术细节**：
    *   **双分支架构**：深度分支侧重几何结构，强度分支侧重局部纹理。
    *   **强度引导深度**：利用强度信号提取边缘掩码，引导深度分支的重建，以增强深度边缘的保真度。
    *   **核心模块**：
        *   **EARB（边缘感知残差块）**：在深度分支中使用空洞卷积扩大感受野，捕捉长程几何上下文。
        *   **RLFB（残差局部特征块）**：在强度分支中优化局部空间纹理。
    *   **残差学习与门控机制**：模型学习双三次插值（Bicubic）基准与目标 HR 图像之间的残差，并通过可学习的 Sigmoid 门控调节残差贡献，稳定训练。
    *   **量化感知训练（QAT）**：将模型压缩为 INT8 格式，以适应嵌入式硬件的内存和算力限制。
*   **损失函数**：结合了 Charbonnier 损失、SSIM/MS-SSIM 损失、Sobel 边缘/梯度损失以及全变分（TV）损失，平衡像素精度与结构完整性。

### 3. 实验设计
*   **数据集**：
    *   **合成数据**：使用 NYUv2 室内数据集生成 3,121 对 LR/HR 图像对进行训练和验证；使用 Middlebury 立体数据集进行独立测试。
    *   **真实数据**：使用 TMF8829 SPAD 传感器采集真实的室内外场景。
*   **Benchmark（基准）**：
    *   **Bicubic（双三次插值）**：作为传统的图像放大基准。
    *   **FP32 vs. INT8**：对比全精度模型与量化模型在性能和资源消耗上的差异。
*   **对比指标**：
    *   强度图：MS-SSIM、PSNR、GMSD（梯度幅值相似度偏差）。
    *   深度图：SSIM、RMSE、$\delta < 1.25$（深度准确率）。
    *   真实数据：拉普拉斯方差（LV，衡量清晰度）和深度平滑度得分。

### 4. 资源与算力
*   **训练算力**：使用单块 **NVIDIA GeForce RTX 5070 Ti GPU**（注：文中原文如此，可能为型号笔误或前瞻性描述），训练时长约 **1 小时**，Batch Size 为 16。
*   **部署硬件**：**Arduino UNO Q** 微控制器（配备 STM32U585 MCU 和 Qualcomm QRB2210 SoC），拥有 2GB RAM。
*   **模型效率**：
    *   INT8 模型大小：约 **36.77 KB** 至 **363.80 KB**（取决于通道配置）。
    *   推理延迟：在嵌入式端最低可达 **172.8 ms**（约 5.8 fps），实际视频流运行速度约为 **4.5 fps**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 8 个合成测试场景上进行了详尽的定量评估。
    *   进行了针对真实室内外场景（包括静态物体和动态手偶）的定性评估。
    *   **消融实验**：分别验证了 EARB 和 RLFB 模块对各自分支的贡献，证明了模块设计的有效性。
    *   **压力测试**：测试了从 48×32 放大到 512×512（8 倍超分）的极限性能。
*   **评价**：实验设计较为充分，涵盖了从算法性能到硬件部署的完整链路，对比实验客观，消融实验证实了架构设计的合理性。

### 6. 主要结论与发现
*   **性能优越**：LiteSR 在深度和强度重建上均显著优于 Bicubic 插值，尤其在边缘和纹理恢复方面。
*   **量化友好**：INT8 量化模型在大幅减小模型体积（减少约 74% 参数存储）和计算量（减少 50% 以上 MACs）的同时，性能下降极小。
*   **实时可行性**：证明了在微控制器级别硬件上实现 SPAD 图像实时超分辨率视频流（~5 fps）的技术可行性。
*   **抗噪性**：模型能够有效处理 SPAD 传感器的热像素（Hot Pixels）和响应非均匀性。

### 7. 优点
*   **端到端嵌入式集成**：不仅提出了算法，还完成了从传感器驱动到 SoC 推理的完整软硬件协同设计。
*   **非融合方案**：降低了对多传感器对齐和同步的要求，使系统更紧凑、成本更低。
*   **架构高效**：通过 1×1 投影卷积等设计，使完整模型比去掉特定模块的简化版模型更小、更高效。

### 8. 不足与局限
*   **帧率限制**：虽然实现了实时，但 4.5 fps 的帧率对于高速运动场景仍显不足。
*   **信息利用不完全**：目前仅利用了深度和强度图，尚未利用 SPAD 原始的直方图（Histogram）数据，后者包含更丰富的时域光子信息。
*   **传感器限制**：受限于 TMF8829 传感器，最大测量范围仅为 1.4 米，限制了其在远距离感知场景的应用。
*   **泛化风险**：训练集主要基于室内场景（NYUv2），在极端室外环境或强背景光下的表现仍需进一步验证。

（完）
