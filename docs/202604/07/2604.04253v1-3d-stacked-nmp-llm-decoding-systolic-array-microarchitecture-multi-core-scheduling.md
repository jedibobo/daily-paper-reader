---
title: "3D-Stacked NMP, LLM Decoding, Systolic Array Microarchitecture, Multi-Core Scheduling"
title_zh: 3D堆叠近内存处理（NMP）、大语言模型（LLM）解码、脉动阵列微架构、多核调度
authors: "Chenyang Ai, Yixing Zhang, Haoran Wu, Yudong Pan, Lechuan Zhao, Wenhui OU"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04253v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 重新思考3D堆叠近内存处理的计算微架构，以加速大语言模型解码。
tldr: 针对大语言模型（LLM）推理中解码阶段受限于内存带宽的问题，本文探讨了3D堆叠近内存处理（NMP）架构。研究发现高带宽使计算成为新瓶颈，为此提出了一种新型计算微架构，通过将脉动阵列与向量核心统一，实现了高面积效率和细粒度可重构性，并配合多核调度框架，显著提升了推理性能和能效。
selection_source: fresh_fetch
motivation: 3D堆叠NMP提供的高带宽使LLM解码算子从带宽受限转变为计算受限，且逻辑芯片面积受限对计算单元设计提出了严峻挑战。
method: 提出一种将面积高效的脉动阵列与现有向量核心统一的微架构，支持形状和数据流的可重构性，并引入多核调度框架。
result: 在稠密模型和混合专家模型（MoE）上，该设计相比Stratum架构实现了平均2.91倍的加速和2.40倍的能效提升。
conclusion: 针对3D堆叠NMP优化的计算微架构和调度策略是解决LLM解码瓶颈、实现高效推理的关键。
---

## 摘要
大语言模型（LLM）解码是主要的推理瓶颈，因为其低算术强度使得性能对内存带宽高度敏感。3D堆叠近内存处理（NMP）提供了比传统片外接口显著更高的本地内存带宽，使其成为解码加速的一个极具前景的基底。然而，我们的分析表明，这种带宽优势也使得3D堆叠NMP上的许多解码算子重新回到了计算受限状态。在逻辑芯片紧凑的面积预算下，计算基底本身的设计因此成为了首要挑战。因此，我们重新思考了先前3D堆叠NMP设计的计算微架构。首先，我们将先前基于MAC树的计算单元替换为面积效率更高的脉动阵列，并进一步观察到解码算子表现出显著的形状多样性，这使得脉动阵列形状和数据流的可重构性对于维持高利用率至关重要。基于这一见解，我们继续利用两个关键机会：高本地内存带宽减少了对大型片上缓冲区的需求，而原本设计用于处理辅助张量计算的现有向量核心，已经提供了脉动阵列细粒度灵活性所需的大部分控制逻辑和多端口缓冲，使我们能够以极高的面积效率统一这两种结构。基于这些见解，我们提出了首个专为3D堆叠NMP LLM解码定制的计算微架构，旨在明确满足低面积成本、高带宽运行和细粒度可重构性的共同要求。我们还提出了一个多核调度框架。与Stratum相比，我们的设计在稠密模型和混合专家（MoE）模型中平均实现了2.91倍的加速和2.40倍的能效提升。

## Abstract
Large language model (LLM) decoding is a major inference bottleneck because its low arithmetic intensity makes performance highly sensitive to memory bandwidth. 3D-stacked near-memory processing (NMP) provides substantially higher local memory bandwidth than conventional off-chip interfaces, making it a promising substrate for decode acceleration. However, our analysis shows that this bandwidth advantage also shifts many decode operators on 3D-stacked NMP back into the compute-bound regime. Under the tight area budget of the logic die, the design of the compute substrate itself therefore becomes a first-order challenge.   Therefore, we rethink the compute microarchitecture of prior 3D-stacked NMP designs. First, we replace prior MAC tree-based compute units with a more area-efficient systolic array, and we further observe that decode operators exhibit substantial shape diversity, making reconfigurability in both systolic array shape and dataflow essential for sustaining high utilization. Building on this insight, we continue to exploit two key opportunities: the high local memory bandwidth reduces the need for large on-chip buffers, and the existing vector core, originally designed to handle auxiliary tensor computations, already provides much of the control logic and multi-ported buffering required for fine-grained flexibility for systolic array, allowing us to unify the two structures in a highly area-efficient manner. Based on these insights, we present the first compute microarchitecture tailored to 3D-stacked NMP LLM decoding, explicitly designed to satisfy the joint requirements of low area cost, high-bandwidth operation, and fine-grained reconfigurability.   We further propose an multi-core scheduling framework. Compared with Stratum, our design achieves an average 2.91x speedup and 2.40x higher energy efficiency across both dense and MoE models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking Compute Substrates for 3D-Stacked Near-Memory LLM Decoding: Microarchitecture–Scheduling Co-Design》的深度结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：大语言模型（LLM）的解码阶段（Decoding）通常受限于内存带宽。虽然3D堆叠近内存处理（NMP）技术提供了极高的本地带宽，但这种带宽优势反而使解码算子从“内存受限”转变为“计算受限”。
*   **研究动机**：
    *   **面积瓶颈**：3D堆叠芯片的逻辑层（Logic Die）面积极其有限，传统的MAC树（MAC-tree）计算单元面积效率低，难以提供足够的算力。
    *   **形状多样性**：解码过程中的张量形状多变（尤其是Batch Size的变化），固定形状的计算阵列利用率低。
    *   **软硬件失配**：现有的NMP设计未充分利用高带宽带来的缓冲区缩减机会，且缺乏针对解码特性的调度框架。

### 2. 方法论：Snake 架构与调度框架
*   **核心思想**：提出一种名为 **Snake** 的可重构脉动阵列（Systolic Array）微架构，通过微架构与调度的协同设计，在极小的面积内实现高性能、高灵活性的LLM解码加速。
*   **关键技术细节**：
    1.  **计算单元替换**：用面积效率更高的脉动阵列取代MAC树，实现约8倍的计算密度提升。
    2.  **缓冲区与计算重分配**：利用3D NMP的高带宽特性，缩减用于掩盖延迟的大型缓冲区，将节省的面积用于部署更多处理元件（PE）。
    3.  **向量-脉动统一设计**：将处理非线性算子的向量核心与脉动阵列统一，共享多端口缓冲区和控制逻辑，降低重构开销。
    4.  **蛇形映射（Snake-like Mapping）**：通过PE间的局部重定向，支持将物理阵列（如64x64）灵活重构为不同逻辑形状（如8x512或16x256），以适配解码中较小的M维度。
*   **调度框架**：
    *   提出**算子感知调度**，支持四种分区模式：IS-S（输入驻留-空间分区）、IS-ST（时空分区）、OS-S（输出驻留-空间分区）和OS-ST。
    *   根据算子形状（N和K维度的相对大小）动态选择最优的数据流和分区策略。

### 3. 实验设计
*   **数据集/模型场景**：涵盖了主流的稠密（Dense）和混合专家（MoE）模型，包括 OPT-66B、LLaMA3-70B、Mixtral 8x22B、Qwen3-30B、DeepSeek-236B。
*   **Benchmark 与对比方法**：
    *   **GPU 基准**：NVIDIA H100。
    *   **NMP 基准**：Stratum（基于MAC树的先进3D NMP架构）。
    *   **固定形状阵列**：48x48（近方形）和8x288（长条形）的脉动阵列。
*   **评估指标**：端到端延迟（E2E Latency）、逐Token延迟（TBT）、能效比、面积效率。

### 4. 资源与算力
*   **硬件实现**：使用 SystemVerilog 实现，基于 7nm ASAP7 预测性 PDK 进行综合。
*   **仿真环境**：使用扩展的 SCALE-Sim v3 进行周期精确仿真，集成 Ramulator 模拟内存行为。
*   **算力参考**：实验假设逻辑芯片运行频率为 800MHz（考虑重构开销），对比的固定阵列为 1GHz。系统配置为 8 卡并行（TP=8）。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 5 种不同架构的 LLM 进行了详尽测试，涵盖了不同的 Batch Size（8 到 64）和序列长度（4K 到 16K）。
*   **消融与对比**：
    *   对比了不同数据流策略（IS vs OS）对性能的影响。
    *   分析了缓冲区大小与 PE 数量的权衡关系。
    *   进行了多核调度策略的敏感性分析。
*   **评价**：实验设计全面，对比了从传统 GPU 到最前沿的 NMP 研究成果，客观地展示了可重构性在不同模型结构下的收益。

### 6. 主要结论与发现
*   **性能提升**：相比 Stratum（MAC-tree），Snake 实现了平均 **2.91倍** 的加速。
*   **能效领先**：能效比提升了 **2.40倍**；相比 H100 GPU，加速比达到 11.47倍，能效提升 5.74倍。
*   **面积效率**：计算面积效率比 MAC 树设计高出 **4.00倍**。
*   **重构价值**：对于 MoE 模型，灵活的调度策略避免了固定策略下高达 6.43倍 的性能损失。

### 7. 优点
*   **针对性强**：精准捕捉到了 3D NMP 环境下“带宽充裕、面积受限、计算转为瓶颈”的独特痛点。
*   **架构优雅**：通过“蛇形映射”巧妙解决了脉动阵列处理小 Batch Size 时利用率低的经典难题。
*   **协同优化**：不仅改进了底层硬件，还配套了上层调度框架，实现了全栈式优化。

### 8. 不足与局限
*   **频率牺牲**：为了实现可重构性，Snake 的运行频率（800MHz）低于固定形状阵列（1GHz），虽然整体性能更强，但在某些极极端规整的算子下可能存在频率劣势。
*   **仿真局限**：所有结果基于周期精确仿真和综合数据，尚未有实际流片验证，实际散热和热管理挑战（3D堆叠的常见问题）虽有提及但未深入探讨。
*   **应用限制**：该架构高度依赖 3D 堆叠技术（如 HBM3 或 Hybrid Bonding），在传统 DDR 内存环境下无法发挥其带宽优势。

（完）
