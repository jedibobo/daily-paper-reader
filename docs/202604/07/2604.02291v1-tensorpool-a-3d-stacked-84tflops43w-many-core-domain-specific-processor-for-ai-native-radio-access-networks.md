---
title: "TensorPool: A 3D-Stacked 8.4TFLOPS/4.3W Many-Core Domain-Specific Processor for AI-Native Radio Access Networks"
title_zh: TensorPool：一种用于 AI 原生无线接入网的 3D 堆叠 8.4TFLOPS/4.3W 多核领域专用处理器
authors: "Marco Bertuletti, Yichao Zhang, Diyou Shen, Alessandro Vanelli-Coralli, Frank K. Gürkaynak, Luca Benini"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02291v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于AI原生无线网络的能效型领域专用处理器
tldr: 针对6G AI原生无线接入网（RAN）物理层的高计算复杂度和低功耗需求，本文提出TensorPool处理器。该处理器集成256个RISC-V核心与16个高性能张量引擎，并采用3D堆叠技术优化内存访问。在TSMC N7工艺下，它实现了8.4TFLOPS的算力与4.3W的低功耗，显著提升了AI-RAN任务的能效和面积效率，满足实时处理需求。
selection_source: fresh_fetch
motivation: 6G物理层引入AI模型导致计算复杂度激增，传统处理器难以在严苛的实时性和功耗限制下高效运行。
method: 设计了一种包含256个RISC-V核心和16个张量引擎的多核架构，并利用3D堆叠技术优化张量引擎与4MiB L1暂存内存之间的互连。
result: "在AI-RAN张量运算中达到89%的利用率，相比纯核心集群性能提升6倍，能效面积比提升9.1倍，且3D堆叠使面积缩小2.32倍。"
conclusion: TensorPool通过领域专用化和3D集成技术，为未来可持续、高吞吐量的AI原生无线通信提供了高效的硬件解决方案。
---

## 摘要
6G 无线接入网 (RAN) 物理层 (PHY) 即将集成的 AI 技术，将在具有挑战性的传输场景中实现更高的服务质量。然而，与传统基带相比，深度优化的 AI 原生 PHY 模型带来了更高的计算复杂度，这使得在现代 PHY 典型的亚毫秒级实时约束下进行部署面临挑战。此外，随着向太赫兹载波的扩展，即将到来的 6G 基站加密进一步限制了基站的功耗，从而约束了可用于计算的预算（≤ 100W）。为了确保长期可持续性所需的灵活性，以及在主导 AI 原生 PHY 的高吞吐量张量计算中获得必要的能效提升，可以通过多核可编程基带处理器的领域专用化来实现。遵循领域专用化策略，我们提出了 TensorPool，这是一个由 256 个 RISCV32IMAF 可编程核心组成的集群，由 16 个 256 MACs/周期 (FP16) 的张量引擎加速，并具有对 4MiB L1 便签存储器 (scratchpad) 的低延迟访问，以实现最大程度的数据复用。TensorPool 采用台积电 (TSMC) N7 工艺实现，在 AI-RAN 的张量运算中达到了 3643 MACs/周期（张量单元利用率为 89%），比没有张量加速的纯核心集群高出 6 倍，同时将 GOPS/W/mm² 效率提升了 9.1 倍。此外，我们展示了通过对 TensorPool 的计算模块进行 3D 堆叠，以更好地展开张量引擎到 L1 内存的布线，与 2D 实现相比，在不降低频率的情况下实现了 2.32 倍的面积效率提升。

## Abstract
The upcoming integration of AI in the physical layer (PHY) of 6G radio access networks (RAN) will enable a higher quality of service in challenging transmission scenarios. However, deeply optimized AI-Native PHY models impose higher computational complexity compared to conventional baseband, challenging deployment under the sub-msec real-time constraints typical of modern PHYs. Additionally, following the extension to terahertz carriers, the upcoming densification of 6G cell-sites further limits the power consumption of base stations, constraining the budget available for compute ($\leq$ 100W). The desired flexibility to ensure long term sustainability and the imperative energy-efficiency gains on the high-throughput tensor computations dominating AI-Native PHYs can be achieved by domain-specialization of many-core programmable baseband processors. Following the domain-specialization strategy, we present TensorPool, a cluster of 256 RISCV32IMAF programmable cores, accelerated by 16 256 MACs/cycle (FP16) tensor engines with low-latency access to 4MiB of L1 scratchpad for maximal data-reuse. Implemented in TSMC's N7, TensorPool achieves 3643~MACs/cycle (89% tensor-unit utilization) on tensor operations for AI-RAN, 6$\times$ more than a core-only cluster without tensor acceleration, while simultaneously improving GOPS/W/mm$^2$ efficiency by 9.1$\times$. Further, we show that 3D-stacking the computing blocks of TensorPool to better unfold the tensor engines to L1-memory routing provides 2.32$\times$ footprint improvement with no frequency degradation, compared to a 2D implementation.

---

## 论文详细总结（自动生成）

### 论文总结：TensorPool —— 用于 AI 原生无线接入网的 3D 堆叠多核领域专用处理器

#### 1. 核心问题与整体含义（研究动机和背景）
随着 6G 时代的到来，无线接入网（RAN）的物理层（PHY）正向 **AI 原生（AI-Native）** 转型。这带来了两个严峻挑战：
*   **计算复杂度激增：** AI 模型（如 ResNet、Transformer）的引入使得计算需求远超传统基带处理，且必须在亚毫秒级（<1ms）内完成。
*   **功耗与部署限制：** 6G 基站密度增加，要求单个基站的数字信号处理功耗控制在 100W 以内。
现有的通用 CPU、GPU 或固定功能的 ASIC 难以同时满足**高灵活性、高吞吐量和极低功耗**的需求。论文的研究动机在于通过“领域专用化（Domain-Specialization）”和“3D 堆叠技术”，设计一种既能处理传统信号处理又能高效加速张量运算的可编程处理器。

#### 2. 论文提出的方法论
论文提出了 **TensorPool** 架构，其核心思想是将通用处理核心与高性能张量引擎深度集成，并通过 3D 芯片堆叠优化互连。
*   **异构多核架构：** 包含 256 个 RISC-V (RV32IMAF) 核心，分为 64 个 Tile。每个 Tile 包含 4 个核心和 1 个张量引擎（TE，每 4 个 Tile 共享一个）。
*   **张量引擎 (RedMulE)：** 每个 TE 拥有 256 个 FMA 单元，支持 FP16 精度，专门优化 GEMM（通用矩阵乘法）运算。
*   **层次化互连与内存：** 拥有 4MiB 的共享 L1 暂存内存（Scratchpad）。为了解决内存访问延迟，设计了**延迟容忍流式传输器（Streamer）**、重排序缓存（ROB）以及支持**突发传输（Burst Support）**的层次化交叉开关（XBAR）。
*   **3D 堆叠技术：** 采用 **Logic-on-Logic** 3D 集成方案，将 4 个 Group 模块垂直堆叠。利用微小的混合键合（Hybrid Bonding）间距（4.5μm）将布线从 2D 平面扩展到 Z 轴，大幅缩短全局布线长度。
*   **理论支撑：** 引用 **Kung's Principle**（平衡计算原则）进行数学建模，证明了在 L1 和 L2 内存层次结构下，该架构的带宽足以支撑张量引擎的满载运行。

#### 3. 实验设计
*   **场景与负载：** 涵盖了 AI 原生 PHY 的核心算子，包括：
    *   **基础算子：** GEMM（不同规模）、Softmax、LayerNorm、ReLU、Batchnorm。
    *   **传统 PHY 任务：** CFFT、MIMO-MMSE 检测、信道估计（LS-CHE）。
    *   **端到端 AI 模块：** 全连接层（FC）、深度可分离卷积、多头注意力机制（MHA）。
*   **Benchmark（基准测试）：**
    *   **TeraPool：** 之前的 1024 核纯 RISC-V 集群（作为能效和面积效率的对比基准）。
    *   **NVIDIA Aerial 平台：** 包括 Blackwell (RTX 6000/5090) 和 L4 Tensor-Core GPU。
    *   **Qualcomm HTA：** 商业张量加速平台。
*   **对比维度：** 吞吐量（TFLOPS）、能效（TFLOPS/W）、面积效率（TFLOPS/mm²）以及 2D 与 3D 实现的布局对比。

#### 4. 资源与算力
*   **工艺节点：** 台积电 (TSMC) **N7 (7nm)** FinFET 工艺。
*   **峰值算力：** 8.4 TFLOPS @ FP16。
*   **功耗：** 典型工作负载下为 **4.32W**。
*   **仿真工具：** 使用 QuestaSim 进行周期精确的 RTL 仿真；使用 Synopsys Fusion Compiler 和 3DIC Compiler 进行物理设计（PPA 分析）；使用 PrimeTime 进行功耗评估。
*   **训练说明：** 文中未提及模型训练时长，因为该处理器专注于**推理（Inference）**和实时基带处理。

#### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **单/多引擎扩展性实验：** 测试了从 1 个 TE 到 16 个 TE 并行时的利用率变化。
*   **互连带宽消融实验：** 通过调整突发传输因子（J, K）验证了互连设计对性能的影响。
*   **混合负载实验：** 同时运行 TE、PE（核心）和 DMA，模拟真实复杂的 AI-RAN 工作流。
*   **物理设计验证：** 提供了完整的 2D 布局图和 3D 堆叠的热力/布线分析。
**评价：** 实验设计非常充分且客观。它不仅关注峰值性能，还深入分析了实际应用中的利用率损失（如 89% 的 GEMM 利用率和 67% 的混合负载利用率），具有很高的工程参考价值。

#### 6. 论文的主要结论与发现
*   **性能飞跃：** TensorPool 在张量运算上的吞吐量比纯核心集群（TeraPool）高出 **6 倍**，综合能效面积比提升了 **9.1 倍**。
*   **高利用率：** 凭借延迟容忍设计，在复杂的层次化内存架构下，张量引擎仍能保持高达 89% 的计算单元利用率。
*   **3D 堆叠优势：** 3D 实现相比 2D 实现，在不损失频率的前提下，实现了 **2.32 倍的面积效率提升**（超线性缩减），布线长度缩短了 20%。
*   **实时性达标：** 证明了在 1ms 的 TTI 约束下，该处理器能轻松处理大规模 MIMO 和复杂的 AI 接收机模型。

#### 7. 优点
*   **领域专用性强：** 紧扣 6G AI-RAN 的痛点，平衡了 RISC-V 的灵活性与张量引擎的高性能。
*   **架构创新：** 提出的延迟容忍流式传输器和突发传输机制，有效解决了多核共享 L1 内存的拥塞问题。
*   **前瞻性技术应用：** 率先在基带处理器中探索了 Logic-on-Logic 3D 堆叠，为未来性能扩展提供了清晰路径。

#### 8. 不足与局限
*   **散热挑战：** 虽然论文提到了 3D 堆叠，但对于 Logic-on-Logic 带来的热密度增加及散热方案（Thermal Management）仅做了简单假设，未给出详细的热仿真数据。
*   **制造复杂性：** 3D 混合键合工艺成本高、良率控制难，论文未讨论大规模商用时的成本效益比。
*   **软件栈依赖：** 高效运行该处理器依赖于复杂的编译器优化和任务调度，论文对软件工具链的成熟度描述较少。

（完）
