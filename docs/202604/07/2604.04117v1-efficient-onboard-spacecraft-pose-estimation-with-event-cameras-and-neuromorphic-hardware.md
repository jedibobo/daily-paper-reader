---
title: Efficient Onboard Spacecraft Pose Estimation with Event Cameras and Neuromorphic Hardware
title_zh: 基于事件相机与类脑硬件的高效星载航天器姿态估计
authors: "Arunkumar Rathinam, Jules Lecomte, Jost Reelsen, Gregor Lenz, Axel von Arnim, Djamila Aouada"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04117v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于类脑硬件的航天器在轨姿态估计
tldr: 针对航天器在极端光照和高速运动下的姿态估计难题，本文提出了一种结合事件相机与BrainChip Akida类脑处理器的6自由度姿态估计流水线。通过在SPADES数据集上训练轻量化MobileNet关键点回归网络，并利用量化感知训练将其转换为脉冲神经网络（SNN），实现了在Akida硬件上的低功耗、低延迟推理。这是首个在Akida硬件上运行的端到端航天器姿态估计演示，为未来自主空间任务提供了高效的感知方案。
selection_source: fresh_fetch
motivation: 传统相机在空间环境的极端光照和高速运动下易失效，需利用事件相机和类脑计算实现鲁棒且低功耗的姿态估计。
method: 采用轻量化MobileNet架构进行关键点回归，结合量化感知训练并将其部署为Akida兼容的脉冲神经网络。
result: 在Akida V1硬件上实现了实时、低功耗推理，并在Akida V2云端验证了基于热图的模型具有更高的姿态精度。
conclusion: 该研究证明了事件相机与类脑硬件结合在航天器自主感知任务中的实用性与高效性。
---

## 摘要
可靠的相对姿态估计是实现自主交会对接和近距离操作的关键，然而，由于极端的照明条件、高对比度和目标的快速运动，空间成像面临着众所周知的挑战。事件相机提供异步的、由变化驱动的测量，在基于帧的图像发生饱和或模糊时仍能保持信息丰富，而类脑处理器可以利用稀疏激活来实现低延迟、高能效的推理。本文提出了一种航天器六自由度（6-DoF）姿态估计流水线，将基于事件的视觉与 BrainChip Akida 类脑处理器相结合。利用 SPADES 数据集，我们在轻量级事件帧表示上训练了紧凑的 MobileNet 风格关键点回归网络，应用了量化感知训练（8/4位），并将模型转换为与 Akida 兼容的脉冲神经网络。我们对三种事件表示进行了基准测试，并在 Akida V1 硬件上演示了实时、低功耗的推理。此外，我们还针对 Akida V2 设计了一个基于热图的模型，并在 Akida 云端进行了评估，从而提高了姿态精度。据我们所知，这是首次在 Akida 硬件上运行航天器姿态估计的端到端演示，为未来自主空间任务实现低延迟、低功耗感知提供了一条切实可行的路径。

## Abstract
Reliable relative pose estimation is a key enabler for autonomous rendezvous and proximity operations, yet space imagery is notoriously challenging due to extreme illumination, high contrast, and fast target motion. Event cameras provide asynchronous, change-driven measurements that can remain informative when frame-based imagery saturates or blurs, while neuromorphic processors can exploit sparse activations for low-latency, energy-efficient inferences. This paper presents a spacecraft 6-DoF pose-estimation pipeline that couples event-based vision with the BrainChip Akida neuromorphic processor. Using the SPADES dataset, we train compact MobileNet-style keypoint regression networks on lightweight event-frame representations, apply quantization-aware training (8/4-bit), and convert the models to Akida-compatible spiking neural networks. We benchmark three event representations and demonstrate real-time, low-power inference on Akida V1 hardware. We additionally design a heatmap-based model targeting Akida V2 and evaluate it on Akida Cloud, yielding improved pose accuracy. To our knowledge, this is the first end-to-end demonstration of spacecraft pose estimation running on Akida hardware, highlighting a practical route to low-latency, low-power perception for future autonomous space missions.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient Onboard Spacecraft Pose Estimation with Event Cameras and Neuromorphic Hardware》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限（SWaP约束：尺寸、重量、功耗）的航天器上，实现针对非合作目标的鲁棒、实时、低功耗 6-DoF（六自由度）相对姿态估计。
*   **研究背景**：
    *   **环境挑战**：空间环境存在极端光照（强高光、深阴影）、高对比度和目标快速运动，传统基于帧的相机易产生运动模糊或曝光饱和。
    *   **传感器优势**：事件相机（Event Cameras）仅记录像素级亮度变化，具有高动态范围和高时间分辨率，能有效弥补传统相机的不足。
    *   **计算需求**：航天器需要持续运行姿态估计，传统的 GPU 方案功耗过高。类脑（Neuromorphic）处理器（如 BrainChip Akida）利用稀疏激活和脉冲神经网络（SNN），提供了一种高能效比的替代方案。

### 2. 方法论
论文提出了一种混合姿态估计流水线，核心步骤如下：
*   **事件表示（Event Representations）**：将异步事件流转换为适合 CNN/SNN 处理的类图像张量。对比了三种轻量化方案：
    *   **E2F (Event-to-Frame)**：记录每个像素最后一次事件的极性。
    *   **2DHist (2D Histogram)**：累加时间窗口内的事件计数。
    *   **LNES (Locally-Normalised Event Surfaces)**：根据归一化时间戳加权累加，保留粗略的时间顺序。
*   **关键点回归网络**：
    *   **Akida V1 架构**：采用 MobileNet 风格的轻量化骨干网络，直接回归 8 个关键点的 (x, y) 坐标（共 16 个标量）。
    *   **Akida V2 架构**：采用编码器-解码器结构，输出 8 个关键点的空间热图（Heatmap），将回归问题转化为空间分类问题。
*   **量化与转换**：
    *   使用**量化感知训练 (QAT)**。Akida V1 采用 4 位权重/激活量化；Akida V2 支持 8 位量化。
    *   利用 BrainChip 工具链将训练好的 CNN 转换为在硬件上运行的脉冲神经网络（SNN）。
*   **姿态恢复**：通过 PnP（Perspective-n-Point）求解器，结合预测的 2D 关键点和已知的 3D 模型恢复 6-DoF 姿态。

### 3. 实验设计
*   **数据集**：使用 **SPADES** 数据集（专门针对航天器姿态估计的事件数据集）。
    *   **规模**：包含约 18 万帧合成数据，分为训练集（10.7万）、验证集（3.5万）和测试集（3.5万）。
*   **基准测试（Benchmark）**：
    *   **硬件对比**：BrainChip Akida AKD1000 SoC (V1) vs. NVIDIA Jetson Orin Nano。
    *   **模型对比**：浮点模型 vs. 量化模型；直接坐标回归 vs. 热图回归。
*   **评估指标**：平移误差 ($E_T$)、旋转误差 ($E_R$)、综合姿态误差 ($E_P$)、关键点正确率 (PCK) 以及姿态拒绝率（Rej）。

### 4. 资源与算力
*   **训练算力**：文中提到使用了 **多 GPU 镜像策略（MirroredStrategy）** 进行训练，但未明确指出具体的 GPU 型号（如 A100 或 RTX 3090）及具体数量。
*   **训练时长**：Akida V1 模型训练了 300 个 Epoch 以确保坐标回归收敛；Akida V2 热图模型仅需 60 个 Epoch 即可达到高精度（PCK > 0.96）。
*   **推理性能**：Akida V1 硬件实现约 17 FPS；Akida V2（云端模拟）在 400MHz 频率下预计可达 37 FPS。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 3 种事件表示、2 种硬件架构版本、2 种回归策略（坐标 vs 热图）以及浮点与量化状态进行了交叉对比实验。
*   **充分性评价**：实验设计较为全面，涵盖了从算法精度到硬件功耗、延迟的端到端评估。通过对比 Jetson 平台，客观展示了类脑硬件在功耗上的巨大优势。
*   **局限性**：虽然 SPADES 包含真实实验数据，但本研究**仅使用了合成数据**进行评估，未在真实物理实验数据上验证模型的泛化能力。

### 6. 主要结论与发现
*   **量化敏感性**：直接坐标回归（V1）对量化极其敏感，4 位量化导致精度大幅下降；而热图回归（V2）对量化具有极强的鲁棒性，8 位量化后的精度几乎无损。
*   **功耗优势**：Akida AKD1000 的动态功耗比 Jetson Orin Nano 低 **3 到 4 倍**（约 900mW vs 2500mW+）。
*   **表示法影响**：在低比特量化下，E2F 和 2DHist 比 LNES 更稳定；但在高容量模型（V2）中，LNES 能提供更好的精度。
*   **实时性**：证明了在类脑硬件上实现实时（>15 FPS）航天器姿态估计的可行性。

### 7. 优点
*   **首创性**：据称是首个在 Akida 类脑硬件上实现端到端航天器姿态估计的演示。
*   **硬件感知设计**：并非简单的算法移植，而是针对 V1/V2 硬件特性优化了网络结构（如从坐标回归转向热图回归）。
*   **高能效比**：成功展示了类脑计算在航天器这种对功耗极度敏感场景下的应用潜力。

### 8. 不足与局限
*   **领域鸿沟（Domain Gap）**：实验完全基于合成数据，缺乏对真实空间环境噪声和传感器非理想特性的实测验证。
*   **接口瓶颈**：Akida V1 的推理速度（17 FPS）远低于 Jetson（300+ FPS），论文指出这主要是由于硬件接口与主机通信未优化，限制了类脑芯片原生速度的发挥。
*   **模型复杂度限制**：为了适配 Akida V1，不得不采用较弱的坐标回归方案，导致在旧版硬件上的精度表现不尽如人意。

（完）
