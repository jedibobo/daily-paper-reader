---
title: "AXELRAM: Quantize Once, Never Dequantize"
title_zh: AXELRAM：一次量化，永不反量化
authors: Yasushi Nishida
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02638v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 无需反量化的量化KV缓存智能SRAM宏架构
tldr: 本研究提出AXELRAM，一种无需反量化即可直接从量化KV缓存计算注意力分数的智能SRAM架构。通过正交变换将数据分布标准化，利用固定码本和查表机制将乘法运算量降低102.4倍。针对部分模型出现的困惑度激增问题，研究揭示了符号模式敏感性这一诱因，并提出一种零硬件成本的无梯度选择方案，显著提升了量化稳定性。
selection_source: fresh_fetch
motivation: 旨在解决KV缓存量化过程中频繁反量化带来的高计算开销和效率瓶颈。
method: 采用基于正交变换的固定码本量化，实现写入时变换、读取时查表且无需逆变换的非对称设计。
result: 实现了102.4倍的乘法运算缩减，并通过符号模式优化消除了Qwen2.5等模型中的灾难性性能下降。
conclusion: AXELRAM通过软硬件协同设计，在极低计算成本下实现了高效且稳定的KV缓存量化处理。
---

## 摘要
我们提出了 AXELRAM，一种智能 SRAM 宏架构，它直接从量化后的 KV 缓存索引计算注意力分数，而无需反量化。其关键实现因素是设计时固定的码本：基于正交变换的量化将每个坐标的分布集中到 N(0,1/d)，因此最优量化器仅取决于维度 d 和位宽 b，而与输入数据无关。这种非对称路径设计——写入时变换，读取时查表且无需逆变换——将每次查询的乘法运算减少了 102.4 倍（数学恒等式）。通过多种子评估（10 个种子 x 3 个模型），我们发现符号模式敏感性在某些模型（Qwen2.5-3B）上会导致灾难性的困惑度（PPL）激增（Delta > 50），而其他模型（LLaMA-3.1-8B）则完全稳定。这一现象将 SpinQuant 在权重量化中观察到的旋转方差扩展到了 KV 缓存领域，且其影响在性质上更为严重。我们将根本原因追溯到层级范数异质性，并提出了一种无梯度的符号模式选择方法（200 个候选，8 个校准样本，一次性完成），在不增加额外硬件成本的情况下消除了灾难性的激增。所有源代码均可在 https://github.com/Axelidea/AXELRAM 获取。

## Abstract
We propose AXELRAM, a smart SRAM macro architecture that computes attention scores directly from quantized KV cache indices without dequantization. The key enabler is a design-time fixed codebook: orthogonal-transform-based quantization concentrates each coordinate's distribution to N(0,1/d), so the optimal quantizer depends only on dimension d and bit-width b, not on input data. The asymmetric path design -- transform on write, table-lookup on read with no inverse transform -- reduces per-query multiplications by 102.4x (a mathematical identity).   Through multi-seed evaluation (10 seeds x 3 models), we discover that sign pattern sensitivity causes catastrophic PPL spikes (Delta > 50) on certain models (Qwen2.5-3B), while others (LLaMA-3.1-8B) are fully stable. This phenomenon extends SpinQuant's observation of rotation variance in weight quantization to the KV cache domain, where the effect is qualitatively more severe. We trace the root cause to layer-wise norm heterogeneity and propose a gradient-free sign pattern selection (200 candidates, 8 calibration samples, one-time) that eliminates catastrophic spikes with zero additional hardware cost. All source code is available at https://github.com/Axelidea/AXELRAM.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **AXELRAM** 的创新硬件架构方案，旨在彻底解决大语言模型（LLM）推理中 KV 缓存（KV Cache）量化带来的计算开销问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在 LLM 推理中，KV 缓存是主要的内存瓶颈。虽然现有的量化技术（如 2-4 bit）能减少存储，但在计算注意力分数（Attention Score）时，必须对每个 Key 进行反量化，这在长文本推理中产生了巨大的计算负担。
*   **研究动机**：作者提出“一次量化，永不反量化”的设想。即能否设计一种 SRAM 架构，直接利用量化后的索引进行计算，从而完全消除反量化过程，提升推理效率。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用正交变换（如 Hadamard 变换）的**内积不变性** $\langle q, k \rangle = \langle Rq, Rk \rangle$。通过在写入 SRAM 前对 Key 进行旋转，在读取时对 Query 进行旋转，使得注意力计算可以在旋转后的域内通过“查表”完成。
*   **关键技术细节**：
    *   **设计时固定码本（Fixed Codebook）**：基于测度集中现象，正交变换后的数据分布趋于标准正态分布 $N(0, 1/d)$。因此，最优量化器（Lloyd-Max）仅取决于维度 $d$ 和位宽 $b$，无需根据输入数据动态调整，码本可预先固化在 ROM 中。
    *   **非对称路径设计**：
        *   **写入路径（Write Path）**：采用快速哈达玛变换（FWHT）和比较器量化，实现**零乘法器**设计。
        *   **读取路径（Read Path）**：Query 仅旋转一次，随后生成一个预计算表（Table）。对于每个 Key，只需根据量化索引查表并累加，最后乘以范数。
    *   **符号模式优化（Sign Pattern Selection）**：针对旋转变换中的符号向量 $s$，提出一种无梯度的选择算法。通过 8 个校准样本从 200 个候选方案中选出使 MSE 最小的符号模式，以应对不同模型的层级范数异质性。

### 3. 实验设计
*   **数据集/场景**：使用 WikiText-2 测试集进行困惑度（PPL）评估，上下文长度设为 2048。
*   **Benchmark 与对比对象**：
    *   **模型**：LLaMA-3.1-8B、Qwen2.5-3B、Qwen3-8B。
    *   **基准**：FP16 精度表现。
    *   **对比维度**：不同位宽（2/3/4-bit）、不同随机种子（10 个种子）下的稳定性。
*   **评估指标**：困惑度增量（$\Delta$ PPL）、乘法运算次数缩减倍数。

### 4. 资源与算力
*   **算力支持**：论文提到部分计算工作在东京科学大学的 **TSUBAME4.0 超级计算机**上完成。
*   **具体消耗**：文中未明确列出具体的 GPU 型号、数量或训练总时长。但指出符号模式优化过程非常轻量，每层仅需约 1 秒，全模型（36 层）仅需约 36 秒。

### 5. 实验数量与充分性
*   **实验规模**：采用了“10 个随机种子 × 3 个模型 × 3 种位宽”的多维度评估。
*   **充分性与客观性**：
    *   实验设计较为充分，特别是通过多周知评估发现了前人（如 TurboQuant）单种子实验中未察觉的“灾难性 PPL 激增”现象。
    *   对比了不同架构模型（LLaMA 与 Qwen）的差异，揭示了层级范数异质性对量化稳定性的影响，具有较高的客观性。

### 6. 主要结论与发现
*   **计算效率**：AXELRAM 将每次查询的乘法运算次数减少了 **102.4 倍**（从 524,288 次降至 5,120 次）。
*   **符号敏感性发现**：首次揭示了 KV 缓存量化中的符号模式敏感性。在 Qwen2.5-3B 上，某些随机种子会导致 PPL 激增超过 50 点，而 LLaMA 则表现稳定。
*   **根因分析**：性能激增源于模型层间范数的巨大差异（Qwen 的范数比例高达 7.8×，而 LLaMA 仅为 1.5×）。
*   **优化效果**：提出的无梯度符号模式选择方案能消除 99% 的灾难性性能下降，且不增加硬件成本。

### 7. 优点
*   **软硬协同创新**：通过数学恒等式巧妙地将计算转移到旋转域，实现了极高的硬件效率。
*   **零硬件额外成本**：优化方案仅需更改 ROM 中存储的符号位，无需修改电路架构。
*   **高压缩比**：在 3-bit 量化下实现了 5.1× 的 KV 缓存压缩，且几乎无精度损失（在 LLaMA 上）。

### 8. 不足与局限
*   **硬件实现缺失**：目前所有结果均基于**位对齐的软件模拟**，尚未有实际的物理芯片流片验证。
*   **极低位宽限制**：在 2-bit 量化下，部分模型（如 Qwen3-8B）即使经过优化仍存在明显的精度损失（$\Delta$ PPL > 9），这反映了标量量化的精度极限。
*   **应用范围**：该方案主要针对集成 SRAM 的专用加速器架构，对于通用的 GPU 软件推理框架，其加速效果可能受限于现有的指令集。

（完）
