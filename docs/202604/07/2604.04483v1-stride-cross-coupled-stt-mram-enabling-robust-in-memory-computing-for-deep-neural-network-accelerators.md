---
title: "STRIDe: Cross-Coupled STT-MRAM Enabling Robust In-Memory-Computing for Deep Neural Network Accelerators"
title_zh: STRIDe：支持深度神经网络加速器鲁棒存内计算的交叉耦合 STT-MRAM
authors: "Imtiaz Ahmed, Sumeet Kumar Gupta"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04483v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对鲁棒存内计算DNN加速器的软硬件协同设计
tldr: 针对深度神经网络在边缘设备部署中面临的能效挑战，本文提出 STRIDe，一种基于交叉耦合 STT-MRAM 的存内计算（IMC）架构。通过引入交叉耦合机制，STRIDe 显著提升了位单元的高低电流比，解决了传统 STT-MRAM 区分度低和鲁棒性差的问题。该设计支持 XNOR 和 AND 运算，在显著提升感测裕度和读取干扰裕度的同时，实现了接近软件水平的推理精度，为高鲁棒性非易失性存内计算提供了新方案。
selection_source: fresh_fetch
motivation: 传统 STT-MRAM 存内计算因其固有的低区分度和工艺偏差导致鲁棒性差，严重影响了深度神经网络的推理精度。
method: 提出 STRIDe 架构，利用交叉耦合作用将位单元高低电流比提升至 8000，并设计了支持 XNOR 和 AND 运算的两种电路变体。
result: "实验显示感测裕度提升高达 3.86 倍，读取干扰裕度提升 27.6%，在 CIFAR10 数据集上的推理精度较标准 MRAM 设计提升了 35% 至 70%。"
conclusion: STRIDe 通过牺牲一定的能效和面积，换取了极高的存内计算鲁棒性和推理精度，是实现可靠边缘 AI 加速的有效途径。
---

## 摘要
随着深度神经网络（DNN）模型规模呈指数级增长，将其部署在资源受限的边缘平台上正变得日益困难。与标准 DNN 硬件平台相比，基于非易失性存储器（NVM）的存内计算（IMC）凭借其更高的能效，已成为一种潜在的解决方案。在各种 NVM 中，STT-MRAM 因其高耐用性和其他优势而极具前景。然而，由于其固有的低区分度，其 IMC 实现面临挑战。阵列非理想性和工艺偏差加剧了这一问题，导致 IMC 鲁棒性差和推理精度严重下降。为解决这一问题，我们提出了 STRIDe——一种基于 STT-MRAM 的 IMC，利用交叉耦合作用将位单元级的高低电流比提升至高达 8000。我们提出了两种 STRIDe 设计方案，均能为 {-1, 1}（XNOR-IMC）和 {0, 1}（AND-IMC）模式下的输入和权重提供鲁棒的 IMC。我们对 STRIDe 阵列的评估显示，与标准 MRAM-IMC 设计相比，XNOR-IMC 和 AND-IMC 的感知裕度（SM）分别提高了 3.86 倍和 1.77 倍，读取干扰裕度（RDM）提高了 27.6%。STRIDe 增强的鲁棒性使得在 CIFAR10 数据集上训练的 ResNet18 BNN 和 4 位 DNN 能够达到接近软件的推理精度（考虑了交叉阵列非理想性和工艺偏差）。与标准 MRAM 设计相比，我们观察到精度提升高达 70%（对于 BNN）和 35%（对于 4 位 DNN），尽管在能量、面积和延迟方面存在一定的开销。

## Abstract
As deep neural network (DNN) models are growing exponentially in size, their deployment on resource-constrained edge platforms is becoming increasingly challenging. In-memory-computing (IMC) with non-volatile memories (NVMs) has emerged as a potential solution by virtue of its higher energy efficiency compared to standard DNN hardware platforms. Amongst various NVMs, STT-MRAM is highly promising owing to its high endurance and other benefits. However, their IMC implementation is challenging because of their inherently low distinguishability. This issue is exacerbated due to array non-idealities and process-variations, leading to poor IMC robustness and severe inference accuracy degradation. To address this problem, we propose STRIDe - STT-MRAM-based IMC leveraging cross-coupling action to boost the bitcell-level high-to-low current ratio to up to 8000. We propose two flavors of STRIDe designs, both offering robust IMC for inputs and weights in {-1, 1}(XNOR-IMC) and {0, 1}(AND-IMC) regime. Our evaluations for STRIDe arrays show up to 3.86x and 1.77x sense margin (SM) improvement for XNOR-IMC and AND-IMC, respectively, and up to 27.6% read disturb margin (RDM) improvement over standard MRAM-IMC designs. The enhanced robustness of STRIDe translates to near-software inference accuracies (considering crossbar non-idealities and process variations) for ResNet18 BNN and 4-bit DNN trained on CIFAR10 dataset. We observe accuracy improvements of up to 70% (for BNN) and up to 35%(for 4-bit DNN) over standard MRAM designs, albeit with some energy-area-latency penalty.

---

## 论文详细总结（自动生成）

这是一份关于论文《STRIDe: Cross-Coupled STT-MRAM Enabling Robust In-Memory-Computing for Deep Neural Network Accelerators》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：深度神经网络（DNN）规模巨大，在边缘设备部署面临“冯·诺依曼瓶颈”。基于非易失性存储器（NVM）的存内计算（IMC）能显著降低功耗。
*   **核心问题**：STT-MRAM 虽然具有高耐用性和 CMOS 兼容性，但其**磁阻比（TMR）较低**，导致逻辑“0”和“1”状态的电流区分度（Distinguishability）极差。在存内计算阵列中，这种低区分度会被阵列非理想性（如 IR 压降）和工艺偏差（PV）进一步放大，导致计算错误和推理精度大幅下降。
*   **研究动机**：开发一种能从位单元（Bitcell）层面根本性提升电流区分度的 MRAM 设计，以实现鲁棒的存内计算。

### 2. 论文提出的方法论
*   **核心思想**：提出 **STRIDe** 架构，利用**交叉耦合（Cross-coupling）**机制。在每个位单元中使用两个磁隧道结（MTJ）存储互补权重，通过晶体管的交叉耦合反馈作用，局部放大高低电流比（$I_H/I_L$）。
*   **关键技术细节**：
    *   **STRIDe-I 与 II**：设计了两种变体。STRIDe-I 拥有独立的写入管，写入速度快；STRIDe-II 结构更紧凑（面积小 11%），但需要两周期写入。
    *   **电流放大原理**：读取时，由于两个 MTJ 电阻不同，节点充电速度产生差异。较快充电的节点会开启对应的交叉耦合管，同时抑制另一支路的电流，从而将 $I_H/I_L$ 从标准 MRAM 的约 5 倍提升至最高 **8156**（STRIDe-I）和 **3932**（STRIDe-II）。
    *   **计算模式**：支持 **XNOR-IMC**（用于二值神经网络 BNN，输入/权重为 {-1, 1}）和 **AND-IMC**（用于多位 DNN，输入/权重为 {0, 1}）。
*   **算法流程**：通过特定的输入编码（将 -1 映射为 0V，1 映射为 $V_{DD}$）和输出后处理（线性变换），在电流域完成矩阵向量乘法（MVM）。

### 3. 实验设计
*   **数据集**：CIFAR10。
*   **模型场景**：ResNet18 结构的二值神经网络（BNN）和 4-bit 量化神经网络（DNN）。
*   **Benchmark（对比方法）**：
    1.  **1T-1MTJ**：标准单管单结设计（带哑列补偿以增强鲁棒性）。
    2.  **2T-2MTJ**：标准差分设计（无交叉耦合）。
*   **评估指标**：感测裕度（Sense Margin, SM）、读取干扰裕度（RDM）、推理精度、能量-面积-延迟开销。

### 4. 资源与算力
*   **仿真工具**：使用 **HSPICE** 进行电路级仿真（基于 45nm 预测技术模型和 PMA MTJ 模型）。
*   **系统评估**：开发了基于 **PyTorch** 的自定义交叉阵列求解器，通过 KVL/KCL 自洽迭代模拟硬件非理想性。
*   **算力说明**：文中未明确提及具体的 GPU 型号或训练时长，实验重点在于**推理阶段**的硬件行为模拟而非模型训练过程。

### 5. 实验数量与充分性
*   **实验规模**：进行了 64×64 规模的阵列仿真。
*   **变体分析**：对比了部分字线激活（PWA）数量为 8 和 16 的情况。
*   **鲁棒性测试**：进行了 **1000 次蒙特卡洛（Monte Carlo）模拟**，考虑了晶体管阈值电压偏差（$\sigma=25mV$）、MTJ 氧化层厚度偏差（1.5%）和尺寸偏差（5%）。
*   **充分性评价**：实验覆盖了从单元级特性到阵列级裕度，再到系统级精度的全流程，对比基准选择合理，考虑了实际硬件中的非理想因素，实验设计较为客观公平。

### 6. 论文的主要结论与发现
*   **区分度极大化**：STRIDe 将电流比提升了几个数量级，显著增强了对 IR 压降的容忍度。
*   **裕度提升**：相比标准设计，感测裕度提升高达 **3.86 倍**，读取干扰裕度提升 **27.6%**。
*   **精度恢复**：在考虑工艺偏差的情况下，STRIDe 在 BNN 上的精度比标准 MRAM 提升了约 **70%**，在 4-bit DNN 上提升了约 **35%**，达到了接近软件（Near-software）的水平。
*   **并行度提升**：由于鲁棒性增强，STRIDe 允许同时开启更多字线（如 PWA=16），从而降低计算延迟。

### 7. 优点（亮点）
*   **底层创新**：从位单元电路拓扑入手，解决了 MRAM 存内计算最核心的低 TMR 痛点。
*   **高鲁棒性**：极高的高低电流比使得电路对工艺偏差和电压降极不敏感，这对于大规模阵列至关重要。
*   **多功能性**：一套电路同时兼容 XNOR 和 AND 两种主流存内计算逻辑。

### 8. 不足与局限
*   **硬件开销**：位单元面积是标准 1T-1MTJ 的 **2.67 到 3 倍**，虽然在宏观层面（含外围电路）开销降至 10%-16%，但仍存在面积惩罚。
*   **写入效率**：写入延迟和能量比标准单元高出约 40%-60%，虽然论文辩称在“权重平稳”架构下写入不频繁，但这限制了其在频繁更新权重的场景下的应用。
*   **电路复杂性**：交叉耦合结构增加了电路设计的复杂度和对读取电压（$V_{READ}$）窗口的精确控制要求。

（完）
