---
title: "Fast-dVLM: Efficient Block-Diffusion VLM via Direct Conversion from Autoregressive VLM"
title_zh: Fast-dVLM：通过从自回归 VLM 直接转换实现的高效块扩散视觉语言模型
authors: "Chengyue Wu, Shiyi Lan, Yonggan Fu, Sensen Gao, Jin Wang, Jincheng Yu, Jose M. Alvarez, Pavlo Molchanov, Ping Luo, Song Han, Ligeng Zhu, Enze Xie"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06832v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 解决边缘设备上的内存带宽受限和硬件并行度利用不足问题
tldr: 针对自回归视觉语言模型（VLM）推理速度受限的问题，本文提出 Fast-dVLM。该模型通过将预训练的自回归 VLM 直接转换为块扩散模型，实现了并行解码。通过引入块大小退火、因果上下文注意力和自动截断掩码等技术，Fast-dVLM 在保持与自回归模型相当的生成质量的同时，在边缘设备上实现了超过 6 倍的推理加速，显著提升了多模态任务的效率。
selection_source: fresh_fetch
motivation: 自回归解码逐个生成 token 导致推理吞吐量低，且在边缘设备上受限于内存带宽，无法充分利用硬件并行性。
method: 提出一种直接将预训练 AR VLM 转换为块扩散模型的方法，并结合块大小退火、因果上下文注意力和视觉高效拼接等优化技术。
result: 在 11 个多模态基准测试中，Fast-dVLM 达到了与自回归基准相当的生成质量，并实现了超过 6 倍的端到端推理加速。
conclusion: 直接转换策略是构建高效块扩散 VLM 的有效途径，能在不损失性能的前提下大幅提升多模态推理速度。
---

## 摘要
视觉语言模型 (VLM) 主要依赖自回归解码，这种方式一次生成一个 token，从根本上限制了推理吞吐量。这种限制在机器人和自动驾驶等物理 AI 场景中尤为突出，在这些场景中，VLM 以 batch size 为 1 的形式部署在边缘设备上，使得自回归 (AR) 解码受限于内存带宽，导致硬件并行性未得到充分利用。虽然块级离散扩散在并行文本生成方面展现出潜力，但将其扩展到 VLM 仍具挑战性，因为需要在保留预训练多模态能力的同时，共同处理连续的视觉表示和离散的文本 token。我们提出了 Fast-dVLM，这是一种基于块扩散的 VLM，支持与 KV 缓存兼容的并行解码和投机块解码，以实现推理加速。我们系统地比较了两种从 AR 到扩散的转换策略：一种是两阶段方法，即在多模态训练之前先通过纯文本扩散微调来适配 LLM 主干网络；另一种是直接方法，即在一个阶段内转换完整的 AR VLM。在相当的训练预算下，直接转换通过利用已经实现多模态对齐的 VLM，证明了其效率显著更高；因此，我们将其作为推荐方案。我们引入了一系列多模态扩散适配技术，包括块大小退火、因果上下文注意力、自动截断掩码和视觉高效拼接，这些技术共同实现了 VLM 环境下的有效块扩散。在 11 个多模态基准测试上的广泛实验表明，Fast-dVLM 在生成质量上与对应的自回归模型相当。通过集成 SGLang 和 FP8 量化，Fast-dVLM 相比 AR 基准实现了超过 6 倍的端到端推理加速。

## Abstract
Vision-language models (VLMs) predominantly rely on autoregressive decoding, which generates tokens one at a time and fundamentally limits inference throughput. This limitation is especially acute in physical AI scenarios such as robotics and autonomous driving, where VLMs are deployed on edge devices at batch size one, making AR decoding memory-bandwidth-bound and leaving hardware parallelism underutilized. While block-wise discrete diffusion has shown promise for parallel text generation, extending it to VLMs remains challenging due to the need to jointly handle continuous visual representations and discrete text tokens while preserving pretrained multimodal capabilities. We present Fast-dVLM, a block-diffusion-based VLM that enables KV-cache-compatible parallel decoding and speculative block decoding for inference acceleration. We systematically compare two AR-to-diffusion conversion strategies: a two-stage approach that first adapts the LLM backbone with text-only diffusion fine-tuning before multimodal training, and a direct approach that converts the full AR VLM in one stage. Under comparable training budgets, direct conversion proves substantially more efficient by leveraging the already multimodally aligned VLM; we therefore adopt it as our recommended recipe. We introduce a suite of multimodal diffusion adaptations, block size annealing, causal context attention, auto-truncation masking, and vision efficient concatenation, that collectively enable effective block diffusion in the VLM setting. Extensive experiments across 11 multimodal benchmarks show Fast-dVLM matches its autoregressive counterpart in generation quality. With SGLang integration and FP8 quantization, Fast-dVLM achieves over 6x end-to-end inference speedup over the AR baseline.

---

## 论文详细总结（自动生成）

### 论文总结：Fast-dVLM —— 通过直接转换实现的高效块扩散视觉语言模型

#### 1. 核心问题与整体含义
当前视觉语言模型（VLM）普遍采用**自回归（AR）解码**，即逐个生成 token。这种方式在机器人、自动驾驶等“物理 AI”场景中存在严重瓶颈：边缘设备通常以 Batch Size 为 1 运行，推理受限于内存带宽，导致硬件并行计算能力被大量闲置。

本文提出了 **Fast-dVLM**，旨在将预训练的自回归 VLM 转换为**块扩散（Block-Diffusion）模型**。其核心意义在于通过并行生成一个块（Block）内的多个 token，将推理从带宽受限转向计算受限，从而大幅提升吞吐量，同时保持模型原有的多模态理解能力。

#### 2. 方法论
Fast-dVLM 的核心思想是利用块级离散扩散实现并行解码，并支持 KV 缓存。

*   **直接转换策略（Direct Conversion）**：研究对比了“两阶段法”（先转 LLM 为扩散模型再训练多模态）和“直接法”（直接对预训练 VLM 进行多模态扩散微调）。结论是**直接法效率更高**，因为它能更好地继承已有的多模态对齐知识。
*   **关键技术细节**：
    *   **块大小退火（Block-size Annealing）**：训练初期使用小块，随进度逐渐增加块大小，帮助模型学习从细粒度到粗粒度的去噪。
    *   **因果上下文注意力（Causal Context Attention）**：在块内使用双向注意力进行并行去噪，但对历史上下文保留因果注意力，以维持预训练模型的序列推理能力。
    *   **自动截断掩码（Auto-truncation Masking）**：在多轮对话中，自动在回复边界截断块，防止去噪过程泄露下一轮的 Prompt 信息。
    *   **视觉高效拼接（Vision-efficient Concatenation）**：由于视觉 token 不会被掩码/去噪，因此仅在“清晰流”中保留，避免在“噪声流”中重复计算，节省约 15% 的显存。
    *   **自投机块解码（Self-speculative Block Decoding）**：利用模型自身的扩散模式作为草稿（Draft），因果模式进行验证（Verify），进一步提升速度和质量。

#### 3. 实验设计
*   **基准模型**：以 **Qwen2.5-VL-3B** 为主干网络。
*   **数据集**：使用了约 200 万条多模态指令微调数据，涵盖 ShareGPT-4V、ChartQA、DocVQA、AI2D 等。
*   **评测基准**：在 11 个多模态基准上进行测试，包括：
    *   **短答案任务**：AI2D, ChartQA, DocVQA, GQA, MMBench, MMMU, POPE, RealWorldQA, SEEDBench2+, TextVQA。
    *   **长答案/推理任务**：MMMU-Pro-V（包含思维链推理）。
*   **对比方法**：对比了原始 AR 基准以及现有的扩散 VLM（如 LLaDA-V, LaViDa, Dimple 等）。

#### 4. 资源与算力
*   **硬件**：使用了 **64 块 NVIDIA H100 GPU**（8 个节点）。
*   **训练配置**：采用 DeepSpeed ZeRO-2 优化器，BF16 混合精度。
*   **时长**：训练 1 个 Epoch 以防止过拟合。
*   **推理环境**：在单块 H100 上测量 Batch Size 为 1 的吞吐量。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **广泛的基准测试**：覆盖了从基础视觉问答到复杂文档理解的 11 个主流榜单。
*   **消融实验**：系统地验证了因果上下文、块大小退火、自动截断等每个组件的贡献。
*   **策略对比**：深入探讨了“两阶段”与“直接转换”的优劣，为后续研究提供了明确的 Recipe。
*   **系统级验证**：不仅在算法层面测试，还集成了 SGLang 推理引擎和 FP8 量化，验证了端到端的工程可行性。

#### 6. 主要结论与发现
*   **性能对齐**：Fast-dVLM 在短答案任务上几乎无损地达到了 AR 模型的精度，在长推理任务（MMMU-Pro-V）上通过投机解码也接近了 AR 水平。
*   **转换效率**：直接从预训练 VLM 转换比从纯文本 LLM 开始转换效果好得多，证明了多模态对齐知识的鲁棒性。
*   **推理加速**：结合 SGLang 优化和 FP8 量化，Fast-dVLM 实现了 **6.18 倍** 的端到端推理加速（从 56.7 tok/s 提升至 350.3 tok/s）。

#### 7. 优点
*   **工程实用性极强**：兼容 KV 缓存，且能直接集成到 SGLang 等生产级推理引擎中。
*   **训练高效**：提出的“直接转换”路径大幅缩短了从 AR 到扩散模型的适配周期。
*   **创新的自投机机制**：巧妙利用模型内部两种注意力模式的切换，无需额外的小模型即可实现投机采样。

#### 8. 不足与局限
*   **长文本推理差距**：在需要极长思维链（CoT）的任务中，块并行去噪与纯序列自回归之间仍存在微小精度差距（约 1.7%）。
*   **训练数据依赖**：虽然转换效率高，但仍需要一定规模（2M）的高质量多模态数据进行微调。
*   **硬件优化依赖**：虽然理论 NFE（前向计算次数）减少，但实际加速高度依赖于针对非标准注意力掩码优化的算子（如 SGLang 中的实现）。

（完）
