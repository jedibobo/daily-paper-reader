---
title: "FaceLiVTv2: An Improved Hybrid Architecture for Efficient Mobile Face Recognition"
title_zh: FaceLiVTv2：一种用于高效移动端人脸识别的改进混合架构
authors: "Novendra Setyawan, Chi-Chia Sun, Mao-Hsiu Hsu, Wen-Kai Kuo, Jun-Wei Hsieh"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09127v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 适用于移动和边缘设备的高效混合架构
tldr: 针对移动端人脸识别对低延迟和高精度的双重需求，本文提出FaceLiVTv2。该架构改进了混合CNN-Transformer设计，核心是Lite MHLA模块，通过线性投影和仿射变换替代复杂注意力机制，并结合RepMix块优化局部与全局特征交互。实验证明，FaceLiVTv2在保持高精度的同时，显著降低了推理延迟，是高效实用的移动端人脸识别方案。
selection_source: fresh_fetch
motivation: 现有的混合架构在移动端部署时难以在全局建模能力与计算效率之间取得理想平衡。
method: 提出Lite MHLA模块简化全局交互，并将其集成到RepMix块中，配合全局深度卷积实现高效的局部与全局特征融合。
result: "在多个主流数据集上，FaceLiVTv2比前代提速22%，且在性能和速度上均优于GhostFaceNets、EdgeFace等先进轻量级模型。"
conclusion: FaceLiVTv2通过优化混合架构设计，为实时移动端人脸识别提供了一个兼顾高精度与低延迟的部署方案。
---

## 摘要
轻量级人脸识别对于在边缘和移动设备上的部署日益重要，这些设备在满足可靠准确性的同时，还必须满足对延迟、内存和能耗的严格限制。尽管最近的 CNN-Transformer 混合架构在全局上下文建模方面取得了进展，但在识别性能和计算效率之间取得有效平衡仍是一个开放性挑战。在这项工作中，我们提出了 FaceLiVTv2，这是我们 FaceLiVT 混合架构的改进版本，旨在实现移动端人脸识别中高效的全局-局部特征交互。其核心是 Lite MHLA，这是一个轻量级的全局 Token 交互模块，它用多头线性 Token 投影和仿射缩放变换取代了原始的多层注意力设计，在减少冗余的同时保留了不同头之间的表示多样性。我们进一步将 Lite MHLA 集成到一个统一的 RepMix 模块中，该模块协调局部和全局特征交互，并在嵌入阶段采用全局深度可分离卷积进行自适应空间聚合。在我们的实验设置下，在 LFW、CA-LFW、CP-LFW、CFP-FP、AgeDB-30 和 IJB 上的结果表明，FaceLiVTv2 在准确率与效率的权衡上始终优于现有的轻量级方法。值得注意的是，FaceLiVTv2 相比 FaceLiVTv1 减少了 22% 的移动端推理延迟，在移动设备上比 GhostFaceNets 实现了高达 30.8% 的加速，并且在保持更高识别准确率的同时，在不同平台上比 EdgeFace 和 KANFace 实现了 20-41% 的延迟改进。这些结果表明，FaceLiVTv2 为实时人脸识别提供了一种实用且可部署的解决方案。代码可在 https://github.com/novendrastywn/FaceLiVT 获取。

## Abstract
Lightweight face recognition is increasingly important for deployment on edge and mobile devices, where strict constraints on latency, memory, and energy consumption must be met alongside reliable accuracy. Although recent hybrid CNN-Transformer architectures have advanced global context modeling, striking an effective balance between recognition performance and computational efficiency remains an open challenge. In this work, we present FaceLiVTv2, an improved version of our FaceLiVT hybrid architecture designed for efficient global--local feature interaction in mobile face recognition. At its core is Lite MHLA, a lightweight global token interaction module that replaces the original multi-layer attention design with multi-head linear token projections and affine rescale transformations, reducing redundancy while preserving representational diversity across heads. We further integrate Lite MHLA into a unified RepMix block that coordinates local and global feature interactions and adopts global depthwise convolution for adaptive spatial aggregation in the embedding stage. Under our experimental setup, results on LFW, CA-LFW, CP-LFW, CFP-FP, AgeDB-30, and IJB show that FaceLiVTv2 consistently improves the accuracy-efficiency trade-off over existing lightweight methods. Notably, FaceLiVTv2 reduces mobile inference latency by 22% relative to FaceLiVTv1, achieves speedups of up to 30.8% over GhostFaceNets on mobile devices, and delivers 20-41% latency improvements over EdgeFace and KANFace across platforms while maintaining higher recognition accuracy. These results demonstrate that FaceLiVTv2 offers a practical and deployable solution for real-time face recognition. Code is available at https://github.com/novendrastywn/FaceLiVT.

---

## 论文详细总结（自动生成）

### FaceLiVTv2：用于高效移动端人脸识别的改进混合架构论文总结

#### 1. 论文的核心问题与整体含义
随着人脸识别（FR）技术向移动端和边缘设备迁移，如何在极低的延迟、内存和能耗约束下保持高精度的识别性能成为核心挑战。现有的轻量级 CNN 缺乏全局建模能力，而传统的 Vision Transformer（ViT）计算开销过大。本文提出的 **FaceLiVTv2** 旨在通过改进的 CNN-Transformer 混合架构，在保持移动端实时推理速度的同时，提升对复杂姿态、年龄变化及低分辨率场景的识别精度。

#### 2. 论文提出的方法论
FaceLiVTv2 采用了金字塔型架构，核心改进包含以下三个关键技术：
*   **Lite MHLA（轻量级多头线性注意力）：** 针对前代 MHLA 存在的冗余问题，Lite MHLA 移除了复杂的 Query-Key 交互和非线性激活函数，改用纯线性的 Token 投影。它引入了**仿射缩放变换（Affine Rescale Transformation）**来稳定训练并校准特征分布，在显著降低计算复杂度的同时保留了多头表示的多样性。
*   **RepMix（重参数化 Token 混合器）：** 用于局部特征提取。在训练阶段使用 3x3 和 1x1 的深度卷积分支以增强梯度传播；在推理阶段，通过**结构重参数化**将多个分支合并为一个等效的 3x3 卷积，从而在不损失精度的情况下降低推理延迟。
*   **GDConv Head（全局深度卷积头）：** 取代了传统的全局平均池化（GAP）。通过深度卷积实现通道级的自适应空间聚合，使模型能够自动关注更具辨别力的面部区域，提升了开放集人脸识别的性能。

#### 3. 实验设计
*   **数据集：** 涵盖了 8 个主流基准测试，包括 LFW（标准）、CA-LFW（跨年龄）、CP-LFW（跨姿态）、CFP-FP（正侧脸）、AgeDB-30（年龄）、IJB-B/C（大规模挑战性场景）以及 TinyFace（原生低分辨率）。
*   **对比方法：** 包括轻量级 CNN（MobileFaceNet, GhostFaceNets, VarGFaceNet）、混合架构（EdgeFace, KANFace, SwiftFaceFormer）以及大型 SOTA 模型（ResNet200-TopoFR, TransFace）。
*   **测试平台：** 在 iPhone 15 Pro (CoreML)、Jetson AGX Orin (边缘端)、Intel i5 CPU 和 RTX 5090 GPU 上进行了跨平台延迟评估。

#### 4. 资源与算力
*   **训练设备：** 使用了 3 台 **NVIDIA RTX A6000 GPU** 进行分布式训练。
*   **训练参数：** 在 Glint360K 数据集（约 1700 万张图像）上训练 50 个 epoch，总 Batch Size 为 1026（补充材料中提到单卡为 342 或 384）。
*   **优化器：** 使用 AdamW 优化器，配合多项式衰减学习率调度。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验：
*   **性能对比：** 在 8 个数据集上与超过 15 种 SOTA 模型进行了对比。
*   **消融实验：** 针对架构组件（RepMix, Lite MHLA）、块配置（不同阶段的组合）、损失函数（ArcFace vs CosFace）、Lite MHLA 的头数、归一化方式及激活函数等进行了多维度测试。
*   **充分性评价：** 实验设计客观、公平，不仅对比了理论上的 FLOPs，还实测了多个硬件平台上的真实延迟，充分验证了模型在实际部署中的优越性。

#### 6. 论文的主要结论与发现
*   **效率提升：** FaceLiVTv2 相比前代 v1 延迟降低了 **22%**；在移动端比 GhostFaceNets 快 **30.8%**；比 EdgeFace 和 KANFace 提速 **20-41%**。
*   **精度领先：** 在保持轻量化的同时，其精度在多个基准上刷新了轻量级模型的记录，甚至在 IJB-C 等复杂任务上逼近或超越了参数量大几十倍的巨型模型（如 ResNet200）。
*   **线性有效性：** 实验发现，在特定的全局交互模块中，移除激活函数的纯线性映射配合仿射变换，比复杂的非线性注意力机制更适合轻量级模型。

#### 7. 优点
*   **部署友好：** 深度结合了结构重参数化技术，使模型在推理时极度精简。
*   **自适应聚合：** GDConv Head 的引入解决了传统池化丢失局部关键信息的问题。
*   **跨平台鲁棒性：** 论文提供了详尽的硬件运行报告，证明了其在 CPU、GPU 及 NPU（苹果神经网络引擎）上的高效性。

#### 8. 不足与局限
*   **极端环境测试不足：** 论文未显式评估模型在极端遮挡（如口罩、严重截断）下的表现。
*   **人口统计学偏差：** 未对不同种族、性别等人口统计学特征下的公平性进行深入分析。
*   **低分辨率依赖微调：** 虽然在 TinyFace 上表现良好，但仍需针对低分辨率数据进行微调，缺乏一个统一的、无需微调的超分辨率增强识别方案。

（完）
