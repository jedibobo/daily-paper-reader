---
title: "Ge$^\\text{2}$mS-T: Multi-Dimensional Grouping for Ultra-High Energy Efficiency in Spiking Transformer"
title_zh: Ge$^2$mS-T：脉冲 Transformer 中实现超高能效的多维度分组
authors: "Zecheng Hao, Shenghao Xie, Kang Chen, Wenxuan Liu, Zhaofei Yu, Tiejun Huang"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08894v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 脉冲Transformer中内存、精度和能耗的并发优化
tldr: 针对脉冲视觉Transformer（S-ViT）在训练与推理中面临的内存、精度和能耗难以兼顾的问题，本文提出了Ge2mS-T架构。该架构通过在时间、空间和网络结构维度引入多维分组计算，利用ExpG-IF模型实现无损转换并降低训练开销，同时通过GW-SSA机制降低计算复杂度。实验证明，该方法在保持超效能的同时，在多个基准测试上达到了领先性能，是首个系统性解决S-ViT三元矛盾的工作。
selection_source: fresh_fetch
motivation: 现有的S-ViT训练范式（如ANN-SNN转换和STBP）在内存占用、模型精度和能源消耗之间存在难以调和的固有局限。
method: 提出了多维分组计算架构，包含支持无损转换的ExpG-IF神经元模型和基于多尺度Token分组的GW-SSA自注意力机制。
result: 在多个挑战性基准测试中，该方法以极高的能源效率实现了卓越的性能表现，有效平衡了计算开销与学习能力。
conclusion: Ge2mS-T通过系统性的多维分组策略，为构建高效且高性能的脉冲视觉Transformer提供了全新的解决路径。
---

## 摘要
脉冲神经网络 (SNN) 相比人工神经网络 (ANN) 具有更优越的能效。然而，当应用于脉冲视觉 Transformer (S-ViT) 时，它们在训练和推理指标上存在显著不足。包括 ANN-SNN 转换和时空反向传播 (STBP) 在内的现有范式受到固有局限性的制约，无法同时优化内存、准确率和能耗。为解决这些问题，我们提出了 Ge$^2$mS-T，这是一种在时间、空间和网络结构维度上实现分组计算的新型架构。具体而言，我们引入了基于分组指数编码的 IF (ExpG-IF) 模型，实现了具有恒定训练开销的无损转换，并能对脉冲模式进行精确调节。此外，我们开发了分组脉冲自注意力 (GW-SSA) 机制，通过混合注意力-卷积框架内的多尺度 Token 分组和无乘法操作来降低计算复杂度。实验证实，我们的方法在具有挑战性的基准测试中能够以超高能效实现卓越性能。据我们所知，这是首项系统性建立多维度分组计算的工作，旨在解决 S-ViT 中内存开销、学习能力和能源预算之间的三难困境。

## Abstract
Spiking Neural Networks (SNNs) offer superior energy efficiency over Artificial Neural Networks (ANNs). However, they encounter significant deficiencies in training and inference metrics when applied to Spiking Vision Transformers (S-ViTs). Existing paradigms including ANN-SNN Conversion and Spatial-Temporal Backpropagation (STBP) suffer from inherent limitations, precluding concurrent optimization of memory, accuracy and energy consumption. To address these issues, we propose Ge$^\text{2}$mS-T, a novel architecture implementing grouped computation across temporal, spatial and network structure dimensions. Specifically, we introduce the Grouped-Exponential-Coding-based IF (ExpG-IF) model, enabling lossless conversion with constant training overhead and precise regulation for spike patterns. Additionally, we develop Group-wise Spiking Self-Attention (GW-SSA) to reduce computational complexity via multi-scale token grouping and multiplication-free operations within a hybrid attention-convolution framework. Experiments confirm that our method can achieve superior performance with ultra-high energy efficiency on challenging benchmarks. To our best knowledge, this is the first work to systematically establish multi-dimensional grouped computation for resolving the triad of memory overhead, learning capability and energy budget in S-ViTs.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Ge$^2$mS-T**，旨在通过多维度分组计算解决脉冲视觉 Transformer（S-ViT）在内存占用、推理精度和能源消耗这三个维度上的固有矛盾。

### 1. 核心问题与研究动机
*   **核心问题**：脉冲神经网络（SNN）虽具高能效潜力，但在应用于 Transformer 架构（S-ViT）时面临“三难困境”：
    1.  **ANN-SNN 转换法**：内存开销低，但推理延迟高，且因包含浮点乘法而非纯脉冲驱动，导致能效优势无法完全发挥。
    2.  **STBP 训练法**：支持原生脉冲推理，但训练内存随时间步线性增长，且受代理梯度误差影响，精度受限。
    3.  **计算复杂度**：S-ViT 的自注意力机制（SSA）和前馈网络（SFFN）在推理时的突触操作数（SOPs）巨大，能耗极高。
*   **研究动机**：通过在时间、空间和网络结构三个维度引入分组计算，实现超高能效且高性能的 S-ViT。

### 2. 提出的方法论
核心思想是**多维度分组（Multi-Dimensional Grouping）**：
*   **时间维度：ExpG-IF 模型**
    *   引入基于分组指数编码的 IF 神经元（ExpG-IF）。
    *   利用非均匀指数化量化思想，将脉冲序列映射到指数编码空间。
    *   **技术细节**：通过模拟非均匀量化的激活函数进行训练，实现 $O(1)$ 的恒定训练内存开销，并支持无损转换。它能精确调节脉冲发放模式，使神经元仅在特定时间步子集发放，从而降低脉冲频率。
*   **空间维度：GW-SSA（分组脉冲自注意力）**
    *   **核心思想**：对脉冲 Token 进行多尺度分组。
    *   **技术细节**：将通道分为全局分支和窗口分支。全局分支通过跨步采样实现近似全局注意力，窗口分支则在局部区域计算。这种分组机制将计算复杂度从 $O(N^2C)$ 降低到 $O(N^2C/|G_s|)$。
*   **结构维度：混合架构与无乘法操作**
    *   结合了脉冲卷积（SConv）和自注意力（SSA）。
    *   采用 Conv-SFFN（深度可分离卷积结构）提取局部特征。
    *   所有操作均为无乘法（Multiplication-free），完全支持原生 SNN 推理。

### 3. 实验设计
*   **数据集**：
    *   **静态图像**：ImageNet-1k（大规模基准）、CIFAR-10、CIFAR-100。
    *   **神经形态数据**：CIFAR10-DVS（事件相机数据）。
*   **对比方法（Benchmark）**：
    *   **S-CNN 架构**：Spiking ResNet, SEW ResNet, MS ResNet 等。
    *   **S-ViT 架构**：Spikformer, Spike-driven Transformer (SDT), SNN-ViT, Spikingformer 等。
*   **评估指标**：准确率（Acc）、参数量（Param）、突触操作数（SOPs）、能耗（Energy，单位 mJ）。

### 4. 资源与算力
*   **算力说明**：论文正文**未明确指出**具体的 GPU 型号、数量或训练总时长。
*   **配置提及**：提到详细的实验配置（如超参数、学习率等）记录在补充材料（Supplementary Materials）中。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ImageNet-1k 上设计了 Small、Base、Large 三种规模的模型。
    *   进行了下游任务（CIFAR 系列）的微调实验。
    *   提供了详细的能耗分布统计（针对 SSA、SConv 和 SFFN 各模块）。
*   **充分性评价**：实验覆盖了从大规模静态分类到时序神经形态数据的多种场景，对比了当前最先进的（SoTA）多种 SNN 模型。通过 SOPs 和能耗的逐层分析，客观地展示了分组策略的有效性，实验设计较为充分且公平。

### 6. 主要结论与发现
*   **高性能与低能耗并存**：Ge$^2$mS-T Large 在 ImageNet-1k 上达到了 **79.82%** 的准确率，而能耗仅为 **2.83mJ**，参数量不到 15M。
*   **能效比极高**：相比于 Spikingformer，在参数量减少约 80% 的情况下，准确率反而提升了 2% 以上，能耗降低了约 82%。
*   **分组优势**：空间分组（GW-SSA）成功抑制了 Token 数量增加带来的计算量爆炸，使 SOPs 保持在稳定且较低的水平。

### 7. 优点
*   **系统性创新**：首次从时、空、结构三个维度系统性地解决 S-ViT 的效率问题。
*   **理论支撑强**：ExpG-IF 模型不仅解决了训练内存问题，还通过数学等价性保证了转换的无损性。
*   **极简推理**：实现了真正的无乘法推理，非常适合在资源受限的神经形态硬件上部署。

### 8. 不足与局限
*   **训练策略复杂**：为了实现非均匀量化激活的稳定训练，采用了渐进式训练方案（从 ClipReLU 逐渐过渡），这可能增加了训练调参的难度。
*   **数据对齐成本**：在处理神经形态数据（DVS）时，仍需额外的卷积层来整合时间与通道维度，以对齐预训练模型的输入要求。
*   **硬件实测缺失**：虽然论文计算了理论能耗（基于 SOPs 标准公式），但未在真实的类脑芯片（如 Loihi 或天机芯）上进行实测功耗验证。

（完）
