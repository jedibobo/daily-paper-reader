---
title: Taming GPU Underutilization via Static Partitioning and Fine-grained CPU Offloading
title_zh: 通过静态分区和细粒度 CPU 卸载解决 GPU 利用率不足问题
authors: "Gabin Schieffer, Ruimin Shi, Jie Ren, Ivy Peng"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08451v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 通过分区和卸载解决GPU利用率不足的问题
tldr: 针对GPU资源利用率不足的问题，本文通过对HPC、AI和数据分析等实际应用（如Llama3、NekRS）进行系统级表征，评估了多实例GPU（MIG）分区的有效性与局限。研究发现MIG虽能提升吞吐量，但存在资源分配不匹配和功率限制等干扰。为此，作者提出一种利用NVLink-C2C互连的细粒度内存卸载方案，旨在弥补粗粒度资源切片间的差距，优化资源利用。
selection_source: fresh_fetch
motivation: GPU资源常因应用特性多样而利用不足，且现有的MIG静态分区方案难以灵活匹配应用对计算与内存的差异化需求。
method: 对多种真实应用进行系统级表征分析，并提出一种利用NVLink-C2C高速互连实现的细粒度CPU内存卸载方案。
result: 研究发现MIG虽能提升能效，但仍存在功率限制干扰，且粗粒度的资源切片往往无法精准满足紧耦合负载的需求。
conclusion: 通过结合静态GPU分区与细粒度的内存卸载机制，可以有效缓解资源分配不均，显著提升GPU系统的整体利用率。
---

## 摘要
GPU 计算吞吐量和显存容量的提升为各种工作负载带来了重大机遇。然而，高效利用这些资源仍然具有挑战性，特别是由于多样化的应用特性可能导致利用率不均衡。多实例 GPU (MIG) 是一种极具前景的方法，它通过将 GPU 计算和内存资源划分为具有隔离性的固定大小切片来提高利用率。然而，其在支持高性能计算 (HPC) 工作负载方面的有效性和局限性仍是一个悬而未决的问题。我们利用真实的科学、人工智能和数据分析应用（包括 NekRS、LAMMPS、Llama3 和 Qiskit），对不同的 GPU 共享选项进行了全面的系统级特性分析。我们的分析表明，虽然通过 MIG 进行 GPU 共享可以显著减少资源利用不足，并实现吞吐量和能效的系统级提升，但通过共享资源（如功率限制）仍会产生干扰。我们的性能-资源扩展结果表明，紧耦合计算和内存资源的粗粒度配置往往与应用需求不匹配。为了解决这种不匹配，我们提出了一种内存卸载方案，该方案利用缓存一致的 Nvlink-C2C 互连来弥合粗粒度资源切片之间的差距，并减少资源利用不足。

## Abstract
Advances in GPU compute throughput and memory capacity brings significant opportunities to a wide range of workloads. However, efficiently utilizing these resources remains challenging, particularly because diverse application characteristics may result in imbalanced utilization. Multi-Instance GPU (MIG) is a promising approach to improve utilization by partitioning GPU compute and memory resources into fixed-size slices with isolation. Yet, its effectiveness and limitations in supporting HPC workloads remain an open question. We present a comprehensive system-level characterization of different GPU sharing options using real-world scientific, AI, and data analytics applications, including NekRS, LAMMPS, Llama3, and Qiskit. Our analysis reveals that while GPU sharing via MIG can significantly reduce resource underutilization, and enable system-level improvements in throughput and energy, interference still occurs through shared resources, such as power throttling. Our performance-resource scaling results indicate that coarse-grained provisioning for tightly coupled compute and memory resources often mismatches application needs. To address this mismatch, we propose a memory-offloading scheme that leverages the cache-coherent Nvlink-C2C interconnect to bridge the gap between coarse-grained resource slices and reduce resource underutilization.

---

## 论文详细总结（自动生成）

这篇论文针对现代 GPU 资源利用率不足的问题，深入探讨了多实例 GPU（MIG）在高性能计算（HPC）、AI 和数据分析领域的表现，并提出了一种结合 CPU 卸载的优化方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着 GPU 计算能力和显存容量的飞速增长（如从 V100 到 B200），许多实际应用的资源需求无法完全填满硬件能力，导致严重的**资源利用不足（Underutilization）**。
*   **研究动机**：虽然 NVIDIA 提供了 MIG（硬件隔离分区）、MPS（多进程服务）和时间分片（Time-slicing）等共享技术，但这些技术在 HPC 负载下的有效性、资源扩展的匹配度以及相互干扰情况尚缺乏系统性研究。特别是 MIG 的资源划分是粗粒度的（固定比例），往往导致应用需求与分配资源之间的不匹配。

### 2. 方法论
*   **系统级表征分析**：通过 NVML GPM 采样技术，量化不同共享模式下的 SM 占用率、内存带宽利用率和显存容量占用。
*   **细粒度 CPU 卸载方案**：利用 Grace Hopper 架构中 **Nvlink-C2C** 互连的缓存一致性特点，提出一种内存卸载策略。当应用所需的显存略微超过某个 MIG 切片（如 12GB）时，不再强行升级到更大的切片（如 24GB，造成浪费），而是将超出部分的数据存放在 CPU 内存中，通过高速互连直接访问。
*   **奖励模型（Reward Model）**：定义了一个综合指标 $R$ 来权衡性能与资源浪费：
    *   公式逻辑：$R = \frac{性能比率}{\alpha + 显存浪费 + SM 浪费}$。
    *   通过调节参数 $\alpha$，系统可以在“性能优先”和“资源利用率优先”之间切换，从而为特定应用选择最优的 MIG 配置。

### 3. 实验设计
*   **应用场景（Benchmark）**：涵盖了 8 类真实世界负载：
    *   **科学计算**：NekRS（流体动力学）、LAMMPS（分子动力学）、AutoDock-GPU（分子对接）。
    *   **量子计算**：Qiskit。
    *   **人工智能**：Llama3 推理、GPT2 训练。
    *   **数据分析**：FAISS（向量搜索）、Rodinia Hotspot。
*   **对比方法**：全 GPU 串行执行（Baseline）、7 实例 MIG 分区、7 实例 MPS 共享、7 进程时间分片。

### 4. 资源与算力
*   **硬件平台**：使用 **NVIDIA Grace Hopper Superchip** 系统。
    *   **GPU**：H100 (96GB HBM3, 132个 SM)。
    *   **CPU**：72 核 Arm Neoverse-V2。
    *   **互连**：Nvlink-C2C（双向 450 GB/s）。
*   **软件环境**：CUDA 12.4，驱动版本 550.54.15。
*   **训练/运行时间**：文中未详细列出总训练时长，但说明了采样周期为 0.2 秒，实验涵盖了从单任务到 7 任务并发的多种组合。

### 5. 实验数量与充分性
*   **实验规模**：作者对 10 多个应用变体在 6 种不同的 MIG 配置（1g.12gb 到 7g.96gb）下进行了详尽的性能-资源扩展性测试。
*   **充分性评价**：实验设计非常充分。不仅测试了吞吐量，还深入分析了能耗、功率限制（Power Throttling）干扰以及 Nvlink-C2C 的实际带宽表现。涵盖了从计算密集型到内存密集型的多种负载，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **吞吐量与能效提升**：通过 GPU 共享，系统平均吞吐量提升了 **1.4倍**，平均能耗降低了 **26%**。
*   **资源不匹配**：MIG 的固定切片（如 1g, 2g, 3g...）导致资源扩展是不连续的，这与应用的线性或非线性需求不匹配，造成了“阶梯式”的资源浪费。
*   **功率限制干扰**：尽管 MIG 在计算和内存上实现了硬件隔离，但**功率限制（Power Limit）是共享的**。当多个计算密集型实例并发时，会触发整卡的功率降频，导致实例间的隐性干扰。
*   **卸载方案的有效性**：对于显存需求略超切片限制的应用（如 Qiskit 31量子位），通过 C2C 卸载到 CPU 内存比升级到更大的 MIG 实例具有更高的综合“奖励”得分。

### 7. 优点
*   **视角独特**：首次深入探讨了 Grace Hopper 架构下 Nvlink-C2C 在缓解 MIG 粗粒度分区问题上的潜力。
*   **实用性强**：提出的奖励模型为数据中心管理员在多租户环境下分配 GPU 资源提供了量化依据。
*   **分析深入**：揭示了 MIG 虽有硬件隔离但仍受限于共享功率预算的底层物理特性。

### 8. 不足与局限
*   **硬件局限性**：研究高度依赖 NVIDIA Grace Hopper 架构，对于缺乏高速 CPU-GPU 互连（如仅有 PCIe）的传统系统，内存卸载的性能损耗可能无法接受。
*   **静态分区限制**：研究主要集中在静态 MIG 配置，未涉及动态重构 MIG 分区的开销分析。
*   **应用范围**：虽然涵盖了多个领域，但对于超大规模分布式训练（多机多卡）场景下的共享影响未做探讨。

（完）
