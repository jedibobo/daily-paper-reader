---
title: Trilinear Compute-in-Memory Architecture for Energy-Efficient Transformer Acceleration
title_zh: 用于高能效 Transformer 加速的三线性存内计算架构
authors: "Md Zesun Ahmed Mia, Jiahui Duan, Kai Ni, Abhronil Sengupta"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07628v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于高效Transformer加速的存内计算架构
tldr: 针对Transformer自注意力机制中动态操作数导致非易失性存储器（NVM）频繁重编程的问题，本文提出TrilinearCIM架构。该架构利用双栅极FeFET（DG-FeFET）的背栅调制特性，实现了三操作数乘累加（MAC）原语，从而在无需动态重编程的情况下，在NVM核心内直接完成完整的注意力计算。实验表明，该方案在BERT和ViT任务上显著降低了功耗和延迟，是首个实现无需运行时重编程的纯NVM注意力加速架构。
selection_source: fresh_fetch
motivation: 传统存算一体架构在处理Transformer自注意力时，因动态操作数需频繁重编程NVM，导致高能耗、低吞吐及器件寿命受损。
method: 提出基于双栅极FeFET的TrilinearCIM架构，通过背栅调制实现三操作数乘累加，支持在NVM内直接进行注意力计算。
result: "在BERT和ViT模型评估中，相比传统FeFET CIM，该架构实现了最高46.6%的能耗降低和20.4%的延迟优化。"
conclusion: 本研究证明了利用多栅极器件特性可有效解决Transformer在NVM存算架构中的重编程瓶颈，实现了高效的端到端注意力加速。
---

## 摘要
Transformer 中的自注意力机制会产生动态操作数，这迫使传统的存内计算（CIM）加速器进入代价高昂的非易失性存储器（NVM）重编程周期，从而降低吞吐量并损害器件寿命。现有的解决方案要么通过矩阵分解或稀疏化来减少但仍保留 NVM 写入，要么以牺牲 NVM 密度为代价将注意力计算转移到数字 CMOS 中。我们提出了 TrilinearCIM，这是一种基于双栅极铁电场效应晶体管（DG-FeFET）的架构，它利用背栅调制实现了用于存内注意力计算的三操作数乘累加原语，且无需动态铁电重编程。在 BERT-base (GLUE) 和 ViT-base (ImageNet 和 CIFAR) 上的评估结果显示，TrilinearCIM 在九项 GLUE 任务中的七项上优于传统 CIM，同时在 37.3% 的面积开销下，相比传统 FeFET CIM 实现了高达 46.6% 的能耗降低和 20.4% 的延迟改进。据我们所知，这是首个完全在 NVM 核心中执行完整 Transformer 注意力计算且无需运行时重编程的架构。

## Abstract
Self-attention in Transformers generates dynamic operands that force conventional Compute-in-Memory (CIM) accelerators into costly non-volatile memory (NVM) reprogramming cycles, degrading throughput and stressing device endurance. Existing solutions either reduce but retain NVM writes through matrix decomposition or sparsity, or move attention computation to digital CMOS at the expense of NVM density. We present TrilinearCIM, a Double-Gate FeFET (DG-FeFET)-based architecture that uses back-gate modulation to realize a three-operand multiply-accumulate primitive for in-memory attention computation without dynamic ferroelectric reprogramming. Evaluated on BERT-base (GLUE) and ViT-base (ImageNet and CIFAR), TrilinearCIM outperforms conventional CIM on seven of nine GLUE tasks while achieving up to 46.6\% energy reduction and 20.4\% latency improvement over conventional FeFET CIM at 37.3\% area overhead. To our knowledge, this is the first architecture to perform complete Transformer attention computation exclusively in NVM cores without runtime reprogramming.

---

## 论文详细总结（自动生成）

这是一份关于论文《Trilinear Compute-in-Memory Architecture for Energy-Efficient Transformer Acceleration》的结构化技术总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心瓶颈**：Transformer 的自注意力机制涉及动态操作数（Query, Key, Value），这些操作数随输入变化。
*   **传统 CIM 的困境**：传统的存内计算（CIM）加速器基于非易失性存储器（NVM），假设权重是静态的。处理自注意力时，必须频繁重编程（写入）NVM 以更新动态矩阵。
*   **后果**：NVM 的写入操作比读取慢几个数量级，且能耗极高，频繁写入会迅速消耗器件寿命（Endurance），导致性能大幅下降。
*   **研究意义**：本文旨在设计一种无需在推理过程中重编程 NVM 即可完成完整自注意力计算的架构，实现真正的“全存内”加速。

### 2. 核心方法论
*   **关键技术：双栅极 FeFET (DG-FeFET)**：
    *   利用 DG-FeFET 器件的物理特性：**顶栅（Top-Gate）**通过铁电层存储非易失性权重；**背栅（Back-Gate）**作为一个独立的挥发性控制端，通过电压调制通道电导。
*   **三线性乘累加原语（Trilinear MAC）**：
    *   提出公式：$I_{DS} \approx V_{DS} \cdot G_0 \cdot (1 + \eta_{BG} \cdot V_{BG})$。
    *   其中 $V_{DS}$ 代表输入，$G_0$ 代表存储的静态权重，$V_{BG}$ 代表动态调制操作数。这使得一个单元可以同时处理三个操作数。
*   **注意力数据流融合**：
    *   将投影（Projection）和得分计算（Score Synthesis）融合。例如，在计算 $Q \cdot K^T$ 时，将投影权重 $W_K$ 存入阵列，输入 $X$ 作为行驱动，另一个输入 $X^T$ 通过背栅调制，直接输出注意力得分，无需生成并存储中间张量。
*   **架构设计**：
    *   设计了支持三线性操作的交叉杆阵列（Crossbar），并集成了数字特殊功能单元（SFU）来处理 Softmax、LayerNorm 和 GELU 等非线性操作。

### 3. 实验设计
*   **模型与数据集**：
    *   **NLP 领域**：使用 BERT-base 模型，在 GLUE 基准测试（包含 CoLA, SST-2, MRPC, RTE, STS-B, WNLI, QNLI, QQP, MNLI 九项任务）上评估。
    *   **CV 领域**：使用 ViT-base 模型，在 CIFAR-10/100 和 ImageNet-1K 上评估。
*   **Benchmark（对比对象）**：
    *   **Digital**：理想的 8 位量化数字硬件（准确率上限）。
    *   **CIM-Bilinear**：传统的单栅极 FeFET CIM 架构（需要在推理时动态写入 NVM）。
*   **评估工具**：开发了 **TransCIM** 仿真框架（基于 NeuroSim 扩展），模拟 7nm CMOS 逻辑和 22nm FeFET 存储工艺。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练时长或型号，因为该研究侧重于**硬件架构仿真**。
*   **仿真参数**：使用了 7nm FinFET 工艺参数模拟外围电路，22nm FDSOI 工艺模拟 FeFET 器件。
*   **存储配置**：默认配置包括 4MB 全局缓冲区，64x64 的子阵列大小，以及 8 位 ADC。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 NLP 和 CV 两大主流 Transformer 应用场景。
    *   进行了详细的**消融实验**，包括子阵列大小（32x32 vs 64x64）、位宽精度（1b/6b 到 2b/9b 的 bitcell/ADC 组合）以及序列长度（64 到 256）的影响。
*   **充分性评价**：实验设计较为全面，不仅关注能效和延迟，还深入探讨了模拟计算非理想性对不同任务准确率的影响，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **能效与性能**：相比传统 Bilinear CIM，TrilinearCIM 实现了高达 **46.6% 的能耗降低**和 **20.4% 的延迟改进**。
*   **准确率**：在 GLUE 任务中，TrilinearCIM 在 7/9 的任务上优于传统 CIM，因为它减少了中间结果在模拟和数字域之间的频繁转换。
*   **写入消除**：成功实现了推理过程中 **零 NVM 写入**，彻底解决了 FeFET 的寿命瓶颈。
*   **视觉任务挑战**：在 ViT 任务上准确率略有下降（如 ImageNet 下降约 4%），归因于背栅 DAC 的量化误差对视觉注意力图中的离群值（Outliers）较为敏感。

### 7. 优点
*   **创新性极强**：首次提出利用 DG-FeFET 的物理特性实现三操作数存内计算，完美契合自注意力机制的数学结构。
*   **数据流优化**：通过融合操作减少了中间张量的存储需求，使全局缓冲区压力降低了约 3 倍。
*   **寿命友好**：消除了运行时重编程，极大地延长了基于 NVM 的加速器的使用寿命。

### 8. 不足与局限
*   **面积开销**：由于增加了背栅驱动电路和每列独立的 DAC，导致芯片面积增加了 **37.3%**。
*   **视觉模型适配性**：在处理 ViT 等对精度分布极其敏感的模型时，背栅调制的线性近似和量化会带来一定的精度损失。
*   **仿真依赖**：目前研究完全基于仿真框架和实验器件数据，尚未有大规模流片验证，实际硬件中的工艺偏差（Variation）影响仍待观察。

（完）
