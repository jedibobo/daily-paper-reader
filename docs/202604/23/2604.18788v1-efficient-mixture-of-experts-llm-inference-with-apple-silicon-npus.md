---
title: Efficient Mixture-of-Experts LLM Inference with Apple Silicon NPUs
title_zh: 基于 Apple Silicon NPU 的高效混合专家模型 (MoE) 大语言模型推理
authors: "Afsara Benazir, Felix Xiaozhu Lin"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18788v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对MoE大模型的NPU友好型算子映射与内核优化
tldr: 针对 Apple Silicon NPU 在处理 MoE 模型时面临的动态形状、不规则算子及调度开销等挑战，本文提出 NPUMoE 推理引擎。该引擎通过离线校准预估专家容量，采用静态分层、分组执行和负载感知图驻留技术，将密集计算卸载至 NPU，显著提升了长文本预填充阶段的推理速度与能效。
selection_source: fresh_fetch
motivation: MoE 模型的动态路由和不规则算子与 Apple NPU 的静态形状约束及算子支持不匹配，导致推理效率低下。
method: 提出 NPUMoE 引擎，利用静态专家容量分层、分组执行和负载感知计算图驻留技术，实现 NPU 与 CPU/GPU 的协同推理。
result: 在 M 系列芯片上，NPUMoE 将推理延迟降低了 1.32x-5.55x，能效提升了 1.81x-7.37x，并大幅减少了 CPU 占用。
conclusion: 通过合理的软硬件协同设计，可以有效利用 Apple NPU 加速复杂的 MoE 模型推理，实现高性能与高能效的平衡。
---

## 摘要
Apple 神经网络引擎 (ANE) 是集成在每颗 Apple Silicon 芯片中的专用神经处理单元 (NPU)。混合专家 (MoE) 大语言模型通过稀疏激活提高了推理效率，但在 NPU 上面临三个挑战：专家路由具有不可预测性，并引入了动态张量形状，这与 NPU 的形状特定约束相冲突；若干不规则算子（如 top-k、scatter/gather 等）对 NPU 并不友好；启动大量小型专家内核会产生巨大的调度和同步开销。NPU 旨在分担 CPU 和 GPU 的 AI 计算任务；我们的目标是实现 MoE 推理的此类卸载，特别是在长上下文工作负载消耗大量系统资源的预填充（prefill）阶段。本文提出了 NPUMoE，这是一种运行时推理引擎，通过将密集的静态计算卸载到 NPU，同时为动态操作保留 CPU/GPU 回退路径，从而加速 Apple Silicon 上的 MoE 执行。NPUMoE 使用离线校准来估计专家容量和热度，从而驱动三项关键技术：(1) 用于应对动态专家路由的专家容量静态分层；(2) 用于缓解 NPU 并发限制的分组专家执行；(3) 用于减少 CPU-NPU 同步开销的负载感知专家计算图驻留。在 Apple M 系列设备上使用三种代表性 MoE 大语言模型和四种长上下文工作负载进行的实验表明，NPUMoE 始终优于基准方案，通过有效的 NPU 卸载，将延迟降低了 1.32 倍至 5.55 倍，能效提高了 1.81 倍至 7.37 倍，并将 CPU 周期使用率降低了 1.78 倍至 5.54 倍。

## Abstract
Apple Neural Engine (ANE) is a dedicated neural processing unit (NPU) present in every Apple Silicon chip. Mixture-of-Experts (MoE) LLMs improve inference efficiency via sparse activation but are challenging for NPUs in three ways: expert routing is unpredictable and introduces dynamic tensor shapes that conflict with the shape-specific constraints of NPUs; several irregular operators, e.g., top-k, scatter/gather, etc., are not NPU-friendly; and launching many small expert kernels incurs substantial dispatch and synchronization overhead. NPUs are designed to offload AI compute from CPU and GPU; our goal is to enable such offloading for MoE inference, particularly during prefill, where long-context workloads consume substantial system resources.   This paper presents NPUMoE, a runtime inference engine that accelerates MoE execution on Apple Silicon by offloading dense, static computation to NPU, while preserving a CPU/GPU fallback path for dynamic operations. NPUMoE uses offline calibration to estimate expert capacity and popularity that drives three key techniques: (1) Static tiers for expert capacity to address dynamic expert routing (2) Grouped expert execution to mitigate NPU concurrency limits (3) Load-aware expert compute graph residency to reduce CPU-NPU synchronization overhead. Experiments on Apple M-series devices using three representative MoE LLMs and four long-context workloads show that NPUMoE consistently outperforms baselines, reducing latency by 1.32x-5.55x, improving energy efficiency by 1.81x-7.37x, and reducing CPU-cycle usage by 1.78x-5.54x through effective NPU offloading.

---

## 论文详细总结（自动生成）

这篇论文针对在 Apple Silicon 的神经网络引擎（ANE/NPU）上高效运行混合专家模型（MoE）大语言模型提出了系统性的优化方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：MoE 架构虽然通过稀疏激活提升了模型容量，但其**动态性**与移动端 NPU（如 Apple ANE）的**静态设计约束**之间存在严重冲突。
*   **研究动机**：
    *   **动态形状冲突**：NPU 要求静态、预定义的张量形状，而 MoE 的专家路由会导致每层专家处理的 Token 数量不可预测。
    *   **算子不友好**：MoE 依赖的 Top-k、Scatter/Gather 等不规则算子在 NPU 上效率极低，常导致回退（Fallback）到 CPU 执行。
    *   **调度开销**：MoE 包含大量小型专家内核，频繁启动这些内核会产生巨大的分发（Dispatch）和同步开销。
    *   **资源竞争**：在端侧设备上，CPU/GPU 需处理 UI 和图形任务，将长文本预填充（Prefill）阶段的密集计算卸载到 NPU 是提升系统响应速度的关键。

### 2. 方法论：NPUMoE 推理引擎
NPUMoE 的核心思想是将 MoE 的动态执行流转化为 NPU 友好的静态流，并实现 CPU 与 NPU 的负载均衡。
*   **静态专家容量分层 (Static Tiers for Expert Capacity)**：
    *   通过离线校准预估各专家的负载。
    *   将专家划分为不同的容量等级（Tiers），热门专家分配大容量，冷门专家分配小容量。
    *   若 Token 超过容量，采用基于显著性得分（L2 范数）的 Token 丢弃策略以维持精度。
*   **分组专家执行 (Grouped Expert Execution)**：
    *   将多个专家合并为一个大型的静态 FFN 计算图。
    *   通过一次 NPU 调用同时执行多个专家，减少分发次数和设备队列的序列化等待时间。
*   **负载感知计算图驻留 (Load-Aware Residency)**：
    *   根据专家热度决定其执行位置。
    *   “热专家”组常驻 NPU 以摊销启动成本；“冷专家”或极小任务组则留在 CPU 执行，避免昂贵的 CPU-NPU 同步开销。
*   **混合分区策略**：将 Attention 和专家 FFN 等密集计算放在 NPU，而将路由、LayerNorm 和动态索引等操作保留在 CPU。

### 3. 实验设计
*   **模型选择**：Phi-3.5-MoE-Instruct (42.6B)、Phi-tiny-MoE (3.8B)、Qwen3-30B-A3B (128 专家)。
*   **硬件平台**：Apple M2 Ultra (192GB 统一内存)、M2 Max (64GB)。
*   **数据集/场景**：针对长文本预填充阶段，使用 HellaSwag (常识推理)、BoolQ (问答)、RULER (长文本检索与聚合)。
*   **对比基准 (Baselines)**：
    *   **Core ML (CPU only)**：全 CPU 执行。
    *   **Core ML (Naive)**：原生 Core ML 调度，专家 FFN 尝试在 NPU 运行。
    *   **ANEMLL**：现有的 NPU 加速库（主要针对密集 Transformer）。

### 4. 资源与算力
*   **推理设备**：实验在配备 32 核 ANE 的 M2 Ultra 和 16 核 ANE 的 M2 Max 上完成。
*   **训练时长**：论文未涉及模型训练，主要关注推理阶段。模型转换和离线校准在单台 M2 设备上即可完成。
*   **精度**：使用 FP16 精度进行计算，部分权重可能涉及 INT8 量化（由 ANE 运行时自动处理）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3.8B 到 42.6B 不同参数规模的模型，以及从 16 到 128 不同专家数量的架构。
*   **消融实验**：详细评估了静态分层、分组执行、负载感知驻留三项技术各自的贡献。
*   **充分性评价**：实验设计较为充分，不仅测量了延迟（TTFT），还深入分析了能效（EPT）、CPU 周期占用以及端到端精度损失。通过跨数据集验证证明了离线校准的稳定性，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：相比基准方案，NPUMoE 将预填充延迟降低了 **1.32x–5.55x**。
*   **能效卓越**：能效提升了 **1.81x–7.37x**，显著延长了电池续航。
*   **CPU 卸载有效**：减少了 **1.78x–5.54x** 的 CPU 周期使用，释放了 CPU 资源用于其他系统任务。
*   **精度保持**：尽管存在 Token 丢弃，但在测试任务上的精度损失极小（<1.1%）。
*   **关键发现**：在 NPU 上，减少分发次数（Grouping）比单纯追求计算并行度更重要；CPU-NPU 同步开销在小任务中可能占据 60% 以上的时间。

### 7. 优点
*   **首创性**：这是首个系统性针对 Apple NPU 优化 MoE 模型推理的工作。
*   **软硬协同**：深入理解了 ANE 的硬件特性（如 16 字节对齐、卷积优化等），并将其转化为软件层面的图优化策略。
*   **实用性强**：在统一内存架构下，解决了大参数模型在端侧运行的资源竞争问题。

### 8. 不足与局限
*   **解码阶段优化有限**：目前主要针对预填充阶段，由于解码（Decode）是内存受限且单 Token 执行，难以有效摊销 NPU 的调度开销。
*   **Token 丢弃风险**：虽然在测试集上精度损失小，但在极端不平衡的输入下，Token 丢弃可能对特定复杂逻辑推理产生负面影响。
*   **离线校准依赖**：性能高度依赖于离线校准的准确性，若实际推理时的分布与校准集差异巨大，可能导致性能下降。

（完）
