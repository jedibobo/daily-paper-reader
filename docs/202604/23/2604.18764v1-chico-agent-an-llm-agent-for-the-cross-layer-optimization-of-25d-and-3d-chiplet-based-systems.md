---
title: "CHICO-Agent: An LLM Agent for the Cross-layer Optimization of 2.5D and 3D Chiplet-based Systems"
title_zh: CHICO-Agent：一种用于 2.5D 和 3D 芯粒系统跨层优化的 LLM 智能体
authors: "Qihang Wu, Aman Arora, Vidya A. Chhabria"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18764v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 芯粒系统的跨层优化与协同设计
tldr: 针对2.5D/3D芯粒系统设计中跨层优化复杂度高、设计空间巨大的挑战，本文提出了CHICO-Agent。这是一个基于大语言模型的优化框架，通过维护持久化知识库捕捉参数趋势，并采用管理-执行多智能体工作流进行协同探索。该方法在降低系统成本方面优于传统模拟退火算法，并为设计者提供了可解释的审计追踪，有效提升了复杂芯片系统的设计效率。
selection_source: fresh_fetch
motivation: 单体芯片已达物理极限，而2.5D/3D芯粒系统在应用、架构、芯片及封装层面的跨层协同设计面临巨大的组合优化挑战。
method: 提出一种基于LLM的多智能体框架，利用持久化知识库捕捉参数与结果趋势，并通过管理与执行智能体的协作进行设计空间探索。
result: 实验表明，CHICO-Agent比模拟退火算法能找到成本更低的配置，并为设计过程提供了可解释的审计轨迹。
conclusion: CHICO-Agent证明了利用大语言模型智能体处理复杂的芯片系统跨层优化问题具有显著的性能优势和决策透明度。
---

## 摘要
大语言模型（LLM）和 AI 工作负载的快速增长已将单片硅片推向了光刻掩模版和经济极限，加速了 2.5D/3D 芯粒（chiplet）系统的采用。然而，这些系统要求在计算栈的多个层面（包括应用、架构、芯片和封装）进行协同设计，从而增加了设计复杂度。由此产生的设计空间具有高度的组合性，需要在延迟、能耗、面积和成本之间进行权衡。为了应对这一挑战，我们提出了 CHICO-Agent，这是一种针对基于 2.5D/3D 芯粒系统的 LLM 驱动优化框架。CHICO-Agent 维护一个持久化知识库以捕捉参数与结果之间的趋势，并通过一种“管理-领域”（admin-field）多智能体工作流来协调探索过程。与模拟退火基准相比，CHICO-Agent 能够发现成本更低的配置，并为设计者提供可解释的审计追踪。

## Abstract
The rapid growth of large language models (LLMs) and AI workloads has pushed monolithic silicon to its reticle and economic limits, accelerating the adoption of 2.5D/3D chiplet systems. However, these systems increase design complexity by requiring co-design across multiple levels of the computing stack, including application, architecture, chip, and package. The resulting design space is highly combinatorial, with trade-offs among latency, energy, area, and cost. To address this challenge, we propose CHICO-Agent, an LLM-driven optimization framework for 2.5D/3D chiplet-based systems. CHICO-Agent maintains a persistent knowledge base to capture parameter-outcome trends and coordinates exploration through an admin-field multi-agent workflow. Compared with a simulated-annealing baseline, CHICO-Agent finds lower-cost configurations and provides an interpretable audit trail for designers.

---

## 论文详细总结（自动生成）

以下是对论文《CHICO-Agent: An LLM Agent for the Cross-layer Optimization of 2.5D and 3D Chiplet-based Systems》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着 AI 工作负载的激增，单体硅片（Monolithic Silicon）已达到物理和经济极限。2.5D 和 3D 芯粒（Chiplet）技术通过异构集成成为主流，但其设计空间极其复杂。
*   **核心问题**：芯粒系统设计涉及应用、架构、芯片和封装四个层面的跨层协同优化。这种组合爆炸的设计空间使得传统的手动搜索或简单的参数扫描不可行。
*   **现有局限**：现有的自动化设计空间探索（DSE）方法（如模拟退火 SA、强化学习 RL）多为“黑盒”优化，容易陷入局部最优，且缺乏人类可理解的设计逻辑，难以复用经验。

### 2. 论文提出的方法论
CHICO-Agent 是一个基于大语言模型（LLM）的多智能体分层优化框架，其核心思想是将硬件优化任务转化为“推理驱动”的迭代过程。
*   **核心架构**：采用**管理智能体（Admin Agent）**与**领域智能体（Field Agent）**的协作模式。
    *   **管理智能体**：负责全局规划、推理历史数据、生成探索计划。
    *   **领域智能体**：并行执行具体的设计评估，利用分析模型计算 PPAC（性能、功耗、面积、成本）指标。
*   **知识上下文管理**：
    *   **持久化上下文**：包含静态领域知识（模型信息、设计规则、非法组合黑名单）。
    *   **演进上下文**：记录探索历史（KNOWHOW.md）、最优设计（BEST.csv）和所有尝试过的配置。
*   **优化流程**：
    1.  **编排阶段**：Admin 智能体根据当前上下文推理出 N 个探索计划。
    2.  **探索阶段**：N 个 Field 智能体并行评估配置并记录结果。
    3.  **评估阶段**：Admin 智能体汇总结果，更新知识库，并决定下一轮的搜索方向。
*   **成本函数**：通过加权公式综合评估能量、面积、延迟和制造成本。

### 3. 实验设计
*   **实验场景**：针对 6 种代表性的 GEMM（通用矩阵乘法）工作负载，涵盖 GPT-2、ViT、ResNet-50、VGG-16 和 MobileNetV2 的计算特性。
*   **应用配置（Profiles）**：定义了四种权重方案：平衡型（Balance）、移动端（Mobile）、汽车（Automotive）和可穿戴设备（Wearables）。
*   **基准对比（Benchmark）**：
    *   主要对比对象是**模拟退火（Simulated Annealing, SA）**算法。
    *   为了公平性，对 SA 进行了广泛的超参数网格搜索（390 种配置）。
*   **评估指标**：最终系统成本（Cost）和达到该成本所需的墙钟时间（Wall-clock time）。

### 4. 资源与算力
*   **模型使用**：实验使用了 **GPT-5.3-codex**（注：论文标注日期为 2026 年，此处模型版本为论文设定）。
*   **并行度**：设置了 $N=100$ 个并行运行的领域智能体。
*   **算力细节**：文中未明确提及具体的 GPU 硬件型号或训练时长，但记录了优化过程的运行时间（最高约 15,000 秒）。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 24 组核心对比实验（6 种工作负载 × 4 种应用配置）。
*   **充分性**：
    *   **SA 基准非常强劲**：通过 390 组超参数扫描确保了对比基准的上限。
    *   **帕累托前沿分析**：通过扫描推理强度和迭代次数，绘制了完整的“时间-成本”帕累托曲线。
    *   **客观性**：实验覆盖了从边缘计算到高性能计算的不同权重需求，证明了框架的通用性。

### 6. 论文的主要结论与发现
*   **性能优越**：在 24 个实验案例中，CHICO-Agent 在 20 个案例中找到了比 SA 更低成本的配置。
*   **跨层洞察**：LLM 能够发现人类难以直观察觉的规律，例如发现“输入驻留（IS）数据流 + Split-K 划分”的组合在特定负载下优于传统的输出驻留（OS）策略。
*   **收敛性**：在复杂约束下（如汽车和可穿戴场景），CHICO-Agent 能够探索到 SA 无法触及的高质量设计区域。
*   **可解释性**：生成的推理轨迹（Reasoning Traces）为设计者提供了明确的决策依据，解释了为何选择特定的芯粒数量或封装协议。

### 7. 优点
*   **减少调参负担**：相比 SA 需要精调初始温度、冷却率等，CHICO-Agent 仅需设置简单的推理强度和迭代次数。
*   **设计透明化**：提供了可审计的推理过程，将“黑盒”优化转变为“白盒”决策。
*   **全局搜索能力**：利用 LLM 的语义理解能力，能够更有效地跨越设计空间中的不连续区域。

### 8. 不足与局限
*   **运行开销**：由于涉及多次 LLM 推理调用，单次迭代的时间成本显著高于轻量级的 SA 扰动。
*   **特定场景表现**：在“平衡型（Balance）”配置下（各权重相等），LLM 的推理引导信号变弱，导致其优势不如在有明确侧重点（如极低功耗需求）的场景下明显。
*   **依赖模型质量**：优化效果高度依赖于底层 LLM 的推理能力和所提供的分析模型的准确性。

（完）
