---
title: Towards Real-Time ECG and EMG Modeling on $μ$NPUs
title_zh: 迈向微型神经网络处理器（$μ$NPUs）上的实时心电图（ECG）与肌电图（EMG）建模
authors: "Josh Millar, Ashok Samraj Thangarajan, Soumyajit Chatterjee, Hamed Haddadi"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18067v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对NPU兼容模型架构的硬件感知层设计
tldr: 针对可穿戴设备中生理信号（ECG/EMG）分析受限于硬件资源和Transformer模型复杂性的问题，本文提出PhysioLite。这是一种轻量级且兼容微型神经网络处理器（μNPU）的模型架构，通过可学习小波滤波器组和硬件感知设计，在保持与SOTA模型相当性能的同时，显著降低了模型体积（约370KB），实现了在低功耗嵌入式设备上的实时推理。
selection_source: fresh_fetch
motivation: 现有的生理信号分析模型（如Transformer）因体积过大且不支持动态注意力机制，难以在资源受限的微型NPU硬件上运行。
method: 提出PhysioLite架构，采用可学习小波滤波器组、CPU分担的位置编码及硬件感知层设计，确保模型与μNPU兼容。
result: "在ECG和EMG基准测试中达到与SOTA Transformer模型相当的性能，且量化后体积仅为后者的10%（约370KB）。"
conclusion: PhysioLite证明了在极低功耗的电池供电硬件上进行高性能、实时的生理信号分析是切实可行的。
---

## 摘要
神经网络处理器（NPUs）及其他低功耗加速器的微型化，使其能够集成到微控制器规模的可穿戴硬件中，从而支持近实时、离线且保护隐私的推理。然而，生理信号分析在此类硬件上仍然难以实现；近期基于 Transformer 的模型虽然展现出了最先进的性能，但对于资源和功耗受限的硬件而言体积过于庞大，且由于其动态注意力操作而与 $μ$NPUs 不兼容。我们提出了 PhysioLite，这是一种用于 ECG/EMG 信号分析的轻量级、NPU 兼容的模型架构和训练框架。通过使用可学习的小波滤波器组、CPU 卸载的位置编码以及硬件感知的层设计，PhysioLite 在 ECG 和 EMG 基准测试中达到了与最先进的基于 Transformer 的基础模型相当的性能，而其体积不足后者的 10%（8 位量化后约为 370KB）。我们还在 MAX78000 和 HX6538 WE2 $μ$NPUs 上对其各组件的延迟和资源消耗进行了分析，证明了其在受限的电池供电硬件上进行信号分析的可行性。我们在以下地址发布了模型和训练框架：https://github.com/j0shmillar/physiolite。

## Abstract
The miniaturisation of neural processing units (NPUs) and other low-power accelerators has enabled their integration into microcontroller-scale wearable hardware, supporting near-real-time, offline, and privacy-preserving inference. Yet physiological signal analysis has remained infeasible on such hardware; recent Transformer-based models show state-of-the-art performance but are prohibitively large for resource- and power-constrained hardware and incompatible with $μ$NPUs due to their dynamic attention operations. We introduce PhysioLite, a lightweight, NPU-compatible model architecture and training framework for ECG/EMG signal analysis. Using learnable wavelet filter banks, CPU-offloaded positional encoding, and hardware-aware layer design, PhysioLite reaches performance comparable to state-of-the-art Transformer-based foundation models on ECG and EMG benchmarks, while being <10% of the size ($\sim$370KB with 8-bit quantization). We also profile its component-wise latency and resource consumption on both the MAX78000 and HX6538 WE2 $μ$NPUs, demonstrating its viability for signal analysis on constrained, battery-powered hardware. We release our model(s) and training framework at: https://github.com/j0shmillar/physiolite.

---

## 论文详细总结（自动生成）

这篇论文《Towards Real-Time ECG and EMG Modeling on $\mu$NPUs》由帝国理工学院、诺基亚贝尔实验室等机构的研究人员合作完成，旨在解决生理信号分析模型在微型嵌入式硬件上部署的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限（微控制器级别）的微型神经网络处理器（$\mu$NPU）上，实现高性能、实时且隐私保护的生理信号（如心电图 ECG 和肌电图 EMG）建模？
*   **研究动机**：
    *   **模型与硬件的不匹配**：目前最先进的生理信号模型多基于 Transformer 架构，虽然性能优异，但体积庞大（数千万参数），且其“动态注意力机制”与当前主流 $\mu$NPU（仅支持静态卷积算子）不兼容。
    *   **嵌入式需求**：可穿戴健康设备需要低延迟、离线运行以保护隐私，并要求极低的功耗以延长电池寿命。
    *   **信号复杂性**：生理信号具有非平稳、多尺度和个体差异大的特点，简单的轻量级模型往往难以捕捉长程依赖关系。

### 2. 方法论：PhysioLite 架构设计
PhysioLite 采用了一种**硬件感知（Hardware-aware）**的设计思路，将 Transformer 的功能模块替换为 $\mu$NPU 兼容的等效组件：
*   **CPU 分担的位置编码（Positional Encoding）**：由于 $\mu$NPU 不支持动态生成位置嵌入，PhysioLite 在 CPU 上预计算正弦/余弦查找表（LUT），将其量化为 Q7 格式并与原始信号拼接后再送入 NPU。
*   **可学习的小波类滤波器组（Multi-branch Front-end）**：模拟小波变换的多尺度特性，使用并行的 1D 卷积分支（卷积核大小分别为 3, 5, 7），直接从原始信号中提取不同频率的特征，避免了昂贵的预处理滤波。
*   **无注意力特征混合（Attention-free Mixing）**：用逐点卷积（Pointwise Convolution）和倒置残差结构（Inverted Residuals）替代自注意力机制（MHSA）。这种设计在保持特征混合能力的同时，完全符合 $\mu$NPU 的静态算子限制。
*   **知识蒸馏（Knowledge Distillation）**：采用教师-学生模型框架，利用大型预训练基础模型（如 PhysioWave）作为教师，引导轻量级的 PhysioLite 进行学习，以弥补参数量减少带来的性能损失。

### 3. 实验设计
*   **数据集**：
    *   **ECG 任务**：PTB-XL（多标签分类）、Chapman-Shaoxing（心律失常分类）、CPSC 2018。
    *   **EMG 任务**：Ninapro DB5（52 种手势识别）、EPN-612、UCI EMG。
*   **对比方法（Benchmarks）**：
    *   **基础模型**：ECGFounder (30.7M)、ECG-FM (90.9M)、PhysioWave (15M)。
    *   **轻量级/专用模型**：WaveFormer、OTiS、ECG-TCN、ECG-BiLSTM。
*   **硬件平台**：
    *   **MAX78000**：集成 CNN 加速器的超低功耗微控制器（442KB 权重 SRAM）。
    *   **Himax HX6538 WE2**：搭载 Ethos-U55 NPU 的平台。

### 4. 资源与算力
*   **训练算力**：文中提到使用 AdamW 优化器，训练 30 个 epoch，Batch Size 为 16。**未明确说明具体使用的 GPU 型号和训练总时长**。
*   **推理算力/资源**：
    *   **模型大小**：约 0.28M 参数，8 位量化后仅为 **370KB**。
    *   **内存占用**：在 MAX78000 上 SRAM 占用为 43.3KB - 76.1KB。
    *   **功耗**：在 MAX78000 上推理平均功耗约为 **33.4 mW**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个主流生理信号数据集，跨越了 ECG 和 EMG 两个重要领域。
*   **消融实验**：详细测试了知识蒸馏（KD）强度、位置编码（PE）的有无、以及多尺度卷积核设计对性能的影响。
*   **硬件分析**：在两种不同的 $\mu$NPU 架构上进行了端到端的延迟分解（包括重采样、归一化、位置编码、推理等阶段）。
*   **评价**：实验设计较为全面，不仅关注算法指标（F1, AUROC），还深入探讨了系统级指标（延迟、功耗、内存），体现了嵌入式 AI 研究的严谨性。

### 6. 主要结论与发现
*   **性能卓越**：PhysioLite 在体积仅为基础模型 <10% 的情况下，性能达到了与之相当的水平（例如在 Chapman-Shaoxing 上 F1 达到 0.942）。
*   **实时性强**：端到端延迟在 **20ms 以内**，完全满足实时监测需求。
*   **NPU 加速显著**：相比纯 CPU 执行，NPU 加速使推理速度提升了 14 倍以上，端到端速度提升 5-6 倍。
*   **关键组件**：位置编码和知识蒸馏对提升轻量级模型的表现至关重要。

### 7. 优点
*   **硬件兼容性极佳**：针对 $\mu$NPU 的算子限制进行了深度定制，而非盲目追求复杂架构。
*   **多尺度建模创新**：通过并行卷积分支成功模拟了小波变换，有效提取了生理信号的关键特征。
*   **开源贡献**：提供了完整的训练框架和预训练模型，具有很高的实用价值。

### 8. 不足与局限
*   **实验环境局限**：主要基于受控的公开数据集，未在真实世界（存在运动伪影、传感器漂移等噪声）的长期监测场景中验证。
*   **通用性待证**：目前仅验证了 ECG 和 EMG 信号，对于其他 1D 时间序列（如 IMU 惯性数据）的泛化能力尚不明确。
*   **硬件覆盖**：虽然测试了两种 $\mu$NPU，但不同厂商的算子融合策略差异巨大，模型的移植性可能仍面临工程挑战。

（完）
