---
title: "CIMple: Standard-cell SRAM-based CIM with LUT-based split softmax for attention acceleration"
title_zh: CIMple：基于标准单元 SRAM 且集成查找表拆分 Softmax 的存内计算注意力加速器
authors: "Bas Ahn, Xingjian Tao, Manil Dev Gomongy, Marc Geilen, Henk Corporaal"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15944v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于边缘大模型加速的基于 SRAM 的存内计算 (CIM)
tldr: 针对大语言模型在边缘设备部署面临的资源受限和数据搬运挑战，本文提出了一种名为CIMple的全数字标准单元SRAM存内计算（CIM）加速器。该架构专为Transformer的自注意力机制设计，通过双库CIM结构、8位并行权重输入以及基于查找表（LUT）的定点Softmax实现，克服了传统CIM仅支持静态乘累加的局限，在28nm工艺下实现了高能效与高性能。
selection_source: fresh_fetch
motivation: 传统存内计算架构难以支持Transformer模型中的非线性操作及多样化配置，限制了其在边缘端部署大语言模型的能力。
method: 提出一种基于标准单元SRAM的全数字双库CIM架构，结合8位并行权重馈送技术和基于查找表的定点Softmax优化。
result: 在28nm工艺下，该32kb加速器在0.85V时达到26.1 TOPS/W的能效，在1.2V时达到2.31 TOPS/mm²的面积效率。
conclusion: CIMple架构通过集成非线性计算与高效存内逻辑，为边缘设备上的Transformer模型加速提供了一种高能效且灵活的硬件方案。
---

## 摘要
大语言模型（LLM），如 LLaMA 和 DeepSeek，均基于 Transformer 架构构建，该架构已成为在自然语言处理任务中实现最先进性能的标准模型。近期，在边缘设备上部署 LLM 的需求日益增长。尽管目前已提出了一些较小的 LLM 模型，但它们通常仍包含数十亿个参数。由于边缘设备资源有限，这给边缘部署带来了巨大挑战。存内计算（CIM）是一种极具前景的架构，它通过将计算逻辑直接集成到存储器中以减少数据搬运，从而解决上述问题。然而，现有的 CIM 架构仅支持静态乘累加（MAC）操作，限制了其在支持非线性操作和各类 Transformer 模型时的可配置性。本文提出了一种名为 CIMple 的全数字、基于标准单元 SRAM 的 CIM 架构自注意力加速器，旨在克服 Transformer 模型中的这些局限性。CIMple 的主要贡献包括：1) 一种新型的基于双库 CIM 的全数字自注意力加速器，采用 8 位并行权重馈送；2) 一种基于查找表（LUT）的定点实现，在精度损失极小的情况下降低了延迟；3) 对采用 28nm 工艺实现的 32kb 基于 CIM 的自注意力加速器进行了性能评估，在 INT8 精度下，其在 0.85V 时达到 26.1 TOPS/W，在 1.2V 时达到 2.31 TOPS/mm²。

## Abstract
Large Language Models (LLMs) such as LLaMA and DeepSeek, are built on transformer architectures, which have become a standard model for achieving state-of-the-art performance in natural language processing tasks. Recently, there has been growing interest in deploying LLMs on edge devices. Although smaller LLM models are being proposed, they often still contain billions of parameters. Since edge devices are limited in their resources this poses a significant challenge for edge deployment. Compute-in-memory (CIM) is a promising architecture that addresses this by reducing data movement through the integration of computational logic directly into memory. However, existing CIM architectures support only static Multiply-Accumulate (MAC) operations which limit their configurability in supporting nonlinear operations and various types of transformer models. This paper presents a fully digital standard-cell SRAM-based CIM architecture accelerator for self-attention, called CIMple, designed to overcome these limitations, inside transformer models. The key contributions of CIMple are: 1) A novel dual-banked CIM-based fully digital self-attention accelerator using 8-bit parallel weight feeding. 2) A look-up-table (LUT) based fixed-point implementation reducing latency with minimal accuracy degradation. 3) A performance evaluation of a 32kb CIM-based self-attention accelerator implemented in 28nm, which achieves 26.1 TOPS/W at 0.85V and 2.31 TOPS/mm$^2$ at 1.2V, both with INT8 precision.

---

## 论文详细总结（自动生成）

这是一份关于论文《CIMple: Standard-cell SRAM-based CIM with LUT-based split softmax for attention acceleration》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）如 LLaMA 和 DeepSeek 在边缘设备部署时面临巨大挑战，主要源于其数十亿参数带来的高内存访问开销和计算资源限制。
*   **核心问题**：
    *   **数据搬运瓶颈**：传统架构中内存与计算单元间的大量数据移动导致高能耗。
    *   **CIM 局限性**：现有的存内计算（CIM）架构大多仅支持静态乘累加（MAC）操作，难以高效处理 Transformer 中的非线性操作（如 Softmax）。
    *   **Softmax 挑战**：Softmax 涉及复杂的指数运算和全局数据依赖，通常需要昂贵的浮点运算，且会造成流水线停顿。
    *   **可移植性差**：多数 CIM 采用定制宏设计，难以快速迁移到新的工艺节点。

### 2. 方法论：核心思想与关键技术
*   **CIMple 架构**：一种基于标准单元 SRAM 的全数字 CIM 加速器，专为自注意力机制设计。
*   **核心技术细节**：
    *   **标准单元 SRAM 设计**：使用 8T SRAM 位元并将其作为标准单元进行数字流程设计（Digital-on-top），提高了工艺可移植性和自动化程度。
    *   **双库（Dual-banked）结构**：支持 8 位并行权重馈送，通过 OAI（与或非）逻辑实现乘法，减少了信号翻转。
    *   **查找表（LUT）拆分 Softmax**：
        *   **定点化处理**：完全在定点域运算，避免了整数与浮点数的频繁转换。
        *   **分子分母拆分**：将指数运算（分子）和累加倒数运算（分母）拆分。利用两个 1D LUT 分别存储指数值和倒数近似值。
        *   **流水线优化**：无需等待所有输入完成即可开始部分计算，将 Softmax 延迟降低了 33%。
    *   **多模式映射**：通过灵活的切片（Tiling）和缓冲策略，支持 Encoder-only、Decoder-only 及 Encoder-Decoder 三种 Transformer 变体。

### 3. 实验设计
*   **实验环境**：采用 ST 28nm FD-SOI 工艺进行实现。
*   **数据集/场景**：
    *   **精度评估**：使用 TinyLlama 模型在 ARC Challenge、HellaSwag、OpenBookQA 和 Winogrande 四个自然语言处理任务上进行测试。
    *   **性能评估**：针对 32kb CIM 核心进行后综合（Post-synthesis）功耗分析和后布局（Post-layout）面积分析。
*   **Benchmark 与对比对象**：
    *   对比了 SOTA（最先进）的 CIM Transformer 加速器，包括 JSSC'24、ISSCC'22 (TranCIM)、ISSCC'23 (MultCIM) 和 ISSCC'25 等论文中的方案。

### 4. 资源与算力
*   **硬件规格**：32kb CIM 核心 + 16kb 全局缓冲区。
*   **算力表现**：
    *   **能效比**：在 0.85V 电压下达到 **26.1 TOPS/W**（INT8 精度，考虑稀疏性）。
    *   **面积效率**：在 1.2V 电压下达到 **2.31 TOPS/mm²**。
*   **训练/仿真说明**：文中未提及模型训练时长（属于推理加速器研究），实验数据基于 EDA 工具（Cadence Genus/Innovus）的仿真结果。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   **精度实验**：对比了 LUT Softmax 与 PyTorch 标准 LogSoftmax 的精度差异。
    *   **消融/参数实验**：测试了不同电压（0.85V-1.2V）和不同稀疏度（50%-87.5%）下的能效表现。
    *   **延迟实验**：对比了拆分 LUT 与非拆分 LUT 的延迟。
*   **充分性评价**：实验设计较为全面，涵盖了从底层电路指标到顶层应用精度的全方位评估。但由于是基于仿真（Post-layout/Synthesis）而非实测芯片（Silicon measurement），其数据在真实环境中的表现仍待验证。

### 6. 主要结论与发现
*   **Softmax 优化显著**：拆分 LUT 方法在保持精度损失极小（±0.6% 以内）的前提下，将 Softmax 相关的计算延迟缩短了 1/3。
*   **高面积效率**：得益于标准单元 SRAM 的紧凑布局，其面积效率显著优于对比的 SOTA 方案。
*   **能效竞争力**：虽然在绝对能效上未超越某些采用极端剪枝技术的方案，但在不牺牲精度的通用计算中表现优异。

### 7. 优点
*   **高可移植性**：采用标准单元流程，极大降低了在不同工艺节点间迁移的难度。
*   **硬件友好型 Softmax**：通过拆分 LUT 巧妙解决了非线性算子的硬件实现难题，实现了计算与存储的深度融合。
*   **灵活性**：单一架构即可适配多种 Transformer 模型结构。

### 8. 不足与局限
*   **缺乏实测数据**：所有性能指标均来自 EDA 仿真，而非流片实测，可能存在寄生参数估计偏差。
*   **电压缩放受限**：受限于标准单元库，电压最低仅能降至 0.85V，限制了在极低功耗场景下的能效潜力。
*   **稀疏性利用简单**：未集成专门的位跳过（Bit-skipping）逻辑，对稀疏性的利用主要体现在计算量的减少，而非动态功耗的深度节省。

（完）
