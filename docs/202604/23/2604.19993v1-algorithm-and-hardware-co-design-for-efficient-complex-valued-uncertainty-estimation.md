---
title: Algorithm and Hardware Co-Design for Efficient Complex-Valued Uncertainty Estimation
title_zh: 高效复值不确定性估计的算法与硬件协同设计
authors: "Zehuan Zhang, Mark Chen, He Li, Wayne Luk"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19993v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 算法与硬件协同设计，并自动搜索最优配置
tldr: 本研究针对复值神经网络（CVNN）无法量化预测不确定性的问题，首次提出了基于Dropout的贝叶斯复值神经网络（BayesCVNN）。通过算法与硬件协同设计，引入了自动搜索方法来优化实部和虚部的配置，并开发了定制化的FPGA加速器框架。实验表明，该方法在提升性能的同时降低了硬件开销，相比GPU实现达到了显著的加速比和极低的功耗，为复值应用提供了高效的不确定性估计方案。
selection_source: fresh_fetch
motivation: 现有的复值神经网络缺乏量化预测不确定性的能力，且在硬件部署上存在效率挑战。
method: 提出基于Dropout的贝叶斯复值神经网络，结合自动搜索算法优化模型配置，并设计了专用的FPGA硬件加速器。
result: 自动搜索找到的最优配置在性能和硬件成本上均优于手动模型，且FPGA加速器相比GPU实现了最高13倍的加速和极低的功耗。
conclusion: 算法与硬件的协同设计成功实现了高效的复值不确定性估计，为复值神经网络在资源受限环境下的应用奠定了基础。
---

## 摘要
复值神经网络 (CVNNs) 在处理涉及复数的任务方面具有显著优势。然而，现有的 CVNNs 无法量化预测不确定性。我们首次提出了基于 dropout 的贝叶斯复值神经网络 (BayesCVNNs)，以实现复值应用的不确定性量化，由于其模块化特性，该网络在硬件实现方面表现出广泛的适用性和高效性。此外，由于复值的双部分特性显著拓宽了设计空间，并支持基于层混合 (layer-mixing) 和部分混合 (part-mixing) 的新型配置，我们引入了一种自动搜索方法，以有效识别实部和虚部组件的最佳配置。为了便于部署，我们提出了一个框架，利用一组优化的构建模块，为 BayesCVNNs 生成定制的基于 FPGA 的加速器。实验表明，通过自动搜索可以有效地找到最佳配置，与人工设计的模型相比，在降低硬件成本的同时获得了更高的性能。与 GPU 实现相比，优化的加速器在不同模型上实现了约 4.5 倍和 13 倍的加速，且功耗不足 10%，在算法和硬件方面均优于现有工作。我们的代码已在 https://github.com/zehuanzhang/BayesCVNN.git 公开。

## Abstract
Complex-Valued Neural Networks (CVNNs) have significant advantages in handling tasks that involve complex numbers. However, existing CVNNs are unable to quantify predictive uncertainty. We propose, for the first time, dropout-based Bayesian Complex-Valued Neural Networks (BayesCVNNs) to enable uncertainty quantification for complex-valued applications, exhibiting broad applicability and efficiency for hardware implementation due to modularity. Furthermore, as the dual-part nature of complex values significantly broadens the design space and enables novel configurations based on layer-mixing and part-mixing, we introduce an automated search approach to effectively identify optimal configurations for both real and imaginary components. To facilitate deployment, we present a framework that generates customized FPGA-based accelerators for BayesCVNNs, leveraging a set of optimized building blocks. Experiments demonstrate the best configuration can be effectively found via the automated search, attaining higher performance with lower hardware costs compared with manually crafted models. The optimized accelerators achieve approximately 4.5x and 13x speedups on different models with less than 10% power consumption compared to GPU implementations, and outperform existing work in both algorithm and hardware aspects. Our code is publicly available at: https://github.com/zehuanzhang/BayesCVNN.git.

---

## 论文详细总结（自动生成）

这是一份关于论文《Algorithm and Hardware Co-Design for Efficient Complex-Valued Uncertainty Estimation》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：复值神经网络（CVNNs）在雷达、MRI和气象预报等领域表现优异，但现有的 CVNNs 缺乏**不确定性估计（Uncertainty Estimation）**能力，这在医疗和安全关键型应用中至关重要。
*   **研究背景**：
    *   直接将实值贝叶斯方法应用于复数域会导致信息丢失。
    *   复数运算（实部+虚部）使设计空间呈指数级增长，且计算复杂度是实值的四倍，内存需求翻倍，导致硬件部署困难。

### 2. 方法论：BayesCVNN 算法与硬件协同设计
*   **核心思想**：首次将蒙特卡罗 Dropout（MC Dropout）扩展到复数域，提出**贝叶斯复值神经网络（BayesCVNN）**，并通过算法与硬件的协同优化实现高效部署。
*   **关键技术细节**：
    *   **数学证明**：通过变分推理证明了在复数权重的实部、虚部或两部分同时应用 Dropout 均是有效的贝叶斯近似。
    *   **设计空间扩展**：由于复数的双部分特性，每层可选择“仅实部、仅虚部、两部分均加”三种 Dropout 配置，N 层网络拥有 $3^N$ 种配置，远大于实值网络的单一配置。
    *   **进化搜索（Evolutionary Search）**：引入硬件感知的进化算法，自动寻找满足精度、不确定性质量（ECE）和硬件资源约束的最优“层混合”与“部分混合”配置。
    *   **FPGA 加速器架构**：
        *   设计了支持复数运算的 HLS 模块。
        *   **映射策略**：提供“延迟优先（Latency-opt）”和“资源优先（Resource-opt）”两种方案，通过复用运算引擎平衡性能与成本。
        *   **开关机制**：在 Dropout 层引入开关，以支持搜索到的混合配置。

### 3. 实验设计
*   **数据集/场景**：
    *   **MNIST**：实值图像分类基准。
    *   **HAR**：基于雷达的人体活动识别（复值数据）。
    *   **S1SLC_CVDL**：合成孔径雷达（SAR）数据集，选取了芝加哥和休斯顿两个场景。
*   **模型与基准（Benchmark）**：
    *   模型包括 Bayesian ComplexLeNet、CHAR、CSAR-small 和 CSAR-large。
    *   对比方法：手动设计的全 Dropout 配置（Manual）、搜索获得的最优精度配置（MaxAcc）和最优校准配置（MinECE）。
    *   硬件对比：NVIDIA GeForce RTX 3090 Ti GPU vs. Xilinx Kintex XCKU115 FPGA。

### 4. 资源与算力
*   **硬件平台**：FPGA 使用 Xilinx Kintex XCKU115（20nm 工艺）；GPU 使用 RTX 3090 Ti（8nm 工艺）。
*   **算力细节**：
    *   GPU 运行频率 1.40 GHz，功耗约 112W。
    *   FPGA 运行频率 181 MHz，功耗仅为 3.81W - 4.65W。
*   **训练/推理时长**：文中未详细说明训练总时长，但给出了推理延迟：FPGA 在小模型上比 GPU 快 13 倍，在大模型上快 4.5 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单（LeNet）到复杂（10层卷积的 CSAR-large）的多种模型，并在实值和复值数据集上均进行了验证。
*   **消融实验**：详细分析了 RVNN 到 CVNN、CVNN 到 BayesCVNN 的硬件开销转换，以及不同映射方案（延迟 vs. 资源）的资源占用对比。
*   **客观性**：通过 Pareto 前沿图展示了搜索算法在不同硬件约束下的有效性，证明了“更多 Dropout 层并不一定带来更好性能”，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **搜索优势**：自动搜索发现的“混合配置”在精度上比手动设计提高 0.5%~2%，且能显著降低 ECE（不确定性误差）。
*   **硬件效率**：FPGA 加速器在保持不确定性估计能力的同时，能耗比 GPU 降低了两个数量级（>100x 能量效率提升）。
*   **协同效应**：证明了通过算法层的灵活配置（如仅在部分层的虚部加 Dropout）可以有效减少硬件开销而不损失模型可靠性。

### 7. 优点
*   **首创性**：填补了复值神经网络在贝叶斯不确定性估计领域的空白。
*   **灵活性**：提出的 Dropout 模块是“即插即用”的，具有很强的通用性。
*   **高效搜索**：利用复数域特有的设计空间进行优化，打破了传统实值贝叶斯网络的思维定式。
*   **软硬结合**：不仅有理论证明，还提供了完整的 HLS 硬件实现框架。

### 8. 不足与局限
*   **量化技术缺失**：虽然提到了量化，但实验主要集中在 Dropout 配置上，未深入探讨复值网络特有的非均匀量化对不确定性的影响。
*   **采样次数限制**：实验中采样次数固定为 3 次，在实际应用中，采样次数对不确定性质量和延迟有巨大影响，文中未对此进行深入的权衡分析。
*   **应用范围**：目前仅在分类任务上验证，未在 MRI 重建等回归类复值任务中测试。

（完）
