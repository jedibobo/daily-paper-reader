---
title: "CIMple: Standard-cell SRAM-based CIM with LUT-based split softmax for attention acceleration"
title_zh: CIMple：基于标准单元 SRAM 且具有基于查找表拆分 Softmax 的存内计算注意力加速器
authors: "Bas Ahn, Xingjian Tao, Manil Dev Gomony, Marc Geilen, Henk Corporaal"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15944v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 利用存内计算和基于查找表的Softmax进行边缘LLM加速的软硬协同设计
tldr: 针对大语言模型在边缘设备部署面临的资源受限和数据搬运挑战，本文提出了一种名为CIMple的全数字标准单元SRAM存内计算（CIM）加速器。该架构专为Transformer的自注意力机制设计，通过双库CIM结构和基于查找表（LUT）的分离式Softmax实现，克服了传统CIM仅支持静态乘累加且难以处理非线性运算的局限。在28nm工艺下，该加速器在INT8精度下实现了极高的能效比和面积效率，为边缘端LLM加速提供了高效方案。
selection_source: fresh_fetch
motivation: 传统存内计算架构主要支持静态乘累加运算，难以灵活支持Transformer模型中的非线性操作及多样化需求。
method: 提出一种基于标准单元SRAM的双库全数字CIM架构，采用8位并行权重馈送及基于查找表的定点Softmax实现。
result: 在28nm工艺下，32kb的CIMple加速器在0.85V时达到26.1 TOPS/W的能效，在1.2V时达到2.31 TOPS/mm²的面积效率。
conclusion: CIMple通过集成非线性运算支持和高效的数字CIM设计，显著提升了边缘设备上自注意力机制的计算效率。
---

## 摘要
大语言模型（LLM），如 LLaMA 和 DeepSeek，是基于 Transformer 架构构建的，该架构已成为在自然语言处理任务中实现最先进性能的标准模型。最近，在边缘设备上部署 LLM 的兴趣日益浓厚。尽管已经提出了较小的 LLM 模型，但它们通常仍包含数十亿个参数。由于边缘设备资源有限，这给边缘部署带来了重大挑战。存内计算（CIM）是一种极具前景的架构，它通过将计算逻辑直接集成到存储器中来减少数据搬运，从而解决这一问题。然而，现有的 CIM 架构仅支持静态乘累加（MAC）操作，这限制了它们在支持非线性操作和各种类型 Transformer 模型方面的可配置性。本文提出了一种名为 CIMple 的全数字、基于标准单元 SRAM 的 CIM 架构自注意力加速器，旨在克服 Transformer 模型中的这些限制。CIMple 的主要贡献包括：1) 一种新型的基于双库 CIM 的全数字自注意力加速器，采用 8 位并行权重馈送；2) 一种基于查找表（LUT）的定点实现，在精度损失极小的情况下降低了延迟；3) 对采用 28nm 工艺实现的 32kb 基于 CIM 的自注意力加速器进行了性能评估，在 INT8 精度下，该加速器在 0.85V 时达到 26.1 TOPS/W，在 1.2V 时达到 2.31 TOPS/mm²。

## Abstract
Large Language Models (LLMs) such as LLaMA and DeepSeek, are built on transformer architectures, which have become a standard model for achieving state-of-the-art performance in natural language processing tasks. Recently, there has been growing interest in deploying LLMs on edge devices. Although smaller LLM models are being proposed, they often still contain billions of parameters. Since edge devices are limited in their resources this poses a significant challenge for edge deployment. Compute-in-memory (CIM) is a promising architecture that addresses this by reducing data movement through the integration of computational logic directly into memory. However, existing CIM architectures support only static Multiply-Accumulate (MAC) operations which limit their configurability in supporting nonlinear operations and various types of transformer models. This paper presents a fully digital standard-cell SRAM-based CIM architecture accelerator for self-attention, called CIMple, designed to overcome these limitations, inside transformer models. The key contributions of CIMple are: 1) A novel dual-banked CIM-based fully digital self-attention accelerator using 8-bit parallel weight feeding. 2) A look-up-table (LUT) based fixed-point implementation reducing latency with minimal accuracy degradation. 3) A performance evaluation of a 32kb CIM-based self-attention accelerator implemented in 28nm, which achieves 26.1 TOPS/W at 0.85V and 2.31 TOPS/mm$^2$ at 1.2V, both with INT8 precision.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CIMple** 的全数字存内计算（CIM）加速器，专门针对 Transformer 架构中的自注意力（Self-Attention）机制进行了优化。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景**：大语言模型（LLM）如 LLaMA 和 DeepSeek 在边缘设备部署时面临巨大挑战，主要源于其数十亿参数带来的高额数据搬运开销和计算资源限制。
*   **核心问题**：
    1.  **CIM 局限性**：现有的存内计算架构大多仅支持静态的乘累加（MAC）运算，难以高效处理 Transformer 中的非线性操作（如 Softmax）。
    2.  **Softmax 瓶颈**：Softmax 涉及指数运算、除法及全局数据依赖，通常需要浮点运算，导致高延迟和高能耗。
    3.  **设计复杂性**：传统 CIM 往往采用定制化模拟设计，难以移植到新的工艺节点。

### 2. 方法论：核心思想与关键技术
*   **全数字标准单元设计**：采用基于标准单元的 8T-SRAM 结构，使设计完全兼容数字集成电路流程（Digital-on-top），易于在不同工艺间移植。
*   **双库（Dual-banked）CIM 架构**：支持 8 位并行权重馈送，通过 OAI（Or-And-Invert）逻辑实现乘法，并利用加法树进行累加。
*   **基于查找表（LUT）的拆分式 Softmax**：
    *   **定点化处理**：完全在定点域（Fixed-point）完成，避免了定点与浮点之间的转换。
    *   **计算拆分**：将分子（指数项）和分母（累加和的倒数）的计算拆分。利用 LUT 存储指数函数和倒数函数。
    *   **流水线优化**：通过预设最大量化值（$z_{quant\_max}$）代替动态寻找最大值，使得 Softmax 能够与矩阵乘法流水线化执行，无需等待整行数据计算完成。
*   **多模式支持**：通过灵活的映射策略，支持 Encoder-only、Decoder-only 和 Encoder-Decoder 三种 Transformer 变体。

### 3. 实验设计与基准测试
*   **工艺节点**：采用 ST 28nm FD-SOI 工艺实现。
*   **数据集/场景**：使用 **TinyLlama** 模型在四个自然语言处理任务上评估精度：ARC Challenge、HellaSwag、OpenBookQA 和 Winogrande。
*   **对比方法（Benchmark）**：
    *   **精度对比**：对比了 PyTorch 原生的 LogSoftmax（浮点精度）。
    *   **硬件对比**：与 SOTA（最先进）的 CIM 加速器进行对比，包括 TranCIM (ISSCC'22)、MultCIM (ISSCC'23)、CIMFormer (JSSC'24) 等。

### 4. 资源与算力说明
*   **硬件规格**：32kb CIM 核心 + 16kb 全局缓冲区。
*   **算力表现**：
    *   在 0.85V 电压下，能效达到 **26.1 TOPS/W**（INT8 精度）。
    *   在 1.2V 电压下，面积效率达到 **2.31 TOPS/mm²**。
*   **训练/仿真说明**：论文主要关注推理加速。功耗分析基于后综合（Post-synthesis）数据，面积分析基于后布局（Post-layout）数据。文中未提及具体的 GPU 训练时长，因为其核心贡献在于硬件架构设计。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   进行了不同稀疏度（50% 到 87.5%）下的能效测试。
    *   进行了电压频率缩放（DVFS）实验（0.85V 至 1.2V）。
    *   在 4 个主流 NLP 任务上验证了精度损失。
*   **充分性评价**：实验设计较为全面，涵盖了从底层电路参数（面积、功耗）到顶层应用精度（NLP 任务准确率）的完整链路。对比 SOTA 方法时采用了相同的工艺节点（28nm），具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **延迟降低**：提出的拆分式 Softmax 架构将自注意力机制的延迟降低了 **33%**。
*   **精度保持**：相比浮点模型，基于 LUT 的定点 Softmax 带来的精度波动极小（在 **±0.6%** 以内）。
*   **高效率**：通过标准单元优化，其面积效率显著优于现有的同类 Transformer 加速器。

### 7. 优点与亮点
*   **易移植性**：使用标准单元 SRAM 单元，极大降低了 CIM 技术的应用门槛和设计周期。
*   **软硬协同**：不仅优化了底层电路，还针对 Softmax 算法进行了硬件友好的重构（拆分 LUT 法）。
*   **高灵活性**：能够适配多种 Transformer 模型，而不仅仅局限于单一架构。

### 8. 不足与局限
*   **非实测数据**：能效和面积数据基于仿真（后综合/后布局），而非实际流片剪裁后的测试数据（Silicon measurements），在实际芯片中可能存在寄生参数导致的偏差。
*   **电压缩放受限**：受限于标准单元库的限制，电压未能进一步下探至更低范围（如 0.6V 以下），这限制了其在极低功耗场景下的能效表现。
*   **存储容量较小**：32kb 的片上存储对于数十亿参数的 LLM 来说仍然很小，实际应用中仍需频繁访问片外内存，这部分能耗未在核心评估中完全体现。

（完）
