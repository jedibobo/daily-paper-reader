---
title: "OneComp: One-Line Revolution for Generative AI Model Compression"
title_zh: OneComp：生成式人工智能模型压缩的一行代码革命
authors: "Yuma Ichikawa, Keiji Kimura, Akihiro Yoshida, Yudai Fujimoto, Hiroki Tokura, Yamato Arai, Yoshiyuki Ishii, Yusei Kawakami, Genki Shikada, Achille Jacquemond, Yoshihiko Fujisawa, Katsuki Fujisawa, Takumi Honda, Akira Sakai"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28845v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于硬件依赖模型压缩的自动化资源自适应流水线
tldr: 针对大模型部署面临的显存、延迟和硬件成本挑战，本文推出了开源压缩框架 OneComp。该框架将复杂的专家级压缩流程转化为自动化、资源自适应的流水线，通过混合精度规划和从层级到全局的渐进式量化策略，实现了模型性能与计算投入的正向关联。OneComp 简化了量化算法的选择与执行，有效填补了算法研究与工业级部署之间的鸿沟。
selection_source: fresh_fetch
motivation: 基础模型部署受限于高昂的硬件成本和复杂的量化流程，缺乏一种简单且高效的自动化压缩方案。
method: 提出 OneComp 框架，通过自动检查模型、规划混合精度并执行从层级到全局的渐进式量化阶段。
result: 实现了可复现、硬件感知的压缩流水线，确保模型质量随计算资源投入的增加而持续提升。
conclusion: OneComp 成功将前沿压缩研究转化为生产级工具，极大地降低了生成式 AI 模型的部署门槛。
---

## 摘要
基础模型的部署日益受到内存占用、延迟和硬件成本的限制。训练后压缩可以通过降低模型参数的精度来缓解这些瓶颈，且不会显著降低性能；然而，由于从业者需要应对量化算法、精度预算、数据驱动的校准策略以及依赖硬件的执行方案等碎片化现状，其实际应用仍然具有挑战性。我们提出了 OneComp，这是一个开源压缩框架，它将这种专家级工作流转化为可复现且资源自适应的流水线。给定模型标识符和可用硬件，OneComp 会自动检查模型，规划混合精度分配，并执行从层级压缩到块级细化再到全局细化的渐进式量化阶段。一个关键的架构选择是将第一个量化检查点视为可部署的基准点，确保后续的每个阶段都在改进同一个模型，并且随着计算投入的增加，模型质量也随之提升。通过将最先进的压缩研究转化为可扩展、开源且感知硬件的流水线，OneComp 弥合了算法创新与生产级模型部署之间的鸿沟。

## Abstract
Deploying foundation models is increasingly constrained by memory footprint, latency, and hardware costs. Post-training compression can mitigate these bottlenecks by reducing the precision of model parameters without significantly degrading performance; however, its practical implementation remains challenging as practitioners navigate a fragmented landscape of quantization algorithms, precision budgets, data-driven calibration strategies, and hardware-dependent execution regimes. We present OneComp, an open-source compression framework that transforms this expert workflow into a reproducible, resource-adaptive pipeline. Given a model identifier and available hardware, OneComp automatically inspects the model, plans mixed-precision assignments, and executes progressive quantization stages, ranging from layer-wise compression to block-wise refinement and global refinement. A key architectural choice is treating the first quantized checkpoint as a deployable pivot, ensuring that each subsequent stage improves the same model and that quality increases as more compute is invested. By converting state-of-the-art compression research into an extensible, open-source, hardware-aware pipeline, OneComp bridges the gap between algorithmic innovation and production-grade model deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《OneComp: One-Line Revolution for Generative AI Model Compression》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）部署受限于巨大的显存占用、高延迟和昂贵的硬件成本。虽然训练后量化（PTQ）是有效的解决方案，但现有的量化算法极其碎片化，涉及复杂的精度预算分配、校准策略和硬件依赖，导致从学术研究到工业生产的转化存在巨大鸿沟。
*   **研究动机**：开发一个自动化、资源自适应的开源框架，让非专家用户通过“一行代码”即可实现模型压缩，并确保模型质量随计算资源投入的增加而单调提升。

### 2. 方法论
OneComp 提出了一个渐进式的压缩流水线，核心思想是将第一个量化模型作为“基准点（Pivot）”，后续阶段不断对其进行优化：
*   **AutoBit（混合精度规划）**：利用整数线性规划（ILP）和激活感知误差度量，在给定的显存预算下自动为不同层分配最优比特数（如 2-8 bit 异构分配）。
*   **等效变换预处理**：集成 SmoothQuant（通道缩放）和 SpinQuant（学习旋转），通过数学等效变换消除激活值中的离群值（Outliers），降低量化难度。
*   **层级 PTQ 增强技术**：
    *   **QEP（量化误差传播）**：通过闭式解修正当前层的量化目标，补偿上游层累积的误差。
    *   **LPCD（子模块感知坐标下降）**：将相互关联的层（如 Q/K 投影）视为整体进行联合优化。
    *   **JointQ**：联合优化量化缩放因子（Scales）和整数权重，超越传统的 GPTQ。
*   **极端低比特格式（DBF/MDBF）**：针对 1-2 bit 压缩，采用双二进制分解格式，通过低秩量级包络提升表达能力。
*   **资源自适应流水线**：根据硬件能力自动选择执行路径：层级 PTQ（单显存受限） $\rightarrow$ 块级细化（MSE 蒸馏） $\rightarrow$ 全局细化（KL 散度蒸馏） $\rightarrow$ QLoRA 微调。

### 3. 实验设计
*   **数据集**：使用 C4 或 WikiText-2 进行校准（通常为 512 个样本）；在 WikiText-2 上评估困惑度（PPL），在 ARC、PIQA、WinoGrande 等任务上评估零样本（Zero-shot）准确率。
*   **模型覆盖**：涵盖 Llama-3 (8B)、Qwen3 (8B, 14B) 以及小规模模型如 Llama-3.2-1B、TinyLlama-1.1B 等。
*   **对比基准**：对比了 Uniform（均匀量化）、GPTQ、AWQ、DBF 等主流方法，并对 AutoBit、QEP、LPCD 等新增组件进行了消融实验。

### 4. 资源与算力
*   **算力需求**：论文强调了“资源自适应性”。层级 PTQ 阶段仅需单张消费级 GPU 即可完成；块级和全局细化阶段则需要更多显存（能容纳教师和学生模型）。
*   **具体说明**：文中未给出所有实验的总耗时或具体 GPU 型号列表，但明确指出层级 PTQ 在分钟到小时级别即可完成，而全局细化则需要更多的计算资源。

### 5. 实验数量与充分性
*   **实验规模**：论文在多个模型家族（Llama, Qwen）、多种参数规模（0.6B 到 14B）以及极宽的比特范围（1 bit 到 4 bit）下进行了测试。
*   **充分性**：实验设计较为全面，不仅验证了最终性能，还通过消融实验证明了每个优化阶段（层级 $\rightarrow$ 块级 $\rightarrow$ 全局）对准确率提升的贡献，实验结果具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **混合精度至关重要**：AutoBit 自动分配精度在相同显存占用下显著优于均匀量化，尤其是在 Llama-3 等对量化敏感的模型上。
*   **误差补偿有效性**：QEP 和 LPCD 能够显著缓解层级量化中的误差累积问题，使轻量级方法接近昂贵的全局优化效果。
*   **单调提升特性**：实验证实，随着计算投入从层级转向全局蒸馏，模型性能呈现稳定的单调增长。
*   **极端压缩可行性**：MDBF 格式在 1-1.5 bit 极低比特下仍能保留模型的基本推理能力。

### 7. 优点
*   **易用性**：极大地简化了量化流程，实现了“一行代码”自动化执行。
*   **灵活性**：框架具有高度可扩展性，支持从 1 bit 到 8 bit 的全频谱压缩。
*   **理论与工程结合**：不仅引入了 QEP、LPCD 等新算法，还考虑了硬件部署的实际约束（如混合精度规划）。

### 8. 不足与局限
*   **部分功能待发布**：文中提到全局 PTQ 阶段和 MDBF 格式在当前版本中仍处于“计划发布”或开发阶段，尚未完全开源。
*   **激活量化缺失**：目前核心重点在于权重（Weight-only）量化，虽然提及了激活和 KV 缓存量化，但在本文的实验部分未作为核心展示。
*   **校准数据依赖**：PTQ 效果仍高度依赖于校准数据集的质量和分布，对于特定领域任务可能需要手动调整校准集。

（完）
