---
title: "TetrisG-SDK: Efficient Convolutional Layer Mapping with Adaptive Windows and Grouped Convolutions for Fast In-Memory Computing"
authors: "Ke Dong, Kejie Huang, Tao Luo, Bo Wang"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25377v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 存内计算的高效卷积层映射
tldr: 提出TetrisG-SDK框架，优化卷积在存内计算硬件上的映射以降低延迟。
selection_source: fresh_fetch
motivation: 存内计算的高效卷积层映射。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Shifted-and-Duplicated-Kernel (SDK) mapping has emerged as an effective strategy to accelerate convolutional layers on compute-in-memory (CIM) hardware. However, existing SDK variants (e.g., VWC-SDK) merely optimize mapping for a single CIM macro, leaving inter-macro parallelism unexplored. Moreover, their mapping methodologies are still suboptimal. To address these limitations, we present TetrisG-SDK, a novel framework that employs adaptive windows to boost mapping performance. The proposed windows accommodate more input channels, increase array utilization at marginal space, and adapt to different channel depths. More importantly, TetrisG-SDK reduces compute latency by searching for optimal window configurations across multiple CIM macros with a fixed hardware budget. Besides, it incorporates grouped convolution to further decrease computing cycles while maintaining near-lossless model accuracy. In addition, TetrisG-SDK integrates a validated CIM hardware simulator to provide accurate system-/application-level estimations of latency, area and energy.   Compared to the single-macro VWC-SDK, the proposed framework achieves a speed-up by 1.2x, 1.3x, and 1.3x for CNN8, GoogLeNet Inception, and DenseNet40 models, respectively. When deployed on the simulator, it reduces system-level latency and energy by 2.4x and 1.7x for CNN8, 1.3x and 1.2x for Inception, and 1.3x and 1.6x for DenseNet40, respectively. When leveraging macro-level parallelism, TetrisG-SDK reduces the Energy-Delay-Area-Product (EDAP) by 70% for CNN8, 68% for Inception, and 36% for DenseNet40 compared to its non-grouped counterpart. These results manifest that TetrisG-SDK is a promising solution to efficiently mapping convolutional layers on CIM hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《TetrisG-SDK: Efficient Convolutional Layer Mapping with Adaptive Windows and Grouped Convolutions for Fast In-Memory Computing》的结构化总结：

### 1. 核心问题与整体含义
本文针对**存内计算（CIM）**硬件在加速卷积神经网络（CNN）时的**映射效率**问题展开研究。
*   **背景**：传统的“平移与重复内核（SDK）”映射方法虽然能提高数据复用，但存在以下痛点：
    *   **单宏限制**：现有方法（如 VWC-SDK）仅优化单个 CIM 宏，未考虑多宏并行。
    *   **窗口僵化**：使用固定形状的滑动窗口，导致 CIM 阵列利用率低，尤其是在处理图像边缘（边缘空间）和不同通道深度时。
*   **核心目标**：提出 **TetrisG-SDK** 框架，通过自适应窗口和分组卷积技术，在多宏硬件预算下最小化计算延迟并提高能效。

### 2. 方法论
TetrisG-SDK 结合了灵活的几何映射策略与模型压缩技术，核心组件包括：
*   **宏网格搜索（Macro-Grid Search）**：在给定的硬件预算（如 $P$ 个宏）下，通过算法搜索最优的 $r \times c$ 宏排列方案，以最小化总计算周期。
*   **正方形倾向窗口（Square-Inclined Window）**：基于算术几何均值不等式，优先搜索接近正方形的窗口形状，以减少 CIM 阵列行数占用，从而在同一阵列中容纳更多输入通道。
*   **边缘窗口优化（Marginal Window）**：针对输入特征图边缘无法被完整窗口覆盖的情况，生成特殊的边缘窗口，消除无效输入（Null-Input），提升阵列利用率。
*   **深度最优窗口（Depth-Optimal Window）**：根据通道划分后的实际深度动态调整窗口形状，并配合轻量级通道剪枝。
*   **分组卷积（Grouped Convolutions, "G"）**：引入分组卷积以减少参数量和计算复杂度，扩大并行窗口的搜索空间，进一步降低计算周期。

### 3. 实验设计
*   **数据集**：MNIST、SVHN、CIFAR-10、Tiny ImageNet。
*   **模型 Benchmark**：CNN8、GoogLeNet Inception 模块、DenseNet40、MobileNet。
*   **对比方法**：img2col（传统方法）、SDK、VW-SDK、VWC-SDK（最先进的 SDK 变体）。
*   **硬件仿真**：集成并改进了 **DNN+NeuroSim** 仿真平台，采用 22nm CMOS 工艺，基于 SRAM 的 CIM 架构进行系统级评估。

### 4. 资源与算力
*   **硬件参数**：仿真设置时钟频率为 1GHz，工作温度 300K，CIM 阵列大小从 $64 \times 64$ 到 $512 \times 512$ 不等。
*   **软件实现**：使用 PyTorch 进行模型训练和分组卷积验证。
*   **说明**：文中未明确提及训练这些模型所使用的具体 GPU 型号或训练时长，重点在于映射算法的计算周期分析和推理阶段的仿真。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单（CNN8）到复杂（Inception, DenseNet）的多种网络拓扑。
*   **消融实验**：论文进行了逐步消融研究（SI 正方形窗口、MW 边缘窗口、DO 深度优化、G 分组卷积），验证了每个组件对延迟和能耗的贡献。
*   **多维度评估**：不仅对比了计算周期（Cycles），还对比了系统级延迟、动态能耗、阵列利用率以及面积延迟能量乘积（EDAP）。
*   **公平性**：在对比精度时，确保了所有方法在相同的数据集和训练策略下进行，实验设计较为客观充分。

### 6. 主要结论与发现
*   **性能提升**：相比单宏 VWC-SDK，TetrisG-SDK 在 CNN8、Inception 和 DenseNet40 上分别实现了 1.2x、1.3x 和 1.3x 的加速。
*   **系统级优势**：在 NeuroSim 仿真中，CNN8 的延迟和能耗分别降低了 2.4x 和 1.7x。
*   **多宏并行**：利用多宏并行性后，EDAP（综合性能指标）在不同模型上降低了 36% 至 70%。
*   **精度保持**：引入分组卷积后，模型精度损失极小（通常小于 0.5%），在某些情况下（如 DenseNet40）甚至因泛化性增强而略有提升。

### 7. 优点与亮点
*   **灵活性**：受“俄罗斯方块（Tetris）”启发，窗口形状不再固定，极大地提高了 CIM 阵列的填充率。
*   **全局优化**：首次将 SDK 映射从单宏扩展到多宏网格配置搜索，更符合现代 AI 芯片的多核架构。
*   **软硬协同**：结合了算法层的分组卷积与硬件层的映射优化，实现了端到端的性能提升。
*   **边缘处理**：精细化处理了卷积边界问题，这在以往的映射研究中常被忽略。

### 8. 不足与局限
*   **精度风险**：虽然实验显示精度损失微小，但对于某些对精度极其敏感的任务，分组卷积的引入仍需重新训练和精细调优。
*   **搜索开销**：虽然宏网格搜索是离线的，但随着网络层数和硬件规模增加，搜索空间会变大。
*   **架构局限**：研究主要集中在基于 SRAM 的 CIM 架构，虽然理论上可扩展，但未详细讨论在 RRAM 或 MRAM 等非易失性存储介质上的非线性影响。

（完）
