---
title: "DenseScout: Algorithm-System Co-design for Budgeted Tiny Object Selection on Edge Platforms"
authors: "Xiong Zhouzhi, Zimo Zeng, Yi Chen, Shuqi Xu, Yunfeng Yan, Donglian Qi"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25300v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对具有计算和延迟约束的边缘平台的算法-系统联合协同设计
tldr: 针对边缘平台微小目标感知的计算与延迟限制，本文提出DenseScout，一种仅1.01M参数的轻量级密集响应选择器。该方案通过算法与系统协同设计，利用轻量级代理输入直接对高分辨率场景中的候选补丁进行排序，并结合传输感知的运行时优化，在低预算场景下显著提升了感知召回率和端到端实时性能。
selection_source: fresh_fetch
motivation: 现有的基于检测器的前端在低计算预算下难以兼顾补丁筛选效率与实际部署中的端到端延迟要求。
method: 提出轻量级选择器DenseScout进行补丁排序，并开发了支持异构边缘设备的传输感知运行时系统及QoS约束召回率指标。
result: 实验显示DenseScout在低预算补丁选择上优于传统检测器，且在RK3588和Jetson Orin NX平台上表现出更高的部署效能。
conclusion: 边缘微小目标感知应通过算法与系统的协同设计来优化，以平衡模型质量与运行时效率。
---

## Abstract
Deploying tiny object perception on edge platforms is challenging because practical systems must satisfy both strict compute budgets and end-to-end latency constraints. A common strategy is to first select a small number of candidate patches from a high-resolution image and then apply downstream processing only to the selected regions. However, existing detector-based frontends are not well aligned with this setting: strong offline detection accuracy does not necessarily yield effective low-budget patch prioritization, nor does it guarantee usable performance once transport and inference delays are considered. In this work, we study budgeted tiny object selection on edge platforms from a joint algorithm--system perspective. We present DenseScout, a lightweight dense-response selector with only 1.01M parameters, which directly ranks candidate patch locations from a high-resolution scene via a lightweight proxy input and is better aligned with low-budget tiny-object prioritization than detector-style frontends. To bridge offline selector quality and deployable utility, we further develop a transport-aware runtime realization on heterogeneous edge devices and adopt QoS-constrained recall, which counts a target as successfully perceived only if it is covered by the selected regions and the end-to-end processing finishes before the deadline. Experiments show that DenseScout consistently outperforms detector-based baselines in offline budgeted patch-selection evaluation, especially in low-budget regimes, while cross-platform results on RK3588 and Jetson Orin NX show that deployable performance depends jointly on selector quality and runtime realization efficiency. These results suggest that edge tiny object perception should be optimized as an algorithm--system co-design problem rather than as isolated model selection.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DenseScout** 的算法-系统协同设计方案，旨在解决边缘平台上高分辨率图像中微小目标（Tiny Object）感知的效率与延迟矛盾。

### 1. 核心问题与整体含义（研究动机和背景）
在无人机巡检、遥感和安防监控等场景中，目标在 4K 高清图像中往往仅占不到 0.05% 的面积。传统的处理方式是全图检测或切片检测，但边缘设备（如嵌入式芯片）算力有限，难以满足实时性要求。
*   **研究动机**：现有的“先筛选补丁、再局部检测”的流水线中，前端筛选器通常直接借用通用检测器（如 YOLO）。然而，高检测精度（mAP）并不等同于在极低计算预算下的补丁筛选效率。此外，离线评估往往忽略了数据传输、内存拷贝和后处理带来的端到端延迟。
*   **核心问题**：如何设计一个既能精准筛选含目标补丁，又能满足边缘平台严格截止日期（Deadline）约束的轻量级前端系统？

### 2. 方法论：核心思想与关键技术
论文提出了 **DenseScout**，一个从算法和系统两方面协同优化的方案：
*   **算法层面（DenseScout 模型）**：
    *   **轻量级架构**：仅 1.01M 参数。采用 MobileNetV3-Small 作为骨干网络，配合 Tiny-FPN 进行多尺度特征融合。
    *   **密集响应预测**：不进行复杂的边界框回归，而是将 640x640 的代理输入图映射为 80x80 的密集响应图（Heatmap）。
    *   **Top-K 筛选**：通过局部最大值抑制（NMS）提取峰值，并根据响应强度对候选补丁中心进行排序，直接输出优先级最高的 K 个补丁。
*   **系统层面（传输感知运行时）**：
    *   **减少拷贝传输**：针对边缘平台（如 RK3588）开发了“零拷贝”启发式的传输路径，减少 CPU 参与和内存同步开销。
    *   **QoS 约束召回率指标**：提出一种新的评价标准，只有当目标被补丁覆盖且整个处理流程在规定时间内（如 33.3ms）完成时，才计为成功感知。

### 3. 实验设计
*   **数据集**：
    *   **VisDrone & DOTA**：用于评估通用的微小目标补丁筛选能力（Recall@Ratio）。
    *   **InsPLAD**：真实的无人机电力巡检数据集，用于评估边缘平台的部署性能。
*   **Benchmark 与对比方法**：
    *   对比了 YOLOv8n, YOLO11n, NanoDet-Plus, RT-DETR-light 等主流轻量级检测器。
    *   对比了专门的补丁筛选方法 DPR。
*   **硬件平台**：NVIDIA Jetson Orin NX 和瑞芯微 RK3588。

### 4. 资源与算力
*   **模型规模**：DenseScout 仅需 0.72 GFLOPs。
*   **训练细节**：使用了 AdamW 优化器、余弦退火学习率策略以及数据增强（翻转、HSV 抖动）。
*   **算力说明**：论文**未明确说明**训练所使用的具体 GPU 型号、数量及训练总时长。实验重点在于推理阶段的资源消耗和延迟表现。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   在两个大型公开数据集上进行了详尽的召回率矩阵测试（Recall@K, Recall@Ratio）。
    *   进行了消融实验，验证了 Tiny-FPN 结构和训练策略对性能的提升。
    *   在两种架构完全不同的边缘芯片上进行了实测，包括单流和多流（3-stream）并发场景。
*   **充分性评价**：实验设计较为客观、公平。通过统一的补丁选择协议对比了不同架构的模型，并引入了系统层面的延迟抖动（Jitter）分析，充分证明了协同设计的必要性。

### 6. 主要结论与发现
*   **筛选与检测的脱节**：实验证明，全图检测精度高的模型（如 RT-DETR）在低预算补丁筛选任务中表现往往不如专门设计的轻量级选择器。
*   **低预算优势**：在 1%–4% 的极低面积预算下，DenseScout 的召回率显著领先。
*   **部署可行性**：在 RK3588 平台上，DenseScout 是唯一能在 33.3ms 内完成任务并保持高召回率的方案，其他检测器前端因后处理或推理过慢而导致 QoS 严重下降。

### 7. 优点
*   **协同设计思路**：不孤立地看模型精度，而是将数据传输、硬件加速和任务截止日期统一考虑。
*   **极致轻量化**：1M 级别的参数量极大地降低了边缘设备的存储和计算压力。
*   **工业实用性**：提出的 QoS 指标和零拷贝优化对实际工程部署具有很强的指导意义。

### 8. 不足与局限
*   **后端依赖性**：DenseScout 仅负责“选哪里”，最终的感知精度仍取决于后端检测器的性能。
*   **场景局限**：该方法假设目标是稀疏分布的。如果图像中目标极其密集（如布满全图），补丁筛选策略的收益将大幅降低。
*   **训练透明度**：缺乏训练资源消耗的具体数据，不便于评估训练成本。

（完）
