---
title: HiFloat4 Format for Language Model Pre-training on Ascend NPUs
title_zh: 昇腾 NPU 上用于语言模型预训练的 HiFloat4 格式
authors: "Mehran Taghian, Yunke Peng, Xing Huang, Yao Wang, Yaoyuan Wang, Wei Guo, Yuanyong Luo, Tianchi Hu, Junsong Wang, Xin Wang, Hu Liu, Yu Cheng, Ziwei Yu, Hongliang Li, Mehdi Rahimifar, Lei Yan, Xuefei Wang, Zhuang Ma, Lei Liu, Hui Yu, Anandharaju Durai Raju, Hoang Le, Hei Yi Mak, Tanzila Rahman, Shadan Golestan"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08826v1"
tags: ["query:hwnas"]
score: 8.0
evidence: NPU上提高计算吞吐量和内存效率的低精度格式
tldr: "本研究针对大语言模型训练的高昂成本，在华为昇腾NPU上探索了HiFloat4低精度格式的应用。通过将线性及专家GEMM操作转换为FP4精度，并对比MXFP4格式，系统评估了其在稠密模型（如Pangu、LLaMA）和混合专家（MoE）模型中的表现。研究提出了一系列稳定化技术，在显著提升计算吞吐量和内存效率的同时，将数值误差控制在全精度基准的1%以内，为大规模模型的高效预训练提供了重要参考。"
selection_source: carryover_cache
motivation: 旨在通过低精度训练技术降低大语言模型在训练和部署过程中的巨大计算与内存开销。
method: 在华为昇腾NPU上实现HiFloat4格式，并针对稠密及MoE架构开发了配套的FP4数值稳定化训练技术。
result: "实验表明HiFloat4能显著提升吞吐量，且稳定化技术使相对误差保持在全精度基准的1%以内。"
conclusion: 该研究验证了HiFloat4在昇腾NPU大规模预训练中的实用性，并揭示了不同FP4格式间的性能权衡。
---

## 摘要
大型基础模型已成为现代机器学习的核心，其性能随模型规模和数据量的增长而呈现出可预测的扩展性。然而，训练和部署此类模型会产生巨大的计算和内存开销，这促使了低精度训练技术的发展。最近的研究表明，4位浮点（FP4）格式（如 MXFP4 和 NVFP4）可以成功应用于大语言模型（LLM）中的线性 GEMM 操作，与高精度基准相比，计算吞吐量和内存效率提升高达 4 倍。在本研究中，我们探讨了近期为华为昇腾 NPU 提出的 HiFloat4 FP4 格式，并在大规模训练场景下将其与 MXFP4 进行了系统比较。所有实验均在昇腾 NPU 集群上进行，线性及专家 GEMM 操作完全以 FP4 精度执行。我们评估了稠密架构（如 Pangu 和 LLaMA 类模型）以及混合专家（MoE）模型，其中标准线性层和专家特定的 GEMM 均在 FP4 下运行。此外，我们探索了专为 FP4 训练定制的稳定化技术，这些技术显著减少了数值退化，在保持 4 位计算效率优势的同时，将相对误差控制在全精度基准的 1% 以内。我们的结果提供了 NPU 上 FP4 训练的全面实证研究，并突出了在大规模稠密模型和 MoE 模型中不同 FP4 格式之间的实际权衡。

## Abstract
Large foundation models have become central to modern machine learning, with performance scaling predictably with model size and data. However, training and deploying such models incur substantial computational and memory costs, motivating the development of low-precision training techniques. Recent work has demonstrated that 4-bit floating-point (FP4) formats--such as MXFP4 and NVFP4--can be successfully applied to linear GEMM operations in large language models (LLMs), achieving up to 4x improvements in compute throughput and memory efficiency compared to higher-precision baselines. In this work, we investigate the recently proposed HiFloat4 FP4 format for Huawei Ascend NPUs and systematically compare it with MXFP4 in large-scale training settings. All experiments are conducted on Ascend NPU clusters, with linear and expert GEMM operations performed entirely in FP4 precision. We evaluate both dense architectures (e.g., Pangu and LLaMA-style models) and mixture-of-experts (MoE) models, where both standard linear layers and expert-specific GEMMs operate in FP4. Furthermore, we explore stabilization techniques tailored to FP4 training that significantly reduce numerical degradation, maintaining relative error within 1% of full-precision baselines while preserving the efficiency benefits of 4-bit computation. Our results provide a comprehensive empirical study of FP4 training on NPUs and highlight the practical trade-offs between FP4 formats in large-scale dense and MoE models.

---

## 论文详细总结（自动生成）

这篇论文由华为团队撰写，重点研究了在华为昇腾（Ascend）NPU上使用 **HiFloat4 (HiF4)** 格式进行大语言模型（LLM）预训练的技术方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着大语言模型规模的指数级增长，训练过程面临巨大的计算开销和内存瓶颈。虽然 FP8 精度已被广泛应用，但进一步降低到 4 位浮点（FP4）精度在理论上能提供更高的吞吐量（最高 4 倍提升），但在实际训练中会面临严重的**数值不稳定、量化偏置和梯度下溢**等挑战。
*   **研究背景**：论文旨在探索华为自研的 HiFloat4 格式在昇腾 NPU 上的表现，并与行业标准 MXFP4 进行对比，验证其在大规模稠密模型和混合专家（MoE）模型预训练中的可行性。

### 2. 方法论：核心思想与关键技术
*   **HiFloat4 (HiF4) 格式设计**：
    *   **三级分层缩放**：采用 64 个元素为一个块（Block）。第一级使用 8 位 E6M2 格式作为全局缩放因子；第二、三级使用 1 位微指数进行细粒度调整。
    *   **数据表示**：4 位数据元素采用 S1P2（等效于 E1M2）格式。这种设计在保持 4 位存储的同时，通过分层元数据显著扩展了动态范围。
*   **稳定化技术**：
    *   **随机哈达玛变换 (RHT)**：在权重梯度（$dW$）计算前应用，通过旋转坐标系使异常值分布更均匀，减少量化误差。
    *   **随机舍入 (SR)**：针对 MXFP4 格式，在梯度量化时使用 SR 以消除系统性偏置；而 HiF4 表现更稳健，仅需最近舍入（NR）。
    *   **无截断缩放 (TF-Scaling)**：针对 MXFP4 调整缩放因子以避免硬截断，HiF4 因其设计特性无需此操作。
*   **量化策略**：将模型中约 75%-90% 的线性层（GEMM 操作）转换为 FP4 精度，包括前向传播、反向传播梯度计算。

### 3. 实验设计
*   **模型架构**：
    *   **OpenPangu-1B**（稠密模型）
    *   **Llama3-8B**（稠密模型）
    *   **Qwen3-MoE-30B**（混合专家模型，Top-8 路由）
*   **基准测试 (Benchmark)**：以全精度 **BF16** 训练作为性能基准。
*   **对比方法**：对比了 **HiF4** 和 **MXFP4** 两种 4 位格式在不同稳定化技术组合下的表现。
*   **数据集**：所有模型均在 50B（500 亿）Token 的数据集上进行预训练。

### 4. 资源与算力
*   **硬件平台**：实验全部在 **华为昇腾（Ascend）NPU 集群**上完成，利用了 Da Vinci 架构中的 Cube 单元进行矩阵运算加速。
*   **具体规模**：文中提到了使用 NPU 集群，但**未明确给出具体的 NPU 型号（如 910B/910C）数量以及总训练时长（小时/天）**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 30B 不同规模、不同架构（Dense 和 MoE）的模型，每组实验训练 50B Token，具有较强的代表性。
*   **消融实验**：针对 RHT、SR、TF-Scaling 等稳定化组件做了详细的消融研究（见表 4），验证了各组件对不同格式的贡献。
*   **客观性**：实验对比了主流的 MXFP4 格式，并采用了统一的优化器配置（Adam/AdamW）和学习率调度，实验设计较为公平。

### 6. 主要结论与发现
*   **HiF4 优于 MXFP4**：在相同条件下，HiF4 的相对损失误差（约 0.85%-1.19%）一致低于 MXFP4（约 1.44%-1.79%）。
*   **稳定性差异**：HiF4 具有更强的固有稳定性，仅需 RHT 即可达到理想精度；而 MXFP4 必须同时结合 SR、RHT 和 TF-Scaling 才能收敛。
*   **规模效应**：随着模型参数量增加，FP4 与 BF16 之间的性能差距趋于缩小，证明了 FP4 在超大规模模型训练中的潜力。
*   **MoE 适配性**：在 MoE 模型中，FP4 能够覆盖约 95.9% 的计算量，显著降低了专家层的存储和通信开销。

### 7. 优点
*   **硬件协同设计**：HiF4 格式与昇腾 NPU 的 256 位指令带宽高度对齐，能够最大化硬件利用率。
*   **低开销稳定化**：相比于其他需要复杂 EMA 或多次量化扫描的方法，本文采用的 RHT 方案计算开销极低。
*   **全流程量化**：实现了前向和反向 GEMM 的全面 FP4 化，而非仅针对部分层，具有更高的实用价值。

### 8. 不足与局限
*   **对比范围有限**：未直接对比 NVIDIA 的 **NVFP4** 格式（文中解释是因为硬件架构差异），这限制了跨平台性能的参考。
*   **训练长度不足**：50B Token 仅为预训练的早期阶段，尚未验证在数万亿（Trillions）Token 完整预训练周期下是否存在后期不稳定性。
*   **下游任务评估缺失**：主要关注 Loss 曲线，缺乏在具体下游任务（如 MMLU、GSM8K）上的准确率评测。
*   **应用场景限制**：目前仅针对预训练，未探讨在强化学习（RLHF）或长文本场景下的表现。

（完）
