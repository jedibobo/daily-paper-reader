---
title: "TCL: Enabling Fast and Efficient Cross-Hardware Tensor Program Optimization via Continual Learning"
title_zh: TCL：通过持续学习实现快速且高效的跨硬件张量程序优化
authors: "Chaoyao Shen, Linfeng Jiang, Yixian Shen, Tao Xu, Guoqing Li, Anuj Pathania, Andy D. Pimentel, Meng Zhang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12891v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 跨多样硬件进行快速张量程序优化的可迁移编译框架
tldr: 针对深度学习编译器在跨硬件优化时面临的数据采集成本高和迁移性差的问题，本文提出TCL框架。该框架通过RDU采样器实现高效主动学习，利用轻量化Mamba代价模型捕捉长程依赖，并结合持续知识蒸馏实现跨平台知识迁移。实验表明，TCL在显著缩短调优时间的同时，有效降低了模型推理延迟，实现了高效且可迁移的张量程序优化。
selection_source: fresh_fetch
motivation: 现有的张量程序优化方法依赖大规模离线数据且跨平台迁移能力不足，导致数据采集成本高昂且优化效率低下。
method: 提出由RDU采样策略、Mamba代价模型和持续知识蒸馏构成的框架，实现高效的数据筛选、性能预测与跨硬件知识迁移。
result: "在CPU和GPU平台上，TCL的调优速度比Tenset-MLP快12-16倍，且推理延迟降低了13%至20%。"
conclusion: TCL通过结合主动学习与持续学习技术，为跨硬件的高效张量程序优化提供了一种低成本、高性能的通用解决方案。
---

## 摘要
深度学习（DL）编译器依赖代价模型和自动调优来针对目标硬件优化张量程序。然而，现有方法依赖于大规模离线数据集，导致了高昂的采集成本，且在不同平台间的迁移性欠佳。在本文中，我们提出了 TCL，这是一个新型的高效且可迁移的编译器框架，旨在解决上述挑战，实现跨多种硬件平台的快速张量程序优化。具体而言，TCL 构建于三个核心赋能技术之上：(1) RDU 采样器：一种数据高效的主动学习策略，通过联合优化代表性（Representativeness）、多样性（Diversity）和不确定性（Uncertainty），仅需选择 10% 的张量程序，在保持接近原始模型准确率的同时，大幅降低了数据采集成本；(2) 一种基于 Mamba 的新型代价模型：该模型能够高效捕捉长程调度依赖关系，并通过减少参数化和轻量级序列建模，在预测准确率与计算成本之间取得了良好的平衡；(3) 持续知识蒸馏框架：该框架能够有效且逐步地在多个硬件平台间迁移知识，同时避免了传统多任务学习通常会导致的参数爆炸和数据依赖问题。广泛的实验验证了各个赋能技术以及 TCL 整体框架的有效性。在 CPU 和 GPU 平台上优化一系列主流深度学习模型时，与 Tenset-MLP 相比，TCL 的调优速度平均分别提升了 16.8 倍和 12.48 倍，推理延迟分别降低了 1.20 倍和 1.13 倍。

## Abstract
Deep learning (DL) compilers rely on cost models and auto-tuning to optimize tensor programs for target hardware. However, existing approaches depend on large offline datasets, incurring high collection costs and offering suboptimal transferability across platforms. In this paper, we introduce TCL, a novel efficient and transferable compiler framework for fast tensor program optimization across diverse hardware platforms to address these challenges. Specifically, TCL is built on three core enablers: (1) the RDU Sampler, a data-efficient active learning strategy that selects only 10% of tensor programs by jointly optimizing Representativeness, Diversity, and Uncertainty, substantially reducing data collection costs while maintaining near-original model accuracy; (2) a new Mamba-based cost model that efficiently captures long-range schedule dependencies while achieving a favorable trade-off between prediction accuracy and computational cost through reduced parameterization and lightweight sequence modeling; and (3) a continuous knowledge distillation framework that effectively and progressively transfers knowledge across multiple hardware platforms while avoiding the parameter explosion and data dependency issues typically caused by traditional multi-task learning. Extensive experiments validate the effectiveness of each individual enabler and the holistic TCL framework. When optimizing a range of mainstream DL models on both CPU and GPU platforms, TCL achieves, on average, 16.8x and 12.48x faster tuning time, and 1.20x and 1.13x lower inference latency, respectively, compared to Tenset-MLP.

---

## 论文详细总结（自动生成）

### 论文总结：TCL - 跨硬件张量程序优化的持续学习框架

#### 1. 核心问题与研究背景
*   **研究动机**：深度学习（DL）编译器（如 TVM、Ansor）依赖“代价模型（Cost Model）”来预测张量程序在特定硬件上的性能。
*   **核心痛点**：
    *   **数据采集成本极高**：为单一硬件收集足够的离线训练数据通常需要 40-60 天。
    *   **模型效率与精度权衡**：传统的 MLP 精度不足，而 Transformer 模型在处理长序列调度指令时计算开销（$O(n^2)$）过大。
    *   **跨硬件迁移困难**：现有的迁移学习多为一对一，而多任务学习（MTL）在硬件数量增加时面临参数爆炸和数据同步依赖问题。
*   **整体含义**：TCL 旨在通过主动学习减少数据需求，利用新型架构提升预测效率，并通过持续学习实现跨硬件知识的无缝积累。

#### 2. 方法论
TCL 框架由三个核心模块组成：
*   **RDU 采样器（主动学习策略）**：
    *   **核心思想**：联合优化代表性（Representativeness，算子类型分布）、多样性（Diversity，性能得分分布）和不确定性（Uncertainty，模型预测方差）。
    *   **流程**：从无标签池中筛选出最具信息量的 10% 样本进行标注，显著降低硬件实测的时间成本。
*   **基于 Mamba 的代价模型**：
    *   **技术细节**：采用状态空间模型（SSM）架构替代 Transformer。利用 Mamba 的选择性机制捕捉调度指令间的长程依赖。
    *   **优势**：将计算复杂度从 $O(n^2)$ 降低至线性 $O(n)$，模型极其轻量（仅 0.35MB），适合编译器集成。
*   **持续知识蒸馏框架（CKD）**：
    *   **核心思想**：采用“硬件知识库（KB）”和“硬件活动列（AC）”的双路结构。
    *   **技术细节**：在学习新硬件时，冻结 KB 以保留旧知识，通过 AC 学习新特征；随后通过知识蒸馏将 AC 的知识整合进 KB，并引入弹性权重巩固（EWC）算法防止灾难性遗忘。

#### 3. 实验设计
*   **数据集**：
    *   公开数据集：Tenset（包含 6 种硬件平台的数据）。
    *   自建数据集：在 Intel i7-12700F CPU 和 NVIDIA RTX 3080Ti GPU 上历时数月采集的大规模张量程序数据集（已开源）。
*   **Benchmark 模型**：ResNet-50, MobileNet-V2, ResNeXt-50, BERT-base, BERT-tiny 等。
*   **对比方法**：
    *   **搜索/调优算法**：Ansor (Zero-shot), Felix (Few-shot)。
    *   **代价模型**：Tenset-MLP, TLP (Transformer-based), MTL-TLP (Multi-task learning)。

#### 4. 资源与算力
*   **硬件环境**：
    *   训练服务器：Intel Xeon Silver 4214 CPU，256GB 内存，4 台 NVIDIA V100 GPU (32GB)。
    *   端到端评估：Intel i7-12700F CPU 和 NVIDIA RTX 3080Ti GPU。
*   **训练细节**：使用 Adam 优化器，训练 100 个 epoch，batch size 为 1024。CKD 框架中引入了余弦退火学习率策略。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 种不同的硬件平台（5 种 CPU，3 种 GPU），测试了 12 种主流深度学习模型。
*   **消融实验**：分别验证了 RDU 采样器、Mamba 架构和 CKD 框架对最终性能的贡献。
*   **充分性评价**：实验设计较为全面，不仅评估了模型在离线数据集上的 Top-k 预测准确率，还进行了端到端的真实调优时间与推理延迟对比，证明了框架在实际生产环境中的有效性。

#### 6. 主要结论与发现
*   **效率提升**：在 CPU 上，TCL 的调优速度比 Tenset-MLP 快 **16.8 倍**；在 GPU 上快 **12.48 倍**。
*   **性能优化**：相比 Ansor，TCL 优化后的模型推理延迟在 CPU 和 GPU 上分别降低了 **22%** 和 **21%**。
*   **数据节省**：RDU 采样器仅需 **10%** 的数据即可达到甚至超过全量数据训练的效果。
*   **模型轻量化**：Mamba 架构在保持高精度的同时，参数量远小于 Transformer 架构。

#### 7. 优点
*   **数据高效性**：通过主动学习解决了深度学习编译器最核心的“数据饥渴”问题。
*   **架构创新**：首次将 Mamba 引入张量程序性能预测，平衡了长序列建模能力与计算开销。
*   **可扩展性**：CKD 框架允许模型像人类一样持续积累不同硬件的优化经验，避免了多任务学习的扩展瓶颈。

#### 8. 不足与局限
*   **跨厂商迁移限制**：实验发现，在不同厂商（如 Intel 与 AMD/ARM）之间进行知识迁移时，仍可能存在负迁移风险，知识增益不如同厂商硬件显著。
*   **硬件覆盖**：虽然涵盖了主流 CPU/GPU，但对于 NPU、TPU 等专用 AI 加速器的验证相对较少。
*   **搜索空间依赖**：TCL 仍依赖于底层编译器（如 TVM/Ansor）定义的搜索空间，若搜索空间本身受限，代价模型的优化上限也会受影响。

（完）
