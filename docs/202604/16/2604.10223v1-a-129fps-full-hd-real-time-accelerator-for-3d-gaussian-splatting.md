---
title: A 129FPS Full HD Real-Time Accelerator for 3D Gaussian Splatting
title_zh: 一种用于3D高斯泼溅的129FPS全高清实时加速器
authors: "Fang-Chi Chang, Tian-Sheuan Chang"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10223v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对3D高斯泼溅的硬件友好型压缩与加速器设计
tldr: "针对AR/VR设备在渲染大规模场景时面临的计算、带宽和存储挑战，本文提出了一种低功耗、低成本的3D高斯泼溅（3DGS）硬件加速器。通过结合迭代剪枝、球谐函数降阶和矢量量化的硬件友好型压缩方案，实现了51.6倍的模型压缩。该加速器采用帧级流水线和无比较排序技术，在28nm工艺下实现了1080p@129FPS的实时渲染，能效比前代提升7.5倍。"
selection_source: fresh_fetch
motivation: 3DGS在AR/VR设备上渲染大规模场景时，面临极高的计算开销、带宽需求和存储成本。
method: 提出一种结合剪枝、SH降阶与矢量量化的压缩方案，并设计了集成点剔除、投影、无比较排序及光栅化的硬件加速架构。
result: "在TSMC 28nm工艺下，该设计以0.219W功耗实现了1080p@129FPS渲染，模型体积缩小51.6倍，能效提升7.5倍。"
conclusion: 该加速器通过软硬件协同优化，显著降低了3DGS的资源消耗，为移动端实时高质量3D渲染提供了高效解决方案。
---

## 摘要
在AR/VR级设备上渲染大规模、无边界场景受限于3D高斯泼溅（3DGS）的计算、带宽和存储成本。我们提出了一种低功耗、低成本的3DGS硬件加速器，能够实时渲染全高清图像，并配备了一个硬件友好的压缩流水线，该流水线结合了迭代式高斯剪枝与微调、渐进式球谐函数（SH）阶数降低，以及对所有SH系数和颜色的矢量量化。该方案实现了51.6倍的模型尺寸缩减，且PSNR损失仅为0.743 dB。该加速器采用帧级流水线，集成了基于点的剔除与投影以及基于分块的排序与光栅化，跳过了零雅可比矩阵乘法（减少了63%的处理单元和53%的计算量），并采用了具有确定性延迟的无比较分块排序。该设计采用TSMC 28nm工艺实现，工作频率为800 MHz，面积为0.66 mm²，包含114.38万门电路和120 kB SRAM，功耗为0.219 W，在267.5 Mpixels/s的吞吐量下达到1219 Mpixels/J的能效，支持1080p分辨率下129 FPS的帧率。总体而言，与之前的3DGS加速器相比，其面积缩小了5.98倍，吞吐量提高了5.94倍，能效提升了7.5倍。

## Abstract
Rendering large-scale, unbounded scenes on AR/VR-class devices is constrained by the computation, bandwidth, and storage cost of 3D Gaussian Splatting (3DGS). We propose a low-power, low-cost 3DGS hardware accelerator that renders full-HD images in real time, together with a hardware-friendly compression pipeline that combines iterative Gaussian pruning and fine-tuning, progressive spherical harmonics (SH) degree reduction, and vector quantization of all SH coefficients and colors. The scheme achieves a $51.6\times$ model-size reduction with a 0.743 dB PSNR loss. The accelerator uses a frame-level pipeline that integrates point-based culling and projection with tile-based sorting and rasterization, skips zero-Jacobian matrix multiplications (reducing processing elements by 63\% and computation by 53\%), and adopts comparison-free tile-based sorting with deterministic latency. Implemented in a TSMC 28-nm process at 800 MHz, the design occupies $0.66~\text{mm}^2$ with 1.1438 M gates and 120 kB SRAM, consumes 0.219 W, and delivers 1219 Mpixels/J at 267.5 Mpixels/s, enabling 1080p at 129 FPS. Overall, it is $5.98\times$ smaller in area, $5.94\times$ higher throughput, and delivers $7.5\times$ higher energy efficiency than prior 3DGS accelerators.

---

## 论文详细总结（自动生成）

这是一份关于论文《A 129FPS Full HD Real-Time Accelerator for 3D Gaussian Splatting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
3D高斯泼溅（3DGS）虽然在GPU上实现了高质量的实时渲染，但其巨大的内存占用、高带宽需求和沉重的计算负担，使其难以部署在资源受限的AR/VR移动端设备上。现有的硬件加速器往往直接实现原始算法，导致芯片面积大、功耗高，且难以达到全高清（1080p）120 FPS的工业标准。本论文旨在通过**软硬件协同设计**，提出一种超轻量级压缩方案和专用硬件架构，以极低的功耗实现全高清实时渲染。

### 2. 方法论：核心思想与关键技术
论文提出了两方面的优化：

*   **硬件友好型压缩流水线：**
    *   **迭代剪枝与微调：** 并非一次性剪枝，而是分阶段进行“剪枝-微调”，以在大幅减少高斯点数量的同时维持图像质量。
    *   **渐进式球谐函数（SH）降阶：** 通过知识蒸馏逐步降低SH阶数，平滑处理质量与压缩率的权衡。
    *   **全量矢量量化（VQ）：** 对所有SH系数和颜色应用VQ，将模型尺寸压缩了51.6倍，而PSNR损失控制在0.743 dB。
*   **硬件架构设计：**
    *   **帧级流水线：** 采用混合粒度处理，预处理阶段（剔除、投影）基于点，渲染阶段（排序、光栅化）基于分块（Tile）。
    *   **零雅可比矩阵跳过：** 识别并跳过投影计算中雅可比矩阵的零元素，减少了63%的处理单元（PE）和53%的计算量。
    *   **无比较分块排序：** 采用基于硬件逻辑的无比较排序算法，具有确定的线性延迟，避免了传统排序算法的复杂比较逻辑和变动延迟。
    *   **早期终止与剔除：** 结合近平面剔除和Alpha混合早期终止，减少无效计算。

### 3. 实验设计
*   **数据集：** 使用了三个主流数据集：Mip-NeRF360（复杂无边界场景）、Tanks&Temples（高细节场景）和Deep Blending（多视图合成场景）。
*   **Benchmark：** 
    *   **软件对比：** 原始3DGS、LightGaussian、EAGLES、HAC等压缩方法。
    *   **硬件对比：** GSCore (ASPLOS'24)、GSNorm (ASP-DAC'25) 以及基于FPGA的加速器。
*   **评估指标：** PSNR、SSIM、LPIPS（图像质量）；FPS、吞吐量（Mpixels/s）、能效（Mpixels/J）、芯片面积和功耗。

### 4. 资源与算力
*   **硬件实现：** 采用 **TSMC 28nm** 工艺进行逻辑综合。
*   **硬件规格：** 工作频率 **800 MHz**，占用面积 **0.66 mm²**，包含 **114.38万门** 电路和 **120 kB SRAM**。
*   **训练/微调算力：** 论文提到剪枝每步迭代5000步，SH蒸馏4000步。虽然未明确标注具体的GPU型号（如A100或RTX 4090），但此类微调通常在单台高性能工作站上即可完成。

### 5. 实验数量与充分性
*   **实验覆盖面：** 论文在13个代表性场景上进行了测试，涵盖了室内、室外、物体和大规模环境。
*   **消融实验：** 针对压缩流水线的各个步骤（剪枝、降阶、量化）以及硬件的各个优化模块（剔除、跳过零计算、早期终止）均做了详细的消融实验。
*   **充分性评价：** 实验设计非常充分，不仅有算法层面的质量对比，还有详尽的硬件性能指标对比，客观地展示了软硬件协同带来的增益。

### 6 主要结论与发现
*   **极致压缩：** 实现了51.6倍的压缩率，模型体积大幅缩小，显著降低了片外内存访问压力。
*   **高性能低功耗：** 在仅 **0.219 W** 的功耗下，实现了 **129 FPS** 的全高清渲染。
*   **能效领先：** 相比于之前的SOTA硬件加速器（如GSCore），面积缩小了5.98倍，吞吐量提升了5.94倍，能效提升了7.5倍。
*   **确定性延迟：** 无比较排序器的引入解决了3DGS硬件化中排序延迟不稳定的痛点。

### 7. 优点
*   **软硬件协同：** 压缩算法充分考虑了硬件实现的便利性（如固定位宽、规则的数据流）。
*   **计算效率极高：** 通过跳过零雅可比矩阵乘法，极大地精简了投影单元的硬件开销。
*   **面积优势：** 0.66 mm² 的面积在同类加速器中极具竞争力，非常适合集成到移动端SoC中。

### 8. 不足与局限
*   **图像质量损失：** 为了追求极致的压缩和速度，PSNR有约0.74 dB的下降，在某些对细节要求极高的应用中可能存在视觉瑕疵（如高频纹理变模糊）。
*   **工艺局限：** 实验结果基于28nm工艺的门级仿真，尚未有实际流片（Silicon）的实测数据。
*   **场景依赖：** 尽管平均性能优异，但对于高斯点分布极度不均的极端场景，分块排序器的缓冲区（Global Buffer）仍可能面临溢出风险。

（完）
