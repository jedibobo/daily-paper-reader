---
title: Quantization Impact on the Accuracy and Communication Efficiency Trade-off in Federated Learning for Aerospace Predictive Maintenance
title_zh: 量化对航空航天预测性维护联邦学习中准确率与通信效率权衡的影响
authors: Abdelkarim Loukili
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08474v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 量化对轻量级航空航天模型准确性与通信效率的影响
tldr: 本研究针对航空预测性维护中联邦学习的通信带宽瓶颈，探讨了对称均匀量化对轻量级1-D卷积模型（AeroConv1D）性能的影响。通过在NASA C-MAPSS数据集上的Non-IID实验，证明了INT4量化能在不损失统计精度的情况下实现8倍的通信开销缩减。研究强调了Non-IID评估在揭示极端量化（如INT2）不稳定性方面的必要性，并验证了INT4在FPGA硬件上的部署可行性。
selection_source: fresh_fetch
motivation: 联邦学习在航空预测性维护中面临带宽受限的通信瓶颈，亟需在模型精度与通信效率之间取得平衡。
method: 在NASA C-MAPSS数据集的Non-IID分区下，对轻量级AeroConv1D模型进行了多位宽（32/8/4/2位）对称均匀量化评估。
result: INT4量化在保持与FP32相当精度的同时实现了8倍通信减量，而INT2虽能降低MAE但会导致严重的性能不稳定。
conclusion: INT4是兼顾精度、通信效率与硬件可行性的最优量化方案，且真实的Non-IID评估对于发现量化缺陷至关重要。
---

## 摘要
联邦学习 (FL) 能够在分布式航空机队中实现保护隐私的预测性维护，但梯度通信开销限制了其在带宽受限的物联网 (IoT) 节点上的部署。本文研究了对称均匀量化 ($b \in \{32,8,4,2\}$ 位) 对定制轻量级一维卷积模型 (AeroConv1D，9,697 个参数) 准确率与效率权衡的影响。该模型在现实的非独立同分布 (Non-IID) 客户端划分下，通过联邦学习在 NASA C-MAPSS 基准数据集上进行训练。通过严格的多种子评估 ($N=10$)，我们证明了 INT4 在 FD001 ($p=0.341$) 和 FD002 ($p=0.264$ MAE, $p=0.534$ NASA 评分) 数据集上的准确率与 FP32 在统计学上无显著差异，同时将每轮梯度通信成本降低了 8 倍 (37.88 KiB $\to$ 4.73 KiB)。一个关键的方法论发现是，简单的独立同分布 (IID) 客户端划分会人为地抑制方差；而正确的 Non-IID 评估揭示了极端量化下真实的运行不稳定性，这一点通过 IID 与 Non-IID 的直接实证对比得到了证实。实证结果表明 INT2 并不适用：虽然它通过极端量化诱导的过度正则化在 FD002 上实现了更低的平均绝对误差 (MAE)，但这种表观增益伴随着灾难性的 NASA 评分不稳定性 (变异系数 CV = 45.8%，而 FP32 为 22.3%)，证实了其在异构运行条件下的不可复现性。在 Xilinx ZCU102 上的分析性 FPGA 资源预测确认，INT4 符合硬件约束 (DSP 利用率为 85.5%)，这可能实现在单个 SoC 上部署完整的联邦学习流水线。完整的仿真代码库和 FPGA 估算脚本已在 https://github.com/therealdeadbeef/aerospace-fl-quantization 公开。

## Abstract
Federated learning (FL) enables privacy-preserving predictive maintenance across distributed aerospace fleets, but gradient communication overhead constrains deployment on bandwidth-limited IoT nodes. This paper investigates the impact of symmetric uniform quantization ($b \in \{32,8,4,2\}$ bits) on the accuracy--efficiency trade-off of a custom-designed lightweight 1-D convolutional model (AeroConv1D, 9\,697 parameters) trained via FL on the NASA C-MAPSS benchmark under a realistic Non-IID client partition. Using a rigorous multi-seed evaluation ($N=10$ seeds), we show that INT4 achieves accuracy \emph{statistically indistinguishable} from FP32 on both FD001 ($p=0.341$) and FD002 ($p=0.264$ MAE, $p=0.534$ NASA score) while delivering an $8\times$ reduction in gradient communication cost (37.88~KiB $\to$ 4.73~KiB per round). A key methodological finding is that naïve IID client partitioning artificially suppresses variance; correct Non-IID evaluation reveals the true operational instability of extreme quantization, demonstrated via a direct empirical IID vs.\ Non-IID comparison. INT2 is empirically characterized as unsuitable: while it achieves lower MAE on FD002 through extreme quantization-induced over-regularization, this apparent gain is accompanied by catastrophic NASA score instability (CV\,=\,45.8\% vs.\ 22.3\% for FP32), confirming non-reproducibility under heterogeneous operating conditions. Analytical FPGA resource projections on the Xilinx ZCU102 confirm that INT4 fits within hardware constraints (85.5\% DSP utilization), potentially enabling a complete FL pipeline on a single SoC. The full simulation codebase and FPGA estimation scripts are publicly available at https://github.com/therealdeadbeef/aerospace-fl-quantization.

---

## 论文详细总结（自动生成）

以下是对论文《Quantization Impact on the Accuracy and Communication Efficiency Trade-off in Federated Learning for Aerospace Predictive Maintenance》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：航空航天领域的预测性维护（如涡扇发动机剩余寿命 RUL 预测）需要跨机队的数据协作，但原始数据中心化面临隐私、法规和带宽限制。
*   **核心问题**：联邦学习（FL）虽能保护隐私，但在带宽受限的物联网（IoT）节点（如 LoRaWAN）上，传输全精度梯度会产生巨大的通信开销。此外，边缘设备（如 FPGA）对计算资源和数值精度有严格限制。
*   **研究目的**：探讨梯度量化（Quantization）如何在保证 RUL 预测精度的前提下，降低通信成本并适配硬件约束，特别是在现实的非独立同分布（Non-IID）数据分布下。

### 2. 论文提出的方法论
*   **AeroConv1D 模型**：设计了一个超轻量级的一维卷积神经网络（仅 9,697 个参数），采用纯前馈结构以利于 FPGA 的深度流水线并行化，避免了循环神经网络（RNN）在低比特量化下的误差累积。
*   **对称均匀量化**：在客户端发送梯度增量（$\Delta w$）前，将其量化为 $b$ 位（32, 8, 4, 2）。
    *   **公式逻辑**：通过计算每层张量的最大绝对值作为缩放因子 $\alpha$，将浮点数映射到 $[- (2^{b-1}-1), +(2^{b-1}-1)]$ 的整数区间。
*   **联邦学习框架**：采用同步 FedAvg 算法，10 个客户端，每轮进行 2 个本地 Epoch 训练。
*   **硬件资源投影**：利用 `hls4ml` 缩放模型，对 Xilinx ZCU102 FPGA 的资源占用（LUT、DSP、延迟）进行分析性预测。

### 3. 实验设计
*   **数据集**：NASA C-MAPSS 基准数据集，包含 FD001（单工况）和 FD002（六种复杂工况）。
*   **基准（Benchmark）**：以 FP32（全精度浮点）作为性能基准。
*   **对比方法**：对比了 INT8、INT4 和 INT2 三种量化级别。
*   **分区策略**：对比了“简单的 IID 划分”与“基于发动机编号的 Non-IID 划分”，以验证评估方法的有效性。
*   **评价指标**：平均绝对误差（MAE）、NASA 非对称评分（对过高估计 RUL 惩罚更重）以及梯度失真隐私代理指标。

### 4. 资源与算力
*   **软件环境**：基于 PyTorch 实现，代码已开源。
*   **算力说明**：文中**未明确说明**具体使用的 GPU 型号、数量或训练总时长。
*   **硬件目标**：实验针对 Xilinx Zynq UltraScale+ ZCU102 SoC 进行资源评估，设定时钟频率为 500 MHz。

### 5. 实验数量与充分性
*   **实验规模**：每个配置均使用了 **10 个不同的随机种子**进行重复实验，并进行了配对 t 检验（$p$-test）以评估统计显著性。
*   **充分性**：实验涵盖了单工况与多工况数据集，并专门设计了 IID 与 Non-IID 的对比实验。通过多种子评估揭示了极端量化下的方差波动，实验设计严谨、客观，能够有效支持其结论。

### 6. 论文的主要结论与发现
*   **INT4 是最优平衡点**：INT4 在 FD001 和 FD002 上的精度与 FP32 在统计上无显著差异（$p > 0.05$），同时实现了 **8 倍的通信减量**（每轮仅 4.73 KiB）。
*   **INT2 的不可用性**：虽然 INT2 在 FD002 上因“过度正则化”表现出较低的平均 MAE，但其 NASA 评分表现出极大的不稳定性（CV 高达 45.8%），在安全关键型任务中不可靠。
*   **IID 评估的偏差**：研究发现 IID 划分会人为抑制方差，掩盖量化带来的不稳定性。只有在 Non-IID 评估下，量化对模型鲁棒性的真实影响才会显现。
*   **硬件可行性**：INT4 方案在 ZCU102 FPGA 上的 DSP 占用率为 85.5%，剩余资源足以集成同态加密（HE）协处理器。

### 7. 优点
*   **方法论严谨**：强调了 Non-IID 评估在联邦学习研究中的必要性，纠正了以往研究中常见的 IID 评估偏差。
*   **软硬结合**：不仅关注算法精度，还通过 `hls4ml` 提供了具体的 FPGA 资源预测，具有很强的工程参考价值。
*   **统计学支撑**：通过多种子实验和显著性检验，避免了偶然性结果对结论的干扰。

### 8. 不足与局限
*   **硬件验证局限**：FPGA 资源占用是基于模型的**分析性预测**，尚未在物理芯片上进行最终的综合与实测验证。
*   **隐私度量简单**：使用的梯度失真指标仅是隐私保护的启发式代理，并非严格的差分隐私（DP）证明。
*   **数据集覆盖**：虽然使用了 C-MAPSS，但尚未在更现代或更大规模的航空数据集（如 FD003, FD004）上进行全面验证。

（完）
