---
title: Cross-Layer Co-Optimized LSTM Accelerator for Real-Time Gait Analysis
title_zh: 面向实时步态分析的跨层协同优化 LSTM 加速器
authors: "Mohammad Hasan Ahmadilivani, Levent Aksoy, Mohammad Eslami, Jaan Raik, Alar Kuusik"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13543v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 面向边缘计算的跨层协同优化LSTM加速器
tldr: 本研究针对医疗步态分析中的实时性和边缘计算需求，提出了一种跨层协同优化的ASIC专用LSTM加速器。通过从软件量化到硬件布局的全面设计空间探索，实现了高性能、低功耗和小面积的平衡。该加速器在65nm工艺下表现优异，不仅面积紧凑，且处理速度比实际应用需求快4.05倍，为实时步态异常检测提供了高效的硬件方案。
selection_source: fresh_fetch
motivation: 针对医疗步态分析对实时性、低功耗和小型化的严苛要求，开发高效的专用集成电路（ASIC）加速器。
method: 采用跨层协同优化方法，结合硬件感知量化、寄存器传输级设计探索及物理布局优化。
result: 在65nm工艺下，加速器核心面积仅为0.325平方毫米，且检测速度达到应用要求的4.05倍。
conclusion: 该跨层优化框架成功平衡了硬件复杂性与检测精度，证明了ASIC在实时边缘医疗应用中的高效性。
---

## 摘要
长短期记忆 (LSTM) 神经网络已广泛应用于对实时性和边缘计算能力有严格要求的医疗保健领域。其中，通过检测异常步幅以防止患者跌倒的步态分析是此类应用中的一个重要课题。鉴于性能、功耗和面积方面极其严苛的设计要求，专用集成电路 (ASIC) 能够实现 LSTM 在步态分析中的高效实时应用并保持高精度。据我们所知，本研究首次提出了一种针对 ASIC 设计的、用于实时步态分析的跨层协同优化 LSTM 加速器。我们进行了从软件层到版图设计的全面设计空间探索。具体而言，我们在软件层面通过硬件感知量化进行位宽优化以降低硬件复杂度，在寄存器传输级探索了多种设计方案，并生成了不同的版图，旨在硬件复杂度和精度之间寻找 LSTM 加速器的高效实现。物理综合结果表明，在 65 nm 工艺下，针对最高精度优化的加速器版图面积为 0.325 mm²，而针对硬件复杂度优化且精度略低的备选设计面积则缩小了 15.4%。此外，所设计的加速器实现准确步态异常检测的速度比应用需求快 4.05 倍。

## Abstract
Long Short-Term Memory (LSTM) neural networks have penetrated healthcare applications where real-time requirements and edge computing capabilities are essential. Gait analysis that detects abnormal steps to prevent patients from falling is a prominent problem for such applications. Given the extremely stringent design requirements in performance, power dissipation, and area, an Application-Specific Integrated Circuit (ASIC) enables an efficient real-time exploitation of LSTMs for gait analysis, achieving high accuracy. To the best of our knowledge, this work presents the first cross-layer co-optimized LSTM accelerator for real-time gait analysis, targeting an ASIC design. We conduct a comprehensive design space exploration from software down to layout design. We carry out a bit-width optimization at the software level with hardware-aware quantization to reduce the hardware complexity, explore various designs at the register-transfer level, and generate alternative layouts to find efficient realizations of the LSTM accelerator in terms of hardware complexity and accuracy. The physical synthesis results show that, using the 65 nm technology, the die size of the accelerator's layout optimized for the highest accuracy is 0.325 mm^2, while the alternative design optimized for hardware complexity with a slightly lower accuracy occupies 15.4% smaller area. Moreover, the designed accelerators achieve accurate gait abnormality detection 4.05x faster than the given application requirement.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种专门用于实时步态分析的跨层协同优化 LSTM（长短期记忆网络）加速器设计。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：针对神经系统疾病患者的步态分析（如检测异常步幅以预防跌倒）对实时性、隐私保护和设备功耗有极高要求。
*   **研究动机**：
    *   **应用需求**：步态信号具有稀疏、多相且受生物力学因素影响的特点，传统的通用处理器难以在低功耗可穿戴设备上实现高效的实时处理。
    *   **技术空白**：虽然已有针对语音识别等领域的 LSTM 加速器，但尚未有专门针对步态分析优化的 ASIC（专用集成电路）设计。
    *   **设计挑战**：需要在极小的芯片面积和功耗预算内，保证异常检测的高精度和实时响应速度。

### 2. 方法论
论文提出了一种**跨层协同优化框架**，涵盖从软件算法到物理布局的四个阶段：
*   **硬件感知设计空间探索（软件层）**：
    *   **定点量化**：将全精度参数量化为定点格式 $FxP(b, f)$。
    *   **近似激活函数**：为了降低硬件复杂度，使用多项式近似法替代复杂的 Sigmoid 和 Tanh 函数。
    *   **位宽优化**：通过软件模拟寻找在精度损失 $<1\%$ 的前提下，参数和操作数的最窄位宽。
*   **硬件加速器设计（RTL层）**：
    *   **并行与资源共享**：在 LSTM 单元内部并行处理四个门（i, f, g, o），并共享点积和激活函数单元。
    *   **片上存储**：设计专用 SRAM 组织结构，减少参数读取的访问次数。
*   **加速器验证**：通过对比 RTL 仿真输出与软件定点模拟结果，确保硬件逻辑的正确性。
*   **物理设计（布局层）**：在 65nm 工艺下生成物理版图，优化面积、功耗和时序。

### 3. 实验设计
*   **数据集**：包含 22 名健康受试者的步态数据，以及四种疾病（共济失调、双瘫、偏瘫、帕金森病）的临床记录。
*   **模型结构**：1 层 LSTM（20 个单元）+ 2 层全连接层（FC），总计 2462 个参数。
*   **Benchmark（基准）**：
    *   **软件基准**：全精度（Float32）训练的 LSTM 模型。
    *   **硬件对比**：与现有的 SOTA（先进）LSTM 加速器（如 Chipmunk, ELSA 等）在面积效率和能量效率上进行对比。
*   **对比方法**：探索了 7 种不同的位宽配置（如参数位宽 8-10 位，操作位宽 12-13 位）。

### 4. 资源与算力
*   **软件环境**：使用 Python 和 PyTorch 进行模型训练和量化探索。
*   **硬件工具**：使用 Verilog 进行 RTL 描述，Cadence Genus 进行逻辑综合，Cadence Innovus 进行物理综合。
*   **工艺节点**：商业 65nm CMOS 工艺。
*   **算力说明**：论文未详细说明训练阶段使用的 GPU 型号及时长（这在 ASIC 推理论文中较常见），重点在于推理阶段的硬件资源消耗（如 10MHz 频率下的功耗为 1.8-2.0 mW）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 种不同的步态异常类型分别进行了训练和验证。
    *   测试了 7 组主要的位宽配置组合，并生成了 2 种最终的物理版图（最高精度版 vs. 最小面积版）。
*   **充分性评价**：实验涵盖了从算法精度损失分析到门级电路仿真，再到物理版图综合的全流程。通过对不同疾病数据的交叉验证，证明了设计的鲁棒性。实验设计较为客观，充分考虑了硬件约束对算法表现的影响。

### 6. 主要结论与发现
*   **性能优异**：在 10MHz 频率下，加速器处理一个步态窗口仅需 0.96ms，比实时应用要求的 3.9ms 快了 **4.05 倍**。
*   **面积与功耗平衡**：
    *   **最高精度设计**：芯片面积 0.325 mm²，功耗 2.038 mW。
    *   **最小面积设计**：通过容忍极小的精度下降，面积缩小了 **15.4%**，功耗降低了 **10.35%**。
*   **位宽敏感性**：发现整数位不足会导致严重的精度崩塌，而增加小数位宽对乘法器硬件复杂度的增加相对较小。

### 7. 优点与亮点
*   **跨层协同**：不仅仅是硬件实现，而是从软件量化策略就开始考虑硬件代价，实现了真正的协同优化。
*   **开源贡献**：提供了开源的软件工具用于 LSTM 的 ASIC 优化探索。
*   **高度可参数化**：设计支持灵活配置输入通道数和处理步数，可适配 ECG 等其他低频生物医学信号处理任务。
*   **物理级验证**：提供了完整的物理布局结果，而非仅仅停留在逻辑综合阶段，更具工程参考价值。

### 8. 不足与局限
*   **工艺节点较旧**：采用 65nm 工艺虽然成本低且适合医疗设备，但相比 28nm 或更先进工艺，其能效比和集成度仍有提升空间。
*   **模型规模限制**：目前设计针对的是较小的 LSTM 模型（20 个单元），对于更复杂的深度循环神经网络，其扩展性（如多层 LSTM 的调度）尚未深入讨论。
*   **对比维度**：在与 SOTA 加速器对比时，由于目标应用（步态分析 vs 语音识别）的频率和吞吐量需求差异巨大，部分能效指标的直接对比可能存在偏差。

（完）
