---
title: "EULER-ADAS: Energy-Efficient & SIMD-Unified Logarithmic-Posit Engine for Precision-Reconfigurable Approximate ADAS Acceleration"
title_zh: EULER-ADAS：用于精度可重构近似 ADAS 加速的高能效且 SIMD 统一的对数 Posit 引擎
authors: "Mukul Lokhande, Ratko Pilipovic, Omkar Kokane, Adam Teman, Santosh Kumar Vishvakarma"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06875v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于节能ADAS加速的软硬件协同设计
tldr: 针对高级驾驶辅助系统（ADAS）对低功耗、低延迟推理的需求，本文提出EULER-ADAS，一种支持SIMD的对数有界Posit神经计算引擎。该引擎通过有界指数编码、对数尾数乘法和精度可重构的SIMD架构，在FPGA和CMOS平台上实现了显著的面积与功耗优化，同时保持了接近FP32的推理精度，非常适合低功耗实时边缘推理场景。
selection_source: fresh_fetch
motivation: 传统Posit算术的变长编码增加了硬件编解码成本和故障风险，限制了其在严苛功耗约束下的ADAS应用。
method: 设计了一种统一的SIMD架构，结合有界Posit表示、自适应对数尾数乘法和共享累加器，支持8/16/32位精度的动态重构。
result: "实验显示该设计在FPGA上减少了约41.4%的资源消耗和71.9%的功耗，且TinyYOLOv3推理延迟仅为78ms。"
conclusion: EULER-ADAS通过近似计算与可重构架构的结合，为资源受限的实时ADAS加速提供了一种高效且高可靠的硬件方案。
---

## 摘要
高级驾驶辅助系统 (ADAS) 需要在严格的功耗和面积限制下提供低延迟推理的神经计算引擎。Posit 算术对于此类加速器具有吸引力，因为它在低精度下提供了高数值保真度，但其变长 regime 编码增加了编解码成本，并使数据通路暴露在巨大的 regime 字段故障影响之下。本文提出了 EULER-ADAS，一种支持 SIMD 的对数有界 Posit 神经计算引擎，用于高能效且可靠性感知的 ADAS 加速。所提出的数据通路结合了有界 regime Posit 表示、带位截断的阶段自适应对数尾数乘法，以及支持 Posit-(8,0)、Posit-(16,1) 和 Posit-(32,2) 执行的 SIMD 共享 quire 累加通路。该统一架构无需重复特定精度的硬件即可实现 4xPosit-8、2xPosit-16 或 1xPosit-32 操作。FPGA 实现表明，与精确 Posit 神经计算引擎相比，所提出的配置将 LUT 数量减少了高达 41.4%，延迟减少了高达 76.1%，功耗减少了高达 71.9%，同时其能效延迟积比基于基-4 Booth 的 Posit 乘法器低 10 倍。在 28 纳米 CMOS 工艺下，有界变体占用 0.013-0.016 mm²，功耗为 19.8-22.1 mW，运行频率高达 1.84 GHz。在图像分类、ADAS 和边缘推理工作负载的应用级评估显示，所评估的 Posit-16 和 Posit-32 配置的准确率与 FP32 相比保持在约 1.5 个百分点以内。在 Pynq-Z2 上的 TinyYOLOv3 原型在 0.29 W 功耗下实现了 78 ms 的延迟和 22.6 mJ/帧的能耗，证明了 EULER-ADAS 适用于低功耗实时 ADAS 推理。

## Abstract
Advanced driver-assistance systems (ADAS) require neural compute engines that deliver low-latency inference under strict power and area constraints. Posit arithmetic is attractive for such accelerators because it provides high numerical fidelity at low precision, but its variable-length regime encoding increases encode/decode cost and exposes the datapath to large regime-field fault effects. This paper presents EULER-ADAS, a SIMD-enabled logarithmic bounded-Posit neural compute engine for energyefficient and reliability-aware ADAS acceleration. The proposed datapath combines bounded-regime Posit representation, stageadaptive logarithmic mantissa multiplication with bit truncation, and a SIMD-shared quire accumulation path supporting Posit- (8,0), Posit-(16,1), and Posit-(32,2) execution. The unified architecture enables 4xPosit-8, 2xPosit-16, or 1xPosit-32 operation without duplicating precision-specific hardware. FPGA implementation shows that the proposed configurations reduce LUT count by up to 41.4%, delay by up to 76.1%, and power by up to 71.9% relative to exact Posit neural compute engines, while achieving up to 10x lower energy-delay product than radix-4 Booth-based Posit multipliers. In 28-nm CMOS, the bounded variants occupy 0.013-0.016 mm2 , consume 19.8-22.1 mW, and operate at up to 1.84 GHz. Application-level evaluation across image-classification, ADAS, and edge-inference workloads shows that the evaluated Posit-16 and Posit-32 configurations remain within about 1.5 percentage points of FP32 accuracy. A TinyYOLOv3 prototype on Pynq-Z2 achieves 78 ms latency at 0.29 W and 22.6 mJ/frame, demonstrating the suitability of EULERADAS for low-power real-time ADAS inference.

---

## 论文详细总结（自动生成）

### EULER-ADAS 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
高级驾驶辅助系统（ADAS）对车载计算引擎提出了严苛的要求：必须在极低的功耗和面积预算下实现低延迟推理。**Posit 算术**因其在低位宽下比传统浮点数（IEEE 754）具有更高的数值保真度和更大的动态范围而备受关注。
然而，标准 Posit 存在两个核心痛点：
*   **硬件开销大**：其变长 regime 编码导致编解码逻辑复杂，增加了数据通路的延迟和面积。
*   **可靠性风险**：regime 字段的位翻转（软错误）会导致数值发生剧烈偏差，这对安全至上的 ADAS 任务是致命的。
**EULER-ADAS** 的提出正是为了通过近似计算和有界编码技术，解决 Posit 在硬件效率与可靠性之间的矛盾。

#### 2. 方法论：核心思想与关键技术
论文提出了一种支持 SIMD 的对数有界 Posit（Bounded-Posit）神经计算引擎，核心技术包括：
*   **有界 Posit 表示（B-Posit）**：通过限制 regime 字段的最大长度，简化了编解码电路，并显著降低了 regime 位错误导致的灾难性数值偏差。
*   **阶段自适应对数尾数乘法器（ILM）**：将复杂的尾数乘法转换为对数域的加法。引入了阶段数（$n$）和截断位宽（$m$）作为精度-成本调节旋钮。其误差边界公式为 $RE(n, m) \leq 2^{-2n} + 2^{-m}$。
*   **统一的 SIMD 架构**：设计了一个可重构的数据通路，无需重复硬件即可支持 4 路 Posit-8、2 路 Posit-16 或 1 路 Posit-32 操作。
*   **共享 Quire 累加器**：使用 128 位的共享 Quire 寄存器进行高精度累加，延迟舍入操作以减少累积误差。
*   **流水线设计**：采用六级流水线（解码、乘法、缩放、累加、归一化、编码），优化了关键路径。

#### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：CIFAR-100, ImageNet, ImageNet-1k。
    *   **ADAS 专项任务**：KITTI（目标检测）、ETH（车辆检测）、Caltech Pedestrian（行人检测）、Cityscapes（语义分割）。
    *   **其他边缘任务**：包括驾驶员监控、交通标志识别、端到端驾驶控制（PilotNet）、视觉惯性里程计（VIO）以及 NLP 任务（LLaMA 3.2, Whisper）。
*   **Benchmark（基准）**：
    *   **数值基准**：FP32, BF16, 精确 Posit（基于基-4 Booth 乘法器）。
    *   **硬件基准**：对比了 SOTA 的近似乘法器（如 ROBA, TOSAM, ACMLC 等）以及现有的 SIMD 神经计算引擎（如 TCAS-II'22, TVLSI'22 等）。

#### 4. 资源与算力
*   **ASIC 实现**：采用 **28-nm CMOS HPC** 工艺。在 0.9V 典型工况下，运行频率高达 **1.84 GHz**，功耗仅为 **19.8-22.1 mW**，面积为 **0.013-0.016 mm²**。
*   **FPGA 实现**：在 **AMD Vivado** 环境下针对 **VC707** 和 **Pynq-Z2** 平台进行了部署。
*   **训练/仿真算力**：文中未明确提及模型训练所使用的具体 GPU 型号或时长，主要侧重于使用 Python 进行位精确（Bit-accurate）的算术模型仿真和推理验证。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽实验，包括：
    *   **算术级误差分析**：对比了不同阶段数和截断位宽下的 MSE, MAE, NMED 等指标。
    *   **硬件性能对比**：涵盖了面积、延迟、功耗、能效比（TOPS/W）和计算密度（TOPS/mm²）。
    *   **应用级验证**：测试了 5 种主流 CNN/ViT 模型和 20 种 ADAS/边缘计算算法。
*   **充分性评价**：实验设计非常充分且客观。不仅涵盖了理论误差分析，还通过 ASIC 合成和 FPGA 原型验证了硬件增益，并跨越多个应用领域证明了算法的鲁棒性。

#### 6. 主要结论与发现
*   **硬件效率极高**：相比精确 Posit 引擎，EULER-ADAS 减少了 **41.4% 的 LUT** 消耗、**76.1% 的延迟**和 **71.9% 的功耗**。
*   **精度损失可控**：Posit-16 和 Posit-32 配置在大多数任务中与 FP32 的准确率差距保持在 **1.5% 以内**。
*   **实时性强**：在 Pynq-Z2 平台上运行 Tiny-YOLOv3，实现了 **78 ms 的延迟**和 **22.6 mJ/帧**的超低能耗，优于现有的嵌入式 GPU 和 MCU 方案。
*   **可靠性提升**：有界 Posit 显著降低了对软错误的敏感度。

#### 7. 优点
*   **软硬件协同优化**：不仅优化了乘法器，还针对 Posit 特有的编解码瓶颈进行了重构。
*   **高度可重构性**：单一硬件支持多种精度模式，适应了深度学习模型不同层对精度的不同需求。
*   **端到端验证**：从底层电路到顶层 ADAS 应用（如目标检测、路径规划）均进行了闭环验证。

#### 8. 不足与局限
*   **8位精度局限**：在 Posit-8 模式下，由于动态范围受限和近似误差叠加，部分复杂任务的准确率下降较明显。
*   **Regime 边界依赖**：B-Posit 的性能高度依赖于预设的 regime 边界值 $R$，对于某些极端分布的数据，可能需要手动调整边界以平衡精度和范围。
*   **软件栈支持**：虽然硬件高效，但目前尚缺乏成熟的编译器自动将主流框架（如 PyTorch）模型映射到这种特殊的对数有界 Posit 格式。

（完）
