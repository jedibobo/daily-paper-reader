---
title: "CARMEN: CORDIC-Accelerated Resource-Efficient Multi-Precision Inference Engine for Deep Learning"
title_zh: CARMEN：一种用于深度学习的CORDIC加速、资源高效型多精度推理引擎
authors: "Sonu Kumar, Mukul Lokhande, Santosh Kumar Vishvakarma, Adam Teman"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06878v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 资源高效的多精度推理引擎，体现软硬件协同设计
tldr: 本研究提出CARMEN，一种基于CORDIC加速的运行时自适应多精度深度学习推理引擎。该引擎利用CORDIC迭代深度与计算精度的直接关联，实现了无需硬件修改的近似与精确模式动态切换。通过集成低资源迭代式MAC单元和多激活函数模块，支持8/16位精度，在28nm工艺下显著提升了计算密度和能效，适用于资源受限的实时推理场景。
selection_source: fresh_fetch
motivation: 针对深度学习推理中资源消耗大且缺乏灵活精度调节的问题，寻求一种高效的多精度硬件加速方案。
method: 利用CORDIC算法的迭代特性，通过控制迭代深度实现动态精度切换，并结合时间复用的多激活函数模块优化硬件利用率。
result: "在28nm工艺下实现了11.67 TOPS/W的能效，并减少了33%的计算周期和21%的功耗。"
conclusion: CARMEN通过CORDIC加速实现了高能效与灵活精度的平衡，为资源受限环境下的深度学习部署提供了有效方案。
---

## 摘要
本文介绍了 CARMEN，这是一种运行时自适应、CORDIC 加速的多精度向量引擎，用于资源高效的深度学习推理。其核心见解在于 CORDIC 迭代深度直接决定了计算精度，从而能够在无需修改硬件的情况下，在近似执行模式和精确执行模式之间进行动态切换。该架构集成了一个基于 CORDIC 的低资源迭代乘累加（MAC）单元和一个时分复用的多激活函数模块，支持灵活的 8/16 位精度和高硬件利用率。在 28 nm CMOS 工艺下的 ASIC 实现达到了高达 33% 的计算周期缩减，且每个 MAC 阶段可节省 21% 的功耗；256 个处理单元（PE）的配置实现了 4.83 TOPS/mm² 的计算密度和 11.67 TOPS/W 的能效。在 PynqZ2 上的 FPGA 部署验证了在 0.43 W 功耗下实现 154.6 ms 的延迟，可用于实时目标检测。

## Abstract
This paper presents CARMEN, a runtime-adaptive, CORDIC-accelerated multi-precision vector engine for resource-efficient deep learning inference. The key insight is that CORDIC iteration depth directly governs computational accuracy, enabling dynamic switching between approximate and accurate execution modes without hardware modification. The architecture integrates a low-resource iterative CORDIC-based MAC unit with a time-multiplexed multi-activation function block, supporting flexible 8/16-bit precision and high hardware utilization. ASIC implementation in 28 nm CMOS achieves up to 33% reduction in computation cycles and 21% power savings per MAC stage; a 256-PE configuration delivers 4.83 TOPS/mm2 compute density and 11.67 TOPS/W energy efficiency. FPGA deployment on PynqZ2 validates 154.6 ms latency at 0.43 W for real-time object detection.

---

## 论文详细总结（自动生成）

### CARMEN：一种用于深度学习的 CORDIC 加速、资源高效型多精度推理引擎

#### 1. 论文的核心问题与整体含义（研究动机和背景）
深度学习模型（如 CNN、ViT、LLM）在边缘侧部署面临严峻的资源挑战。研究发现，乘累加（MAC）操作占计算量的 90%，而激活函数（AF）仅占 2-5%，但现有硬件设计往往为 AF 分配了过多的静态资源，导致硬件利用率低下（空闲率高达 84%）。此外，现有的加速器通常在设计时固定了计算精度，无法根据不同网络层对误差的敏感度动态调整。
**核心动机**：利用 CORDIC 算法的迭代特性，通过控制迭代深度来实现计算精度与资源消耗的动态平衡，并解决 MAC 与 AF 之间的硬件资源分配失衡问题。

#### 2. 论文提出的方法论
论文提出了 **CARMEN** 架构，其核心思想和关键技术包括：
*   **运行时自适应迭代 CORDIC MAC**：不同于传统的流水线 CORDIC（每一级都需要独立硬件），CARMEN 采用迭代式设计，复用同一组算术资源。通过动态调整迭代次数，可以在“近似模式”（低延迟、低功耗）和“精确模式”之间切换，无需修改硬件。
*   **时分复用多激活函数（Multi-AF）模块**：针对 AF 占比小的特点，设计了一个共享 CORDIC 数据通路的支持模块，通过时分复用支持 ReLU、GELU、SoftMax、Tanh、Sigmoid、Swish 和 SELU 等多种函数，面积开销增加不到 4%。
*   **多精度支持**：支持 8 位和 16 位定点精度的灵活切换。
*   **外围组件优化**：集成了 AAD 池化单元（相比传统池化能提升 0.5-1% 的精度）和归一化单元，通过 AXI 总线与外部存储交互。

#### 3. 实验设计
*   **数据集与模型**：
    *   数据集：MNIST、CIFAR-10、CIFAR-100、ImageNet。
    *   模型：LeNet-5、ResNet-18、CaffeNet、VGG-16、Tiny YOLO-v3。
*   **Benchmark（对比对象）**：
    *   对比了近年来的顶级会议/期刊成果（2022-2025 年），包括 Flex-PE、LPRE、QuantMAC、MSDF-MAC 等。
    *   硬件对比对象包括 NVIDIA Jetson Nano 和 Raspberry Pi 等商用边缘平台。
*   **验证流程**：使用 Python（FxP-Math 和 QKeras）进行周期精确的软件模拟，随后使用 Verilog HDL 实现并在 Synopsys VCS 中进行 RTL 仿真验证。

#### 4. 资源与算力
*   **ASIC 实现**：采用 28 nm CMOS HPC+ 工艺。
    *   64-PE 配置：面积 0.43 $mm^2$，能效 3.84 TOPS/W。
    *   256-PE 配置：计算密度 4.83 $TOPS/mm^2$，能效 11.67 TOPS/W。
*   **FPGA 实现**：部署于 Pynq-Z2（Zynq-7000 系列）和 VC707 开发板。
    *   在 Pynq-Z2 上，功耗仅为 0.43 W，处理目标检测任务的延迟为 154.6 ms。
*   **训练算力**：论文未明确说明模型训练所使用的 GPU 型号或时长，因为该研究重点在于**推理引擎（Inference Engine）**的硬件架构设计。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 5 种主流模型和 4 个数据集上进行了精度评估，并在 FPGA 和 ASIC 两种平台上进行了详尽的硬件性能测试。
*   **充分性**：实验涵盖了从底层算子（MAC、AF）到系统级（Tiny YOLO-v3 目标检测）的全面对比。通过消融实验展示了不同迭代深度对精度的影响，实验设计较为客观、公平，对比基准均为近年来的 SOTA（先进水平）设计。

#### 6. 论文的主要结论与发现
*   **精度与效率的平衡**：在 8 位定点精度下，CORDIC 计算的精度损失控制在 2% 以内，而 16 位精度下几乎无损失。
*   **硬件优势**：相比于之前的 CORDIC 设计，CARMEN 的迭代 MAC 单元减少了约 50% 的 FPGA 资源占用。
*   **性能提升**：在 28 nm 工艺下，相比同类竞争方案，计算密度提升了 1.75 倍，能效提升了 1.5 倍。
*   **实时性**：在低功耗 FPGA 平台上实现了优于 NVIDIA Jetson Nano 的推理延迟和能效比。

#### 7. 优点
*   **极高的资源效率**：通过迭代复用硬件，极大地缩小了芯片面积，非常适合资源受限的边缘设备。
*   **高度灵活性**：无需重新配置 FPGA 或更换芯片，即可在运行时根据任务需求调整精度和功耗。
*   **全面的 AF 支持**：一个模块支持 7 种激活函数，解决了异构网络层对不同激活函数的需求。
*   **无 DSP 依赖**：在 FPGA 实现中不使用 DSP 块，纯逻辑实现，提高了设计的可移植性。

#### 8. 不足与局限
*   **单次操作延迟**：迭代式 CORDIC 虽然节省了面积，但相比于全流水线结构，单次 MAC 操作的时钟周期数增加，必须依靠大规模并行（PE 阵列）来弥补吞吐量。
*   **软件映射复杂性**：运行时自适应精度需要配套的编译器支持，论文中提到未来将开发自动化的层级精度调整框架，目前可能仍需手动配置。
*   **应用范围限制**：目前主要针对定点运算优化，对于需要极高动态范围的浮点运算（如某些科学计算场景）支持有限。

（完）
