---
title: "TREA: Low-precision Time-Multiplexed, Resource-Efficient Edge Accelerator for Object Detection and Classification"
title_zh: TREA：用于目标检测与分类的低精度时分复用、资源高效型边缘加速器
authors: "Vijay Pratap Sharma, Mukul Lokhande, Ratko Pilipovic, Omkar Kokane, Santosh Kumar Vishvakarma"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07321v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 与边缘加速器协同设计的硬件感知还原剪枝策略
tldr: TREA 是一种针对边缘视觉任务设计的低功耗、资源高效型 AI 加速器。它通过双精度（4/8位）SIMD 乘累加单元（DQ-MAC）和硬件感知的结构化剪枝策略（SHARP），在不增加硬件开销的情况下显著提升了吞吐量。结合可重构的非线性激活函数核心（RQ-NAF）和时间复用架构，TREA 在满足严苛的面积和功耗约束的同时，大幅降低了目标检测与分类任务的延迟，为实时边缘视觉应用提供了高能效的解决方案。
selection_source: fresh_fetch
motivation: 针对边缘视觉平台在面积、功耗和延迟方面的严苛限制，开发一种高效且灵活的低精度硬件加速器。
method: 提出基于最高有效位优先计算的双精度 SIMD 单元，并结合硬件感知的结构化剪枝策略与可重构的 CORDIC 激活函数核心。
result: 在 4 位精度模式下实现了 4 倍的吞吐量提升，并在卷积核计算层面实现了最高 9 倍的延迟缩减。
conclusion: TREA 通过创新的硬件复用和低精度计算技术，验证了其作为实时边缘视觉工作负载高效解决方案的有效性。
---

## 摘要
本研究提出了 TREA，一种用于目标检测与分类的低精度时分复用且资源高效的边缘人工智能加速器，旨在满足边缘视觉平台严苛的面积-功耗-延迟约束。所提出的架构集成了一个双精度（4/8位）单指令多数据流乘累加（DQ-MAC）单元，该单元基于最高有效位优先（MSDF）的移位加法计算并结合运行时位截断，消除了传统乘法器的开销并减小了累加器的位宽。DQ-MAC 每个周期支持 4 次 FxP4 或 1 次 FxP8 操作，在不增加硬件副本的情况下实现了高达 4 倍的吞吐量提升。针对 SIMD 数据通路协同设计了一种结构化硬件感知还原剪枝（SHARP）策略，在保持 MAC 满载利用率的同时实现了接近 50% 的结构化稀疏。这使得 3x3 卷积核在 FxP4 模式下仅需 1 个周期即可完成计算（相比之下 FxP8 模式需 9 个周期），5x5 卷积核仅需 3 个周期（相比之下需 25 个周期），在算子层级实现了高达 9 倍的延迟降低。该加速器还包含一个基于 CORDIC 的可重构非线性激活函数（RQ-NAF）内核，采用 9 级流水线，在流水线填满后每个周期可支持一个 Sigmoid、Tanh 或 ReLU 输出，同时通过时分复用实现了 (N-1) 倍的硬件复用。完整的 TREA 架构采用由 100 个 SIMD DQ-MAC 单元组成的 1 维阵列，并结合逐层硬件复用，显著降低了面积和控制复杂度。实验结果表明，与传统的固定精度和不可重构加速器相比，TREA 在延迟、硬件利用率和能效方面均有显著提升，验证了其作为实时边缘视觉任务有效解决方案的可行性。

## Abstract
This work presents TREA, a low-precision time-multiplexed and resource-efficient edge-AI accelerator for object detection and classification, targeting stringent area-power-latency constraints of edge vision platforms. The proposed architecture integrates a dual-precision (4/8-bit) SIMD multiply-accumulate (DQ-MAC) unit based on most-significant-digit-first (MSDF) shift-and-add computation with run-time bit truncation, eliminating conventional multiplier overhead and reducing accumulator bit-width. The DQ-MAC supports 4x FxP4 or 1x FxP8 operations per cycle, achieving up to 4x throughput improvement without hardware duplication. A structured hardware-aware reductive pruning (SHARP) strategy is co-designed with the SIMD datapath, enabling near 50% structured sparsity while maintaining full MAC utilization. This allows a 3x3 convolution kernel to be computed in 1 cycle in FxP4 mode compared to 9 cycles in FxP8, and a 5x5 kernel in 3 cycles versus 25 cycles, yielding up to 9x latency reduction at the kernel level. The accelerator further incorporates a reconfigurable CORDIC-based nonlinear activation function (RQ-NAF) core with a 9-stage pipeline, supporting Sigmoid, Tanh, and ReLU at one output per cycle after pipeline fill, while enabling (N-1) hardware reuse through time-multiplexing. The complete TREA architecture employs a 1D array of 100 SIMD DQ-MAC units with layer-wise hardware reuse, significantly reducing area and control complexity. Experimental results demonstrate substantial improvements in latency, hardware utilization, and energy efficiency compared to conventional fixed-precision and non-reconfigurable accelerators, validating TREA as an effective solution for real-time edge vision workloads.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TREA** 的边缘人工智能加速器，旨在通过算法与硬件的深度协同设计，解决边缘视觉设备在处理目标检测和分类任务时面临的面积、功耗和延迟（Trilemma）挑战。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
边缘视觉系统（EV2AI）如目标检测和实时场景识别，通常部署在资源受限的嵌入式平台上。这些模型（如 ViT 或 YOLO）计算量巨大且内存密集，传统的硬件加速器往往在精度、延迟和能效之间难以平衡。
*   **核心问题**：如何在极小的面积和功耗预算下，实现高吞吐量、低延迟的深度学习推理？
*   **研究动机**：现有的优化手段（量化、剪枝、硬件复用）往往是孤立设计的。本研究旨在提出一个统一的协同设计框架，从算术单元、剪枝策略到架构复用进行全面优化。

### 2. 方法论：核心思想与关键技术
TREA 的核心在于四个关键技术的集成：
*   **DQ-MAC（双精度 SIMD 乘累加单元）**：
    *   **无乘法器设计**：基于最高有效位优先（MSDF）的移位加法计算，利用“符号幂之和”（PoT）近似权重。
    *   **预累加截断**：在累加前进行位截断，显著减小了累加器位宽和寄存器开销。
    *   **双精度支持**：支持 4位（4路 SIMD）或 8位（1路）操作，在 4位模式下吞吐量提升 4 倍。
*   **SHARP（硬件感知结构化还原剪枝）**：
    *   **SIMD 对齐**：强制剪枝后的权重数量与 SIMD 通道数（4路）对齐（如 3x3 卷积保留 4 个权重，5x5 保留 12 个）。
    *   **效率提升**：确保硬件通道 100% 利用率，消除空闲周期。使 3x3 卷积计算从 9 周期缩减至 1 周期。
*   **RQ-NAF（可重构量化激活函数核心）**：
    *   基于 **CORDIC 算法** 的 9 级流水线设计，通过时分复用支持 Sigmoid、Tanh 和 ReLU，避免了为每层重复构建激活硬件。
*   **TREA 架构**：
    *   采用 100 个 DQ-MAC 单元组成的 1D 阵列，通过时分复用实现逐层硬件复用，简化了控制逻辑并降低了静态功耗。

### 3. 实验设计
*   **数据集**：
    *   **目标检测**：PASCAL VOC 2007+2012, COCO 2017。
    *   **图像分类**：ImageNet (ILSVRC-2012)。
*   **Benchmark 模型**：YOLO 变体（Tiny, S, M, L）、MobileNet (v1/v2/v3)、EfficientNet-B0、Inception-v3。
*   **对比方法**：
    *   **软件层面**：对比 FP32 基准、统一 8位/4位量化、以及 AHCO、Bose-8 等剪枝量化方案。
    *   **硬件层面**：对比 Xilinx 官方 IP、Flex-PE、QuantMAC 以及近年的 SOTA 加速器（如 TVLSI'25, TCAS-I'24 等）。

### 4. 资源与算力
*   **软件训练**：使用 **NVIDIA L4 GPU**（在 Google Colab 环境下执行）。
*   **硬件实现**：
    *   **FPGA**：基于 Xilinx VC707 评估板。
    *   **ASIC**：采用 **28-nm HPC CMOS** 工艺进行综合与布局布线。
*   **训练细节**：YOLO 模型训练 200-300 epochs，分类模型训练 90 epochs。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **消融实验**：分别验证了 DQ-MAC、SHARP 剪枝以及两者结合对延迟和精度的影响。
*   **精度评估**：涵盖了不同 YOLO 规模和多种分类网络，证明 mAP 下降控制在 3% 以内。
*   **硬件指标**：对比了 LUT 利用率、功耗、关键路径延迟（CPD）、能效（GOPS/W）等。
*   **充分性评价**：实验设计非常充分，不仅有软件层面的精度验证，还有完整的 ASIC 后仿真数据，对比对象涵盖了过去 3-5 年内的顶级期刊成果，具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **性能飞跃**：通过 SHARP 与 DQ-MAC 的协同，卷积核层级的延迟降低了 **8.3x 至 9.0x**。
*   **资源高效**：相比 Xilinx 官方 IP，LUT 利用率降低了高达 **72%**，功耗降低了约 **50%**。
*   **精度保持**：在大幅压缩和量化的情况下，目标检测的 mAP 损失极小，且在大模型（YOLO-L）上表现更优。
*   **能效领先**：在 FPGA 部署中实现了 **40.00 GOPS/W** 的能效，优于绝大多数对比的 SOTA 加速器。

### 7. 优点
*   **全栈协同**：打破了算法优化与硬件设计之间的壁垒，特别是剪枝模式与 SIMD 通道的精确对齐非常巧妙。
*   **无乘法器架构**：利用 MSDF 移位加法代替昂贵的乘法器，极大地压缩了芯片面积。
*   **灵活性**：支持运行时重构激活函数和双精度切换，适应多种视觉任务。

### 8. 不足与局限
*   **精度敏感性**：虽然平均 mAP 下降小于 3%，但对于某些对精度极度敏感的工业检测场景，4位量化可能仍存在风险。
*   **算子覆盖**：论文重点讨论了卷积和全连接层，对于 Vision Transformer (ViT) 中日益复杂的注意力机制算子的原生支持细节描述较少。
*   **工艺节点**：虽然 28nm 是边缘设备的常用工艺，但未讨论在更先进工艺（如 7nm/5nm）下的扩展性或漏电流表现。

（完）
