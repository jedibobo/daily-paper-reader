---
title: "XiYOLO: Energy-Aware Object Detection via Iterative Architecture Search and Scaling"
title_zh: XiYOLO：通过迭代架构搜索与缩放实现的能效感知目标检测
authors: "Tony Tran, Richie R. Suganda, Bin Hu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06927v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 面向边缘设备的能耗感知迭代架构搜索
tldr: 针对边缘设备目标检测中能耗优化难、设备依赖性强的问题，本文提出XiYOLO框架。该框架结合了XiResOFA搜索空间、两阶段能量评估器和迭代搜索，旨在寻找能效最优的基础架构。通过复合缩放生成XiYOLO系列模型，在PascalVOC和COCO数据集上，相比YOLOv12等基准模型，在保持高精度的同时显著降低了GPU和NPU的能耗，实现了更优的能效平衡。
selection_source: fresh_fetch
motivation: 边缘设备上的目标检测面临严苛的能耗和延迟约束，且实际能耗难以测量且高度依赖特定硬件。
method: 提出一种包含XiResOFA搜索空间、两阶段能量评估器和迭代搜索的框架，并结合复合缩放构建XiYOLO系列模型。
result: "在PascalVOC和COCO上，XiYOLO相比YOLOv12在GPU和NPU上分别实现了最高53.7%和51.6%的能耗降低。"
conclusion: XiYOLO通过高效的架构搜索和缩放策略，为异构边缘设备提供了一种在极低样本下即可实现高能效目标检测的有效方案。
---

## 摘要
异构边缘设备上的目标检测必须满足严格的能耗、延迟和内存限制，同时仍需为下游自主系统提供可靠的感知。现有的能效感知神经架构搜索（NAS）方法通常针对有限的部署场景，而实际能耗由于高度依赖设备且测量成本高昂，仍然难以优化。我们通过一个能量自适应框架来应对这些挑战，该框架结合了能效感知的 XiResOFA 搜索空间、两阶段能量估计器和迭代搜索，以确定单一的高能效基础架构。随后，我们应用复合缩放将该基础设计转化为适用于不同部署预算的 XiYOLO 系列，从而在稀疏的硬件测量下实现可解释的精度-能耗权衡。在 PascalVOC、COCO 和真实设备部署上的实验表明，XiYOLO 实现了比 YOLO 基准模型更强的能耗-精度权衡。在 PascalVOC 上，中型 XiYOLO 模型达到了 86.15 mAP50，同时在 GPU 和 NPU 上相对于 YOLOv12m 分别降低了 20.6% 和 35.9% 的能耗。在 COCO 上，小型 XiYOLO 在 GPU 和 NPU 上相对于 YOLOv12 分别降低了高达 53.7% 和 51.6% 的能耗。所提出的两阶段估计器在仅有 2-20 个目标设备样本的小样本自适应下，其样本效率也优于联合预测器。

## Abstract
Object detection on heterogeneous edge devices must satisfy strict energy, latency, and memory constraints while still providing reliable perception for downstream autonomy. Existing energy-aware NAS methods often target limited deployment settings, while real energy remains difficult to optimize because it is highly device-dependent and costly to measure. We address these challenges with an energy-adaptive framework that combines an energy-aware XiResOFA search space, a two-stage energy estimator, and iterative search to identify a single energy-efficient base architecture. We then apply compound scaling to transform this base design into the XiYOLO family across deployment budgets, enabling interpretable accuracy-energy tradeoffs under sparse hardware measurements. Experiments on PascalVOC, COCO, and real-device deployment show that XiYOLO achieves a stronger energy-accuracy tradeoff than YOLO baselines. On PascalVOC, the medium XiYOLO model reaches 86.15 mAP50 while reducing energy relative to YOLOv12m by 20.6% on GPU and 35.9% on NPU. On COCO, XiYOLO reduces energy relative to YOLOv12 by up to 53.7% on GPU and 51.6% on NPU at the small scale. The proposed two-stage estimator also improves sample efficiency over a joint predictor under few-shot adaptation with only 2-20 target-device samples.

---

## 论文详细总结（自动生成）

以下是对论文《XiYOLO: Energy-Aware Object Detection via Iterative Architecture Search and Scaling》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在具有严苛能耗、延迟和内存约束的异构边缘设备（如无人机、移动机器人）上，实现高精度且能效最优的目标检测。
*   **研究背景**：
    *   **能耗优化的复杂性**：传统的 FLOPs 或参数量指标无法准确反映实际硬件能耗，因为能耗高度依赖于内存移动、算子组合及特定平台的执行行为。
    *   **硬件异构性**：不同设备的能效特性差异巨大，现有的 NAS 方法往往只针对单一模型或特定硬件，缺乏可扩展性。
    *   **测量成本高**：在大量候选架构上直接测量硬件能耗成本极高，导致能效感知搜索难以实施。

### 2. 方法论
论文提出了 **XiYOLO** 框架，包含以下四个核心组件：
*   **XiResOFA 搜索空间**：设计了一种新型的 XiResOFA 模块，支持弹性架构选择，包括：
    *   **压缩率 (Compression Ratio)**：{1, 0.5, 0.25}，控制通道数。
    *   **卷积核大小 (Kernel Size)**：{1, 3, 5}，调节感受野。
    *   **注意力机制 (Attention)**：Lite 或 Full 两种模式。
*   **两阶段能量估计器 (Two-Stage Energy Estimator)**：
    *   **第一阶段**：通用预测器，学习架构与能耗之间的通用先验。
    *   **第二阶段**：硬件特定残差模型，通过极少量（2-20个）目标设备样本进行微调，捕捉特定硬件的执行偏差。
*   **迭代架构搜索 (Iterative Search)**：
    *   不采用全局联合搜索，而是依次对 **Backbone（骨干网络）**、**FPN（特征金字塔）** 和 **PAN（路径聚合网络）** 进行迭代优化。这种阶段式精炼降低了搜索复杂度，同时允许各组件相互适配。
*   **复合缩放 (Compound Scaling)**：
    *   搜索确定一个高效的“基础架构”后，通过复合缩放（调整宽度、深度等）生成 Nano、Small、Medium 等不同规格的 XiYOLO 模型家族。

### 3. 实验设计
*   **数据集**：PascalVOC 和 MS COCO。
*   **硬件平台**：
    *   ModalAI Sentinel 开发无人机（搭载 Qualcomm QRB5165，含 Adreno 650 GPU 和 15 TOPS NPU）。
    *   NVIDIA Jetson Orin Nano。
*   **基准模型 (Benchmarks)**：YOLOv5、YOLOv8、YOLO11 和最新的 YOLOv12。
*   **对比维度**：mAP（精度）、每帧能耗、累计能耗、延迟、平均功率。

### 4. 资源与算力
*   **训练设备**：
    *   **PascalVOC**：使用 1 张 NVIDIA RTX 4090 GPU。
    *   **COCO**：使用 8 张 NVIDIA V100 GPU。
*   **训练时长**：
    *   超网络（Supernet）训练：300 轮（Epochs）。
    *   子网络（Subnet）均匀采样训练：300 轮。
    *   搜索后的模型微调：300 轮。
*   **搜索成本**：采样 1000 个子网络用于训练精度和能量代理模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 Nano 到 Medium 的全量程对比，并在 GPU、NPU 和 CPU 多种后端上进行了测试。
*   **消融实验**：
    *   验证了 XiResOFA 模块中卷积类型和残差连接对精度/能耗的影响。
    *   对比了迭代搜索空间与全局搜索空间的优劣。
    *   在 HW-NAS-Bench 上验证了两阶段能量估计器的样本效率。
*   **充分性评价**：实验设计较为充分，不仅有标准数据集的离线测试，还有真实无人机硬件上的在线能耗监测，客观地展示了模型在实际部署中的优势。

### 6. 主要结论与发现
*   **能效显著提升**：在 PascalVOC 上，XiYOLO-m 精度达到 86.15 mAP50，比 YOLOv12m 能耗降低了 20.6% (GPU) 和 35.9% (NPU)。
*   **小模型优势更大**：在 COCO 上，小型 XiYOLO 在 GPU 和 NPU 上的能耗降低均超过 50%，且精度保持竞争力。
*   **估计器高效性**：两阶段能量估计器在仅需 2-20 个样本的情况下，预测精度显著优于传统的联合预测模型。
*   **搜索空间质量**：XiResOFA 搜索空间天然偏向低能耗区域，平均能耗比 YOLOv12 基准低 21.1%。

### 7. 优点
*   **软硬结合**：不仅优化算法架构，还深入考虑了硬件底层的能耗特性。
*   **样本效率高**：通过残差建模解决了硬件能耗数据获取难的问题，使 NAS 更易迁移到新硬件。
*   **可解释性**：XiResOFA 的弹性选择（如核大小、压缩率）与精度-能耗权衡之间有直观的对应关系。
*   **实用性强**：直接在无人机平台上进行验证，证明了其在自主系统中的应用潜力。

### 8. 不足与局限
*   **架构基准限制**：搜索空间主要基于 YOLOv12 衍生，可能限制了探索更激进或完全不同架构的可能性。
*   **缩放策略**：采用的是搜索后缩放（Post-search scaling），而非针对每个预算点进行独立搜索，虽然提高了效率，但可能在极端预算点下不是理论最优。
*   **硬件覆盖**：虽然测试了高通和 NVIDIA 平台，但对于更低功耗的单片机（MCU）或更多样化的 DSP 平台覆盖有限。

（完）
