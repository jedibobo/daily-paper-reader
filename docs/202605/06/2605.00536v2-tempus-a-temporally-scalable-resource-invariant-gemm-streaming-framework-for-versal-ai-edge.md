---
title: "Tempus: A Temporally Scalable Resource-Invariant GEMM Streaming Framework for Versal AI Edge"
title_zh: Tempus：一种面向 Versal AI Edge 的时间可扩展且资源恒定的 GEMM 流式框架
authors: "M. Grailoo, J. Núñez-Yáñez"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00536v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 在资源受限的边缘SoC上最大化性能的GEMM流框架
tldr: 针对边缘端大语言模型推理中GEMM加速面临的资源受限问题，本文提出Tempus框架。不同于传统的空间扩展方法，Tempus在AMD Versal AI Edge SoC上采用固定16核的资源不变设计，通过时间维度的迭代执行、数据分块及级联流技术实现高效扩展。该框架在显著降低功耗和资源占用的同时，实现了极高的平台感知效能，为边缘端LLM推理提供了可持续且可扩展的基础。
selection_source: fresh_fetch
motivation: 传统的空间扩展GEMM加速器在资源受限的边缘SoC上容易导致物理实现失败、带宽饱和及功耗过高。
method: 采用固定16个AIE-ML核心的计算块，通过可编程逻辑中的算法分块、数据复用及无死锁数据流协议实现时间维度的可扩展性。
result: 在10.677W功耗下达到607 GOPS，相比SOTA框架在核心节省、功耗降低和I/O需求减少方面表现优异，且URAM/DSP占用率为零。
conclusion: Tempus证明了通过时间扩展而非空间扩展，可以在极低资源消耗下实现高效且可持续的边缘端GEMM加速。
---

## 摘要
大语言模型 (LLM) 的缩放法则表明，模型质量随计算规模的增加而提升，但边缘部署对计算、内存和功耗提出了严格限制。由于通用矩阵乘法 (GEMM) 占据了高达 90% 的推理时间，高效的 GEMM 加速对于边缘 AI 至关重要。AMD Versal 自适应 SoC 中的自适应智能引擎 (AIE) 非常适合此任务，但现有的最先进 (SOTA) 框架通过空间缩放来最大化性能，将工作负载分布在数百个核心上——这种方法在资源受限的边缘 SoC 上会因物理实现失败、带宽饱和以及过度的资源消耗而失效。我们提出了 Tempus，一种面向 AMD Versal AI Edge SoC 的资源恒定时间 GEMM 框架。Tempus 并非随矩阵大小扩展硬件资源，而是采用由 16 个 AIE-ML 核心组成的固定计算块，通过迭代图执行以及可编程逻辑 (PL) 中的算法数据分块和复制来实现可扩展性。高速级联流确保了在启动间隔 (II) 为 1 时实现低延迟的部分和归约，而无死锁的 DATAFLOW 协议则最大化了传输与计算的重叠以及 PLIO 的复用。在 GEMM 工作负载的评估中，Tempus 在 10.677 W 的总片上功耗下实现了 607 GOPS 的性能。通过平台感知效用 (PAU) 指标表征系统级效率，我们证明了 Tempus 的显著因子 (prominence factor) 比领先的空间 SOTA (ARIES) 高出 211.2 倍。此外，该框架保持了 0.00% 的 URAM/DSP 利用率，实现了 22.0 倍的核心节约率、7.1 倍的功耗节约率以及 6.3 倍的 I/O 需求降低，为边缘 LLM 推理奠定了可持续且可扩展的基础。

## Abstract
Scaling laws for Large Language Models (LLMs) establish that model quality improves with computational scale, yet edge deployment imposes strict constraints on compute, memory, and power. Since General Matrix Multiplication (GEMM) accounts for up to 90% of inference time, efficient GEMM acceleration is critical for edge AI. The Adaptive Intelligent Engines available in the AMD Versal adaptive SoCs are well suited for this task, but existing state-of-the-art (SOTA) frameworks maximize performance through spatial scaling, distributing workloads across hundreds of cores -- an approach that fails on resource-limited edge SoCs due to physical implementation failures, bandwidth saturation, and excessive resource consumption. We propose Tempus, a Resource-Invariant Temporal GEMM framework for the AMD Versal AI Edge SoC. Rather than expanding hardware resources with matrix size, Tempus employs a fixed compute block of 16 AIE-ML cores, achieving scalability through iterative graph execution and algorithmic data tiling and replication in the Programmable Logic. High-speed cascade streaming ensures low-latency partial sum reduction at Initiation Interval (II) of 1, while a deadlock-free DATAFLOW protocol maximizes transfer-compute overlap and PLIO reuse. Evaluated on GEMM workloads, Tempus achieves 607 GOPS at 10.677 W total on-chip power. By characterizing system-level efficiency through the Platform-Aware Utility (PAU) metric, we prove that Tempus achieves a 211.2x higher prominence factor than the leading spatial SOTA (ARIES). Furthermore, the framework maintains a 0.00% utilization of URAM/DSP, yielding 22.0x core frugality, 7.1x power frugality, and a 6.3x reduction in I/O demand, establishing a sustainable, scalable foundation for edge LLM inference.

---

## 论文详细总结（自动生成）

以下是对论文《Tempus: A Temporally Scalable Resource-Invariant GEMM Streaming Framework for Versal AI Edge》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）推理中，通用矩阵乘法（GEMM）占据了约90%的计算时间。现有的加速框架（如ARIES、CHARM）倾向于“空间扩展”，即动用数百个核心来追求峰值吞吐量。
*   **研究动机**：这种空间扩展方法在资源受限的边缘级SoC（如AMD Versal VE2302）上会失效，原因包括：
    *   **物理实现失败**：高核心利用率导致布线拥塞。
    *   **资源挤占**：耗尽了可编程逻辑（PL）资源（如URAM/DSP），导致无法集成Softmax、LayerNorm等其他必要算子。
    *   **功耗过高**：不符合边缘端的散热和电力限制。
*   **整体含义**：本文提出了一种“时间扩展”的资源恒定框架，旨在通过固定少量的计算资源，利用迭代执行和高效流式传输来实现可持续的边缘端加速。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将3D矩阵乘法映射到固定的2D AIE-ML核心阵列（本研究中固定为16核），通过在时间维度上迭代执行AIE图（Graph）来处理大规模工作负载，而非增加硬件规模。
*   **关键技术细节**：
    *   **资源恒定框架**：解耦资源利用率与矩阵大小。通过算法数据复制和分块（Tiling）策略，在PL中仅使用轻量级FIFO。
    *   **7阶段控制流**：由处理系统（PS）编排，包括初始化、数据准备、设备映射、内核启动、迭代计算（`GRAPH_ITER_CNT`控制）、结果同步等。
    *   **高效流式传输**：
        *   **级联流（Cascade Stream）**：在核心间直接进行部分和归约，实现启动间隔（II）为1，比传统的缓冲区共享快约50%。
        *   **混合交换模式**：对矩阵A采用广播电路交换，对矩阵B采用分组交换，最大化PLIO接口复用。
    *   **无死锁DATAFLOW协议**：在PL内核中使用Vitis HLS的DATAFLOW指令，确保计算与数据传输高度重叠。

### 3. 实验设计
*   **实验场景**：在AMD Versal AI Edge VE2302 SoC上进行评估。
*   **工作负载**：涵盖了从 $32^3$ 到 $1024^3$ 的立方GEMM，以及针对LLM特定层（如Attention投影、FFN扩展层）的长方形GEMM。
*   **Benchmark（对比方法）**：
    *   **ARIES**：基于MLIR的领先空间扩展框架。
    *   **CHARM / CHARM 2.0**：异构加速器组合框架。
    *   **AUTO MM / AUTO SA**：基于搜索或多面体编译的自动化框架。
*   **评估指标**：吞吐量（GOPS）、功耗（W）、资源占用率、平台感知效用（PAU）。

### 4. 资源与算力
*   **硬件平台**：AMD Versal AI Edge VE2302（包含34个AIE-ML核，本框架仅使用其中**16个**）。
*   **算力表现**：在1024³ INT16任务下达到 **607 GOPS**。
*   **功耗**：总片上功耗仅为 **10.677 W**（其中AIE引擎仅占2.381 W）。
*   **PL资源利用率**：实现了 **0.00% 的 URAM 和 DSP 占用**，LUT占用率约6.16%，BRAM占用率约62.58%（主要用于流式FIFO）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了不同分块尺寸（DIM从4到128）的消融实验，验证了微内核尺寸对效率的影响。
    *   测试了多种数据类型（INT16, INT32）。
    *   针对LLM实际应用，测试了15种以上不同形状的矩阵（包括极窄、极宽等长方形矩阵）。
*   **充分性评价**：实验设计较为充分，不仅对比了吞吐量，还引入了“节约率”（Frugality）和PAU指标，客观地评价了在资源受限环境下的架构优劣。

### 6. 主要结论与发现
*   **时间扩展的可行性**：通过迭代执行，小规模核心阵列也能处理大规模矩阵，且随着工作负载增加，系统初始化开销被有效摊薄。
*   **极高的资源效率**：相比ARIES，Tempus在核心节约上达到22倍，功耗节约7.1倍，I/O需求减少6.3倍。
*   **平台感知效用（PAU）**：Tempus的PAU显著因子比ARIES高出 **211.2倍**，证明了其在边缘芯片上的架构优越性。
*   **形状无关性**：在处理LLM中不平衡的矩阵形状时，Tempus表现稳定，避免了空间框架常见的“利用率崩溃”现象。

### 7. 优点
*   **资源保护**：0%的URAM/DSP占用为边缘设备集成其他复杂算子（如Softmax）留出了充足空间。
*   **高可持续性**：极低的功耗和核心占用，解决了边缘端的散热和布线难题。
*   **开源贡献**：提供了开源代码，增加了研究的可验证性。
*   **模型化分析**：引入了分析模型来推导`GRAPH_ITER_CNT`等关键参数，具有理论指导意义。

### 8. 不足与局限
*   **绝对延迟较高**：由于核心数少且采用时间迭代，其绝对推理延迟（Latency）明显高于动用数百核的空间扩展框架（如3.537ms vs 0.135ms）。
*   **内存受限**：微内核尺寸（DIM）受限于AIE核心的本地内存容量，这限制了进一步提升效率的空间。
*   **软件工具链限制**：受限于AMD Vitis 2024.1工具链，目前仅报告了INT16/INT32结果，未充分展示AIE-ML对INT4/INT8/BFLOAT16的硬件原生支持。
*   **应用范围**：主要针对GEMM，对于非线性算子的集成虽然留出了资源，但尚未在本文中展示完整的端到端LLM推理流程。

（完）
