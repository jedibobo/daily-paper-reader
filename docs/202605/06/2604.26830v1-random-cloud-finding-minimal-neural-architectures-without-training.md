---
title: "Random Cloud: Finding Minimal Neural Architectures Without Training"
title_zh: Random Cloud：无需训练即可寻找极小神经网络架构
authors: Javier Gil Blázquez
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26830v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 无需训练的神经架构搜索方法，寻找最小拓扑
tldr: 本研究提出Random Cloud，一种无需训练的神经架构搜索方法，通过随机探索和渐进式结构缩减寻找最小前馈网络拓扑。该方法在不使用反向传播的情况下评估随机初始化的网络，仅在最后训练最优候选模型。实验表明，它在多个基准测试中优于传统剪枝方法，且由于避免了全尺寸网络的训练，计算效率显著提升。
selection_source: fresh_fetch
motivation: 旨在解决传统神经网络剪枝方法中“训练-剪枝-再训练”循环带来的高昂计算成本问题。
method: 通过对随机初始化的网络进行随机探索和结构缩减来评估拓扑性能，跳过全量训练过程直接锁定最小架构。
result: 在7个数据集中有6个优于剪枝基准，在实现大幅参数缩减的同时提升了准确率，且运行速度仅为完整训练的0.67至0.94倍。
conclusion: Random Cloud证明了无需预训练即可高效发现高性能最小架构的可行性，为低资源环境下的模型压缩提供了新思路。
---

## 摘要
我提出了 Random Cloud 方法，这是一种无需训练的神经架构搜索方法，通过随机探索和渐进式结构缩减来发现极小前馈网络拓扑。与需要完整“训练-剪枝-再训练”周期的训练后剪枝方法不同，该方法在不进行反向传播的情况下评估随机初始化的网络，逐步缩减其拓扑结构，并仅在最后训练最佳的极小候选网络。我在 7 个分类基准测试中将其与量级剪枝（magnitude pruning）和随机剪枝（random pruning）基准进行了对比评估。Random Cloud 在 7 个数据集中的 6 个上达到或超过了这两个基准，在 Sonar 数据集上实现了显著的统计学提升（准确率提高 4.9 个百分点，$p=0.017$，对比量级剪枝），同时减少了 87% 的参数。至关重要的是，该方法在 5 个数据集中的 4 个上比两种剪枝基准都快（成本仅为完整训练的 0.67-0.94 倍），因为它完全避免了对全尺寸网络的训练。

## Abstract
I propose the \emph{Random Cloud} method, a training-free approach to neural architecture search that discovers minimal feedforward network topologies through stochastic exploration and progressive structural reduction. Unlike post-training pruning methods that require a full train-prune-retrain cycle, this method evaluates randomly initialized networks without backpropagation, progressively reduces their topology, and only trains the best minimal candidate at the end. I evaluate on 7 classification benchmarks against magnitude pruning and random pruning baselines. The Random Cloud matches or outperforms both baselines in 6 of 7 datasets, achieving statistically significant improvements on Sonar ($+4.9$pp accuracy, $p{=}0.017$ vs magnitude pruning) with 87\% parameter reduction. Crucially, the method is faster than both pruning baselines in 4 of 5 datasets (0.67--0.94$\times$ the cost of full training), since it avoids training the full-size network entirely.

---

## 论文详细总结（自动生成）

这是一份关于论文《Random Cloud: Finding Minimal Neural Architectures Without Training》的结构化深入总结：

### 1. 论文的核心问题与整体含义
该研究旨在解决神经网络在寻找“极小拓扑结构”（Minimal Topology）时的高昂计算成本问题。
*   **研究动机**：传统的模型压缩方法（如剪枝）通常遵循“训练-剪枝-再训练”的循环，这要求至少进行两次完整的训练周期。而神经架构搜索（NAS）虽然能自动化寻找拓扑，但往往需要数千个 GPU 小时。
*   **核心理念**：作者提出，在足够大的随机初始化网络“云”中，某些网络即使不经过训练也能表现出非平凡的分类能力。通过识别并缩减这些网络，可以在**完全不进行反向传播**的情况下找到高效的极小架构。

### 2. 论文提出的方法论：Random Cloud
该方法分为三个阶段，核心在于“预训练结构缩减”：

*   **阶段 1：探索（Exploration）**
    *   生成 $N$ 个具有随机权重（服从 $U(-1, 1)$ 分布）的网络。
    *   **无需训练**，仅通过前向传播评估每个网络在训练集上的分类准确率。
    *   **渐进式缩减**：从最后一个隐藏层开始，逐步移除 $n_{elim}$ 个神经元。
    *   **结构保留**：缩减时保留原始权重矩阵的左上角子矩阵（Top-left submatrix），重新评估准确率。
*   **阶段 2：选择（Selection）**
    *   在所有探索过的网络中，选择准确率超过阈值 $\theta$ 且拓扑结构最小（参数最少）的网络作为候选。
*   **阶段 3：精炼（Refinement）**
    *   仅对选定的这个极小候选网络进行标准的反向传播训练（$E$ 个 Epoch）。

### 3. 实验设计
*   **数据集**：涵盖 7 个分类基准，包括二分类（Breast Cancer, Sonar, Ionosphere, Adult Income）和多分类（Iris, Wine, Optical Digits）。数据维度从 4 到 104 不等。
*   **基准方法（Benchmarks）**：
    1.  **Full Training**：对初始完整拓扑进行完整训练。
    2.  **Magnitude Pruning（量级剪枝）**：训练全量网络 -> 移除 L2 范数最小的神经元 -> 微调。
    3.  **Random Pruning（随机剪枝）**：训练全量网络 -> 随机移除神经元 -> 微调。
*   **对比指标**：准确率（Accuracy）、F1 分数、AUC-ROC 以及墙钟时间（Wall-clock time）。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 型号，但提到实验是在 **8 线程** 环境下运行的。
*   **计算效率**：Random Cloud 的搜索过程（仅前向传播）占总耗时的 25%–44%。在 5 个主要数据集中，有 4 个的运行速度比“全量训练”快（耗时仅为全量训练的 **0.67–0.94 倍**），显著优于需要 1.5–1.8 倍时间的剪枝方法。

### 5. 实验数量与充分性
*   **实验规模**：每个配置均使用 **10 个不同的随机种子** 运行，并报告了均值和标准差。
*   **统计验证**：使用了 **Wilcoxon 符号秩检验**（Wilcoxon signed-rank tests）来验证 Random Cloud 与基准方法之间的差异是否具有统计学意义（$p < 0.05$）。
*   **充分性评价**：实验覆盖了不同规模和维度的表格数据集，并进行了超参数鲁棒性分析（如云大小 $N$、阈值 $\theta$ 的影响），实验设计较为客观、严谨。

### 6. 主要结论与发现
*   **性能优异**：在 7 个数据集中的 6 个上，Random Cloud 达到或超过了量级剪枝的效果。
*   **大幅压缩**：在 Sonar 数据集上，该方法在减少 **87.2%** 参数的情况下，准确率反而比量级剪枝高出 **4.9 个百分点**（具有统计学显著性）。
*   **成本优势**：由于跳过了全量大网络的训练阶段，该方法比传统的“训练后剪枝”方法快得多。
*   **原理验证**：证明了随机初始化网络在 Sigmoid 激活函数下产生的概率化输出足以作为拓扑筛选的信号。

### 7. 优点
*   **高效节能**：实现了真正的“训练前剪枝”，极大地节省了搜索阶段的算力。
*   **简单易行**：算法逻辑清晰，不依赖复杂的强化学习或梯度估计，且对超参数（如阈值 $\theta$）不敏感。
*   **发现能力**：能够直接发现极小拓扑，而不仅仅是在预定义的搜索空间中选择。

### 8. 不足与局限
*   **高维扩展性受限**：在输入维度极高（如 MNIST 的 784 维）时，未经训练的评估信号会变弱，导致效果不如量级剪枝。
*   **应用范围**：目前主要在表格数据（Tabular Data）和前馈神经网络上验证，尚未扩展到卷积神经网络（CNN）或 Transformer 等复杂架构。
*   **理论深度**：虽然实验结果扎实，但对于“为什么随机权重能有效指示拓扑潜力”的理论分析仍有待加强。

（完）
