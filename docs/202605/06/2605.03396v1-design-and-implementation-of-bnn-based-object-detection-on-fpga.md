---
title: Design and Implementation of BNN-Based Object Detection on FPGA
title_zh: 基于BNN的FPGA目标检测设计与实现
authors: "Xuyu Zhao, Yunpeng Wu, Mengyuan Zhu, Haoyu Huang, Xiaoyu Xu, Yanjing Li, Gaolong Zhang, Baochang Zhang"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03396v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在FPGA上使用RTL和量化实现基于BNN的目标检测
tldr: "本研究在低成本FPGA上实现了一种基于二值神经网络（BNN）的类YOLOv3-tiny目标检测器。该设计采用1位权重和8位激活值，通过Verilog RTL实现了包括卷积、量化、池化及检测头在内的完整硬件架构。通过将通道补偿参数融合进处理单元，优化了计算效率。在VOC数据集上达到39.6%的mAP50，且硬件仿真结果与ONNX模型高度一致，为低功耗嵌入式目标检测提供了高效方案。"
selection_source: fresh_fetch
motivation: 旨在低成本FPGA平台上实现高效、低功耗的目标检测，解决传统深度学习模型计算资源消耗大的问题。
method: 采用类YOLOv3-tiny架构，结合1位权重和8位激活的二值化策略，并利用Verilog RTL实现参数融合优化的硬件加速器。
result: "在VOC数据集上实现39.6%的mAP50，参数量仅为0.74M，且硬件输出与软件模型具有极高的相关性（0.999964）。"
conclusion: 该设计证明了基于BNN的目标检测器在低成本FPGA上实现高性能与高精度平衡的可行性。
---

## 摘要
本文在低成本FPGA上实现了一种基于二值神经网络（BNN）的类YOLOv3-tiny目标检测器。该网络以320*320*3的RGB图像作为输入。其主要卷积层采用1位权重和8位激活，而Conv1和最终检测头则采用定点标准卷积。从训练好的ONNX模型中提取权重、偏置和量化参数，并将其转换为定点数，打包成COE文件后存储在Vivado BRAM ROM中。硬件部分完全使用Verilog RTL编写，涵盖了填充（padding）、行缓冲（line buffering）、二值卷积、量化后处理、最大池化以及检测头计算。针对Mul_prev按输入通道索引且Div_current按输出通道索引的层，将Mul_prev融合进BNN处理单元（PE）中，从而在累加过程中应用通道级补偿。在VOC数据集上，该模型以0.098 GFLOPs的计算量和0.74 M的参数量实现了39.6%的mAP50。RTL仿真结果表明，最终的原始检测输出与对应的ONNX节点相比，相关系数达到0.999964，平均绝对误差为0.020027。

## Abstract
This paper implements a Binary Neural Network (BNN) based YOLOv3-tiny-like object detector on a low-cost FPGA. The network takes 320*320*3 RGB images as input. Its main convolution layers use 1-bit weights and 8-bit activations, while Conv1 and the final detection head use fixed-point standard convolutions. From the trained ONNX model, weights, biases, and quantization parameters are extracted, converted to fixed point, packed into COE files, and stored in Vivado BRAM ROMs. The hardware is written fully in Verilog RTL and includes padding, line buffering, binary convolution, quantization post-processing, max pooling, and detection-head computation. For layers where Mul_prev is indexed by input channel and Div_current by output channel, Mul_prev is fused in-to the BNN PE so that channel-wise compensation is applied during accumulation. On VOC, the model obtains 39.6% mAP50 with 0.098 GFLOPs and 0.74 M parameters. RTL simulation shows that the final raw detection output reaches a correlation coefficient of 0.999964 and a mean absolute error of 0.020027 against the corresponding ONNX node.

---

## 论文详细总结（自动生成）

### 论文总结：基于 BNN 的 FPGA 目标检测设计与实现

#### 1. 核心问题与整体含义（研究动机和背景）
本文旨在解决深度学习目标检测模型在**低成本、资源受限的嵌入式 FPGA 平台**（如 PYNQ-Z2）上部署难的问题。由于目标检测需要处理高分辨率特征图，对片上存储、计算能力和带宽要求极高。传统的全精度模型难以直接运行，而全二值化网络（W1A1）在检测任务中表现不稳定。因此，作者提出了一种 **W1A8（1位权重，8位激活）** 的量化方案，在极低存储成本和高精度之间取得平衡，并实现了从训练好的 ONNX 模型到硬件 RTL 的完整部署流程。

#### 2. 方法论
*   **核心思想**：采用类 YOLOv3-tiny 的轻量化结构，通过 W1A8 量化大幅压缩权重，并利用 Verilog RTL 手写硬件逻辑以实现精细的位宽控制和流水线优化。
*   **关键技术细节**：
    *   **混合量化策略**：首层（Conv1）和末层（检测头 Conv11）采用定点标准卷积以保精度；中间层（Conv2-Conv10）采用 W1A8 二值卷积。
    *   **符号控制累加**：在 W1A8 卷积中，将 1 位权重（-1, +1）转化为对 8 位激活值的加减法运算，完全替代了乘法器，降低了 DSP 消耗。
    *   **通道级缩放融合**：为了对齐 ONNX 模型的量化参数，将前一层的输入通道缩放因子（Mul_prev）融合进当前层的处理单元（PE）中，在累加过程中进行补偿，提高了数值一致性。
    *   **硬件架构**：设计了包括 Padding 适配器、行缓冲区（Line Buffer）、二值卷积 PE、后处理模块（缩放、偏置、截断）和最大池化模块的流式处理架构。
*   **算法流程**：从 ONNX 提取参数 -> 定点化转换 -> 生成 COE 文件 -> 存储于 BRAM ROM -> RTL 逻辑根据 ROM 延迟调度计算。

#### 3. 实验设计
*   **数据集**：使用 **PASCAL VOC 2007** 测试集进行精度评估。
*   **Benchmark（基准）**：对比了多种轻量化检测模型，包括 MobileNet-SSD、YOLOv5s/n、YOLOv3-tiny、YOLOv4-tiny 和 YOLOv2-tiny。
*   **验证方法**：
    *   **精度验证**：计算 mAP50。
    *   **数值对齐验证**：通过 Vivado 仿真将 RTL 输出与 ONNX Runtime 的中间节点逐层对比，计算相关系数和平均绝对误差（MAE）。

#### 4. 资源与算力
*   **目标硬件**：Xilinx Zynq XC7Z020 (PYNQ-Z2 开发板)。
*   **算力消耗**：模型计算量仅为 **0.098 GFLOPs**。
*   **训练资源**：文中**未明确说明**训练该模型所使用的具体 GPU 型号、数量及训练时长，仅提到使用了带有直通估计器（STE）的训练方法。
*   **推理性能**：在 30 MHz 时钟频率下，推理延迟为 795.58 ms，帧率为 **1.26 FPS**。

#### 5. 实验数量与充分性
*   **实验规模**：作者进行了模型精度对比实验、硬件资源占用统计以及逐层的数值一致性仿真实验。
*   **充分性评价**：实验设计较为**客观且针对性强**。通过与 ONNX 节点的逐层对比（相关系数达 0.999964），有力证明了 RTL 实现的正确性。但在数据集覆盖上稍显单一（仅 VOC），且 1.26 FPS 的帧率在实时性要求高的场景下略显不足。

#### 6. 主要结论与发现
*   **精度与成本平衡**：该模型以 0.74M 的极小参数量实现了 39.6% 的 mAP50，虽然低于现代大型模型，但比早期的 YOLOv2-tiny 高出 4.2%，证明了 W1A8 在极低资源下的有效性。
*   **硬件可行性**：成功在资源极其有限的 XC7Z020 上部署，BRAM 占用 73%，DSP 占用 91%，证明了手写 RTL 在资源优化方面的优势。
*   **数值一致性**：通过通道补偿融合技术，硬件输出与软件模型几乎完全一致，消除了量化带来的额外漂移。

#### 7. 优点
*   **极低资源占用**：通过 W1A8 量化和 RTL 优化，将模型压缩至不到 1MB，非常适合廉价 FPGA。
*   **高精度对齐**：提出的通道级缩放融合方法解决了量化模型软硬件不一致的痛点。
*   **全 RTL 实现**：相比 HLS，手写 Verilog 提供了更优的位宽控制和时序调度能力。

#### 8. 不足与局限
*   **吞吐量较低**：1.26 FPS 的速度限制了其在高速实时检测场景的应用，主要瓶颈在于 30 MHz 的低主频和 PE 并行度不足。
*   **小目标检测能力弱**：由于仅使用单一的 10x10 检测头，缺乏多尺度特征融合，导致对小目标的召回率较低。
*   **硬件利用率接近极限**：DSP 占用率已达 91%，进一步增加并行度或引入更复杂的结构会面临资源枯竭的风险。

（完）
