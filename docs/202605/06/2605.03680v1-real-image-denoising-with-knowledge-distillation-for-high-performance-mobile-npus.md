---
title: Real Image Denoising with Knowledge Distillation for High-Performance Mobile NPUs
title_zh: 面向高性能移动端 NPU 的知识蒸馏真实图像去噪
authors: "Faraz Kayani, Sarmad Kayani, Asad Ahmed, Radu Timofte, Dmitry Ignatov"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03680v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对移动NPU的硬件算法协同设计
tldr: 本研究针对移动端NPU在图像去噪中面临的算子不兼容和内存开销问题，提出了一种硬件感知的高性能协同设计方案。通过知识蒸馏技术，将高容量教师模型的知识迁移到仅含1.96M参数的轻量级学生模型LiteDenoiseNet中。该模型采用NPU原生算子，在保持高保真度的同时，在联发科和高通旗舰芯片上实现了极速推理，显著缩小了轻量化模型与大型模型间的性能差距，为移动端实时图像修复提供了新思路。
selection_source: fresh_fetch
motivation: 深度学习图像修复模型在移动端NPU部署时常受限于算子不兼容和内存访问开销，导致性能与速度难以兼得。
method: 提出一种硬件感知协同设计方法，利用高比例知识蒸馏将大模型能力迁移至仅使用NPU原生算子的轻量级网络。
result: LiteDenoiseNet在主流旗舰芯片上实现了毫秒级推理，并以极小的参数量达到了与教师模型相近的去噪精度。
conclusion: 硬件感知的蒸馏策略能有效统一高保真去噪与跨平台NPU的高效部署，证明了算子兼容性对提升推理速度的关键作用。
---

## 摘要
虽然基于深度学习的图像修复已达到前所未有的保真度，但在移动端神经网络处理器（NPU）上的部署仍受限于算子不兼容和内存访问开销。我们提出了一种面向移动端 NPU 的 NPU 感知硬件-算法协同设计方法，用于真实场景图像去噪。我们的方法采用高容量教师网络来监督轻量级学生网络，该学生网络专为利用现代移动端 SoC 的平铺内存（tiled-memory）架构而设计。通过优先使用 NPU 原生算子——标准 3x3 卷积、ReLU 激活和最近邻上采样——并采用渐进式上下文扩展策略（最高达 1024x1024 裁剪），该模型在 Mobile AI 2026 挑战赛的全分辨率（2432x3200）验证基准上达到了 37.66 dB PSNR / 0.9278 SSIM，在预留测试基准上达到了 37.58 dB PSNR / 0.9098 SSIM。根据官方挑战赛规则，推理运行时间是在标准全高清（1088x1920）协议下测量的，在联发科天玑 9500 上运行时间为 34.0 ms，在骁龙 8 Elite NPU 上为 46.1 ms。我们进一步揭示了“推理反转”（Inference Inversion）效应，即严格遵守 NPU 兼容操作可使专用 NPU 的执行速度比集成移动端 GPU 快达 3.88 倍。拥有 1.96M 参数的学生网络通过高 alpha 知识蒸馏（alpha = 0.9）恢复了教师网络 99.8% 的修复质量，在实现 21.2 倍参数缩减的同时，将 PSNR 差距从 1.63 dB 缩小到仅 0.05 dB。这些结果证明硬件感知蒸馏是将高保真去噪与跨多种移动端 NPU 架构的实际部署相结合的有效策略。所提出的轻量级学生模型（LiteDenoiseNet）及其训练统计数据已在 NN 数据集中提供，网址为 https://github.com/ABrain-One/NN-Dataset。

## Abstract
While deep-learning-based image restoration has achieved unprecedented fidelity, deployment on mobile Neural Processing Units (NPUs) remains bottlenecked by operator incompatibility and memory-access overhead. We propose an NPU-aware hardware-algorithm co-design approach for real-world image denoising on mobile NPUs. Our approach employs a high-capacity teacher to supervise a lightweight student network specifically designed to leverage the tiled-memory architectures of modern mobile SoCs. By prioritizing NPU-native primitives -- standard 3x3 convolutions, ReLU activations, and nearest-neighbor upsampling -- and employing a progressive context expansion strategy (up to 1024x1024 crops), the model achieves 37.66 dB PSNR / 0.9278 SSIM on the validation benchmark and 37.58 dB PSNR / 0.9098 SSIM on the held-out test benchmark at full resolution (2432x3200) in the Mobile AI 2026 challenge. Following the official challenge rules, the inference runtime is measured under a standardized Full HD (1088x1920) protocol, where it runs in 34.0 ms on the MediaTek Dimensity 9500 and 46.1 ms on the Qualcomm Snapdragon 8 Elite NPU. We further reveal an "Inference Inversion" effect, where strict adherence to NPU-compatible operations enables dedicated NPU execution up to 3.88x faster than the integrated mobile GPU. The 1.96M-parameter student recovers 99.8% of the teacher's restoration quality via high-alpha knowledge distillation (alpha = 0.9), achieving a 21.2x parameter reduction while closing the PSNR gap from 1.63 dB to only 0.05 dB. These results establish hardware-aware distillation as an effective strategy for unifying high-fidelity denoising with practical deployment across diverse mobile NPU architectures. The proposed lightweight student model (LiteDenoiseNet) and its training statistics are provided in the NN Dataset, available at https://github.com/ABrain-One/NN-Dataset.

---

## 论文详细总结（自动生成）

这是一份关于论文《Real Image Denoising with Knowledge Distillation for High-Performance Mobile NPUs》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度学习图像去噪模型虽然在桌面端表现优异，但在移动端神经网络处理器（NPU）上部署时，常因**算子不兼容**（如自注意力、变形卷积）和**内存访问开销大**（如转置卷积导致的 fallback）而导致性能大幅下降或无法运行。
*   **研究背景**：在 Mobile AI 2026 图像去噪挑战赛背景下，研究如何在严格的移动端功耗、内存和算子限制下，实现高保真度的实时真实图像去噪。

### 2. 方法论
论文采用了**硬件-算法协同设计**的思想，核心由“高容量教师网络”和“硬件感知学生网络”组成：
*   **核心思想**：利用一个庞大的教师模型（41.6M 参数）提取高质量去噪流形，通过**高 $\alpha$ 知识蒸馏**（$\alpha=0.9$）指导一个极小的学生模型（1.96M 参数），使其在仅使用 NPU 原生算子的情况下逼近教师性能。
*   **关键技术细节**：
    *   **LiteDenoiseNet（学生网络）**：采用 U-Net 拓扑，基础通道宽度为 16（适配 SIMD 向量化执行）。
    *   **NPU 原生算子**：仅使用标准 3x3 卷积、ReLU 激活和最近邻上采样（替代转置卷积），避免硬件回退到 CPU 执行。
    *   **渐进式上下文扩展（PCE）**：训练初期使用 256×256 裁剪块，后期逐步增加至 512×512 和 1024×1024，以增强轻量级模型捕捉全局结构的能力。
*   **损失函数**：
    $L_{total} = \lambda_{gt} L_{gt} + \lambda_{distill} L_{distill} + \lambda_{L1} L_{L1}$
    其中 $\lambda_{distill}$ 权重最高（900），强调学生对教师输出的模仿。

### 3. 实验设计
*   **数据集**：Mobile AI 2026 图像去噪挑战赛数据集（650 对训练图，50 对验证图）。
*   **Benchmark**：
    *   **质量指标**：全分辨率（2432×3200，约 8MP）下的 PSNR 和 SSIM。
    *   **速度指标**：全高清（1088×1920，FHD）下的 FP16 推理延迟。
*   **对比对象**：包括注意力增强模型、高容量教师模型、不同宽度的基础模型，以及挑战赛中其他团队的方法（如 TeamPAK, TLG, Z6 等）。
*   **测试平台**：联发科天玑 9500 (Dimensity 9500) 和高通骁龙 8 Elite (Snapdragon 8 Elite)。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：完整的训练和微调流程通常需要 **3-6 小时**（取决于 Batch Size 和裁剪块大小）。
*   **部署工具**：使用 AI Edge Torch 将 PyTorch 模型转换为 TensorFlow Lite (TFLite) 格式。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了 **5 个阶段的消融实验**，从注意力机制的失败尝试到高容量教师的建立，再到不同宽度学生模型的对比及最终的蒸馏优化。
*   **充分性**：实验涵盖了主流的两大移动端旗舰芯片，并提供了 95% 置信区间分析。通过逐像素 PSNR 差异分布图证明了蒸馏的稳定性。实验设计客观，严格遵循了挑战赛的官方评估协议。

### 6. 主要结论与发现
*   **蒸馏效果显著**：1.96M 参数的学生模型恢复了教师模型 **99.8%** 的修复质量，将 PSNR 差距从 1.63 dB 缩小到仅 0.05 dB。
*   **推理反转（Inference Inversion）效应**：由于严格遵守 NPU 原生算子，模型在专用 NPU 上的执行速度比集成 GPU 快 **2.86x 至 3.88x**。这颠覆了“GPU 是图像修复首选”的传统认知。
*   **性能表现**：在天玑 9500 NPU 上仅需 34.0 ms，在骁龙 8 Elite 上需 46.1 ms，达到了实时处理的要求。

### 7. 优点
*   **硬件感知度高**：不盲目追求复杂算子，而是针对 NPU 的平铺内存架构和算子支持进行定制。
*   **部署友好**：通过 PCE 策略解决了轻量级模型感受野不足的问题，实现了“小模型、大效果”。
*   **实用性强**：提供了完整的从训练到 Android 端 TFLite 部署的路径，且模型参数量极小（7.52 MB）。

### 8. 不足与局限
*   **算子保守性**：为了确保跨平台稳定性，完全放弃了 Transformer 等先进算子，可能限制了模型在未来更强 NPU 上的潜力。
*   **数据依赖**：推理延迟数据部分引用自挑战赛官方，而非完全独立的第三方多环境测试。
*   **分辨率差异**：运行时间是在 FHD 下测量的，虽然能反映架构效率，但与实际 8MP 全分辨率部署时的端到端延迟（含 I/O 开销）可能存在差异。

（完）
