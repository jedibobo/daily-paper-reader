---
title: "Nano-U: Efficient Terrain Segmentation for Tiny Robot Navigation"
title_zh: Nano-U：面向微型机器人导航的高效地形分割
authors: "Federico Pizzolato, Francesco Pasti, Nicola Bellotto"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10210v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 适用于微控制器和微型机器人的高效神经网络
tldr: 针对微型机器人受限于内存和算力的挑战，本文提出Nano-U框架，旨在低成本单片机上实现高效地形分割。该方案核心是一个仅有数千参数的紧凑网络，通过量化感知蒸馏（QAD）技术提升模型精度，并利用基于Rust的MicroFlow编译器在ESP32-S3上实现无解释器开销的推理，为小型机器人提供了低功耗、低延迟的感知能力。
selection_source: fresh_fetch
motivation: 现有的地形分割模型因内存和计算需求过高，难以部署在资源极度受限的微型机器人单片机平台上。
method: 设计了极简的Nano-U分割网络，结合量化感知蒸馏（QAD）进行训练，并使用MicroFlow编译器优化推理过程。
result: 模型在Botanic Garden和TinyAgri数据集上取得优异表现，并在ESP32-S3上实现了极小的内存占用和高效执行。
conclusion: 该研究为低成本机器人平台提供了一种可行且节能的视觉感知解决方案，推动了微型机器人在复杂环境中的自主导航。
---

## 摘要
地形分割是在非结构化户外环境中运行的自主移动机器人的基本能力。然而，最先进的模型与微控制器典型的内存和计算限制不兼容，限制了在小型机器人平台上的大规模部署。为了填补这一空白，我们开发了一个在低成本微控制器上进行鲁棒二值地形分割的完整框架。我们方法的核心是设计了 Nano-U，这是一个仅有数千个参数的高度紧凑的二值分割网络。为了弥补网络极小的容量，我们通过量化感知蒸馏（QAD）来训练 Nano-U，该方法结合了知识蒸馏和量化感知训练。这使得最终的量化模型在 Botanic Garden 数据集上取得了优异的结果，并在具有更具挑战性场景的自定义农田数据集 TinyAgri 上表现出色。我们通过扩展 MicroFlow（一种用 Rust 实现的基于编译器的 TinyML 推理引擎），将量化后的 Nano-U 部署在商用微控制器上。通过消除解释器开销和动态内存分配，量化模型在 ESP32-S3 上以极小的内存占用和低延迟运行。这种基于编译器的执行方式为低成本机器人平台上的感知提供了一种可行且节能的解决方案。

## Abstract
Terrain segmentation is a fundamental capability for autonomous mobile robots operating in unstructured outdoor environments. However, state-of-the-art models are incompatible with the memory and compute constraints typical of microcontrollers, limiting scalable deployment in small robotics platforms. To address this gap, we develop a complete framework for robust binary terrain segmentation on a low-cost microcontroller. At the core of our approach we design Nano-U, a highly compact binary segmentation network with a few thousand parameters. To compensate for the network's minimal capacity, we train Nano-U via Quantization-Aware Distillation (QAD), combining knowledge distillation and quantization-aware training. This allows the final quantized model to achieve excellent results on the Botanic Garden dataset and to perform very well on TinyAgri, a custom agricultural field dataset with more challenging scenes. We deploy the quantized Nano-U on a commodity microcontroller by extending MicroFlow, a compiler-based inference engine for TinyML implemented in Rust. By eliminating interpreter overhead and dynamic memory allocation, the quantized model executes on an ESP32-S3 with a minimal memory footprint and low latency. This compiler-based execution demonstrates a viable and energy-efficient solution for perception on low-cost robotic platforms.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Nano-U** 的轻量级框架，旨在解决微型机器人在资源极度受限的硬件（如单片机）上进行实时地形分割的难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的先进地形分割模型（如 U-Net、DeepLab）参数量巨大（千万级），内存和计算需求远超微控制器（MCU）的承载能力（通常仅有几百 KB RAM）。
*   **研究动机**：微型机器人需要在非结构化户外环境（如农田、森林）中识别可通行区域，但受限于成本、体积和功耗，无法携带 GPU 或高性能处理器。研究者旨在开发一种能在 10 美元级别的通用单片机（如 ESP32-S3）上运行的鲁棒二值分割方案。

### 2. 方法论
该论文提出了一个从网络设计、训练优化到硬件部署的完整流水线：
*   **Nano-U 网络设计**：
    *   **极简架构**：基于 U-Net 启发，采用 7 层编码器-解码器结构。
    *   **内存优化**：**去除了标准 U-Net 中的跳跃连接（Skip Connections）**，以避免在 RAM 中同时保留多层特征图，从而降低峰值内存占用。
    *   **计算优化**：全量使用**深度可分离卷积（Depthwise Separable Convolutions）**，大幅减少参数量和乘加运算（MACs）。
    *   **输入分辨率**：固定为 60×80，以适配硬件的张量缓冲区限制。
*   **量化感知蒸馏（QAD）训练**：
    *   核心思想是将**知识蒸馏（KD）**与**量化感知训练（QAT）**结合。
    *   使用一个大型教师模型（BU-Net，12.85M 参数）引导极小的学生模型（Nano-U，约 3.3k 参数）学习。
    *   **损失函数**：结合了硬标签的交叉熵损失（$L_{CE}$）和基于温度缩放（Temperature Scaling）的软标签蒸馏损失（$L_{KD}$）。
    *   在训练过程中模拟 INT8 量化，确保模型在转换为 8 位整数推理时精度损失最小。
*   **MicroFlow 部署引擎**：
    *   使用基于 **Rust 语言**的 MicroFlow 编译器。
    *   **关键技术**：在编译时解析网络拓扑，生成静态分配内存的推理代码，彻底消除了运行时解释器开销和动态内存分配。

### 3. 实验设计
*   **数据集**：
    1.  **Botanic Garden**：一个包含 1.1k+ 图像的户外机器人导航基准数据集。
    2.  **TinyAgri（自定义）**：研究者使用小型漫游车在番茄地和玉米地采集的挑战性数据集，包含遮挡、光照变化和运动模糊。
*   **Benchmark 与对比**：
    *   对比了教师模型（BU-Net）、Nano-U 的浮点版本（FP32）以及量化部署版本（INT8）。
    *   评估指标包括 mIoU（平均交并比）、F1 分数、精确率（Precision）和召回率（Recall）。
*   **硬件平台**：ESP32-S3-CAM（双核 Xtensa LX7，240MHz，512KB SRAM）。

### 4. 资源与算力
*   **训练算力**：使用了单张 **NVIDIA GeForce RTX 5060 Laptop GPU**。
*   **训练细节**：模型训练了 200 个 epoch，使用 AdamW 优化器。论文未明确说明具体的训练总时长，但考虑到模型极小，预计训练时间较短。
*   **推理算力**：在 ESP32-S3 上运行，推理完全依赖通用算术逻辑单元（ALU），未使用专门的 AI 加速器。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同性质的数据集进行了测试，涵盖了从标准基准到实际挑战场景的迁移。
*   **硬件分析**：进行了详尽的硬件性能剖析，包括延迟（Latency）、峰值 RAM 占用、功耗（mW）和能量消耗（mJ）。
*   **充分性评价**：实验设计较为客观，通过对比教师与学生模型的性能差距，清晰展示了极度压缩带来的精度损失。但缺乏与其他轻量级模型（如 MobileNet-UNet 极小变体）在相同内存限制下的横向对比。

### 6. 主要结论与发现
*   **精度表现**：在 Botanic Garden 数据集上，Nano-U（INT8）达到了 **87.0% 的 mIoU**，与 FP32 版本几乎持平，证明了 QAD 策略的有效性。
*   **硬件效率**：在 ESP32-S3 上，单次推理延迟为 **845ms**（约 1.2 FPS），峰值 RAM 占用为 **257 KB**，推理功耗增量仅为 333 mW。
*   **鲁棒性**：尽管参数量减少了约 4000 倍，模型仍能可靠地识别出主要的可通行路径，表现出较强的保守性（高精确率），有利于机器人安全导航。

### 7. 优点
*   **极度轻量化**：将分割模型压缩至仅 **3.3k 参数（约 33 KB）**，这是目前已知最紧凑的地形分割模型之一。
*   **端到端优化**：不仅优化了算法，还通过 Rust 编译器解决了嵌入式部署中的解释器开销问题。
*   **开源贡献**：公开发布了 Nano-U 模型和针对微型机器人视角优化的 TinyAgri 数据集。

### 8. 不足与局限
*   **实时性瓶颈**：1.2 FPS 的帧率对于快速移动的机器人来说仍然偏慢，难以应对高速避障需求。
*   **复杂场景精度下降**：在 TinyAgri 数据集上，量化后的 mIoU 掉至 70%，说明在极小参数量下，模型难以处理复杂的植被重叠和精细边缘。
*   **分辨率限制**：60×80 的分辨率较低，可能会导致远距离路径识别的精度不足，影响路径规划的平滑度。

（完）
