---
title: "MicroViTv2: Beyond the FLOPS for Edge Energy-Friendly Vision Transformers"
title_zh: MicroViTv2：超越 FLOPS，面向边缘端能效友好的视觉 Transformer
authors: "Novendra Setyawan, Chi-Chia Sun, Mao-Hsiu Hsu, Wen-Kai Kuo, Jun-Wei Hsieh"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10148v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对真实设备效率优化的轻量级视觉Transformer
tldr: 针对视觉Transformer在边缘设备部署的高能耗问题，本文提出MicroViTv2。该模型通过重参数化补丁嵌入（RepEmbed）和深度卷积混合器（RepDW）提升推理速度，并引入单深度转置注意力（SDTA）捕获长程依赖。实验表明，MicroViTv2在Jetson AGX Orin上表现出优异的能效比，在ImageNet-1K和COCO任务上超越了MobileViTv2等主流轻量化模型，证明了硬件感知设计在降低实际能耗中的关键作用。
selection_source: fresh_fetch
motivation: 传统的视觉Transformer计算成本高，且FLOPs指标不能完全反映边缘设备上的实际推理速度和能效。
method: 采用重参数化设计（RepEmbed和RepDW）优化推理效率，并结合单深度转置注意力（SDTA）以低冗余方式捕捉全局信息。
result: "在ImageNet-1K上比前代提升0.5%精度，且在Jetson AGX Orin上的推理速度和能效优于MobileViTv2和EfficientViT。"
conclusion: 硬件感知设计和结构重参数化是实现高精度与低能耗平衡的关键，模型评估应超越单一的FLOPs指标。
---

## 摘要
视觉 Transformer (ViT) 在各项视觉任务中取得了卓越的准确率，但在边缘端部署时计算开销依然巨大。本文提出了 MicroViTv2，这是一种针对实际设备效率进行优化的轻量级视觉 Transformer。该模型基于原始 MicroViT 构建，采用了重参数化设计，具体包括用于加速推理的重参数化 Patch 嵌入 (RepEmbed) 和重参数化深度可分离卷积混叠器 (RepDW)，并引入了单深度可分离转置注意力 (SDTA) 以在最小冗余下捕获远程依赖关系。尽管 FLOPs 略高，但 MicroViTv2 与其前身相比准确率提升了高达 0.5%，并超越了 MobileViTv2、EdgeNeXt 和 EfficientViT，同时在 Jetson AGX Orin 上保持了快速推理和高能效。在 ImageNet-1K 和 COCO 上的实验表明，硬件感知设计和结构重参数化是实现高准确率和低能耗的关键，验证了在 FLOPs 之外评估效率的必要性。代码可在 https://github.com/novendrastywn/MicroViT 获取。

## Abstract
The Vision Transformer (ViT) achieves remarkable accuracy across visual tasks but remains computationally expensive for edge deployment. This paper presents MicroViTv2, a lightweight Vision Transformer optimized for real-device efficiency. Built upon the original MicroViT, the proposed model is designed based on reparameterized design, specifically Reparameterized Patch Embedding (RepEmbed) and Reparameterized Depth-Wise convolution mixer (RepDW) for faster inference, and introduces the Single Depth-Wise Transposed Attention (SDTA) to capture long-range dependencies with minimal redundancy. Despite slightly higher FLOPs, MicroViTv2 improves accuracy up to 0.5% compared to its predecessor and surpassing MobileViTv2, EdgeNeXt, and EfficientViT while maintaining fast inference and high energy efficiency on Jetson AGX Orin. Experiments on ImageNet-1K and COCO demonstrate that hardware-aware design and structural re-parameterization are key to achieving high accuracy and low energy consumption, validating the need to evaluate efficiency beyond FLOPs. Code is available at https://github.com/novendrastywn/MicroViT.

---

## 论文详细总结（自动生成）

### MicroViTv2：超越 FLOPS，面向边缘端能效友好的视觉 Transformer 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
视觉 Transformer (ViT) 虽然在各项视觉任务中表现卓越，但其巨大的计算开销和参数量限制了在资源受限的边缘设备（如移动端、嵌入式系统）上的部署。
*   **研究动机**：传统的轻量化模型设计往往过度依赖 **FLOPs（浮点运算数）** 作为效率指标。然而，低 FLOPs 并不等同于低延迟或低能耗，因为内存访问、算子调度和硬件实现开销同样关键。
*   **核心目标**：开发一种名为 **MicroViTv2** 的轻量化 ViT，旨在通过硬件感知设计，在实际边缘设备上实现高准确率、高吞吐量和低能耗的平衡，打破“低 FLOPs 即高效”的迷思。

#### 2. 论文提出的方法论
MicroViTv2 采用了 3 阶段金字塔架构，核心技术包括：
*   **结构重参数化设计 (Structural Re-parameterization)**：
    *   **RepEmbed (重参数化补丁嵌入)**：在训练阶段使用 3×3 卷积、1×1 卷积和恒等映射的多分支结构以增强特征提取；在推理阶段，通过数学变换将多分支融合为单一卷积，减少内存访问开销。
    *   **RepDW (重参数化深度卷积混叠器)**：在模型的前两个阶段使用，利用深度可分离卷积的重参数化形式进行空间特征混合。
*   **单深度转置注意力 (SDTA, Single Depth-Wise Transposed Attention)**：
    *   **核心思想**：在模型的后期阶段（Stage 3）引入。它将注意力操作从空间维度转移到通道维度（转置注意力），使计算复杂度与空间分辨率呈线性关系。
    *   **技术细节**：结合了深度卷积（提取局部特征）和转置注意力（捕获长程依赖），并采用单头设计以消除多头注意力中的冗余。
*   **硬件感知优化**：通过减少残差连接带来的内存访问次数和优化算子融合，提升在 GPU/NPU 上的执行效率。

#### 3. 实验设计
*   **数据集**：
    *   **ImageNet-1K**：用于图像分类任务（128万训练集，5万验证集）。
    *   **MS COCO**：用于目标检测任务（配合 RetinaNet 框架）。
*   **Benchmark 与对比方法**：
    *   对比了多种 SOTA 轻量化模型，包括 **MobileViT (v1/v2)**、**EdgeNeXt**、**FastViT**、**EfficientViT**、**SHViT**、**MobileNet (v2/v3)** 和 **RepVGG**。
*   **评估指标**：除了 Top-1 准确率，重点评估了**吞吐量 (Throughput)**、**延迟 (Latency)**、**每图像能耗 (Energy per image)** 以及**准确率-能效权衡比 ($\eta$)**。

#### 4. 资源与算力
*   **训练环境**：使用了 **4 台 NVIDIA RTX 4090 GPU**。
*   **训练细节**：采用 AdamW 优化器，Batch Size 为 512，共训练 300 个 Epoch。初始学习率为 0.004，并使用了 DeiT 的数据增强策略。
*   **测试环境**：推理性能和能耗在 **NVIDIA Jetson AGX Orin** 边缘设备上测得（使用 ONNX Runtime，Batch Size 为 64 或 1）。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 S1 到 S3 三种不同规模的模型变体。
*   **消融实验**：针对 RepEmbed、RepDW、SDTA 等核心组件进行了详细的消融研究，并对比了不同注意力机制（如 MDTA）的影响。
*   **充分性评价**：实验设计较为充分，不仅在标准数据集上验证了精度，还深入探讨了硬件层面的性能表现。通过对比 FLOPs 增加但能耗降低的现象，有力地支持了其核心论点。实验过程遵循了学术界的标准流程（如 300 epoch 训练、标准增强等），具有较高的客观性。

#### 6. 主要结论与发现
*   **超越 FLOPs**：MicroViTv2 证明了 FLOPs 略微增加（约 7%）的情况下，通过优化内存访问和算子结构，实际吞吐量和能效反而能得到显著提升。
*   **性能领先**：MicroViTv2-S3 在 ImageNet-1K 上达到 77.4% 的准确率，吞吐量（1335.5 Img/s）优于 FastViT-T8，且能耗远低于 EdgeNeXt 和 MobileViT。
*   **通用性**：在 COCO 目标检测任务中，MicroViTv2 作为骨干网络表现出比前代更好的推理速度和一致的检测精度。

#### 7. 优点
*   **实战导向**：直接针对边缘设备的真实能耗和延迟进行优化，而非仅仅追求理论上的计算量减少。
*   **重参数化应用巧妙**：成功将重参数化技术从纯 CNN 扩展到 ViT 架构中，有效解决了训练强度与推理速度的矛盾。
*   **能效比极高**：提出的 $\eta$ 指标为边缘端模型评估提供了一个更全面的视角。

#### 8. 不足与局限
*   **硬件覆盖面**：实验主要集中在 NVIDIA Jetson 平台上，对于其他架构（如 ARM CPU、手机端 DSP 或其他厂商的 NPU）的适配性和表现尚未充分展示。
*   **训练成本**：重参数化设计虽然加快了推理，但在训练阶段由于存在多分支结构，会增加训练时的显存占用和计算时间。
*   **任务单一性**：目前主要验证了分类和检测，对于语义分割、姿态估计等更复杂的边缘端视觉任务尚未提及。

（完）
