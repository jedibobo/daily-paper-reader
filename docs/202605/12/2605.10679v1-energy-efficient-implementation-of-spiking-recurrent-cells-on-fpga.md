---
title: Energy-Efficient Implementation of Spiking Recurrent Cells on FPGA
title_zh: FPGA上脉冲循环单元的高能效实现
authors: "Pascal Harmeling, Florent De Geeter, Guillaume Drion"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10679v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 具有硬件友好数学简化的SNN FPGA加速器
tldr: "本研究针对脉冲神经网络（SNN）在FPGA上实现时，生物真实性与硬件成本难以兼顾的问题，提出了一种基于脉冲循环单元（SRC）的高能效加速器。通过数学简化、分段近似和定点化处理，消除了昂贵的非线性运算。实验在Artix-7上验证，MNIST准确率达96.31%，并在低能耗配置下表现优异，证明了SRC在保持复杂动态特性的同时具有极高的硬件友好性。"
selection_source: fresh_fetch
motivation: 旨在解决生物学真实的神经元模型在FPGA上实现成本过高，而简单模型又缺乏丰富神经元动态特性的矛盾。
method: 提出一种基于SRC神经元的FPGA加速器，通过分段近似和缩放技术取代复杂的非线性函数，并采用定点数运算和LUT存储权重。
result: "在Artix-7 FPGA上实现了96.31%的MNIST识别准确率，且在4位权重和缩短脉冲序列的情况下，能耗仅为0.45mJ每数字。"
conclusion: SRC模型在FPGA上能够以较低的硬件开销和能耗提供比标准LIF模型更丰富的动态特性和竞争力的性能。
---

## 摘要
当脉冲活动稀疏且神经元模型硬件友好时，脉冲神经网络（SNNs）相比传统的人工神经网络（ANNs）能够降低功耗。然而，生物学忠实度高的模型在FPGA上的实现成本通常过高，而极简模型（如IR/LIF）则牺牲了部分神经元动力学特性。在本研究中，我们提出了一种使用脉冲循环单元（SRC）神经元的SNN FPGA加速器，在生物合理性与硬件成本之间取得了平衡。我们提出了一系列数学简化方法，通过缩放和分段定义近似，消除了昂贵的一元算子（tanh、exp）并避免了浮点运算。整个网络采用VHDL实现，并使用源自MNIST数据集的脉冲轨迹进行验证。离线计算的权重矩阵直接存储在LUT寄存器中，无需任何调整，这证明了SRC单元的鲁棒性。实验在主频为100 MHz的Artix-7 XC7A200T上进行。基准实现在220图像脉冲轨迹下达到了96.31%的准确率，处理每个数字的时间为1.7424 ms。随后，我们通过缩短脉冲轨迹长度并将突触权重优化至4位，研究了准确率与能量之间的权衡，在每个数字0.55 mJ（55图像，5位权重）时达到93.32%的准确率，在0.45 mJ（44图像，4位权重）时达到92.89%的准确率。这些结果表明，基于SRC的SNNs能够在降低能耗的同时提供具有竞争力的性能，并保留比标准LIF/IR模型更丰富的神经元动力学。

## Abstract
Spiking Neural Networks (SNNs) can reduce energy consumption compared to conventional Artificial Neural Networks (ANNs) when spiking activity is sparse and the neuron model is hardware-friendly. However, biologically faithful models are often too costly to implement on FPGAs, whereas very simple models (e.g., IR/LIF) sacrifice part of the neuronal dynamics. In this work, we present an FPGA accelerator for an SNN using Spiking Recurrent Cell (SRC) neurons, providing a trade-off between biological plausibility and hardware cost. We propose a set of mathematical simplifications that remove costly unary operators (\textit{tanh}, \textit{exp}) and avoid floating-point arithmetic through scaling and piecewise-defined approximations. The complete network is implemented in VHDL and validated using spiking traces derived from the MNIST dataset. The weight matrices computed off-line are stored directly in LUT-registers without any adaptation. This demonstrates the robustness of SRC cells. Experiments were conducted on an Artix-7 XC7A200T clocked at 100 MHz. The reference implementation achieves 96.31\% accuracy with a 220-image spiking trace and a processing time of 1.7424 ms per digit. We then investigate accuracy/energy trade-offs by reducing the spiking trace length and quantizing synaptic weights down to 4 bits, achieving 93.32\% accuracy at 0.55 mJ per digit (55 images, 5-bit weights) and 92.89\% at 0.45 mJ (44 images, 4-bit weights). These results show that SRC-based SNNs can deliver competitive performance with reduced energy consumption, while preserving richer neuronal dynamics than standard LIF/IR models.

---

## 论文详细总结（自动生成）

这篇论文《Energy-Efficient Implementation of Spiking Recurrent Cells on FPGA》由列日大学的研究团队撰写，重点探讨了如何在FPGA硬件上高效实现具有生物合理性的脉冲循环单元（SRC）。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：传统人工神经网络（ANN）能耗巨大，而脉冲神经网络（SNN）通过模拟生物大脑的稀疏脉冲活动，被视为低功耗替代方案。
*   **核心矛盾**：现有的SNN硬件实现面临两难选择：要么使用过于简单的神经元模型（如LIF），虽然硬件友好但牺牲了复杂的神经动力学；要么使用生物学忠实度高的模型（如Hodgkin-Huxley），但其复杂的数学运算（指数、浮点运算）在FPGA上实现成本极高。
*   **研究目的**：提出一种基于脉冲循环单元（SRC）的FPGA加速器，在保持丰富神经元动力学的同时，通过数学简化实现极高的能效比。

### 2. 方法论
核心思想是将原始SRC方程进行“硬件友好型”改造，消除昂贵的非线性算子。
*   **数学简化技术**：
    *   **定点化处理**：将所有变量和参数缩放1000倍并转换为整数（INT16），完全避免了浮点运算。
    *   **分段线性近似（Stanh）**：用斜率为0.75的分段函数取代复杂的 `tanh` 激活函数，仅需移位和加法即可实现。
    *   **阈值规则取代指数函数**：将控制放松时间的 $z_s[t]$ 方程（原含 `exp`）简化为一个简单的阈值开关逻辑。
    *   **移位运算优化**：调整参数（如将 $r_s$ 从-7改为-8），使乘法运算能够通过硬件上的位移（Shift）操作完成。
*   **硬件架构**：
    *   采用VHDL实现的四层结构：输入接口、SRC神经元层（100个神经元）、IR集成神经元层（10个神经元）和输出比较器。
    *   **并行化设计**：利用VHDL的 `GENERATE` 语句实现神经元并行处理。
    *   **存储优化**：权重矩阵直接存储在LUT寄存器中而非BRAM，以实现单时钟周期访问。

### 3. 实验设计
*   **数据集**：使用MNIST手写数字数据集。为了适应SNN，将其转换为“脉冲轨迹”（Spiking Traces），每个数字由220张二进制图像序列表示。
*   **Benchmark（基准）**：
    *   **软件基准**：使用Julia编写的原始SRC模型和简化后的Pseudo-VHDL（PsV）模型。
    *   **硬件对比**：与近年来主流的基于LIF模型的FPGA加速器（如Carpegna等人的Spiker+、Han等人的实现）进行对比。
*   **对比维度**：识别准确率、处理时间（ms/digit）、能耗（mJ/digit）、资源占用（LUT/DSP/BRAM）。

### 4. 资源与算力
*   **硬件平台**：AMD Xilinx Artix-7 XC7A200T FPGA（Alinx AX7A200开发板）。
*   **时钟频率**：100 MHz。
*   **算力与功耗**：
    *   逻辑功耗约为1.28 W。
    *   处理每个数字的时间：1.74 ms（标准配置）至0.17 ms（极速配置）。
    *   能耗：最低可达0.22 mJ/digit。
*   **资源占用**：每个SRC神经元占用约830个LUT和132个FF，使用了100个DSP单元。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   **准确率验证**：对比了Julia原始模型与FPGA实现的性能一致性。
    *   **消融/参数实验**：测试了不同脉冲轨迹长度（220到22帧）对准确率的影响。
    *   **量化实验**：对权重位宽进行了从9位到2位的深度扫描。
    *   **鲁棒性测试**：改变神经元发放频率参数（$z_{hyp}$）观察系统稳定性。
*   **充分性评价**：实验设计较为全面，涵盖了从算法简化到硬件实现的完整链路，通过多维度的权衡（Trade-off）分析，客观展示了模型在不同约束下的表现。

### 6. 主要结论与发现
*   **高性能与低能耗**：在100 MHz频率下，基准实现达到了96.31%的准确率。通过优化（44帧轨迹、4位权重），能在保持92.89%准确率的同时，将能耗降至0.45 mJ/digit。
*   **模型鲁棒性**：SRC模型表现出极强的鲁棒性，离线训练的权重无需任何重新调整即可直接部署在简化后的硬件架构上，且准确率损失极小（仅约0.17%）。
*   **优于LIF**：相比于传统的LIF模型，SRC在FPGA上能以相近的资源开销提供更复杂的动力学特性，且在低位宽量化下表现更稳健。

### 7. 优点
*   **数学简化巧妙**：通过Stanh和阈值逻辑成功规避了FPGA处理非线性函数的短板。
*   **无需重训练**：展示了从高精度浮点模型到低精度定点硬件的无缝迁移能力。
*   **模块化架构**：提出的Binder-NetWorkLevel架构具有高度的可重构性和扩展性。

### 8. 不足与局限
*   **任务复杂度有限**：实验仅局限于MNIST数据集，对于更复杂的视觉任务（如CIFAR-10或ImageNet）或时序任务的泛化能力尚待验证。
*   **网络规模较小**：目前仅实现了百级别神经元的单层隐藏层结构，尚未探讨大规模深层SNN在FPGA上的布线与资源竞争问题。
*   **事件驱动潜力未完全挖掘**：虽然提到了稀疏性，但目前的硬件实现仍基于同步时钟处理，未完全转化为异步事件驱动架构以进一步压低功耗。

（完）
