---
title: Bayesian Optimization of Crossbar-Based Compute-In-Memory System Design for Efficient DNN Inference
title_zh: 面向高效 DNN 推理的基于交叉棒存算一体系统设计的贝叶斯优化
authors: "Arnob Saha, Bibhas Manna, Nikhil Kotikalapudi, Md Zesun Ahmed Mia, Rahul Kumar, Madhavan Swaminathan, Abhronil Sengupta"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08461v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对DNN推理的存内计算系统设计的贝叶斯优化
tldr: 本研究针对存算一体（CIM）加速器在深度神经网络推理中的设计空间探索难题，提出了一种多目标贝叶斯优化（BO）框架。该框架能够协同优化硬件参数与算法参数，有效应对高维、非凸且极其复杂的设计空间。通过在VGG8和VGG16模型上的验证，该方法在保持高精度的同时，显著降低了芯片面积、读取延迟和动态能耗，并提升了内存利用率，为高效CIM系统设计提供了自动化优化方案。
selection_source: fresh_fetch
motivation: 复杂的DNN模型和异构层负载导致CIM加速器的设计空间呈现高维非凸特性，传统设计方法难以兼顾多目标优化效率。
method: 提出一种多目标贝叶斯优化框架，通过协同优化硬件架构与算法参数，在极大的搜索空间中高效寻找最优设计方案。
result: "实验表明，该方法在维持精度的同时，使芯片面积减少了高达65.52%，动态能耗降低了52.07%，并显著提升了内存利用率。"
conclusion: 多目标贝叶斯优化是解决复杂CIM系统设计空间探索的有效手段，能实现硬件资源与算法性能的深度协同与权衡。
---

## 摘要
利用基于交叉棒（crossbar）的存算一体（CIM）深度神经网络（DNN）加速器的高密度和高能效，需要进行最优的设计空间探索（DSE）。随着面向先进 AI 工作负载的复杂模型扩展了高度非凸的设计空间，这一过程变得日益具有挑战性。此外，异构的层工作负载（例如访存受限型与计算受限型）和学习表示使得逐层神经网络参数分配对提升效率大有裨益，但通过增加多目标优化中需同时调优的参数数量，严重加剧了设计空间的复杂性。在现有的 DSE 方法中，多目标贝叶斯优化（BO）展现出巨大潜力，因为它能在选择性地查询昂贵的 CIM 仿真器的同时，探索高质量的设计方案。在这项工作中，我们提出了一个多目标 BO 框架，旨在为各种 DNN 推理任务全面协同优化基于 CIM 交叉棒硬件加速器的硬件和算法参数。根据神经网络模型的深度，该框架能够处理高维设计空间（分别为 26 维和 50 维），以及针对 VGG8/CIFAR-10 和 VGG16/Tiny-ImageNet-200 的极大规模搜索复杂度（分别达 $O(10^{12})$ 和 $O(10^{27})$ 数量级）。我们的方法分别达到了 91.72% 和 57.2% 的准确率，与基准设计相当，同时优化了芯片面积（分别减少 65.52% 和 50.7%）、读取延迟（降低 9.52% 和 13.27%）、读取动态能耗（降低 31.23% 和 52.07%），并提升了内存利用率（增加 13.41% 和 2.67%）。

## Abstract
Leveraging the high density and energy efficiency of Compute-In-Memory (CIM) crossbar-based Deep Neural Network (DNN) accelerators requires optimal Design Space Exploration (DSE), which becomes increasingly challenging as complex models for advanced AI workloads expand the highly non-convex design space. Moreover, heterogeneous layer workloads (e.g., memory- vs. compute-bound) and learning representations make layer-wise NN parameter allocation beneficial for efficiency but severely exacerbate the design space complexity by expanding the number of parameters to be tuned for simultaneous multi-objective optimization. Among existing DSE approaches, multi-objective Bayesian Optimization (BO) is promising, as it explores high-quality design solutions while querying costly CIM simulators selectively. In this work, we propose a multi-objective BO framework that holistically co-optimizes hardware and algorithm parameters of a CIM crossbar-based hardware accelerator for various DNN inference tasks. Depending on NN model depth, our framework handles high-dimensional design spaces (with $26$ and $50$ dimensions) and extremely large search complexities on the order of $O(10^{12})$ and $O(10^{27})$ for VGG8/CIFAR-10 and VGG16/Tiny-ImageNet-200. Our method attains $91.72 \%$ and $57.2 \%$ accuracy, respectively, comparable to baseline designs, while improving chip area ($65.52 \%$ and $50.7 \%$), read latency ($9.52 \%$ and $13.27 \%$), read dynamic energy ($31.23 \%$ and $52.07 \%$) and increasing memory utilization ($13.41 \%$ and $2.67 \%$).

---

## 论文详细总结（自动生成）

这是一份关于论文《Bayesian Optimization of Crossbar-Based Compute-In-Memory System Design for Efficient DNN Inference》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在极高维且非凸的设计空间中，高效地优化基于交叉棒（Crossbar）的存算一体（CIM）硬件加速器，以实现深度神经网络（DNN）推理在精度、面积、延迟和能效之间的最佳平衡。
*   **研究动机**：
    *   **冯·诺依曼瓶颈**：传统架构受限于存储与计算的分离，而 CIM 能够显著提升能效。
    *   **设计空间爆炸**：为了提升效率，需要对不同神经网络层进行异构参数分配（逐层优化），这导致设计空间呈指数级增长（如 $O(10^{27})$）。
    *   **仿真成本高昂**：CIM 硬件仿真器（如 NeuroSim）运行缓慢，传统的穷举法或简单的启发式搜索难以在有限时间内找到最优解。

### 2. 方法论
论文提出了一种**多目标贝叶斯优化（BO）框架**，旨在协同优化硬件和算法参数：
*   **核心思想**：利用概率代理模型（Surrogate Model）来模拟昂贵的硬件仿真器，通过智能采样减少对仿真器的查询次数。
*   **关键技术细节**：
    1.  **设计空间定义**：包括逐层权重位宽（WBP）、逐层输入位宽（IBP）、逐层交叉棒子阵列大小（CSS）、ADC 分辨率（ABP）以及列复用数量（CCM）。
    2.  **代理模型**：为每个目标函数（精度、面积、延迟、能耗、内存利用率）构建独立的**高斯过程（GP）模型**。
    3.  **采集函数（Acquisition Function）**：采用**置信下限（LCB）**来平衡探索（寻找未知区域）与利用（优化已知优秀区域）。
    4.  **多目标优化**：在每一轮迭代中，使用 **NSGA-II 算法**在代理模型上优化采集函数，生成候选设计点。
    5.  **闭环反馈**：将选出的最优候选点输入 **NeuroSim 仿真器**获取真实性能数据，随后更新 GP 模型。

### 3. 实验设计
*   **数据集与模型**：
    *   VGG8 模型应用于 CIFAR-10 数据集（26 维设计空间，复杂度 $O(10^{12})$）。
    *   VGG16 模型应用于 Tiny-ImageNet-200 数据集（50 维设计空间，复杂度 $O(10^{27})$）。
*   **基准测试（Benchmark）**：
    *   **均匀参数分配设计**：即所有层使用相同的硬件/算法参数。
    *   **NSGA-II 算法**：作为传统的进化算法对比对象，评估在相同计算预算下的优化效率。
*   **对比指标**：推理精度、芯片面积、读取延迟、读取动态能耗、内存利用率。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号或 CPU 核心数量。
*   **训练/搜索时长**：提到 VGG8 进行了 200 次 BO 迭代，VGG16 进行了 120 次迭代。由于使用了代理模型，其核心优势在于减少了调用 NeuroSim 仿真器的次数（每轮迭代仅查询一次仿真器）。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同复杂度的任务（CIFAR-10 和 Tiny-ImageNet）进行了实验，涵盖了从 26 维到 50 维的设计空间。
*   **充分性评估**：
    *   **对比充分**：不仅对比了均匀分配的基准，还通过超体积（Hypervolume）指标与 NSGA-II 进行了收敛性对比。
    *   **客观性**：实验考虑了 5 个相互冲突的目标函数，展示了帕累托前沿（Pareto Front）的演变，证明了多目标权衡的有效性。
    *   **局限性**：实验主要集中在 VGG 系列模型上，未涉及更现代的 Transformer 或轻量级架构（如 MobileNet）。

### 6. 主要结论与发现
*   **性能显著提升**：在保持与基准相当的精度下，该方法在 VGG8 上减少了 65.52% 的面积和 31.23% 的能耗；在 VGG16 上减少了 50.7% 的面积和 52.07% 的能耗。
*   **逐层优化的优越性**：证明了为不同层分配不同位宽和子阵列大小能显著提升硬件效率，而不会大幅牺牲精度。
*   **BO 的高效性**：在相同的仿真查询次数下，贝叶斯优化比 NSGA-II 能够发现质量更高（超体积更大）的帕累托解。

### 7. 优点
*   **高效探索**：成功将贝叶斯优化应用于极高维（50维）的 CIM 硬件设计空间，解决了仿真成本高的痛点。
*   **协同设计**：实现了算法层（量化位宽）与硬件层（子阵列、ADC、复用器）的深度跨层协同优化。
*   **模块化与扩展性**：框架设计具有通用性，可以轻松添加新的设计约束或目标函数。

### 8. 不足与局限
*   **模型覆盖面**：实验仅限于卷积神经网络（CNN），对于当前主流的 Transformer 架构，其设计空间和工作负载特性可能有所不同。
*   **硬件开销忽略**：虽然考虑了面积和能耗，但未深入讨论逐层异构配置在实际制造和控制逻辑上的额外复杂性（如控制不同层不同位宽的逻辑开销）。
*   **验证集限制**：在 Tiny-ImageNet 实验中，由于资源限制，仅使用了部分采样数据进行验证，可能存在一定的统计偏差。

（完）
