---
title: End-to-End Keyword Spotting on FPGA Using Graph Neural Networks with a Neuromorphic Auditory Sensor
title_zh: 基于图神经网络与神经形态听觉传感器的 FPGA 端到端关键词检测
authors: "Wiktor Matykiewicz, Piotr Wzorek, Kamil Jeziorek, Tomás Muñoz, Antonio Rios-Navarro, Angel Jiménez-Fernández, Tomasz Kryjak"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09570v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于边缘平台高效设备端数据处理的端到端 FPGA 实现
tldr: "本研究针对移动机器人和嵌入式智能对高效边缘处理的需求，提出了首个在FPGA上实现的端到端关键词识别（KWS）系统。该系统集成了神经形态听觉传感器（NAS）和图神经网络（GNN），直接处理稀疏的事件化音频流，无需传统预处理。通过近存计算架构，在保持87.43%准确率的同时，实现了极低的延迟（小于35微秒）和功耗（1.12瓦），为实时边缘语音处理提供了高效方案。"
selection_source: fresh_fetch
motivation: 旨在解决移动机器人和嵌入式设备在边缘端进行高效、低功耗实时音频数据处理的挑战。
method: 提出一种集成神经形态听觉传感器与图神经网络的FPGA架构，直接在事件化音频流上进行端到端处理而无需传统预处理。
result: "在Google Speech Commands v2数据集上实现了87.43%的量化准确率，且端到端延迟低于35微秒，平均功耗仅为1.12瓦。"
conclusion: 该研究证明了结合神经形态传感器与图神经网络在FPGA上实现超低延迟、低功耗关键词识别的可行性与卓越性能。
---

## 摘要
随着移动机器人和嵌入式智能的快速发展，边缘平台对高效的设备端数据处理需求日益增长。一个极具前景的研究方向是使用受人类感觉系统启发的神经形态传感器，这类传感器能够生成编码环境变化的稀疏、基于事件的数据。在这项工作中，我们展示了首个在单个 FPGA 器件上集成神经形态听觉传感器 (NAS) 和图神经网络 (GNN) 的端到端关键词检测系统实现，实现了对原始音频数据的实时处理。所提出的架构消除了传统的信号预处理，直接在基于事件的音频流上运行。利用近内存计算网络架构，该系统实现了低延迟、低功耗的高效推理。实验结果表明，在经过神经形态传感器处理的 Google Speech Commands v2 数据集上，量化后的准确率达到 87.43%，端到端延迟低于 35 μs，平均功耗为 1.12 W。处理后的数据集、软件模型和硬件模块可在 https://github.com/vision-agh/NAS-GNN-KWS 获取。

## Abstract
With the rapid growth of mobile robotics and embedded intelligence, there is an increasing demand for efficient on-device data processing on edge platforms. A promising research direction is the use of neuromorphic sensors inspired by human sensory systems, which generate sparse, event-based data encoding changes in the environment. In this work, we present the first end-to-end FPGA implementation of a keyword spotting system that integrates a Neuromorphic Auditory Sensor (NAS) and a graph neural network (GNN) on a single FPGA device, enabling real-time processing of raw audio data. The proposed architecture eliminates conventional signal preprocessing and operates directly on event-based audio streams. Leveraging a compute-near-memory network architecture, the system achieves efficient inference with low latency and low power consumption. Experimental results demonstrate an accuracy of 87.43% after quantization on the Google Speech Commands v2 dataset processed through the neuromorphic sensor, with end-to-end latency below 35 us and average power consumption of 1.12 W. The processed datasets, software models, and hardware modules are available at https://github.com/vision-agh/NAS-GNN-KWS.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种在 FPGA 上实现的端到端关键词检测（KWS）系统，该系统结合了神经形态听觉传感器（NAS）和图神经网络（GNN）。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的音频处理流水线（如基于采样和 DSP 的方法）在边缘设备（如移动机器人、IoT）上往往面临高功耗和高延迟的挑战。
*   **研究动机**：生物听觉系统具有异步、稀疏处理的特性。研究者希望利用**神经形态听觉传感器（NAS）**产生的稀疏事件流，结合**图神经网络（GNN）**的非结构化数据处理能力，在单个 FPGA 芯片上构建一个无需传统预处理、超低延迟且低功耗的端到端语音识别系统。

### 2. 方法论
*   **核心思想**：将原始音频通过硬件传感器直接转化为事件（Events），利用 GNN 对这些事件在时空维度的关联性进行建模，并全程在 FPGA 逻辑中实现。
*   **关键技术细节**：
    *   **神经形态听觉传感器 (NAS)**：采用全数字 FPGA 实现，将音频信号分解为不同频率通道的异步事件流（AER 格式）。设计了**级联（Cascade）**和**并行（Parallel）**两种架构。
    *   **事件过滤（Filtration）**：提出一种基于**漏电积分发放（LIF）**神经元模型的硬件友好型过滤算法。通过通道特定的阈值和电荷衰减机制，在保留关键信息的同时减少了约 47% 的冗余事件。
    *   **GNN 架构**：采用改进的 **PointNetConv** 层提取特征。每个事件被视为图中的一个节点，根据预设的时间半径（$R_t$）和通道半径（$R_c$）建立有向边。
    *   **硬件优化**：引入了**背压调度机制（Back-pressure scheduling）**和**时间戳传播机制**，允许系统根据瞬时工作负载动态调整处理速度，解决了稀疏事件流带来的计算负载不均问题。

### 3. 实验设计
*   **数据集**：使用了 **Google Speech Commands v2 (GSCv2)**。
*   **实验场景**：研究者并未仅使用模拟数据，而是通过物理 NAS 硬件录制了该数据集的神经形态版本，涵盖了 32、64、128 通道的多种配置。
*   **对比方法（Benchmark）**：
    *   **SNN（脉冲神经网络）**：如 SpikGRU、DCLS-Delays 等。
    *   **SSM（状态空间模型）**：如 Event-SSM、S5-RF。
    *   **GNN 基准**：对比了之前在 SoC FPGA 上运行的 Spectro-temporal GNN。

### 4. 资源与算力
*   **训练算力**：使用单台 **NVIDIA GH200 GPU** 进行训练。
*   **训练细节**：FP32 模式训练 50 个 epoch，随后进行 5 个 epoch 的 **8位量化感知训练（QAT）**。
*   **部署平台**：**AMD Zynq UltraScale+ ZCU104 FPGA**。
*   **功耗与性能**：平均功耗约 **1.12W - 1.16W**，推理延迟在 **25μs 至 42μs** 之间。

### 5. 实验数量与充分性
*   **实验规模**：进行了大量的消融实验（Ablation Studies），包括模型通道数（18到126）、图生成的时空半径、过滤器的衰减因子及阈值策略（线性、指数、常数）。
*   **充分性评价**：实验设计非常充分且客观。研究者不仅测试了分类准确率，还引入了**时间戳条件准确率（Ts-acc）**来评估关键词末尾定位的精准度。通过 6 种不同的 NAS 配置进行对比，验证了并行架构在准确率和延迟上的优势。

### 6. 主要结论与发现
*   **性能卓越**：在 32 通道并行配置下，量化后的准确率达到 **87.43%**，优于大多数现有的 SNN 模型。
*   **极低延迟**：端到端延迟（从音频输入到预测输出）低于 **35μs**，远快于传统的基于窗口处理的方法。
*   **硬件效率**：通过优化，LUT 资源占用比前代工作减少了约 30%，且实现了全逻辑处理，无需 CPU 干预。
*   **过滤有效性**：LIF 过滤器不仅降低了计算压力，还通过滤除噪声提升了预测准确率。

### 7. 优点
*   **端到端集成**：这是首个将神经形态传感器和 GNN 完整集成在单一 FPGA 上的 KWS 系统，具有极高的工程参考价值。
*   **真实硬件数据**：不同于许多使用模拟器生成脉冲的研究，本文使用了真实硬件录制的数据集，更具说服力。
*   **开源贡献**：公开了处理后的数据集、软件模型和硬件模块，促进了社区研究。

### 8. 不足与局限
*   **传感器依赖性**：系统性能高度依赖于特定的 NAS 硬件配置，对于其他类型的神经形态传感器（如 DVS 视觉传感器转音频）的泛化能力未知。
*   **环境鲁棒性**：虽然在 GSCv2 上表现良好，但在极端噪声环境或多说话人重叠场景下的鲁棒性仍需进一步验证。
*   **应用限制**：目前仅针对短词（关键词）检测，对于长句识别或连续语音处理的扩展性尚未讨论。

（完）
