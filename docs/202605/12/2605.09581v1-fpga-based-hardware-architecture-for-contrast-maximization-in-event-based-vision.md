---
title: FPGA-Based Hardware Architecture for Contrast Maximization in Event-Based Vision
title_zh: 基于 FPGA 的事件视觉对比度最大化硬件架构
authors: "Michal Filipkowski, Marcin Kowalczyk, Tomasz Kryjak"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09581v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于FPGA的事件视觉硬件架构
tldr: 本文提出了一种基于 FPGA 的硬件架构，用于加速事件视觉中的对比度最大化（CM）算法。针对事件相机高时间分辨率和低空间冗余的特点，利用 FPGA 的并行流水线结构实现了事件扭曲、对比度计算及迭代优化模块。该设计是首个针对 CM 算法的硬件加速方案，显著提升了运动估计的实时性和能效，为高速嵌入式视觉系统提供了高效的运动参数估计手段。
selection_source: fresh_fetch
motivation: 旨在解决对比度最大化算法在通用处理器上计算开销大、难以满足实时嵌入式应用对高吞吐量和低功耗需求的问题。
method: 利用 FPGA 的大规模并行和深度流水线特性，设计了包含事件扭曲、对比度计算和迭代优化的专用硬件模块及硬件感知优化方案。
result: 实验结果显示，该架构在运动参数估计速度上比 CPU 和 GPU 实现快 200 倍以上，并具有极高的能效比和资源利用率。
conclusion: 这是首个实现 CM 算法加速的硬件架构，为实时、高速、低功耗的嵌入式运动估计应用奠定了坚实基础。
---

## 摘要
本文提出了一种在现场可编程门阵列（FPGA）资源中实现对比度最大化（CM）算法的硬件架构，用于事件视觉系统。CM 通过最大化从异步事件流重建的扭曲事件图像（IWE）的对比度来估计运动参数。事件视觉传感器产生具有高时间分辨率和低空间冗余的稀疏数据，这使其非常适合硬件处理。利用 FPGA 的确定性、大规模并行结构，设计了一种深度流水线架构，能够进行高吞吐量、高能效的处理，适用于实时嵌入式应用。本文详细介绍了负责事件扭曲、对比度计算和迭代优化的硬件模块，讨论了关键的实现决策，并展示了设计中使用的硬件感知优化方法。实验结果表明，与基于 CPU 和 GPU 的实现相比，该架构在速度和效率上有显著提升，运动参数估计的执行速度快了 200 倍以上。据我们所知，这是首个能够加速 CM 算法计算的硬件架构。其性能在处理速度、能效和硬件资源利用率方面进行了评估。所提出的设计通过一个基于事件的目标跟踪应用进行了验证。结果证实，该架构为高速、低功耗嵌入式系统中的实时运动估计提供了坚实的基础。

## Abstract
This paper presents a hardware architecture that implements the Contrast Maximization (CM) algorithm in Field-Programmable Gate Array (FPGA) resources for event-based vision systems. CM estimates motion parameters by maximizing the contrast of an Image of Warped Events (IWE) reconstructed from asynchronous event streams. Event-based vision sensors generate sparse data with high temporal resolution and low spatial redundancy, which makes them well suited for hardware processing. The deterministic, massively parallel structure of the FPGA is leveraged to design a deeply pipelined architecture capable of high-throughput, energy-efficient processing suitable for real-time embedded applications. This paper details the hardware modules responsible for event warping, contrast computation, and iterative optimization, discusses key implementation decisions, and presents the hardware-aware optimization method used in the design. Experimental results demonstrate a substantial speed and efficiency improvement over CPU- and GPU-based implementations, with motion parameter estimation executing over 200 times faster. To the best of our knowledge, this is the first hardware architecture enabling acceleration of CM algorithm computations. Its performance is evaluated in terms of processing speed, energy efficiency, and hardware resource utilization. The proposed design is validated using an event-based object tracking application. The results confirm that the architecture provides a solid foundation for real-time motion estimation in high-speed, low-power embedded systems.

---

## 论文详细总结（自动生成）

以下是对论文《FPGA-Based Hardware Architecture for Contrast Maximization in Event-Based Vision》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：事件相机（Event-based Camera）具有高时间分辨率和低延迟优势，但其主流算法——**对比度最大化（Contrast Maximization, CM）**涉及复杂的迭代优化过程，在传统的 CPU 或 GPU 上运行速度慢、功耗高，难以满足高速无人机、自动驾驶等嵌入式实时应用的需求。
*   **研究背景**：CM 算法通过将异步事件流“扭曲（Warping）”到参考时间点并最大化图像方差来估计运动参数。由于事件数据具有稀疏性和异步性，FPGA 的并行流水线结构被认为是加速此类计算的理想平台。

### 2. 方法论
*   **核心思想**：设计一种深度流水线的 FPGA 架构，将事件预处理、坐标扭曲、双线性投票（Bilinear Voting）、梯度计算和迭代优化整合在硬件电路中。
*   **关键技术细节**：
    *   **事件扭曲与双线性投票**：根据当前的运动速度参数（$v_x, v_y$），计算每个事件在参考时间的投影坐标。为了提高精度，采用双线性投票法将事件权重分配给相邻的 4 个像素。
    *   **并行存储架构**：为了解决双线性投票带来的高频内存访问冲突，设计了 **12 个独立的 BRAM 存储库**（4 个用于存储扭曲图像 IWE，8 个用于存储两个维度的梯度图像）。这种划分允许在一个时钟周期内同时写入 4 个像素的贡献值。
    *   **冲突处理机制**：引入了一个基于寄存器的微型缓冲区，用于存储最近 3 次写入的地址和值，防止因流水线延迟导致的“写后读（RAW）”冲突。
    *   **优化循环**：在硬件中直接实现梯度上升法（Gradient Ascent），每个事件批次（Batch）进行 100 次迭代，以寻找最优运动参数。
    *   **ROI 过滤**：仅处理感兴趣区域（ROI）内的事件，以减少计算量并适应有限的硬件资源。

### 3. 实验设计
*   **数据集与场景**：使用了标准的 **DAVIS 240C 数据集**，验证场景为基于事件的目标跟踪。
*   **Benchmark（基准）**：
    *   **软件参考模型**：基于 Python 和 PyTorch 开发。
    *   **对比硬件**：Intel Core i5-11300H CPU (3.10 GHz) 和 Nvidia GeForce RTX 3050 Ti GPU。
*   **评估指标**：处理时间（ms）、时钟频率、资源利用率、功耗。

### 4. 资源与算力
*   **FPGA 平台**：使用了 **Xilinx Kria KV260 Vision AI Starter Kit**（搭载 Zynq UltraScale+ MPSoC）。
*   **算力与性能指标**：
    *   **时钟频率**：最高可达 210 MHz。
    *   **处理速度**：处理 5000 个事件（100 次迭代）仅需 **0.92 ms**。
    *   **功耗**：片上总功耗约为 **0.813 W**。
    *   **GPU/CPU 算力说明**：文中未详细列出 GPU 的训练时长（因为是推理/优化过程），但明确指出 GPU 执行相同任务需 473.51 ms。

### 5. 实验数量与充分性
*   **实验规模**：论文通过对特定数据集序列的跟踪实验验证了算法的正确性，并给出了详细的资源消耗表。
*   **充分性评价**：
    *   **客观性**：通过与 CPU 和 GPU 的直接耗时对比，客观展示了 FPGA 在确定性低延迟任务中的优势。
    *   **局限性**：实验主要集中在 64x64 的 ROI 尺寸上，对于全分辨率（如 240x180）的实验仅通过公式进行了推算，未在当前硬件上完全展开（受限于 BRAM 容量）。

### 6. 主要结论与发现
*   **性能飞跃**：FPGA 实现的 CM 算法比 CPU 快 **200 倍以上**，比 GPU 快约 **450 倍**。
*   **首创性**：这是学术界**首个**实现 CM 算法硬件加速的架构。
*   **实时性**：该架构证明了在功耗低于 1W 的情况下，可以实现微秒级的运动参数估计，完全满足高速嵌入式视觉的需求。

### 7. 优点
*   **高效的内存管理**：通过多库 BRAM 划分和寄存器缓冲解决了硬件实现 CM 算法最核心的内存访问瓶颈问题。
*   **深度流水线**：实现了从事件输入到 ROI 更新的全硬件闭环，无需主机干预。
*   **极高能效比**：在极低功耗下实现了远超高性能 GPU 的处理速度。

### 8. 不足与局限
*   **资源限制**：由于 BRAM 资源限制，目前仅支持较小的 ROI（64x64），若要支持全画幅处理，需要更高规格的 FPGA 或引入外部 DDR 存储（但这会增加延迟）。
*   **运动模型单一**：目前仅支持二维恒定速度模型，对于旋转、缩放或更复杂的非线性运动模型尚不支持。
*   **优化算法简单**：采用的是基础的梯度上升法，可能在某些复杂场景下陷入局部最优，未来可引入更先进的优化策略。

（完）
