---
title: "DSPE: An Energy-Efficient Edge Processor for DeepSeek Inference with MerkleTree-based Incremental Pruning, Multi-Stage Boothing Lookup and Dynamic Adaptive Posit Processing"
title_zh: DSPE：一种用于 DeepSeek 推理的高能效边缘处理器，支持基于默克尔树的增量剪枝、多阶段 Booth 查找和动态自适应 Posit 处理
authors: "Yuhan Zhang, Zhou Wang, Zhou Shu, Jiuren Zhou, Yanqing Xu, Xiaonan Tang, Shushan Qiao, Tianchun Ye, Yang Liu, Anil A. Bharath, Emm Mic Drakakis"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08615v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 采用剪枝和自适应处理的DeepSeek能效边缘处理器
tldr: 本研究针对DeepSeek模型在边缘设备部署面临的高能耗和计算压力，提出了DSPE处理器架构。通过引入基于MerkleTree的增量剪枝方案（MIPS）、多阶段Boothing查找乘法（MBLM）以及动态自适应Posit处理机制（DAPPM），DSPE在TSMC 28nm工艺下实现了109.4 TFLOPS/W的极高能效，为大模型在资源受限环境下的高效推理提供了可扩展的硬件基础。
selection_source: fresh_fetch
motivation: 旨在解决DeepSeek等大语言模型在能耗受限的边缘设备上难以高效部署和运行的挑战。
method: 提出了集成增量剪枝、近似乘法查找和动态自适应Posit格式处理的三大核心硬件优化技术。
result: 在TSMC 28nm工艺下实现了109.4 TFLOPS/W的能效比，相比现有先进设计具有显著优势。
conclusion: DSPE架构为边缘端大模型推理提供了一种高能效、可扩展且具备安全冗余削减能力的硬件解决方案。
---

## 摘要
近年来，DeepSeek 展现了强大的推理性能，但在能耗受限的边缘设备上部署仍然困难。本文提出了 DeepSeek 处理单元（DSPE），这是一种面向边缘的架构，旨在缓解模型沉重的计算和能耗需求。DSPE 引入了三项技术：用于安全冗余向量缩减的基于默克尔树的增量剪枝方案（MIPS）、用于位翻转感知近似乘法的多阶段 Booth 查找方法（MBLM），以及引入了新 DA-Posit 格式及其相应硬件乘法架构的动态自适应 Posit 处理机制（DAPPM）。DSPE 采用 TSMC 28nm CMOS 工艺实现，与现有最先进设计相比，实现了 109.4 TFLOPS/W 的能效，并为边缘部署提供了可扩展的基础。

## Abstract
In recent years, DeepSeek has achieved strong inference performance but remains hard to deploy on energy-constrained edge devices. This paper presents the DeepSeek Processing Element (DSPE), an edge-oriented architecture that alleviates the model's heavy computational and energy demands. DSPE introduces three techniques: the MerkleTree-based Incremental Pruning Scheme (MIPS) for secure redundant-vector reduction, the Multi-Stage Boothing Lookup Method (MBLM) for bit-flip-aware approximate multiplication, and the Dynamic Adaptive Posit Processing Mechanism (DAPPM), which introduces a new DA-Posit format and its corresponding hardware multiplication architecture. Implemented in TSMC 28nm CMOS, DSPE achieves 109.4 TFLOPS/W energy efficiency compared with state-of-the-art designs and offers a scalable foundation for edge deployment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DSPE**（DeepSeek Processing Element）的专用集成电路（ASIC）架构，旨在解决大语言模型（特别是 DeepSeek-V3）在边缘设备上部署时面临的高能耗和计算冗余问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：DeepSeek-V3 等大模型虽然推理效率高，但其巨大的参数量、复杂的混合专家模型（MoE）架构以及多头潜在注意力（MLA）机制，使得在资源受限的边缘设备上运行面临严峻的挑战。
*   **研究动机**：
    *   **计算冗余**：层间存在大量时空冗余的中间表示。
    *   **能耗瓶颈**：大规模线性变换和 KV Cache 维护导致频繁的位翻转和内存访问，功耗极高。
    *   **矩阵运算延迟**：传统 Booth 乘法在处理共享权重时，若激活值差异大，会引发大量无效计算。

### 2. 核心方法论
论文提出了三大创新技术：
*   **基于默克尔树的增量剪枝方案 (MIPS)**：
    *   **流程**：相似性重排序 $\rightarrow$ 默克尔树编码 $\rightarrow$ 动态重用。
    *   **细节**：利用向量间的余弦相似度进行排序，通过轻量级 MAC 运算将高维向量投影到语义空间并构建默克尔树。通过比较哈希值（$\Delta H$），实现“早期跳过”（完全相同）、“差异重用”（近似匹配）或“全量计算”（新模式），从而大幅减少 DRAM 和 SRAM 访问。
*   **多阶段 Booth 查找方法 (MBLM)**：
    *   **细节**：引入贝叶斯网络（BN）根据输入分布动态选择 Radix-4 或 Radix-8 Booth 编码路径。通过位变化矩阵（BVM）和查找表（Booth-LUT）识别并消除重复的位翻转模式，优化乘法过程中的能量消耗。
*   **动态自适应 Posit 处理机制 (DAPPM)**：
    *   **DA-Posit 格式**：提出一种改进的 Posit 数值系统，通过合并指数和尾部的冗余位（位折叠），在不增加位宽的前提下实现结构化压缩。
    *   **硬件架构**：设计了可重构的乘法器阵列，支持 16/9/4 个不同规模的 PE 模式切换，以匹配不同的压缩精度需求。

### 3. 实验设计
*   **数据集**：主要使用 **MMLU**（Massive Multitask Language Understanding）数据集来评估 DeepSeek-V3 在该处理器上的推理表现。
*   **Benchmark（基准测试）**：
    *   **硬件对比**：对比了 NVIDIA GPU H100 以及近年来在 ISSCC 和 VLSI 会议上发表的顶级 AI 加速器（如基于 5nm、22nm 或 28nm 工艺的设计）。
    *   **评估指标**：能效比（TFLOPS/W）、吞吐量、芯片面积、功耗、频率。

### 4. 资源与算力
*   **硬件实现**：采用 **TSMC 28nm CMOS** 工艺。
*   **物理参数**：芯片面积为 **8.23 $mm^2$**；工作电压范围 0.6V - 1.1V；最高频率 **710MHz**。
*   **算力表现**：在 1.1V 下达到 **22.8 TFLOPS** (@POSIT8) 的峰值性能。
*   **训练/推理时长**：文中未提及模型训练时长（该工作侧重于推理硬件设计），但给出了推理能效数据。

### 5. 实验数量与充分性
*   **实验规模**：论文通过逻辑综合（Synopsys Design Compiler）和布局布线（IC Compiler）获得了准确的硬件参数。针对 MMLU 数据集进行了深度分析，验证了 MIPS 减少了 33.5% 的 DRAM 访问，MBLM 减少了 39.1% 的计算量。
*   **充分性评价**：实验在硬件仿真和综合层面较为充分，对比了多种精度（FP8, INT8, BF16, POSIT8）下的能效。但由于是针对 DeepSeek 模型的特定优化，缺乏在其他 LLM 模型上的泛化性测试。

### 6. 主要结论与发现
*   **极高能效**：DSPE 在 0.6V/200MHz 下实现了 **109.4 TFLOPS/W** 的峰值能效，这一数值是 GPU H100 (@FP8) 的 **19.35 倍**。
*   **冗余削减有效**：通过 MIPS 和 MBLM 技术，成功将边缘端推理的内存带宽压力和位翻转功耗降低了约 30%-40%。
*   **精度保持**：DAPPM 机制在提升 1.47 倍计算速度的同时，通过自适应位压缩保持了计算精度。

### 7. 优点
*   **针对性强**：专门针对 DeepSeek 的 MLA 和 MoE 结构设计，解决了当前通用加速器处理稀疏专家选择时的低效问题。
*   **跨层优化**：从数值表示（DA-Posit）、算法逻辑（Merkle Tree 剪枝）到电路级（多阶段 Booth）进行了全栈优化。
*   **安全性考量**：引入默克尔树不仅是为了剪枝，其哈希机制也为数据完整性验证提供了潜在支持。

### 8. 不足与局限
*   **模型绑定**：架构高度耦合 DeepSeek-V3 的特性，如果模型架构发生重大改变（如弃用 MoE 或 MLA），该处理器的优势可能会削弱。
*   **工艺代差对比**：虽然能效比远超 H100，但 H100 是通用 GPU 且采用更先进工艺（4nm），而 DSPE 是专用 ASIC，这种对比在灵活性上对 GPU 不公平。
*   **实际部署验证**：目前结果主要基于 EDA 工具的综合与仿真，缺乏在真实边缘场景（如复杂电磁环境、多任务并发）下的实测数据。

（完）
