---
title: "VDCores: Resource Decoupled Programming and Execution for Asynchronous GPU"
title_zh: VDCores：面向异步 GPU 的资源解耦编程与执行模型
authors: "Zijian He, Adrian Sampson, Yiying Zhang, Zhiyuan Guo"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03190v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 自动重叠内存与计算以提高利用率
tldr: 针对现代GPU异步硬件单元利用率低的问题，VDCores提出了一种资源解耦的编程与执行模型。它将异步硬件抽象为资源隔离的虚拟核心，并将工作负载表示为依赖连接的微操作。该模型消除了程序员的手动编排，实现了内存与计算的自动重叠，在LLM推理任务中显著提升了吞吐量并大幅降低了开发难度。
selection_source: fresh_fetch
motivation: 传统的单体内核模型与现代GPU的异步硬件特性不匹配，导致硬件资源利用率低下且编程复杂度极高。
method: 通过将硬件抽象为虚拟核心并将任务分解为微操作，利用运行时系统实现基于依赖关系的自动资源调度与计算重叠。
result: "在GH200等GPU上的LLM推理测试显示，该方法平均提升吞吐量24%，最高达77%，同时减少了90%的编程工作量。"
conclusion: VDCores证明了资源解耦模型能有效解决异步GPU硬件的利用率难题，是优化高性能计算和AI推理效率的有效方案。
---

## 摘要
现代 GPU 越来越依赖专用且异步的硬件单元来提供高性能。然而，由于当今的 GPU 软件栈仍围绕着与异步硬件不匹配的单体算子（monolithic kernel）模型来组织编程和执行，这些单元往往未得到充分利用。为解决这一问题，虚拟解耦引擎（VDCores）提出了一种面向异步 GPU 的新型解耦编程与执行模型。VDCores 将异步硬件执行单元抽象为资源隔离的虚拟核心，并将工作负载表示为由依赖关系连接的微操作（micro-ops）。这种抽象消除了程序员的静态编排负担，能够根据依赖关系和资源就绪情况实现访存与计算的自动重叠，从而提高异步硬件资源的利用率。在当今的 GPU 上高效实现这种解耦抽象本身具有挑战性，VDCores 通过针对 GPU 优化的编程模型和运行时设计解决了这一问题，在保持灵活性的同时最大限度地降低了实现开销。在 GH200、H100 和 RTX 6000 Pro GPU 上的四种大语言模型（LLM）推理工作负载中，VDCores 将解码吞吐量平均提高了 24%，在动态输入下最高可提高 77%，同时将算子编程和专门化工作量减少了 90%。我们已在 https://github.com/vdcores/vdcores 开源了 VDCores。

## Abstract
Modern GPUs increasingly rely on specialized and asynchronous hardware units to deliver high performance. Yet these units are often underutilized because today's GPU software stacks still organize programming and execution around a monolithic kernel model that mismatches asynchronous hardware. To address this issue, Virtual Decoupled Engines (VDCores) presents a new decoupled programming and execution model for asynchronous GPUs. VDCores abstracts asynchronous hardware execution units as resource isolated virtual cores and represents workloads as dependency-connected micro-operations (micro-ops). this abstraction removes static orchestration from the programmer, enables automatic overlap of memory and compute based on dependency and resource readiness, and thereby improves utilization of asynchronous hardware resources.   Realizing such a decoupled abstraction efficiently on today's GPUs is itself challenging, VDCores addresses this through a GPU-specialized programming model and GPU runtime design that preserves the flexibility while minimizing implementation overhead. Across four LLM inference workloads on GH200, H100, and RTX 6000 Pro GPUs, VDCores significantly improves decoding throughput by 24% on average and by up to 77% under dynamic inputs, while reducing kernel programming and specialization effort by 90%. We have open sourced VDCores at https://github.com/vdcores/vdcores.

---

## 论文详细总结（自动生成）

这篇论文提出了 **VDCores**，一种针对现代异步 GPU 的资源解耦编程与执行模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 GPU 硬件（如 NVIDIA Hopper/Blackwell）引入了大量异步单元（如 Tensor Cores, TMA），但软件栈仍沿用传统的**单体内核（Monolithic Kernel）模型**。
*   **研究动机**：
    *   **编程复杂度高**：程序员必须手动编排内存移动、计算和同步，以避免硬件空转（Bubbles）。
    *   **资源利用率低**：单体内核将资源绑定在一起，导致内核边界处出现利用率空洞，且静态调度无法适应动态工作负载（如不同长度的 LLM 推理）。
    *   **开发成本大**：为了追求性能，开发者不得不针对不同输入形状编写大量专门化的融合内核（Kernel Specialization）。

### 2. 方法论：VDCores 核心设计
VDCores 的核心思想是将 GPU 硬件资源抽象为独立的虚拟核心，并通过微操作（$\mu$ops）流驱动执行。
*   **资源解耦抽象**：
    *   **虚拟内存核心 (VMC)**：负责全局内存与共享内存之间的数据搬运及控制流。
    *   **虚拟计算核心 (VCC)**：负责 Tensor Core 和 CUDA Core 的计算任务。
*   **微操作 ($\mu$ops) 与依赖图**：
    *   将算子分解为细粒度的 $\mu$ops，通过依赖边连接。
    *   **依赖驱动执行**：运行时根据资源就绪情况自动重叠内存和计算，无需手动编排。
*   **关键技术细节**：
    *   **$\mu$op 生成器**：支持自适应分块（Adaptive Tiling）和动态融合（Dynamic Fusion），能根据运行时形状自动重写内存路径。
    *   **GPU 执行器**：在 GPU 上实现了一个软件流水线化的指令解释器。利用 SIMT 线程并行处理指令解码、地址生成和资源分配。
    *   **虚拟流 (Virtual Flows)**：允许不相关的指令流乱序执行，解决头端阻塞（Head-of-line blocking）问题。

### 3. 实验设计
*   **实验场景**：大语言模型（LLM）离线解码推理。
*   **测试模型**：Qwen3-1.7B/8B, Llama3.2-1B, Llama3.1-8B。
*   **对比基准 (Benchmarks)**：
    *   **算子级执行**：vLLM, SGLang。
    *   **专家优化的大内核 (Megakernels)**：Mirage, ThunderKittens (TK)。
    *   **混合模式**：Torch + ThunderKittens。
*   **评估指标**：Token 生成吞吐量、解码延迟、编程代码量（LoC）。

### 4. 资源与算力
*   **硬件平台**：
    *   **NVIDIA H100** (80GB HBM3)
    *   **NVIDIA GH200** (Grace Hopper)
    *   **NVIDIA RTX 6000 Pro** (Ada Lovelace)
*   **配置细节**：每个 SM 部署 1 个 VMC 和 2 个 VCC，共享内存槽位大小为 8KB。
*   **训练/执行时长**：论文未提及模型训练，主要关注推理阶段的实时性能。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种硬件架构、4 种模型、多种 Batch Size（1-8），共计 **48 组端到端组合实验**。
*   **消融实验**：详细分析了自动重叠（Auto-overlapping）、动态融合（Dynamic Fusion）和自适应调度（Adaptive Scheduling）的贡献。
*   **充分性评价**：实验设计非常充分，不仅对比了吞吐量，还深入探讨了运行时开销（如指令发射延迟、内存带宽利用率）以及在动态 LoRA 任务下的表现，客观地展示了系统在不同负载下的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：VDCores 平均比最强的基准系统提升了 **24%** 的吞吐量；在动态输入场景下，性能提升高达 **77%**。
*   **开发效率**：相比于手动编写复杂的融合内核，VDCores 将编程和专门化工作量减少了 **90%**（代码量从数千行减少到几百行）。
*   **运行时开销**：尽管是软件模拟的解耦执行，但其运行时开销仅占总核心时间的 **3.1%**，在 4KB-16KB 的数据块大小下即可跑满带宽。

### 7. 优点与亮点
*   **范式转移**：从“手动编排的单体内核”转向“依赖驱动的解耦执行”，更符合现代异步硬件的物理特性。
*   **自动优化**：实现了跨算子边界的自动重叠和动态融合，无需为每种模型结构重新编写内核。
*   **高移植性**：同一套 $\mu$op 逻辑可以跨 Hopper、Blackwell 和 Ada 架构运行，屏蔽了底层硬件指令（如 TMA, WGMMA）的差异。

### 8. 不足与局限
*   **应用范围限制**：目前主要针对 LLM 推理等具有规则分块特征的任务，对于极度稀疏或不规则的计算任务，$\mu$op 的开销可能会放大。
*   **内存压力**：虚拟核心的上下文和消息队列需要占用一定的共享内存（Shared Memory），在资源极度紧张的算子中可能会限制并发度（Occupancy）。
*   **编译器依赖**：虽然减少了内核编写工作，但高效生成 $\mu$op 流仍依赖于上层调度器的启发式策略。

（完）
