---
title: Split CNN Inference on Networked Microcontrollers
title_zh: 联网微控制器上的拆分卷积神经网络推理
authors: "Junyu Lu, Shashwath Suresh, Hao Liu, Qi Hong, Qing Wang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09357v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对内存受限微控制器的拆分式CNN推理
tldr: 针对微控制器（MCU）内存受限导致无法运行大型CNN模型的问题，本文提出了一种细粒度的分布式推理系统。该系统突破了传统的层级划分限制，通过在子层级（卷积核和神经元维度）对预训练模型进行拆分，将模型参数和中间激活值分布到多个联网MCU上。实验表明，该方法能有效降低单台MCU的峰值RAM占用，使原本无法运行的模型在多设备协作下得以实现，且保持了实用的推理延迟。
selection_source: fresh_fetch
motivation: 单个微控制器有限的内存资源（尤其是中间激活值的峰值RAM占用）严重限制了复杂CNN模型的部署。
method: 提出一种细粒度的拆分推理系统，通过在子层级（卷积核和神经元）重新解释模型，将计算任务和内存负载分布到多个异构MCU上。
result: 在多达8台MCU上成功运行了MobileNetV2，显著降低了单设备的峰值RAM需求，并保持了可接受的端到端延迟。
conclusion: 通过细粒度的协作推理，可以有效打破单机内存瓶颈，在资源受限的联网微控制器集群上实现高性能深度学习推理。
---

## 摘要
在微控制器单元（MCU）上运行深度神经网络受到有限内存资源的严重限制。虽然 TinyML 技术减小了模型大小和计算量，但由于推理过程中由中间激活主导的峰值随机存取存储器（RAM）占用过高，它们在实践中往往会失败。因此，许多模型在独立 MCU 上仍然无法运行。在这项工作中，我们提出了一种用于联网 MCU 的细粒度拆分推理系统，该系统能够跨多个设备协作推理卷积神经网络（CNN）模型。我们的核心见解是，打破内存瓶颈需要以子层粒度而非层边界来拆分推理。我们重新解释了预训练模型，以实现卷积核级和神经元级的分区，并将模型参数和中间激活分布到多个 MCU 上。一个轻量级的资源感知协调器在具有异构资源的 MCU 设备之间编排推理。我们在真实测试平台上实现了所提出的系统，并使用代表性 CNN 模型 MobileNetV2 在多达 8 个 MCU 上进行了评估。我们的实验结果表明，在单个 MCU 上无法运行的 CNN 模型可以在联网 MCU 上执行，在降低每个 MCU 峰值 RAM 占用的同时，保持了实际的端到端推理延迟。本工作的所有源代码均可在以下网址找到：https://github.com/shashsuresh/split-inference-on-MCUs。

## Abstract
Running deep neural networks on microcontroller units (MCUs) is severely constrained by limited memory resources. While TinyML techniques reduce model size and computation, they often fail in practice due to excessive peak Random Access Memory (RAM) usage during inference, dominated by intermediate activations. As a result, many models remain infeasible on standalone MCUs. In this work, we present a fine-grained split inference system for networked MCUs that enables collaborative inference of Convolutional Neural Networks (CNN) models across multiple devices. Our key insight is that breaking the memory bottleneck requires splitting inference at sub-layer granularity rather than at layer boundaries. We reinterpret pre-trained models to enable kernel-wise and neuron-wise partitioning, and distribute both model parameters and intermediate activations across multiple MCUs. A lightweight, resource-aware coordinator orchestrates the inference across MCU devices with heterogeneous resources. We implement the proposed system on a real testbed and evaluate it on up to 8 MCUs using MobileNetV2, a representative CNN model. Our experimental results show that CNN models infeasible on a single MCU can be executed across networked MCUs, reducing the per-MCU peak RAM usage while maintaining the practical end-to-end inference latency. All the source code of this work can be found here: https://github.com/shashsuresh/split-inference-on-MCUs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Split CNN Inference on Networked Microcontrollers》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在微控制器（MCU）上部署深度学习模型（TinyML）时，主要的瓶颈并非仅是模型权重的大小，而是推理过程中的**峰值 RAM 占用**。中间层激活值（Activations）往往远超单个 MCU 仅有的几十至几百 KB 的内存容量，导致即使经过压缩的模型也无法运行。
*   **研究背景**：现有的拆分推理（Split Inference）多基于“层”边界进行划分，但对于 MCU 而言，单个层（如 MobileNetV2 的某些层）的内存需求就已超过单机极限。因此，需要一种更细粒度的协作推理模式，利用联网的多个廉价 MCU 共同完成任务。

### 2. 方法论
该论文提出了一种细粒度的拆分推理系统，其核心思想是将计算任务从“层级”细化到“神经元级”或“卷积核级”。
*   **模型重解释（Reinterpretation）**：在离线阶段分析预训练模型，提取神经元间的依赖关系、感受野信息和张量维度，建立细粒度的计算图。
*   **细粒度拆分策略**：
    *   **卷积层**：按卷积核（Kernel-wise）拆分，将输出特征图划分为多个补丁（Patches），分配给不同的 Worker MCU。
    *   **全连接层**：按列（Column-wise）拆分权重矩阵，使每个 MCU 独立计算部分输出神经元。
*   **资源感知协调机制**：
    *   **能力评分（Rating）**：公式化定义了 MCU 的评分 $R_i$，综合考虑了时钟频率（计算能力）、通信带宽和延迟。
    *   **负载分配**：根据评分比例分配权重碎片和计算任务，并根据各 MCU 的 Flash 和 RAM 硬件限制进行迭代调整，确保负载均衡。
*   **跨层激活映射**：协调器（Coordinator）维护一个映射表，负责将上一层产生的中间激活值精确路由到下一层所需的 Worker 节点。

### 3. 实验设计
*   **硬件平台**：使用 8 台 Teensy 4.1 MCU（ARM Cortex-M7 @ 600MHz，1MB RAM，8MB Flash），通过以太网交换机连接。
*   **测试模型**：MobileNetV2（代表性的轻量化但内存需求高的 CNN 模型）。
*   **对比基准（Benchmarks）**：
    *   **均匀分配（Evenly）**：不考虑硬件差异，平均分配任务。
    *   **仅频率分配（Freq-only）**：仅根据 MCU 主频比例分配任务。
    *   **优化分配（Optimized）**：本文提出的基于评分的资源感知分配。
*   **评估指标**：端到端延迟、通信开销、单机峰值内存占用。

### 4. 资源与算力
*   **推理端算力**：使用了 8 台 Teensy 4.1 嵌入式开发板。
*   **训练/离线端**：模型重解释和拆分逻辑在 PC 端（作为协调器）完成。
*   **算力说明**：文中未提及模型训练所需的 GPU 资源，因为研究重点在于**推理阶段**的系统架构和资源调度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在真实测试平台上进行了 3、5、8 台 MCU 的扩展性实验。
    *   通过仿真器模拟了多达 120 台 MCU 的内存缩放趋势。
    *   设置了 8 种不同的异构场景（改变频率和通信延迟）来验证负载均衡算法。
*   **充分性评价**：实验设计较为充分，涵盖了从单机不可行到多机协作的可行性验证，并对比了不同调度策略。通过真实硬件测试而非纯仿真，增强了结果的说服力。

### 6. 主要结论与发现
*   **打破内存壁垒**：通过细粒度拆分，原本在单台 MCU 上因内存溢出（OOM）无法运行的 MobileNetV2 成功在多台 MCU 上运行。
*   **内存缩放**：随着 MCU 数量增加，单机峰值 RAM 显著下降，但在 10-20 台设备后边际效应递减。
*   **性能权衡**：增加 MCU 数量可以显著降低计算时间，但会增加通信开销。在 8 台设备以内，系统能保持实用的端到端延迟。
*   **异构优化有效性**：在存在通信延迟和频率差异的异构网络中，本文的评分分配机制比均匀分配提升了约 18.9% 的性能。

### 7. 优点
*   **粒度创新**：突破了传统的“层”拆分限制，实现了子层级的细粒度划分，真正解决了 MCU 的 RAM 瓶颈。
*   **实战导向**：在真实 MCU 硬件和以太网环境下完成，考虑了层融合、量化等实际部署技术。
*   **资源感知**：提出的评分系统能够很好地处理 MCU 集群中的异构性问题。

### 8. 不足与局限
*   **通信瓶颈**：由于 MCU 间需要频繁交换激活值，通信开销随设备增加剧增，限制了大规模扩展。
*   **拓扑限制**：目前依赖中心协调器进行数据转发，可能存在单点瓶颈；文中提到 MCU 间直接通信（P2P）尚待未来研究。
*   **模型单一**：实验主要集中在 MobileNetV2，虽然具有代表性，但未测试 Transformer 或 RNN 等其他架构。
*   **应用场景限制**：需要多个 MCU 联网环境，增加了布线或网络维护成本，适用于特定的工业监控或传感器阵列场景。

（完）
