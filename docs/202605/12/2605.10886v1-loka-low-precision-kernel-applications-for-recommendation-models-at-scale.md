---
title: "LoKA: Low-precision Kernel Applications for Recommendation Models At Scale"
title_zh: LoKA：面向大规模推荐模型的低精度算子应用
authors: "Liang Luo, Yinbin Ma, Quanyu Zhu, Vasiliy Kuznetsov, Yuxin Chen, Jian Jiao, Jiecao Yu, Buyun Zhang, Tongyi Tang, Xiaohan Wei, Yanli Zhao, Zeliang Chen, Yuchen Hao, Venkatesh Ranganathan, Sandeep Parab, Yantao Yao, Maxim Naumov, Chunzhi Yang, Shen Li, Ellie Wen, Wenlin Chen, Santanu Kolay, Chunqiang Tang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10886v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对低精度算子的系统-模型协同设计方法
tldr: 针对大规模推荐模型（LRM）在应用FP8低精度计算时面临的数值敏感和效率挑战，本文提出了LoKA框架。该框架采用系统与模型的协同设计，包含在线统计分析工具LoKA Probe、模型适配组件LoKA Mods以及运行时调度器LoKA Dispatch。LoKA能够精准识别可安全应用FP8的层级，在保证模型精度的前提下，显著提升了LRM在现代GPU上的训练效率和硬件利用率。
selection_source: fresh_fetch
motivation: LRM对数值极其敏感且计算模式特殊，直接应用FP8会导致模型质量下降和训练时间延长，无法直接复用大语言模型的成功经验。
method: 提出LoKA框架，通过在线探测统计分布、协同设计模型组件以增强稳定性，并根据误差分析动态调度最优的FP8算子。
result: LoKA成功在保持模型准确性的前提下，解决了LRM中FP8应用的数值稳定性问题，并最大化了低精度硬件的性能收益。
conclusion: 通过系统与模型的协同设计，LoKA证明了FP8低精度计算在大规模、高敏感度推荐模型任务中的实用性和高效性。
---

## 摘要
最近几代 GPU 通过使用 FP8 等低精度算术提供了显著更高的 FLOPs。虽然 FP8 已成功应用于大语言模型 (LLM)，但在大型推荐模型 (LRM) 中的采用却十分有限。这是因为 LRM 对数值敏感，其计算以小矩阵乘法 (GEMM) 及其后的归一化操作为主，且通常在通信密集型环境中训练。直接将 FP8 应用于 LRM 往往会降低模型质量并延长训练时间。这些挑战是 LRM 工作负载固有的，无法仅通过引入更好的 FP8 算子来解决。相反，需要一种系统-模型协同设计的方法来成功集成 FP8。我们提出了 LoKA（低精度算子应用），这是一个通过三个原则使 FP8 在 LRM 中具有实用性的框架：在真实分布下进行性能分析以确定低精度的安全位置，将模型组件与硬件协同设计以扩展安全范围，以及跨算子库进行编排以最大化收益。具体而言，LoKA Probe 是一种基于统计的在线基准测试方法，用于学习激活和权重的统计信息并量化逐层误差，从而精确识别采用 FP8 的安全与不安全、快速与缓慢的位置。LoKA Mods 是一组可重用的模型适配方案，旨在提高 FP8 的数值稳定性和执行效率。LoKA Dispatch 是一个运行时系统，它利用 LoKA Probe 的统计洞察来选择满足精度要求的、速度最快的 FP8 算子。

## Abstract
Recent GPU generations deliver significantly higher FLOPs using lower-precision arithmetic, such as FP8. While successfully applied to large language models (LLMs), its adoption in large recommendation models (LRMs) has been limited. This is because LRMs are numerically sensitive, dominated by small matrix multiplications (GEMMs) followed by normalization, and trained in communication-intensive environments. Applying FP8 directly to LRMs often degrades model quality and prolongs training time. These challenges are inherent to LRM workloads and cannot be resolved merely by introducing better FP8 kernels. Instead, a system-model co-design approach is needed to successfully integrate FP8. We present LoKA (Low-precision Kernel Applications), a framework that makes FP8 practical for LRMs through three principles: profile under realistic distributions to know where low precision is safe, co-design model components with hardware to expand where it is safe, and orchestrate across kernel libraries to maximize the gains. Concretely, LoKA Probe is a statistically grounded, online benchmarking method that learns activation and weight statistics, and quantifies per-layer errors. This process pinpoints safe and unsafe, fast and slow sites for FP8 adoption. LoKA Mods is a set of reusable model adaptations that improve both numerical stability and execution efficiency with FP8. LoKA Dispatch is a runtime that leverages the statistical insights from LoKA Probe to select the fastest FP8 kernel that satisfies the accuracy requirements.

---

## 论文详细总结（自动生成）

### LoKA: 面向大规模推荐模型的低精度算子应用总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管 FP8 等低精度计算在大型语言模型（LLM）中取得了成功，但在大型推荐模型（LRM）中应用却面临巨大挑战。直接应用 FP8 会导致模型质量显著下降（相对对数损失增加 2.5%）和训练速度变慢（1.3 倍减速）。
*   **研究背景**：LRM 与 LLM 不同，具有数值敏感度高、由大量小矩阵乘法（GEMM）组成、归一化层密集以及通信开销大等特点。传统的低精度库（如 TorchAO）在处理 LRM 真实的、具有长尾分布的数据时，往往会产生巨大的量化误差。
*   **整体含义**：本文提出了 **LoKA** 框架，通过“系统-模型协同设计”的方法，将 FP8 从一种风险转变为提升大规模 LRM 性能的可靠杠杆。

#### 2. 方法论：核心思想与关键技术
LoKA 框架基于三个核心原则：
*   **LoKA Probe（分布感知分析）**：
    *   **核心思想**：在真实数据分布下评估量化风险，而非使用合成的随机张量。
    *   **技术细节**：在线学习激活值（多元高斯分布）和权重（矩阵正态分布）的统计特性。通过 Welford 算法和 Kronecker 因子更新，以极低的开销（<1%）捕捉二阶相关性，从而在离线状态下模拟真实误差（MERE），识别哪些层适合 FP8。
*   **LoKA Mods（精度感知模型-硬件协同设计）**：
    *   **No Bias**：移除线性层中的偏置项，减少数值不稳定性和 FSDP 通信开销。
    *   **BlockNorm**：将归一化（RMSNorm）融合进 GEMM 的算子尾部，按计算块（Block）进行局部归一化，避免了全局同步和昂贵的 HBM 读写。
    *   **Hard Swish**：用分段线性的 Hard Swish 替代含指数运算的 Swish，降低计算开销并提高低精度下的稳定性。
*   **LoKA Dispatch（逐算子内核编排）**：
    *   **核心思想**：没有单一的库能胜过所有场景。
    *   **技术细节**：运行时根据 LoKA Probe 的反馈，在 TorchAO、DeepGEMM 和 FBGEMM 等多个库中动态选择满足精度约束且速度最快的算子实现。

#### 3. 实验设计
*   **数据集/场景**：使用拥有数百亿样本、数千个特征的工业级真实数据集。
*   **模型家族（Benchmark）**：
    *   **Wukong**：包含因子分解机和线性压缩模块。
    *   **Interformer**：基于 Transformer 的异构交互模型。
    *   **ELFM**：集成 DCN、DHEN、DLRM 等多种架构的超大规模基础模型。
*   **对比方法**：以高精度（TF32/BF16）训练为基准，对比了直接应用 TorchAO、DeepGEMM、FBGEMM 等库的效果。

#### 4. 资源与算力
*   **GPU 型号**：涵盖了 NVIDIA H100、B200、GB200 (NVL72) 以及 AMD MI300X、MI350X。
*   **算力规模**：实验在 16 到 256 个 GPU 组成的集群上进行。
*   **训练细节**：针对数百亿样本进行全轨迹训练，确保了实验结果在生产环境下的可参考性。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 种不同的模型家族、5 种硬件架构上进行了广泛测试。
*   **消融实验**：详细分析了 LoKA Mods 中每个组件（No Bias, BlockNorm, Hard Swish）对延迟和损失的影响，并对比了 LoKA Dispatch 与单一库策略的性能差异。
*   **客观性**：实验涵盖了从单算子分析到端到端训练/推理的全过程，且在实际生产系统中进行了部署验证，实验设计充分且公平。

#### 6. 主要结论与发现
*   **性能提升**：LoKA 在生产环境中实现了高达 **20% 的训练吞吐量提升**和 **40% 的推理加速**。
*   **质量无损**：通过协同设计，LoKA 成功消除了 FP8 带来的精度损失，在全训练轨迹上实现了与高精度基准一致的收敛效果。
*   **硬件通用性**：LoKA 的原则在 NVIDIA 和 AMD 的最新硬件上均表现出良好的泛化能力，证明其并非针对特定硬件的过度优化。

#### 7. 优点
*   **实用性极强**：直接针对工业界大规模推荐系统的痛点，且已在服务数十亿用户的生产环境中上线。
*   **协同设计思路**：不局限于优化算子，而是通过修改模型结构（如 BlockNorm）来适配硬件特性，这种跨层优化非常高效。
*   **统计驱动**：LoKA Probe 提供了一种比随机测试更科学的量化误差评估方法。

#### 8. 不足与局限
*   **误差传播建模缺失**：LoKA Probe 主要关注逐层误差，尚无法完全模拟误差在深层网络中的累积与抵消效应，可能导致决策过于保守。
*   **维护成本**：LoKA Dispatch 需要集成多个第三方库，当新算子或新硬件出现时，可能需要额外的人工干预和适配工作。
*   **架构依赖**：虽然涵盖了主流模型，但对于极其特殊的非标准架构，可能仍需定制化的内核开发。

（完）
