---
title: "Reconfigurable Computing Challenge: Real-Time Graph Neural Networks for Online Event Selection in Big Science"
title_zh: 可重构计算挑战：用于大科学在线事件选择的实时图神经网络
authors: "Marc Neu, Frank Baptist, Thomas Lobmaier, Fabio Papagno, Torben Ferber, Jürgen Becker"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10612v1"
tags: ["query:hwnas"]
score: 9.0
evidence: Versal AI引擎上的算子融合与映射自动化设计流
tldr: 针对大科学装置中高粒度探测器带来的实时事件选择挑战，本文提出了一种在AMD Versal VCK190平台上部署动态图神经网络（GNN）的端到端方案。通过结合FPGA逻辑与AI引擎（AIE），并开发半自动化设计流程进行算子融合与并行优化，实现了极高的吞吐量和极低延迟，显著降低了硬件资源消耗，为嵌入式实时触发系统提供了高效的异构计算范式。
selection_source: fresh_fetch
motivation: 随着探测器粒度提升，传统的仅FPGA方案在处理大规模GNN推理时面临严重的资源瓶颈和延迟挑战。
method: 开发了一套基于Python的半自动化流程，将GNN算子映射并优化至AMD Versal的FPGA逻辑与AI引擎异构架构上。
result: "实现了每秒294万次事件的吞吐量和7.15微秒的端到端延迟，相比纯FPGA方案吞吐量提升53%且大幅降低了DSP占用。"
conclusion: 异构计算架构（FPGA+AIE）能有效解决大科学实验中实时GNN部署的性能与资源矛盾，具有极高的应用价值。
---

## 摘要
图神经网络越来越多地被应用于对撞机实验的触发系统中，其中严格的延迟和吞吐量限制使得在嵌入式平台上的部署面临挑战。随着探测器向更高粒度发展，每次推理的输入数量不断增加，仅使用 FPGA 的解决方案面临资源瓶颈。本研究展示了一个在 AMD Versal VCK190 上实时部署动态图神经网络的端到端演示器，用于 Belle II 电磁量能器硬件触发，该方案同时利用了 FPGA 架构和 AI 引擎（AI Engine）阵列。我们开发了一个基于 Python 的半自动化设计流程，涵盖了算子融合、分区、映射、空间并行化和内核级优化。我们的设计在 7.15 微秒的端到端延迟下，实现了每秒 294 万次事件的吞吐量。与仅使用 FPGA 的基准相比，这实现了 53% 的吞吐量提升，同时在 29% 的 AI 引擎阵列利用率下，将 DSP 利用率从 99% 降低到 19%。为了验证部署，我们开发了一个交互式可视化流水线，能够对物理演示器上的推理结果进行实时监控。

## Abstract
Graph neural networks are increasingly adopted in trigger systems for collider experiments, where strict latency and throughput constraints render deployment on embedded platforms challenging. As detectors move towards higher granularity, the number of inputs per inference increase and FPGA-only solutions face resource bottlenecks. This work presents an end-to-end demonstrator for the real-time deployment of a dynamic Graph Neural Network for the Belle II electromagnetic calorimeter hardware trigger on the AMD Versal VCK190, leveraging both FPGA fabric and AI Engine tiles. We develop a Python-based semi-automated design flow covering operator fusion, partitioning, mapping, spatial parallelization, and kernel-level optimization. Our design achieves a throughput of 2.94 million events per second at an end-to-end latency of 7.15 microseconds. Compared to the FPGA-only baseline, this represents a 53% throughput improvement while reducing DSP utilization from 99% to 19% at 29% AI Engine tile utilization. To validate the deployment, an interactive visualization pipeline enables real-time monitoring of inference results on the physical demonstrator.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reconfigurable Computing Challenge: Real-Time Graph Neural Networks for Online Event Selection in Big Science》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在高能物理实验（如日本 Belle II 实验）中，硬件触发系统需要在极短时间内（<10μs）从海量探测器数据中筛选出有意义的“事件”。
*   **核心挑战**：随着探测器粒度提升，输入数据量剧增（从 32 个增加到 128 个以上稀疏输入）。传统的**纯 FPGA 方案**在处理复杂的图神经网络（GNN）时，面临 DSP 资源枯竭（利用率接近 100%）和吞吐量难以提升的瓶颈。
*   **论文意义**：本文旨在探索如何利用 AMD Versal 这种异构 SoC 架构（结合 FPGA 逻辑与 AI 引擎 AIE），在满足严苛延迟约束的同时，提升 GNN 的处理性能并优化资源分配。

### 2. 方法论：核心思想与技术细节
论文开发了一套**半自动化设计流程**，将预训练的量化 GNN 模型（CaloClusterNet）映射到异构硬件上：
*   **算子融合（Operator Fusion）**：将线性层与 ReLU 激活融合，并合并具有相同前驱的并行 Dense 层，以减少 AIE 内存缓冲区的占用。
*   **异构分区（Partitioning）**：采用贪心策略。将具有规则数据访问模式的层（如 Dense、ReLU、Concat）分配给 **AIE**（性能功耗比更高）；将具有不规则、数据依赖访问模式的 GNN 特有层（如 GravNetConv、CPS）保留在 **FPGA 逻辑**中。
*   **空间并行化（Spatial Parallelization）**：通过复制算子链来提升吞吐量，利用穷举搜索找到满足目标吞吐量的最小并行因子 $P$。
*   **内核级优化（Kernel-Level Opt.）**：针对 AIE 内核，使用 `chess_flatten_loop` 替代传统的循环流水线（Loop Pipelining），以消除小规模矩阵运算中的流水线启动开销，确保内核在 1μs 内完成。

### 3. 实验设计
*   **场景与模型**：针对 Belle II 电磁量能器（ECL）触发系统的 CaloClusterNet（动态 GNN）。
*   **对比基准（Benchmark）**：
    1.  **FPGA-only**：之前的纯 FPGA HLS 实现方案。
    2.  **GPU 参考**：使用 NVIDIA L40S 显卡配合 TensorRT 进行推理。
*   **实验版本**：设计了三个迭代版本（Design 1-3），分别引入分区、并行化和内核优化，以验证各阶段的效果。
*   **验证方式**：通过 QKeras 软件仿真、硬件仿真和 VCK190 物理硬件运行，确保位对位（Bit-accurate）的一致性。

### 4. 资源与算力
*   **硬件平台**：AMD Versal VCK190 评估板。
*   **运行频率**：FPGA 逻辑运行在 **250 MHz**，AI 引擎（AIE）运行在 **1.25 GHz**。
*   **GPU 算力**：对比组使用了 **NVIDIA L40S**，但主要用于延迟基准参考，未详细说明训练时长（重点在于推理部署）。
*   **资源占用**：最终方案（Design 3）使用了 53% 的 LUT、19% 的 DSP 和 29% 的 AIE Tiles。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了纯 FPGA、GPU 以及三个不同优化阶段的异构方案。
*   **充分性评价**：实验设计较为充分。不仅测量了宏观的端到端延迟和吞吐量，还详细列出了各项硬件资源（FF, LUT, DSP, BRAM, AIE Tiles）的占用情况。通过消融实验（Design 1 vs 2 vs 3）清晰地展示了算子融合和内核优化对性能的贡献。

### 6. 主要结论与发现
*   **性能飞跃**：异构方案（Design 3）实现了 **2.94 MHz** 的吞吐量（每秒处理 294 万个事件），比纯 FPGA 基准提升了 **53%**。
*   **延迟达标**：端到端延迟为 **7.15 μs**，成功控制在触发系统要求的 10 μs 阈值内。
*   **资源优化**：通过将计算密集型任务移至 AIE，**DSP 利用率从 99% 骤降至 19%**，为 FPGA 逻辑留出了大量空间用于处理其他触发逻辑。
*   **精度保持**：混合精度部署（边界层 16-bit，中间层 8-bit）在保证推理质量的同时优化了性能。

### 7. 优点
*   **异构协同**：成功解决了 GNN 中规则计算与不规则访存共存的部署难题，充分发挥了 AIE 的计算密度和 FPGA 的灵活性。
*   **端到端流程**：提供了一套从 Python 模型到硬件比特流的半自动化工具链，具有较强的工程参考价值。
*   **实时可视化**：开发了交互式 3D 事件显示流水线，增强了实验结果的可验证性和直观性。

### 8. 不足与局限
*   **延迟代价**：相比纯 FPGA 方案，异构方案由于跨分区数据传输（FPGA 与 AIE 之间）引入了约 18% 的额外延迟（虽然仍在 10μs 限制内）。
*   **自动化程度**：流程被称为“半自动化”，意味着在分区策略或特定算子映射上可能仍需要人工干预。
*   **扩展性限制**：目前主要针对特定的 CaloClusterNet 模型，对于其他拓扑结构更复杂的 GNN 模型的通用性有待进一步验证。

（完）
