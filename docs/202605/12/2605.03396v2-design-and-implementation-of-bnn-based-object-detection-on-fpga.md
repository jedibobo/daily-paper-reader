---
title: Design and Implementation of BNN-Based Object Detection on FPGA
title_zh: 基于 BNN 的 FPGA 目标检测设计与实现
authors: "Xuyu Zhao, Yunpeng Wu, Mengyuan Zhu, Haoyu Huang, Xiaoyu Xu, Yanjing Li, Gaolong Zhang, Baochang Zhang"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03396v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 硬件友好型BNN目标检测器的FPGA实现
tldr: "本研究在低成本FPGA上实现了一种基于二值神经网络（BNN）的类YOLOv3-tiny目标检测器。该设计采用1位权重和8位激活的混合量化策略，通过Verilog RTL实现了包括卷积、池化及检测头在内的完整硬件加速器。通过将通道补偿参数融合进处理单元，优化了计算效率。在VOC数据集上实现了39.6%的mAP50，且硬件仿真结果与ONNX模型高度一致，为低功耗嵌入式目标检测提供了高效方案。"
selection_source: fresh_fetch
motivation: 旨在低成本FPGA平台上实现高效、低功耗的实时目标检测系统。
method: 采用1位权重和8位激活的BNN架构，通过Verilog RTL实现硬件加速，并将通道补偿参数融合至PE单元中。
result: "在VOC数据集上达到39.6%的mAP50，计算量仅为0.098 GFLOPs，硬件仿真与软件模型相关性高达0.999964。"
conclusion: 该设计证明了BNN在低成本FPGA上实现高性能目标检测的可行性，具有极高的计算效率和精度保持能力。
---

## 摘要
本文在低成本 FPGA 上实现了一种基于二值神经网络（BNN）的类 YOLOv3-tiny 目标检测器。该网络以 320*320*3 的 RGB 图像作为输入。其主要卷积层使用 1 位权重和 8 位激活，而第一层卷积（Conv1）和最终检测头则使用定点标准卷积。从训练好的 ONNX 模型中提取权重、偏置和量化参数，将其转换为定点数，打包成 COE 文件，并存储在 Vivado BRAM ROM 中。硬件完全采用 Verilog RTL 编写，包括填充（padding）、行缓冲（line buffering）、二值卷积、量化后处理、最大池化以及检测头计算。对于 Mul_prev 按输入通道索引且 Div_current 按输出通道索引的层，Mul_prev 被融合进 BNN 处理单元（PE）中，以便在累加过程中应用通道级补偿。在 VOC 数据集上，该模型以 0.098 GFLOPs 的计算量和 0.74 M 的参数量实现了 39.6% 的 mAP50。RTL 仿真结果显示，最终的原始检测输出与相应的 ONNX 节点相比，相关系数达到 0.999964，平均绝对误差为 0.020027。

## Abstract
This paper implements a Binary Neural Network (BNN) based YOLOv3-tiny-like object detector on a low-cost FPGA. The network takes 320*320*3 RGB images as input. Its main convolution layers use 1-bit weights and 8-bit activations, while Conv1 and the final detection head use fixed-point standard convolutions. From the trained ONNX model, weights, biases, and quantization parameters are extracted, converted to fixed point, packed into COE files, and stored in Vivado BRAM ROMs. The hardware is written fully in Verilog RTL and includes padding, line buffering, binary convolution, quantization post-processing, max pooling, and detection-head computation. For layers where Mul_prev is indexed by input channel and Div_current by output channel, Mul_prev is fused in-to the BNN PE so that channel-wise compensation is applied during accumulation. On VOC, the model obtains 39.6% mAP50 with 0.098 GFLOPs and 0.74 M parameters. RTL simulation shows that the final raw detection output reaches a correlation coefficient of 0.999964 and a mean absolute error of 0.020027 against the corresponding ONNX node.

---

## 论文详细总结（自动生成）

这篇论文详细介绍了在低成本 FPGA 平台上实现基于二值神经网络（BNN）的目标检测器的全过程。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在资源极度受限的嵌入式 FPGA（如 PYNQ-Z2）上部署高分辨率的目标检测网络。
*   **研究动机**：传统的深度卷积神经网络（CNN）计算量大、内存占用高，难以直接在低成本边缘设备上运行。虽然全二值网络（W1A1）能极大压缩模型，但在目标检测任务中表现不稳定。
*   **整体含义**：本文提出了一种 **W1A8（1位权重，8位激活）** 的混合量化方案，通过手写 Verilog RTL 实现了类 YOLOv3-tiny 的检测器，旨在平衡硬件成本、计算速度与检测精度。

### 2. 方法论
*   **核心思想**：利用 W1A8 量化减少存储和乘法运算，将权重二值化为 $\{-1, +1\}$，从而将卷积操作简化为受符号控制的加减法累加。
*   **关键技术细节**：
    *   **混合量化策略**：首层（Conv1）和末层（检测头）采用定点标准卷积以保精度，中间层采用 W1A8。
    *   **通道级补偿融合**：为了解决量化带来的误差，将前一层的输入通道缩放因子（Mul_prev）直接融合进当前层的处理单元（PE）中，在累加过程中进行补偿。
    *   **硬件架构设计**：完全采用 **Verilog RTL** 编写（而非 HLS），实现了包括 Padding 适配器、行缓冲区（LineBuffer）、W1A8 PE 阵列、后处理模块和最大池化模块在内的完整流水线。
    *   **参数部署**：从训练好的 ONNX 模型提取参数，转换为定点数并打包成 COE 文件，存储在 FPGA 的 BRAM ROM 中。

### 3. 实验设计
*   **数据集**：使用 **PASCAL VOC 2007** 测试集进行精度评估。
*   **Benchmark（基准）**：对比了多种轻量级模型，包括 MobileNet-SSD、YOLOv5s/n、YOLOv3-tiny、YOLOv4-tiny 和 YOLOv2-tiny。
*   **验证方法**：
    *   **数值一致性验证**：将 RTL 仿真输出与 ONNX Runtime 的中间节点进行逐层对比。
    *   **硬件资源评估**：在 Xilinx XC7Z020 FPGA 上进行综合与实现，统计资源占用和功耗。

### 4. 资源与算力
*   **推理硬件**：Xilinx Zynq-7000 系列 **XC7Z020** (PYNQ-Z2 开发板)。
*   **算力指标**：模型计算量仅为 **0.098 GFLOPs**，参数量 **0.74 M**。
*   **训练算力**：文中**未明确说明**训练该模型所使用的 GPU 型号、数量及训练时长。
*   **推理性能**：时钟频率 30 MHz，推理延迟约 795.58 ms，吞吐量为 **1.26 FPS**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了模型精度对比实验（表5）。
    *   进行了逐层的 RTL 与软件模型数值对齐实验（表6）。
    *   进行了 FPGA 资源占用与功耗分析（表7）。
*   **充分性评价**：实验设计较为客观，特别是**数值一致性验证**（相关系数达 0.999964）充分证明了硬件实现的正确性。但由于目标是极低成本平台，对比的模型大多在算力上远超本项目，且未在多个数据集（如 COCO）上进行测试，实验覆盖面略窄。

### 6. 主要结论与发现
*   **精度与效率平衡**：该模型在 VOC 数据集上达到 **39.6% mAP50**，虽然低于现代大型模型，但优于早期的 YOLOv2-tiny（35.4%），且计算量仅为后者的约 1/70。
*   **硬件可行性**：证明了在 4.9 Mb BRAM 限制的 FPGA 上实现完整目标检测流水线的可行性。
*   **高保真度**：通过 RTL 实现的定点化推理与软件浮点/定点模型高度一致，平均绝对误差仅为 0.02 左右。

### 7. 优点
*   **精细的硬件控制**：放弃 HLS 采用手写 RTL，能够精确控制 ROM 延迟、流水线状态和位宽，最大限度利用 BRAM 资源。
*   **补偿机制创新**：将通道级缩放因子融合进 PE 累加过程，有效缓解了二值化带来的精度损失。
*   **极低资源占用**：参数量和计算量极小，非常适合对存储空间极其敏感的嵌入式应用。

### 8. 不足与局限
*   **吞吐量较低**：1.26 FPS 的速度尚不足以满足实时视频流检测的需求，瓶颈在于 30 MHz 的时钟频率和 PE 的并行度。
*   **小目标检测能力弱**：由于采用了单一的 10x10 检测头且模型极度简化，对小目标的召回率和定位精度有限。
*   **精度差距**：与主流轻量级模型（如 YOLOv5n 的 67.8% mAP）相比，精度仍有较大差距，仅适用于对精度要求不高的基础检测场景。

（完）
