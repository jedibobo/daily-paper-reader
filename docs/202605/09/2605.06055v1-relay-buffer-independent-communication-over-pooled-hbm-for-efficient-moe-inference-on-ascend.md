---
title: Relay Buffer Independent Communication over Pooled HBM for Efficient MoE Inference on Ascend
title_zh: 基于池化 HBM 的无中继缓冲区通信，用于昇腾平台上的高效 MoE 推理
authors: "Tianlun Hu, Tiancheng Hu, Shengsheng Litang, Sheng Wang, Xiaoming Bao, Yuxing Li, Wei Wang, Zhongzhe Hu, Lijun Li, Hongwei Sun, Jingbin Zhou\\\\"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06055v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 昇腾硬件上MoE的内存带宽与通信优化
tldr: 针对昇腾系统上混合专家模型（MoE）推理中的通信瓶颈，本文提出了一种无中继缓存的通信设计。该设计利用全局池化高带宽内存（HBM）和对称内存分配，消除了中间中继和重排缓存，实现了专家窗口的直接读写。通过针对预填充和解码阶段优化的调度方案，显著降低了调度与合并延迟，提升了首字生成时间（TTFT）并优化了推理性能。
selection_source: fresh_fetch
motivation: 传统的MoE通信依赖显式的中继和重排缓存，导致在昇腾系统上进行大规模Token交换时产生显著的额外开销。
method: 利用全局池化HBM实现无中继缓存设计，通过直接将数据放置到目标专家窗口和从远程专家窗口读取，并针对预填充和解码阶段设计了不同的调度策略。
result: 实验表明该方法在昇腾平台上有效降低了Dispatch和Combine的延迟，缩短了首字生成时间（TTFT），并扩大了实际延迟约束下的调度空间。
conclusion: 在具有全局可寻址设备内存的平台上，减少专家执行前后的中间缓存和输出恢复是加速MoE推理的有效方向。
---

## 摘要
混合专家模型 (MoE) 推理需要在设备间进行大规模的 token 交换，使得分发 (dispatch) 和合并 (combine) 成为预填充 (prefill) 和解码 (decode) 阶段的主要瓶颈。除了网络传输外，路由驱动的布局转换、临时中继和输出恢复也会增加大量开销。现有的 MoE 通信路径通常以缓冲区为中心，在集合通信前后使用显式的进程间中继和重排序缓冲区。本报告提出了一种针对昇腾系统 MoE 推理加速的无中继缓冲区通信设计。该设计围绕直接放置到目标专家窗口和从远程专家窗口直接读取来重新组织分发和合并。基于全局池化的高带宽内存 (HBM) 和对称内存分配，它消除了大部分中间中继和重排序缓冲区，仅保留轻量级的控制状态，包括计数、偏移量和同步元数据。我们将该设计实例化为 MoE 推理主要阶段的两种调度方案：一种是具有更丰富规划状态、面向吞吐量的预填充调度，另一种是面向延迟敏感执行的紧凑解码调度。在基于昇腾的 MoE 工作负载上的实验表明，两种设置下的分发和合并延迟均有所降低。在推理服务层面，该实现缩短了首字延迟 (TTFT)，保持了具有竞争力的逐 token 延迟 (TPOT)，并在实际延迟约束下扩大了可行的调度空间。这些结果表明，在具有全局可寻址设备内存的平台上，减少专家执行前后的中间缓冲和输出恢复是加速 MoE 推理的一个有效方向。

## Abstract
Mixture-of-Experts (MoE) inference requires large-scale token exchange across devices, making dispatch and combine major bottlenecks in both prefill and decode. Beyond network transfer, routing-driven layout transformation, temporary relay, and output restoration can add substantial overhead. Existing MoE communication paths are often buffer-centric, using explicit inter-process relay and reordering buffers around collective transfer. This report presents a relay-buffer-free communication design for MoE inference acceleration on Ascend systems. The design reorganizes dispatch and combine around direct placement into destination expert windows and direct reading from remote expert windows. Built on globally pooled high-bandwidth memory and symmetric-memory allocation, it removes most intermediate relay and reordering buffers while retaining only lightweight control state, including counts, offsets, and synchronization metadata. We instantiate the design as two schedules for the main phases of MoE inference: a prefill schedule with richer planning state for throughput-oriented execution, and a compact decode schedule for latency-sensitive execution. Experiments on Ascend-based MoE workloads show reduced dispatch and combine latency in both settings. At the serving level, the implementation improves time to first token (TTFT), preserves competitive time per output token (TPOT), and enlarges the feasible scheduling space under practical latency constraints. These results indicate that, on platforms with globally addressable device memory, reducing intermediate buffering and output restoration around expert execution is an effective direction for accelerating MoE inference.

---

## 论文详细总结（自动生成）

这篇论文由华为技术有限公司的研究团队撰写，重点探讨了如何在昇腾（Ascend）硬件平台上通过优化通信机制来加速混合专家模型（MoE）的推理过程。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在分布式 MoE 推理中，Token 在不同设备间的“分发（Dispatch）”和“合并（Combine）”是主要的性能瓶颈。
*   **研究动机**：传统的通信方式是“以缓冲区为中心”的，即 Token 需要先打包、通过中继缓冲区传输、再进行重排序和恢复。这种方式产生了大量的内存流量、额外的 HBM（高带宽内存）占用以及显著的延迟，尤其是在长文本和高并发推理场景下。
*   **整体含义**：论文旨在利用昇腾平台的硬件特性（全局池化 HBM），消除中间中继缓冲区，实现一种更直接、更高效的通信路径。

### 2. 论文提出的方法论
*   **核心思想**：提出“无中继缓冲区（Relay-Buffer-Free）”通信设计。利用全局池化 HBM 和对称内存分配，让数据直接写入目标位置或从远程位置直接读取。
*   **关键技术细节**：
    *   **全局池化 HBM**：通过 `AclShmem` 或 `Memfabric` 实现跨 Rank 的设备内存可见，建立统一的虚拟地址空间。
    *   **直接放置与直接读取**：分发阶段直接将 Token 写入目标专家的内存窗口；合并阶段由消费者直接从远程专家窗口读取结果并进行加权还原。
    *   **轻量级控制状态**：用极小的计数器、偏移量和同步符号取代大尺寸的中间张量。
*   **调度流程**：
    *   **预填充（Prefill）调度**：针对高吞吐量设计，包含显式的布局分析（Layout）和通知（Notify）阶段，以优化大规模 Token 的规划。
    *   **解码（Decode）调度**：针对低延迟设计，将控制逻辑压缩进内核，减少主机侧参与，缩短关键路径。

### 3. 实验设计
*   **测试场景**：涵盖了典型的 MoE 推理任务，包括 DeepSeek 3.1/3.2 和 Qwen-235B 等模型配置。
*   **Benchmark（基准）**：对比了开启 HCCL（华为集合通信库）优化的 **DeepEP**（DeepSeek 开发的专家并行通信库）作为基线。
*   **对比维度**：
    *   **算子级**：不同 Token 数量、不同隐藏层维度（4096, 7168）、是否量化下的分发/合并延迟。
    *   **服务级**：首字生成时间（TTFT）和逐 Token 生成时间（TPOT）。
    *   **调度空间**：在给定延迟约束下的可行配置范围。

### 4. 资源与算力
*   **硬件平台**：明确指出基于 **Ascend（昇腾）系统**。
*   **算力细节**：文中未详细列出具体的 NPU 型号（如 910B 等）或具体的集群规模（如多少节点），但强调了利用了昇腾特有的对称内存分配能力。实验主要关注推理性能，未涉及模型训练时长。

### 5. 实验数量与充分性
*   **实验规模**：进行了算子级微基准测试（涵盖多种 Batch Size 和隐藏维度）以及端到端的服务模拟。
*   **充分性评价**：
    *   **算子测试较为充分**：对比了量化与非量化、预填充与解码等多种组合，证明了方法的普适性。
    *   **服务测试属于初步验证**：论文自述为“早期实验结果（early set of experiments）”，虽然涵盖了 TTFT 和 TPOT 等核心指标，但模型覆盖范围（主要集中在 DeepSeek 和 Qwen 架构）和更大规模集群的扩展性数据仍有提升空间。
    *   **客观性**：对比基线选择了目前业内领先的 DeepEP，具有较强的说服力。

### 6. 论文的主要结论与发现
*   **延迟降低**：在解码阶段，分发算子的加速比最高可达 31%~40%，合并算子稳定在 22%~28% 左右。
*   **服务质量提升**：在 DeepSeek 3.1 场景下，TTFT 从 11.2 秒显著降至 6.8 秒，同时保持了 TPOT 在目标范围内。
*   **调度灵活性**：由于通信效率提高，系统在满足延迟约束的前提下，能够支持更多的并发配置或更长的上下文处理。

### 7. 优点
*   **硬件感知设计**：紧密结合昇腾 HBM 池化特性，将通信问题转化为内存寻址问题，思路清晰且高效。
*   **内存效率高**：消除了巨大的中继缓冲区，降低了推理时的瞬时内存压力，这对长文本推理至关重要。
*   **读优化（Read-favored）**：针对昇腾硬件特性优化的读取模式，有效提升了合并阶段的性能。

### 8. 不足与局限
*   **平台依赖性强**：该方法高度依赖昇腾的全局池化内存特性，在缺乏类似硬件支持（如 NVLink 域外通信或非统一内存访问架构）的平台上难以直接移植。
*   **内存足迹测量缺失**：虽然理论上减少了缓冲区，但文中缺乏具体的 HBM 节省数值对比。
*   **模型多样性有限**：实验主要基于 DeepSeek 和 Qwen 架构，对于其他稀疏化策略（如不同 Top-K 策略或更复杂的路由算法）的兼容性讨论较少。

（完）
