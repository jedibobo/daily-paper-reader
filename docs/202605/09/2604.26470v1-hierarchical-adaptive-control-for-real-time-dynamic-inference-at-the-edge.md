---
title: Hierarchical adaptive control for real-time dynamic inference at the edge
title_zh: 边缘实时动态推理的层级自适应控制
authors: "Francesco Daghero, Mahyar Tourchi Moghaddam, Mikkel Baun Kjærgaard"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26470v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对具有延迟、功耗和内存限制的边缘节点的动态机器学习模型
tldr: 针对工业边缘计算中动态机器学习模型难以应对数据漂移和硬件资源波动的问题，本文提出一种两层分级自适应架构。该架构在全局层调度分类器级联以满足延迟和内存约束，在节点层通过本地控制器实时调整专用预测器的开关，以应对数据分布变化。实验表明，该方法在保持高精度的同时，显著降低了推理延迟和能耗，提升了边缘设备的运行效率。
selection_source: carryover_cache
motivation: 动态机器学习模型在边缘部署时，常因预设超参数无法适应实际运行中的数据漂移和资源波动而导致性能下降。
method: 提出一种结合全局调度器（配置模型级联）与本地控制器（实时监控并调整专用预测器状态）的两层分级自适应控制架构。
result: "在嵌入式硬件上的实验显示，该方法相比静态基准可降低高达2.45倍的延迟和2.86倍的能耗，且准确率损失低于4%。"
conclusion: 该分级控制方案有效提升了边缘推理的能效与实时性，并增强了系统在动态环境下的鲁棒性。
---

## 摘要
工业系统日益依赖机器学习（ML），并运行在必须满足严格延迟、能耗和内存限制的异构节点上。动态机器学习模型在运行时重新配置其计算占用空间，有望在适度的精度权衡下实现高能效和更低的平均延迟；然而，由于其依赖额外的超参数，部署过程非常复杂。这些控制精度与平均延迟权衡的超参数通常在校准数据集上进行调整，而该数据集必须与测试时的分布相匹配。这一假设在现实场景中很少成立，从而导致运行状态不佳，甚至可能劣于静态模型。我们提出了一种双层自适应架构，共同优化模型和系统决策。在全局层面，调度器为每个边缘节点配置并部署由轻量级专用模型和通用后备模型组成的级联分类器，以满足延迟和内存约束。在节点层面，局部控制器跟踪数据漂移和硬件资源，启用或禁用专用预测器（SP），以在多变的环境下保持高能效并避免违反延迟约束。这种设计允许更长的运行时间，而无需强制执行全局重新部署步骤，并能在远程全局控制器无法连接的情况下实现高效执行。我们在受控的分布失配场景下，通过两个数据集对该方法进行了评估。结果显示，与静态基准相比，单次推理的平均延迟降低了高达 2.45 倍，能耗降低了高达 2.86 倍，而精度下降不到 4%。我们的贡献包括：（1）一种受预算限制的 SP 级联公式，可保证最坏情况下的延迟约束；（2）一种在数据和资源变化下保持效率的层级控制器；以及（3）在嵌入式硬件上的实验评估。

## Abstract
Industrial systems increasingly depend on Machine Learning (ML), and operate on heterogeneous nodes that must satisfy tight latency, energy, and memory constraints. Dynamic ML models, which reconfigure their computational footprint at runtime, promise high energy efficiency and lower average latency for modest accuracy tradeoffs; however, their deployment is complex due to the additional hyperparameters they rely on. These hyperparameters, controlling the accuracy versus average latency tradeoff, are often tuned on a calibration dataset that must match the test time distribution, an assumption that rarely holds in real-world scenarios, leading to suboptimal operational conditions, possibly below static models. We propose a two-tier adaptive architecture that co-optimizes model and system decisions. At the global level, a scheduler configures and deploys, for each edge node, a cascade of classifiers composed of lightweight specialized models and a generalist fallback, satisfying latency and memory constraints. At the node level, a local controller tracks data drifts and hardware resources, enabling or disabling specialized predictors (SP) to preserve high energy efficiency and avoid latency-constraint violations under varying conditions. This design allows longer operating times without forcing a global redeployment step, and enables efficient execution in case of an unreachable remote global controller. We evaluate the approach on two datasets under controlled distribution mismatch scenarios, showing average per-inference reductions of latency up to 2.45x and energy up to 2.86x, with less than 4% accuracy drop compared to static baselines. Our contributions are:(1) a budgeted SP-cascade formulation that preserves worst-case latency constraints;(2) a hierarchical controller that maintains efficiency under data and resource changes; and (3) an experimental evaluation on embedded hardware.

---

## 论文详细总结（自动生成）

这篇论文针对边缘计算环境下的实时动态推理提出了一种层级自适应控制架构。以下是对该论文的详细总结：

### 1. 核心问题与研究动机
*   **核心问题**：在工业边缘场景中，机器学习模型需在严格的延迟、能耗和内存约束下运行。虽然“动态推理”（根据输入复杂度调整计算路径）能提高效率，但其超参数通常在静态验证集上预调优。
*   **研究动机**：在实际部署中，**数据分布漂移**（如类别频率变化）和**硬件资源波动**（如热限制、后台任务）会导致预设的动态策略失效，甚至造成延迟违规或效率低于静态模型。因此，系统需要一种能够实时感知环境并自我调整的机制。

### 2. 方法论
论文提出了一种**双层自适应架构**，将模型配置与系统决策共同优化：
*   **核心思想**：将多分类任务分解为多个轻量级的“专用预测器”（Specialized Predictors, SPs，即一对多二分类器）和一个“后备模型”（Fallback, FB）。通过级联这些模型，让简单样本尽早退出。
*   **全局控制器 (GC)**：
    *   **任务调度**：利用束搜索（Beam Search）在满足内存和最坏情况延迟约束的前提下，为每个节点选择最优的 SP 级联顺序。
    *   **在线更新**：低频运行，根据节点反馈的数据分布，决定是否向节点推送新的 SP 模块。
*   **本地控制器 (LC)**：
    *   **实时监控**：高频运行，监控 CPU、内存、温度及预测类别的指数移动平均（EMA）。
    *   **动态调整**：根据类别频率对 SP 进行重排序、启用或禁用。
    *   **安全保障**：执行每步推理前的安全检查，确保 `LC开销 + 已启用SP总延迟 + FB延迟 ≤ 截止日期`，若不满足则强制缩减级联深度。

### 3. 实验设计
*   **数据集**：
    *   **SCD (土壤分类数据集)**：7类，样本不平衡，模拟工业感知任务。
    *   **CIFAR-10**：通用视觉任务，用于验证复杂场景下的泛化性。
*   **实验场景**：
    *   **Base**：平稳分布。
    *   **Mismatch Minor**：级联中已有的某个 SP 对应类别频率激增。
    *   **Mismatch Major**：级联中未包含的某个类别频率激增（最困难的情况）。
*   **Benchmark (对比方法)**：
    *   **Static**：仅运行完整的 MobileNetV2 后备模型。
    *   **Dynamic**：使用固定顺序且无自适应能力的级联模型。
    *   **Ours**：本文提出的层级自适应架构。

### 4. 资源与算力
*   **边缘节点**：树莓派 4B (Raspberry Pi 4B)，限制为单核运行，使用 ONNX Runtime 执行推理。
*   **全局控制器/训练端**：Asus NUC (Intel i7 四核, 16GB RAM)，使用 PyTorch 2.5 进行训练。
*   **模型规格**：SP 采用 LeNet 架构（约 0.65M MACs），FB 采用 MobileNetV2（31M-94M MACs）。
*   **能耗测量**：使用在线 USB 功率计实时监测树莓派的功耗。
*   **说明**：文中未明确给出具体的训练时长，但由于 SP 架构极小且独立训练，计算成本较低。

### 5. 实验数量与充分性
*   **实验规模**：针对两个数据集分别测试了三种分布偏移场景，并展示了推理过程中的实时延迟波动曲线。
*   **充分性**：实验涵盖了从平稳到剧烈漂移的多种情况，并对比了延迟、能耗和平衡准确率（Balanced Accuracy）。
*   **客观性**：通过在真实嵌入式硬件（而非模拟器）上运行，并考虑了 Python 实现带来的额外开销，实验结果具有较高的工程参考价值。

### 6. 主要结论与发现
*   **能效显著提升**：在 SCD 数据集上，相比静态模型，平均延迟降低高达 **2.45倍**，能耗降低 **2.86倍**。
*   **精度损失微小**：在实现大幅加速的同时，准确率下降控制在 **4% 以内**。
*   **自适应的必要性**：在分布发生重大偏移（Mismatch Major）时，传统动态推理的效率会大幅下降，而本文的层级控制能通过重新配置级联顺序恢复效率，能耗比传统动态方法再降低 39%。

### 7. 优点
*   **层级解耦**：将长期的模型部署（GC）与短期的实时响应（LC）分离，既保证了全局最优性，又实现了毫秒级的反应速度。
*   **最坏情况保障**：明确引入了硬实时约束公式，这对于工业控制等安全敏感领域至关重要。
*   **部署灵活**：SP 模块可以独立训练和按需推送，无需像“早期退出网络”那样重新训练整个骨干网络。

### 8. 不足与局限
*   **软件开销**：作者提到 LC 和 GC 的 Python 实现引入了一定开销，在极高性能要求的场景下可能需要 C++ 重构。
*   **任务类型单一**：目前仅在图像分类任务上验证，尚未探讨目标检测或序列处理等更复杂的任务。
*   **SP 架构固定**：目前所有 SP 使用相同的 LeNet 架构，未根据不同类别的识别难度进行异构 SP 设计。

（完）
