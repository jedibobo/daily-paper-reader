---
title: "Efficient, VRAM-Constrained xLM Inference on Clients"
title_zh: 客户端上高效且显存受限的 xLM 推理
authors: "Aditya Ukarande, Deep Shekhar, Marc Blackstein, Ram Rangan"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26334v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 在显存限制下最大化计算利用率的CPU-GPU混合调度
tldr: 针对客户端设备显存受限的问题，本文提出了一种名为“流水线分片”的CPU-GPU混合调度技术，旨在实现大语言模型（LLM）和视觉语言模型（VLM）的高效、无损推理。该方法结合了子层级模型分片、CPU卸载、流水线拷贝计算及显存张量优先级放置等策略，显著优化了首字生成时间和推理速度。此外，针对VLM引入了VLMOpt优化，大幅降低了显存需求，为NVIDIA相关产品的AI推理提供了关键技术支撑。
selection_source: carryover_cache
motivation: 客户端系统显存有限，难以高效运行高精度的大规模语言和视觉模型，亟需优化推理效率。
method: 提出流水线分片技术，通过子层级分片、CPU-GPU混合调度及流水线化拷贝与计算来优化推理性能。
result: LLM的首字生成时间提升达6.7倍，推理速度提升达30倍，且VLM的显存需求降低了10倍。
conclusion: 该研究通过创新的混合调度策略，成功解决了客户端设备在显存受限环境下运行高性能AI模型的瓶颈。
---

## 摘要
为了开启下一轮客户端 AI 创新，迫切内需在客户端系统上实现高精度大语言模型 (LLM) 和视觉语言模型 (VLM)（统称为 xLM）的高效、无损推理。为此，我们提出了流水线分片 (pipelined sharding)，这是一种新颖的、基于基准测试分析引导的 CPU-GPU 混合调度技术，旨在为稠密型和混合专家 (MoE) LLM 实现高效的显存受限推理。通过结合子层级模型分片、CPU 卸载、流水线拷贝-计算以及显存中的优先级张量放置，该技术优化了首字延迟 (TTFT) 和每秒生成词数 (TPS) 指标，同时能够灵活适应系统和推理条件。针对高效、高精度的 VLM 推理，我们将流水线分片与 llama.cpp 实现的三种成熟先验思路（统称为 VLMOpt）相结合，即视觉张量 CPU 卸载、Flash Attention 以及视觉与语言模型显存重叠规避。这些增强功能旨在提升 NVIDIA 两款重要产品未来版本中的客户端 xLM 推理性能：游戏内推理软件开发工具包 (IGI SDK) 和 Cosmos-Reason1 (CR1) 物理 AI 推理 VLM。我们在多个模型和客户端系统上进行了严格评估，亮点包括：在交互式使用中，LLM 的 TTFT 提升高达 6.7 倍，TPS 提升高达 30 倍，CR1 推理的显存需求降低了 10 倍；而在批处理模式下，吞吐量提升高达 8.2 倍（均与各自的激进基准线相比）。本文已被第九届 MLSys 会议（工业轨道，2026年）接收。代码和工件可在以下地址获取：https://github.com/deepshnv/pipeshard-mlsys26-ae

## Abstract
To usher in the next round of client AI innovation, there is an urgent need to enable efficient, lossless inference of high-accuracy large language models (LLMs) and vision language models (VLMs), jointly referred to as xLMs, on client systems. To address this, we present pipelined sharding, a novel, benchmark-profile-guided CPU-GPU hybrid scheduling technique to achieve efficient, VRAM-constrained inference for both dense and mixture-of-experts (MoE) LLMs. Using a combination of model sharding at the sub-layer level, CPU offloading, pipelined copy-compute, and prioritized tensor placement in VRAM, it optimizes both time-to-first-token (TTFT) and tokens per second (TPS) metrics, while flexibly adapting to system and inference conditions. For efficient, high-accuracy VLM inference, we combine pipelined sharding with a llama$.$cpp implementation of three well-understood prior ideas (jointly called VLMOpt), namely, vision tensor CPU offloading, flash attention, and vision and language model VRAM overlap avoidance.   These enhancements are targeted at improving client xLM inference in future releases of two important NVIDIA products - the In-Game Inferencing software development kit (IGI SDK) and the Cosmos-Reason1 (CR1) physical AI reasoning VLM. Highlights from our rigorous evaluation spanning multiple models and client systems include: for interactive use, TTFT improves by up to 6.7x and TPS by up to 30x for LLMs, and CR1 inference's VRAM demand is down by 10x, while in batched mode, throughput improves by up to 8.2x, all compared to their respective aggressive baselines. This paper is accepted at the 9th MLSys Conference (Industry Track), 2026. Code and artifact available at: https://github.com/deepshnv/pipeshard-mlsys26-ae

---

## 论文详细总结（自动生成）

这篇论文由 NVIDIA 的研究团队发表于 MLSys 2026（工业轨道），重点解决了在显存（VRAM）受限的客户端设备（如个人电脑、笔记本）上高效运行高精度大模型的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在显存有限的客户端 GPU 上，实现高精度大语言模型（LLM）和视觉语言模型（VLM）的**高效、无损**推理。
*   **研究动机**：
    *   **隐私与成本**：云端推理成本高且存在隐私隐患，用户倾向于本地推理。
    *   **硬件限制**：客户端 GPU 显存通常远小于模型参数量（如 77GB 的模型运行在 16GB 显存的显卡上）。
    *   **现有方案不足**：现有的量化、剪枝等方案是有损的；而现有的 CPU-GPU 混合调度方案通常无法同时兼顾交互式/批处理模式、MoE 架构、高分辨率 VLM 以及动态系统环境（如 CPU 线程数、PCIe 带宽的变化）。

### 2. 方法论：流水线分片 (Pipelined Sharding) 与 VLMOpt
该论文提出了一种基于基准测试分析引导的混合调度技术，核心分为三个阶段：

*   **安装阶段 (Install Phase)**：在软件安装时运行一次基准测试，测量 CPU 和 GPU 在不同量化、线程数、PCIe 负载下的算力（FLOPS）和带宽，建立性能剖析数据库。
*   **规划阶段 (Planning Phase)**：
    *   **子层级分片**：将模型在子层级（如 Attention 和 FFN 边界）进行切割，以便更精细地控制张量放置。
    *   **优先级放置**：根据优先级（Attention > KV Cache > FFN > Outputs）将张量固定在显存中。
    *   **三种调度方案**：针对剩余未固定的层，评估三种计划：
        1.  **GPU-only**：所有层在 GPU 执行，权重通过 PCIe 实时流式传输。
        2.  **Static (静态分割)**：部分层固定在 CPU 执行，部分在 GPU 执行，减少 PCIe 传输。
        3.  **Dynamic (动态流水线)**：利用 CPU 计算与 GPU 权重传输的重叠（Overlap）。
    *   **Token Tier 设计**：为不同的 Token 数量级（如 1, 64, 512 等）预生成最优调度表。
*   **推理阶段 (Inference Phase)**：根据当前批处理的 Token 总数，从预生成的表中查找并执行最优调度方案。
*   **VLMOpt (针对 VLM)**：
    *   **视觉张量 CPU 卸载**：将视觉编码器权重放在系统内存。
    *   **视觉 Flash Attention**：通过分块计算降低高分辨率图像处理时的显存峰值。
    *   **显存重叠规避**：确保视觉和语言部分的显存分配在时间上错开。

### 3. 实验设计
*   **测试模型**：
    *   **稠密 LLM**：Mistral-Nemo (4B, 8B)。
    *   **混合专家模型 (MoE)**：Qwen3-30B (A3B), Qwen3-235B (A22B)。
    *   **视觉模型**：Nemotron-Vision-4B, Cosmos-Reason1 (CR1)。
*   **硬件场景 (Benchmark)**：
    *   **cli1 (笔记本)**：Intel Ultra 7, 16GB RAM, RTX 3500 Ada。
    *   **cli2 (主流桌面)**：Ryzen 7, 128GB RAM, RTX 5070 Ti (16GB VRAM)。
    *   **cli3 (高端桌面)**：EPYC, 256GB RAM, RTX 5090 (32GB VRAM)。
*   **对比方法**：以 `llama.cpp` 的激进手动层卸载（Manual Offloading）作为 Baseline。

### 4. 资源与算力
*   **硬件使用**：实验涵盖了从移动端 GPU 到高端消费级 GPU（RTX 5090）。
*   **训练/运行时间**：论文未涉及模型训练（侧重推理优化）。安装阶段的基准测试剖析仅需约 15 分钟。
*   **存储需求**：剖析数据库仅占 170KB，但模型权重（如 Qwen-235B）需要约 175GB 的磁盘空间。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 105 种配置的 Oracle 对比实验，涵盖了不同的 PCIe 代际（Gen3/4/5）、CPU 线程数（1-16）、上下文长度（1K-64K）和显存预算（2G-32G）。
*   **维度丰富**：包含了消融实验（验证调度器选择的准确性）、敏感性分析（PCIe 带宽、CPU 线程影响）以及实际应用场景（与《赛博朋克 2077》游戏并发运行的性能表现）。
*   **客观性**：实验在 Windows 和 Linux 双系统下验证，并提供了开源工件以供复现，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能飞跃**：在交互式模式下，LLM 的首字生成时间（TTFT）提升高达 **6.7 倍**，每秒生成词数（TPS）提升高达 **30 倍**。
*   **显存压缩**：对于 Cosmos-Reason1 (CR1) 模型，显存需求降低了 **10 倍**（从 20GB 降至 2GB），且仍能保持高效运行。
*   **批处理优化**：在批处理模式下，吞吐量平均提升 2.3 倍，最高达 **8.2 倍**。
*   **自适应性**：调度器在 100% 的测试案例中都能准确选出最优的执行计划。

### 7. 优点
*   **全自动自适应**：无需用户手动调整卸载层数，系统根据显存预算自动优化。
*   **无损推理**：不依赖量化或剪枝，保证了高精度模型的原始性能。
*   **工业级应用**：直接针对 NVIDIA 的 IGI SDK 和 CR1 模型优化，具有极强的实用价值。
*   **粒度精细**：通过子层级（Sub-layer）分片，比传统的按层（Layer）分片更灵活。

### 8. 不足与局限
*   **硬件依赖**：虽然算法逻辑通用，但目前实现深度绑定了 NVIDIA CUDA 生态。
*   **环境动态性**：目前尚未完全处理热节流（Thermals）导致的动态频率变化，虽然设计上预留了扩展空间。
*   **软件更新**：如果底层算子库（如 cuBLAS）发生重大更新，可能需要重新运行安装阶段的剖析。
*   **应用限制**：主要针对单 GPU 客户端环境，未讨论多 GPU 协同。

（完）
