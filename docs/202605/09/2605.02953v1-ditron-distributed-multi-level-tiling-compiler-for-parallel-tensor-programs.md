---
title: "DITRON: Distributed Multi-level Tiling Compiler for Parallel Tensor Programs"
title_zh: DITRON：用于并行张量程序的分布式多级分块编译器
authors: "Size Zheng, Xuegui Zheng, Hanshi Sun, Qi Hou, Wenlei Bao, Shiyu Li, Haojie Duanmu, Jin Fang, Chenli Xue, Chenhui Huang, Yuanqiang Liu, Renze Chen, Ningxin Zheng, Dongyang Wang, Li-Wen Chang, Liqiang Lu, Yun Liang, Jidong Zhai, Xin Liu"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.02953v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于将张量程序映射到异构硬件的分块编译器
tldr: 针对大语言模型分布式编程灵活性不足及现有编译器难以处理复杂内存层级的问题，本文提出 DITRON。这是一款可扩展的块级编译器，通过引入核心、设备和任务三层分层编程抽象，实现了张量程序在异构分布式硬件上的高效映射。DITRON 简化了节点间通信复杂度，在保持高性能的同时提供了极佳的跨平台移植性，已在企业级训练和推理任务中显著降低了成本并提升了效率。
selection_source: fresh_fetch
motivation: 现有的高性能库缺乏灵活性，而传统张量编译器又无法有效应对分布式集群复杂的内存层级结构。
method: 提出一种涵盖核心、设备和任务层级的多级分层编程抽象，将张量程序高效映射至异构分布式硬件并屏蔽通信复杂性。
result: "在 NVIDIA 和 AMD 平台上均表现优异，推理性能提升 5%-30%，并在实际生产中每月节省约 50 万 GPU 小时的训练成本。"
conclusion: DITRON 成功弥补了分布式内核开发的效率与灵活性鸿沟，是支持大规模模型训练与推理的高效且通用的工具。
---

## 摘要
大语言模型（LLMs）的扩展目前受到分布式编程僵化性的瓶颈限制。虽然像 CuBLAS 和 NCCL 这样的高性能库提供了优化的算子原语，但它们缺乏快速演进的模型架构所需的灵活性。相反，现有的张量编译器无法有效处理分布式集群复杂的存储层级。为了弥补这一差距，我们提出了 DITRON，这是一种可扩展的分块级（tile-level）编译器，使高性能分布式算子开发变得大众化。DITRON 引入了一种涵盖核心（Core）、设备（Device）和任务（Task）层级的新型分层编程抽象，以将张量程序高效地映射到异构分布式硬件上。这种抽象使 DITRON 能够支持多种并行策略，同时屏蔽了节点间和节点内通信的复杂性。在大规模集群上的评估表明，DITRON 的性能与专家调优的 CUDA 库相当甚至更优，在独立算子上实现了 6%-30% 的加速，在 vLLM 的端到端推理中实现了 5%-30% 的加速。此外，DITRON 展示了强大的可移植性，在 NVIDIA 和 AMD 平台上均实现了显著的加速。DITRON 已在企业级训练和推理任务中部署。它在训练任务中实现了超过 10% 的模型算力利用率（MFU）提升，每月节省约 50 万 GPU 小时的训练成本。对于推理任务，它提供了超过 20% 的端到端增益，并已应用于云服务推理和边缘推理场景。

## Abstract
The scaling of large language models (LLMs) is currently bottlenecked by the rigidity of distributed programming. While high-performance libraries like CuBLAS and NCCL provide optimized primitives, they lack the flexibility required for rapidly evolving model architectures. Conversely, existing tensor compilers fail to address the complex memory hierarchy of distributed clusters effectively. To bridge this gap, we propose DITRON, a scalable tile-level compiler that democratizes high-performance distributed kernel development. DITRON introduces a novel hierarchical programming abstraction spanning Core, Device, and Task levels to map tensor programs efficiently onto heterogeneous distributed hardware. This abstraction allows DITRON to support diverse parallelism strategies while abstracting away the complexity of inter-node and intra-node communication.   Evaluated across large-scale clusters, DITRON achieves performance parity with or exceeding expert-tuned CUDA libraries, delivering speedups of $6\%-30\%$ on isolated kernels and $5\%-30\%$ on end-to-end inference in vLLM. Furthermore, DITRON demonstrates strong portability, achieving significant speedups on both NVIDIA and AMD platforms. \ours{} has been deployed at the enterprise level for both training and inference. It achieves an MFU improvement of over 10\% in training tasks, saving approximately 500,000 GPU hours of training cost per month. For inference tasks, it delivers an end-to-end gain of over 20\% and has been applied to cloud service inference and edge inference scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《DITRON: Distributed Multi-level Tiling Compiler for Parallel Tensor Programs》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）的性能优化目前面临“两极分化”的困境：
*   **高性能库（如 CuBLAS, NCCL）**：虽然性能极高，但属于硬编码的算子原语，缺乏灵活性，难以适配快速演进的新模型架构。
*   **现有张量编译器（如 Triton）**：擅长单设备优化，但在处理分布式集群复杂的内存层级（HBM、L2、NVLink、PCIe、以太网）时力不从心。
*   **通信瓶颈**：在分布式训练和推理中，通信开销占运行时间的 20%~80%。
**DITRON 的目标**是提供一个可扩展的分块级（Tile-level）编译器，通过统一的编程抽象，在保持灵活性的同时，自动实现计算与通信的高效重叠（Overlap）。

### 2. 方法论：核心思想与关键技术
DITRON 引入了**三层分层编程抽象**和**分布式 Swizzling** 技术：
*   **三层编程接口**：
    1.  **核心级（Core-level）**：继承 Triton 的分块语义，管理单 GPU 内的静态形状分块，利用 Tensor Core 和 TMA。
    2.  **设备级（Device-level）**：操作由多个 Tile 组成的“数据块（Chunk）”，直接映射到 DMA/RDMA 引擎，支持动态形状（如 MoE 架构）。
    3.  **任务级（Task-level）**：将整个分布式工作负载视为任务的有向无环图（DAG），将多个通信和计算内核融合为一个“MegaKernel”，消除内核启动开销。
*   **分布式 Swizzling（重排优化）**：
    *   **Gather 模式**：提前调度远程数据获取请求。
    *   **Scatter 模式**：优先计算需要发送到最远节点的数据块。
    *   通过重排 Tile 的执行顺序，确保计算单元在等待远程数据时保持繁忙。
*   **硬件无关原语**：后端采用类似 OpenSHMEM 的原语（如 `putmem`, `getmem`, `barrier`），使其能轻松移植到 NVIDIA、AMD 等不同硬件。

### 3. 实验设计
*   **场景与模型**：涵盖推理（张量并行 TP）和训练（TP、序列并行 SP、专家并行 EP）。测试了 LLaMA3 (70B/405B)、Qwen3 (32B/8B)、Mixtral、DeepSeek 等主流模型。
*   **Benchmark 与对比方法**：
    *   **算子级**：对比 CuBLAS+NCCL（非重叠基准）、TileLink、FLUX、COMET。
    *   **端到端**：在 vLLM 框架中集成 DITRON，对比原始 vLLM。
    *   **MegaKernel**：对比 PyTorch (Eager/CUDAGraph) 和 Mirage。
*   **平台**：NVIDIA H800、Hopper 96GB、AMD GPU 以及 PCIe 接口的 GPU。

### 4. 资源与算力
*   **硬件型号**：主要使用 NVIDIA H800 集群（单节点 8 GPU，200GB/s NVLink）和 Hopper 96GB 集群（450GB/s NVLink）。
*   **规模**：实验涵盖了从单节点（8 GPU）到多节点（最高 128 GPU）的扩展性测试。
*   **生产贡献**：论文提到 DITRON 已在字节跳动大规模部署，每月节省约 **500,000 GPU 小时**的训练成本。

### 5. 实验数量与充分性
*   **实验量**：非常充分。针对单算子测试了 26 种不同的形状配置；进行了强扩展性（Strong Scaling）和弱扩展性（Weak Scaling）分析；并在 NVIDIA 和 AMD 两个平台上进行了交叉验证。
*   **客观性**：对比了目前业界最先进的专家调优库（如 FLUX），并展示了在不同 Batch Size 和序列长度下的性能曲线，结果具有较高的客观参考价值。

### 6. 主要结论与发现
*   **性能优异**：在独立算子上比专家调优的 CUDA 库快 6%~30%；在 vLLM 端到端推理中提升 5%~30%（大 Batch Size 下更明显）。
*   **通信隐藏**：对于大形状输入，DITRON 能隐藏近 87.5% 的通信延迟。
*   **高移植性**：在 AMD 平台上相比 RocmBLAS+RCCL 实现了 1%~38% 的加速；在低带宽的 PCIe GPU 上平均加速达 8.33 倍。
*   **生产价值**：在企业级训练中提升 MFU（模型算力利用率）超过 10%。

### 7. 优点（亮点）
*   **多级抽象**：成功将单机编译器（Triton）的成功经验扩展到了分布式领域，降低了高性能分布式内核的开发门槛。
*   **非完美形状支持**：针对 LLM 中常见的非对齐、非完美分块形状设计了复杂的 Swizzling 逻辑。
*   **任务融合**：通过 MegaKernel 技术显著降低了小 Batch 推理时的内核启动开销。

### 8. 不足与局限
*   **强扩展性限制**：在强扩展性实验中，当 GPU 数量过多导致每个 GPU 分配到的计算任务（GEMM 形状）过小时，计算无法完全掩盖通信延迟，加速比会下降。
*   **开发复杂度**：虽然比手写 CUDA 简单，但编写高效的分布式 Swizzling 逻辑仍需要开发者对硬件拓扑有较深理解。
*   **应用范围**：目前主要集中在基于张量的 LLM 算子，对于非张量类或极度稀疏的分布式计算支持尚不明确。

（完）
