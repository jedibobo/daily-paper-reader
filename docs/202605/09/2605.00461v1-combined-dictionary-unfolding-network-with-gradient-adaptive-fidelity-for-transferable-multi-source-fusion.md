---
title: Combined Dictionary Unfolding Network with Gradient-Adaptive Fidelity for Transferable Multi-Source Fusion
title_zh: 结合梯度自适应保真度的组合字典展开网络，用于可迁移的多源融合
authors: "Ge Luo, Jun-Jie Huang, Qi Yu, Tianrui Liu, Ke Liang, Yuming Xiang, Wentao Zhao, Xinwang Liu, Meng Wang"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00461v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对资源受限边缘设备的轻量化网络设计
tldr: 针对多源图像融合中深度展开网络计算开销大的问题，本文提出了一种轻量级的组合字典展开网络（CDNet）。该方法将耦合字典学习中的唯一-共同分解先验转化为结构受限的联合展开架构，通过CDBlock实现特征的联合更新。结合高低频图像保真度损失，CDNet在红外可见光、多曝光等多种融合任务中表现出色，显著提升了效率和融合质量。
selection_source: fresh_fetch
motivation: 现有的深度展开融合方法多采用交替最小化策略分别更新不同模态特征，导致计算和内存开销巨大，限制了在边缘设备上的应用。
method: 提出CDNet架构，利用耦合字典学习的先验知识构建联合展开模块CDBlock，实现公共与模态特定表示的同步更新，并设计了高低频图像保真度损失进行无监督训练。
result: 实验表明CDNet在红外可见光、多曝光及医疗图像融合等四个任务中均达到领先水平，尤其在TNO和RoadScene数据集的PSNR指标上显著超越现有方法。
conclusion: CDNet通过模型驱动的联合更新机制，在保持轻量化的同时实现了高效且高性能的多源图像融合，证明了其在资源受限场景下的应用潜力。
---

## 摘要
基于深度展开网络的方法通过结合模型驱动的迭代优化与数据驱动的深度学习，已成为多源图像融合的有效解决方案。然而，大多数现有的深度展开图像融合方法源于交替最小化，即分别更新不同模态的特征。这种设计引入了巨大的计算和内存开销，限制了在资源受限的边缘设备上的部署。为解决这一问题，我们提出了 CDNet，一种用于多源图像融合的轻量级组合字典展开网络。CDNet 并非引入新的稀疏编码先验或凭经验压缩现有的融合网络，而是将耦合字典学习的“唯一-共同”分解先验转化为结构受限的联合展开架构。由此产生的 CDBlock 遵循块稀疏交互拓扑，并对共同表示和模态特定表示进行模型导出的联合更新，从而简化了特征学习并提高了效率。此外，我们设计了一种紧凑的高低频图像保真度损失，用于无地面真值图像的无监督训练。我们在四项任务上评估了 CDNet，包括多曝光图像融合、红外与可见光图像融合、医学图像融合以及用于语义分割的红外与可见光图像融合。实验结果表明，CDNet 以高效率实现了具有竞争力的或更优的融合性能。在红外与可见光图像融合方面，CDNet 在 TNO 数据集的六项指标中优于竞争方法四项，在 RoadScene 数据集的六项指标中优于五项。特别是，它在 TNO 和 RoadScene 上的峰值信噪比（PSNR）分别比排名第二的方法高出 1.23 dB 和 1.59 dB。

## Abstract
Deep Unfolding Network-based methods have emerged as effective solutions for multi-source image fusion by combining model-driven iterative optimization with data-driven deep learning. However, most existing deep unfolding image fusion methods are derived from alternating minimization, which updates the features of different modalities separately. This design introduces considerable computational and memory overhead, limiting deployment on resource-constrained edge devices. To address this issue, we propose CDNet, a lightweight Combined Dictionary Unfolding Network for multi-source image fusion. Rather than introducing a new sparse coding prior or empirically compressing an existing fusion network, CDNet translates the unique-common decomposition prior of coupled dictionary learning into a structurally constrained joint unfolding architecture. The resulting CDBlock follows a block-sparse interaction topology and performs a model-derived joint update of common and modality-specific representations, thereby streamlining feature learning and improving efficiency.In addition, we design a compact High- and Low-frequency Image Fidelity loss for unsupervised training without ground-truth images. We evaluate CDNet on four tasks, including multi-exposure image fusion, infrared and visible image fusion, medical image fusion, and infrared and visible image fusion for semantic segmentation. Experimental results show that CDNet achieves competitive or superior fusion performance with high efficiency. For infrared and visible image fusion, CDNet outperforms competing methods on four of six metrics on the TNO dataset and five of six metrics on the RoadScene dataset. In particular, it surpasses the second-best method by 1.23 dB and 1.59 dB in PSNR on TNO and RoadScene, respectively.

---

## 论文详细总结（自动生成）

以下是对论文《Combined Dictionary Unfolding Network with Gradient-Adaptive Fidelity for Transferable Multi-Source Fusion》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：多源图像融合（MSIF）旨在整合不同传感器（如红外、可见光、多曝光）的互补信息。
*   **核心痛点**：现有的深度展开网络（DUN）虽然结合了模型驱动的解释性和数据驱动的性能，但大多基于“交替最小化”策略。这意味着模型需要分别更新不同模态的特征，导致计算分支多、内存访问频繁、推理延迟高，难以部署在资源受限的边缘设备上。
*   **研究动机**：开发一种既能保持深度展开架构的解释性，又能实现极高性能与极低开销平衡的轻量化融合模型，且具备跨任务的迁移能力。

### 2. 方法论
*   **核心思想**：将耦合字典学习中的“唯一-共同（Unique-Common）”分解先验，转化为一个**结构受限的联合展开架构（CDNet）**。
*   **关键技术细节**：
    *   **联合表示（Joint Representation）**：不再交替更新三个独立变量，而是将模态特定特征和共同特征组合成一个统一的向量 $W$，并将耦合字典整合为一个块结构字典 $D$。
    *   **CDBlock（核心模块）**：基于 LISTA（学习迭代收缩阈值算法）原理，将迭代优化步骤映射为卷积层。通过结构化卷积核（遵循块稀疏拓扑），在单次前向传播中同步更新所有特征组件，消除了碎片化的分支计算。
    *   **HLIF 损失函数**：设计了“高低频图像保真度损失”。利用源图像梯度生成自适应权重，分别在梯度域（高频）约束结构细节，在图像域（低频）约束亮度一致性，实现无监督训练。
*   **算法流程**：特征扩张 $\rightarrow$ $T$ 次 CDBlock 迭代细化 $\rightarrow$ 融合与重建（残差连接补偿信息）。

### 3. 实验设计
*   **实验场景**：涵盖四类主流融合任务：
    1.  多曝光图像融合 (MEF)
    2.  红外与可见光图像融合 (IVF)
    3.  医学图像融合 (MIF)
    4.  下游任务：红外可见光融合用于语义分割 (IVF4Seg)
*   **数据集**：SICE（主要训练集）、MEFB、TNO、RoadScene、PET-MRI、SPECT-MRI、FMB。
*   **对比方法**：包括 LRRNet (TPAMI'23), CDDFuse (CVPR'23), EMMA (CVPR'24), GIFNet (CVPR'25), Mask-DiFuser (TPAMI'25) 等 10 余种前沿方法。
*   **评价指标**：MSE, PSNR, SSIM, CC, Nabf（伪影指标）, HyperIQA（感知质量）以及分割指标 mIoU/mAcc。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **NVIDIA GeForce RTX 4090 GPU** 的计算机。
*   **训练细节**：Adam 优化器，Batch size 为 10，学习率 0.0005，训练 50 个 epoch。
*   **模型规模**：极度轻量化，仅有 **6.5K (0.0065M)** 参数量。
*   **推理效率**：处理 256×256 图像的平均推理时间仅为 **1.55ms**，计算量仅 **0.4 GFLOPs**。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流公开数据集上进行了定量和定性评估，并针对下游语义分割任务进行了验证。
*   **消融实验**：非常充分。对比了“联合更新”与“交替更新”的效率差异，验证了 HLIF 损失函数的鲁棒性，并测试了不同训练数据集对迁移性能的影响。
*   **客观性**：采用了统一的下采样预处理以确保与高内存消耗方法（如 GIFNet）在相同基准下对比，评价指标涵盖了统计学、信息论及人类视觉感知多个维度，结果较为客观。

### 6. 主要结论与发现
*   **性能卓越**：在红外可见光融合中，CDNet 在 TNO 和 RoadScene 上的 PSNR 分别领先第二名 **1.23 dB** 和 **1.59 dB**。
*   **极致效率**：参数量仅为 GIFNet 的约 1/94，推理速度极快，且在低算力下性能不降反升。
*   **强迁移性**：模型仅在多曝光数据集（SICE）上训练，无需任何微调即可直接应用于红外、医疗等完全不同的模态任务，证明了其捕获通用结构特征的能力。

### 7. 优点
*   **架构创新**：通过数学重组将交替优化转为联合更新，解决了 DUN 难以并行化的顽疾。
*   **轻量化巅峰**：在保持深度展开模型解释性的同时，将参数量压缩到了千级别，极具工业应用价值。
*   **损失函数普适**：HLIF 损失不依赖预训练网络（如 VGG），计算简单且对不同任务具有良好的泛化性。

### 8. 不足与局限
*   **颜色处理简化**：模型主要针对 YCbCr 空间的 Y 通道进行融合，色度通道（Cb, Cr）仍采用传统的加权融合，在极端色彩失真场景下可能表现受限。
*   **分辨率敏感性**：为了公平对比对部分数据集做了下采样，虽然证明了有效性，但在超高分辨率（如 4K）下的细节保持能力有待进一步验证。
*   **配准依赖**：实验假设输入图像已严格配准，未探讨在存在空间偏移（Misalignment）情况下的鲁棒性。

（完）
