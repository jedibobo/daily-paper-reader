---
title: Surrogate Neural Architecture Codesign Package (SNAC-Pack)
title_zh: 代理神经架构协同设计软件包 (SNAC-Pack)
authors: "Jason Weitz, Dmitri Demler, Benjamin Hawks, Aaron Wang, Nhan Tran, Javier Duarte"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.16138v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 用于硬件感知神经架构协同设计和FPGA部署的AutoML框架
tldr: SNAC-Pack是一个开源AutoML框架，旨在解决神经网络在FPGA部署时硬件成本估算不准的问题。它结合了多目标全局搜索和硬件代理模型，避免了高昂的综合成本。通过量化感知训练和剪枝进行局部优化，并利用hls4ml实现端到端部署。在大型强子对撞机和超导量子比特读取任务中，该框架显著降低了资源消耗，并将设计周期从数月缩短至数小时。
selection_source: fresh_fetch
motivation: 现有的神经架构搜索往往只关注准确率或使用与FPGA硬件成本相关性差的代理指标，导致部署效率低下且设计周期长。
method: 采用基于硬件代理模型的多目标全局搜索，结合量化感知训练与迭代剪枝的局部优化，并通过hls4ml实现自动化FPGA固件生成。
result: 在喷注分类和量子比特读取任务中，该框架发现了性能优异且资源占用更少的架构，并将设计空间探索时间从数月缩短至数小时。
conclusion: SNAC-Pack提供了一种高效的端到端硬件感知神经架构协同设计方案，显著提升了FPGA上深度学习模型的开发效率和性能。
---

## 摘要
神经架构搜索 (NAS) 是自动化模型设计的一种强大方法，但现有方法通常仅针对准确率进行优化，或者依赖于位操作 (BOPs) 等与硬件成本相关性较差的代理指标。这种差距在 FPGA 部署中尤为明显，因为其成本受查找表 (LUTs)、DSPs、触发器 (flip-flops)、BRAM 和延迟等多维预算的支配。我们提出了代理神经架构协同设计软件包 (SNAC-Pack)，这是一个用于硬件感知的神经架构协同设计和端到端 FPGA 部署的开源 AutoML 框架。SNAC-Pack 使用 Optuna 和 NSGA-II 运行多目标全局搜索，将试验加载到共享的 SQLite 存储中，从而实现跨计算节点的并行工作。硬件代理模型输出每次试验的资源和延迟估计，避免了原本会主导搜索循环的综合成本。随后，局部搜索阶段在组合压缩循环中应用量化感知训练 (QAT) 和迭代幅度剪枝，之后通过 hls4ml Python 库将最终模型综合为 FPGA 固件。YAML 配置和可选的智能体前端允许用户在不修改框架的情况下在新的数据集上运行该流水线。我们在大型强子对撞机 (LHC) 的射流分类和超导量子比特读取任务上演示了 SNAC-Pack，发现了在任务指标上达到或超过强基准线的紧凑架构，同时降低了 FPGA 资源利用率；在量子比特读取案例中，将设计空间探索过程从数月的繁琐手动微调缩短至数小时的自动化搜索。

## Abstract
Neural architecture search (NAS) is a powerful approach for automating model design, but existing methods often optimize for accuracy alone or rely on proxy metrics such as bit operations (BOPs) that correlate poorly with hardware cost. This gap is particularly large for FPGA deployment, where cost is dominated by a multi-dimensional budget of lookup tables, DSPs, flip-flops, BRAM, and latency. We present the Surrogate Neural Architecture Codesign Package (SNAC-Pack), an open-source AutoML framework for hardware-aware neural architecture codesign and end-to-end FPGA deployment. SNAC-Pack runs a multi-objective global search with Optuna and NSGA-II, loading trials to a shared SQLite store that enables parallel workers across compute nodes. A hardware surrogate model outputs per-trial resource and latency estimates, avoiding the synthesis cost that would otherwise dominate the search loop. A local search stage then applies quantization-aware training (QAT) together with iterative magnitude pruning in a combined compression loop, after which the final model is synthesized to FPGA firmware via the hls4ml Python library. A YAML configuration and an optional agentic frontend let users run the pipeline on new datasets without modifying the framework. We demonstrate SNAC-Pack on jet classification at the Large Hadron Collider and superconducting qubit readout, discovering compact architectures that match or exceed strong baselines on the task metric while reducing FPGA resource utilization and, in the qubit readout case, reducing the design space exploration process from months of manual fine-tuning to hours of automated search.

---

## 论文详细总结（自动生成）

### 代理神经架构协同设计软件包 (SNAC-Pack) 论文总结

#### 1. 核心问题与整体含义
*   **研究背景**：在边缘计算、嵌入式加速器和高能物理触发器等资源受限环境中，FPGA 是实现微秒级推理的首选。然而，FPGA 的资源预算是多维的（包括 LUT、DSP、BRAM、FF 和时钟周期），传统的神经架构搜索（NAS）往往只优化准确率，或使用位操作（BOPs）等与实际硬件成本相关性较弱的代理指标。
*   **核心问题**：如何在搜索循环中获得快速且准确的硬件反馈，以弥合算法设计与物理综合之间的鸿沟，避免高昂的硬件综合时间成本。
*   **整体含义**：论文提出了 SNAC-Pack，一个开源的端到端 AutoML 框架，通过引入硬件代理模型（Surrogate Models）实现了硬件感知的神经架构协同设计。

#### 2. 方法论
*   **核心思想**：采用“全局搜索 + 局部压缩”的两阶段工作流，并利用预训练的代理模型代替真实的硬件综合来评估候选架构。
*   **关键技术细节**：
    *   **全局搜索**：基于 Optuna 框架，使用 NSGA-II 多目标遗传算法。搜索空间涵盖层数、宽度、激活函数、归一化和正则化等。
    *   **硬件代理模型**：集成 `rule4ml` 和 `wa-hls4ml` 预测器。这些模型（如 GNN）预先在数十万个已综合的模型上训练，能够快速估计 LUT、FF、BRAM、DSP 的利用率及延迟，无需调用 Vivado 综合。
    *   **局部搜索与压缩**：对全局搜索选出的帕累托最优（Pareto-optimal）候选者进行量化感知训练（QAT）和迭代幅度剪枝（Iterative Magnitude Pruning）。
    *   **端到端部署**：通过 `hls4ml` 库将压缩后的模型转换为 FPGA 固件。
    *   **接口设计**：支持 YAML 配置和可选的 MCP（模型上下文协议）前端，便于智能体（Agent）调用。

#### 3. 实验设计
*   **数据集/场景**：
    1.  **LHC 喷注分类（Jet Classification）**：基于大型强子对撞机数据，将喷注分为五类。
    2.  **超导量子比特读取（Qubit Readout）**：对超导量子比特的 I/Q 信号进行分类，以确定其状态。
*   **基准（Benchmark）**：
    *   喷注分类：Odagiu 等人 (2024) 提出的 SOTA MLP 架构。
    *   量子比特读取：Guglielmo 等人 (2025) 手动微调的基准模型。
*   **对比方法**：对比了 NAC（Neural Architecture Codesign，仅以 BOPs 为效率目标）和 SNAC-Pack（以硬件代理得分为目标）。

#### 4. 资源与算力
*   **喷注分类**：使用单台 NVIDIA A100 GPU。全局搜索耗时约 6 小时，局部搜索（涵盖多种位宽精度）耗时约 8 小时。
*   **量子比特读取**：使用 10 个 Slurm 节点，每个节点配备 1 台 A100 GPU。全局搜索并行运行约 3 小时，局部搜索约 1.5 小时。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务运行了 500 次全局搜索试验（Trials），并进行了 3 折交叉验证（3-fold CV）。
*   **充分性**：实验涵盖了两个完全不同的科学机器学习领域，展示了框架的通用性。通过帕累托前沿（Pareto Front）分析了准确率与资源之间的权衡，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能提升**：SNAC-Pack 发现的架构在保持或超过基准准确率的同时，显著降低了硬件资源占用。
*   **资源优化**：在量子比特读取任务中，延迟从 6 个时钟周期降至 4 个，LUT 和 FF 资源减少了约一半。
*   **效率飞跃**：在量子比特读取案例中，SNAC-Pack 将原本需要数月的手动调优过程缩短至数小时的自动化搜索。
*   **代理模型有效性**：虽然代理模型能有效引导搜索，但在某些极端情况下（如延迟预测），代理模型的排名可能与 Vivado 实际综合结果存在偏差。

#### 7. 优点
*   **端到端自动化**：实现了从架构搜索、压缩到 FPGA 固件生成的全流程自动化。
*   **搜索效率高**：通过代理模型解决了 NAS 中最耗时的硬件评估瓶颈。
*   **易用性**：YAML 驱动的设计和智能体前端降低了非硬件专家使用 FPGA 加速技术的门槛。

#### 8. 不足与局限
*   **代理模型精度**：论文承认代理模型在延迟预测上存在“失真”风险，可能导致搜索到的“最优”模型在实际综合时并非最优。
*   **搜索空间限制**：目前的实验主要集中在 MLP（多层感知机）架构，对于卷积（CNN）或更复杂的架构在 FPGA 上的协同设计支持尚需进一步验证。
*   **硬件平台覆盖**：实验仅针对特定的 Xilinx FPGA 芯片，跨厂商或跨架构的泛化能力有待观察。

（完）
