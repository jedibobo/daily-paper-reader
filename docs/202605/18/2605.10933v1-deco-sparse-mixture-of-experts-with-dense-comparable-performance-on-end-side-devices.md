---
title: "DECO: Sparse Mixture-of-Experts with Dense-Comparable Performance on End-Side Devices"
title_zh: DECO：在端侧设备上具有与稠密模型性能相当的稀疏混合专家模型
authors: "Chenyang Song, Weilin Zhao, Xu Han, Chaojun Xiao, Yingfa Chen, Zhiyuan Liu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10933v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 专为高效端侧部署设计的MoE架构，具有较小的存储开销
tldr: "针对混合专家模型（MoE）在端侧设备部署时面临的存储和内存瓶颈，本文提出DECO架构。DECO通过基于ReLU的可微路由、可学习的专家缩放以及NormSiLU激活函数，在相同参数预算下实现了与稠密Transformer相当的性能。该架构简化了专家结构并提高了稀疏度，仅需激活20%的专家即可在硬件上实现3倍的推理加速，为高效端侧部署提供了新方案。"
selection_source: fresh_fetch
motivation: 解决MoE模型因庞大参数量导致的存储和内存访问瓶颈，以满足端侧设备对高性能和低开销的双重需求。
method: 引入基于ReLU的灵活路由、可学习专家缩放、NormSiLU激活函数，并采用非门控MLP专家简化架构。
result: "在仅激活20%专家的情况下，DECO性能媲美稠密模型，并在真实硬件上获得3.00倍的推理加速。"
conclusion: DECO通过创新的路由与激活机制，成功在端侧设备上实现了高性能与高效率的平衡。
---

## 摘要
虽然混合专家模型（MoE）在不按比例增加计算量的情况下扩展了模型容量，但其庞大的总参数占用造成了显著的存储和内存访问瓶颈，阻碍了同时要求高性能、低计算成本和低存储开销的高效端侧部署。为了实现这些特性，我们提出了 DECO，这是一种稀疏 MoE 架构，旨在在相同的总参数预算和训练 Token 数量下，达到与稠密 Transformer 相当的性能。DECO 利用了通过可学习的专家级缩放增强的可微且灵活的基于 ReLU 的路由，从而自适应地平衡路由专家和共享专家的贡献。此外，我们引入了 NormSiLU，这是一种在 SiLU 算子之前对输入进行归一化的激活函数，它能产生更稳定的路由专家激活率趋势和更高的内在稀疏度。我们还发现在基于 ReLU 的路由中使用非门控 MLP 专家具有经验性优势，这表明了简化 MoE 架构的可能性。实验表明，DECO 仅激活 20% 的专家即可达到稠密模型的性能，并优于现有的 MoE 基准模型。与稠密推理相比，我们的专用加速算子在真实硬件上实现了 3.00 倍的加速。代码和检查点将会发布。

## Abstract
While Mixture-of-Experts (MoE) scales model capacity without proportionally increasing computation, its massive total parameter footprint creates significant storage and memory-access bottlenecks, which hinder efficient end-side deployment that simultaneously requires high performance, low computational cost, and small storage overhead. To achieve these properties, we present DECO, a sparse MoE architecture designed to match the performance of dense Transformers under identical total parameter budgets and training tokens. DECO utilizes the differentiable and flexible ReLU-based routing enhanced by learnable expert-wise scaling, which adaptively balances the contributions of routed and shared experts. Furthermore, we introduce NormSiLU, an activation function that normalizes inputs prior to SiLU operators, producing a more stable trend of routed-expert activation ratio and a higher intrinsic sparsity level. We also identify an empirical advantage in using non-gated MLP experts with ReLU-based routing, indicating the possibility of MoE architecture simplification. Experiments demonstrate that DECO, activating only 20% of experts, matches dense performance and outperforms established MoE baselines. Our specialized acceleration kernel delivers a 3.00$\times$ speedup on real hardware compared with dense inference. Codes and checkpoints will be released.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DECO** 的新型稀疏混合专家模型（MoE）架构，旨在解决大模型在端侧设备（如手机、嵌入式设备）部署时面临的存储空间不足和内存访问延迟高的痛点。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的 MoE 虽然通过稀疏激活降低了计算量（FLOPs），但其“总参数量”通常远超同性能的稠密模型。这导致了巨大的存储开销，并在推理时产生严重的内存访问瓶颈（从闪存/内存加载专家参数的延迟抵消了计算节省）。
*   **核心目标**：在**总参数预算**和**训练 Token 数量**完全一致的严苛条件下，设计一种性能能够媲美稠密 Transformer 的稀疏 MoE 架构，实现高性能、低计算、小存储的“理想三角”。

### 2. 方法论
DECO 架构对 MoE 的路由、专家和正则化机制进行了根本性修订：
*   **基于 ReLU 的路由**：弃用不可微且激活数固定的 TopK 路由，采用基于 ReLU 的可微路由，允许模型根据 Token 复杂度自适应地决定激活专家的数量。
*   **可学习的专家级缩放（Learnable Expert-wise Scaling）**：在路由分数上应用可学习的向量系数 $\alpha$，以平衡共享专家与路由专家的贡献，并补偿不同专家之间的输出异质性。
*   **NormSiLU 激活函数**：在专家内部的 SiLU 算子前引入双阶段归一化：
    1.  **专家间均值归一化**：使预激活分布零中心化，防止 SiLU 输出消失。
    2.  **专家内 RMS 归一化**：稳定激活幅值，抑制激活率无序激增。
*   **非门控 MLP 专家**：发现非门控 MLP 在 ReLU 路由下比常用的 SwiGLU（门控）更稳定，能减少正则化压力，提升性能。
*   **自适应稀疏正则化**：利用路由熵（Router Entropy）作为损失函数，并通过动态调整系数 $\lambda$ 来精确控制目标稀疏度。

### 3. 实验设计
*   **数据集**：使用大规模高质量混合语料（FineWeb, Nemotron-CC, Pile, Wikipedia 等），涵盖网页、代码、数学等。
*   **模型规模**：设置了四个梯度：Small (0.11B), Medium (0.24B), Large (0.53B), XLarge (1.18B)。
*   **Benchmark**：
    *   **语言建模**：C4 数据集验证集困惑度（PPL）。
    *   **常识推理**：PIQA, SIQA, HellaSwag, ARC-E/C, WinoGrande, LAMBADA。
*   **对比方法**：Dense (LLaMA 风格)、TopP 路由、DeepSeek-V3 风格 MoE、ReMoE、BlockFFN。

### 4. 资源与算力
*   **训练细节**：文中未明确提及具体的 GPU 型号和总训练时长，但指出训练 Token 数严格控制在参数量的 40 倍左右（例如 1.18B 模型训练了约 47B Tokens）。
*   **推理测试硬件**：使用 NVIDIA RTX 4090 (24GB) 和 **Jetson AGX (64GB)** 嵌入式设备进行实际速度评估。

### 5. 实验数量与充分性
*   **实验规模**：进行了跨越 0.1B 到 1.2B 参数量的多组对比实验，验证了架构的可扩展性。
*   **消融实验**：非常充分。分别针对专家缩放因子、NormSiLU 的两个归一化步骤、门控/非门控策略进行了详尽的对比分析。
*   **超参分析**：深入探讨了激活率、共享专家大小、专家粒度（Granularity）对性能的影响。
*   **客观性**：实验在“相同总参数”和“相同训练量”下进行，相比以往 MoE 论文常用的“相同计算量（但更多参数）”对比方式，对端侧部署场景更具参考价值。

### 6. 主要结论与发现
*   **性能媲美稠密模型**：在仅激活约 20% 参数的情况下，DECO 在所有规模上均达到或超过了稠密模型的性能。
*   **架构简化优势**：在 ReLU 路由场景下，非门控 MLP 专家因梯度更稳定，表现优于复杂的门控结构（SwiGLU）。
*   **硬件加速显著**：通过定制的 CUTLASS 内核，DECO 在 Jetson AGX 上实现了 **3.00 倍**的端到端推理加速。
*   **数据异质性影响**：DECO 在多样化数据集上表现更好，而在分布单一的数据集（如纯 FineWeb）上优势会有所收窄。

### 7. 优点
*   **端侧友好**：直接针对存储和内存带宽瓶颈设计，具有极高的实用部署价值。
*   **稳定性创新**：NormSiLU 有效解决了稀疏模型训练中常见的激活率波动和神经元利用率低的问题。
*   **高效路由**：ReLU 路由结合自适应正则化，实现了比 TopK 更灵活的计算分配。

### 8. 不足与局限
*   **对齐阶段验证缺失**：实验仅限于预训练阶段，未验证 DECO 在指令微调（SFT）和强化学习（RL）阶段的稳定性。
*   **规模上限**：目前实验最大规模为 1.18B，尚未在百亿或千亿级参数模型上验证其“稠密等价性”是否依然成立。
*   **数据依赖性**：论文提到在低熵、单一领域数据上，MoE 的优势可能不如高熵混合数据明显。

（完）
