---
title: "Hardware-Software Co-Design of Scalable, Energy-Efficient Analog Recurrent Computations"
title_zh: 可扩展、高能效模拟循环计算的软硬件协同设计
authors: "Arthur Fyon, Julien Brandoit, Loris Mendolia, Damien Ernst, Jean-Michel Redouté, Guillaume Drion"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.15216v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 能效模拟循环计算的硬件软件协同设计
tldr: 针对环境传感器和生物医学植入等始终在线的AI应用，本研究通过软硬件协同设计，克服了模拟电路在循环神经网络（RNN）中因噪声累积而难以实现的难题。提出了一种基于双稳态存储循环单元（BMRU）的超低功耗电流模式模拟电路，利用离散输出抑制噪声，实现了参数与电路元件的一一对应。在180nm CMOS工艺下的仿真验证了其高保真度和亚微瓦级的推理功耗。
selection_source: fresh_fetch
motivation: 模拟电路虽能实现超低功耗推理，但因反馈过程中的噪声累积问题，此前难以有效支持循环神经网络架构。
method: 采用软硬件协同设计，利用具有离散输出和迟滞特性的双稳态存储循环单元（BMRU），并将其重新表述为适合模拟电路实现的电流模式架构。
result: 180nm CMOS晶体管级仿真显示软件模型与硬件行为高度一致，且在关键词识别任务中实现了核心功耗低于1微瓦的推理。
conclusion: 该设计证明了通过离散化反馈可以有效抑制模拟噪声，使可扩展且高能效的模拟循环计算在超低功耗AI应用中成为可能。
---

## 摘要
从环境传感器到生物医学植入物，常开型（Always-on）人工智能应用都需要极低的功耗。模拟电路为亚微瓦级推理提供了一条路径，但现有的模拟实现仅限于前馈架构：由于通过时间反馈产生的噪声累积，将它们扩展到循环动力学一直被认为是不切实际的。我们证明了通过软硬件协同设计可以克服这一障碍。具体而言，我们发现双稳态记忆循环单元（BMRUs）——一类具有离散值输出和滞回动力学的循环神经网络（RNNs）——支持超低功耗的电流模式模拟实现，并基于第一性原理对其进行了设计。所得电路在每个学习参数与电路元件之间建立了一一对应关系。离散输出在每个单元边界处将模拟噪声抑制了至少 20 倍，打破了阻碍模拟循环的噪声累积。我们针对固定阈值的第一象限操作重新表述了 BMRUs，在保持表达能力和可训练性的同时实现了直接对应。在 180 nm 互补金属氧化物半导体（CMOS）工艺下的晶体管级仿真显示，软件预测与电路级行为之间几乎完美一致，因此软件模型可以作为物理硬件的高保真模拟器，且计算成本较低。我们利用这种保真度进行了大规模的抗噪性和功耗缩放分析：增加循环的功耗成本随状态维度线性缩放，而占据总功耗主导地位的前馈层则呈平方级缩放，这意味着相对于前馈骨干网络，循环是以线性边际成本增加的。端到端关键词检测在 RNN 核心实现了亚微瓦级推理。

## Abstract
Always-on AI applications, from environmental sensors to biomedical implants, require ultra-low power consumption. Analog circuits offer a path to sub-microwatt inference, yet existing analog implementations are limited to feedforward architectures: extending them to recurrent dynamics has been considered impractical due to noise accumulation through temporal feedback. We demonstrate that this barrier can be overcome through hardware-software co-design. Specifically, we identify that Bistable Memory Recurrent Units (BMRUs), a class of Recurrent Neural Networks (RNNs) with discrete-valued outputs and hysteretic dynamics, admit an ultra-low power current-mode analog implementation which we design from first principles. The resulting circuit establishes a one-to-one correspondence between each learned parameter and a circuit element. The discrete outputs suppress analog noise by at least 20-fold at each cell boundary, breaking the noise accumulation that prevents analog recurrence. We reformulate BMRUs for first-quadrant operation with fixed thresholds, enabling the direct correspondence while preserving expressivity and trainability. Transistor-level simulations in 180 nm Complementary Metal-Oxide-Semiconductor (CMOS) show near-perfect agreement between software predictions and circuit-level behavior, with the software model thereby serving as a high-fidelity simulator of the physical hardware at low computational cost. We leverage this fidelity to conduct large-scale noise immunity and power scaling analyses: the power cost of adding recurrence scales linearly with state dimension, while the feedforward layers dominating total power scale quadratically, meaning recurrence is added at linear marginal cost relative to the feedforward backbone. End-to-end keyword spotting achieves sub-microwatt inference at the RNN core.

---

## 论文详细总结（自动生成）

这篇论文提出了一种通过软硬件协同设计实现超低功耗模拟循环神经网络（RNN）的新方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：始终在线（Always-on）的AI应用（如环境监测、生物医学植入物）对功耗有极端要求（亚微瓦级）。
*   **核心挑战**：虽然模拟电路在处理前馈神经网络时具有极高能效，但在处理循环神经网络（RNN）时，由于时间反馈路径上的**模拟噪声累积**，导致系统极不稳定且难以扩展。
*   **整体含义**：论文证明了通过特定的算法选择与电路设计，可以打破模拟循环计算中的噪声壁垒，实现可扩展且高能效的模拟RNN。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用**双稳态存储循环单元（BMRU）**的特性。BMRU具有离散值输出和滞回（Hysteretic）动力学，这种“离散性”天然地在每个单元边界处起到了噪声抑制的作用。
*   **关键技术细节**：
    *   **第一象限 BMRU (FQ BMRU)**：为了适配模拟电路，将原始BMRU重新表述为仅在第一象限运行（非负电流信号），并使用固定阈值。
    *   **电流模式模拟实现**：基于180nm CMOS工艺，利用亚阈值区晶体管的指数特性。
    *   **一一对应映射**：建立算法参数与电路元件的直接联系。权重对应于晶体管的宽长比（Width Ratios），阈值和增益对应于偏置电流（Bias Currents）。
    *   **结构化抗噪**：离散输出在每个单元边界将噪声抑制了至少20倍，阻止了误差在时间步长间的传播。

### 3. 实验设计
*   **数据集/场景**：
    *   **关键词识别 (KWS)**：使用 Google Speech Commands 数据集（识别“yes”）。
    *   **序列建模基准**：包括 Sequential MNIST (sMNIST)、Permuted MNIST (pMNIST)、ListOps。
    *   **语言建模**：Shakespeare 字符级语言建模。
*   **Benchmark（对比方法）**：
    *   **LRU (Linear Recurrent Unit)**：线性循环单元。
    *   **minGRU**：最小化门控循环单元。
    *   **原始 BMRU**。
*   **硬件验证**：使用 Cadence Spectre 进行晶体管级仿真，验证软件模型与物理电路的一致性。

### 4. 资源与算力
*   **软件训练**：使用了 **1 张 NVIDIA Tesla A100 GPU** (40GB VRAM)。每个任务的训练时长约为数小时。
*   **硬件仿真**：在配备 **AMD Ryzen Threadripper 3970X** (32核) 和 256GB RAM 的工作站上运行 Cadence Virtuoso/Spectre 仿真。
*   **实现框架**：软件端使用 JAX/Flax 框架。

### 5. 实验数量与充分性
*   **实验规模**：
    *   软件实验针对每个任务运行了 **5 个随机种子**，并报告了均值和最值范围。
    *   硬件仿真针对 50 个测试样本进行了详细的瞬态分析。
    *   **鲁棒性测试**：进行了 **200 组 Monte Carlo 随机失配分析**（3σ 偏差）以及覆盖 5 个工艺角（TT, FF, SS, FS, SF）、温度（-27°C 到 81°C）和电压波动（±10%）的 **PVT 仿真**。
*   **充分性评价**：实验设计非常全面，不仅涵盖了算法层面的准确率对比，还深入到了电路层面的可靠性验证，客观地展示了模拟电路在非理想情况下的表现。

### 6. 主要结论与发现
*   **极低功耗**：在 KWS 任务中，RNN 核心功耗仅为 **~100 nW**（亚微瓦级）。
*   **高保真度**：软件预测与硬件仿真在信号层面几乎完全吻合，证明软件模型可作为硬件的高效模拟器。
*   **线性边际成本**：增加循环层（Recurrence）的功耗随维度线性增长，而前馈层随维度平方增长。这意味着在现有模拟前馈平台上添加循环能力的成本极低。
*   **抗噪性**：FQ BMRU 在模拟噪声水平下表现稳定，而 LRU 等线性模型在相同噪声下会发生灾难性失效。

### 7. 优点
*   **软硬件深度融合**：不是简单的算法适配，而是从第一性原理出发，让算法特性（离散、滞回）直接服务于硬件需求（抗噪、低功耗）。
*   **参数映射直观**：1:1 的映射关系简化了从训练到部署的流程。
*   **可扩展性**：通过离散化反馈解决了模拟 RNN 无法做深、做大的长期难题。

### 8. 不足与局限
*   **缺乏物理芯片实测**：虽然 Cadence 仿真是工业标准且精度极高，但论文尚未提供流片后的实测数据。
*   **任务复杂度有限**：目前的硬件验证主要集中在二分类 KWS 任务，对于更复杂的语言建模任务，虽然软件端有测试，但硬件层面的大规模集成仍待验证。
*   **应用限制**：该方法依赖于 BMRU 这种特定架构，可能不适用于所有类型的循环动力学需求。

（完）
