---
title: Adapting AlphaEvolve to Optimize Fully Homomorphic Encryption on TPUs
title_zh: 适配 AlphaEvolve 以优化 TPU 上的全同态加密
authors: "Shruthi Gorantala, Jianming Tong, Asra Ali, Baiyu Li, Jonathan Katz, Jeremy Kun, Thomas Steinke, Abhradeep Thakurta, Julian Walker, Amir Yazdanbakhsh"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14718v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 内核与 TPU 硬件之间的协同优化
tldr: 针对全同态加密（FHE）在TPU等硬件上优化难度大的问题，本研究利用AlphaEvolve框架，通过大语言模型驱动的进化搜索，实现了硬件感知的密码学算子自动优化。该方法利用真实硬件反馈指导代码演化，在Google Cloud TPUv5e上显著提升了TFHE和CKKS方案的性能，超越了人工设计的现有最优实现，为加速FHE的大规模应用提供了新途径。
selection_source: fresh_fetch
motivation: 全同态加密计算开销巨大，且在TPU等复杂硬件架构上进行手动底层优化效率低下且难以充分利用资源。
method: 利用AlphaEvolve框架，结合LLM代码生成与进化搜索算法，通过硬件执行反馈和正确性测试自动探索最优算子实现。
result: 在TPUv5e上，该方法使TFHE自举延迟降低了2.5倍，CKKS旋转和乘法延迟分别降低了1.31倍和1.18倍。
conclusion: AlphaEvolve能够有效自动化处理密码学与硬件加速器之间的复杂优化权衡，显著优于人工编写的先进实现。
---

## 摘要
全同态加密 (FHE) 的大规模部署因其沉重的计算开销而受到阻碍。虽然像 Google 张量处理单元 (TPU) 这样的专用硬件加速器可以提供帮助，但将复杂的加密内核映射到此类架构上仍然是一个挑战。高效执行需要基于脉动阵列的矩阵乘法单元 (MXU) 与向量处理单元 (VPU) 之间的协同优化，以及跨向量寄存器堆的数据移动编排。现有的编译器栈通常抽象了底层硬件利用率，要求开发人员采用手动的试错过程，这往往导致执行碎片化和资源利用不足。为了加速这一开发过程，我们使用 AlphaEvolve 来自动探索硬件感知的加密内核优化。我们将优化框架化为一个进化搜索问题，利用 AlphaEvolve 提供的闭环系统，该系统利用了 LLM 驱动的代码生成。我们使用来自硬件执行的真实反馈和严格的正确性测试来指导进化过程。我们在 Google Cloud TPUv5e（一种当代 TPU 架构）上，针对 TFHE (Jaxite) 和 CKKS (CROSS) FHE 方案的原语评估了 AlphaEvolve 的优化效果。在 24 小时的自动探索中，AlphaEvolve 发现了实现层面的优化，相对于人工设计的现有技术，将 TFHE 自举延迟降低了 2.5 倍，将 CKKS 旋转和乘法延迟分别降低了 1.31 倍和 1.18 倍。这些结果表明，AlphaEvolve 可用于使研究人员能够在密码学、编译器和硬件加速器之间权衡优化。

## Abstract
The deployment of Fully Homomorphic Encryption (FHE) at scale is hindered due to its heavy computational overhead. While specialized hardware accelerators like Google Tensor Processing Units (TPUs) can help, mapping complex cryptographic kernels onto such architectures remains a challenge. Efficient execution requires co-optimization between the systolic array-based Matrix Multiplication Unit (MXU) and Vector Processing Units (VPUs), as well as the orchestration of data movement across the vector register files. Existing compiler stacks often abstract low-level hardware utilization, requiring developers to adopt a manual trial-and-error process that often results in fragmented execution and underutilized resources. To accelerate this development process, we use AlphaEvolve to automate the exploration of hardware-aware cryptographic-kernel optimizations. We frame optimization as an evolutionary search problem, utilizing the closed-loop system provided by AlphaEvolve, that leverages LLM-driven code generation. We use real-world feedback from hardware execution and rigorous correctness testing to guide the evolution process. We evaluate AlphaEvolve optimization on primitives for both the TFHE (Jaxite) and CKKS (CROSS) FHE schemes on Google Cloud TPUv5e, a contemporary TPU architecture. Within 24 hours of automated exploration, AlphaEvolve discovered implementation-level optimizations that improve TFHE bootstrap latency by 2.5x and CKKS rotation and multiplication latency by 1.31x and 1.18x, respectively, relative to human-engineered state of the art. These results demonstrate that AlphaEvolve can be used to enable researchers to navigate the optimization trade-offs between cryptography, compilers, and hardware accelerators.

---

## 论文详细总结（自动生成）

### 论文总结：适配 AlphaEvolve 以优化 TPU 上的全同态加密（FHE）

#### 1. 核心问题与研究动机
*   **核心问题**：全同态加密（FHE）虽然能实现密文计算，但其计算和内存开销巨大（比明文慢 2-5 个数量级）。虽然 Google TPU 等硬件加速器可缓解这一问题，但将复杂的加密算子映射到 TPU 架构（如 MXU 矩阵单元与 VPU 向量单元的协同、向量寄存器 VReg 的利用）极其困难。
*   **研究动机**：现有的编译器栈（如 XLA）抽象了底层硬件细节，导致开发者只能通过低效的“试错法”进行手动优化。本文旨在利用 AI Agent 框架（AlphaEvolve）自动化这一过程，在保证加密安全性和正确性的前提下，探索硬件感知的算子优化。

#### 2. 方法论
*   **核心思想**：将加密内核优化建模为一个**进化搜索问题**，利用大语言模型（LLM）驱动代码生成，并通过真实硬件反馈（Hardware-in-the-loop）指导演化。
*   **关键技术细节**：
    *   **AlphaEvolve 框架**：结合 LLM 与进化算法（如岛屿模型或 MAP-Elites），通过“生成-评估-反馈”的闭环不断改进代码。
    *   **多层评估引擎**：
        1.  **正确性检查**：通过随机输入向量进行端到端测试，确保密文计算结果解密后与明文一致。
        2.  **安全性检查**：使用格密码估计器（Lattice Estimator）确保优化后的参数仍满足至少 128 位安全强度。
        3.  **硬件性能反馈**：在真实 TPU 上运行，以微秒级延迟作为奖励分数，并利用 XProf 性能分析工具捕获计算/内存带宽利用率作为 LLM 的诊断反馈。
    *   **优化维度**：涵盖循环展开（参数复用）、细粒度调度（隐藏内存延迟）、消除冗余类型转换、以及针对 XLA 编译器的分块（Tiling）策略。

#### 3. 实验设计
*   **场景与方案**：
    *   **TFHE 方案**：优化 `Jaxite` 库中的 `blind_rotate`（自举操作的核心）。
    *   **CKKS 方案**：优化 `CROSS` 库中的旋转（Rotation）和乘法（Multiplication）算子。
*   **Benchmark（基准）**：对比的是由领域专家手动优化的最先进（State-of-the-art）实现：`Jaxite` 和 `CROSS`。
*   **硬件环境**：Google Cloud TPUv5e。

#### 4. 资源与算力
*   **算力规模**：实验使用了 **100 个 TPUv5e 芯片**（配置为 10 个控制器，每个控制器配备 10 个评估器）。
*   **搜索时长**：在 **24 小时** 的自动化探索内完成了优化。

#### 5. 实验数量与充分性
*   **实验覆盖**：针对 FHE 的两大主流方案（标量型的 TFHE 和向量型的 CKKS）分别进行了实验。
*   **充分性评价**：实验设计较为充分。研究不仅给出了最终的加速比，还深入分析了 AI 发现的具体优化模式（如 TFHE 中的类型转换消除、CKKS 中的 VReg 利用率提升）。通过 100 块 TPU 的大规模并行搜索，验证了该方法在复杂搜索空间中的有效性。

#### 6. 主要结论与发现
*   **性能提升**：
    *   **TFHE**：自举（Bootstrap）延迟降低了 **2.5 倍**。
    *   **CKKS**：旋转延迟降低了 **1.31 倍**，乘法延迟降低了 **1.18 倍**。
*   **核心发现**：AI 能够发现人类专家难以直观察觉的“编译器甜点区”。例如，在 TFHE 中，AI 识别出某些位转换（Bitcast）是冗余的；在 CKKS 中，通过特定的张量切分触发了 XLA 的特殊优化，使自动同态（Automorphism）的延迟占比从 12% 降至 4%。

#### 7. 优点
*   **自动化闭环**：解决了密码学专家不熟悉底层硬件、硬件专家不熟悉密码学算法的跨领域协同难题。
*   **安全性保障**：在 AI 优化流程中嵌入了严格的正确性验证和格密码安全强度评估，降低了 LLM 幻觉带来的风险。
*   **真实反馈**：直接基于硬件执行轨迹（Trace）进行反馈，而非依赖不准确的模拟器。

#### 8. 不足与局限
*   **人工干预**：目前最终生成的代码仍需要人类程序员进行最后审查才能合并到原始库中，尚未实现完全的端到端无人值守。
*   **架构局限性**：实验主要针对 Google TPU 架构，虽然理论上可扩展至 GPU 或其他加速器，但文中未给出跨硬件平台的迁移验证。
*   **搜索成本**：虽然只需 24 小时，但 100 块 TPUv5e 的算力消耗对于普通研究者而言门槛较高。
*   **算法创新限制**：目前的优化主要集中在实现层面（调度、布局），而非发现全新的密码学数学算法。

（完）
