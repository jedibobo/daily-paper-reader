---
title: "VitaLLM: A Versatile and Tiny Accelerator for Mixed-Precision LLM Inference on Edge Devices"
title_zh: VitaLLM：一种用于边缘设备混合精度大语言模型推理的通用微型加速器
authors: "Zi-Wei Lin, Tian-Sheuan Chang"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00320v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于边缘设备LLM推理的混合精度加速器
tldr: VitaLLM 是一款专为边缘设备设计的微型混合精度大模型加速器，支持三值权重（BitNet b1.58）推理。它通过 TINT 和 BoothFlex 双核架构实现高效计算，并引入预测性稀疏注意力机制以减少 KV 缓存访问。在 16nm 工艺下，该芯片以极小的面积和功耗实现了高性能推理，为边缘端混合精度 LLM 部署提供了紧凑且高效的解决方案。
selection_source: fresh_fetch
motivation: 针对边缘设备资源受限的挑战，旨在实现三值权重 LLM 的高效推理并降低 KV 缓存带宽压力。
method: 结合了无乘法器的 TINT 核心、复用 Booth 逻辑的 BoothFlex 核心，以及基于前导一（LO）代理的预测性稀疏注意力机制。
result: 16nm 原型芯片在 1GHz 频率下实现了 72.46 tokens/s 的解码速度，面积仅为 0.214 mm²，显著提升了资源利用率。
conclusion: VitaLLM 证明了在极小硬件开销下实现高性能 BitNet 推理的可行性，为未来边缘混合精度加速器设计提供了蓝图。
---

## 摘要
我们提出了 VitaLLM，这是一种混合精度加速器，能够使三值权重（ternary weight）大语言模型在边缘设备上高效运行。该设计结合了两个计算核心：一个用于三值-整数（ternary-INT）投影的无乘法器 TINT 核心，以及一个复用基-4 Booth 数据通路的 BoothFlex 核心，后者在不重复阵列的情况下同时支持 INT8×INT8 注意力计算和三值-整数的高效利用。一种预测性稀疏注意力机制采用前导 1（LO）代理和无比较 top-K 选择器，将 M 个缓存令牌的键/值（KV）获取量减少约 1-K/M，从而将精确注意力限制在 K 个候选对象中。系统级集成采用多头级流水线和基于 absmax 的量化屏障，以标准化跨核心接口，并将非线性归约与线性分块计算重叠。在 16 nm 工艺、1 GHz/0.8 V 下，硅原型在 0.214 mm² 面积和 120 KB 片上存储内实现了 72.46 tokens/s 的解码速度和 0.88 s 的预填充（64 tokens），同时在消融实验中减少了 KV 流量并提高了利用率。这些结果展示了在边缘级平台上实现实用的 BitNet b1.58 (3B) 推理，并为未来的混合精度大语言模型加速器提供了一个紧凑的蓝图。

## Abstract
We present VitaLLM, a mixed precision accelerator that enables ternary weight large language models to run efficiently on edge devices. The design combines two compute cores, a multiplier free TINT core for ternary-INT projections and a BoothFlex core that reuses a radix-4 Booth datapath for both INT8$\times$INT8 attention and ternary-INT-sustaining utilization without duplicating arrays. A predictive sparse attention mechanism employs a leading-one (LO) surrogate with a comparison-free top-$K$ selector to prune key/value (KV) fetches by roughly $1-K/M$ for $M$ cached tokens, confining exact attention to $K$ candidates. System-level integration uses head-level pipelining and an absmax-based quantization barrier to standardize cross-core interfaces and overlap nonlinear reductions with linear tiles. A 16 nm silicon prototype at 1 GHz/0.8 V achieves 72.46 tokens/s in decode and 0.88 s prefill (64 tokens) within 0.214 mm^2 and 120 KB on-chip memory, while reducing KV traffic and improving utilization in ablations. These results demonstrate practical BitNet b1.58 (3B) inference on edge-class platforms and provide a compact blueprint for future mixed-precision LLM accelerators.

---

## 论文详细总结（自动生成）

这是一份关于论文《VitaLLM: A Versatile and Tiny Accelerator for Mixed-Precision LLM Inference on Edge Devices》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）的规模给边缘设备的计算、存储和功耗带来了巨大压力。虽然三值权重模型（如 BitNet b1.58）能显著压缩模型，但现有硬件在处理三值与 INT8 混合精度时存在阵列利用率低、KV 缓存带宽压力大以及非线性接口复杂等挑战。
*   **研究动机**：设计一种极小规模、高效率的硬件加速器，专门优化三值权重 LLM 在边缘端的推理表现，实现低延迟和低功耗的平衡。

### 2. 方法论与关键技术
VitaLLM 采用了软硬件协同设计，核心技术包括：
*   **双核计算架构**：
    *   **TINT 核心**：无乘法器设计，专门处理三值（-1, 0, +1）与 INT8 激活值的投影运算，通过简单的选择器（Mux）和符号取反实现累加。
    *   **BoothFlex 核心**：复用基-4 Booth 数据通路。在处理 INT8×INT8（注意力计算）时运行 5 个周期，在处理三值-INT 时通过零填充折叠为 1 个周期，避免了硬件阵列的重复设计。
*   **预测性稀疏注意力机制（LOP）**：
    *   使用**前导一（Leading-One, LO）**代理函数近似点积运算，将复杂的乘法简化为移位和加法。
    *   配合**无比较 Top-K 选择器**（基于桶排序和前缀扫描），在获取完整 KV 缓存前先筛选候选键，将 KV 流量减少约 $1-K/M$。
*   **系统级优化**：
    *   **多头级流水线（Head-Level Pipelining）**：在头（Head）粒度上重叠 TINT 的投影运算与 BoothFlex 的注意力运算，减少中间变量存储。
    *   **Absmax 量化屏障**：将非线性归约（如 Softmax/RMSNorm）与线性分块计算重叠，通过统一的量化接口简化跨核心数据交换。

### 3. 实验设计
*   **目标模型**：BitNet b1.58 (3B 参数规模)。
*   **硬件原型**：采用 TSMC 16nm CMOS 工艺，运行频率 1GHz，电压 0.8V。
*   **对比基准（Benchmark）**：
    *   **ASIC 方案**：Slim-Llama (2025)。
    *   **FPGA 方案**：TerEffic (U280), TeLLMe (KV260)。
*   **评估指标**：吞吐量（tokens/s）、预填充时间、芯片面积、功耗、片上内存占用。

### 4. 资源与算力
*   **算力/规格**：16nm 工艺下，核心面积仅 **0.214 mm²**，片上存储仅 **120 KB**。
*   **功耗**：在 1GHz 频率下功耗为 **59.12 mW**。
*   **训练时长**：文中未提及训练细节，该研究专注于推理加速器的硬件设计与实现。

### 5. 实验充分性
*   **实验数量**：包含了端到端性能对比实验和多项消融实验（针对 LOP、流水线调度、BoothFlex 双模切换）。
*   **充分性评价**：实验设计较为充分，对比了 2025 年最新的同类研究（如 Slim-Llama），涵盖了面积、功耗、吞吐量等关键 PPA 指标。消融实验清晰地展示了每个技术点对性能的贡献（如 LOP 减少了 54.86 倍的 KV 流量）。

### 6. 主要结论
*   **高性能与微型化**：VitaLLM 在极小的面积（0.214 mm²）内实现了 72.46 tokens/s 的解码速度，证明了 3B 规模 BitNet 在边缘端运行的可行性。
*   **效率提升**：通过 LOP 机制显著降低了 KV 缓存带宽需求；通过头级流水线和 BoothFlex 复用，整体吞吐量提升了 38.17%。
*   **架构优势**：混合精度接口的标准化有效隐藏了非线性算子的延迟。

### 7. 优点
*   **极高的资源效率**：通过复用 Booth 逻辑和无乘法器设计，将芯片面积压缩到了极致。
*   **创新的稀疏策略**：LOP 预测机制无需重新训练模型即可减少 KV 缓存访问，直接解决了边缘端带宽瓶颈。
*   **紧凑的系统集成**：Absmax 量化屏障和头级流水线设计非常适合资源受限的流式处理场景。

### 8. 不足与局限
*   **模型适用性**：该加速器高度针对三值权重模型（BitNet 风格），对于传统的 FP16 或 INT4 量化模型兼容性有限。
*   **内存依赖**：虽然片上存储极小（120KB），但这意味着对外部 DDR 带宽和 DMA 效率有极高要求，在极低端 MCU 环境下可能受限。
*   **精度验证**：论文主要关注硬件性能，未详细展示 LOP 稀疏注意力对不同下游任务（如 MMLU 评测）准确率的具体影响。

（完）
