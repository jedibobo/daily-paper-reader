---
title: Multibit neural inference in a N-ary crossbar architecture
title_zh: N 进制交叉阵列架构中的多位神经网络推理
authors: "Anatole Moureaux, Anthony Lopes Temporao, Flavio Abreu Araujo"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26979v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 能效神经推理的存内计算
tldr: 本研究针对存内计算（IMC）提出了一种N进制交叉阵列架构的仿真框架，旨在通过模拟磁隧道结（MTJ）的多态特性实现高效的矩阵向量乘法。通过在XOR和MNIST数据集上的测试，验证了该架构在神经网络推理中的可行性。研究深入分析了量化误差、系统非理想性和随机噪声对性能的影响，揭示了随机噪声的平均效应，并确定了优化推理准确率的最佳单元状态数。
selection_source: fresh_fetch
motivation: 旨在利用存内计算技术克服传统计算架构在处理神经网络矩阵向量乘法时的能效瓶颈。
method: 提出了一种通用的N进制交叉阵列仿真框架，并采用四态磁隧道结（MTJ）器件进行电路级推理模拟。
result: "MNIST分类准确率达到94.48%，并发现量化误差是影响精度的核心因素，而单元随机噪声因阵列平均效应影响较小。"
conclusion: 通过平衡量化误差与电阻状态分辨率，可以确定每个单元的最佳状态数以最小化总推理误差。
---

## 摘要
存内计算 (IMC) 通过在存储交叉阵列中计算模拟矩阵-向量乘法 (MVM)，实现了高能效的神经网络推理。在这项工作中，我们提出了一个针对 N 进制交叉阵列架构的仿真框架，该框架能在最小化实现假设的情况下获取 MVM 结果。使用由 (4x4) 个 4 态磁隧道结 (MTJ) 组成的模拟交叉阵列，成功推理了 XOR 和 MNIST 分类任务。MNIST 准确率达到 94.48%（软件基准为 97.56%）。通过使用 PCA 降维，进一步缩小了软硬件性能差距。我们确定权重量化是主要的误差源，并研究了其与系统非理想性和随机噪声共同产生的影响。我们发现，由于阵列的平均效应，特定单元的随机噪声比系统误差的危害更小。最后，我们展示了每个单元的最佳状态数，该数量在量化误差与电阻状态分辨率之间取得平衡，从而最小化总 MVM 误差。

## Abstract
In-memory computing (IMC) enables energy-efficient neural network inference by computing analog matrix-vector multiplications (MVM) in memory crossbar arrays. In this work we present a simulation framework for N-ary crossbar architectures that retrieves MVM results with minimal implementation assumptions. The XOR and MNIST classification tasks were successfully inferred using a simulated crossbar array of (4x4) 4-states magnetic tunnel junctions (MTJ). MNIST accuracy reached 94.48% (vs. 97.56% software baseline). The software-hardware performance gap was further reduced using PCA dimensionality reduction. We identified weight quantization as the primary error source, and studied its impact alongside systematic nonidealities and random noise. We find that cell-specific random noise is less detrimental than systematic errors due to averaging across the array. Finally, we demonstrate an optimal number of states per cell that balances quantization error against resistance state resolution to minimize total MVM error.

---

## 论文详细总结（自动生成）

这篇论文《N 进制交叉阵列架构中的多位神经网络推理》由比利时鲁汶大学的研究团队撰写，探讨了如何利用多态存储器件提升存内计算（IMC）的精度与效率。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统冯·诺依曼架构存在“存储墙”和“功耗墙”瓶颈，限制了大规模 AI 模型的推理效率。
*   **研究动机**：存内计算（IMC）通过在交叉阵列（Crossbar）中直接利用物理定律（欧姆定律和基尔霍夫定律）执行矩阵向量乘法（MVM），能显著降低能耗。然而，目前的 IMC 器件多为二进制或三进制，精度受限。本文旨在研究**多位（Multibit/N-ary）**存储单元在交叉阵列中的应用，并解决模拟信号与数字算法之间的转换与误差补偿问题。

### 2. 方法论
*   **核心思想**：开发一个通用的仿真框架，模拟具有 $N$ 个等间距电阻状态的存储单元（如多态磁隧道结 $M^2TJ$）组成的交叉阵列。
*   **关键技术细节**：
    *   **MVM 结果检索算法**：提出了一种线性缩放公式，仅通过输入信号和测得的输出电压即可还原 MVM 结果，无需改变阵列内部架构。
    *   **权重等间距量化**：采用基于 Powell 最小化方法的算法，将全精度权重映射到 $N$ 个等间距的电阻水平上，以最小化量化误差。
    *   **PCA 降维**：引入主成分分析（PCA）来简化网络输入维度，使大型模型能适配有限尺寸的交叉阵列，并减少多路复用（Multiplexing）的次数。
    *   **误差建模**：将误差分为量化误差、系统性非理想性（如磁层对齐偏差）和单元随机噪声（如热波动和器件差异）。

### 3. 实验设计
*   **数据集/场景**：
    *   **XOR 逻辑运算**：用于验证框架的基本非线性分类能力。
    *   **MNIST 手写数字识别**：用于评估复杂任务下的推理准确率。
*   **Benchmark（基准）**：全精度软件推理结果（Software Baseline）。
*   **对比实验**：
    *   对比了 4 态 $M^2TJ$ 阵列推理与软件基准的准确率。
    *   对比了原始 MNIST 输入与 PCA 降维后的推理表现。
    *   进行了误差敏感度分析，通过改变状态数 $N$、噪声强度 $\sigma$ 和阵列尺寸来观察 RMSE（均方根误差）的变化。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件算力（如 GPU 型号或训练时长）。由于该研究侧重于算法仿真和物理特性建模，其计算工作主要在 Python 环境下（使用 Scipy、Tensorflow 等库）完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 MNIST 进行了 10,000 个样本的测试。
    *   在误差评估中，每组 RMSE 数据均基于 2,000 次随机 MVM 运算的平均值。
    *   对状态数 $N$ 从 1 扫频至 64，对噪声水平进行了多梯度覆盖。
*   **充分性评价**：实验设计较为充分，不仅涵盖了标准分类任务，还深入探讨了物理层面的非理想因素，实验逻辑闭环，能够客观反映 $N$ 进制架构的优劣。

### 6. 主要结论与发现
*   **准确率表现**：在 4 态 MTJ 模拟下，MNIST 准确率达到 **94.48%**（软件基准为 97.56%）。
*   **主要误差源**：**权重量化**是导致精度下降的首要原因。
*   **噪声平均效应**：特定单元的随机噪声对准确率的影响小于系统性误差，因为随机噪声在阵列累加过程中会产生相互抵消的“平均效应”。
*   **最佳状态数（Optimal N）**：发现存在一个最佳的单元状态数。当状态数过多时，电阻状态间的间距变小，导致其对噪声的容忍度下降。因此，在噪声较大的环境下，较少的状态数反而更稳健。

### 7. 优点
*   **通用性强**：提出的 MVM 检索框架不依赖特定的物理器件模型，可推广至 RRAM、PCM 等多种 memristor 技术。
*   **软硬协同优化**：通过 PCA 降维有效解决了硬件尺寸限制与计算复杂度的矛盾。
*   **深入的误差分析**：定量分析了系统误差与随机误差的不同缩放特性，为未来硬件设计提供了理论指导。

### 8. 不足与局限
*   **阵列尺寸限制**：实验主要基于 (4x4) 的微型阵列进行多路复用模拟，未充分讨论超大规模阵列中的寄生电阻（IR drop）和漏电流问题。
*   **缺乏实测数据**：研究完全基于仿真（Digital Twin），虽然参考了已发表的 MTJ 实验数据，但尚未在真实的物理交叉阵列芯片上进行验证。
*   **应用范围**：目前仅验证了简单的全连接层推理，未涉及卷积神经网络（CNN）或 Transformer 等更复杂的架构。

（完）
