---
title: "Real-Time Frame- and Event-based Object Detection with Spiking Neural Networks on Edge Neuromorphic Hardware: Design, Deployment and Benchmark"
title_zh: 边缘类脑硬件上基于脉冲神经网络的实时帧与事件目标检测：设计、部署与基准测试
authors: "Udayanga G. W. K. N. Gamage, Yan Zeng, Cesar Cadena, Matteo Fumagalli, Silvia Tolu"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2605.00146v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对神经形态平台设计 SNN 架构
tldr: 本研究针对无人机和移动机器人等能耗受限场景，提出了一套在Intel Loihi 2神经形态处理器上部署脉冲神经网络（SNN）进行实时目标检测的完整方法。通过设计通用的SNN检测架构及蒸馏感知训练，实现了在保持高精度的同时显著降低功耗。实验对比了Loihi 2与NVIDIA Jetson等传统硬件，证明了SNN在边缘端实时检测中的极高能效比。
selection_source: fresh_fetch
motivation: 旨在解决无人机和机器人等边缘设备在实时目标检测中面临的高能耗与实时性平衡的挑战。
method: 提出了一种针对Loihi 2硬件优化的SNN检测架构设计方法，并引入蒸馏感知训练以弥补SNN与ANN之间的精度差距。
result: "Loihi 2上的SNN实现了最低的单次推理动态能耗，且蒸馏训练使SNN恢复了ANN 87-100%的精度并保持低延迟。"
conclusion: 研究证明了神经形态系统在边缘端实现高能效、实时目标检测的巨大潜力，是传统硬件的有力竞争方案。
---

## 摘要
在能源受限的平台上进行实时目标检测，对于基于无人机的巡检、自主导航和移动机器人等应用至关重要。类脑硬件上的脉冲神经网络 (SNN) 被认为比传统的人工神经网络 (ANN) 具有显著更高的能效。在这项工作中，我们提出了一套针对类脑平台设计通用 SNN 检测架构的综合方法论，以及将其部署在最先进的类脑处理器 Intel Loihi 2 上所需的工程适配。我们在 Loihi 2 上使用基于帧和基于事件的数据集对基于 SNN 的目标检测进行了基准测试，并将其性能与 NVIDIA Jetson Orin Nano、NVIDIA Jetson Nano B01 以及 Apple M2 CPU 上的基于 ANN 的检测进行了对比。我们的结果表明，Loihi 2 上的 SNN 能够执行实时检测，同时在所有平台中实现了最低的单次推理动态能耗。此外，尽管 Jetson Orin Nano 上的 ANN 实现了更高的推理速率，但 Loihi 2 在功耗方面优于其他平台。此外，我们的 ANN 到 SNN 蒸馏感知训练使 SNN 能够恢复其对应 ANN 87-100% 的检测精度，同时保持较低的推理延迟；若不采用蒸馏，SNN 的精度会下降 11-27%。这些结果突显了类脑系统在边缘端实现高效能、实时目标检测的潜力。

## Abstract
Real-time object detection on energy-constrained platforms is critical for applications such as UAV-based inspection, autonomous navigation, and mobile robotics. Spiking neural networks (SNNs) on neuromorphic hardware are believed to be significantly more energy-efficient than conventional artificial neural networks (ANNs). In this work, we present a comprehensive methodology for designing general SNN detection architectures targeting neuromorphic platforms, along with the engineering adaptations required to deploy them on the state-of-the-art Neuromorphic processor, Intel Loihi 2. We benchmark SNN-based object detection on Loihi 2 using both frame-based and event-based datasets, comparing performance with ANN-based detection on the NVIDIA Jetson Orin Nano, NVIDIA Jetson Nano B01, and the Apple M2 CPU. Our results show that SNNs on Loihi 2 can perform real-time detection while achieving the lowest per-inference dynamic energy among all platforms. Also, Loihi 2 outperforms the other platforms in terms of power consumption, though ANNs on Jetson Orin Nano achieve higher inference rates. Furthermore, our ANN-to-SNN distillation-aware training enables SNNs to recover 87-100% of the detection accuracy of their ANN counterparts while maintaining lower inference latency; without distillation, SNNs exhibit an 11-27% accuracy drop. These results highlight the potential of neuromorphic systems for energy-efficient, real-time object detection at the edge.

---

## 论文详细总结（自动生成）

这篇论文对在边缘类脑硬件（Intel Loihi 2）上实现实时、高能效的脉冲神经网络（SNN）目标检测进行了深入研究。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在资源受限的边缘平台（如无人机、移动机器人）上，利用类脑硬件（Neuromorphic Hardware）实现兼顾实时性、高精度和超低功耗的目标检测。
*   **研究动机**：
    *   传统人工神经网络（ANN）在边缘端运行能耗高，且依赖冯·诺依曼架构。
    *   SNN 理论上具有极高能效，但在复杂任务（如目标检测）中的设计、训练及在真实类脑芯片上的部署存在巨大挑战。
    *   现有研究多集中于分类任务，缺乏针对类脑硬件优化的检测架构及跨平台的系统性基准测试。

### 2. 论文提出的方法论
*   **核心思想**：开发了一套从 SNN 架构设计、直接训练、知识蒸馏到类脑硬件部署的完整流水线。
*   **关键技术细节**：
    *   **三种 SNN 架构**：设计了 Model-1（基于 YOLOv3-tiny）、Model-2（轻量化残差结构）和 Model-3（基于事件重建编码器），均控制在 100 万神经元以内以适配单片 Loihi 2。
    *   **无锚点耦合检测头（Anchor-free Coupled Head）**：针对 Loihi 2 不支持分支（Branching）的限制，将分类和回归任务整合进单一耦合头。
    *   **ANN 到 SNN 的知识蒸馏**：引入中间特征图蒸馏、分类逻辑值蒸馏和边界框回归蒸馏（基于 DFL 损失），显著提升直接训练 SNN 的精度。
    *   **硬件适配工程**：
        *   使用 **RepVGG 重参数化**将残差块转换为单路结构。
        *   将标准 BatchNorm 转换为 **Mean-only BatchNorm** 以适配 Loihi 2 硬件限制。
        *   采用 **8 位量化感知训练（QAT）**。
        *   设置特定的脉冲发放阈值和周期性神经元重置策略。

### 3. 实验设计
*   **数据集/场景**：
    *   **事件驱动数据集**：evCIVIL-ev（基础设施缺陷）、Prophesee GEN1（汽车/行人）。
    *   **帧驱动数据集**：evCIVIL-fr、PASCAL VOC。
    *   **新数据集**：引入了一个 **UAV 隧道巡检视频数据集**，包含真实的混凝土缺陷标注。
*   **基准测试平台（Benchmark）**：
    *   **类脑平台**：Intel Loihi 2（Oheo Gulch 开发板）。
    *   **边缘 GPU**：NVIDIA Jetson Nano B01、NVIDIA Jetson Orin Nano。
    *   **高效能 CPU**：Apple M2。
*   **对比指标**：检测精度（mAP 0.5, mAP 0.5:0.95）、推理速率（FPS）、动态能耗、总功耗、能量延迟积（EDP）。

### 4. 资源与算力
*   **训练设备**：使用了一台配备 **NVIDIA RTX 4090 GPU (24 GB VRAM)** 的工作站。
*   **训练时长**：PASCAL VOC 训练 800 次迭代，其他数据集 400 次迭代，均包含 150 次微调迭代。
*   **部署环境**：通过 Intel 的 Oheo Gulch 集群远程访问 Loihi 2 芯片。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 个公开数据集和 1 个自建数据集进行了详尽测试。
    *   **蒸馏稳定性**：每个数据集重复 5 次实验并计算标准差，证明了蒸馏框架的鲁棒性。
    *   **硬件评测**：每个样本运行 5 次迭代，取 20 个随机样本的平均值，符合类脑硬件评测的标准协议。
*   **充分性评价**：实验设计非常充分，涵盖了从算法精度到硬件底层功耗的多个维度，对比了不同代际的边缘硬件，具有很高的客观性和公平性。

### 6. 主要结论与发现
*   **能效优势**：Loihi 2 上的 SNN 实现了所有平台中**最低的单次推理动态能耗**，比 Jetson Orin Nano 高出 1.61–3.74 倍能效。
*   **实时性**：SNN 在 Loihi 2 上达到了 **62–170 FPS**，完全满足实时检测需求。
*   **精度恢复**：通过知识蒸馏，SNN 能够恢复其对应 ANN 精度 的 **87–100%**，解决了直接训练 SNN 精度低的问题。
*   **硬件对比**：虽然 Jetson Orin Nano 的推理速率更高，但 Loihi 2 在总功耗和动态能耗上具有绝对优势。
*   **SOP 影响**：实验证实减少突触操作（SOP）数量能显著提升推理速率并降低能耗。

### 7. 优点
*   **端到端完整性**：不仅停留在算法仿真，还解决了大量真实的硬件部署难题（如 BatchNorm 转换、分支限制）。
*   **跨模态支持**：同时支持基于帧（传统相机）和基于事件（DVS 相机）的数据，适用性广。
*   **蒸馏框架创新**：针对目标检测任务定制了多维度的蒸馏损失，效果显著。
*   **新数据集贡献**：提供的无人机隧道巡检数据集为工业应用提供了宝贵资源。

### 8. 不足与局限
*   **精度差距**：尽管有蒸馏，SNN 的绝对精度仍略低于最先进的轻量化 ANN（如 YOLOv8n）。
*   **硬件限制**：受限于 Loihi 2 当前的架构，无法使用多尺度检测头（FPN 等），这限制了对极小目标的检测能力。
*   **I/O 开销未计入**：基准测试遵循 Intel 指南未计入数据传输（I/O）开销，而在实际无人机系统中，I/O 功耗是不容忽视的。
*   **静态功耗**：Loihi 2 开发板的静态功耗（漏电流等）仍然较高，甚至超过了动态功耗。

（完）
