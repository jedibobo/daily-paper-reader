---
title: "Vision SmolMamba: Spike-Guided Token Pruning for Energy-Efficient Spiking State-Space Vision Models"
title_zh: Vision SmolMamba：用于高能效脉冲状态空间视觉模型的脉冲引导 Token 剪枝
authors: "Dewei Bai, Hongxiang Peng, Yunyun Zeng, Ziyu Zhang, Hong Qu, Yi Zhang"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25570v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 具有令牌剪枝功能的节能脉冲状态空间架构
tldr: 针对脉冲Transformer计算复杂度高且与脉冲稀疏性不匹配的问题，本文提出Vision SmolMamba架构。该模型结合了线性时间的脉冲状态空间模型与创新的脉冲引导时空Token剪枝技术（SST-TP），通过脉冲强度和延迟评估Token重要性并移除冗余。实验证明，该方法在保持高精度的同时，能显著降低能耗，为高效脉冲视觉系统提供了新范式。
selection_source: fresh_fetch
motivation: 脉冲Transformer的二次方复杂度与脉冲神经网络的稀疏、事件驱动特性不匹配，限制了其在大规模视觉任务中的能效表现。
method: 提出一种基于脉冲激活强度和首脉冲延迟的Token剪枝机制（SST-TP），并将其集成到双向脉冲状态空间模型中以实现高效建模。
result: 在ImageNet和DVS等多个基准测试中，该模型比现有脉冲Transformer和Mamba变体降低了至少1.5倍的能耗，且维持了竞争力的精度。
conclusion: 结合脉冲引导的Token稀疏性与状态空间建模，为构建可扩展且高能效的脉冲视觉系统提供了一种高效的范式。
---

## 摘要
脉冲 Transformer 通过脉冲驱动的自注意力机制，在长程视觉建模方面展现出强大潜力。然而，其二次方复杂度的 Token 交互在根本上与脉冲神经计算的稀疏性和事件驱动特性不匹配。为解决这一局限性，我们提出了 Vision SmolMamba，这是一种将脉冲驱动动力学与线性时间选择性递归相结合的高能效脉冲状态空间架构。其核心思想是脉冲引导的时空 Token 剪枝器（SST-TP），它利用脉冲激活强度和首个脉冲延迟来估计 Token 的重要性。该机制在保留显著时空信息的同时逐步移除冗余 Token，从而实现随 Token 稀疏度高效扩展。基于该机制，所提出的 SmolMamba 模块将脉冲事件直接整合到双向状态空间递归中，构建了一个用于高效长程建模的脉冲状态空间视觉骨干网络。在包括 ImageNet-1K、CIFAR10/100、CIFAR10-DVS 和 DVS128 Gesture 在内的静态及基于事件的基准测试上的广泛实验表明，Vision SmolMamba 始终能实现卓越的精度-效率权衡。特别是，与之前的脉冲 Transformer 基准模型和一种 Spiking Mamba 变体相比，它在保持具有竞争力的或更高精度的同时，将估计能耗降低了至少 1.5 倍。这些结果表明，将脉冲引导的 Token 稀疏性与状态空间建模相结合，为脉冲视觉系统提供了一种可扩展且高能效的范式。

## Abstract
Spiking Transformers have shown strong potential for long-range visual modeling through spike-driven self-attention. However, their quadratic token interactions remain fundamentally misaligned with the sparse and event-driven nature of spiking neural computation. To address this limitation, we propose Vision SmolMamba, an energy-efficient spiking state-space architecture that integrates spike-driven dynamics with linear-time selective recurrence. The key idea is a Spike-Guided Spatio-Temporal Token Pruner (SST-TP), which estimates token importance using both spike activation strength and first-spike latency. This mechanism progressively removes redundant tokens while preserving salient spatio-temporal information, enabling efficient scaling with token sparsity. Based on this mechanism, the proposed SmolMamba block incorporates spike events directly into bidirectional state-space recurrence, forming a spiking state-space vision backbone for efficient long-range modeling. Extensive experiments on both static and event-based benchmarks, including ImageNet-1K, CIFAR10/100, CIFAR10-DVS, and DVS128 Gesture, demonstrate that Vision SmolMamba consistently achieves superior accuracy-efficiency trade-offs. In particular, it reduces the estimated energy cost by at least 1.5x compared with prior spiking Transformer baselines and a Spiking Mamba variant while maintaining competitive or improved accuracy. These results demonstrate that combining spike-guided token sparsity with state-space modeling offers a scalable and energy-efficient paradigm for spiking vision systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Vision SmolMamba** 的新型脉冲神经网络（SNN）架构，旨在通过结合状态空间模型（SSM/Mamba）和动态 Token 剪枝技术，解决脉冲视觉模型在处理长程依赖时的计算冗余与能效瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的脉冲 Transformer 虽然能建模长程依赖，但其**二次方复杂度的 Token 交互**与脉冲计算的**稀疏性、事件驱动特性**在本质上是不匹配的，导致在大规模视觉任务中能效优势受限。
*   **研究背景**：SNN 具有高能效潜力，但传统卷积 SNN 感受野受限，而 Transformer 复杂度过高。新兴的 Mamba 架构具有线性复杂度，但其原始设计针对的是稠密的人工神经网络（ANN），未充分利用脉冲信号的空域和时域稀疏性。

### 2. 方法论
论文提出了 **Vision SmolMamba** 架构，其核心组件包括：
*   **脉冲引导的时空 Token 剪枝器 (SST-TP)**：
    *   **空间维度**：利用 Z-score 归一化评估脉冲激活强度，识别并保留“高于平均水平”的显著 Token。
    *   **时间维度**：受生物学“首脉冲延迟”启发，定义时间阈值，优先保留反应迅速（首脉冲出现早）的 Token。
    *   **动态演化**：通过这种双重准则，模型在层与层之间逐步移除冗余 Token（类似生物“蜕皮”过程）。
*   **SmolMamba 模块**：
    *   将脉冲神经元集成到双向 Mamba 递归中，通过线性时间的扫描（Selective Scan）处理 Token。
    *   移除了传统 Mamba 中的门控分支，改用脉冲生成的隐式门控，进一步降低能耗。
*   **掩码感知全局平均池化 (MGAP)**：
    *   针对剪枝后各样本 Token 长度不一的问题，采用掩码机制确保全局特征聚合的无偏性。

### 3. 实验设计
*   **数据集**：
    *   **静态图像**：ImageNet-1K（大规模）、CIFAR-10/100（小规模）。
    *   **事件驱动（神经形态）数据**：CIFAR10-DVS、DVS128 Gesture。
*   **Benchmark 与对比方法**：
    *   **脉冲 Transformer**：Spikformer, Spikingformer, Spike-driven Transformer, SNN-ViT。
    *   **ANN Mamba 变体**：Vim, DyVM。
    *   **其他 SNN 架构**：SEW-ResNet, MS-ResNet 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 GPU** 和 AMD EPYC 7282 16核 CPU。
*   **训练细节**：
    *   ImageNet-1K 训练 300 个 epoch，Batch size 为 128。
    *   CIFAR 训练 300 个 epoch。
    *   DVS 数据集分别训练 106 和 200 个 epoch。
*   **能耗评估**：基于 45nm CMOS 工艺模型（MAC=4.6pJ, AC=0.9pJ）进行理论能耗估计。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单手势识别到复杂大规模图像分类的多种任务，实验设计较为全面。
*   **消融实验**：针对 SST-TP 的各个组件（仅空间剪枝、仅时间剪枝、有无 Z-score 归一化、不同阈值等）进行了详细对比。
*   **客观性**：通过在相同硬件环境下测试推理吞吐量（Throughput），并对比参数量、能耗与精度的权衡，证明了结果的客观性。

### 6. 主要结论与发现
*   **能效显著提升**：在 ImageNet-1K 上，Vision SmolMamba 在保持同等或更高精度的前提下，比主流脉冲 Transformer 降低了 **1.5倍至 2.25倍** 的估计能耗。
*   **精度优势**：在 ImageNet 上达到 78.9% 的 Top-1 精度（10层模型），优于同规模的 Spike-driven Transformer。
*   **吞吐量领先**：在 CIFAR 数据集上，其推理速度（Img/s）显著高于 Spikformer 和 SparseSpikformer。
*   **剪枝稳定性**：Z-score 归一化对于大规模数据集（如 ImageNet）的训练稳定性至关重要，能防止掩码崩溃。

### 7. 优点
*   **创新性结合**：首次将脉冲状态空间模型与动态时空 Token 剪枝结合，填补了 SNN-SSM 混合架构在通用视觉任务上的空白。
*   **生物启发式设计**：引入首脉冲延迟作为剪枝依据，既符合神经科学原理，又有效提升了计算效率。
*   **线性扩展性**：相比 Transformer 的二次方复杂度，该模型在处理高分辨率或长序列输入时具有更好的扩展性。

### 8. 不足与局限
*   **能耗评估局限**：能耗数据基于理论公式估计，而非在真实的类脑芯片（如天机芯、Loihi）上实测，实际硬件上的访存开销未被完全计入。
*   **剪枝风险**：虽然 SST-TP 能减少冗余，但在极端剪枝情况下（如阈值设置过高），可能会丢失细微的视觉特征，导致精度下降。
*   **超参数敏感性**：时间阈值 $\phi_l$ 和空间阈值需要针对不同数据集进行微调，缺乏完全自动化的自适应机制。

（完）
