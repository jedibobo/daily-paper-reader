---
title: DPU or GPU for Accelerating Neural Networks Inference -- Why not both? Split CNN Inference
title_zh: DPU 还是 GPU 用于加速神经网络推理——为何不两者兼得？拆分 CNN 推理
authors: "Ali Emre Oztas, Mahir Demir, James Garside, Mikel Luj'an"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2605.00174v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在DPU和GPU之间划分CNN推理
tldr: 本研究针对边缘设备视频流处理的低延迟需求，提出了一种将CNN推理任务在DPU和GPU之间进行切分（Split CNN Inference）的协同加速方案。通过在Versal VCK190的DPU上处理初始层以减少数据传输，并利用NVIDIA RTX 2080 GPU异步流水线处理后续层，同时引入基于图神经网络（GNN）的自动切分预测方法。实验表明，该方案显著降低了推理延迟，在多种主流模型上均优于单一硬件执行。
selection_source: fresh_fetch
motivation: 传统的单一硬件加速方案在处理边缘设备低延迟推理时仍存在性能瓶颈，未能充分利用异构计算资源的协同潜力。
method: 提出将CNN模型切分，由DPU处理靠近数据源的前几层以减少带宽压力，GPU异步流水线处理剩余层，并利用GNN自动预测最优切分点。
result: "在ResNet、VGG等模型上，该方案比纯DPU执行快2.48倍，比纯GPU执行快3.37倍，且GNN切分预测准确率达96.27%。"
conclusion: 结合DPU和GPU的异构协同推理能有效降低CNN延迟，且基于GNN的自动化切分是实现该目标的有效手段。
---

## 摘要
边缘设备上的视频和图像流传输需要低延迟。为解决这一问题，神经网络（NN）被广泛应用，且先前的工作主要集中在使用单一硬件单元进行加速，如图形处理器（GPU）、现场可编程门阵列（FPGA）和深度学习处理单元（DPU）。然而，通过结合这些单元可以观察到延迟的进一步降低。本文提出了一种跨 DPU 和 GPU 划分 CNN 推理的方法（拆分 CNN 推理）。第一部分在 Versal VCK190 的 AI 引擎（DPU）上运行，由处理输入图像的初始 CNN 层组成。DPU 在靠近数据源的位置处理第一部分。通过异步流水线方式，GPU 运行剩余的层。GPU（NVIDIA RTX 2080）处理第二部分，同时减少了数据源（存储/摄像头）与 GPU 之间的数据传输。此外，提出了一种基于图神经网络（GNN）的划分索引预测方法，以自动执行拆分推理所需的 CNN 划分。本文对 LeNet-5、ResNet18/50/101/152、VGG16 和 MobileNetv2 等成熟模型进行了分析。结果表明，与仅使用 DPU 执行相比，延迟提升高达 2.48 倍；与仅使用 GPU 执行相比，延迟提升高达 3.37 倍。经过训练的 GNN 模型以 96.27% 的准确率在相应设备之间划分层。

## Abstract
Video and image streaming on edge devices requires low latency. To address this, Neural Networks (NNs) are widely used, and prior work mainly focuses on accelerating them with single hardware units such as Graphics Processing Units (GPUs), Field Programmable Gate Arrays (FPGAs), and Deep Learning Processing Units (DPUs). However, further reductions in latency can be observed by combining these units. In this paper, partitioning CNN inference across DPU and GPU (Split CNN Inference) is proposed. The first partition runs on the AI engines (DPU) of a Versal VCK190, which consists of initial CNN layers processing the input images. The DPU processes the first partition near the source of the data. Pipelined asynchronously, a GPU runs the remaining layers. The GPU (NVIDIA RTX 2080) processes the second partition, albeit having reduced the data transfer between the data source (storage/camera) and the GPU. Furthermore, a Graph Neural Network (GNN)-based partition index prediction method is proposed to automate the partitioning of CNNs needed for the Split Inference. Well established models such as LeNet-5, ResNet18/50/101/152, VGG16, and MobileNetv2 are analyzed. Results demonstrate up to 2.48x latency improvement over DPU-only execution and up to 3.37x over GPU-only execution. The trained GNN model splits the layers between the appropriate devices with 96.27% accuracy.

---

## 论文详细总结（自动生成）

这是一份关于论文《DPU or GPU for Accelerating Neural Networks Inference – Why not both? Split CNN Inference》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在边缘设备进行视频和图像流处理时，如何进一步降低卷积神经网络（CNN）的推理延迟。
*   **研究动机**：
    *   **单一硬件局限性**：目前主流方案通常只使用单一加速器（如 GPU、FPGA 或 DPU），但不同硬件在处理不同类型的网络层时各有优劣。
    *   **异构协同潜力**：FPGA/DPU 具有直接连接摄像头（如 MIPI 接口）的优势，适合在数据源附近处理初始层；而 GPU 在处理大规模并行计算（如深层卷积）时效率更高。
    *   **自动化需求**：手动寻找最优的跨设备切分点（Partition Point）耗时耗力，需要一种自动化的预测机制。

### 2. 方法论
*   **核心思想**：提出“拆分 CNN 推理”（Split CNN Inference），将 CNN 模型按层切分为两部分，利用异步流水线（Asynchronous Pipelining）在 DPU 和 GPU 上并行执行。
*   **关键技术细节**：
    *   **硬件分工**：第一部分在 Versal VCK190 的 DPU 上运行（INT8 量化），处理靠近数据源的初始层；第二部分在 NVIDIA RTX 2080 GPU 上运行（FP16 精度），处理剩余层。
    *   **数据传输优化**：研究了两种路径：Path 1（经由主机 DRAM 转发）和 Path 2（利用 PCIe P2P 直接传输至 GPU 显存），以减少通信开销。
    *   **GNN 自动化切分预测**：
        *   将 CNN 模型建模为图（Graph），层为节点，连接为边。
        *   **特征提取**：节点特征包括输入尺寸、卷积核大小、通道数等。
        *   **模型架构**：采用图卷积网络（GCN）结合长短期记忆网络（LSTM）。LSTM 用于捕捉层之间的顺序相关性和累积延迟特征。
        *   **预测目标**：直接预测最优切分索引或预测各部分的执行延迟。

### 3. 实验设计
*   **数据集**：使用 ImageNet 2012 验证集（50,000 张图像）进行精度验证。
*   **测试模型**：涵盖了多种经典架构，包括 LeNet-5（基准/悲观场景）、ResNet18/50/101/152（残差网络）、VGG16（非残差网络）和 MobileNetv2（轻量化/深度可分离卷积）。
*   **Benchmark（对比基准）**：
    *   **DPU-only**：模型完全在 Versal VCK190 DPU 上运行。
    *   **GPU-only**：模型完全在 NVIDIA RTX 2080 GPU 上运行。
*   **切分策略**：对于残差模型，以基本块（Basic Block）为最小切分单位，避免破坏残差连接。

### 4. 资源与算力
*   **硬件设备**：
    *   **FPGA/DPU**：AMD Versal VCK190（包含 AI 引擎）。
    *   **GPU**：NVIDIA RTX 2080。
    *   **CPU**：Intel i9-7900X（作为主机控制数据传输）。
*   **算力消耗**：
    *   **GNN 训练**：使用了 1700 个随机生成的 CNN 模型作为训练集，训练了 200 个 Epoch。
    *   **推理精度**：DPU 部分采用 INT8 量化，GPU 部分采用 FP16。
    *   *注：文中未明确说明 GNN 训练的具体时长（小时/天）。*

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 7 种主流模型进行了详尽的切分点遍历实验（如 ResNet152 进行了 57 种不同切分的测试）。
    *   GNN 预测模型基于 1700 个随机模型进行验证，具有较好的泛化性。
*   **充分性评价**：实验设计较为充分，涵盖了从简单到复杂的多种 CNN 架构，并考虑了 PCIe 不同传输路径的影响。通过穷举搜索确定的“地面真值”（Ground Truth）保证了对比的客观性。

### 6. 主要结论与发现
*   **性能提升**：相比纯 DPU 执行，延迟最高降低 **2.48 倍**；相比纯 GPU 执行，延迟最高降低 **3.37 倍**。
*   **模型特性影响**：
    *   **VGG16** 在该方案下获益最大，因为其密集的 3x3 卷积在 GPU 上并行效率极高，而 DPU 减轻了前端数据压力。
    *   **ResNet/MobileNet** 在 DPU 上处理残差和深度卷积更高效，切分点通常偏向后期。
*   **自动化准确性**：提出的 GNN 模型预测最优切分点的准确率达到 **96.27%**，平均绝对百分比误差（MAPE）仅为 3.93%。
*   **流水线增益**：深度越深的模型，切分粒度越细，越容易实现 DPU 和 GPU 之间的负载均衡。

### 7. 优点
*   **异构协同创新**：打破了“非此即彼”的加速器使用习惯，充分利用了 FPGA 的近感处理能力和 GPU 的高吞吐计算能力。
*   **硬件感知预测**：GNN 模型不仅考虑了网络结构，还结合了硬件特性，实现了高效的自动化部署。
*   **实用性强**：针对流媒体应用，通过异步流水线显著提升了帧率（降低了单帧推理延迟）。

### 8. 不足与局限
*   **硬件组合局限**：实验仅基于特定的 DPU（Versal）和 GPU（RTX 2080）组合，对于其他硬件对（如移动端 GPU + DSP）的适用性需重新训练 GNN。
*   **通信开销风险**：对于层数较少或参数量极小的模型（如 LeNet-5），跨设备的数据传输延迟可能抵消计算加速带来的收益。
*   **量化一致性**：DPU（INT8）与 GPU（FP16）之间的精度切换可能在某些极敏感模型上导致微小的精度波动（尽管文中称已检查精度）。

（完）
