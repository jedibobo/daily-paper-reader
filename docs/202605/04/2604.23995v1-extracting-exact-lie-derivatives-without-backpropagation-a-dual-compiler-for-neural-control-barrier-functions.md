---
title: "Extracting Exact Lie Derivatives Without Backpropagation: A Dual Compiler for Neural Control Barrier Functions"
title_zh: 无需反向传播的精确李导数提取：一种用于神经控制障碍函数的对偶编译器
authors: Mohammadreza Kamaldar
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.23995v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于在嵌入式硬件上部署神经网络的双代数编译器
tldr: 针对嵌入式硬件部署神经控制屏障函数（CBF）时反向传播内存开销大且难以进行最坏情况执行时间分析的问题，本文提出一种双代数编译器。该方法利用对偶数和超对偶数算术，通过前向网络评估即可提取精确的一阶和二阶李导数，消除了动态计算图和内存分配。实验证明该编译器能将训练好的CBF转化为高效的C++代码，在ESP32-S3上实现千赫兹级的安全滤波。
selection_source: fresh_fetch
motivation: 传统的反向传播自动微分在嵌入式设备上存在内存占用高和难以通过安全认证的执行时间分析等局限。
method: 提出一种基于对偶代数的编译器，通过将系统状态和向量场编码为对偶数，实现仅靠前向传播即可计算精确的李导数。
result: 实现了零动态内存分配的AOT编译器，在ESP32-S3微控制器上达到了亚毫秒级的控制周期，支持高频安全过滤。
conclusion: 该方法为在资源受限的嵌入式系统上部署具有严格安全保障的神经网络控制器提供了一种高效且可验证的解决方案。
---

## 摘要
在嵌入式硬件上部署神经网络控制障碍函数 (CBF) 要求在每个控制周期内评估障碍函数值及其沿系统向量场的李导数。用于精确梯度提取的标准机制——反向模式自动微分——会构建动态计算图，其内存占用随网络深度增加而增长，且其反向遍历阻碍了安全关键认证所需的最坏情况执行时间 (WCET) 分析。本文提出了一种对偶代数编译器，仅通过前向网络评估即可提取精确的障碍函数值及其李导数。通过将系统状态编码为对偶数的实部，将目标向量场编码为对偶部，我们证明了每个仿射层和逐元素激活层都支持对偶扩展，从而在传播激活值的同时传播精确的方向导数，且组合后的对偶扩展网络能以零截断误差评估精确的雅可比-向量场乘积。我们推导了对偶传递浮点运算次数和峰值内存占用的闭式表达式，证明了所提算法消除了动态图分配，并利用超对偶算术将该框架扩展到二阶相对度 CBF 所需的二阶李导数。一个开源的提前编译器将训练好的神经 CBF 转换为自包含的 C++ 头文件，在 ESP32-S3 微控制器上通过静态分配的缓冲区组装完整的安全约束，实现零动态内存分配，并在亚毫秒级周期预算下支持千赫兹速率的安全滤波器。

## Abstract
Deploying neural-network control barrier functions (CBFs) on embedded hardware requires evaluating the barrier value and its Lie derivatives along the system vector fields at every control cycle. The standard mechanism for exact gradient extraction, reverse-mode automatic differentiation, constructs a dynamic computational graph whose memory footprint grows with network depth and whose backward traversal obstructs the worst-case execution time analysis required for safety-critical certification. This paper presents a dual-algebraic compiler that extracts the exact barrier value and its Lie derivatives through forward network evaluation alone. Encoding the system state as the real part of a dual number and a target vector field as the dual part, we prove that every affine and componentwise-activation layer admits a dual extension that propagates the exact directional derivative alongside the activation, and that the composed dual-extended network evaluates the exact Jacobian--vector-field product with zero truncation error. We derive closed-form expressions for the dual-pass floating-point operation count and peak memory footprint, prove that the proposed algorithm eliminates dynamic graph allocation, and extend the framework to the second-order Lie derivatives required by relative-degree-two CBFs using hyper-dual arithmetic. An open-source ahead-of-time compiler translates trained neural CBFs into self-contained C++ headers that assemble the complete safety constraint on an ESP32-S3 microcontroller from a statically allocated buffer, with zero dynamic memory allocation and a sub-millisecond cycle budget that supports kilohertz-rate safety filters.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“对偶代数编译器”的技术，旨在解决神经控制障碍函数（Neural CBFs）在嵌入式系统上部署时的计算与内存瓶颈。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：在安全关键型系统（如机器人、自动驾驶）中，控制障碍函数（CBF）用于保证系统的安全性。使用神经网络参数化 CBF 时，硬件必须在每个控制周期内计算精确的李导数（Lie Derivatives）。
*   **现有技术局限**：目前主流方法是使用**反向模式自动微分（Backpropagation）**。这种方法需要构建动态计算图，内存占用随网络深度线性增长，且由于涉及动态内存分配（堆分配），难以进行最坏情况执行时间（WCET）分析，这违反了航空、汽车等领域的安全认证标准（如 ISO 26262）。
*   **研究目标**：开发一种无需反向传播、无需动态内存分配、且能提供精确导数（非有限差分近似）的前向计算机制。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用**对偶数（Dual Numbers）**代数。对偶数形式为 $z = a + b\epsilon$，其中 $\epsilon^2 = 0$。在对偶空间中评估函数，其实部为函数值，对偶部则自动携带精确的方向导数。
*   **关键技术细节**：
    *   **对偶扩展层**：论文证明了神经网络的仿射层（全连接层）和逐元素激活层（如 ReLU, Tanh）都可以扩展到对偶域。
    *   **前向李导数提取**：通过将系统状态编码为实部，向量场编码为对偶部，一次前向传播即可获得雅可比矩阵与向量场的乘积（即李导数），无需显式计算完整的雅可比矩阵。
    *   **批处理算法**：设计了批处理前向传递算法，通过一次矩阵运算同时计算多个输入向量场的李导数。
    *   **二阶扩展**：引入**超对偶数（Hyper-dual Numbers）**，通过四个分量的代数运算提取二阶李导数，适用于相对度为 2 的机械系统（如考虑加速度输入的系统）。
    *   **AOT 编译器**：开发了一个提前（Ahead-of-Time）编译器，将 PyTorch/ONNX 模型直接转换为不含动态分配的 C++ 静态代码。

### 3. 实验设计
*   **实验平台**：ESP32-S3 微控制器（240 MHz, 512 KB SRAM），运行裸机代码（无 OS）。
*   **测试场景**：
    1.  **运动学自行车模型**：4 状态，2 输入，用于自动车道保持。
    2.  **Van der Pol 振荡器**：2 状态，1 输入，测试非线性包络。
    3.  **倒立摆**：2 状态，1 输入，测试二阶李导数（加速度级安全约束）。
*   **Benchmark（基准对比）**：
    *   手动优化的 C++ 反向模式自动微分（实现最佳可能的反向传播性能）。
    *   对于二阶导数，对比了“单次反向传播 + 有限差分”的混合方法。

### 4. 资源与算力
*   **训练阶段**：论文未明确说明训练所用的 GPU 型号或时长，因为重点在于部署。训练使用了标准的 PyTorch 框架。
*   **部署阶段**：
    *   **硬件**：ESP32-S3（双核 Xtensa LX7）。
    *   **算力消耗**：自行车模型约 14k FLOPs，VdP 约 34k FLOPs。
    *   **内存**：实现了 **0 字节** 的动态内存分配（堆分配），完全使用静态缓冲区。

### 5. 实验数量与充分性
*   **实验规模**：针对三个具有代表性的非线性系统进行了验证。每个系统在 ESP32 上运行了 1000 次随机采样状态的迭代，以获取中值执行时间。
*   **充分性评价**：实验设计较为充分。它不仅对比了执行时间，还量化对比了动态内存占用（这是嵌入式认证的关键指标）。通过不同相对度（1阶和2阶）的系统验证了算法的通用性。

### 6. 主要结论与发现
*   **零动态分配**：该编译器彻底消除了运行时的堆分配，使得 WCET 分析成为可能。
*   **性能优势**：
    *   在自行车模型上，尽管计算量是反向传播的 2.99 倍，但由于避开了内存管理开销，实际运行速度反而更快（120μs vs 140μs）。
    *   在二阶导数计算中，比传统的有限差分法快约 2 倍，且具有零截断误差。
*   **高频支持**：所有测试案例均在亚毫秒级完成，能够支持千赫兹（kHz）级别的实时安全过滤。

### 7. 优点
*   **确定性**：静态内存布局和前向计算路径提供了极高的执行时间确定性，符合安全认证需求。
*   **精确性**：相比有限差分法，对偶代数提供的是机器精度级别的精确导数，避免了因近似误差导致的安全违规。
*   **易用性**：提供了开源工具链，可将主流深度学习框架模型直接转化为嵌入式 C++ 代码。

### 8. 不足与局限
*   **算力权衡**：随着系统控制输入维度 $m$ 的增加，前向模式的计算量会线性增长。当 $m$ 非常大时，其算力开销可能会显著超过反向传播。
*   **网络规模限制**：虽然内存占用是静态的，但对于极宽的网络，静态缓冲区的大小仍受限于嵌入式 SRAM 的容量。
*   **应用范围**：目前主要针对前馈神经网络，尚未扩展到循环神经网络（RNN）或 Transformer 等更复杂的架构。

（完）
