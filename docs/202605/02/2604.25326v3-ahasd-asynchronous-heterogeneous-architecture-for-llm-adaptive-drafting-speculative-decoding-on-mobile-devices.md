---
title: "AHASD: Asynchronous Heterogeneous Architecture for LLM Adaptive Drafting Speculative Decoding on Mobile Devices"
title_zh: AHASD：面向移动设备大语言模型自适应草拟投机解码的异步异构架构
authors: "Ma Zirui, Fan Zhihua, Li Wenxing, Wu Haibin, Zhang Fulin, Ye Xiaochun, Li Wenming"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25326v3"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对移动端NPU-PIM系统的异构架构设计
tldr: 针对移动设备上大语言模型投机采样推理效率低的问题，本文提出AHASD架构。该架构利用NPU与PIM（存内计算）的异构特性，通过任务级异步解耦实现草稿生成与验证的并行。引入熵历史感知和时间感知控制机制优化自适应草稿长度，并集成专用硬件单元提升PIM端调度与计算效率，显著提升了推理吞吐量和能效。
selection_source: fresh_fetch
motivation: 传统移动端投机采样在同步执行时存在硬件空闲开销，而异步执行时因草稿长度波动导致严重的计算浪费。
method: 提出任务级异步异构架构，实现PIM生成草稿与NPU验证的解耦并行，并结合动态草稿控制与专用PIM硬件单元优化执行。
result: "实验显示AHASD相比现有最优GPU+PIM方案提升了1.5倍吞吐量和1.24倍能效，且硬件面积开销低于3%。"
conclusion: AHASD通过软硬件协同设计有效解决了移动端LLM投机采样的异步执行瓶颈，为低功耗设备的高效推理提供了新思路。
---

## 摘要
投机解码通过使用小型草拟语言模型（DLM）生成草稿，并使用大型目标语言模型（TLM）进行批量验证，从而提高大语言模型（LLM）的推理效率。然而，在移动端单 NPU-PIM 系统上进行自适应草拟推理时，传统的算子级同步执行面临空闲开销，而异步执行则因草拟长度的波动而导致计算浪费。本文介绍了 AHASD，一种用于投机解码的任务级异步移动 NPU-PIM 异构架构。值得注意的是，AHASD 通过任务级的 DLM-TLM 解耦，实现了 PIM 上的并行草拟和单个 NPU 上的验证；具体而言，它结合了熵历史感知草拟控制和时间感知预验证控制，以动态管理自适应草拟算法的执行和预验证时机，从而抑制基于低置信度草稿的无效草拟。此外，AHASD 在 LPDDR5-PIM 中集成了注意力算法单元和门控任务调度单元，以在 PIM 端实现注意力链路本地化和亚微秒级任务切换。针对不同 LLM 和自适应草拟算法的实验结果表明，与纯 GPU 基准相比，AHASD 的吞吐量提高了 4.2 倍，能效提高了 5.6 倍；与最先进的 GPU+PIM 基准相比，吞吐量提高了 1.5 倍，能效提高了 1.24 倍，且硬件开销低于 DRAM 面积的 3%。

## Abstract
Speculative decoding enhances the inference efficiency of large language models (LLMs) by generating drafts using a small draft language model (DLM) and verifying them in batches with a large target language model (TLM). However, adaptive drafting inference on a mobile single-NPU-PIM system faces idle overhead in traditional operator-level synchronous execution and wasted computation in asynchronous execution due to fluctuations in draft length. This paper introduces AHASD, a task-level asynchronous mobile NPU-PIM heterogeneous architecture for speculative decoding. Notably, AHASD achieves parallel drafting on the PIM and verification on a single NPU through task-level DLM-TLM decoupling and specifically, it incorporates Entropy-History-Aware Drafting Control and Time-Aware Pre-Verification Control to dynamically manage adaptive drafting algorithm execution and pre-verification timing, suppressing invalid drafting based on low-confidence drafts. Additionally, AHASD integrates Attention Algorithm Units and Gated Task Scheduling Units within LPDDR5-PIM to enable attention link localization and sub-microsecond task switching on the PIM side. Experimental results for different LLMs and adaptive drafting algorithms show that AHASD achieves up to 4.2$\times$ in throughput and 5.6$\times$ in energy efficiency improvements over a GPU-only baseline, and 1.5$\times$ in throughput and 1.24$\times$ in energy efficiency gains over the state-of-the-art GPU+PIM baseline, with hardware overhead below 3% of the DRAM area.

---

## 论文详细总结（自动生成）

以下是对论文《AHASD: Asynchronous Heterogeneous Architecture for LLM Adaptive Drafting Speculative Decoding on Mobile Devices》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：投机解码（Speculative Decoding）通过小模型（DLM）草拟和模型（TLM）验证来加速LLM推理。在移动端，NPU（擅长计算）与PIM（存内计算，擅长访存）的异构组合是理想平台。
*   **核心问题**：现有的移动端异构方案在处理“自适应草拟”（草稿长度动态变化）时存在两大瓶颈：
    1.  **算子级同步开销**：由于草稿长度波动，NPU和PIM经常互相等待，导致硬件空闲。
    2.  **异步执行的计算浪费**：在异步模式下，若不加控制地超前草拟，会产生大量低质量（接受率低）的草稿，浪费PIM算力。
*   **研究意义**：提出AHASD架构，旨在通过任务级异步协作和智能硬件控制，解决移动端LLM推理的负载不均衡与无效计算问题。

### 2. 方法论
*   **核心思想**：实现任务级的DLM-TLM解耦，让PIM负责草拟，NPU负责验证，并引入硬件级预测器动态调节执行策略。
*   **关键技术细节**：
    *   **任务级异步框架**：利用异步队列（草稿队列、反馈队列、预验证队列）解耦NPU与PIM，允许两者并行工作。
    *   **熵历史感知草拟控制 (EDC)**：硬件模块通过计算草稿Token的Softmax熵值，并结合历史接受率模式（Pattern History Table），预测当前草拟路径的置信度。若置信度过低，则提前停止草拟以节省算力。
    *   **时间感知预验证控制 (TVC)**：建立双侧延迟模型，实时估算NPU验证剩余时间。在不阻塞NPU的前提下，在PIM端插入小批量预验证，过滤错误草稿。
    *   **硬件增强设计**：在LPDDR5-PIM中集成**注意力算法单元 (AAU)** 处理非线性算子，减少数据跨芯片传输；引入**门控任务调度单元**实现亚微秒级任务切换。

### 3. 实验设计
*   **数据集/场景**：使用 Alpaca 指令微调数据集，模拟移动端单批次（Batch Size = 1）推理场景。
*   **Benchmark**：
    1.  **GPU-Only**：基于 NVIDIA RTX 4090 (Laptop) 的串行投机解码。
    2.  **SpecPIM**：最先进的算子级并行 GPU+PIM 异构加速方案。
*   **对比方法**：在 OPT (1.3B/6.7B)、LLaMA2 (7B/13B)、PaLM-like (8B/30B) 三种模型规模上，测试了 SpecDec++、SVIP、AdaEDL、BanditSpec 四种自适应草拟算法。

### 4. 资源与算力
*   **算力设备**：
    *   基准测试使用了 **NVIDIA GeForce RTX 4090 (Laptop)** GPU（频率 1.335GHz）。
    *   核心实验基于周期精确的模拟器：**ONNXim**（模拟移动 NPU，16 TOPS INT8）和 **SAITPublic-PIMSimulator**（模拟 LPDDR5-PIM，102.4 GOPS INT8）。
*   **训练/推理时长**：文中未明确给出总推理时长，但设定了生成长度为 1024 Token。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了3种主流模型架构、4种自适应算法，并进行了详尽的消融实验（验证了异步架构、AAU、EDC、TVC各自的增益）。
*   **充分性评价**：实验设计较为充分。不仅对比了吞吐量和能效，还利用 CACTI 和 Yosys/OpenROAD 工具对硬件面积开销（28nm工艺）进行了精确评估，确保了方案在硬件实现上的客观性和公平性。

### 6. 主要结论与发现
*   **性能大幅提升**：AHASD 相比纯 GPU 方案，吞吐量提升最高达 **4.2倍**，能效提升 **5.6倍**。
*   **超越 SOTA**：相比 SpecPIM 方案，吞吐量提升 **1.5倍**，能效提升 **1.24倍**。
*   **低硬件成本**：新增的控制逻辑和计算单元仅占 DRAM 芯片面积的 **2.68%**。
*   **关键发现**：任务级异步结合“熵感知”的动态控制，能有效将自适应草拟的接受率损失从 25.1% 挽回至接近同步执行的水平，同时大幅消减硬件空闲时间。

### 7. 优点
*   **软硬协同深度优化**：不只是简单的任务分配，而是针对 LLM 概率特性（熵）设计了专门的硬件预测电路。
*   **解决动态波动问题**：通过 TVC 延迟建模，巧妙解决了自适应草拟中“草稿长度不可知”带来的调度难题。
*   **高能效比**：通过减少无效草拟和跨芯片通信，显著降低了移动设备的功耗负担。

### 8. 不足与局限
*   **仿真依赖**：所有数据均来自模拟器，尚未在真实的移动端 SoC（如集成了 PIM 的商用芯片）上进行实测验证。
*   **算法泛化性**：虽然测试了四种算法，但 EDC 模块中的熵阈值和历史表可能需要针对不同领域的模型进行微调。
*   **量化精度限制**：实验主要基于 INT8 量化，未探讨在更低比特（如 INT4 或 FP8）下控制逻辑的有效性。

（完）
