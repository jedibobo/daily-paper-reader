---
title: A comprehensive evaluation of spatial co-execution on GPUs using MPS and MIG technologies
authors: "Jorge Villarrubia, Luis Costero, Francisco D. Igual, Katzalin Olcoz"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22430v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 通过空间共享方法缓解GPU计算资源利用不足的问题
tldr: "针对现代GPU资源利用率不足的问题，本文对NVIDIA的两大空间共享技术——多进程服务（MPS）和多实例GPU（MIG）进行了全面评估。研究揭示了MPS的灵活性与MIG的隔离性之间的关键权衡：MPS在资源配置合理时可提升30%性能并降低20%能耗，但在内存竞争严重时表现不佳；而MIG通过完全的硬件隔离解决了竞争问题，提供更稳定的性能，但受限于较高的系统开销和固定的划分方案。"
selection_source: fresh_fetch
motivation: 旨在解决现代GPU计算资源日益严重的利用率不足问题，通过空间共享技术实现多应用并行执行以优化资源利用。
method: 对NVIDIA的MPS和MIG技术在不同作业负载下的性能、能耗及资源隔离表现进行了系统性的对比实验与评估。
result: "MPS在最优场景下可提升30%性能并节能20%，但在内存竞争时性能下降30%，而MIG虽能有效隔离干扰但灵活性较低且存在额外开销。"
conclusion: 研究强调了根据作业特征选择共享技术的重要性，建议在追求灵活性时使用MPS，而在需要严格硬件隔离以避免干扰的场景下选择MIG。
---

## Abstract
To mitigate the increasingly common underutilization of computational resources in modern GPUs, spatial sharing methods enable multiple applications to use them simultaneously. This work presents a comprehensive evaluation of NVIDIA's primary technologies to achieve that goal: Multi-Process Service (MPS) and Multi-Instance GPU (MIG). Our findings reveal a crucial trade-off between MPS's flexibility and MIG's isolation, and provide many key insights for improving the co-execution strategy according to job profiles. In the most favorable scenarios, MPS improves performance by up to 30% and reduces energy by about 20%, using its provisioning option to avoid resource monopolization. However, under memory contention, it suffers severe degradation, worsening performance by around 30%. Conversely, MIG's full hardware isolation resolves memory contention, leading to more consistent improvements, but these gains are tempered by higher overhead, and its rigid scheme can degrade performance in certain cases.

---

## 论文详细总结（自动生成）

这篇论文对 NVIDIA GPU 的两种主流空间共享技术——**MPS (Multi-Process Service)** 和 **MIG (Multi-Instance GPU)** 进行了全面且深入的实测评估。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现代 GPU（如 H100、B200）的计算资源（核心数、内存带宽）呈指数级增长，但许多应用（如 AI 推理、中小型 HPC 任务）无法充分利用这些资源，导致严重的资源浪费和能效低下。
*   **核心问题**：虽然 NVIDIA 提供了 MPS 和 MIG 两种空间共享技术来允许应用并行执行，但现有文献缺乏对两者在不同负载组合下的性能、能效、隔离性及扩展性的深度对比。本文旨在揭示两者的权衡（Trade-off），为优化 GPU 协同执行策略提供指导。

### 2. 论文提出的方法论
论文对比了四种执行方案，并分析了其底层技术细节：
*   **Time-slicing（时间分片）**：默认的调度方式，通过快速上下文切换实现时间共享，不属于真正的空间并行。
*   **MPS（多进程服务）**：
    *   **核心思想**：通过一个服务端进程管理统一的 CUDA 上下文，将多个客户端的内核（Kernels）同时发射到硬件队列。
    *   **关键技术**：支持 **SM Provisioning（资源配置）**，可限制每个进程使用的流处理器（SM）百分比，以防止资源垄断。
*   **MIG（多实例 GPU）**：
    *   **核心思想**：在硬件层面将 GPU 划分为多个独立的实例，每个实例拥有隔离的计算单元（GPC）、二级缓存（L2）和内存带宽。
    *   **关键技术**：提供物理级别的强隔离，确保应用间互不干扰。
*   **评估指标**：包括个体减速比（Individual Slowdown）、整体减速比（Overall Slowdown/Makespan）、功耗（Watts）和能耗（Joules）。

### 3. 实验设计
*   **数据集与负载**：
    *   **Altis Benchmark Suite**：涵盖 Sort（排序）、GUPS（随机访问）、KMeans、SRAD、GeMM（矩阵乘法）、LavaMD 等。
    *   **深度学习模型（RDL）**：ResNet50、BERT、Enformer（基因组预测）、PosEGNN（材料科学）。
*   **实验场景**：
    *   **成对协同执行**：将内存密集型（Type M）和计算密集型（Type C）应用两两组合。
    *   **并发扩展性**：对比 2 个应用和 4 个应用并发时的性能表现。
    *   **动态重构**：测试 MIG 实例创建和销毁的延迟与开销。
*   **Benchmark 对象**：以串行执行（Sequential）和默认时间分片（Time-slicing）作为基准。

### 4. 资源与算力
*   **GPU 型号**：主要使用 **NVIDIA A30 (24GB)** 进行详尽测试，并使用 **NVIDIA A100 (40GB)** 进行验证。
*   **算力配置**：开启持久化模式（Persistence Mode），锁定内存频率（1215 MHz）和 SM 频率（1440 MHz），以确保实验的可重复性。
*   **监控工具**：使用 DCGM 采样资源利用率，NVML 测量能耗，Nsight Compute (NCU) 进行细粒度内核分析。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的组合实验，涵盖了 10 种不同负载的成对组合（数十组）、不同 SM 配置下的 MPS 扫描、以及 4 并发下的扩展性实验。
*   **充分性与客观性**：实验设计非常严谨。作者不仅关注总时长，还通过 **基尼系数（Gini coefficient）** 评估了应用间的公平性；同时考虑了功耗与性能的权衡，使用了能量延迟积（EDP）进行综合评价。实验覆盖了从传统 HPC 到现代大模型推理的多种负载，具有很强的代表性。

### 6. 论文的主要结论与发现
*   **MPS 的两极分化**：在非竞争场景下，MPS 表现最优，性能提升可达 30%，能耗降低 20%。但在**内存竞争**严重时，由于缺乏内存系统隔离，性能会剧降 30% 以上。
*   **MIG 的稳定性**：MIG 凭借硬件隔离解决了内存竞争问题，性能表现非常稳定，但其**划分粒度较粗**（如 A30 仅支持 1, 2, 4 实例），且存在固定的硬件开销。
*   **资源垄断问题**：在 MPS 中，计算密集型应用（如 GeMM）会垄断 SM，导致协同应用严重减速；通过 MPS 的 SM Provisioning（如 70/30 分配）可以有效缓解这一问题。
*   **能效权衡**：空间共享虽然增加了瞬时功耗（约 7%-25%），但由于大幅缩短了执行时间，通常能实现净节能。MIG 的硬件隔离成本使其能效略低于最优状态下的 MPS。
*   **动态开销**：MIG 的重构（创建/销毁实例）延迟在亚秒级（约 0.1s），能耗开销极小（约 2J），证明了动态调整分区的可行性。

### 7. 优点
*   **多维度评估**：不仅看性能，还深入分析了功耗、能效、公平性和硬件底层指标（如 L2/DRAM 吞吐）。
*   **实用性强**：为系统管理员和调度器开发者提供了明确的建议（例如：内存密集型任务选 MIG，计算密集型任务选带配置的 MPS）。
*   **负载多样化**：引入了基因组学和材料科学的真实深度学习模型，比单纯使用标准 Benchmark 更具说服力。

### 8. 不足与局限
*   **硬件覆盖**：实验主要基于 Ampere 架构（A30/A100），虽然提及了 Hopper 和 Blackwell，但未在这些最新架构上进行实测（可能受限于设备获取）。
*   **厂商局限**：仅针对 NVIDIA 技术，未涉及 AMD (MxGPU) 或 Intel 的类似分区技术。
*   **并发上限**：受限于 A30 的硬件限制，最高仅测试了 4 并发，未探讨在更高规格 GPU（如 H100）上更高并发度下的复杂干扰情况。

（完）
