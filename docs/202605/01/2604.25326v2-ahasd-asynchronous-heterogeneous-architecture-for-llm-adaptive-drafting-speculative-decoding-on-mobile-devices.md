---
title: "AHASD: Asynchronous Heterogeneous Architecture for LLM Adaptive Drafting Speculative Decoding on Mobile Devices"
title_zh: AHASD：面向移动设备大语言模型自适应草稿投机解码的异步异构架构
authors: "Ma Zirui, Fan Zhihua, Li Wenxing, Wu Haibin, Zhang Fulin, Ye Xiaochun, Li Wenming"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25326v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对移动端NPU-PIM系统的异构架构，以及提升LLM推理效率的任务级解耦技术。
tldr: 针对移动端大模型推理中投机采样存在的同步开销和计算浪费问题，本文提出AHASD架构。该架构采用任务级异步异构设计，在LPDDR5-PIM上进行草稿生成，在NPU上进行验证。通过引入熵历史感知和时间感知控制机制，动态优化草稿长度与验证时机，并集成专用硬件单元提升调度效率。实验表明，AHASD显著提升了推理吞吐量和能效比，且硬件开销极低。
selection_source: fresh_fetch
motivation: 传统移动端投机采样在同步执行时存在空闲开销，而在异步执行时因草稿长度波动导致计算资源浪费。
method: 提出一种任务级异步异构架构，通过解耦DLM与TLM任务，并结合动态草稿控制与PIM硬件优化实现高效并行。
result: "相比现有GPU+PIM方案，AHASD在吞吐量和能效上分别提升了1.5倍和1.24倍，且DRAM面积开销低于3%。"
conclusion: AHASD通过软硬件协同设计，有效解决了移动端大模型投机采样的效率瓶颈，是实现高性能移动端AI推理的有效方案。
---

## 摘要
投机解码通过使用小型草稿语言模型（DLM）生成草稿并由大型目标语言模型（TLM）进行批量验证，提升了大语言模型（LLM）的推理效率。然而，在移动端单 NPU-PIM 系统上，由于草稿长度的波动，传统的算子级同步执行存在空闲开销，而异步执行则会导致计算浪费。本文提出了 AHASD，一种用于投机解码的任务级异步移动 NPU-PIM 异构架构。AHASD 通过任务级 DLM-TLM 解耦，实现了 PIM 上的并行草稿生成与单个 NPU 上的验证。具体而言，它引入了熵历史感知草稿控制和时间感知预验证控制，以动态管理自适应草稿算法的执行与预验证时机，从而抑制基于低置信度草稿的无效生成。此外，AHASD 在 LPDDR5-PIM 中集成了注意力算法单元和门控任务调度单元，实现了 PIM 端的注意力链路本地化和亚微秒级任务切换。针对不同 LLM 和自适应草稿算法的实验结果表明，与纯 GPU 基准相比，AHASD 的吞吐量和能效分别提升了高达 4.2 倍和 5.6 倍；与最先进的 GPU+PIM 基准相比，吞吐量和能效分别提升了 1.5 倍和 1.24 倍，且硬件开销低于 DRAM 面积的 3%。

## Abstract
Speculative decoding enhances the inference efficiency of large language models (LLMs) by generating drafts using a small draft language model (DLM) and verifying them in batches with a large target language model (TLM). However, adaptive drafting inference on a mobile single-NPU-PIM system faces idle overhead in traditional operator-level synchronous execution and wasted computation in asynchronous execution due to fluctuations in draft length. This paper introduces AHASD, a task-level asynchronous mobile NPU-PIM heterogeneous architecture for speculative decoding. Notably, AHASD achieves parallel drafting on the PIM and verification on a single NPU through task-level DLM-TLM decoupling and specifically, it incorporates Entropy-History-Aware Drafting Control and Time-Aware Pre-Verification Control to dynamically manage adaptive drafting algorithm execution and pre-verification timing, suppressing invalid drafting based on low-confidence drafts. Additionally, AHASD integrates Attention Algorithm Units and Gated Task Scheduling Units within LPDDR5-PIM to enable attention link localization and sub-microsecond task switching on the PIM side. Experimental results for different LLMs and adaptive drafting algorithms show that AHASD achieves up to 4.2$\times$ in throughput and 5.6$\times$ in energy efficiency improvements over a GPU-only baseline, and 1.5$\times$ in throughput and 1.24$\times$ in energy efficiency gains over the state-of-the-art GPU+PIM baseline, with hardware overhead below 3\% of the DRAM area.

---

## 论文详细总结（自动生成）

### AHASD：面向移动设备大语言模型自适应草稿投机解码的异步异构架构

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在移动端的推理受限于内存带宽和计算资源。**投机解码（Speculative Decoding）**通过小模型（DLM）预生成草稿、大模型（TLM）批量验证来提升效率。然而，在移动端 NPU-PIM（存内计算）异构系统中，现有的方案存在两大瓶颈：
*   **算子级同步开销：** 传统的同步执行假设草稿长度固定，但在自适应草稿算法下，草稿长度波动剧烈，导致 NPU 和 PIM 频繁互等，产生大量空闲。
*   **异步执行的计算浪费：** 若采用简单的异步调度，DLM 可能会基于低置信度的 Token 生成大量无效草稿，导致计算资源和能量的严重浪费。

#### 2. 方法论：核心思想与关键技术
AHASD 提出了一种**任务级异步异构架构**，核心思想是将内存密集型的 DLM 任务部署在 PIM 上，将计算密集型的 TLM 任务部署在 NPU 上，并实现两者的解耦并行。

*   **任务级异步执行框架：** 通过异步队列（未验证草稿队列、反馈队列、预验证队列）解耦 DLM 和 TLM，允许两者独立推进，减少同步等待。
*   **熵历史感知草稿控制（EDC）：** 硬件级在线学习模块。它通过计算草稿 Token 的平均 Softmax 熵，结合历史接受率模式（Pattern History Table），动态判断是否继续“超前生成”，从而抑制低质量草稿的产生。
*   **时间感知预验证控制（TVC）：** 建立 NPU 和 PIM 的双向延迟模型。根据 NPU 当前验证任务的剩余时间，判断是否在 PIM 端插入小批量预验证（Pre-verification），以确保 NPU 在完成当前任务后能立即获得高概率正确的草稿，减少 NPU 空闲。
*   **PIM 硬件增强：** 在 LPDDR5-PIM 中集成**注意力算法单元（AAU）**处理非线性算子，以及**门控任务调度单元（GTSU）**实现亚微秒级的任务切换。

#### 3. 实验设计
*   **数据集与场景：** 使用 Alpaca 数据集，模拟移动端典型的单批次（Batch Size = 1）推理场景。
*   **模型配置：** 涵盖了 OPT (1.3B/6.7B)、LLaMA2 (7B/13B) 和 PaLM-Like (8B/30B) 三种规模。
*   **自适应算法：** 对比了 SpecDec++、SVIP、AdaEDL 和 BanditSpec 四种主流自适应草稿算法。
*   **Benchmark（对比方法）：**
    1.  **GPU-Only：** 在 NVIDIA RTX 4090 (Laptop) 上交替执行。
    2.  **SpecPIM：** 现有的 SOTA 算子级并行 GPU+PIM 加速方案。

#### 4. 资源与算力
*   **模拟环境：** 实验基于周期精确（Cycle-accurate）的模拟器。使用 **ONNXim** 模拟移动 NPU，使用 **SAITPublic-PIMSimulator** 模拟 PIM。
*   **硬件参数：** NPU 算力为 16 TOPS (INT8)，PIM 算力为 102.4 GOPS，内存带宽 51.2 GB/s。
*   **基准算力：** 对比用的 GPU 为 NVIDIA GeForce RTX 4090 (Laptop)，运行频率 1.335GHz。
*   **训练/运行：** 论文未提及模型训练时长（属于推理加速研究），主要关注推理阶段的周期级模拟。

#### 5. 实验数量与充分性
实验设计较为充分且客观：
*   **多维度评估：** 涵盖了吞吐量（Throughput）、能效比（Energy Efficiency）、接受率（Acceptance Rate）和硬件面积开销。
*   **消融实验：** 逐步验证了异步架构、AAU 单元、EDC 模块和 TVC 模块对性能的贡献，清晰展示了各组件的增益。
*   **广泛性：** 测试了三种不同架构的 LLM 和四种不同的自适应算法，证明了方案的通用性。

#### 6. 主要结论与发现
*   **性能提升：** 相比纯 GPU 基准，AHASD 吞吐量提升达 **4.2倍**，能效提升 **5.6倍**。
*   **超越 SOTA：** 相比 SpecPIM，吞吐量提升 **1.5倍**，能效提升 **1.24倍**。
*   **有效抑制浪费：** EDC 模块成功将因异步导致的接受率下降（约 25%）修复，显著减少了无效计算。
*   **低开销：** 增加的硬件逻辑（EDC, TVC, AAU 等）仅占 LPDDR5-PIM 芯片面积的 **2.68%**，具有极高的工程可行性。

#### 7. 优点
*   **软硬协同：** 不仅是架构改进，还结合了 LLM 推理时的统计特性（熵）进行硬件控制。
*   **解决动态负载不均：** 针对自适应草稿长度波动的痛点，通过异步化和时间建模给出了优雅的解法。
*   **存内计算优化：** AAU 的引入减少了中间数据在 NPU 和内存间的频繁搬运。

#### 8. 不足与局限
*   **模拟器依赖：** 所有结论均基于模拟器数据，缺乏在真实移动 SoC（如骁龙或天玑系列）上的实测验证。
*   **量化限制：** 实验主要针对 INT8 量化模型，未讨论更低比特（如 4-bit）或浮点格式下的表现。
*   **算法依赖：** EDC 模块的效果依赖于自适应算法产生的熵分布特征，对于某些不产生明显熵波动的模型可能增益受限。

（完）
