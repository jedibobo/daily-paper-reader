---
title: "Tempus: A Temporally Scalable Resource-Invariant GEMM Streaming Framework for Versal AI Edge"
title_zh: Tempus：一种面向 Versal AI Edge 的时间可扩展且资源恒定的 GEMM 流式框架
authors: "M. Grailoo, J. Núñez-Yáñez"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00536v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对资源受限边缘 AI 的硬件感知 GEMM 流式框架
tldr: 针对边缘端大模型推理中GEMM计算受限的问题，本文提出Tempus框架。不同于传统的空间扩展方法，Tempus在AMD Versal AI Edge SoC上采用固定的16核AIE-ML计算块，通过时间维度的迭代执行、数据分块及可编程逻辑中的数据复用实现扩展。该框架在保持极低资源消耗的同时，实现了高效的计算重叠与低延迟，为资源受限环境下的LLM推理提供了可持续且可扩展的方案。
selection_source: fresh_fetch
motivation: 现有的空间扩展GEMM加速框架在资源受限的边缘SoC上常因物理实现困难、带宽饱和及资源消耗过大而失效。
method: 采用固定16核AIE-ML计算块，通过时间维度的迭代图执行、算法数据分块以及无死锁的数据流协议实现计算与传输的最大化重叠。
result: 在仅10.677W功耗下实现607 GOPS性能，相比领先的ARIES框架，其平台感知效用提升211.2倍，且实现了零URAM/DSP占用。
conclusion: Tempus证明了通过时间扩展而非空间扩展，可以在极低资源需求下为边缘端LLM推理提供高效、可持续的GEMM加速支持。
---

## 摘要
大语言模型 (LLM) 的缩放法则表明，模型质量随计算规模的增加而提升，但边缘部署对计算、内存和功耗提出了严格限制。由于通用矩阵乘法 (GEMM) 占据了高达 90% 的推理时间，高效的 GEMM 加速对于边缘人工智能至关重要。AMD Versal 自适应 SoC 中的自适应智能引擎 (AIE) 非常适合此任务，但现有的最先进 (SOTA) 框架通过空间缩放来最大化性能，将工作负载分布在数百个核心上——这种方法在资源受限的边缘 SoC 上会因物理实现失败、带宽饱和以及过度的资源消耗而失效。我们提出了 Tempus，这是一种面向 AMD Versal AI Edge SoC 的资源恒定时间 GEMM 框架。Tempus 并非随矩阵大小扩展硬件资源，而是采用由 16 个 AIE-ML 核心组成的固定计算块，通过迭代图执行以及在可编程逻辑 (PL) 中的算法数据分块和复制来实现可扩展性。高速级联流确保了在启动间隔 (II) 为 1 时实现低延迟的部分和归约，而无死锁的 DATAFLOW 协议则最大化了传输与计算的重叠以及 PLIO 的复用。在 GEMM 工作负载上的评估显示，Tempus 在 10.677 W 的总片上功耗下实现了 607 GOPS 的性能。通过平台感知效用 (PAU) 指标表征系统级效率，我们证明了 Tempus 的显著性因子比领先的空间 SOTA 框架 (ARIES) 高出 211.2 倍。此外，该框架保持了 0.00% 的 URAM/DSP 利用率，实现了 22.0 倍的核心节俭度、7.1 倍的功耗节俭度以及 6.3 倍的 I/O 需求降低，为边缘 LLM 推理奠定了可持续且可扩展的基础。

## Abstract
Scaling laws for Large Language Models (LLMs) establish that model quality improves with computational scale, yet edge deployment imposes strict constraints on compute, memory, and power. Since General Matrix Multiplication (GEMM) accounts for up to 90\% of inference time, efficient GEMM acceleration is critical for edge AI. The Adaptive Intelligent Engines available in the AMD Versal adaptive SoCs are well suited for this task, but existing state-of-the-art (SOTA) frameworks maximize performance through spatial scaling, distributing workloads across hundreds of cores -- an approach that fails on resource-limited edge SoCs due to physical implementation failures, bandwidth saturation, and excessive resource consumption. We propose Tempus, a Resource-Invariant Temporal GEMM framework for the AMD Versal AI Edge SoC. Rather than expanding hardware resources with matrix size, Tempus employs a fixed compute block of 16 AIE-ML cores, achieving scalability through iterative graph execution and algorithmic data tiling and replication in the Programmable Logic. High-speed cascade streaming ensures low-latency partial sum reduction at Initiation Interval (II) of 1, while a deadlock-free DATAFLOW protocol maximizes transfer-compute overlap and PLIO reuse. Evaluated on GEMM workloads, Tempus achieves 607 GOPS at 10.677 W total on-chip power. By characterizing system-level efficiency through the Platform-Aware Utility (PAU) metric, we prove that Tempus achieves a 211.2x higher prominence factor than the leading spatial SOTA (ARIES). Furthermore, the framework maintains a 0.00\% utilization of URAM/DSP, yielding 22.0x core frugality, 7.1x power frugality, and a 6.3x reduction in I/O demand, establishing a sustainable, scalable foundation for edge LLM inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tempus: A Temporally Scalable Resource-Invariant GEMM Streaming Framework for Versal AI Edge》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的规模扩展，通用矩阵乘法（GEMM）占据了推理时间的 90% 以上。在边缘端部署时，计算、内存和功耗受到严格限制。
*   **研究动机**：现有的最先进（SOTA）加速框架（如 ARIES, CHARM）主要依赖“空间扩展”，即通过占用数百个核心来换取高性能。这种方法在资源受限的边缘 SoC（如 AMD Versal VE2302）上会失效，导致物理布线失败、带宽饱和，并耗尽可编程逻辑（PL）资源，使得无法集成 Softmax 等其他必要算子。
*   **核心问题**：如何在极低资源占用的前提下，实现高性能且可扩展的 GEMM 加速？

### 2. 方法论：核心思想与关键技术
Tempus 提出了**资源恒定的时间扩展（Temporal Scaling）**方案：
*   **固定计算块**：不随矩阵增大而增加核心，而是固定使用 16 个 AIE-ML 核心。
*   **时间迭代执行**：通过 `GRAPH_ITER_CNT` 参数控制 AIE 图的重复运行次数，将 3D 矩阵乘法映射到固定的 2D 核心阵列上。
*   **算法数据分块与复制**：在 PL 端实现高效的数据分块（Tiling）和重复利用逻辑，通过公式计算复制因子（Replication Factor）以最大化数据复用。
*   **高速级联流（Cascade Streaming）**：利用 AIE 核心间的 512 位级联接口进行部分和归约，实现启动间隔（II）为 1 的流水线，比传统的缓冲区共享方式快约 50%。
*   **无死锁 DATAFLOW 协议**：在 PL 端采用轻量级流式 FIFO，实现计算与数据传输的完全重叠，并确保 0% 的 URAM/DSP 占用。

### 3. 实验设计
*   **硬件平台**：AMD Versal AI Edge VE2302 SoC。
*   **测试场景**：
    *   标准立方矩阵：规模从 $32^3$ 到 $1024^3$。
    *   LLM 典型形状：涵盖解码阶段（窄矩阵）、注意力头逻辑（碎片化矩阵）和前馈网络 FFN（宽/高矩阵），如 LLaMA-2 7B、TinyLlama、BERT 等。
*   **对比基准（Benchmarks）**：ARIES、CHARM 2.0、AUTO MM、AUTO SA 等空间扩展框架。
*   **评价指标**：吞吐量（GOPS）、功耗（W）、资源利用率、平台感知效用（PAU）以及核心/功耗/IO 节俭度（Frugality）。

### 4. 资源与算力
*   **算力使用**：在 VE2302 上使用了 **16 个 AIE-ML 核心**（占总核心数的 47%）。
*   **性能表现**：在 INT16 精度下实现 **607 GOPS** 的吞吐量。
*   **功耗**：总片上功耗仅为 **10.677 W**。
*   **PL 资源**：实现了 **0.00% 的 URAM 和 DSP 占用**，仅消耗少量 LUT（约 6%）和 BRAM（约 62% 用于流式 FIFO）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括不同分块尺寸（DIM）的消融实验、从极小到极大的工作负载扩展实验，以及针对多种主流 LLM 架构的形状适配性实验。
*   **充分性与客观性**：实验不仅在仿真中进行，还在真实硬件板卡上进行了验证。通过引入 PAU 指标，将性能与硬件物理潜力挂钩，使得在不同规模器件（如 VCK190 与 VE2302）之间的对比更加公平、客观。

### 6. 主要结论与发现
*   **时间扩展的可行性**：证明了通过增加迭代次数而非核心数量，可以在边缘端实现稳定的性能扩展。
*   **极高的资源效率**：相比 ARIES 框架，Tempus 的平台感知效用（PAU）高出 **211.2 倍**。
*   **资源节俭**：实现了 22 倍的核心节俭度、7.1 倍的功耗节俭度和 6.3 倍的 I/O 需求降低。
*   **形状无关性**：在处理 LLM 中常见的非对称长方形矩阵时，Tempus 保持了稳定的效率，避免了空间扩展框架常见的“利用率崩溃”现象。

### 7. 优点
*   **架构优雅**：通过软硬件协同设计，解决了边缘端布线拥塞和资源枯竭的痛点。
*   **可持续性**：0% 的 URAM/DSP 占用为部署完整的 LLM 推理流水线（集成其他非 GEMM 算子）留出了充足空间。
*   **高度灵活**：能够自动适配各种不规则的矩阵形状，具有很强的通用性。

### 8. 不足与局限
*   **绝对延迟**：由于核心数量固定且较少，其绝对延迟（Latency）高于使用数百个核心的高端芯片方案。
*   **内存限制**：微内核尺寸（DIM）受限于 AIE 核心的本地内存容量，这在一定程度上限制了进一步降低延迟的潜力。
*   **数值格式**：受限于 AMD Vitis 2024.1 工具链的软件支持，目前主要评估了 INT16/INT32，对 INT4/INT8 等更低精度的硬件原生支持尚未在文中完全展开实验。

（完）
