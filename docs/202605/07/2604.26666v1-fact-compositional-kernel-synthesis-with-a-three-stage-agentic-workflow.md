---
title: "FACT: Compositional Kernel Synthesis with a Three-Stage Agentic Workflow"
title_zh: FACT：基于三阶段智能体工作流的组合式算子合成
authors: "Sina Heidari, Dimitrios S. Nikolopoulos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26666v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于算子内核合成和GPU微架构优化的智能体工作流
tldr: 针对深度学习编译器优化受限及手动编写CUDA代码门槛高的问题，本文提出FACT框架。该框架采用三阶段智能体工作流：首先通过LLM进行图级模式发现，随后基于CUTLASS库实现算子并进行自动调优，最后将多个优化模式组合成完整模块。实验表明，FACT在GEMM任务上比PyTorch基准提升1.06-1.18倍，在MiniGPT块上实现2.79倍端到端加速，有效简化了高性能算子的生成流程。
selection_source: fresh_fetch
motivation: 现有的深度学习编译器优化能力有限，而手动编写高性能CUDA代码对开发者要求极高，且现有LLM生成原始代码往往忽略了成熟库的优化经验。
method: 提出一种三阶段智能体工作流，包括基于LLM的模式发现、基于CUTLASS的算子实现与自动调优，以及多模式的端到端组合。
result: 在NVIDIA A100上，该方法在基础矩阵运算中获得最高1.18倍加速，在MiniGPT模型块中实现了2.79倍的显著性能提升。
conclusion: FACT通过自动化CUTLASS算子合成与调优，为从PyTorch计算图到高性能部署算子提供了一条实用且高效的路径。
---

## 摘要
深度学习编译器和供应商库提供了强大的基准性能，但受限于有限的、由工程师维护的算子库。当这些库缺失所需的优化时，从业者通常会改用手写的 CUDA 或 CUTLASS，这要求具备 GPU 微架构和 C++ 模板元编程方面的专业知识。最近基于大语言模型（LLM）的智能体专注于生成原始 CUDA 算子，这迫使它们重新发现那些已经编码在成熟库中的优化。我们提出了 FACT（智能体 CUTLASS 转译框架），该框架采用三阶段智能体驱动的工作流，通过多模式组合优化 PyTorch 模块，同时将合成过程建立在 CUTLASS C++ 的基础之上。(1) 模式发现：LLM 智能体检查追踪图（traced graph），将子图与优化规则进行匹配，从特定架构的索引中检索经过验证的示例，并输出优先级排序后的模式。(2) 模式实现：每个模式都被实现为封装在 PyTorch 扩展中的 CUTLASS 算子，经过验证，并通过对从 CUTLASS 层级结构中推断出的参数进行扫描来进行自动调优。(3) 模式组合：将这些扩展共同加载到一个组合模块中，进行端到端基准测试。我们在 NVIDIA A100 上使用 KernelBench 评估框架和提供的模块对该工作流进行了评估。在 Level 1 中，我们将该工作流应用于三个 GEMM 工作负载（方阵乘法、批量矩阵乘法和大 K 矩阵乘法）。自动调优的 CUTLASS 算子相比 PyTorch cuBLAS 基准提升了 1.06 倍至 1.18 倍。在 Level 3 MiniGPT 块上，将融合多头注意力与融合 MLP GEMM+GELU 相组合，实现了 2.79 倍的端到端加速。我们的工作将智能体图级模式发现与自动调优及动态模式表相结合，通过自动化 CUTLASS 算子合成和自动调优，为从追踪的 PyTorch 到可部署算子提供了一条切实可行的路径。

## Abstract
Deep learning compilers and vendor libraries deliver strong baseline performance but are bounded by finite, engineer-curated catalogs. When these omit needed optimizations, practitioners substitute hand-written CUDA or CUTLASS, demanding expertise in GPU microarchitecture and C++ template metaprogramming. Recent LLM-based agents target kernel generation in raw CUDA, forcing rediscovery of optimizations already encoded in mature libraries.   We present FACT (Framework for Agentic CUTLASS Transpilation), a framework that employs a three-stage, agent-driven workflow optimizing PyTorch modules through multi-pattern composition while grounding synthesis in CUTLASS C++. (1) Pattern discovery: an LLM agent inspects the traced graph, matches subgraphs to optimization rules, retrieves vetted examples from an architecture-specific index, and outputs prioritized patterns. (2) Pattern realization: each pattern is implemented as a CUTLASS kernel wrapped in a PyTorch extension, verified, and auto-tuned by sweeping parameters inferred from the CUTLASS hierarchy. (3) Pattern composition: extensions are loaded together into a single composed module for end-to-end benchmarking.   We evaluate the workflow using KernelBench's evaluation framework and provided modules on an NVIDIA A100. On Level 1, we apply the workflow to three GEMM workloads (square matrix multiply, batched matrix multiply, and large-$K$ matrix multiply). Auto-tuned CUTLASS kernels improve over PyTorch cuBLAS baseline by $1.06\times$--$1.18\times$. On Level 3 MiniGPT block, composing fused multi-head attention with fused MLP GEMM+GELU yields $2.79\times$ end-to-end speedup. Our work couples agentic graph-level pattern discovery with auto-tuning and a dynamic pattern table, offering a practical path from traced PyTorch to deployable kernels by automating CUTLASS kernel synthesis and auto-tuning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **FACT**（Framework for Agentic CUTLASS Transpilation）的框架，旨在通过大语言模型（LLM）驱动的智能体工作流，自动化地将 PyTorch 计算图转化为基于高性能库 CUTLASS 的优化 GPU 算子。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：深度学习编译器（如 TVM、PyTorch Inductor）受限于预定义的优化模式，而手动编写高性能 CUDA/CUTLASS 代码需要极高的专家知识。现有的 LLM 智能体通常生成原始 CUDA 代码，这导致它们在不断“重新发明轮子”，忽略了成熟库（如 CUTLASS）中已有的架构级优化。
*   **整体含义**：FACT 试图通过“以库为基础的合成”（Library-grounded synthesis）来解决这一矛盾。它不让 LLM 从零写代码，而是让 LLM 学习如何调用和组合 CUTLASS 模板，从而利用已有的微架构优化，并实现自动化的算子融合与调优。

### 2. 方法论：核心思想与关键技术
FACT 采用了一个三阶段的智能体驱动工作流：
*   **阶段 1：模式发现（Pattern Discovery）**
    *   智能体分析 PyTorch 追踪到的计算图，识别可优化的子图（如 GEMM、注意力机制）。
    *   从特定架构的索引中检索经过验证的 CUTLASS 示例，并根据性能潜力对优化模式进行排序。
*   **阶段 2：模式实现（Pattern Realization）**
    *   **三层级合成**：在 **Tile 级**（配置 Tensor Core 指令）、**Kernel 级**（选择流水线策略，如 Ampere 的多级流水线或 Hopper 的 Warp 特化）和 **Grid 级**（选择调度策略，如 Stream-K）进行合成。
    *   **自动调优**：智能体从 CUTLASS 测试文件中推断搜索空间，自动扫描 Tile 形状、流水线阶段等参数，寻找最优配置。
    *   **动态模式表**：将成功验证和调优的算子存入动态表，供未来跨模型复用。
*   **阶段 3：模式组合（Pattern Composition）**
    *   将多个优化后的算子（如融合了 GELU 的 MLP 和 FMHA）重新组合进原始计算图，加载为 PyTorch 扩展，进行端到端基准测试。

### 3. 实验设计
*   **数据集/场景**：采用了 **KernelBench** 评估框架。
*   **任务级别**：
    *   **Level 1**：基础矩阵乘法（方阵、批量 GEMM、大 K 维度 GEMM），分别测试不同的调度策略（Data-Parallel, Batched, Stream-K）。
    *   **Level 3**：MiniGPT 块（包含多头注意力 FMHA 和 MLP 融合算子）。
*   **对比方法**：
    *   PyTorch Eager 模式（调用 cuBLAS）。
    *   PyTorch Inductor 编译器（max-autotune 模式）。
    *   Torch-TensorRT。

### 4. 资源与算力
*   **硬件**：NVIDIA A100-SXM4-80GB GPU（Ampere 架构）。
*   **软件**：CUTLASS 3.8.0, CUDA 12.4, PyTorch 2.4.1。
*   **算力细节**：文中未详细说明 LLM 智能体本身的训练时长（因为它使用的是现成的 LLM），但详细记录了自动调优过程中的配置扫描数量（例如在方阵 GEMM 中扫描了 98 种配置）。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 种不同类型的 GEMM 算子和 1 个复杂的 Transformer 模块进行了深入测试。
*   **充分性**：实验涵盖了从单算子到多算子组合的完整链路，并进行了消融实验（如单独测试 FMHA 或 MLP 融合对总性能的贡献）。
*   **客观性**：对比了主流的编译器后端（Inductor 和 TensorRT），并使用了标准的 KernelBench 框架，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：在 Level 1 任务中，FACT 生成的算子比 PyTorch cuBLAS 基准快 **1.06x – 1.18x**。
*   **端到端加速**：在 Level 3 MiniGPT 块上，通过组合 FMHA 和融合 MLP 算子，实现了 **2.79x** 的端到端加速。
*   **超越编译器**：FACT 的性能显著优于 PyTorch Inductor（1.86x）和 Torch-TensorRT（1.81x），证明了基于库的专家级模板合成在复杂模式下的优越性。
*   **调优的重要性**：自动调优使性能比默认配置提升了 14% 到 34%，证明了系统化搜索 Tile 形状和流水线阶段的必要性。

### 7. 优点
*   **结合了库的深度与智能体的灵活性**：避免了 LLM 生成低效原始代码的问题，同时解决了编译器模式固定的局限。
*   **三层级抽象**：清晰地划分了 GPU 优化的不同维度（Tile, Kernel, Grid），使智能体的推理更有条理。
*   **动态累积**：动态模式表允许系统随着处理更多任务而变得更强大。

### 8. 不足与局限
*   **硬件覆盖有限**：目前主要在 A100（Ampere）上验证，虽然提到了 Hopper（H200）的支持，但相关实验仍在进行中。
*   **任务多样性**：Level 3 实验仅展示了 MiniGPT 块，尚未覆盖更广泛的模型架构（如卷积网络或 GNN）。
*   **依赖 LLM 质量**：模式发现和代码合成的成功率仍高度依赖于底层 LLM 的推理能力和对 CUTLASS API 的理解。

（完）
