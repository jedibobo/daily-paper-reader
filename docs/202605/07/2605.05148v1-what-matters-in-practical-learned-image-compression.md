---
title: What Matters in Practical Learned Image Compression
title_zh: 实用学习型图像压缩的关键因素
authors: "Kedar Tatwawadi, Parisa Rahimzadeh, Zhanghao Sun, Zhiqi Chen, Ziyun Yang, Sanjay Nair, Divija Hasteer, Oren Rippel"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05148v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对设备端运行时间的性能感知神经架构搜索
tldr: 本研究旨在解决学习型图像压缩在感知质量与运行速度之间的平衡难题。通过对模型设计选择进行全面研究，并结合性能感知的神经架构搜索（NAS），作者提出了一种新型编解码器。该方案在保持极高压缩效率的同时，实现了在移动设备上的实时运行，显著优于现有传统及学习型标准，为实用化感知图像压缩提供了新范式。
selection_source: fresh_fetch
motivation: 现有的学习型图像压缩方案难以在追求人类视觉感知质量的同时，满足实际应用中的运行速度要求。
method: 通过对建模选择进行全面研究并结合性能感知的神经架构搜索，在数百万种配置中筛选出兼顾速度与感知性能的最优模型。
result: 该编解码器在iPhone 17 Pro Max上处理12MP图像仅需百毫秒级，且比AV1、VVC等标准节省2.3-3倍码率。
conclusion: 本研究证明了通过合理的架构搜索与感知优化，可以构建出兼具高性能与实用性的学习型图像压缩系统。
---

## 摘要
与硬编码的传统编解码器相比，学习型编解码器展现出的主要优势之一是能够直接针对人类视觉系统进行优化。尽管具有这种潜力，但目前尚未提出一种既具备感知性又实用的图像编解码器。在这项工作中，我们旨在填补这一空白。我们对决定实用学习型图像编解码器设计的关键建模选择进行了全面研究，同时针对感知质量和运行时间进行了联合优化，并在消融实验中包含了几项新技术。随后，我们在数百万个骨干网络配置上执行了性能感知的神经架构搜索，以识别出在达到目标设备端运行时间的同时，最大化感知指标所捕获的压缩性能的模型。我们结合各种优化手段构建了一种新型编解码器，在速度和感知质量之间实现了显著改进的权衡。基于严格的主观用户研究，与 AV1、AV2、VVC、ECM 和 JPEG-AI 相比，它节省了 2.3-3 倍的码率；与最佳的学习型编解码器替代方案相比，节省了 20-40% 的码率。同时，在 iPhone 17 Pro Max 上，它编码 1200 万像素图像的速度快至 230 毫秒，解码速度为 150 毫秒，比大多数在 V100 GPU 上运行的顶级基于机器学习的编解码器还要快。

## Abstract
One of the major differentiators unlocked by learned codecs relative to their hard-coded traditional counterparts is their ability to be optimized directly to appeal to the human visual system. Despite this potential, a perceptual yet practical image codec is yet to be proposed.   In this work, we aim to close this gap. We conduct a comprehensive study of the key modeling choices that govern the design of a practical learned image codec, jointly optimized for perceptual quality and runtime -- including within the ablations several novel techniques. We then perform performance-aware neural architecture search over millions of backbone configurations to identify models that achieve the target on-device runtime while maximizing compression performance as captured by perceptual metrics.   We combine the various optimizations to construct a new codec that achieves a significantly improved tradeoff between speed and perceptual quality. Based on rigorous subjective user studies, it provides 2.3-3x bitrate savings against AV1, AV2, VVC, ECM and JPEG-AI, and 20-40% bitrate savings against the best learned codec alternatives. At the same time, on an iPhone 17 Pro Max, it encodes 12MP images as fast as 230ms, and decodes them in 150ms -- faster than most top ML-based codecs run on a V100 GPU.

---

## 论文详细总结（自动生成）

这是一份关于论文《What Matters in Practical Learned Image Compression》（实用学习型图像压缩的关键因素）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管学习型图像压缩（LIC）在理论上可以通过优化直接提升人类视觉感知质量，但现有的感知型编解码器普遍存在运行速度极慢（无法在移动设备实时运行）、缺乏跨平台鲁棒性、以及缺乏有效的码率控制等问题。
*   **核心问题**：如何在保证极高感知质量的同时，实现满足实际应用要求的运行速度和跨平台部署能力？
*   **整体含义**：本文旨在填补学术研究与工业部署之间的空白，通过系统性地研究建模选择和神经架构搜索（NAS），提出了一种名为 **PICO**（Perceptual Image Codec）的实用化感知图像编解码器。

### 2. 方法论：核心思想与关键技术
*   **架构设计**：
    *   **超先验架构改进**：将超解码器拆分为“尺度解码器”（Scale Decoder，负责熵编码参数，需位精确）和“上下文解码器”（Context Decoder）。
    *   **跨平台鲁棒性**：尺度解码器量化为 UINT8 并运行在 CPU 上，以确保不同硬件间的位精确一致性，防止熵解码失败。
    *   **ConvScale 模块**：引入可重参数化的学习缩放因子，在推理时不增加计算开销的情况下提升模型表达能力。
    *   **One-shot 上下文模型**：采用空间自回归（如棋盘格或 2x2 网格）但在加速器上一次性完成，避免了传统自回归模型在 CPU 和 GPU 间频繁切换导致的延迟。
    *   **Haar 重采样**：使用 Haar 小波代替传统的步长卷积或像素重组，提供更好的多尺度表示。
*   **训练策略与损失函数**：
    *   **组合损失**：$D = MSE + LPIPS + MS-SSIM + TAL + TextFidelity + GAN$。
    *   **针对性优化**：引入 **TAL（平铺伪影损失）** 解决分块处理带来的边界颜色不匹配；引入 **TextFidelity（文本保真度损失）** 通过文本检测掩码强化文字区域的清晰度。
*   **性能感知 NAS**：
    *   在 140 万个候选配置中，通过 kMACs 过滤、设备端（iPhone 16 Pro）实际运行时间测量、以及初步训练性能筛选，最终选出最优骨干网络。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：约 9 万张通用图像、2.3 万张文本图像，以及来自 Div2K、CLIC 和 Flickr2K 的 2.8 万张高分辨率图像。
    *   **测试集**：CLIC 2020 Test（主）、Kodak、DIV2K。
*   **Benchmark（基准）**：
    *   **传统标准**：HEIC, BPG, AV1, VVC (VTM), AV2, ECM。
    *   **学习型编解码器**：HiFiC, JPEG-AI (HOP/BOP), MLIC++, CDC, TCM, MRIC, C3-WD, DCVC-RT。
*   **评估指标**：主观 Elo 评分（基于 7.5 万次两两比较）、CMMD、FID、LPIPS、PSNR。

### 4. 资源与算力
*   **训练资源**：文中未明确给出具体的 GPU 型号、数量或总训练时长。提到使用了 Adam 优化器，并分为 MSE 预训练和感知微调两个阶段。
*   **推理/测试资源**：主要在 **iPhone 17 Pro Max** 上进行基准测试。
*   **模型规模**：编码器 15.2M 参数（30.4MB），解码器 9.6M 参数（19.4MB）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **NAS 搜索**：涵盖了 140 万种配置，并对其中 1000 个模型进行了部分训练，对前 20 个模型进行了完整训练。
    *   **主观研究**：招募了 610 名独立评审员，进行了 74,925 次成对比较，规模巨大且符合工业标准。
    *   **消融实验**：针对自回归方式、量化宽度、学习缩放、重采样方式、以及特定损失函数（文本/平铺）均做了详细对比。
*   **充分性评价**：实验设计非常充分且客观。通过大规模主观测试弥补了客观指标（如 PSNR）在感知评价上的不足，且对比了几乎所有最先进的传统和学习型算法。

### 6. 主要结论与发现
*   **性能领先**：在相同感知质量下，PICO 比 AV1、VVC 等传统标准节省 **2.3-3 倍** 码率；比最强的学习型感知编解码器（如 HiFiC）节省 **20-40%** 码率。
*   **速度极快**：在 iPhone 17 Pro Max 上处理 12MP 图像，编码仅需 230ms，解码仅需 150ms，达到了实用级别。
*   **感知与指标的矛盾**：再次验证了 PSNR 无法反映人类视觉偏好，优化感知质量必然导致 PSNR 下降。
*   **关键因素**：NAS 发现，在相同的计算预算下，增加低分辨率层的通道数比增加高分辨率层的重复次数更有利于性能。

### 7. 优点
*   **极强的实用性**：不仅关注压缩率，还解决了跨平台一致性、分块伪影、文本清晰度等实际部署中的痛点。
*   **软硬件结合优化**：通过 NAS 直接针对目标硬件（iPhone）优化运行时间，而非仅仅优化理论上的 FLOPs。
*   **主观评价权威**：采用了大规模、盲测的 A/B 测试和 Elo 评分系统，结果更具说服力。

### 8. 不足与局限
*   **合成内容表现**：作者承认 PICO 针对自然内容优化，在极简单的合成内容（如卡通）上，由于其生成式特性，码率可能高于传统编解码器。
*   **算力细节缺失**：未披露训练所需的具体算力资源，对于想要复现研究的机构来说缺乏参考。
*   **硬件依赖性**：虽然具有跨平台鲁棒性，但 NAS 结果是针对特定移动平台优化的，在其他架构（如桌面端 CPU/GPU）上可能不是最优配置。

（完）
