---
title: "Delta-Based Neural Architecture Search: LLM Fine-Tuning via Code Diffs"
title_zh: 基于增量的神经架构搜索：通过代码差异进行大语言模型微调
authors: "Santosh Premi Adhikari, Radu Timofte, Dmitry Ignatov"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04903v1"
tags: ["query:hwnas"]
score: 7.0
evidence: 基于LLM的增量式神经架构搜索
tldr: "针对现有大语言模型（LLM）在神经架构搜索（NAS）中生成完整代码导致的高计算成本和冗余问题，本文提出 Delta-Code Generation 方法。该方法通过 LoRA 微调 LLM 生成统一差异（diffs）来改进基准架构。实验表明，该方法在多个数据集上显著提高了生成代码的有效率和模型准确率，同时将输出长度减少了 75-85%，为 LLM 驱动的 NAS 提供了一种高效且通用的新途径。"
selection_source: fresh_fetch
motivation: 现有的 LLM 驱动 NAS 方法通常从头生成完整代码，存在计算成本高、代码冗余且有效率低的问题。
method: 提出 Delta-Code Generation，通过 LoRA 微调 LLM 生成紧凑的统一差异（deltas）来迭代优化基准架构，并利用 MinHash-Jaccard 过滤保证结构多样性。
result: "在 CIFAR-10 等六个数据集上，该方法将代码有效率提升至 75.3%，首轮准确率最高达 85.5%，且代码长度缩减了 75-85%。"
conclusion: 基于增量（Delta）的代码生成是一种 Token 效率高、跨领域且模型无关的 NAS 替代方案，优于传统的全量模型合成。
---

## 摘要
大语言模型（LLMs）在神经架构生成方面展现出强大的潜力，但现有方法通常从头开始生成完整的模型实现，这不仅计算成本高昂，而且产生的代码冗长。我们提出了增量代码生成（Delta-Code Generation），即通过微调后的 LLM 生成紧凑的统一差异（deltas）来改进基准架构，而非合成整个模型。我们的流水线通过 LoRA 在 LEMUR 数据集精选的架构上对 LLM 进行迭代微调，并利用 MinHash-Jaccard 新颖性过滤来保证结构多样性。我们在六个数据集（CIFAR-10、CIFAR-100、MNIST、SVHN、ImageNette、CelebA）上评估了三种 7B 级 LLM——DeepSeek-Coder-7B、Qwen2.5-Coder-7B 和 Mistral-7B，采用了 22 个周期的协议（每个 LLM 生成 1,100 个候选架构）。这三种模型均显著超越了全量生成基准（有效率 50.6%，平均首轮准确率 42.3%）：DeepSeek-Coder 达到了 75.3% 的有效率和 65.8% 的平均准确率；Qwen2.5-Coder 为 72.1%/64.6%；Mistral 为 66.6%/66.1%。在 CIFAR-10 上，最佳首轮准确率分别达到 85.5% (Mistral)、85.2% (DeepSeek) 和 80.6% (Qwen)，远高于全量生成的 63.98% 以及 Gu 等人同期方法的 71.5%。输出长度为 30-50 行，而全量生成则超过 200 行（减少了 75-85%）。一项 50 轮次的实验证实，1 轮次代理评估能够保持排名一致性（Mistral：Spearman ρ = 0.926）。基于增量的生成是一种 Token 高效、多领域且与 LLM 无关的方案，可作为 LLM 驱动的神经架构搜索（NAS）中全量模型合成的替代方案。

## Abstract
Large language models (LLMs) show strong potential for neural architecture generation, yet existing approaches produce complete model implementations from scratch -- computationally expensive and yielding verbose code. We propose Delta-Code Generation, where fine-tuned LLMs generate compact unified diffs (deltas) to refine baseline architectures rather than synthesizing entire models. Our pipeline iteratively fine-tunes the LLM via LoRA on curated architectures from the LEMUR dataset, with MinHash-Jaccard novelty filtering for structural diversity. We evaluate three 7B-class LLMs -- DeepSeek-Coder-7B, Qwen2.5-Coder-7B, and Mistral-7B -- across six datasets (CIFAR-10, CIFAR-100, MNIST, SVHN, ImageNette, CelebA) using a 22-cycle protocol (1,100 candidates per LLM). All three substantially surpass the full-generation baseline (50.6% valid rate, 42.3% mean first-epoch accuracy): DeepSeek-Coder reaches 75.3% valid rate and 65.8% mean accuracy; Qwen2.5-Coder 72.1%/64.6%; Mistral 66.6%/66.1%. On CIFAR-10, best first-epoch accuracies reach 85.5% (Mistral), 85.2% (DeepSeek), 80.6% (Qwen) -- well above 63.98% full generation and 71.5% for the concurrent approach of Gu et al. Output lengths are 30-50 lines versus 200+ for full generation (75-85% reduction). A 50-epoch study confirms the 1-epoch proxy preserves rankings (Mistral: Spearman $ρ$ = 0.926). Delta-based generation is a token-efficient, multi-domain, LLM-agnostic alternative to full-model synthesis for LLM-driven NAS.