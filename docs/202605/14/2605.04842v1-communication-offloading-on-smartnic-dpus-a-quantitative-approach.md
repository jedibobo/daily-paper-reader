---
title: "Communication Offloading on SmartNIC DPUs: A Quantitative Approach"
title_zh: SmartNIC DPU 上的通信卸载：一种定量方法
authors: "Jacob Wahlgren, Andong Hu, Roger Pearce, Maya Gokhale, Ivy Peng"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04842v1"
tags: ["query:hwnas"]
score: 6.0
evidence: SmartNIC DPU 上的通信卸载
tldr: 本研究探讨了利用SmartNIC DPU卸载通信任务以节省CPU资源的潜力。作者设计了名为Buddy的通信卸载引擎，支持“发后即忘”异步通信模型，并在Nvidia BlueField-3 DPU和x86 CPU上进行了评估。研究发现内存与通信比是预测性能的关键指标，在特定负载下实现了1.55倍加速，并揭示了DPU缺乏直接缓存访问（DCA）导致的内存流量激增问题，为未来硬件设计提供了指导。
selection_source: fresh_fetch
motivation: 旨在通过将通信任务卸载到可编程SmartNIC DPU，解决高性能计算中CPU资源被通信开销过度占用的问题。
method: 开发了名为Buddy的通信卸载引擎，实现了将消息路由等通信任务从应用进程中解耦，并支持在DPU和通用CPU上灵活运行。
result: 在Quicksilver等应用中实现了高达1.55倍的加速，并确定了内存与通信比是衡量卸载效果的核心指标。
conclusion: 实验揭示了DPU因缺乏直接缓存访问支持而导致DRAM流量大幅增加，指出了未来SmartNIC硬件设计的关键改进方向。
---

## 摘要
SmartNIC 数据处理单元 (DPU) 通过将任务卸载到网络接口附近的可编程核心，为节省高端 CPU 资源提供了一种极具前景的解决方案。在这项工作中，我们探讨了 SmartNIC DPU 在支持名为“fire-and-forget”（发后即忘）的异步通信模型方面的可行性，特别是其核心消息路由服务。我们设计了一个名为 Buddy 的通信卸载引擎，将通信任务从应用程序进程中解耦。Buddy 可以灵活地运行在 Nvidia BlueField-3 DPU 等 SmartNIC DPU 以及通用 x86 CPU 上。我们对五个应用程序的评估结果表明，内存与通信比是卸载性能的关键预测指标。对于以主机为主的工作负载（如 Quicksilver 和稀疏矩阵转置），将通信卸载到 DPU 可实现高达 1.55 倍的加速。我们进一步发现，由于 DPU 缺乏直接缓存访问 (Direct Cache Access) 支持，导致 DRAM 流量增加了 625 倍，这凸显了未来 SmartNIC 设计中的一项关键需求。

## Abstract
SmartNIC Data Processing Units (DPUs) offer a promising solution for saving high-end CPU resources by offloading tasks to programmable cores near the network interface. In this work, we explore the feasibility of SmartNIC DPUs in supporting an asynchronous communication model called "fire-and-forget", particularly its core message routing service. We design a communication offloading engine called Buddy that decouples communication tasks from the application process. Buddy runs flexibly on SmartNIC DPUs such as the Nvidia BlueField-3 DPU and generic x86 CPUs. Our evaluation results in five applications identify the memory-to-communication ratio as a key predictor of the offloading performance. Host-dominated workloads, such as Quicksilver and Sparse Matrix Transpose, achieved up to 1.55x speedup with communication offloaded to the DPU. We further identify a 625x increase in DRAM traffic due to the absence of Direct Cache Access support on the DPU, highlighting a critical need in future SmartNIC designs.