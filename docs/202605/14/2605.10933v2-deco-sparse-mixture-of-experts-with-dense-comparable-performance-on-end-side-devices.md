---
title: "DECO: Sparse Mixture-of-Experts with Dense-Comparable Performance on End-Side Devices"
title_zh: DECO：在端侧设备上具有与稠密模型相当性能的稀疏混合专家模型
authors: "Chenyang Song, Weilin Zhao, Xu Han, Chaojun Xiao, Yingfa Chen, Zhiyuan Liu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10933v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 面向端侧设备的稀疏MoE架构
tldr: "针对端侧设备存储和内存受限的问题，本文提出DECO，一种稀疏混合专家（MoE）架构。它通过基于ReLU的可微路由、可学习专家缩放以及NormSiLU激活函数，在相同参数规模下实现了与稠密模型相当的性能。DECO仅激活20%的专家即可达到稠密模型水平，并在实际硬件上实现3倍推理加速，为端侧高效部署提供了新方案。"
selection_source: fresh_fetch
motivation: 混合专家模型（MoE）巨大的参数量导致存储和内存访问瓶颈，难以在资源受限的端侧设备上兼顾高性能与低开销。
method: 采用基于ReLU的可微路由和可学习专家缩放，引入NormSiLU激活函数以提高稀疏度，并简化专家架构为非门控MLP。
result: "DECO在仅激活20%专家的情况下性能媲美稠密模型，且在真实硬件上实现了3.00倍的推理加速。"
conclusion: DECO证明了通过优化路由和激活机制，稀疏MoE可以在端侧设备上实现与稠密模型相当的性能并显著提升推理效率。
---

## 摘要
虽然混合专家模型（MoE）在不按比例增加计算量的情况下扩展了模型容量，但其庞大的总参数量带来了显著的存储和内存访问瓶颈，这阻碍了在同时要求高性能、低计算成本和低存储开销的端侧设备上的高效部署。为了实现这些特性，我们提出了 DECO，这是一种稀疏 MoE 架构，旨在在相同的总参数预算和训练 Token 数量下，达到与稠密 Transformer 相当的性能。DECO 利用了由可学习的专家级缩放增强的可微且灵活的基于 ReLU 的路由，从而自适应地平衡了路由专家和共享专家的贡献。此外，我们引入了 NormSiLU，这是一种在 SiLU 算子之前对输入进行归一化的激活函数，它产生了更稳定的路由专家激活率趋势和更高的内在稀疏度。我们还发现在使用基于 ReLU 的路由时，采用非门控 MLP 专家具有经验性优势，这表明了简化 MoE 架构的可能性。实验表明，DECO 仅激活 20% 的专家即可匹配稠密模型的性能，并优于现有的 MoE 基准模型。与稠密推理相比，我们的专用加速内核在真实硬件上实现了 3.00 倍的加速。代码和检查点均可在 https://github.com/thunlp/DECO 获取。

## Abstract
While Mixture-of-Experts (MoE) scales model capacity without proportionally increasing computation, its massive total parameter footprint creates significant storage and memory-access bottlenecks, which hinder efficient end-side deployment that simultaneously requires high performance, low computational cost, and small storage overhead. To achieve these properties, we present DECO, a sparse MoE architecture designed to match the performance of dense Transformers under identical total parameter budgets and training tokens. DECO utilizes the differentiable and flexible ReLU-based routing enhanced by learnable expert-wise scaling, which adaptively balances the contributions of routed and shared experts. Furthermore, we introduce NormSiLU, an activation function that normalizes inputs prior to SiLU operators, producing a more stable trend of routed-expert activation ratio and a higher intrinsic sparsity level. We also identify an empirical advantage in using non-gated MLP experts with ReLU-based routing, indicating the possibility of MoE architecture simplification. Experiments demonstrate that DECO, activating only 20% of experts, matches dense performance and outperforms established MoE baselines. Our specialized acceleration kernel delivers a 3.00$\times$ speedup on real hardware compared with dense inference. Codes and checkpoints are all available at https://github.com/thunlp/DECO.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DECO** 的新型稀疏混合专家（MoE）架构，旨在解决端侧设备（如手机、嵌入式设备）在部署大模型时面临的存储、内存访问和计算效率之间的矛盾。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：传统的 MoE 虽然降低了计算量（活跃参数少），但其**总参数量**巨大，导致存储占用高且内存访问带宽受限，难以在资源匮乏的端侧设备上部署。
*   **研究动机**：作者提出了“理想三角形”目标，即同时实现**高性能**、**低计算成本**和**小存储开销**。论文探讨了一个关键问题：在相同的总参数预算和训练 Token 数量下，稀疏 MoE 能否达到与稠密（Dense）模型相当的性能？

### 2. 核心方法论
DECO 通过对 MoE 架构的四个关键维度进行重新设计来实现“稠密等价”性能：
*   **基于 ReLU 的路由（ReLU-based Routing）**：放弃了不可微且固定激活数量的 TopK 路由，采用 ReLU 激活函数。这使得路由过程完全可微，并允许每个 Token 根据需要灵活选择不同数量的专家。
*   **可学习的专家级路由缩放（Learnable Expert-wise Scaling）**：引入向量化的可学习缩放因子，为每个专家分配独立的权重，以平衡路由专家与共享专家之间的贡献差异，并处理专家的异质性。
*   **NormSiLU 激活函数**：在专家内部的 SiLU 算子前加入双阶段归一化：
    1.  **专家间均值归一化**：使预激活输入分布零中心化，稳定激活分布。
    2.  **专家内 RMS 归一化**：保持激活强度，防止神经元输出消失，并稳定路由器的激活率趋势。
*   **非门控 MLP 专家（Non-Gated MLP）**：发现非门控 MLP 在 ReLU 路由下比常用的 SwiGLU（门控）更稳定，能减少对激进稀疏正则化的依赖，从而保护性能。
*   **自适应稀疏正则化**：利用路由熵损失和动态调整的系数 $\lambda$，精确控制模型达到预设的稀疏度（如 20% 激活率）。

### 3. 实验设计
*   **模型规模**：涵盖了从 0.11B（Small）到 1.18B（XLarge）四个参数量级。
*   **数据集**：使用高质量混合数据集（FineWeb, Nemotron-CC, Pile, Wikipedia 等），涵盖网页、代码、数学等。
*   **Benchmark**：
    *   **语言建模**：C4 数据集的验证集困惑度（PPL）。
    *   **常识推理**：PIQA, SIQA, HellaSwag, ARC, WinoGrande, LAMBADA 等。
*   **对比方法**：Dense（稠密 LLaMA 风格）、TopP、DeepSeek-V3、ReMoE、BlockFFN。
*   **硬件测试**：在 NVIDIA RTX 4090 和 Jetson AGX 上测试推理加速比。

### 4. 资源与算力
*   **训练配置**：论文提到训练 Token 数量严格控制在总参数量的 40 倍左右（例如 1.18B 模型训练了约 47B tokens）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号数量及总训练时长，但提供了详细的超参数表（学习率、Batch Size、WSD 调度器等）。
*   **推理测试**：明确使用了 RTX 4090 (24GB) 和 Jetson AGX (64GB) 进行端侧推理性能评估。

### 5. 实验数量与充分性
*   **实验规模**：做了大量对比实验，涵盖 4 种模型规模和 5 种主流基准架构。
*   **消融实验**：针对 NormSiLU 的各个组件（均值归一化 vs RMS）、专家门控策略（Gated vs Non-gated）、路由缩放方式（固定 vs 标量 vs 向量）进行了详尽的消融研究。
*   **超参数分析**：深入探讨了激活率、共享专家大小、专家粒度对性能的影响。
*   **充分性评价**：实验设计严谨，遵循了“相同总参数”和“相同训练 Token”的严格限制，确保了对比的公平性和客观性。

### 6. 主要结论与发现
*   **稠密等价性**：在仅激活 20% 专家的情况下，DECO 的性能完全匹配甚至超过了相同参数规模的稠密模型。
*   **架构简化**：在稀疏路由场景下，简单的非门控 MLP 专家反而优于复杂的门控变体。
*   **推理加速**：通过定制的 CUTLASS 加速内核，DECO 在真实硬件上实现了比稠密模型快 **3.00 倍**的推理速度。
*   **数据敏感性**：DECO 在高度异质（多样化）的数据集上表现更好，而在单一领域数据集上优势相对减弱。

### 7. 优点与亮点
*   **端侧友好**：直接针对端侧设备的存储瓶颈，实现了“小体积、高性能、高速度”的平衡。
*   **理论与实践结合**：不仅提出了 NormSiLU 等创新算子，还提供了数学证明（见附录）和实际的硬件加速内核实现。
*   **灵活性**：基于 ReLU 的路由允许模型根据输入难度动态分配计算资源。

### 8. 不足与局限
*   **缺乏后训练验证**：实验主要集中在预训练阶段，未展示在指令微调（SFT）和强化学习（RL）阶段的表现，而 MoE 在 RL 阶段往往存在不稳定性风险。
*   **数据依赖性**：如文中所述，在同质化严重的数据集上，其相对于稠密模型的优势会收窄。
*   **规模外推**：目前实验最高只到 1.18B 参数，更大规模（如 7B 或 70B）下的表现和激活率缩放规律仍需进一步验证。

（完）
