---
title: "NeuroRing: Scaling Spiking Neural Networks via Multi-FPGA Bidirectional Ring Topologies and Stream-Dataflow Architectures"
title_zh: NeuroRing：通过多 FPGA 双向环形拓扑与流数据流架构扩展脉冲神经网络
authors: "Muhammad Ihsan Al Hafiz, Artur Podobas"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28059v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于FPGA流式数据流架构的高能效SNN加速器
tldr: NeuroRing是一个基于FPGA的模块化可扩展脉冲神经网络（SNN）加速器，旨在解决大规模SNN执行中的通信和同步瓶颈。它采用流式数据流架构和双向环形拓扑结构，通过HLS实现，并支持多FPGA部署。实验表明，该系统在皮层微电路基准测试中实现了超实时运行，具有良好的扩展性和能效，为神经科学模拟和事件驱动应用提供了灵活平台。
selection_source: fresh_fetch
motivation: 针对大规模脉冲神经网络在执行过程中存在的稀疏脉冲通信和同步导致的性能瓶颈问题。
method: 提出了一种基于双向环形拓扑和流式数据流架构的模块化FPGA加速器NeuroRing，并集成了NEST模拟器工作流。
result: 在皮层微电路基准测试中实现了0.83的实时因子（超实时运行），并展现出显著的强扩展性和弱扩展性。
conclusion: NeuroRing为大规模SNN模拟和事件驱动应用提供了一个兼具灵活性、可扩展性和高能效的硬件平台。
---

## 摘要
脉冲神经网络 (SNN) 是高效能事件驱动计算的一种极具前景的范式，但大规模 SNN 的执行仍面临挑战，因为稀疏的脉冲通信和同步可能会主导运行时间。现有的基于 CPU、GPU、ASIC 和 FPGA 平台的解决方案在可编程性、效率和可扩展性之间提供了不同的权衡。为了填补这一空白，我们提出了 NeuroRing，这是一种基于流数据流架构和双向环形拓扑的模块化、可扩展 SNN 加速器，在可编程 FPGA 上通过高层次综合 (HLS) 实现。NeuroRing 支持模块化的单 FPGA 和多 FPGA 部署，并通过与 NEST 模拟器集成，与现有的 SNN 工作流兼容。我们在皮层微电路基准测试和数独约束满足工作负载上对 NeuroRing 进行了评估。结果表明，NeuroRing 保留了 NEST 参考模型的关键活动统计特性，实现了全规模皮层微电路的超实时执行（实时因子 RTF 为 0.83），展现了显著的强扩展性和弱扩展性，并在两个可编程 FPGA 上提供了具有竞争力的能效。这些结果使 NeuroRing 成为神经科学模拟和更广泛的事件驱动应用中一个灵活且可扩展的平台。

## Abstract
Spiking neural networks (SNNs) are a promising paradigm for energy-efficient event-driven computation, but large-scale SNN execution remains challenging because sparse spike communication and synchronization can dominate runtime. Existing solutions across CPU, GPU, ASIC, and FPGA platforms offer different trade-offs between programmability, efficiency, and scalability. To address this gap, we present NeuroRing, a modular and scalable SNN accelerator based on a stream-dataflow architecture and a bidirectional ring topology, implemented in High-Level Synthesis (HLS) on programmable FPGAs. NeuroRing supports modular single- and multi-FPGA deployment and is compatible with existing SNN workflows through integration with the NEST simulator. We evaluate NeuroRing on the cortical microcircuit benchmark and a Sudoku constraint-satisfaction workload. Results show that NeuroRing preserves the key activity statistics of the NEST reference model, achieves faster-than-real-time execution of the full-scale cortical microcircuit with a real-time factor (RTF) of 0.83, exhibits meaningful strong and weak scaling, and provides competitive energy efficiency on two programmable FPGAs. These results position NeuroRing as a flexible and scalable platform for both neuroscience simulation and broader event-driven applications.

---

## 论文详细总结（自动生成）

以下是对论文《NeuroRing: Scaling Spiking Neural Networks via Multi-FPGA Bidirectional Ring Topologies and Stream-Dataflow Architectures》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大规模脉冲神经网络（SNN）的模拟面临严峻的挑战，特别是高密度的突触连接会导致脉冲通信和同步开销主导运行时间。
*   **研究背景**：虽然 CPU、GPU 和专用 ASIC（如 SpiNNaker）提供了不同的解决方案，但仍缺乏一种既能保持高能效、又具备模块化可扩展性且易于与现有神经科学工作流（如 NEST 模拟器）集成的通用平台。
*   **整体含义**：论文提出了 **NeuroRing**，这是一种基于 FPGA 的流式数据流（Stream-Dataflow）架构，通过双向环形拓扑结构实现了单芯片及多芯片间的灵活扩展，旨在为大规模 SNN 提供超实时的模拟能力。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用高层次综合（HLS）构建模块化的计算内核，通过流式通信解耦计算与数据传输，并采用双向环形拓扑降低路由延迟和死锁风险。
*   **关键技术细节**：
    *   **NeuroRing 核心 (Core)**：由两个计算单元（CU）组成。
        *   **NeuroRing CU**：包含神经元处理单元（NPU）、突触列表获取和脉冲记录器。NPU 采用 8 路流水线架构，支持 LIF（Leaky Integrate-and-Fire）模型。
        *   **SynapseRouter CU**：独立运行，负责本地脉冲的分发和环路数据包的转发。
    *   **双向环形拓扑**：核心之间通过 AXI-Stream 接口连接。在多 FPGA 部署中，通过 Aurora 内核和高速串行链路（QSFP）将环路扩展至跨芯片。
    *   **路由策略**：根据目标核心 ID 选择最短路径（左或右），并优先处理来自邻居核心的转发数据包以防止拥塞。
    *   **集成流**：通过 Python 脚本从 NEST 模拟器提取网络拓扑，转换为 NeuroRing 兼容的扁平化突触列表格式。

### 3. 实验设计
*   **数据集/场景**：
    1.  **皮层微电路 (Cortical Microcircuit)**：神经科学领域的标准基准，包含约 7.7 万个神经元和 3 亿个突触，具有极高的连接密度。
    2.  **数独求解器 (Sudoku Solver)**：代表约束满足问题（CSP），展示架构在非神经科学领域的通用性。
*   **Benchmark 与对比方法**：
    *   **软件基准**：NEST 模拟器（运行于 Dardel 超级计算机 CPU 节点）。
    *   **硬件对比**：SpiNNaker (ASIC)、GeForce RTX 2080 Ti (GPU)、Agilex 7 (FPGA) 以及其他大规模 FPGA 集群（如 neuroAIx）。
*   **评估指标**：实时因子（RTF，越小越快）、每突触事件能耗（nJ/event）、脉冲统计特性（发放率、变异系数、皮尔逊相关性）。

### 4. 资源与算力
*   **硬件平台**：使用了 **2 张 AMD/Xilinx Alveo U55C FPGA** 加速卡。
*   **连接方式**：通过 PCIe 连接主机，两张卡之间通过双 QSFP 电缆（Aurora 协议）互连。
*   **算力细节**：
    *   内核目标频率 300 MHz（实际达成 198-304 MHz，视配置而定）。
    *   使用了 HBM（高带宽内存）存储突触列表，URAM 存储累加器数据。
    *   未涉及传统意义上的“训练时长”，因为 SNN 实验侧重于推理/模拟。

### 5. 实验数量与充分性
*   **实验设置**：
    *   **正确性验证**：对比了 NeuroRing 与 NEST 在全规模皮层微电路下的层级脉冲统计数据。
    *   **设计空间探索**：测试了不同神经元容量（2048 到 8192 神经元/核心）对性能和功耗的影响。
    *   **扩展性测试**：进行了强扩展性（固定负载增加资源）和弱扩展性（负载与资源同步增加）实验。
*   **充分性评价**：实验设计较为全面，涵盖了从功能正确性到性能极限、再到跨领域应用的多个维度。对比了多种异构平台，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **超实时性能**：在 2 张 U55C FPGA 上，全规模皮层微电路的 RTF 达到 **0.83**（即比生物实时快 1.2 倍）。
*   **高能效**：每突触事件能耗仅为 **73 nJ**，显著优于 CPU（480 nJ）和 GPU（180 nJ）方案。
*   **扩展性**：展示了从单 FPGA 到双 FPGA 的有效扩展，强扩展性下实现了约 1.43 倍的加速。
*   **通用性**：成功解决了数独难题，证明了架构处理事件驱动约束满足问题的能力。

### 7. 优点
*   **模块化与可移植性**：基于 HLS 开发，易于修改神经元模型，且能快速适配不同的 FPGA 芯片。
*   **后端无关性**：通过通用的突触列表表示，能够无缝集成到现有的神经科学模拟生态中。
*   **拓扑优势**：双向环形结构简化了路由逻辑，同时通过 Aurora 链路实现了低成本的多芯片扩展。

### 8. 不足与局限
*   **频率回退**：随着核心数量增加，时序收敛变得困难，导致工作频率下降，抵消了部分并行化带来的增益。
*   **非线性扩展**：在弱扩展性实验中，由于跨芯片通信和同步开销，RTF 随规模增大而有所上升，未能达到理想的线性扩展。
*   **内存限制**：虽然使用了 HBM，但对于更大规模或更高连接密度的网络，片上存储（URAM/BRAM）可能成为累加器设计的瓶颈。

（完）
