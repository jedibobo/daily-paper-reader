---
title: "AutoSP: Unlocking Long-Context LLM Training Via Compiler-Based Sequence Parallelism"
title_zh: AutoSP：通过基于编译器的序列并行解锁长上下文大语言模型训练
authors: "Ahan Gupta, Zhihao Wang, Neel Dani, Masahiro Tanaka, Olatunji Ruwase, Minjia Zhang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27089v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于编译器的LLM训练序列并行自动化方案
tldr: AutoSP是一个基于编译器的自动化序列并行方案，旨在解决大语言模型在长文本训练中面临的内存瓶颈和开发复杂性。它通过自动应用序列并行和长文本感知的激活检查点技术，在不显著降低吞吐量的前提下，显著提升了模型可处理的上下文长度，降低了长文本训练的开发门槛。
selection_source: fresh_fetch
motivation: 现有训练库主要针对大参数量优化，缺乏易用的长文本训练抽象，导致开发者需手动实现复杂的序列并行逻辑。
method: 引入AutoSP编译器，自动对模型应用序列并行和长文本感知的激活检查点优化，以增强长文本训练能力。
result: 在NVIDIA和AMD硬件上，AutoSP将训练上下文长度分别提升了2.7倍和2.5倍，且运行性能损耗几乎可以忽略不计。
conclusion: AutoSP通过自动化编译技术有效解决了长文本LLM训练的扩展性难题，显著提升了开发效率和硬件利用率。
---

## 摘要
大语言模型（LLM）在长上下文任务中展现出巨大的效用，这些任务需要处理包含数万到数十万个 token 的提示词。然而，现有的 LLM 训练库并未提供易于使用的抽象来优化长上下文训练，而是专注于通过 ZeRO-3/FSDP、张量并行和流水线并行来优化具有海量参数的模型。这迫使用户重写 LLM 训练库，以将各种复杂的长上下文优化组合（如序列并行）整合到训练流水线中；这一过程需要深厚的专业知识，降低了开发者的生产力。为了应对这些挑战，我们推出了 AutoSP：首个自动优化长上下文 LLM 训练的自动化解决方案。AutoSP 编译模型并应用一组针对性的优化：自动序列并行和长上下文感知的激活检查点（activation-checkpointing），在吞吐量损失微乎其微的情况下大幅增强了 LLM 的可训练性。我们的评估证明了 AutoSP 在 NVIDIA 和 AMD 硬件上的能力，与具有竞争力的手写基准相比，在运行时性能损失忽略不计的情况下，分别将训练上下文长度增加了高达 2.7 倍和 2.5 倍。

## Abstract
Large-language-models (LLMs) demonstrate enormous utility in long-context tasks which require processing prompts that consist of tens to hundreds of thousands of tokens. However, existing LLM training libraries do not provide easy to use abstractions to optimize for long-context training, instead focusing on optimizations for models with large parameter counts through ZeRO-3/FSDP, Tensor and Pipeline parallelism. This forces users to rewrite LLM training libraries to incorporate compositions of various complex long-context optimizations, such as sequence-parallelism, to training pipelines; a process that requires in-depth expertise, reducing developer productivity. To tackle these challenges, we introduce AutoSP: the first automated solution to automatically optimize LLM training for longer-contexts. AutoSP compiles models and applies a targeted set of optimizations: automated sequence parallelism, and long-context aware activation-checkpointing, to drastically enhance LLM trainability at negligible cost to throughput. Our evaluation demonstrates AutoSP's capability on both NVIDIA and AMD hardware, increasing training contexts by upto 2.7$\times$ and 2.5$\times$ respectively over competitive hand-written baseline at negligible cost to runtime performance.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **AutoSP**，这是首个基于编译器的自动化序列并行（Sequence Parallelism, SP）解决方案，旨在简化大语言模型（LLM）的长上下文训练优化。

### 1. 核心问题与整体含义
*   **研究动机**：随着 LLM 对长上下文（数万到数十万 token）需求的增加，激活值产生的内存压力成为瓶颈。现有的并行策略（如 ZeRO-3/FSDP）主要针对模型参数量优化，而非序列长度。
*   **核心挑战**：现有的序列并行方案（如 DeepSpeed-Ulysses, RingAttention）通常以“急切模式（Eager Mode）”实现，与特定框架深度耦合。开发者需要手动插入通信算子（如 `all2all`）、管理张量布局并处理复杂的反向传播逻辑，这严重降低了开发效率和代码的可移植性。
*   **整体含义**：AutoSP 试图将序列并行逻辑“提升”到编译器层面（PyTorch 2.0 堆栈），通过自动化的编译器通行证（Pass）实现长上下文优化，让开发者无需手动重构代码即可获得高性能的长文本训练能力。

### 2. 方法论
AutoSP 核心包含两个关键的编译器通行证：
*   **自动化序列并行通行证（SP-Pass）**：
    *   **分析阶段**：在 **Torch-IR** 层级运行，通过分析计算图输入节点提取 Batch Size、序列长度和隐藏维度等关键参数。
    *   **转换阶段**：自动在注意力层前后插入 `all_to_all` 通信算子；根据 SP 组大小调整中间缓存（Buffer）的大小；自动重新计算手动索引的张量（如因果掩码/Causal Mask），以确保分片后的逻辑正确。
*   **序列感知激活检查点通行证（SAC-Pass）**：
    *   **核心思想**：利用长上下文训练的计算特性。论文推导发现，当序列长度 $s$ 远大于模型维度 $d$ 时，线性层和 MLP 层的计算量占比随 $s$ 增加而趋于 $O(1/s)$。
    *   **技术细节**：修改了 PyTorch 2.0 默认的保守 AC 策略（默认禁止重计算矩阵乘法等重计算算子）。SAC 允许在注意力层之外重计算这些算子，从而以极小的计算开销换取巨大的内存节省。

### 3. 实验设计
*   **模型对象**：Llama-3.2 (1B, 3B), Llama-3.1 (8B), Llama-2 (13B)，涵盖了 GQA（分组查询注意力）和全注意力机制。
*   **对比基准（Baselines）**：
    1.  **ZeRO-3 (FSDP)**：通过 `torch.compile()` 优化的标准数据并行方案。
    2.  **DeepSpeed-Ulysses**：手写的高性能序列并行实现。
    3.  **RingAttention**：另一种主流的手写序列并行实现。
*   **评估指标**：最大可训练序列长度（Trainability）、单次迭代耗时（Throughput）。

### 4. 资源与算力
*   **硬件平台**：
    *   **NVIDIA**：GH200 (96GB) 和 A100 (80GB)。
    *   **AMD**：MI250 (64GB)。
*   **算力规模**：使用了 8 张 A100 组成的集群进行主实验，以及 2 张 GH200/MI250 进行跨硬件移植性测试。
*   **软件环境**：PyTorch 2.7, CUDA 12.8, ROCm 6.4。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 13B 不同参数规模的模型，并在 NVIDIA 和 AMD 两种主流 AI 芯片上进行了验证。
*   **消融实验**：分别测试了仅开启 SP-Pass 和同时开启 SP+SAC-Pass 的效果，验证了各组件的贡献。
*   **充分性**：实验设计较为全面，不仅对比了最大长度，还详细分析了算子级的内存消耗和时间开销，证明了该方法在不同硬件和模型架构下的通用性。

### 6. 主要结论与发现
*   **提升显著**：相比于手写的 DeepSpeed-Ulysses，AutoSP 在 NVIDIA 和 AMD 硬件上分别将最大训练上下文长度提升了 **2.7 倍**和 **2.5 倍**。
*   **性能保持**：AutoSP 的运行效率达到了手写高度优化基准的 **97%-98%**，性能损耗几乎可以忽略。
*   **AC 策略有效性**：在长文本场景下，重计算线性层算子的开销极低，但能显著释放内存，这是 AutoSP 超越手写基准的关键。

### 7. 优点
*   **易用性极高**：开发者只需添加几行编译器配置代码即可启用，无需侵入式修改模型定义。
*   **跨平台移植性**：作为编译器通行证，它天然支持多种硬件后端（NVIDIA/AMD），解决了手写算子难以跨平台的问题。
*   **理论支撑扎实**：通过对 Transformer 计算复杂度的分析，找出了长文本训练中重计算的最优权衡点。

### 8. 不足与局限
*   **模型架构局限**：虽然覆盖了 Llama 系列，但对于非 Transformer 架构（如 SSM/Mamba）的适用性尚未讨论。
*   **大模型瓶颈转移**：在 13B 及更大模型上，优化器状态（Optimizer States）开始占据内存主导地位（约 50%），此时 AutoSP 针对激活值的优化边际效应会减弱。
*   **依赖编译器成熟度**：该方案深度依赖 PyTorch 2.0 编译器的稳定性，如果底层 IR 发生重大变化，通行证可能需要重新适配。

（完）
