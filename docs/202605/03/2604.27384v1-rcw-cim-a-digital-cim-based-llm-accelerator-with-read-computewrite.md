---
title: "RCW-CIM: A Digital CIM-based LLM Accelerator with Read-Compute/Write"
title_zh: RCW-CIM：一种具有读计算/写功能的基于数字存内计算的大语言模型加速器
authors: "Yan-Cheng Guo, Tian-Sheuan Chang, Jian-Wei Su"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27384v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于数字存算一体的LLM加速器及算子融合
tldr: 针对大语言模型（LLM）权重规模远超存内计算（CIM）容量导致的更新延迟问题，本文提出RCW-CIM加速器。通过读计算/写（RCW）并行架构、非线性算子融合以及WS-OCS数据流，显著降低了权重更新和外部存储访问开销。在TSMC 22nm工艺下，该芯片实现了高效的推理性能，为高精度、低延迟的LLM边缘部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有存内计算架构在处理超大规模LLM时，频繁的权重更新成为了制约推理速度的关键瓶颈。
method: 提出了读计算与写入并行的RCW架构、非线性算子融合机制以及减少访存的WS-OCS数据流。
result: "在Llama2-7B模型上，解码计算延迟降低21.59%，非线性算子延迟降低69.17%，整体预填充延迟缩短至4.2ms。"
conclusion: RCW-CIM通过软硬件协同优化，有效解决了大模型在存内计算架构中的权重搬运瓶颈，实现了高能效的LLM加速。
---

## 摘要
数字存内计算（DCIM）已成为大语言模型（LLM）加速的一种极具前景的解决方案，它通过最小化外部 DRAM 与片上加速器之间的数据传输，同时保持高精度以实现卓越的准确性。然而，现有的 CIM 架构往往忽略了权重更新延迟，由于 LLM 权重远大于单个 CIM 宏单元的容量，这一问题变得至关重要。为解决此问题，本文提出了一种读计算/写（RCW）架构，可有效最小化权重更新延迟，并结合非线性算子融合进一步减轻依赖引起的延迟。所提出的 RCW 在 Llama2-7B 模型上将解码计算延迟降低了 21.59%。此外，非线性算子融合机制通过高效的部分累加和基于组的近似，实现了 69.17% 的延迟降低。此外，本文还引入了权重固定和输出列固定（WS-OCS）数据流，在 1024 个 token 的预填充阶段，分别将外部 DRAM 访问和内部 CIM 权重更新减少了 51.6% 和 87.6%，从而使总延迟降低了 49.76%。该 RCW-CIM 采用 TSMC 22 nm CMOS 工艺制造，工作频率为 100 MHz，实现了 3.28 TOPS 的算力和 42.3 TOPS/W 的能效，在配备双 DDR5-6400 内存的情况下，针对 INT4 权重的 Llama2 模型，实现了 4.2 ms 的预填充延迟和每秒 26.87 个解码 token。

## Abstract
Digital computing-in-memory (DCIM) has emerged as a promising solution for large language model (LLM) acceleration by minimizing data transfers between external DRAM and on-chip accelerators while maintaining high precision for superior accuracy. However, existing CIM architectures often overlook weight update latency, which becomes critical as LLM weights are far larger than a single CIM macro capacity. To address this issue, this paper proposes a read-compute/write (RCW) architecture that effectively minimizes weight update latency, along with a nonlinear operator fusion that further mitigates dependencyinduced latency. The proposed RCW reduces decoding computing latency by 21.59% on the Llama2-7B model. In addition, the nonlinear operator fusion mechanism achieves a 69.17% latency reduction through efficient partial accumulation and group-based approximation. Furthermore, a weight-stationary and output column stationary (WS-OCS) dataflow is introduced to reduce both external DRAM access and internal CIM weight updates by 51.6% and 87.6% respectively during the prefill phase of 1024 tokens, leading to an overall 49.76% latency reduction. Fabricated using TSMC 22 nm CMOS technology and operating at 100 MHz, the proposed RCW-CIM achieves 3.28 TOPS and 42.3 TOPS/W, enabling 4.2 ms prefill latency and 26.87 decoded tokens per second for the INT4-weight Llama2 model with dual DDR5-6400 memory.

---

## 论文详细总结（自动生成）

这是一份关于论文《RCW-CIM: A Digital CIM-based LLM Accelerator with Read-Compute/Write》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）的参数量巨大，远超单个存内计算（CIM）宏单元的存储容量。这导致在推理过程中需要频繁从外部 DRAM 更新 CIM 内的权重，产生巨大的**权重更新延迟**。
*   **背景**：虽然数字存内计算（DCIM）能减少数据搬运并保持高精度，但现有研究多关注数据对齐或 KV 缓存编码，忽视了权重更新这一性能瓶颈。此外，Softmax 和 RMSNorm 等非线性算子在 CIM 架构中通常效率低下且存在精度损失。
*   **整体含义**：本文旨在通过硬件架构创新（RCW）和数据流优化（WS-OCS），解决 LLM 在 CIM 芯片上的权重搬运开销，实现高能效、低延迟的边缘端大模型加速。

### 2. 论文提出的方法论
*   **RCW（读计算/写）架构**：
    *   **核心思想**：将权重更新与计算过程重叠（Overlap）。
    *   **流程**：分为两个阶段。第一阶段将当前权重读取并锁存至加法树；第二阶段在执行乘累加（MAC）运算的同时，并行写入下一组权重。通过保持字线（WL）激活，降低了解码延迟和预充功耗。
*   **WS-OCS（权重固定-输出列固定）数据流**：
    *   **核心思想**：优化数据复用模式。权重以块为单位加载，并与输入数据按列进行计算，中间结果缓存在片上。
    *   **效果**：相比传统的 WS-OS 数据流，显著减少了输入数据的 DRAM 访问次数，并大幅降低了 CIM 内部权重的更新频率。
*   **非线性算子融合机制**：
    *   **技术细节**：采用基于 64 段查找表（LUT）的近似计算。
    *   **创新点**：在 CIM 宏单元中支持“部分累加”模式以并行计算指数，并使用 FP16 高精度处理非线性函数，减少了全局依赖导致的延迟。

### 3. 实验设计
*   **模型与场景**：针对 **Llama2-7B** 模型进行评估。
*   **精度配置**：权重采用 INT4，激活值采用 INT8，非线性算子采用 FP16。
*   **Benchmark（对比对象）**：
    *   对比了 2025 年 VLSI 研讨会的两项前沿工作：**CELLA** [4] 和 **LLM-CIM** [5]。
*   **硬件环境**：基于 TSMC 22nm CMOS 工艺制造，运行频率 100 MHz，配备双通道 DDR5-6400 内存。

### 4. 资源与算力
*   **芯片规格**：包含 8 个 CIM 集群，共 64 个多 CIM 核心。
*   **峰值算力**：**3.28 TOPS**。
*   **能效比**：**42.3 TOPS/W**。
*   **推理表现**：
    *   预填充（Prefill）延迟：4.2 ms（1024 tokens）。
    *   解码（Decode）速度：26.87 tokens/s。
*   **训练说明**：文中未提及模型训练细节，重点在于推理加速器的硬件实现与实测。

### 5. 实验数量与充分性
*   **实验内容**：
    *   对比了不同数据流（IS, WS, IS-OS, WS-OS, WS-OCS）在 DRAM 访问和权重更新量上的差异。
    *   进行了消融实验，分别验证了 WS-OCS、RCW 和算子融合对延迟降低的贡献。
    *   提供了芯片实测的功耗、面积和吞吐量数据。
*   **充分性评价**：实验设计较为充分，涵盖了从底层硬件指标到顶层模型推理性能的全面评估。对比对象均为同年度的最先进（SOTA）设计，具有较强的说服力。

### 6. 论文的主要结论与发现
*   **延迟大幅降低**：WS-OCS 数据流使预填充延迟降低了 **49.76%**；RCW 架构使解码计算延迟降低了 **21.59%**。
*   **非线性优化显著**：算子融合机制结合组级 Softmax/RMSNorm，将非线性操作的延迟降低了 **69.17%**。
*   **访存开销优化**：WS-OCS 减少了 51.6% 的外部 DRAM 访问和 87.6% 的内部权重更新操作。
*   **综合性能**：在 22nm 工艺下实现了极高的能效（42.3 TOPS/W），证明了数字 CIM 在处理 LLM 时的潜力。

### 7. 优点
*   **软硬协同**：不仅改进了硬件电路（RCW 宏单元），还优化了顶层调度（WS-OCS 数据流），全方位解决瓶颈。
*   **高精度保持**：在非线性算子中使用 FP16，避免了同类设计中因低比特量化导致的精度大幅下降。
*   **实测验证**：论文基于实际流片（TSMC 22nm）数据，而非纯模拟，结果具有高度可靠性。

### 8. 不足与局限
*   **模型覆盖面**：实验主要集中在 Llama2-7B，未测试其他架构（如 MoE 模型）或更大规模（如 70B）模型的表现。
*   **频率限制**：100 MHz 的工作频率相对较低，虽然能效高，但在追求绝对峰值性能的场景下可能受限。
*   **应用限制**：该架构高度依赖于特定的权重更新模式，对于权重动态变化的场景（如某些在线学习任务）可能不适用。

（完）
