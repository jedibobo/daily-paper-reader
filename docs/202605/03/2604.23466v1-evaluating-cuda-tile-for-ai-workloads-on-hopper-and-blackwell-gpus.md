---
title: Evaluating CUDA Tile for AI Workloads on Hopper and Blackwell GPUs
title_zh: 在 Hopper 和 Blackwell GPU 上评估用于 AI 工作负载的 CUDA Tile
authors: "Divakar Kumar Yadav, Tian Zhao, Deepak Kumar"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23466v1"
tags: ["query:hwnas"]
score: 7.0
evidence: AI工作负载的GPU内核效率评估
tldr: 本文对 NVIDIA 推出的 Python 化分块编程模型 CUDA Tile (CuTile) 进行了首次独立评估，涵盖了 Hopper 和 Blackwell 架构。研究通过 GEMM 和注意力机制等 AI 核心负载，对比了 CuTile 与 cuBLAS、Triton 等框架的性能。结果显示 CuTile 在特定架构（如 B200）上能以极简代码实现超越 FlashAttention-2 的性能，但在跨架构可移植性方面存在明显短板，整体表现受工作负载和硬件架构影响较大。
selection_source: fresh_fetch
motivation: 旨在评估 NVIDIA 新推出的 CuTile 编程模型在简化 GPU 内核开发的同时，能否在现代架构上保持极高的计算效率和良好的可移植性。
method: 在 H100 和 Blackwell GPU 上针对 GEMM、融合注意力机制等任务，对比评估了 CuTile 与 cuBLAS、Triton、WMMA 等多种编程方法的性能与代码复杂度。
result: "CuTile 在 B200 上实现的注意力机制性能高达 1007 TFLOP/s，是 FlashAttention-2 的 2.5 倍，但在 RTX PRO 6000 上性能大幅下降，且 GEMM 性能仅为 cuBLAS 的 52-79%。"
conclusion: CuTile 是手写 CUDA 内核的高效替代方案，但在跨架构优化的一致性和通用库替代能力上仍不及 Triton 或厂商优化库。
---

## 摘要
NVIDIA 的 CUDA Tile (CuTile) 引入了一种基于 Python 的、以分块（tile）为中心的 GPU 核函数（kernel）开发抽象，旨在简化编程，同时在现代 GPU 上保留 Tensor Core 和张量内存加速器 (TMA) 的效率。我们在跨越 Hopper 和 Blackwell 架构的三款 NVIDIA GPU（H100 NVL、B200 和 RTX PRO 6000 Blackwell 服务器版）上，首次对 CuTile 与 cuBLAS、Triton、WMMA 和原始 SIMT 等既有方法进行了独立的跨架构评估。我们对代表性的 AI 工作负载进行了基准测试，包括 BF16/FP16 精度的 GEMM、融合多头注意力机制以及端到端大语言模型 (LLM) 推理，以评估其性能和可移植性。我们的结果表明，CuTile 的有效性在很大程度上取决于工作负载和架构。在数据中心级 Blackwell (B200) 上，CuTile 在融合注意力机制方面实现了高达 1007 TFLOP/s 的性能，在仅需 60 行 Python 核函数代码的情况下，性能优于 FlashAttention-2 达 2.5 倍。对于 GEMM，CuTile 在 22 行代码中达到了 cuBLAS 性能的 52-79%（相比之下 WMMA 需要 123 行），使其成为手写 CUDA 核函数的实用替代方案，但尚不能取代供应商优化的库。然而，相同的 CuTile 注意力核函数在 RTX PRO 6000 (sm_120) 上仅达到 FlashAttention-2 吞吐量的 53%，暴露了显著的跨架构优化差距。相比之下，Triton 在所有测试平台上无需针对特定架构进行调优即可维持 cuBLAS 62-101% 的性能，展现出明显更强的可移植性。

## Abstract
NVIDIA's CUDA Tile (CuTile) introduces a Python-based, tile-centric abstraction for GPU kernel development that aims to simplify programming while retaining Tensor Core and Tensor Memory Accelerator (TMA) efficiency on modern GPUs. We present the first independent, cross-architecture evaluation of CuTile against established approaches such as cuBLAS, Triton, WMMA, and raw SIMT on three NVIDIA GPUs spanning Hopper and Blackwell: H100 NVL, B200, and RTX PRO 6000 Blackwell Server Edition. We benchmark representative AI workloads, including GEMM, fused multi-head attention, and end-to-end LLM inference in BF16/FP16 precision, to assess both performance and portability.   Our results show that CuTile effectiveness is strongly workload- and architecture-dependent. On datacenter-class Blackwell (B200), CuTile achieves up to 1007 TFLOP/s for fused attention, outperforming FlashAttention-2 by 2.5x while requiring only 60 lines of Python kernel code. For GEMM, CuTile reaches 52-79% of cuBLAS performance in 22 lines of code (versus 123 for WMMA), making it a practical replacement for hand-written CUDA kernels but not yet for vendor-optimized libraries. However, the same CuTile attention kernel achieves only 53% of FlashAttention-2 throughput on RTX PRO 6000 (sm_120), exposing significant cross-architecture optimization gaps. In contrast, Triton sustains 62-101% of cuBLAS performance across all tested platforms without architecture-specific tuning, demonstrating substantially stronger portability.