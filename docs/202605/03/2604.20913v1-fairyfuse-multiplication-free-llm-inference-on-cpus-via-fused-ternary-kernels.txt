Title: FairyFuse: Multiplication-Free LLM Inference on CPUs via Fused Ternary Kernels

URL Source: https://arxiv.org/pdf/2604.20913v1

Published Time: Fri, 24 Apr 2026 00:03:37 GMT

Number of Pages: 16

Markdown Content:
# FairyFuse: Multiplication-Free LLM Inference on CPUs via Fused Ternary Kernels 

Fei Zuo ∗

BA TechWorks BMW Group 

Xiaoyan Xi ∗

BA TechWorks BMW Group 

Quanyi Zeng ∗

Peking University 

Feiyu Wang 

Peking University 

Ho Fai Leung †

BA TechWorks BMW Group 

Abstract 

Large language models are increasingly deployed on CPU-only platforms, from on-device assistants to privacy-sensitive edge servers, where memory bandwidth is the dominant bot-tleneck for autoregressive token generation. Weight quanti-zation to four bits or below reduces memory pressure, yet existing inference systems still dequantize the compressed weights and execute floating-point multiplications, leaving much of the potential bit-width savings unrealized. Ternary weights, which take values in {− 1, 0, +1}, offer a more radical alternative: every weight-activation product can be replaced by a conditional addition, subtraction, or no-op, eliminat-ing multiplications altogether. Fairy2i recently demonstrated that complex-valued quantization-aware training can pro-duce ternary LLMs whose quality rivals FP16, yet its refer-ence runtime still dispatches the quantized weights through standard floating-point routines, leaving the ternary struc-ture unexploited at the hardware level. Here we present Fairy-Fuse, a companion inference system for Fairy2i that realizes the multiplication-free promise on commodity CPUs for the first time. FairyFuse fuses the eight real-valued sub-GEMVs of each widely-linear layer into a single AVX-512 loop driven entirely by masked additions and subtractions; assembly in-spection confirms that the inner loop contains zero floating-point multiplication instructions. Roofline analysis reveals a structural asymmetry: the 16 × data compression from ternary packing shifts deeply memory-bound GEMV op-erations toward the compute ridge on bandwidth-limited CPUs, yielding a 29 .6× kernel speedup, whereas the same compression provides negligible benefit on bandwidth-rich GPUs. End-to-end, FairyFuse sustains 32.4 tokens per sec-ond on a single Intel Xeon 8558P socket, outperforming llama.cpp Q4_K_M by 1.24 × while preserving near-lossless quality (WikiText-2 perplexity 5.52 versus 5.47 for FP16; av-erage downstream accuracy 66.0%). All measurements are reproducible with a coefficient of variation below 2%. 

1 Introduction 

Large language models (LLMs) [ 30 , 33 ] are increasingly deployed beyond datacenters: on-device assistants, desk-top copilots, and embedded agents all demand low-latency, privacy-preserving inference without discrete GPUs. On 

> ∗Equal contribution.
> †Corresponding author.

such platforms the CPU is the only available compute en-gine, and memory bandwidth is the dominant bottleneck for autoregressive decoding [ 27 ]. Weight quantization is the pri-mary lever for fitting billion-parameter models into limited DRAM and for keeping the weight-streaming working set cache-feasible. At the extreme end of this spectrum, ternary weights 

{− 1, 0, +1} achieve 16 × compression over FP32, and each weight-activation product reduces to a conditional addition, subtraction, or no-op, eliminating multiplications entirely. This idea has roots in early ternary [ 18 , 42 ] and binary [ 13 ,28 ] networks, and recent work confirms that ternary LLMs can match FP16 quality: BitNet b1.58 [ 23 ] trains models from scratch at ternary precision, while Fairy2i [ 34 ] post-trains existing checkpoints into complex-valued ternary models via quantization-aware training. Fairy2i solves the algorithmic side of the problem; the systems challenge is to translate ternary weights into multiplication-free execution on real hardware. Existing inference systems, however, do not yet exploit the ternary structure at the microarchitectural level. Fairy2i’s reference runtime, designed primarily as a research proto-type for validating model quality, dispatches the quantized weights through standard PyTorch linear operators, leaving room for a dedicated inference engine that eliminates multi-plications at the instruction level. Similarly, llama.cpp [ 22 ]applies dequantization followed by floating-point FMA for its Q2_K and Q4_K_M formats, so the low-bit encoding serves only as a storage optimization while the compute path re-mains multiply-heavy. An alternative line of work replaces multiplication with bitwise lookup tables: T-MAC [ 36 ] and BitNet.cpp [ 25 ] use VPSHUFB -based LUT lookups, but the resulting kernels incur indirect memory accesses and non-trivial on-chip LUT footprints that compete with activation data for L1/L2 cache bandwidth, limiting sustained through-put on wide SIMD units. Our starting observation is that ternary arithmetic on x86 CPUs admits a strictly simpler datapath than either dequan-tization or table lookup. We pack sixteen {− 1, 0, +1} weights into a single 32-bit word, decode positive and negative masks in a single-cycle _pext_u32 pair (BMI2), and accumulate via AVX-512 masked vaddps /vsubps . The resulting inner loop contains zero floating-point multiplications and zero table lookups, a property we verify through assembly inspection. 

> arXiv:2604.20913v1 [cs.LG] 22 Apr 2026

Figure 1. FairyFuse compared with existing approaches for ternary LLM inference. (a) Conventional systems dequantize ternary weights to FP16 and perform standard multiplication. (b) LUT-based systems (e.g., T-MAC) replace multiplications with table lookups but incur on-chip memory pressure. (c) FairyFuse directly applies masked addition and subtraction on packed 2-bit weights, with zero multiplications and zero lookup-table overhead. Realizing this idea for complex-valued models introduces an additional complication. Fairy2i’s widely-linear repre-sentation decomposes each linear layer into eight coupled real-valued sub-GEMVs. A naive schedule launches eight sep-arate parallel regions, repeatedly loads the same activation tiles, spills intermediate buffers to DRAM, and re-decodes related mask patterns, erasing the bandwidth gains from ternary packing. To overcome this overhead, we design a 

Fused Widely-Linear Kernel that consolidates all eight sub-GEMVs into one row-parallel AVX-512 loop. Four design decisions make fusion profitable: each decoded mask pair serves both real and imaginary accumulation paths, amortiz-ing decode cost (O1); activation registers are loaded once and shared across all eight sub-GEMVs (O2); complex conjuga-tion reduces to a precomputed sign-flipped alias, eliminating per-lane negation (O3); and all partial sums remain register-resident until the final horizontal reduction, removing in-termediate DRAM traffic (O4). Roofline analysis confirms that the 16 × compression shifts GEMV from deeply memory-bound (AI = 0.25 ) toward the CPU ridge point (AI = 8), yield-ing a 29 .6× speedup over FP32, while the same compression provides negligible benefit on bandwidth-rich GPUs. End-to-end, FairyFuse sustains 32.4 tok/s on a single Intel Xeon 8558P socket, 1.24 × faster than llama.cpp Q4_K_M, while preserving near-lossless quality: WikiText-2 perplexity is 5.52 (vs. 5.47 FP16) and average downstream accuracy is 66.0%. Assembly analysis confirms zero multiplication instructions in the inner loop, and all measurements are reproducible with CV < 2%. In summary, this paper makes three contributions: 

• We implement the first ternary-weight GEMV kernel on x86 CPUs that uses only masked AVX-512 additions and subtractions, with no floating-point multiplica-tions or table lookups in the inner loop. 

• We consolidate the eight sub-GEMVs of a complex-valued widely-linear layer into a single SIMD-friendly loop via mask reuse, input sharing, sign-swap aliasing, and register-resident accumulation, achieving 1.55 ×

speedup over unfused execution. 

• Through full-stack evaluation on a 7B-parameter model, we demonstrate that ternary packing makes CPUs, not GPUs, the natural target for extreme quantization, and deliver throughput competitive with 4-bit baselines at 2-bit storage. The rest of the paper is organized as follows. Section 2 provides background on CPU inference, ternary quantization, and the widely-linear GEMV structure. Section 3 presents the FairyFuse algorithm and fused kernel design. Section 4 describes the implementation. Section 5 evaluates kernel performance, end-to-end throughput and quality, Roofline analysis, and design ablations. Section 6 discusses related work, and Section 7 concludes. 

2 Background and Motivation 

2.1 LLM Inference on CPUs 

Autoregressive large language model inference [ 33 ] is dom-inated by the decode phase, where each new token is pro-duced by applying a stack of linear layers to a short activa-tion vector. In this regime, the critical operation is a batched matrix-vector product (GEMV) whose cost is set almost en-tirely by how quickly model weights can be streamed from DRAM. Concretely, generating one token requires touching every weight parameter at least once, so throughput scales with memory bandwidth rather than peak floating-point throughput [ 27 ]. On commodity CPUs with approximately 200 GB/s of DRAM bandwidth, this bottleneck is especially acute: even aggressively optimized FP16 or INT8 kernels remain far below arithmetic ceilings because the memory system cannot feed operands fast enough. CPU-based inference nevertheless remains attractive for many deployment settings. General-purpose processors are ubiquitous on laptops, desktops, and embedded systems; they avoid the power, thermal, and binary-stack constraints Figure 2. System overview of FairyFuse. Offline, Fairy2i’s quantization converts FP16 weights to 2-bit packed ternary format (3.3 GB). Online, the FairyFuse kernel performs fused 8-GEMV using only masked AVX-512 additions and subtractions, achieving 32.4 tok/s. of discrete accelerators and simplify compliance with data sovereignty requirements by keeping models entirely on the host. Low-latency, privacy-preserving, and fully offline as-sistants are therefore natural targets for CPU execution, pro-vided that inference throughput can be pushed high enough to be interactive at billion-parameter scales. 

2.2 Ternary Quantization for LLMs 

Post-training and quantization-aware methods have sub-stantially reduced the precision of weights and activations while preserving model quality. Representative families in-clude post-training quantization for generative transform-ers [ 9 , 40 ], activation-aware weight quantization [ 19 ], and accurate 8-bit schemes with careful scaling [ 38 ]. These ap-proaches typically operate at four to eight bits per weight and still rely on multiply-accumulate hardware at inference time. At the extreme, ternary weights in {− 1, 0, +1} occupy only 2 bits, a 16 × footprint reduction versus FP32, and replace each weight-activation product with a conditional add, sub-tract, or no-op. BitNet b1.58 [ 23 ] showed that such ternary LLMs can match FP16 quality when trained appropriately. Closest to our setting, Fairy2i [ 34 , 35 ] encodes pretrained FP16 models into complex-valued ternary tensors through a lightweight quantization-aware training procedure called PhaseQuant, often requiring only a couple of calibration epochs. Weights live in {1, −1, 𝑖, −𝑖 } in the complex domain; equivalently, each real and imaginary component is ternary in {− 1, 0, +1}, yielding the same 2-bit storage budget while exploiting phase structure during training. FairyFuse is de-signed as the inference engine for these Fairy2i models, con-verting the algorithmic promise of ternary weights into ac-tual multiplication-free execution. 

2.3 Complex-Valued Widely-Linear GEMV 

Complex-valued neural networks [ 31 ] have shown benefits in signal processing and representation learning. A widely linear transformation augments a standard complex linear map with a conjugate term [ 26 ]. For complex weight matrices 

U, W ∈ C𝑛 ×𝑚 and activations x ∈ C𝑚 , a widely linear layer computes 

y = Ux + Wx , (1) where x denotes complex conjugation. Decomposing x =

xre + 𝑖 xim and writing U = Ure + 𝑖 Uim , W = Wre + 𝑖 Wim with learned per-channel scales 𝑠 𝑈 

> re

, 𝑠 𝑈 

> im

, 𝑠 𝑊  

> re

, 𝑠 𝑊 

> im

, one can separate real and imaginary outputs into eight real-valued GEMVs: 

yre = 𝑠 𝑈  

> re

Ure xre − 𝑠 𝑈  

> im

Uim xim + 𝑠 𝑊  

> re

Wre xre + 𝑠 𝑊  

> im

Wim xim ,

(2) 

yim = 𝑠 𝑈  

> re

Ure xim + 𝑠 𝑈  

> im

Uim xre + 𝑠 𝑊  

> re

Wre xim − 𝑠 𝑊  

> im

Wim xre .

(3) Because every weight block is ternary, each of the eight GEMVs reduces to masked additions and subtractions over activations: the 𝑂 (𝑛𝑚 ) dominant work is multiplication-free .Only the 𝑂 (𝑛 ) application of scalar scales to partial sums retains floating-point multiplies, which is asymptotically negligible relative to the inner dimension for large language models. 

2.4 Deployment Challenges of Ternary LLMs 

Despite the arithmetic structure above, practical systems have not yet translated it into multiplication-free execution on real hardware. Fairy2i’s reference implementation focuses on validating model quality and naturally relies on standard PyTorch operators ( F.linear ), which dispatch the quantized weights through conventional multiply-accumulate routines; the ternary structure remains unexploited at the instruction level. Alternative low-bit CPU engines such as BitNet.cpp and T-MAC replace multiplications with table-driven decod-ing [ 36 ], but LUT kernels trade arithmetic for irregular table lookups that increase L1 pressure and can limit sustained bandwidth on wide SIMD units. Even when ternary structure is respected, the widely-linear form in Eqs. (1) – (3) introduces a second challenge: each complex layer maps to eight real-valued GEMVs. A naive implementation issues eight separate parallel regions (or kernel launches), multiplying synchronization, loop over-head, and register-spill pressure, and may repeatedly load the same activation tiles from memory for different sub-products. These overheads erode the theoretical gains from ternary arithmetic and complicate integration with existing LLM inference frameworks. The design of FairyFuse directly targets both issues: multiplication-free ternary GEMV on real CPUs and fusion across the eight sub-GEMVs, so that complex-valued quantization becomes a deployment asset rather than a constant-factor slowdown. 

3 Design 

Figure 2 summarizes the FairyFuse system. The offline stage extracts ternary weights from the Fairy2i-W2 checkpoint and packs them into the 2-bit encoding described below, yielding a 3.3 GB footprint for seven billion parameters ( 16 ×

smaller than FP32). During online decoding, each generated token passes through 32 Transformer layers; every linear projection that implements the widely-linear map of Eqs. (1) –(3) is evaluated by the FairyFuse fused AVX-512 kernel, which performs all eight real-valued GEMVs as a single row-parallel loop (Section 3.2). We describe the ternary GEMV algorithm first, then present the fused kernel design. 

3.1 FairyFuse Algorithm 

Let 𝑤 ∈ {− 1, 0, +1} be a ternary weight and 𝑎 ∈ R an acti-vation. The product decomposes without any multiply as 

𝑤 · 𝑎 = 𝑎 if 𝑤 = + 1, −𝑎 if 𝑤 = − 1, and 0 if 𝑤 =0. We pack sixteen weights into one uint32 using two bits (𝑏 1, 𝑏 0) per weight: (1, 0) encodes +1 (add ), (0, 1) encodes −1 (subtract ), and (0, 0) encodes 0; the pattern (1, 1) is unused. This repre-sentation achieves 16 × compression relative to FP32 while keeping decode logic SIMD-friendly. 

Parallel bit decode. Let 𝑝 ∈ { 0, . . . , 232 −1} denote one packed word. Two selection masks pick alternating bit lanes in BMI2 _pext_u32 (parallel bits extract): 

𝑀 pos = 0xAAAAAAAAu , 𝑀 neg = 0x55555555u .

We obtain 16-bit masks over one 512-bit SIMD lane by 

𝑘 pos = _pext_u32 (𝑝, 𝑀 pos ) , (4) 

𝑘 neg = _pext_u32 (𝑝, 𝑀 neg ) . (5) Intuitively, 𝑘 pos collects the “add” bits and 𝑘 neg the “subtract” bits after compaction into contiguous sub-registers. 

Masked accumulation. Given a 16-wide FP32 chunk 

x ∈ R16 loaded into an AVX-512 register 𝑋 , the ternary multiply-accumulate reduces to two masked floating-point operations: 

acc ← _mm512_mask_add_ps (acc , 𝑘 pos , acc , 𝑋 ) , (6) 

acc ← _mm512_mask_sub_ps (acc , 𝑘 neg , acc , 𝑋 ) . (7) Each 16-element chunk therefore executes zero floating-point multiplications: only bitwise extract, mask logic, and masked add/sub operations touch the execution units. 

Algorithm 1: FairyFuse Fused 8-GEMV (one output row 𝑖 )

Input : Packed rows 

(Ure )𝑖, ·, (Uim )𝑖, ·, (Wre )𝑖, ·, (Wim )𝑖, · ∈ uint32 𝑚 /16 ;vectors xre , xim , neg_x_im ∈ R𝑚 ; scales 

𝑠 𝑈 

> re

, 𝑠 𝑈 

> im

, 𝑠 𝑊  

> re

, 𝑠 𝑊  

> im

∈ R

Output : Scalars 𝑦 re ,𝑖 , 𝑦 im ,𝑖  

> 1

Initialize a𝑟𝑒 𝑘 , a𝑖𝑚 𝑘 ← 0512 for 𝑘 ∈ { 1, 2, 3, 4} // O4: eight register-resident accumulators  

> 2

for 𝑗 = 0 to 𝑚 /16 − 1 do  

> 3

𝑋 𝑟𝑒 ← load 512 (xre [16 𝑗 :16 𝑗 +15 ]) // O2  

> 4

𝑋 𝑖𝑚 ← load 512 (xim [16 𝑗 :16 𝑗 +15 ])  

> 5

𝑋 𝑛𝑖𝑚 ← load 512 (neg_x_im [16 𝑗 :16 𝑗 +15 ]) // O3  

> 6

𝑝 ← ( Ure )𝑖,𝑗  

> 7

𝑘 + ← _pext_u32 (𝑝, 𝑀 pos ) 

> 8

𝑘 − ← _pext_u32 (𝑝, 𝑀 neg ) // O1  

> 9

a𝑟𝑒  

> 1

← MaskedAS (a𝑟𝑒  

> 1

, 𝑘 +, 𝑘 −, 𝑋 𝑟𝑒 ) 

> 10

a𝑖𝑚  

> 1

← MaskedAS (a𝑖𝑚  

> 1

, 𝑘 +, 𝑘 −, 𝑋 𝑖𝑚 ) // Eqs. (6) –(7)  

> 11

𝑝 ← ( Uim )𝑖,𝑗  

> 12

𝑘 + ← _pext_u32 (𝑝, 𝑀 pos ) 

> 13

𝑘 − ← _pext_u32 (𝑝, 𝑀 neg ) 

> 14

a𝑟𝑒  

> 2

← MaskedAS (a𝑟𝑒  

> 2

, 𝑘 +, 𝑘 −, 𝑋 𝑖𝑚 ) 

> 15

a𝑖𝑚  

> 2

← MaskedAS (a𝑖𝑚  

> 2

, 𝑘 +, 𝑘 −, 𝑋 𝑟𝑒 ) // O4  

> 16

𝑝 ← ( Wre )𝑖,𝑗  

> 17

𝑘 + ← _pext_u32 (𝑝, 𝑀 pos ) 

> 18

𝑘 − ← _pext_u32 (𝑝, 𝑀 neg ) 

> 19

a𝑟𝑒  

> 3

← MaskedAS (a𝑟𝑒  

> 3

, 𝑘 +, 𝑘 −, 𝑋 𝑟𝑒 ) 

> 20

a𝑖𝑚  

> 3

← MaskedAS (a𝑖𝑚  

> 3

, 𝑘 +, 𝑘 −, 𝑋 𝑖𝑚 ) 

> 21

𝑝 ← ( Wim )𝑖,𝑗  

> 22

𝑘 + ← _pext_u32 (𝑝, 𝑀 pos ) 

> 23

𝑘 − ← _pext_u32 (𝑝, 𝑀 neg ) 

> 24

a𝑟𝑒  

> 4

← MaskedAS (a𝑟𝑒  

> 4

, 𝑘 +, 𝑘 −, 𝑋 𝑖𝑚 ) 

> 25

a𝑖𝑚  

> 4

← MaskedAS (a𝑖𝑚  

> 4

, 𝑘 +, 𝑘 −, 𝑋 𝑟𝑒 ) // O4  

> 26

𝑦 re ,𝑖 ←

𝑠 𝑈  

> re

hsum (a𝑟𝑒  

> 1

)− 𝑠 𝑈  

> im

hsum (a𝑟𝑒  

> 2

)+ 𝑠 𝑊  

> re

hsum (a𝑟𝑒  

> 3

)+ 𝑠 𝑊  

> im

hsum (a𝑟𝑒  

> 4

) 

> 27

𝑦 im ,𝑖 ← 𝑠 𝑈  

> re

hsum (a𝑖𝑚  

> 1

) + 𝑠 𝑈  

> im

hsum (a𝑖𝑚  

> 2

) + 𝑠 𝑊  

> re

hsum (a𝑖𝑚  

> 3

) − 

𝑠 𝑊  

> im

hsum (a𝑖𝑚  

> 4

) // weighted horizontal sums 

Comparison to T-MAC.. T-MAC [ 36 ] rewrites low-bit convolutions into table lookups and feeds lookup results through VPSHUFB , retaining an indirect memory access and a non-zero LUT footprint in cache. Our scheme has no LUT: masks drive SIMD units directly, avoiding gather/scatter-style shuffles on activation-dependent tables and eliminating the associated TLB and cache pressure. Where T-MAC’s bit-wise LUT path issues packed byte shuffles, we issue a single-cycle _pext pair followed by AVX-512 masked add/sub on the live activation vector, a structurally different datapath aligned with ternary {− 1, 0, +1} arithmetic rather than small-integer product tables. Here MaskedAS (a, 𝑘 +, 𝑘 −, 𝑋 ) denotes applying Eqs. (6) –(7) in sequence for chunk 𝑋 .Operation Instruction Purpose                   

> Weight decode _pext_u32 (BMI2) Extract pos/neg masks from packed uint32
> Masked addition _mm512_mask_add_ps Accumulate where weight =+1
> Masked subtraction _mm512_mask_sub_ps Accumulate where weight =−1
> Sign flip _mm512_xor_ps Pre-negate x𝑖𝑚 for conjugate terms Horizontal sum _mm512_reduce_add_ps Final per-row reduction

Table 1. Key AVX-512 and BMI2 intrinsics used in the Fairy-Fuse kernel. 

3.2 Fused 8-GEMV Widely-Linear Kernel 

A naïve scheduler runs the eight real GEMVs implied by Eqs. (2) –(3) as independent kernels. That design incurs eight OpenMP fork/join barriers, eight redundant scans over xre 

and xim , and eight sets of intermediate buffers, each pass re-loading the same activations and re-decoding related mask patterns. FairyFuse collapses all eight into one fused row-parallel loop: each thread owns a batch of output indices 𝑖 ,walks the inner dimension 𝑚 once per row, and updates eight AVX-512 accumulators before emitting 𝑦 re [𝑖 ] and 𝑦 im [𝑖 ].Four design decisions, described below and annotated as O1 through O4 in Algorithm 1, make this fusion profitable. The first decision, mask reuse (O1) , exploits the fact that each of the four packed weight matrices contributes one 

(𝑘 +, 𝑘 − ) mask pair per 16-element chunk. In the fused loop, every pair is applied to both a real and an imaginary activa-tion path (using xim or neg_x_im as required by Eqs. (2) –(3) ), so each decode from 𝑝 amortizes across two SIMD updates instead of one. The second, input vector reuse (O2) , loads xre and xim 

into 512-bit registers exactly once per chunk and shares them across all eight sub-GEMVs, halving activation traffic versus eight independent passes. Third, the sign-swap trick (O3) handles the terms in-volving x, which flip the sign of xim in half of the products. We precompute neg_x_im = −xim once per layer input and switch pointers inside the inner loop, turning conjugation into a zero-cost alias rather than a per-lane negate. Finally, register-resident accumulators (O4) keep all eight sets of partial sums in AVX-512 registers for the du-ration of the inner loop; only after the final chunk do we invoke a horizontal reduction ( _mm512_reduce_add_ps ) and apply the scalar scales. This removes all intermediate spills to DRAM along the reduction dimension. Together, these four optimizations yield a 1.55 × speedup over eight separate GEMVs under DRAM-cold, single-thread measurement, and 1.39 × at 48 threads where eliminating barriers and redundant parallel regions dominates. 

4 Implementation 

Table 1 lists the hardware intrinsics that constitute the Fairy-Fuse kernel’s inner loop [ 14 , 15 ]. The entire GEMV path is built on two arithmetic instructions ( vaddps , vsubps ) gated by 16-bit masks, plus a single-cycle bit-extraction ( pext ) for weight decoding; no multiplication or fused multiply-add            

> Device Processor Performance Max. Memory Cores Bandwidth (GB/s) Server CPU Intel Xeon 8558P 48 ∼200 Server GPU NVIDIA H200 NVL –4,800

Table 2. Hardware platforms. instructions appear in the critical path. The kernel is com-piled with g++ -O3 and the relevant AVX-512 plus BMI2 flags, then exposed as a shared library ( libternary_gemv.so ). The minimum ISA requirement is AVX-512F + BMI2, avail-able on Intel Skylake-X and later server processors. Row-level parallelism is exploited via OpenMP: each thread processes a contiguous range of output rows, with no inter-thread synchronization during the inner loop. A single #pragma omp parallel for directive over the out-put dimension suffices for the fused kernel, eliminating the eight separate fork/join barriers that a naïve implementation would incur. The thread count is set to match the physi-cal core count of a single NUMA node (48 on Xeon 8558P) to avoid cross-socket traffic. All experiments use single-socket NUMA binding via numactl with --cpunodebind=0 --membind=0 ; cross-socket execution degrades throughput by approximately 10% due to remote DRAM latency (Appen-dix B). Packed weights are stored contiguously in row-major order with per-row FP32 scales; the 3.3 GB model file is memory-mapped via mmap (Appendix H). The pipeline com-prises approximately 1500 lines of C++ (kernel plus forward pass) and 500 lines of Python, connected through ctypes .Each token traverses 32 Transformer layers: RMSNorm (FP32), the fused 8-GEMV (multiplication-free), and SiLU activa-tion (FP32). The fused GEMV accounts for over 90% of per-layer compute, ensuring the vast majority of arithmetic is multiplication-free (Appendix J). 

5 Evaluation 

We evaluate FairyFuse along three axes: (1) kernel-level GEMV speedup and assembly verification of the multiplication-free claim, (2) end-to-end throughput and model quality against llama.cpp baselines, and (3) a Roofline-guided analysis explaining why CPUs, not GPUs, benefit from ternary packing. 

5.1 Setup 

All experiments run on the platforms listed in Table 2. The CPU is an Intel Xeon 8558P with 48 cores and approximately 200 GB/s of DRAM bandwidth. The GPU is an NVIDIA H200 NVL with 140 GB HBM3 and 4.8 TB/s peak band-width; it serves as a bandwidth-rich reference point for the Roofline analysis. We compare FairyFuse (Fairy2i-W2, ternary, 3.3 GB) against llama.cpp baselines on LLaMA-2-7B [ 30 ]: FP16 (13.5 GB), Q4_K_M (4.1 GB), and Q2_K (2.8 GB). Microbenchmarks use 10 warmup iterations and at least 1000 timed iterations; end-to-end throughput is measured over Matrix Size FP32 1t Tern. 1t Tern. 48t 1t Speedup 48t Speedup 

(𝜇 s) (𝜇 s) (𝜇 s) vs FP32 1t vs FP32 1t 

4096 × 4096 12,550 2,410 424 5.2× 29 .6×

11008 × 4096 42,730 6,480 786 6.6× 54 .4×

4096 × 11008 13,220 6,500 447 2.0× 29 .6×

Table 3. DRAM-cold GEMV latency. Single-threaded speedup isolates the algorithmic advantage of ternary arith-metic; 48-thread speedup combines this with parallelism. 4096 × 4096 11008 × 4096 4096 × 11008        

> 0
> 2
> 4
> 6
> 8
> Speedup vs FP32
> 5.2x
> 6.6x
> 2.0x
> (a) 1-thread speedup
> 4096 × 4096 11008 × 4096 4096 × 11008
> 0
> 10
> 20
> 30
> 40
> 50
> 60
> Speedup vs FP32
> 29.6x
> 54.4x
> 29.6x
> (b) 48-thread speedup
> 1416 48
> Threads
> 10 2
> 10 3
> Latency (  s)
> (c) Thread scaling ( 4096 × 4096 )
> DRAM-cold
> L3-warm
> 4096 × 4096 11008 × 4096 4096 × 11008
> 0
> 200
> 400
> 600
> 800
> Latency (  s)
> (d) L3 vs DRAM at 48 threads
> L3-warm
> DRAM-cold

Figure 3. GEMV kernel performance (DRAM-cold). (a) 1-thread speedup ( 2–6.6×). (b) 48-thread speedup ( 29 .6–54 .4×). (c) Thread scaling. (d) L3 vs. DRAM at 48 threads. at least 128 generated tokens. All stochastic experiments are repeated with seeds {42 , 123 , 2026 }; we report median latency for GEMV and mean ± std for end-to-end through-put. Coefficient of variation stays below 2% across all metrics (Appendix E). 

5.2 Kernel Performance 

Table 3 and Figure 3 present DRAM-cold GEMV results across three matrix sizes drawn from LLaMA-2-7B linear layers. At equal thread count, the ternary kernel achieves 

2.0–6.6× speedup over FP32, purely from eliminating mul-tiplications and reducing data movement via 16 × weight compression. When all 48 cores are utilized, the combined speedup reaches 29 .6–54 .4×. The 11008 ×4096 case achieves super-linear scaling ( 54 .4×) because its packed working set (approximately 174 KB) fits in last-level cache (105 MB), sub-stantially reducing DRAM traffic. We verify the multiplication-free property via objdump 

inspection of the compiled kernel. Table 4 summarizes the inner-loop instruction mix: each 16-element chunk executes eight pext (mask decode), eight vaddps (masked addition), and eight vsubps (masked subtraction), with zero vmulps 

or vfmadd instructions. The only multiplications are O( 𝑛 )

scalar vmulss for per-channel scale application, accounting for less than 0.025% of dynamic arithmetic. 

Instruction Count Role 

pext 8 Weight mask decode 

vaddps 8 Masked addition 

vsubps 8 Masked subtraction 

vmulps / vfmadd 0 Multiplication 

Table 4. Inner-loop instruction counts per 16-element GEMV chunk. 

Method Throughput Memory WikiText-2 Downstream 

tok/s ↑ GB PPL ↓ Avg Acc. ↑

FP16 8.24 13.5 5.47 67.3% llama.cpp Q4_K_M 26.15 4.1 5.68 65.1% 

FairyFuse (ours) 32.43 3.3 5.52 66.0% 

llama.cpp Q2_K 20.10 2.8 7.82 56.6% 

Table 5. End-to-end throughput, memory, and quality. Fairy-Fuse achieves the highest throughput ( 32 .43 ± 0.41 tok/s) while preserving near-FP16 quality. FairyFuse’s 3.3 GB in-cludes FP16 embeddings and per-channel FP32 scales (Ap-pendix H). FP16 Q4_K_M FairyFuse            

> (ours)
> Q2_K
> 0
> 5
> 10
> 15
> 20
> 25
> 30
> Throughput (tok/s)
> 8.2
> 26.1
> 32.4
> 20.1
> (a) Token generation throughput
> FP16 Q4_K_M FairyFuse
> (ours)
> Q2_K
> 5
> 6
> 7
> 8
> 9
> WikiText-2 PPL (  )
> 5.47 5.68 5.52
> 7.82
> (b) Perplexity
> FP16 Q4_K_M FairyFuse
> (ours)
> Q2_K
> 50
> 55
> 60
> 65
> 70
> Avg Accuracy (%)  67.3%
> 65.1% 66.0%
> 56.6%
> (c) Downstream accuracy
> FP16 Q4_K_M FairyFuse
> (ours)
> Q2_K
> 0
> 2
> 4
> 6
> 8
> 10
> 12
> 14
> Memory (GB)
> 13.5
> 4.1 3.3 2.8
> (d) Memory footprint

Figure 4. End-to-end results. (a) Throughput ( 1.24 × vs. Q4_K_M). (b) Perplexity (5.52, within 0.05 of FP16). (c) Down-stream accuracy (66.0%). (d) Memory efficiency. 

5.3 End-to-End Throughput and Quality 

Having established the kernel-level advantage, we now as-sess end-to-end performance and model quality. Table 5 and Figure 4 report the main results. FairyFuse achieves 32 .43 ± 0.41 tok/s, 1.24 × faster than Q4_K_M (26.15 tok/s) and 1.61 × faster than Q2_K (20.1 tok/s), while using half the bit-width of Q4_K_M. Notably, FairyFuse re-mains faster than Q2_K despite Q2_K’s smaller footprint (2.8 GB vs. 3.3 GB), because the multiplication-free datapath reduces compute latency in addition to compressing memory traffic. In terms of model quality, WikiText-2 perplexity is 5.52 versus 5.47 for FP16 (a gap of only 0.05), better than Q4_K_M (5.68) and substantially better than Q2_K (7.82). Downstream Platform GEMV Latency BW Util. Speedup 

𝜇 s GB/s vs FP32 GPU H200 (FP16) 24.5 1,368 –GPU H200 (Ternary) 3,200 – 130 × slower CPU Xeon (FP32 1t) 12,550 4.98 1.0×

CPU Xeon (Ternary 48t) 424 9.24 29 .6×

Table 6. GPU vs. CPU GEMV ( 4096 × 4096 ). GPU ternary regresses 130 × vs. FP16 due to lack of efficient pext -class instructions; CPU ternary achieves 29 .6× speedup. 10 1 10 0 10 1 10 2      

> Arithmetic Intensity (OP/byte)
> 10 0
> 10 1
> 10 2
> 10 3
> 10 4
> 10 5
> Performance (GOP/s)
> FP32 dense
> (AI=0.25)
> Ternary fused
> (AI=8.0)
> (a) Roofline model
> CPU Xeon 8558P
> GPU H200
> 10 210 310 410 5
> 10 6
> GEMV Latency ( s, log scale)
> GPU FP16
> (raw)
> CPU FP32
> (1t)
> CPU Tern
> (48t)
> GPU Tern
> (PhaseQ)
> 24.5 s
> 12.6 ms
> 424.0 s
> 3.2 s
> (b) Platform comparison ( 4096 × 4096 )

Figure 5. GPU vs. CPU analysis. (a) Roofline: ternary packing raises AI from 0.25 to 8.0, shifting the kernel toward the CPU ridge ( 29 .6×) while GPU bandwidth renders compression negligible. (b) Platform comparison: GPU ternary regresses 

130 ×; CPU ternary outperforms all CPU alternatives. accuracy is 66.0% versus 67.3% for FP16 ( −1.3 pp), outper-forming Q4_K_M (65.1%) and Q2_K (56.6%). Per-task break-downs are provided in Appendix G. The quality preservation stems from Fairy2i’s complex-valued QAT [ 34 ]; FairyFuse contributes a lossless, multiplication-free inference path that reproduces these numbers with higher throughput. 

5.4 Roofline Analysis: Why CPUs, Not GPUs 

A surprising result from our evaluation is that ternary pack-ing, which provides a 29 .6× speedup on CPUs, actually hurts 

GPU performance by over two orders of magnitude. Table 6 and Figure 5 explain this asymmetry through the Roofline model. FP32 dense GEMV has arithmetic intensity AI = 0.25 FLOP/byte, placing it deep in the memory-bound regime on both platforms. Ternary packing raises AI to 8.0 OP/byte. On the CPU (ridge point = 13.5), this shift moves the kernel toward the compute ceiling, unlocking a 

29 .6× speedup. On the GPU (ridge point = 27.9), AI = 8.0 still falls well below the ridge, so the kernel remains memory-bound, and the abundant HBM bandwidth (4.8 TB/s) leaves no headroom for compression to help. Worse, GPUs lack efficient equivalents to BMI2’s _pext_u32 , forcing the ternary decode into multiple shifts and masks that degrade ALU utilization. We note that the GPU ternary result uses a custom CUDA kernel with the same algorithmic structure as the CPU version (Appendix F); while further GPU optimization is possible, the fundamental absence of single-cycle conditional add/sub and parallel bit extract 1t 16t 48t 

> 0
> 2500
> 5000
> 7500
> 10000
> 12500
> 15000
> 17500
> 20000
> Latency (  s)
> 1.55x
> 1.38x
> 1.39x
> (a) Unfused vs Fused (DRAM-cold)
> 8x Unfused
> FairyFuse Fused
> O1
> Mask
> reuse
> O2
> Input
> reuse
> O3
> Sign
> swap
> O4
> Register
> accum
> OMP
> Barrier
> elim
> 0
> 5
> 10
> 15
> 20
> 25
> Contribution (%)
> 15%
> 20%
> 5%
> 10%
> 5%
> (b) Per-optimization contribution

Figure 6. Design ablation. (a) Unfused vs. fused latency (DRAM-cold): fusion yields 1.39 –1.55 × speedup. (b) Contri-bution of each fusion optimization (O1–O4). in CUDA makes the mismatch structural rather than an implementation artifact. This analysis positions ternary inference as a structurally CPU-favorable workload: the combination of limited DRAM bandwidth and efficient bitwise ISA extensions ( pext , AVX-512 masked ops) makes CPUs uniquely suited for this arith-metic regime. 

5.5 Design Ablation 

To isolate the contribution of kernel fusion, we compare two configurations: (1) an Unfused baseline that calls the optimized single-GEMV kernel eight times sequentially with separate OpenMP regions, and (2) the FairyFuse Fused 

kernel that consolidates all eight into one loop. Figure 6(a) shows that fusion yields 1.55 × speedup at 1 thread (DRAM-cold) and 1.39 × at 48 threads, consistent across matrix sizes (Appendix C). The benefit at 1 thread is dominated by O4 (register-resident accumulators eliminate intermediate spills); at 48 threads, O2 (input vector reuse) and barrier elimination dominate. Figure 6(b) decomposes the fusion gain into individual con-tributions: O1 (mask reuse) contributes approximately 15%, O2 (input reuse) approximately 20%, O3 (sign-swap) approx-imately 5%, O4 (register accumulators) approximately 10%, and OpenMP barrier elimination approximately 5%. Each optimization targets a distinct overhead of the widely-linear structure, and their composition is approximately additive. 

5.6 Scalability 

Thread scaling exhibits three distinct regimes. From 1 to 16 threads, scaling is near-linear with parallel efficiency above 80%, because the ternary kernel’s small working set (packed weights plus three activation vectors) fits comfort-ably in L3 cache, and each thread operates on indepen-dent output rows without synchronization. Between 16 and 48 threads, scaling becomes sub-linear as DRAM bandwidth saturates; adding more cores no longer reduces memory-access latency proportionally. Beyond 48 threads (i.e., span-ning both sockets), throughput degrades by approximately 10% due to cross-socket NUMA penalties, where remote memory accesses double the effective latency. The optimal configuration is therefore single-socket binding (48 threads, one NUMA node), which achieves the full 32.4 tok/s. Full thread-scaling curves and NUMA analysis are in Appendix B. 

6 Related Works 

LLM Quantization Algorithms. Post-training quanti-zation has become the standard approach for compressing large language models. GPTQ [ 9] and AWQ [ 19 ] pioneered weight-only compression at four to eight bits with minimal accuracy loss, while SmoothQuant [ 38 ] addressed activation outliers to enable combined weight-activation quantization. OmniQuant [ 29 ] and ZeroQuant [ 40 ] further refined calibra-tion strategies for generative transformers. Mixed-precision regimes such as LLM.int8() [ 5 ] and QLoRA [ 6 ] demonstrated that quality can be preserved at moderate compression by keeping outlier channels in higher precision. More aggres-sive methods push below four bits: SqueezeLLM [ 16 ] in-troduced dense-and-sparse decomposition, QuIP [ 2 ] and QuIP# [ 32 ] exploited incoherence processing and lattice code-books, and AQLM [ 8] achieved competitive two- to three-bit compression via additive quantization. At the ternary and binary extreme, BiLLM [ 12 ], PB-LLM [ 41 ], OneBit [ 39 ], and BitDistiller [ 7] target one to two bits, while BitNet b1.58 [ 23 ]demonstrated competitive quality with ternary weights trained from scratch. On the training side, LLM-QAT [ 21 ]and EfficientQAT [ 3 ] proposed data-free and block-wise quantization-aware training for practical low-bit pipelines. All of these methods focus on the algorithmic dimension of quantization; our work is complementary in that, given a ternary-quantized model (Fairy2i [ 34 ]), we provide the sys-tem that makes multiplication-free inference a reality on commodity hardware. 

CPU Inference Systems. The strongest publicly avail-able CPU inference baseline is llama.cpp [ 22 ], which pro-vides hand-tuned AVX-512 and ARM NEON kernels across a range of quantization formats including Q4_K_M and Q2_K. T-MAC [ 36 ] takes a different approach by replacing scalar multiplication with VPSHUFB -based table lookups, eliminat-ing multiplications but introducing on-chip LUT memory pressure and indirect accesses; it reports up to 4× throughput gain over llama.cpp on ARM CPUs (Apple M2 Ultra) at one to two bits. On our x86 platform, the LUT approach faces additional headwinds: Intel’s VPSHUFB operates on 128-bit lanes, limiting throughput relative to our 512-bit masked add/sub path. BitNet.cpp [ 25 ] targets ternary models but similarly relies on LUT-driven approaches. QServe [ 20 ] co-designs W4A8KV4 quantization with GPU serving kernels for cloud deployment. Our approach is fundamentally differ-ent: rather than lookup tables or dequantization, FairyFuse performs direct masked addition and subtraction on packed ternary weights, eliminating both multiplication and table-memory overhead while exploiting the widely-linear struc-ture of complex-valued quantization for kernel fusion. 

Efficient LLM Serving. Efficient inference has been stud-ied from multiple complementary angles. FlashAttention [ 4]optimizes the attention mechanism via IO-aware tiling to reduce memory reads and writes. vLLM [ 17 ] introduces Page-dAttention for memory-efficient KV-cache management in serving scenarios. DeepSpeed-Inference [ 1 ] provides multi-GPU inference parallelism at scale. These systems target GPU-centric deployments and focus on scheduling, memory, or attention optimizations; FairyFuse addresses a comple-mentary axis by eliminating arithmetic complexity on CPUs via ternary quantization. 

Binary and Ternary Network Foundations. The idea of replacing multiplications with simpler operations dates back to binary [ 13 , 28 ] and ternary [ 18 , 42 ] neural networks, as well as the Deep Compression pipeline [ 11 ], which combined pruning, quantization, and Huffman coding for efficient stor-age. These early methods targeted convolutional networks at moderate scale; extending the approach to billion-parameter LLMs with complex-valued quantization [ 31 , 34 , 35 ] and efficient SIMD execution on commodity CPUs is the contri-bution of FairyFuse. 

7 Conclusion 

We have presented FairyFuse, an inference system that re-places floating-point multiplication with direct masked ad-dition and subtraction for ternary-weight LLMs on com-modity CPUs. By fusing the eight sub-GEMVs of a complex-valued widely-linear layer into a single AVX-512 loop, Fairy-Fuse achieves 32.4 tokens per second on a single Xeon 8558P socket, outperforming llama.cpp Q4_K_M by 1.24 ×

while preserving near-lossless quality (WikiText-2 perplexity 5.52, downstream accuracy 66.0%). Roofline analysis reveals that the 16 × data compression from ternary packing shifts deeply memory-bound GEMV toward the compute ridge on bandwidth-limited CPUs, making CPUs, rather than GPUs, the natural target for extreme quantization. FairyFuse thus demonstrates a practical, multiplication-free path for deploy-ing billion-parameter LLMs on the processors that are most widely available. 

Limitations and future work. Our evaluation covers a single model (LLaMA-2-7B) and a single ISA (x86 AVX-512). Extending FairyFuse to ARM platforms would require adapting the bit-decode path to NEON’s narrower 128-bit SIMD lanes, and scaling to larger models (13B, 70B) may shift the optimal thread-to-row mapping. FairyFuse currently re-lies on Fairy2i’s complex-valued quantization; supporting alternative ternary recipes would broaden the range of com-patible models. Finally, extending the fused design to batched GEMM for prompt-processing and speculative decoding is a natural next step. References 

[1] Reza Yazdani Aminabadi, Samyam Rajbhandari, Ammar Ahmad Awan, Cheng Li, Du Li, Elton Zheng, Olatunji Ruwase, Shaden Smith, Minjia Zhang, Jeff Rasley, and Yuxiong He. DeepSpeed-Inference: Enabling efficient inference of transformer models at unprecedented scale. SC ,2022. [2] Jerry Chee, Yaohui Cai, Volodymyr Kuleshov, and Christopher De Sa. QuIP: 2-bit quantization of large language models with guarantees. In 

NeurIPS , 2023. [3] Mengzhao Chen, Wenqi Shao, Peng Xu, et al. EfficientQAT: Efficient quantization-aware training for large language models. ACL , 2025. [4] Tri Dao, Dan Fu, Stefano Ermon, Atri Rudra, and Christopher Ré. FlashAttention: Fast and memory-efficient exact attention with IO-awareness. NeurIPS , 2022. [5] Tim Dettmers, Mike Lewis, Younes Belkada, and Luke Zettlemoyer. LLM.int8(): 8-bit matrix multiplication for transformers at scale. 

NeurIPS , 2022. [6] Tim Dettmers, Artidoro Pagnoni, Ari Holtzman, and Luke Zettlemoyer. QLoRA: Efficient finetuning of quantized language models. In NeurIPS ,2023. [7] Dayou Du et al. BitDistiller: Unleashing the potential of sub-4-bit LLMs via self-distillation. In ACL , 2024. [8] Vage Egiazarian, Andrei Panferov, Denis Kuznedelev, Elias Frantar, Artem Babenko, and Dan Alistarh. Extreme compression of large language models via additive quantization. In ICML , 2024. [9] Elias Frantar, Saleh Ashkboos, Torsten Hoefler, and Dan Alistarh. GPTQ: Accurate post-training quantization for generative pre-trained transformers. In ICLR , 2023. [10] Leo Gao et al. A framework for few-shot language model evaluation. 

https://github.com/EleutherAI/lm-evaluation-harness , 2024. [11] Song Han, Huizi Mao, and William J Dally. Deep compression: Com-pressing deep neural network with pruning, trained quantization and Huffman coding. ICLR , 2016. [12] Wei Huang, Yangdong Liu, Haotong Qin, Ying Li, Shiming Zhang, Xianglong Liu, Michele Magno, and Xiaojuan Qi. BiLLM: Pushing the limit of post-training quantization for LLMs. In ICML , 2024. [13] Itay Hubara, Matthieu Courbariaux, Daniel Soudry, Ran El-Yaniv, and Yoshua Bengio. Binarized neural networks. In NeurIPS , 2016. [14] Intel Corporation. Intel 64 and IA-32 Architectures Software Developer’s Manual , 2024. Volume 2: Instruction Set Reference. [15] Intel Corporation. Intel intrinsics guide. https://www.intel.com/ content/www/us/en/docs/intrinsics-guide/ , 2024. [16] Sehoon Kim, Coleman Hooper, Amir Gholami, Zhen Dong, Xiuyu Li, Sheng Shen, Michael W Mahoney, and Kurt Keutzer. SqueezeLLM: Dense-and-sparse quantization. ICML , 2024. [17] Woosuk Kwon, Zhuohan Li, Siyuan Zhuang, Ying Sheng, Lianmin Zheng, Cody Hao Yu, Joseph E Gonzalez, Hao Zhang, and Ion Stoica. Efficient memory management for large language model serving with PagedAttention. SOSP , 2023. [18] Fengfu Li, Bo Zhang, and Bin Liu. Ternary weight networks. arXiv preprint arXiv:1605.04711 , 2016. [19] Ji Lin, Jiaming Tang, Haotian Tang, Shang Yang, Wei-Ming Chen, Wei-Chen Wang, Guangxuan Xiao, Xingyu Dang, Chuang Gan, and Song Han. AWQ: Activation-aware weight quantization for LLM compression and acceleration. In MLSys , 2024. [20] Yujun Lin, Haotian Tang, Shang Yang, Zhekai Zhang, Guangxuan Xiao, Chuang Gan, and Song Han. QServe: W4A8KV4 quantization and system co-design for efficient LLM serving. In MLSys , 2025. [21] Zechun Liu, Barlas Oguz, Changsheng Zhao, Ernie Chang, Pierre Stock, Yashar Mehdad, Yangyang Shi, Raghuraman Krishnamoorthi, and Vikas Chandra. LLM-QAT: Data-free quantization aware training for large language models. In Findings of ACL , 2024. [22] llama.cpp contributors. llama.cpp: Inference of Meta’s LLaMA model in C/C++. https://github.com/ggerganov/llama.cpp , 2024. [23] Shuming Ma, Hongyu Wang, Lingxiao Ma, Lei Wang, Wenhui Wang, Shaohan Huang, Li Dong, Ruiping Wang, Jilong Xue, and Furu Wei. The era of 1-bit LLMs: All large language models are in 1.58 bits. arXiv preprint arXiv:2402.17764 , 2024. [24] Stephen Merity, Caiming Xiong, James Bradbury, and Richard Socher. Pointer sentinel mixture models. ICLR , 2017. [25] Microsoft. BitNet.cpp: Official inference framework for 1-bit LLMs. 

https://github.com/microsoft/BitNet , 2024. [26] Bernard Picinbono and Pascal Chevalier. Widely linear estimation with complex data. IEEE Transactions on Signal Processing , 43(8):2020–2024, 1995. [27] Reiner Pope, Sholto Douglas, Aakanksha Chowdhery, Jacob Devlin, James Bradbury, Jonathan Heek, Kefan Xiao, Shivani Agrawal, and Jeff Dean. Efficiently scaling transformer inference. In MLSys , 2023. [28] Mohammad Rastegari, Vicente Ordonez, Joseph Redmon, and Ali Farhadi. XNOR-Net: ImageNet classification using binary convolu-tional neural networks. In ECCV , 2016. [29] Wenqi Shao, Mengzhao Chen, Zhaoyang Zhang, Peng Xu, Lirui Zhao, Zhiqian Li, Kaipeng Zhang, Peng Gao, Yu Qiao, and Ping Luo. Omni-Quant: Omnidirectionally calibrated quantization for large language models. In ICLR , 2024. [30] Hugo Touvron, Louis Martin, Kevin Stone, et al. Llama 2: Open foun-dation and fine-tuned chat models. arXiv preprint arXiv:2307.09288 ,2023. [31] Chiheb Trabelsi, Olexa Bilaniuk, Ying Zhang, et al. Deep complex networks. ICLR , 2018. [32] Albert Tseng, Jerry Chee, Qingyao Sun, Volodymyr Kuleshov, and Christopher De Sa. QuIP#: Even better LLM quantization with Hadamard incoherence and lattice codebooks. In ICML , 2024. [33] Ashish Vaswani, Noam Shazeer, Niki Parmar, Jakob Uszkoreit, Llion Jones, Aidan N Gomez, Łukasz Kaiser, and Illia Polosukhin. Attention is all you need. In NeurIPS , 2017. [34] Feiyu Wang, Xinyu Tan, Bokai Huang, Yihao Zhang, Guoan Wang, Peizhuang Cong, and Tong Yang. Fairy2i: Training complex LLMs from real LLMs with all parameters in {± 1, ±𝑖 }. arXiv preprint arXiv:2512.02901 , 2025. [35] Feiyu Wang, Guoan Wang, Yihao Zhang, Shengfan Wang, Weitao Li, Bokai Huang, Shimao Chen, Zihan Jiang, Rui Xu, and Tong Yang. iFairy: the first 2-bit complex LLM with all parameters in {± 1, ±𝑖 }.

arXiv preprint arXiv:2508.05571 , 2025. [36] Jianyu Wei, Shijie Cao, Ting Cao, Lingxiao Ma, Lei Wang, Yanyong Zhang, and Mao Yang. T-MAC: CPU renaissance via table lookup for low-bit LLM deployment on edge. In EuroSys , 2025. [37] Samuel Williams, Andrew Waterman, and David Patterson. Roofline: An insightful visual performance model for multicore architectures. 

Communications of the ACM , 52(4):65–76, 2009. [38] Guangxuan Xiao, Ji Lin, Mickael Seznec, Hao Wu, Julien Demouth, and Song Han. SmoothQuant: Accurate and efficient post-training quantization for large language models. In ICML , 2023. [39] Yuzhuang Xu et al. OneBit: Towards extremely low-bit large language models. In NeurIPS , 2024. [40] Zhewei Yao, Reza Yazdani Aminabadi, Minjia Zhang, Xiaoxia Wu, Conglong Li, and Yuxiong He. ZeroQuant: Efficient and affordable post-training quantization for large-scale transformers. In NeurIPS ,2022. [41] Zhihang Yuan, Yuzhang Shang, Qiang Wu, and Zhen Dong. PB-LLM: Partially binarized large language models. In ICLR , 2024. [42] Chenzhuo Zhu, Song Han, Huizi Mao, and William J Dally. Trained ternary quantization. ICLR , 2017. Appendix 

This appendix provides detailed experimental data, analysis, and implementation specifics that support the main text. 

Table of Contents A Detailed GEMV Micro-Benchmark Results 

B Thread Scalability, NUMA, and Cache Analysis 

C Kernel Optimization Ablation Details 

D Assembly Analysis: Multiplication-Free Verification 

E Reproducibility and Cache Analysis 

F GPU Implementation Details 

G Quality Evaluation Details 

H Weight Packing Format 

I Extended Roofline Analysis 

J End-to-End Inference Pipeline Details 

K llama.cpp Baseline Configuration 

A Detailed GEMV Micro-Benchmark Results 

This appendix provides complete GEMV micro-benchmark data across all matrix sizes, thread counts, and cache condi-tions. Table 7 provides the full numeric data underlying Figure 7. 

Analysis. Under L3-warm conditions, ternary GEMV achieves over 500 × speedup at 16 threads for square matrices, where the ∼2 MB packed working set fits in L3 ( 105 MB) and the operation becomes purely compute-bound at ∼0.25 ns per add/sub. The DRAM-cold results are more relevant to real inference: the 29 .6× speedup for 4096 ×4096 reflects the 

16 × compression ratio amplified by ILP-unrolled pipelining. The apparent decrease in speedup from 16 to 48 threads under L3-warm conditions occurs because the FP32 baseline also benefits from parallelism, compressing the ratio. In abso-lute terms, 48-thread ternary GEMV is faster than 16-thread for DRAM-cold data. 

B Thread Scalability, NUMA, and Cache Analysis 

Thread scaling analysis. Figure 8(a)–(c) reveals three distinct regimes: (1) 1–16 threads : near-linear scaling (ef-ficiency >80% ); the row-parallel decomposition distributes work evenly and per-thread DRAM bandwidth is not satu-rated. (2) 16–48 threads : sub-linear scaling (efficiency ∼40% ); aggregate DRAM bandwidth approaches the single-socket ceiling of ∼200 GB/s. (3) 48–96 threads : negative returns; cross-socket NUMA remote memory accesses ( ∼2× higher latency) dominate. 

NUMA configuration. Figure 8(d) compares eight con-figurations. The optimal strategy is clear: bind to a single NUMA node (NUMA 2,3 48t achieves 30.5 tok/s). Dual-socket and unbound configurations suffer 10 –30% throughput loss from remote memory access penalties. 

Table 7. Detailed GEMV latency ( 𝜇 s) and speedup data. “L3” = L3-warm (data pre-loaded in cache); “DRAM” = DRAM-cold (L3 flushed before measurement). Speedup is computed against FP32 dense (1 thread).                                        

> Matrix Mode FP32-1t Tern-1t Tern-4t Tern-16t Tern-48t
> 4096 ×4096 L3 8,264 194 52 15 64 DRAM 12,550 2,410 680 210 424
> 11008 ×4096 L3 22,520 490 135 40 173 DRAM 42,730 6,480 1,780 540 786
> 4096 ×11008 L3 8,390 524 140 40 65 DRAM 13,220 6,500 1,740 530 447

Table 8. GEMV speedup ratios (against FP32 dense, 1 thread). 

Matrix Mode 1t 4t 16t 48t 

4096 ×4096 L3 42 .6× 158 .9× 550 .9× 129 .1×

DRAM 5.2× 18 .5× 59 .8× 29 .6×

11008 ×4096 L3 46 .0× 166 .8× 563 .0× 130 .2×

DRAM 6.6× 24 .0× 79 .1× 54 .4×

4096 ×11008 L3 16 .0× 59 .9× 209 .8× 129 .1×

DRAM 2.0× 7.6× 24 .9× 29 .6×

Table 9. Kernel optimization ablation ( 4096 ×4096 GEMV). All latencies in 𝜇 s, measured as the median of 1000 iterations.                                              

> Optimization L3-warm ( 𝜇 s) DRAM-cold ( 𝜇 s) 1t 16t 48t 1t 16t 48t
> Baseline (SIMD) 485 38 90 6,020 520 980 +ILP unrolling 194 15 64 2,410 210 424 +Prefetch 188 13 59 2,300 195 405 8×Unfused (sep.) 1,552 120 512 19,280 1,680 3,392 FairyFuse Fused 1,118 87 368 12,430 1,220 2,444 Fusion speedup 1.39 ×1.38 ×1.39 ×1.55 ×1.38 ×1.39 ×
> Note: Fused rows are for the full widely-linear layer (8 GEMVs); single-GEMV rows show per-kernel latency. “8 ×Unfused” calls the optimized kernel eight times sequentially.

Cache effect. The ∼42% efficiency at 48 threads under DRAM-cold indicates that DRAM bandwidth is the primary bottleneck, consistent with the Roofline analysis of Sec-tion 5.4. L3-warm efficiency remains high ( 81 .9% at 16t) be-cause the packed working set fits in cache, making the kernel compute-bound. 

C Kernel Optimization Ablation Details 

Table 9 provides the full comparison across all kernel ver-sions. 

Per-optimization breakdown. ILP unrolling (2.5× over baseline): breaks the single-accumulator dependency chain, allowing the CPU’s out-of-order engine to overlap four inde-pendent add/sub streams. Software prefetch (1.05 –1.12 ×1 4 16 48 

Threads    

> 10 2
> 10 3
> 10 4
> Latency (  s)
> 4096 × 4096
> L3-warm
> Ternary
> FP32 1t
> 1416 48

Threads    

> 10 3
> 10 4
> Latency (  s)
> 4096 × 4096
> DRAM-cold
> Ternary
> FP32 1t
> 1416 48

Threads    

> 10 2
> 10 3
> 10 4
> Latency (  s)
> 11008 × 4096
> L3-warm
> Ternary
> FP32 1t
> 1416 48

Threads    

> 10 3
> 10 4
> Latency (  s)
> 11008 × 4096
> DRAM-cold
> Ternary
> FP32 1t
> 1416 48

Threads    

> 10 2
> 10 3
> 10 4
> Latency (  s)
> 4096 × 11008
> L3-warm
> Ternary
> FP32 1t
> 1416 48

Threads 

> 10 3
> 10 4
> Latency (  s)
> 4096 × 11008
> DRAM-cold
> Ternary
> FP32 1t

Figure 7. Complete GEMV micro-benchmark results. Latency (log-scale) for three representative matrix sizes from LLaMA-2-7B linear layers, comparing FP32 dense (1 thread, dashed line) against FairyFuse ternary at 1/4/16/48 threads under both L3-warm (top) and DRAM-cold (bottom) conditions. The speedup annotations show compression ratios far exceeding the 16 × footprint reduction, confirming that the multiplication-free datapath contributes additional gains beyond bandwidth savings. additional): marginal for L3-warm data but meaningful at 16+ threads under DRAM-cold conditions. Fusion (1.39 –1.55 ×

over unfused): the approximate contribution of each sub-optimization is O1 (mask reuse) ∼15% , O2 (input reuse) ∼20% ,O3 (sign swap) ∼5% , O4 (register accumulators) ∼10% , with the remaining ∼5% from OpenMP barrier elimination. 

D Assembly Analysis: Multiplication-Free Verification 

We disassembled the FairyFuse kernel’s inner loop using 

objdump -d libternary_gemv.so and analyzed the instruc-tion mix. 

Key observations. (1) The inner loop contains ex-actly zero multiplication instructions ( vmulps , vmulss , or 

vfmadd231ps ). (2) The only multiplications in the entire ker-nel are eight scalar vmulss at the end of each row for per-channel scaling, 𝑂 (𝑛 ) versus the 𝑂 (𝑛𝑚 ) inner loop. (3) The dominant instructions are 16 masked add/sub operations per chunk, matching the theoretical analysis. For 𝑛 =𝑚 =4096 : inner-loop iterations = 4096 /16 = 256 

chunks per row; total masked add/sub = 256 ×16 ×4096 ×8 =

134 ,217 ,728 ; total scalar multiplies = 4096 ×8 = 32 ,768 ; multi-plication fraction = 0.024% .1 2 4 8 16 32 48 96 

Threads 

> 0
> 20
> 40
> 60
> 80
> 100
> Speedup vs 1t

(a) Thread scaling speedup        

> L3-warm
> DRAM-cold
> Ideal
> 124816 32 48 96

Threads 

> 0
> 20
> 40
> 60
> 80
> 100
> Efficiency (%)

(b) Parallel efficiency        

> L3-warm
> DRAM-cold
> 124816 32 48 96

Threads 

> 10 2
> 10 3
> Latency (  s)

(c) Absolute latency   

> L3-warm
> DRAM-cold
> N0
> 24t
> N0,1
> 48t
> N0,1
> 44t
> N2,3
> 48t
> N2,3
> 44t
> All
> 96t
> All
> 48t
> 0
> 5
> 10
> 15
> 20
> 25
> 30
> tok/s  18.8
> 29.3 28.8
> 30.5 29.2
> 25.5
> 20.1

(d) NUMA configuration impact Figure 8. Scalability and NUMA analysis for ternary GEMV ( 4096 ×4096 ). (a) Thread scaling speedup: near-linear under L3-warm up to 16 threads; DRAM-cold scaling is limited by bandwidth saturation. (b) Parallel efficiency: L3-warm maintains 

>80% up to 16 threads; DRAM-cold efficiency drops rapidly due to shared bandwidth. (c) Absolute latency on log scale: the L3/DRAM gap is 3–12 × depending on thread count. (d) NUMA configuration impact on end-to-end throughput: single-socket binding (NUMA 2,3 48t) achieves the best 30.5 tok/s; cross-socket access degrades performance. 

Table 10. Instruction count in the FairyFuse kernel’s inner loop (processing one 16-element chunk of the widely-linear GEMV). 

Category Instructions Count 

Ternary decode pext (BMI2) 8Masked add vaddps (masked) 8Masked sub vsubps (masked) 8Memory load vmovups /vmovaps 3Weight load mov (u32) 4

Multiply vmulps /vfmadd 0

Loop control add /cmp /jne 3

E Reproducibility and Cache Analysis 

Measurement conditions. Each seed controls: (1) ran-dom input vector initialization for GEMV benchmarks, 

Table 11. Per-seed measurements. CV = coefficient of varia-tion (std/mean × 100%).                         

> Metric Seed 42 Seed 123 Seed 2026 Mean Std CV
> GEMV latency ( 𝜇 s) 424 418 430 424.0 6.0 1.4% E2E tok/s 32.4 32.1 32.8 32.4 0.35 1.1% PPL (WikiText-2) 5.52 5.52 5.52 5.52 0.00 0.0%

(2) weight perturbation pattern for numerical stability test-ing, and (3) token generation sampling temperature (set to 0 for deterministic greedy decode). Perplexity is determinis-tic and shows zero variance. GEMV and end-to-end metrics exhibit small variance from OS scheduling, thermal effects, and memory controller arbitration. 

System stability. All measurements were taken with fixed CPU frequency (2.1 GHz, governor performance , no turbo boost), single-socket NUMA binding via numactl , no other user processes, and 60 s cool-down between runs. 1t 16t 48t       

> 10 2
> Latency (  s)
> (a) L3-warm optimization levels
> Baseline
> +ILP
> +Prefetch
> 1t 16t 48t
> 10 3
> Latency (  s)
> (b) DRAM-cold optimization levels
> Baseline
> +ILP
> +Prefetch
> 1t 16t 48t
> 0
> 250
> 500
> 750
> 1000
> 1250
> 1500
> Latency (  s)
> (c) Unfused vs Fused (L3-warm)
> 8x Unfused
> Fused
> 1t 16t 48t
> 0
> 5000
> 10000
> 15000
> 20000
> Latency (  s)
> 1.55x
> 1.38x
> 1.39x
> (d) Unfused vs Fused (DRAM-cold)
> 8x Unfused
> Fused

Figure 9. Kernel optimization analysis. (a)–(b) Effective DRAM and L3 bandwidth utilization by optimization level and thread count: ILP-unrolled and prefetch-enabled variants at 48 threads approach the practical DRAM ceiling of ∼180 GB/s, while L3-warm achieves ∼73 GB/s. (c)–(d) Unfused (8 independent GEMVs) vs. FairyFuse fused widely-linear comparison: fusion yields 1.02 –1.52 × speedup, with the benefit largest at 1 thread under DRAM-cold conditions (O4 eliminates intermediate buffers) and at 48 threads (O2 reduces memory traffic). 

F GPU Implementation Details 

F.1 CUDA Ternary Kernel 

We implement a custom CUDA kernel for ternary GEMV on the H200 to provide a fair comparison. The kernel uses the same 2-bit packed format and per-row scales as the CPU im-plementation. Each CUDA thread processes one output row and performs warp-level reduction via __shfl_down_sync .The custom CUDA ternary kernel achieves 3.2 ms per GEMV ( 4096 ×4096 ), compared with 24 .5 𝜇 s for cuBLAS FP16 GEMV, a 130 × slowdown. The cause is twofold: (1) CUDA lacks efficient equivalents to BMI2’s _pext_u32 ; the decode requires multiple shifts and masks. (2) GPU hardware is optimized for fused multiply-add, not conditional add/sub, leading to poor ALU utilization. 

F.2 cuBLAS FP16 Baseline 

cuBLAS FP16 GEMV achieves 24 .5 𝜇 s latency for 4096 ×4096 

on the H200, approaching the theoretical bandwidth limit. This confirms that the GPU is already operating near its memory bandwidth ceiling, leaving no room for compression to help. 

F.3 Roofline Model Construction 

The Roofline model [ 37 ] plots achievable performance against arithmetic intensity. 

• CPU : peak compute = 2.7 TFLOP/s (48 cores × 2.1 GHz ×

16 FP32 ops/cycle × 2 ports); peak BW = 200 GB/s. 

• GPU : peak compute = 134 TFLOP/s (FP16 tensor cores); peak BW = 4.8 TB/s. 

• Ridge point : CPU = 13.5 FLOP/byte; GPU = 27.9 FLOP/byte. FP32 dense GEMV (AI =0.25 FLOP/byte) sits deep in the memory-bound region on both platforms. Ternary fused GEMV (AI =8 OP/byte) exceeds the CPU ridge point but re-mains below the GPU ridge point, explaining the asymmetric benefit. 4t 16t 48t 

> 10 2
> Speedup vs FP32 1t

(a) L3 vs DRAM speedup ( 4096 × 4096 )  

> L3-warm
> DRAM-cold
> 4t 16t 48t
> 0
> 2
> 4
> 6
> 8
> L3/DRAM speedup ratio  8.6x
> 9.2x
> 4.4x

(b) Cache benefit ratio   

> 42 123 2026

Seed 

> 30.0
> 30.5
> 31.0
> 31.5
> 32.0
> 32.5
> 33.0
> 33.5
> 34.0
> tok/s

(c) E2E throughput (CV=1.26%)   

> Mean=32.43
> 42 123 2026

Seed 

> 370
> 380
> 390
> 400
> 410
> 420
> Latency (  s)

(d) GEMV DRAM-cold (CV=1.25%) 

> Mean=391.8

Figure 10. Cache effect and reproducibility analysis. (a) L3-warm vs. DRAM-cold speedup over FP32 at 4/16/48 threads: L3-warm consistently outperforms DRAM-cold by 3–5×. (b) L3/DRAM speedup ratio: the cache benefit ranges from 2.8× (48t, where threads share bandwidth) to 5× (16t, where DRAM latency is exposed). (c)–(d) Reproducibility across three seeds (42, 123, 2026): both E2E throughput (CV = 1.26% ) and GEMV latency (CV = 1.25% ) are well within the <5% criterion. 

Table 12. WikiText-2 perplexity comparison with additional baselines. 

Method Bits/Weight PPL Δ vs. FP16 

FP16 (HuggingFace) 16 5.47 —FairyFuse (ours) 2 5.52 +0.05 llama.cpp Q4_K_M 4.2 5.68 +0.21 llama.cpp Q2_K 2.5 7.82 +2.35 GPTQ-4bit 4 5.65 +0.18 GPTQ-2bit 2 12.4 +6.93 

G Quality Evaluation Details 

G.1 WikiText-2 Perplexity 

We evaluate perplexity on the WikiText-2 test set [ 24 ] ( ∼245 Ktokens) using the standard sliding-window protocol with stride equal to sequence length (2048 tokens). FairyFuse’s perplexity of 5.52 is remarkably close to FP16 and significantly better than any other method at comparable 

Table 13. Per-task downstream accuracy (%).                               

> Method ARC-E ARC-C HellaSwag PIQA WinoGrande Avg
> FP16 75.4 43.5 72.1 79.0 66.5 67.3 FairyFuse (ours) 74.2 41.8 71.6 78.3 64.2 66.0 Q4_K_M 73.8 41.2 70.8 78.0 61.7 65.1 Q2_K 62.1 33.4 58.2 72.4 56.8 56.6

bit-widths. Standard GPTQ 2-bit quantization degrades per-plexity to 12.4 ( +6.93 points), underscoring the effectiveness of Fairy2i’s complex-valued approach for extreme quantiza-tion. 

G.2 Downstream Task Details 

We evaluate on five zero-shot benchmarks via the LM Evalu-ation Harness [10]. FairyFuse shows consistent performance across tasks, with the largest drop on WinoGrande ( −2.3 points) and minimal degradation on HellaSwag ( −0.5 points). Q2_K suffers severe degradation on all tasks, particularly ARC-Challenge ( −10 .1Table 14. Memory footprint for LLaMA-2-7B.                      

> Component FP16 Ternary (2-bit) Compression
> Attention (Q/K/V/O) 4.0 GB 0.50 GB 8.0×
> MLP (gate/up/down) 8.6 GB 1.08 GB 8.0×
> Embedding + LM Head 0.5 GB 0.50 GB 1.0×
> RMSNorm + scales 0.04 GB 0.04 GB 1.0×
> Per-channel scales —0.12 GB —
> Total 13.1 GB 2.24 GB 5.8×

points) and HellaSwag ( −13 .9 points), indicating that stan-dard 2-bit quantization destroys reasoning capabilities that Fairy2i preserves through its complex-valued representation. 

H Weight Packing Format 

H.1 Binary File Layout 

Each linear layer is stored as: 

[Header: 16 bytes] n (uint32): output dimension m (uint32): input dimension scale_re (float32): real scale scale_im (float32): imag scale [Packed weights: n*ceil(m/16)*4 B each] U_re, U_im, W_re, W_im 

H.2 Packing Details 

For each group of 16 ternary values 𝑡 0, 𝑡 1, . . . , 𝑡 15 ∈ {− 1, 0, +1}:

packed =

> 15

∑︁ 

> 𝑘 =0

⊮[𝑡 𝑘 =+1] · 22𝑘 +1 + ⊮[𝑡 𝑘 =−1] · 22𝑘  (8) Bit 2𝑘 +1 is set for positive weights and bit 2𝑘 for negative weights. The _pext_u32 instruction with mask 0xAAAAAAAA 

extracts all positive indicators; mask 0x55555555 extracts all negative indicators. The resulting 16-bit masks are directly usable as AVX-512 __mmask16 operands. 

H.3 Memory Footprint 

The embedding and LM head remain in FP16 as they use standard dense operations. The per-channel scales (four FP32 values per row per linear layer) add a small constant overhead but enable significantly better quantization quality than per-tensor scaling. 

I Extended Roofline Analysis 

I.1 Arithmetic Intensity Derivation 

For a GEMV y = Ax with A ∈ R𝑛 ×𝑚 :

FP32 dense. Compute: 𝑛𝑚 FMA = 𝑛𝑚 FLOP. Data: 4𝑛𝑚 

bytes (weights) + 4𝑚 (input) + 4𝑛 (output) ≈ 4𝑛𝑚 bytes. AI 

= 0.25 FLOP/byte. 

Table 15. Roofline parameters for CPU and GPU platforms. 

Parameter CPU (Xeon 8558P) GPU (H200 NVL) 

Peak BW 200 GB/s 4,800 GB/s Peak compute 2,700 GFLOP/s 134,000 GFLOP/s Ridge point 13.5 FLOP/byte 27.9 FLOP/byte FP32 dense AI 0.25 0.25 Ternary (single) AI 4.0 4.0 Ternary (fused) AI 8.0 8.0 FP32 regime Memory-bound Memory-bound Ternary regime Near ridge Memory-bound 

Table 16. Per-layer timing breakdown (single-token decode, 48 threads).                                    

> Operation Time ( 𝜇 s) Fraction Mul-Free?
> RMSNorm (attention) 12 1.3% No Q/K/V projection (FairyFuse) 285 30.2% ✓
> Attention (softmax) 45 4.8% No O projection (FairyFuse) 95 10.1% ✓
> RMSNorm (MLP) 12 1.3% No Gate+Up projection (FairyFuse) 380 40.3% ✓
> SiLU activation 80.8% No Down projection (FairyFuse) 95 10.1% ✓
> Residual add 10 1.1% ✓
> Total per layer 942 100%
> Fused GEMV fraction 855 90.8% ✓

Ternary (single GEMV, packed 2-bit). Compute: 𝑛𝑚 

add/sub = 𝑛𝑚 OP. Data: 𝑛𝑚 /4 bytes (packed weights) + 4𝑚 

(input) + 4𝑛 (output) ≈ 𝑛𝑚 /4 bytes. AI = 4 OP/byte. 

Ternary (fused 8-GEMV).. Compute: 8𝑛𝑚 OP. Data: 4 ×

𝑛𝑚 /4 bytes (four weight matrices) + 3 × 4𝑚 (three input vectors, loaded once via O2) ≈ 𝑛𝑚 bytes. AI = 8 OP/byte, a 

32 × improvement over FP32 dense GEMV. On the CPU, the fused ternary AI of 8.0 approaches the ridge point of 13.5, transitioning from deeply memory-bound to near-compute-bound. On the GPU, AI =8.0 remains well below the ridge point of 27.9, so the kernel stays memory-bound and abundant HBM bandwidth leaves little headroom for compression to help. 

J End-to-End Inference Pipeline Details 

J.1 Per-Layer Timing Breakdown 

The fused GEMV accounts for 90.8% of per-layer computa-tion, all of which is multiplication-free. The remaining 9.2% (RMSNorm, attention, SiLU) uses standard FP32 operations but is 𝑂 (𝑑 ) versus the 𝑂 (𝑑 2) GEMV. Table 17. llama.cpp baseline results on Intel Xeon 8558P (48 threads, single-socket NUMA binding).                     

> Format Bits/W Size (GB) tok/s PPL
> FP16 16 13.5 8.3 5.47 Q4_K_M 4.2 4.1 26.2 5.68 Q2_K 2.5 2.8 20.1 7.82 FairyFuse (ours) 2.0 2.2 32.4 5.52

J.2 End-to-End Timing 

Per-layer: 942 𝜇 s × 32 = 30 .1 ms. Embedding lookup: 0.05 ms. Final RMSNorm + LM Head: 0.5 ms. Total: 30 .85 ms per token 

→ 32 .4 tok/s. 

J.3 Implementation Details 

The inference engine comprises ∼1500 lines of C++ (ker-nel + forward pass) and ∼500 lines of Python (weight load-ing, tokenization, sampling). The C++ code is compiled as libternary_gemv.so and invoked via Python ctypes 

with zero-copy NumPy array passing. Packed weights are loaded via mmap for lazy page-fault residency management. Total runtime memory: ∼3.5 GB (2.24 GB packed weights + 0.5 GB embeddings + 0.5 GB KV cache + 0.26 GB acti-vation buffers). OpenMP is configured with 48 threads, 

OMP_PROC_BIND=close and OMP_PLACES=cores .

K llama.cpp Baseline Configuration 

K.1 Build Configuration 

> git clone https://github.com/ggerganov/ llama.cpp cd llama.cpp && git checkout <commit-hash> cmake -B build -DGGML_AVX512=ON \ -DGGML_AVX512_VNNI=ON \ -DCMAKE_BUILD_TYPE=Release cmake --build build -j$(nproc)

K.2 Model Conversion and Quantization 

> python convert_hf_to_gguf.py <model> \ --outfile llama2-7b-f16.gguf \ --outtype f16 ./build/bin/llama-quantize \ llama2-7b-f16.gguf \ llama2-7b-q4km.gguf Q4_K_M ./build/bin/llama-quantize \ llama2-7b-f16.gguf \ llama2-7b-q2k.gguf Q2_K

K.3 Benchmark Command 

> numactl --cpunodebind=0 --membind=0 \ ./build/bin/llama-bench \ -m llama2-7b-q4km.gguf \ -t 48 -p 0 -n 128 -r 3

K.4 Baseline Results