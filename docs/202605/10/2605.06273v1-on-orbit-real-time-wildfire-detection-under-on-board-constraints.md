---
title: On-Orbit Real-Time Wildfire Detection Under On-Board Constraints
title_zh: 星载约束下的轨道实时林火检测
authors: "Matthias Rötzer, Veronika Pörtge, Martin Ickerott, Jayendra Praveen Kumar Chorapalli, Dimitri Scheftelowitsch, Max Bereczky, Dmitry Rashkovetsky, Sai Manoj Appalla, Julia Gottfriedsen"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06273v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 具有严格星载内存和延迟限制的在轨火灾检测系统
tldr: 本研究针对商业热红外卫星星座，开发了一套受限资源下的在轨实时野火监测系统。针对单波段中波红外图像中火点呈亚像素级且类别极度不平衡的挑战，提出利用轻量化密集掩码自编码器（DenseMAE）进行预训练。该系统在NVIDIA Jetson Xavier NX上实现了亚兆字节模型大小和极低延迟，显著提升了检测精度和预警速度，为全球野火实时监控提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的野火监测系统难以在资源受限的卫星硬件上实时处理单波段、非校准且类别极度不平衡的红外遥感数据。
method: 提出了一种基于密集掩码自编码器（DenseMAE）及其蒸馏变体的轻量化表示学习方法，用于在轨实时火点检测。
result: 最佳配置在模型大小小于1MB且推理延迟极低的情况下，实现了0.699的平均精度和0.744的火灾F1分数，优于监督学习基准。
conclusion: 该系统证明了自监督预训练在提升轻量化模型性能方面的有效性，成功实现了从卫星过境到火灾预警低于10分钟的端到端目标。
---

## 摘要
我们展示了一个部署在由九颗卫星组成的商业热红外星座上的轨道林火检测系统，该系统在严苛的联合约束下运行：模型占用空间小于 1 MB，在 NVIDIA Jetson Xavier NX 上使用 TensorRT FP16 推理的每批次延迟低于 150 毫秒，且端到端警报流水线的目标是从卫星过境到火灾事件通信的时间少于 10 分钟。该系统运行在地面采样距离为 200 米的未校准中波红外（MWIR）单波段图像上，在极端的类别不平衡下，火灾通常表现为亚像素或单像素的热异常——这些挑战是目前主导业务化火灾监测的上下文热阈值流水线（如 MODIS、VIIRS）所无法解决的。我们利用专有的九星 MWIR 数据集，针对这一领域进行了轻量级密集表示学习的实证研究。我们对比了密集掩码自编码（DenseMAE）及其混合 DenseMAE+EMA（指数移动平均）蒸馏变体，并通过线性探测和极端类别不平衡下的全分布像素级平均精度（AP）评估了表示性能。DenseMAE 预训练使得下游紧凑模型能够处于延迟-准确率帕累托前沿：在不进行剪枝或压缩的情况下，我们最快的自监督学习（SSL）预训练模型实现了 0.640 的测试 AP 和 0.69 的事件级 Fire-F1，每批次延迟为 65.34 毫秒，引擎大小仅为 0.52 MB。在相似约束下，最佳配置在小于 1 MB 的情况下达到了 0.699 AP 和 0.744 Fire-F1，优于有监督基准模型（0.650 AP）。

## Abstract
We present a deployed system for on-orbit wildfire detection aboard a nine-satellite commercial thermal infrared constellation, operating under demanding joint constraints: sub-megabyte model footprint, sub-150 ms per-batch TensorRT FP16 inference on an NVIDIA Jetson Xavier NX, and an end-to-end alert pipeline targeting under 10 minutes from satellite overpass to fire event communication. The system operates on uncalibrated mid-wave infrared (MWIR) single-band imagery at 200 m ground sampling distance, where fires frequently appear as sub-pixel or single-pixel thermal anomalies under extreme class imbalance -- challenges not addressed by the contextual thermal-thresholding pipelines (MODIS, VIIRS) that currently dominate operational fire monitoring. We present an empirical study of lightweight dense representation learning for this regime using a proprietary nine-satellite MWIR dataset. We compare dense masked autoencoding (DenseMAE) and a hybrid DenseMAE+EMA (exponential moving average) distillation variant, and evaluate representations via linear probing and full-distribution pixel-level average precision (AP) under extreme class imbalance. DenseMAE pretraining enables compact downstream models on the latency-accuracy Pareto frontier: our fastest SSL-pretrained model achieves 0.640 test AP and 0.69 event-level Fire-F1 with 65.34 ms latency per batch and a 0.52 MB engine, without pruning or compression. The best configuration reaches 0.699 AP and 0.744 Fire-F1 below 1 MB, outperforming a supervised baseline (0.650 AP) under comparable constraints.

---

## 论文详细总结（自动生成）

这篇论文《On-Orbit Real-Time Wildfire Detection Under On-Board Constraints》（星载约束下的轨道实时林火检测）详细介绍了 OroraTech 公司为其商业热红外卫星星座开发的在轨实时野火监测系统。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：野火频发严重威胁生态与人类安全。现有的卫星监测系统存在权衡：地球静止轨道（GEO）卫星时间分辨率高但空间分辨率差（2-3km），低地轨道（LEO）卫星空间分辨率高但过境频率低。
*   **核心问题**：为了实现从卫星过境到发出警报低于 10 分钟的目标，必须在卫星本地（在轨）进行实时处理。然而，星载硬件（如 NVIDIA Jetson Xavier NX）资源极度受限，且中波红外（MWIR）数据存在噪声大、未校准、火点呈亚像素级以及类别极度不平衡（火点像素仅占 0.018%）等挑战。

### 2. 提出方法论
*   **核心思想**：利用自监督学习（SSL）在未校准的单波段 MWIR 图像上进行密集表示学习，以提升轻量化模型在下游火灾分割任务中的性能。
*   **关键技术细节**：
    *   **DenseMAE**：一种专为密集预测设计的全卷积掩码自编码器。与标准 ViT 架构的 MAE 不同，它采用分阶段卷积编码器，通过一次下采样提高效率，并使用空洞卷积（Dilated Conv）扩大感受野。
    *   **预训练目标**：在图像空间进行随机块掩码，通过轻量化解码器重建被遮盖的像素（L1 损失），仅针对有效像素计算。
    *   **混合蒸馏（可选）**：引入 EMA（指数移动平均）教师模型进行一致性蒸馏，以测试其在噪声环境下的鲁棒性。
    *   **下游任务头**：设计了超轻量级的 **TRT-Head**（针对 TensorRT 优化）和 **DW-Res Head**（深度可分离残差块），并结合**高分辨率细化头（HR Refinement）**，将编码器浅层特征重新注入，以捕捉单像素级的火点。

### 3. 实验设计
*   **数据集**：使用来自 OroraTech OTC-P1 任务中 9 颗卫星的专有 MWIR 数据集，包含 839 个标注场景（2023-2025 年），划分为 589/126/124（训练/验证/测试）。
*   **Benchmark（基准方法）**：
    1.  **有监督基准**：基于 EfficientNet 后干的 U-Net++ 架构。
    2.  **外部验证**：与 NASA 的 VIIRS（375m 分辨率）主动火灾产品进行跨传感器对比。
*   **对比维度**：对比了不同嵌入维度（32, 64, 128）、不同掩码比例、冻结编码器 vs 全微调、以及不同轻量化头部的性能。

### 4. 资源与算力
*   **推理算力**：目标平台为 **NVIDIA Jetson Xavier NX**（10W 模式，384 核 Volta GPU）。
*   **训练算力**：文中未明确说明训练所用的具体 GPU 型号和数量，但提到使用了 AdamW 优化器和混合精度训练。
*   **部署规格**：所有模型均编译为 TensorRT FP16 引擎进行延迟测试。

### 5. 实验数量与充分性
*   **实验规模**：进行了 5 折交叉验证。针对 SSL 预训练进行了多组消融实验（如表 3 所示，涵盖了维度、掩码比例、块大小等参数）。
*   **充分性评价**：实验设计较为充分，不仅评估了像素级的平均精度（AP），还引入了事件级的 Fire-F1 分数，并绘制了延迟-准确率的帕累托前沿（Pareto Frontier），客观地展示了模型在实际部署中的权衡。

### 6. 主要结论与发现
*   **DenseMAE 的有效性**：DenseMAE 预训练显著优于从头训练的有监督模型。全微调（Full fine-tuning）效果优于冻结编码器的线性探测。
*   **性能表现**：最佳配置（嵌入维度 64 + DW-Res 头 + HR 细化）在模型大小 **< 1 MB** 的情况下，达到了 **0.699 AP** 和 **0.744 Fire-F1**。
*   **部署优势**：最快模型在 Jetson Xavier NX 上的批处理延迟仅为 **65.34 ms**，引擎大小仅 **0.52 MB**，完全满足在轨实时处理需求。
*   **外部一致性**：与 VIIRS 产品的对比显示了良好的跨传感器一致性，证明了模型在未标注数据上的泛化能力。

### 7. 优点
*   **极度轻量化**：在不使用剪枝或压缩技术的情况下，实现了亚兆字节（Sub-MB）级别的模型，非常适合星载存储。
*   **针对性设计**：DenseMAE 放弃了昂贵的 Transformer 结构，改用卷积和空洞卷积，平衡了感受野与计算开销。
*   **实战导向**：直接处理未校准的原始数据（DN 值），省去了复杂的辐射校正步骤，极大降低了端到端延迟。

### 8. 不足与局限
*   **误报风险**：在处理未校准单波段数据时，太阳反光（Sunglint）和传感器伪影仍可能导致误报。
*   **缺乏时序信息**：目前系统仅处理单次过境的静态图像，未利用多时序上下文，这限制了对火灾动态演变的理解。
*   **光谱局限性**：仅依赖单波段 MWIR，若能融合长波红外（LWIR）或其他物理特征（如观测几何），性能可能进一步提升，但会增加计算负担。

（完）
