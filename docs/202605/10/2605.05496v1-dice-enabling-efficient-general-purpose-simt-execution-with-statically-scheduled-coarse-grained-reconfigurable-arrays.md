---
title: "DICE: Enabling Efficient General-Purpose SIMT Execution with Statically Scheduled Coarse-Grained Reconfigurable Arrays"
title_zh: DICE：利用静态调度的粗粒度可重构架构实现高效的通用 SIMT 执行
authors: "Jiayi Wang, Ang Da Lu, Zhichen Zeng, Ang Li"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05496v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 静态调度CGRA的软硬件协同设计
tldr: DICE 针对 GPU 在 SIMT 编程模型下寄存器访问功耗高的问题，提出用静态调度的粗粒度可重构阵列（CGRA）替换传统的 SIMD 后端。通过将线程流水线化地调度到 CGRA 织物上，实现处理单元间的数据直接传递，并引入 p-graph 编译技术处理动态性。实验表明，DICE 在保持性能的同时，显著降低了寄存器访问量和功耗，大幅提升了能效比。
selection_source: fresh_fetch
motivation: 传统 GPU 的 SIMD 执行模式因频繁的寄存器堆访问和复杂的控制逻辑导致了巨大的能量开销。
method: 提出 DICE 架构，利用静态调度的 CGRA 织物实现空间流水线执行，并通过 p-graph 编译技术、双缓冲配置内存和时间内存合并单元优化动态操作。
result: "实验显示 DICE 平均减少了 68% 的寄存器访问，能效比提升 1.77-1.90 倍，功耗降低约 42%-46%，且性能与主流 GPU 相当。"
conclusion: DICE 证明了空间流水线执行可以在不牺牲通用 SIMT 编程模型性能的前提下，显著提高处理器的能效。
---

## 摘要
虽然 GPU 通过单指令多线程（SIMT）编程模型在海量并行计算中占据主导地位，但其底层的单指令多数据（SIMD）执行由于频繁的寄存器堆（RF）访问和复杂的控制逻辑，导致了巨大的能量开销。我们提出了 DICE，这是一种新型架构，通过将 SIMD 后端替换为极低开销、静态调度的粗粒度可重构架构（CGRA）来解决这些效率低下的问题。与以同步步进（lockstep）方式执行线程束（warps）的 SIMD 单元不同，DICE 以流水线方式将活跃线程分派到 CGRA 阵列上，数据在处理单元（PE）之间直接流动，从而减少了中间值的寄存器堆访问。为了在保持静态调度的同时处理具有运行时动态性的操作（如变延迟内存加载和数据相关的控制流），DICE 通过将动态依赖边划分到不同的 CGRA 配置中，将程序编译为“p-graphs”。DICE 进一步引入了几项关键优化：双缓冲配置存储器以隐藏重构延迟、编译时 p-graph 展开以提高资源利用率，以及时间内存合并单元（TMCU）以合并来自连续流水线线程的内存请求。在 Accel-sim 中对 Rodinia 基准测试集的评估表明，DICE 平均减少了 68% 的寄存器堆访问。在计算和内存资源相当的情况下，与模拟的 NVIDIA Turing 流式多处理器（SM）相比，DICE 的 CGRA 处理器（CP）实现了 1.77-1.90 倍（几何平均值）的动态能效提升和 42.0%-45.9% 的平均功耗降低，而完整的 DICE 系统实现了与模拟的 Turing GPU 基准相当的性能。DICE 证明了空间流水线执行可以在不牺牲性能的情况下实现显著的节能。

## Abstract
While GPUs dominate massively parallel computing through the single-instruction, multiple-thread (SIMT) programming model, their underlying single-instruction, multiple-data (SIMD) execution incurs substantial energy overhead from frequent register file (RF) accesses and complex control logic. We present DICE, a novel architecture that addresses these inefficiencies by replacing the SIMD backend with minimal-overhead, statically scheduled coarse-grained reconfigurable arrays (CGRAs). Unlike SIMD units that execute warps of threads in lockstep, DICE dispatches active threads in a pipelined manner onto the CGRA fabric, where data flow directly between processing elements (PEs), reducing RF accesses for intermediate values. To handle operations with runtime dynamism, such as variable-latency memory loads and data-dependent control flow, while preserving static scheduling, DICE compiles programs into "p-graphs" by partitioning dynamic dependence edges across separate CGRA configurations. DICE further introduces several key optimizations: double-buffered configuration memory to hide reconfiguration latency, compile-time p-graph unrolling to enhance resource utilization, and a temporal memory coalescing unit (TMCU) to merge memory requests from consecutive, pipelined threads. Evaluations on Rodinia benchmarks in Accel-sim demonstrate that DICE reduces register file accesses by 68% on average. With equivalent computation and memory resources, DICE's CGRA Processors (CPs) achieve a geometric mean of 1.77-1.90x dynamic energy efficiency and 42.0%-45.9% average power reduction compared to the modeled NVIDIA Turing Streaming Multiprocessors (SMs), while the full DICE system achieves performance comparable to the modeled Turing GPU baselines. DICE demonstrates that spatial pipeline execution can deliver substantial energy savings without sacrificing performance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DICE**（Dataflow Intelligent Compute Engine）的新型架构，旨在通过将 GPGPU 的 SIMD 后端替换为静态调度的粗粒度可重构阵列（CGRA），在保持通用 SIMT 编程模型的同时，大幅提升能效。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：传统 GPGPU 虽然在并行计算上表现优异，但其底层的 SIMD 执行模式存在严重的能效瓶颈。主要体现在：
    1.  **频繁的寄存器堆（RF）访问**：中间计算结果必须通过中心化的 RF 交换，导致巨大的动态功耗。
    2.  **复杂的控制逻辑**：为了管理线程束（Warp）的同步执行、指令取指和解码，控制逻辑消耗了高达 30%-40% 的动态能量。
*   **整体含义**：DICE 试图证明，通过“空间流水线”执行模式（数据在处理单元 PE 间直接传递）替代“同步步进”模式，可以在不牺牲通用编程能力的前提下，解决 GPU 的功耗难题。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：保留 SIMT 编程模型（如 CUDA），但将执行后端改为静态调度的空间 CGRA。线程不再在单条指令上同步，而是像流水线一样逐个流过 CGRA 织物。
*   **关键技术细节**：
    *   **p-graph 划分**：为了在静态调度的 CGRA 上处理动态性（如变延迟内存访问、分支），编译器将程序划分为多个 p-graph。其约束包括：内部无分支、无“加载-使用”依赖、无同步屏障。
    *   **四阶段控制流水线**：包括 CTA 调度（CS）、取指/解码/重构（FDR）、分派/执行（DE）和退休（RE）。
    *   **时间内存合并单元（TMCU）**：由于线程是流水线进入的，无法像传统 GPU 那样在同一时刻合并内存请求。TMCU 通过缓存和计时器机制，在时间维度上合并连续线程的内存请求，维持内存带宽效率。
    *   **线程展开（Unrolling）**：针对小型 p-graph 导致 CGRA 利用率低的问题，支持在同一周期分派多个线程。
    *   **双缓冲配置内存**：在执行当前任务时预加载下一个任务的配置，隐藏重构延迟。

### 3. 实验设计
*   **数据集/场景**：选用 **Rodinia 基准测试集**，涵盖了神经网络（NN）、广度优先搜索（BFS）、反向传播（BPNN）、流聚类（SC）、高斯消元（GE）、热点图（HS）和路径查找（PF）等多种典型并行负载。
*   **Benchmark 与对比方法**：
    *   **基准硬件**：NVIDIA Turing 架构（RTX 2060 Super, RTX 5000, RTX 6000）。
    *   **对比配置**：DICE-naïve（基础版）、DICE-naïve+unroll（仅开启展开）、DICE-naïve+TMCU（仅开启内存合并）以及完整版 DICE。
*   **模拟工具**：使用扩展的 **Accel-sim** 进行周期精确的性能模拟。

### 4. 资源与算力
*   **算力说明**：论文未涉及模型训练，而是架构仿真。
*   **仿真环境**：
    *   **性能模拟**：Accel-sim。
    *   **功耗建模**：AccelWattch 结合 Cadence Joules。
    *   **硬件实现**：使用 **FreePDK45 (45nm)** 工艺进行 RTL 综合，并等效缩放至 **12nm FinFET** 工艺以与 Turing GPU 进行公平对比。
    *   **存储建模**：使用 CACTI 建模 SRAM 和缓存的面积与时延。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 个核心内核的详细评估。
*   **实验维度**：
    1.  **消融实验**：验证了线程展开和 TMCU 对性能的贡献。
    2.  **扩展性实验**：包括 Scale-up（增加单个核心的 PE 数量）和 Scale-out（增加核心总数）。
    3.  **跨代对比**：额外对比了 Ampere 架构（RTX 3070）。
*   **充分性评价**：实验设计非常全面，不仅考虑了性能（Speedup），还深入分析了寄存器访问次数、周期分解、面积开销和动态功耗，论证过程客观且公平。

### 6. 主要结论与发现
*   **寄存器访问减少**：DICE 平均减少了 **68%** 的寄存器堆访问。
*   **能效提升**：相比 Turing SM，DICE 实现了 **1.77x - 1.90x** 的动态能效提升。
*   **功耗降低**：平均动态功耗降低了 **42.0% - 45.9%**。
*   **性能持平**：在资源对等的情况下，DICE 的性能与主流 GPU 相当（平均加速比 1.16x），证明了空间执行的竞争力。

### 7. 优点
*   **软硬协同创新**：通过 p-graph 编译器策略巧妙地解决了静态 CGRA 难以处理动态内存延迟的顽疾。
*   **显著的节能效果**：针对 GPU 最耗电的 RF 和控制逻辑进行了精准打击。
*   **兼容性好**：保留了 SIMT 编程模型，意味着现有的 CUDA/PTX 代码经过编译器转换即可运行。
*   **TMCU 设计**：解决了流水线执行模式下内存合并的难题，这是将 CGRA 用于类 GPU 负载的关键。

### 8. 不足与局限
*   **共享内存瓶颈**：对于频繁访问共享内存且存在银行冲突（Bank Conflict）的内核，p-graph 会变得很小，导致利用率下降。
*   **编译器依赖**：性能高度依赖于编译器对 p-graph 的划分和映射能力，手动优化仍有空间。
*   **控制流开销**：虽然处理了分支，但在高度分叉的程序中，谓词执行（Predicated Execution）可能会导致部分 PE 资源浪费。
*   **面积开销**：虽然定性分析显示面积增加约 10%，但 CGRA 的互连开关（Crossbar）在更大规模时可能会带来显著的面积压力。

（完）
