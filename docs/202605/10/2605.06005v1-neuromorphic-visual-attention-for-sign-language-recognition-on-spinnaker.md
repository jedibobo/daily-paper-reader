---
title: Neuromorphic visual attention for Sign-language recognition on SpiNNaker
title_zh: 基于 SpiNNaker 的手语识别类脑视觉注意力机制
authors: "Sarka Liskova, Olha Vedmedenko, Mazdak Fatahi, Matej Hoffmann, P. Michael Furlong, Giulia D Angelo"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06005v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于实时部署的能效神经形态架构
tldr: 本研究针对手语识别在实时部署中面临的高延迟和高功耗挑战，提出了一种基于SpiNNaker神经形态平台的端到端架构。该系统集成了脉冲视觉注意力机制用于提取感兴趣区域，并结合紧凑型脉冲神经网络（SNN）进行美国手语字母识别。实验表明，该方案在保持竞争力的识别准确率的同时，实现了极低的功耗（0.565 mW）和延迟（3 ms），证明了神经形态视觉注意力在边缘计算中的应用潜力。
selection_source: fresh_fetch
motivation: 传统手语识别方法的高延迟和高功耗限制了其在实时边缘设备上的部署。
method: 提出一种集成脉冲视觉注意力机制和紧凑型脉冲神经网络的端到端神经形态架构，并部署于SpiNNaker硬件。
result: "系统在模拟中达到92.27%的准确率，硬件部署准确率为83.1%，且功耗仅为0.565 mW，延迟低至3 ms。"
conclusion: 该研究证明了基于任务相关的视觉注意力机制的神经形态系统在低功耗、低延迟边缘部署中的优越性。
---

## 摘要
近年来，手语识别在分类准确率方面取得了显著进展；然而，大多数现有方法的延迟和功耗要求限制了它们在实时部署中的适用性。类脑传感与处理提供了一种基于稀疏、事件驱动计算的替代范式，支持低延迟和高能效的感知。在这项工作中，我们介绍了一种用于美式手语（ASL）指拼识别的端到端类脑架构，该架构集成了一个用于在线感兴趣区域提取的脉冲视觉注意力机制，以及一个部署在 SpiNNaker 类脑平台上的紧凑型脉冲神经网络。我们在两个数据集上对所提出的系统进行了基准测试：合成生成的事件化 Sign Language MNIST 数据集和原生录制的 ASL-DVS 数据集，同时对手语识别及相关工作进行了全面概述。这项工作在仿真中表现出极具竞争力的性能（92.27%），在类脑硬件部署上也取得了相当的性能（83.1%），同时在所有基准测试方法中实现了最高的能效（0.565 mW）和低延迟（3 ms）。尽管设计紧凑，该系统仍证明了任务相关的视觉注意力应用在边缘部署中的适用性。

## Abstract
Sign-language recognition has achieved substantial gains in classification accuracy in recent years; however, the latency and power requirements of most existing methods limit their suitability for real-time deployment. Neuromorphic sensing and processing offer an alternative paradigm based on sparse, event-driven computation that supports low-latency and energy-efficient perception. In this work, we introduce an end-to-end neuromorphic architecture for American Sign Language (ASL) fingerspelling recognition that integrates a spiking visual attention mechanism for online region-of-interest extraction with a compact spiking neural network deployed on the SpiNNaker neuromorphic platform. We benchmark the proposed system against two datasets: a synthetically generated event-based version of the Sign Language MNIST dataset and a natively recorded ASL-DVS dataset, whilst providing a comprehensive overview of Sign-language recognition and related work. This work yields competitive performance in simulation (92.27%) and comparable performance on neuromorphic hardware deployment (83.1%), while achieving the most energy-efficient architecture (0.565 mW) and low latency (3 ms) across all benchmarked approaches. Despite its compact design, the system demonstrates the suitability of task-dependent visual attention applications for edge deployment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种专为实时、低功耗边缘设备设计的类脑（Neuromorphic）手语识别系统。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的手语识别（SLR）系统主要依赖深度人工神经网络（ANN）和帧相机，虽然准确率高，但存在**高延迟**和**高功耗**的问题，难以满足实时交互（延迟需低于100-200ms）和边缘设备（如机器人、可穿戴设备）的部署需求。
*   **研究背景**：类脑计算和事件相机（DVS）通过异步、稀疏的事件流处理，提供了低延迟和高能效的替代方案。然而，现有研究大多关注动态手势，且缺乏将视觉注意力机制与识别任务在类脑硬件上进行端到端集成的方案。

### 2. 方法论
论文提出了一种集成视觉注意力机制的端到端脉冲神经网络（SNN）架构：
*   **脉冲视觉注意力模型 (sVA)**：
    *   **核心思想**：模拟大脑皮层的边缘所有权细胞（Border Ownership Cells），通过感知分组识别显著区域。
    *   **技术细节**：使用 **von Mises 滤波器**（公式涉及半径 $R_0$、方向 $\theta$ 和贝塞尔函数 $I_0$）对事件流进行池化，提取感兴趣区域（ROI），从而过滤背景噪声并减少后续计算量。
*   **脉冲识别模型 (sR)**：
    *   **架构**：一个紧凑的 3 层前馈 SNN。
        *   **L1 (卷积层)**：4 通道，5×5 核，256 个神经元，用于空间特征提取。
        *   **L2 (隐藏层)**：512 个 LIF 神经元。
        *   **L3 (输出层)**：24 个神经元，对应 ASL 字母表（排除动态字母 J 和 Z）。
    *   **学习机制**：使用 snnTorch 框架，通过代理梯度反向传播（Surrogate Gradient Backpropagation）进行离线训练。

### 3. 实验设计
*   **数据集**：
    1.  **Sign Language MNIST (SL-MNIST)**：将静态灰度图通过随机行走像素偏移模拟成事件流。
    2.  **ASL-DVS**：原生录制的真实事件相机手语数据集，包含 5 名受试者，超过 10 万个样本。
*   **Benchmark（基准对比）**：
    *   对比了部署在 **Intel Loihi**、**TrueNorth**、**SpiNNaker2** 等平台上的多种 SNN 和 ANN 模型。
    *   对比指标包括：准确率（Accuracy）、推理延迟（Latency）和动态功耗（Power）。

### 4. 资源与算力
*   **硬件平台**：
    *   **SpiNNaker (SpiNN-3)**：部署识别模型（sR），包含 4 颗芯片，每颗芯片有 18 个 ARM968 核心。
    *   **MacBook M4 Pro**：用于 sVA 模型的运行（基于 Metal Performance Shaders 加速）以及 SNN 的仿真训练。
*   **算力说明**：文中未详细列出训练总时长，但指出了训练参数（如 AdamW 优化器、学习率 $3 \times 10^{-3}$、130-310 个 Epoch）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从合成数据到真实原生 DVS 数据的跨度。
    *   进行了**仿真（Simulation）**与**硬件部署（Hardware Deployment）**的对比实验。
    *   提供了逐类（Per-class）准确率分析。
*   **充分性评价**：实验设计较为充分，通过跨数据集验证了模型的泛化能力，并客观展示了硬件部署后的性能下降（Simulation-to-Hardware Gap），体现了科研的严谨性。

### 6. 主要结论与发现
*   **极低功耗**：在 ASL-DVS 数据集上，动态功耗仅为 **0.565 mW**，是目前已知手语识别系统中能效最高的。
*   **超低延迟**：推理延迟仅为 **3 ms**，远优于 Loihi 平台上的同类模型（12.64 ms）。
*   **准确率**：仿真中达到 **92.27%**（ASL-DVS），硬件部署后为 **83.1%**。虽然硬件准确率略有下降，但仍具有竞争力。
*   **注意力机制的有效性**：sVA 能够可靠地定位手部区域，避免了简单中心裁剪（Center-crop）导致的误分类。

### 7. 优点
*   **端到端集成**：首次将脉冲视觉注意力与识别任务结合并部署在类脑硬件上。
*   **极致轻量化**：模型极其紧凑（仅 3000 多个神经元），非常适合资源受限的边缘设备。
*   **开源贡献**：提供了事件化 SL-MNIST 数据集和架构代码，便于社区复现。

### 8. 不足与局限
*   **硬件性能缺口**：由于 SpiNNaker 使用定点数表示权重，而仿真使用 32 位浮点数，导致硬件部署后的准确率下降了约 9%。
*   **任务范围限制**：目前仅针对静态手势（指拼），未涵盖动态手语识别（如涉及 J 和 Z 字母）。
*   **异构运行**：虽然 sVA 模型在理论上兼容 SpiNNaker，但本实验中 sVA 仍运行在主机 CPU/GPU 上，未实现完全的单芯片闭环。

（完）
