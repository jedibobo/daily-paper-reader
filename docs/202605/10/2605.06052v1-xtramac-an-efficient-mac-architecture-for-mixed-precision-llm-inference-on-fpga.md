---
title: "XtraMAC: An Efficient MAC Architecture for Mixed-Precision LLM Inference on FPGA"
title_zh: XtraMAC：一种用于 FPGA 上混合精度 LLM 推理的高效 MAC 架构
authors: "Feng Yu, Hongshi Tan, Yao Chen, Weng-Fai Wong, Bingsheng He"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06052v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对FPGA上混合精度LLM推理的软硬件协同设计
tldr: 针对大语言模型（LLM）混合精度量化推理的需求，本文提出了 XtraMAC，一种高效的 FPGA 乘累加（MAC）架构。它通过将不同数据类型分解为共享的整数尾数乘法，实现了整数、浮点及混合精度的统一处理。XtraMAC 支持动态操作数打包和高效 DSP 资源共享，显著提升了计算密度和能效，为混合精度 LLM 推理提供了高性能的硬件支持。
selection_source: fresh_fetch
motivation: 现有的 FPGA MAC 方案在处理混合精度 LLM 推理时存在数据类型固定、资源共享效率低以及 DSP 利用率不足的问题。
method: 提出一种将所有支持的 MAC 格式分解为共享整数尾数乘法，并结合轻量级符号和指数处理的统一微架构，支持动态操作数打包。
result: "在 AMD Xilinx U55c 上，XtraMAC 实现了 1.4-2.0 倍的计算密度提升，减少了 27-51% 的资源消耗，并提升了 1.9 倍的能效。"
conclusion: XtraMAC 通过高效的资源共享和灵活的架构设计，显著优化了 FPGA 上的混合精度 LLM 推理性能。
---

## 摘要
混合精度量化在大语言模型（LLM）中的广泛采用，产生了对能够跨混合数据类型高效执行乘累加（MAC）操作并在运行时切换数据类型的硬件的需求。现有的基于 FPGA 的 MAC 方案由于固定数据类型设计的局限性、低效的空间或时间资源共享以及对混合精度执行的支持不足而表现不佳。这些限制共同导致了 DSP 资源的利用率低下，限制了可实现的并行度和吞吐量。在这项工作中，我们提出了 XtraMAC，这是一种新颖的 MAC 架构，它在单一的数据类型自适应微架构中统一了整数、浮点和混合精度运算。XtraMAC 将所有支持的 MAC 格式分解为共享的整数尾数乘积，并配合轻量级的符号和指数处理，从而实现了动态操作数打包和高效的 DSP 资源共享，且在所有数据类型下均具有恒定的延迟和为 1 的启动间隔。在 AMD Xilinx U55c FPGA 上的评估结果显示，XtraMAC 实现了 1.4-2.0 倍的计算密度提升，将单个操作的 LUT、FF 和 DSP 消耗降低了 27-51%，并在代表性的混合精度 LLM 工作负载上提供了高达 1.9 倍的能效提升和 1.2 倍的加速。XtraMAC 的实现已在 https://github.com/Xtra-Computing/XtraMAC 开源。

## Abstract
The widespread adoption of mixed-precision quantization in large language models (LLMs) has created demand for hardware that can efficiently perform multiply-accumulate (MAC) operations across mixed datatypes and switch datatypes at runtime. Existing FPGA-based MAC solutions fall short due to limitations in fixed-datatype design, inefficient spatial or temporal resource sharing, and poor support for mixed-precision execution. These limitations collectively lead to under-utilization of DSP resources, limiting achievable parallelism and throughput. In this work, we present XtraMAC, a novel MAC architecture that unifies integer, floating-point, and mixed-precision operations within a single, datatype-adaptive microarchitecture. XtraMAC decomposes all supported MAC formats into a shared integer mantissa product with lightweight sign and exponent handling, enabling dynamic operand packing and efficient DSP resource sharing with constant latency and initiation interval of one across all datatypes. Evaluated on an AMD Xilinx U55c FPGA, XtraMAC achieves 1.4-2.0x higher compute density, reduces per-operation LUT, FF, and DSP consumption by 27-51%, and delivers up to 1.9x greater energy efficiency and 1.2x speedup on representative mixed-precision LLM workloads. The implementation of XtraMAC is open-sourced at https://github.com/Xtra-Computing/XtraMAC.

---

## 论文详细总结（自动生成）

这是一份关于论文《XtraMAC: An Efficient MAC Architecture for Mixed-Precision LLM Inference on FPGA》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）广泛采用混合精度量化（如INT4、FP8、BF16等）来降低内存占用和能耗。
*   **核心问题**：现有的FPGA乘累加（MAC）架构在处理混合精度时存在严重缺陷：
    *   **精度固定**：低精度操作数向上转型（Upcasting）会导致DSP资源位宽浪费。
    *   **共享低效**：空间复制或时间共享方案导致DSP利用率低下（如TATAA在处理BF16时利用率仅为8.9%）。
    *   **切换困难**：难以在运行时无缝、高效地在不同数据类型间切换。
*   **研究动机**：设计一种能够统一处理整数、浮点及混合精度，且支持运行时单周期切换的高效MAC架构。

### 2. 方法论
*   **核心思想**：将所有MAC操作（INT、FP、混合精度）分解为**“共享的整数尾数乘法”**，并配合轻量级的符号和指数处理逻辑。
*   **关键技术细节**：
    *   **统一公式化**：证明了FP×FP和INT×FP的乘法核心都是整数尾数乘积，仅在映射（Mapping）和后计算（Post-compute）阶段处理指数和符号。
    *   **DSP Packing（操作数打包）**：利用FPGA DSP48E2的27×18位乘法器，通过位偏移（Shift-and-Mask）技术，在单个DSP内并行执行多个低精度通道（如4路FP4或2路BF16）。
    *   **解耦累加路径**：由于浮点加法需要昂贵的桶形移位器（Shifter），而整数加法不需要，XtraMAC将两者路径分离，避免整数运算浪费移位资源。
    *   **四级流水线架构**：
        1.  **Stage 1**：操作数解析与位映射（根据数据类型打包）。
        2.  **Stage 2**：DSP乘法与通道提取（后处理）。
        3.  **Stage 3**：解耦的整数/浮点累加。
        4.  **Stage 4**：输出选择与组装。
    *   **运行时切换**：通过随流水线同步传递的“数据类型信号”，实现每个时钟周期切换一种计算模式，且延迟恒定。

### 3. 实验设计
*   **硬件平台**：AMD Xilinx Alveo U55c FPGA（配备HBM2）。
*   **对比基准（Benchmark）**：
    *   **AMD Xilinx官方Floating-Point Operator IP**（代表工业标准）。
    *   **TATAA (2025)**：最新的可重构混合精度Transformer加速架构。
    *   **NVIDIA H100 GPU**：用于GEMV（矩阵-向量乘法）性能对比。
*   **测试场景**：
    *   单算子资源分析（覆盖INT8, FP4, FP8, BF16, FP16等数十种组合）。
    *   系统级GEMV内核测试。
    *   端到端LLM推理模拟（涉及Qwen-3-8B, Llama-3.1-8B, GPT-oss-20B等模型）。

### 4. 资源与算力
*   **FPGA资源**：详细记录了在U55c上的LUT、FF和DSP消耗。例如，在1920个XtraMAC实例的配置下，频率可维持在250-270 MHz。
*   **GPU对比**：使用了NVIDIA H100 PCIe版进行对比，监测其在执行GEMV时的功耗（nvidia-smi）和延迟。
*   **训练/推理时长**：文中未涉及模型训练，主要关注推理阶段的算子延迟（ms级）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了**数十组**不同数据类型组合的资源消耗实验。
    *   针对**5种**主流开源LLM量化检查点进行了端到端推理模拟。
    *   进行了**可扩展性实验**，验证了随着支持数据类型增加，频率和资源的波动情况。
*   **充分性评价**：实验设计非常充分，不仅有微观的算子对比，还有宏观的系统级模拟，且对比对象包括了最新的学术成果（TATAA）和工业界最强的硬件（H100），评估维度客观、公平。

### 6. 主要结论与发现
*   **计算密度**：相比官方IP，XtraMAC实现了**1.4–2.0倍**的计算密度提升。
*   **资源优化**：单个操作的LUT、FF和DSP消耗降低了**27–51%**。
*   **系统性能**：在GEMV任务中，相比H100 GPU，实现了**1.2倍**的加速和**1.9倍**的能效提升。
*   **端到端表现**：在Batch Size为32的LLM推理中，相比传统方案可减少**1.5–1.8倍**的延迟。

### 7. 优点
*   **极高的DSP利用率**：通过位级打包技术，彻底解决了FPGA在处理低精度浮点时的资源浪费问题。
*   **真正的动态性**：支持在同一个硬件单元上逐周期切换数据类型，完美契合现代LLM复杂的量化策略。
*   **架构通用性**：不仅支持标准浮点，还支持INT×FP这种极具挑战性的混合精度模式。
*   **开源贡献**：代码已开源，具有很高的实用价值。

### 8. 不足与局限
*   **频率折损**：由于增加了复杂的映射和后处理逻辑，其最高频率比官方简单IP低约**22%**（虽然吞吐量提升弥补了这一损失）。
*   **硬件平台依赖**：该设计高度针对Xilinx DSP48E2原语优化，若要移植到Intel或其它厂商的FPGA，需要重新设计位打包策略。
*   **累加位宽限制**：为了节省资源，累加器的位宽和精度处理（如舍入模式）可能在极端数值敏感场景下存在微小偏差（尽管文中称与A100/H100位一致）。

（完）
