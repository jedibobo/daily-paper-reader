---
title: "EdgeLPR: On the Deep Neural Network trade-off between Precision and Performance in LiDAR Place Recognition"
title_zh: EdgeLPR：论激光雷达位置识别中深度神经网络精度与性能之间的权衡
authors: "Pierpaolo Serio, Hetian Wang, Zixiang Wei, Vincenzo Infantino, Lorenzo Gentilini, Lorenzo Pollini, Valentina Donzella"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02275v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对资源受限边缘AI的高效LiDAR位置识别
tldr: 本研究针对自动驾驶中激光雷达场景识别在边缘设备部署难的问题，提出了一种基于鸟瞰图（BEV）表示的轻量级方案。通过对多种代表性深度学习架构进行基准测试，并对比FP32、FP16及INT8量化下的表现，揭示了精度与性能间的权衡。研究发现FP16能在不损失精度的情况下提升效率，而INT8的表现受架构影响较大，为未来边缘AI的量化研究提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在解决深度学习模型在资源受限的边缘平台上进行激光雷达场景识别时，计算开销与识别精度难以兼顾的挑战。
method: 采用鸟瞰图表示法将激光雷达数据转化为轻量级图像网络输入，并利用统一的描述符方案对不同架构在FP32、FP16和INT8量化下进行基准测试。
result: 实验显示FP16量化在保持与FP32相当精度的同时显著降低了成本，而INT8量化带来的性能下降则具有明显的架构依赖性。
conclusion: 该研究为边缘AI部署中针对特定用例的神经网络量化策略提供了坚实的实验基础和指导方向。
---

## 摘要
位置识别对于长期自主导航至关重要，能够实现回环检测和一致性建图。尽管深度学习提高了性能，但在资源受限的平台上部署此类模型仍然具有挑战性。本研究通过利用鸟瞰图（Bird's Eye View）表示来实现轻量级基于图像的网络，探索了针对边缘人工智能（EdgeAI）的高效激光雷达位置识别。我们使用基于全局池化和线性投影的统一描述符方案，对不带聚合头的代表性架构进行了基准测试，并评估了在 FP32、FP16 和 INT8 量化下的性能。实验揭示了准确性、鲁棒性和效率之间的权衡：FP16 在较低成本下达到了与 FP32 相当的水平，而 INT8 则引入了依赖于架构的性能退化。总体而言，本文结果为未来针对边缘部署的“用例感知”神经网络量化研究奠定了坚实基础。

## Abstract
Place recognition is essential for long-term autonomous navigation, enabling loop closure and consistent mapping. Although deep learning has improved performance, deploying such models on resource-constrained platforms remains challenging. This work explores efficient LiDAR-based place recognition for EdgeAI by leveraging Bird's Eye View representations to enable lightweight image-based networks. We benchmark representative architectures without aggregation heads using a unified descriptor scheme based on global pooling and linear projection, and evaluate performance under FP32, FP16, and INT8 quantization. Experiments reveal trade-offs between accuracy, robustness, and efficiency: FP16 matches FP32 with lower cost, while INT8 introduces architecture-dependent degradation. Overall, the presented results are a strong basis for future research on 'use-case'-aware quantisation of Neural Networks for Edge deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《EdgeLPR: On the Deep Neural Network trade-off between Precision and Performance in LiDAR Place Recognition》的结构化总结：

### 1. 研究动机与背景
*   **核心问题**：在机器人自主导航中，激光雷达（LiDAR）位置识别（回环检测）对消除累计误差至关重要。然而，现有的深度学习模型通常计算量巨大且内存占用高，难以在资源受限的边缘计算平台（EdgeAI）上实时运行。
*   **研究动机**：旨在探索如何在保证识别精度的前提下，通过轻量级架构和模型量化技术，优化LiDAR位置识别模型在边缘设备上的部署性能。

### 2. 方法论
*   **核心思想**：将复杂的3D点云投影为2D鸟瞰图（BEV），从而能够利用成熟且轻量级的图像分类卷积神经网络（CNN）作为特征提取器。
*   **关键技术细节**：
    *   **表示转换**：将LiDAR点云投影为256×256的BEV图像，利用反射强度信息作为像素值。
    *   **统一架构**：采用“骨干网络（Backbone）+ 全局池化 + 线性投影”的简洁结构。去除了复杂的聚合层（如NetVLAD），以减少计算开销。
    *   **量化策略**：评估了三种精度模式：**FP32**（全精度）、**FP16**（半精度）和 **INT8**（静态后量化，PTQ）。INT8量化通过最小化均方误差（MSE）来确定缩放因子和零点。
    *   **损失函数**：使用三元组损失（Triplet Loss）进行端到端微调，使空间邻近的帧在描述符空间中距离更近。

### 3. 实验设计
*   **数据集/场景**：
    *   **KITTI数据集**：用于评估短期视角变化下的鲁棒性（序列00训练，02、05、06测试）。
    *   **NCLT数据集**：用于评估长期季节性和环境外观变化下的鲁棒性（跨越数月的多次采集）。
*   **对比方法（Benchmark）**：
    *   选取了三种代表性的轻量级CNN架构：**MobileNetV3**、**ShuffleNetV2** 和 **ResNet18**。
    *   对比了不同精度（FP32/16/INT8）对检索指标（Recall@k, F1, AUC, MRR）的影响。

### 4. 资源与算力
*   **算力来源**：论文提到计算资源由比萨大学（University of Pisa）的 computing@unipi 提供。
*   **硬件细节**：文中**未明确说明**具体的GPU型号、数量或训练总时长。但在推理测试部分提到，FP32/FP16在GPU上评估，而INT8在CPU后端执行以模拟边缘部署环境。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了3种架构、3种量化精度、2个大型公开数据集（包含多个子序列）。
    *   每组实验均运行了3个随机种子，并报告了均值和标准差。
*   **充分性评价**：实验设计较为客观、公平。通过统一描述符维度和池化策略，排除了额外组件的干扰，专注于研究骨干网络架构与量化之间的相互作用。

### 6. 主要结论与发现
*   **FP16是“免费的午餐”**：在所有架构中，FP16的精度几乎与FP32完全一致，但能显著降低内存占用并提升兼容硬件上的速度。
*   **INT8量化具有架构敏感性**：
    *   **MobileNetV3**：在FP32下表现最好，但在INT8量化后性能下降最严重。
    *   **ResNet18**：对量化最不敏感，表现出极强的鲁棒性。
    *   **ShuffleNetV2**：在内存占用和精度之间达到了最佳平衡，尤其在INT8下依然稳定。
*   **环境影响**：在长期的NCLT数据集上，所有模型性能均有下降，说明季节性变化对轻量级模型仍是巨大挑战。

### 7. 优点与亮点
*   **实用性强**：直接针对边缘部署痛点，提供了关于量化如何影响位置识别精度的实证数据。
*   **BEV范式简化**：证明了无需复杂的3D卷积或Transformer，简单的BEV + 轻量级2D CNN也能在位置识别任务中取得不错的效果。
*   **硬件导向**：不仅关注算法精度，还分析了推理延迟和吞吐量，对工程实践有指导意义。

### 8. 不足与局限
*   **量化方法单一**：仅采用了静态后量化（PTQ），未探讨量化感知训练（QAT），后者可能进一步提升INT8的性能。
*   **硬件利用率矛盾**：实验发现ShuffleNetV2虽然参数少，但在某些CPU环境下的INT8推理效率反而不如MobileNetV3，说明架构设计与特定硬件指令集的优化仍存在脱节。
*   **BEV局限性**：BEV投影虽然高效，但会丢失部分垂直高度信息，在某些极端地形下可能不如原始点云方法。

（完）
