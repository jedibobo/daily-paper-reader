---
title: Energy-Efficient Implementation of Spiking Recurrent Cells on FPGA
title_zh: 脉冲循环单元在 FPGA 上的能效实现
authors: "Pascal Harmeling, Florent De Geeter, Guillaume Drion"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10679v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对硬件友好型脉冲神经网络的FPGA加速器
tldr: 本研究针对脉冲神经网络（SNN）在FPGA上实现时生物合理性与硬件成本之间的矛盾，提出了一种基于脉冲循环单元（SRC）的高效加速器。通过数学简化去除昂贵的tanh和exp算子，并采用定点数缩放与分段线性近似技术，在保持丰富神经元动力学的同时显著降低了硬件开销。在Artix-7 FPGA上的实验证明，该方案在MNIST数据集上实现了高精度与低能耗的平衡，为低功耗边缘计算提供了高效的硬件实现路径。
selection_source: fresh_fetch
motivation: 旨在解决生物学特性丰富的神经元模型在FPGA上实现成本过高，而简单模型如LIF又难以捕捉复杂神经动力学特性的问题。
method: 通过数学简化移除复杂的一元算子，利用分段线性近似和定点数运算替代浮点运算，并使用VHDL在FPGA上实现SRC加速器。
result: "在Artix-7上实现了96.31%的初始精度，通过权重降至4位和缩短脉冲序列，能在仅0.45 mJ/数字的能耗下保持92.89%的精度。"
conclusion: 基于SRC的SNN能够在保留复杂神经元动力学的同时，通过针对性的硬件优化实现极具竞争力的能效比和分类准确率。
---

## 摘要
脉冲神经网络（SNN）在脉冲活动稀疏且神经元模型硬件友好时，相比传统人工神经网络（ANN）可以显著降低功耗。然而，生物学保真度高的模型在 FPGA 上的实现成本通常过高，而极简模型（如 IR/LIF）则牺牲了部分神经元动力学特性。在这项工作中，我们提出了一种使用脉冲循环单元（SRC）神经元的 SNN FPGA 加速器，在生物合理性与硬件成本之间取得了平衡。我们提出了一系列数学简化方法，通过缩放和分段定义近似，消除了高成本的一元算子（tanh、exp）并避免了浮点运算。整个网络采用 VHDL 实现，并使用源自 MNIST 数据集的脉冲轨迹进行了验证。离线计算的权重矩阵直接存储在 LUT 寄存器中，无需任何调整，这证明了 SRC 单元的鲁棒性。实验在主频为 100 MHz 的 Artix-7 XC7A200T 上进行。参考实现在 220 图像脉冲轨迹下达到了 96.31% 的准确率，处理每个数字的时间为 1.7424 ms。随后，我们通过缩短脉冲轨迹长度并将突触权重降采样至 4 位，研究了准确率与能量之间的权衡，在每个数字 0.55 mJ（55 幅图像，5 位权重）时达到 93.32% 的准确率，在 0.45 mJ（44 幅图像，4 位权重）时达到 92.89% 的准确率。这些结果表明，基于 SRC 的 SNN 能够提供具有竞争力的性能并降低能耗，同时保留比标准 LIF/IR 模型更丰富的神经元动力学。

## Abstract
Spiking Neural Networks (SNNs) can reduce energy consumption compared to conventional Artificial Neural Networks (ANNs) when spiking activity is sparse and the neuron model is hardware-friendly. However, biologically faithful models are often too costly to implement on FPGAs, whereas very simple models (e.g., IR/LIF) sacrifice part of the neuronal dynamics. In this work, we present an FPGA accelerator for an SNN using Spiking Recurrent Cell (SRC) neurons, providing a trade-off between biological plausibility and hardware cost. We propose a set of mathematical simplifications that remove costly unary operators (\textit{tanh}, \textit{exp}) and avoid floating-point arithmetic through scaling and piecewise-defined approximations. The complete network is implemented in VHDL and validated using spiking traces derived from the MNIST dataset. The weight matrices computed off-line are stored directly in LUT-registers without any adaptation. This demonstrates the robustness of SRC cells. Experiments were conducted on an Artix-7 XC7A200T clocked at 100 MHz. The reference implementation achieves 96.31\% accuracy with a 220-image spiking trace and a processing time of 1.7424 ms per digit. We then investigate accuracy/energy trade-offs by reducing the spiking trace length and quantizing synaptic weights down to 4 bits, achieving 93.32\% accuracy at 0.55 mJ per digit (55 images, 5-bit weights) and 92.89\% at 0.45 mJ (44 images, 4-bit weights). These results show that SRC-based SNNs can deliver competitive performance with reduced energy consumption, while preserving richer neuronal dynamics than standard LIF/IR models.

---

## 论文详细总结（自动生成）

这篇论文详细介绍了在 FPGA 上实现脉冲循环单元（Spiking Recurrent Cells, SRC）的高效硬件加速器方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在 FPGA 硬件上平衡脉冲神经网络（SNN）的**生物合理性**（模型复杂度）与**硬件效率**（计算成本）。
*   **研究动机**：
    *   传统的 ANN 功耗极高，SNN 凭借稀疏脉冲特性成为低功耗替代方案。
    *   现有的 SNN 硬件实现多采用极简的 LIF（Leaky Integrate-and-Fire）模型，虽然节省资源但牺牲了神经元动力学的丰富性（如适应性、非线性等）。
    *   复杂的生物模型（如 Hodgkin-Huxley）在 FPGA 上实现成本过高，难以大规模部署。
    *   **目标**：验证 SRC 模型在 FPGA 上的可行性，证明其在保持复杂动力学的同时，能达到与 LIF 相当的能效比。

### 2. 方法论
*   **核心思想**：对原始 SRC 方程进行硬件友好的数学简化，将复杂的连续动力学转化为高效的定点数逻辑。
*   **关键技术细节**：
    *   **消除浮点运算**：通过将所有变量和参数缩放 1000 倍，将浮点运算转换为 11 位或 16 位定点整数运算。
    *   **算子近似**：
        *   使用**分段线性函数 ($S_{tanh}$)** 替代昂贵的 `tanh` 激活函数。
        *   将包含 `exp` 的复杂函数 $z_s[t]$ 简化为基于阈值的**开关规则**（根据神经元状态在“快”和“慢”两种动力学模式间切换）。
    *   **移位替代乘法**：调整参数（如将 $r_s$ 从 -7 改为 -8），利用位移操作（Shift）替代乘法器，显著降低逻辑资源消耗。
*   **架构设计**：采用分层模块化架构（Binder-NetWorkLevel），利用 VHDL 的 `GENERATE` 语句实现神经元的完全并行处理。

### 3. 实验设计
*   **数据集**：使用 **MNIST** 手写数字识别数据集。
*   **数据预处理**：将 MNIST 图像通过伯努利过程转换为“脉冲轨迹”（Spiking Traces），每张图像对应 220 帧二进制脉冲序列。
*   **Benchmark（基准）**：
    *   对比了多种基于 FPGA 的 LIF 模型实现（如 Carpegna 等人和 Han 等人的工作）。
    *   对比了原始 Julia 浮点模型与简化后的 Pseudo-VHDL（PsV）定点模型。
*   **对比维度**：准确率（Accuracy）、处理时间（Latency）、能耗（Energy per digit）、硬件资源占用（LUT/DSP/BRAM）。

### 4. 资源与算力
*   **硬件平台**：AMD Xilinx **Artix-7 XC7A200T** FPGA（Alinx AX7A200B 开发板）。
*   **时钟频率**：100 MHz。
*   **算力/功耗说明**：
    *   **训练阶段**：在离线环境（Julia 语言）中完成，论文未详细说明训练时长，重点在于权重的直接迁移。
    *   **推理阶段**：总功耗约为 **1.28 W**（含显示单元）。
    *   **资源占用**：单个 SRC 神经元在 9 位权重下占用约 830 个 LUT 和 1 个 DSP。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 10,000 张 MNIST 测试集图像进行了完整验证。
    *   **消融/权衡实验**：
        1.  **权重精细度**：测试了从 9 位到 2 位的权重转换对准确率的影响。
        2.  **脉冲轨迹长度**：测试了从 220 帧缩减至 20 帧对能效和精度的影响。
        3.  **参数敏感性**：调节 $z_{hyp\_s}$ 参数以观察脉冲稀疏度与准确率的关系。
*   **评价**：实验设计较为充分，涵盖了从算法仿真到硬件实测的完整闭环，且通过对比不同量化水平，客观展示了性能边界。

### 6. 主要结论与发现
*   **高鲁棒性**：离线训练的权重无需任何微调即可直接导入 FPGA 简化模型，准确率仅从 96.48% 轻微下降至 96.31%。
*   **能效优势**：在 44 帧脉冲轨迹和 4 位权重下，系统能以 **0.45 mJ/数字** 的极低能耗维持 **92.89%** 的准确率。
*   **性能竞争力**：SRC 模型在 FPGA 上的处理速度（约 1.74 ms/数字）和能效表现与主流的 LIF 模型实现处于同一水平，但具备更强的动力学表达能力。

### 7. 优点
*   **模型鲁棒性**：证明了 SRC 模型对量化和数学简化的极高容忍度，简化过程不依赖复杂的重训练。
*   **硬件优化深入**：通过分段近似和位移操作，巧妙避开了 FPGA 处理非线性函数的短板。
*   **模块化设计**：提出的 Binder 架构具有高度可扩展性，易于适配不同的网络规模或多 FPGA 系统。

### 8. 不足与局限
*   **任务复杂度较低**：实验仅限于 MNIST 数据集，对于更复杂的视觉任务（如 CIFAR-10 或 ImageNet）或时序任务，SRC 的优势和硬件压力尚待验证。
*   **同步处理限制**：目前采用每层同步处理方案，虽然并行度高，但在脉冲极度稀疏时，其能效可能不如纯异步（Event-driven）架构。
*   **资源消耗**：相比于最简单的 IF/LIF 模型，SRC 神经元由于内部状态更多，单个神经元的 LUT 占用量仍然相对较高。

（完）
