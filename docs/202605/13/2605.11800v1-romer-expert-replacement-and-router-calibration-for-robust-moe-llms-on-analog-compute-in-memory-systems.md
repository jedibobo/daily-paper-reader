---
title: "ROMER: Expert Replacement and Router Calibration for Robust MoE LLMs on Analog Compute-in-Memory Systems"
title_zh: ROMER：面向模拟存算一体系统上鲁棒 MoE 大语言模型的专家替换与路由器校准
authors: "Wenyong Zhou, Yuannuo Feng, Yizhe Chen, Taiqiang Wu, Wendong Xu, Wenbo Qi, Zhengwu Liu, Wang Kang, Ngai Wong"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11800v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 存算一体系统上MoE的软硬件协同设计
tldr: 针对模拟存算一体（CIM）硬件噪声对混合专家模型（MoE）LLM 性能的负面影响，本文提出了 ROMER 框架。该框架通过替换低频专家以恢复负载均衡，并利用百分位归一化校准路由逻辑，有效解决了硬件噪声导致的路由失效问题。实验证明，ROMER 在多种 MoE 架构上显著降低了困惑度，提升了模型在噪声环境下的鲁棒性。
selection_source: fresh_fetch
motivation: 模拟存算一体系统的硬件噪声会破坏 MoE 模型的专家负载均衡并导致路由决策次优，严重影响模型性能。
method: 提出 ROMER 框架，通过高频专家替换低频专家来恢复负载均衡，并采用基于百分位的归一化方法校准路由器的 Logit 输出。
result: "在真实芯片噪声条件下，ROMER 使 DeepSeek-MoE、Qwen-MoE 和 OLMoE 的困惑度分别降低了高达 58.6%、58.8% 和 59.8%。"
conclusion: ROMER 是一种通用的后训练校准方案，能显著增强不同 MoE 架构在模拟存算一体硬件上的鲁棒性。
---

## 摘要
采用混合专家（MoE）架构的大语言模型（LLM）通过为每个 token 稀疏地激活一部分专家来实现卓越的可扩展性，但其频繁的专家切换产生了内存带宽瓶颈，而存算一体（CIM）架构非常适合缓解这一问题。然而，模拟 CIM 系统受固有的硬件缺陷影响，会导致存储权重受到扰动，而这种缺陷在噪声 CIM 环境下对基于 MoE 的 LLM 的负面影响仍未得到探索。在这项工作中，我们首次对基于真实芯片测量校准的噪声模型下的 MoE 大语言模型进行了系统性研究，揭示了硬件噪声会严重破坏专家的负载均衡，并使得在无噪声环境下训练的路由决策始终处于次优状态。基于这些发现，我们提出了 ROMER，这是一个训练后校准框架，它（1）用高频专家替换激活不足的专家以恢复负载均衡，以及（2）通过基于百分位数的归一化重新校准路由器 logit，以稳定噪声下的路由。在多个基准测试上的广泛实验表明，在真实芯片噪声条件下，ROMER 分别使 DeepSeek-MoE、Qwen-MoE 和 OLMoE 的困惑度降低了高达 58.6%、58.8% 和 59.8%，证明了其在不同 MoE 架构上的有效性和通用性。

## Abstract
Large language models (LLMs) with mixture-of-experts (MoE) architectures achieve remarkable scalability by sparsely activating a subset of experts per token, yet their frequent expert switching creates memory bandwidth bottlenecks that compute-in-memory (CIM) architectures are well-suited to mitigate. However, analog CIM systems suffer from inherent hardware imperfections that perturb stored weights, and its negative impact on MoE-based LLMs in noisy CIM environments remains unexplored. In this work, we present the first systematic investigation of MoE-based LLMs under noise model calibrated with real chip measurements, revealing that hardware noise critically disrupts expert load balance and renders clean-trained routing decisions consistently suboptimal. Based on these findings, we propose ROMER, a post-training calibration framework that (1) replaces underactivated experts with high-frequency ones to restore load balance, and (2) recalibrates router logits via percentile-based normalization to stabilize routing under noise. Extensive experiments across multiple benchmarks demonstrate that ROMER achieves up to 58.6\%, 58.8\%, and 59.8\% reduction in perplexity under real-chip noise conditions for DeepSeek-MoE, Qwen-MoE, and OLMoE, respectively, establishing its effectiveness and generalizability across diverse MoE architectures.

---

## 论文详细总结（自动生成）

### 论文总结：ROMER —— 面向模拟存算一体系统的鲁棒 MoE 大语言模型框架

#### 1. 核心问题与研究动机
*   **背景**：混合专家模型（MoE）通过稀疏激活显著提升了 LLM 的扩展性，但其巨大的参数量导致了严重的内存带宽瓶颈。模拟存算一体（CIM）架构通过在存储单元内直接计算，消除了数据搬运，是部署 MoE 的理想方案。
*   **核心问题**：模拟 CIM 硬件存在固有的物理噪声（如编程噪声、ADC/DAC 噪声），这些噪声会扰动权重。研究发现，这种噪声对 MoE 模型有独特且致命的影响：它会破坏专家的负载均衡，导致路由决策失效（出现局部“热点”专家和大量“休眠”专家），使模型性能剧烈下降。
*   **目标**：在不进行高成本重训练的前提下，提升 MoE 模型在噪声 CIM 硬件上的鲁棒性。

#### 2. 方法论（ROMER 框架）
ROMER 是一个**无需训练（Training-free）**的后处理校准框架，包含两个核心组件：
*   **专家替换（Expert Replacement）**：
    *   **核心思想**：利用硬件冗余降低关键专家的噪声敏感度。
    *   **技术细节**：通过校准集统计各专家的激活频率，识别出最常激活的 Top-k 专家和最少激活的 Bottom-k 专家。将 Bottom-k 专家的存储位置替换为 Top-k 专家的权重副本。
    *   **效果**：推理时，对两个相同专家的输出取平均，根据统计学原理，这能将关键专家的噪声方差降低一倍。
*   **基于百分位数的路由器校准（Percentile-Based Router Calibration）**：
    *   **核心思想**：纠正噪声导致的路由 Logit 分布扩张。
    *   **技术细节**：硬件噪声会使路由器输出的 Logit 差异变大，导致决策过自信。ROMER 使用四分位距（IQR）作为鲁棒统计量，通过一个强度参数 $\lambda$ 将 Logit 分布向中位数压缩。
    *   **效果**：通过解析式地减小 Logit 传播范围，恢复稳定的专家分配逻辑。

#### 3. 实验设计
*   **模型对象**：涵盖了多种主流 MoE 架构，包括 DeepSeek-MoE-16B、Qwen3-30B-A3B、Qwen1.5-MoE-A2.7B 和 OLMoE-1B-7B。
*   **数据集/场景**：
    *   **语言建模**：WikiText-103, WikiText-2, LAMBADA（评估困惑度 PPL）。
    *   **常识推理**：PIQA, ARC-Easy, ARC-Challenge（评估准确率 Accuracy）。
*   **Benchmark 与对比方法**：
    *   对比了 Clean（无噪声）、Vanilla（无缓解措施）、Bit-slicing（位切分）、Average（权重平均）、Weight-remapping（权重重映射）以及 k-b Calibration（缩放偏置校准）。
    *   噪声模型基于真实模拟 CIM 芯片测量数据校准，模拟了不同温度（25°C 和 80°C）下的噪声强度。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。
*   **原因**：由于 ROMER 是一种**训练后校准（Post-training Calibration）**方法，不需要对大规模参数进行梯度下降，其计算开销主要集中在离线统计专家激活频率和 Logit 分布上，对算力需求极低。
*   **硬件模拟**：使用了专门的 CIM 硬件模拟器进行推理延迟和功耗的评估。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个模型、6 个基准数据集、2 种典型温度（噪声水平）下进行了全面测试。
*   **消融实验**：针对专家替换数量 $n$ 和校准强度 $\lambda$ 进行了详细的参数敏感性分析。
*   **客观性**：实验对比了多种现有的硬件鲁棒性提升技术，并提供了硬件开销（延迟、功耗）的量化分析，证明了该方法在提升性能的同时，硬件代价在可接受范围内（平均延迟增加约 7.8%）。

#### 6. 主要结论与发现
*   **路由失效是主因**：硬件噪声通过干扰路由 Logit，使 MoE 退化为性能极差的稠密模型。
*   **显著提升**：在真实芯片噪声环境下，ROMER 将不同 MoE 模型的困惑度降低了 58.6% 至 59.8%，几乎恢复到了接近无噪声的水平。
*   **通用性强**：该方法在不同规模（从 1B 到 30B）和不同路由策略的 MoE 模型上均表现出一致的有效性。

#### 7. 优点
*   **针对性强**：首次系统研究并解决了 MoE 在 CIM 硬件上特有的路由失衡问题。
*   **部署友好**：无需重新训练，无需标注数据，非常适合大模型的快速硬件迁移。
*   **软硬协同**：通过简单的权重复制和 Logit 变换，以极小的硬件开销换取了巨大的鲁棒性增益。

#### 8. 不足与局限
*   **硬件开销**：专家替换会引入额外的内存访问和数据移动，导致约 6%-12% 的推理延迟和功耗增加。
*   **超参数依赖**：校准强度 $\lambda$ 和替换专家数量 $n$ 可能需要针对不同硬件平台进行微调。
*   **容量权衡**：虽然替换的是低频专家，但在极端复杂的任务下，这种替换理论上可能会损失模型的部分长尾知识表达能力（尽管实验中未观察到明显下降）。

（完）
