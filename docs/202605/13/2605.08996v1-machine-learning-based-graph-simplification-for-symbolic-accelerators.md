---
title: Machine Learning-Based Graph Simplification for Symbolic Accelerators
title_zh: 基于机器学习的符号加速器图简化方法
authors: "Tiffany Yu, Rye Stahle-Smith, Darssan Eswaramoorthi, Rasha Karakchi"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08996v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 硬件加速器的自动剪枝以减少资源使用
tldr: 针对基因组学、网络安全和人工智能等领域中基于图的符号加速器存在的内存占用高和冗余结构效率低的问题，本文提出了AutoSlim框架。该框架利用机器学习（随机森林分类器）分析历史执行特征，识别并剪枝低影响的节点和边。在NAPOLY+架构上的实验表明，该方法在保证功能等效的同时，显著降低了FPGA资源消耗并提升了吞吐量与能效。
selection_source: fresh_fetch
motivation: 现有的符号数据处理加速器常因冗余的图结构导致内存使用过度和执行效率低下。
method: 提出AutoSlim框架，利用随机森林分类器基于历史执行数据对自动机图进行数据驱动的剪枝。
result: "在NAPOLY+架构上，该方法减少了高达40%的FPGA资源使用，并显著提升了吞吐量和功率效率。"
conclusion: 机器学习驱动的图简化技术能有效优化硬件加速器性能，并为硬件优化和安全性提供了新方向。
---

## 摘要
基于图的加速器已广泛应用于基因组学、网络安全和人工智能等符号数据处理应用中。然而，这些系统通常面临内存占用过高以及由冗余图结构导致的效率低下问题。我们提出了 AutoSlim，这是一个基于机器学习的框架，利用数据驱动的方法为硬件加速器剪枝自动机图。AutoSlim 利用从先前图执行中提取的特征和随机森林分类器，识别并移除影响较小的节点和边。当应用于非确定性有限自动机（NFA）覆盖架构（NAPOLY+）时，AutoSlim 可将 FPGA 资源使用量减少高达 40%，并相应提高吞吐量和能效。该框架包含一个验证步骤，以确保剪枝后的功能等效性，并为硬件优化和安全性提供了极具前景的研究方向。

## Abstract
Graph-based accelerators have been widely adopted in symbolic data processing applications such as genomics, cybersecurity, and artificial intelligence. However, these systems often suffer from excessive memory usage and inefficiencies stemming from redundant graph structures. We present AutoSlim, a machine learning-based framework that leverages data-driven methods to prune automata graphs for hardware accelerators. Using features extracted from prior graph executions and a Random Forest classifier, AutoSlim identifies and removes low-impact nodes and edges. When applied to a Non-deterministic Finite Automata overlay architecture (NAPOLY+), AutoSlim reduces FPGA resource usage by up to 40%, with corresponding improvements in throughput and power efficiency. The framework includes a verification step to ensure functional equivalence after pruning and suggests promising directions for both hardware optimization and security.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AutoSlim** 的框架，旨在通过机器学习技术优化用于符号数据处理的硬件加速器（如 FPGA）。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：符号数据处理（如基因组测序、网络安全日志分析）通常使用有限自动机（NFA）模型。虽然 FPGA 等硬件加速器能提供高并行性，但随着自动机图规模的增大，系统面临**内存占用过高**、**布线拥塞**以及**冗余计算**（如不可达状态或重复结构）等瓶颈，严重限制了其扩展性和效率。
*   **研究动机**：传统的图简化方法（如状态合并或启发式规则）往往忽略了实际负载的概率特性，且可能增加硬件复杂度。作者希望引入一种数据驱动的、能感知硬件特性的剪枝方法，在保持功能等效的同时显著降低硬件资源消耗。

### 2. 方法论
AutoSlim 的核心思想是在硬件部署前的预处理阶段，利用机器学习识别并移除低影响的节点和边。
*   **数据集生成**：扩展了 ANMLZoo 格式，为自动机转换（Transitions）嵌入评分指标（如频率、成本）。
*   **特征提取**：从自动机图中提取结构和行为特征，包括边权重（Score）、节点度数、路径中心性等。
*   **机器学习模型**：采用**随机森林（Random Forest）分类器**。通过监督学习，模型根据特征预测每个转换组件的“效用值”。
*   **剪枝流程**：
    1.  解析 XML/CSV 格式的自动机图。
    2.  利用训练好的模型对转换进行分类，识别冗余或低贡献的边/节点。
    3.  执行剪枝并进行验证，确保所有关键的接受路径（Accepting Paths）被保留，维持功能等效。
*   **硬件集成**：使用 Vivado HLS 重新实现了 NAPOLY+ 加速器架构，使其能直接接收 AutoSlim 优化后的配置向量。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：节点规模从 1K 到 64K 不等，用于测试扩展性。
    *   **生物信息学数据集**：基于基因组数据库搜索的真实基准测试。
*   **Benchmark**：以原始的 **NAPOLY+** 覆盖架构（未剪枝版本）作为基准。
*   **对比维度**：对比了剪枝前后的执行时间、转换总数、节点平均扇出、FPGA 资源利用率（LUT、寄存器、URAM）、延迟和吞吐量。

### 4. 资源与算力
*   **硬件平台**：目标设备为 Xilinx Zynq UltraScale+ **ZCU104** (XCZU7EV)。
*   **开发工具**：Vitis HLS 2023.2 和 Vivado 2023.2。
*   **算力说明**：文中详细记录了 FPGA 的时钟频率（目标 500MHz，实际约 597MHz）和资源消耗。但对于**训练机器学习模型**所使用的具体算力（如 CPU/GPU 型号及训练时长），文中**未明确说明**，仅提到使用了随机森林算法。

### 5. 实验数量与充分性
*   **实验规模**：针对不同节点规模（1K, 4K, 8K, 16K, 32K, 64K）进行了多组实验，每种规模选取了 10 个代表性文件进行测试。
*   **消融/敏感性分析**：专门针对 8K 节点数据集进行了**扇出（Fanout）影响分析**，探讨了连接密度与资源消耗的关系。
*   **充分性评价**：实验涵盖了从算法层面的图简化率到硬件层面的物理实现指标，数据较为全面。通过对比“估计值”与“实际值”，验证了模型预测的准确性，实验设计较为客观。

### 6. 主要结论与发现
*   **资源大幅节省**：在 64K 节点的大规模图中，LUT 使用量从 47,000 降至不足 7,000，寄存器从 30K 降至 2K，**FPGA 资源占用减少了约 40%**，URAM 消耗降低了 50%。
*   **性能提升**：剪枝后的图简化了布线，使系统**吞吐量提升了 18%**，并显著降低了功耗。
*   **扩展性强**：AutoSlim 的剪枝效率具有规模不变性，对于超大规模图，其处理速度和简化效果依然稳定。
*   **扇出是关键**：实验发现小规模数据集资源占用高往往是因为初始扇出过大，AutoSlim 能有效识别并修剪这些高密度的冗余连接。

### 7. 优点
*   **软硬结合**：不仅在算法层面做图优化，还通过 HLS 实现了端到端的硬件部署验证。
*   **数据驱动**：相比静态规则，机器学习能更好地捕捉复杂符号负载中的统计特性。
*   **多功能性**：除了性能优化，论文还指出了该方法在**安全检测**（如识别木马逻辑或异常访问模式）方面的潜力。

### 8. 不足与局限
*   **模型复杂度较低**：目前仅使用了基础的随机森林，对于更复杂的图结构，可能需要图神经网络（GNN）等更先进的模型。
*   **验证细节不足**：虽然提到了“验证步骤”以确保功能等效，但未详细说明如何自动化地在大规模图中验证所有可能的匹配路径。
*   **应用领域局限**：实验主要集中在基因组学相关的 NFA 匹配，对于其他类型的符号处理任务（如复杂的深度包检测）的泛化能力尚待进一步验证。

（完）
