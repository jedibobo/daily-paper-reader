---
title: Leveraging Non-Equilibrium ECRAM Dynamics for Short-Term Plasticity in Neuromorphic Circuits
title_zh: 利用非平衡态 ECRAM 动力学实现类脑电路中的短期可塑性
authors: "Alex Currie, Sean Borkholder, Nithil Harris Manimaran, Huayuan Han, Cory Merkel, Ke Xu, Tejasvi Das"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11243v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 神经形态硬件的跨层器件-电路-系统协同设计
tldr: 本研究针对神经形态硬件中短程可塑性（STP）实现效率低的问题，提出利用电化学随机存取存储器（ECRAM）的非平衡离子动力学作为计算资源。通过器件-电路-系统跨层协同设计，开发了集成ECRAM突触的延迟反馈LIF神经元架构。该方案利用ECRAM的瞬态电导调制实现突触易化和神经元兴奋性调节，每脉冲功耗仅2 pJ。研究证明了原本被视为噪声的器件特性可转化为高效的时间信息处理能力，为低功耗类脑计算提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决神经形态硬件中短程可塑性（STP）难以高效实现的问题，并将ECRAM中通常被视为缺陷的非平衡动力学转化为计算资源。
method: 采用器件-电路-系统协同设计，引入一种结合ECRAM突触与可调延迟反馈路径的漏电积分发放（LIF）神经元架构。
result: 仿真结果显示该架构成功实现了突触易化和兴奋性调制，每脉冲功耗仅为2 pJ，并支持频率选择性脉冲处理。
conclusion: 该工作证明了ECRAM的非平衡动力学可作为实现神经形态电路中STP和时间计算的原生硬件基础。
---

## 摘要
短期可塑性（STP）是生物神经系统时间信息处理的基础，但在类脑硬件中仍难以高效实现。忆阻型电化学随机存取存储器（ECRAM）器件天然表现出产生瞬态电导调制的非平衡离子动力学；然而，在以存储为中心的计算范式中，这些行为通常被视为不理想的变异性，或作为副作用被容忍。在这项工作中，我们通过跨层器件-电路-系统协同设计框架，将这些挥发性动力学从被动容忍的器件缺陷转化为计算资源。我们引入了一种与 ECRAM 突触协同设计的延迟反馈漏能积分发放（LIF）神经元架构，该架构利用了活动依赖的电导调制，且额外电路开销极小。该架构将基于 ECRAM 的突触与可调延迟反馈脉冲生成路径相结合，使瞬态器件动力学能够直接调节神经元兴奋性和突触效能。我们利用具有瞬态电导调制特性（每脉冲 1.5 KOhms）的实验表征 ECRAM 器件，开发了适用于电路级仿真的紧凑行为模型。电路仿真展示了两种关键的 STP 行为——突触易化和内在兴奋性调节，同时每脉冲功耗仅为 2 pJ，且相同的器件驱动机制可扩展至多种神经元拓扑结构。网络级分析进一步展示了频率选择性脉冲处理，使单个突触能够在脉冲神经网络中充当可调的时间滤波器。这项工作证明了非平衡态 ECRAM 动力学可以作为类脑电路中 STP 和时间计算的原生硬件基底。

## Abstract
Short-term plasticity (STP) is fundamental to temporal information processing in biological neural systems but remains difficult to realize efficiently in neuromorphic hardware. Memristive electrochemical random-access memory (ECRAM) devices naturally exhibit non-equilibrium ionic dynamics that produce transient conductance modulation; however, these behaviors are typically treated as undesirable variability or tolerated as side effects in memory-centric computing paradigms. In this work, we instead transform these volatile dynamics from a tolerated device artifact into a computational resource through a cross-layer device-circuit-system co-design framework. We introduce a delay-feedback leaky integrate-and-fire (LIF) neuron architecture co-designed with ECRAM synapses that exploits activity-dependent conductance modulation with negligible additional circuit overhead. The architecture integrates ECRAM-based synapses with a tunable delay-feedback spike-generation path, enabling transient device dynamics to directly modulate neuron excitability and synaptic efficacy. We used experimentally characterized ECRAM devices exhibiting transient conductance modulation (1.5 KOhms per spike) to develop a compact behavioral model suitable for circuit-level simulation. Circuit simulations demonstrate two key STP behaviors -- synaptic facilitation and intrinsic excitability modulation -- while consuming 2 pJ per spike, and the same device-driven mechanisms extend across multiple neuron topologies. Network-level analysis further demonstrates frequency-selective spike processing, allowing individual synapses to act as tunable temporal filters within spiking neural networks. This work demonstrates that non-equilibrium ECRAM dynamics can serve as a native hardware substrate for STP and temporal computation in neuromorphic circuits.

---

## 论文详细总结（自动生成）

这篇论文由罗切斯特理工学院（RIT）的研究团队撰写，探讨了如何利用电化学随机存取存储器（ECRAM）的非平衡动力学来实现神经形态电路中的短期可塑性（STP）。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在生物神经系统中，短期可塑性（STP）对于处理时间信息至关重要。然而，在传统的神经形态硬件中，实现 STP 通常需要复杂的额外电路，导致面积和功耗开销巨大。
*   **核心问题**：忆阻型器件（如 ECRAM）在偏置下会表现出瞬态（挥发性）电导变化，这在传统的存储应用中被视为不稳定的“缺陷”或噪声。
*   **整体含义**：本文提出一种跨层协同设计框架，将 ECRAM 的这种“缺陷”转化为计算资源，利用其原生的离子动力学在硬件层面直接模拟生物突触的易化（Facilitation）和神经元兴奋性调节，从而实现高效的时间计算。

### 2. 论文提出的方法论
*   **核心思想**：利用三端 ECRAM 器件的特性，通过其栅极（Gate）控制离子的重新分布。低幅值/短脉冲触发挥发性的双电层（EDL）效应（对应 STP），高幅值/长脉冲触发非挥发性的电化学掺杂（对应长期记忆）。
*   **关键技术细节**：
    *   **1T1E 突触结构**：采用一个晶体管（1T）和一个 ECRAM（1E）串联。晶体管负责脉冲输入，ECRAM 负责权重存储及 STP 动力学。
    *   **延迟反馈 LIF 神经元**：设计了一种新型漏电积分发放（LIF）电路。利用电流镜进行电流缩放，并使用“电流饥饿型反相器”（Current-starved inverters）产生毫秒级的延迟反馈，以匹配 ECRAM 的离子动力学时间尺度。
    *   **STP 实现机制**：
        *   **突触易化**：将 ECRAM 栅极连接到突触输入端，输入脉冲会暂时提升该突触的电导。
        *   **内在兴奋性调节**：将 ECRAM 栅极连接到神经元输出端，神经元发放脉冲后会暂时提升所有输入突触的有效增益。
*   **算法/模型**：开发了一个基于物理特性的 Verilog-A 紧凑模型，将离子弛豫动力学与 Enz-Krummenacher-Vittoz (EKV) 晶体管框架耦合，确保仿真能准确反映实验观测到的电导漂移与恢复。

### 3. 实验设计
*   **器件表征**：基于二硫化钼（MoS2）沟道和 PEO:LiClO4 固体聚合物电解质制备了真实的 ECRAM 器件，表征了 128 个非挥发性状态及不同电压下的挥发性响应。
*   **电路仿真场景**：
    *   使用 GlobalFoundries 55nm CMOS 工艺进行电路级仿真。
    *   测试了不同频率（50Hz, 100Hz, 200Hz）下的脉冲输入响应。
    *   对比了两种 LIF 拓扑：本文提出的“延迟反馈型”和文献中的“电容折返型”（Capacitive Refractory）。
*   **网络级分析**：在 Python 环境下使用 `snnTorch` 框架，将硬件参数抽象为数学模型，模拟突触作为频率滤波器的行为。

### 4. 资源与算力
*   **硬件制备**：使用了标准的平面微纳加工技术（AFM 表征、离子栅极转移特性测试）。
*   **仿真工具**：Cadence Spectre 用于电路仿真，Python (snnTorch) 用于网络级分析。
*   **算力说明**：文中未明确提及具体的 GPU 型号或训练时长，因为研究重点在于电路行为仿真和器件建模，而非大规模深度学习训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 ECRAM 器件进行了详尽的电压脉冲响应测试（0.1V 至 2.5V）。
    *   进行了 100 次蒙特卡洛（Monte-Carlo）仿真，以评估工艺偏差（Mismatch）对膜电位变化、能耗和发放时间的影响。
    *   对比了长短两种时间尺度（毫秒级 vs 微秒级）下的电路表现。
*   **充分性评价**：实验设计较为全面，涵盖了从底层器件物理到顶层网络功能的跨层验证。通过蒙特卡洛分析证明了方案的鲁棒性，实验结果具有客观性。

### 6. 论文的主要结论与发现
*   **原生 STP 实现**：ECRAM 的非平衡动力学可以完美模拟生物 STP，无需额外电路。
*   **高能效**：在微秒量级操作下，每脉冲功耗仅为 **2 pJ**。
*   **时间滤波功能**：通过调节 ECRAM 动力学参数，单个突触即可实现高通、低通或带通滤波，增强了 SNN 处理时序数据的能力。
*   **架构通用性**：证明了该机制可以集成到多种不同的 LIF 电路拓扑中。

### 7. 优点
*   **跨层创新**：从器件物理特性出发解决系统级难题，实现了真正的“器件-电路协同设计”。
*   **极低开销**：利用器件固有特性实现 STP，几乎不增加额外的硅面积。
*   **多功能集成**：在同一个物理单元内同时实现了长期记忆（非挥发性）和短期动态（挥发性）。
*   **可调性强**：通过改变反馈路径的偏置电压，可以灵活调整神经元的发放宽度和不应期。

### 8. 不足与局限
*   **速度限制**：目前实验用的聚合物电解质 ECRAM 响应速度在毫秒级，虽然文中讨论了微秒级的可能性，但尚未在实际硬件中完全验证高速离子材料的集成。
*   **材料兼容性**：MoS2 和聚合物电解质在大规模 CMOS 后端工艺（BEOL）中的集成仍面临挑战。
*   **网络规模**：目前的验证主要集中在单神经元和小型突触阵列，在大规模深度脉冲神经网络中的系统级表现（如准确率提升）仍需进一步量化。

（完）
