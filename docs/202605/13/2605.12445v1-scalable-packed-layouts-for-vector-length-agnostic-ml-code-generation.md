---
title: Scalable Packed Layouts for Vector-Length-Agnostic ML Code Generation
title_zh: 面向向量长度无关机器学习代码生成的可扩展打包布局
authors: "Ege Beysel, Maximilian Bartel, Jan Moritz Joseph"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12445v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 具有向量长度感知打包布局的编译器代码生成
tldr: 针对 Arm SVE 等可伸缩向量指令集在编译时难以确定向量长度的问题，本文提出了一种支持向量长度无关（VLA）代码生成的方案。通过引入感知向量长度的打包数据布局，并将其集成到 MLIR/IREE 编译器流水线中，实现了自动化的分块、融合与向量化。实验表明，该方法在 Arm CPU 上比 NEON 实现提升了高达 1.45 倍性能，且优于 PyTorch 等框架，证明了其在生产级编译器中的有效性与性能可移植性。
selection_source: fresh_fetch
motivation: 现有的机器学习编译器难以应对可伸缩向量指令集（如 Arm SVE）中向量长度在编译时不可知所带来的布局和分块挑战。
method: 提出了一种感知向量长度的打包布局方法，并扩展了 MLIR/IREE 编译器以支持可伸缩向量的分块、融合和向量化。
result: 在真实机器学习工作负载中，该方法比 NEON 代码生成最高提速 1.45 倍，且在性能上超越了 PyTorch 生态系统中的多个框架。
conclusion: 该研究通过可伸缩向量化技术实现了机器学习代码在不同硬件配置下的高效执行和性能可移植性。
---

## 摘要
诸如 Arm SVE 之类的可扩展向量指令集支持向量长度无关（VLA）执行，允许单一实现适配具有不同向量长度的硬件。然而，由于分块（tiling）和数据布局决策不再能在编译时固定，这增加了编译器代码生成的复杂性。我们提出了一种通过向量长度感知的打包数据布局及相应的编译器扩展，在端到端机器学习编译流水线中实现 VLA 代码生成的方法。我们将这些机制集成到 MLIR/IREE 中，并扩展了分块、融合和向量化，使其能够处理可扩展的向量长度。在 Arm CPU 上的真实机器学习工作负载评估表明，我们生成的内容与 IREE 中现有的基于 NEON 的代码生成相比具有竞争力，且通常表现更优，实现了高达 1.45 倍的加速。我们还优于包括 ExecuTorch、TorchInductor 和 eager 执行在内的 PyTorch 生态系统框架，证明了可扩展向量化在生产级编译器环境中的有效性。基于模拟器的研究进一步表明，在计算密集型工作负载上，生成的代码随 SVE 向量长度的增加而扩展，支持了跨硬件配置的性能可移植性。

## Abstract
Scalable vector instruction sets such as Arm SVE enable vector-length-agnostic (VLA) execution, allowing a single implementation to adapt across hardware with different vector lengths. However, they complicate compiler code generation, as tiling and data layout decisions can no longer be fixed at compile time.   We present an approach for enabling VLA code generation in an end-to-end ML compilation pipeline through vector-length-aware packed data layouts and corresponding compiler extensions. We integrate these mechanisms into MLIR/IREE and extend tiling, fusion, and vectorization to operate with scalable vector lengths.   Evaluated on real-world ML workloads on Arm CPUs, our approach generates SVE code that is competitive with, and often outperforms, existing NEON-based code generation within IREE, achieving up to $1.45\times$ speedup. We also outperform PyTorch ecosystem frameworks, including ExecuTorch, TorchInductor, and eager execution, demonstrating the effectiveness of scalable vectorization in a production compiler setting. A simulator-based study further shows that the generated code scales with increasing SVE vector length on compute-bound workloads, supporting performance portability across hardware configurations.

---

## 论文详细总结（自动生成）

这篇论文针对现代 CPU 架构中可伸缩向量指令集（如 Arm SVE）带来的编译挑战，提出了一种创新的代码生成方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 SIMD（如 Arm NEON）具有固定向量长度，编译器可以在编译时确定分块（Tiling）大小和数据布局。而现代架构（如 Arm SVE、RISC-V RVV）采用**向量长度无关（VLA）**模式，向量长度在运行时才确定。这导致编译器难以在编译时做出最优的布局和优化决策。
*   **研究动机**：
    *   **性能可移植性**：希望实现“一次编译，到处运行”，且在不同向量长度的硬件上都能发挥高性能。
    *   **消除手动调优**：减少对特定硬件手工编写内核（Kernel）的依赖，通过端到端编译器自动生成高效代码。
    *   **填补工具链空白**：现有的机器学习编译器（如 IREE、TVM）对可伸缩向量的支持尚不完善，尤其是在数据打包布局（Packed Layouts）方面。

### 2. 方法论
*   **核心思想**：引入**可伸缩打包布局（Scalable Packed Layouts）**。将矩阵分块的大小定义为硬件向量长度（$VL$）的函数，而非固定常数。
*   **关键技术细节**：
    *   **参数化布局**：定义分块尺寸 $m_r = f_m(VL), n_r = f_n(VL), k_r = f_k(VL)$。例如，针对 SVE 的 FP32 内核，分块可能被设定为 $8 \times 2VL$。
    *   **编译器集成**：在 MLIR/IREE 框架中扩展了 `linalg.pack` 和 `linalg.unpack` 操作，使其支持动态/可伸缩的维度。
    *   **算子融合与传播**：打包布局不仅应用于矩阵乘法（MatMul），还会在编译流水线中通过融合（Fusion）传播到前后的算子，减少数据重排开销。
    *   **VLA 代码生成**：利用 MLIR 的 `vector` 方言生成与长度无关的向量指令，并利用 SVE 的谓词寄存器（Predication）处理非对齐的边界问题，消除传统的标量清理循环。

### 3. 实验设计
*   **硬件平台**：
    *   **Radxa Orion O6**：搭载 CIX P1 处理器（12 核，支持 128-bit SVE）。
    *   **Google Pixel 9**：搭载 Tensor G4 处理器（8 核，支持 128-bit SVE）。
    *   **gem5 模拟器**：用于模拟 128、256、512-bit 不同长度的 SVE 环境以测试扩展性。
*   **测试模型（Benchmark）**：涵盖 11 种真实模型，包括 LLM（SmolLM2, Llama-3.2, Qwen2.5）、视觉模型（ViT, YOLOS）和音频模型（Whisper, AST）。
*   **对比方法**：
    *   **IREE (NEON)**：现有的固定长度向量化流水线。
    *   **ExecuTorch**：使用 XNNPack 和 KleidiAI 优化内核的生产级框架。
    *   **TorchInductor**：PyTorch 2.0 的默认编译器后端。
    *   **PyTorch Eager Mode**：原生动态执行模式。

### 4. 资源与算力
*   **算力设备**：实验主要在移动端和边缘端 CPU 上完成（Orion O6 和 Pixel 9）。
*   **训练/编译时长**：论文未提及模型训练时长（属于推理优化研究），但强调了其编译器生成的代码在推理时的延迟（Latency）。
*   **内存环境**：Orion O6 配备 64GB LPDDR5，Pixel 9 配备 12GB LPDDR5X。

### 5. 实验数量与充分性
*   **实验规模**：在两个真实硬件平台上测试了 11 个主流模型，每个实验运行 50 次取平均值，并包含预热过程。
*   **充分性**：实验不仅对比了同类编译器（IREE 内部对比），还跨生态对比了 PyTorch 体系。通过模拟器补全了当前市面缺少宽向量 SVE 硬件的遗憾，验证了理论上的扩展性。实验设计客观、公平，涵盖了计算密集型和内存密集型等多种场景。

### 6. 主要结论与发现
*   **性能提升**：在相同向量长度（128-bit）下，SVE VLA 代码生成比 NEON 最高提速 **1.45 倍**，主要归功于更高效的谓词处理和编译器分块决策。
*   **超越生产框架**：相比 ExecuTorch，在 Pixel 9 上某些模型提速高达 **12.38 倍**（主要在矩阵乘法占主导的模型中）。
*   **良好的扩展性**：模拟器结果显示，在计算密集型任务中，性能随向量长度增加呈近线性增长（512-bit 相比 128-bit 提速约 3.4 倍）。
*   **VLA 的优势**：证明了 VLA 编程模型不仅能提供可移植性，在性能上也完全可以媲美甚至超越针对特定长度优化的代码。

### 7. 优点
*   **系统性强**：将 VLA 概念从单纯的指令生成提升到了**数据布局**层面，解决了端到端编译的核心瓶颈。
*   **生产级集成**：直接集成在 IREE 和 MLIR 中，具有很强的实用价值，且代码已开源并计划合入主线。
*   **前瞻性**：为未来更宽向量的 CPU（如 512-bit SVE 或 RISC-V 向量机）提供了现成的软件栈支持。

### 8. 不足与局限
*   **算子覆盖有限**：目前主要针对矩阵乘法（MatMul）进行了深度优化。对于卷积（Convolution），目前仍依赖 `im2col` 转换，这会带来额外的内存开销。
*   **硬件限制**：由于目前市面主流 Arm CPU 的 SVE 长度多为 128-bit（与 NEON 一致），无法在真实硬件上充分展示宽向量带来的巨大红利，只能依赖模拟器。
*   **内存绑定任务瓶颈**：实验发现，对于非计算密集型算子（如 Softmax、LayerNorm），向量长度的增加对性能提升有限，受限于内存带宽。

（完）
