---
title: Heterogeneous SoC Integrating an Open-Source Recurrent SNN Accelerator for Neuromorphic Edge Computing on FPGA
title_zh: 集成开源循环 SNN 加速器的异构 SoC，用于 FPGA 上的类脑边缘计算
authors: "Michelangelo Barocci, Vittorio Fra, Enrico Macii, Gianvito Urgese"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12217v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于FPGA神经形态边缘计算的异构SoC和SNN加速器
tldr: 本研究针对神经形态边缘计算，提出了一种集成开源循环脉冲神经网络（SNN）加速器ReckOn的异构SoC架构。为解决专用集成电路流片成本高昂的问题，该方案利用FPGA的灵活性，将加速器与RISC-V（X-HEEP）及ARM处理器集成。通过在Braille数字数据集上的在线学习实验，验证了该系统在保持高精度的同时，具备低功耗和高效能的特性，为开源神经形态硬件的普及提供了低成本且高效的途径。
selection_source: fresh_fetch
motivation: 神经形态硬件流片成本极高，限制了其在边缘计算领域的普及，因此需要基于FPGA的灵活且低成本的开源替代方案。
method: 设计并实现了一个异构SoC，将ReckOn循环SNN加速器与RISC-V微控制器及ARM处理器集成在FPGA平台上进行协同管理。
result: 实验证明该FPGA实现版本在分类精度和物理特性上与流片版本一致，并成功在Braille数字数据集上验证了在线学习能力。
conclusion: 该异构SoC为边缘侧神经形态计算提供了一个高效、可扩展且经济的开源硬件参考设计，证明了FPGA在推动该领域发展中的潜力。
---

## 摘要
脉冲神经网络 (SNN) 及其应用的日益普及，促使能够模拟生物神经元典型脉冲数据处理的类脑架构快速增长。SNN 的高效功耗和并行计算能力引导研究人员开发数字加速器，利用这些特性在边缘设备上实现快速、低功耗的计算。然而，数字类脑硬件的普及受到电路芯片流片高昂成本的阻碍，因此，采用现场可编程门阵列 (FPGA) 成为一种可行的替代方案，它为实现数字类脑系统提供了一个灵活且具有成本效益的平台，并有助于开源硬件设计的推广。在这项工作中，我们提出了一种异构片上系统 (SoC)，其中循环 SNN 加速器 ReckOn 的操作通过与传统处理器的集成进行管理。这些处理器包括基于 RISC-V 的开源微控制器 X-HEEP 以及 Zynq Ultrascale 系统中的 ARM 处理器。我们通过在 FPGA 上实现已流片版本的 ReckOn 并重现分类结果来验证我们的设计，以检查其在准确性和物理实现特性方面的等效性。在第二组实验中，我们评估了该方案在分类 Braille 数字数据集子集时的在线学习能力，该数据集近期被用于比较类脑框架和平台。

## Abstract
The growing popularity of Spiking Neural Networks (SNNs) and their applications has led to a significant fast-paced increase of neuromorphic architectures capable of mimicking the spike-based data processing typical of biological neurons. The efficient power consumption and parallel computing capabilities of the SNNs lead researchers towards the development of digital accelerators, which exploit such features to bring fast and low-power computation on edge devices. The spread of digital neuromorphic hardware however is slowed down by the prohibitive costs that the silicon tape out of circuits brings, that's why targeting Field Programmable Gate Arrays (FPGAs) could represent a viable alternative, offering a flexible and cost-effective platform for implementing digital neuromorphic systems and helping the spread of open-source hardware designs. In this work we present an heterogeneous System-on-Chip (SoC) where the operations of ReckOn, a Recurrent SNN accelerator, are managed through the integration with traditional processors. These include the RISC-V-based, open-source microcontroller X-HEEP and the ARM processor featured in Zynq Ultrascale systems. We validate our design by reproducing the classification results through the implementation on FPGA of the taped-out version of ReckOn in order to check the equivalence of the accuracy and the characteristics in terms of physical implementation. In a second set of experiments, we evaluate the online learning capability of the solution in classifying a subset of the Braille digit dataset recently used to compare neuromorphic frameworks and platforms.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种在 FPGA 上实现的异构片上系统（SoC），该系统集成了开源的循环脉冲神经网络（RSNN）加速器 ReckOn，旨在为边缘计算提供低成本、灵活且支持在线学习的类脑计算方案。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然脉冲神经网络（SNN）在低功耗边缘计算中具有巨大潜力，但专用类脑芯片（ASIC）的流片成本极高，且缺乏灵活的开源硬件实现。此外，独立的 SNN 加速器通常需要外部控制器管理，缺乏高效的本地集成方案。
*   **研究动机**：利用 FPGA 的可重构性，将开源 RSNN 加速器（ReckOn）与通用处理器（RISC-V 或 ARM）集成在同一芯片内，构建异构 SoC，以降低开发门槛并提升边缘侧的计算效率。

### 2. 方法论
论文提出了两种异构集成架构，核心思想是利用通用处理器进行配置管理，利用专用加速器进行神经形态计算：
*   **ReckOn 加速器**：支持 e-prop 学习算法，可进行在线学习（Online Learning）。包含 256 个输入/循环 LIF 神经元和 16 个输出神经元。
*   **架构一：X-HEEP + ReckOn**：
    *   **核心**：基于 RISC-V 指令集的开源微控制器 X-HEEP。
    *   **连接**：通过 SPI 接口配置加速器参数，使用 BRAM 存储数据集。
    *   **特点**：全开源工具链，适合对开源生态有严格要求的场景。
*   **架构二：ARM + ReckOn (Zynq SoC)**：
    *   **核心**：Zynq 平台自带的硬核 ARM 处理器。
    *   **连接**：利用 AXI 总线（AXI-GPIO, AXI-BRAM, AXI-SPI）进行高速通信。
    *   **特点**：支持 Linux 系统，通过 Python/Jupyter 进行控制；支持“批处理（Batching）”模式，能处理超出 FPGA 内存限制的大型数据集。
*   **AER 解码器**：设计了一个基于有限状态机（FSM）的地址事件表示（AER）解码器，负责将存储的 32 位字解码为脉冲、标签和时间戳，并管理 ReckOn 的推理与训练时序。

### 3. 实验设计
*   **数据集与场景**：
    1.  **二元决策导航（Cue Accumulation）**：模拟啮齿动物在迷宫中根据视觉线索转向。用于验证 FPGA 实现与原始硅片实现的一致性。
    2.  **Braille 数字分类**：通过触觉传感器滑动读取盲文字符。用于评估系统的在线学习能力。
*   **Benchmark 与对比方法**：
    *   对比了 ReckOn 原始 **28nm 硅片流片版本** 的准确率。
    *   对比了 **RTL 仿真** 结果。
    *   在 Braille 任务中，参考了 NIR（神经形态中间表示）框架下的分类表现。

### 4. 资源与算力
*   **硬件平台**：TUL PYNQ-Z2 开发板（搭载 Xilinx Zynq-7020 芯片）。
*   **算力/频率**：
    *   X-HEEP 版本运行频率为 **10 MHz**。
    *   ARM 版本运行频率为 **15 MHz**。
*   **资源占用**：
    *   **X-HEEP 版本**：占用约 91.1% 的 LUT（48483 个）、65.9% 的 DSP 和 71% 的 BRAM。
    *   **ARM 版本**：占用约 77.7% 的 LUT（41335 个）、65.4% 的 DSP 和 86% 的 BRAM。
*   **训练时长**：文中未明确给出具体训练时长，但提到 Braille 任务训练了 200 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：进行了两类主要任务的验证。
    *   导航任务：10 个 Epoch 的训练与验证。
    *   Braille 任务：分别针对 3 分类（A, E, U）和 4 分类（增加 Space 或 O）进行了 200 个 Epoch 的在线学习实验。
*   **充分性评价**：实验设计较为客观，通过与已流片的芯片结果对比，证明了 FPGA 实现的等效性。但在 Braille 任务上仅使用了子集（3-4 类），未涵盖完整的 7 类或更多类别，实验规模属于中等。

### 6. 主要结论与发现
*   **等效性验证**：FPGA 版本的 ReckOn 在导航任务中达到了 **96.4% - 96.8%** 的验证准确率，与原始硅片版本（96.4%）几乎完全一致。
*   **在线学习能力**：在 Braille 3 分类任务中，测试集准确率达到 **90%**；在 4 分类任务中，准确率在 **60% - 78.8%** 之间。
*   **架构优势**：证明了异构 SoC 架构在管理复杂神经形态任务时的可行性，尤其是 ARM 架构通过批处理解决了 FPGA 内部存储不足的问题。

### 7. 优点
*   **开源与灵活性**：整合了 ReckOn 和 X-HEEP 两个开源项目，为研究人员提供了可修改的硬件模板。
*   **在线学习**：不同于仅支持推理的加速器，该系统支持在边缘侧直接进行权重更新（e-prop 算法）。
*   **异构集成**：提供了两种控制方案，兼顾了全开源需求（RISC-V）和高性能/易用性需求（ARM + Python）。

### 8. 不足与局限
*   **时钟频率较低**：10-15 MHz 的频率远低于现代数字电路的潜力，限制了处理速度。
*   **资源受限**：在 Zynq-7020 这种低端 FPGA 上，资源占用接近饱和（尤其是 LUT 和 BRAM），难以扩展更大规模的神经网络。
*   **任务复杂度有限**：Braille 实验仅针对 3-4 个类别，对于更复杂的时空模式识别能力尚待进一步验证。
*   **功耗分析缺失**：虽然论文强调了低功耗背景，但未给出 FPGA 实现的具体功耗测量数据。

（完）
