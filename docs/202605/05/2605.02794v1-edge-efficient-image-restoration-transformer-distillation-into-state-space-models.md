---
title: "Edge-Efficient Image Restoration: Transformer Distillation into State-Space Models"
title_zh: 边缘高效图像恢复：将 Transformer 蒸馏至状态空间模型
authors: "Srinivas Soumitri Miriyala, Sowmya Vajrala, Sravanth Kodavanti, Vikram Nelvoy Rajendiran, Sharan Kumar Allur"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02794v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 提高边缘硬件运行效率并自动发现有效的块组合
tldr: 本研究针对 Transformer 在边缘设备上处理高分辨率图像恢复时延迟过高的问题，提出了一种结合 Transformer 和状态空间模型（SSM）的混合框架。通过将 Transformer 块的特征蒸馏至轻量级 SSM 块中，并利用高效网络搜索（ENS）在预对齐组件中自动发现最优混合配置，实现了精度与效率的平衡。该方法在保持高质量图像恢复的同时，显著降低了移动端的推理开销，为边缘侧高效视觉任务提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决 Transformer 架构在移动端边缘硬件上因自注意力机制导致的推理延迟过高，以及 SSM 在精细图像恢复任务中表现不足的矛盾。
method: 采用特征蒸馏技术将 Transformer 块转化为 SSM 替代块，并引入多目标搜索策略 ENS 自动优化混合架构的性能与延迟。
result: 在骁龙 8 Elite 平台上，该方法在去模糊、去雨和去噪任务中分别实现了 3.4 倍、1.74 倍和 1.17 倍的加速，且质量依然出色。
conclusion: 通过 Transformer 到 SSM 的蒸馏与自动化架构搜索，可以在边缘设备上实现高效且高质量的图像恢复。
---

## 摘要
我们提出了一种用于混合图像恢复的模块化框架，该框架集成了 Transformer 和状态空间模型（SSM）块，重点在于提高边缘硬件上的运行效率。虽然 Transformer 通过自注意力机制提供了强大的全局建模能力，但其注意力算子在移动设备上会产生显著的延迟，尤其是在处理高分辨率输入时。相比之下，诸如 Mamba 之类的 SSM 提供了具有较低运行开销的线性时间序列建模，但在细粒度恢复任务上可能表现不佳。为了平衡准确性和效率，我们将轻量级 SSM 块训练为 Transformer 块的特征蒸馏替代模型，并利用它们构建混合 U-Net 风格的架构。为了自动发现有效的块组合，我们引入了高效网络搜索（ENS），这是一种多目标搜索策略，可从预对齐的组件中选择特定任务的混合配置。ENS 在优化恢复质量的同时惩罚 Transformer 的使用，作为延迟的轻量级代理，并能够在无需重复硬件分析的情况下进行架构发现。在骁龙 8 Elite CPU 上，Restormer 基准测试的推理时间为 10119.52 毫秒。相比之下，通过 ENS 发现的混合模型显著降低了运行时间：ENS-Deblurring 运行时间为 2973 毫秒（快 3.4 倍），ENS-Deraining 为 5816 毫秒（快 1.74 倍），ENS-Denoising 为 8666 毫秒（快 1.17 倍），同时保持了具有竞争力的恢复质量。

## Abstract
We propose a modular framework for hybrid image restoration that integrates transformer and state-space model (SSM) blocks with a focus on improving runtime efficiency on edge hardware. While transformers provide strong global modeling through self-attention, their attention kernels incur substantial latency on mobile devices, especially for high-resolution inputs. In contrast, SSMs such as Mamba offer lineartime sequence modeling with lower runtime overhead but may underperform on fine grained restoration tasks. To balance accuracy and efficiency, we train lightweight SSM blocks as feature-distilled surrogates of transformer blocks and use them to construct hybrid U-Net-style architectures. To automatically discover effective block combinations, we introduce Efficient Network Search (ENS), a multi-objective search strategy that selects task-specific hybrid configurations from pre-aligned components. ENS optimizes restoration quality while penalizing transformer usage, serving as a lightweight proxy for latency and enabling architecture discovery without repeated hardware profiling. On a Snapdragon 8 Elite CPU, the Restormer baseline requires 10119.52 ms for inference. In contrast, ENS-discovered hybrids significantly reduce runtime: ENS-Deblurring runs in 2973 ms (3.4x faster), ENS-Deraining in 5816 ms (1.74x faster), and ENS-Denoising in 8666 ms (1.17x faster), while maintaining competitive restoration quality.

---

## 论文详细总结（自动生成）

这篇论文由三星研究院（印度）的研究团队撰写，提出了一种针对边缘设备优化的混合图像恢复框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：Transformer 架构（如 Restormer）虽然在图像恢复任务中表现卓越，但其自注意力机制在移动端边缘硬件（如手机芯片）上存在极高的推理延迟。而新兴的状态空间模型（SSM，如 Mamba）虽然推理速度快且具有线性复杂度，但在处理精细的像素级恢复任务时精度往往不如 Transformer。
*   **研究动机**：旨在结合 Transformer 的高精度和 SSM 的高效率，解决在移动设备上实现高质量、实时图像恢复的矛盾。

### 2. 方法论
该论文提出了一个模块化的混合框架，包含以下核心技术：
*   **特征级知识蒸馏（FwKD）**：将 Transformer 块（教师）的输出作为目标，训练轻量级的 MambaIR 块（学生）作为其“数字孪生”替代品。通过最小化两者输出特征的 $L_2$ 距离，使 SSM 块能够学习到 Transformer 的全局建模能力。
*   **高效网络搜索（ENS）**：
    *   **搜索空间**：基于 U-Net 骨干网络，将每个阶段的块替换选项参数化（包括全量 Transformer、不同规模的 SSM 块等）。
    *   **多目标优化**：使用贝叶斯优化（BO）和期望超体积改进（EHVI）策略，同时优化“恢复质量（PSNR）”和“架构复杂度（Transformer 使用惩罚项）”。
    *   **硬件感知代理**：引入惩罚项作为延迟的轻量级代理，避免了在搜索过程中反复进行耗时的硬件实测。
*   **端到端微调**：在搜索到最优混合架构后，进行整体微调以消除模块替换带来的级联误差。

### 3. 实验设计
*   **任务与数据集**：
    *   **去雨**：Rain13K（训练），Rain100H/L、Test100/1200/2800（测试）。
    *   **去模糊**：GoPro（运动模糊）、DPDD（离焦模糊）、RealBlur、HIDE。
    *   **去噪**：SIDD、SenseNoise。
*   **基准模型（Benchmark）**：以 **Restormer** 作为主要教师模型和性能上限基准。
*   **对比方法**：包括传统的 CNN 方法（MPRNet、NAFNet）、纯 Transformer 方法（Uformer、SwinIR）以及纯 SSM 方法（MambaIR、VMambaIR）。

### 4. 资源与算力
*   **蒸馏训练**：预训练 22 个 MambaIR 替代块大约消耗了 **20 个 A100 GPU 小时**。
*   **架构搜索**：ENS 过程运行了 500 次函数评估。
*   **硬件测试平台**：高通骁龙 8 Elite (SM8750) CPU，在 Android 智能手机环境下进行实测。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同的图像恢复任务，进行了大量的定量对比（PSNR/SSIM）和定性视觉对比。
*   **消融实验**：
    *   验证了特征蒸馏（FwKD）的必要性（不使用蒸馏会导致 PSNR 显著下降）。
    *   验证了 ENS 搜索的有效性（对比随机选择和固定比例混合）。
    *   **泛化性测试**：将该框架应用于 Uformer-MambaIR 组合，证明了方法的通用性。
*   **评价**：实验设计全面，涵盖了从算法精度到硬件延迟的闭环验证，对比基准选择合理，实验结果具有说服力。

### 6. 主要结论与发现
*   **显著加速**：在骁龙 8 Elite 平台上，相比纯 Restormer，ENS 发现的混合模型在去模糊任务上快了 **3.4 倍**，去雨快了 **1.74 倍**，去噪快了 **1.17 倍**。
*   **精度保持**：混合架构在大幅降低延迟的同时，保持了与 Transformer 基准模型相当甚至更优的恢复质量。
*   **互补效应**：通过有效感受野（ERF）分析发现，蒸馏后的 SSM 块获得了更广的全局视野，而混合架构中的 Transformer 层也变得更加关注全局上下文。

### 7. 优点与亮点
*   **模块化解耦**：将复杂的混合架构设计简化为“块级蒸馏 + 离散搜索”，无需从头训练无数个候选模型。
*   **硬件友好**：直接针对移动端 SoC 延迟进行优化，而非仅仅关注理论上的 FLOPs 或参数量（论文指出 FLOPs 低并不代表延迟低）。
*   **训练效率高**：搜索过程无需重新训练模型，仅需在预对齐的组件库中进行选择。

### 8. 不足与局限
*   **依赖教师模型**：混合模型的性能上限受限于教师模型（Restormer/Uformer）的质量。
*   **代理指标偏差**：虽然使用了惩罚项作为延迟代理，但对于某些特定硬件算子优化，这种间接指标可能无法完全反映真实的运行耗时。
*   **块级假设**：该方法假设块与块之间是功能可替代的，可能忽略了层与层之间更深层次的协同进化关系。

（完）
