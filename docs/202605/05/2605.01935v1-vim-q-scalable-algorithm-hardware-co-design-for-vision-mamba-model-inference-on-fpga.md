---
title: "ViM-Q: Scalable Algorithm-Hardware Co-Design for Vision Mamba Model Inference on FPGA"
title_zh: ViM-Q：面向 FPGA 的 Vision Mamba 模型推理的可扩展算法-硬件协同设计
authors: "Shengzhe Lyu, Yuhan She, Patrick S. Y. Hung, Ray C. C. Cheung, Weitao Xu"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01935v1"
tags: ["query:hwnas"]
score: 9.0
evidence: FPGA上Vision Mamba的算法-硬件协同设计
tldr: 本研究提出 ViM-Q，一种针对 Vision Mamba (ViM) 模型在 FPGA 上推理的软硬件协同设计方案。针对 ViM 在量化时面临的激活离群值和权重分布挑战，引入了动态逐 token 量化与 4-bit APoT 权重量化；硬件上设计了可重构的线性引擎与细粒度流水线 SSM 引擎，解决了内存访问不匹配问题。实验表明，该方案在 ZCU102 FPGA 上相比 RTX 3090 GPU 实现了显著的加速与能效提升，为边缘端部署 ViM 提供了高效路径。
selection_source: fresh_fetch
motivation: 旨在解决 Vision Mamba 模型在 FPGA 部署中遇到的量化精度下降及 SSM 算子内存访问与流式计算不匹配的难题。
method: 结合硬件感知的动态激活量化、4-bit APoT 权重量化，以及支持运行时配置的线性与细粒度流水线 SSM 硬件引擎。
result: 在 AMD ZCU102 FPGA 上，ViM-Q 相比量化后的 RTX 3090 GPU 实现了 4.96 倍的推理加速和 59.8 倍的能效提升。
conclusion: 该协同设计证明了通过算法优化与专用硬件架构，可以在资源受限的边缘设备上高效部署 Vision Mamba 模型。
---

## 摘要
Vision Mamba (ViM) 模型通过利用状态空间模型 (SSM) 的线性复杂度，相比 Transformer 具有显著的效率优势，但在 FPGA 上高效部署这些模型仍然具有挑战性。线性层难以处理导致静态量化失效的动态激活离群值，而均匀量化在低位宽下无法捕捉权重分布。此外，虽然关联扫描（associative scan）在 GPU 上加速了 SSM，但其内存访问模式与 FPGA 所需的流式数据流不匹配。为了应对这些挑战，我们提出了 ViM-Q，这是一种用于边缘端端到端 ViM 推理的可扩展算法-硬件协同设计。我们引入了一种硬件感知的量化方案，结合了动态逐 token 激活量化和逐通道平滑以减轻离群值的影响，并采用了定制的 4 位逐块加法二次幂 (APoT) 权重量化。模型部署在运行时可参数化的 FPGA 加速器上，该加速器包含一个采用查找表 (LUT) 单元将乘法替换为移位累加操作的线性引擎，以及一个在保持顺序递归的同时并行化状态维度的细粒度流水线 SSM 引擎。至关重要的是，该硬件支持运行时配置，能够适应 ViM 系列中不同的维度和输入分辨率。在 AMD ZCU102 FPGA 上实现，ViM-Q 在 ViM-tiny 的低批次推理中，相比量化后的 NVIDIA RTX 3090 GPU 基准，实现了平均 4.96 倍的加速和 59.8 倍的能效提升。这种协同设计为在资源受限的边缘设备上部署 ViM 模型展示了一条可行的路径。

## Abstract
Vision Mamba (ViM) models offer a compelling efficiency advantage over Transformers by leveraging the linear complexity of State Space Models (SSMs), yet efficiently deploying them on FPGAs remains challenging. Linear layers struggle with dynamic activation outliers that render static quantization ineffective, while uniform quantization fails to capture the weight distribution at low bit-widths. Furthermore, while associative scan accelerates SSMs on GPUs, its memory access patterns are misaligned with the streaming dataflow required by FPGAs. To address these challenges, we present ViM-Q, a scalable algorithm-hardware co-design for end-to-end ViM inference on the edge. We introduce a hardware-aware quantization scheme combining dynamic per-token activation quantization and per-channel smoothing to mitigate outliers, alongside a custom 4-bit per-block Additive Power-of-Two (APoT) weight quantization. The models are deployed on a runtime-parameterizable FPGA accelerator featuring a linear engine employing a Lookup-Table (LUT) unit to replace multiplications with shift-add operations, and a fine-grained pipelined SSM engine that parallelizes the state dimension while preserving sequential recurrence. Crucially, the hardware supports runtime configuration, adapting to diverse dimensions and input resolutions across the ViM family. Implemented on an AMD ZCU102 FPGA, ViM-Q achieves an average 4.96x speedup and 59.8x energy efficiency gain over a quantized NVIDIA RTX 3090 GPU baseline for low-batch inference on ViM-tiny. This co-design shows a viable path for deploying ViM models on resource-constrained edge devices.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **ViM-Q**，这是首个针对 **Vision Mamba (ViM)** 模型在 FPGA 上实现端到端推理的可扩展算法-硬件协同设计方案。

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：Vision Mamba (ViM) 凭借状态空间模型 (SSM) 的线性复杂度，在视觉任务中展现出超越 Transformer 的潜力，非常适合资源受限的边缘设备。
*   **核心问题**：
    1.  **量化挑战**：ViM 存在严重的动态激活离群值（Outliers），传统的静态量化会导致精度大幅下降；同时，低位宽下的均匀量化无法有效捕捉权重的分布特性。
    2.  **硬件适配挑战**：GPU 常用的关联扫描（Associative Scan）算法在 FPGA 上会产生内存访问不匹配问题，难以适配流式数据流。
    3.  **可扩展性需求**：ViM 系列模型具有不同的隐藏维度和输入分辨率，硬件需要具备运行时可重构能力。

### 2. 方法论：核心思想与关键技术
*   **硬件感知量化方案 (W4A8)**：
    *   **激活量化**：采用“逐通道平滑 (Smoothing)”缓解离群值压力，并引入“动态逐 token 量化”，实时计算每个 token 的缩放因子，最大化利用 INT8 范围。
    *   **权重量化**：提出“4-bit 逐块加法二次幂 (APoT) 量化”。APoT 将权重限制为 2 的幂次之和，将复杂的乘法转化为逻辑高效的位移加法操作。
*   **硬件架构设计**：
    *   **统一线性引擎**：利用查找表 (LUT) 预计算位移结果。输入 tile 流入时，先生成 8 种位移候选值存入 LUT，后续计算仅需根据 4-bit 权重索引进行查表和累加，完全消除了乘法器。
    *   **细粒度流水线 SSM 引擎**：放弃关联扫描，回归递归更新模式。通过在状态维度（State Dimension）进行空间并行，同时在 Token 维度保持流水线递归，解决了内存访问冲突，并融合了后续的矩阵和元素级操作。
    *   **运行时参数化**：支持在不重新编译硬件的情况下，通过控制参数适配不同的模型维度和图像分辨率。

### 3. 实验设计
*   **数据集**：ImageNet-1k 图像分类基准。
*   **模型变体**：ViM-tiny (ViM-t), ViM-small (ViM-s), ViM-base (ViM-b)。
*   **Benchmark (基准)**：
    *   **算法对比**：对比了 FP16 基准、均匀量化 (W8A8)、普通二次幂量化 (PoT) 以及不同粒度（逐通道 vs 逐块）的量化效果。
    *   **硬件对比**：对比了 NVIDIA RTX 3090 GPU（包括 FP16 和量化后的 INT8 版本）。
*   **场景**：低 Batch (Batch size = 1) 的边缘推理场景。

### 4. 资源与算力
*   **FPGA 平台**：AMD ZCU102 (16nm 工艺)。
*   **硬件参数**：运行频率 350 MHz。
*   **资源占用**：LUT (44.4%), BRAM (80.8%), DSP (54.6%)。
*   **GPU 算力参考**：对比对象为 RTX 3090 (8nm 工艺)，其功耗约为 150W-200W，而 FPGA 方案功耗仅约 12.8W。
*   **训练/量化时长**：论文未明确说明 PTQ（后训练量化）的具体耗时，但提到采用了数据量较小的校准集。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 ViM 全系列模型（Tiny 到 Base）的精度测试。
    *   进行了详尽的**消融实验**，验证了动态量化、平滑处理、APoT 权重以及预位移技术对精度的贡献。
    *   进行了**设计空间探索**，分析了不同位宽 (W3/4/5) 和块大小 (B16/32/64) 对性能的影响。
*   **充分性评价**：实验设计较为全面，不仅关注算法精度，还深入分析了硬件层面的延迟、功耗和能效比，对比基准（RTX 3090）具有较强的说服力。

### 6. 主要结论与发现
*   **精度保持**：ViM-Q 在 W4A8 配置下，ViM-b 的 Top-1 精度仅下降 0.21%，证明了 APoT 和动态量化的有效性。
*   **性能飞跃**：在 ViM-t 模型上，FPGA 相比量化后的 RTX 3090 实现了 **4.96 倍的加速**和 **59.8 倍的能效提升**。
*   **GPU 瓶颈**：实验发现 GPU 在处理动态量化时开销巨大（计算缩放因子的时间超过了矩阵乘法本身），而 FPGA 的流式架构能完美隐藏这一延迟。

### 7. 优点
*   **软硬协同度高**：APoT 量化与 LUT 位移引擎的结合非常巧妙，充分利用了 FPGA 的逻辑资源特性。
*   **端到端实现**：不仅优化了线性层，还针对 SSM 的递归特性设计了专门的流水线，解决了数据流瓶颈。
*   **灵活性**：运行时可配置性使其能够适应多种视觉任务需求，具备实际部署价值。

### 8. 不足与局限
*   **SSM 模块精度**：为了避免递归误差累积，SSM 核心模块仍保留了高精度（FP16/32），未实现全模型低位宽量化。
*   **硬件覆盖**：目前仅在 ZCU102 这一款中高端 FPGA 上验证，未展示在更低端（如 Zynq-7000 系列）设备上的表现。
*   **Batch Size 限制**：主要针对 Batch=1 的实时推理优化，未讨论在大 Batch 吞吐量优先场景下的表现。

（完）
