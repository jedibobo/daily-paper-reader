---
title: "PipeRTL: Timing-Aware Pipeline Optimization at IR-Level for RTL Generation"
title_zh: PipeRTL：用于 RTL 生成的 IR 级时序感知流水线优化
authors: "Shuo Yin, Fangzhou Liu, Lancheng Zou, Rongliang Fu, Wenqian Zhao, Chen Bai, Tsung-Yi Ho, Yuan Xie, Bei Yu"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01836v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 硬件编译器和RTL生成的IR级流水线优化
tldr: PipeRTL是一个针对硬件编译器的IR级流水线优化框架，旨在解决传统流程中后端重定时因丢失算子结构而受限的问题。它在CIRCT中实现，通过显式化寄存器重定位合法性，结合机器学习时序预测器，将优化建模为带约束的全局最小费用流问题。实验表明，该方法能显著改善RTL生成的时序、功耗和面积，为后端工具提供更优的起点。
selection_source: fresh_fetch
motivation: 传统硬件编译流程将流水线优化推迟到后端网表阶段，导致丢失了IR层级的算子结构信息，限制了全局优化潜力。
method: 在CIRCT中引入显式寄存器重定位，利用学习型时序预测器估算延迟，并采用全局最小费用流算法在时序约束下优化寄存器位置。
result: 在商业后端流程测试中，PipeRTL平均降低了关键路径延迟、功耗和面积，并显著提升了下游实现的质量。
conclusion: 将流水线优化作为显式的编译器阶段，能有效利用高层结构信息，从而在硬件生成过程中取得显著的性能增益。
---

## 摘要
现代硬件编译器越来越依赖丰富的中间表示（IR），以便在生成 RTL 代码之前保留与优化相关的语义。然而，一项重要的优化在很大程度上仍被推迟到后端工具中：流水线优化。在常见的 RTL 流程中，寄存器由前端启发式方法或硬件设计人员插入，并在设计降低到更低级别的网表表示后，由后端重定时（retiming）进行调整。在那个阶段，编译器 IR 最初暴露的大部分算子级结构已经减弱或丢失，限制了全局、编译器级流水线优化的机会。本文提出了 PipeRTL，这是一个用于硬件编译器的 IR 级流水线优化框架，并在 CIRCT 中实例化。PipeRTL 在 IR 中明确了寄存器重定位的合法性，使用学习到的时序预测器来近似后端延迟行为，并将时序感知的寄存器重定位建模为时序约束下的全局最小费用流问题。在商业后端综合流程下对开源设计的评估表明，PipeRTL 平均提高了后端实现质量，在评估的基准测试中降低了关键路径延迟、功耗和面积，同时也为后端重定时提供了更强的起点。这些结果表明，将流水线优化作为显式的编译器 Pass，可以通过改善呈现给后续阶段的时序结构以及最终的后端实现质量，带来对后端有意义的收益。

## Abstract
Modern hardware compilers increasingly rely on rich intermediate representations (IRs) to preserve optimization-relevant semantics before generating RTL code. However, one important optimization is still largely deferred to backend tools: pipeline optimization. In common RTL flows, registers are inserted by frontend heuristics or hardware designers and later adjusted by backend retiming after the design has been lowered to a much lower-level netlist representation. At that point, much of the operator-level structure originally exposed by the compiler IR has already been weakened or lost, limiting opportunities for global, compiler-level pipeline optimization.   This paper presents PipeRTL, an IR-level pipeline optimization framework for hardware compilers, instantiated in CIRCT. PipeRTL makes the legality of register relocation explicit in the IR, uses a learned timing predictor to approximate downstream delay behavior, and formulates timing-aware register relocation as a global min-cost flow problem under timing constraints. Evaluation on open-source designs under a commercial backend synthesis flow shows that PipeRTL improves downstream implementation quality on average, reducing critical-path delay, power, and area across the evaluated benchmarks, while also providing a stronger starting point for backend retiming. These results indicate that exposing pipeline optimization as an explicit compiler pass can deliver backend-meaningful gains by improving the sequential structure presented to later stages and the resulting downstream implementation quality.

---

## 论文详细总结（自动生成）

这是一份关于论文《PipeRTL: Timing-Aware Pipeline Optimization at IR-Level for RTL Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在传统的硬件编译流程中，流水线优化（如寄存器放置）通常被推迟到后端工具（网表级）进行。此时，原始中间表示（IR）中的算子级结构信息已经丢失，导致后端重定时（Retiming）只能在扁平化的网表上进行局部优化，难以实现全局最优。
*   **研究动机**：前端工具（如 HLS）通常依赖启发式算法插入寄存器，缺乏对下游物理实现的感知。作者认为，在编译器 IR 级别进行流水线优化可以弥补前端决策与后端物理实现之间的鸿沟，利用 IR 保留的算子边界和数据流语义来实现更高效的全局优化。

### 2. 方法论
PipeRTL 提出了一个在 CIRCT 编译器框架内实现的 IR 级流水线优化框架，核心步骤包括：
*   **Pipe Dialect 与 wGraph 表示**：引入了专门的 `Pipe` 方言，将电路建模为加权图 `wGraph`。节点代表组合逻辑算子，边代表依赖关系。边权重包含寄存器数量（$w$）和数据位宽（$\beta$）。
*   **学习型时序预测器**：由于 IR 层级缺乏物理信息，PipeRTL 使用 XGBoost 回归模型来预测算子的延迟。特征包括算子类型、操作数数量和位宽，标签来自商业后端工具的合成结果。
*   **组合逻辑传播（Combinational Propagation）**：定义了合法的寄存器移动规则。通过改变节点的阶段电位（Stage Potential, $\Delta s$），在保持电路功能等效的前提下重新分配寄存器。
*   **全局优化建模**：将流水线优化建模为**带时序约束的最小费用流（Min-Cost Flow）问题**。
    *   **目标函数**：最小化总寄存器数据容量（位宽与寄存器数量的乘积）。
    *   **约束条件**：确保每条边上的寄存器数非负，且关键路径延迟不超过设定的目标值 $T$。
*   **IR 降级与生成**：优化后的 `Pipe` IR 被重新映射回标准的 CIRCT 方言（如 `seq` 和 `comb`），最终生成 SystemVerilog。

### 3. 实验设计
*   **数据集/基准测试**：选取了 10 个具有代表性的开源硬件模块，涵盖处理器（BOOM 的分支预测器、香山的 CSR/ICache/TLB）、加速器（Gemmini、TensorLib 的 GEMM/Conv2D）和 IP 生成器（WinoGen）。
*   **实验场景**：使用 ASAP7（7nm 预测性工艺库）和 Synopsys Design Compiler (DC) 作为后端评估环境。
*   **对比方法**：
    1.  原始 RTL（由 CIRCT `firtool` 生成）。
    2.  开启后端重定时（`compile_ultra -retime`）的原始设计。
    3.  PipeRTL 优化后的设计（分别测试开启和关闭后端重定时的情况）。
    4.  消融实验：不带时序约束的 PipeRTL。

### 4. 资源与算力
*   **硬件环境**：实验在一台运行 Ubuntu 20.04 的服务器上完成，配置为 Intel i7-10750H CPU @ 2.60 GHz，64 GB 内存。
*   **算力说明**：论文未提及使用 GPU。由于核心算法是基于 CPU 的最小费用流求解和 XGBoost 推理，其计算开销相对较小。PipeRTL 的编译优化耗时在 0.4s 到 62.2s 之间，比后端工具的重定时速度快约 262 倍。

### 5. 实验数量与充分性
*   **实验规模**：对 10 个不同规模和类别的电路进行了完整的 PPA（性能、功耗、面积）评估。
*   **充分性**：
    *   进行了**消融实验**，证明了时序约束在优化中的必要性（不带约束会导致时序严重违例）。
    *   进行了**时序预测器准确性分析**，对比了 MLP、决策树、SVR 等多种模型。
    *   进行了**时序目标扫描（Clock Sweep）**，验证了 PipeRTL 在不同频率目标下的稳定性。
    *   提供了 **PnR（布局布线）后仿真结果**，证明优化效果能延续到物理实现阶段。
*   **客观性**：使用了商业级后端流程和标准工艺库，对比基准包含了工业界常用的后端重定时技术，实验设计较为客观。

### 6. 主要结论与发现
*   **资源减少**：PipeRTL 平均减少了 19.8% 的寄存器数量和 12.6% 的寄存器数据容量。
*   **PPA 提升**：在开启后端重定时的情况下，PipeRTL 相比原始设计平均进一步降低了 2.1% 的延迟、6.6% 的动态功耗、5.1% 的漏电功耗和 5.4% 的单元面积。
*   **协同效应**：PipeRTL 并不是替代后端重定时，而是为其提供了一个更优的初始顺序结构，两者结合能达到最佳效果。
*   **效率优势**：在 IR 层级处理流水线问题的计算效率远高于在网表层级处理。

### 7. 优点
*   **层级优势**：成功证明了在硬件编译器中显式处理流水线语义的价值，填补了前端与后端之间的优化空白。
*   **数学严谨性**：将复杂的寄存器重定位问题转化为成熟的最小费用流问题，保证了求解的全局性和多项式时间复杂度。
*   **实用性强**：集成在主流框架 CIRCT 中，且能与商业 EDA 工具链无缝衔接。

### 8. 不足与局限
*   **预测器依赖**：优化的质量高度依赖于 XGBoost 时序预测器的准确性。如果工艺库或算子类型发生剧烈变化，需要重新训练模型。
*   **黑盒限制**：对于外部 IP 或 SRAM 宏单元，PipeRTL 只能将其视为黑盒（Pin/Sink），无法优化其内部流水线。
*   **布线延迟估算**：虽然考虑了算子延迟，但在 IR 级别仍难以精确建模复杂的互连布线延迟（Wire Delay），这可能导致在极高频率设计中预测偏差增大。

（完）
