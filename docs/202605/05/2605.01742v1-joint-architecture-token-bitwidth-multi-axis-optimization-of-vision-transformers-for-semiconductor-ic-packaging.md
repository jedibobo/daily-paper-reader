---
title: Joint Architecture-Token-Bitwidth Multi-Axis Optimization of Vision Transformers for Semiconductor IC Packaging
title_zh: 半导体集成电路封装中 Vision Transformer 的架构-Token-位宽联合多轴优化
authors: "Phat Nguyen, Xue Geng, Kaixin Xu, Wang Zhe, Xulei Yang, Ngai-Man Cheung"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01742v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对资源受限环境的架构、Token和位宽联合优化
tldr: 针对视觉Transformer（ViT）在半导体封装检测等资源受限工业场景中部署难的问题，本文提出了一种多轴联合优化框架。该框架整合了神经架构搜索（AutoFormer）、令牌合并（ToMe）和混合精度推理（fp16），从架构、令牌和位宽三个维度协同压缩模型。在半导体缺陷分类任务中，该方法在保持精度的同时，实现了吞吐量、参数量和能耗超过10倍的优化，为工业级高效ViT部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有的ViT优化研究多集中于单一维度，难以满足半导体制造等工业场景对计算、内存和能耗的严苛要求。
method: 提出一个整合AutoFormer架构搜索、ToMe令牌压缩和fp16混合精度推理的协同优化框架。
result: 在实际半导体3D X射线缺陷分类任务中，实现了吞吐量提升10倍以上，且参数量、计算量和能耗均降低了10倍以上。
conclusion: 本研究证明了通过多轴联合优化可以显著提升ViT在工业部署中的效率，是半导体制造领域资源受限部署的先驱性工作。
---

## 摘要
Vision Transformer (ViT) 在视觉识别领域表现优异，但在资源受限的工业环境中的部署仍面临挑战。主要障碍包括高昂的计算成本、内存需求和能源消耗。尽管神经架构搜索 (NAS)、Token 压缩和低精度推理等单一效率技术已得到广泛研究，但以往的工作大多仅针对单一优化轴，限制了在保持准确性的前提下所能获得的整体部署收益。本文提出了首批整体框架之一，联合优化了三个互补维度：架构、Token 和位宽。具体而言，该框架通过神经架构搜索 (AutoFormer) 识别紧凑的主干网络，通过 Token 合并 (ToMe) 减少信息处理量，并利用 fp16 混合精度推理加速单次操作的执行。以 DeiT-B/16 为基准，我们首先分析了在激进压缩方案下 ImageNet-1K 上的准确率与效率权衡。随后，我们将选定的配置应用于用于集成电路芯片封装检测的真实内部 3D X 射线半导体缺陷分类数据集。结果表明，所提出的多轴框架在吞吐量上实现了 10 倍以上的提升，同时在参数量、FLOPs 和能耗方面均减少了 10 倍以上，并保持了下游工业任务所需的准确性。据我们所知，这是最早在 ViT 中联合优化架构、Token 和位宽维度的研究之一，也是首个针对半导体制造量身定制的、以部署为中心的资源高效型研究。

## Abstract
Vision Transformers (ViTs) have achieved strong performance in visual recognition, yet their deployment in resource-constrained industrial environments remains limited. Some main challenges are their high computational cost, memory requirement, and energy consumption. While individual efficiency techniques such as neural architecture search (NAS), token compression, and low-precision inference have been extensively studied, most prior work targets only a single optimization axis, limiting overall deployment gains while preserving accuracy. In this paper, we present one of the first holistic frameworks that jointly optimizes three complementary axes: architecture, token, and bit-width. Specifically, the framework identifies compact backbones via Neural Architecture Search (AutoFormer), reduces information processing via token merging (ToMe), and accelerates per-operation execution via fp16 mixed-precision inference. Starting from a DeiT-B/16 baseline, we first analyze accuracy-efficiency trade-offs on ImageNet-1K under aggressive compression. Then, we apply the selected configurations to a real-world in-house 3D X-ray semiconductor defect classification dataset for IC chip packaging inspection. Results show that the proposed multi-axis framework achieves more than 10 times improvement in throughput along with over 10 times reductions in parameter count, FLOPs, and energy consumption, while maintaining the required accuracy on the downstream industrial task. To the best of our knowledge, this is among the earliest works to jointly optimize architecture, token, and bit-width dimensions in ViTs and the first such resource-efficient, deployment-focused study tailored to semiconductor manufacturing.

---

## 论文详细总结（自动生成）

这是一份关于论文《Joint Architecture-Token-Bitwidth Multi-Axis Optimization of Vision Transformers for Semiconductor IC Packaging》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：Vision Transformer (ViT) 虽然性能强大，但其高昂的计算成本、内存需求和能耗限制了其在资源受限的工业环境（如半导体集成电路封装检测）中的部署。
*   **研究动机**：
    *   **单轴优化的局限性**：现有的优化研究（如 NAS、Token 压缩或量化）大多只关注单一维度，难以充分挖掘模型压缩的潜力。
    *   **工业落地需求**：半导体制造中的 3D X 射线缺陷检测需要极高的吞吐量和低能耗，且必须在保持高精度的前提下实现。
    *   **研究空白**：缺乏将架构、Token 和位宽联合优化的整体框架，尤其是在半导体制造这一特定领域。

### 2. 方法论
论文提出了一个**多轴联合优化框架**，从三个互补维度协同压缩 ViT：
*   **架构轴 (Architecture)**：利用 **AutoFormer**（一种 one-shot 神经架构搜索框架）识别紧凑的主干网络。通过权重共享超网，快速获取 AutoFormer-Small (AutoF-S) 和 AutoFormer-Tiny (AutoF-T) 等子网，避免从头训练。
*   **Token 轴 (Token)**：在紧凑主干上应用 **Token Merging (ToMe)**。该技术在推理过程中逐步合并相似的视觉 Token，减少序列长度，从而降低计算量，且无需重新设计架构。
*   **位宽轴 (Bit-width)**：采用 **FP16 混合精度推理**。通过 TensorRT 将模型转换为 FP16 引擎，利用硬件加速器提升吞吐量并降低能耗。
*   **工作流**：首先在 ImageNet-1K 上分析准确率与效率的权衡，筛选出候选配置，随后在下游工业任务上进行微调以恢复精度。

### 3. 实验设计
*   **数据集**：
    1.  **ImageNet-1K**：用于初步筛选压缩配置，分析不同 Token 合并比例（$r=10, 15$）下的性能。
    2.  **内部 3D X 射线半导体缺陷数据集**：包含 93 个 3D 体积数据（80 个训练，13 个测试），涵盖 Pillar、Solder、Void、Pad 等 5 类缺陷。
*   **基准模型 (Benchmark)**：以 **DeiT-B/16** 作为原始基线。
*   **对比方法**：对比了不同规模的 AutoFormer 子网（Small, Tiny）以及不同 Token 合并比例（$r=10, 15$）的组合方案。

### 4. 资源与算力
*   **硬件**：推理基准测试在 **NVIDIA RTX 6000 Ada Generation GPU** 上完成。
*   **软件**：使用 TensorRT 10 进行引擎转换，支持 ONNX 导出。
*   **训练细节**：
    *   ImageNet-1K 预训练：100 个 epoch，Batch Size 为 1024，使用 AdamW 优化器。
    *   工业数据集微调：在 3D X 射线数据上进行微调以确保 100% 的分类准确率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ImageNet-1K 上测试了 7 种不同的模型配置（从基线到极度压缩的 AutoF-T）。
    *   在工业数据集上测试了 5 种选定配置的部署表现。
*   **充分性评价**：实验设计逻辑清晰，遵循了“基准测试筛选 -> 下游任务迁移 -> 硬件实测”的流程。虽然工业数据集样本量较小（93 个体积），但考虑到半导体缺陷数据的稀缺性和标注成本，这在工业研究中具有代表性。实验涵盖了吞吐量 (FPS)、能耗 (J)、参数量和 GFLOPs 等多个硬件感知指标，评估较为全面。

### 6. 主要结论与发现
*   **多轴协同效应显著**：将 NAS 发现的紧凑架构与 Token 合并相结合，比单一优化手段能获得更好的帕累托最优。
*   **性能大幅提升**：在半导体缺陷分类任务中，最优配置（AutoF-T, $r=15$）相比 DeiT-B 基线实现了：
    *   **吞吐量提升 12.3 倍**（从 13.1 FPS 提升至 161.1 FPS）。
    *   **能耗降低约 13 倍**。
    *   **参数量减少 15.1 倍**，**计算量 (GFLOPs) 减少 24.1 倍**。
*   **精度保持**：通过下游微调，压缩后的模型在工业任务上能够完全恢复精度（达到 100% 准确率）。

### 7. 优点
*   **整体性框架**：打破了以往研究只关注单一优化维度的局限，证明了架构、Token 和位宽联合优化的互补性。
*   **工业导向**：直接针对半导体封装这一高价值、高要求的实际场景，具有很强的应用参考价值。
*   **硬件感知评估**：不仅关注理论上的 FLOPs，还实测了 FPS 和焦耳能耗，更贴近真实部署需求。

### 8. 不足与局限
*   **量化深度不足**：目前仅探索了 FP16 混合精度，未进一步尝试 INT8 或 INT4 等更低位宽的后训练量化 (PTQ)，这可能还有进一步提升的空间。
*   **数据集规模限制**：工业数据集的样本量（93 个）相对较小，虽然在特定任务上达到了 100% 精度，但在更复杂、更多样化的生产线上的泛化能力仍需验证。
*   **任务单一性**：目前仅验证了图像分类任务，未探讨该框架在目标检测或分割等更复杂的半导体检测任务中的表现。

（完）
