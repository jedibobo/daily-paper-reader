---
title: "LoKA: Low-precision Kernel Applications for Recommendation Models At Scale"
title_zh: LoKA：大规模推荐模型的低精度算子应用
authors: "Liang Luo, Yinbin Ma, Quanyu Zhu, Vasiliy Kuznetsov, Yuxin Chen, Jian Jiao, Jiecao Yu, Buyun Zhang, Tongyi Tang, Xiaohan Wei, Yanli Zhao, Zeliang Chen, Yuchen Hao, Venkatesh Ranganathan, Sandeep Parab, Yantao Yao, Maxim Naumov, Chunzhi Yang, Shen Li, Ellie Wen, Wenlin Chen, Santanu Kolay, Chunqiang Tang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10886v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对低精度算子的系统-模型协同设计
tldr: 针对大规模推荐模型（LRM）在应用FP8低精度计算时面临的数值敏感和性能瓶颈问题，本文提出了LoKA框架。该框架采用系统与模型协同设计的方法，通过LoKA Probe进行在线精度探测，利用LoKA Mods进行模型结构优化，并由LoKA Dispatch实现运行时内核调度。LoKA成功解决了FP8在LRM中应用导致的质量下降问题，在保证模型精度的同时显著提升了训练效率。
selection_source: fresh_fetch
motivation: LRM对数值高度敏感且计算模式特殊，直接应用FP8低精度计算会导致模型精度受损和训练时间延长。
method: 提出LoKA框架，通过统计探测识别安全层、协同设计模型组件增强稳定性，并动态调度最优FP8内核。
result: LoKA能够有效平衡计算精度与速度，在维持模型质量的同时充分发挥了新一代GPU的低精度算力优势。
conclusion: 通过系统与模型的深度协同设计，FP8可以被成功且高效地应用于大规模推荐模型的生产环境。
---

## 摘要
最近几代 GPU 通过使用 FP8 等低精度算术提供了显著更高的 FLOPs。虽然 FP8 已成功应用于大语言模型 (LLMs)，但在大规模推荐模型 (LRMs) 中的采用却非常有限。这是因为 LRMs 对数值敏感，其计算以小矩阵乘法 (GEMMs) 及其后的归一化操作为主，且通常在通信密集型环境中进行训练。直接将 FP8 应用于 LRMs 往往会降低模型质量并延长训练时间。这些挑战是 LRM 工作负载固有的，无法仅通过引入更好的 FP8 算子来解决。相反，需要一种系统-模型协同设计的方法来成功集成 FP8。我们提出了 LoKA (Low-precision Kernel Applications)，这是一个通过三个原则使 FP8 在 LRMs 中具有实用性的框架：在真实分布下进行性能分析以确定低精度的安全位置，将模型组件与硬件协同设计以扩大安全范围，以及跨算子库进行编排以最大化收益。具体而言，LoKA Probe 是一种基于统计的在线基准测试方法，用于学习激活值和权重的统计信息并量化逐层误差。这一过程能够精确识别 FP8 应用中安全与不安全、快速与缓慢的位置。LoKA Mods 是一组可重用的模型适配方案，旨在提高 FP8 的数值稳定性和执行效率。LoKA Dispatch 则是一个运行时系统，它利用 LoKA Probe 的统计洞察来选择满足精度要求的最快 FP8 算子。

## Abstract
Recent GPU generations deliver significantly higher FLOPs using lower-precision arithmetic, such as FP8. While successfully applied to large language models (LLMs), its adoption in large recommendation models (LRMs) has been limited. This is because LRMs are numerically sensitive, dominated by small matrix multiplications (GEMMs) followed by normalization, and trained in communication-intensive environments. Applying FP8 directly to LRMs often degrades model quality and prolongs training time. These challenges are inherent to LRM workloads and cannot be resolved merely by introducing better FP8 kernels. Instead, a system-model co-design approach is needed to successfully integrate FP8. We present LoKA (Low-precision Kernel Applications), a framework that makes FP8 practical for LRMs through three principles: profile under realistic distributions to know where low precision is safe, co-design model components with hardware to expand where it is safe, and orchestrate across kernel libraries to maximize the gains. Concretely, LoKA Probe is a statistically grounded, online benchmarking method that learns activation and weight statistics, and quantifies per-layer errors. This process pinpoints safe and unsafe, fast and slow sites for FP8 adoption. LoKA Mods is a set of reusable model adaptations that improve both numerical stability and execution efficiency with FP8. LoKA Dispatch is a runtime that leverages the statistical insights from LoKA Probe to select the fastest FP8 kernel that satisfies the accuracy requirements.

---

## 论文详细总结（自动生成）

### 论文总结：LoKA —— 大规模推荐模型的低精度算子应用

#### 1. 核心问题与整体含义（研究动机和背景）
尽管 FP8 等低精度计算在大型语言模型（LLM）中取得了成功，但在**大规模推荐模型（LRM）**中的应用却面临巨大挑战。LRM 与 LLM 的架构差异显著：LRM 对数值极其敏感，计算模式以大量小规模矩阵乘法（GEMM）紧随归一化层（Normalization）为主，且训练过程受通信带宽限制严重。
直接应用现有的低精度库（如 TorchAO）会导致 LRM 训练速度下降（约 1.3 倍）且模型质量显著恶化（Log Loss 增加 2.5%）。因此，论文提出 **LoKA 框架**，旨在通过“系统-模型协同设计”解决 LRM 在低精度计算下的数值稳定性和执行效率问题。

#### 2. 方法论：核心思想与关键技术
LoKA 框架基于三大原则构建：
*   **LoKA Probe（分布感知分析）：** 
    *   **核心思想：** 传统的随机张量测试无法反映 LRM 真实的激活值和权重分布。
    *   **技术细节：** 在线学习激活值（多元高斯分布）和权重（矩阵正态分布，利用 Kronecker 因子分解减少存储）的统计特性。
    *   **流程：** 在线收集统计量 -> 离线合成代表性输入 -> 量化逐层误差（MERE 指标）与加速比，识别“安全”和“高效”的算子位置。
*   **LoKA Mods（精度感知模型协同设计）：**
    *   **No Bias：** 移除除预测层外的所有偏置项，减少数值不稳定性和 FSDP 通信开销。
    *   **BlockNorm（块归一化）：** 将 RMSNorm 融合进 GEMM 的输出阶段（Epilogue）。不同于全局归一化，它在计算块内部进行归一化，避免了昂贵的跨块同步和全局内存访问。
    *   **Hard Swish：** 用分段线性函数替代含指数运算的 Swish 激活函数，降低计算复杂度并提高低精度下的稳定性。
*   **LoKA Dispatch（算子运行时调度）：**
    *   **核心思想：** 没有单一的库能胜任所有形状。
    *   **技术细节：** 运行时根据 LoKA Probe 的反馈，在 TorchAO、DeepGEMM、FBGEMM 等多个库中动态选择满足精度阈值且速度最快的内核。

#### 3. 实验设计
*   **数据集与场景：** 使用 Meta 生产环境的真实广告推荐数据，包含数百亿样本和数千个特征。
*   **模型家族（Benchmark）：** 
    *   **Wukong：** 包含因子分解机和线性压缩模块。
    *   **Interformer：** 基于 Transformer 的序列处理模型。
    *   **ELFM：** 包含 DCN、DHEN、DLRM 等多种架构的超大规模基础模型。
*   **对比方法：** 以 TF32/BF16 高精度训练为基准，对比了 TorchAO、DeepGEMM、FBGEMM 的原生 FP8 实现。

#### 4. 资源与算力
论文在极大规模的工业级集群上进行了验证：
*   **GPU 型号：** NVIDIA H100, B200, GB200 (NVL72)；AMD MI300X, MI350X。
*   **算力规模：** 实验涵盖了从 16 到 256 个 GPU 的分布式环境。
*   **训练细节：** 针对参数量从 257B 到 1.3T 不等的模型进行了全轨迹训练。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了涵盖 3 种模型家族、5 种硬件架构、多种集群规模的端到端训练实验。
*   **消融实验：** 详细分析了 LoKA Probe 的检测灵敏度、LoKA Mods 各组件（No Bias, BlockNorm, Hard Swish）对延迟的贡献，以及 LoKA Dispatch 相比单一库的性能优势。
*   **充分性评价：** 实验非常充分且具有高度的客观性。它不仅在实验室环境测试，还直接在服务数十亿用户的生产系统中进行了部署验证，证明了其在真实分布偏移下的鲁棒性。

#### 6. 主要结论与发现
*   **性能提升：** LoKA 在生产环境中实现了高达 **20% 的训练吞吐量提升**和 **40% 的推理加速**。
*   **质量无损：** 通过协同设计，LoKA 成功消除了 FP8 带来的精度损失，实现了与高精度基准一致的收敛曲线。
*   **硬件通用性：** 证明了 LoKA 的原则在 NVIDIA 和 AMD 的最新硬件（如 GB200 和 MI350X）上均有效，具有良好的跨平台迁移能力。

#### 7. 优点
*   **系统性强：** 不仅仅是优化算子，而是从统计分析、模型架构到运行时调度进行了全栈式协同。
*   **实用性高：** 解决了 LRM 中小 GEMM 导致低精度收益被归一化开销抵消的顽疾。
*   **统计驱动：** LoKA Probe 提供了一种比随机基准测试更科学的精度评估方法，能发现隐藏的数值风险。

#### 8. 不足与局限
*   **架构依赖：** 虽然覆盖了主流模型，但对于完全新颖的非标准架构，可能仍需手动开发对应的 LoKA Mods 组件。
*   **误差传播建模：** 目前的 Probe 主要关注逐层误差，缺乏对误差在深层网络中累积或抵消效应的深度建模，可能导致在某些层上过于保守地禁用低精度。
*   **编译开销：** LoKA Dispatch 在引入新内核时，可能需要与 `torch.compile` 等编译框架进行复杂的集成调试。

（完）
