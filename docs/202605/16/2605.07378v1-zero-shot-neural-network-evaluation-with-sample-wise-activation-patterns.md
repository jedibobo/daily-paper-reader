---
title: Zero-Shot Neural Network Evaluation with Sample-Wise Activation Patterns
title_zh: 基于样本激活模式的零样本神经网络评估
authors: "Yameng Peng, Andy Song, HaythamM. Fayek, Vic Ciesielski, Xiaojun Chang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07378v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于神经网络架构搜索评估的零样本代理指标
tldr: 本研究针对现有零样本神经网络评估指标在跨架构（CNN与Transformer）和跨任务泛化性差的问题，提出了基于样本激活模式（SWAP）的评估指标SWAP-Score。该指标通过衡量小批量样本在网络中的表达能力来预测性能，具有标签无关性，在CV和NLP任务中均表现出极高的相关性，并能显著加速神经架构搜索（NAS）。
selection_source: fresh_fetch
motivation: 现有的零样本评估指标在不同网络架构间通用性差且与真实性能相关性弱，难以同时适用于CNN和Transformer。
method: 提出SWAP-Score指标，通过分析小批量样本在神经网络中的激活模式来量化模型的表达能力。
result: SWAP-Score在多项CV和NLP任务中表现优异，与真实性能的相关性显著超过现有指标，并在NAS搜索中实现了极高的计算效率。
conclusion: SWAP-Score是一种通用、高效且标签无关的零样本评估工具，为跨架构的神经网络性能预测和快速架构搜索提供了新方案。
---

## 摘要
零样本代理（也称为无训练指标）被广泛用于减少神经网络评估中的计算开销，例如在神经架构搜索（NAS）等场景中，因为它们不需要任何训练。现有的零样本指标存在若干局限性，包括与真实性能的相关性较弱，以及在不同网络或下游任务中的泛化能力较差。例如，大多数此类指标仅适用于卷积神经网络（CNN）或 Transformer，而不能同时适用于两者。为了解决这些局限性，我们提出了样本激活模式（Sample-Wise Activation Patterns, SWAP）及其衍生指标 SWAP-Score，这是一种新颖且高效的零样本指标。SWAP-Score 广泛适用于各种架构家族和任务领域，在大多数任务中表现出强大的预测性能。该指标衡量神经网络在小批量样本上的表达能力，显示出与神经网络真实性能的高度相关性。对于 CNN 和 Transformer，SWAP-Score 在计算机视觉和自然语言处理任务中均优于现有的零样本指标。例如，在 DARTS CNN 上，SWAP-Score 与 CIFAR-10 验证准确率之间的 Spearman 相关系数为 0.93，而在 GLUE 任务上的 FlexiBERT Transformer 中为 0.71。此外，SWAP-Score 与标签无关，因此可以应用于语言模型的预训练阶段，以估计其在下游任务中的性能。当应用于 NAS 时，由 SWAP 赋能的 NAS（即 SWAP-NAS）在 CIFAR-10 和 ImageNet 上分别仅需约 6 分钟和 9 分钟的 GPU 时间即可达到具有竞争力的性能。我们的代码可在以下网址获取：https://github.com/pym1024/SWAP_Universal

## Abstract
Zero-shot proxies, also known as training-free metrics, are widely adopted to reduce the computational overhead in neural network evaluation for scenarios such as Neural Architecture Search (NAS), as they do not require any training. Existing zero-shot metrics have several limitations, including weak correlation with the true performance and poor generalisation across different networks or downstream tasks. For example, most of these metrics apply only to either convolutional neural networks (CNNs) or Transformers, but not both. To address these limitations, we propose Sample-Wise Activation Patterns (SWAP), and its derivative, SWAP-Score, a novel and highly effective zero-shot metric. SWAP-Score is broadly applicable across both architecture families and task domains, demonstrating strong predictive performance in the majority of tasks. This metric measures the expressivity of neural networks over a mini-batch of samples, showing a high correlation with the neural networks' ground-truth performance. For both CNNs and Transformers, the SWAP-Score outperforms existing zero-shot metrics across computer vision and natural language processing tasks. For instance, Spearman's correlation coefficient between the SWAP-Score and CIFAR-10 validation accuracy for DARTS CNNs is 0.93, and 0.71 for FlexiBERT Transformers on GLUE tasks. Moreover, SWAP-Score is label-independent, hence can be applied at the pre-training stage of language models to estimate their performance for downstream tasks. When applied to NAS, SWAP-empowered NAS, SWAP-NAS can achieve competitive performance using only approximately 6 and 9 minutes of GPU time, on CIFAR-10 and ImageNet respectively. Our code is available at: https://github.com/pym1024/SWAP_Universal

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SWAP (Sample-Wise Activation Patterns)** 的新型零样本（Zero-Shot）神经网络评估指标及其衍生算法 **SWAP-NAS**。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在神经架构搜索（NAS）、模型剪枝等领域，评估网络性能通常需要昂贵的反向传播训练。现有的“零样本代理指标”（如 NWOT, Zen-Score 等）虽然计算快，但存在**相关性弱**和**泛化性差**的问题（例如，某些指标仅适用于 CNN，而不能用于 Transformer）。
*   **研究背景**：现有的指标往往受限于输入样本量，导致在区分复杂模型时分辨率不足。作者旨在开发一种既能跨架构（CNN/Transformer）又能跨任务（CV/NLP）的高精度、低成本评估工具。

### 2. 方法论：SWAP-Score
*   **核心思想**：基于网络表达能力（Expressivity）理论。ReLU 或 GELU 等激活函数将输入空间划分为不同的线性/非线性区域。
*   **关键技术细节**：
    *   **标准激活模式 (AP) 的局限**：传统方法关注“每个样本在所有神经元上的激活向量”，其唯一模式数量的上限受限于样本数 $S$，在深度网络中容易饱和。
    *   **样本激活模式 (SWAP)**：作者反其道而行之，关注**“每个神经元在所有样本上的激活向量”**。通过 Signum 函数将激活值二值化（ReLU）或三值化（GELU），计算这些神经元向量中唯一模式的数量（基数）。
    *   **SWAP-Score ($\Psi$)**：即上述唯一模式集合的基数。由于神经元数量 $V$ 远大于样本数 $S$，该指标具有极高的分辨率和区分度。
    *   **正则化**：为了防止指标盲目追求大模型，引入了基于参数量 $\Theta$ 的高斯形状正则化函数，引导搜索过程落在合理的参数区间内。
*   **算法流程**：将 SWAP-Score 集成到进化算法中（SWAP-NAS），通过变异和交叉操作筛选高分架构，无需任何训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **计算机视觉 (CV)**：涵盖图像分类（CIFAR-10/100, ImageNet）、目标检测、场景分类、拼图（Jigsaw）和自动编码。
    *   **自然语言处理 (NLP)**：GLUE 基准测试（CoLA, MNLI, MRPC, QNLI, QQP, RTE, SST, STS）。
*   **Benchmark 与对比方法**：
    *   使用了 NAS-Bench-101/201/301 和 TransNAS-Bench-101 等主流搜索空间。
    *   对比了 15 种以上的 SOTA 零样本指标（如 Synflow, Snip, TE-NAS, NWOT, Zen-Score, ZiCo 等）。
    *   在 Transformer 领域对比了 Synaptic Diversity, Attention Confidence 等专用指标。

### 4. 资源与算力
*   **搜索效率**：
    *   **CIFAR-10**：完成一次完整搜索仅需 **0.004 GPU days（约 6 分钟）**。
    *   **ImageNet**：直接在 ImageNet 上搜索仅需 **0.006 GPU days（约 9 分钟）**。
*   **算力说明**：文中主要以“GPU days”作为衡量标准，强调其相比传统 NAS（动辄数千 GPU days）具有量级上的优势。虽然未指明具体 GPU 型号（如 A100 或 V100），但 6-9 分钟的耗时意味着在单张消费级显卡上即可轻松运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 CV 领域测试了 5 个搜索空间和 8 个任务。
    *   在 NLP 领域测试了 500 个预训练的 FlexiBERT 模型。
    *   进行了关于 Batch Size、输入维度、输入类型（真实数据 vs 噪声）的详细消融实验。
*   **充分性评价**：实验设计非常全面。通过跨领域（CV & NLP）和跨架构（CNN & Transformer）的对比，验证了指标的通用性。使用 Spearman 相关系数作为评价标准，客观地展示了指标与真实准确率之间的对齐程度。

### 6. 主要结论与发现
*   **高相关性**：SWAP-Score 与真实性能的相关性极高。在 DARTS 空间上达到 0.93，在 Transformer 空间上达到 0.71，显著优于现有指标。
*   **通用性**：它是少数能同时在 CNN 和 Transformer 上保持高性能的指标。
*   **标签无关性**：计算 SWAP-Score 不需要数据标签，这意味着它可以用于自监督学习或预训练阶段的模型评估。
*   **NAS 性能**：SWAP-NAS 在极短时间内搜索到的架构在 CIFAR-10 和 ImageNet 上均达到了 SOTA 水平。

### 7. 优点与亮点
*   **视角创新**：从“样本维度”转向“神经元维度”计算激活模式，解决了指标饱和问题。
*   **极速评估**：将 NAS 的搜索成本降低到了分钟级，极大地提高了架构设计的迭代速度。
*   **跨模态支持**：打破了 CNN 指标和 Transformer 指标之间的壁垒，具有很强的工程实用价值。

### 8. 不足与局限
*   **Batch Size 敏感性**：在小型 CNN 模型中，相关性随 Batch Size 增加而下降，需要谨慎选择超参数。
*   **特定任务偏差**：在 NLP 的 RTE 任务上表现较弱（相关性仅 0.11），虽然作者解释这是由于 RTE 任务本身规模小、方差大导致的，但也反映出指标在极小数据集上的不稳定性。
*   **正则化依赖**：正则化函数需要预验知识（如 μ 和 σ 的设定），虽然有自适应方案，但在全新搜索空间中可能需要初步采样。

（完）
