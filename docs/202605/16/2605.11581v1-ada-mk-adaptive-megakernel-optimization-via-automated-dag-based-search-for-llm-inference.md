---
title: "Ada-MK: Adaptive MegaKernel Optimization via Automated DAG-based Search for LLM Inference"
title_zh: Ada-MK：通过基于 DAG 的自动化搜索实现大语言模型推理的自适应 MegaKernel 优化
authors: "Wenxin Dong, Mingqing Hu, Guanghui Yu, Qiang Fu, Peng Xu, Hui Xu, Yue Xing, Xuewu Jiao, Shuanglong Li, Lin Liu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11581v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于自动化DAG搜索的算子融合与映射
tldr: 针对大模型在线广告系统推理中的内核启动开销问题，本文提出Ada-MK。它通过MegaKernel技术将多个算子融合，并利用MLIR进行细粒度DAG离线搜索以消除运行时分支开销。结合三维共享内存约束模型和异构混合推理引擎，Ada-MK在NVIDIA L20上显著提升了吞吐量，是首个在商业广告系统中落地的MegaKernel方案。
selection_source: fresh_fetch
motivation: 现有的MegaKernel方案在资源受限的GPU上存在可移植性差或运行时动态调度开销大的问题，限制了低延迟推理性能。
method: 提出三维共享内存约束模型、基于MLIR的离线DAG搜索以及将MegaKernel作为插件嵌入TensorRT-LLM的混合推理引擎。
result: "在NVIDIA L20上，单批次吞吐量较TensorRT-LLM提升高达23.6%，较vLLM提升50.2%，并在所有测试场景中均获得正向收益。"
conclusion: Ada-MK通过将动态决策提前至编译期并优化内存使用，成功解决了MegaKernel的效率瓶颈，实现了工业级的高效LLM推理。
---

## 摘要
当大语言模型（LLM）在商业在线广告系统中提供实时推理服务时，端到端延迟必须严格限制在毫秒级范围内。然而，在解码（decode）阶段生成的每个 token 都会触发数千次算子内核（kernel）启动，仅内核启动开销就可能占端到端推理时间的 14.6%。MegaKernel 通过将多个算子融合为一个单一的持久化内核，消除了启动开销和算子间的高带宽内存（HBM）往返。然而，现有的 MegaKernel 实现方案在 NVIDIA Ada 等资源受限的 GPU 上面临着可移植性与效率之间的根本矛盾：手工调优的方案与特定架构紧密耦合且缺乏可移植性，而自动编译的方法则引入了运行时动态调度，其分支开销在延迟敏感的场景下是不可接受的。我们观察到，在固定的部署配置下，MegaKernel 的最优执行路径是唯一确定的，运行时的动态决策可以完全提前到编译阶段。基于这一洞察，我们提出了 Ada-MK：(1) 一种结合 K 维度拆分的三维共享内存约束模型，可将峰值共享内存使用量降低 50%；(2) 基于 MLIR 的细粒度 DAG 离线搜索，用于固化最优执行路径，完全消除运行时分支；(3) 一种异构混合推理引擎，将 MegaKernel 作为插件嵌入到 TensorRT-LLM 中，结合了高吞吐量的预填充（Prefill）和低延迟的解码。在 NVIDIA L20 上，Ada-MK 的单批次吞吐量比原生 TensorRT-LLM 提升了高达 23.6%，比 vLLM 提升了 50.2%，在所有测试场景中均实现了正向收益——这是 MegaKernel 在商业在线广告系统中的首次工业化部署。

## Abstract
When large language models (LLMs) serve real-time inference in commercial online advertising systems, end-to-end latency must be strictly bounded to the millisecond range. Yet every token generated during the decode phase triggers thousands of kernel launches, and kernel launch overhead alone can account for 14.6% of end-to-end inference time. MegaKernel eliminates launch overhead and inter-operator HBM round-trips by fusing multiple operators into a single persistent kernel. However, existing MegaKernel implementations face a fundamental tension between portability and efficiency on resource-constrained GPUs such as NVIDIA Ada: hand-tuned solutions are tightly coupled to specific architectures and lack portability, while auto-compiled approaches introduce runtime dynamic scheduling whose branch penalties are unacceptable in latency-critical settings. We observe that under a fixed deployment configuration, the optimal execution path of a MegaKernel is uniquely determined, and runtime dynamic decision-making can be entirely hoisted to compile time. Building on this insight, we propose Ada-MK: (1) a three-dimensional shared-memory constraint model combined with K-dimension splitting that reduces peak shared memory usage by 50%; (2) MLIR-based fine-grained DAG offline search that solidifies the optimal execution path, completely eliminating runtime branching; and (3) a heterogeneous hybrid inference engine that embeds MegaKernel as a plugin into TensorRT-LLM, combining high-throughput Prefill with low-latency Decode. On an NVIDIA L20, Ada-MK improves single-batch throughput by up to 23.6% over vanilla TensorRT-LLM and 50.2% over vLLM, achieving positive gains across all tested scenarios--the first industrial deployment of MegaKernel in a commercial online advertising system.

---

## 论文详细总结（自动生成）

以下是对论文《Ada-MK: Adaptive MegaKernel Optimization via Automated DAG-based Search for LLM Inference》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）在线推理（尤其是商业广告系统）中，**内核启动开销（Kernel Launch Overhead）**和**算子间内存读写（HBM Round-trip）**严重制约了毫秒级延迟目标的实现。
*   **研究背景**：
    *   在 Decode 阶段，内核启动开销可占总推理时间的 14.6%。
    *   **MegaKernel** 技术虽能融合算子，但在 NVIDIA Ada（如 L20）等资源受限 GPU 上面临挑战：共享内存（Shared Memory）容量仅为 H100 的一半，且缺乏 TMA 硬件支持。
    *   现有方案要么针对特定架构手工调优（缺乏可移植性），要么采用运行时动态调度（引入了不可接受的分支预测开销）。

### 2. 方法论
Ada-MK 提出了“离线规划代替在线适配”的核心思想，主要包含以下技术细节：
*   **自适应共享内存管理**：
    *   **三维约束模型**：结合硬件规格、模型架构和动态负载，建立共享内存分配模型。
    *   **K 维度细粒度拆分**：将 K 维度 Tile 减半，使峰值共享内存需求降低 50%，从而在受限硬件上构建更深的流水线。
    *   **页面复用**：实现 Activation-Weight 和 Activation-Output 的算子间页面原位复用。
*   **基于 DAG 的自动化搜索**：
    *   **细粒度分解**：利用 MLIR 将粗粒度算子分解为 PTX 级别的细粒度依赖图（DAG）。
    *   **角色划分与流水线化**：将 Warp 划分为 Loader（加载）、Consumer（计算）、Storer（存储）、Controller（控制）等角色。
    *   **路径固化**：通过离线 Profiling 确定最优执行轨迹并直接序列化为内核代码，完全消除了运行时的 `if-else` 分支判断。
*   **异构混合推理引擎**：
    *   将 MegaKernel 作为插件嵌入 TensorRT-LLM。
    *   **相位自适应**：Prefill 阶段使用原生算子（计算密集型），Decode 阶段自动切换至 MegaKernel（IO 密集型）。

### 3. 实验设计
*   **模型与量化**：Qwen3-1.7B 和 Qwen2.5-1.5B，采用 GPTQ-W4A16 量化。
*   **测试场景**：
    1.  **固定短序列**：输入 64 token，输出 12 token（模拟广告业务）。
    2.  **真实数据集**：CSL（摘要任务，中长上下文）和 Human-eval（代码生成）。
*   **对比基准（Benchmarks）**：vLLM (v0.19.0)、SGLang (v0.5.10)、原生 TensorRT-LLM (v1.1.0rc5)。
*   **评估指标**：端到端吞吐量（tokens/s）。

### 4. 资源与算力
*   **硬件环境**：单台服务器，配备 Intel Xeon Platinum 8558 处理器（96 核）和 1TB DDR5 内存。
*   **GPU 规格**：单张 **NVIDIA L20 GPU**（48GB GDDR6，864GB/s 带宽，Ada 架构）。
*   **搜索成本**：文中提到搜索是在离线阶段完成的，但未明确给出具体的搜索耗时（通常为编译期的一次性成本）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种模型版本、3 类典型任务负载，并在 5 种不同的 Batch Size（1, 2, 4, 8, 16）下进行了详尽对比。
*   **充分性评价**：实验设计较为充分，不仅对比了吞吐量，还通过消融实验分析了 Warp 分配和异步预取的收益。对比对象涵盖了目前工业界最主流的推理框架，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：在单批次（BS=1）场景下，Ada-MK 较原生 TensorRT-LLM 提升 23.6%，较 vLLM 提升 50.2%。
*   **场景适用性**：在**小 Batch、短序列**的在线推理任务中优势最明显。
*   **系统级瓶颈**：随着 Batch Size 增加（如 BS=16），vLLM 等框架的系统级调度优势开始显现，Ada-MK 的领先幅度会缩小，甚至在某些长文本场景下被 vLLM 超越。
*   **跨模型一致性**：在不同版本的 Qwen 模型上均表现出稳定的正向收益。

### 7. 优点
*   **工业级落地**：这是 MegaKernel 技术在商业在线广告系统中的首次大规模工业部署，证明了该技术的实用价值。
*   **极致优化**：针对资源受限的 Ada 架构进行了深度定制，通过 K 维拆分和离线 DAG 搜索突破了硬件物理限制。
*   **无缝集成**：插件化设计使其能复用 TensorRT-LLM 的业务能力（如前缀树解码），降低了迁移成本。

### 8. 不足与局限
*   **硬件依赖性**：虽然提出了自适应模型，但核心优化逻辑高度针对 NVIDIA Ada 架构，在 Hopper 或 Blackwell 架构上的增益可能因硬件特性改变而不同。
*   **扩展性边界**：实验主要集中在 2B 以下的小模型，对于百亿或千亿级参数的大模型，MegaKernel 的显存管理压力和收益比尚待验证。
*   **调度缺失**：Ada-MK 侧重于算子层面的融合，在极高并发下的请求调度和 KV Cache 管理方面仍依赖于宿主框架。

（完）
