---
title: Time Domain Near Memory Computing Engine
title_zh: 时域近存计算引擎
authors: "Sarthak Antal, Steve Enosh"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14162v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于AI的高能效近内存计算
tldr: 针对AI负载中数据搬运能耗高的问题，本文提出一种时域近内存计算架构。该架构利用SRAM存储权重，通过电流舵DAC和N脉冲生成器将乘法运算转换至时域执行，并对比了基于延迟单元和计数器的两种累加方案。该设计结合了数字接口的易用性与模拟计算的高能效，在1V电压下实现了7.62 TOPS/W的能效，为低精度MAC运算提供了高效的硬件支持。
selection_source: fresh_fetch
motivation: 旨在解决传统数字架构数据搬运能耗高以及模拟内存计算中ADC/DAC带来的额外面积与功耗开销问题。
method: 提出一种将数字输入编码为脉冲并在时域执行乘法运算的近内存计算架构，并研究了基于延迟单元和计数器的两种累加方案。
result: 4x4 MAC原型在40MHz频率下功耗仅为42uW，能效达到7.62 TOPS/W。
conclusion: 该时域计算引擎通过混合信号设计平衡了技术可移植性与能效，是低精度AI推理任务的理想选择。
---

## 摘要
AI工作负载不断增长的计算需求加剧了对高能效存内计算和近存计算架构的需求，特别是由于数据搬运消耗的能量通常远高于计算本身。虽然全数字架构提供了强大的可扩展性并支持高分辨率计算，但模拟存内计算在低精度工作负载中表现出更高的能效。然而，其对外部DAC和ADC的依赖引入了额外的功耗、面积和设计开销。为了应对这些挑战，本研究提出了一种用于低精度乘累加（MAC）运算的时域近存计算架构。在所提出的方法中，存储在SRAM中的数字权重位通过电流舵DAC进行转换，而数字输入向量由N脉冲发生器编码。这使得乘法能够在时域内执行，同时保持数字友好的接口。研究并比较了两种累加方案：基于延迟单元的架构和基于计数器的架构，涉及设计权衡、线性度、可扩展性和功率效率。为了提高技术可移植性，N脉冲发生器和计数器采用RTL综合实现，而电流舵DAC保留在模拟域。实现了一个4 x 4 MAC原型，在1 V电源下工作频率为40 MHz，功耗为42 uW，能效达到7.62 TOPS/W。

## Abstract
The increasing computational demand of AI workloads has intensified the need for energy-efficient in-memory and near-memory computing architectures, particularly because data movement often consumes significantly more energy than computation itself. While fully digital architectures provide robust scalability and support higher-resolution computation, analog in-memory computing has demonstrated improved energy efficiency for low-precision workloads. However, its reliance on peripheral DACs and ADCs introduces additional power, area, and design overhead. To address these challenges, this work presents a time-domain near-memory computing architecture for low-precision multiply-and-accumulate (MAC) operations. In the proposed approach, digital weight bits stored in SRAM are converted using a current-steering DAC, while the digital input vector is encoded by an N-pulse generator. This enables multiplication to be performed in the time domain while maintaining a digital-friendly interface. Two accumulation schemes, a delay-cell-based architecture and a counter-based architecture, are investigated and compared in terms of design trade-offs, linearity, scalability, and power efficiency. To improve technology portability, the N-pulse generator and counters are implemented using RTL synthesis, while the current-steering DAC remains in the analog domain. A 4 x 4 MAC prototype is implemented with a 1 V supply, achieving an operating frequency of 40 MHz, power consumption of 42 uW, and energy efficiency of 7.62 TOPS/W.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于时域（Time Domain）的近存计算（Near Memory Computing）引擎，旨在解决AI工作负载中数据搬运能耗高以及传统模拟计算中电压域数据转换器（ADC/DAC）扩展性差的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在AI运算中，数据搬运的能耗远超计算本身。虽然模拟存内计算在低精度下能效极高，但其依赖的电压域ADC/DAC在先进工艺下受电压裕量限制，功耗随分辨率呈指数级增长。
*   **研究动机**：随着工艺缩减，器件的翻转频率（$f_t$）提升，时域电路（如时间数字转换器TDC）比电压域电路具有更好的扩展性和更高的分辨率潜力。因此，探索时域计算架构以平衡数字接口的鲁棒性与模拟计算的高能效。

### 2. 方法论
该架构实现了4位精度（4-bit）的乘累加（MAC）运算，核心流程如下：
*   **乘法实现**：
    *   **权重处理**：从SRAM读取数字权重，通过一个4位电流舵DAC（Current-Steering DAC）转换为比例电流 $I_{DAC}$。
    *   **输入处理**：数字输入通过N脉冲生成器（N-pulse generator）转换为脉冲宽度 $T_{pulse}$。
    *   **时域乘法**：利用电流对电容充电，公式为 $V_{MAC} = (I_{DAC} \cdot T_{pulse}) / C$。乘法结果体现为电容上的电压。
*   **累加方案（对比了两种架构）**：
    *   **架构I（基于延迟单元）**：将乘法电压 $V_{MAC}$ 作为控制电压，驱动电流受限型反相器链。多个乘法单元的延迟直接级联，在时域内一次性完成累加。
    *   **架构II（基于计数器）**：每个乘法单元的延迟被独立测量并转换为数字计数值，然后在数字域进行累加。
*   **技术实现**：N脉冲生成器和计数器采用RTL综合实现以提高可移植性，DAC部分保留在模拟域。

### 3. 实验设计
*   **实现环境**：采用65nm CMOS工艺实现，工作电压为1V。
*   **实验场景**：实现了一个4×4 MAC原型，用于验证低精度AI推理任务。
*   **Benchmark（基准对比）**：对比了近年来的主流研究成果，包括ISCAS'25、ISSCC'16、ASSCC'16等发表的存内/近存计算架构，涵盖了电荷域、时域等不同技术路径。

### 4. 资源与算力
*   **硬件规格**：4×4 MAC 阵列。
*   **运行参数**：时钟频率40 MHz，电源电压1 V。
*   **算力表现**：功耗为42 $\mu$W，能效比达到 **7.62 TOPS/W**。
*   **训练说明**：文中未提及模型训练细节，重点在于推理阶段的硬件架构仿真与实现。

### 5. 实验数量与充分性
*   **实验内容**：论文对两种架构（级联延迟链 vs. 计数器累加）进行了详细的线性度对比分析，并给出了DAC输出、脉冲生成器波形及延迟单元的仿真结果。
*   **充分性评价**：实验涵盖了关键的电路级仿真和系统级能效对比。虽然作为原型设计规模较小（4×4），但对于验证时域计算的可行性以及两种累加方案的权衡（Trade-off）是充分且客观的。

### 6. 主要结论
*   **架构对比**：基于计数器的架构（架构II）线性度更好，但由于需要顺序测量和复杂的控制逻辑，功耗和时延较高；基于延迟单元的架构（架构I）虽然存在非线性累积，但吞吐量更高、控制开销更小。
*   **性能表现**：所提出的时域架构在65nm工艺下实现了与28nm/40nm工艺下先进架构相当的能效（7.62 TOPS/W），证明了时域计算在低功耗边缘AI设备中的潜力。

### 7. 优点
*   **数字化友好**：通过N脉冲生成器和RTL综合，降低了模拟电路的设计复杂度和工艺迁移难度。
*   **能效平衡**：巧妙利用时域特性规避了高功耗电压域ADC的需求。
*   **深入对比**：对两种累加策略进行了详尽的数学建模和非线性分析，为后续研究提供了理论参考。

### 8. 不足与局限
*   **精度受限**：目前仅支持4位精度，难以直接应用于需要高精度（如INT8或FP16）的复杂深度学习模型。
*   **线性度挑战**：在架构I中，随着矩阵规模增加，延迟单元的非线性误差会累积，限制了阵列的大规模扩展。
*   **仿真局限**：目前主要基于仿真数据，缺乏实际流片（Tape-out）后的实测数据验证（如环境噪声、工艺偏差对时域抖动的影响）。

（完）
