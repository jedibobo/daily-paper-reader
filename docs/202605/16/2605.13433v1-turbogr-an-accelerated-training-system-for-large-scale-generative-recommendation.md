---
title: "TurboGR: An Accelerated Training System for Large-Scale Generative Recommendation"
title_zh: TurboGR：一种面向大规模生成式推荐的加速训练系统
authors: "Huichao Chai, Zhixin Wu, Xuemiao Li, Shiqing Fan, Hengfeng Wang, Maojun Peng, Lu Xu, Yaoyuan Wang, Yibo Jin, Wei Guo, Yongxiang Feng"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13433v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对NPU亲和性的训练系统，解决架构不匹配和算子映射问题
tldr: TurboGR是一个针对华为昇腾（Ascend）NPU优化的生成式推荐系统训练框架。针对大规模GR模型在NPU上存在的锯齿状算子缺失、计算不均衡及通信瓶颈等问题，该系统通过锯齿状算子融合、分层稀疏并行、半异步训练及负采样优化等技术，显著提升了训练效率和硬件利用率，支持亿级参数规模的高效扩展。
selection_source: fresh_fetch
motivation: 现有的生成式推荐系统在昇腾NPU上由于缺乏高性能锯齿状算子支持和架构不匹配，导致大规模训练效率低下。
method: 提出了锯齿状算子加速、分布式通信优化（含半异步训练）以及基于异步卸载和Logit共享的负采样优化方案。
result: "在KuaiRand-27K数据集上，该系统支持0.2B参数模型训练，硬件利用率（MFU）达54.71%，且具备近乎线性的扩展性。"
conclusion: TurboGR通过系统级的协同优化，成功解决了大规模生成式推荐在国产NPU上的性能瓶颈，为工业级应用提供了高效的训练方案。
---

## 摘要
生成式推荐（GR）已成为一种极具前景的范式，它利用统一的 Transformer 模型取代了碎片化的、特定场景的架构，并表现出缩放法则（scaling-law）行为，即推荐质量随模型容量和训练数据的增加而系统性提升。然而，在昇腾（Ascend）NPU 上大规模部署 GR 面临着根本性的系统级挑战。由于缺乏针对锯齿状算子（jagged operators）的高性能实现，以及不规则稀疏原语与 NPU 密集计算优化设计之间的架构不匹配，这些挑战在昇腾 NPU 上进一步加剧。在本文中，我们提出了 TurboGR，这是一个面向生成式推荐的昇腾亲和训练系统，通过三项核心创新系统地解决了这些瓶颈：(i) 昇腾亲和的锯齿状加速，包括消除填充冗余的融合算子，以及将设备间不平衡从 47% 降低到 2.4% 的动态负载均衡；(ii) 分布式通信优化，包括分层稀疏并行、具有收敛性证明的半异步训练，以及维持 94% NPU 利用率的细粒度流水线编排；(iii) 负采样优化，通过异步卸载、锯齿感知的 FP16 量化和批次内 Logit 共享，在不增加额外嵌入查找的情况下扩大了有效负样本空间。在 KuaiRand-27K 数据集上的评估结果显示，TurboGR 支持高达 2 亿参数的模型训练，并实现了 54.71% 的 MFU 以及近线性的扩展性（0.97）。

## Abstract
Generative recommendation (GR) has emerged as a promising paradigm that replaces fragmented, scenario-specific architectures with unified Transformer-based models, exhibiting scaling-law behavior where recommendation quality improves systematically with increased model capacity and training data. However, deploying GR at scale on Ascend NPUs faces fundamental system-level challenges. These challenges are further exacerbated on Ascend NPUs due to the absence of high-performance implementations for jagged operators and the architectural mismatch between irregular sparse primitives and NPU's dense-computation-optimized design. In this paper, we present \model, an Ascend-affinity training system for generative recommendation that systematically addresses these bottlenecks through three core innovations: (i) Ascend-affinity jagged acceleration, including fusion operators that eliminate padding redundancy and dynamic load balancing that reduces inter-device imbalance from 47\% to 2.4\%; (ii) distributed communication optimization, comprising hierarchical sparse parallelism, semi-asynchronous training with proven convergence guarantees, and fine-grained pipeline orchestration that sustains 94\% NPU utilization; and (iii) negative sampling optimization via asynchronous offloading, jaggedness-aware FP16 quantization, and intra-batch logit sharing that expand the effective negative space without additional embedding lookups. Evaluated on the KuaiRand-27K dataset, \model supports training at up to 0.2B parameters and achieves 54.71\% MFU with near-linear scalability (0.97).

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **TurboGR** 的加速训练系统，专门针对华为昇腾（Ascend）NPU 架构优化，旨在解决大规模生成式推荐（Generative Recommendation, GR）模型在训练过程中的效率瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
生成式推荐（GR）通过统一的 Transformer 架构取代了传统碎片化的推荐模型，并表现出显著的“缩放法则”（Scaling Law）。然而，在昇腾 NPU 上部署大规模 GR 训练面临三大挑战：
*   **锯齿状序列导致的冗余：** 用户行为序列长度不一，传统填充（Padding）导致超过 50% 的计算冗余，且 NPU 对非连续内存访问的算子支持较弱。
*   **稀疏-密集通信瓶颈：** 稀疏嵌入表（Embedding）与密集 Transformer 骨干网的耦合导致分布式训练的线性扩展性低于 0.6。
*   **负采样内存压力：** 长序列下的 Token 级负采样消耗海量显存（HBM），限制了召回任务的训练规模。

### 2. 方法论：核心思想与关键技术
TurboGR 提出了三项核心创新技术：
*   **昇腾亲和的锯齿状加速（Jagged Acceleration）：**
    *   **融合算子：** 设计了针对 Attention 和相对位置偏置（RAB）的锯齿状融合算子，消除 Padding，统一内部表示。
    *   **嵌入查找优化：** 采用 KeyedJaggedTensor 结构，仅处理有效索引，并重组批次数据以提高 L2 缓存命中率。
    *   **动态负载均衡：** 针对短序列采用“Token 感知动态批次缩放”，针对长序列采用“全局 Token 重新分配”，解决设备间计算不均。
*   **分布式通信优化：**
    *   **分层稀疏并行（HSP）：** 将设备分组，组内模型并行，组间数据并行，将 All-to-All 通信复杂度从 $O(N)$ 降至 $O(I)$。
    *   **半异步训练（Semi-Async）：** 允许稀疏部分的更新滞后于密集部分，通过数学证明其收敛性，从而掩盖通信耗时。
    *   **细粒度流水线：** 设计 6 阶段流水线编排，最大化 CPU 与 NPU 的操作重叠。
*   **负采样优化：**
    *   **异步卸载：** 将负样本嵌入存储在 CPU 内存，通过双缓冲机制分段异步拉取至 NPU。
    *   **量化与 Logit 共享：** 使用 FP16 量化减少显存占用，并利用批次内 Logit 共享（Intra-batch sharing）在不增加查找开销的情况下扩大负样本空间。

### 3. 实验设计
*   **数据集：** 主要使用 **KuaiRand-27K**（短视频推荐数据集，具有长序列特征）和 Amazon-all。
*   **Benchmark 模型：** 选取了 **HSTU**（高性能序列转导单元）和 **FuXi**（华为自研的特征交互增强 Transformer）作为基础架构。
*   **对比维度：** 涵盖了不同规模（Tiny 到 Large/Long）的模型变体，对比了系统优化前后的 MFU（模型算力利用率）、吞吐量、线性扩展性以及推荐精度（HR@k, NDCG@k）。

### 4. 资源与算力
*   **硬件环境：** 使用了 **Ascend 910B1 (64GB) NPU 集群**。
*   **规模：** 实验覆盖了从 **32 到 128 个 NPU**（分布在 4 到 16 个节点上）。
*   **CPU：** 搭载鲲鹏-920 ARM 处理器。
*   **训练细节：** 使用 AdamW 优化器，TF32 精度，学习率为 $4 \times 10^{-3}$。

### 5. 实验数量与充分性
论文进行了较为充分的实验验证：
*   **端到端性能：** 测试了不同模型规模下的 MFU 和扩展性。
*   **消融实验：** 针对锯齿状算子、负载均衡、HSP 通信、半异步策略、负采样卸载等每个核心组件都进行了专项对比实验。
*   **精度验证：** 证明了半异步训练和 FP16 量化对推荐指标（HR/NDCG）的影响微乎其微。
*   **客观性：** 实验涵盖了不同序列长度（2K 到 4K）和不同参数规模（最高 0.2B），数据展示详实。

### 6. 主要结论与发现
*   **效率提升：** TurboGR 在 0.2B 参数模型上实现了 **54.71% 的 MFU**，远高于传统 DLRM 模型的利用率。
*   **扩展性强：** 在 128 NPU 规模下，线性扩展性达到 **0.97**。
*   **资源节省：** 锯齿状融合算子减少了 **70% 的显存开销**，并将延迟降低了 2.2 倍；嵌入查找速度提升了 6 倍。
*   **通信优化：** HSP 将 All-to-All 延迟降低了 **75.9%**。

### 7. 优点
*   **底层适配性深：** 并非简单的框架迁移，而是深入昇腾架构（如 AI Core 的 Tiling 策略、异步拷贝等）进行的算子级重构。
*   **系统性解决瓶颈：** 同时处理了计算（锯齿状）、通信（并行策略）和存储（负采样）三个维度的难题。
*   **理论与实践结合：** 为半异步训练提供了收敛性证明，确保了工程优化的科学性。

### 8. 不足与局限
*   **模型参数规模：** 虽然在推荐领域 0.2B 已算不小，但相比目前主流的 LLM（千亿级），其验证的参数量级仍有提升空间。
*   **硬件单一性：** 实验完全基于昇腾 NPU，虽然这是其研究目标，但缺乏与其他加速器（如 H100）在相同优化思路下的横向对比。
*   **MoE 支持：** 论文结论中提到，目前尚未支持混合专家架构（MoE），这在大规模 GR 模型中是未来的重要方向。

（完）
