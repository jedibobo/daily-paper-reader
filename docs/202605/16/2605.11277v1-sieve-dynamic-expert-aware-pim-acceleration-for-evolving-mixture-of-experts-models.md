---
title: "Sieve: Dynamic Expert-Aware PIM Acceleration for Evolving Mixture-of-Experts Models"
title_zh: Sieve：面向演进中混合专家模型的动态专家感知 PIM 加速
authors: "Jungwoo Kim, Rubens Lacouture, Genghan Zhang, Gina Sohn, Qizheng Zhang, Swapnil Gandhi, Christos Kozyrakis, Kunle Olukotun"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11277v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对MoE模型的PIM加速硬件软件协同设计
tldr: 混合专家模型（MoE）已成为大模型主流架构，但其专家激活分布呈现明显的双峰特性，导致现有存内计算（PIM）系统因静态卸载策略而效率低下。本文提出Sieve框架，通过动态调度器根据运行时Token分布，在GPU和PIM之间灵活划分专家执行任务，并优化了计算与通信的重叠。实验表明，Sieve在多个主流MoE模型上显著提升了吞吐量和交互性。
selection_source: fresh_fetch
motivation: 现代MoE模型专家激活分布不均且动态变化，导致传统PIM系统在处理负载失衡和GPU间通信时效率受限。
method: 提出一种动态调度器和Sieve运行时框架，根据实时Token分布在GPU与PIM间分配专家任务，并实现计算与通信的高效重叠。
result: 在Qwen3.5和GPT-OSS等模型上，Sieve相比现有最先进的PIM系统，将吞吐量和交互性提升了1.3倍至1.6倍。
conclusion: 通过感知专家负载分布并动态协同GPU与PIM资源，可以有效解决大规模MoE模型推理中的内存瓶颈和负载失衡问题。
---

## 摘要
混合专家模型 (MoE) 已成为扩展大语言模型 (LLM) 的主流架构。然而，MoE 推理的执行特性正在迅速变化，且日益与现有存内计算 (PIM) 系统的底层假设不匹配。先前的 LLM PIM 系统依赖静态规则将受内存带宽限制的操作卸载到 PIM，而未考虑负载不均衡和 GPU 间通信的综合影响。同时，现代 MoE 模型在专家总数不断增加的情况下激活的专家却更少，从而形成了双峰专家分布：一小部分专家接收大量 token，而长尾专家仅接收一个或少数几个。我们识别出现代 MoE 模型中 token 到专家分布日益呈现双峰化的趋势，量化了由此导致的专家间算术强度的差异，并证明这种差异显著降低了最先进 LLM PIM 系统的效率。为解决这一问题，我们提出了一种调度器，用于在配备 HBM-PIM 堆栈的多 GPU 系统上部署 MoE 模型。该调度器根据运行时的 token 到专家分布，在 GPU 和 PIM 之间划分专家执行，同时综合考虑互连开销、内存带宽、GPU 吞吐量和 PIM 吞吐量。此外，我们提出了 Sieve，这是一个利用该调度器协调跨 GPU 及其附属 HBM-PIM 堆栈执行的运行时框架。Sieve 在保留由专家并行引起的跨设备依赖关系的同时，实现了 GPU 计算、PIM 计算以及设备内和设备间通信的重叠。Sieve 在基于 Ramulator 2.0 的周期精确模拟器上进行了评估。与最先进的 MoE PIM 系统相比，Sieve 在 Qwen3.5-397B-A17B、GPT-OSS-120B 和 Qwen3-30B-A3B 上分别将吞吐量和交互性提高了 1.3 倍、1.3 倍和 1.6 倍。

## Abstract
Mixture-of-Experts (MoE) has become a dominant architecture for scaling large language models (LLMs). However, the execution characteristics of MoE inference are changing rapidly and increasingly mismatch the assumptions underlying existing Processing-in-Memory (PIM) systems. Prior PIM systems for LLMs rely on static rules to offload memory-bound operations to PIM, without accounting for the combined effects of load imbalance and inter-GPU communication. Meanwhile, modern MoE models activate fewer experts out of increasingly many, creating a bimodal expert distribution: a small set of experts receives many tokens, while a long tail of experts receives only one or a few.   We identify a trend in modern MoE models toward increasingly bimodal token-to-expert distributions, quantify the resulting disparity in arithmetic intensity across experts, and show that this disparity dramatically reduces the efficiency of state-of-the-art PIM systems for LLMs. To address this problem, we propose a scheduler for serving MoE models on multi-GPU systems with attached HBM-PIM stacks. Our scheduler partitions expert execution between GPU and PIM based on runtime token-to-expert distributions, while jointly considering interconnect overhead, memory bandwidth, GPU throughput, and PIM throughput. Moreover, we propose Sieve, a runtime framework that employs the scheduler to coordinate execution across GPUs and their attached HBM-PIM stacks. Sieve overlaps GPU computation, PIM computation, and intra- and inter-device communication while preserving cross-device dependencies induced by expert parallelism. Sieve is evaluated on our cycle-accurate simulator based on Ramulator 2.0. Compared to state-of-the-art PIM systems for MoE, Sieve improves both throughput and interactivity by 1.3x, 1.3x, and 1.6x on Qwen3.5-397B-A17B, GPT-OSS-120B, and Qwen3-30B-A3B, respectively.

---

## 论文详细总结（自动生成）

### 论文总结：Sieve —— 面向演进中 MoE 模型的动态专家感知 PIM 加速

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向混合专家模型（MoE）架构演进，推理性能正面临“AI存储墙”的严峻挑战。论文指出，现代 MoE 模型呈现出**双峰专家分布（Bimodal Expert Distribution）**趋势：极少数“热门”专家处理大量 Token（计算密集型），而大量“冷门”专家仅处理极少数 Token（内存受限型）。
现有的存内计算（PIM）加速方案存在局限性：
*   **静态卸载策略：** 仅将注意力机制（Attention）卸载到 PIM，或使用固定阈值决定专家卸载，无法适应动态变化的 Token 分布。
*   **忽视通信开销：** 未能综合考虑多 GPU 系统中的专家并行（Expert Parallelism）带来的 GPU 间互连（Interconnect）开销。
*   **资源利用不均：** 导致 GPU 或 PIM 在特定阶段处于闲置状态，无法发挥异构系统的最大效能。

#### 2. 论文提出的方法论
论文提出了 **Sieve**，一个动态专家感知的运行时框架，核心包含一个调度器和一套协调系统。

*   **核心思想：** 根据运行时 Token 到专家的分布，动态地将计算任务在 GPU 和其附属的 HBM-PIM 之间进行划分。算术强度低的专家（冷门专家）分配给 PIM，算术强度高的专家（热门专家）保留在 GPU。
*   **关键技术细节：**
    *   **目标函数：** 调度器通过最小化“互连通信时间”、“GPU 执行时间”和“PIM 执行时间”这三者中的最大值（瓶颈点）来优化整体延迟。
    *   **贪心调度算法：** 在推理运行时，将专家按 Token 数量降序排列，迭代评估将最热门专家从 PIM 移至 GPU 是否能降低总执行时间，直到达到最优平衡点。
    *   **Sieve 运行时系统：** 构建了一个依赖图（DAG），实现了 GPU 计算、PIM 计算、GPU 内部通信以及 GPU 间通信（如 AllGather, Dispatch）的高度重叠。
    *   **PIM 通道间张量并行：** 在 PIM 内部采用张量并行而非专家并行，确保所有 PIM 通道负载均衡，避免因专家分布不均导致的硬件利用率低下。

#### 3. 实验设计
*   **数据集/场景：** 使用了真实的请求轨迹数据集 **HH-RLHF**（对话类）和 **MATH-500**（数学推理类）。
*   **评估模型：** 选取了三种具有代表性的现代 MoE 模型：
    *   **Qwen3.5-397B-A17B**（超大规模，高稀疏度）
    *   **GPT-OSS-120B**（中等规模）
    *   **Qwen3-30B-A3B**（较小规模）
*   **Benchmark 与对比方法：**
    *   **GPU-Only：** 纯 GPU 执行。
    *   **NoExp：** 仅将 Attention 卸载到 PIM（如 NeuPIMs）。
    *   **AllExp：** 将所有专家卸载到 PIM（如 PAPI）。
    *   **PIMoE：** 使用静态阈值进行专家卸载的最先进方案。

#### 4. 资源与算力
*   **硬件配置：** 实验基于周期精确模拟器（Ramulator 2.0 和 Duplex）。
*   **GPU 型号：** 模拟了 **NVIDIA B200 GPU**，配备 HBM3E-PIM 堆栈。
*   **算力规模：**
    *   Qwen3.5 使用 **8 个 B200 GPU**。
    *   GPT-OSS 使用 **4 个 B200 GPU**。
    *   Qwen3 使用 **1 个 B200 GPU**。
*   **PIM 参数：** 模拟了 8.0 TB/s 的内部带宽，计算密度为 1 op/byte。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **性能曲线：** 评估了不同 Batch Size（16 到 256）下的吞吐量与交互性（Pareto 曲线）。
*   **混合负载实验：** 针对 Prefill（预填充）和 Decode（解码）阶段共存的场景进行了压力测试。
*   **消融与分析：** 分析了 PIM 通道利用率、调度器开销（约 20μs）以及不同模型稀疏度下的表现。
*   **充分性评价：** 实验覆盖了从单卡到多卡的扩展性，且对比了多种最先进的静态/动态基准，实验设计客观且具有前瞻性。

#### 6. 论文的主要结论与发现
*   **性能提升：** 相比最先进的 PIM 系统（PIMoE），Sieve 在 Qwen3.5、GPT-OSS 和 Qwen3 上分别实现了 **1.3倍、1.3倍和 1.6倍** 的吞吐量与交互性提升。
*   **双峰分布的影响：** 证实了随着模型演进，冷门专家占比极高（如 Qwen3-Next 中 44.2% 的专家仅处理 1 个 Token），这为 PIM 提供了巨大的优化空间。
*   **系统协同的重要性：** 证明了如果不考虑 GPU 间通信开销，单纯优化 PIM 卸载会导致整体性能受限于网络互连。

#### 7. 优点
*   **动态自适应：** 能够根据每批次请求的实时分布调整策略，比静态阈值更具鲁棒性。
*   **软硬件协同：** 无需修改现有的 PIM 硬件指令集，完全通过软件调度层实现，具有较强的实用性。
*   **全局优化：** 首次在 PIM 加速研究中深入探讨了专家并行通信与存内计算的重叠优化。

#### 8. 不足与局限
*   **模拟器依赖：** 所有的评估均基于模拟器完成，虽然使用了周期精确的工具，但在真实硬件（尤其是 HBM-PIM 尚未大规模商用）上的表现仍待验证。
*   **架构特定性：** 方案高度依赖于 HBM-PIM 这种特定的硬件形态，对于其他类型的存内计算（如 SRAM-PIM 或 CXL-PIM）可能需要重新调整调度逻辑。
*   **调度开销：** 虽然在 B200 上仅需 20μs，但如果模型专家数量进一步膨胀到数万个，调度算法的复杂度可能成为新的瓶颈。

（完）
