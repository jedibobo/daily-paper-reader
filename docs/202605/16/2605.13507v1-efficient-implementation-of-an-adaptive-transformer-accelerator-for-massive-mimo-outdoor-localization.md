---
title: Efficient Implementation of an Adaptive Transformer Accelerator for Massive MIMO Outdoor Localization
title_zh: 大规模 MIMO 室外定位中自适应 Transformer 加速器的高效实现
authors: "Ilayda Yaman, Sijia Cheng, Ove Edfors, Liang Liu"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13507v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 使用混合数据流架构和向量处理的硬件实现
tldr: 本研究针对5G大规模MIMO室外定位，提出了一种基于自适应Transformer的硬件加速器实现方案。通过利用信道稀疏性引入行跳过机制，并结合混合数据流架构与轻量级运行时模型切换，在FPGA上实现了亚毫秒级的推理延迟。该设计在保持高定位精度的同时，显著提升了计算效率和吞吐量，满足实时定位需求。
selection_source: fresh_fetch
motivation: 旨在解决5G大规模MIMO定位中Transformer模型计算复杂度高、难以满足亚10毫秒实时性要求的问题。
method: 提出一种利用波束时延信道稀疏性的行跳过机制，并采用混合数据流架构和轻量级感知器路由实现动态模型切换。
result: "在FPGA上实现了高达65%的行稀疏度和2倍的计算加速，定位精度优于1.15米，推理延迟低至0.51毫秒。"
conclusion: 证明了结合传播特性感知稀疏化与高效硬件架构设计，可实现高性能、低延迟的实时5G自适应定位系统。
---

## 摘要
我们提出了一种针对 5G 大规模 MIMO 的基于自适应 Transformer 的定位系统实现，旨在实现低于 10ms 的实时定位。该设计利用了传播特性，其中波束延迟信道表示表现出稀疏性，从而实现了一种行跳过机制，能够以极低的控制开销移除低能量波束分量。本文的贡献集中在模型的硬件实现上，采用了一种混合数据流架构，结合了输入驻留（input-stationary）和输出驻留（output-stationary）的执行方式，并将其映射到具有并行处理单元和加法器树的异构向量处理引擎上，以实现高效的矩阵计算。通过轻量级的运行时模型切换机制支持环境依赖型处理，其中单层感知器路由器的时域滤波输出能够在降低延迟的同时，在专用模型之间实现稳定选择。该设计在 Xilinx Zynq UltraScale+ FPGA 上实现，并在真实世界的大规模 MIMO 测量数据上进行了评估，实现了高达 65% 的行稀疏度，产生了约 2 倍的峰值计算加速，同时相对于浮点基准模型，将平均定位精度损失限制在 10% 以内。该加速器在各种场景下均达到了低于 1.15m 的定位精度，推理延迟为 0.51-2.11ms，吞吐量高达 1961 个位置/秒。这些结果表明，传播感知稀疏性、混合数据流执行以及高效的运行时模型切换，为实时 5G 系统中基于自适应 Transformer 的定位提供了可扩展且低延迟的硬件实现。

## Abstract
We present the implementation of an adaptive Transformer-based localization system for 5G massive MIMO targeting sub-10ms real-time positioning. The design exploits propagation characteristics, where beam-delay channel representations exhibit sparsity, enabling a row-wise skipping mechanism that removes low-energy beam components with minimal control overhead. The contribution is focused on hardware realization of the model using a mixed dataflow architecture, combining input- and output-stationary execution, mapped onto a heterogeneous vector processing engine with parallel processing elements and adder trees for efficient matrix computation. Environment-dependent processing is supported through a lightweight runtime model-switching mechanism, where temporally filtered outputs of a single-layer perceptron router enable stable selection between specialized models with reduced latency. Implemented on a Xilinx Zynq UltraScale+ FPGA and evaluated on real-world massive MIMO measurements, the design achieves up to 65% row sparsity, yielding peak computational speedups of approximately 2x while limiting the average localization accuracy degradation to below 10%, relative to the floating-point baseline model. The accelerator attains below 1.15m localization accuracy across scenarios, with inference latency of 0.51-2.11ms and throughput of up to 1961 positions/s. These results demonstrate that propagation-aware sparsity, mixed dataflow execution, and efficient runtime model switching enable a scalable and low-latency hardware realization of adaptive Transformer-based localization for real-time 5G systems.

---

## 论文详细总结（自动生成）

这篇论文针对 5G 大规模 MIMO（Massive MIMO）环境下的实时室外定位问题，提出并实现了一种基于 FPGA 的自适应 Transformer 硬件加速器。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3GPP Release 17 对 5G 定位提出了严苛的实时性要求（亚 10 毫秒延迟）。虽然大规模 MIMO 提供了丰富的空间特征以实现高精度定位，但其产生的高维信道状态信息（CSI）带来了巨大的计算压力、内存开销和数据传输负担。
*   **研究动机**：传统的 Transformer 模型虽然在定位精度上表现优异，但其计算复杂度高，且通用的硬件加速器未考虑无线电信号特有的“传播感知稀疏性”（Propagation-aware Sparsity）。此外，单一模型难以同时高效应对视距（LoS）和非视距（NLoS）等多种复杂的传播环境。

### 2. 核心方法论
论文提出了一种软硬件协同设计的方案，关键技术包括：
*   **自适应模型切换机制**：使用一个轻量级的单层感知器（SLP）作为路由器，根据输入的波束-时延特征实时预测当前的传播环境（S1-LoS, S2-NLoS, S3-混合），并动态切换到预先训练好的专用轻量级 Transformer 模型。
*   **传播感知稀疏性（行跳过机制）**：利用无线信道在波束-时延域的天然稀疏性，通过元素级阈值处理和行级零值计数，识别并跳过贡献较小的波束分量（行），从而减少冗余计算。
*   **硬件友好的激活函数**：将传统的 Softmax 替换为带偏置的 Sigmoid 激活函数。Sigmoid 是逐元素操作，消除了 Softmax 所需的全局归一化和指数运算，从而支持流式处理并降低硬件复杂度。
*   **混合数据流硬件架构**：在 FPGA 上实现了一个异构向量处理引擎。针对不同计算阶段采用不同的驻留策略：
    *   **输入驻留（Input-stationary）**：用于 Q/K/V 投影阶段，减少权重读取。
    *   **输出驻留（Output-stationary）**：用于注意力输出和全连接层，减少写回操作。
*   **量化策略**：采用 16 位对称定点量化（Q8.8 格式），确保零值在量化后仍为零，以支持稀疏性检测。

### 3. 实验设计
*   **数据集**：使用来自商业 5G 大规模 MIMO 基站（3.85 GHz，100 MHz 带宽）的真实世界测量数据。
*   **场景设置**：
    *   **S1**：楼顶停车场（主要是 LoS）。
    *   **S2**：地面街道（主要是 NLoS）。
    *   **S3**：混合环境（存在遮挡）。
*   **Benchmark（基准）**：
    *   浮点数基准模型（FP32）。
    *   基于 CPU（3.6 GHz）的推理实现。
    *   现有的基于 CNN 的大规模 MIMO 定位 ASIC 实现（22nm 工艺）。
*   **对比维度**：定位精度（平均误差 ME）、95% 置信水平（CL）、推理延迟、吞吐量及功耗。

### 4. 资源与算力
*   **硬件平台**：Xilinx Zynq UltraScale+ ZCU104 FPGA。
*   **算力参数**：时钟频率仅为 100 MHz。
*   **资源占用**：使用了 14.4% 的 LUT、63% 的 BRAM 和 30.6% 的 DSP。
*   **功耗**：总片上功耗估计为 1.29 W。
*   **训练说明**：论文重点在于硬件推理加速，未详细说明离线训练所用的 GPU 型号及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：针对三个场景分别进行了数千个快照（Snapshot）的测试（S1: 4200, S2: 4850, S3: 5000）。
*   **消融实验**：详细对比了 Softmax 与各种 Sigmoid 变体（带偏置、带归一化）的精度差异，并逐步分析了量化和稀疏性对误差的影响。
*   **充分性评价**：实验设计较为充分，使用了真实测量数据而非纯仿真数据，增强了结果的说服力。对比了不同硬件平台（CPU vs ASIC vs FPGA），评估维度全面。

### 6. 主要结论与发现
*   **精度**：在所有场景下均实现了低于 1.15 米的平均定位精度。
*   **性能**：推理延迟在 0.51 ms 至 2.11 ms 之间，远低于 10 ms 的实时性要求。
*   **稀疏性增益**：实现了高达 65% 的行稀疏度，带来了约 2 倍的计算加速，而精度损失控制在 10% 以内。
*   **吞吐量**：峰值吞吐量达到 1961 次位置预测/秒，优于对比的 555 MHz ASIC 实现。

### 7. 优点
*   **领域感知优化**：紧密结合无线电传播特性（稀疏性）进行硬件设计，而非盲目套用通用的 Transformer 加速方案。
*   **软硬协同**：通过修改算法（Sigmoid 替代 Softmax）来简化硬件实现，同时保持了极高的定位精度。
*   **自适应性**：引入 SLP 路由器实现运行时模型切换，增强了系统在复杂多变环境下的鲁棒性。

### 8. 不足与局限
*   **量化深度有限**：目前仅探索了 16 位定点量化，未来若能实现 8 位甚至更低位宽的量化，性能可能进一步提升。
*   **场景覆盖**：虽然使用了真实数据，但仅覆盖了三个特定轨迹，对于更极端或高速移动场景的泛化能力有待验证。
*   **FPGA 频率较低**：100 MHz 的频率相对保守，若能优化时序提升频率，延迟表现会更佳。

（完）
