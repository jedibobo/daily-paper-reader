---
title: "FalconGEMM: Surpassing Hardware Peaks with Lower-Complexity Matrix Multiplication"
title_zh: FalconGEMM：利用低复杂度矩阵乘法超越硬件峰值性能
authors: "Honglin Zhu, Jiaping Cao, Jiang Shao, Siyuan Feng, Qian Qiu, Peng Chen, Xu Zhang, Yixian Zhou, Man Lung Yiu, Guang Ji, Minwen Deng, Wenxi Zhu, Jintao Meng"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06057v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 通过性能模型最大化片上数据复用并选择最优算法
tldr: 针对大语言模型训练与推理中矩阵乘法的性能瓶颈，本文提出FalconGEMM框架，旨在通过低复杂度矩阵乘法算法（LCMA）突破硬件性能峰值。该框架实现了跨平台的自动化部署、利用组并行优化提升数据复用，并结合轻量级性能模型进行策略选择。实验表明，FalconGEMM在多种GPU和CPU架构上均优于cuBLAS、MKL等主流库及AlphaTensor，为异构硬件下的高效计算提供了实用方案。
selection_source: fresh_fetch
motivation: 旨在通过低复杂度算法突破传统矩阵乘法在深度学习任务中的硬件性能瓶颈。
method: 开发了包含部署、执行优化和决策三大模块的跨平台框架，实现LCMA的自动化生成与最优策略选择。
result: "在H20、A100及主流CPU上，性能超越cuBLAS等库7.59%-17.85%，并显著领先于AlphaTensor。"
conclusion: 该研究证明了低复杂度矩阵乘法算法在现代异构硬件生产部署中的实用性与优越性。
---

## 摘要
突破峰值性能的矩阵乘法是提升深度学习（DL）性能的一种极具前景的技术，尤其是在大语言模型（LLM）的训练和推理中。我们提出了 FalconGEMM，这是一个跨平台框架，可在多种硬件上实现低复杂度矩阵乘法算法（LCMA）的自动化部署、优化和选择。该框架包含三项关键创新：(1) 部署模块，通过代码生成实现跨多种硬件和输入配置的可移植执行；(2) 执行模块，采用组并行优化，最大化片上数据复用，充分利用并行资源并降低带宽开销；(3) 决策模块，包含一个轻量级分析性能模型，根据矩阵形状和硬件配置文件选择最优策略。我们在 GPU（H20、A100）和 CPU（ARM、x86）架构上，针对多种数据类型的 LLM 工作负载进行了广泛评估。FalconGEMM 成功实现了突破峰值的性能，相比 GEMM 库（如 cuBLAS、CUTLASS、Intel MKL 等）性能提升了 7.59%-17.85%，相比 AlphaTensor 等 LCMA 竞争对手提升了 12.41%-55.61%。我们的框架使 LCMA 的理论潜力在现代硬件的异构环境中具备了生产部署的实用性。

## Abstract
Peak breaking Matrix Multiplication is a promising technique to improve the performance of DL, especially in LLM training and inference. We present FalconGEMM, a cross-platform framework that automates the deployment, optimization, and selection of Lower-Complexity Matrix Multiplication Algorithms (LCMAs) across diverse hardware. There are three key innovations: (1) a Deployment Module that enables portable execution across various hardware and input configurations through code generation; (2) an Execution Module with Group-Parallel Optimizations that maximizes on-chip data reuse, utilizes parallel resources, and reduces bandwidth overhead; and (3) a Decision Module featuring a lightweight analytical performance model to select the optimal strategy based on matrix shapes and hardware profiles. Extensive evaluation is conducted on LLM workloads across GPU (H20, A100) and CPU (ARM, x86) architectures with multiple data types. FalconGEMM succeeds in delivering peak breaking performance and outperforms GEMM libraries (e.g., cuBLAS, CUTLASS, Intel MKL, etc) by 7.59%-17.85% and LCMA competitors like AlphaTensor by 12.41%-55.61%. Our framework makes the theoretical promise of LCMAs practical for production deployment across the heterogeneous landscape of modern hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《FalconGEMM: Surpassing Hardware Peaks with Lower-Complexity Matrix Multiplication》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
矩阵乘法（GEMM）是深度学习和高性能计算的核心。随着模型规模指数级增长，标准 GEMM 的 $O(N^3)$ 复杂度成为瓶颈。虽然理论上存在低复杂度矩阵乘法算法（LCMA，如 Strassen 算法或 AlphaTensor 发现的算法），但在实际生产中应用面临三大挑战：
*   **缺乏跨平台可移植性**：LCMA 涉及复杂的数据依赖和线性组合，手动为不同硬件（Tensor Cores, AVX, NEON）优化成本极高。
*   **内存开销巨大**：LCMA 需要生成大量中间张量，频繁读写显存（Off-chip memory）产生的带宽开销往往抵消了计算量减少带来的收益。
*   **决策困难**：LCMA 并非在所有矩阵形状下都比标准 GEMM 快，缺乏轻量级模型来预测性能拐点。
**FalconGEMM** 旨在通过自动化框架解决这些问题，使 LCMA 能够真正突破硬件物理峰值性能。

### 2. 方法论：核心思想与关键技术
FalconGEMM 由三个核心模块组成：
*   **部署模块（Deployment Module）**：
    *   **核心思想**：利用深度学习编译器（如 TVM、TileLang、Triton）生成 IR，将 LCMA 的逻辑与底层硬件解耦。
    *   **技术细节**：通过元编程将算法系数（Coefficient Tensors）编码为编译时常量，应用指令交织（Instruction Interleaving）和寄存器复用优化，实现百万级实现变体的自动化生成。
*   **执行模块（Execution Module）**：
    *   **组并行优化（Group-Parallel Optimization）**：将具有相同坐标的元素归为一组，由同一个计算单元处理。这样可以实现“算子融合”，在片上（On-chip）完成线性组合，避免中间结果写回显存。
    *   **细粒度调度**：引入“拆分组并行”（Split-Group Parallelism）解决负载不均衡，以及“缓存感知调度”（Cache-Aware Scheduling）通过重排计算顺序提高 L2 缓存命中率，防止因带宽压力触发硬件降频。
*   **决策模块（Decision Module）**：
    *   **分析模型**：基于硬件的算力（FLOPS）和带宽（$\beta$）参数，建立算术强度分析模型。
    *   **逻辑流程**：根据矩阵形状 $(M, N, K)$ 自动计算各阶段的理论耗时，在多个候选 LCMA 与标准 GEMM 库之间选择最优策略。

### 3. 实验设计
*   **硬件场景**：涵盖 NVIDIA GPU (H20, A100)、x86 CPU (Intel Xeon, AMD EPYC) 和 ARM CPU (Amazon M7g)。
*   **工作负载**：从主流大模型（DeepSeek-R1, Qwen3.5-397B, HunyuanVideo）中提取了 960 种线性层形状。
*   **数据类型**：支持 FP32, FP16, BF16 以及带块缩放（Block-wise scaling）的 FP8。
*   **Benchmark 与对比方法**：
    *   **标准库**：cuBLAS, CUTLASS, Intel MKL, OpenBLAS, Arm Compute Library (ACL)。
    *   **LCMA 竞争对手**：DeepMind 的 AlphaTensor（基于 JAX 实现）。

### 4. 资源与算力
*   **硬件型号**：NVIDIA H20 (96GB, HBM3), NVIDIA A100 (40GB, HBM2), Intel Xeon 8255C, AMD EPYC 9K84, ARM Neoverse-V1。
*   **软件栈**：CUDA 12.x, TileLang v0.1.8, Triton v3.6.0, TVM v0.23.0。
*   **训练/测试时长**：文中未明确提及具体的编译或搜索总时长，但强调了决策模块是“轻量级”的，且部署模块支持 Just-In-Time (JIT) 编译。

### 5. 实验数量与充分性
*   **实验规模**：进行了 960 组算子级形状测试，并在 3 个真实 LLM 模型上进行了端到端（End-to-End）推理评估。
*   **消融实验**：针对执行模块的四种优化路径（算法1基础版 -> 组并行 -> 拆分组 -> 缓存感知）做了详细的步骤验证。
*   **客观性**：实验采用了“有效 TFLOPS”（Effective TFLOPS）作为指标，即以标准 GEMM 的计算量除以实际运行时间。这种方法非常客观，因为它直接展示了算法改进带来的“等效速度提升”，即使该数值超过了硬件的物理理论峰值。

### 6. 主要结论与发现
*   **性能超越峰值**：FalconGEMM 在多种硬件上突破了物理峰值。相比最强的厂商库（如 cuBLAS/MKL），性能提升了 **7.59%–17.85%**。
*   **大幅领先同行**：相比 AlphaTensor，性能提升了 **12.41%–55.61%**，且在小矩阵下表现更稳定。
*   **端到端收益**：在 HunyuanVideo 等模型的 Prefill 阶段，FalconGEMM 作为 PyTorch 后端带来了 **11.46%–18.12%** 的平均性能增益。
*   **数值精度**：由于减少了中间结果的下采样（Downcasting）和显存读写，其数值误差比 AlphaTensor 低约 17.2%。

### 7. 优点
*   **实用性极强**：将 LCMA 从纯数学理论推向了跨平台、生产级的部署。
*   **优化深入底层**：不仅关注计算量减少，还深入处理了 GPU 调度中的“尾部效应”和“缓存抖动”等系统级问题。
*   **全栈覆盖**：从高层算法选择到底层代码生成，提供了一套完整的闭环方案。

### 8. 不足与局限
*   **FP8 限制**：虽然支持 FP8，但主要针对 Hopper 架构（H20），在旧硬件或其他架构上的 FP8 优化覆盖较少。
*   **编译开销**：虽然是自动化生成，但 JIT 编译在首次运行时可能存在冷启动延迟（文中未详细讨论此开销的量级）。
*   **算法库依赖**：目前的 LCMA 系数主要来源于 AlphaTensor 已发现的成果，框架本身不负责发现新的数学分解公式。

（完）
