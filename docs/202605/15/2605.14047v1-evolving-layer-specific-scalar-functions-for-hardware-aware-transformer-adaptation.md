---
title: Evolving Layer-Specific Scalar Functions for Hardware-Aware Transformer Adaptation
title_zh: 演化层特定标量函数以实现硬件感知的 Transformer 适配
authors: "Kieran Carrigg, Sigur de Vries, Amirhossein Sadough, Marcel van Gerven"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14047v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对边缘部署演化层特定标量函数的硬件感知框架
tldr: "针对视觉Transformer（ViT）在边缘设备上因层归一化（LN）导致的计算复杂度和内存瓶颈，本文提出一种硬件感知的演化框架。该框架利用遗传算法为不同层定制异构标量函数以替代LN，并结合训练后重对齐策略，无需从头训练即可在20个epoch内恢复84.25%的ImageNet准确率，显著提升了ViT在边缘端的部署效率。"
selection_source: fresh_fetch
motivation: 视觉Transformer中的层归一化操作在边缘设备上存在严重的计算瓶颈，且现有的简单替代方案难以兼顾性能与训练效率。
method: 采用遗传算法演化出特定于层的异构标量函数来模拟归一化行为，并引入训练后重对齐策略以避免昂贵的重训练。
result: "该方法捕获了91.6%的归一化方差，相比同质化基准大幅提升，并在极短的微调时间内实现了高精度恢复。"
conclusion: 通过消除全局归约瓶颈并优化内存流量，该研究为ViT在硬件受限环境下的高效部署提供了极具竞争力的解决方案。
---

## 摘要
视觉 Transformer (ViTs) 在具有挑战性的视觉任务中取得了最先进的性能，但其在边缘设备上的部署受限于层归一化 (layer normalization) 带来的计算复杂性和全局归约 (global reduction) 瓶颈。最近的方法尝试通过将归一化层替换为硬件友好的标量近似来绕过这一问题。然而，这些同质化的替换无法最优地拟合所有层的行为，且依赖于昂贵的模型重训练。在这项工作中，我们提出了一个高效的硬件感知框架，利用遗传规划 (GP) 直接从预训练权重中演化出异构的、层特定的标量函数。结合一种新颖的训练后重对齐策略，我们的方法完全消除了从头开始重训练模型的需求。我们演化出的表达式能够准确地近似目标归一化行为，捕获了 $91.6\%$ 的方差 ($R^2$)，而同质化基准仅为 $70.2\%$，这使得我们修改后的架构仅需 20 个 epoch 即可恢复 $84.25\%$ 的 Top-1 ImageNet-1K 准确率。通过在消除全局归约瓶颈的同时保持性能，我们的方法在算术复杂度和片外内存流量之间建立了极佳的权衡，消除了 ViTs 在边缘加速器上高效部署的主要障碍。

## Abstract
Vision Transformers (ViTs) achieve state-of-the-art performance on challenging vision tasks, but their deployment on edge devices is severely hindered by the computational complexity and global reduction bottleneck imposed by layer normalization. Recent methods attempt to bypass this by replacing normalization layers with hardware-friendly scalar approximations. However, these homogeneous replacements do not optimally fit to all layers' behaviour and rely on expensive model retraining. In this work, we propose a highly efficient, hardware-aware framework that utilizes genetic programming (GP) to evolve heterogeneous, layer-specific scalar functions directly from pre-trained weights. Coupled with a novel post-training re-alignment strategy, our approach eliminates the need to retrain models from scratch entirely. Our evolved expressions accurately approximate the target normalization behaviours, capturing $91.6\%$ of the variance ($R^2$) compared to only $70.2\%$ for homogeneous baselines, allowing our modified architecture to recover $84.25\%$ Top-1 ImageNet-1K accuracy in only 20 epochs. By preserving this performance while eliminating the global reduction bottleneck, our approach establishes a highly favourable trade-off between arithmetic complexity and off-chip memory traffic, removing a primary barrier to the efficient deployment of ViTs on edge accelerators.

---

## 论文详细总结（自动生成）

以下是对论文《Evolving Layer-Specific Scalar Functions for Hardware-Aware Transformer Adaptation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
Vision Transformer (ViT) 虽然性能卓越，但在边缘设备（如 FPGA、ASIC）上部署面临严重瓶颈。其核心障碍在于**层归一化（LayerNorm, LN）**。
*   **全局归约瓶颈**：LN 需要在特征维度计算均值和方差，这种跨特征的数据依赖性阻止了算子融合，导致频繁的片外内存读写（占执行时间的 40% 以上）。
*   **现有方案不足**：此前有研究尝试用同质化的标量函数（如 Dynamic Tanh）替代 LN，但忽略了网络不同深度归一化行为的差异（从线性到 S 型曲线的转变），且通常需要昂贵的从头训练（300+ epoch）。

### 2. 方法论：核心思想与关键技术
本文提出了一种硬件感知的适配框架，包含两个核心阶段：
*   **符号发现框架（基于遗传规划 GP）**：
    *   **映射提取**：从预训练的 ViT-B 模型中提取各层 LN 的输入输出映射关系。
    *   **异构演化**：利用遗传规划（Kozax 库）为 25 个归一化层分别演化出特定的数学表达式。算子集包括加、乘、tanh、sigmoid、取反和裁剪（clip）。
    *   **稳定性约束**：在适应度函数中引入“拉向零（pull-to-zero）”惩罚项，强制表达式在离群值处回归零，以模拟 LN 的正则化效果并防止训练时的梯度爆炸。
*   **高效重对齐策略（Post-training Re-alignment）**：
    *   将演化出的标量函数替换原 LN，并继承原有的仿射权重（weight）和偏置（bias）。
    *   通过极短的微调（仅 20 epoch）恢复模型性能，提供三种变体：仅优化仿射参数 (-A)、全参数微调 (-F) 和基于逻辑值的知识蒸馏 (-D)。

### 3. 实验设计
*   **数据集**：ImageNet-1K。
*   **基准模型（Benchmark）**：预训练的 ViT-B/16。
*   **对比方法**：
    *   标准 LayerNorm（LN）。
    *   Dynamic Tanh (DyT)：一种同质化的标量替代方案。
*   **评估指标**：Top-1/Top-5 准确率、函数拟合优度（$R^2$、MSE）、算术复杂度（FLOPs）和内存访问量（Bytes）。

### 4. 资源与算力
*   **硬件**：单张 NVIDIA A100 (80GB) GPU。
*   **训练时长**：每个 20 epoch 的微调任务耗时约 5-10 小时。
*   **总算力消耗**：包括超参数搜索、初步试验和最终实验，总计约 **1000 GPU 小时**。

### 5. 实验数量与充分性
*   **实验规模**：针对 25 个归一化层分别进行了演化；每种微调变体均进行了 5 次独立运行以确保统计稳健性。
*   **充分性**：实验覆盖了从函数拟合精度到下游分类任务性能，再到硬件复杂度的全方位对比。
*   **公平性**：为所有对比变体（GP 和 DyT）进行了独立的超参数网格搜索（学习率、权重衰减等），确保了对比的客观性。

### 6. 主要结论与发现
*   **拟合精度高**：GP 演化的异构函数捕获了 $91.6\%$ 的方差 ($R^2$)，远高于 DyT 的 $70.2\%$。
*   **性能恢复快**：最佳变体 GP-D 在仅 20 epoch 内恢复了 $84.25\%$ 的 Top-1 准确率，与标准 LN 的差距缩小至 $0.69\%$。
*   **硬件效率提升**：
    *   **内存**：通过消除全局归约，将归一化层的片外内存读取量降低了 **50%**。
    *   **计算**：GP 方案比 DyT 减少了 **21%** 的计算量，且 14/25 的层计算成本低于原始 LN。

### 7. 优点（亮点）
*   **异构性**：首次揭示并利用了 Transformer 不同层归一化行为的结构性差异。
*   **高效适配**：将硬件适配的时间成本从数百 epoch 压缩至 20 epoch，极具实用价值。
*   **硬件友好**：通过符号回归直接优化了数据流，恢复了片上流式处理能力。

### 8. 不足与局限
*   **数据泛化风险**：GP 演化仅基于 50,000 个激活点，可能无法完全覆盖极端分布。
*   **硬件成本度量**：目前主要以节点数和 FLOPs 作为复杂度指标，尚未在真实边缘芯片上测量端到端延迟。
*   **架构覆盖**：实验目前仅限于 ViT-B，尚未验证在 NLP 领域或其他 Transformer 变体（如 Swin）上的通用性。
*   **存储开销**：每层需要存储不同的指令序列，虽然开销较小，但增加了系统实现的复杂性。

（完）
