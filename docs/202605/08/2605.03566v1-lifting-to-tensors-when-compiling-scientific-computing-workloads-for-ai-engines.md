---
title: Lifting to tensors when compiling scientific computing workloads for AI Engines
title_zh: 在为 AI 引擎编译科学计算工作负载时提升至张量表示
authors: "Nick Brown, Gabriel Rodriguez-Canal"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03566v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 将科学计算工作负载映射到AI引擎和边缘计算的编译流水线
tldr: 本研究针对 AMD AI 引擎（AIE）在科学计算中编程门槛高的问题，提出了一种自动编译流水线。该方法通过将带有 OpenMP 注解的通用循环语义提升为高层张量表示，实现了从通用代码到 AIE 执行模型的有效映射。实验表明，该方法在简化开发复杂性的同时，使 NPU 在 float32 任务中达到与 CPU 相当的性能且能耗更低，且 CPU 与 NPU 协同工作可显著提升性能并降低功耗。
selection_source: fresh_fetch
motivation: 将现有的科学计算代码移植到 AMD AI 引擎（AIE）等专用架构需要极高的专业知识和时间成本。
method: 提出一种编译流水线，通过将 OpenMP 注解的循环语义提升为张量表示，捕捉高层意图并映射至 AIE 架构。
result: "在六个基准测试中，NPU 的 float32 性能与多核 CPU 相当但能耗更低，且双核协同可提升 40% 性能并降低 15% 能耗。"
conclusion: 该方法显著降低了针对 AIE 架构的编程复杂性，证明了张量提升技术在科学计算负载迁移中的高效性。
---

## 摘要
事实证明，FPGA 和 AMD 的 AI 引擎 (AIEs) 等专用架构具有为科学计算提供能效和性能优势的潜力。鉴于 AIE 已集成到 AMD 的 CPU 中，这成为一个具有吸引力的潜在途径，特别是在边缘执行或更好地利用本地受限计算资源时。然而，一个主要的挑战是使现有代码无需大规模修改即可在此架构上运行。简而言之，将代码移植到 AIE 的执行模型需要大量的专业知识和时间。在本文中，我们探索了一种编译流水线，用于将通用科学计算代码中的循环高效映射到 AIE。通过将应用程序的语义提升为张量，我们证明了这能够捕捉带有 OpenMP 注解的通用循环的意图，并且这种高级张量信息提供了在映射到 AIE 时非常有效的丰富语义。我们的方法仅需要 OpenMP 装饰的循环，显著降低了针对该架构时的代码复杂度。对于代表 AI 和科学计算的六个内核基准测试，使用我们的方法，NPU 在 float32 上的性能与多核 CPU 相当，且在所有情况下都降低了求解能耗。对于两个科学计算内核，同时在 CPU 和 NPU 上运行与仅在 CPU 上运行相比，性能提升高达 40%，能耗降低了 15%。

## Abstract
It has been demonstrated that specialised architectures, such as FPGAs and AMD's AI Engines (AIEs), have the potential to deliver energy and performance advantages for scientific computing. Given the integration of AIEs into AMD's CPUs, this is an interesting potential avenue especially when executing on the edge or making better use of local compute constrained resources. However, a major challenge is in enabling existing codes to run on this architecture without extensive modification. Put simply, it requires significant expertise and time to port codes to the AIE's execution model.   In this paper we explore a compilation pipeline for efficiently mapping loops in general purpose, scientific codes to AIEs. Lifting the semantics of an application into tensors, we demonstrate that this is able to capture the intention of general purpose loops annotated with OpenMP and such high-level tensor information provides a richness that is effective when mapping to the AIEs. Requiring only an OpenMP decorated loop, our approach significantly reduces code complexity when targeting the architecture. For six kernel benchmarks, representing AI and scientific computing, using our approach the NPU performs comparatively to the multicore CPU for float32, in all cases at reduced energy to solution. For two scientific computing kernels running across both the CPU and NPU together delivers up to a 40% improvement in performance and 15% reduction in energy usage compared to the CPU alone.

---

## 论文详细总结（自动生成）

这是一份关于论文《Lifting to tensors when compiling scientific computing workloads for AI Engines》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何降低将现有的科学计算代码（尤其是 Fortran 编写的代码）移植到 AMD AI 引擎（AIE/NPU）架构上的门槛。
*   **研究动机**：
    *   **硬件潜力**：AMD 将 AIE 集成到 Ryzen CPU 中形成 NPU，为边缘计算和本地科学计算提供了高能效的矢量计算资源。
    *   **编程困境**：目前 AIE 的编程模型极其复杂，开发者需要同时掌握 C++（编写内核）、Python（构建 IRON 图）和 XRT API（主机端交互），且需手动处理数据流和内存管理。
    *   **语言鸿沟**：科学计算约 60% 的代码使用 Fortran，而 AIE 现有工具链主要面向 C++/Python，缺乏对传统 HPC 语言和 OpenMP 标准的原生支持。

### 2. 方法论：基于张量提升的编译流水线
核心思想是利用 **MLIR（多级中间表示）** 框架，将低级的循环语义“提升”到高级的张量表示，从而利用张量丰富的语义信息进行自动化硬件映射。
*   **前端处理**：使用 Flang 编译器将带有 `!$omp target` 注解的 Fortran 循环转换为 MLIR 的 `hlfir`、`fir` 和 `omp` 方言。
*   **张量提升（Lifting to Tensors）**：
    *   将 OpenMP 循环转换为 `tensor` 和 `tosa`（张量算子集架构）方言。
    *   **关键优势**：张量表示具有“值语义（Value Semantics）”和“目的传递风格（DPS）”，能够清晰地描述数据依赖关系，避开了复杂的内存别名分析。
*   **分解与映射（Decomposition）**：
    *   利用张量依赖图，自动决定将计算任务在 NPU 的多个 AIE 磁贴（Tiles）之间进行空间分解（算子并行）或迭代分解（数据并行）。
*   **自动化后端生成**：
    *   将高级张量操作逐步降级（Lowering）为 `hlaie`（自定义高层 AIE 方言）和 AMD 的 `aie` 方言。
    *   **自动向量化**：通过“分块（Chunking）”技术自动生成适配 AIE 512位矢量单元的代码，并调用 AMD 的 `aie-translate` 生成带内联函数的 C++ 内核。

### 3. 实验设计
*   **硬件平台**：AMD Ryzen AI 8945HS CPU（8核 x86，集成 Hawk Point XDNA v1 NPU）。
*   **基准测试（Benchmarks）**：
    1.  **基础内核**：Softmax, ReLU, SAXPY, Dot Product, L2Norm, GEMM（涵盖 AI 和基础线性代数）。
    2.  **科学计算应用**：
        *   **PW Advection**：英国气象局 MONC 大气模型中的 2D 平流方案。
        *   **SWE**：NCAR 开发的浅水方程（Shallow Water Equation）微应用。
*   **对比方法**：
    *   **性能对比**：与手动编写的 AIE 代码（使用 IRON 和 C++）对比。
    *   **能效对比**：与 8 核 CPU 上运行的多线程 OpenMP 代码对比。
    *   **协同模式**：测试 CPU 与 NPU 同时运行的混合执行模式。

### 4. 资源与算力
*   **算力设备**：单台笔记本电脑（Ryzen AI 8945HS），使用了 NPU 中的 16 个 AIE 磁贴（4列 x 4行）。
*   **软件栈**：GCC 14, XRT 2025.1, LLVM/MLIR 20.1.7, AIE-MLIR 1.1.0。
*   **训练/编译时长**：文中未明确提及编译耗时，但由于是静态编译流程，通常在秒级或分钟级。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 个基础内核和 2 个实际科学计算应用进行了测试。
*   **重复性**：所有结果均为 10 次运行的平均值。
*   **充分性**：实验覆盖了性能（耗时）、代码复杂度（行数）和能效（焦耳）三个关键维度。对比了纯 CPU、纯 NPU 以及 CPU+NPU 混合模式，实验设计较为全面且客观，真实反映了自动化工具链与专家级手工优化之间的差距。

### 6. 主要结论与发现
*   **性能对等**：在 `float32` 任务中，自动化生成的 NPU 代码性能与 8 核 CPU 相当，且接近手工优化的 AIE 代码（GEMM 除外，因 AMD 对其有极深度的手工优化）。
*   **能效卓越**：在所有测试中，NPU 的能耗均显著低于 CPU。
*   **生产力极大提升**：代码行数从手工编写的数百行（需维护三种语言）减少到仅需几行 OpenMP 注解，显著降低了开发成本。
*   **协同增效**：CPU 和 NPU 协同工作可使科学计算内核性能提升高达 40%，同时比纯 CPU 运行节能 15%。

### 7. 优点
*   **无缝集成**：首次实现了从标准 Fortran/OpenMP 到 AMD NPU 的端到端自动编译，无需重写算法。
*   **抽象层级高**：通过“提升至张量”这一策略，巧妙地解决了底层硬件映射的复杂性，使编译器能更好地理解计算意图。
*   **混合执行**：展示了在消费级处理器上实现异构协同计算的可行性。

### 8. 不足与局限
*   **功能限制**：目前不支持 OpenMP 的原子操作（Atomic），遇到此类操作会退回到 CPU 执行。
*   **特定优化缺失**：对于像 GEMM 这样需要极度精细磁贴管理和双缓冲优化的内核，自动化生成的代码仍落后于专家级手工优化。
*   **硬件版本**：实验基于 XDNA v1 架构，对于更新的 v2 架构或其他厂商的 NPU 适配性尚待验证。
*   **数据类型**：主要关注 `float32`，而 AIE 在低精度（int8/bf16）下更具优势，未充分展示其在低精度下的潜力。

（完）
