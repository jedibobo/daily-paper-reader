---
title: "PoTAcc: A Pipeline for End-to-End Acceleration of Power-of-Two Quantized DNNs"
title_zh: PoTAcc：一种用于 2 的幂次量化深度神经网络端到端加速的流水线
authors: "Rappy Saha, Jude Haris, Nicolas Bohm Agostini, David Kaeli, José Cano"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06082v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在定制硬件加速器上实现量化DNN的端到端加速
tldr: PoTAcc是一个针对2的幂（PoT）量化深度神经网络的端到端加速流水线。针对通用边缘设备缺乏位移操作优化支持的问题，该研究提出了集成TensorFlow Lite的部署框架，并设计了基于位移处理单元（shift-PE）的FPGA硬件加速器。实验表明，该方案在CNN和Transformer模型上显著提升了推理速度并降低了功耗，为资源受限设备的PoT量化应用提供了完整工具链。
selection_source: fresh_fetch
motivation: 通用边缘处理器缺乏对PoT量化位移操作的优化支持，且现有推理框架难以在定制硬件上部署此类模型。
method: 开发了PoTAcc开源流水线，通过TFLite支持跨平台部署，并针对不同PoT量化策略设计了专用的位移处理单元硬件加速器。
result: "在FPGA平台上，该设计相比纯CPU执行实现了最高3.6倍的加速和78%的能耗降低，且适用于CNN和Transformer等多种架构。"
conclusion: PoTAcc有效解决了PoT量化模型在边缘设备上的部署与加速难题，显著提升了资源受限环境下的推理效率。
---

## 摘要
2 的幂次（PoT）量化显著减小了深度神经网络（DNN）的大小，并在推理过程中将乘法替换为位移操作。先前的研究表明，PoT 量化 DNN 在图像分类等任务中可以保持准确性；然而，它们在资源受限的边缘设备上的性能仍未得到充分了解。虽然通用边缘 CPU 和 GPU 不提供针对位移操作优化的后端，但定制硬件加速器可以通过实现专用的基于位移的处理单元（shift-PE）来更好地利用 PoT 量化。然而，由于现有推理框架的支持有限，在这些加速器上部署 PoT 量化模型具有挑战性。此外，不同 PoT 量化策略在完整推理过程中对硬件设计、性能和能效的影响尚未得到系统探索。为了应对这些挑战，我们提出了 PoTAcc，这是一个开源的端到端流水线，用于在资源受限的边缘设备上加速和评估 PoT 量化 DNN。PoTAcc 通过 TensorFlow Lite (TFLite) 在异构平台（包括纯 CPU 系统和带有定制加速器的 CPU-FPGA 混合系统）上实现了 PoT 量化模型的无缝准备和部署。我们为三种 PoT 量化方法设计了基于位移的处理单元（shift-PE）加速器，并在两个 FPGA 平台上实现了它们。我们评估了一系列模型（包括 CNN 和基于 Transformer 的架构）的准确性、性能、能效和资源利用率。结果表明，在 PYNQ-Z2 和 Kria 开发板上，与纯 CPU 执行相比，我们的 CPU 加速器设计为 PoT 量化 DNN 实现了高达 3.6 倍的加速和 78% 的能耗降低。代码将在 https://github.com/gicLAB/PoTAcc 公开发布。

## Abstract
Power-of-two (PoT) quantization significantly reduces the size of deep neural networks (DNNs) and replaces multiplications with bit-shift operations for inference. Prior work has shown that PoT-quantized DNNs can preserve accuracy for tasks such as image classification; however, their performance on resource-constrained edge devices remains insufficiently understood. While general-purpose edge CPUs and GPUs do not provide optimized backends for bit-shift operations, custom hardware accelerators can better exploit PoT quantization by implementing dedicated shift-based processing elements. However, deploying PoT-quantized models on such accelerators is challenging due to limited support in existing inference frameworks. In addition, the impact of different PoT quantization strategies on hardware design, performance, and energy efficiency during full inference has not been systematically explored.   To address these challenges, we propose PoTAcc, an open-source end-to-end pipeline for accelerating and evaluating PoT-quantized DNNs on resource-constrained edge devices. PoTAcc enables seamless preparation and deployment of PoT-quantized models via TensorFlow Lite (TFLite) across heterogeneous platforms, including CPU-only systems and hybrid CPU-FPGA systems with custom accelerators. We design shift-based processing element (shift-PE) accelerators for three PoT quantization methods and implement them on two FPGA platforms. We evaluate accuracy, performance, energy efficiency, and resource utilization across a range of models, including CNNs and Transformer-based architectures. Results show that our CPU-accelerator design achieves up to 3.6x speedup and 78% energy reduction compared to CPU-only execution for PoT-quantized DNNs on PYNQ-Z2 and Kria boards. The code will be publicly released at https://github.com/gicLAB/PoTAcc

---

## 论文详细总结（自动生成）

### PoTAcc：一种用于 2 的幂次量化深度神经网络端到端加速的流水线

#### 1. 论文的核心问题与整体含义
*   **研究背景**：2 的幂次（Power-of-Two, PoT）量化通过将深度神经网络（DNN）中的乘法操作替换为位移（bit-shift）操作，理论上能显著降低计算复杂度和功耗。
*   **核心问题**：尽管 PoT 量化在算法层面表现优异，但在边缘设备上落地面临两大挑战：
    1.  **硬件支持缺失**：通用边缘 CPU 和 GPU 缺乏针对位移操作优化的后端，无法发挥 PoT 的优势。
    2.  **部署工具链断裂**：现有的推理框架（如 TensorFlow Lite）不支持将 PoT 模型无缝部署到定制的硬件加速器上。
*   **整体含义**：本文提出了 **PoTAcc**，这是一个开源的端到端流水线，涵盖了从模型量化、软件框架集成到定制 FPGA 硬件加速的全过程，旨在解决 PoT 量化模型在资源受限边缘设备上的部署与加速难题。

#### 2. 论文提出的方法论
PoTAcc 的核心思想是**软硬件协同设计**，主要包含以下三个关键部分：
*   **量化策略支持**：支持三种 PoT 变体：
    *   **标准 PoT**：权重表示为 $2^k$。
    *   **加法 PoT (APoT)**：权重表示为多个 PoT 项的和，提高精度。
    *   **对数 PoT (LogPoT)**：在对数域进行量化。
*   **软件框架（TFLite 集成）**：
    *   开发了自定义的 **TFLite Delegate**，能够识别 PoT 算子并将其从 CPU 卸载（Offload）到 FPGA 加速器。
    *   实现了模型转换工具，将量化后的权重打包成适合硬件读取的格式。
*   **硬件架构（Shift-PE 加速器）**：
    *   设计了基于位移的处理单元（**shift-PE**），直接在硬件电路中实现位移和加法，完全替代昂贵的乘法器（DSP）。
    *   针对不同的 PoT 策略设计了可配置的计算引擎，支持并行处理多个通道和特征图。

#### 3. 实验设计
*   **数据集**：CIFAR-10 和 ImageNet。
*   **模型架构**：涵盖了卷积神经网络（CNN：ResNet-18, MobileNetV2）和视觉 Transformer（ViT-Tiny），展示了方法的通用性。
*   **Benchmark 与对比方法**：
    *   **基准（Baseline）**：纯 CPU 执行（使用 TFLite 默认后端）。
    *   **对比维度**：推理延迟（Latency）、功耗（Power）、能效（Energy Efficiency）、资源利用率（LUTs, FFs, DSPs）以及模型准确率（Accuracy）。
*   **硬件平台**：Xilinx PYNQ-Z2（低端）和 Kria KV260（中端）FPGA 开发板。

#### 4. 资源与算力
*   **训练阶段**：论文未详细说明训练/量化这些模型所使用的具体 GPU 型号和时长，但提到使用了 PyTorch 和 TensorFlow 进行量化感知训练（QAT）。
*   **推理阶段（硬件资源）**：
    *   **PYNQ-Z2**：使用了约 13k-35k 的 LUTs，极少使用 DSP（仅用于非 PoT 部分或地址计算）。
    *   **Kria KV260**：利用了更多的并行度，资源消耗相应增加，以换取更高性能。
*   **算力体现**：实验重点在于 FPGA 上的逻辑资源利用率和吞吐量。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 3 种不同的量化方法（PoT, APoT, LogPoT）。
    *   测试了 3 种主流模型架构。
    *   在 2 个不同的 FPGA 硬件平台上进行了验证。
    *   进行了消融实验，分析了不同并行度（Tile size）对性能和资源的影响。
*   **充分性评价**：实验设计较为全面，不仅关注速度，还深入分析了能耗和硬件资源开销，能够客观反映 PoTAcc 在异构计算环境下的实际表现。

#### 6. 论文的主要结论与发现
*   **性能提升**：在 FPGA 加速器的支持下，PoTAcc 相比纯 CPU 执行实现了最高 **3.6 倍** 的加速。
*   **能效显著**：由于位移操作比乘法更省电，系统能耗最高降低了 **78%**。
*   **精度保持**：APoT 和 LogPoT 在大幅压缩模型的同时，能够保持与 8 位整数（INT8）量化相当的准确率。
*   **资源优化**：Shift-PE 设计极大地减少了对 DSP 资源的依赖，使得在逻辑资源有限的小型 FPGA 上部署大型模型成为可能。

#### 7. 优点
*   **端到端完整性**：不仅有硬件设计，还打通了与主流推理框架 TFLite 的连接，具有很强的实用价值。
*   **架构通用性**：证明了 PoT 量化不仅适用于 CNN，在 Transformer 模型上同样有效。
*   **开源贡献**：代码开源有助于社区进一步研究和优化边缘 AI 部署。

#### 8. 不足与局限
*   **硬件平台局限**：目前仅在 Xilinx FPGA 上验证，尚未探讨在 RISC-V（带位移指令扩展）或其他定制 ASIC 上的表现。
*   **算子覆盖率**：虽然支持核心的卷积和矩阵乘法，但对于一些特殊的激活函数或归一化层，可能仍需依赖 CPU 处理，存在通信开销。
*   **自动化程度**：论文未提及是否支持自动搜索最优的 PoT 量化参数（如位宽或项数），目前可能仍需人工调优。

（完）
