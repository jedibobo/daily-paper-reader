---
title: Towards Compute-Aware In-Switch Computing for LLMs Tensor-Parallelism on Multi-GPU Systems
title_zh: 面向多 GPU 系统中大语言模型张量并行的计算感知交换机内计算
authors: "Chen Zhang, Qijun Zhang, Zhuoshan Zhou, Yijia Diao, Haibo Wang, Zhe Zhou, Zhipeng Tu, Zhiyao Li, Guangyu Sun, Zhuoran Song, Zhigang Ji, Jingwen Leng, Minyi Guo"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05628v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 计算感知的交换机内计算以提高资源利用率
tldr: 针对大语言模型张量并行中集合通信与计算内核内存语义不匹配导致的资源利用率低和重叠受限问题，本文提出了首个感知计算的交换机内计算框架CAIS。CAIS通过扩展指令集架构与微架构、引入合并感知的线程块协调机制以及图级数据流优化器，实现了通信模式与计算需求的深度对齐。实验表明，CAIS在LLM训练中相比现有NVLS方案提升了1.38倍速度，显著优化了多GPU系统的并行效率。
selection_source: fresh_fetch
motivation: 现有的交换机内计算方案因通信模式与计算内核内存语义不匹配，导致计算与通信阶段孤立，限制了多GPU系统的资源利用率和重叠效率。
method: 提出了CAIS框架，通过指令集扩展、线程块协调机制和图级数据流优化器，使交换机内计算能够感知并对齐计算的内存语义需求。
result: 实验结果显示，CAIS在LLM训练任务中比SOTA的NVLS方案快1.38倍，比不使用NVLS的重叠方案T3快1.61倍。
conclusion: CAIS通过实现计算与通信的深度融合，有效消除了张量并行中的通信瓶颈，是加速大规模LLM训练和推理的有效方案。
---

## 摘要
大规模大语言模型（LLM）推理和训练中的张量并行（TP）引入了频繁的集合操作，这些操作在 GPU 间通信中占据主导地位。虽然以 NVLink SHARP (NVLS) 为代表的交换机内计算通过减少冗余数据传输加速了集合操作，但其以通信为中心的设计理念导致了通信模式与 LLM 计算内核的内存语义需求之间的不匹配。这种不匹配使计算和通信阶段相互隔离，导致多 GPU 系统中的资源利用不足以及重叠受限。为了解决这一局限性，我们提出了 CAIS，这是首个将通信模式与计算内存语义需求相对齐的计算感知交换机内计算框架。CAIS 包含三项核心技术：(1) 计算感知的指令集架构（ISA）和微架构扩展，以实现计算感知的交换机内计算；(2) 合并感知的线程块（TB）协调，以改善时间对齐从而实现高效的请求合并；(3) 图级数据流优化器，以实现紧密的跨内核重叠。在 LLM 工作负载上的评估表明，CAIS 相比于支持 NVLS 的最先进（SOTA）方案实现了 1.38 倍的平均端到端训练加速，相比于未利用 NVLS 的 SOTA 计算-通信重叠方案 T3 实现了 1.61 倍的加速，证明了其在加速多 GPU 系统张量并行方面的有效性。

## Abstract
Tensor parallelism (TP) in large-scale LLM inference and training introduces frequent collective operations that dominate inter-GPU communication. While in-switch computing, exemplified by NVLink SHARP (NVLS), accelerates collective operations by reducing redundant data transfer, its communication-centric design philosophy introduces the mismatch between its communication mode and the memory semantic requirement of LLM's computation kernel. Such a mismatch isolates the compute and communication phases, resulting in underutilized resources and limited overlap in multi-GPU systems. To address the limitation, we propose CAIS, the first Compute-Aware In-Switch computing framework that aligns communication modes with computation's memory semantics requirement. CAIS consists of three integral techniques: (1) compute-aware ISA and microarchitecture extension to enable compute-aware in-switch computing. (2) merging-aware TB (Thread Block) coordination to improve the temporal alignment for efficient request merging. (3) graph-level dataflow optimizer to achieve a tight cross-kernel overlap. Evaluations on LLM workloads show that CAIS achieves 1.38$\times$ average end-to-end training speedup over the SOTA NVLS-enabled solution, and 1.61$\times$ over T3, the SOTA compute-communicate overlap solutions but do not leverage NVLS, demonstrating its effectiveness in accelerating TP on multi-GPU systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Compute-Aware In-Switch Computing for LLMs Tensor-Parallelism on Multi-GPU Systems》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大规模大语言模型（LLM）的分布式训练和推理中，张量并行（TP）产生的集合通信（如 AllReduce, AllGather）占据了总延迟的 40%-60%。
*   **研究动机**：现有的交换机内计算技术（如 NVIDIA 的 NVLS）是“以通信为中心”的，其通信模式（推/拉模式）与计算内核（如 GEMM）的内存语义（读/写需求）存在严重不匹配。例如，AllGather 采用主动推送模式，而后续的 GEMM 需要按需读取，这种不匹配导致计算与通信无法在细粒度上重叠，迫使 GPU 在通信期间处于闲置状态。

### 2. 方法论：CAIS 框架
CAIS 提出了“计算感知”的交换机内计算，核心思想是让计算内核直接发出符合其语义的通信请求，由交换机动态合并。
*   **ISA 与微架构扩展**：
    *   引入新指令 `ld.cais`（合并加载）和 `red.cais`（合并归约），允许计算内核直接触发通信。
    *   在交换机（NVSwitch）中增加**合并单元（Merge Unit）**，包含 CAM 查找表和合并表，用于在传输过程中动态聚合来自不同 GPU 的相同地址请求。
*   **合并感知的线程块（TB）协调**：
    *   针对不同 GPU 上 TB 调度不一致导致的时间偏移（约 35μs），通过编译器将具有数据依赖的 TB 划分为“TB 组”。
    *   引入轻量级硬件同步机制（预启动和预访问同步），将请求到达的时间偏差降低至 3μs 以内，极大提高了交换机合并成功率。
*   **图级数据流优化器**：
    *   利用 TB 级的细粒度依赖关系打破内核边界，实现深度的内核融合。
    *   **非对称内核重叠**：针对加载（下行流量大）和归约（上行流量大）的流量特性，通过调度互补的内核同时执行，平衡双向链路带宽利用率。

### 3. 实验设计
*   **数据集与场景**：评估了 LLaMA-7B、Mega-GPT-4B 和 Mega-GPT-8B 三种模型，涵盖训练和推理（Prefill 阶段）任务。
*   **Benchmark 与对比方法**：
    *   **基础方案**：TP-NVLS, SP-NVLS（支持 NVLS 的基础张量并行）。
    *   **重叠方案**：CoCoNet, FuseLib, T3（最先进的计算-通信重叠方案）。
    *   **增强方案**：将上述重叠方案结合 NVLS 进行增强（如 T3-NVLS）。
    *   **局部性感知方案**：LADM。

### 4. 资源与算力
*   **模拟环境**：使用周期精确模拟器 Accel-Sim 扩展了 Hopper 架构特性，并集成 BookSim2 模拟 NVSwitch 网络。
*   **硬件配置**：模拟了一个 8-GPU 系统，参数参考 NVIDIA H100（900 GB/s 带宽）。
*   **算力说明**：由于模拟器限制，实验采用了等比例缩减模型（如减少 SM 数量和矩阵维度），但通过验证实验证明了缩减模型与全尺寸模型在性能趋势上的一致性。

### 5. 实验数量与充分性
*   **实验规模**：进行了端到端性能测试、子层（Sub-layer）性能分析、消融实验（验证 TB 协调和数据流优化的有效性）、敏感性分析（针对交换机合并表大小）以及扩展性分析（从 8 GPU 扩展到 32 GPU）。
*   **充分性评价**：实验设计较为全面，不仅对比了多种 SOTA 方案，还通过硬件开销分析（TSMC 12nm 工艺下仅增加极小面积）证明了方案的可行性。通过与真实硬件（H100）上的 NCCL 性能对比验证了模拟器的准确性（误差仅 3.87%），实验结果客观公平。

### 6. 主要结论与发现
*   **性能提升**：CAIS 在训练任务中比 SOTA 的 NVLS 方案平均加速 **1.38x**，比不带 NVLS 的重叠方案 T3 加速 **1.61x**。
*   **资源利用**：通过 TB 协调，交换机合并表的需求量减少了 **87%**；通过数据流优化，链路带宽利用率从 62.4% 提升至 **90.2%**。
*   **扩展性**：在增加 GPU 数量时，CAIS 保持了良好的性能扩展性，且硬件开销不随 GPU 数量增加而线性增长。

### 7. 优点与亮点
*   **首创性**：首次提出了计算感知的交换机内计算框架，解决了通信原语与计算语义不匹配的根本问题。
*   **软硬协同**：结合了 ISA 扩展、硬件同步和编译器优化，实现了从指令级到图级的全栈优化。
*   **细粒度重叠**：实现了比传统方案更细粒度的 TB 级计算-通信重叠，显著提升了 GPU 利用率。

### 8. 不足与局限
*   **模拟局限**：所有结论均基于周期精确模拟器得出，尚未在真实硅片上验证。
*   **模型规模**：受限于模拟时长，无法直接运行万亿参数规模的完整模型，虽然有缩减验证，但超大规模下的长尾效应可能更复杂。
*   **应用限制**：该方案高度依赖于特定的 NVSwitch 架构修改，对于非交换机架构（如纯环形拓扑）的分布式系统不适用。

（完）
