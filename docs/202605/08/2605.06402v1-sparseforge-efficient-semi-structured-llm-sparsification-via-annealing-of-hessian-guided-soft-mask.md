---
title: "SparseForge: Efficient Semi-Structured LLM Sparsification via Annealing of Hessian-Guided Soft-Mask"
title_zh: SparseForge：通过 Hessian 引导的软掩码退火实现高效的半结构化大语言模型稀疏化
authors: "Liu Hanzuo, Chaofan Lin, Weixuan Sun, Yulong Wang, Key, Rayying, Mingyu Gao"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06402v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于LLM加速的硬件可执行半结构化稀疏
tldr: 4 稀疏度下达到甚至超过稠密模型的精度，显著提升了稀疏化的效率与效果。
selection_source: fresh_fetch
motivation: 现有的半结构化剪枝方法在保持模型精度时往往需要极高的重训练计算成本。
method: 提出一种结合 Hessian 信息与软掩码渐进退火的框架，通过直接优化稀疏掩码实现高效的稀疏恢复。
result: 4 稀疏度下超越稠密模型精度，效率远超现有 SOTA 方法。
conclusion: SparseForge 有效平衡了模型稀疏化的精度与训练效率，为大模型在硬件上的高效部署提供了实用方案。
---

## 摘要
半结构化稀疏性为利用原生硬件支持加速大语言模型（LLMs）提供了一条切实可行的路径，但由于强结构耦合，训练后半结构化剪枝通常会遭受严重的质量退化。现有方法依赖大规模稀疏重训练来恢复精度，导致了高昂的计算成本。我们提出了 SparseForge，这是一个训练后框架，它通过直接优化稀疏掩码而非增加重训练 token 的规模来提高恢复效率。SparseForge 将 Hessian 感知的显著性估计与软掩码向硬件可执行结构化稀疏性的渐进退火相结合，实现了稳定且高效的稀疏恢复。在 2:4 稀疏度下的 LLaMA-2-7B 上，SparseForge 仅使用 5B 个重训练 token 就达到了 57.27% 的平均零样本准确率，超过了稠密模型的 56.43%，并接近了使用 40B 个 token 的最先进方法所取得的 57.52% 的结果。SparseForge 在准确率与效率权衡方面的这种改进在不同模型系列中表现出了一致性。

## Abstract
Semi-structured sparsity provides a practical path to accelerate large language models (LLMs) with native hardware support, but post-training semi-structured pruning often suffers from substantial quality degradation due to strong structural coupling. Existing methods rely on large-scale sparse retraining to recover accuracy, resulting in high computational cost.   We propose SparseForge, a post-training framework that improves recovery efficiency by directly optimizing the sparsity mask rather than scaling up retraining tokens. SparseForge combines Hessian-aware importance estimation with progressive annealing of soft masks into hardware-executable structured sparsity, enabling stable and efficient sparse recovery. On LLaMA-2-7B under 2:4 sparsity, SparseForge achieves 57.27% average zero-shot accuracy with only $\textbf{5B}$ retraining tokens, surpassing the dense model's 56.43% accuracy and approaching the 57.52% result of a state-of-the-art method using $\textbf{40B}$ tokens. Such improvements on the accuracy-efficiency trade-off from SparseForge are shown to be consistent across model families.

---

## 论文详细总结（自动生成）

这是一份关于论文《SparseForge: Efficient Semi-Structured LLM Sparsification via Annealing of Hessian-Guided Soft-Mask》的结构化总结：

### 1. 研究动机与核心问题
*   **核心问题**：如何在保持大语言模型（LLM）性能的同时，实现高效的**半结构化稀疏化**（如 NVIDIA 硬件原生支持的 2:4 稀疏模式）。
*   **研究动机**：
    *   **硬件加速需求**：2:4 稀疏模式能提供实际的硬件加速，但现有的剪枝方法在强结构约束下会导致严重的精度下降。
    *   **效率瓶颈**：现有方法（如 CAST）依赖大规模的重训练（如 40B tokens）来恢复精度，计算成本极高。
    *   **核心假设**：稀疏恢复的瓶颈在于**掩码（Mask）的优化质量**，而非单纯的重训练规模。过早地确定离散掩码会限制模型的探索空间。

### 2. 方法论：SparseForge 框架
SparseForge 提出了一个“掩码退火”管道，将掩码视为连续可优化的变量，其核心思想包括：
*   **双轨重训练循环（Dual-track Retraining）**：在“加热阶段”，同时优化模型权重和连续的“软掩码”。权重通过标准梯度下降更新，而掩码则根据特定的学习规则定期更新。
*   **Hessian 引导的软掩码更新**：
    *   利用二阶 Hessian 信息（通过 Hutchinson 随机估计器估计对角线）来衡量权重的重要性。
    *   在每个约束组内（如 4 个元素一组），根据 Hessian 感知的得分生成“软门控”信号，避免了硬投影带来的不稳定性。
*   **渐进式淬火（Progressive Quenching）**：
    *   引入**二值偏好正则项（Binary-preference Regularizer）**，惩罚处于 0 和 1 之间的模糊掩码值。
    *   通过调节温度参数和结构混合因子，使软掩码在训练过程中平滑地向硬件可执行的二进制模式靠拢。
*   **辅助技术**：结合了稀疏低秩分支（SLoRB）和知识蒸馏，以进一步补偿剪枝带来的容量损失。

### 3. 实验设计
*   **模型覆盖**：GPT-2 (M/L/XL), OPT-2.7B, LLaMA-2-7B, Qwen3 (1.7B/8B/14B), 以及混合专家模型 DeepSeek-MoE (16B)。
*   **数据集**：重训练使用 Dolmino-mix-1124 或 C4 语料库；评估使用 WikiText-2（困惑度）和 7 项零样本任务（ARC, BoolQ, HellaSwag, OpenBookQA, RTE, WinoGrande, RACE, PIQA）。
*   **对比方法**：
    *   **训练后剪枝**：Wanda, SparseGPT。
    *   **重训练方法**：MaskLLM, AST, CAST（当前最强基准）。

### 4. 资源与算力
*   **硬件环境**：使用由 **32 台 NVIDIA L20A GPU** 组成的集群。
*   **训练时长**：对于 LLaMA-2-7B，使用 5B tokens 进行完整的 SparseForge 训练大约需要 **50 个 GPU 小时**。
*   **推理测试**：在 **NVIDIA H800 GPU** 上使用 TensorRT-LLM 测试了端到端推理加速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨模型家族的广泛验证（8 种不同架构/规模的模型）。
    *   针对 LLaMA-2-7B 的深度对比实验。
    *   **消融实验**：涵盖了重训练数据质量、token 预算、重要性准则（Hessian vs 模长）、SLoRB 的影响、教师模型规模及蒸馏的作用。
    *   **扩展性实验**：验证了在 Block-16 稀疏模式下的通用性。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了精度，还对比了 token 效率（SparseForge 用 5B tokens 达到了 CAST 用 40B tokens 的效果），并提供了实际的硬件加速数据（1.40x–1.44x 吞吐量提升），具有很强的说服力。

### 6. 主要结论与发现
*   **效率飞跃**：SparseForge 在 LLaMA-2-7B 上仅需 5B tokens 即可达到 57.27% 的准确率，超越了稠密模型（56.43%），且效率比 SOTA 方法 CAST 提升了约 **8 倍**。
*   **掩码质量至上**：实验证明，优化掩码的生成过程比单纯增加重训练数据量更能有效恢复稀疏模型的性能。
*   **通用性**：该方法在 Dense 和 MoE 架构上均表现稳健，且能扩展到 2:4 以外的其他结构化稀疏模式。

### 7. 优点与亮点
*   **掩码退火机制**：巧妙地解决了“软掩码探索”与“硬掩码部署”之间的矛盾，使模型在训练初期能灵活调整结构。
*   **Hessian 感知**：引入二阶信息处理组内竞争，比传统的基于模长的剪枝更精准。
*   **极高的实用性**：直接面向硬件加速（2:4 模式），且大幅降低了稀疏化所需的计算资源，使其更易于被工业界采用。

### 8. 不足与局限
*   **数据敏感性**：消融实验显示，重训练数据的质量对最终结果影响显著（如 Qwen3 在 C4 数据上表现略逊），说明该方法对数据分布有一定的依赖。
*   **超参数调优**：退火过程涉及温度、正则化权重等多个超参数的调度，可能需要针对不同模型进行一定的微调。
*   **极端稀疏度**：论文主要集中在 50% 稀疏度（2:4），对于更高稀疏度下的表现探讨较少。

（完）
