---
title: "FalconGEMM: Surpassing Hardware Peaks with Lower-Complexity Matrix Multiplication"
title_zh: FalconGEMM：利用低复杂度矩阵乘法超越硬件峰值性能
authors: "Honglin Zhu, Jiaping Cao, Jiang Shao, Siyuan Feng, Qian Qiu, Peng Chen, Xu Zhang, Yixian Zhou, Man Lung Yiu, Guang Ji, Minwen Deng, Wenxi Zhu, Jintao Meng"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06057v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 超越硬件峰值并最大化片上数据复用
tldr: FalconGEMM是一个跨平台框架，旨在通过低复杂度矩阵乘法算法（LCMA）提升深度学习性能。它通过自动化的部署、优化和决策模块，在GPU和CPU等多种硬件上实现了超越硬件理论峰值的性能。该框架解决了LCMA在实际生产部署中的复杂性，在LLM工作负载下显著优于cuBLAS和AlphaTensor等现有库，为异构硬件环境下的高效矩阵运算提供了实用方案。
selection_source: fresh_fetch
motivation: 旨在解决低复杂度矩阵乘法算法在不同硬件上难以自动化部署、优化以及难以突破硬件理论性能峰值的问题。
method: 开发了包含自动化代码生成、组并行数据复用优化以及基于分析模型的策略决策三大核心模块的跨平台框架。
result: "在GPU和CPU上相比主流GEMM库性能提升达7.59%-17.85%，相比AlphaTensor等算法提升达12.41%-55.61%。"
conclusion: FalconGEMM成功将低复杂度矩阵乘法算法从理论推向生产实践，在异构硬件上实现了超越硬件峰值的性能表现。
---

## 摘要
突破峰值性能的矩阵乘法是提升深度学习（DL）性能的一种极具前景的技术，尤其是在大语言模型（LLM）的训练和推理中。我们提出了 FalconGEMM，这是一个跨平台框架，可在多种硬件上实现低复杂度矩阵乘法算法（LCMA）的自动化部署、优化和选择。该框架包含三项关键创新：(1) 部署模块，通过代码生成实现跨多种硬件和输入配置的可移植执行；(2) 执行模块，采用组并行优化（Group-Parallel Optimizations），最大化片上数据复用，充分利用并行资源并降低带宽开销；(3) 决策模块，包含一个轻量级分析性能模型，根据矩阵形状和硬件配置文件选择最优策略。我们在 GPU（H20、A100）和 CPU（ARM、x86）架构上，针对多种数据类型的 LLM 工作负载进行了广泛评估。FalconGEMM 成功实现了突破峰值的性能，相比 GEMM 库（如 cuBLAS、CUTLASS、Intel MKL 等）性能提升了 7.59%-17.85%，相比 AlphaTensor 等 LCMA 竞争对手提升了 12.41%-55.61%。我们的框架使 LCMA 的理论潜力在现代硬件的异构环境中具备了生产部署的实用性。

## Abstract
Peak breaking Matrix Multiplication is a promising technique to improve the performance of DL, especially in LLM training and inference. We present FalconGEMM, a cross-platform framework that automates the deployment, optimization, and selection of Lower-Complexity Matrix Multiplication Algorithms (LCMAs) across diverse hardware. There are three key innovations: (1) a Deployment Module that enables portable execution across various hardware and input configurations through code generation; (2) an Execution Module with Group-Parallel Optimizations that maximizes on-chip data reuse, utilizes parallel resources, and reduces bandwidth overhead; and (3) a Decision Module featuring a lightweight analytical performance model to select the optimal strategy based on matrix shapes and hardware profiles. Extensive evaluation is conducted on LLM workloads across GPU (H20, A100) and CPU (ARM, x86) architectures with multiple data types. FalconGEMM succeeds in delivering peak breaking performance and outperforms GEMM libraries (e.g., cuBLAS, CUTLASS, Intel MKL, etc) by 7.59%-17.85% and LCMA competitors like AlphaTensor by 12.41%-55.61%. Our framework makes the theoretical promise of LCMAs practical for production deployment across the heterogeneous landscape of modern hardware.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **FalconGEMM** 的跨平台框架，旨在通过低复杂度矩阵乘法算法（LCMA）在实际生产中超越硬件的理论性能峰值。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：通用矩阵乘法（GEMM）是深度学习的核心，标准算法复杂度为 $O(N^3)$。虽然存在如 Strassen 等低复杂度算法（LCMA），但在实际应用中面临三大挑战：
    1.  **缺乏跨平台可移植性**：不同硬件架构（GPU、CPU）和数据类型（FP8、BF16等）需要高度定制的优化。
    2.  **内存开销巨大**：LCMA 引入的中间张量会导致大量的离片（off-chip）内存读写，抵消了计算量减少带来的收益。
    3.  **决策困难**：LCMA 并非在所有矩阵形状下都比标准 GEMM 快，缺乏轻量级的模型来选择最优策略。
*   **整体含义**：FalconGEMM 通过自动化手段将 LCMA 从理论数学公式转化为可在现代异构硬件上稳定运行的高性能库，实现了“超越硬件峰值”的实际加速。

### 2. 方法论：核心思想与关键技术
FalconGEMM 由三个核心模块组成：
*   **部署模块（Deployment Module）**：
    *   利用深度学习编译器（如 TVM、TileLang、Triton）生成代码，屏蔽硬件差异。
    *   采用元编程技术将算法系数编码为编译时常量，并进行寄存器复用和指令交织优化。
*   **执行模块（Execution Module）**：
    *   **组并行优化（Group-Parallel Optimization）**：将具有相同坐标的元素归为一组，由同一个计算单元处理，从而实现 GEMM 阶段与线性组合阶段的融合，大幅减少内存流量。
    *   **拆分组并行（Split-Group Parallelism）**：利用持久化内核（Persistent Kernels）解决粗粒度并行导致的负载不均衡。
    *   **缓存感知调度（Cache-Aware Scheduling）**：通过重排计算顺序提高 L2 缓存命中率，防止因内存压力触发硬件降频。
*   **决策模块（Decision Module）**：
    *   建立轻量级分析模型，基于算术强度（Arithmetic Intensity）对比标准 GEMM 和各类 LCMA 的理论耗时。
    *   自动判断是否使用 LCMA 以及选择哪种具体算法（如 $R=7$ 的 Strassen 或 $R=49$ 的两级递归算法）。

### 3. 实验设计
*   **硬件平台**：涵盖 NVIDIA H20 (Hopper)、A100 (Ampere)、Intel Xeon (x86)、AMD EPYC (x86) 和 ARM Neoverse-V1。
*   **工作负载**：从 DeepSeek-R1、Qwen3.5 和 HunyuanVideo 三个大模型中提取了 960 种线性层形状。
*   **对比基准（Benchmark）**：
    *   **标准库**：cuBLAS、CUTLASS、Intel MKL、OpenBLAS、ARM Compute Library (ACL)。
    *   **LCMA 竞争对手**：DeepMind 的 AlphaTensor（基于 JAX 实现）。
*   **数据类型**：支持 FP32、BF16、FP16，以及针对 Hopper 架构的 FP8（带块级缩放）。

### 4. 资源与算力
*   **硬件型号**：明确列出了 H20、A100、Intel Xeon 8255C、AMD EPYC 9K84 和 ARM M7g 实例。
*   **算力消耗**：文中未明确给出总的训练或调优时长（GPU-hours），但提到其采用即时编译（JIT）技术，且决策模型是轻量级的分析模型，不需要大规模预训练或搜索。

### 5. 实验数量与充分性
*   **实验规模**：进行了 960 组算子级形状测试，并在 3 个主流 LLM 上进行了端到端推理评估。
*   **充分性**：实验覆盖了从移动端（ARM）到数据中心（H20/A100）的异构环境，涵盖了多种精度（包括最新的 FP8），并进行了消融实验（Step-wise Evaluation）来验证执行模块中各项优化的有效性。实验设计客观、对比公平（使用了 Effective TFLOPS 作为统一衡量标准）。

### 6. 主要结论与发现
*   **性能超越峰值**：FalconGEMM 在多个平台上突破了硬件理论峰值。相比厂商优化库提升了 **7.59%–17.85%**。
*   **大幅领先对手**：相比 AlphaTensor，性能提升达 **12.41%–55.61%**。
*   **端到端收益**：在 DeepSeek-R1 等模型的 Prefill 阶段，实现了 **11.46%–18.12%** 的平均性能提升。
*   **数值稳定性**：由于减少了中间结果的精度损失（在寄存器中完成融合计算），其数值误差比 AlphaTensor 低约 17.2%。

### 7. 优点
*   **实用性强**：首次将 LCMA 成功应用于 FP8 精度和复杂的 LLM 生产环境。
*   **全栈优化**：不仅关注算法减少的计算量，还深入解决了底层硬件的缓存、带宽和并行粒度问题。
*   **自动化程度高**：通过代码生成和分析模型，用户无需手动编写复杂的 LCMA 内核。

### 8. 不足与局限
*   **算术强度依赖**：对于算术强度极低（AI < 200）的矩阵，LCMA 无法提供收益，必须回退到标准 GEMM。
*   **内存占用**：虽然优化了带宽，但 LCMA 仍需额外的中间存储空间（尽管文中提到通过融合减少了部分需求）。
*   **数值精度风险**：尽管比 AlphaTensor 稳定，但 LCMA 本质上在数值稳定性上仍略逊于标准 GEMM，对于某些对精度极其敏感的科学计算任务可能仍需谨慎。

（完）
