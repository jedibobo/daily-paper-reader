---
title: LLM-Driven Design Space Exploration of FPGA-based Accelerators
title_zh: LLM驱动的基于FPGA加速器的设计空间探索
authors: "Vinamra Sharma, Xingjian Fu, Jude Haris, José Cano"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05920v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 使用大语言模型自动化FPGA加速器的软硬件协同设计
tldr: 针对现代AI工作负载的FPGA加速器设计空间极其复杂，传统SECDA方法仍需大量人工干预。本文提出SECDA-DSE框架，将大语言模型（LLM）集成到SECDA生态中，通过检索增强生成（RAG）和思维链（CoT）引导的推理，结合反馈循环实现自动化的设计空间探索。实验证明该框架能生成符合时序和资源约束的加速器设计，显著提升了FPGA硬件设计的自动化水平。
selection_source: fresh_fetch
motivation: 传统的FPGA加速器设计空间探索过程耗时且依赖专家经验，难以高效应对复杂的硬件参数和数据流策略优化。
method: 提出SECDA-DSE框架，利用LLM结合RAG、CoT技术及反馈强化微调机制，自动化生成并优化加速器配置。
result: 在Zynq-7000 FPGA上的高层次综合评估表明，该框架生成的加速器设计能够满足时序和资源约束。
conclusion: 将LLM引入FPGA设计流程可有效自动化设计空间探索，降低硬件开发门槛并提高设计效率。
---

## 摘要
为现代人工智能工作负载设计基于现场可编程门阵列（FPGA）的加速器，需要探索包含架构参数、数据流策略和存储层级的庞大且复杂的硬件设计空间，这使得该过程既耗时又耗费资源。虽然 SECDA 方法论通过 SystemC 仿真和 FPGA 执行实现了加速器的快速软硬件协同设计，但确定最优加速器配置仍需要大量的人工努力和领域专业知识。本研究提出了 SECDA-DSE，这是一个将大语言模型（LLMs）集成到 SECDA 生态系统中的框架，包含围绕 SECDA 构建的工具，旨在自动化基于 FPGA 加速器的设计空间探索（DSE）。SECDA-DSE 结合了一个用于生成加速器配置的结构化 DSE 探索器（DSE Explorer），以及一个利用检索增强生成和思维链提示进行推理引导探索的 LLM 栈（LLM Stack），同时还包含一个支持强化微调以实现持续改进的反馈循环。我们通过对生成的加速器设计进行初步的高级综合评估，证明了 SECDA-DSE 的可行性，该设计在 Zynq-7000 FPGA 上满足了综合时序和资源约束。

## Abstract
Designing field-programmable gate array (FPGA)-based accelerators for modern artificial intelligence workloads requires navigating a large and complex hardware design space encompassing architectural parameters, dataflow strategies, and memory hierarchies, making the process time-consuming and resource-intensive. While the SECDA methodology enables rapid hardware-software co-design of accelerators through SystemC simulation and FPGA execution, identifying optimal accelerator configurations still requires substantial manual effort and domain expertise. This work presents SECDA-DSE, a framework that integrates Large Language Models (LLMs) into the SECDA ecosystem, comprising tools built around SECDA to automate the design space exploration (DSE) of FPGA-based accelerators. SECDA-DSE combines a structured DSE Explorer for generating accelerator configurations with an LLM Stack that performs reasoning-guided exploration using retrieval-augmented generation and chain-of-thought prompting, alongside a feedback loop that enables reinforced fine-tuning for continuous improvement. We demonstrate the feasibility of SECDA-DSE through an initial high-level synthesis based evaluation of a generated accelerator design that meets synthesis timing and resource constraints on an Zynq-7000 FPGA.

---

## 论文详细总结（自动生成）

以下是对论文《LLM-Driven Design Space Exploration of FPGA-based Accelerators》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：为现代AI工作负载设计FPGA加速器涉及极其复杂的硬件设计空间（包括架构参数、数据流策略、存储层级等）。
*   **研究动机**：
    *   **人工依赖性强**：现有的软硬件协同设计方法（如SECDA）虽然缩短了开发周期，但寻找最优配置仍高度依赖领域专家的经验和繁琐的手动尝试。
    *   **迭代成本高**：每次设计迭代都需要昂贵的高级综合（HLS）和评估过程，导致探索效率低下。
    *   **自动化需求**：利用大语言模型（LLM）的推理能力来自动化设计空间探索（DSE），降低硬件设计门槛并加速优化过程。

### 2. 方法论：SECDA-DSE 框架
SECDA-DSE 框架通过将 LLM 集成到现有的 SECDA 生态系统中，实现了自动化的迭代优化。其核心由两个主要组件构成：

*   **DSE 探索器 (DSE Explorer)**：
    *   **输入**：目标AI工作负载、目标FPGA设备参数、架构指令（如分块策略、计算单元维度）。
    *   **功能**：根据指令生成符合 SECDA 规范的硬件配置模板，并管理设计运行文件夹（包含源码、RTL代码等）。
*   **LLM 栈 (LLM Stack)**：
    *   **检索增强生成 (RAG)**：通过向量数据库索引 SECDA-TFLite 代码库，为 LLM 提供必要的上下文和 API 调用知识，避免超出 Token 限制。
    *   **思维链 (CoT) 提示**：引导 LLM 进行多步推理，分析架构权衡，而非直接生成结果。
    *   **反馈与微调 (LoRA)**：收集仿真和硬件执行的性能数据（延迟、资源利用率等）作为“硬件数据点”，利用低秩自适应（LoRA）对模型进行参数高效微调，使其更倾向于生成高性能且可行的设计。
*   **反馈循环**：形成“生成配置 -> 仿真/综合评估 -> 结果反馈给 LLM -> 优化配置”的闭环。

### 3. 实验设计
*   **实验场景**：验证框架将自然语言规范转化为可综合硬件设计的能力。具体任务是设计一个**元素级向量乘法加速器**（Element-wise Vector Multiplication）。
*   **目标平台**：Xilinx Zynq-7000 FPGA (xc7z020-clg400-1)。
*   **评估工具**：Vivado HLS 2019.2。
*   **Benchmark/对比**：由于是初步研究（Preliminary Results），主要对比的是生成的硬件设计是否满足预设的时序约束（5.00 ns 周期）和资源限制，尚未进行大规模跨方法对比。

### 4. 资源与算力
*   **LLM 推理**：使用了开源框架 **Ollama** 进行本地设备上的模型推理，支持灵活切换不同的开源模型（如 Llama, Qwen）。
*   **算力细节**：文中**未明确说明**具体使用的 GPU 型号、数量或微调所需的具体时长。实验重点在于 HLS 综合结果而非 LLM 训练过程的算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：目前仅展示了一个基础的向量乘法算子生成实验。
*   **充分性评价**：
    *   **初步验证有效**：实验证明了从自然语言到 SECDA 兼容代码的转换路径是通畅的，且能通过 HLS 综合。
    *   **局限性**：实验数量较少，缺乏复杂 AI 模型（如完整的 CNN 或 Transformer 层）的端到端 DSE 验证。作为一篇研讨会（Workshop）论文，其目的是展示愿景和初步可行性，而非详尽的基准测试。

### 6. 主要结论与发现
*   **可行性确认**：LLM 能够成功理解自然语言描述的硬件需求，并生成符合 SECDA 框架、可通过 HLS 综合且满足时序约束的 SystemC 代码。
*   **自动化潜力**：通过 RAG 和 CoT，LLM 可以在不需要人类专家深度参与的情况下，根据硬件反馈数据点进行推理和设计改进。
*   **资源效率**：生成的向量乘法器在 Zynq-7000 上仅占用极少的资源（约 2% 的 LUT 和 1% 的 DSP），证明了生成设计的精简性。

### 7. 优点
*   **闭环优化**：引入了硬件性能反馈循环，使 LLM 能够从失败或低效的设计中学习。
*   **模块化设计**：通过 MCP（模型上下文协议）API 接口连接各组件，易于扩展和更换底层 LLM。
*   **降低门槛**：允许非硬件专家通过自然语言描述来启动 FPGA 加速器的设计过程。

### 8. 不足与局限
*   **工作负载简单**：目前的实验仅限于简单的向量运算，尚未证明其在处理复杂非线性层或大规模并行架构时的推理能力。
*   **搜索效率挑战**：即使是仿真评估，在大规模设计空间中依然耗时，LLM 引导的搜索策略是否优于传统的启发式算法（如遗传算法、贝叶斯优化）仍需进一步量化对比。
*   **局部最优风险**：LLM 可能会收敛到局部最优设计，或者在未探索区域产生次优建议。
*   **幻觉风险**：尽管有 RAG 约束，LLM 仍可能生成语法正确但逻辑错误或不可综合的硬件描述。

（完）
