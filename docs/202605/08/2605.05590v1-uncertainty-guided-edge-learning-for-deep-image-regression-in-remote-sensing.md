---
title: Uncertainty-Guided Edge Learning for Deep Image Regression in Remote Sensing
title_zh: 遥感中深度图像回归的不确定性引导边缘学习
authors: "Anh Vu Nguyen, Dino Sejdinovic, Tat-Jun Chin"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05590v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 遥感卫星上的边缘学习
tldr: 本研究针对遥感卫星等边缘设备上的深度图像回归任务，提出了不确定性引导边缘学习（UGEL）算法。由于边缘设备计算资源受限，传统不确定性估计方法难以适用。UGEL利用深度贝塔回归在单次前向传播中估计预测分布参数，从而准确识别高价值数据以加速模型训练收敛。该方法不仅降低了计算开销，还克服了高斯分布假设的局限性，为边缘端的模型持续优化提供了高效方案。
selection_source: fresh_fetch
motivation: 边缘设备计算资源有限，传统不确定性估计方法因计算成本高或分布假设过严而难以有效指导模型在线更新。
method: 提出UGEL算法，利用深度贝塔回归在单次前向传播中估计预测分布参数，实现高效的不确定性量化与数据优先级排序。
result: 实验结果显示，UGEL在训练收敛速度上显著优于现有的主动学习和半监督学习方法。
conclusion: 该研究证明了基于深度贝塔回归的不确定性引导方法能有效提升资源受限环境下边缘学习的效率和准确性。
---

## 摘要
边缘学习是指在部署于边缘平台的机器学习模型上进行训练，通常使用星载积累的新数据。边缘设备的计算限制不仅影响模型优化，还影响当前模型对未标记数据预测不确定性的计算，而这对于指导模型更新至关重要。在本文中，我们研究了在遥感卫星上执行深度图像回归背景下的边缘学习，其中星载计算机运行深度网络，从输入图像中回归标量 $y$，例如，$y$ 是表示云覆盖或土地利用的像素百分比。我们提出了一种不确定性引导的边缘学习（UGEL）算法，该算法能够准确地对数据进行优先级排序，以加速星载回归模型的训练收敛。UGEL 的基础是基于深度 Beta 回归（deep beta regression）的预测不确定性计算，其中深度网络用于估计 Beta 分布的参数，使得输入图像的目标值 $y$ 具有高似然性。与现有的不确定性估计方法相比，这些方法要么在边缘设备上成本过高（例如，每个样本需要多次前向传递），要么对预测分布有严格假设（例如，高斯分布），而深度 Beta 回归可以在单次前向传递中计算，并允许更通用的预测分布。结果表明，UGEL 比主动学习或半监督学习具有更快的边缘学习收敛速度。代码和模型可在 https://github.com/anh-vunguyen/UGEL 公开获取。

## Abstract
Edge learning refers to training machine learning models deployed on edge platforms, typically using new data accumulated onboard. The computational limitations on edge devices affect not only model optimisation, but also calculation of the predictive uncertainty of the current model on the unlabelled data, which is vital for informing model updating. In this paper, we investigate edge learning in the context of performing deep image regression on a remote sensing satellite, where a deep network is executed by an onboard computer to regress a scalar $y$ from an input image, e.g., $y$ is the percentage of pixels indicating cloud coverage or land use. We propose an uncertainty-guided edge learning (UGEL) algorithm that can accurately prioritise the data to speed up training convergence of the on-board regression model. Underpinning UGEL is the calculation of predictive uncertainty based on deep beta regression, where a deep network is used to estimate the parameters of a beta distribution for which the target $y$ for an input image has a high likelihood. Compared to established methods for uncertainty estimation that are either too costly on edge devices (e.g., require many forward passes per sample) or make strict assumptions on the predictive distribution (e.g., Gaussian), deep beta regression is computable in a single forward pass and allows more general predictive distributions. Results show that UGEL delivers faster-converging edge learning than active or semi-supervised learning. Code and models are publicly available at https://github.com/anh-vunguyen/UGEL.

---

## 论文详细总结（自动生成）

这篇论文针对遥感卫星等资源受限边缘设备上的深度图像回归任务，提出了一种高效的在线模型更新框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在遥感（RS）任务中，部署在卫星上的深度学习模型常面临“领域差异”（Domain Gap），如传感器漂移或环境变化，需要利用星载积累的新数据进行模型更新（边缘学习）。
*   **核心问题**：边缘设备（卫星）计算能力和电力极度受限。现有的模型更新方法（如主动学习或半监督学习）依赖不确定性估计来筛选数据，但传统方法（如蒙特卡洛 Dropout）计算成本过高，或假设分布（如高斯分布）不符合图像回归任务中目标值 $y \in [0, 1]$ 的有界特性。
*   **研究意义**：提出一种既能准确量化不确定性、又能在单次前向传播中完成计算的高效算法，以加速模型在边缘端的收敛。

### 2. 方法论
*   **核心思想**：提出**不确定性引导边缘学习（UGEL）**框架，结合了主动学习（AL）和半监督学习（SSL）。
*   **关键技术细节**：
    *   **深度 Beta 回归（DBR）**：模型不直接预测标量，而是预测 Beta 分布的参数（均值 $\mu$ 和精度 $\nu$）。Beta 分布天然适合 $[0, 1]$ 区间。
    *   **单次前向不确定性估计**：利用 Beta 分布的**微分熵**作为不确定性度量，仅需一次推理即可获得，远快于需要多次推理的贝叶斯方法。
    *   **UGEL 算法流程**：
        1.  **数据筛选**：从无标签数据中选出最不确定的子集 $T_U$（送往地面人工标注）和最确定的子集 $T_C$（进行星载伪标注）。
        2.  **双模型交叉监督**：维护一个主模型和一个孪生模型（架构相同但初始化不同），通过最小化两者在伪标注数据上的预测差异（RMSE 损失）来实现交叉监督。
        3.  **模型更新**：结合人工标注的监督损失和伪标注的半监督损失，定期重新训练模型。

### 3. 实验设计
*   **数据集**：
    *   **RSRC-L8**：来自 Landsat-8 的真实云覆盖图像。
    *   **RSRC-S2**：来自 Sentinel-2 的真实云覆盖图像。
    *   **RSLC**：来自 LandCover.ai 的土地覆盖百分比图像。
    *   **RSSC**：人工合成的云覆盖数据集，用于测试不同标签分布（双峰、偏态、均匀、高斯）下的鲁棒性。
*   **对比方法（Benchmarks）**：
    *   **主动学习**：随机采样（Random）、BALD（基于 MC Dropout）。
    *   **半监督学习**：CPS（交叉伪监督）。
    *   **主动半监督学习**：ASSL（Yao 等人的方法）。
*   **骨干网络**：ResNet18、MobileNetV3、MobileNetV4。

### 4. 资源与算力
*   **边缘硬件测试**：使用了 **Nvidia Jetson Orin NX** 模块（被公认为适用于卫星任务的硬件）进行运行时间评估。
*   **算力细节**：
    *   文中对比了不同不确定性估计方法的耗时：DBR 的计算耗时（约 68-74 秒）远低于 MC Dropout（约 495-514 秒）。
    *   模型训练在高性能计算（HPC）集群上模拟进行，但未详细说明具体 GPU 型号和训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：每组实验重复 **10 次**以获取均值和标准差，并进行了 **Wilcoxon 符号秩检验**（p-value）以验证统计显著性。
*   **消融实验**：对比了 DBR 与随机采样、MC Dropout、深度证据回归（DER）在 UGEL 框架下的表现。
*   **充分性评价**：实验覆盖了多种真实遥感场景、多种轻量级骨干网络以及合成数据的分布测试，实验设计客观、公平，对比基准具有代表性。

### 6. 主要结论与发现
*   **收敛速度**：UGEL 显著优于单纯的 AL 或 SSL 方法，能以更少的人工标注样本达到更低的 RMSE 误差。
*   **DBR 的优越性**：在处理有界回归任务（0 到 1 之间）时，基于 Beta 分布的 DBR 比基于高斯分布的 DER 更准确，且计算效率极高。
*   **数据选择策略**：同时利用“最不确定”的数据进行人工标注和“最确定”的数据进行自我监督，是加速边缘学习的关键。

### 7. 优点
*   **高效性**：DBR 实现了单次前向传播的不确定性估计，极大地节省了卫星的能耗和计算资源。
*   **数学严谨性**：使用 Beta 分布处理百分比回归问题，比强制使用高斯分布更符合物理逻辑。
*   **实用性**：框架考虑了卫星通信带宽限制（BU 设得很小），非常贴合实际航天应用场景。

### 8. 不足与局限
*   **计算开销**：虽然推理快，但 UGEL 使用了**孪生模型**架构，这在训练阶段会增加一倍的内存和计算负担。
*   **场景敏感性**：在物体多样性极高的 RSLC 数据集上，改进效果有所减弱，说明该方法在极度复杂的场景下仍有提升空间。
*   **应用限制**：目前仅针对标量回归任务，尚未扩展到更复杂的多目标回归或目标检测任务。

（完）
