---
title: "AscendOptimizer: Episodic Agent for Ascend NPU Operator Optimization"
title_zh: AscendOptimizer：用于昇腾 NPU 算子优化的情节式智能体
authors: "Jiehao Wu, Zixiao Huang, Wenhao Li, Chuyun Shen, Junjie Sheng, Xiangfeng Wang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23566v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于硬件反馈和分块搜索的NPU算子优化
tldr: 针对华为昇腾NPU算子优化中参考资料稀缺及主机/内核代码高度耦合的挑战，本文提出AscendOptimizer智能体。该工具通过闭环反馈将执行转化为经验：在主机侧利用演化搜索确定最优分块与数据传输配置；在内核侧通过逆向合成“由差到优”的轨迹挖掘优化模式，并构建经验库引导代码重写。实验表明，该方法在127个算子上实现了1.19倍的几何平均加速，显著提升了算子性能。
selection_source: fresh_fetch
motivation: 华为昇腾NPU算子优化面临参考资料匮乏以及主机侧分块与核函数调度高度耦合的知识瓶颈。
method: 提出AscendOptimizer智能体，通过主机侧演化搜索优化分块配置，并结合核函数侧的优化模式挖掘与经验库引导重写。
result: 在127个算子测试中实现1.19倍几何平均加速，近半数算子性能超越开源基准。
conclusion: 该研究证明了通过闭环反馈和经验蒸馏可以有效解决特定硬件架构下的算子自动优化难题。
---

## 摘要
华为昇腾神经处理单元（NPU）上的 AscendC 算子优化面临双重知识瓶颈：与 CUDA 生态系统不同，可供学习的公开参考实现较少；且性能取决于一个耦合的两部分产物——负责编排数据移动的主机端分块（tiling）程序，以及负责调度和流水线化指令的核函数程序。我们提出了 AscendOptimizer，这是一个情节式智能体，它通过将执行转化为经验来引导这些缺失的专业知识。在主机端，AscendOptimizer 执行带性能分析循环（profiling-in-the-loop）的进化搜索，直接根据硬件反馈发现有效且高性能的分块与数据移动配置。在核函数端，它通过回溯已优化的核函数来挖掘可迁移的优化基元（motifs）——系统地对它们进行去优化以合成具有指导意义的“从差到优”轨迹——并将这些基元提炼到可检索的经验库中，用于指导重写。通过在闭环中交替进行主机端调优和核函数重写，AscendOptimizer 稳步扩展了可行性并降低了延迟。在包含 127 个真实 AscendC 算子的基准测试中，AscendOptimizer 相比开源基准实现了 1.19 倍的几何平均加速，其中 49.61% 的算子性能优于参考实现，表现优于强大的智能体和搜索基准。

## Abstract
AscendC (Ascend C) operator optimization on Huawei Ascend neural processing units (NPUs) faces a two-fold knowledge bottleneck: unlike the CUDA ecosystem, there are few public reference implementations to learn from, and performance hinges on a coupled two-part artifact - a host-side tiling program that orchestrates data movement and a kernel program that schedules and pipelines instructions. We present AscendOptimizer, an episodic agent that bootstraps this missing expertise by turning execution into experience. On the host side, AscendOptimizer performs profiling-in-the-loop evolutionary search to discover valid and high-performing tiling and data-movement configurations directly from hardware feedback. On the kernel side, it mines transferable optimization motifs by rewinding optimized kernels - systematically de-optimizing them to synthesize instructive "bad-to-good" trajectories - and distills these motifs into a retrievable experience bank for guided rewriting. By alternating host tuning and kernel rewriting in a closed loop, AscendOptimizer steadily expands feasibility and pushes latency down. On a benchmark of 127 real AscendC operators, AscendOptimizer achieves a 1.19x geometric-mean speedup over the open-source baseline, with 49.61% of operators outperforming their references, outperforming strong agent and search baselines.

---

## 论文详细总结（自动生成）

### AscendOptimizer：用于昇腾 NPU 算子优化的情节式智能体

#### 1. 核心问题与研究动机
*   **背景**：随着大模型规模增长，底层算子性能成为计算瓶颈。华为昇腾（Ascend）NPU 是重要替代方案，但其采用显式内存管理的达芬奇架构（Da Vinci），开发门槛高。
*   **核心痛点**：
    1.  **知识匮乏**：相比 CUDA 生态，AscendC 的开源参考代码极少，LLM 生成代码的成功率（Pass@1）不足 2.1%。
    2.  **高度耦合**：算子性能由主机端分块程序（Tiling，负责数据搬运）和设备端内核程序（Kernel，负责指令调度）共同决定，两者优化空间复杂且相互依赖。
*   **研究目标**：在缺乏专家经验和大规模训练数据的情况下，实现昇腾算子的自动化、高性能优化。

#### 2. 方法论
AscendOptimizer 采用**情节式智能体（Episodic Agent）**架构，通过“执行即经验”的思路，将优化过程分为两个交替进行的阶段：
*   **阶段 I：演化引导的程序搜索（针对 Tiling）**
    *   **核心思想**：将分块优化视为受限的程序搜索问题。
    *   **技术细节**：利用 LLM 作为变异算子，对分块函数的参数和逻辑进行改写。引入**硬件在环（HIL）反馈**，将真实的 NPU 编译结果和执行延迟作为适应度函数，通过演化算法在不连续的搜索空间中逼近最优配置。
*   **阶段 II：基于优化回溯的经验引导（针对 Kernel）**
    *   **核心思想**：通过“逆向工程”从少量专家代码中提取可迁移的优化模式。
    *   **技术细节**：
        1.  **优化回溯（Rewind）**：系统性地对高性能种子内核进行“去优化”（如移除双缓冲、简化流水线），构造“差到优”的对比轨迹。
        2.  **经验库构建**：利用 LLM 提炼这些轨迹中的语义模式（如消除同步屏障、DMA 效率提升），形成可检索的经验库。
        3.  **RAG 引导重写**：在线优化时，诊断当前内核瓶颈，检索最相关的优化模式，引导 LLM 进行结构化重写。
*   **交替优化**：采用类似坐标下降法的策略，固定内核优化分块，固定分块优化内核，实现端到端性能提升。

#### 3. 实验设计
*   **数据集/场景**：构建了一个包含 **127 个真实 AscendC 算子**的基准测试集，涵盖加法、卷积、归一化、FlashAttention 等不同复杂度等级（Level 1-3）。
*   **基准（Benchmark）**：以华为官方开源的 `cann-ops` 算子库实现作为性能基准。
*   **对比方法**：
    *   **BoN@5 / BoN@40**：基于 LLM 的 Best-of-N 采样。
    *   **OpenEvolve**：一种先进的开源演化编程智能体。

#### 4. 资源与算力
*   **硬件环境**：实验在 **Huawei Ascend 910B4 NPU** 上运行。
*   **软件栈**：使用 CANN 8.3 软件栈。
*   **模型使用**：
    *   离线阶段（模板合成、经验构建）：使用 **GPT-4o**。
    *   在线优化循环（变异、重写）：使用 **DeepSeek-V3**。
*   **优化预算**：每个算子分配 40 次迭代的优化预算。

#### 5. 实验数量与充分性
*   **实验规模**：对 127 个算子进行了完整的优化测试，并按复杂度分层评估。
*   **消融实验**：对比了“仅阶段 I”、“仅阶段 II”与完整系统的表现，验证了两阶段的互补性。
*   **客观性**：实验基于真实硬件反馈而非模拟器，确保了结果的真实性。通过语义聚类分析证明了经验库不仅涵盖了官方文档内容，还挖掘出了未公开的底层技巧。

#### 6. 主要结论与发现
*   **性能提升**：AscendOptimizer 实现了 **1.19 倍的几何平均加速**。
*   **超越专家**：**49.61%** 的算子性能超过了官方专家编写的参考实现。
*   **复杂度正相关**：在最复杂的 Level 3 算子上，加速比达到 **1.81 倍**，证明了该方法处理复杂逻辑的能力。
*   **知识发现**：通过“回溯”机制，智能体能够自动发现如“精细化事件同步”和“标量指令消除”等文档之外的优化手段。

#### 7. 优点与亮点
*   **无需训练（Training-free）**：无需对大模型进行昂贵的微调，通过 RAG 和演化搜索即可适配特定硬件。
*   **全栈覆盖**：同时解决了主机侧分块和设备侧内核的耦合优化问题。
*   **自给自足**：在数据稀缺环境下，通过“自我回溯”产生高质量的优化经验，具有极强的冷启动能力。

#### 8. 不足与局限
*   **硬件在环开销**：依赖真实硬件反馈意味着优化过程受限于编译和运行速度，时间成本较高。
*   **动态形状支持**：目前主要针对固定 Shape 优化，对动态 Shape 的泛化能力有待进一步验证。
*   **噪声敏感性**：对于执行时间极短的小算子，硬件计时的随机噪声可能干扰优化方向。
*   **搜索深度**：对于需要彻底改变算法逻辑（如从 O(n^2) 变为 O(n log n)）的极高难度优化，目前仍存在挑战。

（完）
