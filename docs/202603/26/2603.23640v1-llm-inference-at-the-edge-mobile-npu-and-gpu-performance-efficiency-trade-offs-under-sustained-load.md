---
title: "LLM Inference at the Edge: Mobile, NPU, and GPU Performance Efficiency Trade-offs Under Sustained Load"
title_zh: 边缘端大语言模型推理：持续负载下移动端、NPU 与 GPU 的性能与效率权衡
authors: "Pranay Tummalapalli, Sahil Arayakandy, Ritam Pal, Kautuk Kundan"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23640v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在热约束下对移动端 NPU 和边缘设备的 LLM 推理进行基准测试
tldr: 本研究评估了在边缘设备上持续运行大语言模型（LLM）的性能与效率权衡。通过在iPhone 16 Pro、三星S24 Ultra、Hailo-10H NPU及RTX 4050 GPU上测试Qwen 2.5 1.5B模型，分析了吞吐量、功耗和热管理。研究发现，移动设备受散热限制严重，而专用NPU在低功耗下表现出极高的稳定性，为边缘侧AI部署提供了关键的硬件选择参考。
selection_source: fresh_fetch
motivation: 旨在探究在功耗、散热和内存受限的边缘设备上，持续运行大语言模型时的性能表现与效率权衡。
method: 在四种不同硬件平台上对4位量化的Qwen 2.5 1.5B模型进行20次连续推理测试，并监测吞吐量、延迟、功耗和热行为。
result: 移动设备因过热导致性能大幅下降，而RTX 4050受限于电池功率，Hailo-10H NPU则在极低功耗下实现了高稳定性的推理。
conclusion: 边缘侧LLM部署的主要瓶颈已从峰值算力转向热管理和内存带宽，专用NPU在能效比和稳定性方面具有显著优势。
---

## 摘要
在设备端部署大语言模型以实现全天候个人智能体，要求硬件在功耗、散热空间和内存受到严格限制的情况下提供持续的推理能力。我们在四个平台上对 Qwen 2.5 1.5B（4位量化）进行了基准测试：配备 Hailo-10H NPU 的树莓派 5、三星 Galaxy S24 Ultra、iPhone 16 Pro 以及笔记本电脑端的 NVIDIA RTX 4050 GPU。在每台设备上使用固定的 258-token 提示词进行 20 次热启动迭代，我们测量了吞吐量、延迟、功耗和热行为。对于移动平台，热管理取代峰值算力成为主要约束：iPhone 16 Pro 在两次迭代内损失了近一半的吞吐量，而 S24 Ultra 则遭遇了操作系统强制执行的 GPU 频率底线，导致推理完全终止。在专用硬件上，不同的约束占据主导地位：RTX 4050 受限于其电池功率上限，而 Hailo-10H 则受限于模块上的内存带宽。RTX 4050 在 34.1 W 功耗下维持 131.7 tok/s 的速度；Hailo-10H 在低于 2 W 的功耗下维持 6.9 tok/s，且方差接近于零，在吞吐量低 19 倍的情况下，其能量比例与 RTX 4050 相当。研究结果应被视为针对单一模型和提示词类型的平台级部署特性描述，反映了硬件与软件的综合表现，而非仅针对硬件能力的通用结论。

## Abstract
Deploying large language models on-device for always-on personal agents demands sustained inference from hardware tightly constrained in power, thermal envelope, and memory. We benchmark Qwen 2.5 1.5B (4-bit quantised) across four platforms: a Raspberry Pi 5 with Hailo-10H NPU, a Samsung Galaxy S24 Ultra, an iPhone 16 Pro, and a laptop NVIDIA RTX 4050 GPU. Using a fixed 258-token prompt over 20 warm-condition iterations per device, we measure throughput, latency, power, and thermal behaviour. For mobile platforms, thermal management supersedes peak compute as the primary constraint: the iPhone 16 Pro loses nearly half its throughput within two iterations, and the S24 Ultra suffers a hard OS-enforced GPU frequency floor that terminates inference entirely. On dedicated hardware, distinct constraints dominate: the RTX 4050 is bounded by its battery power ceiling, while the Hailo-10H is limited by on-module memory bandwidth. The RTX 4050 sustains 131.7 tok/s at 34.1 W; the Hailo-10H sustains 6.9 tok/s at under 2 W with near-zero variance, matching the RTX 4050 in energy proportionality at 19x lower throughput. Results should be interpreted as platform-level deployment characterisations for a single model and prompt type, reflecting hardware and software combined, rather than general claims about hardware capability alone.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLM Inference at the Edge: Mobile, NPU, and GPU Performance Efficiency Trade-offs Under Sustained Load》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 2B 参数以下的小型语言模型（SLM）性能提升，在边缘设备（手机、单板计算机、笔记本）上部署“全天候个人智能体”成为可能。然而，现有的基准测试多关注**峰值性能**或**单次推理延迟**，忽略了在**持续负载（Sustained Load）**下，设备受限于功耗、散热空间和内存带宽时的真实表现。本研究旨在揭示热节流（Thermal Throttling）如何影响边缘端 LLM 的推理稳定性，并探讨专用 NPU 相比传统移动 SoC 和 GPU 的优劣。

### 2. 论文提出的方法论
研究采用对比实验法，核心思想是在受控的“热启动”条件下，模拟真实智能体连续处理查询的场景。
*   **核心流程**：
    1.  **设备准备**：在 22°C 环境下静置，确保初始热平衡。
    2.  **模型加载**：加载 4-bit 量化的 Qwen 2.5 1.5B 模型，进行一次预热推理。
    3.  **连续迭代**：执行 20 次连续推理任务，每次迭代间隔 1 秒。
    4.  **多维监测**：同步记录每秒生成的 Token 数（TPS）、预填充时间、系统功耗（通过 INA219 传感器或 API）、设备温度及热状态（如 iOS 的 Thermal State）。
*   **关键技术细节**：针对不同平台使用最适配的推理框架（如 MLX 用于 iPhone，MLC-LLM 用于安卓，vLLM 用于 NVIDIA GPU，hailo-ollama 用于 NPU）。

### 3. 实验设计
*   **测试场景**：使用一个固定的 258-token 复杂提示词，要求模型生成长篇论文，以施加持续的解码压力。
*   **对比平台（Benchmark）**：
    *   **边缘 NPU**：树莓派 5 + Hailo-10H M.2 模块（40 TOPS）。
    *   **安卓旗舰**：三星 Galaxy S24 Ultra（骁龙 8 Gen 3）。
    *   **iOS 旗舰**：iPhone 16 Pro（A18 Pro）。
    *   **移动 GPU**：笔记本端 NVIDIA RTX 4050（受限于电池模式）。
*   **评估指标**：吞吐量（tok/s）、预填充延迟、每 Token 能耗（mJ/token）、温度变化曲线。

### 4. 资源与算力
本研究侧重于**推理性能**而非模型训练，因此未涉及大规模训练算力。
*   **硬件规格**：
    *   RTX 4050：6GB VRAM，测试时功耗限制在约 34W（电池模式）。
    *   Hailo-10H：功耗 < 5W，通过 PCIe 连接。
    *   手机端：均使用各自集成的 GPU 进行推理（iPhone 使用 Metal，S24 使用 OpenCL）。
*   **训练时长**：不适用（直接使用预训练量化模型）。

### 5. 实验数量与充分性
*   **实验规模**：每个平台进行 20 次迭代测试。
*   **充分性评价**：
    *   **客观性**：研究诚实地记录了失败案例（如 S24 Ultra 因过热在第 6 次迭代时被系统强制降频导致测试中断）。
    *   **公平性**：尽管尽量统一了模型和提示词，但由于各平台软件栈（vLLM vs MLX vs MLC）和量化格式（GGUF vs GPTQ 等）的差异，实验结果反映的是“硬件+软件”的综合部署表现，而非纯硬件理论性能。
    *   **局限性**：每种平台仅测试了一台设备，未考虑个体差异。

### 6. 主要结论与发现
*   **热管理是移动端的致命伤**：iPhone 16 Pro 在两次推理后即开始降速，最终吞吐量下降 44%；三星 S24 Ultra 则因热保护直接触发“频率底线”，导致推理无法持续。
*   **专用 NPU 的稳定性优势**：Hailo-10H 虽然绝对速度慢（6.9 tok/s），但表现出**近乎零方差**的稳定性，且功耗极低（< 2W），非常适合后台异步任务。
*   **能效比惊人一致**：尽管 RTX 4050 的吞吐量是 Hailo-10H 的 19 倍，但两者的“每 Token 能耗”非常接近（约 270-300 mJ），说明专用 NPU 在低功耗下实现了与高性能 GPU 相当的计算效率。
*   **瓶颈转移**：边缘端 LLM 推理的瓶颈已从“峰值算力（TOPS）”转向“散热能力”和“内存带宽”。

### 7. 优点
*   **实战导向**：不同于实验室的单次跑分，该研究模拟了“全天候智能体”的真实高频使用场景。
*   **独立性**：提供了 Hailo-10H NPU 的首个独立第三方基准测试数据，打破了仅有厂商宣传数据的现状。
*   **深度分析**：不仅记录数据，还分析了 iOS 和 Android 不同的热处理策略（优雅降级 vs 暴力关停）。

### 8. 不足与局限
*   **软件变量过多**：由于没有统一的跨平台推理引擎，框架差异（如 MLC-LLM 在安卓上的 OpenCL 优化不足）可能掩盖了硬件的真实潜力。
*   **量化不统一**：虽然都是 4-bit，但 GPTQ、GGUF 等格式的细微差别可能影响 Token 生成速度。
*   **测量限制**：由于系统权限限制，无法获取 iPhone 和安卓的精确每组件功耗，导致能效比对比不完整。
*   **应用范围**：仅测试了 Qwen 2.5 1.5B 一个模型，结论在更大参数模型上的适用性有待验证。

（完）
