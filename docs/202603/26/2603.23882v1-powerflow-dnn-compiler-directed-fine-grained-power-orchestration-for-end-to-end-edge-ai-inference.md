---
title: "PowerFlow-DNN: Compiler-Directed Fine-Grained Power Orchestration for End-to-End Edge AI Inference"
title_zh: PowerFlow-DNN：面向端到端边缘 AI 推理的编译器导向细粒度功耗编排
authors: "Paul Chen, Jeongeun Kim, Wenbo Zhu, Yuanhan Li, Shunyao Huang, Chenjie Weng, Christopher Torng"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23882v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 编译器指导的边缘AI细粒度功耗编排
tldr: 针对边缘AI推理中的能效挑战，本文提出PowerFlow-DNN框架。该框架通过编译器引导的细粒度电源编排（如DVFS和电源门控），解决了层间耦合导致的调度难题。它将截止日期约束下的推理建模为统一的层间电源调度问题，在巨大的搜索空间中自动寻找能耗最小的调度方案，显著提升了超低功耗加速器的能效。
selection_source: fresh_fetch
motivation: 传统的层级电源管理忽略了层间耦合和转换开销，且在复杂的组合调度空间中难以实现全局最优能效。
method: 提出一种编译器引导的框架，将端到端电源状态编排建模为受限的优化问题，利用结构化层级状态图进行高效求解。
result: "在TSMC 40nm工艺下，该方法比无电源编排的基准降低了高达37%的能耗，且结果接近整数线性规划的最优解。"
conclusion: PowerFlow-DNN证明了通过编译器层面的细粒度电源状态协同优化，可以在满足实时性要求的同时极大提升边缘AI系统的能效。
---

## 摘要
边缘 AI 系统通常在严格的能量和体积约束下运行，要求在有限的电池容量下实现极高的效率，且随着智能能力需求的提升，这些要求变得愈发苛刻。现有文献表明，包括 DVFS 和电源门控在内的细粒度功耗编排能够带来显著的能效收益，具有极大的开发潜力，但仍面临尚未解决的挑战。我们观察到，由于功耗控制决策的层间耦合，层级方法会产生意外的开销，且在有限电压轨和转换开销等实际约束下联合管理这些机制会导致组合调度空间呈爆炸式增长。针对这一问题，我们提出了 PowerFlow-DNN，这是一个用于超低功耗加速器中端到端功耗状态编排的编译器导向框架。通过将受截止时间约束的实时周期性推理构建为统一的层间功耗调度问题，我们的框架能够自动发现满足截止时间要求的最小能耗功耗状态调度方案，同时兼顾端到端的层间影响。我们在采用 TSMC 40nm 工艺的 DNN 加速器 VLSI 实现上评估了该框架。在代表性的边缘网络中，我们证明了 PowerFlow-DNN 在离散化建模下能够发现近乎最优的解，其能耗与精确的 ILP 理想值相比误差在 0.68% 以内；与不采用功耗编排的激进基准相比，能耗降低了高达 37%。尽管该框架需要在超过 10^160 种可能功耗状态分配的组合调度空间中进行推理，但它基于结构化的分层状态图运行，从而实现了高效优化，并通过轻量级剪枝获得了高达 2.14 倍的求解器加速。

## Abstract
Edge AI systems often operate under stringent energy and volume constraints that demand extreme efficiency under limited battery capacity, with requirements worsening as intelligent capability demands advance. Prior literature suggests that fine-grained power orchestration, including DVFS and power gating, enables significant energy efficiency benefits that cannot be left unexploited, while still exhibiting unexplored challenges. We observe that layer-level approaches incur unintended overheads due to inter-layer coupling of power control decisions, and that jointly managing these mechanisms under practical constraints such as limited voltage rails and transition overheads leads to a rapidly growing combinatorial schedule space. To address this, we propose PowerFlow-DNN, a compiler-directed framework for end-to-end power-state orchestration in ultra-low-power accelerators. By constructing a rigorous problem formulation for deadline-constrained, real-time, periodic inference as a unified inter-layer power-scheduling problem, our framework enables automated discovery of energy-minimal power-state schedules that adhere to a deadline while accounting for end-to-end, inter-layer impacts. We evaluate the framework on a DNN accelerator VLSI implementation in TSMC 40nm technology. Across representative edge networks, we show that PowerFlow-DNN discovers near-optimal solutions under the discretized formulation and achieves energy within 0.68\% of the exact ILP oracle, reducing energy by up to 37\% compared to an aggressive baseline without power orchestration, while reasoning over a combinatorial schedule space of over $10^{160}$ possible power-state assignments, yet operating on a structured layered state graph that enables efficient optimization, achieving up to 2.14$\times$ solver speedup via lightweight pruning.

---

## 论文详细总结（自动生成）

### PowerFlow-DNN 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：边缘 AI 系统（如植入式医疗设备、智能胶囊）面临极端的功耗和体积约束。虽然细粒度电源管理技术（如 DVFS 和电源门控）能提高能效，但在实际应用中存在两大挑战：
    1.  **层间耦合**：传统的层级优化忽略了层与层之间由于共享电压轨和转换开销（时间/能量）带来的相互影响。
    2.  **组合爆炸**：在有限电压轨、多种电源域和复杂转换约束下，寻找全局最优电源状态调度的搜索空间巨大（可达 $10^{160}$ 以上）。
*   **研究动机**：现有的反应式或层级独立优化方法无法充分挖掘硬件的节能潜力。本文旨在通过编译器导向的全局编排，在满足实时性（截止日期）约束的前提下，实现端到端的能耗最小化。

#### 2. 方法论
*   **核心思想**：将 DNN 推理过程建模为一个统一的、受限的层间电源状态调度问题，并利用编译器在部署前静态生成最优调度表。
*   **关键技术细节**：
    *   **统一建模**：将 DVFS（针对计算域）和电源门控（针对高漏电的 RRAM 存储库）统一为“电源状态”。
    *   **问题形式化**：目标函数是最小化总能耗（包括运算能耗、状态转换能耗和空闲能耗），约束条件包括推理截止时间、可用电压轨数量限制（$N_{max}$）以及离散的电压等级。
    *   **$\lambda$-DP 求解器**：采用基于拉格朗日松弛的动态规划算法。通过引入乘子 $\lambda$ 平衡能量与延迟，在分层状态图（Layered State Graph）上寻找最短路径。
    *   **结构化剪枝**：利用局部支配关系（Pareto-dominated）移除冗余状态，在不损失最优性的前提下减小搜索空间。
    *   **调度锚点**：以层边界作为 DVFS 切换点，以存储访问阶段作为电源门控切换点。

#### 3. 实验设计
*   **技术栈**：基于 TSMC 40nm LP 工艺的 VLSI 实现，包含 8×8 脉动阵列、RRAM 权重存储和 SRAM 缓冲区。
*   **数据集/模型**：选取了四种具有代表性的边缘 AI 网络：SqueezeNet 1.1、MobileNetV3-Small、ResNet18、MobileViT-xxs。
*   **对比基准（Benchmarks）**：
    1.  **Baseline**：无电源编排的常规加速器。
    2.  **+gating**：仅加入细粒度存储电源门控。
    3.  **+greedy**：采用基于边际效用的层级独立 DVFS 启发式方法。
    4.  **+gating+greedy**：结合上述两者。
    5.  **ILP Oracle**：整数线性规划精确解（用于小规模实例的验证）。

#### 4. 资源与算力
*   **硬件模拟**：使用周期精确的性能模型估算延迟，结合后仿真（Post-layout）门级功耗分析建立能量查找模型。
*   **算力说明**：论文未提及训练模型所用的 GPU 资源，因为其核心贡献在于**编译阶段的调度优化**。求解器在普通 CPU 上运行，经过剪枝后可在数秒内完成复杂网络的调度搜索。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同类型的网络架构（卷积、深度可分离卷积、残差、注意力机制）。
*   **实验维度**：
    *   不同推理速率（FPS）下的能耗对比。
    *   电压轨数量（1 到 5 条）对能效的影响。
    *   转换开销（Transition Overhead）的敏感度分析。
    *   求解器扩展性与运行时间评估。
*   **充分性评价**：实验设计较为全面，通过与 ILP 精确解对比证明了算法的近优性（误差 < 0.68%），客观地展示了在不同截止日期约束下的性能增益。

#### 6. 主要结论与发现
*   **能效提升**：相比无优化基准，PowerFlow-DNN 降低了高达 **37%** 的能耗；相比贪婪启发式方法，进一步降低了 **2-7%**。
*   **电压轨收益递减**：增加电压轨数量可提升能效，但超过 3 条后收益显著减小。优化选择特定的电压轨比单纯增加数量更有效。
*   **层间耦合的重要性**：证明了考虑层间转换成本和共享约束的全局优化优于局部最优决策。
*   **求解效率**：通过结构化剪枝实现了高达 **2.14 倍** 的求解加速，能够处理超大规模的组合空间。

#### 7. 优点
*   **垂直整合**：从编译器算法到 VLSI 电路实现进行了全栈式验证，考虑了真实的物理约束（如转换延迟、电压轨限制）。
*   **数学严谨性**：将复杂的工程问题转化为清晰的受限优化问题，并提供了高效的近似求解方案。
*   **通用性**：框架不局限于特定硬件，可扩展至其他具有多电源域的加速器架构。

#### 8. 不足与局限
*   **适用场景受限**：主要针对**确定性、周期性**的推理任务（如固定帧率的监控或医疗监测），对于输入高度动态、非确定性的负载（如动态跳层网络）支持不足。
*   **静态调度风险**：完全依赖编译时生成的静态表，若运行环境发生剧烈变化（如温度导致的时序波动），缺乏运行时的动态调整机制。
*   **工艺节点**：实验基于 40nm 工艺，虽然符合超低功耗边缘设备需求，但在更先进工艺节点（如 FinFET）下的漏电特性和转换开销表现尚待验证。

（完）
