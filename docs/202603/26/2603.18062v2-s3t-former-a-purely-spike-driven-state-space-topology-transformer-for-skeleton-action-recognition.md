---
title: "S3T-Former: A Purely Spike-Driven State-Space Topology Transformer for Skeleton Action Recognition"
title_zh: S3T-Former：一种用于骨骼动作识别的纯脉冲驱动状态空间拓扑 Transformer
authors: "Naichuan Zheng, Hailun Xia, Zepeng Sun, Weiyi Li, Yujia Wang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18062v2"
tags: ["query:hwnas"]
score: 8.0
evidence: 面向边缘设备的能效脉冲神经网络
tldr: 针对骨骼动作识别中人工神经网络功耗高的问题，本文提出了首个纯脉冲驱动的Transformer架构S3T-Former。该模型通过多流解剖脉冲嵌入（M-ASE）将多模态特征转化为高稀疏事件流，利用侧向脉冲拓扑路由（LSTR）实现按需传播，并引入脉冲状态空间（S3）引擎捕捉长程动态，有效解决了现有脉冲神经网络稀疏性受损和短期遗忘的问题，在保持高精度的同时显著降低了能耗。
selection_source: fresh_fetch
motivation: 现有的骨骼动作识别脉冲神经网络因密集矩阵运算和非稀疏变换导致稀疏性受损，且难以克服脉冲神经元的短期遗忘问题。
method: 提出了一种纯脉冲驱动的架构，包含多流解剖脉冲嵌入、侧向脉冲拓扑路由以及用于捕捉长程时间动态的脉冲状态空间引擎。
result: 在多个大规模数据集上的实验表明，该模型在达到极具竞争力的准确率的同时，理论能耗远低于传统人工神经网络。
conclusion: S3T-Former 成功实现了高效能的神经形态动作识别，为资源受限边缘设备上的骨骼数据处理树立了新的基准。
---

## 摘要
基于骨骼的动作识别对多媒体应用至关重要，但其严重依赖高功耗的人工神经网络 (ANN)，这限制了它们在资源受限的边缘设备上的部署。脉冲神经网络 (SNN) 提供了一种高能效的替代方案；然而，现有的骨骼数据脉冲模型往往通过采用密集矩阵聚合、沉重的多模态融合模块或非稀疏的频域变换，损害了 SNN 固有的稀疏性。此外，它们还深受脉冲神经元短期遗忘问题的困扰。在本文中，我们提出了脉冲状态空间拓扑 Transformer (S3T-Former)，据我们所知，这是首个专门为高能效骨骼动作识别设计的纯脉冲驱动 Transformer 架构。我们没有依赖沉重的融合开销，而是构建了一种多流解剖脉冲嵌入 (M-ASE)，它作为一个广义运动学微分算子，能够优雅地将多模态骨骼特征转换为异构且高度稀疏的事件流。为了实现真正的拓扑和时间稀疏性，我们引入了用于按需条件脉冲传播的侧向脉冲拓扑路由 (LSTR)，以及一个脉冲状态空间 (S3) 引擎，以便在不使用非稀疏频谱变通方案的情况下系统地捕获长程时间动态。在多个大规模数据集上的广泛实验表明，S3T-Former 在实现极具竞争力的准确率的同时，理论上比传统 ANN 显著降低了能耗，为高能效类脑动作识别树立了新的标杆。

## Abstract
Skeleton-based action recognition is crucial for multimedia applications but heavily relies on power-hungry Artificial Neural Networks (ANNs), limiting their deployment on resource-constrained edge devices. Spiking Neural Networks (SNNs) provide an energy-efficient alternative; however, existing spiking models for skeleton data often compromise the intrinsic sparsity of SNNs by resorting to dense matrix aggregations, heavy multimodal fusion modules, or non-sparse frequency domain transformations. Furthermore, they severely suffer from the short-term amnesia of spiking neurons. In this paper, we propose the Spiking State-Space Topology Transformer (S3T-Former), which, to the best of our knowledge, is the first purely spike-driven Transformer architecture specifically designed for energy-efficient skeleton action recognition. Rather than relying on heavy fusion overhead, we formulate a Multi-Stream Anatomical Spiking Embedding (M-ASE) that acts as a generalized kinematic differential operator, elegantly transforming multimodal skeleton features into heterogeneous, highly sparse event streams. To achieve true topological and temporal sparsity, we introduce Lateral Spiking Topology Routing (LSTR) for on-demand conditional spike propagation, and a Spiking State-Space (S3) Engine to systematically capture long-range temporal dynamics without non-sparse spectral workarounds. Extensive experiments on multiple large-scale datasets demonstrate that S3T-Former achieves highly competitive accuracy while theoretically reducing energy consumption compared to classic ANNs, establishing a new state-of-the-art for energy-efficient neuromorphic action recognition.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **S3T-Former** 的新型脉冲神经网络（SNN）架构，专门用于高能效的骨骼动作识别。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：传统的骨骼动作识别主要依赖人工神经网络（ANN），如 GCN 和 Transformer，虽然精度高但功耗巨大，难以部署在电池驱动的边缘设备上。
*   **研究动机**：
    *   **现有 SNN 的局限性**：现有的骨骼脉冲模型往往为了处理复杂动力学而引入密集矩阵运算或非稀疏变换，破坏了 SNN 固有的高稀疏性和纯加法（AC）计算优势。
    *   **短期遗忘问题**：脉冲神经元（如 LIF）具有内在的指数衰减特性，导致模型难以捕捉长程的时间因果关系。
*   **整体含义**：S3T-Former 是首个**纯脉冲驱动**的状态空间拓扑 Transformer，旨在在保持极低功耗的同时，通过模拟生物视觉机制解决稀疏性受损和长程记忆缺失的问题。

### 2. 方法论：核心思想与关键技术
S3T-Former 抛弃了密集的浮点乘累加（MAC）运算，核心组件包括：
*   **多流解剖脉冲嵌入 (M-ASE)**：
    *   充当“运动学微分算子”，计算零阶恒等状态、一阶时间梯度（速度）和一阶空间梯度（骨骼/角度）。
    *   将原始坐标转换为高度稀疏的事件流，模拟视网膜神经节细胞对时空梯度的响应。
*   **非对称时间梯度 QKV (ATG-QKV)**：
    *   模拟灵长类视觉系统的 M/P 细胞分工。强制 Query 和 Key 仅在动作梯度上触发脉冲，而 Value 保留静态结构特征，从而极大抑制了静态冗余。
*   **侧向脉冲拓扑路由 (LSTR)**：
    *   将 $O(V^2)$ 的全连接注意力简化为沿人体解剖图的条件传播。
    *   利用二进制脉冲特性，将空间聚合转化为硬件友好的**零 MAC 条件加法**。
*   **脉冲状态空间 (S3) 引擎**：
    *   构建线性复杂度的时域记忆池，通过可学习的衰减因子系统性地累积拓扑特征，克服 LIF 神经元的“短期遗忘症”。
*   **U-Readout 机制**：
    *   在输出层使用非脉冲 IF 积分器（阈值设为无穷大），直接利用最终膜电位进行分类，避免了传统脉冲计数带来的量化误差。

### 3. 实验设计
*   **数据集**：
    *   **NTU RGB+D 60**：包含 60 类动作，采用 Cross-Subject (X-Sub) 和 Cross-View (X-View) 协议。
    *   **NTU RGB+D 120**：目前规模最大的骨骼数据集，采用 X-Sub 和 Cross-Setup (X-Set) 协议。
    *   **NW-UCLA**：用于验证跨视角识别能力。
*   **对比方法 (Benchmarks)**：
    *   **ANN 模型**：ST-GCN, 2S-AGCN, CTR-GCN, Hyperformer, SkateFormer 等。
    *   **SNN 模型**：Spikformer, Spike-driven Transformer (V1/V2), Signal-SGN, MK-SGN 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA Tesla V100 GPU** 进行分布式训练。
*   **软件框架**：基于 PyTorch 和 **SpikingJelly** 开源库实现。
*   **训练细节**：
    *   训练 250 个 Epoch，Batch Size 为 64。
    *   使用了自动混合精度（AMP）技术以减少显存占用。
    *   采用 AdamW 优化器，初始学习率为 0.01，配合余弦退火调度。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽实验，包括三个主流数据集的性能对比、消融实验、可视化分析等。
*   **消融实验**：通过 6 个步骤逐步验证了 U-Readout、S3-Engine、LSTR、M-ASE 和 ATG-QKV 的增量贡献。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了精度，还深入分析了层级脉冲发放率（Sparsity）、特征分布（t-SNE）、时空激活图以及硬件能效比，证明了性能提升并非源于参数堆叠。

### 6. 主要结论与发现
*   **性能领先**：在 NTU-120 数据集上，S3T-Former 的集成模型比之前的 SNN 州级模型（Signal-SGN）准确率高出 **6.38%**。
*   **超越部分 ANN**：S3T-Former 的纯精度甚至超过了一些经典的 ANN 模型（如 ST-GCN），这在 SNN 领域是罕见的成就。
*   **极高能效**：理论能效分析表明，S3T-Former 的功耗**不足同等规模 ANN 的 10%**。
*   **稀疏性演进**：可视化显示，随着网络加深，脉冲会从全身物理跟踪演变为仅聚焦于关键语义端点（如拳头、脚踝）。

### 7. 优点与亮点
*   **纯脉冲驱动**：真正实现了全流程的事件驱动计算，最大化了类脑芯片的 AC 运算优势。
*   **生物启发式设计**：ATG-QKV 和 M-ASE 的设计紧扣生物视觉处理机制，具有很强的物理可解释性。
*   **解决核心痛点**：通过 S3-Engine 优雅地解决了 SNN 长期存在的长程记忆缺失问题，且保持了线性复杂度。
*   **代码开源**：提供了详细的伪代码和 GitHub 链接，增强了研究的可复现性。

### 8. 不足与局限
*   **微小动作识别困难**：混淆矩阵显示，模型在处理“读书”、“写字”、“玩手机”等仅涉及手指/手腕微小位移的动作时表现较差。
*   **原因分析**：由于 ATG-QKV 机制为了节能而过度过滤了静态特征，导致模型对缺乏宏观位移的微小动作感知不足。
*   **应用限制**：虽然理论能效极高，但目前大多数实验仍是在通用 GPU 上模拟完成的，在真实类脑硬件（如天机芯、Loihi）上的实测表现仍有待验证。

（完）
