---
title: "MINISA: Minimal Instruction Set Architecture for Next-gen Reconfigurable Inference Accelerator"
title_zh: MINISA：面向下一代可重构推理加速器的极简指令集架构
authors: "Jianming Tong, Devansh Jain, Yujie Li, Charith Mendis, Tushar Krishna"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20623v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在可重构加速器中维持高利用率的极简指令集
tldr: 针对现代可重构AI加速器在追求灵活性时面临的微控制开销过大问题，本文提出了MINISA指令集。它基于改进的FEATHER+架构，以“虚拟神经元（VN）”为粗粒度控制单位，在保留硬件灵活性的同时最小化控制成本。通过仅四条核心指令实现布局与映射控制，MINISA显著降低了指令带宽需求，消除了取指停顿，在多种AI及非AI负载下实现了巨大的性能提升。
selection_source: fresh_fetch
motivation: 细粒度微控制带来的巨大指令获取开销已成为限制可重构加速器利用率的瓶颈。
method: 提出以虚拟神经元为粒度的MINISA指令集，通过三条布局指令和一条映射指令抽象控制逻辑，并优化了FEATHER+架构以支持动态数据流切换。
result: 在50个GEMM负载中，MINISA将离线指令流量降低了35倍至40万倍，并实现了最高31.6倍的端到端加速。
conclusion: MINISA证明了通过合理的抽象粒度可以在不牺牲灵活性的前提下，极大地优化可重构加速器的控制效率和整体性能。
---

## 摘要
现代可重构 AI 加速器依赖丰富的映射和数据布局灵活性，以在矩阵乘法、卷积以及 AI 之外的新兴应用中维持高利用率。然而，通过细粒度的微控制（micro-control）暴露这种灵活性会导致从片外存储器获取配置位的控制开销过高。本文提出了 MINISA，这是一种极简指令集，它以虚拟神经元（Virtual Neurons, VNs）为粒度对可重构加速器进行编程。VNs 是既能保留硬件灵活性，又是避免不必要控制成本的最粗控制粒度。首先，我们介绍了 FEATHER+，它是对 FEATHER 的适度改进，消除了运行时数据流/布局协同切换所需的冗余片上复制，并支持输入和权重数据在执行前不可用（无法进行离线布局处理）的动态场景。随后，MINISA 将 FEATHER+ 的控制抽象为三条针对输入、权重和输出 VN 的布局设置指令，以及一条用于设置数据流的映射指令。这在保留 FEATHER+ 支持的合法映射和布局空间的同时，减少了控制和指令占用空间。实验结果表明，在涵盖 AI (GPT-oss)、全同态加密 (FHE) 和零知识证明 (ZKP) 的 50 个不同规模的 GEMM 工作负载下，MINISA 将片外指令流量的几何平均值降低了 35 倍至 4x10^5 倍。这消除了消耗 96.9% 微指令周期的取指停顿，使 16x256 规模的 FEATHER+ 实现了高达 31.6 倍的端到端加速。代码开源地址：https://github.com/maeri-project/FEATHER/tree/main/minisa。

## Abstract
Modern reconfigurable AI accelerators rely on rich mapping and data-layout flexibility to sustain high utilization across matrix multiplication, convolution, and emerging applications beyond AI. However, exposing this flexibility through fine-grained micro-control results in prohibitive control overhead of fetching configuration bits from off-chip memory. This paper presents MINISA, a minimal instruction set that programs a reconfigurable accelerator at the granularity of Virtual Neurons (VNs), the coarsest control granularity that retains flexibility of hardware and the finest granularity that avoids unnecessary control costs. First, we introduce FEATHER+, a modest refinement of FEATHER, that eliminates redundant on-chip replication needed for runtime dataflow/layout co-switching and supports dynamic cases where input and weight data are unavailable before execution for offline layout manipulation. MINISA then abstracts control of FEATHER+ into three layout-setting instructions for input, weight, and output VNs and a single mapping instruction for setting dataflow. This reduces the control and instruction footprint while preserving the legal mapping and layout space supported by the FEATHER+. Our results show that MINISA reduces geometric mean off-chip instruction traffic by factors ranging from 35x to (4x10^5)x under various sizes under 50 GEMM workloads spanning AI (GPT-oss), FHE, and ZKP. This eliminates instruction-fetch stalls that consume 96.9% of micro-instruction cycles, yielding up to 31.6x end-to-end speedup for 16x256 FEATHER+. Our code: https://github.com/maeri-project/FEATHER/tree/main/minisa.

---

## 论文详细总结（自动生成）

以下是对论文《MINISA: Minimal Instruction Set Architecture for Next-gen Reconfigurable Inference Accelerator》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现代可重构 AI 加速器（如 FEATHER）为了支持多样化的算子（卷积、矩阵乘法及新兴的加密算法），引入了极高的映射和布局灵活性。然而，这种灵活性通常依赖于**细粒度的微控制（Micro-control）**，即需要为每个开关和缓冲区生成大量的配置位。
*   **核心瓶颈**：随着计算阵列规模扩大，从片外存储器获取这些海量指令的开销（指令带宽和存储）变得不可接受，导致计算单元频繁因等待指令而停顿（Stall）。在大型阵列中，取指停顿甚至占用了超过 96% 的周期。
*   **研究目标**：设计一种极简的指令集架构（ISA），在不损失硬件灵活性的前提下，大幅压缩指令规模，消除控制开销瓶颈。

### 2. 论文提出的方法论
*   **核心思想：虚拟神经元（Virtual Neuron, VN）抽象**：
    *   论文提出以 VN 作为编程的最小单位。一个 VN 对应硬件中最小的点积原子（即单个 PE 执行的一次 $A_H$ 元素点积）。
    *   **VN 粒度平衡**：它是保留硬件灵活性的最粗粒度，同时也是避免不必要微控制成本的最细粒度。
*   **关键技术细节**：
    *   **FEATHER+ 硬件改进**：在原版 FEATHER 基础上增加了“全对全（All-to-all）”分发网络，消除了片上缓冲区的冗余数据复制，并支持在运行时动态加载权重（无需离线预处理布局）。
    *   **MINISA 指令集设计**：仅包含 8 条指令。核心包括 3 条**布局设置指令**（SetIVNLayout/SetWVNLayout/SetOVNLayout）和 1 条**映射执行指令**（ExecuteMapping）。
    *   **布局参数化**：通过 4 级张量划分和 3 位排列编码（Permutation Encoding），用极少的比特描述复杂的缓冲区数据组织方式。
    *   **映射参数化**：使用 6 个参数（起始索引、组大小、步长等）来定义 VN 如何映射到 PE 阵列，支持列复制和并行处理。
*   **编译流程**：开发了 FEATHER+ Mapper，采用“映射优先、布局随后”的搜索策略，自动将工作负载转换为 MINISA 指令序列。

### 3. 实验设计
*   **数据集/场景**：涵盖了 50 个 GEMM（通用矩阵乘法）工作负载，分为三大领域：
    1.  **AI 推理**：GPT-oss (20B 参数规模)。
    2.  **全同态加密 (FHE)**：Bootstrapping 过程中的矩阵运算。
    3.  **零知识证明 (ZKP)**：数论变换 (NTT) 相关的矩阵运算。
*   **Benchmark 与对比方法**：
    *   **基准 1**：微指令（Micro-instruction）控制模式（即显式控制每个开关的传统方式）。
    *   **基准 2**：工业级硬件，包括 NVIDIA RTX 5090 GPU 和 Google TPU v6e-8。
*   **阵列规模**：测试了从 $4 \times 4$ 到 $16 \times 256$ 的多种 PE 阵列配置。

### 4. 资源与算力
*   **硬件实现**：使用 TSMC 28nm 工艺进行了逻辑综合（PnR），评估了面积和功耗。
*   **仿真环境**：使用了周期精确的 RTL 模型验证正确性，并结合分析模型进行大规模阵列的性能评估。
*   **算力对比**：在对比实验中，将 64 个 FEATHER+ 实例（16x256 规模）与 8 个 TPU v6e 核心及 RTX 5090 进行等功耗（约 575W）对比。

### 5. 实验数量与充分性
*   **实验规模**：针对 50 种不同形状的矩阵运算，在 9 种不同的硬件阵列尺寸下进行了详尽测试，总计 450 个评估点。
*   **充分性**：实验覆盖了从极小到极大的矩阵形状，并对比了指令流量、取指停顿、端到端延迟、计算利用率等多个维度。通过消融实验分析了阵列宽度（AW）和高度（AH）对性能的影响，实验设计客观且具有说服力。

### 6. 论文的主要结论与发现
*   **指令压缩**：MINISA 将片外指令流量降低了 **35 倍至 400,000 倍**。
*   **性能提升**：通过消除取指停顿，在 16x256 规模下实现了高达 **31.6 倍** 的端到端加速。
*   **能效与利用率**：在处理不规则形状的矩阵时，FEATHER+ 的利用率远高于传统的脉动阵列（Systolic Array）。相比 RTX 5090 和 TPU v6e，分别实现了 **23.7 倍** 和 **7.8 倍** 的几何平均加速。
*   **控制占比**：MINISA 使指令周期占比降至 0.1% 以下，彻底解决了可重构加速器的控制开销问题。

### 7. 优点
*   **抽象精妙**：通过“虚拟神经元”这一抽象层，完美解决了灵活性与控制开销之间的矛盾。
*   **通用性强**：不仅适用于 AI，还能高效支持 FHE 和 ZKP 等对数据布局极其敏感的新兴计算领域。
*   **硬件开销低**：FEATHER+ 相比原版硬件仅增加了约 7% 的面积，却换取了支持动态负载的能力。

### 8. 不足与局限
*   **算子覆盖**：虽然 GEMM 是核心，但论文主要关注矩阵类运算，对于非线性算子或极度稀疏且无规则的计算模式讨论较少。
*   **互连网络挑战**：虽然全对全分发网络在 28nm 下面积可控，但如果阵列进一步扩展到数千列，跨栏（Crossbar）的物理布线和延迟可能会成为新的挑战。
*   **软件依赖**：高性能高度依赖于 Mapper 的搜索效率，对于超大规模计算图的全局优化复杂度较高。

（完）
