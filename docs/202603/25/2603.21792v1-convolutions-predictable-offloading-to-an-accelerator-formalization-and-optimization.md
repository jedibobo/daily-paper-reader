---
title: "Convolutions Predictable Offloading to an Accelerator: Formalization and Optimization"
title_zh: 卷积向加速器的可预测卸载：形式化与优化
authors: "Benjamin Husson, Mohammed Belcaïd, Thomas Carle, Claire Pagetti"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21792v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 优化受限于片上内存的加速器卷积卸载
tldr: 针对卷积神经网络在片上内存受限的加速器上运行时的实时性挑战，本文提出了一种将卷积层计算分解为多个卸载步骤的形式化方法。通过将该问题转化为约束求解问题，研究旨在寻找执行时间最短的最优卸载策略，并开发了Python模拟器进行深入分析，为优化硬件加速器的任务调度提供了理论基础和工具支持。
selection_source: fresh_fetch
motivation: 专用加速器的片上内存往往不足以存储整个卷积层的数据，需要通过分步卸载来满足实时计算需求。
method: 提出了一种卸载步骤序列的形式化框架，并利用约束编程方法对卷积分解策略进行建模与优化。
result: 成功实现了对卷积计算卸载过程的精确建模，并通过约束求解找到了执行时间最优的调度策略。
conclusion: 该研究为受限内存环境下的CNN加速器优化提供了有效的形式化工具和自动化搜索最优策略的方法。
---

## 摘要
卷积神经网络 (CNN) 需要大量的乘累加 (MAC) 操作。为了满足实时性约束，它们通常需要在由片上存储器和处理单元组成的专用加速器上执行。然而，片上存储器往往不足以存储计算单个 CNN 层所需的全部数据。因此，计算必须通过多个卸载步骤来完成。我们对这类步骤序列进行了形式化，并将该形式化方法应用于一种最先进的卷积分解技术。为了在执行时长方面找到最优策略，我们使用一组约束对该问题进行了编码。一个基于 Python 的模拟器能够对计算出的策略进行深入分析。

## Abstract
Convolutional neural networks (CNNs) require a large number of multiply-accumulate (MAC) operations. To meet real-time constraints, they often need to be executed on specialized accelerators composed of an on-chip memory and a processing unit. However, the on-chip memory is often insufficient to store all the data required to compute a CNN layer. Thus, the computation must be performed in several offloading steps. We formalise such sequences of steps and apply our formalism to a state of the art decomposition of convolutions. In order to find optimal strategies in terms of duration, we encode the problem with a set of constraints. A Python-based simulator allows to analyse in-depth computed strategies.

---

## 论文详细总结（自动生成）

这是一份关于论文《Convolutions Predictable Offloading to an Accelerator: Formalization and Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：卷积神经网络（CNN）计算量巨大，通常需要专用硬件加速器（如TPU、NPU、FPGA）。然而，加速器的片上内存（On-chip Memory）通常远小于存储整个卷积层数据所需的空间。
*   **研究动机**：为了在内存受限的情况下运行CNN，必须将计算分解为多个“卸载步骤”（Offloading Steps）。现有的卸载策略往往缺乏形式化描述，且难以证明其在执行时间（Latency）上的最优性。
*   **整体含义**：本文旨在通过数学形式化方法定义卸载策略，并利用整数线性规划（ILP）寻找在特定硬件约束下执行时间最短的最优调度方案，以满足安全关键型实时系统（如无人机探测、视觉着陆）的可预测性需求。

### 2. 方法论
*   **形式化模型**：
    *   将加速器抽象为包含片上内存（MEM）和处理单元（PE）的架构。
    *   定义一个“步骤”（Step）包含六个动作：释放输入内存、释放权重内存、写回结果、加载输入切片、加载权重子集、执行计算。
    *   通过集合论（交集、并集、差集）跟踪片上内存的状态演变。
*   **卷积切片技术**：
    *   将输入张量分解为“补丁”（Patches），每个补丁对应输出张量中的一个或多个元素。
    *   定义策略 **S1**：假设片上内存足以容纳所有卷积核（Kernels），重点优化输入补丁的加载顺序和分组。
*   **关键技术细节**：
    *   **ILP 建模**：将补丁到步骤的映射、像素到步骤的映射、像素重叠（Overlap）情况转化为线性约束。
    *   **目标函数**：最小化总执行时间 $\delta$，该时间由数据加载总量（从DRAM到MEM）和计算周期组成。
*   **Python 模拟器**：开发了一个基于 Python 的模拟器，用于可视化执行过程、跟踪内存占用并验证计算结果的正确性。

### 3. 实验设计
*   **场景与数据集**：
    *   使用了经典的 **LeNet-5** 和 **ResNet8** 卷积层参数。
    *   测试了不同输入尺寸（从 4x4 到 12x12）和不同的加速器计算能力（单步可处理的补丁数量 $nb\_patches\_max$ 从 2 到 10）。
*   **Benchmark（对比方法）**：
    *   **Row-by-Row（逐行策略）**：按行扫描顺序加载补丁。
    *   **ZigZag（之字形策略）**：奇偶行交替方向扫描，以增加相邻行间的像素复用。
    *   **S1-baseline**：来自文献 [23] 的原始策略（每步仅处理一个补丁）。
*   **评估指标**：总执行时长（Duration），假设加载开销与数据大小成正比。

### 4. 资源与算力
*   **硬件环境**：24核 Intel(R) Xeon(R) CPU E5-2420 @ 1.90GHz。
*   **软件工具**：使用 IBM CPLEX Optimizer (v22.1) 求解 ILP 问题。
*   **求解时长**：设置了 0.5 到 5 小时的求解超时限制。
*   **优化技术**：采用了 MIP Start（利用启发式解作为起点）和 Solution Polishing（遗传算法）来加速收敛。

### 5. 实验数量与充分性
*   **实验规模**：针对 9 种不同的图像尺寸和 9 种不同的分组大小（SG）进行了交叉实验。
*   **充分性**：实验覆盖了从“内存极度受限”到“内存相对充裕”的多种场景。
*   **客观性**：通过模拟器对所有策略进行了功能性验证，确保对比是在相同硬件约束（内存容量、计算能力）下进行的。
*   **局限性**：由于 ILP 的组合爆炸问题，实验仅限于较小的输入尺寸（最大 12x12），未在大尺寸工业级图像上直接运行 ILP 求解。

### 6. 论文的主要结论与发现
*   **ILP 的优越性**：在内存受限场景下，ILP 找到的最优策略比传统的 ZigZag 或 Row-by-Row 策略性能提升最高可达 **30%**。
*   **启发式策略的局限**：ZigZag 和 Row-by-Row 的优劣取决于补丁分组大小与图像宽度的关系。当分组大小是图像宽度的倍数时，两者表现一致；在其他情况下，存在明显的性能交叉点。
*   **数据复用关键点**：最优策略的核心在于最大化相邻步骤间留在片上内存中的像素重叠，从而减少从 DRAM 重复加载数据的次数。

### 7. 优点
*   **高度通用性**：提出的形式化框架不仅适用于卷积，也可扩展至矩阵乘法（GeMM）等其他算子。
*   **严谨的可预测性**：通过数学建模确保了计算时间的确定性，这对实时安全系统至关重要。
*   **工具链完整**：不仅有理论推导，还提供了开源模拟器，方便开发者可视化分析内存瓶颈。

### 8. 不足与局限
*   **可扩展性问题**：ILP 求解器在处理大规模网络层时会遭遇维度灾难，难以直接应用于超大分辨率图像。
*   **内存模型简化**：假设内存是抽象集合，未考虑物理内存布局（如连续地址访问、对齐等）对实际带宽的影响。
*   **策略局限性**：目前的优化主要集中在 S1 策略（卷积核全部驻留），未深入探讨卷积核也需要分片加载的更极端情况（尽管作者表示这是未来工作）。
*   **硬件抽象**：未考虑计算与通信的重叠（DMA 并发），这在现代高性能加速器中是常见的优化手段。

（完）
