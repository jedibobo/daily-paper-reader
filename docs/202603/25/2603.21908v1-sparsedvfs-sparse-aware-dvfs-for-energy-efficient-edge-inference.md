---
title: "SparseDVFS: Sparse-Aware DVFS for Energy-Efficient Edge Inference"
title_zh: SparseDVFS：面向高效能边缘推理的稀疏感知 DVFS 框架
authors: "Ziyang Zhang, Zheshun Wu, Jie Liu, Luca Mottola"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21908v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 通过稀疏感知的DVFS实现能效边缘推理
tldr: "针对边缘设备深度学习推理的能效挑战，本文提出SparseDVFS框架。该框架利用算子稀疏性作为调频核心指标，区分计算密集型与访存密集型算子，通过离线建模确定最优频率三元组，并结合运行时图划分与统一协同调度技术，在摊销硬件切换开销的同时消除组件干扰。实验表明，SparseDVFS在保证性能的前提下，相比现有方案能效提升达78.17%，显著优化了边缘推理的能耗表现。"
selection_source: fresh_fetch
motivation: 传统DVFS在边缘推理中面临模型级粒度过粗或算子级切换开销过大的困境，且未能有效利用算子稀疏性带来的能效优化空间。
method: 提出一种包含离线稀疏性建模、基于贪心合并的运行时图划分以及消除硬件干扰的统一协同调度器的细粒度DVFS框架。
result: "SparseDVFS在边缘设备上实现了平均78.17%的能效提升，并保持了14%的优异成本收益比。"
conclusion: 通过结合稀疏性感知和细粒度频率调度，SparseDVFS有效解决了边缘推理中能效与性能切换开销之间的平衡难题。
---

## 摘要
在功耗敏感的边缘设备上部署深度神经网络（DNN）面临着巨大挑战。虽然动态电压频率调节（DVFS）被广泛用于能效优化，但传统的模型级调节往往过于粗糙，无法捕捉推理过程中的内部变化，而细粒度的算子级调节则因显著的硬件切换延迟而导致严重的性能下降。本文提出了 SparseDVFS，这是一种专为高效能边缘推理设计的细粒度、稀疏感知 DVFS 框架。我们的核心见解是，算子稀疏度是硬件频率调制的首要指标。通过区分计算密集型的稠密算子和访存密集型的稀疏算子，系统可以应用专门的频率三元组来最大化能效。为了克服切换开销和组件干扰，SparseDVFS 包含了三项关键创新：（1）离线建模器，通过白盒时间线分析建立算子稀疏度与最优频率三元组（CPU/GPU/EMC）之间的确定性映射；（2）运行时图分区器，利用贪心合并启发式算法将算子聚合为超块（super-blocks），通过延迟摊销约束平衡调节粒度与 DVFS 切换延迟；（3）统一协同调节器，采用频率统一调节引擎（FUSE）和前瞻指令队列，以消除独立控制器之间的拮抗效应并隐藏硬件转换延迟。广泛的评估表明，SparseDVFS 与现有最先进的解决方案相比，平均实现了 78.17% 的能效提升，同时保持了 14% 的优异成本收益比。

## Abstract
Deploying deep neural networks (DNNs) on power-sensitive edge devices presents a formidable challenge. While Dynamic Voltage and Frequency Scaling (DVFS) is widely employed for energy optimization, traditional model-level scaling is often too coarse to capture intra-inference variations, whereas fine-grained operator-level scaling suffers from prohibitive performance degradation due to significant hardware switching latency. This paper presents SparseDVFS, a fine-grained, sparse-aware DVFS framework designed for energy-efficient edge inference. Our key insight is that operator sparsity is a primary metric for hardware frequency modulation. By distinguishing between compute-bound dense operators and memory-bound sparse operators, the system can apply specialized frequency triplets to maximize energy efficiency. To overcome switching overheads and component interference, SparseDVFS incorporates three key innovations: (1) an offline modeler that established a deterministic mapping between operator sparsity and optimal frequency triplets (CPU/GPU/EMC) via white-box timeline analysis; (2) a runtime graph partitioner that utilizes a greedy merging heuristic to aggregate operators into super-blocks, balancing scaling granularity and DVFS switching latency through a latency amortization constraint; and (3) a unified co-governor that employs a frequency unified scaling engine (FUSE) and a look-ahead instruction queue to eliminate antagonistic effects between independent controllers and hide hardware transition latencies. Extensive evaluations show that SparseDVFS achieves an average 78.17% energy efficiency gain over state-of-the-art solutions while maintaining a superior 14% cost-gain ratio.

---

## 论文详细总结（自动生成）

### SparseDVFS：面向高效能边缘推理的稀疏感知 DVFS 框架论文总结

#### 1. 核心问题与研究动机
在边缘设备（如 NVIDIA Jetson）上部署深度神经网络（DNN）时，能效是核心挑战。现有的动态电压频率调节（DVFS）方案存在两极分化：
*   **模型级 DVFS**：粒度太粗，无法捕捉推理过程中不同层（算子）之间的负载波动。
*   **算子级 DVFS**：粒度极细，但硬件频率切换延迟（通常 5-10ms）往往超过了轻量级算子的执行时间，导致切换开销抵消了节能收益。
*   **研究动机**：论文发现**算子稀疏度（Sparsity）**是决定硬件瓶颈（计算密集型 vs. 访存密集型）的关键指标。稀疏算子通常受限于内存带宽而非计算频率，因此存在巨大的降频节能空间。

#### 2. 方法论
SparseDVFS 提出了一种“块级（Block-level）”的稀疏感知 DVFS 框架，包含三个核心组件：
*   **白盒离线建模器 (Offline Modeler)**：
    *   建立确定性的物理模型，而非黑盒深度学习模型。
    *   **性能模型**：结合算子稀疏度系数，通过时间线分析预测算子在不同频率三元组（CPU/GPU/内存控制器 EMC）下的执行时间。
    *   **热感知功耗模型**：考虑静态漏电流与温度的非线性关系，精确估计不同状态下的能耗。
*   **运行时图分区器 (Runtime Graph Partitioner)**：
    *   采用**贪心合并算法**将细粒度算子聚合成“超块（Super-blocks）”。
    *   引入**延迟摊销约束**：仅当超块的预计执行时间显著大于硬件切换延迟的 $N$ 倍时才允许切换频率，从而确保节能收益大于切换成本。
*   **统一协同调节器 (Unified Co-governor)**：
    *   **FUSE 策略**：将 CPU、GPU 和内存频率视为耦合向量统一调节，消除独立控制器之间的拮抗效应（如 GPU 密集计算时 CPU 误降频导致的流水线停顿）。
    *   **前瞻指令队列 (Look-ahead)**：在当前块执行时，提前异步发出下一块的调频指令，利用计算时间掩盖硬件转换延迟。

#### 3. 实验设计
*   **硬件平台**：NVIDIA Jetson Orin Nano (8GB DRAM, Ampere GPU, ARM CPU)。
*   **测试模型**：涵盖 CNN（ResNet-18/101）和 Transformer（ViT-B16/L16）两类典型架构。
*   **数据集**：ImageNet-2012（主流分类）、DOTA-v1.0 和 VisDrone 2019（用于消融实验场景）。
*   **对比基准 (Benchmarks)**：
    1.  **Default DVFS**：Linux 内核默认调节器（schedutil/ondemand）。
    2.  **nvpmodel (MAX-N)**：NVIDIA 官方提供的最高性能静态模式。
    3.  **GearDVFS**：基于强化学习的模型级 DVFS 方案。
    4.  **Ascend-DVFS**：基于遗传算法的算子级细粒度 DVFS 方案。

#### 4. 资源与算力
*   **推理设备**：单台 NVIDIA Jetson Orin Nano 边缘开发板。
*   **训练/建模开销**：论文强调其离线建模器是“轻量级”的（模型大小仅 32 字节），相比于需要大量内核级分析的 nn-Meter（约 856MB）或需要长时间强化学习训练的方案，SparseDVFS 的计算资源消耗极低，适合快速部署。
*   **软件环境**：PyTorch 2.1, TensorRT, JetPack 6.0。

#### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **端到端性能对比**：在四种模型上对比了延迟、功耗和总能耗。
*   **消融实验**：验证了 CPU 锁定、GPU 调节和 FUSE 统一调度各自的贡献。
*   **稳定性分析**：进行了长达 600 秒的持续负载测试，观察热节流（Thermal Throttling）对帧率抖动的影响。
*   **敏感度分析**：探讨了聚合因子 $N$ 对超块数量、切换延迟和能效的权衡关系。
*   **客观性**：通过对比官方 MAX-N 模式和最新的学术方案，证明了其在“成本-收益比”上的优越性。

#### 6. 主要结论
*   **能效提升**：SparseDVFS 相比默认 DVFS 平均实现了 **78.17%** 的能效提升。
*   **性能平衡**：在大幅节能的同时，相比最高性能模式（MAX-N）仅增加了约 12.8% 的延迟，远优于其他细粒度方案。
*   **热稳定性**：由于平均功耗更低，SparseDVFS 能有效延迟热节流的发生，使边缘设备在无风扇环境下运行更稳定。
*   **成本收益**：实现了 **14%** 的极低成本收益比（Cost-Gain Ratio），证明了块级调度的合理性。

#### 7. 优点与亮点
*   **稀疏性驱动**：敏锐捕捉到稀疏算子在边缘硬件上的访存瓶颈，将其转化为节能机会。
*   **开销摊销机制**：通过超块聚合解决了 DVFS 切换延迟这一长期存在的工程痛点。
*   **物理建模**：采用白盒物理模型而非黑盒 AI 模型，具有极高的可解释性和极低的运行时开销。
*   **系统级协同**：FUSE 策略解决了 CPU 和 GPU 互等导致的资源浪费。

#### 8. 不足与局限
*   **设备依赖性**：离线建模器仍需针对特定硬件进行初步画像（Profiling），跨平台迁移（如从 Jetson 到 Edge TPU）需要重新校准物理参数。
*   **稀疏模式简化**：目前主要将稀疏度视为标量比例，未深入区分结构化稀疏与非结构化稀疏对内存访问模式（随机 vs. 顺序）的不同影响。
*   **应用范围**：主要针对确定性的 DNN 推理任务，对于负载高度动态变化的生成式模型（如大语言模型 LLM）的适配性尚待验证。

（完）
