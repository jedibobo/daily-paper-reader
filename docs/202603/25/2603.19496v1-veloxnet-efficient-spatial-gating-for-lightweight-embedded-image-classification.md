---
title: "VeloxNet: Efficient Spatial Gating for Lightweight Embedded Image Classification"
title_zh: VeloxNet：用于轻量级嵌入式图像分类的高效空间门控
authors: "Md Meftahul Ferdaus, Elias Ioup, Mahdi Abdelguerfi, Anton Netchaev, Steven Sloan, Ken Pathak, Kendall N. Niles"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19496v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对具有严格内存和延迟限制的嵌入式设备的轻量级CNN架构
tldr: "VeloxNet是一种专为嵌入式图像分类设计的轻量级CNN架构，旨在平衡精度与资源限制。该研究通过引入带有空间门控单元（SGU）的gMLP模块替换SqueezeNet的Fire模块，实现了在减少46.1%参数量的同时，显著提升了在航拍灾害监测等任务中的分类性能，为资源受限设备的部署提供了高效方案。"
selection_source: fresh_fetch
motivation: 针对嵌入式设备在航拍监测等任务中对模型尺寸、内存和延迟的严格限制，寻求比传统局部卷积更高效的空间建模方法。
method: 提出VeloxNet架构，利用空间门控单元（SGU）通过学习空间投影和乘法门控来捕获全局空间依赖性，替代传统的局部卷积模块。
result: "在三个航拍数据集上，VeloxNet比SqueezeNet减少了46.1%的参数，且在F1分数上最高提升了30.83%，优于多种主流轻量级模型。"
conclusion: 证明了在轻量级网络中使用空间门控块替代局部卷积模块，能有效提升资源受限环境下的模型精度与参数效率。
---

## 摘要
在嵌入式设备上部署深度学习模型，用于航空灾害监测和基础设施巡检等任务，需要能够平衡准确性与模型大小、内存及延迟等严格限制的架构。本文介绍了 VeloxNet，这是一种轻量级 CNN 架构，它在嵌入式图像分类中采用门控多层感知机（gMLP）模块取代了 SqueezeNet 的 fire 模块。每个 gMLP 模块使用一个空间门控单元（SGU），该单元应用学习到的空间投影和乘法门控，使网络能够在单层中捕获整个特征图的空间依赖关系。与受限于小卷积核定义的局部感受野的 fire 模块不同，SGU 以更少的参数在每一层提供全局空间建模。我们在三个航空图像数据集上评估了 VeloxNet：应急响应航空图像数据库（AIDER）、综合灾害数据集（CDD）和堤防缺陷数据集（LDD），并与包括 MobileNet 变体、ShuffleNet、EfficientNet 以及最新的视觉 Transformer 在内的 11 个基准模型进行了对比。VeloxNet 相比 SqueezeNet 减少了 46.1% 的参数量（从 740,970 降至 399,366），同时在 AIDER、CDD 和 LDD 数据集上的加权 F1 分数分别提升了 6.32%、30.83% 和 2.51%。这些结果表明，用空间门控模块替换局部卷积模块可以提高资源受限部署下的分类准确性和参数效率。源代码将在论文被接收后公开。

## Abstract
Deploying deep learning models on embedded devices for tasks such as aerial disaster monitoring and infrastructure inspection requires architectures that balance accuracy with strict constraints on model size, memory, and latency. This paper introduces VeloxNet, a lightweight CNN architecture that replaces SqueezeNet's fire modules with gated multi-layer perceptron (gMLP) blocks for embedded image classification. Each gMLP block uses a spatial gating unit (SGU) that applies learned spatial projections and multiplicative gating, enabling the network to capture spatial dependencies across the full feature map in a single layer. Unlike fire modules, which are limited to local receptive fields defined by small convolutional kernels, the SGU provides global spatial modeling at each layer with fewer parameters. We evaluate VeloxNet on three aerial image datasets: the Aerial Image Database for Emergency Response (AIDER), the Comprehensive Disaster Dataset (CDD), and the Levee Defect Dataset (LDD), comparing against eleven baselines including MobileNet variants, ShuffleNet, EfficientNet, and recent vision transformers. VeloxNet reduces the parameter count by 46.1% relative to SqueezeNet (from 740,970 to 399,366) while improving weighted F1 scores by 6.32% on AIDER, 30.83% on CDD, and 2.51% on LDD. These results demonstrate that substituting local convolutional modules with spatial gating blocks can improve both classification accuracy and parameter efficiency for resource-constrained deployment. The source code will be made publicly available upon acceptance of the paper.

---

## 论文详细总结（自动生成）

以下是对论文《VeloxNet: Efficient Spatial Gating for Lightweight Embedded Image Classification》的结构化总结：

### 1. 研究动机与核心问题
*   **研究背景**：在无人机灾害监测、基础设施巡检等嵌入式应用中，深度学习模型面临严格的参数量（P）、计算量（C）、内存占用（M）和推理延迟（T）限制。
*   **核心问题**：传统的轻量级架构（如 SqueezeNet、MobileNet）主要依赖局部卷积，感受野受限；而视觉 Transformer（ViT）虽具备全局建模能力，但计算开销过大，难以在资源极度受限的硬件上运行。
*   **研究动机**：探索如何通过引入门控多层感知机（gMLP）中的空间门控机制，在不增加硬件负担的前提下，赋予轻量级网络全局空间建模能力，从而打破现有轻量级模型在精度与效率之间的权衡瓶颈。

### 2. 方法论
*   **核心思想**：将 SqueezeNet 中的“Fire模块”（基于局部卷积）替换为经过效率优化的“gMLP模块”，利用**空间门控单元（SGU）**实现单层内的全局特征交互。
*   **关键技术细节**：
    *   **gMLP 模块结构**：包含层归一化（LayerNorm）、通道投影（线性层）、GeLU 激活函数、SGU 单元以及残差连接。
    *   **空间门控单元 (SGU)**：将输入特征沿通道维度拆分为 $Z_1$ 和 $Z_2$。对 $Z_2$ 应用一个可学习的空间投影矩阵 $W_g$（维度为 $n \times n$，其中 $n$ 为空间像素数），通过 $f(Z_2) = W_g \text{LN}(Z_2) + b_g$ 生成门控权重，最后与 $Z_1$ 进行哈达玛积（逐元素相乘）。
    *   **VeloxNet 架构优化**：
        *   采用固定的 **156 通道**设计，避免了 SqueezeNet 中频繁压缩/扩张带来的信息瓶颈。
        *   简化了归一化流程，每个模块仅使用一次 LayerNorm。
        *   保留了 SqueezeNet 的总体拓扑（初始卷积、三个池化阶段、最终分类器），但将 8 个 Fire 模块全部替换为 gMLP 模块。

### 3. 实验设计
*   **数据集**：
    1.  **AIDER**（应急响应航空图像）：5 类，6,433 张图像。
    2.  **CDD**（综合灾害数据集）：5 类，13,316 张图像。
    3.  **LDD**（堤防缺陷数据集）：5 类，3,060 张图像。
*   **基准模型 (Benchmarks)**：对比了 11 种主流模型，包括：
    *   轻量级 CNN：MobileNet V1/V2/V3, ShuffleNet, EfficientNet, SqueezeNet, ConvNeXt V1/V2。
    *   视觉 Transformer 及 MLP 类模型：gMLP (原始版), FasterViT, EdgeViT。
*   **评价指标**：加权 F1 分数（主指标）、精确率、召回率、参数量、FLOPs、模型大小（MB）、推理速度（FPS）。

### 4. 资源与算力
*   **训练配置**：使用 Adam 优化器，学习率 0.001，Batch Size 为 32，训练 300 个 Epoch。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。仅提供了模型在测试时的推理速度（FPS）和计算量（FLOPs）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的专业领域数据集上进行了全方位的对比测试，涵盖了从经典轻量级网络到最新视觉 Transformer 的 11 个基准模型。
*   **消融实验**：针对 AIDER 数据集做了详细的消融研究，验证了 SGU 单元、残差连接、层归一化、网络深度以及通道维度（$d_{model}$）对性能的影响。
*   **充分性评价**：实验设计较为充分且客观。通过跨数据集验证和多维度指标对比，证明了模型在参数效率和分类精度上的优势。

### 6. 主要结论与发现
*   **性能提升**：VeloxNet 在所有数据集上均取得了最高的 F1 分数。相比 SqueezeNet，在 AIDER 上提升了 6.32%，在 CDD 上大幅提升了 30.83%。
*   **参数极度精简**：参数量仅为 39.9 万，比 SqueezeNet 减少了 **46.1%**，比 MobileNet 系列减少了 80% 以上。
*   **全局建模的价值**：SGU 提供的全局感受野对于识别具有大尺度结构（如洪水、建筑倒塌）的航拍图像至关重要，这是局部卷积模块的短板。

### 7. 优点
*   **架构创新**：成功将 gMLP 理论应用于极低参数量的嵌入式场景，证明了空间门控可以作为局部卷积的高效替代方案。
*   **参数效率极高**：在保持极小模型体积（1.52 MB）的同时，性能优于参数量大得多的模型（如 ConvNeXt）。
*   **针对性强**：针对航拍灾害监测这一特定领域进行了深度优化和验证，具有较强的实际应用参考价值。

### 8. 不足与局限
*   **计算量 (FLOPs) 并非最低**：虽然参数量极少，但 VeloxNet 的 FLOPs（461M）高于 ShuffleNet 和 MobileNetV3，在某些计算受限而非内存受限的硬件上可能不占优势。
*   **任务覆盖有限**：目前仅验证了 5 分类的图像分类任务，尚未在目标检测、语义分割等更复杂的下游任务中进行测试。
*   **泛化风险**：固定的 156 通道设计和特定的空间投影尺寸可能需要针对不同分辨率的输入进行重新调整，通用性有待进一步验证。

（完）
