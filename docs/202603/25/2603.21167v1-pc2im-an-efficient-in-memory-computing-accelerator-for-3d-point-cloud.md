---
title: "PC2IM: An Efficient In-Memory Computing Accelerator for 3D Point Cloud"
title_zh: PC2IM：一种高效的3D点云存内计算加速器
authors: "Dengfeng Wang, Shunqin Cai, Yanan Sun"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21167v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于3D点云网络的SRAM存内计算加速器
tldr: 针对3D点云神经网络在移动端面临的高访存和高计算负载挑战，本文提出了一种基于SRAM的存内计算（CIM）加速器PC2IM。该加速器通过引入近似距离存内计算（APD-CIM）、双级Ping-Pong-MAX内容寻址存储器以及拆分拼接式SRAM-CIM引擎，显著减少了数据预处理中的访存开销并提升了特征计算效率，在性能和能效上均大幅优于现有SOTA加速器和GPU。
selection_source: fresh_fetch
motivation: 3D点云算法在数据预处理阶段存在严重的访存瓶颈，且特征计算负载沉重，导致移动端设备面临高能耗和高延迟问题。
method: 提出PC2IM架构，利用APD-CIM减少空间划分访存，结合Ping-Pong-MAX CAM进行原位最大值搜索，并采用拆分拼接式SRAM-CIM优化特征计算。
result: PC2IM相比现有最先进加速器实现1.5倍加速和2.7倍能效提升，相比GPU实现3.5倍加速和1518.9倍能效提升。
conclusion: PC2IM通过创新的存内计算架构有效解决了点云处理中的访存与计算瓶颈，为资源受限系统的3D感知提供了高效能硬件方案。
---

## 摘要
3D点云神经网络显著增强了资源受限的移动智能系统的感知能力。然而，尽管具有变革性的影响，点云算法在数据预处理过程中仍面临大量的访存需求，并给特征计算带来了沉重的负载，导致高能耗和高延迟。本文提出了一种基于SRAM的高效存内计算（SRAM-CIM）加速器（PC2IM），旨在缓解基于点的3D点云网络中的访存瓶颈。提出了一种由定制CIM引擎驱动的数据预处理模块，并将其整合到内存高效的数据流中。具体而言，引入了一种近似距离SRAM-CIM（APD-CIM），以消除按中值进行空间划分的点云的重复片上访存，并减少临时距离数据的量。在APD-CIM的基础上，引入了一种两级乒乓最大值内容寻址存储器（Ping-Pong-MAX CAM），用于自适应更新临时距离并执行原位最大值搜索，从而进一步减少访存。此外，提出了一种名为拆分-拼接SRAM-CIM的高效CIM特征计算引擎，旨在最小化高精度输入多层感知器中的计算延迟，同时保持高面积和能量效率。实验结果表明，与最先进的点云加速器相比，所提出的PC2IM实现了1.5倍的加速和2.7倍的能效提升。此外，与GPU实现相比，PC2IM实现了3.5倍的加速和1518.9倍的能效提升。

## Abstract
3D point cloud neural networks have significantly enhanced the perceptual capabilities of resource-limited mobile intelligent systems. However, despite the transformative impact, the point cloud algorithm suffers from substantial memory access during data preprocessing and imposes a burdensome workload on feature computing, resulting in high energy consumption and latency. In this paper, an efficient SRAM-based computing-in-memory (SRAM-CIM) accelerator (PC2IM), is proposed to alleviate memory access bottlenecks in point-based 3D point cloud networks. A data preprocessing module driven by the customized CIM engines is proposed and incorporated into a memory-efficient data flow. Specifically, an approximate distance SRAM-CIM (APD-CIM) is introduced to eliminate the repetitive on-chip memory access for point clouds that are spatially partitioned by the median and reduce the volume of temporary distance data. Building on the APD-CIM, a two-level Ping-Pong-MAX Content Addressable Memory (Ping-Pong-MAX CAM) is introduced to adaptively update temporary distances and perform in-situ search for the maximum, further reducing memory access. Additionally, an efficient CIM-based feature computing engine, named split-concatenate SRAM-CIM, is presented to minimize computation latency in multi-layer perceptron with high-precision input, while maintaining high area and energy efficiency. Experiment results show that the proposed PC2IM demonstrates 1.5x speedup and 2.7x enhanced energy efficiency compared to state-of-the-art point cloud accelerator. Moreover, PC2IM achieves 3.5x speedup and 1518.9x enhanced energy efficiency compared to GPU implementations.

---

## 论文详细总结（自动生成）

以下是对论文《PC2IM: An Efficient In-Memory Computing Accelerator for 3D Point Cloud》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3D点云神经网络（PCN）在移动端部署面临两大瓶颈：
    1.  **访存瓶颈**：数据预处理阶段（如最远点采样 FPS 和近邻搜索 kNN）涉及频繁的片外和片上内存访问，FPS 甚至占大型点云处理时间的 70%。
    2.  **计算瓶颈**：特征计算阶段（MLP）通常需要 8 位以上的高精度计算，传统的位串行（Bit-serial）存内计算（CIM）架构在处理高精度输入时延迟高、能效低。
*   **研究背景**：现有的加速器虽通过空间划分减少了片外访存，但片上数据传输（尤其是临时距离更新）仍占据了 99% 的访存开销。

### 2. 方法论
论文提出了 **PC2IM**，一种基于 SRAM 的高效存内计算加速器，核心技术包括：
*   **近似距离存内计算（APD-CIM）**：
    *   将传统的欧氏距离（L2）简化为曼哈顿距离（L1），以适应 CIM 架构，避免复杂的乘法器。
    *   引入**基于中值的空间划分（MSP）**，将点云划分为等大小的局部块，使阵列利用率平均提升 15%。
*   **双级乒乓最大值内容寻址存储器（Ping-Pong-MAX CAM）**：
    *   支持原位（In-situ）比较和搜索，用于快速寻找最大距离点（质心）。
    *   采用乒乓架构，在搜索模式和加载模式间切换，消除了频繁读写临时距离数据的开销。
*   **拆分-拼接 SRAM-CIM（SC-CIM）**：
    *   针对高精度 MLP 计算，将输入按位拆分为 4 位簇，将权重按块拆分。
    *   使用**融合加法器（FuA）**和稀疏-稠密累加树，通过拼接而非乘法实现计算，相比位串行架构减少了 4 倍计算周期。

### 3. 实验设计
*   **数据集**：
    *   **ModelNet40**（小型，分类任务，1k点）。
    *   **S3DIS**（中型，语义分割，4k点）。
    *   **SemanticKITTI**（大型，语义分割，16k点）。
*   **Benchmark（对比基准）**：
    *   **Baseline-1**：传统数字电路 + 全局 FPS 采样。
    *   **Baseline-2 (TiPU, DAC'23)**：最先进的基于局部空间划分和位串行近存计算的加速器。
    *   **Baseline-3 (GPU)**：服务器级显卡 NVIDIA RTX 4090。
*   **评估指标**：能效（TOPS/W）、吞吐量（TOPS）、加速比、精度损失、面积效率（FoM）。

### 4. 资源与算力
*   **硬件规格**：采用 **40nm CMOS** 工艺实现，工作频率 **250MHz**。
*   **存储容量**：SC-CIM 为 256KB，APD-CIM 为 12KB，MAX-CAM 为 19KB，另有 512KB 标准片上 SRAM。
*   **算力表现**：在 16 位精度下，吞吐量达到 **2 TOPS**，能效为 **2.53 TOPS/W**。
*   **训练/推理说明**：论文侧重于推理加速器的硬件设计与仿真，未提及模型训练时长。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从分类到大规模分割的三种典型场景，验证了算法的通用性。
*   **消融与对比**：
    *   针对数据预处理，对比了不同采样方法的能耗。
    *   针对特征计算，对比了不同存储计算比（SCR）下的 FoM 值。
    *   进行了精度验证实验，证明 L1 距离近似和 16 位量化对精度的影响极小（<2%）。
*   **充分性评价**：实验设计较为充分，通过与 2023 年及 2024 年的最先进（SOTA）工作对比，体现了其在架构层面的创新优势。

### 6. 主要结论与发现
*   **性能提升**：PC2IM 相比 SOTA 加速器（TiPU）实现了 **1.5 倍**的加速和 **2.7 倍**的能效提升。
*   **对比 GPU**：在处理大规模点云（SemanticKITTI）时，相比 RTX 4090 实现了 **3.5 倍**的加速和 **1518.9 倍**的能效提升。
*   **访存优化**：CIM 驱动的预处理模块将数据预处理阶段的能耗降低了 **73.4%**。
*   **精度保持**：通过自适应缩放因子（1.6倍），L1 距离采样能有效保持点云的原始空间特征。

### 7. 优点
*   **架构创新**：首次将 CIM 技术深度整合进点云的预处理（采样/搜索）和特征计算两个阶段，而非仅关注计算。
*   **软硬协同**：通过 L1 距离近似和 MSP 划分，使复杂的点云算法能够高效适配简单的 SRAM 逻辑结构。
*   **高精度支持**：SC-CIM 解决了传统 CIM 在处理 8 位以上高精度输入时性能大幅下降的痛点。

### 8. 不足与局限
*   **应用限制**：该加速器主要针对基于点的网络（如 PointNet++），对于体素（Voxel）或柱状（Pillar）架构的点云网络适配性未做详细讨论。
*   **工艺偏差风险**：虽然使用了 40nm 工艺仿真，但 CIM 架构在实际流片中可能面临模拟计算的噪声和工艺偏差挑战（尽管本文采用的是数字式 CIM）。
*   **硬件开销**：为了实现原位搜索和拼接计算，增加了定制化的 CAM 和加法器树逻辑，可能会增加芯片设计的复杂度和非标准单元的面积。

（完）
