---
title: "Meeting in the Middle: A Co-Design Paradigm for FHE and AI Inference"
title_zh: 相向而行：全同态加密与 AI 推理的协同设计范式
authors: "Bernardo Magri, Benjamin Marsh, Paul Gebheim"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20504v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对推理电路专门优化FHE方案和编译器的协同设计
tldr: 本研究针对云端AI推理中的双向隐私保护难题，指出全同态加密（FHE）因高昂开销难以直接应用。为此，作者提出了一种“中途相遇”的协同设计范式：一方面针对推理电路的静态结构定制FHE方案与编译器，另一方面通过约束AI模型架构来减少同态运算的核心成本。该研究明确了两个维度的具体优化目标，旨在通过算法与架构的深度融合，推动高效、安全的隐私保护推理技术的发展。
selection_source: fresh_fetch
motivation: 现代云端推理面临用户输入隐私与模型权重保护的双重挑战，而现有的全同态加密技术计算成本过高，难以支持现代AI架构。
method: 提出一种协同设计范式，通过为推理电路定制FHE方案并同时约束推理架构以降低加密运算开销。
result: 制定了“中途相遇”的研究议程，并明确了FHE编译器优化与推理架构调整的具体技术目标。
conclusion: 协同设计是克服FHE性能瓶颈、实现实用化隐私保护AI推理的关键路径。
---

## 摘要
现代云端推理产生了一个双向隐私问题：用户会向提供商泄露敏感输入，而提供商则必须在可能存在泄露风险的执行环境中运行其专有的模型权重。全同态加密（FHE）虽然提供了密码学保证，但对于现代架构而言其开销依然极高。我们认为，取得进展需要进行协同设计，即针对推理电路的静态结构专门优化 FHE 方案和编译器，同时约束推理架构以减少主要的同态计算成本驱动因素。我们概述了一个“相向而行”的议程，并提出了两个维度上的具体优化目标。

## Abstract
Modern cloud inference creates a two sided privacy problem where users reveal sensitive inputs to providers, while providers must execute proprietary model weights inside potentially leaky execution environments. Fully homomorphic encryption (FHE) offers cryptographic guarantees but remains prohibitively expensive for modern architectures. We argue that progress requires co-design where specializing FHE schemes/compilers for the static structure of inference circuits, while simultaneously constraining inference architectures to reduce dominant homomorphic cost drivers. We outline a meet in the middle agenda and concrete optimization targets on both axes.

---

## 论文详细总结（自动生成）

这是一份关于论文《Meeting in the Middle: A Co-Design Paradigm for FHE and AI Inference》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文是一篇**观点性论文（Position Paper）**，旨在解决云端 AI 推理中的双向隐私矛盾：用户希望保护输入数据隐私，而服务商希望保护专有模型权重。
*   **背景：** 目前主流方案是可信执行环境（TEE），但其存在侧信道漏洞且依赖硬件厂商。全同态加密（FHE）虽能提供纯密码学保障，但计算开销极大，尤其是处理 Transformer 等现代模型时，非线性操作会导致性能灾难。
*   **核心问题：** 如何克服 FHE 的性能瓶颈，使其能够支持实用的 AI 推理？作者认为，不应将 FHE 视为通用的黑盒计算工具，而应利用 AI 推理任务的**高度可预测性**进行协同设计。

### 2. 方法论：核心思想与关键技术细节
论文提出了**“中途相遇”（Meeting in the Middle）**的协同设计范式，分为两个互补维度：

*   **维度一：为 AI 推理定制 FHE 方案（Specializing FHE）**
    *   **利用静态特性：** AI 推理没有数据依赖的分支，电路拓扑固定。可以进行离线电路优化。
    *   **优化打包与变换：** 利用环形加密方案（如 BFV/CKKS）的 SIMD 特性，根据张量形状设计特定的打包策略，减少昂贵的旋转（Rotation）和密钥切换操作。
    *   **静态自举调度：** 预先估计噪声增长，制定全局最优的自举（Bootstrapping）计划，而非在运行时动态触发。
    *   **定制模数：** 根据量化后的激活值和权重范围，定制明文模数和缩放策略。

*   **维度二：为 FHE 约束 AI 推理架构（Constraining AI）**
    *   **FHE 感知的架构搜索（NAS）：** 在自动寻找模型架构时，将“乘法深度”和“旋转次数”作为核心约束目标，倾向于选择浅而宽的层。
    *   **非线性算子替换：** 将 ReLU、GeLU 或 Softmax 等非多项式函数替换为低次多项式近似。
    *   **量化感知训练（QAT）：** 让模型在训练阶段就适应 FHE 的明文模数限制。
    *   **块结构稀疏化：** 使模型的稀疏模式与 FHE 的 SIMD 打包策略对齐，从而在加密状态下直接跳过无效运算。

### 3. 实验设计
由于是观点论文，文中仅包含一个**概念验证型（Proof of Concept）的微基准测试**：
*   **场景：** 针对 Softmax 函数在有限输入范围内的多项式近似。
*   **Benchmark：** 对比了在相同 CPU 上运行的、未经优化的标量（Scalar）明文 Rust 实现。
*   **指标：** 误差率（Observed error）和执行耗时。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件型号（如 GPU 型号、内存大小）或大规模训练的时长。
*   微基准测试仅提到在“同一 CPU”上运行，且明确指出未利用 SIMD/AVX 或 GPU 加速。

### 5. 实验数量与充分性
*   **实验数量：** 极少。仅展示了一个关于 Softmax 近似的微基准测试结果。
*   **充分性：** 作为观点论文，其目的在于阐述理论框架而非提供详尽的实验数据。目前的实验仅能证明“特定算子近似在 FHE 下可行”，但不足以证明整个端到端推理流程的实用性。实验对比对象（标量 Rust 代码）较为单一，缺乏与工业级优化方案的对比。

### 6. 主要结论与发现
*   **协同设计是必经之路：** 简单地将 SOTA 模型套用在通用 FHE 方案上是行不通的，必须双向适配。
*   **性能潜力：** 实验发现，在特定条件下，同态评估的多项式近似算子甚至可以比未优化的明文标量实现更快（利用了 FHE 的并行性）。
*   **容错性利用：** AI 模型对量化和近似具有天然的鲁棒性，这为 FHE 的多项式近似提供了巨大的优化空间。

### 7. 优点
*   **系统性视角：** 首次将 FHE 优化与神经网络架构搜索（NAS）及量化训练（QAT）整合进统一的协同设计框架。
*   **洞察深刻：** 准确抓住了 AI 推理“静态、形状固定、无分支”的特点，并将其转化为密码学上的优化优势。
*   **前瞻性：** 为解决隐私保护计算中“效能-安全”权衡问题提供了清晰的路线图。

### 8. 不足与局限
*   **缺乏端到端验证：** 论文没有提供完整模型（如完整的 Transformer 或 ResNet）在 FHE 下的运行数据。
*   **误差累积问题：** 虽然单个算子误差小于 0.5%，但在深度网络中，多项式近似导致的误差累积和梯度消失/爆炸问题未被深入探讨。
*   **比较基准偏低：** 与未优化的标量 Rust 代码对比可能夸大了 FHE 的性能表现，若对比经过 SIMD 或 GPU 优化的明文推理，FHE 的差距依然会非常巨大。
*   **工程门槛：** 该范式要求开发者同时具备深厚的密码学背景和 AI 架构设计能力，实际落地难度大。

（完）
