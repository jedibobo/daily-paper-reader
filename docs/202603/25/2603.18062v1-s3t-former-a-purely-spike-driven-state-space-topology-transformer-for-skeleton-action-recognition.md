---
title: "S3T-Former: A Purely Spike-Driven State-Space Topology Transformer for Skeleton Action Recognition"
title_zh: S3T-Former：一种用于骨骼动作识别的纯脉冲驱动状态空间拓扑 Transformer
authors: "Naichuan Zheng, Hailun Xia, Zepeng Sun, Weiyi Li, Yujia Wang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18062v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对资源受限边缘设备的能效脉冲神经网络设计
tldr: 针对骨骼动作识别中人工神经网络功耗高的问题，本文提出了首个纯脉冲驱动的Transformer架构S3T-Former。该模型通过多流解剖脉冲嵌入（M-ASE）将多模态特征转化为高稀疏事件流，利用侧向脉冲拓扑路由（LSTR）实现按需传播，并引入脉冲状态空间（S3）引擎捕捉长程动态，有效解决了现有脉冲神经网络稀疏性受损和短期遗忘的问题，在保持高精度的同时显著降低了能耗。
selection_source: fresh_fetch
motivation: 现有的骨骼动作识别脉冲神经网络因密集矩阵运算和非稀疏变换导致稀疏性受损，且难以克服脉冲神经元的短期遗忘问题。
method: 提出了一种纯脉冲驱动的架构，包含多流解剖脉冲嵌入、侧向脉冲拓扑路由以及用于捕捉长程时间动态的脉冲状态空间引擎。
result: 在多个大规模数据集上的实验表明，该模型在达到极具竞争力的准确率的同时，理论能耗远低于传统人工神经网络。
conclusion: S3T-Former 成功实现了高效能的神经形态动作识别，为资源受限边缘设备上的骨骼数据处理树立了新的基准。
---

## 摘要
基于骨骼的动作识别对多媒体应用至关重要，但其严重依赖高功耗的人工神经网络 (ANN)，限制了在资源受限边缘设备上的部署。脉冲神经网络 (SNN) 提供了一种高能效的替代方案；然而，现有的骨骼数据脉冲模型往往通过采用密集矩阵聚合、沉重的多模态融合模块或非稀疏的频域变换，损害了 SNN 固有的稀疏性。此外，它们还深受脉冲神经元短期遗忘问题的困扰。在本文中，我们提出了脉冲状态空间拓扑 Transformer (S3T-Former)，据我们所知，这是首个专门为高能效骨骼动作识别设计的纯脉冲驱动 Transformer 架构。我们没有依赖沉重的融合开销，而是构建了一种多流解剖脉冲嵌入 (M-ASE)，它作为一个广义运动学微分算子，能够优雅地将多模态骨骼特征转换为异构且高度稀疏的事件流。为了实现真正的拓扑和时间稀疏性，我们引入了用于按需条件脉冲传播的侧向脉冲拓扑路由 (LSTR)，以及一个脉冲状态空间 (S3) 引擎，以便在不使用非稀疏频谱变通方案的情况下系统地捕获远程时间动态。在多个大规模数据集上的广泛实验表明，S3T-Former 在实现极具竞争力的准确率的同时，理论上比传统 ANN 显著降低了能耗，为高能效类脑动作识别树立了新的标杆。

## Abstract
Skeleton-based action recognition is crucial for multimedia applications but heavily relies on power-hungry Artificial Neural Networks (ANNs), limiting their deployment on resource-constrained edge devices. Spiking Neural Networks (SNNs) provide an energy-efficient alternative; however, existing spiking models for skeleton data often compromise the intrinsic sparsity of SNNs by resorting to dense matrix aggregations, heavy multimodal fusion modules, or non-sparse frequency domain transformations. Furthermore, they severely suffer from the short-term amnesia of spiking neurons. In this paper, we propose the Spiking State-Space Topology Transformer (S3T-Former), which, to the best of our knowledge, is the first purely spike-driven Transformer architecture specifically designed for energy-efficient skeleton action recognition. Rather than relying on heavy fusion overhead, we formulate a Multi-Stream Anatomical Spiking Embedding (M-ASE) that acts as a generalized kinematic differential operator, elegantly transforming multimodal skeleton features into heterogeneous, highly sparse event streams. To achieve true topological and temporal sparsity, we introduce Lateral Spiking Topology Routing (LSTR) for on-demand conditional spike propagation, and a Spiking State-Space (S3) Engine to systematically capture long-range temporal dynamics without non-sparse spectral workarounds. Extensive experiments on multiple large-scale datasets demonstrate that S3T-Former achieves highly competitive accuracy while theoretically reducing energy consumption compared to classic ANNs, establishing a new state-of-the-art for energy-efficient neuromorphic action recognition.

---

## 论文详细总结（自动生成）

以下是对论文《S3T-Former: A Purely Spike-Driven State-Space Topology Transformer for Skeleton Action Recognition》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证高精度的前提下，实现极低功耗的骨骼动作识别。
*   **研究背景**：传统的动作识别依赖人工神经网络（ANN），其密集的浮点乘加运算（MAC）在边缘设备上能耗极高。脉冲神经网络（SNN）虽能通过二值脉冲和加法运算（AC）降低能耗，但现有骨骼脉冲模型存在以下痛点：
    1.  **稀疏性受损**：为了处理多模态特征，常引入密集的矩阵聚合或非稀疏的频域变换，违背了 SNN 的稀疏初衷。
    2.  **短期遗忘**：脉冲神经元（LIF）具有固有的指数级衰减，难以捕捉长程的时间因果关系。
    3.  **拓扑建模低效**：沿用 GCN 的密集矩阵乘法，未能实现真正的“按需”路由。

### 2. 方法论
S3T-Former 提出了一套纯脉冲驱动的 Transformer 架构，核心组件包括：
*   **多流解剖脉冲嵌入 (M-ASE)**：模拟视网膜神经节细胞对时空梯度的响应。它将原始骨骼坐标转化为零阶（恒等）、一阶空间梯度（骨骼向量）和一阶时间梯度（关节速度）三个流，通过微分运算提取高阶运动学特征，将密集数据转化为高度稀疏的事件流。
*   **非对称时间梯度 QKV (ATG-QKV)**：受视觉系统 M/P 细胞分工启发。强制查询（Q）和键（K）仅在动态运动梯度上触发脉冲，而值（V）保留静态结构特征。这极大地抑制了背景冗余，使脉冲触发率降低了 1-2 个数量级。
*   **侧向脉冲拓扑路由 (LSTR)**：将 $O(N^2)$ 的全连接注意力简化为基于人体解剖图的条件加法。脉冲仅在有物理连接的关节间传播，将复杂的矩阵运算转化为硬件友好的稀疏加法（AC）。
*   **脉冲状态空间 (S3) 引擎**：引入线性复杂度的状态空间存储池，系统性地累积全局历史拓扑特征，解决了 LIF 神经元的“短期遗忘”问题。
*   **U-Readout 机制**：在输出层使用非脉冲的积分发放（IF）神经元作为无损累加器，直接利用膜电位进行分类，避免了传统脉冲计数带来的量化误差。

### 3. 实验设计
*   **数据集**：
    *   **NTU RGB+D 60**：包含 60 类动作，采用 Cross-Subject (X-Sub) 和 Cross-View (X-View) 协议。
    *   **NTU RGB+D 120**：目前规模最大、最具挑战性的数据集，包含 120 类动作。
    *   **NW-UCLA**：用于验证跨视角识别能力。
*   **Benchmark 与对比方法**：
    *   **ANN 模型**：ST-GCN, 2S-AGCN, CTR-GCN, 以及最新的 Transformer 模型如 Hyperformer 和 SkateFormer。
    *   **SNN 模型**：Spikformer, Spike-driven Transformer V2, MK-SGN, Signal-SGN 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA Tesla V100 GPU** 进行加速训练。
*   **训练细节**：采用 AdamW 优化器，基础学习率 0.01，权重衰减 0.0005，训练 250 个 epoch。
*   **能效分析**：基于 45nm CMOS 工艺计算，S3T-Former 的理论能耗被证明**低于同等规模 ANN 的 10%**。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流数据集上进行了详尽测试，涵盖了单模态（关节）和多模态（关节+骨骼+运动）的对比。
*   **消融实验**：非常充分。作者逐步验证了 M-ASE、LSTR、S3-Engine、ATG-QKV 和 U-Readout 的增益，并对 LSTR 的路由策略（多头数量、缩放因子）和 S3 引擎的衰减因子进行了参数敏感性分析。
*   **可视化分析**：提供了 t-SNE 聚类、时空激活热力图、拓扑路由动态图以及输出置信度轨迹图，从多个维度证明了模型的有效性和客观性。

### 6. 主要结论与发现
*   **性能领先**：S3T-Former 在 SNN 领域达到了新的 SOTA，在 NTU-120 数据集上比之前的最优脉冲模型 Signal-SGN 准确率提升了高达 **6.38%**。
*   **超越部分 ANN**：其单流模型（84.94%）在准确率上已经超越了经典的 ANN 模型如 ST-GCN（81.5%）。
*   **极高稀疏性**：通过 ATG-QKV 机制，模型在深层网络中实现了极高的语义稀疏性，仅在关键动作节点（如踢腿时的脚踝）产生脉冲。

### 7. 优点
*   **纯脉冲驱动**：完全摒弃了浮点乘法，实现了真正的事件驱动计算。
*   **生物启发式设计**：ATG-QKV 和 M-ASE 的设计具有明确的神经科学依据，逻辑自洽。
*   **线性复杂度**：S3 引擎和 LSTR 将 Transformer 的计算复杂度降至线性，适合处理长序列。
*   **能效卓越**：在保持高精度的同时，理论功耗极低，具有极强的边缘端应用潜力。

### 8. 不足与局限
*   **微小动作识别困难**：由于模型为了节能而采取了激进的稀疏化策略（过滤静态特征），在识别如“读书”、“写字”、“玩手机”等仅有手指微小位移且空间位置极其接近的动作时，容易产生混淆（错误率接近 40%-50%）。
*   **硬件依赖**：虽然理论能效高，但要发挥其 AC 运算优势，仍需依赖专门的类脑芯片（如显存访问优化），在传统 GPU 上无法直接体现速度优势。
*   **应用限制**：目前主要针对离线或固定窗口的骨骼数据，对于实时流式数据的自适应阈值调整仍有待探索。

（完）
