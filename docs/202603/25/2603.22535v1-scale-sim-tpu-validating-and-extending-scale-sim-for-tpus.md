---
title: "SCALE-Sim TPU: Validating and Extending SCALE-Sim for TPUs"
title_zh: SCALE-Sim TPU：针对 TPU 的 SCALE-Sim 验证与扩展
authors: "Jingtian Dang, Ritik Raj, Changhai Man, Jianming Tong, Tushar Krishna"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22535v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对真实硬件验证脉动阵列加速器模拟器，并与机器学习编译栈集成
tldr: 本研究针对脉动阵列加速器模拟器缺乏硬件验证及编译器集成的问题，提出了 SCALE-Sim TPU。该工具基于 SCALE-Sim v3，通过 Google TPU v4 硬件验证建立了 GEMM 周期与延迟的线性映射，并引入轻量级学习模型预测非脉动操作延迟，同时集成 StableHLO 前端以支持 JAX 和 PyTorch 工作负载。这显著提升了全模型性能分析的准确性与实用性。
selection_source: fresh_fetch
motivation: 现有的脉动阵列模拟器往往缺乏真实硬件验证，且难以与现代机器学习编译器栈无缝集成。
method: 通过 TPU v4 验证 GEMM 模型，引入基于张量特征的轻量级非脉动操作延迟模型，并开发了 StableHLO 编译器前端。
result: "模拟周期与硬件延迟呈现强线性相关，非脉动操作预测中位误差低于 3%，实现了端到端的高精度模拟。"
conclusion: SCALE-Sim TPU 增强了周期精确模拟在 TPU 类加速器上的保真度、覆盖范围和实用价值。
---

## 摘要
周期精确模拟器被广泛用于研究脉动阵列加速器，但其准确性和易用性往往受限于缺乏针对真实硬件的验证，以及与现代机器学习编译栈的集成度较低。本文介绍了 SCALE-Sim TPU，这是针对 TPU 风格加速器经过验证且扩展的 SCALE-Sim v3 版本。具体而言，我们做出了三项贡献：(1) 我们针对 Google TPU v4 的测量结果验证了 SCALE-Sim 的脉动 GEMM 模型，并证明模拟周期数与硬件延迟之间存在强线性相关性，从而实现了简单的周期到延迟映射。(2) 我们为非脉动逐元素操作引入了轻量级学习型延迟模型，仅利用张量大小和形状即可实现低于 3% 的中值相对误差，显著提高了端到端延迟估计的准确性。(3) 我们集成了一个基于 StableHLO 的前端，使得来自 JAX 和 PyTorch 等现代机器学习框架的工作负载能够通过统一的编译器中间表示（IR）直接进行模拟。总之，这些贡献提高了针对 TPU 全模型性能分析的周期精确模拟的保真度、覆盖范围和实用性。

## Abstract
Cycle-accurate simulators are widely used to study systolic accelerators, yet their accuracy and usability are often limited by weak validation against real hardware and poor integration with modern ML compiler stacks. This paper presents SCALE-Sim TPU, a validated and extended version of SCALE-Sim v3 for TPU-style accelerators. Specifically, we make three contributions: (1) We validate SCALE-Sim's systolic GEMM model against measurements on Google TPU v4 and show that simulated cycle counts exhibit a strong linear correlation with hardware latency, enabling a simple cycle-to-latency mapping. (2) We introduce lightweight learned latency models for non-systolic elementwise operations, achieving median relative errors below 3 percent using only tensor size and shape, substantially improving end-to-end latency estimation. (3) We integrate a StableHLO-based frontend that allows workloads from modern ML frameworks such as JAX and PyTorch to be simulated directly via a unified compiler IR. Together, these contributions improve the fidelity, coverage, and practicality of cycle-accurate simulation for whole-model performance analysis on TPUs.

---

## 论文详细总结（自动生成）

### 论文总结：SCALE-Sim TPU

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：脉动阵列（Systolic Array）加速器（如 Google TPU）是现代机器学习的核心硬件。然而，现有的周期精确模拟器（如 SCALE-Sim）存在两个主要痛点：
    1.  **缺乏真实硬件验证**：模拟结果往往是理论性的，缺乏与生产级硬件（如 TPU v4）的对比验证。
    2.  **覆盖范围有限**：大多数模拟器仅关注 GEMM（矩阵乘法）和卷积，忽略了非脉动操作（如逐元素加法、激活函数等），而这些操作在实际模型中可能占据 15%–48% 的延迟。
    3.  **集成困难**：模拟器通常需要手写的配置格式，难以直接对接 JAX、PyTorch 等现代 ML 框架。
*   **整体含义**：本文提出了 **SCALE-Sim TPU**，通过硬件验证、引入学习型延迟模型和 StableHLO 前端，构建了一个更具保真度、覆盖全模型且易于使用的 TPU 性能分析工具。

#### 2. 方法论：核心思想与关键技术
*   **TPU 驱动的 GEMM 验证**：
    *   将 SCALE-Sim 配置为模拟 TPU v4 的 128×128 脉动阵列。
    *   建立线性映射模型：$\hat{t} = \alpha \cdot \text{cycles} + \beta$。其中 $\alpha$ 是模拟周期到实际时间的转换系数，$\beta$ 是固定开销。
*   **非脉动操作的学习型延迟模型**：
    *   针对 Add、ReLU 等逐元素操作，采用**直方图梯度提升回归（HGBR）**模型。
    *   **特征选择**：使用张量的大小（Size）和形状（Shape）作为输入特征，以捕捉硬件在向量化、对齐和调度阈值方面的非线性行为。
*   **StableHLO 编译器前端**：
    *   利用 StableHLO 作为统一的中间表示（IR），自动解析 JAX 或 PyTorch 编译后的算子元数据（形状、类型、属性）。
    *   **算子路由**：将 GEMM/卷积路由至验证过的脉动模型，将逐元素操作路由至学习型延迟模型。

#### 3. 实验设计
*   **硬件平台**：Google Cloud TPU v4。
*   **GEMM 实验设置**：
    *   划分为三个规模：Small (32–128)、Medium (128–1024)、Large (1024–4096)。
    *   对 $M, K, N$ 维度进行参数扫描，记录模拟周期与 TPU 实际内核运行时间。
*   **逐元素操作实验**：
    *   对 1D 张量（长度 32–8192）和 2D 张量（维度 64–1024）进行扫描。
    *   对比算子：bf16 类型的 Elementwise Add 和 ReLU。
*   **基准指标**：$R^2$ 相关系数、均方根误差（RMSE）、平均绝对误差（MAE）和平均绝对百分比误差（MAPE）。

#### 4. 资源与算力
*   **硬件资源**：实验在 Google TPU v4 上进行测量。
*   **算力消耗**：文中未明确提及训练 HGBR 模型所需的具体算力或时长，但强调该模型是“轻量级（lightweight）”的，且推理过程在模拟器中几乎不增加额外负担。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   GEMM 验证涵盖了从极小到大规模的多种形状组合，样本量足以支撑线性回归分析。
    *   逐元素操作通过 log-uniform 采样覆盖了高达 16M 个元素的张量形状。
*   **充分性评价**：实验设计较为充分，特别是在 GEMM 验证中区分了不同的规模区间（Regimes），揭示了模拟器在不同负载下的表现差异。对比了多种算子（Add, ReLU），证明了学习型模型的泛化能力。

#### 6. 主要结论与发现
*   **GEMM 线性相关性**：模拟周期与 TPU 实际延迟呈现强线性相关，中、大规模下的 $R^2$ 超过 0.97。
*   **延迟预测精度**：
    *   逐元素操作的预测非常精确，Add 的中位相对误差为 1.78%，ReLU 为 2.55%。
    *   GEMM 的整体 MAPE 为 32.2%，误差主要集中在由于编译器调度和内存带宽限制导致的“中等规模”负载中。
*   **全模型潜力**：通过结合脉动模型和学习模型，SCALE-Sim TPU 能够实现更准确的端到端延迟估计。

#### 7. 优点
*   **硬件落地**：打破了模拟器仅停留在理论周期的局限，提供了到物理时间（秒）的可靠映射。
*   **混合建模**：巧妙结合了分析型模型（针对结构化的脉动阵列）和机器学习模型（针对复杂的非脉动逻辑），平衡了速度与精度。
*   **生态集成**：支持 StableHLO，使其能够直接处理来自主流深度学习框架的真实工作负载。

#### 8. 不足与局限
*   **数据传输建模缺失**：目前的验证主要针对内核执行（on-chip execution），排除了 HBM 到核心的数据传输延迟。
*   **算子覆盖范围**：虽然引入了逐元素操作，但对于更复杂的非脉动算子（如 Softmax、LayerNorm 或 Attention 专用硬件逻辑）尚未完全覆盖。
*   **编译器黑盒**：TPU 编译器的某些优化（如算子融合、复杂的平铺策略）在模拟器中仍难以完全还原，导致中等规模负载下存在预测偏差。

（完）
