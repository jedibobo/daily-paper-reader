---
title: "DS2SC-Agent: A Multi-Agent Automated Pipeline for Rapid Chiplet Model Generation"
title_zh: DS2SC-Agent：一种用于快速 Chiplet 模型生成的多智能体自动化流水线
authors: "Yiwei Wu, Yifan Wu, Yunhao Xiong, Dengwei Zhao, Jiaxuan Shen, Jianfei Jiang, Guanghui He, Shikui Tu, Yanan Sun"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21190v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 用于架构探索的自动化芯粒模型生成
tldr: 针对传统Chiplet行为级建模耗时且易错的问题，本文提出DS2SC-Agent，这是首个将原始数据手册直接转换为SystemC模型的全自动多智能体流水线。该框架通过解耦复杂建模任务，实现了长文档解析、核心代码构建、测试激励生成及闭环调试的自动化。实验证明，该系统能高效处理模拟、数字及射频领域的复杂手册，生成功能正确的模型，显著提升了敏捷建模效率并减少了人工干预。
selection_source: fresh_fetch
motivation: 传统手动编写Chiplet模型效率低下，且现有LLM框架难以直接处理非结构化、长篇幅的原始数据手册。
method: 提出一种多智能体协作框架，通过解耦建模任务实现从文档解析到代码生成、测试及自适应调试的全流程自动化。
result: 在模拟、数字和射频等多种功能芯片组上的评估显示，该流水线能稳定生成功能正确的SystemC模型。
conclusion: DS2SC-Agent为敏捷构建芯片模型库提供了一种高效可靠的范式，极大减少了人工干预。
---

## 摘要
构建行为级 Chiplet 模型（如 SystemC）对于早期异构架构探索至关重要。传统的手动建模过程耗时且易出错。近年来，大语言模型（LLM）在自动化硬件代码生成方面展现出巨大潜力。然而，现有的 LLM 辅助设计框架主要针对高度结构化或定义明确的设计规范。在实际工程场景中，原始数据手册（datasheet）通常包含冗长、复杂且高度非结构化的信息。因此，直接从这些原始数据手册中生成可靠代码面临严峻挑战，包括上下文消失和逻辑幻觉。为克服这一关键瓶颈，本文提出了 DS2SC-Agent（数据手册到 SystemC 智能体）：首个将原始数据手册直接转换为 SystemC Chiplet 模型的端到端全自动化生成流水线。该系统建立了一个高效的多智能体协作框架。通过解耦复杂的建模任务，所提出的流水线编排了一个全自动工作流，涵盖了非结构化长文档解析、SystemC 核心代码构建、测试平台（testbench）激励生成以及自适应闭环调试。我们在模拟、数字和射频（RF）领域的代表性单功能 Chiplet 上对所提框架进行了全面评估，具体包括限幅放大器（LA）、快速傅里叶变换（FFT）模块和功率放大器（PA）。评估结果表明，我们的流水线能够无缝处理复杂的真实世界数据手册，并稳定生成功能正确的 SystemC 模型。这为敏捷模型库构建提供了一种高效且可靠的范式，同时极大地减少了人工干预。

## Abstract
Constructing behavioral-level chiplet models (e.g., SystemC) is crucial for early-stage heterogeneous architecture exploration. Traditional manual modeling is notoriously time-consuming and error-prone. Recently, Large Language Models (LLMs) have demonstrated immense potential in automating hardware code generation. However, existing LLM-assisted design frameworks predominantly target highly structured or well-defined design specifications. In practical engineering scenarios, raw datasheets typically encompass lengthy, complex, and highly unstructured information. Consequently, reliable code generation directly from these raw datasheets suffers from severe challenges, including context vanishing and logical hallucinations.To overcome this critical bottleneck, this paper proposes DS2SC-Agent(Datasheet-to-SystemC-Agent): the first end-to-end, fully automated generation pipeline that translates raw datasheets directly into SystemC chiplet models. This system establishes a highly efficient multi-agent collaborative framework. By decoupling the intricate modeling tasks, the proposed pipeline orchestrates a fully automated workflow encompassing unstructured long-document parsing, SystemC core code construction, testbench stimulus generation, and adaptive closed-loop debugging. We comprehensively evaluate the proposed framework on representative single-function chiplets across the analog, digital, and radio frequency (RF) domains--specifically, a Limiting Amplifier (LA), a Fast Fourier Transform (FFT) module, and a Power Amplifier (PA). The evaluation demonstrates that our pipeline seamlessly processes complex real-world datasheets to consistently generate functionally correct SystemC models. This provides a highly efficient and reliable paradigm for agile model library construction while drastically minimizing manual intervention.

---

## 论文详细总结（自动生成）

### DS2SC-Agent：用于快速 Chiplet 模型生成的多智能体自动化流水线总结

#### 1. 核心问题与研究动机
*   **研究背景**：在后摩尔时代，Chiplet（芯粒）技术成为异构集成的关键。为了进行早期的架构探索和软硬件协同验证，构建精确的行为级模型（如 SystemC 模型）至关重要。
*   **核心问题**：传统的手动建模依赖领域专家阅读动辄数十页的工业级数据手册（Datasheet），过程耗时、易错且难以扩展。
*   **LLM 的局限性**：虽然大语言模型（LLM）在代码生成上表现出色，但直接处理非结构化、长篇幅且包含图表的原始数据手册时，容易出现“上下文消失”和“逻辑幻觉”，难以生成功能正确的硬件代码。

#### 2. 方法论：DS2SC-Agent 框架
该论文提出了一个由四个专门智能体协作的端到端流水线，核心思想是将复杂的建模任务解耦：
*   **规范解析智能体 (Specification Parsing Agent)**：
    *   采用“混合填充”策略，利用预定义的 **Spec IR (中间表示)** 模板（JSON 格式）。
    *   从原始 PDF 中提取引脚列表、寄存器映射、电气特性等关键参数，将其标准化为机器可读的格式。
*   **代码生成智能体 (Code Generation Agent)**：
    *   基于 Spec IR 构建 SystemC 核心代码。
    *   采用“仅头文件 (Header-only)”设计模式，将模块声明、端口定义和行为逻辑整合，便于后续集成。
*   **测试平台生成智能体 (Testbench Generation Agent)**：
    *   根据 Spec IR 中的测试用例生成 `main.cpp`。
    *   实现黑盒验证，包括激励产生、响应监控和结果比对逻辑。
*   **自动调试智能体 (Auto Debugging Agent)**：
    *   **闭环反馈**：若编译或仿真失败，该智能体捕获错误日志。
    *   **思维链 (CoT) 推理**：分析错误根源，制定修改策略，并重新生成完整的修复代码，直到通过验证。

#### 3. 实验设计
论文在三个不同物理领域的代表性单功能 Chiplet 上进行了评估：
*   **数字领域 (FFT)**：基于 96 页的 Xilinx FFT IP 手册，验证其对复杂算法逻辑和 TLM-2.0 总线接口的提取能力。
*   **模拟领域 (限幅放大器 LA)**：基于 6 页的密集成电器特性表，验证其对连续时间域物理行为（线性放大、非线性钳位）的建模。
*   **射频领域 (功率放大器 PA)**：基于 18 页包含性能曲线的手册，验证其对非线性转移函数（如 AM-AM 转换）的拟合能力。
*   **Benchmark**：由于该领域尚无直接的自动化基准工具，实验主要对比了生成模型与原始数据手册规范的功能一致性（如 FFT 的计算精度、PA 的功率曲线拟合度）。

#### 4. 资源与算力
*   **核心引擎**：使用了 **Gemini 1.5 Flash** 模型（通过 API 调用）。
*   **超参数设置**：解析智能体温度设为 0.2（高确定性），生成和调试智能体设为 0.3-0.4（兼顾灵活性）。
*   **算力说明**：文中未提及具体的本地 GPU 训练资源，因为该框架基于现有的预训练 LLM API 构建。仿真环境使用了标准的 GCC 编译器及 SystemC 2.3.3 / SystemC-AMS 2.3.4 库。

#### 5. 实验数量与充分性
*   **实验规模**：针对数字、模拟、射频三个跨度极大的领域各进行了一个深度案例研究。
*   **充分性评价**：
    *   **多样性**：涵盖了从 6 页到 96 页不同长度、不同数据模态（文本、表格、图表）的手册，具有较好的代表性。
    *   **客观性**：通过量化的误差分析（如 FFT 误差在 $10^{-16}$ 数量级，PA 拟合误差小于 1dB）证明了结果的可靠性。
    *   **局限性**：实验主要集中在单功能 Chiplet，尚未涉及多 Chiplet 互连的复杂系统级验证。

#### 6. 主要结论与发现
*   **端到端可行性**：DS2SC-Agent 成功实现了从原始 PDF 到可执行 SystemC 模型的全自动转换。
*   **多智能体优势**：通过任务解耦和 Spec IR 约束，有效解决了 LLM 处理长文档时的幻觉问题。
*   **高保真度**：生成的模型不仅能通过语法编译，还能精确还原复杂的物理特性（如非线性饱和、动态使能逻辑）。

#### 7. 优点与亮点
*   **首创性**：这是首个针对原始数据手册到 SystemC 模型的全自动生成流水线。
*   **Spec IR 设计**：引入中间表示层作为“单一事实来源”，确保了下游智能体的一致性。
*   **闭环自愈**：具备自动调试功能，能够自主修复语法和逻辑错误，显著减少人工干预。
*   **跨领域支持**：框架具有良好的通用性，能同时处理数字、模拟和射频组件。

#### 8. 不足与局限
*   **人工预填充需求**：Spec IR 模板仍需人工预填部分系统级约束（如特定的仿真场景），尚未达到 100% 的“零人工”输入。
*   **复杂系统限制**：目前仅验证了单功能组件，对于具有复杂协议栈或多核交互的 Chiplet 系统，其建模能力有待验证。
*   **依赖外部 API**：对商业 LLM API 的依赖可能带来数据隐私风险和推理延迟问题。
*   **图表解析挑战**：虽然通过数据点辅助解决了 PA 曲线问题，但 LLM 对复杂原始图表的直接视觉解析能力仍是瓶颈。

（完）
