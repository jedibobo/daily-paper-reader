---
title: "Beyond MACs: Hardware Efficient Architecture Design for Vision Backbones"
title_zh: 超越 MACs：视觉骨干网络的硬件高效架构设计
authors: "Moritz Nottebaum, Matteo Dunnhofer, Christian Micheloni"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26551v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 边缘设备视觉骨干网的硬件高效架构设计
tldr: 本研究指出传统的MACs指标在衡量视觉骨干网络效率（尤其是边缘设备）时存在局限性。通过分析架构设计对执行时间的影响，作者提出了LowFormer系列网络。该架构采用简化的宏观与微观设计，并引入了轻量级的Lowtention机制替代传统多头自注意力。LowFormer在ImageNet及目标检测、分割等任务中表现优异，在多种硬件平台上实现了显著的加速，为高效视觉模型设计提供了新思路。
selection_source: fresh_fetch
motivation: 传统的MACs指标无法准确预测边缘设备上的实际执行时间，因此需要探索更符合硬件特性的架构设计准则。
method: 提出了LowFormer架构，其核心是利用轻量级的Lowtention机制简化注意力计算，并结合了优化的宏观与微观设计。
result: LowFormer在ImageNet上取得了优于基准的效果，并在多种硬件平台上显著提升了推理速度，同时在下游任务中表现出色。
conclusion: LowFormer证明了结合硬件效率洞察进行架构优化的重要性，是一种在多种视觉任务中兼顾速度与性能的高效骨干网络。
---

## 摘要
视觉骨干网络在现代计算机视觉中发挥着核心作用。提高其效率直接惠及广泛的下游应用。为了衡量效率，许多文献依赖 MACs（乘加运算次数）作为执行时间的预测指标。在本文中，我们通过实验证明了该指标的局限性，特别是在边缘设备的背景下。通过对比常见架构设计元素的 MAC 计数和执行时间，我们确定了高效执行的关键因素，并为优化骨干网络设计提供了见解。基于这些见解，我们提出了 LowFormer，一个新颖的视觉骨干网络系列。LowFormer 具有精简的宏观和微观设计，其中包括 Lowtention，这是一种多头自注意力机制（Multi-Head Self-Attention）的轻量级替代方案。Lowtention 不仅被证明更高效，而且在 ImageNet 上取得了更优异的结果。此外，我们还提出了 LowFormer 的边缘 GPU 版本，它可以进一步提高其在边缘 GPU 和桌面 GPU 上的基准速度。我们通过在较小的图像分类数据集上进行评估，并将其应用于目标检测、语义分割、图像检索和视觉目标跟踪等多个下游任务，证明了 LowFormer 的广泛适用性。与最近的最先进骨干网络相比，LowFormer 模型在各种硬件平台上始终实现了显著的加速。代码和模型可在 https://github.com/altair199797/LowFormer/blob/main/Beyond_MACs.md 获取。

## Abstract
Vision backbone networks play a central role in modern computer vision. Enhancing their efficiency directly benefits a wide range of downstream applications. To measure efficiency, many publications rely on MACs (Multiply Accumulate operations) as a predictor of execution time. In this paper, we experimentally demonstrate the shortcomings of such a metric, especially in the context of edge devices. By contrasting the MAC count and execution time of common architectural design elements, we identify key factors for efficient execution and provide insights to optimize backbone design. Based on these insights, we present LowFormer, a novel vision backbone family. LowFormer features a streamlined macro and micro design that includes Lowtention, a lightweight alternative to Multi-Head Self-Attention. Lowtention not only proves more efficient, but also enables superior results on ImageNet. Additionally, we present an edge GPU version of LowFormer, that can further improve upon its baseline's speed on edge GPU and desktop GPU. We demonstrate LowFormer's wide applicability by evaluating it on smaller image classification datasets, as well as adapting it to several downstream tasks, such as object detection, semantic segmentation, image retrieval, and visual object tracking. LowFormer models consistently achieve remarkable speed-ups across various hardware platforms compared to recent state-of-the-art backbones. Code and models are available at https://github.com/altair199797/LowFormer/blob/main/Beyond_MACs.md.

---

## 论文详细总结（自动生成）

### 论文总结：超越 MACs —— 视觉骨干网络的硬件高效架构设计

#### 1. 核心问题与整体含义（研究动机和背景）
论文指出，当前视觉骨干网络设计中普遍存在一个误区：将 **MACs（乘加运算次数）** 作为衡量模型效率的唯一或主要指标。然而，在实际部署（尤其是边缘设备）中，MACs 并不能准确预测执行时间（Latency）。
*   **研究动机**：内存访问成本（Memory Access Cost）和硬件并行度（Parallelism）对实际速度影响巨大。低 MACs 的模型可能因为频繁的内存读写或低并行度操作（如深度卷积）而在硬件上运行缓慢。
*   **核心目标**：通过深入分析不同硬件平台上的架构设计元素，提出一种真正“硬件友好”的视觉骨干网络架构，以优化实际的“速度-精度”权衡。

#### 2. 方法论：核心思想与关键技术
作者提出了 **LowFormer** 架构及其核心组件 **Lowtention**，主要技术细节包括：
*   **Lowtention（轻量级注意力机制）**：
    *   **通道压缩**：在进行缩放点积注意力（SDA）之前，通过点卷积将通道数减半，计算后再恢复，显著降低内存带宽压力。
    *   **分辨率下采样**：利用深度卷积在注意力计算前对特征图进行下采样，使 SDA 在较低分辨率上运行，随后再上采样恢复。
*   **宏观架构设计（Macro Design）**：
    *   **混合卷积策略**：在通道数较少的早期阶段使用 **Fused MBConv**（融合移动翻转瓶颈层），在通道数较多的后期阶段使用标准的 **MBConv**，以平衡计算量与并行效率。
    *   **非均匀层分布**：将更多层集中在低分辨率阶段（最后两个阶段），减少高分辨率下的计算开销。
*   **边缘 GPU 变体（E1-E3）**：针对边缘 GPU（如 Jetson TX2）的高并行特性，通过移除 MLP 层、减少深度或移除注意力模块，进一步压榨硬件性能。

#### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：ImageNet-1K（核心基准）、Oxford-IIIT-Pets、Stanford Cars、Flowers-102。
    *   **下游任务**：COCO 2017（目标检测）、ADE20K（语义分割）、GPR1200（图像检索）、GOT10K/LaSOT/TREK-150 等（视觉跟踪）。
*   **对比方法**：包括 MobileOne, FastViT, RepViT, EfficientMod, EdgeViT, BiFormer, PVTv2 等近年来主流的高效视觉模型。
*   **硬件平台**：Nvidia A40 (服务器 GPU), Titan RTX (桌面 GPU), Jetson TX2 (边缘 GPU), iPhone 13 (手机 NPU/GPU), Raspberry Pi 5 (ARM CPU)。

#### 4. 资源与算力
*   **训练算力**：使用了 Nvidia A40 和 Titan RTX 进行训练。
*   **训练细节**：ImageNet 训练 320 个 epoch，使用 AdamW 优化器，学习率为 $10^{-3}$。LowFormer-B3 的 Batch Size 达到 2400。
*   **推理测试**：在多种设备上进行了详尽的延迟（Latency）和吞吐量（Throughput）测试，使用了 TensorRT、ONNX Runtime 和 CoreML 等推理框架。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从基础分类到多个复杂下游任务的广泛实验。
*   **消融实验**：针对 Fused MBConv、Lowtention 的通道压缩、下采样策略以及 MLP 的必要性做了详细的对比实验。
*   **客观性**：实验跨越了多种硬件架构（GPU, ARM CPU, NPU），不仅关注 MACs，更侧重于实际延迟和功耗（Power Consumption），对比维度非常全面且客观。

#### 6. 主要结论与发现
*   **MACs 的局限性**：实验证明，在相同 MACs 下，不分组卷积在边缘设备上往往比深度卷积更快；高分辨率阶段的层数对延迟影响远超低分辨率阶段。
*   **LowFormer 的优越性**：LowFormer-B0 到 B3 系列在 ImageNet 上以更低的延迟实现了更高的精度。例如，LowFormer-B0 在 Jetson TX2 上的速度是同精度模型的 2 倍。
*   **Lowtention 的高效性**：通过通道和分辨率的双重压缩，Lowtention 在保持注意力的全局建模能力的同时，极大地缓解了其计算瓶颈。

#### 7. 优点
*   **硬件导向**：设计思路紧贴实际部署需求，而非盲目追求理论计算量的降低。
*   **通用性强**：不仅在分类任务上表现好，在目标检测、分割、检索甚至视频跟踪任务中均展现出极高的适配性和效率提升。
*   **多平台优化**：提供了针对不同硬件特性（如边缘 GPU 与手机 NPU）的定制化建议和变体。

#### 8. 不足与局限
*   **手机端优化偏差**：虽然在边缘 GPU（Jetson）和 CPU 上表现惊人，但在 iPhone 13 的 CoreML 环境下，其边缘变体（E 系列）的加速效果不如在 GPU 上明显，说明不同硬件加速器的偏好仍有差异。
*   **参数量较大**：为了换取执行速度，LowFormer 的参数量（Params）通常比同等 MACs 的模型略大，这在对存储空间极其敏感的超微型设备上可能是一个限制。
*   **训练成本**：虽然推理快，但文中提到大模型（如 B3）需要较大的 Batch Size 和特定的训练策略，训练资源需求相对较高。

（完）
