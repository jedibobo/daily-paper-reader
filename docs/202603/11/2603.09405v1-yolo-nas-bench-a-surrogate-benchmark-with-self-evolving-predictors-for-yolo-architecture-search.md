---
title: "YOLO-NAS-Bench: A Surrogate Benchmark with Self-Evolving Predictors for YOLO Architecture Search"
title_zh: YOLO-NAS-Bench：一种用于 YOLO 架构搜索的带有自进化预测器的代理基准
authors: "Zhe Li, Xiaoyu Ding, Jiaxin Zheng, Yongtao Wang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09405v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 针对YOLO架构搜索的算子和深度代理基准
tldr: 针对目标检测NAS评估成本高昂的问题，本文推出了首个针对YOLO系列检测器的代理基准测试集YOLO-NAS-Bench。该基准涵盖了从YOLOv8到YOLO12的核心模块搜索空间，通过采样1500个架构并结合自进化机制训练LightGBM预测器，显著提升了高精度区域的预测准确性。实验证明，基于该预测器搜索出的架构在COCO-mini上超越了官方基线模型。
selection_source: fresh_fetch
motivation: 目标检测NAS因训练成本极高且缺乏专用基准，导致研究受阻。
method: 构建涵盖YOLO核心组件的搜索空间，并提出一种自进化机制，通过迭代优化预测器在高性能前沿的分布对齐。
result: 预测器的R2和Sparse Kendall Tau指标显著提升，且搜索出的新架构在同等延迟下性能优于YOLOv8至YOLO12官方模型。
conclusion: YOLO-NAS-Bench为检测器架构搜索提供了高效且准确的评估工具，推动了高性能YOLO架构的自动化设计。
---

## 摘要
目标检测领域的神经架构搜索（NAS）受到高昂评估成本的严重制约，因为在 COCO 数据集上完整训练每个候选 YOLO 架构需要数天的 GPU 时间。与此同时，现有的 NAS 基准主要针对图像分类，使得检测社区缺乏用于 NAS 评估的可比基准。为了填补这一空白，我们推出了 YOLO-NAS-Bench，这是首个专为 YOLO 风格检测器定制的代理基准。YOLO-NAS-Bench 定义了一个涵盖骨干网络（backbone）和颈部网络（neck）的通道宽度、模块深度和算子类型的搜索空间，覆盖了从 YOLOv8 到 YOLO12 的核心模块。我们通过随机、分层和拉丁超立方采样策略抽取了 1,000 个架构，在 COCO-mini 上对其进行训练，并构建了一个 LightGBM 代理预测器。为了提高预测器在与 NAS 最相关的高性能区域的精度，我们提出了一种自进化机制（Self-Evolving Mechanism），通过在每次迭代中使用预测器自身来发现和评估具有信息量的架构，从而逐步使预测器的训练分布与高性能前沿对齐。该方法将架构池扩大到 1,500 个，并将集成预测器的 R2 从 0.770 提升至 0.815，Sparse Kendall Tau 从 0.694 提升至 0.752，展示了强大的预测准确性和排名一致性。使用最终的预测器作为进化搜索的适应度函数，我们发现了在 COCO-mini 上以相当的延迟超越了所有官方 YOLOv8-YOLO12 基准的架构，证实了该预测器对高性能检测架构的判别能力。

## Abstract
Neural Architecture Search (NAS) for object detection is severely bottlenecked by high evaluation cost, as fully training each candidate YOLO architecture on COCO demands days of GPU time. Meanwhile, existing NAS benchmarks largely target image classification, leaving the detection community without a comparable benchmark for NAS evaluation. To address this gap, we introduce YOLO-NAS-Bench, the first surrogate benchmark tailored to YOLO-style detectors. YOLO-NAS-Bench defines a search space spanning channel width, block depth, and operator type across both backbone and neck, covering the core modules of YOLOv8 through YOLO12. We sample 1,000 architectures via random, stratified, and Latin Hypercube strategies, train them on COCO-mini, and build a LightGBM surrogate predictor. To sharpen the predictor in the high-performance regime most relevant to NAS, we propose a Self-Evolving Mechanism that progressively aligns the predictor's training distribution with the high-performance frontier, by using the predictor itself to discover and evaluate informative architectures in each iteration. This method grows the pool to 1,500 architectures and raises the ensemble predictor's R2 from 0.770 to 0.815 and Sparse Kendall Tau from 0.694 to 0.752, demonstrating strong predictive accuracy and ranking consistency. Using the final predictor as the fitness function for evolutionary search, we discover architectures that surpass all official YOLOv8-YOLO12 baselines at comparable latency on COCO-mini, confirming the predictor's discriminative power for top-performing detection architectures.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **YOLO-NAS-Bench**，这是首个专门针对 YOLO 系列目标检测器的代理基准（Surrogate Benchmark）。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：目标检测领域的神经架构搜索（NAS）面临极高的评估成本。在 COCO 数据集上完整训练一个 YOLO 架构通常需要数天的 GPU 时间，这使得需要评估数千个候选架构的 NAS 算法变得不可行。
*   **研究背景**：现有的 NAS 基准（如 NAS-Bench-101/201/301）主要针对图像分类任务，其搜索空间和评估协议无法直接应用于检测任务。此外，现有的检测 NAS 方法通常使用私有搜索空间，缺乏统一的公平比较平台。

### 2. 方法论
*   **搜索空间设计**：涵盖了骨干网络（Backbone）和颈部网络（Neck）。
    *   **维度**：通道宽度（Channel）、块深度（Depth）和算子类型（Operator）。
    *   **算子库**：集成了 YOLOv8 至 YOLO12 的核心模块，如 C2f、C3k2、C2PSA、C2fCIB 以及下采样算子 SCDown。
*   **初始数据库构建**：采用随机采样（200个）、分层采样（400个）和拉丁超立方采样（LHS，400个）三种策略，确保架构分布的广泛性和代表性。
*   **自进化预测器（Self-Evolving Predictor）**：
    *   **核心思想**：为了解决均匀采样数据与 NAS 关注的高性能区域之间的分布差异，提出迭代优化机制。
    *   **流程**：将延迟范围划分为 10 个桶（Buckets），在每个桶内利用当前预测器通过进化算法（EA）搜索高性能架构，将其训练后加入数据库，重新训练预测器。
    *   **集成学习**：最终使用 10 个 LightGBM 模型组成集成预测器，以提高排名稳定性。

### 3. 实验设计
*   **数据集**：**COCO-mini**（从 COCO 训练集中按类别和目标大小分层抽取的 10% 子集，保留了原始分布）。
*   **Benchmark 对象**：针对 YOLO 风格的检测器架构。
*   **对比方法**：
    *   **基线模型**：官方 YOLOv8、YOLOv9、YOLOv10、YOLO11、YOLO12。
    *   **预测器类型**：对比了 LightGBM、XGBoost、NGBoost、随机森林和 MLP。
    *   **搜索算法**：使用基于预测器引导的进化搜索（EA）。

### 4. 资源与算力
*   **硬件**：延迟测量在单张 **NVIDIA P40 GPU** 上进行（FP32 精度，Batch Size=1）。
*   **训练细节**：共训练了 **1,500 个架构**（初始 1,000 个 + 自进化 500 个）。每个架构在 COCO-mini 上训练 **120 个 Epoch**。
*   **算力消耗**：虽然文中未给出总 GPU 小时数，但考虑到 1,500 个模型且每个模型在检测任务上训练 120 轮，总算力消耗非常巨大（估计在数千 GPU 小时级别）。

### 5. 实验数量与充分性
*   **实验规模**：训练了 1,500 个完整的检测模型，这在检测 NAS 研究中属于非常大规模的数据积累。
*   **消融实验**：
    *   对比了不同采样策略的效果。
    *   对比了“自进化扩展”与“随机扩展”对预测器性能的影响（证明了针对性增强高性能区域的有效性）。
    *   验证了预测器在不同延迟约束下的搜索能力。
*   **客观性**：所有模型（包括官方基线）均在相同的 COCO-mini 环境和统一的训练协议下重新训练，确保了对比的公平性。

### 6. 主要结论与发现
*   **预测器精度**：自进化机制将预测器的 R² 从 0.770 提升至 0.815，Sparse Kendall Tau (sKT) 从 0.694 提升至 0.752。
*   **架构发现**：通过该基准搜索出的架构（如 Arch-A/B/C/D）在相同的延迟下，性能全面超越了从 YOLOv8 到 YOLO12 的所有官方基线模型。
*   **性能前沿**：在小模型和大模型区间，搜索出的架构均展现出更优的精度-速度平衡（Pareto 优势）。

### 7. 优点
*   **填补空白**：填补了目标检测领域缺乏 YOLO 风格 NAS 代理基准的空白。
*   **自进化机制**：创新性地解决了代理模型在高性能区域预测不准的问题，使预测器更具实用价值。
*   **时效性强**：涵盖了最新的 YOLO12 算子，具有很高的工程参考价值。

### 8. 不足与局限
*   **数据集限制**：基准基于 COCO-mini（10% 数据），虽然分布一致，但在全量 COCO 数据集上的泛化表现仍需进一步验证。
*   **硬件单一**：延迟数据仅基于 NVIDIA P40，不同硬件平台（如端侧 NPU 或最新 GPU）的延迟特性可能存在差异。
*   **延迟预测缺失**：目前仍需实测延迟，未提供高精度的延迟代理模型（尽管文中解释是为了避免硬件偏差）。

（完）
