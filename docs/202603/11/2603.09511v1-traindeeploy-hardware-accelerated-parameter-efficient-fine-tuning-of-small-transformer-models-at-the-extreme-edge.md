---
title: "TrainDeeploy: Hardware-Accelerated Parameter-Efficient Fine-Tuning of Small Transformer Models at the Extreme Edge"
title_zh: TrainDeeploy：极端边缘端小型 Transformer 模型的硬件加速参数高效微调
authors: "Run Wang, Victor J. B. Jung, Philip Wiese, Francesco Conti, Alessio Burrello, Luca Benini"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09511v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 极端边缘SoC的设备端训练
tldr: 本研究针对极低功耗边缘设备上深度神经网络微调面临的计算与内存挑战，提出了TrainDeeploy框架。该框架在异构RISC-V SoC上实现了首个支持CNN和Transformer模型的完整链上训练流水线，集成了层级微调和LoRA等参数高效策略。实验表明，TrainDeeploy在保持高能效的同时，显著降低了内存占用和参数量，为极端边缘侧的长期自适应学习提供了可能。
selection_source: fresh_fetch
motivation: 旨在解决极低功耗边缘设备在处理Transformer模型反向传播时面临的计算资源受限和内存不足问题。
method: 提出TrainDeeploy框架，通过硬件加速和LoRA等参数高效微调策略，在异构SoC上实现端到端的链上训练。
result: "在RISC-V SoC上实现了每秒11张图像的CCT微调速度，LoRA将动态内存使用降低了23%，训练参数量减少了15倍。"
conclusion: TrainDeeploy成功扩展了边缘侧训练框架的适用范围，证明了在极端受限硬件上高效微调Transformer模型的可行性。
---

## 摘要
深度神经网络的设备端调优能够在保护数据隐私的同时，实现边缘端的长期自适应。然而，反向传播的高计算和内存需求给超低功耗、内存受限的极端边缘设备带来了巨大挑战。由于架构复杂性和计算规模，这些挑战在基于注意力机制的模型中被进一步放大。我们提出了 TrainDeeploy，这是一个在异构超低功耗片上系统（SoC）上统一了高效推理和设备端训练的框架。TrainDeeploy 为极端边缘 SoC 提供了首个完整的设备端训练流水线，支持卷积神经网络（CNN）和 Transformer 模型，并提供多种训练策略，如选择性逐层微调和低秩自适应（LoRA）。在基于 RISC-V 的异构 SoC 上，我们展示了紧凑型卷积 Transformer（CCT）的首个端到端设备端微调，每秒可处理多达 11 张训练图像。我们展示了与全量反向传播相比，LoRA 将动态内存使用量减少了 23%，可训练参数和梯度数量减少了 15 倍，内存传输量减少了 1.6 倍。TrainDeeploy 在 CCT（0.28M 参数，71-126M FLOPs）上实现了高达 4.6 FLOP/cycle 的性能，在 Deep-AE（0.27M 参数，0.8M FLOPs）上实现了高达 13.4 FLOP/cycle 的性能，同时扩展了先前框架的范围，支持在极端边缘平台上对 CNN 和 Transformer 模型进行参数高效调优。

## Abstract
On-device tuning of deep neural networks enables long-term adaptation at the edge while preserving data privacy. However, the high computational and memory demands of backpropagation pose significant challenges for ultra-low-power, memory-constrained extreme-edge devices. These challenges are further amplified for attention-based models due to their architectural complexity and computational scale. We present TrainDeeploy, a framework that unifies efficient inference and on-device training on heterogeneous ultra-low-power System-on-Chips (SoCs). TrainDeeploy provides the first complete on-device training pipeline for extreme-edge SoCs supporting both Convolutional Neural Networks (CNNs) and Transformer models, together with multiple training strategies such as selective layer-wise fine-tuning and Low-Rank Adaptation (LoRA). On a RISC-V-based heterogeneous SoC, we demonstrate the first end-to-end on-device fine-tuning of a Compact Convolutional Transformer (CCT), achieving up to 11 trained images per second. We show that LoRA reduces dynamic memory usage by 23%, decreases the number of trainable parameters and gradients by 15x, and reduces memory transfer volume by 1.6x compared to full backpropagation. TrainDeeploy achieves up to 4.6 FLOP/cycle on CCT (0.28M parameters, 71-126M FLOPs) and up to 13.4 FLOP/cycle on Deep-AE (0.27M parameters, 0.8M FLOPs), while expanding the scope of prior frameworks to support both CNN and Transformer models with parameter-efficient tuning on extreme-edge platforms.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **TrainDeeploy** 的框架，旨在解决在极低功耗（Ultra-Low-Power, ULP）边缘设备上进行 Transformer 模型训练和微调的难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限（内存仅几百 KB 到几 MB，功耗在毫瓦级）的极端边缘设备（如微控制器 MCU）上，高效地执行计算密集型和内存密集型的 Transformer 模型反向传播（Backpropagation）。
*   **研究动机**：
    *   **隐私与个性化**：设备端训练（On-device Training）可实现长期自适应，且无需将私密数据上传云端。
    *   **现有局限**：目前的边缘训练框架大多针对 CNN，且缺乏对异构硬件（如带有专用加速器的 SoC）的端到端编译支持。Transformer 的注意力机制和大规模矩阵运算（GEMM）对这些设备的内存和算力提出了严峻挑战。

### 2. 方法论：TrainDeeploy 框架
TrainDeeploy 是一个从模型定义到硬件执行的完整编译和执行流，核心思想是**将参数高效微调（PEFT）与硬件感知编译优化相结合**。
*   **核心流程**：
    1.  **前端（Frontend）**：从 PyTorch 导出 ONNX 模型，通过自动微分引擎生成静态训练图（包含前向、反向和优化器更新）。
    2.  **中端（Midend）**：执行**算子平铺（Tiling）**和**静态内存分配**。利用约束编程（TetriSched）解决 2D 装箱问题，最小化 L1/L2/L3 存储层级间的峰值内存占用。
    3.  **后端（Backend）**：生成针对目标异构 SoC 优化的 C 代码，支持 RISC-V 指令集和专用硬件加速器。
*   **关键技术**：
    *   **LoRA（低秩自适应）支持**：通过引入低秩矩阵 $A$ 和 $B$ 代替全量权重更新，大幅减少梯度存储需求。
    *   **硬件加速集成**：专门针对 **RedMulE**（一种浮点 GEMM 加速器）进行算子映射，将计算压力从 CPU 转移到专用硬件。

### 3. 实验设计
*   **目标模型**：紧凑型卷积 Transformer（CCT-2），包含 0.28M 参数。
*   **数据集与场景**：
    *   **Few-shot 迁移学习**：在 CIFAR-10 上预训练，然后在 MNIST 和 EuroSAT 数据集上进行 50-shot 微调。
*   **对比策略**：
    *   **LP（线性探测）**：仅训练分类头。
    *   **FT-1/FT-2**：全量微调最后 1 或 2 个 Transformer 块。
    *   **LoRA-1/LoRA-2**：对最后 1 或 2 个块应用 LoRA（秩 $r=4$）。
*   **Benchmark 对象**：与 PULP-TrainLib、POET、MiniLearn 和 TTE 等最先进的边缘训练框架进行对比。

### 4. 资源与算力
*   **硬件平台**：基于 RISC-V 的异构 SoC（在 GVSoC 模拟器中建模）。
    *   **核心**：1 个主机核心 + 8 个计算簇核心（RISC-V）。
    *   **加速器**：RedMulE（FP32 矩阵乘法引擎）。
    *   **内存层级**：128 KB L1 (TCDM), 2 MB L2 SRAM, 32 MB L3 HyperRAM。
    *   **频率**：360 MHz。
*   **算力表现**：在 CCT 模型上达到 **4.6 FLOP/cycle**，在较小的 Deep-AE 模型上达到 **13.4 FLOP/cycle**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同的微调策略，并在两个不同的迁移学习任务上进行了测试。
*   **统计严谨性**：每个 Few-shot 实验重复运行了 **30 次**并报告了均值和标准差，确保了结果的客观性。
*   **对比维度**：从准确率、计算量（FLOPs）、内存占用（L3 峰值）、数据传输量（L3 与 L2 之间）以及端到端延迟等多个维度进行了全面评估。实验设计较为充分，能够证明框架在异构硬件上的优越性。

### 6. 主要结论与发现
*   **LoRA 的高效性**：相比全量微调（FT-2），LoRA-2 将可训练参数减少了 **15 倍**，动态内存占用降低了 **23%**，内存传输量减少了 **1.6 倍**，且准确率损失极小（在 EuroSAT 上仅差 1%）。
*   **硬件加速效果**：引入 RedMulE 加速器后，训练速度相比 8 核 RISC-V 簇提升了 **2.3 至 3.5 倍**。
*   **端到端性能**：在极端边缘 SoC 上实现了每秒 **11 次**梯度更新（CCT 模型），证明了 Transformer 在 MCU 级别设备上实时微调的可行性。

### 7. 优点
*   **首创性**：这是首个支持在超低功耗异构 SoC 上对 Transformer 进行端到端、硬件加速微调的框架。
*   **全栈优化**：不仅在算法层面（LoRA）优化，还在编译器层面（静态内存调度）和硬件层面（加速器集成）进行了深度协同。
*   **通用性**：框架同时支持 CNN 和 Transformer，具有较强的适配性。

### 8. 不足与局限
*   **模拟器依赖**：实验主要在 GVSoC 模拟器上完成，虽然模型精确，但实际物理芯片上的热管理和功耗波动可能带来额外挑战。
*   **模型规模限制**：虽然支持 Transformer，但目前仅限于“小型”版本（如 CCT），对于参数量更大的模型（如标准 ViT 或小型 LLM），内存压力依然巨大。
*   **优化器单一**：实验主要基于 SGD 优化器，未深入探讨 Adam 等更复杂（但更占内存）的优化器在边缘端的表现。

（完）
