---
title: Hardware Efficient Approximate Convolution with Tunable Error Tolerance for CNNs
title_zh: 针对卷积神经网络的具有可调容错性的硬件高效近似卷积
authors: "Vishal Shashidhar, Anupam Kumari, Roy P Paily"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10100v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于RISC-V指令的硬件高效近似卷积
tldr: 针对CNN在边缘设备部署时计算量大的问题，本文提出一种“软稀疏”范式。通过硬件高效的最高有效位（MSB）代理来识别并跳过可忽略的非零乘法运算，并将其集成到RISC-V指令集中。在LeNet-5模型上，该方法在保持零精度损失的同时，显著减少了ReLU和Tanh激活下的乘累加运算量，相比传统跳零方法性能提升5倍，有效降低了硬件功耗。
selection_source: fresh_fetch
motivation: 传统的硬稀疏方法在深层网络或平滑激活函数中效果有限，难以满足边缘设备的高效计算需求。
method: 提出一种基于最高有效位（MSB）代理的软稀疏计算范式，通过自定义RISC-V指令跳过对结果影响较小的非零乘法。
result: "在LeNet-5上实现ReLU和Tanh运算量分别减少88.42%和74.87%，且无精度损失，功耗降低约30%-35%。"
conclusion: 该方法通过优化资源受限环境下的推理过程，为CNN在边缘硬件上的高效部署提供了显著的性能和功耗优势。
---

## 摘要
现代卷积神经网络（CNN）的高计算需求阻碍了其在边缘端的部署，因为传统的“硬稀疏性”（跳过数学上的零值）在深层网络或使用 Tanh 等平滑激活函数时会失去效力。我们提出了一种“软稀疏”范式，利用硬件高效的最高有效位（MSB）代理来跳过可忽略的非零乘法。该方法作为自定义 RISC-V 指令集成，并在 LeNet-5 (MNIST) 上进行了评估，在零精度损失的情况下，将 ReLU 的乘累加（MAC）操作减少了 88.42%，将 Tanh 的 MAC 操作减少了 74.87%，性能优于零跳过（zero-skipping）方法 5 倍。通过对非活动乘法器进行时钟门控，我们估计 ReLU 的功耗可降低 35.2%，Tanh 的功耗可降低 29.96%。尽管内存访问使得功耗降低与操作减少之间呈亚线性关系，但该方法显著优化了资源受限下的推理。

## Abstract
Modern CNNs' high computational demands hinder edge deployment, as traditional ``hard'' sparsity (skipping mathematical zeros) loses effectiveness in deep layers or with smooth activations like Tanh. We propose a ``soft sparsity'' paradigm using a hardware efficient Most Significant Bit (MSB) proxy to skip negligible non-zero multiplications. Integrated as a custom RISC-V instruction and evaluated on LeNet-5 (MNIST), this method reduces ReLU MACs by 88.42% and Tanh MACs by 74.87% with zero accuracy loss--outperforming zero-skipping by 5x. By clock-gating inactive multipliers, we estimate power savings of 35.2\% for ReLU and 29.96\% for Tanh. While memory access makes power reduction sub-linear to operation savings, this approach significantly optimizes resource-constrained inference.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对卷积神经网络（CNN）的硬件高效近似卷积方法，旨在解决边缘设备在运行深度学习模型时面临的功耗和计算资源限制问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 CNN 计算量巨大，传统的优化方法主要依赖“硬稀疏性”（Hard Sparsity），即跳过数学上的零值。然而，在深层网络中，ReLU 激活产生的零值比例会大幅下降（仅剩 20-50%），而 Tanh 等平滑激活函数几乎不产生零值，导致传统跳零技术失效。
*   **研究动机**：提出一种“软稀疏”（Soft Sparsity）范式，通过跳过那些对最终输出贡献微乎其微的非零乘法运算，在保证精度的前提下，进一步降低硬件开销和功耗。

### 2. 方法论
*   **核心思想**：利用最高有效位（MSB）作为对数幅度的低成本硬件代理。
*   **关键技术细节**：
    *   **MSB 代理对数**：对于正整数 $x$，其 MSB 的位置 $k$ 近似于 $\log_2(x)$。因此，两个数乘积的量级可以通过其 MSB 位置之和来估算：$MSB(a \cdot b) \approx MSB(a) + MSB(b)$。
    *   **近似判定**：系统计算卷积窗口内所有乘积项的 MSB 之和，找到最大值 $MSB_{max}$。对于任一乘积项，若其 $(MSB_{max} - MSB_{current}) \ge T$（$T$ 为可调阈值），则认为该项贡献极小，直接跳过该乘法运算。
    *   **硬件集成**：将该算法实现为自定义指令 `conv_approx()`，集成到 32 位 RISC-V 处理器（RI5CY 内核）的执行阶段。
    *   **有限状态机（FSM）**：设计了一个 5 阶段 FSM（IDLE, GET_DATA, STAGE_1-3, DONE）来处理 4x4 输入与 3x3 卷积核的近似运算。

### 3. 实验设计
*   **数据集**：MNIST 手写数字识别数据集。
*   **模型架构**：LeNet-5。
*   **Benchmark（基准）**：
    *   精确卷积（Exact Convolution）。
    *   传统的硬跳零方法（Hard-zero skipping）。
*   **对比维度**：针对 ReLU 和 Tanh 两种激活函数，对比了不同阈值 $T$ 下的乘法器操作数（MACs）减少量、推理准确率以及估计功耗。

### 4. 资源与算力
*   **硬件实现**：使用 65nm CMOS 工艺进行综合分析。
*   **处理器**：基于 PULPino 平台的 RI5CY 内核（RV32IMFC）。
*   **算力说明**：文中未明确提及训练所用的 GPU 型号或时长，研究重点在于**推理阶段**的硬件加速器设计与评估。硬件面积增加了约 108%（相对于基础内核），但功耗仅增加 11.5%，且未增加关键路径延迟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 MNIST 10,000 张测试图像进行了推理实验。
    *   针对 ReLU 和 Tanh 分别设置了 6 组不同的误差阈值（0.05 到 0.5）。
    *   进行了层级的稀疏性统计分析。
*   **充分性评价**：实验涵盖了主流的激活函数对比和阈值消融实验，客观地展示了精度与计算量之间的权衡。但实验仅限于 LeNet-5 这种小型网络和 MNIST 简单数据集，对于更深层的网络（如 ResNet）或复杂数据集（如 ImageNet）的泛化能力尚待验证。

### 6. 主要结论与发现
*   **大幅减算**：在无精度损失的情况下，ReLU 模型减少了 **88.42%** 的 MAC 操作；Tanh 模型减少了 **74.87%** 的操作。
*   **超越传统方法**：相比于传统的硬跳零方法，该方法在 Tanh 激活下的乘法减少量提升了约 **5 倍**。
*   **功耗降低**：通过时钟门控（Clock Gating）技术，估计 ReLU 和 Tanh 推理的单次功耗分别降低了 **35.2%** 和 **29.96%**。
*   **亚线性关系**：由于内存访问是功耗的主要贡献者，功耗的降低幅度小于计算量的减少幅度。

### 7. 优点
*   **通用性强**：不依赖于特定的激活函数（如 ReLU），对平滑激活函数同样有效。
*   **硬件友好**：使用 MSB 加法代替复杂的对数运算或乘法判定，逻辑简单，易于集成到 RISC-V 等开源指令集架构中。
*   **动态可调**：通过调节阈值 $T$，可以在运行时灵活平衡推理精度与能效。

### 8. 不足与局限
*   **模型复杂度有限**：实验仅在 LeNet-5 上完成，未在现代大型 CNN（如 EfficientNet 或 Transformer）上验证其有效性。
*   **内存瓶颈**：虽然大幅减少了乘法运算，但并未减少数据读取操作。如文中所述，内存访问仍占据功耗的大头，限制了整体能效的进一步提升。
*   **误差累积风险**：在极深的网络中，多层近似可能会导致误差逐层累积，论文未深入探讨长路径下的误差传播稳定性。

（完）
