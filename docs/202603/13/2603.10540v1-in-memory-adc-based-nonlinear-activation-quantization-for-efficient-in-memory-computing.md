---
title: In-Memory ADC-Based Nonlinear Activation Quantization for Efficient In-Memory Computing
title_zh: 基于存内 ADC 的非线性激活量化，用于高效存内计算
authors: "Shuai Dong, Junyi Yang, Biyan Zhou, Hongyang Shang, Gourav Datta, Arindam Basu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10540v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对存内计算的硬件驱动量化
tldr: 针对存内计算（IMC）中激活值分布不均导致的量化效率低问题，本文提出边界抑制K-Means量化（BS-KMQ）方法。该方法通过在聚类前抑制边界离群值，实现了更均衡的非线性量化，并结合可重构存内非线性ADC硬件设计，显著降低了ADC面积。实验表明，该方案在多种深度学习模型上大幅提升了量化精度，并实现了显著的系统能效提升。
selection_source: fresh_fetch
motivation: 深度网络中的ReLU和硬件截断操作导致激活值在分布边缘聚集，使得传统非线性量化方法在存内计算中效果不佳且ADC分辨率需求过高。
method: 提出BS-KMQ算法，通过在聚类前抑制边界离群值来优化量化能级，并配套设计了面积效率提升7倍的可重构存内非线性ADC。
result: "在ResNet-18等模型上，量化误差降低至少3倍，后训练量化准确率提升高达67.7%，且系统能效比现有加速器提升24倍。"
conclusion: BS-KMQ结合非线性ADC设计，为高效存内计算提供了一种低位宽、高精度且极具面积和能效优势的量化解决方案。
---

## 摘要
在深度网络中，ReLU 和硬件驱动的钳位（clamping）等操作通常会导致激活值在分布边缘聚集，从而导致现有非线性（NL）量化方法中出现有偏聚类和次优量化。本文提出了一种边界抑制 K-Means 量化（BS-KMQ），这是一种新型非线性量化方法，旨在降低存内计算（IMC）系统中模数转换器（ADC）的分辨率要求。通过在聚类前抑制边界离群值，BS-KMQ 实现了更均衡且更具信息量的非线性量化能级。所得的非线性参考值通过可重构存内非线性 ADC 实现，与之前的非线性 ADC 设计相比，面积优化了 7 倍。在 ResNet-18、VGG-16、Inception-V3 和 DistilBERT 上的评估结果显示，与线性、Lloyd-Max、累积分布函数（CDF）和 K-means 方法相比，BS-KMQ 的量化误差至少降低了 3 倍。与线性量化相比，它还将训练后量化精度分别提高了高达 66.8%、25.4%、66.6% 和 67.7%。经过低比特微调后，BS-KMQ 在使用显著更少的非线性 ADC 能级（3/3/4/4b）的情况下，仍能保持具有竞争力的精度。在 ResNet-18 (6/2/3b) 上的系统级仿真表明，与现有的存内计算加速器相比，其速度提升了高达 4 倍，能效提升了 24 倍。

## Abstract
In deep networks, operations such as ReLU and hardware-driven clamping often cause activations to accumulate near the edges of the distribution, leading to biased clustering and suboptimal quantization in existing nonlinear (NL) quantization methods. This paper introduces Boundary Suppressed K-Means Quantization (BS-KMQ), a novel NL quantization approach designed to reduce the resolution requirements of analog-to-digital converters (ADCs) in in-memory computing (IMC) systems. By suppressing boundary outliers before clustering, BS-KMQ achieves more balanced and informative NL quantization levels. The resulting NL references are implemented using a reconfigurable in-memory NL-ADC, achieving a 7x area improvement over prior NL-ADC designs. When evaluated on ResNet-18, VGG-16, Inception-V3, and DistilBERT, BS-KMQ achieves at least 3x lower quantization error compared to linear, Lloyd-Max, cumulative distribution function (CDF), and K-means methods. It also improves post-training quantization accuracy by up to 66.8%, 25.4%, 66.6%, and 67.7%, respectively, compared to linear quantization. After low-bit fine-tuning, BS-KMQ maintains competitive accuracy with significantly fewer NL-ADC levels (3/3/4/4b). System-level simulations on ResNet-18 (6/2/3b) demonstrate up to a 4x speedup and 24x energy efficiency improvement over existing IMC accelerators.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **BS-KMQ（边界抑制 K-Means 量化）** 的新型非线性量化方案及其配套的硬件架构，旨在解决存内计算（IMC）中模数转换器（ADC）带来的能效与面积瓶颈。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：存内计算（IMC）虽然缓解了冯·诺依曼架构的“存储墙”问题，但其外围的 ADC（模数转换器）占据了系统约 90% 的功耗和面积。
*   **量化矛盾**：为了降低 ADC 复杂度，通常采用低比特（3-6 bit）量化。然而，神经网络的激活值分布极不均匀（受 ReLU 和硬件截断影响，大量数值聚集在零点或边界），传统的**线性量化**会导致巨大的精度损失。
*   **现有非线性方案的缺陷**：Lloyd-Max、CDF 和标准 K-means 等非线性量化方法要么计算开销大，要么对离群值（Outliers）过于敏感，且难以在硬件上高效实现。

### 2. 论文提出的方法论
论文提出了 **BS-KMQ 算法**及配套的**可重构存内非线性 ADC（IM NL-ADC）**：

*   **BS-KMQ 算法核心思想**：
    1.  **稳健统计校准**：利用指数移动平均（EMA）动态跟踪激活值的全局边界，并剔除上下各 0.5% 的极端离群值。
    2.  **边界抑制聚类**：在进行 K-means 聚类前，先移除所有饱和在边界上的样本。这样聚类中心会集中在信息最丰富的内部区域，避免被边界处的“堆积”数据拉偏。
    3.  **硬件映射**：将学习到的聚类中心转换为 ADC 的参考电平，通过“地板函数（Floor）”操作实现最近邻量化。
*   **硬件架构设计**：
    1.  **双 9T SRAM 单元**：支持三元输入和三元权重乘法，通过正/负读取字线（RWL+/-）实现差分计算。
    2.  **可重构 IM NL-ADC**：利用 SRAM 阵列中的副本单元产生非线性斜坡电压。通过控制开启的单元数量来调整步长，从而实现 1-7 比特的灵活非线性量化。

### 3. 实验设计
*   **数据集与模型**：
    *   **CNN 任务**：ResNet-18 (CIFAR-10)、VGG-16 (CIFAR-100)、Inception-V3 (Tiny ImageNet)。
    *   **Transformer 任务**：DistilBERT (SQuAD)。
*   **Benchmark（对比方法）**：
    *   **算法对比**：线性量化、Lloyd-Max、基于 CDF 的量化、标准 K-means。
    *   **硬件对比**：与 TCASI'24、VLSI'23、SSCL'24 等最先进的 IMC 加速器进行系统级对比。

### 4. 资源与算力
*   **硬件仿真**：使用 **65nm CMOS 工艺**进行 SPICE 电路仿真。
*   **系统评估**：使用 **NeuroSim V2.0** 评估互连、缓冲器和累加单元等系统级开销。
*   **软件训练**：论文未明确说明具体的 GPU 型号和训练时长，但提到了进行了训练后量化（PTQ）和低比特微调（Fine-tuning）。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从底层电路仿真（工艺角分析 SS/TT/FF）、算法误差分析（MSE）、模型精度评估（PTQ/FT）到系统级性能对比（TOPS/W）。
*   **充分性评价**：实验设计较为充分。不仅在视觉模型上验证，还扩展到了 Transformer 模型；不仅对比了算法指标，还考虑了硬件非理想性（ADC 噪声、工艺偏差）对精度的影响。对比基准涵盖了经典的非线性量化算法，具有较强的说服力。

### 6. 主要结论与发现
*   **误差降低**：在 3-bit 分辨率下，BS-KMQ 的量化均方误差（MSE）比线性和其他非线性方法降低了 **3-8 倍**。
*   **精度提升**：在 PTQ 测试中，相比线性量化，准确率提升高达 **66.8%**。微调后，仅需 3-4 bit 的 ADC 即可达到接近浮点模型的精度（损失仅 0.3%-1.2%）。
*   **硬件优势**：
    *   **面积**：相比之前的非线性 ADC 设计，面积开销优化了 **7 倍**（仅占 MAC 阵列面积的 3.3%）。
    *   **能效**：系统级仿真显示，相比现有加速器，能效提升高达 **24 倍**，吞吐量提升 **4 倍**。

### 7. 优点
*   **软硬协同优化**：BS-KMQ 算法直接考虑了硬件 ADC 的实现特性（如 Floor 操作映射），而非纯数学优化。
*   **极高的面积效率**：通过复用 SRAM 单元产生参考电压，消除了传统非线性 ADC 复杂的模拟电路需求。
*   **高灵活性**：支持 1-7 bit 输入/输出和 2-4 bit 权重的动态重构，适应不同层的精度需求。

### 8. 不足与局限
*   **功耗权衡**：由于非线性 ADC 需要开启更多的 SRAM 单元来产生特定步长，其 ADC 部分的功耗比同等条件的线性 IM ADC 增加了约 **30%**。
*   **硬件依赖性**：该方案高度依赖于特定的双 9T SRAM 结构，对于其他类型的存储介质（如 RRAM 或传统 6T SRAM）的迁移适配性未做深入讨论。
*   **离群值比例固定**：算法中剔除 0.5% 尾部比例的参数 $\alpha$ 可能是经验值，对于分布极度异常的数据集可能需要重新调优。

（完）
