---
title: Mask-aware inference with State-Space Models
title_zh: 基于状态空间模型的掩码感知推理
authors: "Ignasi Mas, Ramon Morros, Javier-Ruiz Hidalgo, Ivan Huerta"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04568v1"
tags: ["query:hwnas"]
score: 6.0
evidence: 针对状态空间模型的架构设计规则，以处理特定数据模式
tldr: 针对计算机视觉中处理任意形状缺失或无效数据的挑战，本文提出了局部视觉Mamba（PVM）。虽然CNN通过局部卷积解决了该问题，但新兴的状态空间模型（SSM）如Mamba尚缺乏此类机制。PVM将局部操作原理引入Mamba骨干网络，并定义了一系列架构设计规则。实验证明，该方法在深度补全、图像修复和带无效数据的分类任务中表现优异，实现了高效且掩码感知的推理。
selection_source: fresh_fetch
motivation: 现有的状态空间模型（SSM）在处理具有任意形状缺失或无效数据的计算机视觉任务时，缺乏像CNN局部卷积那样的有效处理机制。
method: 提出了局部视觉Mamba（PVM），将局部操作的重归一化原理移植到Mamba架构中，并制定了相应的架构设计规则。
result: 在深度补全、图像修复以及含有无效数据的图像分类等多个任务上验证了该方法的有效性和泛化能力。
conclusion: PVM成功为SSM引入了掩码感知推理能力，使其在处理不完整数据时既能保持线性复杂度又能获得高性能。
---

## 摘要
许多现实世界的计算机视觉任务（如深度补全）必须处理具有任意形状缺失或无效数据区域的输入。对于卷积神经网络 (CNN)，部分卷积 (Partial Convolutions) 通过仅以有效像素为条件的掩码感知重归一化解决了这一问题。最近，像 Mamba 这样的状态空间模型 (SSM) 已经出现，并以线性复杂度提供了高性能。然而，这些架构在推理时缺乏处理此类任意形状无效数据的内在机制。为了填补这一空白，我们引入了部分视觉 Mamba (Partial Vision Mamba, PVM)，这是一种将部分操作原理移植到 Mamba 骨干网络的新型架构组件。我们还定义了一系列使用 PVM 设计架构的规则。我们在深度补全、图像修复以及带有无效数据的分类任务中展示了该方法的有效性和泛化能力。

## Abstract
Many real-world computer vision tasks, such as depth completion, must handle inputs with arbitrarily shaped regions of missing or invalid data. For Convolutional Neural Networks (CNNs), Partial Convolutions solved this by a mask-aware re-normalization conditioned only on valid pixels. Recently, State Space Models (SSMs) like Mamba have emerged, offering high performance with linear complexity. However, these architectures lack an inherent mechanism for handling such arbitrarily shaped invalid data at inference time. To bridge this gap, we introduce Partial Vision Mamba (PVM), a novel architectural component that ports the principles of partial operations to the Mamba backbone. We also define a series of rules to design architectures using PVM. We show the efficacy and generalizability of our approach in the tasks of depth completion, image inpainting, and classification with invalid data.