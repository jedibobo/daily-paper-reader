---
title: "AdaFuse: Accelerating Dynamic Adapter Inference via Token-Level Pre-Gating and Fused Kernel Optimization"
title_zh: AdaFuse：通过 Token 级预门控与融合算子优化加速动态适配器推理
authors: "Qiyang Li, Rui Kong, Yuchen Li, Hengyi Cai, Shuaiqiang Wang, Linghe Kong, Guihai Chen, Dawei Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11873v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 架构与融合算子优化的紧密协同设计
tldr: 针对动态适配器（如LoRA+MoE）在LLM推理中因频繁CUDA内核启动导致的严重延迟问题，本文提出AdaFuse框架。该框架通过算法与硬件协同设计，引入Token级预门控策略，在处理Token前进行全局路由决策，并开发定制化CUDA内核实现参数融合切换。实验表明，AdaFuse在保持模型精度的同时，将解码延迟降低了2.4倍以上，显著提升了推理效率。
selection_source: fresh_fetch
motivation: 动态适配器推理的性能瓶颈在于频繁且碎片化的CUDA内核启动开销，而非计算量本身。
method: 采用Token级预门控策略进行全局路由决策，并利用定制CUDA内核实现适配器参数与主干模型的融合切换。
result: 在主流开源LLM上，AdaFuse在保持与现有动态适配器同等精度的同时，将解码延迟降低了2.4倍以上。
conclusion: AdaFuse通过软硬件协同优化，有效解决了动态适配器的推理效率难题，实现了模型性能与推理速度的平衡。
---

## 摘要
将混合专家模型（MoE）等动态稀疏结构与参数高效适配器（如 LoRA）相结合，是增强大语言模型（LLM）的一种强大技术。然而，这种架构增强带来了高昂的代价：尽管计算量增加极小，但推理延迟往往会激增，导致解码速度降低 2.5 倍以上。通过细粒度的性能分析，我们发现主要的瓶颈不在于计算本身，而在于传统动态路由所需的碎片化、顺序化 CUDA 算子启动所带来的严重开销。为了应对这一挑战，我们推出了 AdaFuse，这是一个基于算法与底层硬件系统紧密协同设计的框架，旨在实现高效的动态适配器执行。与传统的逐层或逐块路由不同，AdaFuse 采用了一种 Token 级预门控策略，在处理 Token 之前为所有适配器层做出单一的全局路由决策。这种“一次决策，处处应用”的方法有效地使每个 Token 的执行路径静态化，为整体优化创造了机会。我们利用这一点开发了一个自定义 CUDA 算子，执行融合切换操作，在单次高效传递中将所有选定 LoRA 适配器的参数合并到骨干模型中。在主流开源 LLM 上的实验结果表明，AdaFuse 在达到与最先进动态适配器相当的准确率的同时，将解码延迟大幅降低了 2.4 倍以上，从而弥合了模型能力与推理效率之间的差距。

## Abstract
The integration of dynamic, sparse structures like Mixture-of-Experts (MoE) with parameter-efficient adapters (e.g., LoRA) is a powerful technique for enhancing Large Language Models (LLMs). However, this architectural enhancement comes at a steep cost: despite minimal increases in computational load, the inference latency often skyrockets, leading to decoding speeds slowing by over 2.5 times. Through a fine-grained performance analysis, we pinpoint the primary bottleneck not in the computation itself, but in the severe overhead from fragmented, sequential CUDA kernel launches required for conventional dynamic routing. To address this challenge, we introduce AdaFuse, a framework built on a tight co-design between the algorithm and the underlying hardware system to enable efficient dynamic adapter execution. Departing from conventional layer-wise or block-wise routing, AdaFuse employs a token-level pre-gating strategy, which makes a single, global routing decision for all adapter layers before a token is processed. This "decide-once, apply-everywhere" approach effectively staticizes the execution path for each token, creating an opportunity for holistic optimization. We capitalize on this by developing a custom CUDA kernel that performs a fused switching operation, merging the parameters of all selected LoRA adapters into the backbone model in a single, efficient pass. Experimental results on popular open-source LLMs show that AdaFuse achieves accuracy on par with state-of-the-art dynamic adapters while drastically cutting decoding latency by a factor of over 2.4x, thereby bridging the gap between model capability and inference efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《AdaFuse: Accelerating Dynamic Adapter Inference via Token-Level Pre-Gating and Fused Kernel Optimization》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：动态适配器（如结合了 MoE 和 LoRA 的结构）虽然能显著增强大语言模型（LLM）的多任务处理能力且计算量增加极小（仅 1-5%），但其**推理延迟极高**（通常导致解码速度变慢 2.5 到 9.5 倍）。
*   **研究动机**：作者通过细粒度分析发现，瓶颈不在于计算量，而在于**碎片化的 CUDA 算子（Kernel）启动开销**。传统的动态路由是逐层或逐块进行的，导致每一层都需要多次额外的算子调用和内存访问，这在 GPU 架构上效率极低。

### 2. 方法论：AdaFuse 框架
AdaFuse 采用了**算法与硬件系统协同设计**的思路，核心包含两个关键技术：
*   **Token 级预门控（Token-Level Pre-Gating）**：
    *   **核心思想**：改变传统的“逐层路由”模式，改为“一次决策，全模型应用”。
    *   **技术细节**：在模型的第一层设置一个全局路由器（Router），为当前 Token 决定要激活的 Top-K 个适配器专家。这个决策结果将直接应用于后续所有层，从而使每个 Token 的执行路径在进入深层网络前就已“静态化”。
*   **融合算子优化（SGMM Kernel）**：
    *   **核心思想**：利用预门控带来的静态路径，将适配器参数直接合并到主干模型权重中。
    *   **关键技术**：开发了名为 **SGMM（Segmented Gather Matrix Multiplication）** 的定制 CUDA 内核。它能在单次算子调用中，完成所有层已激活适配器的合并（Merge）与未激活适配器的卸载（Unmerge）操作。
    *   **流程**：通过公式 $(f^* )_t = (f^* )_{t-1} + Fused\_LoRA\_DOWN \times Fused\_LoRA\_UP$ 实现权重的快速切换，将原本分散的矩阵乘法合并为批处理操作，极大减少了算子启动次数。

### 3. 实验设计
*   **模型与基准（Benchmark）**：
    *   **基础模型**：Llama2-7B 和 Mistral-7B。
    *   **通用能力评估**：在 ARC、HellaSwag、MMLU、TruthfulQA、WinoGrande 和 MT-Bench 上测试。
    *   **领域特定任务**：ScienceQA、CommonsenseQA 和 OpenbookQA。
    *   **推理性能测试**：使用 ShareGPT 数据集模拟真实用户查询。
*   **对比方法**：
    *   静态适配器：LoRA。
    *   动态适配器基准：MOLA（逐层路由）、PESC（逐块路由）、MoRAL（逐层路由）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到了使用 CUDA 进行算子开发，并在推理实验中对比了毫秒级的 Token 解码延迟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了通用能力（5 个主流数据集）和领域特定能力（3 个 QA 数据集）的准确率对比。
    *   进行了详细的推理性能分析（延迟、显存占用）。
    *   包含消融实验，验证了 SGMM 算子对性能提升的贡献。
*   **充分性评价**：实验设计较为全面，不仅验证了“快”（推理速度），还证明了“准”（准确率未受损）。通过在不同架构（Llama2 和 Mistral）上测试，展示了方法的通用性。对比实验涵盖了当前最主要的动态适配器变体，具有较强的说服力。

### 6. 主要结论与发现
*   **推理加速**：AdaFuse 将解码延迟降低了 **2.4 倍以上**。相比原始主干模型，其延迟增量仅为 29%，而传统动态方法增量高达 250%-950%。
*   **准确率保持**：在通用和领域任务上，AdaFuse 的准确率与最先进的动态适配器（如 PESC）持平，甚至在某些知识密集型任务（如 MMLU）上表现更优。
*   **显存效率**：相比 MOLA 等方法，AdaFuse 的显存峰值开销显著降低（从 +104% 降至 +7%）。

### 7. 优点与亮点
*   **软硬协同设计**：不只是改进算法，而是深入到底层算子优化，解决了动态模型落地的实际痛点。
*   **极高的实用价值**：将原本因延迟太高而无法商用的动态适配器技术，提升到了接近原生模型的推理速度。
*   **创新的预门控机制**：通过简化路由逻辑，为系统级优化创造了空间，同时实验证明这种简化对模型表达能力的损害极小。

### 8. 不足与局限
*   **预填充阶段优化不足**：论文提到目前主要针对解码（Decoding）阶段优化，对于预填充（Prefilling）阶段的延迟提升有限。
*   **路由灵活性受限**：由于采用“一次决策，处处应用”，模型失去了在不同层选择不同专家的灵活性，虽然实验显示准确率未降，但在更复杂的任务中可能存在表达瓶颈。
*   **硬件依赖性**：SGMM 算子针对 NVIDIA GPU 开发，在其他硬件平台（如昇腾、寒武纪或移动端）上的移植和优化效果尚不明确。

（完）
