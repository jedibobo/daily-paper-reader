---
title: "NL2GDS: LLM-aided interface for Open Source Chip Design"
title_zh: NL2GDS：面向开源芯片设计的大语言模型辅助接口
authors: "Max Eland, Jeyan Thiyagalingam, Dinesh Pamunuwa, Roshan Weerasekera"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05489v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 大模型辅助的自动化硬件设计与布局接口
tldr: 针对硬件设计复杂性高及高层规范与RTL实现间存在鸿沟的问题，本文提出了NL2GDS框架。该框架利用大语言模型（LLM）将自然语言描述转化为可综合的RTL，并通过开源OpenLane流程生成GDSII版图。通过模块化流水线实现设计意图捕获、HDL生成与验证及自动化后端设计，显著降低了ASIC设计门槛并提升了设计效率。
selection_source: fresh_fetch
motivation: 硬件设计日益复杂且高层规范与底层实现之间存在巨大差距，阻碍了快速原型开发。
method: 提出一种利用多LLM引擎将自然语言转化为RTL，并结合OpenLane自动化流程生成GDSII版图的模块化框架。
result: "在ISCAS'85和'89基准测试中，该方法相比基准设计实现了最高36%的面积缩减、35%的延迟降低和70%的功耗节省。"
conclusion: NL2GDS有效推动了ASIC设计的民主化，为加速硬件创新提供了强有力的自动化工具。
---

## 摘要
硬件设计复杂性的增加以及高级规范与寄存器传输级（RTL）实现之间日益扩大的差距，阻碍了快速原型设计和系统设计。我们介绍了 NL2GDS（自然语言到版图），这是一个利用大语言模型（LLM）将自然语言硬件描述转换为可综合 RTL，并通过开源 OpenLane ASIC 流程生成完整 GDSII 版图的新型框架。NL2GDS 采用模块化流水线，捕捉非正式的设计意图，使用多个 LLM 引擎生成硬件描述语言（HDL）并进行验证，并编排自动化综合与布局布线。在 ISCAS'85 和 ISCAS'89 基准设计上的评估表明，与基准设计相比，面积减少了高达 36%，延迟减少了 35%，功耗节省了 70%，突显了其在普及 ASIC 设计和加速硬件创新方面的潜力。

## Abstract
The growing complexity of hardware design and the widening gap between high-level specifications and register-transfer level (RTL) implementation hinder rapid prototyping and system design. We introduce NL2GDS (Natural Language to Layout), a novel framework that leverages large language models (LLMs) to translate natural language hardware descriptions into synthesizable RTL and complete GDSII layouts via the open-source OpenLane ASIC flow. NL2GDS employs a modular pipeline that captures informal design intent, generates HDL using multiple LLM engines and verifies them, and orchestrates automated synthesis and layout. Evaluations on ISCAS'85 and ISCAS'89 benchmark designs demonstrate up to 36% area reduction, 35% delay reduction, and 70% power savings compared to baseline designs, highlighting its potential to democratize ASIC design and accelerate hardware innovation.

---

## 论文详细总结（自动生成）

以下是对论文《NL2GDS: LLM-aided interface for Open Source Chip Design》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：硬件设计流程（从规格说明到物理版图）极其复杂，且高级语言描述与底层寄存器传输级（RTL）实现之间存在巨大鸿沟。此外，传统的 EDA 工具链昂贵且门槛高，限制了中小企业和教育机构的创新。
*   **核心问题**：如何利用大语言模型（LLM）的自然语言处理能力，构建一个端到端的自动化流程，使非专家用户也能通过自然语言直接生成可制造的芯片版图（GDSII）。
*   **整体含义**：NL2GDS 填补了现有研究中“仅能生成 RTL 代码而缺乏后端物理设计集成”的空白，实现了从自然语言到 GDSII 的全流程自动化。

### 2. 论文提出的方法论
NL2GDS 采用了一个模块化的 AI 驱动平台，核心思想是将 LLM 代理（Agents）与开源 EDA 流程（OpenLane）深度集成。
*   **多代理架构（Agentic Approach）**：系统由多个专门的 Agent 组成，分别负责设计意图捕获、Verilog 代码生成、错误修复和后端参数优化。
*   **思维链（CoT）与反馈环**：
    *   将复杂任务分解为子任务。
    *   集成 Verilator 进行语法检查（Linting），通过反馈循环自动修正 LLM 生成的代码错误。
*   **检索增强生成（RAG）**：针对 OpenLane 约 800 个复杂的配置参数，系统通过 RAG 检索项目文档和成功案例，生成最优的流程配置。
*   **后端感知优化**：
    *   **检测算子**：$I = f_{detect}(M, E)$，从指标 $M$ 和错误 $E$ 中提取时序、面积、拥塞等违例。
    *   **查询算子**：$Q = f_{query}(I, C, G)$，根据当前配置 $C$ 和目标 $G$ 生成针对性的 RAG 查询，以获取优化建议。
*   **云端并行化**：解耦 Python 的全局解释器锁（GIL），在云端并行运行多个 OpenLane 实例，加速参数搜索和 PPA（功耗、性能、面积）优化。

### 3. 实验设计
*   **数据集/场景**：
    *   **ISCAS'85 & ISCAS'89**：经典的组合逻辑和时序逻辑基准电路。
    *   **VerilogEval**：用于验证代码生成能力的挑战性测试集（如 FSM、复杂算法）。
*   **Benchmark（基准）**：
    *   对比了原始的、经过门级优化的 ISCAS 基准 Verilog 代码。
    *   对比了不同 LLM（如 Gemini 2.5 Pro, GPT-5, Sonnet 4.5 等）在有无 NL2GDS 框架下的表现。
*   **评估指标**：硅片面积（Area）、关键路径延迟（Delay）、估计功耗（Power）以及设计完成时间。

### 4. 资源与算力
*   **算力说明**：论文提到系统运行在云端多核服务器或云节点上。
*   **具体细节**：
    *   在 SPM 乘法器设计测试中，通过并行化将单次流程时间从 132 秒缩短至平均 37.5 秒（提升 3.46 倍）。
    *   **成本**：单次设计的 LLM 调用与云算力成本极低，约在 **$0.13 至 $0.56** 之间。
    *   **GPU/型号**：文中未明确指出具体的 GPU 型号，主要强调了 CPU 多核并行处理 EDA 任务的能力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 8 个主要的基准电路（包括 8-bit ALU、16-bit 乘法器、流水线乘法器等）进行了深入的 PPA 评估。
    *   针对每个设计进行了 48 到 100 次不等的自动化运行以进行优化。
    *   在 VerilogEval 子集上进行了代码生成准确率对比。
*   **充分性评价**：实验覆盖了从简单组合逻辑到复杂流水线算术电路的多种类型，且对比了多个主流 LLM 模型，实验设计较为客观。通过与高度优化的 ISCAS 基准对比，证明了其生成的电路在物理指标上具有竞争力。

### 6. 论文的主要结论与发现
*   **性能卓越**：NL2GDS 生成的设计在所有 PPA 指标上几乎全面超越了 ISCAS 基准。
    *   **面积**：减少了 7.54% 至 54.71%。
    *   **延迟**：减少了 17.69% 至 35.29%。
    *   **功耗**：节省了 11.81% 至 69.55%。
*   **效率提升**：原本需要数天或数周的人工设计流程，现在可以在 **1 小时内**完成从自然语言到优化版图的转化。
*   **LLM 增强**：NL2GDS 框架显著提升了基础 LLM 生成硬件代码的成功率（例如 Gemini 2.5 Pro 的通过率从 3.79% 提升至 83.70%）。

### 7. 优点
*   **全流程自动化**：首个实现从自然语言直接到 GDSII 版图的端到端开源框架。
*   **RAG 解决领域知识缺失**：巧妙地利用 RAG 解决了 LLM 对特定 EDA 工具参数理解不足的问题。
*   **低成本与民主化**：极大地降低了 ASIC 设计的资金和技术门槛，适合快速原型开发。
*   **自优化能力**：系统不仅能“生成”，还能根据后端反馈自动“迭代优化”。

### 8. 不足与局限
*   **工艺库限制**：目前仅在 Skywater 130nm 开源 PDK 上进行了验证，尚未测试更先进的先进制程（如 7nm/5nm）。
*   **设计规模**：测试案例仍集中在中小规模的基准电路上，对于数百万门级的复杂 SoC（片上系统）的有效性尚待观察。
*   **模型依赖**：高度依赖底层 LLM 的推理能力，且文中提到的部分模型（如 GPT-5）在论文发布时可能属于前瞻性引用或特定版本，其通用性需进一步验证。
*   **验证深度**：虽然通过了 Linting 和测试平台检查，但对于复杂的异步时钟域交叉（CDC）等高级验证问题的处理能力未详细说明。

（完）
