---
title: Resource-Efficient Iterative LLM-Based NAS with Feedback Memory
title_zh: 具有反馈记忆的资源高效迭代式基于大语言模型的神经架构搜索
authors: "Xiaojie Gu, Dmitry Ignatov, Radu Timofte"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12091v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在单个消费级 GPU 上进行资源高效的 NAS
tldr: 本研究提出一种资源高效的闭环神经架构搜索（NAS）框架，利用大语言模型（LLM）在单张消费级GPU上迭代优化CNN架构。该方法引入基于马尔可夫链的历史反馈记忆机制，通过结构化诊断三元组记录失败与成功经验，并采用双LLM分工模式（代码生成与提示词改进）。该方案无需微调LLM，在受限显存下自动倾向于生成紧凑高效的模型，为低成本、可复现的硬件感知NAS提供了新范式。
selection_source: fresh_fetch
motivation: 传统神经架构搜索方法计算资源消耗巨大，且现有基于LLM的优化器往往忽略了失败轨迹中的学习信号。
method: 提出一种基于历史反馈记忆和双LLM协作的闭环迭代框架，在单GPU环境下通过结构化诊断三元组引导模型持续改进。
result: 在CIFAR-10等数据集上，DeepSeek-Coder-6.7B等模型在2000次迭代内实现了显著的准确率提升，且全过程仅需约18小时。
conclusion: 该研究证明了在有限预算下，利用冻结的轻量级LLM进行高效、硬件感知的自动化网络设计是切实可行的。
---

## 摘要
神经架构搜索 (NAS) 实现了网络设计的自动化，但传统方法需要大量的计算资源。我们提出了一种闭环流水线，利用大语言模型 (LLM) 在单张消费级 GPU 上迭代地生成、评估和改进用于图像分类的卷积神经网络架构，且无需对 LLM 进行微调。我们方法的核心是受马尔可夫链启发的历史反馈记忆：一个包含 $K{=}5$ 次近期改进尝试的滑动窗口在保持上下文大小恒定的同时，为迭代学习提供了充足的信号。与以往丢弃失败轨迹的 LLM 优化器不同，每个历史条目都是一个结构化的诊断三元组——记录识别出的问题、建议的修改以及产生的结果——将代码执行失败视为一等学习信号。双 LLM 专业化分工降低了每次调用的认知负荷：代码生成器 (Code Generator) 负责生成可执行的 PyTorch 架构，而提示词改进器 (Prompt Improver) 负责诊断推理。由于 LLM 和架构训练共享有限的显存 (VRAM)，搜索过程隐式地倾向于适合边缘部署的紧凑且硬件高效的模型。我们在不受限的开放代码空间中，对三个冻结的指令微调 LLM（参数量 ${\leq}7$B）进行了多达 2000 次迭代的评估，并使用 CIFAR-10、CIFAR-100 和 ImageNette 上的单轮 (one-epoch)

## Abstract
Neural Architecture Search (NAS) automates network design, but conventional methods demand substantial computational resources. We propose a closed-loop pipeline leveraging large language models (LLMs) to iteratively generate, evaluate, and refine convolutional neural network architectures for image classification on a single consumer-grade GPU without LLM fine-tuning. Central to our approach is a historical feedback memory inspired by Markov chains: a sliding window of $K{=}5$ recent improvement attempts keeps context size constant while providing sufficient signal for iterative learning. Unlike prior LLM optimizers that discard failure trajectories, each history entry is a structured diagnostic triple -- recording the identified problem, suggested modification, and resulting outcome -- treating code execution failures as first-class learning signals. A dual-LLM specialization reduces per-call cognitive load: a Code Generator produces executable PyTorch architectures while a Prompt Improver handles diagnostic reasoning. Since both the LLM and architecture training share limited VRAM, the search implicitly favors compact, hardware-efficient models suited to edge deployment. We evaluate three frozen instruction-tuned LLMs (${\leq}7$B parameters) across up to 2000 iterations in an unconstrained open code space, using one-epoch proxy accuracy on CIFAR-10, CIFAR-100, and ImageNette as a fast ranking signal. On CIFAR-10, DeepSeek-Coder-6.7B improves from 28.2% to 69.2%, Qwen2.5-7B from 50.0% to 71.5%, and GLM-5 from 43.2% to 62.0%. A full 2000-iteration search completes in ${\approx}18$ GPU hours on a single RTX~4090, establishing a low-budget, reproducible, and hardware-aware paradigm for LLM-driven NAS without cloud infrastructure.

---

## 论文详细总结（自动生成）

这是一份关于论文《Resource-Efficient Iterative LLM-Based NAS with Feedback Memory》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：传统的神经架构搜索（NAS）方法（如强化学习或进化算法）计算成本极高，动辄消耗数千个 GPU 天。虽然近期有研究利用大语言模型（LLM）生成架构，但大多是“单次生成（Single-shot）”，缺乏迭代改进机制，且往往忽略了代码运行失败（Error）中蕴含的学习信号。
*   **核心问题**：如何在有限的计算资源（单张消费级 GPU）下，利用冻结的小规模 LLM（≤7B），通过学习自身的失败与成功经验，在不受限的代码空间中迭代发现高性能的神经网络架构？

### 2. 方法论与核心技术
该论文提出了一种闭环迭代流水线，核心思想是将 NAS 建模为一个基于马尔可夫链的优化过程：
*   **双 LLM 协作模式**：
    *   **代码生成器 (Code Generator)**：负责根据当前最优架构和改进建议，编写完整的 PyTorch 模型代码。
    *   **提示词改进器 (Prompt Improver)**：负责诊断评估结果，生成针对性的改进建议。这种分工降低了单个 LLM 的认知负荷。
*   **历史反馈记忆 (Historical Feedback Memory)**：
    *   采用 $K=5$ 的滑动窗口记录最近的尝试，满足马尔可夫性质，确保上下文长度恒定，避免长文本导致的性能下降。
    *   **诊断三元组 (Diagnostic Triples)**：每个历史条目包含“识别的问题”、“建议的修改”和“产生的结果”。
*   **失败作为一等信号**：与以往只保留高分样本的优化器不同，该方法将代码执行错误（如维度不匹配）视为关键学习信号，引导模型避开错误的编程逻辑。
*   **硬件感知机制**：由于 LLM 推理与架构训练共享单卡显存（VRAM），搜索过程会隐式地迫使 LLM 发现参数量较小、显存占用低的紧凑型架构，从而自然地适应边缘设备部署。

### 3. 实验设计与基准
*   **数据集**：CIFAR-10、CIFAR-100 和 ImageNette（ImageNet 的子集）。
*   **评估指标**：采用“单轮训练准确率（One-epoch proxy accuracy）”作为快速排序信号。
*   **对比基准 (Benchmark)**：
    *   **Single-shot**：LLM 在没有任何反馈的情况下生成的第一个有效架构。
    *   **消融实验**：对比了“无反馈记忆”和“无参考代码”的情况。
*   **测试模型**：选择了三个不同背景的冻结指令微调模型：DeepSeek-Coder-6.7B-Instruct（代码专精）、Qwen2.5-7B-Instruct（通用型）、GLM-5（大规模通用型）。

### 4. 资源与算力消耗
*   **硬件环境**：单张 **NVIDIA GeForce RTX 4090 (24GB)** 显存 GPU。
*   **训练时长**：完成一个完整的 **2000 次迭代** 搜索过程大约需要 **18 小时**。
*   **LLM 状态**：所有 LLM 均处于冻结状态，**无需任何微调（Fine-tuning）**，极大地降低了算力门槛。

### 5. 实验数量与充分性
*   **实验规模**：针对 DeepSeek 和 Qwen 模型进行了长达 2000 次的迭代实验，GLM-5 进行了 100 次迭代。
*   **覆盖面**：涵盖了三个不同难度和分辨率的数据集，并进行了统计学显著性分析（计算 Spearman $\rho$ 和 Kendall $\tau$ 相关系数）。
*   **充分性评价**：实验设计较为充分，通过长序列迭代证明了性能提升的趋势而非偶然。消融实验清晰地展示了反馈记忆对防止搜索停滞的关键作用。

### 6. 主要结论与发现
*   **性能显著提升**：在 CIFAR-10 上，DeepSeek-Coder 从 28.2% 提升至 69.2%，Qwen2.5 从 50.0% 提升至 71.5%。
*   **模型特性差异**：
    *   **DeepSeek-Coder**：在代码可靠性上表现最好，CIFAR-100 成功率高达 95.1%，提升幅度最大。
    *   **Qwen2.5**：虽然代码成功率较低（约 18.8%），但倾向于尝试复杂架构，最终挖掘出的峰值准确率最高。
    *   **GLM-5**：表现最稳健，仅需 100 次迭代即可展现出明显的上升趋势。
*   **反馈的重要性**：消融实验证明，如果没有历史反馈记忆，搜索过程会迅速陷入停滞或退化。

### 7. 亮点与优势
*   **极低成本**：将 NAS 从“实验室级”算力需求降至“个人玩家级”，单卡 18 小时即可完成。
*   **开放搜索空间**：不同于以往受限于固定“单元（Cell）”的 NAS，该方法在纯 Python 代码空间搜索，能产生更具创意的架构。
*   **结构化诊断**：通过“诊断三元组”将错误信息转化为改进动力，解决了 LLM 在复杂逻辑任务中容易“复读”错误的问题。

### 8. 局限性与不足
*   **代理指标局限**：仅使用 1-epoch 的准确率作为指标，虽然能反映趋势，但可能无法完全代表模型收敛后的最终性能。
*   **代码成功率波动**：部分模型（如 Qwen）在特定任务下的代码成功率极低，说明提示词工程或模型对复杂空间约束的理解仍有待加强。
*   **任务规模限制**：目前主要在小分辨率数据集（32x32 或 160x160）上验证，尚未在 ImageNet-1K 等大规模、高分辨率任务上证明其扩展性。

（完）
