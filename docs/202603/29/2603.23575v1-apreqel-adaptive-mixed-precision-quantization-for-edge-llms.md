---
title: "APreQEL: Adaptive Mixed Precision Quantization For Edge LLMs"
title_zh: APreQEL：面向边缘大语言模型的自适应混合精度量化
authors: "Meriem Bouzouad, Yuan-Hao Chang, Jalil Boukhobza"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23575v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对边缘端大语言模型的自适应混合精度量化
tldr: 针对大语言模型在边缘设备部署时面临的高计算和内存挑战，本文提出 APreQEL 框架。该方法摒弃了传统的统一量化方式，通过分析各层对模型贡献的差异以及在特定硬件上的性能表现，实现自适应混合精度量化。它能根据用户定义的优先级平衡内存、延迟和准确性，为资源受限环境下的 LLM 部署提供了更优的配置方案。
selection_source: fresh_fetch
motivation: 传统的统一量化忽略了模型不同层对精度的敏感度差异，且内存消耗与计算吞吐量在边缘硬件上往往并不对等。
method: 通过分析层级贡献和推断不同量化类型在目标硬件上的行为，为每一层分配最合适的量化精度以满足用户定义的优先级。
result: 该方法成功解锁了统一量化无法实现的配置设计，在资源受限的设备上扩展了 LLM 部署的有效解空间。
conclusion: APreQEL 证明了结合层重要性与硬件特性进行自适应量化是实现边缘 LLM 高效部署的关键路径。
---

## 摘要
如今，大语言模型在推理、代码生成和复杂问题解决等各种任务中展现出了强大的实力。然而，这种进步伴随着高昂的计算成本和内存需求，使得在边缘设备上部署这些模型以确保实时响应和数据隐私变得具有挑战性。量化是减少内存使用的一种常用方法，但大多数方法在所有层中统一应用量化。这忽略了不同层对精度降低的反应可能不同这一事实。重要的是，内存消耗和计算吞吐量并不一定对齐，这进一步增加了部署决策的复杂性。本文提出了一种自适应混合精度量化机制，在用户定义的优先级下，平衡边缘部署中的内存、延迟和准确性。这是通过分析逐层贡献并推断不同量化类型在目标硬件平台上的表现来实现的，从而为每一层分配最合适的量化类型。这种集成确保了在该设计中能够同时兼顾层重要性和整体性能的权衡。我们的工作开启了统一量化无法实现的新配置设计，扩展了在资源受限设备上高效部署大语言模型的解决方案空间。

## Abstract
Today, large language models have demonstrated their strengths in various tasks ranging from reasoning, code generation, and complex problem solving. However, this advancement comes with a high computational cost and memory requirements, making it challenging to deploy these models on edge devices to ensure real-time responses and data privacy. Quantization is one common approach to reducing memory use, but most methods apply it uniformly across all layers. This does not account for the fact that different layers may respond differently to reduced precision. Importantly, memory consumption and computational throughput are not necessarily aligned, further complicating deployment decisions. This paper proposes an adaptive mixed precision quantization mechanism that balances memory, latency, and accuracy in edge deployment under user-defined priorities. This is achieved by analyzing the layer-wise contribution and by inferring how different quantization types behave across the target hardware platform in order to assign the most suitable quantization type to each layer. This integration ensures that layer importance and the overall performance trade-offs are jointly respected in this design. Our work unlocks new configuration designs that uniform quantization cannot achieve, expanding the solution space to efficiently deploy the LLMs on resource-constrained devices.

---

## 论文详细总结（自动生成）

### 论文总结：APreQEL —— 面向边缘大语言模型的自适应混合精度量化

#### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的边缘设备（如嵌入式系统）上高效部署大语言模型（LLM），同时平衡**内存占用、推理延迟和模型准确性**这三个相互冲突的指标。
*   **研究动机**：
    *   **统一量化的局限性**：现有的量化方法通常对所有层应用相同的精度（如统一 4-bit），忽略了不同层对模型输出贡献的差异。
    *   **硬件表现的非线性**：在边缘硬件上，更低的比特率并不总是意味着更快的速度。由于反量化（De-quantization）开销的存在，某些低比特量化（如 3-bit）的延迟反而可能高于高比特量化。
    *   **用户需求多样化**：不同应用场景对性能指标的优先级不同（如实时对话在意延迟，离线分析在意准确性）。

#### 2. 方法论
APreQEL 提出了一种自适应混合精度量化框架，包含三个核心模块：
*   **层级贡献分析模块**：
    *   利用**余弦相似度**测量连续层之间隐藏嵌入（Embeddings）的变化。
    *   采用**奖励-惩罚（Reward-Penalty）机制**：如果某层引起的变化超过阈值 $\gamma$（设为 0.9），则认为该层贡献大，赋予高分；反之则视为冗余或次要层。
*   **量化类型分布模块**：
    *   **多准则决策（MCDA）**：引入 **TOPSIS 算法**，根据用户定义的权重（内存、延迟、准确性）对所有可能的量化比例组合进行评估。
    *   **性能估算**：通过少量统一量化的基准测试数据，线性估算混合精度配置下的整体性能，避免了穷举搜索的组合爆炸问题。
*   **层级量化分配模块**：
    *   将重要性得分最高的层分配给精度最高（最不具侵略性）的量化类型，得分低的层分配给低精度量化，从而在满足资源约束的同时最大化保留模型能力。

#### 3. 实验设计
*   **模型对象**：Llama-3.1-8B、Phi-3.5-Mini、Qwen3-4B（注：文中称 Qwen3，可能指代特定版本或系列）。
*   **量化工具**：基于 `llama.cpp` 的 K-quant 类型（3, 4, 5, 6, 8-bit）。
*   **数据集**：使用 WikiText-2 评估困惑度（Perplexity，作为准确性指标）；使用 WebInstructSub-prometheus 评估层间相似度。
*   **对比基准（Benchmark）**：与传统的**统一精度量化**（Uniform Quantization）进行对比。
*   **评估指标**：推理延迟（ms/token）、内存占用（GB）、困惑度（PPL）以及**超体积增益（Hypervolume Gain）**。

#### 4. 资源与算力
*   **硬件平台**：NVIDIA Jetson Orin-AGX 嵌入式板卡（12核 ARM CPU, Ampere 架构 GPU, 64GB LPDDR5 内存）。
*   **算力消耗**：论文未明确提及具体的训练/量化时长，但由于采用的是后训练量化（PTQ）思路且基于 `llama.cpp`，其计算开销远低于模型微调。实验中对每个模型生成了 28 种不同的混合精度配置进行评估。

#### 5. 实验数量与充分性
*   **实验规模**：针对 3 种不同架构的模型，每种模型测试了 28 个 APreQEL 生成的配置，涵盖了“公平平衡”、“延迟优先”、“内存优先”和“准确性优先”四种用户场景。
*   **充分性**：实验覆盖了主流的边缘 LLM 架构，并使用了多目标优化的标准指标（超体积）来衡量帕累托前沿（Pareto Front）的扩展。实验设计较为客观，考虑了硬件实际运行时的反量化开销，而非仅停留在理论压缩比上。

#### 6. 主要结论与发现
*   **性能提升**：APreQEL 生成的配置在帕累托前沿上优于统一量化，超体积指标（HV）平均提升了约 **8.4% - 9.3%**。
*   **打破线性认知**：实验证实 5-bit 在某些模型上比 8-bit 更快，而 APreQEL 能够自动识别并利用这种硬件特性。
*   **高度自适应**：该方法能根据用户权重精确调整模型形态。例如，在内存优先模式下，它能自动将更多层分配给 3-bit，同时保留关键层在 4-bit 以维持基本准确性。

#### 7. 优点
*   **硬件感知**：不仅考虑模型参数，还考虑了特定硬件上的实际推理延迟，具有很强的工程实践意义。
*   **灵活的用户定制**：通过 TOPSIS 算法允许用户直观地通过权重调整部署策略。
*   **搜索效率高**：通过层级评分和性能估算，避免了在巨大的混合精度空间中进行昂贵的暴力搜索。

#### 8. 不足与局限
*   **启发式阈值**：层级贡献分析中的阈值 $\gamma$ 设定为 0.9 属于启发式经验值，可能需要针对不同任务进一步调优。
*   **量化库限制**：目前仅支持 `llama.cpp` 的量化算法，尚未验证在 TensorRT-LLM 或其他推理引擎上的通用性。
*   **指标覆盖**：虽然考虑了延迟和内存，但尚未将**功耗（Energy）**和**碳足迹**等边缘计算中同样重要的指标纳入多准则分析。

（完）
