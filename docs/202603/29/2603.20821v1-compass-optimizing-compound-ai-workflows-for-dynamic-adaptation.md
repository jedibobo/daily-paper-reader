---
title: "Compass: Optimizing Compound AI Workflows for Dynamic Adaptation"
title_zh: Compass：面向动态自适应的复合 AI 工作流优化
authors: "Milos Gravara, Juan Luis Herrera, Stefan Nastic"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20821v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 精度与延迟的帕累托最优配置
tldr: 针对复合AI系统在固定硬件资源下难以兼顾精度、延迟和成本的问题，本文提出Compass框架。该框架通过离线优化发现帕累托最优配置，并利用在线适配机制根据实时负载动态切换配置。Compass包含配置发现算法、策略规划器和运行时控制器，在保证SLO合规性的同时，显著提升了系统在动态负载下的运行效率和精度表现。
selection_source: fresh_fetch
motivation: 复合AI系统在固定基础设施上运行，需要一种能在动态负载下通过权衡精度与延迟来满足服务水平目标（SLO）的自适应机制。
method: 提出Compass框架，包含用于搜索帕累托最优配置的COMPASS-V算法、基于排队论制定切换策略的Planner以及根据队列深度执行动态切换的Elastico控制器。
result: "实验显示COMPASS-V在减少57.5%评估次数的同时达到100%召回率，且运行时适配在动态负载下实现了90-98%的SLO合规率。"
conclusion: Compass通过离线搜索与在线动态适配相结合，有效解决了复合AI工作流在资源受限环境下的性能优化与负载适应难题。
---

## 摘要
复合 AI 是一种分布式智能方法，它代表了一个将专门的 AI/ML 模型与工程化软件组件编排为 AI 工作流的统一系统。复合 AI 的生产部署必须在变化的负载下满足准确率、延迟和成本目标。然而，许多部署运行在无法进行水平扩展的固定基础设施上。现有方法仅针对准确率进行优化，而未考虑工作负载条件的变化。我们观察到，复合 AI 系统可以在不同配置之间切换以适应基础设施容量，根据当前负载在准确率和延迟之间进行权衡。这需要从组合搜索空间中发现多个帕累托最优（Pareto-optimal）配置，并确定在运行时何时进行切换。我们提出了 Compass，这是一个通过离线优化和在线自适应实现动态配置切换的新型框架。Compass 由三个组件组成：用于配置发现的 COMPASS-V 算法、用于切换策略推导的 Planner 以及用于运行时自适应的 Elastico 控制器。COMPASS-V 使用有限差分引导搜索以及爬山算法和横向扩展的结合来发现准确率可行的配置。Planner 在目标硬件上对这些配置进行性能分析，并使用基于排队论的模型推导切换策略。Elastico 监控队列深度并根据推导出的阈值切换配置。在两个复合 AI 工作流的实验中，与穷举搜索相比，COMPASS-V 实现了 100% 的召回率，同时平均减少了 57.5% 的配置评估，在严格的准确率阈值下效率提升高达 95.3%。运行时自适应在动态负载模式下实现了 90-98% 的 SLO 合规性，比静态高准确率基准提高了 71.6% 的 SLO 合规性，同时比静态快速基准提高了 3-5% 的准确率。

## Abstract
Compound AI is a distributed intelligence approach that represents a unified system orchestrating specialized AI/ML models with engineered software components into AI workflows. Compound AI production deployments must satisfy accuracy, latency, and cost objectives under varying loads. However, many deployments operate on fixed infrastructure where horizontal scaling is not viable. Existing approaches optimize solely for accuracy and do not consider changes in workload conditions. We observe that compound AI systems can switch between configurations to fit infrastructure capacity, trading accuracy for latency based on current load. This requires discovering multiple Pareto-optimal configurations from a combinatorial search space and determining when to switch between them at runtime. We present Compass, a novel framework that enables dynamic configuration switching through offline optimization and online adaptation. Compass consists of three components: COMPASS-V algorithm for configuration discovery, Planner for switching policy derivation, and Elastico Controller for runtime adaptation. COMPASS-V discovers accuracy-feasible configurations using finite-difference guided search and a combination of hill-climbing and lateral expansion. Planner profiles these configurations on target hardware and derives switching policies using a queuing theory based model. Elastico monitors queue depth and switches configurations based on derived thresholds. Across two compound AI workflows, COMPASS-V achieves 100% recall while reducing configuration evaluations by 57.5% on average compared to exhaustive search, with efficiency gains reaching 95.3% at tight accuracy thresholds. Runtime adaptation achieves 90-98% SLO compliance under dynamic load patterns, improving SLO compliance by 71.6% over static high-accuracy baselines, while simultaneously improving accuracy by 3-5% over static fast baselines.

---

## 论文详细总结（自动生成）

### Compass: 面向动态自适应的复合 AI 工作流优化 —— 论文总结

#### 1. 论文的核心问题与整体含义
随着 AI 从单体模型转向由多个模型、工具和算法组成的“复合 AI（Compound AI）”系统，如何在固定基础设施（如边缘计算、私有服务器）上平衡**准确率、延迟和成本**成为了核心挑战。
*   **研究动机：** 现有的优化方法通常只关注静态的准确率提升，忽略了生产环境中波动的查询负载。在无法进行水平扩展（增加机器）的场景下，系统必须能够根据负载动态调整自身配置。
*   **核心问题：** 如何从巨大的组合搜索空间中高效发现帕累托最优配置，并制定在运行时根据负载进行配置切换的策略。

#### 2. 方法论
Compass 框架分为**离线优化**和**在线适配**两个阶段，包含三个核心组件：

*   **COMPASS-V（离线配置搜索）：**
    *   **核心思想：** 在非微分的组合空间中寻找满足精度阈值 $\tau$ 的所有可行配置。
    *   **技术细节：** 使用**反距离加权有限差分**来估算精度梯度；引入**Wilson 置信区间**进行统计早期停止（减少不必要的评估）；结合**爬山算法**（向高精度区域移动）和**横向扩展**（沿边界探索可行区域）。
*   **Planner（策略规划器）：**
    *   **核心思想：** 利用**分析排队模型（AQM）**将推理系统建模为 M/G/1 队列。
    *   **技术细节：** 根据目标硬件的性能分析，计算每个配置的“切换阈值”。公式基于 P95 延迟 SLO 目标减去配置本身的尾部延迟，推导出最大安全队列深度。
*   **Elastico Controller（运行时控制器）：**
    *   **核心思想：** 实时监控队列深度，执行配置切换。
    *   **技术细节：** 采用**非对称滞后策略**，即在负载增加时快速切换到低延迟配置以保住 SLO，而在负载降低时缓慢切换回高精度配置以防止系统震荡。

#### 3. 实验设计
*   **实验场景与数据集：**
    1.  **RAG（检索增强生成）工作流：** 使用 SQuAD 2.0 数据集，涉及 Llama3/Gemma 模型、不同检索器（BM25）和重排序器。
    2.  **目标检测级联工作流：** 使用 COCO 数据集，涉及 YOLOv8 不同尺寸模型（n/s/m/l/x）的级联。
*   **Benchmark（基准测试）：**
    *   **离线：** 与穷举搜索（Grid Search）对比搜索效率和召回率。
    *   **在线：** 与三种静态配置（Static-Fast, Static-Medium, Static-Accurate）对比。
*   **负载模式：** 测试了 Spike（突发高峰）和 Bursty（频繁小波动）两种动态负载模式。

#### 4. 资源与算力
*   **硬件环境：** 实验在一台配备 **NVIDIA RTX 4090 (24GB VRAM)**、32 核 CPU 和 128GB RAM 的专用服务器上完成。
*   **算力细节：** 论文明确指出所有评估的配置都预加载在 GPU 显存中（总计约 18GB），以消除运行时切换的冷启动开销，确保切换延迟小于 10ms。

#### 5. 实验数量与充分性
*   **实验规模：** 
    *   离线搜索涵盖了 RAG（234 种配置）和目标检测（385 种配置）的完整空间。
    *   针对 16 个不同的精度 SLO 阈值进行了 COMPASS-V 的效率测试。
    *   在线适配测试了 3 种不同的延迟 SLO 目标（500ms, 1000ms, 1500ms）。
*   **充分性评价：** 实验设计较为充分，涵盖了 NLP 和 CV 两个主流领域，且通过与穷举搜索的对比证明了算法的 100% 召回率，结果具有客观性和说服力。

#### 6. 论文的主要结论与发现
*   **搜索效率：** COMPASS-V 平均减少了 **57.5%** 的配置评估次数，在严格精度要求下效率提升高达 **95.3%**，且保持了 100% 的可行配置召回。
*   **运行时表现：** Elastico 在动态负载下实现了 **90-98%** 的 SLO 合规性。
*   **性能提升：** 相比于静态高精度基准，SLO 合规率提升了 **71.6%**；相比于静态快速基准，在满足延迟的同时将精度提升了 **3-5%**。

#### 7. 优点
*   **针对性强：** 专门解决了固定基础设施（如边缘侧）无法水平扩展时的复合 AI 优化问题。
*   **理论与实践结合：** 将复杂的组合优化问题与经典的排队论相结合，推导出的切换阈值具有理论支撑。
*   **高效性：** COMPASS-V 算法通过统计学手段显著降低了昂贵的模型评估成本。

#### 8. 不足与局限
*   **显存依赖：** 假设所有帕累托最优配置都能预加载到显存中。对于超大规模模型（如多个 70B 模型组成的工作流），单卡显存可能无法支撑。
*   **反应式调节：** 目前的切换是基于当前队列深度的“反应式”调整，而非基于负载预测的“主动式”调整，在极端瞬时峰值下可能仍有短暂延迟。
*   **原子切换限制：** 框架目前将整个工作流配置视为一个整体进行切换，未来若能实现组件级的细粒度动态调整，可能会进一步提升资源利用率。

（完）
