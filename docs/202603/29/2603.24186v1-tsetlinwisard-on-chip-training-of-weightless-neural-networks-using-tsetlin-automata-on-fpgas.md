---
title: "TsetlinWiSARD: On-Chip Training of Weightless Neural Networks using Tsetlin Automata on FPGAs"
title_zh: TsetlinWiSARD：在 FPGA 上利用 Tsetlin 自动机实现无权重神经网络的片上训练
authors: "Shengyu Duan, Marcos L. L. Sartori, Rishad Shafik, Alex Yakovlev"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24186v1"
tags: ["query:hwnas"]
score: 9.0
evidence: FPGA上无权重神经网络的片上训练
tldr: 针对边缘计算对片上训练的需求，本文提出TsetlinWiSARD，将Tsetlin自动机引入无权重神经网络（WNN）。该方法通过概率反馈驱动的迭代优化克服了传统WiSARD单次训练易过拟合的问题。在FPGA上实现的架构不仅实现了高精度，还比传统WiSARD训练快1000倍以上，且在资源、延迟和功耗方面显著优于现有FPGA机器学习加速器。
selection_source: fresh_fetch
motivation: 传统无权重神经网络的单次训练模式易导致过拟合且难以在片上高效调整，限制了其在边缘设备上的应用。
method: 结合Tsetlin自动机与WiSARD架构，利用概率反馈机制实现迭代优化，并设计了高效的FPGA片上训练硬件架构。
result: "训练速度比传统WiSARD快1000倍以上，且相比其他FPGA加速器，资源占用减少22%，延迟降低93.3%，功耗降低64.2%。"
conclusion: TsetlinWiSARD为边缘设备提供了一种高效、低功耗且具备片上训练能力的机器学习解决方案。
---

## 摘要
边缘端对适应性、隐私和安全性的需求不断增加，持续推动着具有片上训练和推理能力的新一代机器学习（ML）算法的前沿。无权重神经网络（WNN）就是这样一种基于查找表（LUT）简单神经元结构的算法。因此，与严重依赖乘累加（MAC）操作的深度神经网络相比，它具有低延迟、低复杂度推理等架构优势。然而，传统的 WNN 依赖于基于记忆的一次性训练，这要么导致过拟合和精度下降，要么需要繁琐的训练后调整，限制了其在高效片上训练中的有效性。在这项工作中，我们提出了 TsetlinWiSARD，这是一种利用 Tsetlin 自动机（TA）实现概率性、反馈驱动学习的 WNN 训练方法。它通过迭代优化克服了 WiSARD 一次性训练的过拟合问题，同时保持了简单、连续的二进制反馈，以实现高效的片上训练。我们方法的核心是一个基于现场可编程门阵列（FPGA）的训练架构，它在提供最先进精度的同时，显著提高了硬件效率。与传统的 WiSARD WNN 实现相比，我们的方法提供了超过 1000 倍的训练速度提升。此外，与实现其他机器学习算法的基于 FPGA 的训练加速器相比，我们证明了资源使用量减少了 22%，延迟降低了 93.3%，功耗降低了 64.2%。

## Abstract
Increasing demands for adaptability, privacy, and security at the edge have persistently pushed the frontiers for a new generation of machine learning (ML) algorithms with training and inference capabilities on-chip. Weightless Neural Network (WNN) is such an algorithm that is principled on lookup table based simple neuron structures. As a result, it offers architectural benefits, such as low-latency, low-complexity inference, compared to deep neural networks that depend heavily on multiply-accumulate operations. However, traditional WNNs rely on memorization-based one-shot training, which either leads to overfitting and reduced accuracy or requires tedious post-training adjustments, limiting their effectiveness for efficient on chip training.   In this work, we propose TsetlinWiSARD, a training approach for WNNs that leverages Tsetlin Automata (TAs) to enable probabilistic, feedback-driven learning. It overcomes the overfitting of WiSARD's one-shot training with iterative optimization, while maintaining simple, continuous binary feedback for efficient on-chip training. Central to our approach is a field programmable gate array (FPGA)-based training architecture that delivers state-of-the-art accuracy while significantly improving hardware efficiency. Our approach provides over 1000x faster training when compared with the traditional WiSARD implementation of WNNs. Further, we demonstrate 22% reduced resource usage, 93.3% lower latency, and 64.2% lower power consumption compared to FPGA-based training accelerators implementing other ML algorithms.

---

## 论文详细总结（自动生成）

以下是对论文《TsetlinWiSARD: On-Chip Training of Weightless Neural Networks using Tsetlin Automata on FPGAs》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：边缘计算设备对实时模型自适应（片上训练）的需求日益增长，以满足隐私、安全和动态环境适应的要求。
*   **现有挑战**：
    *   **深度神经网络（DNN）**：训练过程涉及大量浮点运算和梯度计算，在资源受限的 FPGA 上能效极低。
    *   **传统无权重神经网络（WNN/WiSARD）**：虽然基于查找表（LUT）的结构非常适合 FPGA，但其依赖“单次记忆”训练，容易导致过拟合，且缺乏高效的迭代优化机制。
*   **核心目标**：提出一种名为 **TsetlinWiSARD** 的新架构，将 Tsetlin 自动机（TA）引入 WNN，实现一种硬件友好、基于概率反馈的迭代片上训练方法。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 Tsetlin 自动机（TA）作为 WiSARD 查找表（LUT）中每个条目的学习单元。TA 通过状态转移来决定 LUT 条目输出 0 还是 1，从而将“记忆过程”转变为“强化学习过程”。
*   **关键技术细节**：
    *   **TA 集成**：每个 $n$ 输入的 LUT 对应 $2^n$ 个 TA。TA 的内部状态决定输出：状态值在前半区间输出 0，后半区间输出 1。
    *   **训练机制**：采用基于反馈的迭代更新。当预测错误时，触发反馈：
        *   **正确类别判别器**：以概率 $P$ 增加（Increment）对应条目的 TA 状态值。
        *   **错误类别判别器**：以概率 $P$ 减少（Decrement）对应条目的 TA 状态值。
    *   **硬件架构设计**：
        *   **TA 团队（TA Team）**：利用 FPGA 的分布式 RAM（LUTRAM）紧凑存储 TA 状态。
        *   **概率生成**：使用线性反馈移位寄存器（LFSR）产生伪随机数，以极低成本实现概率反馈 $P$。
        *   **全硬件实现**：无需处理器参与，通过简单的逻辑比较、加法和减法实现训练逻辑。

### 3. 实验设计
*   **数据集**：涵盖了 6 个主流基准数据集，包括图像识别（MNIST, Fashion-MNIST, KMNIST）和传感器数据（EMG 手势、HAR 人体活动、GPS 手势相位）。
*   **对比方法（Benchmark）**：
    *   **算法对比**：标准 WiSARD（单次训练）、B-bleaching（基于统计的改进版）、Tsetlin Machine (TM)。
    *   **硬件对比**：Hashed WNN（基于处理器的训练）、LogicWiSARD、BTHOWeN（仅推理加速器）、DTM（Tsetlin Machine 加速器）、F-CNN（卷积神经网络训练加速器）。

### 4. 资源与算力
*   **硬件平台**：Xilinx Zynq XC7Z020 (PYNQ-Z1) FPGA。
*   **算力与资源消耗**：
    *   **逻辑资源**：使用了 33,596 个 LUT 和 25,927 个 FF（触发器）。
    *   **功耗**：动态功耗仅为 **1.56W**。
    *   **训练时长**：单样本训练/推理延迟仅为 **6.4 $\mu$s**。
*   **对比结论**：比传统的 WiSARD 训练加速器快 **1000 倍以上**。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 个不同领域的数据集进行了详尽测试，验证了算法的通用性。
*   **消融实验/参数研究**：深入探讨了 LUT 数量、每个 LUT 的输入位数、TA 状态数以及反馈概率 $P$ 对准确率的影响。
*   **充分性评价**：实验设计客观且全面，不仅涵盖了软件层面的准确率对比，还提供了详尽的硬件资源、延迟和功耗击穿分析，足以支撑其结论。

### 6. 主要结论与发现
*   **准确率提升**：在所有测试数据集上，TsetlinWiSARD 的准确率均显著优于传统的 WiSARD 和 B-bleaching 方法。
*   **硬件效率极高**：相比于 TM（Tsetlin Machine），在达到同等准确率的情况下，TsetlinWiSARD 减少了超过 1/3 的 TA 需求。
*   **性能优势**：与 FPGA 上的 CNN 训练加速器相比，资源占用减少 **22%**，延迟降低 **93.3%**，功耗降低 **64.2%**。

### 7. 优点
*   **架构创新**：首次将 TA 与 WNN 结合，解决了 WNN 难以进行高效迭代片上训练的顽疾。
*   **硬件匹配度高**：算法逻辑（LUT 基础）与 FPGA 底层架构（LUT 单元）完美契合，实现了极高的硬件利用率。
*   **无处理器依赖**：实现了真正的全硬件、纯逻辑片上训练，极大地降低了边缘端的响应延迟。

### 8. 不足与局限
*   **绝对准确率限制**：尽管优于传统 WNN，但在 MNIST 等复杂任务上，其准确率仍略低于最先进的深度学习模型（DNN/CNN）。
*   **随机性偏差**：硬件实现的 LFSR 伪随机性在某些情况下可能导致训练偏差，使得片上准确率略低于离线软件模拟。
*   **资源扩展性**：对于极大规模的模型，受限于 FPGA 内部 LUTRAM 的总量，可能需要更高规格的芯片支持。

（完）
