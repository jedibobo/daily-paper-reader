---
title: "Space Fabric: A Satellite-Enhanced Trusted Execution Architecture"
title_zh: Space Fabric：一种卫星增强型可信执行架构
authors: "Filip Rezabek, Dahlia Malkhi, Amir Yahalom"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23745v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 轨道计算的可信执行架构
tldr: Space Fabric 针对去中心化卫星网络提出了一种新型可信执行架构。它利用卫星在轨后的物理不可接触性作为天然防御屏障，通过拜占庭容错的地面站仲裁机制，实现了对工作负载内容及地理位置的双重认证。该架构在发射后生成密钥，并结合双硬件安全元件消除对单一厂商的依赖，为轨道计算提供了独立审计的可信基础。
selection_source: fresh_fetch
motivation: 传统的地面可信执行环境依赖厂商预置密钥且易受物理攻击，无法满足卫星网络对去中心化、抗物理篡改及独立审计的信任需求。
method: 利用卫星在轨物理隔离特性，结合双安全元件协同签名和拜占庭容错地面站集群，实现发射后密钥生成与位置感知认证。
result: 在 USB Armory Mk II 硬件上成功实现了基于 ARM TrustZone 的原型，并通过 Veraison 完成了端到端的可信度量验证。
conclusion: Space Fabric 通过硬件冗余和地理位置绑定，构建了一个不依赖单一厂商且具备强抗攻击能力的卫星可信执行体系，解决了轨道计算的信任根问题。
---

## 摘要
去中心化卫星网络的兴起，迫切需要一种无需物理接触硬件、无需预置厂商密钥、且不依赖单一制造商远程度量服务的信任架构。陆基可信执行环境（TEE）已不足以满足需求：基于硬件的设计易受物理攻击，且大多数平台的度量链根植于制造过程中预置的密钥，从而产生了发射前的信任窗口和无法独立审计的单一厂商依赖。我们提出了 Space Fabric，这是一种通过将可信计算栈迁移到卫星基础设施，为轨道计算提供缺失信任基础的架构。它利用发射后物理上的不可接触性作为防篡改屏障，这是陆基部署无法实现的。我们的卫星执行保证协议通过分布式地面站的拜占庭容错背书法定人数，将工作负载执行绑定到特定卫星，不仅证明了 TEE 内部执行的内容，还证明了执行的位置。所有加密密钥均在发射后于同址部署的安全元件中生成，地球上的任何地方在任何时候都无法访问签名密钥。为了减少对单一厂商的依赖，Space Fabric 将其信任锚分布在两个独立的安全元件（NXP SE050 和 TROPIC01）上，两者必须共同签署度量证据。我们在带有 ARM TrustZone 的 USB Armory Mk II 上实现了 Space Fabric，使用 Veraison 进行了端到端度量验证，并提供了在强自适应对手下的安全性分析，包括满足性论证和不可能界限。

## Abstract
The emergence of decentralized satellite networks creates a pressing need for trust architectures that operate without physical access to hardware, without pre-provisioned vendor secrets, and without dependence on a single manufacturer's attestation service. Terrestrial TEEs are insufficient: hardware-based designs are susceptible to physical attacks, and most platforms root their attestation chains in secrets provisioned during manufacturing, creating a pre-launch trust window and single-vendor dependency that cannot be independently audited.   We present Space Fabric, an architecture that provides the missing trust foundation for orbital computing by relocating the trusted computing stack to satellite infrastructure, exploiting post-launch physical inaccessibility as a tamper barrier unattainable by terrestrial deployments. Our Satellite Execution Assurance Protocol binds workload execution to a specific satellite via a Byzantine-tolerant endorsement quorum of distributed ground stations, certifying not only \emph{what} executes inside the TEE but also \emph{where}. All cryptographic secrets are generated within co-located secure elements after launch, with no signing keys accessible on Earth at any point. To reduce single-vendor dependence, Space Fabric distributes its trust anchor across two independent secure elements, an NXP SE050 and a TROPIC01, both of which must co-sign attestation evidence. We implement Space Fabric on a USB Armory Mk II with ARM TrustZone, verify attestation end-to-end using Veraison, and provide a security analysis with satisfaction arguments and impossibility bounds under a strong adaptive adversary.

---

## 论文详细总结（自动生成）

### Space Fabric：一种卫星增强型可信执行架构详细总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着去中心化卫星网络和轨道计算（Orbital Computing）的兴起，如何在无法物理接触硬件、不信任运营商且不依赖单一硬件厂商的情况下，验证轨道上运行程序的真实性与完整性成为了核心挑战。
*   **现有技术的局限：** 陆基可信执行环境（TEE，如 Intel SGX/TDX, ARM TrustZone）依赖于制造阶段预置的厂商私钥，存在“发射前信任窗口”和单一厂商依赖风险。此外，陆基硬件无法防御拥有持续物理接触能力的攻击者（如总线探测、内存提取）。
*   **核心目标：** 建立一种“卫星原生”的信任架构，利用卫星在轨后物理上不可接触的特性作为天然屏障，并证明工作负载不仅执行了正确的代码（What），而且是在特定的轨道平台上执行的（Where）。

#### 2. 论文提出的方法论
论文提出了 **Space Fabric** 架构，其核心思想是将可信计算栈迁移至轨道，并引入以下关键技术：
*   **在轨密钥生成（On-orbit Key Genesis）：** 所有加密签名密钥（身份密钥和度量密钥）均在卫星发射入轨后的首次启动时，由硬件安全元件（SE）内部生成。地球上没有任何人（包括制造商和运营商）曾接触过这些私钥，消除了供应链泄密风险。
*   **双安全元件信任锚（Dual-SE Trust Anchor）：** 为了打破单一厂商依赖，架构同时使用两个独立厂商的安全元件：NXP SE050（闭源、高等级认证）和 TROPIC01（全开源、可审计）。两者必须协同签名才能产生有效的远程度量证据。
*   **卫星执行保证协议（SEAP）：** 这是一个基于拜占庭容错的分布式背书协议。
    1.  **挑战-响应：** 地面站（GS）向卫星发送随机随机数（Nonce）。
    2.  **在轨度量：** 卫星 TEE 测量当前软件栈，结合 Nonce 由双 SE 签名返回。
    3.  **位置绑定（Proof of ET）：** 只有当卫星在特定时间窗口内与足够数量（$t_{GS} + 2t_{ch} + 1$）的分布式地面站完成交互后，才能合成“授权证书”。这利用了轨道力学约束，证明了 TEE 确实位于太空中，而非地面伪造的服务器。

#### 3. 实验设计
*   **硬件原型：** 使用 **USB Armory Mk II**（搭载 NXP i.MX6UL, ARM Cortex-A7 处理器）模拟卫星载荷，通过 USB 连接到 Raspberry Pi 5（作为非对称通信中继）。
*   **软件栈：** 采用 **ARM TrustZone** 技术，运行 **GoTEE** 可信操作系统和 **OP-TEE** 框架。
*   **验证工具：** 使用 IETF RATS 标准的 **Veraison** 项目作为远程度量验证服务器。
*   **场景模拟：** 模拟了 500km 高度的近地轨道（LEO）卫星，考虑了 95 分钟的轨道周期和 5-10 分钟的地面站过境窗口。
*   **Benchmark：** 主要对比了 Intel SGX/TDX、AMD SEV-SNP 等主流 TEE 的信任根模型和密钥配置流程。

#### 4. 资源与算力
*   **算力设备：** 实验基于嵌入式 SoC（ARM Cortex-A7），而非高性能 GPU。
*   **算力消耗：** 论文未提及 GPU 训练时长（因为不涉及深度学习训练），但详细记录了加密操作的延迟：双 SE 协同签名约需 100-400ms，单次 SEAP 交互总延迟约 210-620ms。
*   **存储开销：** 每次交互带宽需求极低（约 1.9 KB），非常适合带宽受限的卫星链路。

#### 5. 实验数量与充分性
*   **实验内容：** 论文进行了端到端的原型实现验证、安全性形式化分析（针对自适应对手模型）、以及针对轨道力学的性能建模。
*   **充分性：** 实验涵盖了从硬件启动、密钥生成到地面站背书合成的全流程。虽然物理实验是在地面实验室环境下完成的（使用真实卫星级嵌入式硬件），但通过数学建模和安全性证明（如针对中继攻击、克隆攻击的分析）弥补了无法进行实际太空测试的局限。实验设计较为客观，明确指出了在强对手控制通信渠道时的“不可能界限”。

#### 6. 论文的主要结论与发现
*   **物理隔离是强安全原语：** 卫星在轨后的物理不可接触性可以替代复杂的陆基硬件防御（如昂贵的内存加密），降低载荷功耗。
*   **位置证明可行性：** 通过分布式地面站的法定人数背书，可以有效防止“地面克隆卫星”进行身份欺诈。
*   **去中心化信任：** 证明了可以在不依赖单一芯片厂商（如 Intel 或 AMD）在线验证服务的情况下，构建一个可独立审计的全球信任网络。

#### 7. 优点
*   **创新性：** 首次提出了结合轨道力学约束与 TEE 度量的“位置感知”信任模型。
*   **供应链安全：** 通过在轨密钥生成和双 SE 架构，彻底解决了传统 TEE 无法规避的预置密钥泄露问题。
*   **开源友好：** 引入了 TROPIC01 开源安全元件，增强了系统的透明度和可审计性。

#### 8. 不足与局限
*   **时间延迟：** 在 LEO 轨道下，由于需要收集多个地面站的背书，获得完整证书可能需要 6-11 小时，不适合极高实时性的初始化需求。
*   **辐射风险：** 实验虽然讨论了单粒子翻转（SEU）的缓解，但在真实极端辐射环境下的长期可靠性仍需更多空间级硬件验证。
*   **通信依赖：** 如果攻击者能完全控制卫星的所有外部通信（C1 挑战），则无法保证系统的可用性。
*   **硬件开销：** 相比单一芯片方案，双 SE 加外部中继的架构增加了硬件复杂度和布线需求。

（完）
