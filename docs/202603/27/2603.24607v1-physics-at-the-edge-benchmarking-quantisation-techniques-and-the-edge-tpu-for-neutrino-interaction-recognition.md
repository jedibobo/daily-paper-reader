---
title: "Physics at the Edge: Benchmarking Quantisation Techniques and the Edge TPU for Neutrino Interaction Recognition"
title_zh: 边缘物理：用于中微子相互作用识别的量化技术与 Edge TPU 基准测试
authors: "Stefano Vergani, Hilary Utaegbulam, Michael Wang, Leigh H. Whitehead, Arden Tsang, Lorenzo Uboldi"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.24607v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 在 Edge TPU 上进行量化和部署的基准测试
tldr: 本研究针对中微子相互作用识别任务，在通用液氩时间投影室模拟数据上，对卷积神经网络的量化技术及Google Coral Edge TPU的性能进行了全面基准测试。通过对比训练后量化和量化感知训练，评估了四种模型在Edge TPU、CPU和GPU上的准确率、推理速度及能耗。结果表明，Edge TPU在保持较高准确率的同时，能耗显著低于传统硬件，为边缘AI在中微子物理中的应用提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在探索低功耗边缘计算硬件和模型量化技术在中微子物理实验实时数据处理中的应用潜力。
method: 对比了训练后整数量化与量化感知训练两种方案，并在Google Coral Edge TPU、AMD CPU和NVIDIA GPU上测试了四种CNN模型的性能。
result: Inception V3模型量化后精度几乎无损，且Edge TPU的能耗比传统CPU和GPU低数个数量级，尽管速度慢于GPU但与CPU相当。
conclusion: 边缘AI硬件在能耗效率方面具有显著优势，为未来中微子物理实验与边缘计算技术的集成奠定了基础。
---

## 摘要
本研究针对应用于中微子相互作用识别的卷积神经网络，对不同的量化技术进行了全面的基准测试。利用通用液氩时间投影室的模拟数据，对模型进行量化并部署在 Google Coral Edge TPU 上。测试了四个 Keras 模型，并在两种不同的流程下测量了准确率：训练后整数量化和量化感知训练。推理速度以 AMD EPYC 7763 CPU 和 NVIDIA A100 GPU 为基准进行了对比。此外，还研究了能耗情况，并关注了潜在成本和环境问题。结果表明，在测试的四个模型中，准确率下降有限，特别是 Inception V3 在两种量化和部署流程中几乎没有准确率损失。Edge TPU 的速度与 CPU 相当，比 GPU 慢一个数量级。此外，部署在 Edge TPU 上的所有模型的能耗比 CPU 和 GPU 低几个数量级。在能耗-延迟参数空间中，CPU、GPU 和 Edge TPU 的性能表现出明显的区分。本文探讨了边缘人工智能技术与中微子物理未来整合的可能性。

## Abstract
This work presents a comprehensive benchmark of different quantisation techniques for convolutional neural networks applied to neutrino interaction recognition. Utilising simulation for a generic liquid argon time-projection chamber, models are quantised and then deployed on the Google Coral Edge TPU. Four Keras models are tested, and accuracy is measured across two different pipelines: using post-training integer quantisation and quantisation-aware training. Inference speed is benchmarked against an AMD EPYC 7763 CPU and NVIDIA A100 GPU. A study of the energy consumption is also presented, with attention to potential costs and environmental issues. Results show that, among the four models tested, accuracy degradation is limited and, in particular, Inception V3 presents almost no accuracy degradation across the two quantisation and deployment pipelines. The speed of the edge TPU is comparable to that of the CPU, and one order of magnitude slower than the GPU. Moreover, the energy consumption of all models deployed on the edge TPU is several orders of magnitude lower than that of the CPU and GPU. In the energy consumption-latency parameter space, CPU, GPU, and edge TPU performances can be clearly separated. This paper explores possible future integrations of edge AI technologies with neutrino physics.

---

## 论文详细总结（自动生成）

### 论文总结：边缘物理——用于中微子相互作用识别的量化技术与 Edge TPU 基准测试

#### 1. 论文的核心问题与整体含义（研究动机和背景）
本文探讨了在粒子物理实验（特别是中微子探测）中引入**边缘人工智能（Edge AI）**的必要性与可行性。
*   **研究动机**：传统的深度学习模型（如 CNN）通常在 GPU 上运行，虽然速度快，但存在功耗极高（数百瓦）、成本昂贵、环境影响大（碳足迹）以及无法直接部署在探测器前端等问题。
*   **核心问题**：能否通过模型量化技术，将复杂的中微子识别模型部署在低功耗、低成本的硬件（如 Google Coral Edge TPU）上，并在保持物理分析准确率的同时，显著降低能耗和延迟？

#### 2. 论文提出的方法论
论文的核心思想是利用**模型量化（Quantization）**将 32 位浮点（float32）模型转换为 8 位无符号整数（uint8），以适配 Edge TPU 的脉动阵列（Systolic Array）架构。
*   **关键技术流程**：
    1.  **模型选择**：选取了四种具有代表性的 CNN 架构（ResNet-50V2, DenseNet-169, InceptionV3, EfficientNetV2B0）。
    2.  **训练后量化（PTQ）**：在模型训练完成后，利用代表性数据集进行校准，确定缩放因子和零点，直接转换权重。
    3.  **量化感知训练（QAT）**：在训练/微调阶段插入“伪量化”算子，模拟低精度损失，使模型在训练过程中适应量化带来的扰动。
    4.  **编译与部署**：使用 Edge TPU 编译器将量化后的模型编译为硬件可执行的 `.tflite` 文件。

#### 3. 实验设计
*   **数据集**：模拟的液氩时间投影室（LArTPC）中微子相互作用数据。包含 22,338 个事件（17,338 用于训练，5,000 用于测试），分为三类：$\nu_\mu$ 充电流（CC）、$\nu_e$ 充电流（CC）和中性流（NC）。
*   **输入形式**：将 LArTPC 的三个视图（u, v, w）堆叠为类似 RGB 的三通道图像，尺寸为 224x224（InceptionV3 为 299x299）。
*   **基准测试（Benchmark）对象**：
    *   **硬件对比**：AMD EPYC 7763 CPU、NVIDIA A100 GPU、Google Coral Edge TPU。
    *   **性能指标**：平衡准确率（Balanced Accuracy）、推理延迟（ms/sample）、能耗代理值（mJ/inference）。

#### 4. 资源与算力
*   **训练环境**：使用了 **NVIDIA A100 GPU** 进行模型的初始训练和 QAT 微调。
*   **软件框架**：TensorFlow 2.15 (tf.keras) 和 TensorFlow Model Optimization (TFMOT) 工具包。
*   **训练细节**：使用了 Adam 优化器，初始学习率 $1 \times 10^{-6}$，配合学习率衰减和早停机制。具体训练时长未明确说明，但提到 Epoch 上限设为 50,000 以确保收敛。

#### 5. 实验数量与充分性
*   **实验规模**：针对 4 种模型，分别在 2 种量化路径（PTQ 和 QAT）下进行了测试，并跨越 3 种硬件平台进行性能对比。
*   **充分性评价**：实验设计较为全面，涵盖了从模型训练、量化优化到硬件部署的全流程。通过对比不同架构在量化下的表现，揭示了模型结构对量化敏感度的差异。实验过程考虑了随机种子和环境一致性，具有较好的客观性和公平性。

#### 6. 论文的主要结论与发现
*   **准确率**：**InceptionV3** 表现最稳健，在量化和部署后几乎没有准确率损失。EfficientNetV2B0 在量化过程中准确率下降最严重，主因是部分层无法直接映射到 Edge TPU 导致必须冻结。
*   **推理速度**：Edge TPU 的速度与高性能 CPU 相当，但比 NVIDIA A100 GPU 慢约一个数量级。
*   **能耗效率**：Edge TPU 的能耗比 CPU 和 GPU 低 **2 到 3 个数量级**。在“能耗-延迟”参数空间中，Edge TPU 占据了极低功耗的独特区间。
*   **可行性**：证明了边缘 AI 硬件可以作为 GPU 的廉价、绿色替代方案，直接集成到中微子探测器（如 DUNE）的实时触发系统中。

#### 7. 优点
*   **环保与成本意识**：在追求物理性能的同时，重点关注了 AI 的环境影响（Green AI）和科研成本。
*   **端到端验证**：不仅停留在算法模拟，还完成了从物理模拟数据到真实边缘硬件部署的完整闭环。
*   **模型对比深入**：详细分析了不同 CNN 模块（如残差连接、密集连接、多分支结构）对量化的适应性。

#### 8. 不足与局限
*   **能耗评估简化**：能耗计算采用了“热设计功耗（TDP）× 延迟”的代理方法，这属于最坏情况估计，可能未能完全反映 GPU 在低负载下的动态功耗优化。
*   **模型范围有限**：研究仅限于 CNN，未涉及目前在粒子物理中日益流行的图神经网络（GNN）或 Transformer 架构。
*   **硬件限制**：Edge TPU 仅支持 uint8 量化，这限制了某些需要更高精度或特定算子的现代模型架构的发挥。

（完）
