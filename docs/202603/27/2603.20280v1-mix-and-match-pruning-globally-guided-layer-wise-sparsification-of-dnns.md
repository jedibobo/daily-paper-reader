---
title: "Mix-and-Match Pruning: Globally Guided Layer-Wise Sparsification of DNNs"
title_zh: Mix-and-Match 剪枝：深度神经网络的全局引导逐层稀疏化框架
authors: "Danial Monachan, Samira Nazari, Mahdi Taheri, Ali Azarpeyvand, Milos Krstic, Michael Huebner, Christian Herglotz"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.20280v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对边缘设备高效部署的架构感知剪枝策略
tldr: 针对边缘设备部署中深度神经网络压缩的需求，本文提出 Mix-and-Match 剪枝框架。该方法通过全局引导的逐层稀疏化策略，结合灵敏度评分和架构规则，克服了单一剪枝策略在不同层和架构上的局限性。通过系统采样多种稀疏配置，该框架在 CNN 和 ViT 上实现了帕累托最优的精度与稀疏度权衡，显著降低了精度损失。
selection_source: carryover_cache
motivation: 现有的单一剪枝策略无法适应不同网络层和架构对稀疏度的差异化响应，导致压缩效果不佳。
method: 提出一种结合灵敏度评分与架构感知规则的框架，通过系统采样生成多种全局引导的逐层稀疏化配置。
result: "在 CNN 和 Vision Transformer 上达到了帕累托最优，其中 Swin-Tiny 的精度下降比标准单准则剪枝减少了 40%。"
conclusion: 通过协调现有的剪枝信号而非引入新准则，可以实现更可靠、高效的模型压缩。
---

## 摘要
在边缘设备上部署深度神经网络 (DNN) 需要在保持最小精度损失的同时进行强力压缩。本文介绍了 Mix-and-Match 剪枝，这是一种全局引导的逐层稀疏化框架，它利用敏感度得分和简单的架构规则来生成多样化、高质量的剪枝配置。该框架解决了不同层和架构对剪枝反应不同这一关键局限性，使得单一策略方法往往表现欠佳。Mix-and-Match 推导出感知架构的稀疏度范围（例如，在保留归一化层的同时更激进地剪枝分类器），并系统地对这些范围进行采样，从而为每个敏感度信号（量级、梯度或其组合）生成十种策略。这消除了重复的剪枝运行，同时提供了可直接部署的精度-稀疏度权衡。在 CNN 和 Vision Transformer 上的实验证明了帕累托最优结果，与标准的单准则剪枝相比，Mix-and-Match 将 Swin-Tiny 的精度下降减少了 40%。这些研究结果表明，与引入新准则相比，协调现有的剪枝信号可以实现更可靠、更高效的压缩模型。

## Abstract
Deploying deep neural networks (DNNs) on edge devices requires strong compression with minimal accuracy loss. This paper introduces Mix-and-Match Pruning, a globally guided, layer-wise sparsification framework that leverages sensitivity scores and simple architectural rules to generate diverse, high-quality pruning configurations. The framework addresses a key limitation that different layers and architectures respond differently to pruning, making single-strategy approaches suboptimal. Mix-and-Match derives architecture-aware sparsity ranges, e.g., preserving normalization layers while pruning classifiers more aggressively, and systematically samples these ranges to produce ten strategies per sensitivity signal (magnitude, gradient, or their combination). This eliminates repeated pruning runs while offering deployment-ready accuracy-sparsity trade-offs. Experiments on CNNs and Vision Transformers demonstrate Pareto-optimal results, with Mix-and-Match reducing accuracy degradation on Swin-Tiny by 40% relative to standard single-criterion pruning. These findings show that coordinating existing pruning signals enables more reliable and efficient compressed models than introducing new criteria.

---

## 论文详细总结（自动生成）

以下是对论文《Mix-and-Match Pruning: Globally Guided Layer-Wise Sparsification of DNNs》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：深度神经网络（DNN）和视觉变换器（ViT）因参数量巨大（100MB至500MB+），难以部署在资源受限的边缘设备上。
*   **核心问题**：现有的剪枝方法通常针对固定的稀疏度目标生成单一模型，且往往忽略了不同网络层和架构组件对剪枝的敏感度差异。当部署约束改变时，需要重新运行整个剪枝流程，效率低下且难以系统性地探索“精度-压缩比”的最优权衡。
*   **整体含义**：本文提出了一种名为 **Mix-and-Match Pruning** 的统一框架，通过全局引导的逐层稀疏化策略，利用架构感知规则一次性生成多个具有不同权衡特性的剪枝模型。

### 2. 方法论
该框架分为三个阶段，旨在通过一次灵敏度分析生成 10 种不同的剪枝策略：
*   **阶段 1：灵敏度分析与范围分配**
    *   计算权重灵敏度得分（支持三种准则：权重绝对值、梯度、或两者乘积）。
    *   根据架构属性设定逐层稀疏度范围 $[\rho_{min}, \rho_{max}]$。例如：归一化层（BatchNorm/LayerNorm）不剪枝；参数量小于 10K 的小层限制在 0-10% 稀疏度；全连接层则允许更激进的剪枝。
*   **阶段 2：策略构建**
    *   在设定的范围内进行系统采样，生成 10 种策略（如：最大激进型、最小保守型、平衡型、参数比例型、以及针对分类器或特征提取器的特定偏置型）。
*   **阶段 3：剪枝执行与微调**
    *   根据选定的灵敏度得分对各层权重排序并生成掩码（Mask）。
    *   在保持掩码不变的情况下进行微调，恢复模型精度。

### 3. 实验设计
*   **数据集与场景**：
    *   **VGG-11**：使用 CIFAR-10 数据集。
    *   **ResNet-18**：使用 GTSRB（交通标志识别）数据集。
    *   **LeViT-384**：使用 CIFAR-10 数据集。
    *   **Swin-Tiny**：使用 CIFAR-100 数据集。
*   **对比基准（Benchmarks）**：
    *   经典方法：**MAG**（量级剪枝）、**SNIP**（单次连接灵敏度）、**GraSP**（梯度信号保留）。
    *   先进方法：**LPViT**（针对 ViT 的半结构化剪枝）、**GETA**（联合剪枝与量化）。
*   **实验公平性**：所有方法使用统一的微调协议（Adam/AdamW 优化器、相同的学习率和 Batch Size）。

### 4. 资源与算力
*   **硬件平台**：使用 **NVIDIA A100 GPU**。
*   **耗时示例**（以 VGG-11 为例）：
    *   阶段 1（灵敏度计算）：少于 2 分钟。
    *   阶段 3（单种策略的剪枝+微调）：约 8-10 分钟。
    *   生成全部 10 种策略总耗时约 85 分钟，比重复运行 10 次基准方法（约 100 分钟以上且需手动调参）更高效。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种代表性架构（传统卷积、残差网络、混合架构、层次化 Transformer），每种架构生成 10 种策略，并与 5 种主流基准方法对比。
*   **充分性评价**：实验设计较为充分，覆盖了从简单 CNN 到复杂 ViT 的多种模型，通过帕累托前沿（Pareto Front）展示了不同稀疏度下的表现。实验结果显示精度标准差极低（±0.07% 至 ±0.43%），证明了框架的稳健性。

### 6 主要结论与发现
*   **帕累托最优**：Mix-and-Match 在所有测试架构上均达到了帕累托最优或极具竞争力的结果。
*   **架构依赖性**：发现不同架构的最优灵敏度准则不同（VGG 适合“量级×梯度”，ResNet 和 Swin 适合“量级”，LeViT 适合“梯度”）。
*   **性能提升**：在 Swin-Tiny 上，该方法比标准单准则剪枝减少了 **40%** 的精度损失。
*   **压缩效果**：成功将 VGG-11 压缩至约 10MB，ResNet-18 压缩至约 4.5MB，且精度损失极小。

### 7. 优点
*   **高效性**：一次计算灵敏度即可产生多种部署方案，避免了重复的剪枝-微调循环。
*   **架构感知**：通过预设的架构规则（如保护归一化层）避免了盲目剪枝导致的精度崩溃。
*   **系统性探索**：提供了一种确定性的方法来覆盖“精度-稀疏度”权衡空间，无需繁琐的手动超参数搜索。

### 8. 不足与局限
*   **非结构化剪枝限制**：论文主要讨论非结构化剪枝，虽然减少了参数量和内存占用，但在不支持稀疏加速的普通硬件上，理论 FLOPs 并没有减少，实际推理速度提升有限。
*   **数据集规模**：实验主要集中在 CIFAR 和 GTSRB 等中小型数据集上，尚未在 ImageNet-1K 等大规模工业级数据集上验证其扩展性。
*   **ViT 压缩瓶颈**：对于 Swin-Tiny 等具有强层间依赖的架构，其最大稀疏度（约 50%）仍显著低于 CNN（约 90%），显示出层次化 Transformer 的剪枝难度。

（完）
