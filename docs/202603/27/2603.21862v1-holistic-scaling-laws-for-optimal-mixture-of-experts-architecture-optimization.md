---
title: Holistic Scaling Laws for Optimal Mixture-of-Experts Architecture Optimization
title_zh: 针对最优混合专家架构优化的全局缩放法则
authors: "Weilin Wan, Jingtao Han, Weizhong Zhang, Cheng Jin"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21862v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 连接缩放法则与设计的整体MoE架构优化
tldr: 本研究针对混合专家模型（MoE）架构设计的复杂性，提出了一个全局缩放法则框架。研究指出仅凭每令牌FLOPs不足以衡量MoE性能，进而建立了结合FLOPs、激活参数和总参数的三重约束。通过代数约束将16维搜索空间简化为两个低维阶段，并在广泛的计算规模下验证了该框架。该成果能为任意计算预算提供最优MoE架构配置，并揭示了随规模扩大架构选择的灵活性。
selection_source: fresh_fetch
motivation: 现有的MoE缩放研究因设计空间巨大且实验成本高昂，难以在全局范围内提供精确的架构优化指导。
method: 提出了一种结合FLOPs、激活参数和总参数的三重约束准则，并将16维架构搜索空间简化为两个低维优化阶段。
result: 通过对跨越六个数量级计算规模的数百个模型进行验证，成功构建了能映射任意计算预算至最优MoE架构的稳健缩放法则。
conclusion: 研究发现近优配置区间随规模扩大而拓宽，为开发者在遵循缩放法则与适应基础设施约束之间提供了量化的灵活性。
---

## 摘要
大语言模型的缩放法则主导着宏观资源分配，但由于设计空间存在巨大的组合可能性，将其转化为精确的混合专家（MoE）架构配置仍是一个悬而未决的问题。现有的 MoE 缩放研究受限于实验预算，要么在缩放公式中增加额外的 MoE 变量（存在拟合不可靠的风险），要么固定所有非 MoE 因素（忽略了全局交互作用）。我们提出了一个可复用的全局 MoE 架构优化框架来弥补这一差距。我们首先证明，仅凭每 token 的 FLOPs 并不是衡量 MoE 模型的充分公平指标，因为不同层类型的计算密度差异可能会在不增加成比例计算成本的情况下导致参数膨胀；为此，我们建立了由每 token FLOPs、激活参数量和总参数量组成的联合约束三元组。随后，我们通过代数约束和隐藏维度的秩保持特性，将 16 维架构搜索空间简化为两个连续的低维阶段。通过在跨越六个计算量级的数百个 MoE 模型上进行验证，我们的框架得出了稳健的缩放法则，能够将任何计算预算映射到完整且最优的 MoE 架构。一个关键发现是，近优配置带随着规模的扩大而变宽，这为从业者提供了量化的灵活性，以便在缩放法则建议与基础设施约束之间取得平衡。

## Abstract
Scaling laws for Large Language Models govern macroscopic resource allocation, yet translating them into precise Mixture-of-Experts (MoE) architectural configurations remains an open problem due to the combinatorially vast design space. Existing MoE scaling studies are constrained by experimental budgets to either augment scaling formulas with extra MoE variables, risking unreliable fits, or fix all non-MoE factors, ignoring global interactions. We propose a reusable framework for holistic MoE architectural optimization that bridges this gap. We first show that FLOPs per token alone is an inadequate fairness metric for MoE models because differing computational densities across layer types can inflate parameters without proportional compute cost, and establish a joint constraint triad of FLOPs per token, active parameters, and total parameters. We then reduce the 16-dimensional architectural search space to two sequential low-dimensional phases through algebraic constraints and a rank-preserving property of the hidden dimension. Validated across hundreds of MoE models spanning six orders of magnitude in compute, our framework yields robust scaling laws that map any compute budget to a complete, optimal MoE architecture. A key finding is that the near-optimal configuration band widens with scale, giving practitioners quantitative flexibility to balance scaling law recommendations against infrastructure constraints.

---

## 论文详细总结（自动生成）

### 针对最优混合专家架构优化的全局缩放法则（Holistic Scaling Laws）论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然大语言模型（LLM）的宏观缩放法则（Scaling Laws）已相对成熟，但混合专家模型（MoE）引入了专家数量、路由稀疏度和专家粒度等额外维度，导致架构设计空间呈指数级增长。
*   **核心问题**：现有的MoE研究通常要么只关注局部MoE参数而固定其他架构因素，要么直接在公式中堆叠变量导致拟合困难。论文旨在解决如何在全球架构视角下，系统性地确定MoE模型的最优配置（如深度、宽度、专家比例等），并建立一套可预测、高效率的优化框架。
*   **背景**：传统的“每令牌FLOPs（$M$）”指标在MoE中存在局限性，因为MoE可以通过调整Attention与FFN的比例在保持$M$不变的情况下膨胀参数量，从而产生性能虚高的偏差。

#### 2. 论文提出的方法论
*   **核心思想**：通过数学解耦和维度压缩，将原本不可行的16维架构搜索问题转化为可操作的低维优化过程。
*   **关键技术细节**：
    *   **联合约束三元组**：提出必须同时约束“每令牌FLOPs（$M$）”、“激活参数量（$N_a$）”和“总参数量（$N$）”才能实现公平的架构对比。
    *   **维度压缩（16D $\rightarrow$ 4D）**：利用Chinchilla法则确定最优训练数据量，并结合硬件/算子约束（如固定序列长度、Head维度等），将搜索空间压缩至四个自由度：$M, N_a, N$ 和隐藏维度 $d$。
    *   **两阶段搜索策略**：
        1.  **阶段一（$d$-Proxy）**：利用隐藏维度 $d$ 的“秩保持特性”（即不同架构配置的相对优劣在不同 $d$ 下保持稳定），使用 $d$ 的中值作为代理，搜索最优的 $(M, N_a, N)$ 组合。
        2.  **阶段二（$d$ 优化）**：在确定宏观参数后，针对性地寻找最优的隐藏维度 $d$。
    *   **坐标变换**：引入“激活计算密度（$M/N_a$）”和“参数扩张率（$N/N_a$）”两个尺度无关的比率，将不规则的搜索空间转化为矩形网格。

#### 3. 实验设计
*   **计算规模**：涵盖了从 $10^{18}$ 到 $3 \times 10^{20}$ FLOPs 的六个计算量级。
*   **数据集**：使用高质量的过滤文本语料库，包含通用文本、代码、数学和多语言内容。
*   **Benchmark与对比**：
    *   基准模型采用类似 DeepSeek-V3 的架构（1个共享专家，288个路由专家，激活8个）。
    *   对比了数百种不同的架构配置（改变层数、宽度、专家维度等）。
    *   通过拟合幂律公式（Power-law）来验证预测值与实际观测值的一致性。

#### 4. 资源与算力
*   **算力规模**：论文明确给出了实验的计算预算范围（最高至 $3 \times 10^{20}$ FLOPs），但**未明确说明具体的 GPU 型号（如 H100 或 A100）及具体的 GPU 数量和总训练时长**。
*   **框架支持**：实验基于 Megatron-LM 分布式训练框架完成。

#### 5. 实验数量与充分性
*   **实验规模**：总计训练了**超过 670 个模型**。
    *   在 $(M, N_a, N)$ 搜索阶段，每个计算规模采样了 36 个点（共 216 个初始配置）。
    *   在 $d$ 优化和 $M/N_a$ 细化阶段又进行了数百次补充实验。
*   **充分性与客观性**：实验覆盖了多个数量级的计算规模，样本量巨大且系统化。通过引入“近优带（Near-optimal band）”的概念，客观地展示了性能对架构偏差的敏感度，而非仅仅给出一个孤立的最优点。

#### 6. 论文的主要结论与发现
*   **最优分配法则**：推导出了关于最优 $M/N_a$ 和最优隐藏维度 $d$ 随计算预算 $C$ 变化的精确幂律公式。
*   **近优带拓宽效应**：**关键发现**是随着计算规模的增大，性能接近最优的架构配置区间（Near-optimal band）会变宽。这意味着模型越大，对架构微小偏差（如为了硬件对齐而调整维度）的容忍度越高。
*   **参数扩张率稳定性**：发现 $N/N_a$ 在 18 到 25 之间时，模型性能表现出极高的平坦度，对计算规模不敏感。
*   **工程指导**：提供了一套闭环流程，开发者只需输入计算预算 $C$，即可直接计算出最优的层数、宽度和专家配置。

#### 7. 优点
*   **系统性强**：首次将 MoE 的局部参数与全局架构参数（如 Attention 比例）结合起来研究缩放法则。
*   **工程实用价值高**：提出的“两阶段搜索”和“近优带”理论为实际部署中的硬件对齐（如将维度设为 8 或 128 的倍数）提供了量化的性能损失评估。
*   **方法论可复用**：解耦框架不仅适用于研究 $d$，也证明了可以扩展到研究 FFN 宽度比等其他结构变量。

#### 8. 不足与局限
*   **稀疏配置固定**：实验中固定了专家总数（288）和激活数（8），虽然这是基于未来大规模预测的考量，但结论可能无法直接外推到极低稀疏度或极高稀疏度的场景。
*   **评价指标单一**：主要基于预训练的交叉熵损失（Loss），未深入探讨架构优化对下游任务（如推理、数学、代码）的具体能力分布影响。
*   **迭代闭环缺失**：在拟合 $(C, M, D)$ 关系时使用了启发式架构，理想情况下应使用优化后的架构重新拟合，但受限于算力未进行此类迭代。

（完）
