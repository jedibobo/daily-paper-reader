---
title: "EdgeDiT: Hardware-Aware Diffusion Transformers for Efficient On-Device Image Generation"
title_zh: EdgeDiT：用于高效设备端图像生成的硬件感知扩散 Transformer
authors: "Sravanth Kodavanti, Manjunath Arveti, Sowmya Vajrala, Srinivas Miriyala, Vikram N R"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28405v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 针对移动NPU的硬件感知优化，并针对移动数据流剪枝结构冗余
tldr: 针对扩散变换器（DiT）在边缘设备上计算量大、内存占用高的问题，本文提出了EdgeDiT。这是一种专为移动端NPU（如高通Hexagon和苹果ANE）设计的硬件感知生成式Transformer系列模型。通过硬件感知优化框架识别并修剪冗余结构，EdgeDiT在保持生成质量的同时，显著降低了参数量、计算量和延迟，为在移动设备上实现高效、私密的生成式AI提供了可扩展的方案。
selection_source: fresh_fetch
motivation: 现有的扩散变换器（DiT）模型由于巨大的计算复杂度和内存需求，难以在资源受限的移动边缘设备上本地部署。
method: 采用硬件感知优化框架，系统地识别并修剪DiT骨干网络中对移动端数据流负担较重的结构冗余。
result: "EdgeDiT在减少20-30%参数和36-46%计算量的基础上，实现了1.65倍的端侧加速，且在FID与延迟的权衡上优于现有模型。"
conclusion: EdgeDiT证明了通过硬件感知设计，可以将大规模基础生成模型高效地迁移至移动端，实现响应迅速且私密的本地AI体验。
---

## 摘要
扩散 Transformer (DiT) 在高保真图像合成领域树立了新的最先进水平；然而，其巨大的计算复杂度和内存需求阻碍了在资源受限的边缘设备上的本地部署。在本文中，我们介绍了 EdgeDiT，这是一系列专门为移动神经网络处理器（NPU，如高通 Hexagon 和苹果神经网络引擎 ANE）设计的硬件高效生成式 Transformer。通过利用硬件感知优化框架，我们系统地识别并剪枝了 DiT 骨干网络中对移动数据流负担特别重的结构冗余。我们的方法产生了一系列轻量级模型，在不牺牲原始 Transformer 架构的缩放优势或表达能力的情况下，实现了参数量减少 20-30%、FLOPs 降低 36-46% 以及设备端延迟降低 1.65 倍。广泛的基准测试表明，与优化的移动端 U-Net 和原生 DiT 变体相比，EdgeDiT 在 Frechet Inception Distance (FID) 和推理延迟之间提供了更优的帕累托最优（Pareto-optimal）权衡。通过直接在设备端实现响应迅速、私密且离线的生成式 AI，EdgeDiT 为将大规模基础模型从高端 GPU 迁移到用户掌中提供了一个可扩展的蓝图。

## Abstract
Diffusion Transformers (DiT) have established a new state-of-the-art in high-fidelity image synthesis; however, their massive computational complexity and memory requirements hinder local deployment on resource-constrained edge devices. In this paper, we introduce EdgeDiT, a family of hardware-efficient generative transformers specifically engineered for mobile Neural Processing Units (NPUs), such as the Qualcomm Hexagon and Apple Neural Engine (ANE). By leveraging a hardware-aware optimization framework, we systematically identify and prune structural redundancies within the DiT backbone that are particularly taxing for mobile data-flows. Our approach yields a series of lightweight models that achieve a 20-30% reduction in parameters, a 36-46% decrease in FLOPs, and a 1.65-fold reduction in on-device latency without sacrificing the scaling advantages or the expressive capacity of the original transformer architecture. Extensive benchmarking demonstrates that EdgeDiT offers a superior Pareto-optimal trade-off between Frechet Inception Distance (FID) and inference latency compared to both optimized mobile U-Nets and vanilla DiT variants. By enabling responsive, private, and offline generative AI directly on-device, EdgeDiT provides a scalable blueprint for transitioning large-scale foundation models from high-end GPUs to the palm of the user.

---

## 论文详细总结（自动生成）

以下是对论文《EdgeDiT: Hardware-Aware Diffusion Transformers for Efficient On-Device Image Generation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散变换器（DiT）虽然在图像合成质量上达到了新高度，但其巨大的计算量、内存需求和高推理延迟，使其难以在资源受限的移动端设备（如手机）上本地运行。
*   **研究动机**：云端推理存在隐私泄露、网络依赖和高能耗问题。为了实现响应迅速、私密且离线的生成式 AI，需要针对移动端神经处理单元（NPU，如高通 Hexagon 和苹果 ANE）的硬件特性，对 DiT 架构进行深度优化和剪枝。

### 2. 方法论
EdgeDiT 采用了一种硬件感知的优化框架，主要包含以下四个关键步骤：
*   **硬件感知架构手术（Hardware-aware Surgery）**：
    *   **块移除（Block removal）**：尝试将每两个连续的 DiT 层替换为一个层，以减少深度。
    *   **MLP 比例修改**：调整前馈网络（FFN）的扩展比例（从 4 降至 2）。
    *   **隐藏维度缩减**：将内部投影维度从 1152 降低到 512，构建低秩表示。
*   **特征级知识蒸馏（FwKD）**：采用“分而治之”策略。让轻量级的“代理块”（Surrogate blocks）通过最小化 MSE 损失来模仿原始 DiT-XL/2 对应块的输出特征。这种方法高度并行且计算开销低。
*   **多目标贝叶斯优化（MOBO）**：在巨大的搜索空间（$2^{14} + 4^{28}$）中，利用 MOBO 寻找生成质量（FID）与硬件效率（延迟）之间的帕累托最优（Pareto-optimal）架构。
*   **端到端训练**：选定最优候选模型（如 EdgeDiT-1 和 EdgeDiT-6）后，进行最终的联合优化训练，以完全适配扩散任务。

### 3. 实验设计
*   **数据集**：ImageNet 256×256 类别条件图像生成。
*   **基准模型（Benchmark）**：以原始 DiT-XL/2 为主要基准，同时对比了 DiT 家族的其他变体（S, B, L）。
*   **对比维度**：参数量（Params）、计算量（GFLOPs/GMACs）、生成质量（FID、IS、Precision、Recall）以及在真实设备上的推理延迟。
*   **测试硬件**：
    *   Apple A18 Pro ANE (iPhone 16 Pro Max)
    *   Qualcomm 8850 NPU (Samsung Galaxy S25 Ultra)

### 4. 资源与算力
*   **硬件设备**：使用了 **4 台 NVIDIA A100 GPU** 进行训练。
*   **训练时长**：
    *   教师模型使用 DiT-XL/2 的 400K 迭代检查点。
    *   选定的 EdgeDiT 模型在蒸馏后又进行了 **100K 次迭代** 的端到端微调。
*   **算力评价**：相比从头训练一个大型 DiT，该方法通过局部蒸馏显著降低了搜索和训练的算力成本。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了敏感性分析，验证了“移除 2 层”优于“移除 3 层”，以及 MLP 比例和隐藏维度的取值合理性。
    *   进行了帕累托分析，展示了多个候选模型在延迟与质量之间的权衡。
    *   在两款主流旗舰移动芯片上进行了实测。
*   **充分性评价**：实验设计较为充分，涵盖了从理论指标（FLOPs）到实际性能（设备延迟）的全面评估。消融实验验证了特征级知识蒸馏（FwKD）对保持图像质量的关键作用。

### 6. 主要结论与发现
*   **性能提升**：EdgeDiT 相比 DiT-XL/2 减少了 **20-30% 的参数**，降低了 **36-46% 的 FLOPs**。
*   **延迟优化**：在三星 S25 Ultra 上实现了 **1.65 倍** 的加速，在 iPhone 16 Pro Max 上实现了 **1.45 倍** 的加速。
*   **质量保持**：在参数大幅减少的情况下，EdgeDiT 的 FID 分数甚至优于仅训练了 400K 次的基准 DiT-XL/2，证明了硬件感知架构搜索的有效性。

### 7. 优点
*   **硬件针对性强**：不仅关注理论计算量，更直接针对移动端 NPU 的 GEMM 优化特性进行架构调整。
*   **搜索效率高**：通过特征级蒸馏和贝叶斯优化，避免了对数以亿计的候选架构进行全量训练，极大地节省了资源。
*   **实用性高**：为大模型从云端走向终端提供了一套可落地的工程化方案。

### 8. 不足与局限
*   **教师模型单一**：目前主要基于 DiT-XL/2 进行优化，尚未在 SiT 或 MDT 等更新的变体上广泛验证。
*   **任务覆盖有限**：实验主要集中在类别条件的图像生成，未深入探讨文生图（Text-to-Image）等更复杂的下游任务。
*   **硬件适配范围**：虽然覆盖了苹果和高通平台，但对于其他边缘设备（如 AR/VR 头显、中低端手机芯片）的适配表现尚不明确。

（完）
