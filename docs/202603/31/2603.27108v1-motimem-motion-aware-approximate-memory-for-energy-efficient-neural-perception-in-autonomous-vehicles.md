---
title: "MotiMem: Motion-Aware Approximate Memory for Energy-Efficient Neural Perception in Autonomous Vehicles"
title_zh: MotiMem：面向自动驾驶车辆高效能神经感知的运动感知近似内存
authors: "Haohua Que, Mingkai Liu, Jiayue Xie, Haojia Gao, Jiajun Sun, Hongyi Xu, Handong Yao, Fei Qiao"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27108v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 软硬件协同设计中的内存带宽优化
tldr: "针对自动驾驶中高分辨率传感器带来的内存墙和高能耗问题，本文提出MotiMem。这是一种软硬协同设计的近似内存接口，利用时间相干性通过2D运动传播识别感兴趣区域（RoI），并结合混合稀疏感知编码减少总线翻转。实验表明，该方法在保持约93%检测精度的同时，降低了43%的内存接口动态能耗，优于传统压缩算法。"
selection_source: fresh_fetch
motivation: 自动驾驶中数据传输能耗远超计算能耗，且传统图像压缩算法缺乏语义感知，无法有效优化内存总线活动。
method: 提出MotiMem接口，利用轻量级2D运动传播动态识别RoI，并采用混合稀疏感知编码（自适应反转和截断）来诱导位级稀疏性。
result: "在多个主流数据集和16个检测模型上，MotiMem将内存接口动态能耗降低了约43%，同时保留了约93%的检测精度。"
conclusion: MotiMem通过结合运动感知和稀疏编码，在自动驾驶感知的能效与精度之间建立了更优的帕累托前沿。
---

## 摘要
高分辨率传感器对于鲁棒的自动驾驶感知至关重要，但给电池受限的电动汽车带来了严重的“内存墙”挑战。在这些系统中，数据传输能耗通常超过了计算能耗。传统的图像压缩并不适用，因为它们缺乏语义感知，且优化目标是存储空间而非总线切换活动。我们提出了 MotiMem，一种软硬件协同设计的接口。利用时间相干性，MotiMem 使用轻量级二维运动传播（2D Motion Propagation）来动态识别感兴趣区域（RoI）。与之相辅相成的是，一种混合稀疏感知编码方案利用自适应反转和截断来诱导位级稀疏性。在 nuScenes、Waymo 和 KITTI 数据集上针对 16 种检测模型进行的广泛实验表明，MotiMem 在保留约 93% 的目标检测准确率的同时，将内存接口动态能耗降低了约 43%，建立了一个显著优于 JPEG 和 WebP 等标准编解码器的新帕累托前沿（Pareto frontier）。

## Abstract
High-resolution sensors are critical for robust autonomous perception but impose a severe memory wall on battery-constrained electric vehicles. In these systems, data movement energy often outweighs computation. Traditional image compression is ill-suited as it is semantically blind and optimizes for storage rather than bus switching activity. We propose MotiMem, a hardware-software co-designed interface. Exploiting temporal coherence,MotiMem uses lightweight 2D Motion Propagation to dynamically identify Regions of Interest (RoI). Complementing this, a Hybrid Sparsity-Aware Coding scheme leverages adaptive inversion and truncation to induce bitlevel sparsity. Extensive experiments across nuScenes, Waymo, and KITTI with 16 detection models demonstrate that MotiMem reduces memory-interface dynamic energy by approximately 43 percent while retaining approximately 93 percent of the object detection accuracy, establishing a new Pareto frontier significantly superior to standard codecs like JPEG and WebP.

---

## 论文详细总结（自动生成）

以下是对论文《MotiMem: Motion-Aware Approximate Memory for Energy-Efficient Neural Perception in Autonomous Vehicles》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：自动驾驶车辆（AV）采用高分辨率传感器（4K/8K）导致了严重的“内存墙”问题。在电池受限的嵌入式平台上，**数据传输（内存访问）的能耗往往超过了计算本身的能耗**。
*   **研究动机**：
    *   **传统压缩算法的局限**：JPEG、WebP 等标准编解码器是“语义盲”的，对背景（天空、路面）和关键目标（行人、车辆）一视同仁，浪费了带宽。
    *   **硬件不友好**：传统压缩优化的是存储体积，而非位级翻转活动（Switching Activity）。高熵的压缩流会导致总线频繁切换，从而产生巨大的动态功耗。
    *   **实时性要求**：复杂的神经编解码器在边缘设备上延迟过高，不适合自动驾驶的实时控制环路。

### 2. 方法论
MotiMem 是一种软硬件协同设计的神经接口，核心思想是**利用时间相干性进行非均匀精度的存储编码**。
*   **时间相干性与 RoI 预测**：
    *   利用前一帧（$T-1$）的检测结果，通过轻量级的**二维运动传播（2D Motion Propagation）**预测当前帧（$T$）的感兴趣区域（RoI）。
    *   使用等速模型预测边界框位置，并进行适度扩张（Inflation）以容忍运动不确定性，生成块粒度的二值掩码 $M_t$。
*   **混合稀疏感知编码（Hybrid Sparsity-Aware Coding）**：
    *   **RoI 路径（高保真）**：保留高位信息，根据汉明权重决定是否对前 $k$ 个最高有效位（MSB）进行自适应反转，并将反转标志位嵌入到最低有效位（LSB）中。这是一种近乎无损的方案（最大误差仅为 1 LSB）。
    *   **背景路径（强压缩）**：仅保留前 $k$ 个 MSB，截断低位，同样进行自适应反转并嵌入标志位。
*   **位级稀疏诱导**：通过截断和反转操作，强制使存储流中的“1”和位翻转减少，从而直接降低内存总线和 RAM 阵列的动态功耗。

### 3. 实验设计
*   **数据集**：使用了三个主流自动驾驶数据集：**nuScenes**、**Waymo** 和 **KITTI**。
*   **Benchmark 模型**：涵盖了 **16 种检测模型**，包括 YOLO 全系列（v5, v8, v9, v10, v11, YOLO26）以及基于 Transformer 的 RT-DETR。
*   **对比方法**：
    *   **标准编解码器**：JPEG (Q10/50/100), WebP (Q10/50/100), 无损 JPEG2000。
    *   **消融实验基准**：Global $k=4$（无 RoI 引导的全局截断）、Uniform 4-bit（无自适应反转的均匀量化）。
    *   **原始基准**：未压缩的 8-bit 原始图像。
*   **评价指标**：感知准确率（mAP@50-95）和能效指标（归一化 Bit-1 密度，作为动态能耗的线性代理指标）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Core Ultra 9 285K CPU**、128GB RAM 和 **NVIDIA RTX 6000 Pro GPU** 的工作站上完成。
*   **实现方式**：MotiMem 的编码逻辑和检测流水线在 **PyTorch** 中进行了功能性仿真。
*   **算力说明**：文中未详细列出每个模型的训练时长，但强调了 MotiMem 逻辑极其轻量（仅涉及位运算），旨在硬件接口层实现，而非依赖重型 GPU 计算。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个数据集上对 16 个模型进行了交叉验证，涵盖了从轻量级（YOLOv5n）到重型（RT-DETR-x）的各种架构，实验组数非常充分。
*   **客观性**：
    *   使用了实际检测结果（而非 Ground Truth）进行闭环 RoI 预测，真实模拟了部署环境。
    *   通过参数 $k$ 的敏感度分析（$k=1$ 到 $7$）确定了帕累托最优解（$k=4$）。
    *   对比了多种工业标准算法，证明了其在“精度-能耗”权衡上的优越性。

### 6. 主要结论与发现
*   **能效显著**：MotiMem 将内存接口的动态能耗代理指标降低了约 **43%**。
*   **精度保持**：在大幅节能的同时，保留了约 **93%** 的原始检测精度（mAP）。
*   **帕累托前沿**：MotiMem 建立了一个全新的帕累托前沿，显著优于 JPEG 和 WebP。例如，JPEG Q100 虽然精度高，但由于高熵编码，其能耗甚至超过了原始未压缩数据。
*   **语义价值**：实验证明，对于神经感知任务，背景区域的保真度往往是“能耗浪费”，优先保护 RoI 的位密度比追求全局 PSNR 更有效。

### 7. 优点
*   **软硬协同创新**：不只是简单的图像压缩，而是将上层语义（运动预测）与底层电路特性（位翻转功耗）深度结合。
*   **极低开销**：RoI 预测基于已有的检测结果，编码过程仅涉及位移、异或等基础位运算，硬件实现成本极低。
*   **通用性强**：在 16 种不同架构的模型上均表现出一致的鲁棒性，证明了该接口的普适性。

### 8. 不足与局限
*   **能耗评估偏差**：目前的 43% 节能是基于位密度和翻转率的理论代理指标计算的，尚未在真实的物理硅片（Silicon）上进行实测功耗验证。
*   **冷启动问题**：系统依赖前一帧的反馈。在“冷启动”或物体突然闯入画面时，RoI 预测可能失效，需要全局高精度模式作为回退方案。
*   **2D 局限性**：目前仅基于 2D 图像空间进行运动传播，未利用 3D 空间信息（如 LiDAR），在复杂遮挡场景下的 RoI 预测准确性仍有提升空间。

（完）
