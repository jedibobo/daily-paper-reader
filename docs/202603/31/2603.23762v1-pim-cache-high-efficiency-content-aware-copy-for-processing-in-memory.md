---
title: "PIM-CACHE: High-Efficiency Content-Aware Copy for Processing-In-Memory"
title_zh: PIM-CACHE：面向存内计算的高效内容感知复制
authors: "Peterson Yuhala, Mpoki Mwaisela, Pascal Felber, Valerio Schiavoni"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23762v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对存内计算架构的软硬件协同设计
tldr: PIM-CACHE 是一种针对存内计算（PIM）架构设计的轻量级数据暂存层，旨在解决主机与 DPU 之间强制性粗粒度数据传输带来的性能瓶颈。该方案通过利用工作负载的相似性，实现内容感知复制（CAC），动态消除冗余数据传输。在合成负载和真实基因组数据集上的评估表明，它能显著降低 PIM 的数据传输开销，提升系统整体效率。
selection_source: fresh_fetch
motivation: 尽管 PIM 减少了处理器与内存间的瓶颈，但主机 DRAM 与 DPU 之间强制性的粗粒度数据传输成为了新的性能瓶颈。
method: 提出 PIM-CACHE，一个利用内容感知复制（CAC）技术来动态识别并消除冗余数据传输的轻量级数据暂存层。
result: 在合成工作负载和真实基因组数据集上的评估结果显示，该方法有效减少了 PIM 架构中的数据传输开销。
conclusion: PIM-CACHE 通过减少冗余通信，为优化 PIM 架构中的数据移动提供了一种高效且实用的解决方案。
---

## 摘要
存内计算 (PIM) 架构使计算更接近数据，减少了传统以处理器为中心的设计中处理器与内存之间的传输瓶颈。新型硬件解决方案（如 UPMEM 的存内处理技术）通过将低功耗 DRAM 处理单元 (DPU) 集成到内存 DIMM 中来实现这一点，从而实现了大规模并行化并提高了内存带宽。然而，矛盾的是，这些 PIM 架构在主机 DRAM 和 DPU 之间引入了强制性的粗粒度数据传输，这往往成为新的瓶颈。我们提出了 PIM-CACHE，这是一个轻量级的数据暂存层，它通过利用工作负载的相似性来动态消除到 PIM DPU 的冗余数据传输，从而实现内容感知复制 (CAC)。我们在合成工作负载和真实基因组数据集上评估了 PIM-CACHE，证明了其在减少 PIM 数据传输开销方面的有效性。

## Abstract
Processing-in-memory (PIM) architectures bring computation closer to data, reducing the processor-memory transfer bottleneck in traditional processor-centric designs. Novel hardware solutions, such as UPMEM's in-memory processing technology, achieve this by integrating low-power DRAM processing units (DPUs) into memory DIMMs, enabling massive parallelism and improved memory bandwidth. However, paradoxically, these PIM architectures introduce mandatory coarse-grained data transfers between host DRAM and DPUs, which often become the new bottleneck. We present PIM-CACHE, a lightweight data staging layer that dynamically eliminates redundant data transfers to PIM DPUs by exploiting workload similarity, achieving content-aware copy (CAC). We evaluate PIM-CACHE on both synthetic workloads and real-world genome datasets, demonstrating its effectiveness in reducing PIM data transfer overhead.