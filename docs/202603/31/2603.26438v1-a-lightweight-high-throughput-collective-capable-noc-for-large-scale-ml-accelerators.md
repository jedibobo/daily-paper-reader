---
title: A Lightweight High-Throughput Collective-Capable NoC for Large-Scale ML Accelerators
title_zh: 一种面向大规模机器学习加速器的轻量级、高吞吐量、支持集合通信的片上网络
authors: "Luca Colagrande, Lorenzo Leone, Chen Wu, Tim Fischer, Raphael Roth, Luca Benini"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26438v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 为机器学习加速器协同设计的片上网络以提高吞吐量
tldr: "针对大规模机器学习加速器中日益增长的片上集体通信需求，本文提出了一种轻量级、高吞吐量的集体通信片上网络（NoC）。通过引入“直接计算访问（DCA）”范式，使互连架构能直接利用核心计算资源，实现了高效的硬件级多播、归约和同步操作。该设计在仅增加16.5%面积开销的情况下，显著提升了GEMM等典型负载的性能与能效，为下一代加速器提供了可扩展的通信方案。"
selection_source: fresh_fetch
motivation: 随着机器学习模型规模扩大，单芯片集成数千计算单元，传统的片上网络难以高效处理日益关键的集体通信任务。
method: 提出“直接计算访问（DCA）”范式，允许NoC直接访问核心计算资源以实现高效的网内归约，并支持硬件级多播和屏障同步。
result: "在16.5%的路由器面积开销下，多播和归约操作分别实现2.9倍和2.5倍加速，GEMM负载性能提升高达3.8倍，能效提升1.17倍。"
conclusion: 该轻量级集体通信NoC通过软硬件协同设计，为下一代大规模机器学习加速器提供了高效、可扩展且低开销的通信解决方案。
---

## 摘要
机器学习（ML）模型规模和复杂性的指数级增长，推动了对高性能加速系统前所未有的需求。随着工艺缩放使得在单个芯片上集成数千个计算单元成为可能，分布式系统与片上系统之间的界限已变得模糊，这使得高效的片上集合通信变得日益关键。在本研究中，我们提出了一种轻量级、支持集合通信的片上网络（NoC），它支持高效的屏障同步以及可扩展、高带宽的多播和归约操作，是专为下一代机器学习加速器协同设计的。我们引入了直接计算访问（DCA），这是一种新颖的范式，允许互连架构直接访问核心的计算资源，从而以仅 16.5% 的路由器面积开销实现高吞吐量的网内归约。通过网内硬件加速，我们在处理 1 到 32 KiB 数据的多播和归约操作上，分别实现了 2.9 倍和 2.5 倍的几何平均加速。此外，通过在通用矩阵乘法（GEMM）工作负载中使通信脱离关键路径，这些特性使我们的架构能够高效扩展到大型网格，与基准单播 NoC 架构相比，通过多播和归约支持分别实现了高达 3.8 倍和 2.4 倍的预估性能提升，以及高达 1.17 倍的预估节能。

## Abstract
The exponential increase in Machine Learning (ML) model size and complexity has driven unprecedented demand for high-performance acceleration systems. As technology scaling enables the integration of thousands of computing elements onto a single die, the boundary between distributed and on-chip systems has blurred, making efficient on-chip collective communication increasingly critical. In this work, we present a lightweight, collective-capable Network on Chip (NoC) that supports efficient barrier synchronization alongside scalable, high-bandwidth multicast and reduction operations, co-designed for the next generation of ML accelerators. We introduce Direct Compute Access (DCA), a novel paradigm that grants the interconnect fabric direct access to the cores' computational resources, enabling high-throughput in-network reductions with a small 16.5% router area overhead. Through in-network hardware acceleration, we achieve 2.9x and 2.5x geomean speedups on multicast and reduction operations involving between 1 and 32 KiB of data, respectively. Furthermore, by keeping communication off the critical path in GEMM workloads, these features allow our architecture to scale efficiently to large meshes, resulting in up to 3.8x and 2.4x estimated performance gains through multicast and reduction support, respectively, compared to a baseline unicast NoC architecture, and up to 1.17x estimated energy savings.

---

## 论文详细总结（自动生成）

这篇论文由苏黎世联邦理工学院（ETH Zurich）的研究团队发表，提出了一种专为大规模机器学习（ML）加速器设计的轻量级、高吞吐量集合通信片上网络（NoC）。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着机器学习模型规模呈指数级增长，单芯片集成的处理单元（PE）已达数千个。然而，计算能力的提升远超内存和通信带宽的增长（即“通信墙”问题）。
*   **背景**：在多核加速器中，集合通信（如多播、归约、屏障同步）对数据交换至关重要。传统的单播（Unicast）NoC在处理这些操作时会迅速饱和带宽，导致计算单元利用率低下。
*   **研究意义**：论文旨在打破分布式系统与片上系统之间的界限，通过软硬件协同设计，在极低面积开销下实现高效的片上集合通信，提升大规模并行计算的扩展性。

### 2. 方法论：核心思想与关键技术
*   **直接计算访问（DCA, Direct Compute Access）**：这是本文最核心的创新。它允许NoC互连架构直接借用计算核心（如Snitch核心）的浮点运算单元（FPU）来执行网内归约（In-network Reduction）。这种“资源共享”避免了在路由器中重复构建昂贵的算术逻辑单元。
*   **多地址编码方案**：采用“基地址+掩码”的编码方式。通过掩码位（Don't care）在单次事务中表示 $2^n$ 个目的地，实现了对数级增长的可扩展性，且与目的地数量无关。
*   **硬件扩展设计**：
    *   **多播路由器扩展**：在输入端口增加 `xy_route_fork` 逻辑，根据掩码将数据包分发至多个输出方向。
    *   **并行归约扩展**：在输出仲裁器中集成同步模块，支持轻量级操作（如屏障同步中的位逻辑归约）。
    *   **宽位归约扩展**：针对高带宽数据流，通过DCA接口将两个输入操作数发送至外部FPU处理，支持流水线化操作。
*   **系统集成**：将该NoC集成到基于Snitch的多核SoC中，扩展了DMA引擎和加载存储单元（LSU）以支持集合操作码。

### 3. 实验设计
*   **实验场景**：
    *   **微基准测试**：评估屏障同步（Barrier）、1D/2D多播（Multicast）和归约（Reduction）的延迟与吞吐量。
    *   **应用级评估**：针对GEMM（通用矩阵乘法）内核，对比了SUMMA算法和FusedConcatLinear数据流。
*   **Benchmark（基准方法）**：
    *   **Baseline NoC**：高性能单播NoC（FlooNoC）。
    *   **软件优化实现**：包括朴素顺序（Naive）、流水线顺序（Pipelined）和树状（Tree-based）软件集合通信算法。
*   **技术栈**：使用 TSMC 7nm 工艺进行物理设计，通过 QuestaSim 进行周期精确的 RTL 仿真。

### 4. 资源与算力
*   **算力说明**：论文主要关注硬件架构设计与仿真，未涉及大规模深度学习模型的实际训练。
*   **仿真环境**：使用了 Synopsys Fusion Compiler 进行综合与布局布线，PrimeTime 进行功耗评估。
*   **硬件规格**：目标频率 1GHz，计算簇包含 8 个 Snitch 核心，每个核心配有 64-bit SIMD FPU。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 1 KiB 到 32 KiB 的不同数据传输规模。
    *   通过分析模型将实验结果扩展到了高达 256x256（65,536个核心）的超大规模网格。
*   **充分性评价**：实验设计非常充分。不仅提供了周期精确的 RTL 仿真数据，还建立了严谨的数学模型（如 $T = \alpha + n\beta$）来解释软件与硬件实现之间的性能差距。同时，进行了面积、时序和功耗的全面物理分析，确保了结果的客观性。

### 6. 主要结论与发现
*   **性能提升**：在 4x4 网格中，硬件加速的多播和归约操作比最优软件实现分别快 **2.9倍** 和 **2.5倍**。
*   **应用加速**：在 256x256 的大规模网格上，硬件多播支持使 GEMM 性能提升高达 **3.8倍**。
*   **能效表现**：通过减少 DMA 操作和允许核心在通信期间进入低功耗状态，实现了高达 **1.17倍** 的系统级节能。
*   **极低开销**：集合通信扩展仅增加了路由器 **16.5%** 的面积，而在整个计算单元（Tile）层面的面积增加 **不足 1%**，且不影响时序。

### 7. 优点
*   **DCA范式的创新性**：巧妙地解决了网内计算（INC）面积开销大的痛点，通过复用现有计算资源实现了高吞吐归约。
*   **高度可扩展**：多地址编码和树状分发机制确保了在大规模 Mesh 拓扑下的高效性。
*   **软硬件协同**：不仅提供硬件，还提供了优化的软件基准和详细的性能模型，具有很强的工程参考价值。
*   **开源贡献**：该设计完全开源，有助于学术界和工业界进一步研究。

### 8. 不足与局限
*   **拓扑限制**：目前的设计高度依赖于规则的 2D Mesh 拓扑，对于非规则或高维拓扑（如 Torus, Dragonfly）的适用性未讨论。
*   **归约输入限制**：宽位归约在每个路由器仅支持 2 个输入，在处理 2D 归约时（如 3 个方向输入）会导致吞吐量减半。
*   **地址映射约束**：为了简化硬件逻辑，对集合通信区域的对齐和大小有较严格的幂次限制（Power-of-two）。
*   **应用覆盖**：虽然 GEMM 很典型，但对于稀疏计算或非线性算子的集合通信需求探讨较少。

（完）
