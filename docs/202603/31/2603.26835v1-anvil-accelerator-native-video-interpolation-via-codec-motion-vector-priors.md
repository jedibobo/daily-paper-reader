---
title: "ANVIL: Accelerator-Native Video Interpolation via Codec Motion Vector Priors"
title_zh: ANVIL：基于编解码器运动矢量先验的加速器原生视频插帧
authors: Shibo Liu
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26835v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 解决结构化部署障碍的加速器原生设计
tldr: 针对移动端高刷新率显示器对实时视频插帧的需求，本文提出 ANVIL。传统方法在移动加速器上存在采样算子慢、量化精度损失大及内存受限等问题。ANVIL 通过复用 H.264 解码器的运动矢量进行预对齐，消除了复杂的流估计和空间采样，构建了一个以卷积为主的轻量化网络。在 Snapdragon 8 Gen 3 上实现了 1080p 实时插帧，显著提升了移动端视频播放的流畅度。
selection_source: fresh_fetch
motivation: 传统基于光流的视频插帧算法在移动端加速器上存在算子不支持、量化失效及内存带宽受限等部署障碍。
method: 复用 H.264 解码器已有的运动矢量进行帧预对齐，并采用纯卷积构成的残差细化网络替代复杂的迭代流估计。
result: 在 Snapdragon 8 Gen 3 上实现了 12.8ms 的 1080p 8位量化推理，端到端中值延迟仅为 28.4ms。
conclusion: ANVIL 证明了利用编解码器先验信息可以有效解决移动端实时视频插帧的硬件部署难题。
---

## 摘要
移动端显示器的刷新率通常为 90-120 Hz，但大多数视频的编码帧率为 24-30 fps；实现实时的帧率翻倍要求在移动端神经网络处理单元（NPU）上，每个合成帧的处理时间需在 33.3 ms 以内。我们指出，主流的基于光流的视频插帧方法在移动加速器上面临三个结构性的部署障碍：空间采样算子超出了帧时间预算或缺乏硬件支持、迭代光流细化在 8 位训练后量化下失效，以及受内存限制（memory-bound）的算子主导了推理图。ANVIL 通过复用 H.264 解码器已计算出的运动矢量来预对齐输入帧，从而解决了这些障碍，并从加速器图中移除了学习型光流、空间采样和迭代累加。剩余的残差由一个以卷积为主的网络进行细化，该网络的推理图几乎完全由受计算限制（compute-bound）的算子组成。在骁龙 8 Gen 3 设备上，ANVIL 在 8 位整数精度下实现了 12.8 ms 的 1080p 网络推理；在 30 分钟的连续播放过程中，一个开源 Android 播放器在 54,623 个连续记录的样本中，每对插帧的端到端延迟中位数保持在 28.4 ms。逐算子因果分析表明，循环光流状态上的量化累加是导致迭代方法中整数型量化失败的关键机制。目前的设计针对的是解码器暴露运动矢量的 H.264 播放场景。

## Abstract
Mobile displays refresh at 90-120 Hz, yet most video is encoded at 24-30 frames per second; real-time frame-rate doubling requires each synthesized frame within 33.3 ms on mobile neural processing units. We show that mainstream flow-based video frame interpolation faces three structural deployment barriers on mobile accelerators: spatial sampling operators exceed the frame budget or lack hardware support, iterative flow refinement collapses under 8-bit post-training quantization, and memory-bound operators dominate the inference graph. ANVIL addresses these barriers by reusing motion vectors already computed by the H.264 decoder to prealign input frames, removing learned optical flow, spatial sampling, and iterative accumulation from the accelerator graph. The remaining residual is refined by a convolution-dominated network whose inference graph is composed almost entirely of compute-bound operators. On a Snapdragon 8 Gen 3 device, ANVIL achieves 12.8 ms 1080p network inference in 8-bit integer precision; an open-source Android player sustains 28.4 ms median end-to-end latency per interpolated frame pair over 54,623 consecutively logged samples during 30-minute continuous playback. Per-operator causal analysis identifies quantized accumulation on recurrent flow states as a key mechanism behind integer quantization failure in iterative methods. The current design targets H.264 playback scenarios with decoder-exposed motion vectors.

---

## 论文详细总结（自动生成）

这是一份关于论文《ANVIL: Accelerator-Native Video Interpolation via Codec Motion Vector Priors》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在移动端设备（如手机）上实现 **1080p @ 60fps** 的实时视频插帧（VFI）。
*   **研究背景**：现代移动显示器支持 90-120Hz 刷新率，但视频多为 24-30fps。现有的主流插帧算法（如 RIFE, IFRNet）在移动端 NPU（神经网络处理器）上部署面临三大障碍：
    1.  **算子不兼容/低效**：如 `grid_sample`（空间采样）在 NPU 上极慢或不支持。
    2.  **量化失效**：为了性能必须使用 INT8 量化，但迭代式光流细化结构在量化后精度会发生崩溃。
    3.  **内存受限**：大量算子属于内存受限型（Memory-bound），无法发挥 NPU 的计算算力。

### 2. 方法论
ANVIL 提出了一个**加速器原生（Accelerator-Native）**的设计，将插帧任务分解为“编解码器预对齐”和“NPU 残差细化”：
*   **核心思想**：复用 H.264 视频解码器中现成的**运动矢量（Motion Vectors, MVs）**作为零参数的运动先验，替代昂贵的深度学习光流估计。
*   **关键技术流程**：
    1.  **运动矢量提取（CPU）**：从 H.264 P 帧中提取块级 MV。
    2.  **空间流平滑（GPU/Vulkan）**：对原始 MV 进行中值滤波和高斯模糊，消除块效应，生成平滑的稠密光流场。
    3.  **预对齐（GPU/Vulkan）**：利用平滑后的光流将相邻两帧向中间时刻进行 Warp（扭曲）对齐。
    4.  **残差细化网络（NPU/INT8）**：设计了一个 4 层非对称 U-Net（UNet-v3b）。该网络仅包含标准卷积、ReLU 和加法，完全避开了 `grid_sample` 和 `Concat` 等 NPU 不友好的算子。
    5.  **部署优化**：在部署时将 Batch Normalization (BN) 融合进卷积权重，并使用加法跳跃连接减少内存拷贝。

### 3. 实验设计
*   **数据集**：使用 **Vimeo90K** 进行训练；在 **Vimeo90K 测试集**和 **Xiph 1080p** 数据集上进行评估。
*   **Benchmark 与对比方法**：
    *   对比了 9 种代表性方法（如 RIFE, IFRNet, AMT, VFIformer 等）的算子兼容性和延迟。
    *   性能对比基准包括 RIFE HDv3 和多种分辨率缩减策略（如 360p/480p 推理后上采样）。
*   **硬件平台**：主要在骁龙 8 Gen 2/3 (HTP V73/V75) 上测试，并在联发科天玑 9300/9400+ (APU) 上进行了跨厂商验证。

### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 16，采用 AMP bfloat16 混合精度训练，并使用了 `torch.compile` 加速。
*   **算力说明**：文中**未明确说明**具体使用的训练 GPU 型号（如 A100 或 RTX 4090）及总训练时长。
*   **推理性能**：在骁龙 8 Gen 3 上，1080p INT8 推理仅需 **12.8 ms**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了系统的算子级 Benchmark，涵盖 17 种算子在不同 NPU 上的表现。
    *   **系统级验证**：在一个开源 Android 播放器中进行了 **30 分钟连续播放测试**，记录了超过 5.4 万个帧对的延迟数据。
    *   **消融实验**：针对预对齐贡献、模型容量缩放、部署优化策略等进行了多组对比。
*   **充分性评价**：实验设计非常贴近工业部署实战，不仅有学术指标（PSNR/SSIM），更有详尽的功耗、热节流（Thermal Throttling）和端到端延迟分析，客观性极强。

### 6. 主要结论与发现
*   **量化崩溃机制**：通过因果分析发现，迭代式方法中“**循环状态上的量化累加（Add-on-recurrent-state）**”是导致 INT8 量化失败的主因。
*   **性能突破**：ANVIL 是首个在移动端 NPU 上实现 1080p 实时（<33.3ms）且量化鲁棒的插帧方案。
*   **端到端表现**：在骁龙 8 Gen 3 上，端到端中值延迟为 **28.4 ms**，94.9% 的帧能满足 33.3ms 的实时预算。

### 7. 优点
*   **部署导向**：不盲目追求 SOTA 榜单排名，而是针对硬件限制（算子支持、内存带宽、INT8 精度）进行协同设计。
*   **系统级创新**：巧妙利用了视频解码器的副产品（MV），大幅降低了计算负担。
*   **深度诊断**：对 VFI 算法在 NPU 上量化失效的原因给出了清晰的解释。

### 8. 不足与局限
*   **应用限制**：目前高度依赖 H.264 的运动矢量提取，对于 HEVC (H.265) 或 AV1 等新格式，由于移动端 API 限制，尚难直接获取块级 MV。
*   **视觉质量代价**：相比于最先进的非实时算法，ANVIL 倾向于产生更平滑的图像（LPIPS 指标稍弱），在处理复杂随机运动（如流水）时表现一般。
*   **解码开销**：由于 Android MediaCodec 不暴露 MV，目前需使用软件解码提取 MV，这会增加 CPU 的功耗负担。

（完）
