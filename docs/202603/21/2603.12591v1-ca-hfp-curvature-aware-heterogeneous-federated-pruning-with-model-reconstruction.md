---
title: "CA-HFP: Curvature-Aware Heterogeneous Federated Pruning with Model Reconstruction"
title_zh: CA-HFP：具有模型重构的曲率感知异构联邦剪枝
authors: "Gang Hu, Yinglei Teng, Pengfei Wu, Shijun Ma"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12591v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 异构边缘设备剪枝与本地计算感知
tldr: 针对异构边缘设备上的联邦学习，本文提出CA-HFP框架。该框架利用曲率感知的重要性评分指导各客户端进行结构化剪枝，并通过轻量级重构将子模型映射回全局空间。研究推导了考虑剪枝扰动的收敛界，实验证明该方法在保持模型精度的同时，显著降低了计算与通信开销，优于现有基准。
selection_source: fresh_fetch
motivation: 旨在解决异构边缘设备在联邦学习中面临的个性化压缩需求与全局聚合兼容性、收敛稳定性之间的矛盾。
method: 提出一种基于曲率感知的结构化剪枝准则，并结合模型重构技术将异构子模型映射至统一的全局参数空间。
result: 在多个数据集和架构上的实验表明，CA-HFP在显著减少计算和通信成本的同时，保持了较高的模型准确率。
conclusion: CA-HFP是一种有效的异构联邦学习剪枝方案，通过理论指导的剪枝准则实现了性能与效率的平衡。
---

## 摘要
异构边缘设备上的联邦学习需要个性化压缩，同时保持聚合兼容性和稳定的收敛性。我们提出了曲率感知异构联邦剪枝（CA-HFP），这是一个实用的框架，使每个客户端能够在曲率信息显著性评分的指导下执行结构化的、特定于设备的剪枝，随后通过轻量级重构将其紧凑的子模型映射回通用的全局参数空间。我们推导了具有多个局部 SGD 步骤的联邦优化的收敛界，该收敛界显式地考虑了局部计算、数据异构性和剪枝引起的扰动；并据此推导出了一个基于损失的有原则的剪枝准则。在不同数据异构程度下，使用 VGG 和 ResNet 架构在 FMNIST、CIFAR-10 和 CIFAR-100 上进行的广泛实验表明，CA-HFP 在保持模型准确性的同时，显著降低了每个客户端的计算和通信成本，优于标准的联邦训练和现有的基于剪枝的基准方法。

## Abstract
Federated learning on heterogeneous edge devices requires personalized compression while preserving aggregation compatibility and stable convergence. We present Curvature-Aware Heterogeneous Federated Pruning (CA-HFP), a practical framework that enables each client perform structured, device-specific pruning guided by a curvature-informed significance score, and subsequently maps its compact submodel back into a common global parameter space via a lightweight reconstruction. We derive a convergence bound for federated optimization with multiple local SGD steps that explicitly accounts for local computation, data heterogeneity, and pruning-induced perturbations; from which a principled loss-based pruning criterion is derived. Extensive experiments on FMNIST, CIFAR-10, and CIFAR-100 using VGG and ResNet architectures under varying degrees of data heterogeneity demonstrate that CA-HFP preserves model accuracy while significantly reducing per-client computation and communication costs, outperforming standard federated training and existing pruning-based baselines.

---

## 论文详细总结（自动生成）

这篇论文提出了 **CA-HFP**（Curvature-Aware Heterogeneous Federated Pruning），旨在解决异构边缘设备在联邦学习（FL）中面临的资源受限与数据非独立同分布（Non-IID）的双重挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在跨设备联邦学习场景中，边缘设备（如手机、IoT传感器）存在严重的**系统异构性**（计算、内存、带宽差异）和**统计异构性**（数据 Non-IID）。
*   **研究动机**：
    *   现有的联邦剪枝方法虽然能降低开销，但在处理高度异构的数据时，剪枝导致的模型偏差会严重破坏全局模型的聚合稳定性。
    *   传统的剪枝准则（如仅基于权重大小）忽略了损失函数的二阶曲率信息，导致在非平滑优化路径上剪掉重要参数。
    *   不同设备剪枝结构不一致，导致直接聚合困难。

### 2. 方法论
CA-HFP 框架包含三个核心技术支柱：
*   **曲率感知剪枝准则（Curvature-Aware Criterion）**：
    *   通过对损失函数进行二阶泰勒展开，推导出剪枝引起的损失扰动公式。
    *   定义了显著性评分 $s_i$，结合了梯度（一阶）和 Hessian 矩阵（二阶曲率）信息。在训练后期梯度变小时，曲率信息成为衡量参数重要性的主导因素。
    *   **近似实现**：为了避免直接计算 Hessian 矩阵的高额开销，服务器利用局部模型的变分来估算参数重要性。
*   **异构模型重构机制（Model Reconstruction）**：
    *   每个客户端根据自身资源等级（Rank）采用不同的剪枝率。
    *   **重构过程**：在服务器聚合前，将客户端上传的紧凑子模型映射回原始全局参数空间。对于被剪掉的神经元或卷积核，使用当前全局模型的对应参数进行填充。这确保了所有子模型在聚合前结构对齐，缓解了聚合偏差。
*   **收敛性理论分析**：
    *   论文推导了在多次局部 SGD 步数下，包含剪枝扰动项 $e_t$ 的收敛界。
    *   理论证明了增加局部迭代次数 $E$ 可以吸收部分剪枝偏差，但过大的 $E$ 会放大异构性带来的误差，从而指导了超参数的选取。

### 3. 实验设计
*   **数据集**：FMNIST、CIFAR-10、CIFAR-100。
*   **模型架构**：VGG16 和 ResNet32（代表不同的结构特征）。
*   **异构性设置**：
    *   **数据异构**：使用 Dirichlet 分布 $Dir(\alpha)$ 划分数据，$\alpha$ 越小异构性越强。
    *   **系统异构**：将客户端分为 4 个等级（Rank 0-3），剪枝率从 0% 覆盖到 90%。
*   **对比基准（Benchmarks）**：
    *   标准 FL：FedAvg, FedProx。
    *   剪枝 FL：PruneFL, FedMP, DapperFL。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX 3090 GPU** 进行模拟实验。
*   **软件框架**：PyTorch。
*   **训练参数**：总计 300 轮通信，每轮 1 个局部 Epoch，Batch Size 为 128，学习率 0.1。
*   **算力说明**：文中未提及多卡并行或超大规模集群，属于典型的单机模拟分布式实验。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集、2 种主流架构、3 种数据异构水平（$\alpha=0.1, 1.0, 10.0$）以及 4 种系统资源等级。
*   **消融实验**：专门针对“有无重构机制”进行了对比，验证了重构在 Non-IID 场景下的关键作用。
*   **评价指标**：包括测试准确率、通信负载（MB）、计算开销（TFLOPs）和任务完成时间。
*   **充分性评价**：实验设计较为全面，通过多维度对比证明了方法在极端剪枝（90%）和极端异构（$\alpha=0.1$）下的鲁棒性，结果具有客观性。

### 6. 主要结论与发现
*   **性能优越性**：CA-HFP 在所有数据集上均优于现有的剪枝基准，尤其在 CIFAR-100 和高异构环境下，准确率提升显著。
*   **效率提升**：在保持与 FedAvg 相当的精度时，CA-HFP 显著降低了通信和计算成本，且任务完成时间最短。
*   **重构的重要性**：消融实验显示，在 $\alpha=0.1$ 时，重构机制将 CIFAR-10 的准确率从 62.84% 提升至 73.12%。
*   **曲率感知的有效性**：相比于仅基于权重的剪枝，基于曲率的准则能更精准地保留对全局收敛关键的参数。

### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还给出了严谨的收敛性证明，并从理论推导出剪枝准则。
*   **解决异构性痛点**：通过重构机制巧妙解决了异构子模型无法直接聚合的工程难题。
*   **高压缩比下的稳定性**：在 90% 的极高剪枝率下仍能保持较好的模型性能。

### 8. 不足与局限
*   **Hessian 近似精度**：虽然使用了近似方法，但在极度非凸的损失曲面上，这种近似的准确性及其对收敛的影响仍有待进一步探讨。
*   **模型覆盖面**：实验主要集中在 CNN 架构，未验证其在 Transformer 或大语言模型（LLM）等更现代架构上的表现。
*   **客户端规模**：实验模拟了 10 个客户端，虽然符合 cross-device 的部分特征，但未在大规模（如成千上万客户端）真实网络环境下测试。

（完）
