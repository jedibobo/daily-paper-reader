---
title: "SlideSparse: Fast and Flexible (2N-2):2N Structured Sparsity"
title_zh: "SlideSparse：快速且灵活的 (2N-2):2N 结构化稀疏"
authors: "Hanyong Shao, Yingbo Hao, Ting Song, Yan Xia, Di Zhang, Shaohan Huang, Xun Wu, Songchen Xu, Le Xu, Li Dong, Zewen Chi, Yi Zou, Furu Wei"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05232v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 为结构化稀疏解锁硬件加速
tldr: "4稀疏模式在LLM中导致精度大幅下降的问题，本文提出SlideSparse系统。它通过滑动窗口分解技术，将更温和的(2N-2):2N稀疏模式（如6:8）转化为硬件支持的2:4模式，并结合激活提升技术降低开销。该方法在保持模型精度的同时，在多种GPU和模型上实现了接近理论极限的加速比（如6:8模式下达1.33倍），为LLM的高效推理提供了新路径。"
selection_source: carryover_cache
motivation: "4结构化稀疏会导致大语言模型精度严重受损，而精度更高的(2N-2):2N稀疏模式缺乏硬件加速支持。"
method: "2N权重块重构为多个兼容硬件的2:4窗口，并利用激活提升技术在量化过程中同步处理激活重排。"
result: 8稀疏模式的理论上限。
conclusion: 2N稀疏模式在商用GPU上的硬件加速能力，实现了精度与性能的平衡。
---

## 摘要
NVIDIA 的 2:4 稀疏张量核心（Sparse Tensor Cores）可提供 2 倍的吞吐量，但要求严格的 50% 剪枝率——这一比例会导致大语言模型（LLM）的推理准确率崩溃（例如 Qwen3 从 54% 降至 15%）。更温和的 (2N-2):2N 模式（如 6:8，即 25% 剪枝）能够保持准确率，但目前缺乏硬件支持，只能回退到稠密执行，无法从稀疏性中获益。我们提出了 SlideSparse，这是首个在商用 GPU 上为 (2N-2):2N 模型系列解锁稀疏张量核心加速的系统。我们的滑动窗口分解（Sliding Window Decomposition）在不损失准确率的情况下，将任何 (2N-2):2N 权重块重构为 N-1 个重叠的符合 2:4 规范的窗口；激活提升（Activation Lifting）则以近乎零的成本将相应的激活重排融合到逐 token 量化中。SlideSparse 已集成到 vLLM 中，并在多种 GPU（A100、H100、B200、RTX 4090、RTX 5080、DGX-spark）、精度（FP4、INT8、FP8、BF16、FP16）和模型系列（Llama、Qwen、BitNet）上进行了评估。在计算密集型工作负载下，Qwen2.5-7B 在 6:8 权重稀疏度下的实测加速比（1.33x）接近理论上限 N/(N-1)=4/3，证明了 (2N-2):2N 是实现保持准确率的 LLM 加速的一条切实可行的路径。代码开源于 https://github.com/bcacdwk/vllmbench。

## Abstract
NVIDIA's 2:4 Sparse Tensor Cores deliver 2x throughput but demand strict 50% pruning -- a ratio that collapses LLM reasoning accuracy (Qwen3: 54% to 15%). Milder $(2N-2):2N$ patterns (e.g., 6:8, 25% pruning) preserve accuracy yet receive no hardware support, falling back to dense execution without any benefit from sparsity. We present SlideSparse, the first system to unlock Sparse Tensor Core acceleration for the $(2N-2):2N$ model family on commodity GPUs. Our Sliding Window Decomposition reconstructs any $(2N-2):2N$ weight block into $N-1$ overlapping 2:4-compliant windows without any accuracy loss; Activation Lifting fuses the corresponding activation rearrangement into per-token quantization at near-zero cost. Integrated into vLLM, SlideSparse is evaluated across various GPUs (A100, H100, B200, RTX 4090, RTX 5080, DGX-spark), precisions (FP4, INT8, FP8, BF16, FP16), and model families (Llama, Qwen, BitNet). On compute-bound workloads, the measured speedup ratio (1.33x) approaches the theoretical upper-bound $N/(N-1)=4/3$ at 6:8 weight sparsity in Qwen2.5-7B, establishing $(2N-2):2N$ as a practical path to accuracy-preserving LLM acceleration. Code available at https://github.com/bcacdwk/vllmbench.

---

## 论文详细总结（自动生成）

### 论文总结：SlideSparse: Fast and Flexible (2N-2):2N Structured Sparsity

#### 1. 论文的核心问题与整体含义
论文针对大语言模型（LLM）在推理加速中面临的“精度与速度不可兼得”的困境。NVIDIA 的 Sparse Tensor Cores 虽然支持 2:4 结构化稀疏（50% 剪枝）并提供 2 倍吞吐量，但 50% 的剪枝率会导致 LLM（尤其是推理任务）准确率大幅下降（如 Qwen3 从 54% 崩溃至 15%）。相比之下，更温和的 (2N-2):2N 模式（如 6:8，即 25% 剪枝）能很好地保持精度，但缺乏硬件支持，只能回退到稠密计算。

**SlideSparse 的核心意义**在于：它通过算法创新，在不修改硬件的前提下，首次在商用 GPU 上实现了 (2N-2):2N 稀疏模式的硬件加速，填补了“高精度、零加速”与“低精度、高加速”之间的空白。

#### 2. 论文提出的方法论
核心思想是**“计算套利”**：通过数据适度膨胀换取硬件兼容性。

*   **滑动窗口分解（Sliding Window Decomposition）：** 论文证明了任何一个 (2N-2):2N 的权重块都可以无损地分解为 $N-1$ 个重叠的、符合 2:4 规范的窗口。例如，6:8 模式（N=4）可分解为 3 个步长为 2、窗口大小为 4 的 2:4 兼容块。
*   **激活提升（Activation Lifting）：** 为了匹配分解后的权重，输入激活值需要进行相应的索引重排和复制。
*   **融合内核设计（Fused Kernel）：** 为了消除激活提升带来的额外内存开销，SlideSparse 将激活重排操作融合进“逐 token 量化”内核中。利用 Triton 编写的内核在寄存器中完成“读取 -> 量化 -> 滑动重排 -> 打包 -> 写入”，使激活处理的边际成本接近于零。
*   **系统流程：** 分为离线权重打包（Packer）、模型加载时的压缩（cuSPARSELt 压缩）以及在线推理时的融合执行。

#### 3. 实验设计
*   **集成平台：** 集成到 vLLM 推理引擎中。
*   **对比基准（Benchmark）：** 
    *   **cuBLASLt：** 稠密矩阵乘法基准。
    *   **cuSPARSELt：** 原生 2:4 稀疏加速基准。
*   **测试场景：** 
    *   **算子级（Kernel-level）：** 测试不同矩阵形状（正方形及模型实际形状）。
    *   **端到端（E2E）：** 测试 Prefill（计算密集型）和 Decode（访存密集型）阶段。
*   **模型与精度：** 涵盖 Llama 3.2 (1B/3B)、Qwen 2.5 (7B/14B)、BitNet 1.58 (2B)；支持 FP4, INT8, FP8, BF16, FP16 五种精度。

#### 4. 资源与算力
论文在极其广泛的硬件平台上进行了验证，包括：
*   **数据中心级：** A100 (80GB), H100 (80GB), B200 (180GB)。
*   **消费级：** RTX 4090 (24GB), RTX 5080 (16GB)。
*   **嵌入式/工作站：** DGX Spark (GB10, 128GB)。
*   **训练/剪枝：** 论文主要使用事后量级剪枝（Post-hoc magnitude pruning），未详细说明训练时长，但提到权重打包速度极快（H100 上超过 10GB/s，Llama-70B 转换仅需不到 30 秒）。

#### 5. 实验数量与充分性
实验设计非常充分且具有高度的客观性：
*   **多维度覆盖：** 跨越了 3 代 GPU 架构（Ampere, Hopper, Blackwell, Ada Lovelace），覆盖了从 1B 到 14B 的不同规模模型。
*   **精度全面：** 几乎涵盖了目前 LLM 推理涉及的所有主流精度。
*   **阶段完整：** 同时分析了 Prefill 和 Decode 阶段，并提出了“算法效率”指标来剔除驱动/库优化不均带来的偏差。
*   **公平性：** 所有对比均基于经过 exhaustive search 优化后的 cuBLASLt 和 cuSPARSELt。

#### 6. 论文的主要结论与发现
*   **加速比接近理论极限：** 在计算密集型任务中，6:8 稀疏度的加速比达到了 1.33x，精确匹配理论上限 $N/(N-1)$。
*   **精度保持优异：** 6:8 稀疏度在 Qwen3 上的推理准确率（51.6%）远高于 2:4 模式（15.3%），接近稠密模型（54.0%）。
*   **效率超越原生：** 在某些配置下，SlideSparse 的效率甚至超过 100%（相对于原生 2:4），这得益于融合内核减少了内存往返。
*   **通用性：** 证明了 (2N-2):2N 是一类通用的、可硬件加速的稀疏族。

#### 7. 优点
*   **无需硬件修改：** 纯软件/算法方案，直接在现有数百万颗 GPU 上运行。
*   **数学证明严谨：** 提供了滑动窗口覆盖的必要性和充分性证明，并给出了通用的扩展因子公式。
*   **工程实现高效：** 通过 Triton 融合内核巧妙解决了数据膨胀带来的访存压力。
*   **实用性极强：** 为 LLM 部署提供了一个精度与性能之间更平滑的权衡点。

#### 8. 不足与局限
*   **剪枝策略：** 实验主要基于简单的事后剪枝，若结合稀疏感知训练（Sparse-aware training），准确率可能进一步提升，但文中未深入探讨训练成本。
*   **小规模矩阵开销：** 在 Decode 阶段或 Batch size 极小时（M < 256），稀疏算子的固定开销（Metadata 处理等）可能抵消计算收益。
*   **访存瓶颈：** 虽然减少了权重加载量，但在极端访存受限的场景下，加速效果受限于带宽而非计算吞吐。

（完）
