---
title: "SpiderCam: Low-Power Snapshot Depth from Differential Defocus"
title_zh: SpiderCam：基于差分焦外成像的低功耗快照式深度相机
authors: "Marcos A. Ferreira, Tianao Li, John Mamish, Josiah Hester, Yaman Sangar, Qi Guo, Emma Alexander"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17910v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于FPGA的低功耗深度感知软硬件协同设计
tldr: SpiderCam 是一款基于 FPGA 的低功耗快照式离焦深度相机，通过自定义硬件同时捕获两张不同焦距的图像，并利用改进的微分离焦深度（DfDD）算法实时生成深度图。该系统在 32.5 FPS 下功耗仅为 624 mW，实现了亚瓦级的被动式 3D 成像，其核心贡献在于针对低功耗传感器优化了算法，并设计了无需存储完整图像的流式计算架构。
selection_source: fresh_fetch
motivation: 针对嵌入式设备对低功耗、实时被动深度感知的需求，解决现有 3D 相机功耗过高且难以在小型 FPGA 上运行的问题。
method: 开发了一种改进的微分离焦深度算法，并利用 SystemVerilog 在低功耗 FPGA 上实现了内存局部化的流式深度计算架构。
result: 在 52 厘米工作范围内实现了 480x400 分辨率、32.5 FPS 的实时深度图输出，总功耗仅为 624 mW。
conclusion: SpiderCam 实现了文献中首个亚瓦级总功耗的被动式 FPGA 3D 相机，证明了高效算法与硬件协同设计在低功耗视觉任务中的潜力。
---

## 摘要
我们介绍了 SpiderCam，这是一款基于 FPGA 的快照式焦外成像测距（depth-from-defocus）相机。它能在 52 cm 的工作范围内，以 32.5 FPS 的帧率实时生成 480x400 的稀疏深度图，而总功耗仅为 624 mW。SpiderCam 包含一个定制相机，可同时捕捉同一场景的两幅焦点不同的图像，并通过在低功耗 FPGA 上实现的 SystemVerilog 差分焦外成像测距（DfDD）算法进行处理。为了实现最先进的功耗水平，我们对 DfDD 进行了算法改进，以克服低功耗传感器带来的挑战，并针对内存容量不足以存储单对图像的微型设备，设计了一种内存局部化（memory-local）实现方案用于流式深度计算。据我们所知，这是文献中首次报道的总功耗低于 1 瓦的基于 FPGA 的被动式 3D 相机。

## Abstract
We introduce SpiderCam, an FPGA-based snapshot depth-from-defocus camera which produces 480x400 sparse depth maps in real-time at 32.5 FPS over a working range of 52 cm while consuming 624 mW of power in total. SpiderCam comprises a custom camera that simultaneously captures two differently focused images of the same scene, processed with a SystemVerilog implementation of depth from differential defocus (DfDD) on a low-power FPGA. To achieve state-of-the-art power consumption, we present algorithmic improvements to DfDD that overcome challenges caused by low-power sensors, and design a memory-local implementation for streaming depth computation on a device that is too small to store even a single image pair. We report the first sub-Watt total power measurement for passive FPGA-based 3D cameras in the literature.

---

## 论文详细总结（自动生成）

以下是对论文《SpiderCam: Low-Power Snapshot Depth from Differential Defocus》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在极低功耗（亚瓦级）和极小内存资源约束下，实现实时的被动式 3D 深度感知？
*   **研究背景**：
    *   现有的深度感知技术（如立体视觉、LiDAR）功耗通常在 2-5W 以上，难以集成到可穿戴设备或微型机器人中。
    *   许多基于 FPGA 的研究仅报告“核心逻辑功耗”，忽略了传感器、I/O 和图像对齐的实际总功耗。
    *   受跳蛛（Jumping Spider）利用多层视网膜进行“微分离焦深度感知”（DfDD）的启发，本文旨在通过软硬件协同设计，打破功耗瓶颈。

### 2. 方法论
*   **核心思想**：利用分束器（Beam Splitter）和两个偏移位置不同的低功耗传感器，同时捕获两张具有不同焦外模糊（Defocus）效果的图像，通过计算图像间的亮度差异与空间拉普拉斯变换的比值来推算深度。
*   **关键技术细节**：
    *   **DfDD 算法改进**：提出联合深度估计公式 $Z = \sum \omega_i V_i W_i / \sum \omega_i W_i^2$，结合了 2 个尺度和 3 种导数阶数（原图、x方向导数、y方向导数），共 6 个假设分支，增强了鲁棒性。
    *   **空间变参数校准**：针对廉价镜头存在的场曲（Petzval field curvature）问题，将图像划分为 16 个径向区域，每个区域独立校准光学参数（a, b）和置信度阈值。
    *   **流式硬件架构**：
        *   **内存局部化**：设计了无需存储完整帧的流式处理模型，仅需缓存少量行（Line Buffers），解决了 FPGA 内存不足以存储单张图像的问题。
        *   **混合精度计算**：预处理阶段使用定点运算，核心计算阶段使用去除了次正规数支持的优化型 FP16 浮点运算。
        *   **高效上采样**：开发了零交错内核（Zero-interleaved kernels），在不增加算力的情况下扩大感受野。

### 3. 实验设计
*   **硬件原型**：定制相机包含两个 Himax HM0360 低功耗传感器、分束器、10mm 消色差透镜及 Lattice ECP5 FPGA 开发板。
*   **数据集/场景**：
    *   **校准数据集**：在 0.24m 至 1.36m 范围内，以 2cm 为步长采集了 56 组带有纹理平面的图像对。
    *   **真实场景**：测试了包含连续深度、遮挡、透明物体（气泡膜）、反射表面及运动模糊的复杂场景。
*   **Benchmark 与对比方法**：
    *   对比了多种基于 FPGA 的立体视觉（Stereo）方案（如 Jin 2014, Mattoccia 2015 等）。
    *   对比了最新的 DfD 方案 Focal Split (Luo 2025)。
    *   进行了消融实验，对比了不同尺度、导数分支及有无空间变参数对精度的影响。

### 4. 资源与算力
*   **硬件平台**：Lattice ECP5 (LFE5U-85F) FPGA。
*   **功耗表现**：
    *   **全系统总功耗**：在 32.5 FPS 下仅为 **624 mW**（包含传感器和 I/O）。
    *   **核心逻辑功耗**：估计约为 240-310 mW。
*   **算力效率**：在 Kintex-7 FPGA 上模拟时，核心计算可达 300 FPS；在 ECP5 上实际运行为 32.5 FPS（受限于 I/O 接口）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 56 组密集采样的深度数据进行定量校准和误差分析。
    *   展示了 6 种不同类型的真实挑战场景（运动、透明度等）。
    *   **充分性评价**：实验设计非常客观且具有说服力。作者不仅报告了理想情况下的精度，还详细对比了“全系统功耗”与“核心功耗”的区别，并提供了详细的消融实验来证明算法改进（如径向校准）的必要性。

### 6. 主要结论
*   **性能指标**：在 0.45m 至 0.97m 的工作范围内（52cm 跨度），深度误差保持在 10% 以内。
*   **功耗突破**：实现了文献中首个亚瓦级（Sub-Watt）总功耗的被动式 FPGA 3D 相机，比现有最先进系统功耗降低了约 3.3 倍。
*   **实时性**：在极低资源消耗下实现了 32.5 FPS 的实时输出。

### 7. 优点
*   **软硬件深度协同**：不仅是算法优化，还针对 FPGA 的流式特性重新设计了上采样和内存管理方案。
*   **真实环境验证**：不同于许多只跑数据集的研究，该工作构建了完整的硬件原型并测量了“墙上功耗”（Wall Power）。
*   **鲁棒性**：通过多分支联合估计和径向校准，成功克服了廉价光学元件和高噪声传感器的非理想特性。

### 8. 不足与局限
*   **深度图稀疏性**：由于 DfD 原理限制，深度图在缺乏纹理的区域是稀疏的，需要后续的深度补全算法。
*   **工作范围有限**：目前有效工作范围仅约 0.5 米（0.45m-0.97m），对于远距离感知能力不足。
*   **噪声敏感度**：低功耗传感器带来的高噪声仍是限制精度的主要因素，虽然算法有所缓解，但在暗光环境下表现可能受限。
*   **应用限制**：目前主要针对近距离、嵌入式场景，不适用于需要高密度、长距离感知的自动驾驶等领域。

（完）
