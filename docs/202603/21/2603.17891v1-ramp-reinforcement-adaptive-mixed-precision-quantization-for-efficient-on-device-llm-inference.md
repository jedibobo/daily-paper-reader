---
title: "RAMP: Reinforcement Adaptive Mixed Precision Quantization for Efficient On Device LLM Inference"
title_zh: RAMP：面向高效端侧大语言模型推理的强化自适应混合精度量化
authors: "Arpit Singh Gautam, Saurabh Jha"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17891v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于高效设备端大模型推理的强化学习自适应混合精度
tldr: 针对大语言模型在资源受限设备上的部署，本文提出RAMP框架，利用强化学习（SAC）实现自动化的逐层混合精度量化。通过引入Scale Folding预处理技术和11维特征嵌入，RAMP能在满足全局显存预算的同时最小化模型困惑度。该方法具有极强的泛化能力，在Llama 2和Mistral等模型上表现优于传统的均匀量化方法，且支持跨平台高效推理。
selection_source: fresh_fetch
motivation: 传统的均匀比特量化无法平衡模型精度与显存效率，且手动设置逐层精度过于复杂。
method: 采用Soft Actor-Critic强化学习框架，结合Scale Folding预处理技术，根据层特征自动分配最优比特位。
result: 在Llama 2 7B上以更小的体积实现了优于4-bit AWQ的精度，并展现出卓越的跨模型零样本迁移能力。
conclusion: RAMP证明了量化敏感性主要由架构决定，通过自适应混合精度可显著提升端侧大模型的推理效率与性能。
---

## 摘要
训练后量化对于在资源受限的硬件上部署大语言模型（LLMs）至关重要，然而目前最先进的方法对各层强制执行统一的位宽，导致精度与效率的权衡并非最优。我们提出了 RAMP（强化自适应混合精度），这是一个离策（off-policy）Soft Actor Critic 框架，通过学习逐层位宽分配，在全局比特预算下最小化困惑度。该策略基于激活统计量、权重属性和结构描述符的 11 维嵌入进行条件化，实现了跨模型家族和规模的零样本迁移。为了实现稳定的 4 比特以下量化，我们引入了尺度折叠（Scale Folding），这是一种通过逐通道缩放和归一化层补偿将激活离群值迁移至权重的预处理技术。一种具有非对称惩罚和预算悬崖的质量优先奖励机制驱动了模型的快速收敛。在 Llama 2 7B 上，RAMP 在 3.68GB（有效位宽 3.65 bits）下实现了 5.54 的困惑度，在模型大小上优于统一 4 比特 AWQ（3.90 GB 下为 5.60）和 GPTQ 约 6%，在质量上提升了 1% 至 3%。关键的是，仅在 Llama 2 7B 上训练的策略可以零样本泛化至 Llama 2 13B 和 Mistral 7B，且表现往往优于针对特定目标的训练，这支持了量化敏感性主要由架构决定的假设。HALO 流水线将分配方案导出为 GGUF 格式，以便在 CPU、GPU 和边缘设备上进行无算子（kernel-free）推理，保留了 FP16 常识推理性能的 99.5%。

## Abstract
Post training quantization is essential for deploying large language models (LLMs) on resource constrained hardware, yet state of the art methods enforce uniform bit widths across layers, yielding suboptimal accuracy efficiency trade offs. We present RAMP (Reinforcement Adaptive Mixed Precision), an off policy Soft Actor Critic framework that learns per layer bit width assignments to minimize perplexity under a global bit budget. The policy conditions on an 11 dimensional embedding of activation statistics, weight properties, and structural descriptors, enabling zero shot transfer across model families and scales. To enable stable sub 4 bit quantization, we introduce Scale Folding, a preconditioning technique that migrates activation outliers into weights via per channel scaling and normalization layer compensation. A quality prioritized reward with asymmetric penalties and budget cliffs drives rapid convergence. On Llama 2 7B, RAMP achieves 5.54 perplexity at 3.68GB (3.65 effective bits), outperforming uniform 4 bit AWQ (5.60 at 3.90 GB) and GPTQ by 6% in size and 1% to3% in quality. Critically, a policy trained only on Llama 2 7B generalizes zero shot to Llama 2 13B and Mistral 7B, often surpassing target specific training, supporting the hypothesis that quantization sensitivity is primarily architectural. The HALO pipeline exports allocations to GGUF format for kernel free inference on CPUs, GPUs, and edge devices, retaining 99.5% of FP16 commonsense reasoning performance.

---

## 论文详细总结（自动生成）

以下是对论文《RAMP: Reinforcement Adaptive Mixed-Precision Quantization for Efficient On-Device LLM Inference》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）部署面临“内存墙”瓶颈，现有的训练后量化（PTQ）方法（如 GPTQ、AWQ）通常对所有层采用**均匀比特位宽**。
*   **研究动机**：
    *   **层敏感度差异**：Transformer 架构中不同层对量化噪声的敏感度不同，均匀分配会导致资源浪费或精度损失。
    *   **缺乏迁移性**：现有混合精度搜索方法通常需要针对每个模型重新优化，计算成本高且无法跨模型通用。
    *   **硬件部署难**：混合精度推理常因算子碎片化导致实际速度反而慢于均匀量化。
*   **整体含义**：提出 RAMP 框架，利用强化学习自动学习逐层比特分配策略，旨在实现精度与显存效率的最优权衡，并支持跨模型的零样本迁移。

### 2. 论文提出的方法论
*   **核心思想**：将量化比特分配建模为**马尔可夫决策过程（MDP）**，利用强化学习（RL）策略根据层的结构和统计特征自动决定比特数。
*   **关键技术细节**：
    *   **SAC 策略网络**：采用离策（Off-policy）的 **Soft Actor-Critic (SAC)** 算法，通过重用经验池提高样本效率。
    *   **11 维层嵌入（State）**：提取层的激活统计量（最大值、重要性）、权重属性（均值、标准差）、结构描述符（归一化深度、输入/输出维度、层类型）及上下文特征。通过归一化处理，使策略具备跨规模迁移能力。
    *   **质量优先奖励函数**：设计了非对称的困惑度（PPL）惩罚机制，并引入“预算悬崖（Budget Cliff）”，对超出显存预算的行为进行阶梯式严厉惩罚。
    *   **尺度折叠（Scale Folding）**：在量化前通过逐通道缩放将激活值中的离群点（Outliers）迁移至权重中，从而稳定 3-bit 等极低比特量化。
    *   **HALO 推理流水线**：将学习到的分配方案映射到 GGUF 格式，利用 `llama.cpp` 的成熟算子实现跨平台（CPU/GPU/边缘端）的高效推理。

### 3. 实验设计
*   **数据集**：
    *   **校准/困惑度评估**：WikiText-2。
    *   **下游任务（常识推理）**：PIQA、HellaSwag、WinoGrande、ARC。
*   **模型对象**：Llama-2 (7B, 13B)、Llama-3 (8B)、Mistral-7B。
*   **Benchmark 与对比方法**：
    *   **均匀量化基准**：RTN、GPTQ、AWQ、SmoothQuant、QuIP#。
    *   **混合精度基准**：SqueezeLLM。
    *   **部署基准**：标准 GGUF (Q4_K_M)。

### 4. 资源与算力
*   **硬件型号**：使用了 NVIDIA RTX PRO 5000 Blackwell 和 A100 GPU。
*   **训练/校准时长**：
    *   **校准（嵌入提取）**：每个模型约 2 分钟。
    *   **策略训练**：SAC 算法在 6 小时内完成收敛（约 120-250 个 Episode），相比 PPO 算法（48 小时）效率提升 8 倍。
*   **推理测试**：涵盖了 RTX 3090、A100、Intel Xeon CPU 及 Apple Silicon (M1/M2/M3)。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 50 组实验，涵盖了不同模型家族、不同参数规模以及跨架构的迁移测试。
*   **消融实验**：详细对比了有无 Scale Folding、不同奖励函数设计、SAC vs PPO 算法性能、以及 11 维嵌入的有效性（PCA 分析）。
*   **客观性**：实验采用了标准化的 WikiText-2 测试集和 `lm-evaluation-harness` 工具，确保了结果的可复现性和公平性。

### 6. 主要结论与发现
*   **Pareto 最优**：在 Llama-2-7B 上，RAMP 以 3.68GB 的体积实现了 5.54 PPL，优于 4-bit AWQ（3.90GB, 5.60 PPL），体积缩小 6% 且质量更高。
*   **零样本迁移性**：**这是本文最重要的发现**。在 Llama-2-7B 上训练的策略可以直接应用于 Llama-2-13B 和 Mistral-7B，且效果往往优于在目标模型上直接训练的结果。这证明了量化敏感性主要由模型架构（而非具体权重值）决定。
*   **下游任务保持**：RAMP 保留了 FP16 模型 99.5% 的常识推理性能。
*   **低比特稳定性**：Scale Folding 技术使得 3-bit 量化在不导致模型崩溃的情况下成为可能。

### 7. 优点（亮点）
*   **通用性强**：首次证明了量化策略的跨模型迁移能力，极大降低了新模型的适配成本。
*   **生产就绪**：通过 HALO 流水线直接对接 GGUF 格式，无需编写自定义 CUDA 算子即可在多种硬件上部署。
*   **高效搜索**：相比传统的 Hessian 矩阵计算或进化算法，基于 SAC 的搜索过程极其迅速且样本效率高。

### 8. 不足与局限
*   **架构覆盖**：实验主要集中在 Decoder-only 的 Transformer 架构（Llama/Mistral），尚未在 Encoder-Decoder（如 T5）或 MoE 架构上验证。
*   **比特位限制**：目前仅支持离散的比特选择（3, 4, 5, 6），不支持分数比特（如 3.5-bit）或更细粒度的通道级分配。
*   **静态分配**：目前的策略是静态的，尚未实现根据输入内容动态调整比特宽度的推理。
*   **QAT 缺失**：该方法属于 PTQ 范畴，若能结合量化感知训练（QAT），在极低比特（<3-bit）下的表现可能进一步提升。

（完）
