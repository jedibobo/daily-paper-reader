---
title: Multi-Objective Load Balancing for Heterogeneous Edge-Based Object Detection Systems
title_zh: 异构边缘目标检测系统的多目标负载均衡
authors: "Daghash K. Alqahtani, Maria A. Rodriguez, Muhammad Aamir Cheema, Adel N. Toosi"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15400v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 异构边缘软硬件的负载均衡
tldr: 针对物联网边缘计算中硬件异构和环境动态变化的挑战，本文提出一种多目标负载均衡方法。该方法针对目标检测任务，通过两阶段决策机制（准确率过滤与延迟-能耗加权评分），在异构设备-模型对之间动态分配任务。实验表明，该方法在仅损失少量准确率的情况下，显著降低了能耗和端到端延迟，提升了边缘系统的整体效率。
selection_source: fresh_fetch
motivation: 边缘计算环境中硬件异构性及动态变化的请求与场景复杂度，使得在资源受限设备上实现高效的负载均衡变得困难。
method: 采用两阶段决策机制，先进行准确率感知过滤筛选候选设备模型，再通过延迟与能耗的加权评分函数选择最优执行目标。
result: "与准确率优先的基准相比，该方法将能耗降低了一半，端到端延迟减少了80%，而准确率下降控制在10%以内。"
conclusion: 该多目标负载均衡策略能有效平衡边缘目标检测系统的准确率、延迟和能耗，适用于动态异构的物联网环境。
---

## 摘要
物联网（IoT）和智能应用的快速普及导致分布式传感设备产生的数据量激增。边缘计算是处理这些数据的一种主流方法，它将计算推向更靠近数据源的地方，通常部署在资源受限的设备上，如单板计算机（SBC）。在此类环境中，硬件和软件不可避免的异构性使得有效的负载均衡变得尤为具有挑战性。在本文中，我们提出了一种专为异构边缘目标检测系统量身定制的多目标负载均衡方法。我们研究了一种场景，其中多个“设备-模型”对表现出不同的准确度、延迟和能耗特性，同时请求强度和场景复杂度随时间波动。为了应对这种动态变化的环境，我们的方法采用了一种两阶段决策机制：首先进行准确度感知过滤，以识别在可接受范围内提供准确度的合适设备-模型候选者；然后对预期延迟和能耗应用加权和评分函数，以选择最终的执行目标。我们通过在真实数据集上进行的大量实验评估了所提出的负载均衡器，并与广泛使用的基准策略进行了比较。结果表明，与以准确度为中心的基准相比，所提出的多目标负载均衡方法使能耗减半，端到端延迟降低了 80%，而检测准确度仅适度下降（最高 10%）。

## Abstract
The rapid proliferation of the Internet of Things (IoT) and smart applications has led to a surge in data generated by distributed sensing devices. Edge computing is a mainstream approach to managing this data by pushing computation closer to the data source, typically onto resource-constrained devices such as single-board computers (SBCs). In such environments, the unavoidable heterogeneity of hardware and software makes effective load balancing particularly challenging. In this paper, we propose a multi-objective load balancing method tailored to heterogeneous, edge-based object detection systems. We study a setting in which multiple device-model pairs expose distinct accuracy, latency, and energy profiles, while both request intensity and scene complexity fluctuate over time. To handle this dynamically varying environment, our approach uses a two-stage decision mechanism: it first performs accuracy-aware filtering to identify suitable device-model candidates that provide accuracy within the acceptable range, and then applies a weighted-sum scoring function over expected latency and energy consumption to select the final execution target. We evaluate the proposed load balancer through extensive experiments on real-world datasets, comparing against widely used baseline strategies. The results indicate that the proposed multi-objective load balancing method halves energy consumption and achieves an 80% reduction in end-to-end latency, while incurring only a modest, up to 10%, decrease in detection accuracy relative to an accuracy-centric baseline.

---

## 论文详细总结（自动生成）

### 异构边缘目标检测系统的多目标负载均衡论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：在物联网（IoT）边缘计算环境中，硬件（如 Raspberry Pi、Jetson Nano）和软件（不同版本的目标检测模型）的**异构性**是不可避免的。如何在保证检测**准确率**的同时，平衡**端到端延迟**和**能耗**，并适应动态变化的请求压力和场景复杂度（如图像中目标的数量），是当前边缘资源管理的难题。
*   **研究动机**：简单的负载均衡策略（如轮询）忽略了设备性能差异；而单一目标的优化（如仅追求高精度）会导致严重的延迟和能耗。论文旨在设计一种能够根据实时负载和图像内容复杂度，在多个“设备-模型”对之间进行智能任务分配的机制。

#### 2. 方法论
论文提出了一种**两阶段决策机制**，集成在边缘网关中：
*   **核心思想**：将准确率视为硬约束，在满足条件的候选池中通过加权评分优化延迟和能耗。
*   **关键技术细节**：
    1.  **目标数量估计**：采用基于输出的估计技术，利用视频流的临时连续性，复用前一帧的检测结果来预测当前帧的复杂度，避免了引入额外计数模型的开销。
    2.  **第一阶段：准确率感知过滤**：根据预先离线分析的数据，筛选出在当前场景复杂度下，准确率（mAP）下降在用户可接受范围（$\Delta mAP$）内的所有“设备-模型”候选对。
    3.  **第二阶段：多目标加权评分**：对候选对的预期延迟（考虑离线推理时间及实时队列长度）和能耗进行归一化处理。
    4.  **公式化决策**：使用加权和函数 $J = \gamma \tilde{L} + (1-\gamma) \tilde{E}$ 计算得分，其中 $\gamma$ 是调节延迟与能耗权重的参数。选择得分最低（最优）的设备执行任务。

#### 3. 实验设计
*   **数据集/场景**：使用真实的道路行人过马路视频流（417张 JPEG 图像），通过高容量的 YOLOv8x 模型生成伪标签作为基准（Ground Truth）。
*   **Benchmark（基准策略）**：
    *   **通用策略**：轮询（RR）、随机（RND）、最小连接数（LC）。
    *   **单目标策略**：最低能耗优先（LE）、最低延迟优先（LT）、最高准确率优先（HA）。
*   **评估指标**：平均响应时间、90%分位数延迟（p90）、吞吐量（Req/s）、每请求能耗、平均精度（mAP）。

#### 4. 资源与算力
*   **硬件测试床**：
    *   **边缘节点**：5个物理设备，包括 Raspberry Pi 5（配备 Coral TPU 或 AI HAT 加速器）和 Jetson Orin Nano（GPU）。
    *   **网关与客户端**：使用 Raspberry Pi 4 承担网关逻辑和模拟 IoT 客户端。
*   **模型**：部署了 SSD v1、SSD Lite 和 YOLOv8 small 等多种模型，涵盖 TensorRT、TFLite 和 HEF 等不同推理后端。
*   **训练/离线分析**：论文未涉及模型训练，但进行了详尽的离线性能分析（Profiling），记录了各设备-模型对在不同目标数量下的 ALE（准确率、延迟、能耗）数据。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   设置了从 1 到 15 个并发用户的不同负载等级。
    *   对参数 $\gamma$ 进行了消融实验（取值 0, 0.25, 0.5, 0.75, 1）。
    *   每组实验重复运行 3 次以确保统计可靠性，并提供了误差棒分析。
*   **充分性评价**：实验设计较为充分，涵盖了从低负载到高负载的演变过程，对比了多种经典负载均衡算法，客观地展示了多目标优化在异构环境下的权衡效果。

#### 6. 主要结论与发现
*   **性能提升**：在 $\gamma=0.5$ 的配置下，与“最高准确率优先（HA）”基准相比，该方法将**端到端延迟降低了 80% 以上**，并将**单次请求能耗减半**。
*   **准确率损失可控**：在获得巨大性能增益的同时，检测准确率（mAP）仅比 HA 基准下降了约 **10%**，且远高于其他非准确率导向的基准策略。
*   **参数灵活性**：通过调整 $\gamma$，系统可以平滑地在“节能模式”和“低延迟模式”之间切换，而对准确率的影响相对较小。

#### 7. 优点
*   **实用性强**：构建了真实的异构硬件测试床，而非纯仿真实验，结果更具参考价值。
*   **低开销设计**：利用视频帧连续性进行目标计数估计，避免了复杂的预处理开销。
*   **多维权衡**：成功解决了边缘计算中准确率、延迟、能耗这三个经常冲突的目标之间的平衡问题。

#### 8. 不足与局限
*   **离线依赖**：系统高度依赖预先测得的离线性能数据（Profiling），如果环境发生变化（如设备老化、环境温度影响功耗），静态数据可能失效。
*   **中心化瓶颈**：所有决策由单一网关做出，在更大规模（成百上千节点）的集群中，网关可能成为通信和计算的瓶颈。
*   **场景单一**：实验仅基于行人检测场景，未在更多样化的任务（如多类别检测、分割）或更极端的动态环境中验证。

（完）
