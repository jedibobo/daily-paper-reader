---
title: "GOMA: Geometrically Optimal Mapping via Analytical Modeling for Spatial Accelerators"
title_zh: GOMA：面向空间加速器的基于解析建模的几何最优映射
authors: "Wulve Yang, Hailong Zou, Rui Zhou, Jionghao Zhang, Qiang Li, Gang Li, Yi Zhan, Shushan Qiao"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07962v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 空间加速器上最优映射的解析建模
tldr: 针对空间加速器上通用矩阵乘法（GEMM）映射空间巨大且现有方法难以兼顾最优性与效率的问题，本文提出GOMA框架。GOMA通过几何抽象建立精确的分析能量模型，将映射搜索转化为整数优化问题。该方法首次实现了在极短时间内为任意负载和硬件组合提供全局最优映射，显著提升了加速器的能效表现。
selection_source: fresh_fetch
motivation: 现有的GEMM映射搜索方法在面对组合爆炸的搜索空间时，往往缺乏全局最优性保证且计算速度缓慢。
method: 引入几何抽象建立O(1)复杂度的分析能量模型，并将映射选择建模为硬件约束下的整数优化问题。
result: 实验显示GOMA在LLM负载下比现有最先进映射器提升了2.24-4.24倍的EDP，同时求解速度提高了3.83-73.6倍。
conclusion: GOMA通过数学建模和优化，成功解决了空间加速器映射搜索中全局最优性与求解效率不可兼得的难题。
---

## 摘要
空间加速器上的通用矩阵乘法（GEMM）在执行效率和能耗方面对映射选择高度敏感。然而，映射空间呈现出组合爆炸的特性，这使得在可接受的时间预算内获得最优映射变得极具挑战性。现有方法通常面临挑战：它们往往缺乏全局最优性保证，且随着映射空间的增大，运行速度变得极慢。为了解决这些局限性，我们提出了 GOMA，这是一个基于几何抽象、通过解析建模实现的全局最优 GEMM 映射框架，它在保证最优性的同时实现了高效求解。GOMA 从第一性原理出发，引入了 GEMM 映射的几何抽象，为任何给定映射生成了具有 O(1) 评估复杂度的精确解析能耗目标函数。该目标函数具有极高的准确性。随后，GOMA 将映射选择建模为硬件和映射约束下的整数优化问题，利用解析能耗模型作为目标函数来自动进行映射搜索。GOMA 能够为任何（GEMM 工作负载，目标硬件）组合快速计算出全局最优映射，这在映射空间探索领域尚属首次。实验证实，在代表性加速器和大型语言模型预填充工作负载下，GOMA 相比最先进的（SOTA）映射器将能量延迟积（EDP）提高了 2.24–4.24 倍，同时将求解时间缩短了 3.83–73.6 倍。

## Abstract
General matrix multiplication (GEMM) on spatial accelerators is highly sensitive to mapping choices in both execution efficiency and energy consumption. However, the mapping space exhibits combinatorial explosion, which makes it extremely challenging to obtain optimal mappings within an acceptable time budget. Existing approaches typically face challenges: They often lack global-optimality guarantees and become prohibitively slow as the mapping space grows. To address these limitations, we propose \textsc{GOMA}, a geometric-abstraction-based, globally optimal GEMM mapping framework via analytical modeling, which achieves efficient solving while guaranteeing optimality. \textsc{GOMA} introduces, from first principles, a geometric abstraction for GEMM mapping, yielding an exact analytical energy objective with $O(1)$ evaluation for any given mapping. The objective is highly accurate. \textsc{GOMA} then formulates mapping selection as an integer optimization problem under hardware and mapping constraints, using the analytical energy model as the objective to automate mapping search. \textsc{GOMA} can quickly compute a global-optimal mapping for any (GEMM workload, target hardware) pair, achieving this for the first time in mapping space exploration. Experiments confirm that across representative accelerators and large language model prefill workloads, \textsc{GOMA} improves the energy--delay product (EDP) by $2.24$--$4.24\times$ over SOTA mappers, while accelerating time-to-solution by $3.83$--$73.6\times$.

---

## 论文详细总结（自动生成）

这是一份关于论文《GOMA: Geometrically Optimal Mapping via Analytical Modeling for Spatial Accelerators》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在空间加速器（Spatial Accelerators）上运行通用矩阵乘法（GEMM）时，如何从天文数字级别（$>10^{10}$）的映射空间中，快速找到能效最优的执行策略（包括分块 Tiling、循环置换 Permutation 和层级旁路 Level Bypass）。
*   **研究动机**：
    *   **映射的重要性**：不同的映射方案会导致能效和性能产生数个数量级的差异。
    *   **现有方法的局限**：随机搜索和启发式搜索（如遗传算法、强化学习）难以保证全局最优性；可微近似方法存在舍入误差；现有的数学规划方法（如 CoSA）在建模硬件成本时不够精确，且在大规模问题上求解缓慢。
    *   **需求**：需要一种既能保证全局最优，又能实现秒级求解的映射框架。

### 2. 方法论
GOMA 的核心思想是将映射问题转化为基于**几何抽象**的解析优化问题。
*   **几何抽象（3D Compute Grid）**：将 GEMM 建模为一个三维计算网格 $(x, y, z)$，其中矩阵 A、B 和输出 P 分别对应网格在三个正交平面上的投影。
*   **更新计数模型（Update-Count Formulation）**：
    *   通过分析“行走轴”（Walking Axis），确定在每一层存储中哪些投影保持不变（实现重用），哪些投影需要更新。
    *   推导出闭式（Closed-form）解析公式，计算跨层数据传输的更新次数。
*   **层级旁路机制（Level Bypass）**：引入二进制开关变量，允许数据跳过某些存储层级，直接从高层传输到低层，并重新定义能耗归属。
*   **整数优化求解**：
    *   **目标函数**：总能耗 $E$，由各层更新次数乘以单位访问能耗组成。该评估函数的复杂度为 $O(1)$。
    *   **约束条件**：包括存储容量约束、PE 阵列并行度约束、分块因子的整除性约束。
    *   **全局求解**：利用 Gurobi 等商业求解器，通过分支定界法（Branch-and-Bound）在极短时间内搜索并输出带有“最优性证明”（Optimality Certificate）的全局最优解。

### 3. 实验设计
*   **工作负载（Workloads）**：涵盖了主流大语言模型（LLM）的预填充（Prefill）阶段，包括 Qwen3（0.6B/32B）和 Llama-3.2/3.3（1B/70B）。
*   **场景设置**：针对边缘（Edge）和中心/云（Center/Cloud）两种部署场景，设置了从 1k 到 128k 不等的序列长度。
*   **硬件模板**：对比了四种代表性加速器架构：Eyeriss-like、Gemmini-like、A100-like 和 TPU v1-like。
*   **对比方法（Benchmarks）**：
    *   Timeloop-mapper (Hybrid)
    *   LOMA（基于剪枝的枚举）
    *   SALSA（基于模拟退火）
    *   CoSA（基于约束优化的 SOTA）
    *   FactorFlow（基于贪心优化）

### 4. 资源与算力
*   **硬件环境**：实验在一台搭载 **AMD Ryzen 7 7840H** 处理器的笔记本电脑上完成。
*   **软件环境**：使用 Python 开发，调用 **Gurobi** 作为底层数学规划求解器。
*   **算力消耗**：论文未涉及大规模 GPU 训练，其核心在于映射算法的搜索效率。GOMA 搜索单个 GEMM 算子的平均时间仅为 **0.65 秒**，最大不超过 3.6 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 **24 组** 完整的案例研究（4 种硬件 $\times$ 6 种模型/长度组合）。
    *   每组案例包含 8 类不同的 GEMM 算子，总计评估了 **192 个** 实际算子实例。
    *   **一致性验证**：通过 8064 组随机映射配置，验证了 GOMA 解析模型与行业标准工具 Timeloop 的一致性，准确率达 **99.9%**。
*   **充分性评价**：实验覆盖了从边缘到云端的多种硬件和模型规模，对比了多种不同类型的 SOTA 映射器，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **能效提升**：在所有测试案例中，GOMA 均找到了最优解。相比 SOTA 映射器，其能量延迟积（EDP）提升了 **2.24–4.24 倍**。
*   **搜索速度**：GOMA 的求解速度比现有方法快 **3.83–73.6 倍**。
*   **稳定性**：启发式方法（如 SALSA、Timeloop Hybrid）在面对大规模搜索空间时表现极不稳定，而 GOMA 凭借数学建模保持了高度的确定性。
*   **旁路价值**：实验证明，灵活的层级旁路（Bypass）选择是降低能耗的关键维度。

### 7. 优点
*   **理论严谨**：首次从第一性原理出发，为 GEMM 映射建立了精确的几何解析模型。
*   **全局最优保证**：不同于启发式搜索，GOMA 能够提供数学证明，确保结果是模型定义下的全局最优。
*   **极速求解**：将复杂的搜索问题转化为高效的整数规划，实现了 $O(1)$ 的代价评估。
*   **开源贡献**：代码已在 GitHub 开源，具有很高的实用价值。

### 8. 不足与局限
*   **算子局限性**：目前主要针对 GEMM 算子。虽然文中提到可以扩展到卷积（Convolution），但尚未展示在非规则算子或稀疏算子上的表现。
*   **硬件模型依赖**：GOMA 的准确性依赖于底层硬件能量参考表（ERT）的准确性，如果硬件物理参数建模有误，最优映射的效果会打折扣。
*   **静态映射**：目前主要针对静态映射，未深入探讨动态电压频率缩放（DVFS）或多租户环境下的动态映射优化。

（完）
