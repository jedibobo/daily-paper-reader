---
title: "EAGLE-Pangu: Accelerator-Safe Tree Speculative Decoding on Ascend NPUs"
title_zh: EAGLE-Pangu：昇腾 NPU 上的加速器安全树状投机解码
authors: "Chang Han, Yijie Hu, Jingling Liu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08088v1"
tags: ["query:hwnas"]
score: 9.0
evidence: NPU上的加速器安全树状推测解码
tldr: EAGLE-Pangu 针对昇腾（Ascend）NPU 平台，将 EAGLE-3 树状推测解码技术移植到 Pangu 模型后端。通过引入显式分支/提交缓存管理、加速器安全的树张量化方法以及兼容融合算子的验证路径，解决了异构后端在 KV 缓存和索引语义上的兼容性难题。实验显示，该系统在 MT-Bench 等任务上显著提升了端到端解码吞吐量，最高可达 2.46 倍。
selection_source: fresh_fetch
motivation: 树状推测解码在不同硬件后端移植时，常面临注意力掩码、KV 缓存布局和索引语义不兼容导致的系统脆弱性问题。
method: 提出了基于 Cache API 的显式缓存管理器、消除负索引的树张量化技术，以及支持融合算子的教师模型验证路径。
result: 在 MT-Bench 和 HumanEval 测试中，相比仅使用教师模型的贪婪解码，端到端吞吐量平均提升 1.27 倍，P99 延迟下最高提升 2.46 倍。
conclusion: EAGLE-Pangu 证明了树状推测解码在昇腾 NPU 上的可行性与高效性，并为异构加速器上的可复现调试提供了参考路径。
---

## 摘要
自回归解码仍然是大语言模型（LLM）推理服务中的主要瓶颈，这促使了投机解码方法的发展，该方法通过每步验证多个候选 token 来减少昂贵的教师模型调用。树状结构投机进一步提升了并行度，但在跨异构后端和加速器栈移植时往往表现脆弱，因为注意力掩码、KV 缓存布局和索引语义并不通用。我们提出了 EAGLE-Pangu，这是一个可复现的系统，它将 EAGLE-3 风格的树状投机解码移植到昇腾 NPU 上的 Pangu 教师后端。EAGLE-Pangu 的贡献包括：(i) 基于 Cache API 构建的显式分支/提交缓存管理器；(ii) 加速器安全的树状张量化方法，通过构造消除了未定义的负索引并验证了结构不变性；(iii) 兼容融合算子内核的教师验证路径，并配备了可调试的 eager 模式回退机制。在来自 MT-Bench 和 HumanEval 风格提示词的 240 轮对话中，在融合算子性能路径下，EAGLE-Pangu 相比仅使用教师模型的贪婪解码，平均将端到端解码吞吐量提高了 1.27 倍，在 p99 分位下最高提升达 2.46 倍。我们还提供了一个不依赖融合算子的参考路径，包含结构化追踪和不变性检查，以支持跨执行模式和树预算的可复现调试与消融实验。

## Abstract
Autoregressive decoding remains a primary bottleneck in large language model (LLM) serving, motivating speculative decoding methods that reduce expensive teacher-model invocations by verifying multiple candidate tokens per step. Tree-structured speculation further increases parallelism, but is often brittle when ported across heterogeneous backends and accelerator stacks, where attention masking, KV-cache layouts, and indexing semantics are not interchangeable. We present EAGLE-Pangu, a reproducible system that ports EAGLE-3-style tree speculative decoding to a Pangu teacher backend on Ascend NPUs. EAGLE-Pangu contributes (i) an explicit branch/commit cache manager built on the Cache API, (ii) accelerator-safe tree tensorization that removes undefined negative indices by construction and validates structural invariants, and (iii) a fused-kernel-compatible teacher verification path with a debuggable eager fallback. On 240 turns from MT-Bench and HumanEval-style prompts, EAGLE-Pangu improves end-to-end decoding throughput by 1.27x on average, up to 2.46x at p99, over teacher-only greedy decoding in the fused-kernel performance path. We also provide a fused-kernel-free reference path with structured traces and invariant checks to support reproducible debugging and ablation across execution modes and tree budgets.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **EAGLE-Pangu**，一个专门针对华为昇腾（Ascend）NPU 平台优化的树状投机解码系统。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）的自回归解码受限于单 token 串行生成的瓶颈。虽然树状投机解码（Tree Speculative Decoding）能通过并行验证多个候选 token 提高效率，但在移植到特定硬件（如昇腾 NPU）和特定后端（如 Pangu）时，面临严重的**系统脆弱性**。
*   **研究背景**：异构加速器对 KV 缓存布局、注意力掩码（Attention Mask）形状以及张量索引语义（如不支持负索引）有严格且不统一的要求。直接移植现有的树解码实现往往会导致计算错误、性能下降或系统崩溃。
*   **整体含义**：本文旨在通过系统级的设计，使 EAGLE-3 风格的树投机解码在昇腾 NPU 上实现**安全、高效且可复现**的部署。

### 2. 方法论：核心思想与关键技术
EAGLE-Pangu 提出了三个关键的系统抽象：
*   **可分支的 KV 缓存抽象（Branchable KV-cache）**：
    *   将缓存分为“已提交前缀状态”和“各分支投机状态”。
    *   通过 `Replicate` 操作隔离不同分支，确保投机计算不污染主缓存。
    *   支持“基于长度”和“基于路径索引”的两种提交模式，并针对前缀共享场景实现了快速重排（Fast Reorder）。
*   **加速器安全的树张量化（Accelerator-safe Tree Tensorization）**：
    *   **消除负索引**：在昇腾等硬件上，传统的 `-1` 填充索引可能导致未定义行为。该方法引入“哑根节点（Dummy Root）”置于索引 0，将所有父节点索引映射为有效正值。
    *   **结构不变性检查**：在内核启动前验证树的无环性、深度一致性和范围合法性，提升调试效率。
*   **兼容融合算子的树掩码执行**：
    *   构建 4D 树状注意力掩码，严格遵循祖先谓词（Ancestor Predicate），防止分支间信息泄露。
    *   支持昇腾 NPU 的融合注意力内核（Fused Attention），同时保留 Eager 模式作为调试回退路径。

### 3. 实验设计
*   **数据集/场景**：使用了 **MT-Bench**（对话类）和 **HumanEval**（代码类）风格的提示词。
*   **实验规模**：共 240 轮（Turns）对话，平均输入长度约 501 tokens，输出长度约 891 tokens。
*   **Benchmark**：以 Pangu 后端仅使用教师模型的**标准贪婪解码（Greedy Decoding）**作为基准。
*   **对比维度**：
    *   端到端吞吐量（Tokens per second）。
    *   树预算（节点数 $M$ 和深度 $D_{max}$）的敏感度分析。
    *   草稿模型（Drafter）上下文截断的影响。
    *   融合算子（Fused）与 Eager 模式的性能对比。

### 4. 资源与算力
*   **硬件平台**：华为**昇腾（Ascend）NPU**。
*   **算力规模**：实验中提到了使用 `torchrun` 在 **8 个 NPU** 上进行分布式评估和确定性分片。
*   **模型后端**：Pangu 教师模型后端。
*   **训练时长**：文中未明确提及草稿模型的训练时长，重点在于推理阶段的系统优化。

### 5. 实验数量与充分性
*   **实验数量**：
    *   完成了 240 轮完整生成的端到端测试。
    *   进行了多组超参数扫描（如节点预算从 16 到 256，深度从 4 到 16）。
    *   包含针对草稿模型上下文窗口（128 到 512）的消融实验。
*   **充分性与客观性**：实验覆盖了性能路径和参考路径，通过 P90/P99 等长尾指标评估稳定性。对比实验在相同的提示词模板和解码设置下进行，确保了公平性。

### 6. 主要结论与发现
*   **性能提升**：在融合算子路径下，EAGLE-Pangu 平均实现 **1.27 倍**的加速，在 P99 分位下最高可达 **2.46 倍**。
*   **预算甜点区**：吞吐量随树规模并非单调增长。在实验设置下，$M=16, D_{max}=10$ 是性能最优的“甜点区”，过大的树会增加掩码和提交开销。
*   **截断风险**：对草稿模型进行硬性的上下文截断（Fixed-window truncation）会显著降低接受率，甚至导致负优化（速度慢于基准）。
*   **瓶颈分布**：时间开销分析显示，树张量化和掩码构建仅占毫秒级，主要的优化空间在于缓存提交效率和长上下文预填充。

### 7. 优点
*   **工程落地性强**：针对昇腾 NPU 的底层特性（如不支持负索引、融合算子限制）提供了具体的解决方案。
*   **安全性设计**：引入哑根节点和结构不变性检查，解决了投机解码在异构后端上容易“静默出错”的问题。
*   **可复现性**：提供了双模式执行协议（参考模式 vs 性能模式）和结构化追踪工具，便于开发者调试。

### 8. 不足与局限
*   **Batch Size 限制**：主要实验集中在 Batch Size = 1 的场景，在大规模并发服务下的表现（如 KV 缓存内存压力）有待进一步验证。
*   **缓存复制开销**：目前使用 `deepcopy` 实现分支隔离，虽然稳健但存在一定的内存和计算开销，未来可探索更轻量级的写时复制（Copy-on-write）机制。
*   **草稿模型依赖**：加速效果高度依赖于草稿模型的质量，论文未深入探讨如何针对昇腾平台优化草稿模型的训练或蒸馏。

（完）
