---
title: "DLRMamba: Distilling Low-Rank Mamba for Edge Multispectral Fusion Object Detection"
title_zh: DLRMamba：面向边缘多光谱融合目标检测的低秩 Mamba 蒸馏
authors: "Qianqian Zhang, Leon Tabaro, Ahmed M. Abdelmoniem, Junshe An"
date: 2026-03-06
pdf: "https://arxiv.org/pdf/2603.06920v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对边缘遥感检测的硬件感知Mamba模型蒸馏
tldr: 针对边缘设备上的多光谱融合目标检测，本文提出DLRMamba。该研究通过引入低秩二维选择性结构化状态空间模型（Low-Rank SS2D）来减少参数冗余，并结合结构感知蒸馏策略，在保持高精度空间建模的同时显著降低了计算复杂度和内存占用，在树莓派5等边缘平台上实现了优异的性能与效率平衡。
selection_source: fresh_fetch
motivation: 现有的Mamba模型在处理高分辨率多光谱图像时存在严重的参数冗余，导致其难以在资源受限的边缘硬件上高效部署。
method: 提出低秩SS2D模块通过矩阵分解重构状态转移，并利用结构感知蒸馏将全秩教师模型的内部潜在状态动力学迁移至学生模型。
result: 在五个基准数据集和树莓派5平台上的实验证明，该方法在显著降低计算开销的同时，检测精度和效率均优于现有的轻量化架构。
conclusion: DLRMamba成功解决了Mamba模型在边缘端部署的效率与精度权衡问题，为高效多光谱融合目标检测提供了有效的技术方案。
---

## 摘要
多光谱融合目标检测是边缘端海上监视和遥感领域的一项关键任务，要求针对高分辨率输入同时具备高推理效率和鲁棒的特征表示。然而，现有的 Mamba 等状态空间模型（SSM）在其标准的二维选择性扫描（SS2D）模块中存在显著的参数冗余，这阻碍了其在资源受限硬件上的部署，并导致在常规压缩过程中丢失细粒度的结构信息。为了应对这些挑战，我们提出了低秩二维选择性结构化状态空间模型（Low-Rank SS2D），该模型通过矩阵分解重新构建状态转移，以利用内在的特征稀疏性。此外，我们引入了一种结构感知蒸馏策略，将学生模型的内部潜在状态动力学与全秩教师模型对齐，以补偿潜在的表示退化。该方法在大幅降低计算复杂度和内存占用的同时，保留了目标识别所需的高保真空间建模。在五个基准数据集和 Raspberry Pi 5 等真实边缘平台上的广泛实验表明，我们的方法实现了卓越的效率-准确度权衡，在实际部署场景中显著优于现有的轻量级架构。

## Abstract
Multispectral fusion object detection is a critical task for edge-based maritime surveillance and remote sensing, demanding both high inference efficiency and robust feature representation for high-resolution inputs. However, current State Space Models (SSMs) like Mamba suffer from significant parameter redundancy in their standard 2D Selective Scan (SS2D) blocks, which hinders deployment on resource-constrained hardware and leads to the loss of fine-grained structural information during conventional compression. To address these challenges, we propose the Low-Rank Two-Dimensional Selective Structured State Space Model (Low-Rank SS2D), which reformulates state transitions via matrix factorization to exploit intrinsic feature sparsity. Furthermore, we introduce a Structure-Aware Distillation strategy that aligns the internal latent state dynamics of the student with a full-rank teacher model to compensate for potential representation degradation. This approach substantially reduces computational complexity and memory footprint while preserving the high-fidelity spatial modeling required for object recognition. Extensive experiments on five benchmark datasets and real-world edge platforms, such as Raspberry Pi 5, demonstrate that our method achieves a superior efficiency-accuracy trade-off, significantly outperforming existing lightweight architectures in practical deployment scenarios.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DLRMamba** 的轻量化多光谱融合目标检测框架，旨在解决 Mamba 模型在边缘设备上部署时的参数冗余与性能退化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多光谱（可见光 RGB + 红外 IR）融合检测在遥感和海上监视中至关重要，但现有的状态空间模型（Mamba/SS2D）在处理高分辨率图像时存在严重的**参数冗余**。
*   **研究动机**：标准的 SS2D 模块虽然具有线性计算复杂度，但其状态转移矩阵过于庞大，难以在资源受限的边缘硬件（如树莓派）上实时运行。此外，传统的模型压缩方法往往会破坏 Mamba 捕获长程空间依赖的能力，导致检测精度大幅下降。

### 2. 方法论
论文提出了两个核心创新点：
*   **低秩二维选择性结构化状态空间模型（Low-Rank SS2D）**：
    *   **核心思想**：利用视觉特征的内在稀疏性，将标准 SS2D 中全秩的状态转移矩阵 $A$ 分解为两个低秩矩阵 $U$ 和 $V^T$（即 $A \approx UV^T$）。
    *   **技术细节**：通过矩阵分解，将高维状态转移过程转化为两个连续的低维投影步骤，显著减少了参数量和计算开销，同时保留了全局感受野。
*   **结构感知蒸馏策略（Structure-Aware Distillation, SAD）**：
    *   为了补偿低秩压缩带来的信息损失，引入全秩教师模型指导低秩学生模型。
    *   **三重对齐损失**：
        1.  **SVD 对齐（矩阵级）**：使学生模型的低秩矩阵逼近教师模型矩阵的奇异值分解分量。
        2.  **隐藏状态序列对齐（动态级）**：强制学生模型模拟教师模型在扫描过程中的内部潜在状态轨迹。
        3.  **特征重构（输出级）**：确保最终输出特征图的语义一致性。
*   **整体架构**：采用像素级多光谱融合模块作为输入，以 Low-Rank SS2D 为骨干网络，后端连接 YOLOv8n 检测头。

### 3. 实验设计
*   **数据集**：在 5 个主流多光谱目标检测基准上进行了验证：**VEDAI**（遥感车船）、**FLIR**（城市交通）、**LLVIP**（低光行人）、**M3FD**（多场景融合）和 **DroneVehicle**（无人机视角）。
*   **对比方法**：对比了包括 SuperYOLO、DMM (TGRS 2025)、C2DFF-Net (TGRS 2025)、LGA-YOLO 等在内的最新（State-of-the-art）多光谱检测模型。
*   **评估指标**：mAP50、参数量（Params）、推理速度（FPS）。

### 4. 资源与算力
*   **训练环境**：使用单张 **NVIDIA RTX A100 (80GB)** GPU。
*   **训练细节**：采用 SGD 优化器，学习率 0.01，Batch Size 为 8，训练 300 个 Epoch。
*   **推理测试平台**：除了高性能 GPU（RTX 4090），还专门在边缘平台 **Raspberry Pi 5** 上进行了实测，以验证实际部署效率。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 5 个不同场景的数据集，实验结果具有较强的普适性。
*   **消融实验**：详细探讨了低秩分解比例（Rank Ratio）对精度与速度的影响，并逐项验证了三种蒸馏损失函数的有效性。
*   **客观性**：对比实验包含了多篇 2025 年发表的最新论文，且在真实边缘硬件上进行 FPS 测试，而非仅停留在理论 FLOPs 的计算，体现了高度的客观性和实用价值。

### 6. 主要结论与发现
*   **效率提升**：在树莓派 5 上，DLRMamba 相比基准模型实现了 **5.5 倍的加速**（FPS 从 0.42 提升至 2.30）。
*   **参数压缩**：模型参数量从 17.1 MB 压缩至 4.3 MB，缩减了约 75%。
*   **精度保持**：通过结构感知蒸馏，低秩模型不仅弥补了压缩损失，在某些数据集（如 VEDAI）上的 mAP 甚至超过了全秩基准模型（84.7% vs 81.5%），证明了蒸馏过程起到了特征强化的作用。

### 7. 优点
*   **硬件感知设计**：针对边缘设备（树莓派）的实际瓶颈进行优化，具有极强的工程落地意义。
*   **创新的蒸馏机制**：不同于传统的输出层蒸馏，该方法深入到 Mamba 的状态转移内部进行动力学对齐，抓住了 SSM 模型的本质特征。
*   **多场景鲁棒性**：在遮挡、高密度、光照变化等复杂遥感环境下表现优异。

### 8. 不足与局限
*   **实时性瓶颈**：尽管在树莓派上加速了 5.5 倍，但 2.3 FPS 对于某些高速实时检测任务（如高速无人机避障）仍显不足。
*   **超参数敏感性**：低秩比例 $r$ 需要手动调节以平衡精度和速度，缺乏一种自适应确定最优秩的机制。
*   **融合策略简单**：采用的是较早期的像素级融合，未充分探讨更先进的特征级或决策级融合对低秩 Mamba 性能的进一步提升。

（完）
