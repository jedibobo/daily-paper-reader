---
title: "AutoResearch-RL: Perpetual Self-Evaluating Reinforcement Learning Agents for Autonomous Neural Architecture Discovery"
title_zh: AutoResearch-RL：用于自主神经架构发现的永续自评估强化学习智能体
authors: "Nilesh Jain, Rohit Yadav, Sagar Kotian, Claude AI"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07300v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 资源约束下的自动神经架构发现
tldr: AutoResearch-RL 是一个无需人工干预的自动研究框架，利用强化学习（PPO）在固定资源预算下自主探索神经网络架构和超参数。该框架通过分离实验环境、可变代码文件和元学习器，将研究过程建模为马尔可夫决策过程。实验证明，该系统在单 GPU 预训练任务中，经过约 300 次迭代即可发现优于人工调优的配置，实现了全自动的神经架构发现。
selection_source: fresh_fetch
motivation: 旨在消除神经架构搜索和超参数优化中对人工监督的依赖，实现完全自主且持续的科研探索。
method: 将研究过程形式化为 MDP，利用 PPO 算法驱动智能体对训练脚本进行代码修改，并根据验证集性能获取奖励。
result: 在 nanochat 预训练基准测试中，该框架在约 300 次迭代后达到了匹配或超越人工手动调优基准的性能水平。
conclusion: AutoResearch-RL 证明了强化学习智能体能够通过自我评估和持续迭代，在无需人类干预的情况下自主发现高效的神经网络配置。
---

## 摘要
我们提出了 AutoResearch-RL，这是一个强化学习智能体在无需人工监督的情况下进行开放式神经架构和超参数研究的框架，该框架会持续运行，直到终止预测器（termination oracle）发出收敛信号或资源耗尽。在每一步中，智能体对目标训练脚本提出代码修改建议，在固定的实际运行时间预算内执行该脚本，观察源自验证集每字节比特数（val-bpb）的标量奖励，并通过近端策略优化（PPO）更新其策略。关键的设计见解是三个关注点的分离：(i) 冻结的环境（数据流水线、评估协议和常量），确保跨实验比较的公平性；(ii) 可变的目标文件（train.py），代表智能体的可编辑状态；以及 (iii) 元学习器（强化学习智能体本身），它积累不断增长的实验结果轨迹，并利用这些结果为后续提案提供信息。我们将此形式化为马尔可夫决策过程，在温和的假设下推导出收敛保证，并在单 GPU nanochat 预训练基准上通过实证表明，AutoResearch-RL 在大约 300 次隔夜迭代后，在无需人工干预的情况下，发现了与手动调优基线相当或更优的配置。

## Abstract
We present AutoResearch-RL, a framework in which a reinforcement learning agent conducts open-ended neural architecture and hyperparameter research without human supervision, running perpetually until a termination oracle signals convergence or resource exhaustion. At each step the agent proposes a code modification to a target training script, executes it under a fixed wall clock time budget, observes a scalar reward derived from validation bits-per-byte (val-bpb), and updates its policy via Proximal Policy Optimisation (PPO).   The key design insight is the separation of three concerns: (i) a frozen environment (data pipeline, evaluation protocol, and constants) that guarantees fair cross-experiment comparison; (ii) a mutable target file (train.py) that represents the agent's editable state; and (iii) a meta-learner (the RL agent itself) that accumulates a growing trajectory of experiment outcomes and uses them to inform subsequent proposals.   We formalise this as a Markov Decision Process, derive convergence guarantees under mild assumptions, and demonstrate empirically on a single GPU nanochat pretraining benchmark that AutoResearch-RL discovers configurations that match or exceed hand-tuned baselines after approximately 300 overnight iterations, with no human in the loop.

---

## 论文详细总结（自动生成）

这是一份关于论文《AutoResearch-RL: Perpetual Self-Evaluating Reinforcement Learning Agents for Autonomous Neural Architecture Discovery》的结构化深度分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：深度学习的进步长期依赖于研究人员的手动试错（假设-实现-训练-分析-迭代），这一过程缓慢且受限于人类工作时间。现有的 AutoML 方法通常局限于预定义的搜索空间，无法处理训练动力学、损失函数或优化器设计的根本性改变。
*   **核心问题**：如何构建一个能够像人类研究员一样，通过直接修改源代码进行开放式探索、自我评估并持续进化的自主智能体？
*   **整体含义**：论文提出了一种将“代码研究”建模为强化学习（RL）任务的框架，使智能体能够在固定算力预算下，自主发现超越人类专家设计的神经网络架构和训练配方。

### 2. 论文提出的方法论
*   **核心思想**：将自主代码研究形式化为一个**马尔可夫决策过程（MDP）**。系统由三个核心组件组成：
    1.  **冻结环境**：固定的数据流水线和评估协议，确保不同实验间的公平比较。
    2.  **可变目标文件**：即 `train.py` 脚本，作为智能体可编辑的状态。
    3.  **元学习器（RL 智能体）**：基于 PPO 算法的 Transformer 语言模型，通过观察实验历史来生成代码补丁（Diff）。
*   **关键技术细节**：
    *   **状态表示**：包含当前源码、过去 $K$ 次实验的历史（代码差异、奖励、评价）以及系统诊断信息。
    *   **奖励函数**：主要基于验证集每字节比特数（val-bpb）的改进量，并加入计算效率奖励。
    *   **自评估模块（SE）**：利用幂律模型（Power-law）对训练损失曲线进行实时预测。如果预测结果不佳，则提前终止实验（Early-stop），从而提高实验吞吐量。
    *   **收敛保证**：通过理论推导证明，在温和假设下，该外部循环是一个随机爬山过程，其最佳性能表现是一个超鞅（Super-martingale），能够概率性地收敛到可达空间内的最优配置。

### 3. 实验设计
*   **场景与数据集**：使用 **nanochat** 预训练基准。数据集为 **FineWeb** 的子集（10B tokens），验证集为 5M tokens。
*   **Benchmark 评估指标**：验证集每字节比特数（**val-bpb**）。该指标与分词器无关，确保了修改词表大小后的公平性。
*   **对比方法**：
    1.  **Human Expert**：手动调优的 GPT-2-small（124M 参数）基准。
    2.  **Random Search**：在预定义超参数网格上的均匀随机采样。
    3.  **Greedy LLM (no RL)**：直接使用 GPT-4o 作为研究智能体（零样本/上下文学习），不进行 RL 微调。
    4.  **AutoResearch-RL (Ours)**：本文提出的 PPO 驱动的完整系统。

### 4. 资源与算力
*   **硬件**：单台 **NVIDIA H100 80GB SXM** GPU。
*   **时间预算**：每个实验固定为 **300 秒（5 分钟）** 的实际运行时间（Wall-clock time），不含编译和加载时间。
*   **训练时长**：
    *   隔夜运行（约 8 小时）：完成约 100 次实验。
    *   长期运行：扩展至 16 小时、48 小时乃至 1 周（完成超过 2100 次实验）。

### 5. 实验数量与充分性
*   **实验规模**：主实验运行了约 100 次迭代，长期实验运行了超过 2000 次迭代。
*   **充分性**：
    *   **消融实验**：对比了有无自评估模块（SE）的效果，证明 SE 提升了 1.35 倍的每小时实验数，最终使样本效率提升 2.4 倍。
    *   **客观性**：通过固定时间预算（Tmax）确保了不同架构（深而窄 vs 浅而宽）在相同算力下的公平竞争。
    *   **多样性**：观察到智能体发现了包括优化器缩放、QK-norm、梯度裁剪调度和深度增加等多种非直观的改进。

### 6. 主要结论与发现
*   **性能超越**：AutoResearch-RL 在 8 小时内达到的 val-bpb（2.681）显著优于人类专家（2.847）和零样本 LLM（2.734）。
*   **持续进化**：随着计算时间的增加（从 8 小时到 1 周），智能体能持续发现更好的配置，尽管边际收益递减，但未表现出停滞。
*   **内化启发式搜索**：相比于纯上下文学习（In-context Learning），RL 允许智能体“内化”研究策略（例如：何时该激进修改，何时该保守微调），避免了长上下文带来的遗忘问题。

### 7. 优点
*   **开放式探索**：不局限于超参数搜索，能够直接修改代码逻辑（如插入新的归一化层或修改优化器算法）。
*   **高效的自评估**：引入 SPRT（序贯概率比检验）和损失预测，极大地减少了在无望实验上的算力浪费。
*   **理论支撑**：为自主研究循环提供了 MDP 形式化定义及收敛性证明，使其不仅是一个工程原型，更具备学术严谨性。

### 8. 不足与局限
*   **算力环境单一**：目前仅在单 GPU 环境下验证，扩展到多节点、多 GPU 的分布式研究环境存在工程挑战。
*   **安全风险**：自主修改并执行代码存在潜在的安全隐患（虽然文中提到了隔离措施，如禁用网络访问和限制文件范围）。
*   **搜索空间限制**：虽然是“开放式”，但仍受限于初始代码库的质量和 LLM 本身的编程能力边界。
*   **评估成本**：虽然单个实验仅 5 分钟，但要达到 SOTA 仍需数百次迭代，对于资源受限的开发者仍有门槛。

（完）
