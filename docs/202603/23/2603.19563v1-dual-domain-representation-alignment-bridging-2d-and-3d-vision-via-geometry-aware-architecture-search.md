---
title: "Dual-Domain Representation Alignment: Bridging 2D and 3D Vision via Geometry-Aware Architecture Search"
title_zh: 双域表示对齐：通过几何感知架构搜索桥接 2D 与 3D 视觉
authors: "Haoyu Zhang, Zhihao Yu, Rui Wang, Yaochu Jin, Qiqi Liu, Ran Cheng"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19563v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对边缘设备的多目标进化架构搜索
tldr: 针对大视觉模型在边缘设备部署时推理成本高、神经架构搜索评估昂贵及子网络排名不一致等挑战，本文提出EvoNAS框架。该框架构建了结合VSS与ViT模块的混合超网络，并采用跨架构双域知识蒸馏策略提升表征能力与排名一致性。此外，引入分布式多模型并行评估显著提高搜索效率。实验证明，搜索得到的EvoNets在多个视觉任务中实现了精度与效率的最优平衡。
selection_source: fresh_fetch
motivation: 旨在解决大视觉模型在资源受限设备上推理成本高，以及进化神经架构搜索中评估开销大和子网络排名不一致的问题。
method: 提出EvoNAS框架，利用VSS-ViT混合超网络、跨架构双域知识蒸馏策略及分布式多模型并行评估机制来优化架构搜索过程。
result: 在COCO、ADE20K等数据集上，EvoNets在严格计算预算下比主流CNN、ViT和Mamba模型具有更低的延迟、更高的吞吐量和更强的泛化能力。
conclusion: EvoNAS通过融合不同架构优势与高效评估机制，成功搜索出在多种下游任务中兼具高性能与高效率的Pareto最优视觉架构。
---

## 摘要
现代计算机视觉要求在预测准确性与实时效率之间取得平衡，然而大型视觉模型（LVMs）的高推理成本限制了其在资源受限的边缘设备上的部署。尽管进化神经架构搜索（ENAS）非常适合多目标优化，但其在实际应用中受到两个问题的阻碍：昂贵的候选评估以及子网络间的排名不一致。为解决这些问题，我们提出了 EvoNAS，一个用于多目标进化架构搜索的高效分布式框架。我们构建了一个集成视觉状态空间与视觉 Transformer（VSS-ViT）模块的混合超网络，并采用跨架构双域知识蒸馏（CA-DDKD）策略对其进行优化。通过将 VSS 模块的计算效率与 ViT 模块的语义表达能力相结合，CA-DDKD 提升了共享超网络的表示能力并增强了排名一致性，从而在进化过程中无需额外微调即可实现可靠的适应度估计。为了降低大规模验证的成本，我们进一步引入了基于 GPU 资源池化和异步调度的分布式多模型并行评估（DMMPE）框架。与传统的数据并行评估相比，DMMPE 通过多 GPU、多模型并发执行，将效率提升了 70% 以上。在 COCO、ADE20K、KITTI 和 NYU-Depth v2 上的实验表明，搜索到的架构（称为 EvoNets）在准确性与效率之间始终实现了帕累托最优权衡。与代表性的基于 CNN、ViT 和 Mamba 的模型相比，EvoNets 在严格的计算预算下提供了更低的推理延迟和更高的吞吐量，同时在诸如新视角合成等下游任务上保持了强大的泛化能力。代码可在 https://github.com/EMI-Group/evonas 获取。

## Abstract
Modern computer vision requires balancing predictive accuracy with real-time efficiency, yet the high inference cost of large vision models (LVMs) limits deployment on resource-constrained edge devices. Although Evolutionary Neural Architecture Search (ENAS) is well suited for multi-objective optimization, its practical use is hindered by two issues: expensive candidate evaluation and ranking inconsistency among subnetworks. To address them, we propose EvoNAS, an efficient distributed framework for multi-objective evolutionary architecture search. We build a hybrid supernet that integrates Vision State Space and Vision Transformer (VSS-ViT) modules, and optimize it with a Cross-Architecture Dual-Domain Knowledge Distillation (CA-DDKD) strategy. By coupling the computational efficiency of VSS blocks with the semantic expressiveness of ViT modules, CA-DDKD improves the representational capacity of the shared supernet and enhances ranking consistency, enabling reliable fitness estimation during evolution without extra fine-tuning. To reduce the cost of large-scale validation, we further introduce a Distributed Multi-Model Parallel Evaluation (DMMPE) framework based on GPU resource pooling and asynchronous scheduling. Compared with conventional data-parallel evaluation, DMMPE improves efficiency by over 70% through concurrent multi-GPU, multi-model execution. Experiments on COCO, ADE20K, KITTI, and NYU-Depth v2 show that the searched architectures, termed EvoNets, consistently achieve Pareto-optimal trade-offs between accuracy and efficiency. Compared with representative CNN-, ViT-, and Mamba-based models, EvoNets deliver lower inference latency and higher throughput under strict computational budgets while maintaining strong generalization on downstream tasks such as novel view synthesis. Code is available at https://github.com/EMI-Group/evonas

---

## 论文详细总结（自动生成）

### 论文总结：Dual-Domain Representation Alignment (EvoNAS)

#### 1. 核心问题与整体含义（研究动机和背景）
现代计算机视觉任务（如自动驾驶、增强现实）需要在预测精度与实时效率之间取得平衡。虽然大型视觉模型（LVMs）表现优异，但其高昂的计算成本限制了在边缘设备上的部署。
*   **研究动机**：现有的进化神经架构搜索（ENAS）虽然适合多目标优化，但面临两大挑战：
    1.  **评估开销巨大**：在大规模验证集上评估大量候选架构极其耗时。
    2.  **排名不一致（表示崩溃）**：在权重共享（Weight-sharing）超网络中，子网络的性能排名与其独立训练时的表现往往不一致，导致搜索结果不可靠。
*   **核心目标**：通过几何感知的架构搜索，桥接 2D 像素预测与 3D 结构渲染，在降低推理成本的同时保持高精度的几何特征提取能力。

#### 2. 论文提出的方法论
论文提出了 **EvoNAS** 框架，包含以下核心技术：
*   **混合 VSS-ViT 超网络**：构建了一个结合视觉状态空间（VSS/Mamba）模块（负责线性时间复杂度的局部几何建模）和视觉 Transformer（ViT）模块（负责全局语义对齐）的搜索空间。
*   **跨架构双域知识蒸馏（CA-DDKD）**：
    *   **空间域对齐**：通过 MSE 损失对齐学生模型与教师模型（如 Depth Anything）的特征图。
    *   **频率域对齐**：利用**离散余弦变换（DCT）**约束高频频谱信号，确保超网络在权重共享时仍能保留锐利的边缘和细微纹理，从而缓解表示崩溃，提高排名一致性。
*   **渐进式超网络训练（PST）**：采用课程学习策略，从最大配置开始逐步解锁搜索维度的约束，确保共享权重的稳定性。
*   **分布式多模型并行评估（DMMPE）**：
    *   利用 GPU 资源池化和异步调度，结合 NVIDIA MPS（多进程服务）实现硬件隔离。
    *   消除因内核干扰导致的延迟抖动（Latency Jitter），确保物理延迟测量的真实性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **2D/2.5D 任务**：COCO（目标检测）、ADE20K（语义分割）、KITTI 和 NYU Depth v2（单目深度估计）。
    *   **3D 任务**：RealEstate10K（新视角合成/3D 高斯泼溅）。
*   **Benchmark 与对比方法**：
    *   对比了主流的 CNN（ResNet, ConvNeXt）、ViT（Swin, PVT）以及新兴的 Mamba 类模型（VMamba, LocalVMamba, Spatial-Mamba）。
    *   在 NAS 领域对比了 UniNet、ViTAS 等方法。

#### 4. 资源与算力
*   **硬件环境**：使用了由 **8 台 NVIDIA RTX 3090 GPU** 和双路 Intel Xeon Gold 5318Y CPU 组成的集群。
*   **训练细节**：
    *   第一阶段：ImageNet-1K 预训练（最大配置 300 轮，随后各维度解锁及对齐）。
    *   第二阶段：下游任务适配（如 MDE 任务 40 轮，语义分割 80k 迭代）。
    *   搜索阶段：种群规模 96，进化 200 代。
*   **效率提升**：DMMPE 框架相比传统数据并行评估，效率提升了 **70% 以上**。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流视觉基准数据集上进行了详尽测试，并扩展到了 3D 渲染应用。
*   **消融实验**：针对 PST 训练策略、CA-DDKD 蒸馏组件、Spatial-Mamba 解码器以及 DMMPE 评估框架分别进行了消融研究。
*   **客观性**：通过 Kendall 秩相关系数（$\tau$）量化了排名一致性（达到 0.8833），并提供了物理延迟与吞吐量的实测数据，而非仅依赖理论 FLOPs，实验设计较为客观、严谨。

#### 6. 论文的主要结论与发现
*   **帕累托最优**：搜索得到的 **EvoNets** 在所有测试任务中均处于准确率-效率曲线的帕累托前沿。
*   **跨维度泛化**：在 2D 任务上搜索到的架构可以直接应用于 3D 高斯泼溅（3DGS），在 RealEstate10K 上以 **12% 的参数量**（44M vs 354M）达到了与 SOTA 模型（DepthSplat）相当的渲染质量。
*   **频率域的重要性**：证明了引入频率域约束是解决权重共享 NAS 中几何特征丢失的关键。

#### 7. 优点
*   **系统性创新**：不仅改进了算法（CA-DDKD），还解决了底层系统评估的噪声问题（DMMPE）。
*   **几何感知**：通过 DCT 引入频率域对齐，使模型在处理边缘和结构化信息时比传统 NAS 模型更具优势。
*   **实用性强**：针对边缘设备优化，提供了真实的延迟测量和极高的参数效率（NID 指标显著领先）。

#### 8. 不足与局限
*   **流程复杂性**：两阶段训练加上复杂的进化搜索流程，对复现者的工程能力要求较高。
*   **教师模型依赖**：CA-DDKD 的效果高度依赖于预训练教师模型（如 Depth Anything）的质量。
*   **硬件偏向性**：虽然 DMMPE 减少了噪声，但搜索出的最优架构可能仍带有特定硬件（RTX 3090）的运行特性偏好，在其他架构（如移动端 ARM GPU）上的表现需进一步验证。

（完）
