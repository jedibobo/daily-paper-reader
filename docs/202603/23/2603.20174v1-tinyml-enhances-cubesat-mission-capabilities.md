---
title: TinyML Enhances CubeSat Mission Capabilities
title_zh: TinyML 增强立方体卫星任务能力
authors: "Luigi Capogrosso, Michele Magno"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20174v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 具有硬件感知算子映射的立方体卫星任务在轨图像分类
tldr: 针对立方体卫星（CubeSat）在地球观测任务中面临的计算、能量和带宽限制，本文提出了一种基于TinyML的卷积神经网络优化与部署流水线。通过结构化剪枝、INT8量化和硬件感知映射，在集成NPU的STM32N6微控制器上实现了高效的星载图像分类。该研究显著降低了模型对内存和功耗的需求，在保持任务可接受精度的前提下，为资源受限的卫星平台提供了实时在轨处理能力。
selection_source: fresh_fetch
motivation: 旨在解决立方体卫星因计算资源、能源和通信带宽极度受限而难以进行在轨实时图像处理的挑战。
method: 开发了一套包含结构化迭代剪枝、INT8量化和硬件感知算子映射的优化流水线，并将其部署在带有神经网络加速器的微控制器上。
result: "模型RAM和Flash占用分别平均减少了89.55%和70.09%，推理延迟缩短至3.22-30.38毫秒，且功耗极低。"
conclusion: 实验证明TinyML技术能够有效提升立方体卫星的在轨分析能力，满足严苛的能量预算和实时性要求。
---

## 摘要
地球观测 (EO) 任务传统上依赖于将原始或经过最低限度处理的图像从卫星传输到地面站，以进行计算密集型分析。由于星载嵌入式处理器、能量可用性和通信带宽的严格限制，这种范式对于立方体卫星 (CubeSat) 系统是不可行的。为了克服这些限制，本文提出了一种基于 TinyML 的卷积神经网络 (ConvNets) 模型优化和部署流水线，用于星载图像分类，从而在立方体卫星级约束下实现准确、节能且硬件感知的推理。我们的流水线集成了结构化迭代剪枝、训练后 INT8 量化和硬件感知算子映射，以压缩模型并使其与意法半导体 (STMicroelectronics) 的 STM32N6 微控制器的异构计算架构相对齐。该微控制器单元 (MCU) 集成了一个新型 Arm Cortex-M55 内核和一个 Neural-ART 神经网络处理器 (NPU)，为立方体卫星星载计算机提供了一个现实的代理。本文在三个 EO 基准数据集（即 EuroSAT、RS_C11、MEDIC）和四个模型（即 SqueezeNet、MobileNetV3、EfficientNet、MCUNetV1）上评估了所提方法。实验结果表明，优化后的模型 RAM 使用量平均减少了 89.55%，Flash 内存减少了 70.09%，在保持任务可接受的准确度（与 Float32 基准相比，下降范围在 0.4 到 8.6 个百分点之间）的同时，显著降低了下行链路带宽需求。单次推理的能耗范围为 0.68 mJ 至 6.45 mJ，延迟跨度为 3.22 ms 至 30.38 ms。这些结果完全满足了高效星载 EO 处理所需的严格能量预算和实时性约束。

## Abstract
Earth observation (EO) missions traditionally rely on transmitting raw or minimally processed imagery from satellites to ground stations for computationally intensive analysis. This paradigm is infeasible for CubeSat systems due to stringent constraints on the onboard embedded processors, energy availability, and communication bandwidth. To overcome these limitations, the paper presents a TinyML-based Convolutional Neural Networks (ConvNets) model optimization and deployment pipeline for onboard image classification, enabling accurate, energy-efficient, and hardware-aware inference under CubeSat-class constraints. Our pipeline integrates structured iterative pruning, post-training INT8 quantization, and hardware-aware operator mapping to compress models and align them with the heterogeneous compute architecture of the STM32N6 microcontroller from STMicroelectronics. This Microcontroller Unit (MCU) integrates a novel Arm Cortex-M55 core and a Neural-ART Neural Processing Unit (NPU), providing a realistic proxy for CubeSat onboard computers. The paper evaluates the proposed approach on three EO benchmark datasets (i.e., EuroSAT, RS_C11, MEDIC) and four models (i.e., SqueezeNet, MobileNetV3, EfficientNet, MCUNetV1). We demonstrate an average reduction in RAM usage of 89.55% and Flash memory of 70.09% for the optimized models, significantly decreasing downlink bandwidth requirements while maintaining task-acceptable accuracy (with a drop ranging from 0.4 to 8.6 percentage points compared to the Float32 baseline). The energy consumption per inference ranges from 0.68 mJ to 6.45 mJ, with latency spanning from 3.22 ms to 30.38 ms. These results fully satisfy the stringent energy budgets and real-time constraints required for efficient onboard EO processing.

---

## 论文详细总结（自动生成）

以下是对论文《TinyML Enhances CubeSat Mission Capabilities》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的地球观测（EO）任务依赖于将原始图像传回地面处理，但立方体卫星（CubeSat）受限于极低的通信带宽（每天仅能传输数十MB）、严苛的功耗预算（2-8W）和极小的内存空间（16-64MB RAM），导致大量采集的数据无法及时下行或处理。
*   **研究动机**：推动从“数据下行”向“在轨计算”的范式转变。通过在轨智能过滤和分类，减少无效数据下行，提升时间敏感型应用（如灾害响应、海事监控）的效率。

### 2. 方法论：TinyML 优化流水线
论文提出了一套针对立方体卫星硬件约束的端到端优化流水线，包含三个关键阶段：
*   **结构化迭代剪枝（Structured Iterative Pruning）**：
    *   基于 L2 范数对卷积层滤波器或全连接层神经元进行重要性排序。
    *   采用三阶段迭代：首先剪枝 10%，随后两次各剪枝 5%，每轮后进行 10 个 Epoch 的微调以恢复精度。
*   **训练后 INT8 静态量化（Post-Training INT8 Quantization）**：
    *   将 32 位浮点数（Float32）权重和激活值转换为 8 位整数（INT8）。
    *   使用校准数据集确定缩放因子（Scale）和零点（Zero-point），利用仿射映射实现压缩。
*   **硬件感知算子映射（Hardware-aware Operator Mapping）**：
    *   针对 STM32N6 的异构架构（Cortex-M55 CPU + Neural-ART NPU），将卷积、深度卷积等算子卸载至 NPU 加速，利用缓冲区复用和算子融合减少内存开销。

### 3. 实验设计
*   **数据集**：涵盖三种不同分辨率和任务的 EO 基准：
    *   **EuroSAT** (64×64)：土地覆盖分类。
    *   **RS_C11** (128×128)：遥感场景识别。
    *   **MEDIC** (224×224)：灾害风险评估。
*   **对比模型**：选取了四种代表性轻量化模型：SqueezeNet、MobileNetV3、EfficientNet 和 MCUNetV1。
*   **基准测试（Benchmark）**：以未压缩的 Float32 模型作为精度基准，对比优化后的 RAM/Flash 占用、推理延迟、能耗及精度损失。

### 4. 资源与算力
*   **训练算力**：所有模型训练和微调均在 **单台 NVIDIA H100 Tensor Core GPU** 上完成。
*   **训练细节**：初始训练 50 个 Epoch（Adam 优化器，学习率 1e-3），剪枝微调阶段每轮 10 个 Epoch（学习率 1e-4）。
*   **部署硬件**：使用 **STM32N6570-DK** 开发板，搭载 800MHz Cortex-M55 内核及 Neural-ART NPU（提供约 600 GOPS 的 INT8 算力）。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 个数据集 × 4 个模型进行了全组合测试，涵盖了从模型压缩到硬件部署的完整流程。
*   **充分性评估**：实验设计较为充分。不仅测量了传统的精度和内存指标，还深入分析了不同分辨率下的能耗（mJ）和延迟（ms），并模拟了“在轨推理+置信度过滤”的混合下行场景，客观展示了 TinyML 对带宽节省的实际贡献。

### 6. 主要结论与发现
*   **极致压缩**：优化后模型 RAM 占用平均减少 **89.55%**，Flash 占用减少 **70.09%**。
*   **高效能表现**：单次推理能耗仅为 **0.68 - 6.45 mJ**，延迟在 **3.22 - 30.38 ms** 之间，远超低轨卫星 5 FPS 的实时性要求。
*   **精度权衡**：精度下降控制在 **0.4 - 8.6 个百分点** 之间，处于任务可接受范围内。
*   **带宽节省**：通过在轨分类并仅传输低置信度样本（混合模式），可减少约 **85.77%** 的下行数据量，同时系统综合精度可提升至 95.2%。

### 7. 优点与亮点
*   **硬件感知度高**：并非单纯的算法压缩，而是紧密结合了 STM32N6 的 NPU 特性进行算子映射优化。
*   **现实代表性强**：选用的硬件和数据集非常贴近当前 1U-3U 立方体卫星的实际资源包络。
*   **正则化发现**：在 MEDIC 数据集上发现剪枝反而提升了精度，证明了剪枝在复杂 EO 数据集上的正则化效应。

### 8. 不足与局限
*   **模型架构局限**：目前流水线仅支持卷积神经网络（ConvNets），尚未覆盖当前流行的 Vision Transformers (ViTs)，因为 NPU 算子支持有限。
*   **环境模拟缺失**：实验在实验室环境下完成，未考虑太空中的单粒子翻转（SEU）等辐射环境对 MCU 推理可靠性的影响。
*   **任务单一**：主要集中在图像分类任务，对于更复杂的语义分割和目标检测（在轨监测更需要的任务）尚待进一步验证。

（完）
