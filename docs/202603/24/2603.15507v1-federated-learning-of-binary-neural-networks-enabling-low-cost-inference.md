---
title: "Federated Learning of Binary Neural Networks: Enabling Low-Cost Inference"
title_zh: 二值神经网络的联邦学习：实现低成本推理
authors: "Nitin Priyadarshini Shankar, Soham Lahiri, Sheetal Kalyani, Saurav Prakash"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15507v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 通过二值神经网络实现边缘设备的低成本推理
tldr: 针对联邦学习在边缘设备推理时面临的计算和内存资源瓶颈，本文提出 FedBNN 框架。该方法通过在本地训练阶段直接学习旋转感知的二值化神经网络（BNN），将 32 位浮点权重压缩为单比特表示。FedBNN 有效解决了传统后训练量化导致的精度损失问题，在显著降低模型体积、推理 FLOPs 和内存需求的同时，保持了与实值联邦学习模型相当的性能，为低功耗边缘设备的隐私保护学习提供了高效方案。
selection_source: fresh_fetch
motivation: 传统深度神经网络在资源受限的边缘设备上推理成本过高，且现有的后训练量化方法会导致严重的精度损失。
method: "提出 FedBNN 框架，在联邦学习的本地训练过程中直接学习旋转感知的二值化权重（+1, -1）。"
result: 在多个基准数据集上显著减少了资源消耗，且模型性能与使用实值权重的联邦学习方法相近。
conclusion: FedBNN 证明了在联邦学习中直接训练二值化模型是实现低成本、高性能边缘推理的有效途径。
---

## 摘要
联邦学习（FL）通过在设备间分布式进行训练来保护隐私。然而，在低功耗边缘设备上，深度神经网络（DNN）在推理阶段具有极高的计算强度。边缘部署要求模型同时优化内存占用和计算效率，而传统 DNN 往往因超出资源限制而难以应对这一困境。传统的训练后二值化虽然能减小模型体积，但由于量化误差，往往面临严重的精度损失。为应对这些挑战，我们提出了 FedBNN，这是一个旋转感知的二值神经网络框架，能够在本地训练过程中直接学习二值表示。通过将每个权重编码为单比特 {+1, -1} 而非 32 位浮点数，FedBNN 缩小了模型占用空间，与使用实值模型的联邦学习方法相比，显著降低了推理时的浮点运算量（FLOPs）和内存需求。在多个基准数据集上的评估表明，FedBNN 在显著降低资源消耗的同时，其性能与现有的使用实值模型的联邦学习方法相当。

## Abstract
Federated Learning (FL) preserves privacy by distributing training across devices. However, using DNNs is computationally intensive at the low-powered edge during inference. Edge deployment demands models that simultaneously optimize memory footprint and computational efficiency, a dilemma where conventional DNNs fail by exceeding resource limits. Traditional post-training binarization reduces model size but suffers from severe accuracy loss due to quantization errors. To address these challenges, we propose FedBNN, a rotation-aware binary neural network framework that learns binary representations directly during local training. By encoding each weight as a single bit $\{+1, -1\}$ instead of a $32$-bit float, FedBNN shrinks the model footprint, significantly reducing runtime (during inference) FLOPs and memory requirements in comparison to federated methods using real models. Evaluations across multiple benchmark datasets demonstrate that FedBNN significantly reduces resource consumption while performing similarly to existing federated methods using real-valued models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Federated Learning of Binary Neural Networks: Enabling Low-Cost Inference》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：联邦学习（FL）虽然解决了数据隐私问题，但训练出的深度神经网络（DNN）在资源受限的边缘设备上进行**推理（Inference）**时，面临巨大的计算量（FLOPs）和内存占用压力。
*   **研究背景**：
    *   传统的实值模型（32-bit）对嵌入式设备太重。
    *   现有的“训练后二值化”方法由于量化误差（实值向量与二值向量间的角度偏差）会导致严重的精度下降。
    *   现有的通信高效型联邦学习（如 FedBAT）虽然压缩了传输数据，但最终部署在设备上的模型往往仍是复杂的实值模型。
*   **整体含义**：本文旨在开发一种名为 **FedBNN** 的框架，通过在联邦学习过程中直接训练**二值神经网络（BNN）**，实现推理侧的极低成本（权重仅占 1-bit），同时保持较高的模型精度。

### 2. 方法论：核心思想与关键技术
FedBNN 的核心思想是结合“旋转感知”优化与联邦学习的聚合机制，减少二值化带来的信息损失。
*   **旋转感知本地训练（Rotation-Aware Training）**：
    *   **减少角度偏差**：引入旋转矩阵 $R$，通过优化使旋转后的实值权重与二值化符号函数（sign）的结果更接近，从而降低量化误差。
    *   **权重融合机制**：提出一个融合公式，将本地权重、服务器全局权重以及旋转后的权重进行凸组合。使用可训练参数 $\lambda, \alpha, \beta$ 动态调节三者的比例，使模型能同时吸收本地特征和全局知识。
*   **训练感知近似（Training-aware Approximation）**：
    *   由于 `sign` 函数不可微，无法直接反向传播。FedBNN 使用一个随训练轮数演进的平滑替代函数。在训练初期提供较多梯度流，后期逐渐逼近 `sign` 函数以确保二值化效果。
*   **服务器端聚合策略**：
    *   服务器维护两套表示：一套是标准的实值权重聚合（用于下发给客户端继续训练）；另一套是**旋转对齐后的聚合**（仅用于服务器端评估和筛选最佳模型）。这种双轨制确保了模型选择的准确性。

### 3. 实验设计
*   **数据集**：涵盖了从简单到复杂的 7 个基准，包括 FMNIST, SVHN, CIFAR-10, Tiny-ImageNet 和 FEMNIST。
*   **数据分布场景**：
    *   **IID**：独立同分布。
    *   **Non-IID 1**：基于 Dirichlet 分布（$\alpha=0.3$）的统计偏斜。
    *   **Non-IID 2**：极端的标签倾斜（每个客户端仅有少量类别的标签）。
*   **Benchmark 与对比方法**：
    *   **FedAvg**：全精度实值模型基准。
    *   **FedBAT**：一种可学习的二值化通信方法。
    *   **FedMUD**：基于低秩分解的通信高效方法。
*   **模型架构**：CNN4, ResNet10, ResNet18, 以及现代化的 ConvNeXt-Tiny。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备了 **2 块 NVIDIA RTX PRO 6000 GPU** 的服务器上运行。
*   **训练参数**：
    *   总客户端数 $N_c = 100$，每轮随机采样 10 个。
    *   本地训练 15 个 epoch（部分数据集为 5 或 10 个）。
    *   总训练轮数（Communication Rounds）通常为 1500 轮（部分简单任务为 500 轮）。
*   **算力说明**：文中未明确给出总的训练时长（小时/天），但给出了详细的超参数设置（学习率 0.1/0.01，Batch Size 64 等）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验，涵盖了 7 个数据集、3 种异构设置、4 种模型架构。
*   **消融实验**：
    *   验证了服务器端对齐（Server Alignment）的重要性。
    *   对比了不同的聚合策略（直接聚合 $\tilde{w}$ 与聚合旋转后的权重）。
    *   对比了“同等复杂度”下的实值模型（证明了 BNN 在极低资源下比缩减版的实值模型更强）。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了精度，还详细量化了推理时的 FLOPs 和内存节省，并考虑了 Non-IID 这一联邦学习的核心挑战。

### 6. 主要结论与发现
*   **极高的资源效率**：相比实值模型，FedBNN 在推理时实现了约 **58 倍的 FLOPs 减少**和 **32 倍的内存节省**。
*   **精度竞争力**：在多数任务中，FedBNN 的精度与全精度 FedAvg 差距在 10% 以内，且显著优于其他二值化基准（如 FedBAT）。
*   **异构鲁棒性**：在 Non-IID 2 极端异构场景下，FedBNN 表现出比 FedAvg 更好的稳定性，甚至在某些架构（如 ConvNeXt-Tiny）上精度反超全精度模型。
*   **二值化鲁棒性**：FedBNN 训练出的模型在直接进行二值化推理时几乎没有精度损失，而传统模型在后训练二值化后精度往往会崩塌（跌至 1%-2%）。

### 7. 优点（亮点）
*   **端到端优化**：将二值化过程嵌入联邦学习循环，而非事后处理，从根本上解决了量化误差问题。
*   **旋转感知创新**：引入旋转矩阵来对齐权重空间，这是一个在中心化 BNN 中有效但在联邦学习中较新颖的应用。
*   **推理友好**：明确区分了通信效率和推理效率，重点解决了边缘设备部署的“最后一公里”问题。

### 8. 不足与局限
*   **精度缺口**：尽管表现优异，但在复杂任务（如 Tiny-ImageNet）上，与全精度模型相比仍存在不可忽视的精度损失。
*   **通信开销微增**：为了实现旋转对齐，客户端需要额外上传旋转矩阵 $R_1, R_2$。虽然文中证明这部分开销小于 1%，但在极端带宽受限的环境下仍需考虑。
*   **任务局限性**：实验主要集中在计算机视觉（CV）任务上，尚未验证其在自然语言处理（NLP）或语音识别等其他领域的有效性。

（完）
