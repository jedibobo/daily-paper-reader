---
title: "SPINONet: Scalable Spiking Physics-informed Neural Operator for Computational Mechanics Applications"
title_zh: SPINONet：用于计算力学应用的可扩展脉冲物理信息神经算子
authors: "Shailesh Garg, Luis Mandl, Somdatta Goswami, Souvik Chakraborty"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21674v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对边缘和嵌入式设备能效的架构感知设计
tldr: 针对计算力学中物理信息算子学习模型的高能耗问题，本文提出SPINONet框架。该框架结合类脑脉冲神经元与可分离架构，通过稀疏、事件驱动的计算模式显著降低能耗，同时保留了计算时空导数所需的连续坐标微分能力。在多种偏微分方程任务中，SPINONet在保持预测精度的同时，展现了卓越的计算效率和在复杂场景下的鲁棒性。
selection_source: fresh_fetch
motivation: 传统物理信息算子学习在边缘设备等功耗受限场景下，因密集网络的重复评估而面临巨大的计算和能源开销。
method: 提出一种架构感知的类脑脉冲算子网络，利用回归友好的脉冲神经元实现稀疏事件驱动计算，并支持物理信息的自动微分。
result: 在多种偏微分方程测试中，SPINONet取得了与传统模型相当的预测精度，并有效减少了计算负载。
conclusion: SPINONet证明了类脑脉冲机制在科学计算中的可行性，为低功耗、高效率的物理信息机器学习提供了新路径。
---

## 摘要
能量效率在部署用于计算力学和科学计算的物理信息算子学习模型时仍然是一个关键挑战，特别是在边缘和嵌入式设备等功耗受限的环境中，稠密网络中重复的算子评估会产生巨大的计算和能量开销。为了应对这一挑战，我们引入了可分离物理信息类脑算子网络（SPINONet），这是一种受神经科学启发的框架，它在减少重复评估中的冗余计算的同时，保持了与物理信息训练的兼容性。SPINONet 通过架构感知设计结合了回归友好的类脑脉冲神经元，实现了稀疏、事件驱动的计算，在提高能量效率的同时，保留了计算时空导数所需的连续、坐标可微路径。我们在代表计算力学问题的一系列偏微分方程（涵盖时变和稳态设置下的空间、时间和参数依赖性）上评估了 SPINONet，并证明了尽管引入了稀疏通信，其预测性能仍可与传统的物理信息算子学习方法相媲美。此外，研究表明，在混合设置中引入有限的数据监督可以提高在挑战性工况下的性能，而纯物理信息训练在这些工况下可能会收敛到伪解。最后，我们通过分析讨论，将 SPINONet 的架构组件和设计选择与计算负载及能耗的降低联系起来。

## Abstract
Energy efficiency remains a critical challenge in deploying physics-informed operator learning models for computational mechanics and scientific computing, particularly in power-constrained settings such as edge and embedded devices, where repeated operator evaluations in dense networks incur substantial computational and energy costs. To address this challenge, we introduce the Separable Physics-informed Neuroscience-inspired Operator Network (SPINONet), a neuroscience-inspired framework that reduces redundant computation across repeated evaluations while remaining compatible with physics-informed training. SPINONet incorporates regression-friendly neuroscience-inspired spiking neurons through an architecture-aware design that enables sparse, event-driven computation, improving energy efficiency while preserving the continuous, coordinate-differentiable pathways required for computing spatio-temporal derivatives. We evaluate SPINONet on a range of partial differential equations representative of computational mechanics problems, with spatial, temporal, and parametric dependencies in both time-dependent and steady-state settings, and demonstrate predictive performance comparable to conventional physics-informed operator learning approaches despite the induced sparse communication. In addition, limited data supervision in a hybrid setup is shown to improve performance in challenging regimes where purely physics-informed training may converge to spurious solutions. Finally, we provide an analytical discussion linking architectural components and design choices of SPINONet to reductions in computational load and energy consumption.

---

## 论文详细总结（自动生成）

这是一份关于论文《SPINONet: Scalable Spiking Physics-informed Neural Operator for Computational Mechanics Applications》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在计算力学和科学计算中，物理信息算子学习（PIOL）模型（如 DeepONet）虽然强大，但在边缘设备或嵌入式系统部署时面临巨大的**能耗和计算开销**。
*   **核心挑战**：传统的神经算子依赖于稠密的连续激活神经元，每次前向评估都会产生大量的乘加运算（MAC）。而受生物启发的脉冲神经网络（SNN）虽然节能，但其离散、不可微的特性与物理信息学习（需要对时空坐标求导以计算 PDE 残差）存在天然矛盾。
*   **整体含义**：本文旨在通过架构创新，将脉冲神经元的稀疏计算优势与物理信息学习的严谨性结合，提出一种可扩展、高能效的神经算子框架。

### 2. 方法论
*   **核心思想**：利用**可分离 DeepONet**的结构特性，将脉冲动力学与物理微分路径进行“解耦”。
*   **关键技术细节**：
    *   **架构分离**：将**变量脉冲神经元（VSN）**仅应用于 **Branch 网络**（负责编码输入函数 $u$）。由于 Branch 网络输出的系数与时空坐标无关，因此不影响 PDE 残差所需的导数计算。
    *   **连续 Trunk 网络**：**Trunk 网络**（负责处理坐标 $\xi$）保持传统的连续、可微激活函数，确保可以通过自动微分精确计算时空导数。
    *   **变量脉冲神经元 (VSN)**：采用一种回归友好的脉冲模型，支持分级、连续值的脉冲输出，解决了传统 SNN 在回归任务中表现不佳的问题。
    *   **前向模式自动微分 (FAD)**：结合可分离架构，利用 FAD 显著降低高维残差计算的内存和计算成本。
    *   **代理梯度训练**：在反向传播中使用平滑函数近似脉冲触发的阶跃函数，实现端到端训练。

### 3. 实验设计
*   **实验场景**：
    1.  **粘性 Burgers 方程**：测试非线性波动和扩散的捕捉能力（1D 空间 + 1D 时间）。
    2.  **带参数扩散的热传导方程**：测试高维参数化问题的处理能力（2D 空间 + 1D 时间 + 1 个物理参数，共 4D 输入）。
    3.  **带参数边界的 Eikonal 方程**：测试对几何输入和符号距离函数的学习能力。
*   **Benchmark 与对比方法**：
    *   **PI-DeepONet**：标准的物理信息 DeepONet（非分离、非脉冲）。
    *   **Vanilla Baseline**：可分离的物理信息 DeepONet（非脉冲）。
    *   **SPINONet**：本文提出的脉冲版本。

### 4. 资源与算力
*   **算力说明**：文中未明确指出具体的 GPU 型号（如 A100 或 H100）及具体的训练总时长。
*   **效率评估指标**：论文通过图表展示了 **GPU 显存占用**和**每轮训练时间（Epoch Time）**随网格分辨率变化的趋势。结果显示 SPINONet 在显存和时间开销上远低于非分离的 PI-DeepONet，且与 Vanilla 可分离模型持平，但具备更高的能效潜力。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低维到高维、从稳态到瞬态的多种 PDE 类型。
*   **充分性评价**：
    *   **多维度对比**：不仅对比了预测精度（L2 误差），还深入分析了神经元脉冲活动率（Sparsity）、计算缩放特性（Scaling）和内存占用。
    *   **消融/鲁棒性分析**：针对 Eikonal 方程，讨论了纯物理驱动可能导致的“伪解”问题，并验证了引入少量数据监督（Hybrid setup）的有效性。
    *   **客观性**：实验设置了统一的隐藏层深度、宽度和优化器参数，确保了对比的公平性。

### 6. 主要结论与发现
*   **精度保持**：SPINONet 在引入稀疏脉冲机制后，预测精度与稠密模型相当（相对 L2 误差保持在较低水平）。
*   **显著稀疏性**：在不同任务中，Branch 网络的平均脉冲活动率仅为 **28% 至 65%**，这意味着可以节省大量的计算功耗。
*   **卓越的可扩展性**：得益于可分离架构，SPINONet 的计算成本随网格分辨率呈线性增长，而非传统模型的指数增长，成功打破了“维度灾难”。
*   **训练稳定性**：通过代理梯度和架构分离，脉冲算子在物理约束下表现出良好的收敛性。

### 7. 优点
*   **架构巧妙**：通过 Branch/Trunk 的功能分离，完美解决了 SNN 不可微与 PINN 需要求导的冲突。
*   **能效潜力**：硬件无关的分析表明，该模型在类脑芯片或支持稀疏计算的硬件上具有极高的能效比。
*   **数学严谨**：提供了关于前向模式自动微分和可分离张量积效率的详细理论推导。

### 8. 不足与局限
*   **硬件依赖性**：虽然理论上节能，但在目前的通用 GPU 架构上，稀疏脉冲的计算优势往往难以完全转化为实际的运行速度提升（GPU 更擅长稠密矩阵运算）。
*   **超参数敏感性**：脉冲神经元的阈值、泄漏率以及代理梯度的斜率等参数可能需要针对不同问题进行精细调节。
*   **精度微降**：在某些复杂任务中，脉冲机制引入的稀疏性导致其精度略低于最顶尖的稠密模型（尽管差距很小）。

（完）
