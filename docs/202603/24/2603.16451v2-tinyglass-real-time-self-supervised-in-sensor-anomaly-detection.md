---
title: "TinyGLASS: Real-Time Self-Supervised In-Sensor Anomaly Detection"
title_zh: TinyGLASS：实时自监督传感器内异常检测
authors: "Pietro Bonazzi, Rafael Sutter, Luigi Capogrosso, Mischa Buob, Michele Magno"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16451v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对资源受限边缘和传感器内平台的轻量化适配
tldr: 针对工业质检中异常检测模型计算量大、难以在边缘或传感器内部署的问题，本文提出 TinyGLASS。它通过将 GLASS 框架的骨干网络替换为轻量级 ResNet-18，并结合 INT8 量化和静态图追踪技术，实现了在 Sony IMX500 智能视觉传感器上的实时运行。该模型在保持高检测精度的同时，显著降低了参数量和功耗，为资源受限环境下的自监督异常检测提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的自监督异常检测模型计算需求过高，无法满足边缘设备及传感器内处理架构的实时性和资源限制要求。
method: 采用轻量化 ResNet-18 替代原有的 WideResNet-50，并引入针对部署优化的静态图追踪与 INT8 量化技术。
result: "在 MVTec-AD 榜单上达到 94.2% 的 AUROC，参数量压缩 8.6 倍，在 IMX500 平台上实现 20 FPS 的实时检测及极低功耗。"
conclusion: TinyGLASS 证明了通过模型轻量化和硬件适配，可以在极低资源消耗下实现高性能、实时的传感器内工业异常检测。
---

## 摘要
异常检测在工业质量控制中起着关键作用，尽管缺乏带标签的故障样本，仍必须识别出缺陷。最近的自监督方法（如 GLASS）仅使用无缺陷数据学习正常的视觉模式，并在工业基准测试中表现出强大的性能。然而，它们的计算需求限制了在资源受限的边缘平台上的部署，在传感器内处理架构中更是如此。本研究介绍了 TinyGLASS，这是 GLASS 框架的一种轻量化改进版本，专为实时边缘和传感器内异常检测而设计。所提出的架构将原始的 WideResNet-50 主干网络替换为紧凑的 ResNet-18，并引入了基于部署的修改，以实现静态图追踪和 INT8 量化。我们在索尼 IMX500 智能视觉传感器上评估了所提方法，并利用索尼模型压缩工具包（Sony Model Compression Toolkit）发挥了传感器内处理器的性能。除了在 MVTec-AD 基准上评估性能外，我们还研究了对受污染训练数据的鲁棒性，并引入了一个名为 MMS 数据集的自定义工业数据集，用于跨设备评估。实验结果表明，TinyGLASS 在保持具有竞争力的检测性能的同时，实现了 8.6 倍的参数压缩，在 MVTec-AD 上达到了 94.2% 的图像级 AUROC，并在 IMX500 平台 8 MB 的内存限制下以 20 FPS 的速度运行。系统分析显示了低功耗（每次推理 4.0 mJ）、实时端到端延迟（20 FPS）和高能效（470 GMAC/J）。此外，该模型在适度水平的训练数据污染下表现出稳定的性能。

## Abstract
Anomaly detection plays a key role in industrial quality control, where defects must be identified despite the scarcity of labeled faulty samples. Recent self-supervised approaches, such as GLASS, learn normal visual patterns using only defect-free data and have shown strong performance on industrial benchmarks. However, their computational requirements limit the deployment on resource-constrained edge platforms, and even more so within in-sensor processing architectures. This work introduces TinyGLASS, a lightweight adaptation of the GLASS framework designed for real-time edge and in-sensor anomaly detection. The proposed architecture replaces the original WideResNet-50 backbone with a compact ResNet-18 and introduces deployment-based modifications that enable static graph tracing and INT8 quantization. We evaluated the proposed approach on the Sony IMX500 intelligent vision sensor, exploiting the in-sensor processor using the Sony Model Compression Toolkit. In addition to evaluating performance on the MVTec-AD benchmark, we investigate robustness to contaminated training data and introduce a custom industrial dataset, named MMS Dataset, for cross-device evaluation. Experimental results show that TinyGLASS achieves 8.6x parameter compression while maintaining competitive detection performance, reaching 94.2% image-level AUROC on MVTec-AD and operating at 20 FPS within the 8 MB memory constraints of the IMX500 platform. System profiling showcases low power consumption (4.0 mJ per inference), real-time end-to-end latency (20 FPS), and high energy efficiency (470 GMAC/J). Furthermore, the model demonstrates stable performance under moderate levels of training data contamination.

---

## 论文详细总结（自动生成）

以下是对论文《TinyGLASS: Real-Time Self-Supervised In-Sensor Anomaly Detection》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：工业质检中的异常检测（AD）通常面临**带标签缺陷样本稀缺**的问题。虽然现有的自监督学习方法（如 GLASS）在基准测试上表现优异，但其巨大的计算开销和内存需求使其难以部署在资源极度受限的边缘平台，尤其是**传感器内处理（In-Sensor Processing）**架构中。
*   **研究动机**：填补高性能异常检测模型与实际工业边缘部署（低成本、实时性、超低功耗）之间的鸿沟，实现首个在智能视觉传感器内部运行的实时异常检测系统。

### 2. 方法论：核心思想与关键技术
TinyGLASS 是对 GLASS 框架的轻量化适配，核心思想是通过架构精简和硬件感知优化实现高效推理：
*   **骨干网络替换**：将原始 GLASS 使用的 WideResNet-50（参数量大、计算重）替换为紧凑的 **ResNet-18**。为了进一步减少开销，仅保留并执行到 Layer 3 的层，将特征维度从 1536 维降至 384 维。
*   **部署感知修改**：
    *   **静态图追踪**：移除 PyTorch hooks 等动态操作，修改前向传播逻辑以直接返回所需特征层，确保兼容硬件加速器的静态图要求。
    *   **算子优化**：消除动态张量变形（Reshape）和不支持的操作，全程保持 4D 张量表示，以便使用标准 Conv2D 算子。
    *   **量化**：利用 Sony Model Compression Toolkit (MCT) 将权重和激活值统一量化为 **INT8**。
*   **核心架构**：包含特征提取器、特征适配器和判别器。利用全局异常分数（GAS）捕捉图像级偏差，利用局部异常分数（LAS）捕捉细微的局部缺陷。
*   **损失函数**：结合了二元交叉熵损失（LBCE）和 Focal Loss（Lfocal），以应对正常区域与缺陷区域之间的类别不平衡。

### 3. 实验设计
*   **数据集**：
    1.  **MVTec-AD**：工业异常检测的标准基准，包含 15 类工业产品。
    2.  **MMS Dataset（自定义）**：作者引入的新数据集，包含微型组件的裂纹、划痕等缺陷，分别使用高分辨率显微镜和 Sony IMX500 传感器采集，用于评估跨设备性能。
*   **基准对比**：主要与原始 GLASS 模型进行对比，评估参数压缩率、图像级/像素级 AUROC。
*   **鲁棒性测试**：通过在训练集中注入 5% 到 30% 的异常样本，模拟现实中训练数据受污染的情况。

### 4. 资源与算力
*   **训练算力**：使用了 **3 台 NVIDIA RTX A6000 GPU** 进行训练。
*   **训练配置**：最大 200 个 Epoch，Batch Size 为 8，使用 AdamW 优化器。
*   **推理平台**：目标部署平台为 **Sony IMX500 智能视觉传感器**（集成神经网络加速器，8MB 内存限制），通过 Raspberry Pi 5 作为主机进行结果聚合。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 MVTec-AD 的 15 个类别上进行了完整测试。
    *   在自定义 MMS 数据集上进行了跨设备验证。
    *   针对“地毯（carpet）”类别和 MMS 数据集进行了 5 组不同比例（0%-30%）的训练集污染消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了精度、速度、功耗、鲁棒性及跨数据集泛化性。对比了量化前后的性能损失，客观展示了轻量化带来的代价。

### 6. 主要结论与发现
*   **高效压缩**：TinyGLASS 实现了 **8.6 倍** 的参数压缩（从 24.9M 降至 2.9M）。
*   **性能平衡**：在 MVTec-AD 上达到 **94.2% 的图像级 AUROC**，相比原版仅下降 4.9%，但在资源消耗上大幅领先。
*   **实时低功耗**：在 IMX500 传感器上实现了 **20 FPS** 的实时推理，每次推理功耗仅 **4.0 mJ**，能效比高达 470 GMAC/J。
*   **鲁棒性**：模型在 10% 以内的中度数据污染下仍能保持相对稳定的检测性能。

### 7. 优点
*   **首创性**：首次展示了在传感器内部（In-Sensor）实现实时视觉异常检测的可能性。
*   **硬件友好**：针对特定硬件约束（内存、算子支持、静态图）进行了深度定制，而非简单的模型剪枝。
*   **实用性强**：引入了 MMS 数据集，考虑了工业现场常见的训练数据污染问题，具有很强的工程参考价值。

### 8. 不足与局限
*   **性能损失**：虽然压缩率高，但在像素级定位精度（P-AUROC）上相比原模型有较明显下降（约 7.4%），对于极微小缺陷的定位可能不够精确。
*   **硬件绑定**：部分优化手段（如 Sony MCT 工具链的使用）与特定硬件平台（IMX500）绑定较深，迁移到其他边缘芯片（如瑞萨或意法半导体）可能需要重新适配。
*   **污染实验覆盖面**：鲁棒性实验仅在部分类别上进行，未覆盖 MVTec-AD 的所有 15 个类别，结论的普适性有待进一步验证。

（完）
