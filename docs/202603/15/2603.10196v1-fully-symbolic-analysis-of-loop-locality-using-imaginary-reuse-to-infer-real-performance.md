---
title: "Fully Symbolic Analysis of Loop Locality: Using Imaginary Reuse to Infer Real Performance"
title_zh: 循环局部性的全符号化分析：利用虚重用推断实际性能
authors: "Yifan Zhu, Yekai Pan, Chen Ding, Yanghui Wu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10196v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 循环局部性与性能推断的编译器支持
tldr: "本研究提出了一种全新的全符号化局部性分析理论及其编译器支持，旨在解决仿射循环嵌套的缓存性能预测问题。该理论通过将局部性推导为多项式，能够以二次和倒数表达式描述缓存性能随输入规模和配置的变化。实验表明，该方法在41个科学计算内核中实现了99.6%的预测准确率，且在推导完成后，预测速度极快，显著优于传统的经验缩放规则。"
selection_source: fresh_fetch
motivation: 传统的经验性缩放规则在预测循环嵌套的缓存性能时缺乏通用性和精确性。
method: 提出一种全符号化分析方法，将局部性建模为多项式，并支持仿射循环嵌套的性能推导。
result: "在41个基准测试中，该方法实现了99.6%的数据移动预测准确率，且单次预测耗时不足1毫秒。"
conclusion: 该全符号化理论为编译器优化提供了高效且精确的局部性分析手段，能够准确推断不同配置下的实际性能。
---

## 摘要
本文提出了一种新的局部性理论及其编译器支持。该理论是全符号化的，将局部性推导为多项式，且编译器分析支持仿射循环嵌套。它们以二次项和倒数表达式的形式推导出缓存性能缩放，比经验缩放规则更具通用性和精确性。在包含 41 个科学计算内核和张量操作的基准测试集上进行评估，编译器平均需要 41 秒来推导局部性多项式。推导完成后，预测任何给定输入规模和缓存配置下的缓存缺失次数所需时间不到一毫秒。在所有测试中（无论是否进行循环融合），与模拟的组相联 L1 数据缓存相比，数据移动预测的准确率达到了 99.6%。

## Abstract
This paper presents a new theory of locality and its compiler support. The theory is fully symbolic and derives locality as polynomials, and the compiler analysis supports affine loop nests. They derive cache-performance scaling in quadratic and reciprocal expressions and are more general and precise than empirical scaling rules.   Evaluated on a benchmark suite of 41 scientific kernels and tensor operations, the compiler requires an average of 41 seconds to derive the locality polynomials. After derivation, predicting the cache miss count for any given input size and cache configuration takes less than a millisecond. Across all tests--with and without loop fusion--the accuracy in the data movement prediction is 99.6\%, compared to simulated set-associative L1 data cache.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fully Symbolic Analysis of Loop Locality: Using Imaginary Reuse to Infer Real Performance》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在编译器层面实现对循环程序局部性（Locality）的**全符号化（Fully Symbolic）**分析，从而在不进行实际运行或大规模模拟的情况下，精确预测不同输入规模和缓存配置下的缓存缺失（Cache Miss）情况。
*   **研究动机**：
    *   **现有方法的局限**：传统的经验缩放规则（如 $\sqrt{2}$ 规则）不够精确；而现有的符号分析技术（如基于整数集方程的方法）通常只能处理线性关系，无法推导出包含二次项或倒数项的复杂多项式，且在处理空间局部性时往往需要固定缓存块大小。
    *   **性能预测需求**：现代数据密集型应用（如张量运算、科学计算）迫切需要一种能够随程序参数（如循环边界 $n$）和机器参数（如缓存大小 $C$、块大小 $b$）自动调整的代数模型。

### 2. 论文提出的方法论：代数局部性理论
论文提出了一种名为“代数局部性（Algebraic Locality）”的新理论，核心步骤如下：
*   **虚重用（Imaginary Reuse）**：这是解决“冷启动缺失困境”的关键。传统分析将首次访问视为无限重用间隔（RI），导致工作集大小发散。论文引入“无限重复执行”假设，将首次访问转化为跨轮次的“虚重用”，从而赋予其有限的 RI 值，使符号化推导成为可能。
*   **丹宁递归（Denning Recursion）的符号化应用**：利用重用间隔分布 $P(ri)$，通过符号化迭代推导出缺失率多项式 $m(x)$ 和工作集大小多项式 $s(x)$。
*   **编译器分析流程**：
    1.  将 MLIR 的 Affine 方言转换为参数化多面体（Parametric Polytopes）。
    2.  使用整数集编程（ISP）和 **Barvinok 计数算法** 计算重用间隔的分布，生成分段拟多项式（Piecewise Quasi-polynomials）。
    3.  通过重参数化技术聚合相同 RI 值的出现次数。
*   **RI 和不变性测试（RI Sum Invariance）**：提出了一种符号化测试方法，通过验证 RI 值与比例的点积是否等于总数据量来检查推导的正确性。

### 3. 实验设计
*   **数据集/场景**：
    *   **Polybench**：30 个经典的科学计算内核（如矩阵乘法、线性代数运算）。
    *   **Einsum Benchmark**：11 个常用的张量操作内核（常用于机器学习和量子电路模拟）。
*   **对比基准（Benchmark）**：
    *   **Cachegrind 模拟器**：模拟全相联（FA）和 12 路组相联（12WA）的 L1 数据缓存。
    *   **硬件计数器**：在实际硬件上通过性能监控单元（PMU）获取真实缺失数据。
*   **变量控制**：测试了原始代码以及经过 MLIR 循环融合（Loop Fusion）优化后的代码。

### 4. 资源与算力
*   **分析设备**：使用 64 核 AMD EPYC 7773X 处理器进行多项式推导。
*   **验证设备**：使用 Nvidia GB10 (Cortex-X925 核心) 的硬件性能计数器进行实测对比。
*   **耗时**：
    *   **模型构建**：平均耗时约 41 秒（复杂张量内核可能更长）。
    *   **预测耗时**：一旦多项式生成，预测特定配置下的缺失数耗时**小于 1 毫秒**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 41 个不同的内核，针对每个内核测试了多种缓存大小（6KB, 12KB, 48KB）以及不同的关联度（全相联 vs 组相联）。
*   **充分性**：实验不仅对比了模拟器，还对比了真实硬件数据；不仅分析了原始程序，还验证了编译器优化（循环融合）对分析准确性的影响。总计进行了超过 180 组对比实验，覆盖了从简单循环到 12 层嵌套循环的复杂场景，实验设计客观且充分。

### 6. 论文的主要结论与发现
*   **高准确性**：在所有测试中，数据移动预测的准确率达到了 **99.6%**。
*   **代数优势**：成功推导出包含二次项和倒数项的缓存性能缩放公式，证明了其比传统的经验规则（如 $\sqrt{2}$ 规则）更精确。例如，它能区分在相同缩放规则下，缺失率可能存在的倍数级差异。
*   **效率**：虽然推导过程（Construction）需要数十秒，但其“一次推导，到处预测”的特性使其在自动调优和空间搜索中极具竞争力。

### 7. 优点
*   **全符号化**：首次实现了将缓存块大小、循环边界、缓存容量全部作为符号变量的代数分析。
*   **理论创新**：通过“虚重用”巧妙解决了冷启动缺失在符号化建模中的发散问题。
*   **实用性强**：集成在 MLIR 框架中，能够直接支持现代编译器流水线，且预测速度极快。

### 8. 不足与局限
*   **移动悬崖误差（Moving-cliff Error）**：在具有多个阶段（Phases）的复杂内核中，模型可能会低估导致缺失率下降所需的缓存大小（预测的下降点早于实际点）。
*   **计算复杂度**：虽然在基准测试中表现良好，但理论上推导 RI 分布是 **NP-hard** 的，对于极高维度的嵌套循环或极其复杂的访问模式，分析时间可能会显著增加。
*   **适用范围**：目前主要针对**仿射（Affine）循环**，对于非仿射访问（如间接寻址 `A[B[i]]`）或高度动态的控制流支持有限。

（完）
