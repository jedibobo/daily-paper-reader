---
title: "HiAP: A Multi-Granular Stochastic Auto-Pruning Framework for Vision Transformers"
title_zh: HiAP：一种用于 Vision Transformer 的多粒度随机自动剪枝框架
authors: "Andy Li, Aiden Durrant, Milan Markovic, Georgios Leontidis"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12222v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于硬件高效子网络的自动剪枝框架
tldr: 针对 Vision Transformers (ViTs) 部署受限的问题，本文提出 HiAP 框架。该框架通过连续松弛技术，在单一端到端训练阶段自动发现最优子网络。HiAP 引入多粒度随机门控机制，在宏观层面剪枝注意力头和 FFN 块，在微观层面剪枝内部维度和神经元，结合 FLOPs 约束优化，显著简化了剪枝流程并提升了模型在边缘设备上的运行效率。
selection_source: fresh_fetch
motivation: 现有的 ViT 结构化剪枝方法通常受限于单一粒度且依赖复杂的多阶段流程，难以在计算和内存带宽之间取得最佳平衡。
method: 提出一种多粒度随机自动剪枝框架，利用 Gumbel-Sigmoid 门控在宏观和微观层面同时优化，并结合 FLOPs 惩罚项实现端到端训练。
result: 在 ImageNet 实验中，HiAP 在 DeiT-Small 等模型上实现了极具竞争力的准确率与效率平衡，达到了与复杂多阶段方法相当的性能。
conclusion: HiAP 通过多粒度联合优化简化了 ViT 剪枝流程，为在资源受限设备上部署高效 Transformer 模型提供了一种简洁且强大的方案。
---

## 摘要
Vision Transformer 需要大量的计算资源和内存带宽，这严重限制了它们在边缘设备上的部署。虽然近期的结构化剪枝方法成功降低了理论 FLOPs，但它们通常在单一结构粒度下运行，并依赖复杂的多阶段流水线及事后阈值处理来满足稀疏度预算。在本文中，我们提出了分层自动剪枝（HiAP），这是一种连续松弛框架，能够在单个端到端训练阶段发现最优子网络，而无需手动的重要性启发式规则或预定义的逐层稀疏度目标。HiAP 在多个粒度上引入了随机 Gumbel-Sigmoid 门：宏观门用于剪枝整个注意力头和 FFN 块，微观门用于选择性地剪枝头内维度和 FFN 神经元。通过同时优化这两个层级，HiAP 解决了加载大矩阵带来的内存受限开销以及数学运算带来的计算受限问题。HiAP 利用结合了结构可行性惩罚和解析 FLOPs 的损失函数，自然地收敛至稳定的子网络。在 ImageNet 上的广泛实验表明，HiAP 有机地发现了高效架构，并在 DeiT-Small 等模型上实现了具有竞争力的准确率-效率帕累托前沿，在显著简化部署流程的同时，达到了复杂多阶段方法的性能。

## Abstract
Vision Transformers require significant computational resources and memory bandwidth, severely limiting their deployment on edge devices. While recent structured pruning methods successfully reduce theoretical FLOPs, they typically operate at a single structural granularity and rely on complex, multi-stage pipelines with post-hoc thresholding to satisfy sparsity budgets. In this paper, we propose Hierarchical Auto-Pruning (HiAP), a continuous relaxation framework that discovers optimal sub-networks in a single end-to-end training phase without requiring manual importance heuristics or predefined per-layer sparsity targets. HiAP introduces stochastic Gumbel-Sigmoid gates at multiple granularities: macro-gates to prune entire attention heads and FFN blocks, and micro-gates to selectively prune intra-head dimensions and FFN neurons. By optimizing both levels simultaneously, HiAP addresses both the memory-bound overhead of loading large matrices and the compute-bound mathematical operations. HiAP naturally converges to stable sub-networks using a loss function that incorporates both structural feasibility penalties and analytical FLOPs. Extensive experiments on ImageNet demonstrate that HiAP organically discovers highly efficient architectures, and achieves a competitive accuracy-efficiency Pareto frontier for models like DeiT-Small, matching the performance of sophisticated multi-stage methods while significantly simplifying the deployment pipeline.

---

## 论文详细总结（自动生成）

以下是对论文《HiAP: A Multi-Granular Stochastic Auto-Pruning Framework for Vision Transformers》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
Vision Transformers (ViTs) 虽然性能卓越，但其巨大的计算开销（FLOPs）和内存带宽需求限制了在边缘设备上的部署。
*   **现有挑战**：
    *   **粒度单一**：多数方法仅针对微观结构（如神经元）或宏观结构（如注意力头）进行剪枝。微观剪枝虽能降计算量，但无法减少内存访问开销；宏观剪枝虽能提速，但易损害模型表达能力。
    *   **流程复杂**：现有的结构化剪枝通常依赖多阶段流水线（搜索、剪枝、微调），且需要手动设置重要性启发式规则或预定义每层稀疏度。
*   **研究目标**：开发一种**单阶段、端到端**的自动剪枝框架，能够同时在宏观和微观层面优化，平衡计算受限（Compute-bound）和内存受限（Memory-bound）的瓶颈。

### 2. 方法论：核心思想与关键技术
HiAP 提出了一种基于**分层随机门控**的连续松弛框架：
*   **多粒度门控机制**：
    *   **宏观门 (Macro-gates)**：控制整个注意力头（Attention Head）和 FFN 块的保留与否，直接减少内存加载开销。
    *   **微观门 (Micro-gates)**：在保留的宏观结构内部，选择性剪枝头内维度（Intra-head dimensions）和 FFN 神经元，精细化调整计算量。
*   **关键技术细节**：
    *   **Gumbel-Sigmoid 采样**：将离散的剪枝决策转化为可微的连续变量，允许通过梯度下降进行端到端优化。
    *   **解析式 MACs 成本建模**：将网络成本分解为静态开销和由门控控制的动态成本，并直接集成到损失函数中。
    *   **损失函数设计**：$L_{total} = L_{task} + \lambda_{macro} L_{macro} + \lambda_{micro} L_{micro} + L_{feasibility}$。其中 $L_{feasibility}$ 是结构可行性惩罚，用于防止“层崩溃”（即某一层被完全剪掉导致梯度断裂）。
    *   **单阶段退火策略**：通过逐渐降低 Gumbel-Sigmoid 的温度参数 $\tau$，使门控从早期的随机探索平滑过渡到后期的确定性二值决策，实现权重与结构的协同进化。

### 3. 实验设计
*   **数据集**：ImageNet-1K（大规模验证）、CIFAR-10（消融实验与延迟分析）。
*   **基准模型 (Backbone)**：DeiT-Small (4.6G MACs) 和自定义的 ViT-Tiny。
*   **对比方法**：包括 WDPruning, S2ViT, ViT-Slim, GOHSP, NViT, SAViT 等多种 SOTA 结构化剪枝方法。
*   **评估指标**：Top-1 准确率、参数量、MACs（计算量）以及实际硬件延迟。

### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，全局 Batch Size 为 256，总计训练 200 个 Epoch。
*   **硬件提及**：文中提到在“单个 GPU”上进行了推理延迟分析（Batch Size=1），但**未明确说明**用于 ImageNet 大规模训练的具体 GPU 型号（如 A100 或 V100）及总算力消耗。
*   **效率提升**：强调了 HiAP 是单阶段完成，相比需要多次微调的传统方法，显著节省了总训练时间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ImageNet 上进行了不同压缩率的对比实验。
    *   在 CIFAR-10 上进行了针对不同剪枝启发式规则（如 L1 范数）的对比。
    *   **消融实验**：详细分析了宏观与微观惩罚比例（$\lambda$）对帕累托前沿的影响，展示了不同比例下网络拓扑的演变过程。
*   **充分性评价**：实验设计较为全面，涵盖了从理论计算量到实际硬件延迟的转化，且通过可视化门控状态验证了算法的自动发现能力。对比基线均为近年来的主流方法，实验结果具有客观性。

### 6. 主要结论与发现
*   **性能优异**：在 DeiT-Small 上，HiAP 在减少约 33% MACs 的情况下，保持了 79.1% 的 Top-1 准确率，达到了与复杂多阶段方法（如 ViT-Slim）相当的水平。
*   **自动发现拓扑**：HiAP 能够自动识别冗余，例如它倾向于在早期训练中关闭深层的整个 FFN 块，而在保留的块中精细修剪神经元。
*   **硬件加速**：在 CIFAR-10 实验中，33.1% 的剪枝率带来了约 1.44 倍的实际推理加速，证明了该结构化剪枝方案在标准硬件上的有效性。

### 7. 优点（亮点）
*   **简洁高效**：将搜索、剪枝和训练统一在一个阶段，无需手动设计重要性指标或复杂的图搜索算法。
*   **多粒度协同**：同时考虑内存带宽（宏观）和计算量（微观），更符合现代硬件的性能瓶颈特征。
*   **数学严谨性**：引入了可行性惩罚项解决可微剪枝中常见的层崩溃问题，并提供了成本分解的理论证明。

### 8. 不足与局限
*   **目标函数偏差**：目前主要优化目标是 MACs（理论计算量），虽然 MACs 与延迟相关，但并非完全线性。未来若能引入平台感知的延迟模型（Latency-aware）会更具实用性。
*   **任务覆盖**：实验主要集中在图像分类任务，尚未在目标检测或分割等下游任务中验证其泛化能力。
*   **硬件细节缺失**：论文未详细列出训练阶段的算力资源清单，不便于其他研究者评估其训练成本。

（完）
