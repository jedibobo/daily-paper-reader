---
title: Evaluating Four FPGA-accelerated Space Use Cases based on Neural Network Algorithms for On-board Inference
title_zh: 评估基于神经网络算法的四种FPGA加速星载推理空间应用场景
authors: "Pedro Antunes, Muhammad Ihsan Al Hafiz, Jonah Ekelund, Ekaterina Dineva, George Miloshevich, Panagiotis Gonidakis, Artur Podobas"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14091v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对星载推理的FPGA加速空间应用案例
tldr: 本研究针对空间任务中传感器数据量过大的挑战，在AMD ZCU104 FPGA平台上评估了四种神经网络应用。通过对比Vitis AI和Vitis HLS两种实现方式，展示了FPGA在提升推理速度（最高34.16倍）和降低能耗方面的显著优势，证明了星载FPGA加速神经网络在缓解下行链路压力方面的巨大潜力。
selection_source: fresh_fetch
motivation: 空间任务中高保真传感器产生的数据量巨大，超出了星载存储和下行链路的传输能力。
method: 在AMD ZCU104平台上，利用Vitis AI和Vitis HLS工具链对四种空间应用场景的神经网络推理性能进行量化评估。
result: Vitis AI实现的推理速率比ARM CPU高出34.16倍，而HLS设计在实现加速的同时支持了DPU不支持的自定义算子。
conclusion: FPGA加速神经网络可实现高效的星载数据处理，显著降低能耗并缓解未来空间任务的通信链路压力。
---

## 摘要
空间任务越来越多地部署高保真传感器，产生的数据量超过了星载缓冲和下行链路容量。本研究在AMD ZCU104开发板上评估了四种空间应用场景下神经网络（NN）的FPGA加速性能。我们使用Vitis AI (AMD DPU)和Vitis HLS实现推理，量化吞吐量和能耗，并揭示了与部署相关的工具链和架构限制。Vitis AI实现的推理速率比嵌入式ARM CPU基准高出多达34.16倍，而定制的HLS设计实现了高达5.4倍的加速，并增加了对DPU中缺失的算子（如sigmoid、3D层）的支持。对于这些实现，测得的MPSoC推理功耗在1.5至6.75 W之间，在所有应用场景中均降低了单次推理能耗（相较于CPU执行）。这些结果表明，神经网络FPGA加速可以实现星载滤波、压缩和事件检测，从而缓解未来任务中的下行链路压力。

## Abstract
Space missions increasingly deploy high-fidelity sensors that produce data volumes exceeding onboard buffering and downlink capacity. This work evaluates FPGA acceleration of neural networks (NNs) across four space use cases on the AMD ZCU104 board. We use Vitis AI (AMD DPU) and Vitis HLS to implement inference, quantify throughput and energy, and expose toolchain and architectural constraints relevant to deployment. Vitis AI achieves up to 34.16$\times$ higher inference rate than the embedded ARM CPU baseline, while custom HLS designs reach up to 5.4$\times$ speedup and add support for operators (e.g., sigmoids, 3D layers) absent in the DPU. For these implementations, measured MPSoC inference power spans 1.5-6.75 W, reducing energy per inference versus CPU execution in all use cases. These results show that NN FPGA acceleration can enable onboard filtering, compression, and event detection, easing downlink pressure in future missions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluating Four FPGA-accelerated Space Use Cases based on Neural Network Algorithms for On-board Inference》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：现代空间任务（近地或深空探测）部署了高保真传感器，产生的数据量远超星载存储和下行链路的传输能力。
*   **核心挑战**：传统的抗辐射通用处理器（CPU）在处理计算密集型的神经网络（NN）时，性能和能效比极低，无法满足实时处理需求。
*   **研究目标**：评估利用 FPGA 硬件加速神经网络推理的可行性，以实现星载数据的原位分析（如过滤、压缩、预报），从而仅传输提炼后的关键结果，缓解通信压力。

### 2. 方法论与关键技术
论文采用了两种互补的工具链和技术路径：
*   **Vitis AI (基于 DPU)**：
    *   **核心思想**：使用 AMD 的深度学习处理器单元（DPU）IP 核，针对卷积神经网络（CNN）进行高度优化。
    *   **技术细节**：采用 8 位整数（INT8）量化（包括 PTQ 后量化和 QAT 量化感知训练），通过架构并行性实现高吞吐量。
    *   **适用场景**：标准 CNN 结构（如 VAE 编码器、CNetPlusScalar）。
*   **Vitis HLS (高层次综合)**：
    *   **核心思想**：将 C/C++ 代码直接转换为 RTL 硬件描述语言，提供极高的灵活性。
    *   **技术细节**：支持 DPU 不支持的特殊算子（如 Sigmoid、3D 卷积、3D 池化、比较器）；保持 IEEE-754 单精度浮点（FP32）运算，确保数值精度。
    *   **适用场景**：非标准或轻量级模型（如 ESPERTA、MMS 3D 网络）。

### 3. 实验设计
*   **应用场景与数据集**：
    1.  **VAE Encoder**：太阳矢量磁图压缩（压缩比 1:16,384）。
    2.  **CNetPlusScalar**：多模态太阳图像回归，预测 X 射线通量。
    3.  **ESPERTA**：太阳高能粒子（SEP）事件早期预报。
    4.  **MMS Neural Networks**：利用 3D 离子能量分布数据进行等离子体区域分类（包含 BaselineNet, ReducedNet, LogisticNet）。
*   **基准测试（Benchmark）**：以嵌入式 ARM Cortex-A53 CPU（ZCU104 上的处理系统 PS 部分）的执行性能为基准。
*   **对比维度**：推理延迟、吞吐量（FPS）、资源利用率（LUT/DSP/BRAM）、功耗（整板功耗 vs MPSoC 功耗）以及单次推理能耗。

### 4. 资源与算力
*   **硬件平台**：AMD ZCU104 开发板（搭载 ZU7EV MPSoC，含 504k 逻辑单元、1728 个 DSP 切片）。
*   **算力细节**：
    *   DPU 配置为 B4096，运行频率最高 600MHz。
    *   HLS 加速器运行频率为 100MHz。
*   **训练说明**：论文重点在于**推理（Inference）**评估，未详细说明模型训练所用的 GPU 资源（模型是在离线环境下训练好后部署到 FPGA 的）。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个不同的空间应用场景，共测试了 6 种不同的神经网络拓扑结构。
*   **充分性评价**：实验设计较为全面，涵盖了从极简模型（24 个参数）到复杂模型（300 万参数）的跨度。通过对比 CPU、DPU 和 HLS 三种实现方式，客观地展示了不同工具链在算子支持、精度和速度之间的权衡（Trade-off）。

### 6. 主要结论与发现
*   **性能大幅提升**：Vitis AI 在大型 CNN 模型上表现卓越，CNetPlusScalar 的推理速度比 CPU 快 **34.16 倍**。
*   **能效比显著优化**：所有 FPGA 实现均降低了单次推理能耗。例如，VAE 编码器的能耗降低了约 11.5 倍，CNetPlusScalar 降低了 13.91 倍。
*   **工具链互补性**：
    *   **DPU** 速度极快但算子受限（不支持 3D 层和 Sigmoid）。
    *   **HLS** 灵活性高，能支持 3D 卷积并保持高精度，但在处理深度网络时，若不进行深度并行优化，其性能可能低于 CPU（如 BaselineNet）。
*   **功耗表现**：MPSoC 推理功耗在 1.5W 至 6.75W 之间，完全符合中小型卫星的功耗预算。

### 7. 论文优点
*   **实战导向**：选取的四个案例均具有极强的空间任务背景，而非通用的 AI 数据集。
*   **异构对比清晰**：详细对比了 DPU 的“黑盒”高效优化与 HLS 的“白盒”灵活定制，为未来星载 AI 硬件选型提供了参考。
*   **精度验证**：在 HLS 实验中验证了 FP32 精度与 CPU 结果的一致性（误差 $\le 10^{-10}$），这对科学任务至关重要。

### 8. 不足与局限
*   **HLS 优化不足**：论文中的 HLS 实现采用了“幼稚（Naive）”的翻译方式，未充分利用流水线（Pipeline）和展开（Unroll）等指令，导致在复杂模型上性能不佳。
*   **量化损失**：Vitis AI 的 INT8 量化导致了明显的精度下降，虽然提到 QAT 可以缓解，但未给出详细的精度损失量化数据。
*   **环境模拟局限**：实验在开发板上完成，未考虑真实空间环境中的极端温度循环和高能粒子翻转（SEU）对推理可靠性的影响。

（完）
