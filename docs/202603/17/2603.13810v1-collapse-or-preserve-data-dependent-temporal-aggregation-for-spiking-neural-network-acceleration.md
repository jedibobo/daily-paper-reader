---
title: "Collapse or Preserve: Data-Dependent Temporal Aggregation for Spiking Neural Network Acceleration"
title_zh: 折叠或保留：面向脉冲神经网络加速的数据依赖型时间聚合
authors: Jiahao Qin
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13810v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 利用SIMD架构效率的时间聚合卷积
tldr: 针对脉冲神经网络（SNN）在GPU上因稀疏性难以加速的问题，本文提出时间聚合卷积（TAC）及其改进版TAC-TP。TAC通过在卷积前聚合多个脉冲帧来减少计算次数，而TAC-TP则在减少卷积调用的同时保留时间分辨率。研究发现，对于率编码数据应采用时间折叠以降噪提速，而对于事件驱动数据则需保留时间维度以维持精度。该方法在多种硬件上实现了显著加速，并已开源。
selection_source: fresh_fetch
motivation: 传统的细粒度非结构化脉冲稀疏性无法在SIMD架构（如GPU）上实现有效加速。
method: 提出TAC方法预聚合脉冲帧以减少卷积调用，并针对事件数据设计TAC-TP以在减少计算的同时保留时间分辨率。
result: "在MNIST上实现13.8倍加速且精度提升，在DVS128-Gesture上以一半的卷积调用维持了95.1%的高精度。"
conclusion: 脉冲神经网络的加速策略应根据数据类型动态调整：率编码数据适合时间折叠，而事件数据则需保留时间信息。
---

## 摘要
普遍认为脉冲稀疏性能够实现脉冲神经网络（SNN）在 GPU 硬件上的高效推理。我们证明这是一种错觉：在 Apple M3 Max 上的五种不同的稀疏计算策略均未能超越稠密卷积，因为 SIMD 架构无法利用独立同分布（i.i.d.）二进制脉冲的细粒度、非结构化稀疏性。相反，我们提出了时间聚合卷积（TAC），它利用卷积的线性性质，在单次卷积调用前预聚合 $K$ 个脉冲帧，从而将 $T$ 次调用减少到 $T/K$ 次。在率编码（rate-coded）数据上，TAC 在 MNIST 上实现了 13.8 倍的加速和 +1.6% 的精度提升，在 Fashion-MNIST 上实现了 +5.4% 的精度提升——同时实现了速度和精度的双重提高。然而，在时间维度携带真实运动信息的基于事件（event-based）的数据上，TAC 的时间折叠是有害的。因此，我们引入了 TAC-TP（时间保留），它在 K 个独立的 LIF 步骤中共享每组的卷积输出，为下游层保留完整的时间分辨率。在 DVS128-Gesture 上，TAC-TP 在减少 50% 卷积调用的情况下实现了 95.1% 的精度（基准为 96.3%），而标准 TAC 的精度下降到了 91.3%。我们的核心发现是，最优的时间聚合策略是数据依赖的：对于率编码数据应折叠时间维度（降噪），而对于事件数据则应保留时间维度（信息保留）。加速效果与硬件无关：TAC 在 NVIDIA V100 上实现了 11.0 倍的加速，证实了该机制可以跨 GPU 架构迁移。mlx-snn 库中的所有算子均已开源。

## Abstract
Spike sparsity is widely believed to enable efficient spiking neural network (SNN) inference on GPU hardware. We demonstrate this is an illusion: five distinct sparse computation strategies on Apple M3 Max all fail to outperform dense convolution, because SIMD architectures cannot exploit the fine-grained, unstructured sparsity of i.i.d. binary spikes. Instead, we propose Temporal Aggregated Convolution (TAC), which exploits convolution linearity to pre-aggregate $K$ spike frames before a single convolution call, reducing $T$ calls to $T/K$. On rate-coded data, TAC achieves 13.8times speedup with +1.6% accuracy on MNIST and +5.4% on Fashion-MNIST -- a simultaneous improvement in both speed and accuracy. However, on event-based data where the temporal dimension carries genuine motion information, TAC's temporal collapse is harmful. We therefore introduce TAC-TP (Temporal Preservation), which shares each group's convolution output across K independent LIF steps, preserving full temporal resolution for downstream layers. On DVS128-Gesture, TAC-TP achieves 95.1% accuracy (vs. 96.3% baseline) with 50% fewer convolution calls, while standard TAC drops to 91.3%. Our key finding is that the optimal temporal aggregation strategy is data-dependent: collapse the temporal dimension for rate-coded data (noise reduction) but preserve it for event data (information retention). Speedup is hardware-agnostic: TAC achieves 11.0times on NVIDIA V100, confirming the mechanism transfers across GPU architectures. All operators in the mlx-snn library are open source.

---

## 论文详细总结（自动生成）

这篇论文《Collapse or Preserve: Data-Dependent Temporal Aggregation for Spiking Neural Network Acceleration》对脉冲神经网络（SNN）在通用硬件（GPU）上的加速问题进行了深入探讨，并提出了创新的时间聚合方案。以下是详细总结：

### 1. 核心问题与研究动机
*   **核心挑战**：长期以来，SNN 被认为能通过“脉冲稀疏性”实现高效推理。然而，作者指出这在 GPU 硬件上是一个“幻觉”。
*   **研究动机**：GPU 的 SIMD（单指令多数据）架构要求向量单元执行相同的操作。对于随机分布的稀疏脉冲，一个 SIMD 单元（如宽度为 32）中所有元素均为零的概率极低，导致分支预测和内存访问开销远超跳过零值带来的收益。
*   **整体含义**：论文旨在寻找一种不依赖于细粒度稀疏性、而是利用卷积线性性质来减少计算频率的 GPU 加速新路径。

### 2. 方法论：时间聚合卷积（TAC）
核心思想是利用卷积的线性性质：$W * (\sum \alpha_i S_i) = \sum \alpha_i (W * S_i)$。
*   **TAC (时间折叠)**：
    *   **聚合**：将连续的 $K$ 个脉冲帧按指数衰减权重（模拟 LIF 神经元的膜电位衰减）预聚合为一个稠密帧。
    *   **计算**：对聚合后的帧仅调用一次卷积，而非 $K$ 次。
    *   **折叠**：LIF 神经元在每组 $K$ 帧后仅发放一次脉冲，输出时间维度从 $T$ 缩减为 $T/K$。
*   **TAC-TP (时间保留)**：
    *   **背景**：针对事件驱动数据（如 DVS 信号），时间维度包含运动信息，折叠会导致精度大幅下降。
    *   **机制**：同样进行 $K$ 帧聚合和单次卷积，但将卷积结果“广播”回 $K$ 个独立的时间步，每个步执行独立的 LIF 神经元更新。
    *   **效果**：在减少卷积调用（计算大头）的同时，保留了完整的时间分辨率 $T$。

### 3. 实验设计
*   **数据集**：
    *   **率编码数据**：MNIST、Fashion-MNIST（将静态图像转换为 Poisson 脉冲序列）。
    *   **事件驱动数据**：DVS128-Gesture（手势识别，原生事件相机数据）。
*   **基准（Benchmark）**：标准的稠密卷积 SNN 实现（基于 SpikingJelly 和 snnTorch 框架）。
*   **对比方法**：
    *   五种失败的稀疏加速尝试（Delta 卷积、权重收集、图编译、稀疏 GEMM、自定义 Metal 内核）。
    *   其他算子：傅里叶时间卷积（FTC）、信息论通道门控（IMC）、时间塌陷卷积（TCC）。

### 4. 资源与算力
*   **硬件平台**：
    *   主要实验：Apple M3 Max (40核 GPU, 128GB 统一内存)，使用 `mlx-snn` 框架。
    *   跨平台验证：NVIDIA Tesla V100-SXM2 (16GB)，使用 PyTorch 2.7 + `snnTorch`。
*   **训练时长**：MNIST/FMNIST 训练 15 个 epoch；DVS-Gesture 训练 256 个 epoch。文中提供了每 epoch 的秒数（如 DVS 从 69s 降至 47s）。

### 5. 实验数量与充分性
*   **实验规模**：针对每个数据集均进行了多种子（通常为 3 个 seed）实验，并报告了均值和标准差。
*   **充分性**：
    *   涵盖了从静态图像到动态事件流的不同数据类型。
    *   进行了跨硬件（Apple Silicon vs. NVIDIA GPU）和跨框架（MLX vs. PyTorch）的验证。
    *   提供了理论误差上界的证明（Theorem 1）。
*   **客观性**：作者诚实地报告了“负面结果”（稀疏性加速失败），并对比了 TAC 在不同数据上的性能差异，实验设计较为公正。

### 6. 主要结论与发现
*   **稀疏性误区**：在 GPU 上，细粒度脉冲稀疏性几乎无法带来加速，甚至可能因内存访问不规则而变慢。
*   **数据依赖性策略**：
    *   **率编码数据**：应使用 **TAC（折叠）**。它不仅能提速（高达 13.8x），还能通过类似“集成平均”的效果降低 Poisson 噪声，从而提升精度（MNIST +1.6%, FMNIST +5.4%）。
    *   **事件驱动数据**：应使用 **TAC-TP（保留）**。它能在减少 50% 卷积调用的情况下，维持 95.1% 的高精度（仅比基准下降 1.2%），远优于直接折叠。
*   **硬件无关性**：加速效果在不同 GPU 架构上具有普适性。

### 7. 优点
*   **打破迷思**：通过系统性实验证伪了 SNN 在 GPU 上利用稀疏性加速的普遍假设。
*   **双重提升**：在率编码数据上实现了速度与精度的双重增益，而非传统的权衡关系。
*   **实用性强**：算子已开源，且 TAC-TP 为处理高频事件流提供了一种低成本的计算方案。
*   **理论支撑**：为聚合带来的近似误差提供了数学证明。

### 8. 不足与局限
*   **模型深度限制**：实验主要集中在较浅的网络（2层或5层卷积），尚未在大规模模型（如 ResNet-18/50）或超大型数据集（ImageNet）上验证。
*   **TAC-TP 加速比有限**：由于 TAC-TP 仍需逐步执行 LIF 神经元计算（虽然卷积减少了），其端到端的墙钟时间加速比（约 1.2x）远低于卷积调用的减少比例。
*   **超参数敏感性**：聚合因子 $K$ 的选择需要根据数据的时间相关性进行手动调整，缺乏自动化的搜索机制。

（完）
