---
title: Covariance-Guided Resource Adaptive Learning for Efficient Edge Inference
title_zh: 协方差引导的资源自适应学习，用于高效边缘推理
authors: "Ahmad N. L. Nabhaan, Zaki Sukma, Rakandhiya D. Rachmanto, Muhammad Husni Santriaji, Byungjin Cho, Arief Setyanto, In Kee Kim"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14577v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 高效边缘推理的资源自适应学习
tldr: 针对边缘设备深度学习推理中硬件配置优化难的问题，本文提出CORAL，一种无需离线分析的在线优化方法。它利用距离协方差捕捉硬件设置与性能间的非线性关系，将推理优化建模为吞吐量与功耗的协同优化问题。在NVIDIA Jetson设备上的实验表明，CORAL能以极低的探索成本实现接近最优的性能，并有效应对严格的双重约束场景。
selection_source: fresh_fetch
motivation: 现有的边缘推理优化方法依赖低效的静态预设或昂贵的离线分析，难以在不同模型和设备间灵活迁移。
method: 提出CORAL方法，利用距离协方差统计建模硬件配置与性能指标间的非线性依赖，实现吞吐量与功耗的在线协同优化。
result: "在多种目标检测模型上，CORAL达到了穷举搜索最优性能的96%-100%，并在严格约束下表现优于基准方法。"
conclusion: CORAL证明了通过在线统计学习可以高效发现边缘设备的近优硬件配置，显著提升了推理能效。
---

## 摘要
在边缘设备上进行深度学习推理时，达到相同吞吐量的硬件配置在功耗上可能相差 2 倍，但操作人员往往难以在不进行详尽性能分析的情况下找到高效的配置。现有方法通常依赖于低效的静态预设，或者需要昂贵的离线性能分析，且针对每个新模型或设备都必须重复这一过程。为解决这一问题，我们提出了 CORAL，这是一种无需离线性能分析即可发现近优配置的在线优化方法。CORAL 利用距离协方差从统计学角度捕捉硬件设置（如 DVFS 和并发级别）与性能指标之间的非线性依赖关系。与以往的工作不同，我们将该挑战明确表述为一个吞吐量-功耗协同优化问题，以同时满足功耗预算和吞吐量目标。我们在两个 NVIDIA Jetson 设备上，针对从轻量级到重量级的三个目标检测模型对 CORAL 进行了评估。在单目标场景下，CORAL 达到了详尽搜索所发现的最优性能的 96% – 100%。在基准方法失效或超出功耗预算的严格双重约束场景下，CORAL 始终能以极少的探索在线找到合适的配置。

## Abstract
For deep learning inference on edge devices, hardware configurations achieving the same throughput can differ by 2$\times$ in power consumption, yet operators often struggle to find the efficient ones without exhaustive profiling. Existing approaches often rely on inefficient static presets or require expensive offline profiling that must be repeated for each new model or device. To address this problem, we present CORAL, an online optimization method that discovers near-optimal configurations without offline profiling. CORAL leverages distance covariance to statistically capture the non-linear dependencies between hardware settings, e.g., DVFS and concurrency levels, and performance metrics. Unlike prior work, we explicitly formulate the challenge as a throughput-power co-optimization problem to satisfy power budgets and throughput targets simultaneously. We evaluate CORAL on two NVIDIA Jetson devices across three object detection models ranging from lightweight to heavyweight. In single-target scenarios, CORAL achieves 96% $\unicode{x2013}$ 100% of the optimal performance found by exhaustive search. In strict dual-constraint scenarios where baselines fail or exceed power budgets, CORAL consistently finds proper configurations online with minimal exploration.

---

## 论文详细总结（自动生成）

这是一份关于论文《Covariance-Guided Resource Adaptive Learning for Efficient Edge Inference》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在边缘设备（如无人机、智能监控）上部署深度学习推理时，面临**吞吐量（Throughput）**与**功耗（Power）**的双重严苛约束。
*   **研究动机**：现代边缘硬件（如 NVIDIA Jetson）拥有复杂的调优参数（CPU/GPU频率、核心数、并发级别），其组合可达数千种。实验发现，相同吞吐量的配置在功耗上可能相差 2 倍。
*   **现有痛点**：
    *   **静态预设**：厂商提供的模式（如 10W/15W）覆盖面窄，难以平衡特定应用的实时性与续航。
    *   **离线分析（Profiling）**：如 ALERT 等方法需要针对每个新模型或新设备进行数小时的详尽测试，耗能且不可扩展。
    *   **强化学习**：在线收敛慢，难以应对瞬息万变的边缘环境。
*   **核心目标**：开发一种**无需离线分析**、能够**在线快速收敛**并同时满足吞吐量和功耗目标的优化方法。

### 2. 论文提出的方法论：CORAL
论文提出了 **CORAL (Covariance-Guided Resource Adaptive Learning)**，其核心思想是利用统计学中的**距离协方差（Distance Covariance）**来引导搜索。

*   **核心技术细节**：
    1.  **非线性依赖捕捉**：不同于皮尔逊相关系数只能捕捉线性关系，距离协方差通过计算配置参数与性能指标（吞吐量、功耗）之间的距离矩阵并进行双重中心化，能够识别复杂的非线性依赖。
    2.  **三步循环算法**：
        *   **奖励评估**：执行推理并测量指标。满足双约束的配置给予效率奖励（吞吐量/功耗），违反约束的配置加入禁止列表并给予惩罚。
        *   **相关性分析**：利用滑动窗口内的观测数据，计算各硬件参数（如 GPU 频率）与目标指标间的距离相关系数（dCor），识别哪些参数是影响性能的关键因子。
        *   **配置搜索**：根据相关性权重动态调整步长。如果当前吞吐量达标，则向降低功耗的方向搜索；否则，优先提升吞吐量。
    3.  **协同优化公式**：将问题明确定义为在满足 $p(s) \le p_{budget}$ 且 $\tau(s) \ge \tau_{target}$ 的前提下，最大化效率 $\eta(s) = \tau(s)/p(s)$。

### 3. 实验设计
*   **硬件平台**：NVIDIA Jetson Xavier NX (Volta 架构) 和 Jetson Orin Nano (Ampere 架构)。
*   **模型选择**：涵盖了 20 倍参数跨度的三个目标检测模型：YOLOv5-N (1.9M)、FRCNN-MobileNetV3 (19.4M)、RetinaNet-ResNet50 (38M)。
*   **对比基准 (Benchmarks)**：
    *   **ORACLE**：通过穷举搜索获得的理论最优解（上限）。
    *   **ALERT**：基于离线分析和卡尔曼滤波的先进方法。
    *   **ALERT-Online**：ALERT 的在线随机探索版本。
    *   **Manufacturer Presets**：厂商自带的 Max-power 和 Default 模式。
*   **数据集**：使用 COCO 数据集训练，推理输入为真实的交通监控视频流。

### 4. 资源与算力
*   **推理算力**：实验在单台 Jetson Xavier NX 和 Orin Nano 上完成，涉及 CPU 核心调度（2-6核）、GPU 频率（最高 1100MHz）及内存频率调节。
*   **训练/搜索开销**：CORAL 是一种**轻量级在线方法**，不涉及深度神经网络的重新训练。其搜索预算被严格限制在 **10 次迭代**内（约 10-20 秒），计算开销极低，适合在边缘端实时运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 Xavier NX 上的 2,160 种配置和 Orin Nano 上的 1,600 种配置进行了穷举测试以建立基准。
    *   测试了单约束（仅吞吐量）和双重约束（吞吐量+功耗）两种主要场景。
    *   跨越了两个硬件代际和三个不同规模的模型。
*   **充分性评价**：实验设计较为充分。通过对比 ORACLE 证明了其接近最优的能力，通过对比 ALERT 证明了其无需离线数据的优势。双约束场景的设置（如 6.5W 限制下达到 30 FPS）非常贴近实际工业需求，体现了客观性和公平性。

### 6. 主要结论与发现
*   **性能优异**：在单目标优化中，CORAL 达到了 ORACLE 性能的 **96% – 100%**。
*   **双约束胜出**：在严格的功耗和吞吐量双重限制下，基准方法（如厂商预设或 ALERT）经常出现功耗超标或吞吐量不足的情况，而 CORAL 始终能找到可行配置。
*   **模型泛化性**：随着模型变大（如从 YOLO 到 RetinaNet），搜索空间变窄，CORAL 的优势更加明显，能够精准锁定极少数能满足约束的“黄金配置”。
*   **快速收敛**：仅需 10 次在线迭代即可收敛，远快于强化学习等方法。

### 7. 优点
*   **零离线开销**：彻底摆脱了对耗时的离线性能分析的依赖，实现了“即插即用”。
*   **统计学创新**：首次将距离协方差引入边缘推理优化，有效解决了硬件参数间复杂的非线性耦合问题。
*   **协同优化**：明确处理了功耗与吞吐量的权衡，而非单一指标优化，更符合边缘部署的真实逻辑。

### 8. 不足与局限
*   **硬件覆盖**：实验仅限于 NVIDIA Jetson 系列，未在其他架构（如树莓派、移动端 NPU 或 RISC-V 平台）上验证通用性。
*   **多模型并发**：论文主要关注单模型推理优化，未深入探讨多个不同模型在同一设备上竞争资源时的动态协调。
*   **搜索预算固定**：虽然 10 次迭代在实验中表现良好，但在参数空间更广阔的未来硬件上，可能需要更复杂的自适应迭代策略。
*   **环境动态性**：实验主要基于稳定的视频流，若输入负载（如图像复杂度）剧烈波动，算法的响应速度和稳定性仍有待进一步验证。

（完）
