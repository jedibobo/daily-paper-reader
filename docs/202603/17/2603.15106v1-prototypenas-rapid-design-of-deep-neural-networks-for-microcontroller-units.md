---
title: "PrototypeNAS: Rapid Design of Deep Neural Networks for Microcontroller Units"
title_zh: PrototypeNAS：面向微控制单元的深度神经网络快速设计
authors: "Mark Deutel, Simon Geis, Axel Plinge"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15106v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 针对资源受限微控制器的零样本神经架构搜索
tldr: PrototypeNAS 是一种针对微控制器（MCU）的零样本神经架构搜索（NAS）方法，旨在解决传统 NAS 资源消耗大且未考虑硬件约束的问题。它通过解耦设计与训练、结合多种架构类型及剪枝量化配置、利用零样本代理集成和超体积子集选择，在几分钟内即可为不同 MCU 自动定制高效 DNN，在图像、时间序列和目标检测任务中均表现优异。
selection_source: fresh_fetch
motivation: 传统 NAS 方法在为资源受限的 MCU 设计专用 DNN 时，存在训练成本极高且难以兼顾硬件约束的局限性。
method: 提出一种三阶段零样本搜索方法，结合了多架构搜索空间、零样本代理集成以及基于超体积的帕累托前沿蒸馏技术。
result: 在 12 个数据集上的实验表明，该方法能在数分钟内找到可部署于商用 MCU 且准确率媲美大型模型的轻量化架构。
conclusion: PrototypeNAS 实现了高效、自动化的 DNN 跨平台定制，显著降低了边缘设备深度学习应用的开发门槛。
---

## 摘要
在具有不同硬件约束的边缘设备上实现高效的深度神经网络（DNN）推理是一项具有挑战性的任务，通常需要针对每个设备分别进行 DNN 架构的专门化。为了避免巨大的手动工作量，可以使用神经架构搜索（NAS）。然而，许多现有的 NAS 方法资源密集且耗时，因为它们需要从头开始训练许多不同的 DNN。此外，它们没有考虑到目标系统的资源约束。为了解决这些缺点，我们提出了 PrototypeNAS，这是一种零样本（zero-shot）NAS 方法，旨在加速并自动化针对不同目标微控制单元（MCU）的 DNN 选择、压缩和专门化。我们提出了一种新颖的三步搜索方法，将给定目标平台的 DNN 设计和专门化与 DNN 训练解耦。首先，我们提出了一个新颖的搜索空间，它不仅从单个大型架构中裁剪出较小的 DNN，而且结合了多种架构类型的结构优化，以及它们的剪枝和量化配置优化。其次，我们探索在优化过程中使用零样本代理（zero-shot proxies）的集成，而不是单个代理。第三，我们建议使用超体积子集选择（Hypervolume subset selection）从多目标优化的帕累托前沿（Pareto front）中提取 DNN 架构，这些架构代表了准确率和 FLOPs 之间最有意义的权衡。我们在 12 个不同的数据集上评估了 PrototypeNAS 在三个不同任务中的有效性：图像分类、时间序列分类和目标检测。我们的结果表明，PrototypeNAS 能够在几分钟内识别出足够小、可部署在现成 MCU 上的 DNN 模型，且仍能达到与大型 DNN 模型性能相当的准确率。

## Abstract
Enabling efficient deep neural network (DNN) inference on edge devices with different hardware constraints is a challenging task that typically requires DNN architectures to be specialized for each device separately. To avoid the huge manual effort, one can use neural architecture search (NAS). However, many existing NAS methods are resource-intensive and time-consuming because they require the training of many different DNNs from scratch. Furthermore, they do not take the resource constraints of the target system into account. To address these shortcomings, we propose PrototypeNAS, a zero-shot NAS method to accelerate and automate the selection, compression, and specialization of DNNs to different target microcontroller units (MCUs). We propose a novel three-step search method that decouples DNN design and specialization from DNN training for a given target platform. First, we present a novel search space that not only cuts out smaller DNNs from a single large architecture, but instead combines the structural optimization of multiple architecture types, as well as optimization of their pruning and quantization configurations. Second, we explore the use of an ensemble of zero-shot proxies during optimization instead of a single one. Third, we propose the use of Hypervolume subset selection to distill DNN architectures from the Pareto front of the multi-objective optimization that represent the most meaningful tradeoffs between accuracy and FLOPs. We evaluate the effectiveness of PrototypeNAS on 12 different datasets in three different tasks: image classification, time series classification, and object detection. Our results demonstrate that PrototypeNAS is able to identify DNN models within minutes that are small enough to be deployed on off-the-shelf MCUs and still achieve accuracies comparable to the performance of large DNN models.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **PrototypeNAS**，一种专为资源受限的微控制单元（MCU）设计的零样本（Zero-shot）神经架构搜索（NAS）框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在 MCU 等边缘设备上部署深度神经网络（DNN）面临严峻的内存（RAM/ROM）和算力约束。传统的 NAS 方法通常需要训练成百上千个模型来评估性能，耗时且资源消耗巨大。
*   **核心目标**：开发一种能够快速（分钟级）、自动化地针对特定硬件约束进行模型选择、结构优化及压缩（剪枝、量化）的方法，且无需在搜索阶段进行模型训练。

### 2. 方法论
PrototypeNAS 采用三阶段流水线设计：
*   **架构原型探索（Multi-Objective Optimization, MOO）**：
    *   **搜索空间**：不仅包含子网选择，还结合了多种基准架构（如 MobileNetV2, ResNet, SqueezeNet 等）、结构参数（深度、卷积核大小、步长）以及尺寸优化（宽度乘子、剪枝稀疏度）。
    *   **目标函数**：最小化 FLOPs，同时最大化四个零样本代理指标（MeCo, ZiCo, NASWOT, SNIP）的集成得分。
    *   **硬件约束**：将目标 MCU 的 RAM、ROM 和 FLOPs 限制作为硬性约束。
*   **超体积子集选择（Hypervolume Subset Selection）**：
    *   从优化得到的帕累托前沿（Pareto front）中，利用进化算法筛选出 $k$ 个（通常为 5 个）最具代表性的模型。这些模型在准确率预估和资源消耗之间提供了最有意义的权衡。
*   **数据集评估器**：
    *   仅对选出的少量原型进行最终的训练、剪枝和量化（PTQ 或 QAT），从而将设计与大规模训练解耦。

### 3. 实验设计
*   **数据集与场景**：涵盖 12 个数据集，分为三大任务：
    *   **图像分类**：CIFAR10/100, GTSRB, Flowers, Birds, Cars, Pets, ArxPhotos314。
    *   **时间序列分类**：Daliac, MAFULDA, BitBrain Sleep。
    *   **目标检测**：COCO 子集（行人检测）。
*   **基准对比（Benchmarks）**：
    *   **TinyNAS (MCUNet)**：针对 MCU 的主流硬件感知 NAS。
    *   **NATS-Bench**：包含数万个已训练模型的 NAS 基准测试集。
*   **硬件平台**：主要针对 ARM Cortex-M7 (iMXRT1062) MCU 进行延迟和能耗测量。

### 4. 资源与算力
*   **搜索算力**：由于采用零样本代理指标，搜索过程**仅需几分钟**即可完成，无需 GPU 训练。
*   **训练算力**：文中未详细列出 GPU 型号，但提到最终仅需训练 3-5 个候选模型（每个模型 100 epoch），相比传统 NAS 减少了 99% 以上的训练负担。
*   **部署测量**：在 iMXRT1062 MCU 上实测了推理延迟（ms）和能耗（mJ）。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个跨领域数据集上进行了验证，每组搜索进行 500 次试验（Trials）。
*   **充分性**：实验涵盖了视觉、传感器数据和检测任务，证明了方法的通用性。
*   **客观性**：通过 Kendall’s $\tau$ 系数深入分析了不同零样本代理指标与实际准确率的相关性，承认了单一指标的局限性，并以此论证了“代理集成”的必要性。

### 6. 主要结论与发现
*   **高效性**：PrototypeNAS 能在极短时间内找到适用于 MCU 的模型，准确率与大型架构相当。
*   **性能优越**：在 CIFAR10 上，找到的模型比 MCUNet 准确率平均高出 **5%**。
*   **代理指标表现**：没有任何单一代理指标能在所有数据集上保持高相关性，但通过多目标优化集成多个指标可以有效抵消偏差。
*   **硬件一致性**：实验证实 FLOPs 与 MCU 上的实际延迟和能耗呈线性正相关，验证了其作为优化目标的合理性。

### 7. 优点
*   **极速搜索**：将 NAS 的时间成本从“天/周”缩短至“分钟”。
*   **多架构融合**：不同于大多数 NAS 仅在单一超网（Supernet）内搜索，它能跨越不同的架构族（如从 SqueezeNet 到 MobileNet）进行优化。
*   **端到端自动化**：整合了架构搜索、剪枝配置和量化策略，直接输出可部署的 C 代码。

### 8. 不足与局限
*   **代理指标的不确定性**：尽管使用了集成方法，零样本指标与最终量化后准确率之间仍存在一定的排序偏差（Disagreement）。
*   **量化策略依赖经验**：实验发现时间序列任务必须使用 QAT（量化感知训练）而非 PTQ，这需要人工干预或先验知识。
*   **搜索空间限制**：虽然支持多种基准架构，但仍受限于预定义的“原型”模板，可能无法发现完全颠覆性的新型算子组合。

（完）
