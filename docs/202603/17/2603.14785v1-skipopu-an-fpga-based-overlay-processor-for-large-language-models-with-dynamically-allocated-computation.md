---
title: "SkipOPU: An FPGA-based Overlay Processor for Large Language Models with Dynamically Allocated Computation"
title_zh: SkipOPU：一种面向动态分配计算的大语言模型 FPGA 覆盖处理器
authors: "Zicheng He, Anhao Zhao, Xiaoyu Shen, Chen Wu, Lei He"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14785v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于动态LLM执行的FPGA覆盖处理器
tldr: 针对大语言模型推理中参数量巨大导致的效率瓶颈，本文提出SkipOPU，一种基于FPGA的覆盖处理器。它利用动态计算分配技术，通过轻量级路由机制实现Token和层级的选择性执行。SkipOPU通过解耦非线性模块、设计浮点定点混合执行的PE阵列以及主动式片上KV历史缓存，显著提升了硬件利用率并减少了冗余计算与内存访问，为动态LLM推理提供了高效的硬件支持。
selection_source: fresh_fetch
motivation: 现有的硬件平台主要针对静态执行优化，难以高效支持大语言模型中利用Token和层级不均匀贡献而进行的动态计算分配。
method: 提出了一种支持动态分配的FPGA处理器架构，通过解耦增量归约、设计混合精度PE阵列及片上KV历史缓存来优化计算与带宽。
result: "在AMD U280 FPGA上的实验显示，SkipOPU的带宽效率比GPU和其他FPGA加速器高出1.23至3.83倍，并减少了高达25.4%的KV存储开销。"
conclusion: SkipOPU通过软硬件协同设计，有效解决了动态LLM推理中的计算冗余和访存瓶颈，是提升大模型推理效率的有力方案。
---

## 摘要
大语言模型（LLM）在广泛的任务中取得了卓越的性能，但由于参数量的快速增长，其推理效率仍然是一个关键瓶颈。动态计算分配的最新进展通过利用不同 token 和层之间高度不均匀的贡献来应对这一挑战，实现了选择性执行，在保持模型准确性的同时显著减少了冗余计算。然而，现有的硬件平台和加速器主要针对统一、静态的执行进行优化，限制了它们高效支持此类动态推理模式的能力。在这项工作中，我们提出了 SkipOPU，这是一种基于 FPGA 的覆盖处理器，通过轻量级路由机制在 token 和层之间灵活地动态分配计算。首先，我们将非线性模块中的归约操作与逐元素计算解耦，并进行增量归约，从而使这两个阶段能够与相邻的线性操作（路由器或矩阵乘法）融合，实现有效的延迟隐藏。其次，受激活值和权重对数值精度敏感性不对称的启发，我们设计了一个高效支持浮点-定点混合执行的 PE 阵列。引入了一种新颖的 DSP 过载封装（overpacking）技术，以在最小化资源开销的同时最大化硬件利用率。最后，我们开发了一种主动式片上 KV 历史缓冲区，利用被剪枝 token 的跨层 KV 不变性，消除了解码过程中的不规则 HBM 访问，并通过高局部性的片上重用补充了片外带宽。实验结果表明，在 AMD U280 FPGA 上，SkipOPU 在动态计算分配的 LLM 推理带宽效率方面优于 GPU 和其他基于 FPGA 的加速器 1.23 倍至 3.83 倍，并且在不同序列长度下可减少高达 25.4% 的 KV 存储开销。

## Abstract
Large language models (LLMs) have achieved remarkable performance across a wide range of tasks, but their inference efficiency remains a critical bottleneck due to rapidly growing parameters. Recent advances in dynamic computation allocation address this challenge by exploiting the highly uneven contributions of different tokens and layers, enabling selective execution that significantly reduces redundant computation while preserving model accuracy. However, existing hardware platforms and accelerators are primarily optimized for uniform, static execution, limiting their ability to efficiently support such dynamic inference patterns. In this work, we propose SkipOPU, an FPGA-based overlay processor that dynamically allocates computation across tokens and layers with high flexibility through a lightweight routing mechanism. First, we decouple reduction operations from element-wise computation in nonlinear modules and perform reductions incrementally, which enables both stages to be fused with adjacent linear operations (router or matrix multiplication) for effective latency hiding. Second, motivated by asymmetric sensitivity to numerical precision between activation and weight, we design a PE array that efficiently supports float-fixed hybrid execution. A novel DSP overpacking technique is introduced to maximize hardware utilization while minimizing resource overhead. Finally, we develop a proactive on-chip KV history buffer that exploits cross-layer KV invariance of pruned tokens, eliminating irregular HBM accesses during decoding and supplementing off-chip bandwidth through high-locality on-chip reuse. Experimental results demonstrate that SkipOPU on an AMD U280 FPGA outperforms GPU and other FPGA-based accelerators by 1.23x-3.83x in bandwidth efficiency for LLMs inference with dynamic computation allocation and can reduce up to 25.4% KV storage overhead across varying sequence lengths.

---

## 论文详细总结（自动生成）

以下是对论文《SkipOPU: An FPGA-based Overlay Processor for Large Language Models with Dynamically Allocated Computation》的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）推理受限于巨大的参数量和内存带宽。虽然“动态计算分配”（如 SkipGPT）能根据 Token 的重要性动态跳过某些层或模块以减少计算量，但现有硬件（GPU/FPGA）多针对静态、均匀的计算模式优化，难以高效处理动态执行带来的非线性算子延迟、混合精度计算低效以及 KV Cache 访问不规则等问题。
*   **研究背景**：动态剪枝算法在理论上减少了 FLOPs，但在实际硬件上往往因为控制流开销和访存碎片化而无法转化为实际的加速。

### 2. 方法论
SkipOPU 提出了一套软硬件协同的设计方案，核心技术包括：
*   **融合数据流（Fused Dataflow）**：
    *   将 RMSNorm 和 Softmax 等非线性算子的“归约（Reduction）”操作与“逐元素计算”解耦。
    *   在执行线性层（如路由器或矩阵乘法）的同时，增量式地完成归约计算，从而将非线性算子的延迟隐藏在流水线中。
*   **混合精度 PE 阵列与 DSP 过载封装（Overpacking）**：
    *   针对权重（INT4）和激活值（FP16）的混合精度需求，设计了新型 PE。
    *   利用 FPGA DSP48E2 的位宽特性，通过截断和符号位反转技术，在一个 DSP 中封装多个尾数乘法，极大提升了单位面积的算力。
    *   采用块浮点（BFP）累加，将昂贵的浮点加法树简化为定点加法，仅在最后进行一次浮点转换。
*   **主动式片上 KV 历史缓冲区**：
    *   利用被跳过 Token 的 KV 值在层间保持不变的特性（KV Invariance），将这些重复的 KV 条目缓存在片上 URAM 中。
    *   通过片上重用减少对 HBM 的随机、小碎块访问，确保 HBM 始终以高效的突发（Burst）模式运行。

### 3. 实验设计
*   **硬件平台**：AMD Alveo U280 FPGA（配备 HBM2 和 DDR4）。
*   **测试模型**：Llama2-7B，采用 SkipGPT 剪枝策略（约 25% 的跳过率），权重对称量化为 INT4，激活值和 KV Cache 保持 FP16。
*   **对比基准（Benchmarks）**：
    *   **GPU 方案**：vLLM (运行于 NVIDIA A100)。
    *   **FPGA 方案**：FlightLLM, ChatOPU, MCoreOPU, DFX 等最先进的 FPGA 加速器。
*   **测试场景**：涵盖 Prefill（预填充）阶段（序列长度 128-1024）和 Decode（解码）阶段。

### 4. 资源与算力
*   **硬件资源**：使用了 U280 FPGA 上的 4096 个 DSP（利用率约 45%）、512 个 URAM（利用率 53%）以及约 1.28M 的 LUT（利用率 98.8%）。
*   **运行频率**：HBM 接口运行在 450MHz，加速器核心逻辑运行在 225MHz。
*   **算力表现**：通过 DSP 过载封装，每个周期可支持 64×128 次混合精度乘法。
*   **训练/剪枝说明**：文中未提及模型训练时长，因为该处理器专注于推理加速，模型剪枝和量化参数是预先生成的。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括：
    *   **消融实验**：验证了融合数据流、KV 重用、KV 缓冲区对吞吐量的逐项提升。
    *   **精度验证**：对比了不同 PE 实现方案的数值误差。
    *   **带宽效率分析**：测试了不同序列长度下的 HBM 有效带宽。
    *   **端到端对比**：与 6 种主流方案在吞吐量和带宽效率上进行了对比。
*   **充分性评价**：实验设计较为全面，客观地展示了在 Batch Size 为 1（边缘/本地推理典型场景）下的性能优势，对比对象涵盖了目前学术界最顶尖的同类研究。

### 6. 主要结论
*   **带宽效率**：SkipOPU 的有效带宽利用率达到 89% 左右，比 GPU 和其他 FPGA 加速器高出 **1.23x 至 3.83x**。
*   **存储优化**：通过跨层 KV 重用策略，KV Cache 的存储开销最高可减少 **25.4%**。
*   **端到端性能**：在 Llama2-7B 模型上，SkipOPU 实现了比现有 FPGA 方案更高的归一化吞吐量，成功将算法层面的“跳过”转化为硬件层面的“加速”。

### 7. 优点
*   **创新的 DSP 利用方式**：提出的 DSP Overpacking 技术突破了 FPGA 原生 DSP 对浮点运算的限制，显著提升了算力密度。
*   **高效的访存管理**：针对动态剪枝导致的访存碎片化问题，提出的 KV 历史缓冲区和 Token 级内存映射方案非常具有针对性。
*   **延迟隐藏**：通过解耦归约操作，解决了 LLM 推理中非线性算子容易成为流水线瓶颈的痛点。

### 8. 不足与局限
*   **应用范围限制**：该架构高度依赖于 SkipGPT 这种“模块级跳过”的剪枝风格，对于非结构化稀疏或其他类型的动态压缩算法（如动态位宽）的通用性未做深入探讨。
*   **Batch Size 局限**：实验主要针对 Batch Size = 1，虽然符合本地部署场景，但在高并发的服务端场景下，动态计算分配带来的调度复杂性可能会削弱其优势。
*   **硬件平台依赖**：设计深度绑定了 Xilinx UltraScale+ 架构的 DSP48E2 特性，迁移到其他 FPGA 平台（如 Intel Agilex）可能需要重新设计 PE 微架构。

（完）
