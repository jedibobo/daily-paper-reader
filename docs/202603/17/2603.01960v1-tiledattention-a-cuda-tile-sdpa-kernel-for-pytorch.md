---
title: "TiledAttention: a CUDA Tile SDPA Kernel for PyTorch"
title_zh: TiledAttention：一个面向 PyTorch 的 CUDA 分块 SDPA 算子
authors: Taimur Khan
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01960v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 用于计算利用率优化的CUDA Tile内核
tldr: 本研究推出了TiledAttention，这是一种专为NVIDIA GPU设计的缩放点积注意力（SDPA）算子。它采用cuTile Python (TileIR) 实现并集成于PyTorch，通过在线Softmax和分块K、V流块传输技术，在保持高性能的同时，允许研究人员直接在Python层面修改分块形状和内存布局。该工具解决了底层CUDA模板开发门槛高的问题，为快速、可复现的注意力内核研究提供了一个兼具灵活性与效率的平衡方案。
selection_source: carryover_cache
motivation: 为了克服底层CUDA模板难以修改的局限性，提供一个既能保持高性能又易于在Python层面进行定制的SDPA内核研究平台。
method: 基于cuTile Python实现，通过在线Softmax和分块流式传输技术，支持在Python中直接调整分块形状、暂存和共享内存布局等调度参数。
result: 在NVIDIA DGX GB10上的测试显示，TiledAttention的性能显著优于PyTorch的标准未融合路径，并能无缝接入PyTorch工作流。
conclusion: TiledAttention在执行效率与开发灵活性之间达成了良好平衡，是进行注意力机制内核创新与优化的实用工具。
---

## 摘要
TiledAttention 是一个用于 NVIDIA GPU 上 SDPA 研究的缩放点积注意力（SDPA）前向算子。它采用 cuTile Python (TileIR) 实现，并作为可供 PyTorch 调用的函数提供。与底层 CUDA 模板相比，它更易于修改，同时通过在线 Softmax 和分块 $K,V$ 流式传输保留了真实的运行行为。该方法既具有高性能，又可以直接在 Python 中对调度层级（分块形状、暂存、共享内存布局）进行编辑，从而实现快速、可复现的算子研究，无需进行繁重的 CUDA/CUTLASS 模板重写。我们在 NVIDIA DGX GB10 节点上使用可复现的测试框架对 TiledAttention 进行了基准测试，并在不同序列长度、头维度和精度（FP16/BF16）下，将其与 PyTorch SDPA（自动分发）以及显式未融合的基准线进行了对比。尽管生产级融合基准线在整体性能上仍然更强，但 TiledAttention 相比标准的 eager 模式注意力路径实现了大幅加速，并可直接用于 PyTorch 工作流中，在性能与可定制性之间提供了实际的平衡。

## Abstract
TiledAttention is a scaled dot-product attention (SDPA) forward operator for SDPA research on NVIDIA GPUs. Implemented in cuTile Python (TileIR) and exposed as a PyTorch-callable function, it is easier to modify than low-level CUDA templates while retaining realistic behavior via online softmax and tiled $K,V$ streaming. The approach is both performant and directly editable at the schedule level from Python (tile shapes, staging, shared-memory layout), enabling rapid, reproducible kernel research without template-heavy CUDA/CUTLASS rewrites. We benchmark TiledAttention on an NVIDIA DGX GB10 node with a reproducible harness and compare against PyTorch SDPA (auto-dispatch) and explicit unfused baselines across sequence length, head dimension, and precision (FP16/BF16). While production fused baselines remain stronger overall, TiledAttention delivers large speedups over standard eager attention paths and is available for direct use within PyTorch workflows, providing a practical balance between performance and customizability.