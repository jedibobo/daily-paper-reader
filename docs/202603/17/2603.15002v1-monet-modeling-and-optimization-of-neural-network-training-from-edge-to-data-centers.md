---
title: "MONET: Modeling and Optimization of neural NEtwork Training from Edge to Data Centers"
title_zh: MONET：从边缘到数据中心的神经网络训练建模与优化
authors: "Jérémy Morlier, Robin Geens, Stef Cuyckens, Arne Symons, Marian Verhelst, Vincent Gripon, Mathieu Léonardon"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15002v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对异构加速器上神经网络训练的软硬件协同设计
tldr: MONET是一个针对异构数据流加速器设计的神经网络训练建模与优化框架。针对现有工具主要关注推理而忽视训练中内存占用和反向传播复杂性的问题，MONET扩展了Stream框架，支持层融合和激活检查点优化。通过对ResNet-18和GPT-2的实验，证明了其在硬件架构搜索和训练工作流优化方面的有效性，为从边缘到数据中心的深度学习部署提供了协同设计方案。
selection_source: fresh_fetch
motivation: 现有的硬件-软件协同设计工具多侧重于推理，缺乏对神经网络训练中特有的内存压力和反向传播复杂性的有效建模。
method: 基于Stream框架构建MONET，引入了对训练工作流、层融合配置以及基于遗传算法的激活检查点策略的建模与优化。
result: 在ResNet-18和GPT-2上的实验表明，MONET能有效探索设计空间，找到更优的硬件架构并平衡训练中的内存与计算开销。
conclusion: MONET证明了在异构加速器上进行训练建模的重要性，通过整体的软硬件协同设计可显著提升深度学习训练的效率和可扩展性。
---

## 摘要
虽然软硬件协同设计显著提高了神经网络推理的效率，但对训练阶段的建模仍然是一个关键且尚未得到充分探索的挑战。训练工作负载施加了独特的约束，特别是在内存占用和反向传播复杂性方面，而现有的专注于推理的工具无法捕捉到这些约束。本文介绍了 MONET，这是一个旨在对异构数据流加速器上的神经网络训练进行建模的框架。MONET 基于 Stream 构建，Stream 是一个经过实验验证的框架，用于对具有层融合的异构数据流加速器上的神经网络推理进行建模。利用 MONET，我们探索了 ResNet-18 和小型 GPT-2 的设计空间，展示了该框架建模训练工作流并寻找更优硬件架构的能力。随后，我们进一步研究了由于设计空间更大而在神经网络训练中变得更加复杂的问题，例如确定最佳的层融合配置。此外，我们借助遗传算法，利用该框架在激活检查点（activation checkpointing）中寻找有趣的权衡。我们的研究结果强调了整体软硬件协同设计方法对于可扩展且高效的深度学习部署的重要性。

## Abstract
While hardware-software co-design has significantly improved the efficiency of neural network inference, modeling the training phase remains a critical yet underexplored challenge. Training workloads impose distinct constraints, particularly regarding memory footprint and backpropagation complexity, which existing inference-focused tools fail to capture. This paper introduces MONET, a framework designed to model the training of neural networks on heterogeneous dataflow accelerators. MONET builds upon Stream, an experimentally verified framework that that models the inference of neural networks on heterogeneous dataflow accelerators with layer fusion. Using MONET, we explore the design space of ResNet-18 and a small GPT-2, demonstrating the framework's capability to model training workflows and find better hardware architectures. We then further examine problems that become more complex in neural network training due to the larger design space, such as determining the best layer-fusion configuration. Additionally, we use our framework to find interesting trade-offs in activation checkpointing, with the help of a genetic algorithm. Our findings highlight the importance of a holistic approach to hardware-software co-design for scalable and efficient deep learning deployment.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MONET**，一个专门用于在异构数据流加速器（HDA）上建模和优化神经网络训练过程的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然针对神经网络“推理”的软硬件协同设计已非常成熟，但“训练”阶段的建模仍面临巨大挑战。
*   **核心问题**：训练工作负载与推理有显著不同，主要体现在：
    *   **内存压力**：训练需要存储前向传播的激活值以供反向传播使用，且优化器状态（如 Adam 的动量）会消耗大量内存。
    *   **计算复杂性**：反向传播引入了新的算子和数据流需求。
    *   **现有工具局限**：现有的建模工具（如 Timeloop, ZigZag）大多专注于推理，无法捕捉训练特有的内存足迹和层融合（Layer Fusion）机会。
*   **整体含义**：MONET 旨在填补这一空白，提供一个从边缘设备到数据中心均适用的训练建模框架，帮助设计者在硬件架构、调度策略和算法权衡之间找到最优解。

### 2. 方法论：核心思想与关键技术
MONET 基于现有的推理建模框架 **Stream** 进行了扩展，核心思想是将训练过程转化为可调度的计算图。
*   **工作流集成**：
    1.  **图生成**：将 PyTorch 模型导出为 ONNX，利用 ONNX Runtime Training 生成包含前向、反向和优化器更新的完整训练图。
    2.  **算子分解**：引入自定义 ONNX 转换 pass，将复杂的梯度算子（如 `ConvGrad`）分解为 Stream 可识别的细粒度原语。
*   **关键技术细节**：
    *   **约束优化层融合（Layer Fusion）**：提出一种基于整数规划（IP）的求解器，在满足内存约束和算子兼容性的前提下，自动寻找最优的层融合子图，以减少 off-chip 内存访问。
    *   **激活检查点（Activation Checkpointing）优化**：针对层融合带来的非线性重计算成本，采用遗传算法（NSGA-II）在内存节省、延迟和功耗之间寻找帕累托最优（Pareto-optimal）配置。
    *   **异构建模**：支持对处理元件（PE）阵列、内存层次结构和互连带宽的详细参数化建模。

### 3. 实验设计
*   **实验场景**：
    *   **边缘训练**：使用 **ResNet-18** 模型，在类 **Edge TPU** 的异构加速器上进行图像分类任务建模。
    *   **云端/数据中心训练**：使用小型 **GPT-2** 模型，在 **FuseMax**（一种针对 Attention 优化的架构）上进行 NLP 任务建模。
*   **Benchmark 与对比方法**：
    *   对比了“逐层调度（Layer-by-layer）”与“手动融合”以及“MONET 自动融合”的效果。
    *   对比了推理与训练在相同硬件配置下的性能分布差异。
    *   评估了不同 PE 规模、内存带宽和 SIMD 单元配置对训练效率的影响。

### 4. 资源与算力
*   **建模工具性质**：MONET 本身是一个分析性建模框架（模拟器），而非直接运行训练的引擎。
*   **算力支持**：
    *   文中提到使用 **NVIDIA RTX 3090** GPU 来测量 ResNet-50 的实际内存消耗基准（用于验证模型准确性）。
    *   实验计算资源由法国国家研究机构（ANR）和 GENCI-IDRIS 提供。
*   **未明确说明项**：论文未详细列出运行 MONET 模拟器本身所需的具体 CPU/GPU 时间，但强调了其搜索空间探索的效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 Edge TPU 进行了广泛的参数搜索（涉及 PE 数量、SIMD 单元、内存大小等多个维度）。
    *   对层融合策略进行了消融实验（不同子图长度限制）。
    *   对激活检查点进行了多代遗传算法迭代。
*   **充分性评价**：实验设计较为充分，涵盖了卷积神经网络（CNN）和 Transformer 两种主流架构，并对比了推理与训练的帕累托前沿，客观地证明了“推理优化配置不等于训练优化配置”的观点。

### 6. 主要结论与发现
*   **硬件不对称性**：针对推理优化的硬件架构在训练任务中往往不是最优的。例如，在训练中，较大的 PE 在高算力预算下更节能，但在延迟优化上不如较小的 PE 灵活。
*   **层融合的价值**：自动层融合算法能显著降低延迟和功耗，且优于手动设计的融合方案。
*   **非线性成本**：证明了在层融合环境下，激活检查点的重计算成本具有非线性特征，传统的线性规划（MILP）无法准确建模，必须使用启发式算法（如遗传算法）。
*   **内存权衡**：通过激活检查点，可以在仅增加约 4% 延迟的情况下，节省大量激活内存。

### 7. 优点
*   **端到端建模**：首次实现了在异构加速器上对包含反向传播和优化器更新的完整训练流进行细粒度建模。
*   **模块化设计**：基于 ONNX 的转换 pass 具有通用性，可以轻松扩展到其他算子或硬件后端。
*   **自动化程度高**：引入约束优化和遗传算法，减少了人工调节层融合和检查点策略的负担。

### 8. 不足与局限
*   **硬件验证局限**：目前主要基于周期精确或解析模型进行模拟，缺乏在真实自研训练芯片上的流片验证数据。
*   **GPU 适配性**：虽然提到了 GPU，但目前框架核心仍侧重于特定领域加速器（DSA），对通用 GPU 复杂的控制流和 Warp 调度建模尚不够深入。
*   **规模限制**：实验主要集中在 ResNet-18 和小型 GPT-2，对于超大规模模型（如千亿参数 LLM）的分布式训练（如 3D 并行策略）建模在本文中尚未完全展开。

（完）
