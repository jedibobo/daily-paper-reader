---
title: Practical FP4 Training for Large-Scale MoE Models on Hopper GPUs
title_zh: Hopper GPU 上大规模 MoE 模型的实用 FP4 训练
authors: "Wuyue Zhang, Chongdong Huang, Chunbo You, Cheng Gu, Fengjuan Wang, Mou Sun"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02731v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对无原生支持GPU的FP4训练软硬协同设计
tldr: "针对大规模MoE模型在Hopper GPU上训练时面临的显存和通信瓶颈，本文提出了一种实用的FP4训练方案。尽管Hopper架构缺乏原生FP4支持，研究者通过引入FP8与FP4之间的直接转换技术及缩放感知的行列转换机制，实现了在FP8计算核心基础上利用MXFP4压缩激活值和专家并行通信。该方法在671B参数模型上减少了14.8%的激活显存，并将吞吐量提升了12.5%，且不影响模型收敛。"
selection_source: carryover_cache
motivation: 大规模MoE模型训练受限于激活显存和专家并行通信带宽，且Hopper GPU缺乏原生FP4硬件支持。
method: 提出一种将FP4集成到BF16/FP8混合训练流水线的方法，通过直接的FP8-FP4转换和缩放感知的行列转换实现高效压缩。
result: "在671B参数规模下，该方法减少了14.8%的峰值激活显存，并将训练吞吐量从每秒1157个token提升至1302个。"
conclusion: 通过软硬件协同设计，即使在缺乏原生硬件支持的情况下，也能在大型MoE模型训练中实现FP4级别的效率提升。
---

## 摘要
训练大规模混合专家（MoE）模型受限于激活内存和专家并行通信，然而在缺乏原生 MXFP4 或 NVFP4 支持的 Hopper 级 GPU 上，FP4 训练仍然难以实现。在这项工作中，我们提出了一种训练方案，使 MoE 模型在没有原生 4 位计算支持的 Hopper 架构上也能实现 MXFP4 的效率。核心挑战在于如何将 FP4 集成到现有的 BF16/FP8 混合训练流水线中，而不产生昂贵的精度转换开销（例如 FP4 ↔ BF16 ↔ FP8）。我们通过引入直接的 FP8 到 FP4 量化与反量化，以及感知缩放的 FP4 行转列转换解决了这一挑战，从而以极低的开销实现了 FP4 激活和专家并行通信。核心 MoE 计算在 FP8 中执行，而激活和专家并行通信则使用 MXFP4 进行压缩，在不降低收敛性的情况下实现了显著的内存和带宽节省。在 671B 参数规模下，我们的方法实现了与强 FP8 基准相当的端到端训练性能，同时将峰值激活内存减少了 14.8% (11.8 GB)，并将训练吞吐量提高了 12.5%，从每 GPU 每秒 1157 个 token 提升至 1302 个 token。这些结果表明，通过精细的软硬件协同设计，即使在没有原生 FP4 Tensor Core 支持的情况下，大规模 MoE 训练也能在实践中实现 FP4 的效率。

## Abstract
Training large-scale Mixture-of-Experts (MoE) models is bottlenecked by activation memory and expert-parallel communication, yet FP4 training remains impractical on Hopper-class GPUs without native MXFP4 or NVFP4 support. In this work, we present a training recipe that enables MXFP4 efficiency for MoE models on Hopper architectures without native 4-bit computation support. A central challenge is to integrate FP4 into an existing BF16/FP8 hybrid training pipeline without incurring costly precision round-trips (e.g., FP4 $\leftrightarrow$ BF16 $\leftrightarrow$ FP8). We address this challenge by introducing direct FP8-to-FP4 quantization and de-quantization, together with scaling-aware FP4 row-wise to column-wise conversion, enabling FP4 activations and expert-parallel communication with minimal overhead. Core MoE computations are executed in FP8, while activations and expert-parallel communication are compressed using MXFP4, achieving substantial memory and bandwidth savings without degrading convergence. At the 671B parameter scale, our method achieves end-to-end training performance comparable to strong FP8 baselines, while reducing peak activation memory by 14.8\% (11.8 GB) and improving training throughput by 12.5\%, from 1157 to 1302 tokens per GPU per second. These results show that FP4 efficiency can be practically realized for large-scale MoE training through careful software-hardware co-design, even without native FP4 Tensor Core support.

---

## 论文详细总结（自动生成）

这是一份关于论文《Practical FP4 Training for Large-Scale MoE Models on Hopper GPUs》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：大规模混合专家模型（MoE）在训练时面临严重的**激活显存（Activation Memory）**和**专家并行通信（All-to-All Communication）**瓶颈。
*   **硬件鸿沟**：虽然 NVIDIA Blackwell 架构原生支持 FP4，但当前主流的 Hopper 架构（如 H100）缺乏原生 FP4 计算和通信指令。
*   **研究动机**：探索如何在不具备原生硬件支持的情况下，通过软硬件协同设计，在 Hopper GPU 上实现 FP4 级别的存储和通信效率，同时不损失模型收敛精度。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用**计算与存储精度解耦**的策略。在计算密集型的 GEMM 算子中使用 FP8（利用 Hopper 的 Tensor Core），而在激活值缓存（用于重计算）和专家并行通信中使用 MXFP4 压缩。
*   **关键技术细节**：
    *   **直接 FP8-FP4 转换算法**：设计了直接的位运算转换逻辑，绕过昂贵的 BF16 中间转换层（避免了 FP4 $\leftrightarrow$ BF16 $\leftrightarrow$ FP8 的开销）。
    *   **分层缩放对齐（Hierarchical Scaling Alignment）**：解决 MXFP4（块大小 32）与 FP8（块大小 128）之间缩放因子粒度不匹配的问题，通过取最大值并补偿指数位实现对齐。
    *   **非对称设计**：前向传播使用 FP4 压缩以节省显存和带宽；反向传播中，由于梯度量化开销大于通信节省，主动回退到 FP8 流程以优化端到端吞吐。
    *   **高性能 Kernel 融合**：开发了融合量化、反量化、转置及处理 MoE 不规则张量（Ragged Tensors）的 CUDA Kernel，减少了显存读写次数。

### 3. 实验设计
*   **模型对象**：671B 参数的大规模 MoE 模型（采用 MLA 架构，类似 DeepSeek-V3 配置）以及 236B 的中型 MoE 模型。
*   **对比基准（Benchmarks）**：
    1.  **BF16**：标准 Megatron-LM 混合精度训练。
    2.  **FP8**：Transformer Engine 提供的块缩放（Blockwise）FP8 训练方案。
*   **评估指标**：训练吞吐量（Tokens per GPU per Second, TGS）、峰值显存占用率、收敛损失（Loss）曲线。

### 4. 资源与算力
*   **硬件环境**：使用了 32 个节点，共计 **256 张 NVIDIA Hopper GPU (80GB HBM3)**。
*   **互联技术**：节点内 NVSwitch，节点间 InfiniBand。
*   **训练时长**：文中未明确给出 671B 模型的总训练时长，但在收敛性验证中，使用 16B 模型消耗了 **160B tokens** 进行测试。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 16B（收敛性验证）到 236B 和 671B（性能验证）的多个量级。
*   **消融实验**：详细对比了算子融合前后的加速比（1.6x-1.9x）、不同重计算策略下的显存收益，以及量化带来的精度偏差（MXFP4 相对 BF16 仅有 +0.61% 的微小偏差）。
*   **客观性**：实验在相同硬件和优化器设置下进行，对比了工业界最强的 FP8 基准，结果具有较高的说服力。

### 6. 主要结论与发现
*   **显存节省**：在 671B 模型上，MXFP4 将峰值激活显存降低了 **14.8% (11.8 GB)**。
*   **吞吐提升**：由于显存压力减小，可以减少重计算（Recomputation）的范围，使端到端训练吞吐量从 1157 提升至 1302 TGS，增幅达 **12.5%**。
*   **通信优化**：专家并行通信负载减少了约 **50%**。
*   **收敛性**：MXFP4 的损失函数轨迹与 BF16 基准高度吻合，证明了该方案在生产环境中的可行性。

### 7. 优点
*   **实用性强**：无需等待下一代硬件，直接在现有 Hopper 集群上释放了 FP4 的红利。
*   **深度优化**：针对 MoE 架构的 Ragged Tensor 和 All-to-All 通信做了专门的 Kernel 级优化，而非通用的量化工具。
*   **开源贡献**：提供了完整的代码实现，包括对 Megatron-LM 和 Transformer Engine 的补丁。

### 8. 不足与局限
*   **软件模拟开销**：由于缺乏原生指令，FP4 到 FP8 的转换仍存在一定的计算开销，在非 MoE 架构（如稠密模型）中，这种开销可能会抵消通信收益。
*   **反向传播限制**：目前在反向传播中无法完全应用 FP4，限制了进一步提升的空间。
*   **硬件依赖**：虽然解决了 Hopper 的支持问题，但其核心逻辑仍高度依赖 NVIDIA 的位运算特性，向其他架构（如 AMD 或国产芯片）迁移需要重新开发底层 Kernel。

（完）
