---
title: "vAccSOL: Efficient and Transparent AI Vision Offloading for Mobile Robots"
title_zh: vAccSOL：面向移动机器人的高效且透明的 AI 视觉任务卸载
authors: "Adam Zahir, Michele Gucciardom Falk Selker, Anastasios Nanos, Kostis Papazafeiropoulos, Carlos J. Bernardos, Nicolas Weber, Roberto Gonzalez"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16685v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于异构边缘平台优化推理的神经网络编译器
tldr: 针对移动机器人在执行复杂视觉任务时面临的计算资源受限和高功耗挑战，本文提出 vAccSOL 框架。该框架结合了 SOL 神经网络编译器和 vAccel 轻量级执行框架，实现了硬件优化的推理以及在机器人本地或边缘基础设施之间的透明任务调度。vAccSOL 无需修改应用程序即可提升推理效率，显著降低了机器人能耗并提高了视觉处理帧率，为延长电池供电机器人的作业时间提供了高效解决方案。
selection_source: fresh_fetch
motivation: 移动机器人运行现代视觉模型时受限于嵌入式硬件的计算能力和严格的功耗约束，且现有加速器缺乏灵活性。
method: 集成 SOL 编译器生成低依赖的优化推理库，并利用 vAccel 框架实现视觉任务在本地与边缘端之间的透明卸载。
result: "实验表明，该框架相比 PyTorch 可降低机器人端 80% 的功耗，并将视觉流水线帧率提升高达 24 倍。"
conclusion: vAccSOL 通过高效的编译优化和灵活的卸载机制，在不改变应用逻辑的前提下显著提升了机器人的感知性能和续航能力。
---

## 摘要
移动机器人越来越多地被部署用于巡检、巡逻和搜救行动，依赖计算机视觉进行感知、导航和自主决策。然而，由于计算资源有限和严格的能量约束，在机载设备上执行现代视觉工作负载具有挑战性。虽然一些平台包含嵌入式加速器，但这些加速器通常与专有软件栈绑定，导致用户定义的工作负载只能在资源受限的伴随计算机上运行。我们提出了 vAccSOL，这是一个用于在异构机器人和边缘平台上高效且透明地执行基于 AI 的视觉工作负载的框架。vAccSOL 集成了两个组件：SOL，一个生成具有最小运行时依赖项的优化推理库的神经网络编译器；以及 vAccel，一个轻量级执行框架，可透明地将推理任务调度到机器人本地或附近的边缘基础设施。这种结合实现了硬件优化的推理和灵活的执行位置分配，且无需修改机器人应用程序。我们在一个包含商用四足机器人和 12 个深度学习模型（涵盖图像分类、视频分类和语义分割）的真实测试平台上评估了 vAccSOL。与 PyTorch 编译器基准相比，SOL 实现了相当或更好的推理性能。通过边缘卸载，与 PyTorch 相比，vAccSOL 将机器人端的功耗降低了高达 80%，边缘端的功耗降低了高达 60%，同时将视觉流水线的帧率提高了多达 24 倍，延长了电池供电机器人的运行寿命。

## Abstract
Mobile robots are increasingly deployed for inspection, patrol, and search-and-rescue operations, relying on computer vision for perception, navigation, and autonomous decision-making. However, executing modern vision workloads onboard is challenging due to limited compute resources and strict energy constraints. While some platforms include embedded accelerators, these are typically tied to proprietary software stacks, leaving user-defined workloads to run on resource-constrained companion computers.   We present vAccSOL, a framework for efficient and transparent execution of AI-based vision workloads across heterogeneous robotic and edge platforms. vAccSOL integrates two components: SOL, a neural network compiler that generates optimized inference libraries with minimal runtime dependencies, and vAccel, a lightweight execution framework that transparently dispatches inference locally on the robot or to nearby edge infrastructure. This combination enables hardware-optimized inference and flexible execution placement without requiring modifications to robot applications.   We evaluate vAccSOL on a real-world testbed with a commercial quadruped robot and twelve deep learning models covering image classification, video classification, and semantic segmentation. Compared to a PyTorch compiler baseline, SOL achieves comparable or better inference performance. With edge offloading, vAccSOL reduces robot-side power consumption by up to 80% and edge-side power by up to 60% compared to PyTorch, while increasing vision pipeline frame rate by up to 24x, extending the operating lifetime of battery-powered robots.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **vAccSOL** 的框架，旨在解决移动机器人在运行现代 AI 视觉任务时面临的计算资源不足和功耗过高的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：移动机器人（如四足机器人、无人机）日益依赖复杂的深度学习模型（如语义分割、动作识别）进行自主决策。然而，这些模型计算量巨大，而机器人机载设备受限于体积、载荷和电池容量，难以在保证实时性的同时维持长续航。
*   **核心挑战**：
    1.  **资源受限**：嵌入式 CPU 算力不足，而机载 GPU 往往被厂商封闭或与安全关键控制回路竞争资源。
    2.  **功耗压力**：机载高强度计算会迅速耗尽电池。
    3.  **部署复杂**：现有的加速工具（如 TensorRT）通常绑定特定硬件，且在本地与边缘端切换时需要大量修改代码。

### 2. 方法论
vAccSOL 结合了编译器优化与透明卸载技术，核心思想是将“如何执行”与“在哪里执行”解耦。
*   **SOL 编译器（编译层）**：
    *   将 PyTorch/ONNX 模型转换为高层中间表示（HLIR）。
    *   执行全局图优化（如层融合、常量折叠）。
    *   利用**深度优先并行化（DFP）**技术生成针对特定硬件（CPU/GPU）优化的独立共享库（`.so`）。
    *   **特点**：生成的二进制文件不依赖庞大的深度学习框架运行时，且支持快速自动调优。
*   **vAccel 框架（运行层）**：
    *   采用客户端-代理（Client-Agent）架构。
    *   通过统一的 **GenOp 接口** 拦截推理请求。
    *   **透明卸载**：应用层只需调用 `infer()`，框架根据配置自动决定是在机器人本地 CPU 运行，还是通过轻量级 RPC 将任务发送到边缘服务器执行。

### 3. 实验设计
*   **实验场景**：在真实的室内走廊环境，使用 **Unitree Go2 EDU 四足机器人** 进行实地测试。
*   **任务与模型**：涵盖 12 个深度学习模型，分为三类：
    1.  **图像分类**：ResNet50, Swin-T, Swin-S, Swin-V2-B。
    2.  **视频动作识别**：R3D-18, R2Plus1D-18, Swin3D-S, Swin3D-B。
    3.  **语义分割**：DeepLabV3-ResNet50/101, FCN-ResNet50/101。
*   **对比基准（Benchmark）**：
    *   **Torch 编译器**：PyTorch 2.0 的 `torch.compile`（Inductor 后端）。
    *   **执行配置对比**：本地 CPU 执行 vs. 边缘 CPU 执行 vs. 边缘 GPU 执行。

### 4. 资源与算力
*   **机器人端（机载伴随电脑）**：Intel N100 CPU (4核, 3.4 GHz), 12 GB RAM。
*   **边缘服务器**：NVIDIA GeForce RTX 5080 Ti GPU, Intel Core Ultra 9 275HX CPU (24核, 6.5 GHz), 32 GB RAM。
*   **网络环境**：Wi-Fi 6 (ASUS RT-AX89X 路由器)。
*   **训练/编译说明**：论文重点在于推理阶段。模型为预训练模型，SOL 编译过程在分钟级完成。

### 5. 实验数量与充分性
*   **实验规模**：针对 12 个模型在 6 种不同配置（SOL/Torch × 本地/边缘CPU/边缘GPU）下进行了详尽测试。
*   **测量指标**：包括端到端帧率（FPS）、延迟分解（预处理、推理、网络传输、序列化）、以及通过 Intel RAPL 和 NVIDIA NVML 接口获取的精确功耗数据。
*   **客观性**：实验在真实机器人运动轨迹中进行，考虑了 ROS2 消息传递的开销，对比了当前最先进的 PyTorch 编译后端，实验设计较为全面且具有实际参考价值。

### 6. 主要结论与发现
*   **性能飞跃**：通过边缘 GPU 卸载，视觉流水线的帧率最高提升了 **24 倍**（如 Swin-V2-B 模型）。
*   **大幅节能**：边缘卸载使机器人端的功耗降低了 **45% 至 80%**，显著延长了作业寿命。
*   **编译器优势**：在边缘端，SOL 编译的模型比 PyTorch 节省了高达 **60%** 的功耗，且在 Transformer 类模型上表现出更好的推理效率。
*   **瓶颈分析**：对于语义分割等任务，由于输出张量巨大，网络传输和序列化开销成为了主要瓶颈，而非推理时间。

### 7. 优点与亮点
*   **完全透明**：开发者无需修改任何视觉应用逻辑，即可实现计算位置的动态切换。
*   **轻量化部署**：SOL 生成的独立二进制文件极大地减少了机器人端的软件依赖和存储占用。
*   **硬件无关性**：vAccel 的 GenOp 接口屏蔽了底层硬件差异，实现了真正的异构计算抽象。

### 8. 不足与局限
*   **网络环境理想化**：实验基于稳定的 Wi-Fi 6 环境，未深入探讨在弱网、高丢包或 5G 切换场景下的系统鲁棒性。
*   **序列化开销**：vAccel 在处理大规模数据（如高分辨率分割掩码）时，通用的序列化机制引入了额外延迟，相比专用插件效率略低。
*   **缺乏动态调度**：目前本地与远程的切换仍依赖静态配置，尚未实现根据实时网络波动或剩余电量自动决策的智能调度算法。

（完）
