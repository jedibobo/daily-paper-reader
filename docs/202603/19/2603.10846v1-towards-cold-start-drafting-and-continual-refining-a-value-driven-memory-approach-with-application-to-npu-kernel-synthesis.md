---
title: "Towards Cold-Start Drafting and Continual Refining: A Value-Driven Memory Approach with Application to NPU Kernel Synthesis"
title_zh: 迈向冷启动草拟与持续精炼：一种价值驱动的记忆方法及其在 NPU 算子内核合成中的应用
authors: "Yujie Zheng, Zhuo Li, Shengtao Zhang, Hanjing Wang, Junjie Sheng, Jiaqian Wang, Junchi Yan, Weinan Zhang, Ying Wen, Bo Tang, Muning Wen"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10846v1"
tags: ["query:hwnas"]
score: 8.0
evidence: NPU算子合成与特定领域架构编程
tldr: 针对新兴NPU架构中编程数据稀缺导致的“数据墙”问题，本文提出EvoKernel框架。该框架采用基于价值驱动记忆的强化学习方法，通过冷启动起草与持续优化机制，实现了算子内核合成的自动化。它利用Q值检索机制和跨任务记忆共享，显著提升了通用大模型在特定硬件生态下的代码生成正确率与执行性能。
selection_source: fresh_fetch
motivation: 解决大语言模型在数据稀缺的NPU编程领域面临的性能骤降及“数据墙”挑战。
method: 提出一种基于价值驱动记忆的智能体框架EvoKernel，通过Q值检索和跨任务记忆共享实现内核的自动起草与迭代优化。
result: "在NPU KernelBench上将模型正确率从11.0%提升至83.0%，并实现中位数3.60倍的性能加速。"
conclusion: 价值引导的经验积累能使通用模型在缺乏数据的利基硬件生态中掌握复杂的内核合成任务。
---

## 摘要
将大语言模型部署到数据稀缺的编程领域面临着重大挑战，特别是在新兴的领域特定架构（DSA）上的算子内核合成，其中“数据墙”限制了可用的训练数据。虽然模型在 CUDA 等数据丰富的平台上表现出色，但在 NPU 编程等数据稀缺的生态系统中，其性能会出现灾难性的下降。为了在无需昂贵微调的情况下克服这一冷启动障碍，我们引入了 EvoKernel，这是一个自我演进的智能体框架，能够自动化从初始草拟到持续精炼的算子内核合成全生命周期。EvoKernel 通过将合成过程建模为基于记忆的强化学习任务来解决这一问题。通过一种新颖的价值驱动检索机制，它学习特定阶段的 Q 值，根据经验对当前目标的贡献（无论是引导生成可行草案还是迭代优化延迟）来优先排序经验。此外，通过启用跨任务记忆共享，智能体能够将从简单算子中获得的见解推广到复杂算子。通过构建 KernelBench 的 NPU 变体并进行评估，EvoKernel 将前沿模型的正确率从 11.0% 提升至 83.0%，并通过迭代精炼实现了相对于初始草案 3.60 倍的中位数加速。这表明，价值引导的经验积累使通用模型能够掌握利基硬件生态系统中的算子内核合成任务。我们的官方页面位于 https://evokernel.zhuo.li。

## Abstract
Deploying Large Language Models to data-scarce programming domains poses significant challenges, particularly for kernel synthesis on emerging Domain-Specific Architectures where a "Data Wall" limits available training data. While models excel on data-rich platforms like CUDA, they suffer catastrophic performance drops on data-scarce ecosystems such as NPU programming. To overcome this cold-start barrier without expensive fine-tuning, we introduce EvoKernel, a self-evolving agentic framework that automates the lifecycle of kernel synthesis from initial drafting to continual refining. EvoKernel addresses this by formulating the synthesis process as a memory-based reinforcement learning task. Through a novel value-driven retrieval mechanism, it learns stage-specific Q-values that prioritize experiences based on their contribution to the current objective, whether bootstrapping a feasible draft or iteratively refining latency. Furthermore, by enabling cross-task memory sharing, the agent generalizes insights from simple to complex operators. By building an NPU variant of KernelBench and evaluating on it, EvoKernel improves frontier models' correctness from 11.0% to 83.0% and achieves a median speedup of 3.60x over initial drafts through iterative refinement. This demonstrates that value-guided experience accumulation allows general-purpose models to master the kernel synthesis task on niche hardware ecosystems. Our official page is available at https://evokernel.zhuo.li.

---

## 论文详细总结（自动生成）

### 论文总结：EvoKernel —— 价值驱动的 NPU 算子内核合成框架

#### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）在数据丰富的领域（如 CUDA 编程）表现优异，但在新兴的领域特定架构（DSA，如华为昇腾 NPU 的 Ascend C）中面临“数据墙”挑战。由于缺乏公开代码和文档，模型在这些领域的性能会发生灾难性下降（如 GPT-5.2 在 CUDA 上正确率为 92%，但在 Ascend C 上仅为 14%）。
*   **研究动机**：传统的微调（SFT）成本过高且依赖专家数据，而简单的检索增强生成（RAG）在数据库稀缺时效果不佳。本文旨在探索如何让智能体在无专家演示、无昂贵微调的情况下，自主从零开始掌握严苛的内核合成任务。

#### 2. 方法论
EvoKernel 将内核合成建模为**基于记忆的马尔可夫决策过程（M-MDP）**，核心思想是通过“自我演进的记忆”来积累经验。
*   **价值驱动检索（Value-Driven Retrieval）**：引入特定阶段的 Q 值来评估记忆项的效用。
    *   **草拟阶段（Drafting, $Q_1$）**：评估记忆项对实现“功能正确性”的贡献。
    *   **精炼阶段（Refining, $Q_2$）**：评估记忆项对“降低延迟”的贡献。
*   **统一价值更新**：采用蒙特卡洛（MC）更新规则，根据验证器的反馈（奖励 $r$）动态调整 Q 值，无需更新模型权重。
*   **两阶段流水线**：
    1.  **冷启动草拟**：利用 $\epsilon$-greedy 策略检索高价值经验，生成初步可行的内核。
    2.  **持续精炼**：在可行内核基础上，结合性能分析（Profiler）反馈，检索优化技巧进行迭代性能提升。
*   **多门控验证器（Multi-Gate Verifier）**：包含防作弊（防止模型调用 PyTorch 原生 API 绕过内核编写）、编译检查、正确性校验和延迟分析四个环节。

#### 3. 实验设计
*   **数据集/场景**：
    *   **KernelBench (NPU 版)**：将原有的 GPU 算子基准适配至 Ascend C 环境，包含 L1（基础）和 L2（融合/复杂）算子。
    *   **Attention Set**：包含 70 个注意力机制相关的算子。
    *   **mHC Kernels**：针对 DeepSeek 架构的最新流形约束超连接内核。
*   **对比方法**：
    *   **Pass@k**：无状态的多次采样基准。
    *   **Refinement**：仅针对单个算子进行迭代修复的智能体（无跨任务记忆）。
    *   **Codex (OpenAI)**：具备 Shell 和文件系统访问权限的自主进化智能体。
*   **评估指标**：编译率（CR）、正确率（Acc）、加速比（Speedup）。

#### 4. 资源与算力
*   **硬件环境**：实验主要在华为昇腾（Ascend）平台上运行，使用 `msprof` 工具进行硬件级性能分析。
*   **算力细节**：文中提到在验证阶段使用了 Ascend 910B NPU 进行算子执行和性能评测。
*   **迭代预算**：每个算子设定了严格的 $T=30$ 次迭代预算。
*   **模型**：使用了 GPT-5.2、DeepSeek-V3.2 和 Qwen3-Coder-30B 作为基础生成器。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 250 个 KernelBench 算子、70 个 Attention 算子及 15 个 mHC 内核，实验规模较大。
*   **消融实验**：
    *   对比了“价值驱动检索”与“启发式检索”的差异。
    *   验证了“跨任务记忆共享”相对于“单任务精炼”的优越性。
    *   测试了记忆在不同模型（如从 GPT-5.2 到 DeepSeek）之间的迁移能力。
*   **充分性评价**：实验设计较为全面，不仅验证了正确性提升，还深入探讨了经验迁移和课程学习（从简单到复杂算子）的涌现现象，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能飞跃**：EvoKernel 将前沿模型的正确率从 11.0% 提升至 **83.0%**。
*   **性能优化**：在获得正确内核后，通过迭代精炼实现了中位数 **3.60 倍** 的加速，部分算子甚至超过了官方 Torch-NPU 的性能。
*   **跨任务迁移**：在简单算子（L1）上积累的记忆能显著加速复杂算子（L2）的解决过程，形成了一种“自动涌现的课程学习”。
*   **模型能力依赖**：发现模型的基础推理能力（In-Context Learning）越强，从价值驱动记忆中获益越多。

#### 7. 优点
*   **无需微调**：在数据极度稀缺的“冷启动”环境下，通过非参数化的记忆演进解决了特定领域编程问题。
*   **价值引导**：不同于传统的语义相似度检索，Q 值机制能精准识别哪些经验真正有助于解决当前的编译或性能瓶颈。
*   **鲁棒的验证体系**：防作弊门控确保了生成的代码是真正的硬件内核，而非逻辑绕过。

#### 8. 不足与局限
*   **对基础模型依赖性强**：实验显示弱模型（如 Qwen3-30B）在复杂算子上的提升有限，说明该方法仍受限于底座模型的理解上限。
*   **迭代成本**：虽然无需微调，但每个算子 30 次的迭代和硬件评测仍需一定的推理时间和设备调试资源。
*   **领域覆盖**：目前主要集中在 Ascend C 和 CUDA，对于其他更小众的 DSA 架构（如类脑芯片）尚未验证。

（完）
