---
title: "K-Search: LLM Kernel Generation via Co-Evolving Intrinsic World Model"
title_zh: K-Search：通过协同演化内在世界模型实现 LLM 算子生成
authors: "Shiyi Cao, Ziming Mao, Joseph E. Gonzalez, Ion Stoica"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19128v2"
tags: ["query:hwnas"]
score: 8.0
evidence: 机器学习系统的自动GPU内核生成与优化
tldr: 针对GPU算子优化中LLM缺乏规划能力且易受中间实现错误干扰的问题，本文提出K-Search框架。该框架引入协同演化的内在世界模型，将高层算法规划与底层程序实例化解耦，利用LLM的领域知识引导搜索。在FlashInfer和GPUMode任务中，K-Search显著超越了现有进化搜索方法，在复杂MoE算子上实现高达14.3倍的性能提升，并刷新了H100上的TriMul性能记录。
selection_source: carryover_cache
motivation: 现有的自动化GPU算子优化方法缺乏显式规划能力，难以处理需要多步协同结构变换的复杂算子。
method: 提出协同演化的世界模型，通过解耦高层规划与底层实现，利用LLM的先验知识在非单调优化路径中进行主动探索。
result: 在FlashInfer多样化算子上平均提升2.10倍性能，并在GPUMode TriMul任务中超越了人类设计和现有进化方案。
conclusion: K-Search证明了结合世界模型的LLM搜索框架能有效解决复杂硬件算子优化难题，具有极高的实用价值。
---

## 摘要
优化 GPU 算子对于高效的现代机器学习系统至关重要，但由于设计因素之间复杂的相互作用以及硬件的快速演进，这仍然是一项具有挑战性的任务。现有的自动化方法通常仅将大语言模型（LLM）视为启发式引导演化循环中的随机代码生成器。这些方法在处理需要协调的多步结构变换的复杂算子时往往表现不佳，因为它们缺乏显式的规划能力，并且经常由于中间实现效率低下或错误而放弃有前景的策略。为了解决这一问题，我们提出了“通过协同演化世界模型进行搜索”的方法，并据此构建了 K-Search。通过将静态搜索启发式替换为协同演化的世界模型，我们的框架利用 LLM 的先验领域知识来引导搜索，主动探索优化空间。该方法显式地将高层算法规划与底层程序实例化解耦，使系统能够导航非单调的优化路径，同时对临时的实现缺陷保持鲁棒性。我们在来自 FlashInfer 的多种复杂算子上评估了 K-Search，包括 GQA、MLA 和 MoE 算子。结果表明，K-Search 显著优于最先进的演化搜索方法，在复杂 MoE 算子上实现了平均 2.10 倍的性能提升，最高可达 14.3 倍的增益。在 GPUMode TriMul 任务中，K-Search 在 H100 上达到了最先进的性能，耗时仅为 1030us，超越了此前的演化方法和人工设计的方案。

## Abstract
Optimizing GPU kernels is critical for efficient modern machine learning systems yet remains challenging due to the complex interplay of design factors and rapid hardware evolution. Existing automated approaches typically treat Large Language Models (LLMs) merely as stochastic code generators within heuristic-guided evolutionary loops. These methods often struggle with complex kernels requiring coordinated, multi-step structural transformations, as they lack explicit planning capabilities and frequently discard promising strategies due to inefficient or incorrect intermediate implementations. To address this, we propose Search via Co-Evolving World Model and build K-Search based on this method. By replacing static search heuristics with a co-evolving world model, our framework leverages LLMs' prior domain knowledge to guide the search, actively exploring the optimization space. This approach explicitly decouples high-level algorithmic planning from low-level program instantiation, enabling the system to navigate non-monotonic optimization paths while remaining resilient to temporary implementation defects. We evaluate K-Search on diverse, complex kernels from FlashInfer, including GQA, MLA, and MoE kernels. Our results show that K-Search significantly outperforms state-of-the-art evolutionary search methods, achieving an average 2.10x improvement and up to a 14.3x gain on complex MoE kernels. On the GPUMode TriMul task, K-Search achieves state-of-the-art performance on H100, reaching 1030us and surpassing both prior evolution and human-designed solutions.

---

## 论文详细总结（自动生成）

以下是对论文《K-Search: LLM Kernel Generation via Co-Evolving Intrinsic World Model》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代机器学习系统极度依赖高性能 GPU 算子（Kernel），但手动优化这些算子面临设计空间巨大、硬件演进极快（如从 Hopper 到 Blackwell）以及实现成本高昂的挑战。
*   **研究动机**：现有的自动化方法（如 OpenEvolve）通常将大语言模型（LLM）视为简单的随机代码生成器，嵌入在基于启发式的进化算法中。这种方式缺乏显式的规划能力，且容易因为中间步骤的临时代码错误或性能下降而过早放弃具有潜力的优化路径，难以处理需要多步协同结构变换的复杂算子。

### 2. 方法论：协同演化内在世界模型
K-Search 的核心思想是将算子生成建模为一个受“世界模型”引导的结构化搜索树规划问题。
*   **核心思想**：将高层的**算法规划（Intent）**与底层的**程序实例化（Implementation）**显式解耦。LLM 充当世界模型，负责维护搜索状态并预测优化意图的潜力。
*   **关键技术流程**：
    1.  **动作选择（Action Selection）**：从搜索树的前沿（Frontier）中，根据世界模型预测的优先级分数 $V$ 选择最有潜力的优化动作（如“应用 Split-K 优化”）。
    2.  **局部细化（Local Refinement）**：针对选定动作，LLM 作为随机策略生成具体代码。系统允许在一定步数（停滞限制 $K$）内进行多次尝试，确保不会因临时的语法错误而丢失正确的优化方向。
    3.  **世界模型协同演化（World Model Update）**：LLM 分析执行反馈（编译日志、性能数据），通过上下文学习（In-Context Learning）更新搜索树：**插入**新假设、**更新**已有节点的优先级、**剪枝**无效分支。

### 3. 实验设计
*   **实验场景**：针对 FlashInfer 库中的高性能算子进行评估，包括 GQA（分组查询注意力）、MLA（多级注意力，DeepSeek-V3 核心）和 MoE（混合专家模型）算子。
*   **Benchmark**：以专家手工优化的 FlashInfer 算子作为性能基准（Score=100 代表达到专家水平）。
*   **对比方法**：
    *   **OpenEvolve**：基于质量-多样性搜索的开源进化算法。
    *   **ShinkaEvolve**：基于种群的演化框架。
*   **特殊任务**：参与了 GPUMode TriMul（三角形乘法更新）竞赛任务，挑战 AlphaFold3 中的核心模块。

### 4. 资源与算力
*   **硬件平台**：主要在 NVIDIA **H100** (80GB HBM3) 和 **B200** GPU 上进行实验。
*   **软件环境**：CUDA 12.8, FlashInfer 0.5.3, PyTorch 2.8.0。
*   **模型使用**：主要实验使用 **Gemini-3-Pro-Preview**；在 TriMul 任务中使用了 **GPT-5.2** 和 Gemini-3-Pro 的组合。
*   **搜索预算**：每个算子固定为 120 次迭代（即 120 次编译与评估过程）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 类代表性复杂算子，总计 152 个不同的测试负载（Workload Traces）。
*   **充分性**：每组实验重复 3 次并报告均值与范围（min-max band）。实验不仅对比了最终性能，还通过搜索轨迹可视化（Trace Visualization）展示了模型如何进行逻辑推理和剪枝。整体实验设计较为客观，使用了统一的编译链和验证套件。

### 6. 主要结论与发现
*   **性能飞跃**：K-Search 平均性能是 OpenEvolve 的 **2.10 倍**，在复杂的 MoE 算子上实现了 **14.3 倍** 的惊人提升。
*   **刷新纪录**：在 GPUMode TriMul 任务中，K-Search 达到了 **1030 μs** 的延迟，超越了所有已知的人类设计方案和基于强化学习（RL）的自动化方案。
*   **规划价值**：实验证明，解耦规划与实现能让系统导航“非单调”路径（即中间步骤性能下降但最终大幅提升的路径）。

### 7. 优点
*   **智能规划**：引入世界模型使 LLM 能够像人类专家一样进行长程规划，而非盲目尝试代码变体。
*   **容错性强**：局部细化机制有效隔离了“逻辑正确但代码有 Bug”的情况，提高了搜索效率。
*   **动态演化**：搜索状态随经验积累而进化，能够自动识别并放弃死胡同，将算力集中在有希望的方向。

### 8. 不足与局限
*   **特定负载下的偏差**：在 GQA 实验中，K-Search 倾向于生成适用于大 Batch 的复杂并行策略（如 Split-K），导致在 Batch Size=1 的极小负载下性能反而不如简单的基准。
*   **模型依赖性**：该框架的效果高度依赖于底层 LLM 的推理和领域知识水平，使用较弱的模型可能无法维持有效的世界模型。
*   **搜索成本**：虽然比进化算法更高效，但 120-300 次的编译/评估循环在实时性要求极高的场景下仍显稍慢。

（完）
