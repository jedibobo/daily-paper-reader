---
title: Explainable and Hardware-Efficient Jamming Detection for 5G Networks Using the Convolutional Tsetlin Machine
title_zh: 基于卷积 Tsetlin 机的 5G 网络可解释且硬件高效的干扰检测
authors: "Vojtech Halenka, Mohammadreza Amini, Per-Arne Andersen, Ole-Christoffer Granmo, Burak Kantarci"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07336v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对FPGA部署的硬件高效位级推理
tldr: 本研究针对5G网络中低功率干扰难以检测的问题，提出了一种基于卷积Tsetlin机（CTM）的轻量级、可解释且硬件高效的干扰检测方案。该方法直接处理5G同步信号块（SSB）特征，通过布尔逻辑子句实现位级推理。实验表明，CTM在保持较高检测准确率的同时，训练速度比CNN快9.5倍，内存占用减少14倍，非常适合资源受限的边缘FPGA部署。
selection_source: fresh_fetch
motivation: 5G网络易受低功率干扰攻击，而传统深度学习模型在边缘设备上存在计算资源消耗大且缺乏可解释性的挑战。
method: 提出利用卷积Tsetlin机（CTM）对5G同步信号块特征进行布尔逻辑建模，实现高效的位级干扰检测与FPGA部署。
result: "在真实5G测试集上，CTM实现了91.53%的准确率，且训练速度比CNN快9.5倍，内存需求降低了14倍。"
conclusion: CTM为5G边缘干扰检测提供了一种兼顾性能、可解释性与硬件效率的实用方案，是未来B5G安全应用的有力竞争者。
---

## 摘要
第五代 (5G) 网络中的所有应用都依赖于稳定的射频 (RF) 环境，以支持移动性、自动化和连接智能中的关键任务服务。暴露于故意干扰或低功率干扰会威胁其可用性和可靠性，特别是当此类攻击处于链路层可观测性之下时。本文研究了使用直接运行在 5G 同步信号块 (SSB) 特征上的卷积 Tsetlin 机 (CTM) 进行轻量级、可解释且硬件高效的干扰检测。CTM 在量化输入上构建布尔逻辑子句，从而实现位级推理和在 FPGA 架构上的确定性部署。这些特性使 CTM 非常适合 5G 中预期的实时、资源受限的边缘环境。所提出的方法在真实的 5G 测试平台上使用空中接口 (OTA) SSB 数据进行了实验验证，模拟了实际的下行链路条件。我们在相同的预处理和训练流水线下，将 CTM 与卷积神经网络 (CNN) 基准进行了对比测试。在真实数据集上，CTM 实现了相当的检测性能（准确率为 91.53 ± 1.01，而 CNN 为 96.83 ± 1.19），同时训练速度快 9.5 倍，内存需求减少 14 倍（45 MB 对比 624 MB）。此外，我们概述了针对 Zybo Z7 (Zynq-7000) 的紧凑型 FPGA 设计，并提供了在针对延迟、功耗和准确性权衡而优化的三种部署配置下的资源预测（未实测）。结果表明，对于射频域干扰检测，CTM 为传统深度神经网络 (DNN) 提供了一种实用、可解释且资源高效的替代方案，使其成为边缘部署、低延迟和安全关键型 5G 应用的有力候选者，同时也为 B5G 系统奠定了基础。

## Abstract
All applications in fifth-generation (5G) networks rely on stable radio-frequency (RF) environments to support mission-critical services in mobility, automation, and connected intelligence. Their exposure to intentional interference or low-power jamming threatens availability and reliability, especially when such attacks remain below link-layer observability. This paper investigates lightweight, explainable, and hardware-efficient jamming detection using the Convolutional Tsetlin Machine (CTM) operating directly on 5G Synchronization Signal Block (SSB) features. CTM formulates Boolean logic clauses over quantized inputs, enabling bit-level inference and deterministic deployment on FPGA fabrics. These properties make CTM well suited for real-time, resource-constrained edge environments anticipated in 5G. The proposed approach is experimentally validated on a real 5G testbed using over-the-air SSB data, emulating practical downlink conditions. We benchmark CTM against a convolutional neural network (CNN) baseline under identical preprocessing and training pipelines. On the real dataset, CTM achieves comparable detection performance (Accuracy 91.53 +/- 1.01 vs. 96.83 +/- 1.19 for CNN) while training $9.5\times$ faster and requiring 14x less memory (45~MB vs.\ 624~MB). Furthermore, we outline a compact FPGA-oriented design for Zybo~Z7 (Zynq-7000) and provide resource projections (not measured) under three deployment profiles optimized for latency, power, and accuracy trade-offs. The results show that the CTM provides a practical, interpretable, and resource-efficient alternative to conventional DNNs for RF-domain jamming detection, establishing it as a strong candidate for edge-deployed, low-latency, and security-critical 5G applications while laying the groundwork for B5G systems.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何利用卷积 Tsetlin 机（CTM）在 5G 网络边缘实现高效、可解释的干扰检测。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：5G 网络极易受到射频（RF）干扰（Jamming）的攻击，尤其是低功率干扰，它们往往能避开传统的链路层监控。
*   **研究动机**：
    *   **现有方案局限性**：目前的深度学习（如 CNN）虽然准确率高，但模型庞大、计算成本高、缺乏透明度（黑盒模型），难以部署在资源受限的 5G 边缘设备或 IoT 平台上。
    *   **5G 特性利用**：5G 的同步信号块（SSB）具有确定的结构，其失真可以揭示物理层的干扰情况。
    *   **需求**：需要一种既能保持高准确率，又具备可解释性、低内存占用且易于硬件（如 FPGA）实现的检测方案。

### 2. 核心方法论
*   **核心思想**：采用**卷积 Tsetlin 机（CTM）**代替传统的卷积神经网络（CNN）。CTM 基于命题逻辑，通过学习布尔逻辑子句（Boolean logic clauses）来识别模式，而非使用浮点数权重。
*   **关键技术细节**：
    *   **特征提取**：直接从空中接口（OTA）捕获的 5G SSB 信号中提取主同步信号（PSS）的时频特征（谱图）。
    *   **布尔化（Binarization）**：由于 Tsetlin 机处理布尔输入，论文对比了多种二值化方法，最终采用 **Enhanced Otsu（增强大津法）**，通过结合原始图像及其旋转版本的阈值处理来捕捉边缘模式。
    *   **CTM 架构**：利用 Tsetlin 自动机（TA）在量化的输入补丁上构建逻辑子句。推理过程仅涉及位运算（AND/OR/NOT），天然适合 FPGA 的查找表（LUT）架构。
    *   **可解释性**：通过统计逻辑子句中文字（Literals）的出现频率，可以直观地看到模型关注的是频谱中的哪些特定资源块。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 ThinkRF RTSA R5500 频谱分析仪在真实的 5G FR1 n71 频段捕获数据。
    *   通过射频合成器注入合成干扰信号，模拟真实的下行链路干扰环境。
*   **Benchmark（基准）**：
    *   一个包含约 120 万个参数的 **CNN 模型**（包含 4 个卷积块和 2 个全连接层）。
*   **对比维度**：检测准确率、训练时间、推理吞吐量、内存占用以及硬件资源消耗预测。

### 4. 资源与算力
*   **硬件环境**：实验在 **Mac M1 Pro (16GB RAM, 2021款)** 上运行。
*   **训练时长**：
    *   **CTM**：约 **322 秒**。
    *   **CNN**：约 **3108 秒**（CTM 训练速度快了约 9.5 倍）。
*   **内存占用**：
    *   **CTM**：45.06 MB。
    *   **CNN**：623.69 MB（CTM 节省了约 14 倍内存）。

### 5. 实验数量与充分性
*   **实验规模**：采用了 **5 折交叉验证（5-fold cross-validation）** 以确保结果的统计显著性。
*   **消融与对比实验**：
    *   对比了 4 种不同的预处理（二值化）方法（Enhanced Otsu, Paper Original, Multi-Level, Adaptive Threshold）。
    *   使用 Optuna 框架进行了 50 次超参数优化实验。
    *   **公平性**：CTM 和 CNN 在相同的预处理流水线和数据集划分下进行测试。实验设计较为客观，充分考虑了边缘计算的实际约束。

### 6. 主要结论与发现
*   **性能权衡**：CNN 的准确率略高（96.83%），而 CTM 达到了 **91.53%**。但在边缘部署场景下，CTM 的效率优势弥补了准确率的微小差距。
*   **硬件潜力**：CTM 的位级推理特性使其在 FPGA（如 Zybo Z7）上的部署具有极高的资源效率，预计仅需数万个 LUT 即可实现数千次/秒的推理。
*   **可解释性验证**：通过可视化发现，CTM 倾向于利用低频段的完整时间槽特征进行分类，而 CNN 则关注更局部的特定频谱区域。

### 7. 优点
*   **极高的训练效率**：训练速度比 CNN 快一个数量级，适合需要频繁更新模型的动态 RF 环境。
*   **硬件友好性**：模型极小（45MB），且逻辑运算特性使其在 FPGA 上的功耗和延迟具有高度确定性。
*   **透明度**：逻辑子句形式使得模型决策过程可追踪、可解释，符合安全关键型应用的要求。

### 8. 不足与局限
*   **准确率差距**：在绝对准确率上仍落后于 CNN 约 5 个百分点，对于极度追求精度的场景可能受限。
*   **通用硬件推理速度**：在通用 CPU/GPU 上，由于缺乏针对位运算优化的库，CTM 的推理吞吐量（46 samples/s）目前低于 CNN（78 samples/s）。其优势需在专用硬件（FPGA/ASIC）上才能完全体现。
*   **干扰类型覆盖**：实验主要针对特定功率和波形的干扰，未来需验证其对更复杂、自适应干扰攻击的泛化能力。

（完）
