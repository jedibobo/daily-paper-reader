---
title: "TinyGLASS: Real-Time Self-Supervised In-Sensor Anomaly Detection"
title_zh: TinyGLASS：实时自监督传感器内异常检测
authors: "Pietro Bonazzi, Rafael Sutter, Luigi Capogrosso, Mischa Buob, Michele Magno"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16451v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对边缘平台传感器内实时检测的轻量化适配
tldr: 针对工业质检中异常样本稀缺及边缘设备算力受限的问题，本文提出TinyGLASS，一种专为索尼IMX500传感器设计的轻量化自监督异常检测框架。通过将WideResNet-50骨干网络替换为ResNet-18，并结合INT8量化与静态图追踪技术，实现了在极低内存占用下的实时检测。该模型在保持高精度的同时，显著降低了参数量和功耗，为嵌入式视觉传感器的实时异常检测提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的自监督异常检测模型计算开销过大，难以部署在资源受限的边缘视觉传感器上进行实时检测。
method: 采用轻量化ResNet-18骨干网络，并利用索尼模型压缩工具包进行INT8量化和部署优化，以适配IMX500传感器的内存限制。
result: "在MVTec-AD数据集上达到94.2%的AUROC，参数量压缩8.7倍，在8MB内存限制下实现20 FPS的实时推理。"
conclusion: TinyGLASS在保证检测性能和鲁棒性的前提下，实现了极高的能效比和实时性，证明了在智能传感器内进行复杂异常检测的可行性。
---

## 摘要
异常检测在工业质量控制中起着关键作用，尽管缺乏有标签的故障样本，仍必须识别出缺陷。最近的自监督方法（如 GLASS）仅使用无缺陷数据学习正常的视觉模式，并在工业基准测试中表现出强大的性能。然而，它们的计算需求限制了在资源受限的边缘平台上的部署。本研究介绍了 TinyGLASS，这是 GLASS 框架的一种轻量化适配版本，专为在索尼 IMX500 智能视觉传感器上进行实时传感器内异常检测而设计。所提出的架构将原始的 WideResNet-50 骨干网络替换为紧凑的 ResNet-18，并引入了面向部署的修改，从而能够使用索尼的模型压缩工具包进行静态图追踪和 INT8 量化。除了在 MVTec-AD 基准上评估性能外，我们还研究了对受污染训练数据的鲁棒性，并引入了一个名为 MMS 数据集的自定义工业数据集，用于跨设备评估。实验结果表明，TinyGLASS 在保持具有竞争力的检测性能的同时，实现了 8.7 倍的参数压缩，在 MVTec-AD 上达到了 94.2% 的图像级 AUROC，并在 IMX500 平台 8 MB 的内存限制下以 20 FPS 的速度运行。系统分析表明其具有低功耗（每次推理 4.0 mJ）、实时端到端延迟（20 FPS）和高能效（470 GMAC/J）。此外，该模型在适度水平的训练数据污染下仍能保持稳定的性能。

## Abstract
Anomaly detection plays a key role in industrial quality control, where defects must be identified despite the scarcity of labeled faulty samples. Recent self-supervised approaches, such as GLASS, learn normal visual patterns using only defect-free data and have shown strong performance on industrial benchmarks. However, their computational requirements limit deployment on resource-constrained edge platforms.   This work introduces TinyGLASS, a lightweight adaptation of the GLASS framework designed for real-time in-sensor anomaly detection on the Sony IMX500 intelligent vision sensor. The proposed architecture replaces the original WideResNet-50 backbone with a compact ResNet-18 and introduces deployment-oriented modifications that enable static graph tracing and INT8 quantization using Sony's Model Compression Toolkit.   In addition to evaluating performance on the MVTec-AD benchmark, we investigate robustness to contaminated training data and introduce a custom industrial dataset, named MMS Dataset, for cross-device evaluation. Experimental results show that TinyGLASS achieves 8.7x parameter compression while maintaining competitive detection performance, reaching 94.2% image-level AUROC on MVTec-AD and operating at 20 FPS within the 8 MB memory constraints of the IMX500 platform.   System profiling demonstrates low power consumption (4.0 mJ per inference), real-time end-to-end latency (20 FPS), and high energy efficiency (470 GMAC/J). Furthermore, the model maintains stable performance under moderate levels of training data contamination.

---

## 论文详细总结（自动生成）

以下是对论文《TinyGLASS: Real-Time Self-Supervised In-Sensor Anomaly Detection》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限的边缘设备（特别是智能视觉传感器内部）实现高精度的实时工业异常检测。
*   **研究动机**：
    *   **样本稀缺性**：工业场景中缺陷样本难以获取，自监督学习（仅利用正常样本训练）是主流方向。
    *   **算力瓶颈**：现有的高性能自监督模型（如 GLASS）通常依赖大型骨干网络（如 WideResNet-50），计算开销巨大，无法在内存仅有几 MB 的边缘芯片上运行。
    *   **实时性需求**：工业产线要求极低的延迟和高能效比，传统的云端或边缘服务器方案在带宽和功耗上存在局限。

### 2. 方法论
TinyGLASS 是对 GLASS 框架的轻量化适配，核心思想是通过架构优化和量化技术实现“传感器内（In-Sensor）”计算。
*   **骨干网络轻量化**：将原始的 WideResNet-50 替换为 ResNet-18。为了进一步节省资源，推理时仅执行到 `layer3`，将有效参数量从 24.9M 压缩至 2.85M（约 8.7 倍压缩）。
*   **部署感知修改**：
    *   **静态图追踪**：移除 PyTorch hooks，修改前向传播逻辑以支持静态形状追踪，确保兼容索尼模型压缩工具包（MCT）。
    *   **张量重塑优化**：消除动态形状操作，全程保持 4D 张量表示，以便使用标准的 Conv2D 操作。
*   **量化技术**：使用 INT8 量化（权重和激活值），以适配索尼 IMX500 传感器的硬件加速器。
*   **损失函数**：结合了二元交叉熵（BCE）损失和 Focal Loss，以解决正常区域与缺陷区域之间的类别不平衡问题。
*   **双重得分机制**：继承了 GLASS 的全局异常得分（GAS）和局部异常得分（LAS），兼顾粗粒度图像级偏差和细粒度空间局部差异。

### 3. 实验设计
*   **数据集**：
    *   **MVTec-AD**：工业异常检测的标准基准，包含 15 类工业产品。
    *   **MMS Dataset（自定义）**：作者引入的新数据集，包含微型组件的四种类别（正常、裂纹孔、划痕、减半），并使用高分辨率显微镜和 IMX500 传感器分别成像，用于跨设备评估。
*   **基准对比（Benchmark）**：主要对比对象是原始的 **GLASS (WideResNet-50)** 模型，旨在衡量轻量化带来的性能损失与资源收益。
*   **评估指标**：图像级 AUROC (I-AUROC) 和像素级 AUROC (P-AUROC)。

### 4. 资源与算力
*   **训练算力**：使用了 **3 台 NVIDIA RTX A6000 GPU** 进行训练。
*   **训练配置**：最大 200 个 epoch，batch size 为 8，使用 AdamW 优化器。
*   **推理平台**：**Sony IMX500** 智能视觉传感器（集成在 Raspberry Pi AI Camera 中）。
*   **硬件约束**：目标平台内存限制为 **8 MB**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 MVTec-AD 全集上进行了性能测试。
    *   针对“地毯（carpet）”类别和 MMS 数据集进行了**训练集污染实验**（污染率 0% 到 30%），验证模型在非纯净数据下的鲁棒性。
    *   进行了详细的系统性能分析（功耗、延迟、吞吐量）。
*   **充分性评价**：实验设计较为全面，不仅关注算法精度，还深入探讨了硬件部署指标和现实场景中的数据污染问题。对比实验虽然主要集中在与原版 GLASS 的纵向对比，但对于 TinyML 领域的论文来说，其硬件指标的详尽程度具有很高的说服力。

### 6. 主要结论与发现
*   **性能表现**：TinyGLASS 在 MVTec-AD 上达到 **94.2% 的 I-AUROC**，相比原版 GLASS 仅下降了约 7.7 个百分点，但参数量减少了 8.7 倍。
*   **硬件效率**：在 IMX500 上实现了 **20 FPS** 的实时推理，总内存占用仅 **5.41 MB**。
*   **能效比**：每次推理功耗仅 **4.0 mJ**，能效高达 **470 GMAC/J**。
*   **鲁棒性**：在 10% 以内的训练数据污染下，性能下降轻微，证明了其在不完美标注环境下的实用性。

### 7. 优点
*   **端到端部署**：这是首个在传感器内（In-Sensor）实现实时视觉异常检测的工作，具有极强的工程实践意义。
*   **部署友好性**：针对硬件约束（静态图、算子限制、内存限制）做了深度的架构级重构，而非简单的模型剪枝。
*   **开源贡献**：引入了新的 MMS 工业数据集，并承诺开源代码和模型权重，有助于推动该领域的研究。

### 8. 不足与局限
*   **性能权衡**：相比 SOTA 模型，为了适配边缘硬件，在像素级定位精度（P-AUROC）上有较明显的下降（约 7.4%）。
*   **对比范围**：实验主要对比了 GLASS 框架内部的轻量化效果，缺乏与其他针对边缘设计的异常检测模型（如 PatchCore 的轻量化版本）的横向对比。
*   **应用限制**：目前主要针对 2D 工业图像，对于更复杂的 3D 缺陷或动态场景的适用性尚未探讨。

（完）
