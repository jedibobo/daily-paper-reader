---
title: Reviving ConvNeXt for Efficient Convolutional Diffusion Models
title_zh: 为高效卷积扩散模型重振 ConvNeXt
authors: "Taesung Kwon, Lorenzo Bianchi, Lennart Wittke, Felix Watine, Fabio Carrara, Jong Chul Ye, Romann Weber, Vinicius Azevedo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09408v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 卷积扩散模型中的硬件友好性与参数效率
tldr: 本研究针对扩散模型日益转向Transformer架构的趋势，重新审视了卷积神经网络的优势，提出了全卷积扩散模型（FCDM）。FCDM借鉴ConvNeXt设计，在保持高效参数利用和硬件友好性的同时，显著提升了训练效率。实验表明，FCDM在仅使用DiT一半计算量的情况下，以更少的训练步数达到了极具竞争力的生成效果，为高效生成模型提供了新选择。
selection_source: fresh_fetch
motivation: 旨在探索卷积神经网络在现代生成模型中被忽视的局部偏置、参数效率和硬件友好性等优势。
method: 提出了一种基于ConvNeXt架构改进的、专为条件扩散建模设计的全卷积扩散模型（FCDM）。
result: FCDM-XL在仅需DiT-XL/2一半计算量的情况下，以快7倍以上的训练速度实现了同等水平的生成性能。
conclusion: 现代卷积设计是扩展扩散模型的高效替代方案，证明了ConvNeXt在高效生成建模中的强大潜力。
---

## 摘要
近期的扩散模型越来越青睐 Transformer 骨干网络，这主要归功于全注意力架构卓越的可扩展性。然而，局部性偏差、参数效率和硬件友好性——这些使卷积网络（ConvNets）成为高效视觉骨干网络的属性——在现代生成建模中的探索却非常有限。在本文中，我们引入了全卷积扩散模型（FCDM），该模型的骨干网络类似于 ConvNeXt，但专为条件扩散建模而设计。我们发现，在仅使用 DiT-XL/2 50% FLOPs 的情况下，FCDM-XL 在 256×256 和 512×512 分辨率下，分别以减少 7 倍和 7.5 倍的训练步数实现了具有竞争力的性能。值得注意的是，FCDM-XL 可以在 4-GPU 系统上进行训练，突显了我们架构卓越的训练效率。我们的结果表明，现代卷积设计为扩展扩散模型提供了一种极具竞争力且高效的替代方案，使 ConvNeXt 重新成为高效生成建模中简单而强大的构建模块。

## Abstract
Recent diffusion models increasingly favor Transformer backbones, motivated by the remarkable scalability of fully attentional architectures. Yet the locality bias, parameter efficiency, and hardware friendliness--the attributes that established ConvNets as the efficient vision backbone--have seen limited exploration in modern generative modeling. Here we introduce the fully convolutional diffusion model (FCDM), a model having a backbone similar to ConvNeXt, but designed for conditional diffusion modeling. We find that using only 50% of the FLOPs of DiT-XL/2, FCDM-XL achieves competitive performance with 7$\times$ and 7.5$\times$ fewer training steps at 256$\times$256 and 512$\times$512 resolutions, respectively. Remarkably, FCDM-XL can be trained on a 4-GPU system, highlighting the exceptional training efficiency of our architecture. Our results demonstrate that modern convolutional designs provide a competitive and highly efficient alternative for scaling diffusion models, reviving ConvNeXt as a simple yet powerful building block for efficient generative modeling.

---

## 论文详细总结（自动生成）

以下是对论文《Reviving ConvNeXt for Efficient Convolutional Diffusion Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在扩散模型领域，Transformer 架构（如 DiT）因其卓越的可扩展性逐渐取代了卷积网络（ConvNets）。然而，Transformer 带来的高性能是以巨大的计算资源、高昂的 GPU 成本和复杂的硬件优化为代价的。
*   **研究动机**：作者重新审视了卷积网络的优势——**局部性归纳偏置（Locality Bias）**、**参数效率**和**硬件友好性**。他们试图证明，通过现代化的卷积设计（如 ConvNeXt），全卷积架构在生成任务中不仅能匹配 Transformer 的性能，还能在效率和收敛速度上实现显著超越。

### 2. 方法论：FCDM 架构设计
作者提出了 **全卷积扩散模型（FCDM）**，其核心思想是将 ConvNeXt 的分类骨干网络改造为适用于条件扩散生成的架构：
*   **ConvNeXt 模块改进**：保留了 7×7 深度卷积（Depthwise Conv）和反向瓶颈（Inverted Bottleneck）结构。
*   **条件注入（Conditional Injection）**：引入了 **自适应层归一化（AdaLN）**。通过一个轻量级 MLP 将时间步（Time）和类别（Class）嵌入映射为缩放和偏移参数（$\gamma, \beta, \alpha$），用于调制归一化后的特征。
*   **易扩展的 U 型架构**：不同于传统的复杂 U-Net，FCDM 仅通过两个超参数——**块数量 $L$** 和 **隐藏通道数 $C$** 来控制缩放。在下采样阶段，通道数和块数均翻倍，形成对称的编码器-解码器结构。
*   **全局响应归一化（GRN）**：采用 ConvNeXt V2 中的 GRN 层来减少通道冗余，增强通道激活的多样性，相比于其他卷积扩散模型（如 DiCo）使用的注意力机制，GRN 更加参数高效。

### 3. 实验设计
*   **数据集**：主要在 **ImageNet-1K** 上进行实验，涵盖 256×256 和 512×512 两种分辨率。
*   **Benchmark（基准）**：以 **DiT (Diffusion Transformer)** 为核心对比对象，确保参数量对齐。
*   **对比方法**：
    *   **Transformer 类**：DiT, SiT, U-ViT, MaskDiT, DiG (Gated Linear Attention)。
    *   **卷积/混合类**：ADM (U-Net), LDM, DiC, DiCo。
    *   **生成对抗网络**：BigGAN, StyleGAN-XL。
*   **评估指标**：FID（主要指标）、sFID、Inception Score (IS)、Precision/Recall、FLOPs 和 Throughput（吞吐量）。

### 4. 资源与算力
论文详细记录了训练所需的算力，展示了该模型极高的硬件效率：
*   **FCDM-XL (256×256)**：在 **4 台 NVIDIA RTX 4090 (24GB)** 组成的消费级系统上即可训练。使用梯度检查点时，训练速度约为 0.9 it/s，全局 Batch Size 为 256。
*   **FCDM-XL (512×512)**：在 **4 台 NVIDIA H100 (80GB)** 上训练，速度约为 0.7 it/s。
*   **内存效率**：作者确认 Batch Size 256 可以完全装入单张 A100 (40GB) 显存中。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 Small (S) 到 XLarge (XL) 四个模型量级，参数量从 32.7M 扩展到 698.8M。
*   **消融实验**：非常充分。作者测试了卷积核大小（3x3 到 11x11）、GRN 的有效性、反向瓶颈的作用、不同 VAE 的影响、以及非对称架构和块缩放策略。
*   **公平性**：实验严格遵循 DiT 的训练协议（AdamW, 学习率 1e-4, 无权重衰减），确保性能提升源于架构改进而非训练技巧。

### 6. 主要结论与发现
*   **极高的计算效率**：在相同参数量下，FCDM 的 **FLOPs 仅为 DiT 的约 50%**，且拥有更高的推理吞吐量。
*   **更快的收敛速度**：在 256×256 分辨率下，FCDM-XL 达到同等 FID 所需的训练步数比 DiT-XL/2 **少 7 倍**；在 512×512 下则 **少 7.5 倍**。
*   **性能优势**：FCDM-XL 在 ImageNet 256×256 上实现了 2.03 的 FID，优于大多数同类模型，且在更高分辨率下表现出更好的扩展性（分辨率翻倍时，DiT 吞吐量下降 4 倍，而 FCDM 仅下降 2 倍）。

### 7. 优点（亮点）
*   **重振卷积信心**：挑战了“只有 Transformer 才能扩展”的流行观点，证明了纯卷积架构在生成建模中的潜力。
*   **硬件友好**：相比 Transformer 的注意力机制，卷积操作在各类硬件（尤其是消费级 GPU）上都有极佳的底层优化。
*   **设计简洁**：仅需两个超参数即可实现模型缩放，且无需复杂的算子（如 FlashAttention）。
*   **训练门槛低**：4 张 4090 即可训练 XL 级模型，对学术界和小型团队非常友好。

### 8. 不足与局限
*   **未达绝对 SOTA**：虽然在 DiT 风格的对比中领先，但其 FID 尚未超越采用更先进训练框架的模型（如 EDM-2 或 Simpler Diffusion）。
*   **任务覆盖有限**：目前主要集中在类别条件生成（Class-conditional），虽然补充了文本到图像（Text-to-Image）的初步实验，但尚未在超大规模图文数据集上进行验证。
*   **架构惯性**：由于采用了 U 型架构和卷积，对于极长程的全局依赖建模能力在理论上仍可能逊于全注意力机制（尽管实验结果显示在图像生成上这并非瓶颈）。

（完）
