---
title: "AutoKernel: Autonomous GPU Kernel Optimization via Iterative Agent-Driven Search"
title_zh: AutoKernel：基于迭代智能体驱动搜索的自主 GPU 算子优化
authors: "Jaber Jaber, Osama Jaber"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21331v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 机器学习系统的自主GPU算子优化
tldr: AutoKernel是一个开源的自主GPU算子优化框架，旨在解决高性能算子编写的高人力成本问题。它通过智能体循环，针对PyTorch模型自动识别瓶颈，并利用Triton或CUDA C++进行迭代优化。该系统包含严格的五阶段正确性验证，在H100上显著优于PyTorch原生及编译模式，为现代Transformer架构提供了高效的自动化优化方案。
selection_source: fresh_fetch
motivation: 高性能GPU算子的编写过程极其耗时耗力，且现有编译器在处理复杂模型时仍难以达到最优性能。
method: 采用基于智能体的迭代搜索机制，结合阿姆达尔定律识别瓶颈，并通过五阶段验证体系确保算子的正确性与稳定性。
result: 在H100上，AutoKernel优化的算子在RMSNorm等任务中比PyTorch原生快5.29倍，且在B200排行榜上取得第一名。
conclusion: AutoKernel证明了通过自主智能体驱动的迭代搜索可以有效实现超越现有编译器的高性能GPU算子自动优化。
---

## 摘要
编写高性能 GPU 算子（Kernel）是机器学习系统工程中最耗费人力的任务之一。我们提出了 AutoKernel，这是一个开源框架，它将自主智能体循环应用于任意 PyTorch 模型的 GPU 算子优化。给定一个模型，AutoKernel 会对其进行性能分析（Profiling）以识别计算瓶颈，根据阿姆达尔定律（Amdahl's law）的影响力对其进行排名，并在无需人工干预的情况下，通过数百次实验迭代优化 Triton 或 CUDA C++ 算子实现。一个包含冒烟测试、形状扫描（Shape Sweeps）、数值稳定性、确定性验证和边缘情况覆盖的五阶段正确性测试框架，确保在记录任何加速效果之前，每个候选算子都经过了验证。该系统包含超过 9,000 行 Python 代码、跨两个后端的 18 个初始算子实现、一套六级优化策略指南（Playbook），并集成了 KernelBench 基准测试套件。AutoKernel 涵盖了现代 Transformer 架构中主流操作的九种算子类型。在 NVIDIA H100 上，我们的 Triton 算子在大多数测试配置中均优于 PyTorch eager 模式和 torch.compile (max-autotune)：在 RMSNorm 上比 eager 模式快 5.29 倍，在 softmax 上快 2.82 倍，在交叉熵（cross-entropy）上快 2.21 倍；同时分别比 torch.compile 快 2.83 倍、3.44 倍和 2.94 倍。在社区部署中，经 AutoKernel 优化的算子在 vectorsum_v2 B200 排行榜上获得了第一名。完整系统可在 https://github.com/RightNow-AI/autokernel 获取。

## Abstract
Writing high-performance GPU kernels is among the most labor-intensive tasks in machine learning systems engineering. We present AutoKernel, an open-source framework that applies an autonomous agent loop to GPU kernel optimization for arbitrary PyTorch models. Given a model, AutoKernel profiles it to identify computational bottlenecks, ranks them by Amdahl's law impact, and iteratively refines Triton or CUDA C++ kernel implementations through hundreds of experiments without human intervention. A five-stage correctness harness covering smoke tests, shape sweeps, numerical stability, determinism verification, and edge-case coverage ensures every candidate kernel is validated before any speedup is recorded. The system comprises over 9,000 lines of Python, 18 starter kernel implementations across two backends, a six-tier optimization playbook, and integration with the KernelBench benchmark suite. AutoKernel covers nine kernel types spanning the dominant operations in modern transformer architectures. On an NVIDIA H100, our Triton kernels outperform both PyTorch eager and torch.compile (max-autotune) on the majority of tested configurations: 5.29x over eager on RMSNorm, 2.82x on softmax, and 2.21x on cross-entropy, while beating torch.compile by 2.83x, 3.44x, and 2.94x respectively. In community deployment, an AutoKernel-optimized kernel achieved first place on the vectorsum_v2 B200 leaderboard. The full system is available at https://github.com/RightNow-AI/autokernel.

---

## 论文详细总结（自动生成）

以下是对论文《AutoKernel: Autonomous GPU Kernel Optimization via Iterative Agent-Driven Search》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：编写高性能 GPU 算子（Kernel）是机器学习工程中最耗时且需要极高专业知识的任务。尽管有 cuBLAS 等供应商库，但深度学习架构的快速创新（如 SwiGLU、RMSNorm 等）往往超出了现有库的覆盖范围。
*   **研究背景**：现有的 LLM 在单次生成（One-shot）高性能算子方面表现不佳（如 KernelBench 评估显示成功率不足 20%）。开发者需要不断在分块策略、内存布局和硬件特性之间进行权衡。AutoKernel 旨在通过自主智能体（Agent）驱动的迭代搜索，将这一过程自动化。

### 2. 方法论：核心思想与技术细节
AutoKernel 采用了一个“分析-提取-优化-验证”的闭环系统，主要包含以下三个阶段：
*   **阶段 A：模型性能分析（Profiling）**：
    *   使用 `torch.profiler` 对完整的 PyTorch 模型进行分析，识别耗时最长的算子。
    *   **阿姆达尔定律（Amdahl's Law）**：根据算子在总运行时间中的占比分配优化优先级，确保优化工作集中在对端到端性能提升最大的瓶颈上。
*   **阶段 B：智能体驱动的迭代搜索**：
    *   **迭代循环**：受 Karpathy 的 `autoresearch` 启发，智能体修改算子代码 -> 运行基准测试 -> 若性能提升且通过验证则保留（Keep），否则回滚（Revert）。
    *   **双后端支持**：支持 Triton（快速迭代）和 CUDA C++（底层精细控制）。
    *   **六级优化策略指南**：为智能体提供结构化指令，涵盖分块大小调整、内存访问优化（如向量化加载）、计算优化（如算子融合）、高级技术（如 Split-K）、硬件特定优化（如 Hopper TMA）以及特定算子算法（如 Welford 算法）。
*   **阶段 C：五阶段正确性验证体系**：
    1.  **冒烟测试**：小规模输入快速捕捉编译错误。
    2.  **形状扫描**：测试 10 种以上不同维度和数据类型（FP16/BF16/FP32）。
    3.  **数值稳定性**：使用对抗性输入（如溢出/欠流）测试边缘情况。
    4.  **确定性验证**：确保多次运行输出位一致。
    5.  **边缘情况覆盖**：测试非 2 的幂次维度，确保边界处理正确。

### 3. 实验设计
*   **硬件环境**：NVIDIA H100 80GB GPU。
*   **对比基准（Baselines）**：
    *   **PyTorch Eager**：使用原生 cuBLAS 或 ATen 算子。
    *   **torch.compile (max-autotune)**：PyTorch 2.0 的官方编译器优化模式。
*   **测试场景**：涵盖 9 种核心算子类型（Matmul, FlashAttention, Softmax, LayerNorm, RMSNorm, CrossEntropy, Rotary Embedding, Reduce, Fused MLP）。
*   **集成工具**：集成了 KernelBench 基准测试套件（250 个标准化问题）。

### 4. 资源与算力
*   **算力设备**：主要在 NVIDIA H100 上进行测试，同时也支持 A100、RTX 4090 等及 AMD MI300 系列。
*   **实验时长**：单次迭代约 90 秒（30s 验证 + 30s 基准测试 + 30s 智能体推理）。
*   **运行规模**：一次典型的“过夜运行”（约 10 小时）可完成 300 到 400 次实验。

### 5. 实验数量与充分性
*   **实验规模**：论文对 7 种算子类型的 34 种生产环境配置进行了详细评估。
*   **充分性**：所有 34 种配置均通过了严格的五阶段正确性验证。此外，论文还引用了社区部署结果（如 B200 排行榜第一名和 FP4 Matmul 优化），证明了系统在实际竞争环境中的有效性。
*   **客观性**：通过固定的基准测试框架（不准智能体修改）和 Git 实验追踪，确保了结果的真实性和可重复性。

### 6. 主要结论与发现
*   **显著加速**：在 H100 上，AutoKernel 优化的算子在 RMSNorm 上比 Eager 模式快 **5.29 倍**，比 `torch.compile` 快 **2.83 倍**。
*   **访存密集型算子获益最大**：Softmax 和 Cross-Entropy 等算子通过融合减少了 HBM 访问，达到了硬件峰值带宽的 80% 以上。
*   **超越专家库**：在特定场景下（如 FP4 Matmul），AutoKernel 生成的 Triton 算子性能超过了 NVIDIA 官方的手工优化库 CUTLASS（提升 1.63x 至 2.15x）。
*   **编译器局限性**：实验发现 `torch.compile` 的自动调优并不总是能找到最优策略，AutoKernel 的针对性搜索能进一步挖掘硬件潜力。

### 7. 优点与亮点
*   **端到端自动化**：从模型 Profiling 到最终算子分发（支持导出至 HuggingFace Kernels）全流程无需人工干预。
*   **严谨的验证机制**：五阶段验证体系极大地降低了 AI 生成代码中常见的数值错误和边界溢出风险。
*   **策略导向**：将专家级优化知识（Playbook）转化为智能体可理解的指令，提升了搜索效率。
*   **开源贡献**：提供了 9,000 多行代码和丰富的初始算子库，具有很强的实用价值。

### 8. 不足与局限
*   **LLM 能力上限**：对于极其复杂的底层优化（如自定义 PTX 指令、多 CTA 协作策略），目前的智能体可能仍难以达到人类顶尖专家的水平。
*   **单卡限制**：目前主要针对单 GPU 算子优化，尚未涉及分布式算子或多设备内存管理。
*   **融合范围**：虽然支持算子内部融合，但跨算子的全局图优化（Cross-kernel fusion）仍有待进一步探索。

（完）
