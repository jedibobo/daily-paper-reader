---
title: "SIMPLER: Efficient Foundation Model Adaptation via Similarity-Guided Layer Pruning for Earth Observation"
title_zh: SIMPLER：通过相似度引导的层剪枝实现地球观测基础模型的高效适配
authors: "Víctor Barreiro, Johannes Jakubik, Francisco Argüello, Dora B. Heras"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19873v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 微调前的架构选择以降低推理和部署成本
tldr: 针对地球观测（EO）基础模型微调成本高的问题，本文提出SIMPLER方法。该方法在微调前通过计算未标记任务数据的层间表示相似度，自动识别并剪枝冗余层。SIMPLER无需梯度或超参数调节，在保持高性能的同时显著降低了训练和推理成本，在Prithvi-EO-2等模型上表现出极高的参数压缩率和加速比，具有跨任务和架构的通用性。
selection_source: fresh_fetch
motivation: 现有的微调优化方法要么无法降低推理成本，要么需要先进行昂贵的全量微调，缺乏一种在微调前降低模型复杂度的有效手段。
method: SIMPLER利用预训练视觉Transformer深层表示的稳定性，通过计算层间相似度评分自动识别并移除冗余层，无需梯度计算或超参数调节。
result: "在Prithvi-EO-2上，该方法在保留94%性能的前提下剪枝了79%的参数，使训练速度提升2.1倍，推理速度提升2.6倍。"
conclusion: SIMPLER提供了一种高效且通用的预微调架构选择方案，能显著降低地球观测基础模型在不同任务和模态下的计算与部署成本。
---

## 摘要
为地球观测（Earth Observation）微调基础模型计算成本高昂，在训练和部署阶段都面临极高的训练时间和内存需求。参数高效方法虽然降低了训练成本，但保留了完整的推理复杂度；而事后压缩仅在昂贵的全量微调之后才优化推理。我们提出了 SIMPLER，这是一种微调前的架构选择方法，通过在适配前确定有效的模型深度来降低推理和部署成本。SIMPLER 利用了预训练视觉 Transformer（Vision Transformer）深层中表示趋于稳定的特性：它在无标签任务数据上计算逐层表示相似度，并应用自动评分函数来选择冗余层，无需梯度、量级启发式方法或超参数调节。在 Prithvi-EO-2 上，SIMPLER 剪枝了高达 79% 的参数，同时保留了 94% 的基准性能，实现了 2.1 倍的训练加速和 2.6 倍的推理加速。该方法可推广至 TerraMind（一种多模态地球观测基础模型）和在 ImageNet 上预训练的 ViT-MAE，证明了其在不同任务、架构和光谱模态下的适用性。代码可在 https://gitlab.citius.gal/hpc4rs/simpler 获取。

## Abstract
Fine-tuning foundation models for Earth Observation is computationally expensive, with high training time and memory demands for both training and deployment. Parameter-efficient methods reduce training cost but retain full inference complexity, while post-hoc compression optimizes inference only after costly full fine-tuning. We introduce SIMPLER, a pre-fine-tuning architecture selection method that reduces inference and deployment costs by identifying an effective model depth before adaptation. SIMPLER exploits stabilization of representations in deeper layers of pre-trained vision transformers: it computes layer-wise representation similarity on unlabeled task data and applies an automated scoring function to select redundant layers, with no gradients, magnitude heuristics, or hyperparameter tuning required. On Prithvi-EO-2, SIMPLER prunes up to 79% of parameters while retaining 94% of baseline performance, yielding a 2.1x training speedup and 2.6x inference speedup. The method generalizes to TerraMind (a multimodal EO foundation model) and ImageNet-pretrained ViT-MAE, demonstrating applicability across tasks, architectures, and spectral modalities. Code is available at https://gitlab.citius.gal/hpc4rs/simpler.

---

## 论文详细总结（自动生成）

以下是对论文《SIMPLER: Efficient Foundation Model Adaptation via Similarity-Guided Layer Pruning for Earth Observation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：地球观测（EO）领域的基础模型（如 Prithvi-EO-2）参数量巨大（300M-600M），导致微调（Fine-tuning）和部署（Deployment）的计算成本极高。
*   **研究动机**：
    *   **现有方法的局限**：参数高效微调（PEFT，如 LoRA）虽能降低训练成本，但推理时仍需运行全量层；事后剪枝（Post-hoc Pruning）虽能优化推理，但必须先完成昂贵的全量微调。
    *   **需求**：需要在**微调开始前**识别并移除冗余的模型层，从而同时降低训练和推理的开销，使模型能够部署在卫星、无人机等边缘设备上。

### 2. 方法论：SIMPLER
*   **核心思想**：利用预训练视觉 Transformer（ViT）在处理特定任务数据时，深层表示会趋于稳定（即连续层产生的特征高度相似）的特性，在微调前自动确定最优截断深度。
*   **关键技术细节**：
    1.  **表示提取**：使用预训练模型在少量无标签任务数据（如 500 个样本）上进行前向传播，提取各层表示。
    2.  **相似度计算**：计算层间相似度矩阵 $Z$。研究对比了 CKA（中心化核对齐）、Jaccard 相似度和 SVCCA，发现 **CKA** 最为稳健。
    3.  **自动评分函数**：将矩阵 $Z$ 划分为保留块（左上）和剪枝块（右下）。通过计算保留层的多样性（Variability）与剪枝层的稳定性之差来评分，自动寻找最优截断点 $c^*$，无需人工调节超参数。
    4.  **轻量化适配**：仅保留前 $c^*$ 层，添加任务头后进行微调。该方法可与 LoRA 等 PEFT 技术结合使用。

### 3. 实验设计
*   **数据集/场景**：
    *   **MADOS**：海洋垃圾/溢油检测（弱监督语义分割）。
    *   **BigEarthNetv2**：多标签土地覆盖分类。
    *   **Sen4Map**：作物类型制图（时间序列分析）。
    *   **CIFAR-100**：通用 RGB 图像分类（用于验证通用性）。
*   **Benchmark 与对比方法**：
    *   **全量微调（Full Fine-tuning）**：基准性能。
    *   **LoRA**：仅优化训练参数的方法。
    *   **事后结构化剪枝（Post-hoc Pruning）**：基于权重量级的剪枝（20% 和 40% 压缩率）。
    *   **SIMPLER + LoRA**：本文提出的组合方案。
*   **基础模型**：Prithvi-EO-2 (300M/600M), TerraMind (L/S/T), ViT-MAE。

### 4. 资源与算力
*   **硬件**：使用了 NVIDIA **H200** GPU 集群（单卡或 4 卡并行，总显存达 128GB VRAM）。
*   **训练时长示例**：
    *   Prithvi-300M 在 BigEarthNetv2 上：基准全量微调需 ~169 分钟，SIMPLER 仅需 ~41 分钟。
    *   在 MADOS 上：基准需 ~16 分钟，SIMPLER 仅需 ~7.5 分钟。
*   **分析开销**：CKA 相似度分析在 CPU/RAM 上完成，500 个样本仅需约 218 秒，内存占用约 36.8GB。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同任务类型（分割、分类、时间序列）、3 种模型家族、多种模型规模（从 Tiny 到 Large）。
*   **消融实验**：
    *   对比了不同相似度指标（CKA 显著优于 Jaccard/SVCCA）。
    *   分析了样本量对结果稳定性的影响（500 个样本即达到稳定）。
    *   验证了预训练的重要性（对比了从头训练与微调）。
    *   使用随机噪声输入验证了表示稳定性源于学习到的特征而非架构偏差。
*   **充分性**：实验设计非常全面，不仅验证了 EO 领域，还跨越到自然图像领域，且每组实验均运行 5 次取平均值，结果具有统计学意义。

### 6. 主要结论与发现
*   **高效压缩**：在 Prithvi-EO-2 上，SIMPLER 剪枝了 **79%** 的参数，保留了 **94%** 的基准性能。
*   **显著加速**：实现了 **2.1 倍** 的训练加速和 **2.6 倍** 的推理加速。
*   **“以大减小”策略优于“原生小模型”**：将大型基础模型（TerraMind-L）剪枝后的性能（mIoU 58.8%）优于直接训练原生小模型（TerraMind-S, mIoU 53.0%），说明剪枝保留了高质量的预训练表示。
*   **通用性**：方法在多光谱 EO 数据和标准 RGB 数据上均有效。

### 7. 优点
*   **双重提效**：罕见地在微调前进行架构优化，同时解决了训练和推理的成本问题。
*   **自动化程度高**：无需梯度计算、无需量级启发式搜索、无需超参数调节。
*   **部署友好**：产出的是标准稠密模型，无需特殊的稀疏推理库即可在任何标准运行时（PyTorch/TensorFlow）部署。
*   **正向增益**：在某些小规模模型上，剪枝甚至起到了正则化作用，提升了性能。

### 8. 不足与局限
*   **预训练目标依赖**：该方法依赖于表示在深层趋于稳定的特性。对于采用显式防止特征崩溃机制的模型（如 DINOv3 使用 KoLeo 正则化），其表示可能呈现振荡而非稳定，此时 SIMPLER 可能失效。
*   **复杂任务的性能损失**：在处理具有复杂时间依赖性的任务（如 Sen4Map 时间序列）时，性能下降略多于简单分类任务，说明某些复杂任务仍需一定的模型深度。
*   **样本代表性**：虽然仅需 500 个样本，但如果这些样本不能代表下游任务的分布，可能会导致错误的层选择。

（完）
