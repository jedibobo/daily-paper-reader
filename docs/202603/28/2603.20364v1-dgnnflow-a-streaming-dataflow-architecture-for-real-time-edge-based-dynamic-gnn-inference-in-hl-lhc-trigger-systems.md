---
title: "DGNNFlow: A Streaming Dataflow Architecture for Real-Time Edge-based Dynamic GNN Inference in HL-LHC Trigger Systems"
title_zh: DGNNFlow：用于 HL-LHC 触发系统中实时基于边的动态 GNN 推理的流式数据流架构
authors: "Davendra Maharaj, Tu Pham, Peter Meiring, Kyungmin Park, Sena Durgut, Cong Hao, Matteo Cremonesi"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20364v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 用于FPGA上实时边缘GNN推理的流式数据流架构
tldr: 针对高亮度大型强子对撞机（HL-LHC）触发系统对实时、低功耗动态图神经网络（GNN）推理的需求，本文提出了DGNNFlow。这是一种新型流式数据流架构，专门支持边嵌入随运行时动态变化的GNN模型。通过硬件支持动态边计算、解决数据依赖以及辅助图构建，DGNNFlow在FPGA上实现了超低延迟推理，相比GPU和CPU在速度和功耗上均有显著优势。
selection_source: fresh_fetch
motivation: 现有的GNN加速器多基于静态图假设，无法满足HL-LHC触发系统中边嵌入随邻居节点动态变化的实时推理需求。
method: 提出DGNNFlow架构，通过硬件支持动态边嵌入计算、解决边与节点间的数据依赖，并实现辅助动态图构建。
result: 在FPGA上，DGNNFlow相比GPU实现1.6x-6.3x加速，相比CPU实现3.2x-5.1x加速，且功耗仅为后者的约四分之一。
conclusion: DGNNFlow为高能物理实验中的实时边缘动态GNN推理提供了一种高效、低延迟且低功耗的硬件解决方案。
---

## 摘要
动态 GNN 推理在高亮度大型强子对撞机（HL-LHC）的高能物理（HEP）实验中表现出显著的有效性，这归功于其对碰撞事件中复杂粒子相互作用的强大建模能力。未来的 HEP 实验将涉及产生 10 倍以上碰撞数据的探测器，以助力揭示新的物理发现。由于离线计算能力和存储的限制，改进后的触发系统需要 FPGA 运行超低延迟的机器学习模型，以便以低功耗在线过滤有用事件。最先进的 GNN 加速器依赖于静态图结构，但这一假设在实时 HL-LHC 触发系统和基于边的动态 GNN 模型中失效，因为在这些场景中，边嵌入（edge embeddings）在运行时会根据相邻节点嵌入进行原位更新。我们提出了 DGNNFlow，这是一种用于实时基于边的动态 GNN 推理应用（特别是 HL-LHC 触发系统）的新型数据流架构，具有三个关键贡献：首先，我们引入了对边嵌入动态计算的硬件支持；其次，我们解决了基于边的动态 GNN 数据流中的数据依赖问题，其中边嵌入是根据其源节点和目标节点构建的；第三，我们执行了输入动态图构建的辅助设置，以完整支持没有预定义边嵌入的模型。我们使用 AMD Alveo U50 FPGA 部署了 DGNNFlow，并在 200 MHz 时钟频率下评估了板载端到端延迟。在批大小（batch size）为 1 到 4 的情况下，与 GPU（NVIDIA RTX A6000）相比，DGNNFlow 实现了 1.6 倍至 6.3 倍的加速和 0.22 倍的功耗；与 CPU（Intel Xeon Gold 6226R）相比，实现了 3.2 倍至 5.1 倍的加速和 0.25 倍的功耗。我们的完整实现已在 GitHub 上公开。

## Abstract
Dynamic GNN inference has exhibited effectiveness in High Energy Physics (HEP) experiments at High Luminosity Large Hadron Collider (HL-LHC) due to strong capability to model complex particle interactions in collision events. Future HEP experiments will involve detectors that produce 10x more collision data to help unlocking physics discoveries. Due to limitations in offline compute capacity and storage, revamped trigger systems require FPGAs to run ultra-low-latency Machine Learning models for online filtering of useful events with low power consumption. State-of-the-art GNN accelerators relied on static graph structures, but this assumption breaks down in real-time HL-LHC trigger systems and edge-based dynamic GNN models where edge embeddings change in-place based on neighbor node embeddings at runtime. We propose DGNNFlow, a novel dataflow architecture for real-time edge-based dynamic GNN inference applications, especially HL-LHC trigger systems, with three key contributions. First, we introduce hardware support for dynamic computation of edge embeddings. Second, we resolve data dependencies in edge-based dynamic GNN dataflow, where edge embedding is formulated using its source and target nodes. Third, we perform input dynamic graph construction auxiliary setup for complete support of models without pre-defined edge embeddings. We deployed DGNNFlow using AMD Alveo U50 FPGA to evaluate end-to-end latency on-board at 200 MHz clock frequency. DGNNFlow achieved 1.6x-6.3x speedup and 0.22x power consumption compared to GPU (NVIDIA RTX A6000) with batch sizes from 1 to 4, 3.2x-5.1x speedup and 0.25x power consumption compared to CPU (Intel Xeon Gold 6226R). Our complete implementation is publicly available on GitHub.

---

## 论文详细总结（自动生成）

### DGNNFlow：用于 HL-LHC 触发系统中实时基于边的动态 GNN 推理的流式数据流架构

#### 1. 论文的核心问题与整体含义（研究动机和背景）
在高能物理（HEP）领域，高亮度大型强子对撞机（HL-LHC）产生的碰撞数据量极大（每秒达 PB 级），远超存储能力。因此，需要在“一级触发器（L1T）”阶段进行实时在线过滤。图神经网络（GNN）在建模粒子相互作用方面表现优异，但现有的硬件加速器大多假设图结构是静态的。
**核心问题：** 在 HL-LHC 的实时触发系统中，粒子间的关系（边）是动态构建的，且边嵌入（Edge Embeddings）需要根据相邻节点的特征在运行时原位更新（如 EdgeConv 算子）。现有的静态图加速器无法高效处理这种动态依赖和不规则的内存访问。

#### 2. 论文提出的方法论
论文提出了 **DGNNFlow**，一种专门针对基于边的动态 GNN 推理的流式数据流架构。其核心思想包括：
*   **增强型消息传递单元（Enhanced MP Units）：** 不同于传统只处理源节点信息的单元，该单元支持在 FPGA 上实时计算边嵌入。公式为 $m_{uv} = e_{uv} = \phi(x_u, x_v - x_u)$，其中 $\phi$ 是可学习的 MLP。
*   **节点嵌入广播机制（Node Embedding Broadcast）：** 为了解决计算边嵌入时对目标节点特征的依赖，系统将节点嵌入复制并广播到所有 MP 单元。各单元根据分配的边选择性捕获所需的特征，从而避免了复杂的非规则内存访问。
*   **流式数据流组织：** 采用双缓冲（Double Buffering）机制在 GNN 层间切换读写角色，并利用压缩稀疏行（CSR）格式存储图数据。
*   **辅助动态图构建：** 在主机端提供轻量级设置，根据粒子间的空间距离（$\Delta R^2 < \delta^2$）动态生成边列表。

#### 3. 实验设计
*   **数据集/场景：** 使用 DELPHES 框架模拟 HL-LHC 质子-质子碰撞事件，生成了 1.6 万个图作为测试集。
*   **模型：** 针对 CMS 实验设计的 L1DeepMETv2 模型（基于 EdgeConv 的动态 GNN）。
*   **Benchmark（基准）：** 
    *   **CPU:** Intel Xeon Gold 6226R。
    *   **GPU:** NVIDIA RTX A6000。
    *   **软件变体：** 原始 PyTorch 版本和经过 `torch.compile` 优化的版本。
*   **对比指标：** 端到端（E2E）延迟、功耗、资源利用率。

#### 4. 资源与算力
*   **FPGA 平台：** AMD Alveo U50，运行频率为 200 MHz。
*   **资源消耗：** 使用了约 23.5 万个 LUT（27%）、22.8 万个寄存器（13%）、488 个 BRAM（36%）和 601 个 DSP（10%）。
*   **对比算力：** 使用了单张 NVIDIA RTX A6000 GPU 和 Intel Xeon Gold CPU 进行推理对比。论文未提及训练时长，因为重点在于推理阶段的硬件加速。

#### 5. 实验数量与充分性
*   **实验规模：** 评估了 1.6 万个样本，涵盖了不同节点数和边数的图结构。
*   **维度：** 进行了不同批大小（Batch Size 1-16）的延迟对比，以及延迟随图规模增长的变化曲线分析。
*   **充分性：** 实验设计较为充分，对比了 CPU/GPU 的优化前后版本，并考虑了实时触发系统最关心的“批大小为 1”的极端低延迟场景，具有较强的客观性和说服力。

#### 6. 论文的主要结论与发现
*   **延迟优势：** DGNNFlow 的平均端到端延迟为 0.283 毫秒。在批大小为 1-4 时，比 GPU 快 1.6x - 6.3x，比 CPU 快 3.2x - 5.1x。
*   **功耗优势：** FPGA 的功耗仅为 5.89W，约为 GPU 的 22% 和 CPU 的 25%。
*   **实时性：** 证明了在处理动态图时，FPGA 的流式架构在低批处理量下比高吞吐量的 GPU 更具延迟优势，能够满足 HL-LHC 触发系统的严苛要求。

#### 7. 优点
*   **针对性强：** 填补了硬件加速器在“基于边的动态 GNN”领域的空白，特别是解决了 EdgeConv 的硬件实现难题。
*   **架构优雅：** 通过广播机制巧妙规避了图计算中常见的不规则内存访问问题，提高了硬件利用率。
*   **开源贡献：** 提供了完整的 GitHub 实现，便于学术界复现和改进。

#### 8. 不足与局限
*   **主机端开销：** 动态图的构建（边列表生成）目前在主机端完成，虽然论文称其为“轻量级”，但在极高频率的触发系统中，主机与 FPGA 间的通信和主机处理能力可能成为新的瓶颈。
*   **扩展性限制：** 广播机制虽然解决了依赖问题，但随着图规模（节点数）极大增加，广播带宽和 MP 单元的局部缓存可能会面临压力。
*   **应用范围：** 实验主要集中在高能物理特定模型上，对于其他类型的动态图（如拓扑结构剧烈变化的社交网络）的适用性有待进一步验证。

（完）
