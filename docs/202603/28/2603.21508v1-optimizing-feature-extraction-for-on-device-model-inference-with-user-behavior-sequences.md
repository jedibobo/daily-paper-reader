---
title: Optimizing Feature Extraction for On-device Model Inference with User Behavior Sequences
title_zh: 针对用户行为序列的端侧模型推理特征提取优化
authors: "Chen Gong, Zhenzhe Zheng, Yiliu Chen, Sheng Wang, Fan Wu, Guihai Chen"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21508v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 优化设备端模型执行流水线
tldr: 本研究针对移动端模型推理中被忽视的特征提取瓶颈，提出了AutoFeature引擎。该引擎通过将特征提取流程抽象为有向无环图，消除跨特征的冗余操作，并利用缓存机制减少连续推理间的重复计算。在搜索、视频和电商等五个工业级场景的在线评估显示，AutoFeature显著降低了端到端推理延迟，在不损失精度的情况下提升了用户体验。
selection_source: fresh_fetch
motivation: 现有的移动端模型加速研究多关注推理本身，而忽略了从原始应用日志中提取特征所带来的巨大延迟瓶颈。
method: 提出AutoFeature引擎，通过图抽象、图优化融合冗余节点以及高效缓存重叠数据来加速特征提取过程。
result: 在五个工业级移动服务中的在线评估表明，该方法使端到端模型执行延迟降低了1.33倍至4.53倍。
conclusion: 优化特征提取流程是提升移动端模型推理性能的关键，AutoFeature为实现低延迟的端到端智能服务提供了有效方案。
---

## 摘要
机器学习模型被广泛集成到现代移动应用中，用于分析用户行为并提供个性化服务。确保低延迟的端侧模型执行对于维持高质量的用户体验至关重要。虽然先前的研究主要集中在利用给定输入特征加速模型推理，但我们发现了现实世界端侧模型执行流水线中一个被忽视的瓶颈：从原始应用日志中提取输入特征。在这项工作中，我们通过分析并消除不同模型特征之间以及连续模型推理之间的冗余提取操作，探索了特征提取优化的一个新方向。随后，我们介绍了 AutoFeature，这是一个自动化的特征提取引擎，旨在不损害模型推理准确性的前提下加速端侧特征提取过程。AutoFeature 包含三个核心设计：(1) 图抽象，将不同输入特征的提取工作流公式化为一个有向无环图；(2) 图优化，识别并融合图中不同特征之间的冗余操作节点；(3) 高效缓存，以最小化连续模型推理之间重叠原始数据的操作。我们实现了 AutoFeature 的系统原型，并将其集成到涵盖搜索、视频和电子商务领域的五个工业级移动服务中。在线评估表明，AutoFeature 在白天将端到端端侧模型执行延迟降低了 1.33 倍至 3.93 倍，在夜间降低了 1.43 倍至 4.53 倍。

## Abstract
Machine learning models are widely integrated into modern mobile apps to analyze user behaviors and deliver personalized services. Ensuring low-latency on-device model execution is critical for maintaining high-quality user experiences. While prior research has primarily focused on accelerating model inference with given input features, we identify an overlooked bottleneck in real-world on-device model execution pipelines: extracting input features from raw application logs. In this work, we explore a new direction of feature extraction optimization by analyzing and eliminating redundant extraction operations across different model features and consecutive model inferences. We then introduce AutoFeature, an automated feature extraction engine designed to accelerate on-device feature extraction process without compromising model inference accuracy. AutoFeature comprises three core designs: (1) graph abstraction to formulate the extraction workflows of different input features as one directed acyclic graph, (2) graph optimization to identify and fuse redundant operation nodes across different features within the graph; (3) efficient caching to minimize operations on overlapping raw data between consecutive model inferences. We implement a system prototype of AutoFeature and integrate it into five industrial mobile services spanning search, video and e-commerce domains. Online evaluations show that AutoFeature reduces end-to-end on-device model execution latency by 1.33x-3.93x during daytime and 1.43x-4.53x at night.

---

## 论文详细总结（自动生成）

以下是对论文《Optimizing Feature Extraction for On-device Model Inference with User Behavior Sequences》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在移动端（端侧）机器学习应用中，学术界长期关注模型推理（Inference）的加速，却忽略了**特征提取（Feature Extraction）**阶段。
*   **核心痛点**：研究发现，在真实的工业级 App（如 TikTok、淘宝）中，从原始应用日志（SQLite）中提取用户行为特征的耗时占端到端执行延迟的 **61%-86%**。
*   **研究目标**：通过消除不同特征之间、以及连续推理请求之间的冗余计算操作，解决特征提取这一性能瓶颈，实现无损精度的端侧模型加速。

### 2. 方法论：AutoFeature 引擎
AutoFeature 被设计为一个独立的优化层，其核心思想是将特征提取过程图谱化并进行冗余消除：
*   **FE-graph（特征提取图）抽象**：将提取流程分解为四个原子操作：`Retrieve`（从数据库检索）、`Decode`（解压/解析 JSON 属性）、`Filter`（过滤特定属性）、`Compute`（统计计算）。
*   **图优化技术**：
    *   **内部分区与间融合**：将特征提取链拆解为更细粒度的子链，合并具有相同事件名称和重叠时间窗口的检索与解码节点。
    *   **分支后置与层次化过滤**：为了减少融合后的拆分成本，提出层次化过滤算法，利用行为日志的时间顺序性，将过滤复杂度从 $O(len \times num\_features)$ 降至 $O(len + num\_features)$。
*   **跨推理缓存策略**：
    *   **建模**：将缓存决策建模为“背包问题”，目标是在有限内存预算下最大化计算节省。
    *   **贪心算法**：根据“效用-成本比”（Utility-to-cost ratio）动态选择高价值的中间结果进行缓存，该比例由行为重叠度、操作成本和内存占用共同决定。

### 3. 实验设计
*   **应用场景**：选取了字节跳动旗下的 5 个工业级移动服务，涵盖视频预加载（CP）、关键词预测（KP）、搜索排序（SR）、产品推荐（PR）和视频推荐（VR）。
*   **Benchmark（基准）**：
    *   **w/o AutoFeature**：工业标准的独立特征提取流程。
    *   **云端方案对比**：`Decoded Log`（离线解码日志）和 `Feature Store`（云端特征存储），用于对比存储开销。
*   **测试数据**：收集了 10 名真实匿名用户在 2 天内三个典型时段（中午、傍晚、深夜）的真实行为轨迹，并通过 Kolmogorov-Smirnov (KS) 检验确保样本与海量真实用户的统计分布一致。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**模型训练所使用的 GPU 型号或训练时长，因为该研究聚焦于**端侧推理阶段**的系统优化，而非模型训练。
*   **执行环境**：实验在集成有 ByteNN 推理引擎的真实移动设备上完成，侧重于 CPU 耗时和内存占用分析。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个领域、5 种不同复杂度的模型、多个时间段的在线评估。
*   **充分性分析**：
    *   **消融实验**：分别验证了图融合（Fusion）和缓存（Cache）对性能的贡献。
    *   **敏感性分析**：测试了不同推理频率（10秒到1天）和不同特征冗余度对加速效果的影响。
    *   **客观性**：通过与云端方案对比，揭示了 AutoFeature 在存储效率上的巨大优势（云端方案会导致 App 日志体积膨胀 2.8 倍，引发用户流失）。实验设计全面且贴近工业实战。

### 6. 主要结论与发现
*   **显著加速**：AutoFeature 将端到端模型执行延迟降低了 **1.33x 至 4.53x**，使总延迟普遍降至 20ms 以下（达到人眼不可察觉的流畅度）。
*   **时间相关性**：夜间的加速效果通常优于白天，因为夜间用户行为更密集，产生的冗余消除空间更大。
*   **低开销**：离线图优化耗时仅约 3ms，在线缓存的额外内存占用通常小于 100KB，对移动设备非常友好。

### 7. 优点
*   **视角独特**：首次系统性地指出并量化了端侧特征提取的瓶颈问题。
*   **模型无关性**：作为独立优化层，无需修改模型结构或推理引擎，具有极强的通用性和易部署性。
*   **工业级验证**：直接在拥有亿级用户的真实 App 环境中测试，结果具有高度的可信度和实用价值。

### 8. 不足与局限
*   **用户依赖性**：对于行为极少的非活跃用户，由于原始日志量小，特征提取本身开销就不大，该引擎的加速收益会边际递减。
*   **协同优化缺失**：由于企业内部组织架构（模型组与架构组分离），目前未能实现“精度换延迟”的模型-引擎协同设计。
*   **应用范围**：主要针对依赖用户行为序列的轻量级模型，对于输入为静态数据（如纯图像、纯文本）的视觉或语言模型，该优化不适用。

（完）
