---
title: An FPGA-Based SoC Architecture with a RISC-V Controller for Energy-Efficient Temporal-Coding Spiking Neural Networks
title_zh: 一种基于 FPGA 的 SoC 架构，配备 RISC-V 控制器，用于高能效时间编码脉冲神经网络
authors: "Mohammad Javad Sekonji, Ali Mahani, Maryam Mirsadeghi, Mahdi Taheri"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.18054v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于FPGA的能效SNN SoC架构，具有内存优化特性
tldr: 针对边缘AI低功耗需求，本文提出一种基于FPGA的SoC架构，集成RISC-V控制器与事件驱动型脉冲神经网络（SNN）核心。通过二值化权重、脉冲时间排序及跳过无效事件等技术，显著降低了计算开销与内存占用，在保持高精度的同时实现了高效的实时神经形态推理。
selection_source: fresh_fetch
motivation: 解决脉冲神经网络在FPGA硬件实现中面临的计算量大、内存占用高及灵活性不足等挑战。
method: 采用集成RISC-V控制器的SoC架构，利用二值化权重替代乘法器，并引入脉冲时间排序器以优化事件驱动计算。
result: "在Artix-7 FPGA上实现了16倍的权重内存缩减，并在MNIST和FashionMNIST数据集上分别达到97.0%和88.3%的准确率。"
conclusion: 该设计为边缘侧实时神经形态推理提供了一个高效、可扩展且自包含的硬件平台。
---

## 摘要
脉冲神经网络 (SNN) 具有高能效和事件驱动计算的特点，是低功耗边缘人工智能的理想选择。然而，由于计算量大、内存占用高以及灵活性有限，其在 FPGA 上的硬件实现面临挑战。本文提出了一种用于时间编码 SNN 的紧凑型片上系统 (SoC) 架构，将 RISC-V 控制器与事件驱动的 SNN 核心集成在一起。该架构利用二值化权重，通过位运算替代乘法器，包含一个用于活动脉冲的脉冲时间排序器，并跳过无信息事件以减少计算量。该架构完全在 Xilinx Artix-7 FPGA 上运行，实现了高达 16 倍的权重内存缩减，并降低了计算开销和延迟，在 MNIST 和 FashionMNIST 数据集上分别达到了 97.0% 和 88.3% 的准确率。这种自包含设计为边缘端的实时类脑推理提供了一个高效、可扩展的平台。

## Abstract
Spiking Neural Networks (SNNs) offer high energy efficiency and event-driven computation, ideal for low-power edge AI. Their hardware implementation on FPGAs, however, faces challenges due to heavy computation, large memory use, and limited flexibility. This paper proposes a compact System-on-Chip (SoC) architecture for temporal-coding SNNs, integrating a RISC-V controller with an event-driven SNN core. It replaces multipliers with bitwise operations using binarized weights, includes a spike-time sorter for active spikes, and skips noninformative events to reduce computation. The architecture runs fully on a Xilinx Artix-7 FPGA, achieving up to 16x memory reduction for weights and lowering computational overhead and latency, with 97.0% accuracy on MNIST and 88.3% on FashionMNIST. This self-contained design provides an efficient, scalable platform for real-time neuromorphic inference at the edge.

---

## 论文详细总结（自动生成）

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：脉冲神经网络（SNN）虽然在理论上具有高能效和事件驱动的优势，但在 FPGA 硬件实现中面临**计算开销大**、**内存占用高**以及**架构灵活性不足**（难以适配不同模型）的挑战。
*   **研究动机**：现有的 SNN 加速器往往依赖外部处理器或采用固定架构，难以在资源受限的边缘设备上实现自主、高效的推理。本文旨在设计一种集成了通用控制器与专用加速核心的 SoC 架构，以解决这些痛点。

### 2. 方法论
*   **核心思想**：提出一种基于 SoC 的架构，将 **RISC-V 控制器**与**事件驱动的 SNN 核心**集成。通过二值化权重（BS4NN）和时间编码（Temporal Coding）来简化计算。
*   **关键技术细节**：
    *   **SoC 架构**：采用 RV32I 指令集的 RISC-V 处理器负责配置参数、管理内存和数据流；SNN 核心负责执行具体的脉冲推理。
    *   **时间编码（TTFS）**：使用“首次脉冲时间”（Time-to-First-Spike）编码，将像素强度转换为脉冲延迟，强度越高脉冲越早。
    *   **二值化权重（Binarized Weights）**：将权重限制为 $\pm1$，从而将复杂的乘法运算替换为简单的加减法位运算，显著降低 DSP 资源消耗。
    *   **脉冲排序器（Spike Sorter）**：按时间顺序排列活动事件，仅处理有效脉冲并跳过无信息事件，从而降低延迟。
    *   **时间复用神经元**：采用时间复用技术实现神经元阵列，相比并行设计减少了近 50% 的逻辑资源占用。
    *   **灵活性设计**：架构支持在二值化模型（BS4NN）和多比特定点模型（S4NN）之间切换，无需重新设计硬件。

### 3. 实验设计
*   **数据集**：MNIST（手写数字）和 Fashion-MNIST（服装分类）。
*   **Benchmark（基准）**：
    *   **软件基准**：BS4NN 和 S4NN 的软件仿真结果。
    *   **硬件对比**：对比了现有的 FPGA SNN 实现，如 Spiker (2022) 和 Fan & Levy (2025) 的 Artix-7 实现。
*   **对比维度**：准确率（Accuracy）、功耗（Power）、延迟（Latency）、吞吐量（FPS）以及资源占用（LUT, FF, BRAM, DSP）。

### 4. 资源与算力
*   **硬件平台**：Xilinx Artix-7 FPGA。
*   **算力/性能指标**：
    *   **功耗**：在 MNIST 上仅为 **72 mW**，在 Fashion-MNIST 上为 158 mW。
    *   **频率**：运行频率约为 163-167 MHz。
    *   **内存优化**：通过二值化权重实现了高达 **16倍** 的权重内存缩减。
*   **训练说明**：论文明确指出模型是**离线训练**（Offline Training）的，训练完成后将权重加载到 FPGA 的 SPI Flash 中。文中未提及训练所用的具体 GPU 型号或训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 MNIST 采用了 600-10 的网络结构。
    *   针对 Fashion-MNIST 采用了 1000-10 的网络结构。
    *   进行了 BS4NN（二值化）与 S4NN（非二值化）在相同硬件架构下的对比实验。
*   **充分性评价**：实验涵盖了准确率、功耗、资源和延迟等关键指标，并与近年来的同类硬件研究进行了直接对比。实验设计较为客观、公平，充分验证了该架构在资源节省和能效比方面的优势。

### 6. 主要结论与发现
*   **高精度与高性能**：在 MNIST 上达到 97.0% 的准确率，在 Fashion-MNIST 上达到 88.3%，均匹配了软件层面的精度。
*   **极低功耗**：相比之前的研究（如 381 mW），该 SoC 架构将功耗降低至 72 mW，非常适合边缘 AI 应用。
*   **资源高效**：通过二值化和时间复用，极大地减少了 BRAM 和 DSP 的使用，甚至在二值化模式下实现了 **零 DSP 占用**。

### 7. 优点
*   **自包含性**：集成了 RISC-V 控制器，使得系统无需外部 PC 即可独立运行，增强了边缘端的自主性。
*   **高度灵活**：模块化设计允许在不重新综合硬件的情况下，通过软件配置支持不同的权重精度和网络拓扑。
*   **事件驱动优化**：引入脉冲排序器和跳过无效事件的机制，充分利用了 SNN 的稀疏性来降低延迟。

### 8. 不足与局限
*   **网络深度有限**：实验主要集中在全连接层（MLP 结构），尚未展示在更深层的卷积脉冲神经网络（CSNN）上的表现。
*   **缺乏片上学习**：目前仅支持推理，不支持片上训练（On-chip Learning），这限制了系统在需要实时在线适配场景中的应用。
*   **编码限制**：主要针对时间编码（每个神经元仅发一个脉冲），对于需要多脉冲频率编码的复杂动态信号处理能力尚待验证。

（完）
