---
title: Mitigating the Bandwidth Wall via Data-Streaming System-Accelerator Co-Design
title_zh: 通过数据流式系统-加速器协同设计缓解带宽墙问题
authors: "Qunyou Liu, Marina Zapater, David Atienza"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19057v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对内存带宽优化的系统-加速器协同设计
tldr: 针对Transformer推理中带宽受限的“带宽墙”问题，本文提出了一种系统与加速器协同设计方案。通过引入MatrixFlow加速器，利用分页流式数据流和计算与传输的显式重叠，结合Gem5-AcceSys全系统模拟器优化互连带宽。研究表明，采用分页流式处理和流水线重叠，而非增加片上SRAM，是提升Transformer推理效率的关键，在标准PCIe环境下即可达到接近HBM的性能。
selection_source: fresh_fetch
motivation: Transformer模型的推理性能主要受限于数据移动和互连带宽，而非单纯的算力，导致传统加速器难以发挥效能。
method: 提出MatrixFlow加速器，采用分页对齐的块矩阵乘法和流水线化DMA调度，并开发Gem5-AcceSys模拟器进行全系统协同设计。
result: "在BERT和ViT模型上，该方案比纯CPU快22倍，比现有加速器快5-8倍，且PCIe设计能达到HBM性能的80%。"
conclusion: 分页流式传输和计算传输重叠是解决现实系统约束下Transformer推理瓶颈的最有效手段。
---

## 摘要
Transformer 架构彻底改变了自然语言处理和计算机视觉领域的 AI，但其巨大的计算和内存需求给硬件加速带来了重大挑战。在实践中，端到端吞吐量通常受限于分页数据移动和互连带宽，而非原始的乘累加（MAC）操作次数。本研究提出了一种用于 Transformer 推理的统一系统-加速器协同设计方法，通过分页流式数据流以及计算与传输的显式重叠，共同优化矩阵加速器及其系统集成。在硬件方面，我们引入了 MatrixFlow，这是一种松耦合的 16x16 脉动阵列加速器，采用基于 4 KB 分块的页对齐分块矩阵乘法、约 20 KB 的小型片上缓冲区，以及 DMA、计算和 DMA 输出的流水线调度，以高效利用互连带宽。在系统方面，我们开发了 Gem5-AcceSys，这是 gem5 全系统模拟器的扩展，用于探索 PCIe 等标准互连以及包括直接内存（Direct Memory）、直接缓存（Direct Cache）和设备内存（Device Memory）模式在内的可配置内存层次结构，并考虑了 SMMU/TLB 效应。我们在多种数据类型和系统设置下，通过 gem5 模拟对 BERT 和 ViT 等代表性 Transformer 模型进行了协同设计评估。结果显示，与仅 CPU 的基准相比，端到端加速比高达 22 倍，与最先进的松耦合和紧耦合加速器相比，性能提升了 5 到 8 倍。我们进一步证明，标准的基于 PCIe 的主机内存设计可以达到设备端 HBM 性能的约 80%。总之，在现实系统约束下，分页流式传输和流水线重叠，而非大型本地 SRAM，是实现高效 Transformer 推理最有效的手段。

## Abstract
Transformers have revolutionized AI in natural language processing and computer vision, but their large computation and memory demands pose major challenges for hardware acceleration. In practice, end-to-end throughput is often limited by paged data movement and interconnect bandwidth rather than raw MAC count. This work proposes a unified system-accelerator co-design approach for transformer inference that jointly optimizes a matrix accelerator and its system integration through paged streaming dataflows and explicit overlap of compute and transfer. On the hardware side, we introduce MatrixFlow, a loosely coupled 16x16 systolic-array accelerator with a page-aligned block matrix multiplication method using 4 KB tiles, a small on-chip buffer of about 20 KB, and a pipelined schedule of DMA, compute, and DMA-out to utilize interconnect bandwidth efficiently. On the system side, we develop Gem5-AcceSys, an extension of the gem5 full-system simulator that explores standard interconnects such as PCIe and configurable memory hierarchies including Direct Memory, Direct Cache, and Device Memory modes with SMMU/TLB effects. We evaluate the co-design using gem5 simulations on representative transformer models including BERT and ViT across multiple data types and system setups. Results show up to 22x end-to-end speedup over a CPU-only baseline and 5x to 8x gains over state-of-the-art loosely and tightly coupled accelerators. We further show that a standard PCIe-based host-memory design can achieve about 80 percent of the performance of on-device HBM. Overall, paged streaming and pipeline overlap, rather than large local SRAMs, are the most effective levers for efficient transformer inference under realistic system constraints.

---

## 论文详细总结（自动生成）

以下是对论文《Mitigating the Bandwidth Wall via Data-Streaming System-Accelerator Co-Design》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：Transformer模型（如BERT、ViT）在AI领域占据主导地位，但其推理性能正面临“带宽墙”瓶颈。传统的硬件加速研究往往只关注原始算力（MAC数量），而忽略了实际系统中分页数据移动、互连带宽（如PCIe）以及地址转换（SMMU/TLB）带来的巨大开销。
*   **核心问题**：在现实系统约束下，如何通过系统与加速器的协同设计，消除数据移动瓶颈，实现高效的Transformer推理？
*   **背景**：现有的加速器模拟框架（如gem5-Aladdin、SMAUG等）对标准互连（PCIe）和内存管理单元（IOMMU/SMMU）的建模过于简化，导致无法准确评估松耦合加速器在全系统中的表现。

### 2. 方法论：核心思想与关键技术
论文提出了一个统一的协同设计方案，包含硬件、系统模拟平台和软件运行时三个层面：
*   **MatrixFlow 加速器（硬件）**：
    *   采用16×16脉动阵列（Systolic Array）。
    *   **极小片上缓存策略**：摒弃了传统加速器动辄数百KB的SRAM，仅使用约20KB（3个4KB缓冲区+双缓冲逻辑）的片上存储。
    *   **分页流式设计**：将矩阵分块大小与系统内存页（4KB）对齐，利用DMA直接从主机内存流式获取数据。
*   **Gem5-AcceSys 模拟平台（系统）**：
    *   扩展了gem5全系统模拟器，增加了对PCIe 3.0-6.0、多通道DMA引擎和SMMU（支持虚拟地址转换）的精确建模。
    *   支持三种内存访问模式：直接内存（DM）、直接缓存（DC）和设备内存（DevMem）。
*   **协同优化软件运行时（算法/流程）**：
    *   **页对齐分块矩阵乘法**：将矩阵A按行分块，矩阵B按行条带化存储，确保每次DMA传输都是连续的4KB页，最大化PCIe带宽利用率并减少TLB缺失。
    *   **流水线重叠**：通过双缓冲技术，使“DMA读入、脉动阵列计算、DMA写回”在时间上完全重叠。

### 3. 实验设计
*   **数据集/模型**：
    *   **微基准测试**：不同规模（64到2048）和精度（INT8/16/32, FP16/32）的方阵乘法（GEMM）。
    *   **全模型推理**：BERT-Base/Large（NLP任务）、ViT-Base/Large/Huge（视觉任务）。
*   **对比方法（Benchmarks）**：
    *   **CPU基准**：单核ARM CPU、多线程CPU（OpenMP, 256线程）、ARM Neon SIMD优化版。
    *   **加速器对比**：TiC-SAT（紧耦合脉动阵列）、SMAUG（基于gem5的松耦合加速器框架）。
*   **评估指标**：端到端推理延迟、吞吐量（FPS）、带宽利用率、PPA（面积、功耗、性能）分析。

### 4. 资源与算力
*   **模拟环境**：实验基于gem5全系统模拟，运行Linux操作系统。
*   **硬件合成**：加速器逻辑使用 **TSMC 28nm** 工艺进行综合，以获取准确的面积和功耗数据。
*   **算力说明**：文中未提及大规模GPU集群训练，因为该研究聚焦于**推理阶段的架构仿真**。模拟运行在高性能服务器上，利用Gem5-AcceSys的多线程加速功能来缩短仿真时间。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽实验，包括：
    *   **参数扫掠**：PCIe带宽（2GB/s到128GB/s）、数据包大小（64B到4096B）、内存技术（DDR3/4/5, HBM2, GDDR6）。
    *   **消融实验**：分析了地址转换（SMMU）开销、非GEMM操作占比对整体性能的影响。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了不同架构，还深入探讨了软硬件接口（如TLB、DMA描述符）的细微影响，覆盖了从底层电路到顶层应用的完整链路。

### 6. 主要结论与发现
*   **性能提升**：MatrixFlow在端到端推理上比纯CPU快 **22倍**，比SOTA紧耦合加速器快 **8倍**，比松耦合加速器快 **5倍**。
*   **带宽胜过缓存**：对于Transformer，增加片上SRAM的收益远低于优化流式带宽。仅需20KB SRAM配合高效流水线，即可达到极高利用率。
*   **互连的重要性**：通过协同设计，基于标准PCIe的主机内存方案可以达到本地HBM内存性能的 **80%**。
*   **瓶颈转移**：随着GEMM被极度加速，非GEMM操作（如Softmax、LayerNorm）和控制面开销（驱动、中断）成为新的瓶颈（占比约24%-37%）。

### 7. 优点
*   **全系统视角**：打破了“孤立设计加速器”的传统，将PCIe、SMMU和驱动程序纳入核心设计考量，更符合工业界部署现状。
*   **资源效率极高**：证明了通过精巧的数据流控制，可以用极小的片上面积（极少SRAM）换取极高的吞吐量。
*   **开源贡献**：开发的Gem5-AcceSys为学术界提供了一个研究现代互连和内存管理对加速器影响的有力工具。

### 8. 不足与局限
*   **仿真与实测差距**：虽然gem5具有周期精确性，但仍是模拟结果，实际硅片上的物理效应（如布线拥塞、热量分布）可能带来偏差。
*   **算子覆盖度**：目前主要针对GEMM算子进行硬件加速，对于Transformer中的非线性算子（如GELU、Softmax）仍依赖主机CPU，这在非GEMM占比高的模型中会限制加速比。
*   **应用限制**：该方案高度依赖于高效的DMA和SMMU支持，在一些低端SoC或缺乏高级IOMMU支持的嵌入式系统中可能难以实施。

（完）
