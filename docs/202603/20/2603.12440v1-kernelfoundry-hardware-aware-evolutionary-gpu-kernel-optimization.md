---
title: "KernelFoundry: Hardware-aware evolutionary GPU kernel optimization"
title_zh: KernelFoundry：硬件感知的演化式 GPU 核函数优化
authors: "Nina Wiedemann, Quentin Leboutet, Michael Paulitsch, Diana Wofk, Benjamin Ummenhofer"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12440v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 硬件感知的进化GPU内核优化
tldr: 针对大语言模型在GPU算子优化中缺乏硬件感知和并行策略理解的问题，本文提出KernelFoundry进化框架。该框架结合MAP-Elites多样性搜索、元提示词进化及模板参数优化，通过协同进化提示词与算子代码，在SYCL和CUDA平台上实现了高效的算子生成。实验表明，该方法在KernelBench上比基准方法平均提速2.3倍，显著提升了跨平台GPU算子的性能。
selection_source: fresh_fetch
motivation: 现有的LLM算子生成方法主要依赖简单的提示词反馈循环，难以深度理解复杂的硬件架构和并行优化策略。
method: 提出一种集成MAP-Elites多样性搜索、元提示词进化和模板参数优化的分布式进化框架，以探索更优的算子设计空间。
result: 在KernelBench等基准测试中，该框架生成的SYCL算子性能优于现有方法，平均加速比达到2.3倍。
conclusion: KernelFoundry证明了结合硬件感知与进化算法能显著提升LLM生成高性能、跨平台GPU算子的能力。
---

## 摘要
与标准代码生成任务相比，优化 GPU 核函数对大语言模型（LLMs）提出了显著更大的挑战，因为它需要理解硬件架构、并行优化策略以及性能分析输出。大多数现有的基于 LLM 的核函数生成方法依赖于简单的提示词和反馈循环，仅通过性能分析反馈间接地融入硬件感知。我们推出了 KernelFoundry，这是一个演化框架，通过三个关键机制高效探索 GPU 核函数设计空间：(1) 具有核函数特定行为维度的 MAP-Elites 质量-多样性搜索，以维持对多样化优化策略的探索；(2) 元提示词演化，使提示词与核函数共同演化，以发现特定任务的优化策略；(3) 基于模板的参数优化，使核函数适应输入和硬件。我们在 KernelBench、robust-kbench 和自定义任务上评估了该框架，生成了作为跨平台 GPU 编程模型的 SYCL 核函数，并生成了 CUDA 核函数以与先前工作进行对比。我们的方法始终优于基准方法，在 KernelBench 的 SYCL 任务上实现了平均 2.3 倍的加速。此外，KernelFoundry 被实现为一个分布式框架，可远程访问多种硬件，从而实现快速基准测试，并具有灵活的用户输入层，支持基准测试之外的广泛真实世界用例的核函数生成。

## Abstract
Optimizing GPU kernels presents a significantly greater challenge for large language models (LLMs) than standard code generation tasks, as it requires understanding hardware architecture, parallel optimization strategies, and performance profiling outputs. Most existing LLM-based approaches to kernel generation rely on simple prompting and feedback loops, incorporating hardware awareness only indirectly through profiling feedback. We introduce KernelFoundry, an evolutionary framework that efficiently explores the GPU kernel design space through three key mechanisms: (1) MAP-Elites quality-diversity search with kernel-specific behavioral dimensions to sustain exploration across diverse optimization strategies; (2) meta-prompt evolution, which co-evolves prompts with kernels to uncover task-specific optimization strategies, and (3) template-based parameter optimization to tune kernels to inputs and hardware. We evaluate this framework on KernelBench, robust-kbench, and custom tasks, generating SYCL kernels as a cross-platform GPU programming model and CUDA kernels for comparison to prior work. Our approach consistently outperforms the baseline methods, achieving an average speedup of 2.3x on KernelBench for SYCL. Moreover, KernelFoundry is implemented as a distributed framework with remote access to diverse hardware, enabling rapid benchmarking and featuring a flexible user input layer that supports kernel generation for a wide range of real-world use cases beyond benchmarking.

---

## 论文详细总结（自动生成）

以下是对论文《KernelFoundry: Hardware-aware evolutionary GPU kernel optimization》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管大语言模型（LLM）在通用代码生成上表现出色，但在编写高性能 GPU 核函数（Kernel）时面临巨大挑战。这要求模型不仅要保证逻辑正确，还要深度理解硬件架构（如内存层次、并行映射）和性能分析反馈。
*   **研究动机**：
    *   **现有方法局限**：目前的 LLM 优化方法多采用简单的“生成-验证-反馈”循环，容易陷入“模式崩溃”（过早收敛于局部最优）和“上下文退化”（失败尝试占满提示词空间）。
    *   **硬件感知缺失**：大多数方法仅间接通过性能数据获取硬件信息，缺乏系统性的设计空间探索。
    *   **跨平台需求**：现有研究几乎完全集中在 NVIDIA 的 CUDA 生态，缺乏对 SYCL 等跨平台编程模型的支持。

### 2. 方法论：KernelFoundry 框架
KernelFoundry 是一个集成了质量-多样性搜索和协同进化的分布式框架，核心思想是通过维持解的多样性来避免局部最优。
*   **关键技术细节**：
    1.  **MAP-Elites 质量-多样性搜索**：将核函数按三个维度（内存访问模式、算法结构、并行协调）划分为 64 个行为单元格。每个单元格仅保留该策略下性能最好的“精英”核函数，确保探索不同的优化路径（如：有的侧重共享内存，有的侧重寄存器分块）。
    2.  **梯度启发式演化**：通过跟踪父子代之间的性能变化，估算“适应度梯度”，并将其转化为自然语言提示词（如“尝试增加共享内存分块”），引导 LLM 向更有潜力的方向变异。
    3.  **元提示词演化（Meta-Prompt Evolution）**：引入一个专门的“元提示词 LLM”，根据实验结果动态更新优化原则、常见陷阱和分析指南，防止上下文污染。
    4.  **模板化参数优化**：让 LLM 生成带有模板参数（如 Block Size, Tile Size）的 C++ 代码，由框架自动在目标硬件上搜索最优参数组合。
*   **算法流程**：任务输入 -> 提示词构建 -> LLM 生成候选核函数 -> 编译与验证 -> 静态代码分析归类 -> 更新 MAP-Elites 存档 -> 元提示词更新 -> 循环迭代。

### 3. 实验设计
*   **数据集/场景**：
    *   **KernelBench**：包含单算子、融合模式和完整架构任务（筛选后保留 111 个任务）。
    *   **Robust-kbench**：包含更复杂的反向传播算子任务。
    *   **自定义任务**：包括 Llama 3.2 的旋转位置编码（RoPE）优化，以及与 Intel oneDNN 高性能库的直接对比。
*   **对比方法**：
    *   **CUDA 平台**：对比了 AI CUDA Engineer、Kernelsseum 和 robust-kbench 的公开最优核函数。
    *   **SYCL 平台**：对比了 OpenEvolve（AlphaEvolve 的开源实现）。
*   **编程语言**：重点评估了 SYCL（针对 Intel 硬件），同时也生成了 CUDA 代码进行基准对比。

### 4. 资源与算力
*   **GPU 型号**：
    *   **Intel 平台**：Intel Arc 140V (LNL 集显) 和 Intel Battlemage B580 (独显)。
    *   **NVIDIA 平台**：RTX A6000 (Ampere 架构)。
*   **LLM 模型**：使用了 GPT-4o、GPT-o3-mini、Claude 3.5/4.5 以及开源模型 GPT-OSS 20B。
*   **实验规模**：每个任务通常运行 40 轮迭代，每轮生成 8 个候选核函数。
*   **分布式架构**：论文提到实现了一个包含编译工人、执行工人和数据库服务器的分布式系统，以支持跨硬件的并行评估。

### 5. 实验数量与充分性
*   **实验规模**：在 111 个 KernelBench 任务和 12 个 Robust-kbench 任务上进行了测试，涵盖了从基础算子到复杂模型组件的广泛范围。
*   **消融与验证**：
    *   进行了**硬件感知交叉实验**：在 A 硬件上优化的核函数在 B 硬件上表现较差，证明了框架确实捕捉到了特定硬件的特性。
    *   对比了不同迭代次数（10 次 vs 40 次）下的性能增益。
*   **客观性**：作者指出了 KernelBench 原有评估标准的不足（如精度要求过低），并采用了更严格的相对精度和余弦相似度验证，确保结果的真实性。

### 6. 主要结论与发现
*   **性能领先**：在 SYCL 平台上，KernelFoundry 相比基准方法实现了平均 **2.3 倍** 的加速；在 CUDA 平台上也优于现有的 SOTA 代理框架。
*   **硬件适配性**：通过演化搜索，框架能自动发现适合特定 GPU 架构（如 Intel Battlemage）的优化策略。
*   **实际应用价值**：在 Llama 3.2 案例中，成功将 RoPE 算子提速 7.9 倍，使整个模型前向传播提速 8%。
*   **多样性的重要性**：MAP-Elites 机制能有效防止 LLM 在优化过程中陷入死循环，发现人类工程师可能忽略的“垫脚石”方案。

### 7. 优点与亮点
*   **跨平台支持**：是少数深入研究 SYCL 自动优化的论文，打破了 CUDA 的垄断。
*   **搜索机制创新**：将演化计算中的“质量-多样性”概念引入核函数优化，解决了 LLM 容易“模式崩溃”的痛点。
*   **闭环反馈**：不仅利用性能数值，还利用静态代码分析和元提示词更新，形成了多层次的反馈回路。
*   **工程化完备**：提供了分布式执行框架，支持真实世界的复杂测试（兼容 pytest）。

### 8. 不足与局限
*   **对闭源模型依赖**：实验显示，使用开源模型（如 GPT-OSS）时，正确率和优化效果显著下降，这限制了框架在私有化部署中的表现。
*   **搜索成本**：每任务 40 轮、每轮 8 个样本的搜索空间意味着较高的 API 调用成本和计算时间。
*   **奖励黑客风险**：虽然作者加强了验证，但在极其复杂的融合算子中，仍可能存在通过牺牲极小精度换取虚高速度的风险。
*   **参数搜索范围**：目前的模板参数优化仍依赖于 LLM 提出的范围，可能无法覆盖所有极端的硬件配置。

（完）
