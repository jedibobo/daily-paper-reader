---
title: "DANCE: Dynamic 3D CNN Pruning: Joint Frame, Channel, and Feature Adaptation for Energy Efficiency on the Edge"
title_zh: DANCE：动态 3D CNN 剪枝：面向边缘端能效的帧、通道与特征联合自适应
authors: "Mohamed Mejri, Ashiqur Rasul, Abhijit Chatterjee"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17275v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于边缘端能效提升的动态3D CNN剪枝
tldr: 针对3D CNN在边缘设备上能效不足的问题，本文提出DANCE框架。该框架通过输入感知的动态剪枝，在保持性能的同时最小化能耗。核心包含激活变异放大（AVA）和自适应激活剪枝（AAP）两步，实现了对帧、通道和特征的细粒度动态调整。实验证明，该方法在多种硬件平台上显著提升了推理速度和能效。
selection_source: fresh_fetch
motivation: 现代3D CNN无法根据输入样本的计算复杂度动态调整，导致在边缘设备上能耗过高。
method: 提出AVA技术增加神经元激活方差，并利用轻量级控制器AAP根据首层输出统计量动态剪枝帧、通道和特征。
result: 在Jetson Nano和Snapdragon平台上分别实现1.37倍和2.22倍加速，能效比现有技术提高1.47倍。
conclusion: DANCE通过细粒度的动态自适应机制，有效降低了3D CNN的计算开销，是实现边缘端高效视频处理的有力方案。
---

## 摘要
现代卷积神经网络（CNN）是视频和图像处理的核心工具，但无法根据输入样本的计算复杂度进行动态调整，以最小化能耗。在本研究中，我们提出了 DANCE，这是一种针对 3D CNN 的细粒度、输入感知的动态剪枝框架，旨在最大化能效，同时对性能的影响微乎其微甚至为零。在所提出的两步法中，第一步称为激活变异性放大（AVA），在此步骤中重新训练 3D CNN 模型，以增加整个网络中神经元激活幅度的方差，从而便于在各种 CNN 输入场景下做出剪枝决策。第二步称为自适应激活剪枝（AAP），通过训练一个轻量级激活控制器网络，根据网络第一层输出的统计数据，动态剪枝 3D 卷积层的帧、通道和特征（每层各不相同）。我们的方法通过在卷积层中引入稀疏性，显著减少了乘加（MAC）运算和内存访问。在 NVIDIA Jetson Nano GPU 和高通骁龙 8 Gen 1 平台上的硬件验证表明，分别实现了 1.37 倍和 2.22 倍的加速，与现有最先进技术相比，能效提高了多达 1.47 倍。

## Abstract
Modern convolutional neural networks (CNNs) are workhorses for video and image processing, but fail to adapt to the computational complexity of input samples in a dynamic manner to minimize energy consumption. In this research, we propose DANCE, a fine-grained, input-aware, dynamic pruning framework for 3D CNNs to maximize power efficiency with negligible to zero impact on performance. In the proposed two-step approach, the first step is called activation variability amplification (AVA), and the 3D CNN model is retrained to increase the variance of the magnitude of neuron activations across the network in this step, facilitating pruning decisions across diverse CNN input scenarios. In the second step, called adaptive activation pruning (AAP), a lightweight activation controller network is trained to dynamically prune frames, channels, and features of 3D convolutional layers of the network (different for each layer), based on statistics of the outputs of the first layer of the network. Our method achieves substantial savings in multiply-accumulate (MAC) operations and memory accesses by introducing sparsity within convolutional layers. Hardware validation on the NVIDIA Jetson Nano GPU and the Qualcomm Snapdragon 8 Gen 1 platform demonstrates respective speedups of 1.37X and 2.22X, achieving up to 1.47X higher energy efficiency compared to the state of the art.

---

## 论文详细总结（自动生成）

以下是对论文《DANCE: Dynamic 3D CNN Pruning: Joint Frame, Channel, and Feature Adaptation for Energy Efficiency on the Edge》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 3D CNN（如用于视频识别的 R(2+1)D、C3D）计算开销巨大，且无法根据输入样本的复杂度动态调整计算量，导致在资源受限的边缘设备上能效低下。
*   **研究动机**：现有的剪枝技术多为静态剪枝或针对 2D CNN 的粗粒度动态剪枝（如仅剪枝通道）。本文旨在提出一种针对 3D CNN 的**细粒度、输入感知**的动态剪枝框架，同时从**帧（Frame）、通道（Channel）和特征（Feature）**三个维度进行自适应调整，以实现极高的能效比。

### 2. 方法论
DANCE 框架分为两个关键步骤：
*   **步骤一：激活变异放大（AVA, Activation Variability Amplification）**
    *   **核心思想**：通过重新训练模型，人为拉大神经元激活幅度的差距，使“重要”和“不重要”的特征更易区分。
    *   **技术细节**：引入类似 CBAM 的分层注意力模块（帧、通道、特征）。在损失函数中加入方差项 $\sigma^2_{f}$，目标是最小化分类误差的同时最大化激活值的方差。
    *   **数学支撑**：证明了在归一化条件下，最大化标准差等同于最大化 Hoyer 稀疏度度量，从而为后续剪枝铺平道路。
*   **步骤二：自适应激活剪枝（AAP, Adaptive Activation Pruning）**
    *   **核心思想**：训练一个轻量级控制器，根据网络第一层的输出统计量，动态预测后续各层的剪枝阈值。
    *   **技术细节**：控制器由简单的全连接网络（FFN）组成，输出全局阈值 $\theta$。使用直通估计器（STE）和类似 Gumbel-Softmax 的软门控操作解决二值掩码不可导的问题，实现端到端训练。
    *   **执行过程**：对于每个输入视频，控制器实时生成阈值，跳过激活值低于阈值的帧、通道或特征像素点。

### 3. 实验设计
*   **数据集**：UCF101（动作识别）、HMDB51（人类动作数据库）。
*   **基准模型（Backbones）**：R(2+1)D 和 C3D。
*   **对比方法**：
    *   静态剪枝：Filter Pruning, KGS, KGRC, DCP, FP, TP, MDP 等。
    *   动态方法：与现有的 SOTA 动态选择策略进行对比。
*   **硬件验证平台**：
    *   NVIDIA Jetson Nano (4GB GPU)。
    *   Qualcomm Snapdragon 8 Gen 1 (Samsung S22 移动端 CPU)。

### 4. 资源与算力
*   **训练阶段**：文中未明确说明训练所使用的具体服务器级 GPU 型号（如 A100 或 V100）及总训练时长。
*   **推理阶段（硬件规格）**：
    *   Jetson Nano：使用 PyTorch 框架测试。
    *   Snapdragon 8 Gen 1：开发了定制的 C++ 内核，利用 **ARMv9 Neon SIMD** 指令集和 **FP16（半精度）** 进行优化。
*   **开销分析**：文中指出 AVA 模块和 AAP 控制器的参数量仅占总体的 0.13% 左右，计算量（FLOPs）占比不到 0.6%，额外开销极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流数据集、两种代表性 3D 架构、以及 GPU 和 CPU 两种不同的硬件后端。
*   **消融实验**：详细对比了“有无 AVA 步骤”以及“AAP 是否与主网络协同训练”的效果，验证了 AVA 对提升剪枝率的关键作用。
*   **可视化分析**：提供了激活分布直方图、每层 FLOPs 减少量分析，以及视频复杂度（空间/时间信息量）与计算量相关性的散点图。
*   **评价**：实验设计较为全面，不仅关注算法指标（准确率、剪枝率），还深入到了底层硬件的延迟和功耗测量，具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：在 UCF101 上，R(2+1)D 模型实现了 **4.0倍** 的剪枝率，且准确率反而提升了 1.55%（得益于 AVA 过滤了噪声特征）。
*   **硬件加速**：在 Jetson Nano 上实现 **1.37倍** 加速和 **1.47倍** 能效提升；在移动端 CPU 上通过定制内核实现了平均 **2.22倍** 的加速。
*   **输入相关性**：实验证实 DANCE 能够根据视频复杂度分配计算资源，复杂视频（如激流皮划艇）耗能多，简单视频（如滑雪）耗能显著减少。

### 7. 优点
*   **多维度细粒度**：不同于以往只剪通道的方法，DANCE 同时处理帧和特征空间，挖掘了更深层的稀疏性。
*   **硬件友好**：针对 CPU 和 GPU 的特性（如分支预测、向量化）分别优化了剪枝策略，具有很强的实用价值。
*   **鲁棒性**：AVA 步骤不仅辅助了剪枝，还起到了特征增强的作用，在大幅压缩的同时维持甚至提升了模型精度。

### 8. 不足与局限
*   **首层依赖**：剪枝决策完全依赖于第一层卷积的输出，如果第一层无法捕捉到足够的全局信息，可能会导致决策失误。
*   **全局阈值限制**：为了降低控制器开销，AAP 使用了跨层的全局阈值，这可能不如逐层（per-layer）定制阈值那样能压榨出极限的稀疏空间。
*   **训练复杂性**：采用两步训练法（先 AVA 后 AAP），相比于一步法增加了训练流程的复杂度。
*   **应用范围**：目前主要验证了动作识别任务，在实时性要求极高的视频流检测等更复杂场景下的表现尚待观察。

（完）
