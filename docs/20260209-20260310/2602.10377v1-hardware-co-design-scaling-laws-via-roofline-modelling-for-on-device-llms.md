---
title: Hardware Co-Design Scaling Laws via Roofline Modelling for On-Device LLMs
title_zh: 基于 Roofline 建模的端侧大语言模型硬件协同设计缩放定律
authors: "Luoyang Sun, Jiwen Jiang, Yifeng Ding, Fengfa Li, Yan Song, Haifeng Zhang, Jian Ying, Lei Ren, Kun Zhan, Wei Chen, Yan Xie, Cheng Deng"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.10377v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 端侧大模型的硬件协同设计缩放定律
tldr: "针对端侧设备部署大语言模型时面临的精度与延迟平衡难题，本文提出了一种基于Roofline模型的硬件协同设计缩放法则。通过将训练损失建模为架构参数的函数，并结合硬件延迟建模，研究者在NVIDIA Jetson Orin上评估了近两千种架构，成功识别出精度-延迟的帕累托前沿。该方法将架构选择周期从数月缩短至数天，且在相同延迟下比Qwen2.5-0.5B的困惑度降低了19.42%。"
selection_source: fresh_fetch
motivation: 在资源受限的端侧设备上，如何在满足严格延迟约束的同时选择最优精度的LLM架构是一个关键挑战。
method: 提出一种结合架构超参数训练损失函数与Roofline延迟建模的硬件协同设计框架，通过联合优化确定最优设计区域。
result: "在NVIDIA Jetson Orin平台上，该方法在相同延迟下实现了比Qwen2.5-0.5B低19.42%的困惑度，并大幅缩短了搜索时间。"
conclusion: 本研究为端侧LLM部署提供了首个原则性且可操作的硬件协同设计缩放法则框架，显著提升了模型在特定硬件上的效率。
---

## 摘要
视觉-语言-动作模型（VLA）已成为具身智能（Physical AI）的关键范式，并越来越多地部署在自动驾驶汽车、机器人和智能空间中。在这些资源受限的端侧场景中，选择合适的大语言模型（LLM）骨干网络是一项关键挑战：模型必须在准确率与严格的推理延迟及硬件效率约束之间取得平衡。这使得软硬件协同设计成为端侧 LLM 部署中改变游戏规则的需求，因为每个硬件平台都需要定制化的架构方案。我们提出了一种硬件协同设计定律，能够同时捕捉模型准确率和推理性能。具体而言，我们将训练损失建模为架构超参数的显式函数，并通过 Roofline 建模来表征推理延迟。我们在 NVIDIA Jetson Orin 上对 1,942 个候选架构进行了实证评估，对选定的 170 个模型各训练了 100 亿（10B）个 token，以拟合关联架构与训练损失的缩放定律。通过将该缩放定律与延迟建模相结合，我们建立了准确率与延迟之间的直接对应关系，并确定了硬件协同设计 LLM 的帕累托前沿（Pareto frontier）。我们进一步将架构搜索公式化为精度与性能的联合优化问题，推导出工业硬件和应用预算下的可行设计区域。我们的方法将架构选择的时间从数月缩短至数天。在目标硬件上，与 Qwen2.5-0.5B 延迟相同的情况下，我们协同设计的架构在 WikiText-2 上的困惑度（perplexity）降低了 19.42%。据我们所知，这是首个用于端侧 LLM 部署中硬件协同设计缩放定律的有原则且可操作的框架。我们将公开代码和相关的模型权重（checkpoints）。

## Abstract
Vision-Language-Action Models (VLAs) have emerged as a key paradigm of Physical AI and are increasingly deployed in autonomous vehicles, robots, and smart spaces. In these resource-constrained on-device settings, selecting an appropriate large language model (LLM) backbone is a critical challenge: models must balance accuracy with strict inference latency and hardware efficiency constraints. This makes hardware-software co-design a game-changing requirement for on-device LLM deployment, where each hardware platform demands a tailored architectural solution. We propose a hardware co-design law that jointly captures model accuracy and inference performance. Specifically, we model training loss as an explicit function of architectural hyperparameters and characterise inference latency via roofline modelling. We empirically evaluate 1,942 candidate architectures on NVIDIA Jetson Orin, training 170 selected models for 10B tokens each to fit a scaling law relating architecture to training loss. By coupling this scaling law with latency modelling, we establish a direct accuracy-latency correspondence and identify the Pareto frontier for hardware co-designed LLMs. We further formulate architecture search as a joint optimisation over precision and performance, deriving feasible design regions under industrial hardware and application budgets. Our approach reduces architecture selection from months to days. At the same latency as Qwen2.5-0.5B on the target hardware, our co-designed architecture achieves 19.42% lower perplexity on WikiText-2. To our knowledge, this is the first principled and operational framework for hardware co-design scaling laws in on-device LLM deployment. We will make the code and related checkpoints publicly available.

---

## 论文详细总结（自动生成）

### 论文总结：基于 Roofline 建模的端侧大语言模型硬件协同设计缩放定律

#### 1. 核心问题与整体含义（研究动机和背景）
随着视觉-语言-动作模型（VLA）在自动驾驶和机器人等具身智能领域的广泛应用，如何在资源受限的端侧设备（如 NVIDIA Jetson Orin）上部署高效的 LLM 成为核心挑战。传统的缩放定律（Scaling Laws）主要关注训练计算量与精度的关系，而忽视了推理延迟和硬件效率。
*   **研究动机**：云端优化的模型在边缘设备上往往因内存带宽或算力限制而表现不佳。
*   **核心问题**：如何建立一个原则性的框架，在给定硬件约束（延迟、内存、带宽）下，直接预测并搜索出精度最优的架构，实现软硬件的深度协同。

#### 2. 方法论：核心思想与关键技术
论文提出了**硬件协同设计缩放定律（Hardware Co-Design Scaling Law）**，将架构搜索转化为精度与性能的联合优化问题。
*   **精度建模（Loss Modeling）**：将验证损失 $L$ 建模为架构超参数的显式多项式函数，包括深度 ($l$)、宽度 ($d$)、FFN 扩展比 ($r$)、MoE 激活率 ($\rho$) 和 GQA 组比率 ($gqa$)。
*   **性能建模（Latency Modeling）**：利用 **Roofline 模型** 分析推理过程。
    *   **Prefill 阶段**：通常受算力限制（Compute-bound）。
    *   **Decode 阶段**：受内存带宽限制（Memory-bandwidth-bound），并考虑了 KV-cache 的影响。
*   **帕累托前沿（Pareto Frontier）分析**：通过联合损失模型和延迟模型，识别出在不同延迟预算下的非支配架构集合。
*   **理论框架**：推导了在延迟受限、内存受限及双重受限场景下的闭式解（Closed-form solutions），揭示了最优激活率 $\rho^*$ 与模型宽度 $d$ 之间的缩放关系（$\rho^* \propto d^{-1.19}$）。

#### 3. 实验设计
*   **硬件平台**：主要在 **NVIDIA Jetson Orin** 上进行基准测试。
*   **搜索空间**：涵盖了 1,942 种候选架构，变量包括深度（4-32层）、宽度（768-3072）、MoE 配置（1-16个专家）和 GQA 设置。
*   **训练设置**：从搜索空间中筛选出 170 个代表性模型，每个模型在 10B（100亿）token 的混合数据集（通用语料、数学、代码）上进行训练。
*   **Benchmark 与对比方法**：
    *   使用 **WikiText-2** 评估困惑度（Perplexity）。
    *   对比基准：**Qwen2.5-0.5B**。
    *   评估指标：验证损失（Validation Loss）、推理延迟（Latency）、困惑度。

#### 4. 资源与算力
*   **GPU 型号与数量**：每个配置在 **8 台 NVIDIA H200 GPU** 上训练。
*   **训练时长**：每个模型训练约 10 小时。
*   **总算力消耗**：总计约 **13,600 GPU-hours**（170个模型 × 8张显卡 × 10小时）。

#### 5. 实验数量与充分性
*   **实验规模**：测试了近 2,000 种架构的延迟，并实际训练了 170 个模型来拟合缩放定律。
*   **充分性**：实验覆盖了从稠密到稀疏（MoE）、从窄深到宽浅的广泛设计空间。拟合结果在训练集上 $R^2=0.975$，验证集上 $R^2=0.952$，证明了模型的预测能力。
*   **客观性**：通过统一的数据混合比例、优化器参数和硬件环境，确保了不同架构之间对比的公平性。

#### 6. 主要结论与发现
*   **MoE 的统治地位**：在端侧 Batch-1 推理场景下，帕累托前沿上的模型 100% 为稀疏 MoE 架构，证明了 MoE 在平衡容量与延迟方面的巨大优势。
*   **“宽而浅”优于“窄而深”**：在严格延迟约束下，增加宽度比增加深度能更有效地降低损失。
*   **FFN 扩展比**：最优 FFN 扩展比通常远小于传统的 4 倍，甚至出现小于 1 倍的情况。
*   **阶段差异性**：Prefill 和 Decode 阶段对专家数量的需求相反，总延迟优化需要在这两者间取得平衡。
*   **性能提升**：在相同延迟下，协同设计的架构比 Qwen2.5-0.5B 的困惑度降低了 **19.42%**。
*   **效率提升**：该框架将架构选择周期从数月缩短至数天。

#### 7. 优点
*   **原则性强**：首次将 Roofline 硬件分析与 Scaling Laws 深度结合，提供了严谨的数学推导。
*   **实用性高**：直接面向工业界端侧部署痛点，给出了针对不同应用场景（如自动驾驶、智能家居）的架构选择指南。
*   **泛化性**：理论框架可推广至其他硬件平台，只需测量基本的算力和带宽参数。

#### 8. 不足与局限
*   **训练量级限制**：缩放定律是基于 10B token 拟合的，对于万亿级（Trillion-scale）token 训练后的长期缩放行为是否完全一致仍需验证。
*   **硬件模型简化**：Roofline 模型是理想化的，未完全考虑算子融合（Operator Fusion）、内核启动开销（Kernel Launch Overhead）等复杂的系统级效应。
*   **架构组件限制**：目前主要针对标准 Transformer 组件，尚未涵盖线性注意力（Linear Attention）或状态空间模型（SSM）等新兴架构。

（完）
