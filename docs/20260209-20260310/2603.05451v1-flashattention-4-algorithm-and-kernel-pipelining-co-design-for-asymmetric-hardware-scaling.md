---
title: "FlashAttention-4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling"
title_zh: FlashAttention-4：针对非对称硬件扩展的算法与算子流水线协同设计
authors: "Ted Zadouri, Markus Hoehnerbach, Jay Shah, Timmy Liu, Vijay Thakkar, Tri Dao"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05451v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对Blackwell GPU的算法与内核流水线协同设计
tldr: "FlashAttention-4 针对 NVIDIA Blackwell 架构中 Tensor Core 与其他单元性能增长不匹配的挑战，通过算法与内核流水线的协同设计，引入了全异步 MMA 操作、软件模拟指数运算及 2-CTA MMA 模式等技术。该方法显著提升了注意力机制的计算效率，在 B200 上达到 71% 的利用率，并采用 Python 嵌入式 DSL 极大缩短了编译时间，为长文本大模型提供了高效支持。"
selection_source: fresh_fetch
motivation: 针对 Blackwell 架构中 Tensor Core 吞吐量翻倍而共享内存和指数运算单元增长缓慢导致的非对称硬件扩展瓶颈。
method: 重新设计了支持全异步 MMA 的流水线，采用软件模拟指数运算减少非矩阵乘法开销，并利用张量内存和 2-CTA 模式优化内存访问。
result: 在 B200 GPU 上，FlashAttention-4 的 BF16 性能达到 1613 TFLOPs/s，比 cuDNN 快 1.3 倍，比 Triton 快 2.7 倍。
conclusion: 通过软硬件协同设计和高效的 DSL 实现，FlashAttention-4 成功解决了新一代 GPU 的硬件瓶颈，显著提升了 Transformer 核心层的执行效率。
---

## 摘要
注意力机制作为无处不在的 Transformer 架构的核心层，是大语言模型和长上下文应用的瓶颈。虽然 FlashAttention-3 通过异步执行和 warp 特化针对 Hopper GPU 优化了注意力机制，但其主要针对 H100 架构。AI 行业已迅速转向部署基于 Blackwell 的系统（如 B200 和 GB200），由于非对称硬件扩展，这些系统表现出截然不同的性能特征：Tensor Core 吞吐量翻倍，而其他功能单元（共享内存带宽、指数运算单元）扩展较慢或保持不变。我们开发了多种技术来解决 Blackwell GPU 上这些变化的瓶颈：(1) 重新设计的流水线，利用完全异步的 MMA 操作和更大的分块（tile）尺寸；(2) 软件模拟的指数运算和条件 Softmax 重缩放，减少了非矩阵乘法（non-matmul）操作；(3) 利用张量内存（tensor memory）和 2-CTA MMA 模式，减少反向传播中的共享内存流量和原子加法（atomic adds）。我们证明了 FlashAttention-4 方法在使用 BF16 的 B200 GPU 上，相比 cuDNN 9.13 实现了高达 1.3 倍的加速，相比 Triton 实现了 2.7 倍的加速，峰值性能达到 1613 TFLOPs/s（利用率 71%）。除了算法创新，我们完全使用嵌入 Python 的 CuTe-DSL 实现了 FlashAttention-4，与传统的基于 C++ 模板的方法相比，在保持完整表达能力的同时，编译速度提高了 20-30 倍。

## Abstract
Attention, as a core layer of the ubiquitous Transformer architecture, is the bottleneck for large language models and long-context applications. While FlashAttention-3 optimized attention for Hopper GPUs through asynchronous execution and warp specialization, it primarily targets the H100 architecture. The AI industry has rapidly transitioned to deploying Blackwell-based systems such as the B200 and GB200, which exhibit fundamentally different performance characteristics due to asymmetric hardware scaling: tensor core throughput doubles while other functional units (shared memory bandwidth, exponential units) scale more slowly or remain unchanged. We develop several techniques to address these shifting bottlenecks on Blackwell GPUs: (1) redesigned pipelines that exploit fully asynchronous MMA operations and larger tile sizes, (2) software-emulated exponential and conditional softmax rescaling that reduces non-matmul operations, and (3) leveraging tensor memory and the 2-CTA MMA mode to reduce shared memory traffic and atomic adds in the backward pass. We demonstrate that our method, FlashAttention-4, achieves up to 1.3$\times$ speedup over cuDNN 9.13 and 2.7$\times$ over Triton on B200 GPUs with BF16, reaching up to 1613 TFLOPs/s (71% utilization). Beyond algorithmic innovations, we implement FlashAttention-4 entirely in CuTe-DSL embedded in Python, achieving 20-30$\times$ faster compile times compared to traditional C++ template-based approaches while maintaining full expressivity.

---

## 论文详细总结（自动生成）

### FlashAttention-4：针对非对称硬件扩展的算法与算子流水线协同设计

#### 1. 核心问题与整体含义
*   **研究动机**：随着 GPU 架构从 Hopper（H100）演进到 Blackwell（B200），硬件资源出现了“非对称扩展”。Blackwell 的 Tensor Core 算力翻倍，但共享内存（SMEM）带宽和指数运算单元（MUFU）的性能增长缓慢甚至停滞。
*   **核心问题**：传统的注意力优化算法（如 FlashAttention-3）在 Blackwell 上会遇到非矩阵乘法单元的瓶颈，导致硬件利用率无法达到最优。
*   **整体含义**：本论文提出了 FlashAttention-4，通过算法与内核流水线的深度协同设计，解决了 Blackwell 架构下的新瓶颈，显著提升了长文本处理和大规模模型训练的效率。

#### 2. 方法论
论文的核心思想是“绕过瓶颈”与“最大化异步重叠”：
*   **全异步 MMA 流水线**：利用 Blackwell 特有的张量内存（TMEM），设计了支持 128x128 大分块的异步流水线。MMA 操作直接写入 TMEM 而不占用寄存器，实现了计算、内存加载和 Softmax 处理的高度并行。
*   **软件模拟指数运算**：针对 MUFU 指数单元吞吐量不足的问题，利用算力充沛的 FMA（乘加运算）单元，通过多项式逼近（Cody-Waite 算法）在软件层面模拟 $2^x$。这有效地将计算压力从瓶颈单元转移到了闲置的算力单元。
*   **条件 Softmax 重缩放**：在在线 Softmax 计算中引入阈值机制。只有当局部最大值的增量足够大时才执行重缩放向量乘法，从而减少了非矩阵乘法的指令开销。
*   **2-CTA 协作模式**：在反向传播中，利用 Blackwell 的 2-CTA MMA 模式，让两个线程块协作处理数据。通过分布式共享内存（DSMEM）交换数据，减少了 50% 的共享内存流量，并将全局原子加法（Atomic Adds）的操作次数减半。
*   **CuTe-DSL 实现**：放弃了复杂的 C++ 模板，完全使用嵌入 Python 的 CuTe-DSL 编写。这在保持底层控制力的同时，将编译时间缩短了 20-30 倍。

#### 3. 实验设计
*   **测试场景**：标准自注意力（MHA）的前向和后向计算，涵盖因果掩码（Causal）和非掩码场景。
*   **Benchmark 设定**：
    *   **序列长度**：从 1k 扩展至 32k。
    *   **头维度（Head Dim）**：64、128 以及 DeepSeek-V3 使用的 (192, 128) 异构维度。
*   **对比方法**：
    *   **cuDNN 9.13/9.19**：NVIDIA 官方深度学习库。
    *   **Triton**：OpenAI 开发的算子编译器实现。
    *   **FlashAttention-2**：前代工业标准实现。
    *   **Gluon**：底层 GPU 编程语言实现。

#### 4. 资源与算力
*   **硬件平台**：NVIDIA B200 GPU (SXM6 1000W)。
*   **软件环境**：CUDA 13.1, PyTorch 2.10.0, CuTe-DSL 4.4.1。
*   **算力表现**：在 BF16 精度下达到了 **1613 TFLOPs/s**，相当于硬件理论峰值的 **71%**。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了数十组不同参数组合的对比实验，包括前向/后向传播、不同头维度、不同序列长度。
*   **消融实验**：针对“确定性反向传播”设计了专门的消融实验，对比了 SPT（最短处理时间优先）和 LPT（最长处理时间优先）等不同调度策略对性能的影响。
*   **充分性评价**：实验设计非常充分且具有针对性。通过与 cuDNN 最新版本的直接对比，证明了其在工业级应用中的领先地位。

#### 6. 主要结论与发现
*   **性能飞跃**：FlashAttention-4 在 B200 上比 cuDNN 9.13 快 1.1-1.3 倍，比 Triton 实现快 2.1-2.7 倍。
*   **瓶颈验证**：实验证实了在 Blackwell 架构上，Softmax 和内存流量确实取代了矩阵乘法成为新的主要瓶颈。
*   **开发效率**：证明了使用 Python 嵌入式 DSL 开发高性能算子的可行性，极大提升了研究人员的迭代速度。

#### 7. 优点
*   **软硬协同的典范**：不只是简单的算法优化，而是针对 Blackwell 架构特性（如 TMEM, 2-CTA）量身定制。
*   **调度策略创新**：引入 LPT 调度解决了因果掩码导致的负载不均衡问题，这在长文本场景下收益显著。
*   **工程友好性**：开源且易于编译（JIT 编译仅需几秒），降低了高性能算子的准入门槛。

#### 8. 不足与局限
*   **硬件绑定**：该算法高度依赖 Blackwell 架构的特定指令和内存结构，在 Hopper 或更旧的架构上无法运行，也不易迁移到非 NVIDIA 硬件。
*   **精度风险**：虽然论文称软件模拟指数运算在 BF16 下误差极小，但在对数值稳定性要求极高的特殊场景（如 FP32 训练）中，可能需要重新评估精度损失。
*   **应用范围**：目前主要针对标准稠密注意力，对于稀疏注意力（Sparse Attention）或线性注意力等变体的直接支持尚待进一步扩展。

（完）
