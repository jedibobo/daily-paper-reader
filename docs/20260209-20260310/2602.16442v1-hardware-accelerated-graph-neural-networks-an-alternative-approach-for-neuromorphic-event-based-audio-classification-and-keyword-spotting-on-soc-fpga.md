---
title: "Hardware-accelerated graph neural networks: an alternative approach for neuromorphic event-based audio classification and keyword spotting on SoC FPGA"
title_zh: 硬件加速图神经网络：一种用于 SoC FPGA 上神经形态基于事件的音频分类和关键词检出的替代方法
authors: "Kamil Jeziorek, Piotr Wzorek, Krzysztof Blachut, Hiroshi Nakano, Manon Dampfhoffer, Thomas Mesquida, Hiroaki Nishi, Thomas Dalgaty, Tomasz Kryjak"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16442v1"
tags: ["query:hwnas"]
score: 9.0
evidence: SoC FPGA上的硬件加速图神经网络
tldr: 本研究针对嵌入式边缘传感器产生的神经形态事件流，提出了一种基于SoC FPGA的硬件加速图神经网络（GNN）架构，用于音频分类和关键词识别。通过人工耳蜗将时间序列信号转换为稀疏事件图，显著降低了计算和存储开销。在SHD和SSC数据集上表现优异，相比SNN具有更高的精度、更低的延迟和功耗，为高效能事件驱动型音频处理提供了新方案。
selection_source: fresh_fetch
motivation: 针对神经形态设备产生的离散事件流，急需一种低延迟、低功耗且硬件友好的架构来实现高效的本地音频处理。
method: 利用人工耳蜗将音频信号转换为稀疏事件数据，并在SoC FPGA上实现结合图卷积层与循环序列建模的事件图神经网络。
result: "在SHD数据集上量化精度达92.3%，关键词识别任务延迟仅为10.53微秒，功耗低至1.18瓦，性能显著优于同类FPGA脉冲神经网络。"
conclusion: 该研究证明了图神经网络是处理神经形态音频任务的高效替代方案，为能源受限的边缘计算提供了强有力的基准。
---

## 摘要
随着嵌入式边缘传感器记录的数据量不断增加，特别是来自产生离散事件流的神经形态设备的数据，对于能够实现高效、低延迟和节能本地处理的硬件感知神经架构的需求日益增长。我们提出了一种用于音频处理的事件图神经网络的 FPGA 实现。我们利用人工耳蜗将时间序列信号转换为稀疏事件数据，从而降低了内存和计算成本。我们的架构在 SoC FPGA 上实现，并在两个开源数据集上进行了评估。在分类任务中，我们的基准浮点模型在 SHD 数据集上达到了 92.7% 的准确率，仅比当前最先进水平低 2.4%，而参数量则减少了 10 倍和 67 倍以上。在 SSC 数据集上，我们的模型达到了 66.9-71.0% 的准确率。与基于 FPGA 的脉冲神经网络相比，我们的量化模型达到了 92.3% 的准确率，性能提升高达 19.3%，同时减少了资源占用和延迟。对于 SSC 数据集，我们报告了首次硬件加速评估。我们进一步展示了首个事件音频关键词检出的端到端 FPGA 实现，该实现结合了图卷积层与循环序列建模。该系统实现了高达 95% 的词尾检测准确率，延迟仅为 10.53 微秒，功耗为 1.18 W，为节能的事件驱动型关键词检出（KWS）树立了强有力的基准。

## Abstract
As the volume of data recorded by embedded edge sensors increases, particularly from neuromorphic devices producing discrete event streams, there is a growing need for hardware-aware neural architectures that enable efficient, low-latency, and energy-conscious local processing. We present an FPGA implementation of event-graph neural networks for audio processing. We utilise an artificial cochlea that converts time-series signals into sparse event data, reducing memory and computation costs. Our architecture was implemented on a SoC FPGA and evaluated on two open-source datasets. For classification task, our baseline floating-point model achieves 92.7% accuracy on SHD dataset - only 2.4% below the state of the art - while requiring over 10x and 67x fewer parameters. On SSC, our models achieve 66.9-71.0% accuracy. Compared to FPGA-based spiking neural networks, our quantised model reaches 92.3% accuracy, outperforming them by up to 19.3% while reducing resource usage and latency. For SSC, we report the first hardware-accelerated evaluation. We further demonstrate the first end-to-end FPGA implementation of event-audio keyword spotting, combining graph convolutional layers with recurrent sequence modelling. The system achieves up to 95% word-end detection accuracy, with only 10.53 microsecond latency and 1.18 W power consumption, establishing a strong benchmark for energy-efficient event-driven KWS.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hardware-accelerated graph neural networks: an alternative approach for neuromorphic event-based audio classification and keyword spotting on SoC FPGA》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着边缘计算和物联网设备（如智能手表、语音助手）的普及，对低功耗、低延迟的本地音频处理需求日益增长。传统的深度学习模型在处理连续信号时功耗较高，而**神经形态传感器（如人工耳蜗）**产生的离散事件流具有天然的稀疏性。
*   **核心挑战**：目前主流处理事件流的方法是脉冲神经网络（SNN），但 SNN 在硬件实现中存在非确定性的内存访问模式，难以充分利用稀疏性。
*   **研究目标**：提出一种基于**图神经网络（GNN）**的替代方案，利用其确定性的权重访问模式，在 SoC FPGA 上实现高效的音频分类和关键词检出（KWS）。

### 2. 论文提出的方法论
论文设计了一套从原始音频到硬件输出的完整流水线，核心思想是将音频事件视为时空图中的节点。
*   **人工耳蜗（Artificial Cochlea）**：将时域音频信号通过带通滤波器组转换为多通道的异步脉冲事件。
*   **图生成模块（Graph Generator）**：
    *   **1D 上下文内存**：仅存储每个通道最新的事件时间戳，显著降低内存开销。
    *   **跳步连接（Skip Step Connection）**：在寻找邻居节点时采用固定间隔采样，将计算复杂度降低了一个数量级。
*   **图卷积层（Graph Convolution）**：采用改进的 **PointNetConv** 算子。引入了**位置归一化（Positional Normalization）**，将相对时间和通道差异缩放到 (0, 1) 区间，解决了量化过程中的精度损失问题。
*   **任务特定架构**：
    *   **分类任务**：采用全局平均池化（Global Average Pooling）提取全样本特征。
    *   **KWS 任务**：将事件流切分为 10ms 的窗口，通过最大池化（Max Pooling）后接入 **GRU（循环神经网络）**，实现对连续音频流的实时在线预测。
*   **硬件优化**：在 SoC FPGA 上实现了全流水线架构，支持 8 位定点量化，并针对双口 BRAM 优化了向量乘法器。

### 3. 实验设计
*   **数据集**：
    *   **SHD (Spiking Heidelberg Digits)**：德语和英语数字语音的事件化版本，用于基准分类测试。
    *   **SSC (Spiking Speech Commands)**：基于 Google Speech Commands 转换的事件数据集，包含 35 类词汇，用于更复杂的分类和 KWS 测试。
*   **Benchmark 与对比方法**：
    *   **软件对比**：与多种 SNN 模型（如 RSNN, TA-SNN）及原始 GNN 模型对比准确率和参数量。
    *   **硬件对比**：与现有的 FPGA SNN 加速器（如 Spiker+, QUANTISENC）对比资源占用、延迟和功耗。
    *   **跨平台对比**：与嵌入式 GPU/CPU 平台（NVIDIA Jetson Orin NX）对比能效比。

### 4. 资源与算力
*   **训练阶段**：使用单张 **NVIDIA A100 GPU** 进行训练。采用了量化感知训练（QAT）策略，先进行 100 轮浮点训练，再进行 20 轮量化微调。
*   **部署阶段**：目标硬件为 **AMD Xilinx Zynq UltraScale+ ZCU104 (XCZU7EV)**。
*   **算力指标**：FPGA 运行频率为 200 MHz。分类任务的理论吞吐量达到 555 kEPS（每秒千事件）。

### 5. 实验数量与充分性
论文进行了多维度的实验验证，具有较高的充分性和客观性：
*   **消融实验**：详细测试了图搜索半径（$r_{ch}$）、跳步步长（$s$）、池化方式（Max/Mean/Add）对准确率的影响。
*   **模型缩放**：设计了从 Tiny（8.6k 参数）到 Large（272k 参数）的五种配置，验证了架构的扩展性。
*   **硬件对比**：不仅对比了逻辑资源（LUT/FF/BRAM/DSP），还通过 Vivado Power Analyzer 模拟了真实翻转率下的动态功耗。
*   **公平性**：在相同的数据集划分下与 SNN 硬件进行对比，并首次在 SSC 数据集上提供了硬件加速的基准数据。

### 6. 论文的主要结论与发现
*   **高精度与轻量化**：在 SHD 数据集上，量化后的 GNN 模型达到 **92.3%** 的准确率，优于所有已知的 FPGA SNN 实现（最高提升 19.3%），且参数量比 SOTA 软件模型少 10-67 倍。
*   **极低延迟**：KWS 任务的端到端延迟仅为 **10.53 μs**，远低于人类感知的毫秒级要求。
*   **高能效**：系统总功耗仅为 **1.18 W**。相比嵌入式 GPU（Jetson Orin NX），FPGA 实现的延迟降低了约 1,700 至 15,000 倍，功耗降低了 4 倍以上。
*   **任务通用性**：证明了 GNN 架构不仅适用于静态分类，通过引入 GRU 也能胜任连续流式的关键词识别任务。

### 7. 优点
*   **硬件友好性**：通过“跳步连接”和“位置归一化”解决了 GNN 在 FPGA 上计算复杂和量化难的问题。
*   **端到端实现**：展示了从事件输入到 GRU 序列建模的完整硬件链路，而非仅加速核心算子。
*   **稀疏性利用**：完全基于事件触发（Event-by-event），在无信号输入时计算开销极低。

### 8. 不足与局限
*   **准确率上限**：虽然优于硬件 SNN，但相比于参数量巨大的顶级软件 SNN 模型（如 96% 以上准确率的模型），仍有约 2-3% 的差距。
*   **超参数敏感性**：图生成的半径和步长对性能影响较大，针对不同音频环境可能需要重新调优。
*   **应用限制**：目前主要在模拟的人工耳蜗数据集上验证，尚未在真实的物理传感器实时接入环境下进行长期的鲁棒性测试。

（完）
