---
title: "When Small Variations Become Big Failures: Reliability Challenges in Compute-in-Memory Neural Accelerators"
title_zh: 当微小波动演变为重大故障：存内计算神经加速器的可靠性挑战
authors: "Yifan Qin, Jiahao Zheng, Zheyu Yan, Wujie Wen, Xiaobo Sharon Hu, Yiyu Shi"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03491v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 连接存算一体的器件物理、架构和学习算法
tldr: 存内计算（CiM）架构虽能提升能效，但非易挥发存储器的器件非理想性（如写入变异、电导漂移）严重威胁其可靠性。本文揭示了微小器件波动在安全关键应用中导致的灾难性失效，并提出了SWIM选择性写入验证机制及右截断高斯噪声训练算法。通过跨层协同设计，在保持CiM高效性的同时，显著提升了神经网络加速器的鲁棒性与可靠性，为安全关键系统的部署奠定了基础。
selection_source: fresh_fetch
motivation: 存内计算中新兴存储器件的物理非理想性会导致严重的精度下降，特别是在对可靠性要求极高的安全关键型应用中存在巨大风险。
method: 提出了SWIM选择性写入验证机制以及基于右截断高斯噪声的神经网络训练方法，从硬件架构和学习算法两方面应对器件变异。
result: 研究发现微小器件波动会导致不成比例的精度损失，而所提方案在维持CiM能效优势的同时，显著增强了系统在最坏情况下的可靠性。
conclusion: 跨层协同设计是解决存内计算可靠性挑战、推动其在安全与可靠性要求严苛的系统中落地应用的关键路径。
---

## 摘要
存内计算 (CiM) 架构通过缓解冯·诺依曼瓶颈，有望显著提高深度神经网络加速的能效和吞吐量。然而，其对新兴非易失性存储器件的依赖引入了器件级的非理想特性——如写入变异性、电导漂移和随机噪声——这从根本上挑战了可靠性、可预测性和安全性，特别是在安全关键型应用中。本次演讲探讨了基于 CiM 的神经加速器的可靠性极限，并提出了一系列跨越器件物理、架构和学习算法的技术来应对这些挑战。我们首先证明，即使是微小的器件波动也可能导致安全关键型推理工作负载中不成比例的巨大精度下降和灾难性故障，揭示了平均情况评估与最坏情况行为之间的关键差距。基于这一见解，我们引入了 SWIM，一种选择性写入-验证机制，该机制战略性地仅在影响最大的地方应用验证，在保持 CiM 效率优势的同时显著提高了可靠性。最后，我们探索了一种以学习为中心的解决方案，通过使用右删失高斯噪声训练神经网络来提高现实中的最坏情况性能，使训练假设与硬件引起的变异性保持一致，并在不增加过多硬件开销的情况下实现鲁棒部署。总之，这些工作强调了 CiM 加速器跨层协同设计的必要性，并为在新兴存储技术上实现可靠、高效的神经推理提供了一条原则性路径，为其在安全和可靠性关键系统中的应用铺平了道路。

## Abstract
Compute-in-memory (CiM) architectures promise significant improvements in energy efficiency and throughput for deep neural network acceleration by alleviating the von Neumann bottleneck. However, their reliance on emerging non-volatile memory devices introduces device-level non-idealities-such as write variability, conductance drift, and stochastic noise-that fundamentally challenge reliability, predictability, and safety, especially in safety-critical applications. This talk examines the reliability limits of CiM-based neural accelerators and presents a series of techniques that bridge device physics, architecture, and learning algorithms to address these challenges. We first demonstrate that even small device variations can lead to disproportionately large accuracy degradation and catastrophic failures in safety-critical inference workloads, revealing a critical gap between average-case evaluations and worst-case behavior. Building on this insight, we introduce SWIM, a selective write-verify mechanism that strategically applies verification only where it is most impactful, significantly improving reliability while maintaining CiM's efficiency advantages. Finally, we explore a learning-centric solution that improves realistic worst-case performance by training neural networks with right-censored Gaussian noise, aligning training assumptions with hardware-induced variability and enabling robust deployment without excessive hardware overhead. Together, these works highlight the necessity of cross-layer co-design for CiM accelerators and provide a principled path toward dependable, efficient neural inference on emerging memory technologies-paving the way for their adoption in safety- and reliability-critical systems.

---

## 论文详细总结（自动生成）

以下是对论文《When Small Variations Become Big Failures: Reliability Challenges in Compute-in-Memory Neural Accelerators》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：基于非易失性存储器（NVM）的存内计算（CiM）架构通过消除冯·诺依曼瓶颈，显著提升了神经网络加速的能效。
*   **核心问题**：NVM 器件存在固有的非理想性（如写入变异、电导漂移、随机噪声）。传统的可靠性评估通常关注“平均精度”，但在安全关键型应用（如自动驾驶）中，极少数极端的器件偏差可能导致精度从正常直接崩溃至接近 0%（灾难性失效）。
*   **整体含义**：本文旨在揭示微小器件波动与最坏情况失效之间的非线性放大关系，并提出跨层（硬件+软件）的协同设计方案来保障 CiM 的可靠性。

### 2. 论文提出的方法论
论文总结了三项互补的研究工作：
*   **最坏情况表征（Worst-Case Characterization）**：将可靠性评估建模为一个优化问题。在给定写入验证（Write-Verify）约束范围内，搜索能使推理精度最小化的权重噪声组合 $\Delta W$。这证明了传统的蒙特卡洛（MC）采样无法有效捕获长尾失效风险。
*   **SWIM（选择性写入验证）**：
    *   **核心思想**：由于全量写入验证开销巨大，SWIM 仅对“最具影响力”的权重进行验证。
    *   **技术细节**：利用基于泰勒展开的损失函数敏感度指标，量化每个权重波动对整体损失的影响。按敏感度降序排列，在给定预算内优先验证高敏感度权重。
*   **TRICE（右截断高斯噪声训练）**：
    *   **核心思想**：在软件训练阶段引入与硬件特性匹配的噪声。
    *   **技术细节**：采用“右截断高斯噪声”进行注入训练。这种方法针对 $k$ 分位性能（KPP，如最差的 1% 情况）进行优化，使模型在部署时对硬件诱导的变异具有更强的鲁棒性。

### 3. 实验设计
*   **评估指标**：除了平均精度外，重点引入了 **KPP（k-th Percentile Performance）**，即在 $k\%$ 最坏情况下的性能表现。
*   **对比基准（Benchmark）**：
    *   **评估对比**：将提出的优化搜索方法与 10 万次运行的蒙特卡洛（MC）采样进行对比。
    *   **硬件对比**：将 SWIM 与基于权重绝对值大小、层顺序等朴素启发式选择方法进行对比。
    *   **软件对比**：将 TRICE 与标准训练及普通高斯噪声注入训练进行对比。
*   **数据集/模型**：文中提到使用了“代表性网络和数据集”（通常指 ImageNet、CIFAR 等，但在本短篇总结中未列出具体名称）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于这是一篇关于可靠性挑战的综述/短篇论文，其重点在于方法论的阐述。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了多达 10 万次的蒙特卡洛模拟以证明其无法捕捉尾部失效，这体现了实验的严谨性。
*   **充分性评价**：实验覆盖了从器件物理特性分析到架构优化，再到算法增强的完整链路。通过对比不同变异强度和不同模型，验证了方案的普适性。虽然作为短篇论文数据展示有限，但其逻辑链条完整，对比维度客观。

### 6. 论文的主要结论与发现
*   **放大效应**：即使每个器件的独立波动很小，它们的组合效应可能导致精度彻底崩溃（从高精度降至接近 0%）。
*   **评估盲区**：传统的平均精度评估掩盖了长尾风险，10 万次 MC 采样仍可能漏掉最坏情况。
*   **协同必要性**：单纯依靠硬件验证或单纯依靠软件训练都难以在效率和可靠性之间取得完美平衡，跨层协同设计是实现可靠 CiM 推理的必经之路。

### 7. 优点
*   **视角独特**：跳出了“平均精度”的思维定式，关注安全关键应用中的“最坏情况”和“长尾风险”。
*   **高效性**：SWIM 机制通过敏感度分析避免了盲目验证，在极低开销下实现了高可靠性。
*   **实用性**：TRICE 训练方法属于“即插即用”型，不需要额外的硬件修改即可提升鲁棒性。

### 8. 不足与局限
*   **实验细节缺失**：由于是短篇论文（2页），缺乏具体数据集上的详细精度数值和硬件开销（如功耗、面积）的定量分析。
*   **噪声模型假设**：主要基于高斯噪声及其变体，对于某些特定 NVM 器件（如具有非对称漂移特性的器件）的适用性有待进一步验证。
*   **应用限制**：主要针对推理阶段，未讨论在线学习或训练过程中的可靠性挑战。

（完）
