---
title: "HAWX: A Hardware-Aware FrameWork for Fast and Scalable ApproXimation of DNNs"
title_zh: HAWX：一种用于深度神经网络（DNN）快速且可扩展近似的硬件感知框架
authors: "Samira Nazari, Mohammad Saeed Almasi, Mahdi Taheri, Ali Azarpeyvand, Ali Mokhtari, Ali Mahani, Christian Herglotz"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16336v2"
tags: ["query:hwnas"]
score: 10.0
evidence: 用于快速探索DNN配置的硬件感知框架
tldr: HAWX是一个硬件感知的深度神经网络（DNN）近似计算框架，旨在解决近似配置搜索空间巨大的挑战。它通过算子、滤波器、层和模型四个层级的多级灵敏度评分，结合精度、功耗和面积的预测模型，指导异构近似计算模块的集成。该框架在保持高精度的同时，显著提升了搜索速度，且其效率优势随网络规模呈指数级增长，支持多种硬件加速器架构。
selection_source: fresh_fetch
motivation: 针对DNN近似计算中搜索空间巨大且难以兼顾硬件效率与模型精度的问题，需要一种高效且可扩展的硬件感知搜索框架。
method: 提出HAWX框架，利用多级灵敏度评分和性能预测模型，在不同抽象层次上快速评估并选择最优的异构近似计算组件。
result: 在LeNet-5、ResNet-18等模型上实现了最高超过三百万倍的搜索加速，且精度与穷举搜索相当。
conclusion: HAWX为大规模DNN的硬件感知近似优化提供了一种高效、可扩展且支持多种架构的通用解决方案。
---

## 摘要
本研究提出了 HAWX，这是一个硬件感知的可扩展探索框架，它在不同的 DNN 抽象层级（算子、滤波器、层和模型）采用多级灵敏度评分，以指导异构近似计算（AxC）模块的选择性集成。在精度、功耗和面积预测模型的支持下，HAWX 加速了候选配置的评估。在具有两个候选近似模块的层级搜索中，它实现了超过 23 倍的加速；而在仅针对 LeNet-5 的滤波器级搜索中，加速比超过了 3×10^6 倍，同时保持了与穷举搜索相当的精度。在 VGG-11、ResNet-18 和 EfficientNetLite 等最先进的 DNN 基准测试上的实验表明，HAWX 的效率收益随网络规模呈指数级增长。HAWX 硬件感知搜索算法支持空间和时间加速器架构，能够利用现成的近似组件或定制设计。

## Abstract
This work presents HAWX, a hardware-aware scalable exploration framework that employs multi-level sensitivity scoring at different DNN abstraction levels (operator, filter, layer, and model) to guide selective integration of heterogeneous AxC blocks. Supported by predictive models for accuracy, power, and area, HAWX accelerates the evaluation of candidate configurations, achieving over 23* speedup in a layer-level search with two candidate approximate blocks and more than (3*106)* speedup at the filter-level search only for LeNet-5, while maintaining accuracy comparable to exhaustive search. Experiments across state-of-the-art DNN benchmarks such as VGG-11, ResNet-18, and EfficientNetLite demonstrate that the efficiency benefits of HAWX scale exponentially with network size. The HAWX hardware-aware search algorithm supports both spatial and temporal accelerator architectures, leveraging either off-the-shelf approximate components or customized designs.

---

## 论文详细总结（自动生成）

这是一份关于论文《HAWX: A Hardware-Aware FrameWork for Fast and Scalable ApproXimation of DNNs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
深度神经网络（DNN）在边缘侧部署时面临严峻的功耗和面积挑战。**近似计算（Approximate Computing, AxC）**通过牺牲极小的精度来换取显著的硬件效率提升。然而，现有的近似计算研究面临三大痛点：
*   **搜索空间爆炸**：当在算子或滤波器级别进行异构近似选择时，配置组合呈指数级增长，穷举搜索不可行。
*   **缺乏硬件感知**：多数框架是硬件无关的，未考虑空间架构（如数据流加速器）与时间架构（如软处理器）在资源映射上的本质差异。
*   **评估成本高**：现有的仿真工具虽然比直接硬件实现快，但在大规模搜索中依然耗时巨大。

**HAWX** 的提出旨在建立一个自动化、可扩展且硬件感知的框架，通过多级灵敏度评分快速定位最优的近似配置。

### 2. 方法论：核心思想与关键技术
HAWX 的核心流程分为四个阶段：
*   **DNN 剖析（Profiling）**：利用 K-means 聚类算法从数据集中提取代表性样本，通过一次 GPU 推理获取计算图、操作类型及损失函数对每个算子输出的梯度（用于衡量灵敏度）。
*   **多级 HAWX 评分**：
    *   **算子级**：定义 `hawx(o, i)` 为“梯度”与“近似误差（Exact vs AxC）”的乘积。
    *   **层级聚合**：通过平均绝对误差（MAE）将评分从算子级向上聚合至滤波器（Filter）、层（Layer）和模型（Model）级别。
*   **设计空间探索（DSE）**：
    *   采用**二分查找策略**确定满足精度阈值的“边界评分”。
    *   根据边界评分对设计空间进行剪枝，仅保留满足精度要求的近似模块组合。
*   **硬件感知映射与估算**：
    *   **空间架构**：考虑 PE 数量和 SIMD 宽度，直接映射近似算子。
    *   **时间架构**：考虑计算向量（CV）的复用，将层进行分组映射。
    *   最终输出功耗和面积的预测报告。

### 3. 实验设计
*   **数据集与模型**：
    *   MNIST (LeNet-5)
    *   CIFAR-10 (VGG-11, ResNet-18)
    *   GTSRB (EfficientNetLite)
*   **近似组件库**：使用 EvoApproxLib 中的 8 位近似乘法器（K 系列和 L 系列）。
*   **Benchmark 与对比方法**：
    *   **基准**：穷举搜索（Exhaustive Search）。
    *   **对比框架**：TFApprox（GPU 仿真）、I-NN（误差评估工具）、AME（架构误差指标）、MAX-DNN（贪心搜索）、以及基于强化学习（RL）的 DSE 方法。
*   **硬件验证**：在 Xilinx Zynq UltraScale+ FPGA 上部署，验证空间架构（Dataflow）和时间架构（PERUN 软处理器）。

### 4. 资源与算力
*   **软件环境**：Intel Core i7-7500U CPU, 16GB RAM, NVIDIA GeForce 940MX GPU。
*   **硬件环境**：Xilinx Vivado 和 Vitis HLS 2020.2 用于 FPGA 综合。
*   **训练/搜索时长**：文中强调了搜索效率的提升。例如，LeNet-5 的滤波器级搜索从不可行的穷举时间缩短到仅需 1 次推理评估，实现了超过 $3 \times 10^6$ 倍的加速。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从小型（LeNet）到大型（EfficientNet）的多种网络拓扑，验证了框架的可扩展性。
*   **消融与对比**：对比了不同近似模块（KV8, L2N 等）在不同模型上的表现，并展示了 `hawx` 评分与实际精度下降之间的高度相关性。
*   **客观性**：通过在两种截然不同的硬件架构（空间 vs 时间）上进行综合测试，证明了框架的通用性。实验设计较为充分且对比维度全面。

### 6. 主要结论与发现
*   **极高的搜索效率**：相比穷举搜索，HAWX 在层级搜索中实现 23 倍以上加速，在细粒度滤波器级搜索中实现指数级加速（如 EfficientNet 提升 $10^{4000}$ 倍以上）。
*   **高精度预测**：`hawx` 评分能准确反映模型对近似误差的敏感度，生成的配置在大幅降低功耗和面积的同时，精度损失极小。
*   **硬件适配性**：成功在 FPGA 上实现了异构近似配置，证明了在实际硬件中减少面积和功耗的可行性。

### 7. 优点（亮点）
*   **多粒度支持**：支持从算子到模型四个层级的分析，允许极细粒度的异构近似。
*   **单次推理分析**：通过梯度分析避免了成千上万次的重复推理，极大地降低了 DSE 的时间成本。
*   **架构通用性**：显式区分并支持空间和时间两种主流加速器架构，这在同类框架中较为少见。

### 8. 不足与局限
*   **近似算子单一**：实验主要集中在 8 位乘法器上，虽然理论上支持加法器和其他位宽，但文中未给出具体实验数据。
*   **代表性样本依赖**：K-means 选取的样本数量虽然减少了开销，但在极端分布的数据集上可能存在代表性不足的风险。
*   **硬件估算模型**：功耗和面积主要基于预测模型和 FPGA 综合结果，对于 ASIC 等其他后端工艺的泛化性有待进一步验证。

（完）
