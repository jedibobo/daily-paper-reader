---
title: "Hexagon-MLIR: An AI Compilation Stack For Qualcomm's Neural Processing Units (NPUs)"
title_zh: Hexagon-MLIR：面向高通神经网络处理器 (NPU) 的 AI 编译栈
authors: "Mohammed Javed Absar, Muthu Baskaran, Abhikrant Sharma, Abhilash Bhandari, Ankit Aggarwal, Arun Rangasamy, Dibyendu Das, Fateme Hosseini, Franck Slama, Iulian Brumar, Jyotsna Verma, Krishnaprasad Bindumadhavan, Mitesh Kothari, Mohit Gupta, Ravishankar Kolachana, Richard Lethin, Samarth Narang, Sanjay Motilal Ladwa, Shalini Jain, Snigdha Suresh Dalvi, Tasmia Rahman, Venkat Rasagna Reddy Komatireddy, Vivek Vasudevbhai Pandya, Xiyue Shi, Zachary Zipper"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19762v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对NPU的AI编译栈及内存局部性优化
tldr: Hexagon-MLIR 是一个针对高通 Hexagon NPU 的开源 AI 编译栈，基于 MLIR 框架构建。它支持将 Triton 内核和 PyTorch 模型自动编译为 NPU 二进制文件，通过生成“巨型内核（mega-kernels）”来优化紧耦合内存（TCM）的数据局部性，从而减少带宽瓶颈。该项目为开发者提供了一个灵活的开源路径，旨在加速 AI 工作负载在 NPU 上的部署与性能优化。
selection_source: fresh_fetch
motivation: 旨在解决传统库方法中的带宽瓶颈，并为开发者提供一个比商业工具链更灵活、开源的 Hexagon NPU 编译方案。
method: 基于 MLIR 框架，通过一系列结构化 pass 将 Triton 内核和 PyTorch 模型转换为针对 NPU 架构优化的二进制文件。
result: 实现了从 Triton 内核到二进制的自动化编译，并通过生成 mega-kernels 显著提升了 TCM 的数据局部性。
conclusion: Hexagon-MLIR 为高通 NPU 提供了一个高效、灵活的开源编译栈，是推动 AI 编译能力进步的重要尝试。
---

## 摘要
在本文中，我们介绍了 Hexagon-MLIR，这是一个针对高通 Hexagon 神经网络处理器 (NPU) 的开源编译栈，为 Triton 内核 (kernels) 和 PyTorch 模型的下放 (lowering) 提供统一支持。该编译器基于 MLIR 框架构建，应用了一系列结构化的 pass，以利用 NPU 架构特性来加速 AI 工作负载。通过提供从内核到二进制文件的自动化编译，它能够为目标平台更快地部署新的 Triton 内核（手写或来自 PyTorch 2.0 的子图）。通过摄取 Triton 内核，我们生成了巨型内核 (mega-kernels)，从而最大限度地提高 NPU 紧耦合内存 (TCM) 中的数据局部性，减少了基于库的方法中固有的带宽瓶颈。这一举措通过为开发者提供基于 MLIR 的开源编译栈，补充了我们的商业工具链，为他们提供了一种通过更灵活的方法来提升 AI 编译能力的途径。Hexagon-MLIR 是一项正在进行中的工作，我们正在持续努力添加更多的优化和功能。

## Abstract
In this paper, we present Hexagon-MLIR,an open-source compilation stack that targets Qualcomm Hexagon Neural Processing Unit (NPU) and provides unified support for lowering Triton kernels and PyTorch models . Built using the MLIR framework, our compiler applies a structured sequence of passes to exploit NPU architectural features to accelerate AI workloads. It enables faster deployment of new Triton kernels (hand-written or subgraphs from PyTorch 2.0), for our target by providing automated compilation from kernel to binary. By ingesting Triton kernels, we generate mega-kernels that maximize data locality in the NPU's Tightly Coupled Memory (TCM), reducing the bandwidth bottlenecks inherent in library-based approaches. This initiative complements our commercial toolchains by providing developers with an open-source MLIR-based compilation stack that gives them a path to advance AI compilation capabilities through a more flexible approach. Hexagon-MLIR is a work-in-progress, and we are continuing to add many more optimizations and capabilities in this effort.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hexagon-MLIR: An AI Compilation Stack For Qualcomm's Neural Processing Units (NPUs)》的结构化总结：

### 1. 核心问题与背景
*   **研究动机**：传统的 AI 模型部署主要依赖于手工优化的算子库。虽然性能高，但开发周期长，且难以应对 LLM（大语言模型）和 MoE（混合专家模型）等快速演进的新架构。
*   **核心问题**：算子库在优化时如同“黑盒”，导致算子间的数据必须频繁返回 DRAM（内存），造成严重的带宽瓶颈。
*   **目标**：开发一个开源的、基于 MLIR 的编译栈，能够自动将 Triton 内核和 PyTorch 模型编译为高效的 NPU 二进制文件，通过“巨型内核（mega-kernels）”策略最大化利用紧耦合内存（TCM），减少数据搬运。

### 2. 方法论
Hexagon-MLIR 采用**生成式方法（Generative Approach）**，核心流程如下：
*   **前端接入**：通过 `Torch-MLIR` 接收 PyTorch 模型，通过 `triton-to-linalg` 接收 Triton 内核，统一转换为 MLIR 的 `Linalg` 方言。
*   **关键技术 Pass**：
    *   **算子融合（Operator Fusion）**：将多个 `linalg.generic` 操作合并，消除中间变量的内存物化，提高局部性。
    *   **内存分块（Tiling）**：将大张量分割为适配 TCM 大小的切片，并插入 DMA 传输指令。
    *   **多线程化（Multi-threading）**：利用 MLIR 的 `Async` 方言，将任务分发到 Hexagon 的多个硬件线程和 HVX（向量扩展）上下文。
    *   **双缓冲（Double Buffering）**：采用 Ping-Pong 缓冲机制，使 DMA 数据传输与 HVX 计算并行，掩盖内存延迟。
    *   **向量化（Vectorization）**：针对 HVX 架构生成 128 字节宽的 SIMD 指令。
    *   **数学库集成**：集成高通 Hexagon 库（QHL），利用多项式逼近等技术加速 `exp`、`tanh` 等超越函数。

### 3. 实验设计
*   **测试场景**：针对常见的 AI 算子内核进行微基准测试。
*   **测试算子**：包括 GELU、Softmax、RMS-norm、SiLU、Flash Attention、Vec-Add-2D 和指数级数（Exponent Series）。
*   **对比基准（Benchmark）**：
    *   标量执行 vs. 向量化执行。
    *   单线程 vs. 多线程执行。
    *   单缓冲 vs. 双缓冲优化。
*   **数据规模**：元素数量从 8K 跨越到 1M，涵盖了从计算密集型到内存受限型的不同区间。

### 4. 资源与算力
*   **硬件平台**：高通 Hexagon NPU（具备 HVX 向量扩展和 TCM 存储）。
*   **算力说明**：文中未提及具体的模型训练时长或 GPU 资源，因为该研究聚焦于**推理侧的编译器优化**，而非模型训练。实验主要在 NPU 硬件模拟器或真实芯片上运行。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了约 7 种核心算子的性能数据，并针对 GELU 算子进行了详细的消融实验（不同数据规模下的多线程加速比）。
*   **充分性评价**：
    *   **优点**：实验涵盖了从基础向量加法到复杂的 Flash Attention 算子，验证了编译栈的通用性。
    *   **局限**：实验主要集中在算子级（Kernel-level），缺乏完整端到端模型（如完整的 Llama-3 推理）的对比数据。
    *   **客观性**：通过展示多线程在小规模数据下的开销（负优化），体现了实验结果的真实性。

### 6. 主要结论
*   **向量化增益显著**：在 `float16` 下，GELU 算子实现了 **63.9x** 的加速，RMS-norm 达到 **46.5x**。
*   **多线程扩展性**：当数据规模超过 32K 元素时，多线程开始产生正收益，在 512K 规模下达到约 **3.95x** 的加速（接近硬件线程极限）。
*   **双缓冲有效性**：通过重叠计算与通信，进一步提升了内存受限型算子的吞吐量。
*   **自动化可行性**：证明了基于 MLIR 的自动编译可以达到接近手工优化库的性能，同时具备更高的开发效率。

### 7. 优点
*   **开源生态**：为高通 NPU 提供了除商业工具链外的开源选择，有利于学术界和开发者定制优化。
*   **架构感知**：深度结合了 NPU 的硬件特性（TCM、DMA、HVX），而非通用的后端生成。
*   **统一前端**：同时支持 Triton 和 PyTorch，顺应了当前 AI 编译器发展的潮流。

### 8. 不足与局限
*   **处于早期阶段**：论文明确提到这是一个“正在进行中（Work-in-progress）”的项目，部分复杂算子的调度启发式算法仍需完善。
*   **硬件依赖**：该编译栈高度绑定高通 Hexagon 架构，无法直接迁移到其他厂商的 NPU。
*   **模型覆盖度**：目前主要验证了算子，对于包含复杂控制流或动态形状的完整模型，其编译稳定性和性能表现尚待观察。

（完）
