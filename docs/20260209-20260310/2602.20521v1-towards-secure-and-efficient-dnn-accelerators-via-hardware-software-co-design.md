---
title: Towards Secure and Efficient DNN Accelerators via Hardware-Software Co-Design
title_zh: 通过软硬件协同设计实现安全高效的 DNN 加速器
authors: "Wei Xuan, Zihao Xuan, Rongliang Fu, Ning Lin, Kwunhang Wong, Zikang Yuan, Lang Feng, Zhongrui Wang, Tsung-Yi Ho, Yuzhong Jiao, Luhong Liang"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20521v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对安全高效DNN加速器的软硬件协同设计
tldr: 针对自动驾驶和医疗等安全关键领域中深度神经网络（DNN）加速器的数据机密性与完整性挑战，本文提出了一种软硬件协同设计的安全内存保护框架。该框架通过带宽感知加密方案、针对分块策略的冗余消除以及多级认证机制，显著降低了硬件资源需求和离线内存访问开销。研究在确保安全性的同时，大幅提升了服务器和边缘端神经处理单元（NPU）的性能与能效。
selection_source: fresh_fetch
motivation: 现有的DNN加速器安全方案存在硬件资源消耗过大和频繁的离线内存访问开销，严重限制了系统的性能和可扩展性。
method: 提出了一种带宽感知加密方案和多级认证机制，通过优化加密粒度并消除由分块策略导致的冗余内存访问与重复加密计算。
result: "实验结果显示，该方案使性能开销降低了12%以上，并在服务器和边缘NPU上实现了87%的能效提升。"
conclusion: 通过软硬件协同设计，可以在保障DNN加速器安全性的同时，实现极高的运行效率和良好的系统扩展性。
---

## 摘要
深度神经网络（DNN）加速器在自动驾驶、医疗系统和金融基础设施等安全关键领域的快速部署，要求建立稳健的机制来保障数据机密性和计算完整性。然而，现有的 DNN 加速器安全解决方案面临硬件资源需求过高和频繁的片外内存访问开销等问题，这降低了性能和可扩展性。为了应对这些挑战，本文提出了一种开销极小的 DNN 加速器安全高效内存保护框架。首先，我们提出了一种带宽感知的加密方案，该方案根据内存流量模式调整加密粒度，在安全性和资源效率之间取得了平衡。其次，我们观察到层内分块（intra-layer tiling）滑动窗口模式中的重叠区域，以及由层间分块（inter-layer tiling）策略差异导致的重叠区域，都会在加密过程中引入大量的冗余内存访问和重复计算开销。第三，我们引入了一种多级认证机制，有效消除了不必要的片外内存访问，提升了性能和能效。实验结果表明，该工作在确保稳健可扩展性的同时，使服务器和边缘神经处理单元（NPU）的性能开销降低了 12% 以上，并实现了 87% 的能效提升。

## Abstract
The rapid deployment of deep neural network (DNN) accelerators in safety-critical domains such as autonomous vehicles, healthcare systems, and financial infrastructure necessitates robust mechanisms to safeguard data confidentiality and computational integrity. Existing security solutions for DNN accelerators, however, suffer from excessive hardware resource demands and frequent off-chip memory access overheads, which degrade performance and scalability.   To address these challenges, this paper presents a secure and efficient memory protection framework for DNN accelerators with minimal overhead. First, we propose a bandwidth-aware cryptographic scheme that adapts encryption granularity based on memory traffic patterns, striking a balance between security and resource efficiency. Second, we observe that both the overlapping regions in the intra-layer tiling's sliding window pattern and those resulting from inter-layer tiling strategy discrepancies introduce substantial redundant memory accesses and repeated computational overhead in cryptography. Third, we introduce a multi-level authentication mechanism that effectively eliminates unnecessary off-chip memory accesses, enhancing performance and energy efficiency. Experimental results show that this work decreases performance overhead by over 12% and achieves 87% energy efficiency improvement for both server and edge neural processing units (NPUs), while ensuring robust scalability.

---

## 论文详细总结（自动生成）

这篇论文《Towards Secure and Efficient DNN Accelerators via Hardware-Software Co-Design》提出了一种通过软硬件协同设计来增强深度神经网络（DNN）加速器安全性和效率的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在自动驾驶、医疗和金融等关键领域，DNN加速器面临**数据机密性**（防止模型/数据被窃取）和**计算完整性**（防止恶意篡改/重放攻击）的双重挑战。
*   **研究动机**：
    *   **硬件开销大**：现有的加密方案（如AES-CTR）为了匹配加速器的高带宽，通常需要堆叠多个加密引擎，导致芯片面积和功耗剧增。
    *   **内存访问瓶颈**：完整性验证需要频繁访问片外内存中的安全元数据（如MAC和版本号VN），这在数据密集型的DNN任务中造成了严重的性能下降和能效损失。
    *   **分块冗余**：现有的保护机制忽略了DNN计算中常见的“分块（Tiling）”重叠现象，导致了大量的重复加密和验证计算。

### 2. 方法论：核心思想与关键技术
论文提出了一个软硬件协同设计的安全框架，包含两个核心组件：

*   **硬件层面：带宽感知加密方案（Bandwidth-Aware Cryptographic Scheme）**
    *   **核心思想**：利用单个AES引擎的内部特性实现多倍带宽，而非简单复制引擎。
    *   **技术细节**：利用AES的“密钥扩展（KeyExpansion）”模块生成的多个轮密钥（Round Keys），通过简单的XOR逻辑与原始OTP（一次性密码本）结合，生成多个唯一的子块OTP。
    *   **优势**：仅需极小的逻辑门增加，即可支持高带宽加密，同时通过为每个子块提供唯一OTP，有效防御了“单元素碰撞攻击（SECA）”。

*   **软件层面：多级认证机制（Multi-level Authentication Mechanism）**
    *   **核心思想**：利用DNN计算的确定性，消除冗余的片外元数据访问。
    *   **关键技术**：
        1.  **GCD算法优化**：提出基于最大公约数（GCD）的计算框架，分析层内滑动窗口和层间分块模式的重叠区域，确定“最优认证块（opt_blk）”。
        2.  **分层MAC聚合**：将块级MAC通过XOR操作聚合为层级（Layer）或模型级（Model）MAC。
        3.  **防御重排列攻击（REPA）**：在MAC计算中绑定物理地址（PA）、版本号（VN）和层索引，打破XOR的交换律，防止攻击者通过调换数据块顺序来绕过验证。

### 3. 实验设计
*   **实验工具**：使用周期精确级模拟器 **SCALE-Sim2**（加速器模拟）、**Ramulator2**（DRAM模拟）和 **DRAMsim3**（能效分析）。
*   **硬件配置**：
    *   **服务器端 NPU**：模拟 Google TPU v1（256x256 脉动阵列）。
    *   **边缘端 NPU**：模拟 Samsung Exynos 990（32x32 脉动阵列）。
*   **工作负载（Benchmark）**：涵盖了13种主流DNN模型，包括 CV（ResNet, YOLO, MobileNet）、NLP（Transformer）、推荐系统（DLRM, NCF）和语音（DeepSpeech2）等。
*   **对比方法**：
    *   **Baseline**：无保护的加速器。
    *   **Intel SGX 风格**：基于完整性树（Merkle Tree）的传统保护方案。
    *   **MGX**：当时最先进的（SOTA）近零开销内存保护方案。

### 4. 资源与算力
*   **算力说明**：论文主要关注**硬件架构设计与推理阶段的模拟**，而非模型训练。
*   **硬件实现分析**：基于 **28nm 工艺库** 对加密引擎的面积和功耗进行了评估。
*   **未明确说明点**：文中未提及运行模拟器所使用的具体宿主机 GPU/CPU 型号，但由于是架构级模拟，这通常不影响结论的有效性。

### 5. 实验数量与充分性
*   **实验规模**：在2种不同的硬件平台（服务器/边缘）上针对13个模型进行了详尽测试。
*   **维度覆盖**：实验涵盖了内存流量（Memory Traffic）、执行性能（Performance）、能效（Energy Efficiency）以及硬件面积/功耗的可扩展性分析。
*   **充分性评价**：实验设计非常充分。通过跨领域的模型测试证明了方案的通用性；通过与 SGX 和 MGX 的对比，客观展示了该方案在减少片外访问方面的显著优势。

### 6. 主要结论与发现
*   **性能提升**：相比于 SOTA 方案（MGX），该框架将性能开销降低了约 **12.3%**，几乎达到了与无保护基准线持平的水平。
*   **能效飞跃**：在服务器和边缘 NPU 上实现了约 **87.6%** 的能效提升，这主要归功于消除了 99% 以上的片外安全元数据访问。
*   **硬件效率**：带宽感知加密方案在带宽需求增加时，面积和功耗保持近乎恒定，而传统方案呈线性增长。

### 7. 优点与亮点
*   **协同设计精妙**：巧妙地将 AES 内部的轮密钥逻辑转化为带宽扩展能力，解决了硬件资源与安全强度的矛盾。
*   **数学化解决重叠问题**：引入 GCD 算法来处理复杂的 Tiling 重叠，从数学层面优化了认证块的划分，具有很强的理论支撑。
*   **安全性与效率兼顾**：在大幅降低开销的同时，通过元数据绑定增强了对重排列攻击的防御能力。

### 8. 不足与局限
*   **侧信道攻击未覆盖**：论文明确指出不考虑功耗分析、电磁辐射等物理侧信道攻击，这在极高安全需求的场景下仍是潜在风险。
*   **推理场景局限**：研究重点在于推理加速器，对于训练阶段（涉及频繁的梯度更新和更复杂的内存访问模式）的适用性未做深入探讨。
*   **片上 SRAM 依赖**：多级认证机制依赖于在片上存储层级 MAC，对于 SRAM 资源极其匮乏的超低功耗芯片，可能需要权衡存储开销。

（完）
