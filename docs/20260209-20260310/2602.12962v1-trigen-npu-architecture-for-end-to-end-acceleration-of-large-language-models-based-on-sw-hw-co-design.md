---
title: "TriGen: NPU Architecture for End-to-End Acceleration of Large Language Models based on SW-HW Co-Design"
title_zh: TriGen：基于软硬件协同设计的端到端大语言模型加速 NPU 架构
authors: "Jonghun Lee, Junghoon Lee, Hyeonjin Kim, Seoho Jeon, Jisup Yoon, Hyunbin Park, Meejeong Park, Heonjae Ha"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.12962v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 基于软硬件协同设计的LLM加速NPU架构
tldr: 针对资源受限的端侧设备，本文提出TriGen NPU架构，通过软硬件协同设计实现大语言模型（LLM）的端到端加速。该架构采用微缩放（MX）低精度计算、基于查找表（LUT）的非线性操作优化以及高效的内存调度策略，在保持精度的同时显著提升了计算效率并降低了内存带宽需求，为端侧LLM推理提供了高性能、低成本的解决方案。
selection_source: fresh_fetch
motivation: 旨在解决端侧设备资源受限环境下，大语言模型因参数量巨大且复用率低而难以实现高效端到端推理的挑战。
method: 通过引入微缩放低精度计算、基于LUT的非线性操作优化以及针对有限片上内存的调度技术实现软硬件协同设计。
result: "实验表明，TriGen在多种LLM上实现了平均2.73倍的性能提升，并减少了52%的内存传输量，且精度损失几乎可以忽略。"
conclusion: TriGen证明了通过软硬件协同优化可以有效克服硬件约束，实现大语言模型在端侧设备上的高效加速。
---

## 摘要
近期的研究广泛探索了用于在资源受限的端侧环境中加速 AI 推理的 NPU 架构。与此同时，基于 Transformer 的大语言模型（LLMs）已占据主导地位，其模型规模迅速增长，但与传统的 CNN 相比，参数重用率较低，这使得在资源受限的设备上进行端到端执行变得极具挑战性。为了应对这些挑战，我们提出了 TriGen，这是一种通过软硬件协同设计为资源受限环境量身定制的新型 NPU 架构。首先，TriGen 采用基于微缩放（microscaling, MX）的低精度计算，在保持准确性的同时提供了额外的优化机会，并解决了采用此类精度所带来的问题。其次，为了联合优化非线性和线性操作，TriGen 通过使用快速且准确的查找表（LUT），消除了对基本非线性操作专用硬件的需求，从而在端侧环境中最大限度地提高性能收益并降低硬件成本。最后，通过考虑实际的硬件约束，TriGen 进一步采用了调度技术，即使在片上存储容量有限的情况下也能最大限度地提高计算利用率。我们在各种 LLM 上评估了 TriGen 的性能，结果表明，与基准 NPU 设计相比，TriGen 在精度损失可忽略不计的情况下，实现了平均 2.73 倍的性能加速和 52% 的内存传输减少。

## Abstract
Recent studies have extensively explored NPU architectures for accelerating AI inference in on-device environments, which are inherently resource-constrained. Meanwhile, transformer-based large language models (LLMs) have become dominant, with rapidly increasing model sizes but low degree of parameter reuse compared to conventional CNNs, making end-to-end execution on resource-limited devices extremely challenging. To address these challenges, we propose TriGen, a novel NPU architecture tailored for resource-constrained environments through software-hardware co-design. Firstly, TriGen adopts low-precision computation using microscaling (MX) to enable additional optimization opportunities while preserving accuracy, and resolves the issues that arise by employing such precision. Secondly, to jointly optimize both nonlinear and linear operations, TriGen eliminates the need for specialized hardware for essential nonlinear operations by using fast and accurate LUT, thereby maximizing performance gains and reducing hardware-cost in on-device environments, and finally, by taking practical hardware constraints into account, further employs scheduling techniques to maximize computational utilization even under limited on-chip memory capacity. We evaluate the performance of TriGen on various LLMs and show that TriGen achieves an average 2.73x performance speedup and 52% less memory transfer over the baseline NPU design with negligible accuracy loss.

---

## 论文详细总结（自动生成）

这篇论文提出了 **TriGen**，一种专为资源受限的端侧（On-device）环境设计的 NPU 架构，旨在通过软硬件协同设计实现大语言模型（LLM）的端到端加速。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：LLM（如 Llama 系列）在端侧设备部署面临巨大挑战。相比传统 CNN，LLM 参数重用率低且规模巨大，导致推理过程受限于内存带宽（Memory-bound）。
*   **核心痛点**：
    1.  **精度与带宽的矛盾**：为了保准精度，通常激活值需保持 FP16，这在 SRAM 极小的端侧设备上导致巨大的内存流量。
    2.  **非线性操作瓶颈**：Softmax、SiLU 和 LayerNorm 等非线性操作计算复杂，通常需跳转到专用硬件或 DSP 处理，随着序列长度增加，其耗时占比显著提升（可达 20% 以上）。
    3.  **资源调度难题**：在有限的片上内存（如 1MiB）下，不合理的平铺（Tiling）和驻留（Stationary）策略会引发严重的流水线停顿。

### 2. 方法论：核心思想与关键技术
TriGen 通过**软硬件协同设计**从三个维度解决上述问题：

*   **微缩放（MX）数据格式与 FI32 中间态**：
    *   首次在端侧 NPU 引入 **MXINT8** 格式，通过共享指数（Shared Exponents）在保持 8 位宽度的同时提供接近 16 位的精度。
    *   设计了 **FI32（Floating Integer 32-bit）** 作为中间数据格式，包含 8 位指数和 24 位整数尾数，简化了硬件电路（无需复杂的浮点对齐），同时保证了累加过程的精度。
*   **基于 LUT 的非线性加速器（PPA 模块）**：
    *   在后处理阵列（PPA）中集成高精度查找表（LUT），通过线性插值和误差补偿表（Value Table + Error Table）实现对指数、倒数平方根、SiLU 等函数的快速逼近。
    *   **算子融合**：将非线性函数与矩阵乘法（TMATMUL）流水线化，避免了数据写回 DRAM 的开销。
*   **资源感知的数据流映射与调度**：
    *   **算子优化**：通过数学等价变换，将注意力机制中的转置操作（Transpose）融合进矩阵乘法；采用合并掩码（Coalesced Masking）减少元素级乘法。
    *   **自动化调度算法**：编译器根据 SRAM 容量和 DRAM 带宽，自动搜索最优的平铺尺寸（Tile Size）和驻留策略（输入驻留或权重驻留），以最大化计算强度。

### 3. 实验设计
*   **模型负载**：Llama-2-7B, Llama-3-8B, Llama-3.2-3B, OPT-1.3B, OPT-2.7B。
*   **数据集**：WikiText-2, C4, PTB（用于评估困惑度 PPL）。
*   **基准测试（Benchmark）**：
    *   **Baseline**：采用 INT16 激活值/UINT4 权重的标准 NPU 架构，使用专用功能单元（SFU）处理非线性操作。
    *   **对比维度**：端到端延迟、DRAM 流量、模型精度（PPL）、多 NPU 扩展性。

### 4. 资源与算力
*   **硬件模拟**：使用 C++ 开发了周期精确（Cycle-level）的模拟器。
*   **硬件规格**：14nm 工艺验证，主频 1GHz，单 NPU 包含 4 个 DLA 核心，每个核心拥有 32x32 MAC 阵列，片上 SRAM 为 1MiB，DRAM 带宽 32GB/s。
*   **训练/运行情况**：论文未提及模型训练时长（主要关注推理加速），实验重点在于不同配置下的推理延迟模拟。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从 1.3B 到 8B 参数量的多种主流 LLM，测试了从 512 到 4096 的不同序列长度。
*   **消融实验**：详细对比了 MXINT8、LUT 优化、QKV 融合、转置融合、掩码优化等各个组件对性能的贡献。
*   **公平性**：对比实验在相同的硬件约束（SRAM 大小、带宽）下进行，并提供了 PPL 精度数据以证明低精度方案的有效性。实验设计较为客观、充分。

### 6. 主要结论
*   **性能提升**：相比基准 NPU，TriGen 实现了平均 **2.73 倍** 的端到端加速。
*   **带宽节省**：通过 MX 格式和调度优化，DRAM 内存传输量减少了 **52%**。
*   **精度保持**：使用 MXINT8 激活值时，PPL 损失微乎其微，远优于传统的 UINT8 量化。
*   **非线性优化**：随着序列长度增加，LUT 带来的收益愈发明显（4K 序列下性能提升贡献从 18% 升至 28%）。

### 7. 优点与亮点
*   **端到端完整性**：不仅关注线性层（GEMM），还深入解决了非线性层和数据转置等实际部署中的“长尾”瓶颈。
*   **MX 格式的首次端侧应用**：证明了微缩放格式在极低资源环境下替代 FP16 的巨大潜力。
*   **软硬协同深度高**：通过数学公式重写（如转置融合）来简化硬件设计，体现了优秀的协同设计思想。

### 8. 不足与局限
*   **模拟器依赖**：所有数据均来自周期精确模拟器和 RTL 验证，缺乏真实的流片芯片实测数据。
*   **模型结构局限**：主要针对 Transformer 架构，对于未来可能出现的非 Transformer 架构（如线性注意力或状态空间模型）的适用性未做讨论。
*   **多任务干扰**：实验假设 NPU 独占 DRAM 带宽，未充分考虑端侧 SoC 中 CPU、GPU 与 NPU 竞争带宽时的性能波动。

（完）
