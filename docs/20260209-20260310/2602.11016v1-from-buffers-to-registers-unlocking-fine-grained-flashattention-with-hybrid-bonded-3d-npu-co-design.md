---
title: "From Buffers to Registers: Unlocking Fine-Grained FlashAttention with Hybrid-Bonded 3D NPU Co-Design"
title_zh: 从缓存到寄存器：通过混合键合 3D NPU 协同设计解锁细粒度 FlashAttention
authors: "Jinxin Yu, Yudong Pan, Mengdi Wang, Huawei Li, Yinhe Han, Xiaowei Li, Ying Wang"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.11016v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 用于注意力机制加速的混合键合 3D NPU 协同设计
tldr: 针对Transformer模型在处理长序列时SRAM访问成为能效瓶颈的问题，本文提出了一种名为3D-Flow的混合键合3D堆叠空间加速器。该架构利用亚10微米垂直TSV实现PE层间的寄存器级通信，并配合3D-FlashAttention细粒度调度方法，消除了片上SRAM往返开销。实验表明，该设计在OPT和QWEN模型上显著降低了能耗并大幅提升了处理速度。
selection_source: fresh_fetch
motivation: "随着片外内存瓶颈的缓解，片上SRAM访问已成为长序列Transformer工作负载中超过60%能耗的瓶颈。"
method: 提出3D-Flow架构，利用垂直TSV实现跨层寄存器通信，并设计3D-FlashAttention实现无气泡的垂直流水线数据流。
result: "与最先进的2D和3D设计相比，该方案在Transformer工作负载上实现了46-93%的能耗降低和1.4x-7.6x的加速。"
conclusion: 通过软硬件协同设计将通信从缓冲区转向寄存器，是解决大模型长序列推理能效瓶颈的有效途径。
---

## 摘要
基于 Transformer 的模型在现代 AI 工作负载中占据主导地位，但由于其二次方复杂度的注意力机制和不断增长的模型规模，加剧了内存瓶颈。现有的加速器（如 Groq 和 Cerebras）通过大型片上缓存来缓解片外流量，而 FlashAttention 等算法创新则通过算子融合来避免实例化大型注意力矩阵。然而，随着片外流量的减少，我们的测量结果显示，在长序列工作负载中，片上 SRAM 访问占能耗的 60% 以上，使得缓存访问成为了新的瓶颈。我们提出了 3D-Flow，这是一种混合键合、3D 堆叠的空间加速器，能够在垂直分层的 PE（处理单元）层级之间实现寄存器到寄存器的通信。与受限于基于 NoC 的路由器到路由器传输的 2D 多阵列架构不同，3D-Flow 利用亚 10 微米的垂直 TSV（硅通孔），以极低的开销维持周期级的算子流水线。在此架构之上，我们设计了 3D-FlashAttention，这是一种细粒度的调度方法，可平衡各层之间的延迟，形成无气泡的垂直数据流，且无需片上 SRAM 的往返访问。在 Transformer 工作负载（OPT 和 QWEN 模型）上的评估表明，与最先进的 2D 和 3D 设计相比，我们的 3D 空间加速器降低了 46-93% 的能耗，并实现了 1.4 倍至 7.6 倍的加速。

## Abstract
Transformer-based models dominate modern AI workloads but exacerbate memory bottlenecks due to their quadratic attention complexity and ever-growing model sizes. Existing accelerators, such as Groq and Cerebras, mitigate off-chip traffic with large on-chip caches, while algorithmic innovations such as FlashAttention fuse operators to avoid materializing large attention matrices. However, as off-chip traffic decreases, our measurements show that on-chip SRAM accesses account for over 60% of energy in long-sequence workloads, making cache access the new bottleneck. We propose 3D-Flow, a hybrid-bonded, 3D-stacked spatial accelerator that enables register-to-register communication across vertically partitioned PE tiers. Unlike 2D multi-array architectures limited by NoC-based router-to-router transfers, 3D-Flow leverages sub-10 um vertical TSVs to sustain cycle-level operator pipelining with minimal overhead. On top of this architecture, we design 3D-FlashAttention, a fine-grained scheduling method that balances latency across tiers, forming a bubble-free vertical dataflow without on-chip SRAM roundtrips. Evaluations on Transformer workloads (OPT and QWEN models) show that our 3D spatial accelerator reduces 46-93% energy consumption and achieves 1.4x-7.6x speedups compared to state-of-the-art 2D and 3D designs.

---

## 论文详细总结（自动生成）

这篇论文由中国科学院计算技术研究所（ICT, CAS）的研究团队发表，提出了一种通过软硬件协同设计解决大语言模型（LLM）推理中内存瓶颈的新方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在长序列 Transformer 推理中，虽然 FlashAttention 等算法通过算子融合减少了片外 DRAM 流量，但**片上 SRAM 访问**成为了新的能效瓶颈（在长序列负载中占总能耗的 60% 以上）。
*   **研究动机**：传统的 2D 加速器架构在执行融合算子时，中间结果仍需通过 SRAM 缓冲区进行交换，导致高延迟和高能耗。此外，不同算子（如矩阵乘法与 Softmax）之间的负载不平衡导致硬件利用率低下。

### 2. 方法论：3D-Flow 与 3D-FlashAttention
论文提出了硬件架构与调度算法的协同设计：
*   **3D-Flow 硬件架构**：
    *   **核心思想**：利用**混合键合（Hybrid-Bonding）**技术实现 3D 堆叠的脉动阵列（Systolic Array）。
    *   **关键技术**：采用亚 10 微米（sub-10 μm）的超高密度垂直 TSV（硅通孔），将通信从“缓冲区（Buffer）”提升到“寄存器（Register）”级别。
    *   **层级划分**：将 PE（处理单元）垂直分为四层，每层负责不同的注意力机制子算子，实现周期级的算子流水线。
*   **3D-FlashAttention 调度算法**：
    *   **细粒度流水线**：将 FlashAttention 算子映射到 3D 层的不同 PE 上。
    *   **负载平衡**：通过精心设计的计算流（Layer 0: $Q \times K^T$；Layer 1: RowMax/减法；Layer 2: Exp/RowSum；Layer 3: $P \times V$ 与缩放），确保各层吞吐量匹配，形成无气泡（bubble-free）的垂直数据流。
    *   **消除 SRAM 往返**：中间结果直接通过垂直链路在寄存器间传递，完全绕过片上 SRAM。

### 3. 实验设计
*   **数据集/场景**：针对 Transformer 模型推理，特别是长序列处理（1K 到 64K 序列长度）。
*   **测试模型（Benchmarks）**：OPT-13B（代表多头注意力 MHA）和 Qwen-13B（代表分组查询注意力 GQA）。
*   **对比方法（Baselines）**：
    1.  **2D-Unfused**：传统非融合脉动阵列。
    2.  **2D-Fused**：如 FuseMax、FLAT、TileFlow 等先进的 2D 融合设计。
    3.  **Dual-SA**：受 COSA 启发的 2D 双阵列架构。
    4.  **3D-Base**：简单的 3D 堆叠架构，但仍使用 SRAM 交换数据。

### 4. 资源与算力
*   **硬件实现**：使用 **TSMC 16nm** 工艺进行了 RTL 综合，验证了功能正确性并校准了能效模型。
*   **模拟器**：使用周期精确的内部模拟器，集成 Ramulator（建模 DRAM）和 Accelergy（估算能耗）。
*   **算力说明**：论文未提及大规模 GPU 训练时长，因为其核心贡献在于 **NPU 架构设计与推理加速**。实验主要基于电路综合结果和架构级仿真。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1K 到 64K 的多种序列长度，对比了 7 种不同的架构方案。
*   **维度覆盖**：评估了能耗（Energy）、加速比（Speedup）、内存数据移动量（Data Movement）以及 PE 利用率。
*   **充分性评价**：实验设计较为充分，不仅对比了性能指标，还进行了**热可行性分析**（估算 3D 堆叠的结温），证明了物理实现的可能性。对比基准涵盖了当前最先进的 2D 融合加速器方案，具有较强的说服力。

### 6. 主要结论与发现
*   **能效提升**：相比 2D-Unfused 方案，能耗降低了 **80.5% - 93%**；相比最先进的 2D 融合方案，节能 **54.2% - 66.7%**。
*   **性能加速**：相比 2D 基准实现了 **1.4x - 7.6x** 的加速。
*   **SRAM 优化**：通过寄存器级通信，片上 SRAM 流量平均减少了 **76.6%**。
*   **硬件效率**：通过垂直流水线设计，PE 平均利用率达到了 **87%**。

### 7. 优点（亮点）
*   **物理与架构协同**：不仅停留在算法层面，还深入到了混合键合 3D-IC 的物理特性（如亚 10 微米 TSV 的单周期延迟）。
*   **解决新瓶颈**：敏锐捕捉到 FlashAttention 在 2D 硬件上从 DRAM 瓶颈转向 SRAM 瓶颈的趋势。
*   **通用性**：提出的 3D-Flow 架构不仅适用于注意力机制，也可扩展到其他需要深度融合的算子。

### 8. 不足与局限
*   **成本与良率**：论文未详细讨论混合键合 3D 芯片的制造难度、成本增加以及良率损失，这在商业化中是关键因素。
*   **模型覆盖**：虽然测试了 OPT 和 Qwen，但未讨论在非 Transformer 架构（如线性注意力或状态空间模型 SSM）上的适用性。
*   **热管理简化**：虽然做了初步热模拟，但在极端高负载下的长期热稳定性仍需更复杂的物理验证。

（完）
