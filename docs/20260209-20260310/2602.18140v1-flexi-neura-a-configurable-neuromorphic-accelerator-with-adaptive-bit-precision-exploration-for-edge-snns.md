---
title: "Flexi-NeurA: A Configurable Neuromorphic Accelerator with Adaptive Bit-Precision Exploration for Edge SNNs"
title_zh: Flexi-NeurA：一种面向边缘 SNN、支持自适应位精度探索的可配置类脑加速器
authors: "Mohammad Farahani, Mohammad Rasoul Roshanshah, Saeed Safari"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.18140v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 具有设计时探索功能的边缘SNN可配置神经形态加速器
tldr: 针对边缘智能中脉冲神经网络（SNN）加速器架构僵化、配置受限的问题，本文提出了Flexi-NeurA。这是一个高度可配置的神经形态加速器，支持在设计时自定义神经元模型、网络结构和精度。配合启发式设计空间探索工具Flex-plorer，该框架能自动优化关键参数的位宽精度，在显著降低硬件资源和功耗的同时，保持高精度和低延迟，为边缘SNN应用提供了高效、可扩展的硬件方案。
selection_source: fresh_fetch
motivation: 现有神经形态加速器架构僵化且配置受限，难以适应异构工作负载和多样化的边缘计算设计目标。
method: 提出可参数化的Flexi-NeurA核心，结合时间复用与事件驱动处理，并利用Flex-plorer工具进行启发式位宽精度探索以优化资源分配。
result: "在MNIST任务上实现97.23%的准确率，推理延迟仅1.1ms，功耗111mW，且仅占用极少的逻辑单元和内存资源。"
conclusion: Flexi-NeurA是一个高效、可扩展且适用于边缘计算的神经形态平台，通过软硬件协同设计成功平衡了计算精度与硬件成本。
---

## 摘要
类脑加速器为脉冲神经网络（SNN）提供了卓越的能效和计算密度，尤其是在边缘智能应用中。然而，大多数现有平台架构僵化且可配置性有限，限制了其对异构工作负载和多样化设计目标的适应性。为了解决这些局限性，我们提出了 Flexi-NeurA —— 一个集可配置性、灵活性和效率于一体的参数化类脑加速器（内核）。Flexi-NeurA 允许用户在设计阶段自定义神经元模型、网络结构和精度设置。通过将这些设计时的可配置性和灵活性特征与分时复用及事件驱动的处理方法相结合，Flexi-NeurA 在保持高效率和低推理延迟的同时，大幅减少了硬件资源需求和总功耗。作为补充，我们引入了 Flex-plorer，这是一种启发式引导的设计空间探索（DSE）工具，可根据用户定义的准确率与资源占用之间的权衡，为关键参数（如衰减因子、突触权重和膜电位）确定具有成本效益的定点精度。根据 Flex-plorer 流程选定的配置，RTL 代码将被配置以匹配指定设计。在 MNIST、SHD 和 DVS 基准测试上的全面评估表明，Flexi-NeurA 与 Flex-plorer 协同框架在准确率、延迟和能效方面取得了显著提升。一个映射到两个处理内核、采用 LIF 神经元的三层 256-128-10 全连接网络在 MNIST 上实现了 97.23% 的准确率，推理延迟为 1.1 ms，仅消耗 1,623 个逻辑单元、7 个 BRAM 和 111 mW 的总功耗，这证明了 Flexi-NeurA 是一个可扩展且边缘就绪的类脑平台。

## Abstract
Neuromorphic accelerators promise unparalleled energy efficiency and computational density for spiking neural networks (SNNs), especially in edge intelligence applications. However, most existing platforms exhibit rigid architectures with limited configurability, restricting their adaptability to heterogeneous workloads and diverse design objectives. To address these limitations, we present Flexi-NeurA -- a parameterizable neuromorphic accelerator (core) that unifies configurability, flexibility, and efficiency. Flexi-NeurA allows users to customize neuron models, network structures, and precision settings at design time. By pairing these design-time configurability and flexibility features with a time-multiplexed and event-driven processing approach, Flexi-NeurA substantially reduces the required hardware resources and total power while preserving high efficiency and low inference latency. Complementing this, we introduce Flex-plorer, a heuristic-guided design-space exploration (DSE) tool that determines cost-effective fixed-point precisions for critical parameters -- such as decay factors, synaptic weights, and membrane potentials -- based on user-defined trade-offs between accuracy and resource usage. Based on the configuration selected through the Flex-plorer process, RTL code is configured to match the specified design. Comprehensive evaluations across MNIST, SHD, and DVS benchmarks demonstrate that the Flexi-NeurA and Flex-plorer co-framework achieves substantial improvements in accuracy, latency, and energy efficiency. A three-layer 256--128--10 fully connected network with LIF neurons mapped onto two processing cores achieves 97.23% accuracy on MNIST with 1.1~ms inference latency, utilizing only 1,623 logic cells, 7 BRAMs, and 111~mW of total power -- establishing Flexi-NeurA as a scalable, edge-ready neuromorphic platform.

---

## 论文详细总结（自动生成）

这是一份关于论文《Flexi-NeurA: A Configurable Neuromorphic Accelerator with Adaptive Bit-Precision Exploration for Edge SNNs》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：边缘人工智能（Edge AI）要求极低的功耗和延迟。脉冲神经网络（SNN）因其事件驱动和稀疏计算的特性，成为边缘设备的理想选择。
*   **核心问题**：现有的类脑（Neuromorphic）硬件加速器通常架构僵化，缺乏可配置性，难以适应不同的神经元模型、网络拓扑和精度需求。
*   **整体含义**：本文旨在开发一个软硬件协同框架，通过高度可配置的硬件核心（Flexi-NeurA）和自动化的设计空间探索工具（Flex-plorer），在保证精度的前提下，最小化边缘设备的硬件资源消耗和功耗。

### 2. 方法论：核心思想与关键技术
*   **Flexi-NeurA 硬件架构**：
    *   **分时复用与事件驱动**：采用分时复用（Time-multiplexed）计算模式，通过顺序重用神经元计算单元来减少面积；仅在脉冲发生时触发计算（Event-driven），降低功耗。
    *   **高度可配置性**：支持在设计阶段自定义神经元模型（IF、LIF、Synaptic）、网络拓扑（全连接、两种循环结构 ATA-F/ATA-T）以及数值精度（权重、膜电位、电流的位宽）。
    *   **无乘法器设计**：引入系数生成器（CG），利用“移位-累加”网络模拟神经元的泄漏（Leak）过程，避免了高成本的硬件乘法器。
*   **Flex-plorer 探索工具**：
    *   **核心算法**：使用**模拟退火（Simulated Annealing）**算法在给定的精度范围内搜索最优配置。
    *   **成本模型**：建立了一个综合考虑任务准确率（Accuracy）和硬件成本（LUT、FF、BRAM 使用量）的加权成本函数。
    *   **自动化流程**：从用户需求出发，经过训练、精度探索，最终自动生成参数化的 RTL 代码、驱动程序和量化后的数据集。

### 3. 实验设计
*   **数据集**：
    *   **MNIST**：静态手写数字识别（速率编码）。
    *   **SHD (Spiking Heidelberg Digits)**：语音脉冲数据集。
    *   **DVS Gesture**：基于事件相机的动态手势识别。
*   **Benchmark 与对比方法**：
    *   对比了近年来多个基于 FPGA 的 SNN 加速器（如 Spiker+、FireFly、S2N2 等）。
    *   评估指标包括：分类准确率、推理延迟（Latency）、逻辑资源占用（Logic Cells/LUTs）、功耗（Power）和能效（Energy per Image/Synapse）。

### 4. 资源与算力
*   **硬件平台**：实验主要在 **Xilinx Zynq-7000 (XC7Z020)** FPGA 上进行验证。
*   **算力与训练说明**：
    *   **训练阶段**：使用 Python 环境下的 `snn-torch` 库进行离线训练（基于反向传播和代理梯度）。
    *   **算力细节**：文中**未明确说明**训练所使用的具体 GPU 型号、数量及训练时长。
    *   **推理性能**：在 60 MHz 时钟频率下，MNIST 推理延迟为 1.1 ms，总功耗仅为 111 mW。

### 5. 实验数量与充分性
*   **实验规模**：
    *   表 1 列出了 20 组针对不同数据集、神经元模型和拓扑结构的训练结果。
    *   针对 DVS 数据集展示了详细的设计空间探索（DSE）过程（图 11）。
*   **充分性评价**：实验覆盖了从静态到动态、从视觉到听觉的多种 SNN 应用场景，对比了多种主流 FPGA 加速器。实验设计较为客观，通过消融不同位宽精度展示了硬件成本与准确率的权衡，证明了框架的灵活性。

### 6. 主要结论与发现
*   **高性能与低成本**：在 MNIST 上，256-128-10 的网络实现了 97.23% 的准确率，而逻辑单元占用仅为 1,623 个，远低于同类研究。
*   **精度权衡**：通过 Flex-plorer 发现，保持前馈权重精度等于或高于循环权重精度，通常能在不显著增加硬件成本的情况下维持较高的准确率。
*   **可扩展性**：由于采用分时复用架构，增加层宽度主要影响内存（BRAM）占用，而逻辑资源（LUT/FF）保持相对稳定，适合扩展。

### 7. 优点
*   **端到端自动化**：实现了从算法训练到硬件 RTL 生成的全流程自动化，降低了 SNN 部署门槛。
*   **极致的资源效率**：通过无乘法器设计和分时复用，极大地压缩了 FPGA 资源占用，非常适合资源受限的边缘设备。
*   **多模型支持**：不仅支持前馈网络，还支持复杂的循环脉冲神经网络（RSNN），适用范围广。

### 8. 不足与局限
*   **并行度限制**：分时复用虽然节省了面积，但在处理超大规模神经元层时，顺序计算会导致推理延迟线性增加，可能不适用于超高性能要求的场景。
*   **网络规模限制**：目前每个核心支持的神经元上限为 256 个，虽然可以通过多核扩展，但对于极深、极宽的现代深度 SNN 架构，其互联开销和映射效率尚待进一步验证。
*   **训练与部署脱节**：虽然推理是事件驱动的，但训练仍依赖传统的反向传播，未涉及片上学习（On-chip learning）。

（完）
