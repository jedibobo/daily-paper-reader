---
title: "The Turbo-Charged Mapper: Fast and Optimal Mapping for Accelerator Modeling and Evaluation"
title_zh: Turbo-Charged Mapper：用于加速器建模与评估的快速且最优映射
authors: "Michael Gilbert, Tanner Andrulis, Vivienne Sze, Joel S. Emer"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.15172v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 加速器建模与评估中的最优映射
tldr: 针对深度神经网络加速器设计中映射空间巨大且现有启发式方法难以找到最优解的问题，本文提出了Turbo-Charged Mapper (TCM)。TCM引入了“数据放置”新概念，通过剪枝技术将搜索空间缩小了32个数量级。它是首个能在可行时间内保证找到最优映射的工具，相比传统方法在速度和能效优化上均有显著提升，为硬件评估提供了可靠基准。
selection_source: fresh_fetch
motivation: 现有的加速器映射工具依赖启发式算法，无法保证最优性且搜索效率低，导致难以准确评估硬件设计的真实性能。
method: 提出了“数据放置”概念并利用其识别并剪除冗余及次优映射，从而将搜索空间缩减高达32个数量级。
result: "TCM能在不到一分钟内找到最优映射，而传统方法即使运行超过10小时，其能量延迟积仍比最优值高出21%。"
conclusion: TCM实现了快速且保证最优的映射搜索，解决了加速器建模与评估中的关键瓶颈。
---

## 摘要
运行深度神经网络（DNN）的加速器的能耗和延迟取决于计算和数据移动在加速器中的调度方式（即映射）。优化映射对于评估和设计加速器至关重要。然而，映射空间巨大，且由于先前的工作使用启发式或元启发式方法来缩小空间，因此无法保证找到最优映射。这些局限性阻碍了正确的硬件评估，因为设计者无法判断性能差异是由硬件变化还是次优映射引起的。为了应对这一挑战，我们提出了 Turbo-Charged Mapper (TCM)，这是一种保证能找到最优映射的快速映射器。我们方法的关键在于定义了一个映射中的新概念，称为数据放置（dataplacement），它与之前的数据流（dataflow）概念类似，能够对映射进行清晰的分析和比较。通过该概念，我们发现了多个剪枝冗余和次优映射的机会，将搜索空间减少了多达 32 个数量级。利用这些见解，TCM 可以执行完整的映射空间搜索，使其成为第一个能在可行运行时间内找到最优映射的映射器。与之前的映射器相比，我们展示了 TCM 可以快速（不到一分钟）找到最优映射，而先前的工作即使在给予 1000 倍运行时间（超过 10 小时）的情况下，也无法找到最优映射（其能量延迟乘积比最优值高出 21%）。

## Abstract
The energy and latency of an accelerator running a deep neural network (DNN) depend on how the computation and data movement are scheduled in the accelerator (i.e., mapping). Optimizing mappings is essential to evaluating and designing accelerators. However, the space of mappings is large, and prior works can not guarantee finding optimal mappings because they use heuristics or metaheuristics to narrow down the space. These limitations preclude proper hardware evaluation, since designers can not tell whether performance differences are due to changes in hardware or suboptimal mapping.   To address this challenge, we propose the Turbo-Charged Mapper (TCM), a fast mapper that is guaranteed to find optimal mappings. The key to our approach is that we define a new concept in mapping, called dataplacement, which, like the prior concept of dataflow, allows for clear analysis and comparison of mappings. Through it, we identify multiple opportunities to prune redundant and suboptimal mappings, reducing search space by up to 32 orders of magnitude.   Leveraging these insights, TCM can perform full mapspace searches, making it the first mapper that can find optimal mappings in feasible runtime. Compared to prior mappers, we show that TCM can find optimal mappings quickly (less than a minute), while prior works can not find optimal mappings (energy-delay-product $21\%$ higher than optimal) even when given $1000\times$ the runtime ($>10$ hours).

---

## 论文详细总结（自动生成）

这是一份关于论文《The Turbo-Charged Mapper: Fast and Optimal Mapping for Accelerator Modeling and Evaluation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在深度神经网络（DNN）加速器设计中，**映射（Mapping）**决定了计算和数据移动在硬件上的调度方式，直接影响能效和延迟。
*   **研究动机**：映射空间（Mapspace）极其庞大（可达 $10^{37}$ 种组合）。现有的映射工具（如 Timeloop, ZigZag）主要依赖启发式或元启发式算法（如随机采样、遗传算法）。
*   **核心问题**：现有工具无法保证找到“最优映射”。这导致硬件设计者无法判断性能瓶颈究竟源于硬件架构本身，还是因为映射方案不够优化，从而误导硬件评估。

### 2. 论文提出的方法论：Turbo-Charged Mapper (TCM)
TCM 的核心目标是在可行的时间内通过全空间搜索保证找到最优映射。
*   **核心思想：数据放置（Dataplacement）**：
    论文引入了“数据放置”这一新概念，用于定义哪些张量分块（Tiles）在何时存储在加速器的哪一层存储器中。它与“数据流（Dataflow）”和“分块形状（Tile Shapes）”共同构成了完整的映射。
*   **关键技术细节（三大剪枝策略）**：
    1.  **冗余数据流剪枝**：识别并剔除在相同存储节点之间仅循环顺序不同但结果一致的冗余数据流。
    2.  **非辅助循环剪枝**：基于数据放置分析，移除那些不增加数据重用却增加存储开销或数据获取次数的循环。
    3.  **部分分块形状剪枝**：利用数学表达式（和、积、最大值等算子）对尚未完全确定的分块参数进行评估，提前剔除那些在任何后续选择下都必然次优的候选方案。
*   **算法流程与加速**：
    *   **模型柯里化（Currying）**：将复杂的硬件模型拆分为两步。首先根据数据放置和数据流生成一个简化的“仅分块形状相关”的数学表达式（符号分析），然后在大规模搜索分块形状时仅进行简单的数值代入。这种方法比传统模型快 1000 倍以上。

### 3. 实验设计
*   **工作负载（Workloads）**：
    *   **GPT-3 6.7B**：涵盖 Query/Key/Value 投影、注意力机制计算（QK, AV）、前馈网络（FFA, FFB）。
    *   **MobileNetV3**：涵盖逐点卷积（Pointwise）和深度卷积（Depthwise）。
*   **硬件场景**：
    *   **类 TPU-V4i**：代表高性能数据中心加速器。
    *   **类 NVDLA**：代表低功耗边缘计算加速器。
*   **Benchmark 与对比方法**：
    *   **Timeloop**：基于随机采样的工业级映射器。
    *   **Timeloop+Hint**：加入人工启发式约束（如强制满载利用 MAC 阵列）的 Timeloop。
    *   **LOMA (ZigZag)**：基于循环顺序的启发式映射器。

### 4. 资源与算力
*   **算力消耗**：论文未明确提及具体的 CPU 或 GPU 硬件型号。
*   **运行时长**：论文强调了 TCM 的高效性。在处理 GPT-3 的复杂算子时，TCM 在 **37 秒**内即可完成全空间搜索并找到最优解。
*   **对比情况**：为了公平性，对比实验均在单线程环境下运行。

### 5. 实验数量与充分性
*   **实验规模**：针对 GPT-3 和 MobileNetV3 的多个关键算子进行了详尽测试。
*   **消融实验**：图 6 专门展示了三种剪枝策略（循环剪枝、数据流剪枝、部分形状剪枝）各自对搜索空间缩减的贡献，证明了每种策略的必要性。
*   **客观性**：通过改变矩阵规模（从 $2^7$ 到 $2^{15}$）和秩（Rank）的数量来测试算法的扩展性（Scaling），证明了 TCM 在面对更大空间时剪枝效率更高。实验设计覆盖了从边缘到云端的典型架构，具有较强的说服力。

### 6. 主要结论与发现
*   **搜索空间缩减**：TCM 成功将搜索空间减少了 **19 到 32 个数量级**，使全空间搜索变得可行。
*   **最优性保证**：TCM 是首个能在分钟级时间内保证找到最优映射的工具。
*   **性能优势**：在相同运行时间内，TCM 找到的映射在能量延迟积（EDP）上比 Timeloop 等工具好 **1.3 到 2400 倍**。即使给竞争对手 1000 倍的时间（>10 小时），它们的 EDP 仍比 TCM 找到的最优值高出 21%。

### 7. 优点
*   **理论严谨**：通过定义“数据放置”填补了映射理论的空白，将启发式搜索转变为确定性的剪枝搜索。
*   **极速建模**：柯里化模型的引入极大地降低了单次映射评估的计算开销。
*   **实用性强**：已集成到开源框架 AccelForge 中，对硬件设计者具有直接的参考价值。

### 8. 不足与局限
*   **模型依赖性**：TCM 依赖于分析型硬件模型（Analytical Models）。如果硬件存在极其复杂的动态非线性行为（如复杂的总线争用或非确定性缓存策略），其数学表达式的准确性可能会受限。
*   **算子覆盖**：目前主要针对基于 Einsum 表达的规则算子，对于某些极其特殊或非规则的稀疏计算模式，文中未做深入探讨。
*   **硬件约束假设**：虽然支持用户自定义约束，但其核心剪枝逻辑假设存储层次结构是相对固定的。

（完）
