---
title: A 16 nm 1.60TOPS/W High Utilization DNN Accelerator with 3D Spatial Data Reuse and Efficient Shared Memory Access
title_zh: 一种采用 16 nm 工艺、能效达 1.60TOPS/W 且具备 3D 空间数据复用与高效共享内存访问的高利用率 DNN 加速器
authors: "Xiaoling Yi, Ryan Antonio, Yunhao Deng, Fanchen Kong, Joren Dumoulin, Jun Yin, Marian Verhelst"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.11357v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 具有3D空间数据复用的高利用率DNN加速器
tldr: 本研究针对通用DNN加速器在多样化AI负载下计算利用率低的问题，提出了Voltra芯片架构。该架构通过3D空间数据复用技术平衡了三个维度的空间数据重用，并结合灵活的共享内存访问机制与硬件预取功能，显著提升了空间和时间利用率。在16nm工艺下，该芯片实现了1.60 TOPS/W的高能效，在保持高性能的同时兼顾了不同负载下的高利用率。
selection_source: fresh_fetch
motivation: 旨在解决通用DNN加速器在处理多样化AI工作负载时计算利用率不足的问题。
method: 提出了Voltra架构，采用3D空间数据流实现平衡的数据复用，并利用灵活的数据流调度器进行混合粒度硬件预取和动态内存分配。
result: 相比传统2D设计，空间利用率提升达2.0倍，时间利用率提升2.12-2.94倍，系统能效达到1.60 TOPS/W。
conclusion: Voltra芯片通过创新的3D数据流和高效内存管理，在多种工作负载下实现了高利用率和极具竞争力的能效表现。
---

## 摘要
在广泛的 AI 工作负载中实现高计算利用率对于通用 DNN 加速器的效率至关重要。本文介绍了 Voltra 芯片及其利用率优化的 DNN 加速器架构，该架构利用了三维（3D）空间数据复用以及高效灵活的共享内存访问。3D 空间数据流实现了跨三个维度的平衡空间数据复用，与传统的 2D 设计相比，空间利用率提高了多达 2.0 倍。在共享内存访问架构内部，Voltra 集成了灵活的数据流控制器，支持混合粒度的硬件数据预取和动态内存分配，与非预取和分离式内存架构相比，其时间利用率进一步提高了 2.12-2.94 倍，总延迟加速比分别达到 1.15-2.36 倍。该芯片采用 16nm 工艺制造，实现了 1.60 TOPS/W 的峰值系统能效和 1.25 TOPS/mm² 的系统面积效率，在多种工作负载下均能保持高利用率，与现有最先进的解决方案相比具有竞争力。

## Abstract
Achieving high compute utilization across a wide range of AI workloads is crucial for the efficiency of versatile DNN accelerators. This paper presents the Voltra chip and its utilization-optimised DNN accelerator architecture, which leverages 3-Dimensional (3D) spatial data reuse along with efficient and flexible shared memory access. The 3D spatial dataflow enables balanced spatial data reuse across three dimensions, improving spatial utilization by up to 2.0x compared to a conventional 2D design. Inside the shared memory access architecture, Voltra incorporates flexible data streamers that enable mixed-grained hardware data pre-fetching and dynamic memory allocation, further improving the temporal utilization by 2.12-2.94x and achieving 1.15-2.36x total latency speedup compared with the non-prefetching and separated memory architecture, respectively. Fabricated in 16nm technology, our chip achieves 1.60 TOPS/W peak system energy efficiency and 1.25 TOPS/mm2 system area efficiency, which is competitive with state-of-the-art solutions while achieving high utilization across diverse workloads.

---

## 论文详细总结（自动生成）

这篇论文介绍了一款名为 **Voltra** 的 16nm DNN 加速器芯片，旨在解决通用加速器在处理多样化 AI 工作负载时计算利用率低和内存访问受限的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：边缘侧 AI 应用对高性能、高能效和设计灵活性有极高要求。
*   **核心问题**：
    *   **空间利用率低**：传统的 2D 脉动阵列在处理尺寸与阵列维度不匹配的算子时，容易出现大量空闲计算单元。
    *   **内存灵活性差**：传统的分离式缓存（输入/权重/部分和缓存独立）限制了分块（Tiling）策略，导致片上内存无法充分利用，增加了访存开销。
    *   **银行冲突（Bank Contention）**：共享内存架构虽然灵活，但多端口同时访问易导致冲突，降低时间利用率。

### 2. 方法论与关键技术
Voltra 架构由 GEMM 核心、量化 SIMD 单元、数据重排器和共享多银行内存组成，核心技术包括：
*   **3D 空间数据复用**：采用 8×8×8 的 3D 空间阵列（共 512 个 MAC），通过在三个计算维度上平衡展开，相比 2D 阵列能更好地适应不同形状的矩阵运算，提高空间利用率。
*   **混合粒度硬件数据预取 (MGDP)**：在数据流调度器（Streamer）中引入 FIFO 缓冲。
    *   **输入流调度器**：采用 64 位细粒度访问，支持复杂的卷积（如隐式 im2col）寻址。
    *   **权重流调度器**：采用 512 位粗粒度访问，通过合并 8 个内存银行实现高带宽。
    *   该机制通过主动预取隐藏了访存延迟，缓解了共享内存的银行冲突。
*   **可编程动态内存分配 (PDMA)**：统一的内存空间允许根据负载特性动态划分输入、权重和输出的比例，减少了与片外内存的数据交换。
*   **时间复用 (Time-Multiplexing)**：对量化 SIMD 单元和部分和/输出调度器进行时间复用设计，在极小性能损失（约 0.7%）的前提下，显著缩小了芯片面积。

### 3. 实验设计与 Benchmark
*   **测试负载**：涵盖了多种主流模型，包括 CNN (MobileNetV2, ResNet50)、Transformer (ViT-B, BERT-Base)、点云网络 (PointNeXt)、RNN (LSTM) 以及大语言模型 (LLaMA 3.2-3B 的 Prefill 和 Decode 阶段)。
*   **对比基准 (Baseline)**：
    *   对比了传统的 2D 空间阵列设计。
    *   对比了无预取机制的共享内存架构。
    *   对比了固定比例的分离式内存架构。
*   **评估指标**：空间利用率、时间利用率、总延迟加速比、系统能效 (TOPS/W) 和面积效率 (TOPS/mm²)。

### 4. 资源与算力
*   **工艺与规格**：采用 **16nm FinFET** 工艺。
*   **算力表现**：在 0.6V-1.0V 电压下，频率为 300-800 MHz。
*   **峰值性能**：系统峰值能效为 **1.60 TOPS/W**（在 0.6V, 300MHz 下测得），峰值面积效率为 **1.25 TOPS/mm²**。
*   **片上资源**：128 KB 共享数据内存，6 KB 指令缓存，集成了一个轻量级 RISC-V Snitch 核心用于控制。
*   **训练说明**：文中未提及模型训练过程，重点在于推理加速器的硬件实测。

### 5. 实验数量与充分性
*   **实验覆盖面**：论文针对 8 种不同的典型 AI 工作负载进行了详尽的测试，涵盖了从视觉到语言处理的多种计算模式。
*   **消融实验**：分别针对 3D 阵列、MGDP 预取技术和 PDMA 内存分配机制进行了对比实验，量化了每项技术带来的提升。
*   **客观性**：实验基于实际流片（Tape-out）后的芯片测量数据，而非单纯的仿真，具有较高的可信度和说服力。

### 6. 主要结论与发现
*   **利用率提升**：3D 空间阵列将空间利用率最高提升了 **2.0 倍**；混合粒度预取技术将时间利用率提升了 **2.12-2.94 倍**。
*   **延迟降低**：通过动态内存分配减少片外通信，总延迟相比分离式架构缩短了 **1.15-2.36 倍**。
*   **综合性能**：Voltra 在保持高灵活性的同时，其能效和面积效率均达到了业界领先水平（SotA），尤其在处理 Transformer 等现代负载时表现优异。

### 7. 优点与亮点
*   **高通用性**：成功证明了一个架构可以同时高效处理 CNN、RNN 和 LLM 等多种异构负载。
*   **架构创新**：3D 空间复用与混合粒度预取的结合，有效解决了共享内存架构中的性能瓶颈。
*   **极致面积优化**：通过时间复用技术，在 0.654 mm² 的微小面积内集成了强大的计算能力。

### 8. 不足与局限
*   **LLM 瓶颈**：在 LLaMA 3.2 的 Decode 阶段（GEMV 运算为主），空间利用率下降至 69.71%，说明 3D 阵列在处理极小 Batch 或向量运算时仍有优化空间。
*   **内存容量限制**：片上仅 128 KB 内存，对于超大规模模型，其性能将高度依赖于片外带宽和 DMA 的调度效率。
*   **软件栈依赖**：高性能依赖于复杂的 Tiling 策略和可编程调度器的配置，对编译器或底层驱动的开发要求较高。

（完）
