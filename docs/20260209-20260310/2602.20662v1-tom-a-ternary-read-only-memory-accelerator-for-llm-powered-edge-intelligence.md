---
title: "TOM: A Ternary Read-only Memory Accelerator for LLM-powered Edge Intelligence"
title_zh: TOM：一种用于大语言模型驱动的边缘智能的三值只读存储器加速器
authors: "Hongyi Guan, Yijia Zhang, Wenqiang Wang, Yizhao Gao, Shijie Cao, Chen Zhang, Ningyi Xu"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20662v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 与量化协同设计的ROM-SRAM混合加速器
tldr: "针对边缘设备部署大语言模型（LLM）面临的内存墙挑战，本文提出了一种名为TOM的混合ROM-SRAM加速器。该方案结合三值量化技术，通过将三值权重合成为标准单元逻辑以消除零位面积开销，并采用分布式架构协同ROM与SRAM适配器，实现了极高的内存密度和带宽。TOM支持基于QLoRA的设备端微调，在BitNet-2B模型上达到了3,306 TPS的推理吞吐量，为实时、低功耗的边缘智能提供了有效支持。"
selection_source: fresh_fetch
motivation: 边缘设备有限的内存容量和带宽严重限制了大语言模型的部署规模、推理速度及设备端适配能力。
method: 提出一种混合ROM-SRAM架构，利用稀疏感知ROM将三值权重合成为逻辑电路，并结合分布式计算单元与动态电源门控技术。
result: "在BitNet-2B模型上实现了3,306 TPS的推理吞吐量，显著提升了内存密度和处理效率。"
conclusion: TOM通过协同设计三值量化与ROM架构，成功解决了边缘端LLM部署的内存瓶颈，兼顾了高性能推理与灵活的微调能力。
---

## 摘要
在边缘设备上部署大语言模型（LLMs）以实现实时智能的需求正迅速增长。然而，传统的硬件架构面临着根本性的存储墙挑战，受限的设备端内存容量和带宽严重制约了可部署模型的规模及其推理速度，同时也限制了设备端的自适应能力。为了应对这一挑战，我们提出了 TOM，这是一种与三值量化协同设计的 ROM-SRAM 混合加速器，它在极致的存储密度与设备端可调性之间取得了平衡。TOM 利用三值量化与 ROM 之间的协同效应来实现极高的存储密度和带宽，同时通过专为基于 QLoRA 的可调性设计的 ROM-SRAM 混合架构保留了灵活性。具体而言，我们引入了：(1) 一种稀疏感知型 ROM 架构，将三值权重合成为标准单元逻辑，消除了零值位带来的面积开销；(2) 一种分布式处理架构，将高密度 ROM 库与灵活的基于 SRAM 的 QLoRA 适配器及计算单元部署在一起；以及 (3) 一种工作负载感知的动态功率门控方案，利用 ROM 基于逻辑的特性来关闭非活动库，从而最大限度地降低动态能耗。TOM 在使用 BitNet-2B 模型时实现了 3,306 TPS 的推理吞吐量，证明了其在提供实时、高能效边缘智能方面的有效性。

## Abstract
The deployment of Large Language Models (LLMs) for real-time intelligence on edge devices is rapidly growing. However, conventional hardware architectures face a fundamental memory wall challenge, where limited on-device memory capacity and bandwidth severely constrain the size of deployable models and their inference speed, while also limiting on-device adaptation. To address this challenge, we propose TOM, a hybrid ROM-SRAM accelerator co-designed with ternary quantization, which balances extreme density with on-device tunability. TOM exploits the synergy between ternary quantization and ROM to achieve extreme memory density and bandwidth, while preserving flexibility through a hybrid ROM-SRAM architecture designed for QLoRA-based tunability. Specifically, we introduce: (1) a sparsity-aware ROM architecture that synthesizes ternary weights as standard-cell logic, eliminating area overhead from zero-valued bits; (2) a distributed processing architecture that co-locates high-density ROM banks with flexible SRAM-based QLoRA adapters and compute units; and (3) a workload-aware dynamic power gating scheme that exploits the logic-based nature of ROM to power down inactive banks, minimizing dynamic energy consumption. TOM achieves an inference throughput of 3,306 TPS using BitNet-2B model, demonstrating its effectiveness in delivering real-time, energy-efficient edge intelligence.

---

## 论文详细总结（自动生成）

这是一份关于论文《TOM: A Ternary Read-only Memory Accelerator for LLM-powered Edge Intelligence》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在智能手机、机器人、AR眼镜等边缘设备上部署大语言模型（LLM）面临严峻的“内存墙”挑战。受限的内存容量和带宽不仅限制了模型规模，还导致推理速度慢、功耗高，且难以进行设备端适配。
*   **研究背景**：
    *   **三值量化（Ternary Quantization）**：如 BitNet b1.58，将权重限制在 $\{-1, 0, +1\}$，在保持精度的同时极大简化了计算（仅需加法）。
    *   **ROM 的潜力**：只读存储器（ROM）比 SRAM 密度更高、功耗更低，适合存储固化的模型权重。
    *   **动机**：通过模型与硬件的协同设计，利用 ROM 存储“知识基础”（基础模型），利用 SRAM 提供“灵活性层”（QLoRA 适配器），以实现极致的存储密度和推理带宽。

### 2. 方法论：核心思想与关键技术
TOM 采用了一种 **ROM-SRAM 混合架构**，其核心技术包括：
*   **稀疏感知型 ROM（Sparsity-aware ROM）**：
    *   **核心思想**：不使用传统的网格式 ROM 结构，而是将三值权重矩阵视为“输入地址到输出位”的组合逻辑电路进行合成。
    *   **技术细节**：利用 EDA 工具的公共子表达式消除（CSE）优化，零值位（在三值模型中占比极高）直接接地，不消耗逻辑面积。通过将 -1 编码为 `10` 而非 `11`，进一步提升了零位比例。
*   **分布式处理架构**：
    *   **层级结构**：由全局控制器和多个并行处理巷道（Processing Lanes）组成。
    *   **计算单元（MVU）**：每个巷道包含多个矩阵-向量乘法单元，内置本地 ROM（存权重）和 SRAM（存 KV Cache）。支持三值×FP8（用于 FFN）和 FP8×FP8（用于 Attention）的异构计算，共享加法树以节省面积。
*   **工作负载感知的动态功率门控**：
    *   利用逻辑 ROM 瞬时唤醒的特性，根据 LLM 逐层推理的预测性，仅为当前活跃层的 ROM 库供电，关闭其余所有库。
*   **设备端可调性**：
    *   通过在 SRAM 中存储三值化的 QLoRA 适配器，实现“双路径执行”：先走 ROM 基础路径，再走 SRAM 适配路径，最后在向量单元（VU）中求和。

### 3. 实验设计
*   **模型与数据集**：主要使用 **BitNet-2B** 模型进行评估。
*   **测试场景**：涵盖了不同输入/输出序列长度（64 到 512 tokens）的推理任务。
*   **Benchmark（基准对比）**：
    *   **通用处理器**：Intel Core i5-12500H（运行 bitnet.cpp）、NVIDIA A100 GPU（单批次推理）。
    *   **专用硬件（ASIC/PIM）**：对比了 Olive、FIGNA、Spatten、TF-MVP、Arc 和 SOFA 等学术界先进加速器。
    *   **存储技术**：对比了 3D-SRAM、3D-DRAM 以及各种多电平（MLC/QLC）ROM 技术。

### 4. 资源与算力
*   **硬件实现**：使用 Verilog HDL 描述，采用 **7nm 工艺节点** 进行合成和评估。
*   **EDA 工具**：使用 Synopsys Fusion Compiler 进行逻辑合成，PowerArtist 进行功耗分析，Verilator 进行周期精确仿真。
*   **算力说明**：文中未提及模型训练所需的算力，因为该研究聚焦于**推理加速器设计**。芯片总面积为 56.9 $mm^2$，运行频率为 500 MHz。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   **面积密度分析**：测试了不同稀疏度（50%-95%）和 ROM 库高度对存储密度的影响。
    *   **性能分析**：对比了首字延迟（TTFT）、字间延迟（TBT）和端到端吞吐量。
    *   **消融实验**：专门验证了动态功率门控对功耗的削减效果。
    *   **扩展性分析**：评估了 LoRA 配置和上下文长度增加带来的开销。
*   **充分性评价**：实验设计较为全面，不仅对比了通用硬件，还与多种前沿 ASIC 进行了归一化对比，数据展示客观，充分验证了稀疏感知 ROM 的优势。

### 6. 主要结论与发现
*   **极致性能**：在 BitNet-2B 上实现了 **3,306 TPS** 的推理吞吐量，比 NVIDIA A100 GPU 快 **63.7倍**，比 CPU 快数千倍。
*   **超高密度**：稀疏感知 ROM 在 70% 稀疏度下达到了 **15.0 MB/$mm^2$** 的存储密度，是标准 ROM 的 5.2 倍，甚至超过了某些 3D 堆叠 DRAM。
*   **极低功耗**：通过动态功率门控，芯片总功耗从 25.8W 降至 **5.33W**（降幅约 80%）。
*   **高带宽**：实现了 **200 TB/s** 的总片上内存带宽。

### 7. 优点（亮点）
*   **创新性存储设计**：将存储问题转化为逻辑合成问题，巧妙利用了三值模型的稀疏性，打破了传统存储阵列的面积限制。
*   **协同设计**：完美结合了三值量化的计算优势（无乘法）与 ROM 的物理优势。
*   **兼顾灵活性**：通过混合架构支持 QLoRA，解决了 ROM 固化后无法更新模型的痛点。
*   **能效比极高**：动态功率门控技术与 LLM 推理特性的结合非常紧密。

### 8. 不足与局限
*   **模型更新成本**：虽然支持 LoRA 微调，但基础模型（Base Model）是硬连线在 ROM 中的。若要更换基础模型架构，必须重新制造芯片（硬件生命周期通常为 1 年以上）。
*   **量化限制**：该架构高度优化于三值（1.58-bit）模型，对于更高比特（如 4-bit 或 8-bit）或其他非线性量化方案的兼容性较差。
*   **制造复杂性**：大规模逻辑合成 ROM 可能导致 EDA 工具在布线阶段面临挑战（尽管文中提到 P&R 偏差小于 5%）。
*   **应用范围**：主要针对推理任务，不具备通用的训练能力。

（完）
