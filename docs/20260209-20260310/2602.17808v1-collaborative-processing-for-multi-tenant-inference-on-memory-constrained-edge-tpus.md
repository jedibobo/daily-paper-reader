---
title: Collaborative Processing for Multi-Tenant Inference on Memory-Constrained Edge TPUs
title_zh: 内存受限边缘 TPU 上多租户推理的协同处理
authors: "Nathan Ng, Walid A. Hanafy, Prashanthi Kadambi, Balachandra Sunil, Ayush Gupta, David Irwin, Yogesh Simmhan, Prashant Shenoy"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17808v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 内存受限的边缘TPU与内存带宽优化
tldr: 针对边缘TPU片上内存受限导致的模型段频繁交换及延迟增加问题，本文提出SwapLess系统。该系统通过CPU与TPU的协同处理，利用解析排队模型实时评估不同负载下的处理时间与交换开销，动态调整模型切分点和CPU核心分配。在多租户动态环境下，SwapLess显著降低了端到端推理延迟，有效提升了边缘设备的资源利用率。
selection_source: fresh_fetch
motivation: 边缘TPU有限的片上内存迫使模型段在主机与加速器间频繁交换，严重影响了多租户推理任务的实时性能。
method: 提出SwapLess系统，利用解析排队模型在线动态优化模型切分点和CPU核心分配，以平衡计算负载并减少内存交换开销。
result: "实验表明，SwapLess在单租户和多租户场景下分别将平均延迟降低了最高63.8%和77.4%。"
conclusion: 通过自适应的CPU-TPU协同处理和精细化资源调度，可以有效解决内存受限加速器在复杂推理负载下的性能瓶颈。
---

## 摘要
物联网（IoT）应用正日益依赖设备端 AI 加速器来确保高性能，特别是在连接受限和安全关键的场景中。然而，这些加速器有限的片上内存迫使推理运行时在主机和加速器内存之间交换模型段，从而大幅增加了延迟。虽然通过在 CPU 和加速器资源之间划分模型处理的协同处理可以减轻加速器内存压力并降低延迟，但简单的划分可能会因为将过多计算转移到 CPU 或未能充分抑制交换而恶化端到端延迟，这一问题在多租户和动态环境中会进一步放大。为了解决这些问题，我们提出了 SwapLess，这是一个针对内存受限边缘 TPU 的自适应多租户 TPU-CPU 协同推理系统。SwapLess 利用一种解析排队模型，该模型能够捕捉不同工作负载组合和请求率下，与划分相关的 CPU/TPU 服务时间以及模型间和模型内的交换开销。利用该模型，SwapLess 在线持续调整划分点和 CPU 核心分配，以极低的决策开销最小化端到端响应时间。在配备边缘 TPU 的平台上的实现表明，相对于默认的边缘 TPU 编译器，SwapLess 将单租户工作负载的平均延迟降低了高达 63.8%，多租户工作负载的平均延迟降低了高达 77.4%。

## Abstract
IoT applications are increasingly relying on on-device AI accelerators to ensure high performance, especially in limited connectivity and safety-critical scenarios. However, the limited on-chip memory of these accelerators forces inference runtimes to swap model segments between host and accelerator memory, substantially inflating latency. While collaborative processing by partitioning the model processing between CPU and accelerator resources can reduce accelerator memory pressure and latency, naive partitioning may worsen end-to-end latency by either shifting excessive computation to the CPU or failing to sufficiently curb swapping, a problem that is further amplified in multi-tenant and dynamic environments.   To address these issues, we present SwapLess, a system for adaptive, multi-tenant TPU-CPU collaborative inference for memory-constrained Edge TPUs. SwapLess utilizes an analytic queueing model that captures partition-dependent CPU/TPU service times as well as inter- and intra-model swapping overheads across different workload mixes and request rates. Using this model, SwapLess continuously adjusts both the partition point and CPU core allocation online to minimize end-to-end response time with low decision overhead. An implementation on Edge TPU-equipped platforms demonstrates that SwapLess reduces mean latency by up to 63.8% for single-tenant workloads and up to 77.4% for multi-tenant workloads relative to the default Edge TPU compiler.

---

## 论文详细总结（自动生成）

这篇论文针对边缘计算设备中 AI 推理的性能瓶颈提出了创新的解决方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：边缘 AI 加速器（如 Google Edge TPU）虽然计算能力强，但**片上内存（SRAM）极度受限**（通常仅 8MB）。当模型大小超过此限制时，系统必须在主机内存和加速器之间频繁交换模型参数（Swapping），导致巨大的延迟开销（实验显示可占总延迟的 62%）。
*   **研究背景**：在多租户（多个模型并发）和动态负载环境下，这种内存压力会进一步演变为“模型间交换”冲突。虽然将部分计算任务交给 CPU 处理（协同处理）可以缓解压力，但如果划分不当，反而会因为 CPU 计算慢或未能有效减少交换而增加总延迟。

### 2. 方法论
SwapLess 系统通过**自适应的 TPU-CPU 协同推理**来最小化端到端延迟，核心组件包括：
*   **解析排队模型（Analytic Queueing Model）**：
    *   **TPU 建模**：将其视为 $M/G/1$ 排队系统，捕捉模型前缀的计算时间、模型内交换开销以及由于多租户竞争导致的“模型间权重缺失概率”（$\alpha$）。
    *   **CPU 建模**：将其视为 $M/D/k$ 排队系统，为每个模型分配独立的 CPU 核心数（$k$）和执行队列，以实现性能隔离。
    *   **端到端延迟公式**：综合考虑了数据传输（USB 3.0 带宽）、TPU 排队、权重加载、TPU 执行、中间张量传输、CPU 排队及 CPU 执行。
*   **贪心爬山资源分配算法（Greedy Hill-Climbing）**：
    *   **在线优化**：系统实时监测请求率，利用排队模型快速搜索最优的模型切分点（Partition Point）和 CPU 核心分配方案。
    *   **多步搜索**：为了跳过局部最优解，算法在每轮迭代中会尝试移动最多两个层级。
*   **离线与在线阶段**：离线负责模型切分、量化编译和性能剖析（Profiling）；在线负责动态调度和执行。

### 3. 实验设计
*   **硬件环境**：Raspberry Pi 5 (4核 ARM CPU) + Google Coral USB Accelerator (Edge TPU)。
*   **测试模型**：选取了 9 种具有代表性的卷积神经网络（CNN），包括轻量级的 SqueezeNet、MobileNetV2 到重型的 ResNet50V2、InceptionV4（大小从 1.4MB 到 43.2MB 不等）。
*   **对比基准（Benchmarks）**：
    1.  **Edge TPU Compiler**：官方默认的静态编译策略。
    2.  **Threshold-based Partitioning**：基于固定阈值的简单切分策略（不考虑排队和多租户竞争）。
    3.  **SwapLess ($\alpha=0$)**：不考虑模型间交换开销的消融版本。
*   **场景设计**：涵盖单租户、多租户（不同模型组合、不同请求比例 50:50 或 90:10）以及动态负载变化场景。

### 4. 资源与算力
*   **推理设备**：使用单台 Raspberry Pi 5 和单个 Edge TPU 协处理器。
*   **算力说明**：Edge TPU 提供约 4 TOPS 的定点运算能力。
*   **训练/编译**：论文侧重于推理优化，未提及模型训练时长。模型通过 TensorFlow Lite 转换器进行 8-bit 量化。
*   **决策开销**：SwapLess 的资源分配算法极其轻量，每次调用耗时小于 2ms。

### 5. 实验数量与充分性
*   **实验规模**：论文对 9 种模型进行了详尽的性能剖析，并在多种 TPU 利用率（$\rho=0.2, 0.5$）下进行了测试。
*   **充分性**：实验不仅验证了排队模型的预测准确性（平均绝对百分比误差 MAPE 在单租户下为 1.9%，多租户下为 6.8%），还通过动态负载实验展示了系统的实时自适应能力。
*   **客观性**：对比了官方编译器和启发式策略，涵盖了从内存拟合到严重超限的各种工作负载组合，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **显著提速**：相比官方编译器，SwapLess 在单租户场景下降低延迟达 **63.8%**，在多租户场景下降低达 **77.4%**。
*   **交换开销是瓶颈**：证实了在内存受限设备上，减少内存交换比单纯追求加速器计算速度更重要。
*   **协同处理的必要性**：在模型末端，CPU 的处理速度往往与 TPU 相当，利用 CPU 运行这些层可以有效释放 TPU 内存空间。
*   **动态调整的价值**：在负载波动时，SwapLess 能通过动态切分和核心重分配，比静态方案降低 75.1% 的延迟。

### 7. 优点
*   **模型驱动的精准性**：引入排队论模型，能够量化多租户环境下的竞争和交换概率，而非依赖简单的启发式规则。
*   **极低开销**：算法复杂度低，适合在资源受限的边缘设备上实时运行。
*   **通用性**：不依赖于特定的私有驱动修改，基于标准的 TFLite 运行时实现。

### 8. 不足与局限
*   **模型架构限制**：目前主要针对标准卷积模型（CNN），未深入探讨具有动态注意力机制或非线性操作的 Transformer 模型（尽管文中提到这是未来方向）。
*   **内存模型简化**：对 TPU 内部内存置换算法（Proprietary）采用了保守的概率估计（$\alpha$），可能在某些特定硬件行为下存在偏差。
*   **切分粒度**：目前的切分是以层为单位的单点切分，未考虑更复杂的子图并行或多点切分。
*   **硬件范围**：实验仅限于 Google Coral TPU，虽然理论可推广，但未在其他加速器（如华为昇腾、瑞芯微 NPU）上验证。

（完）
