---
title: Bitwise Systolic Array Architecture for Runtime-Reconfigurable Multi-precision Quantized Multiplication on Hardware Accelerators
title_zh: 硬件加速器上用于运行时可重构多精度量化乘法的位级脉动阵列架构
authors: "Yuhao Liu, Salim Ullah, Akash Kumar"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23334v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于多精度量化乘法的脉动阵列
tldr: 针对边缘设备中量化神经网络在低比特下精度损失严重的问题，本文提出了一种支持运行时重构的多精度多通道位脉动阵列架构。该设计解决了传统硬件无法在运行时灵活调整乘法精度的难题，通过在FPGA上的实现验证，显著提升了混合精度模型的推理速度并优化了时钟频率，为高效能混合精度加速器提供了新方案。
selection_source: fresh_fetch
motivation: 传统硬件加速器难以在运行时支持混合精度量化神经网络的精度重构，导致资源利用与推理精度难以平衡。
method: 提出一种运行时可重构的多精度多通道位脉动阵列架构，专门用于处理量化神经网络中的多精度乘法运算。
result: 在Ultra96 FPGA平台上，该设计实现了1.3185至3.5671倍的推理加速，并能稳定运行在250MHz的高时钟频率下。
conclusion: 该位脉动阵列架构通过灵活的精度重构能力，有效解决了混合精度模型在硬件部署中的效率与灵活性问题。
---

## 摘要
神经网络加速器已广泛应用于边缘设备，用于处理目标跟踪、图像识别等复杂任务。先前的研究在相关的轻量级加速器设计中探索了量化技术，以减少硬件资源消耗。然而，低精度会导致推理过程中的高精度损失。因此，混合精度量化成为一种替代方案，通过在不同层应用不同的精度来权衡资源消耗和准确性。由于常规的硬件乘法设计无法支持多精度量化神经网络（QNN）模型在运行时的精度重构，我们为 QNN 加速器提出了一种运行时可重构的多精度多通道位级脉动阵列设计。我们在 Ultra96 FPGA 平台上实现并评估了我们的工作。结果表明，我们的工作在推理混合精度模型时可以实现 1.3185 到 3.5671 倍的加速，并且具有更短的关键路径延迟，支持更高的时钟频率（250MHz）。

## Abstract
Neural network accelerators have been widely applied to edge devices for complex tasks like object tracking, image recognition, etc. Previous works have explored the quantization technologies in related lightweight accelerator designs to reduce hardware resource consumption. However, low precision leads to high accuracy loss in inference. Therefore, mixed-precision quantization becomes an alternative solution by applying different precision in different layers to trade off resource consumption and accuracy. Because regular designs for multiplication on hardware cannot support the precision reconfiguration for a multi-precision Quantized Neural Network (QNN) model in runtime, we propose a runtime reconfigurable multi-precision multi-channel bitwise systolic array design for QNN accelerators. We have implemented and evaluated our work on the Ultra96 FPGA platform. Results show that our work can achieve 1.3185 to 3.5671 times speedup in inferring mixed-precision models and has less critical path delay, supporting a higher clock frequency (250MHz).

---

## 论文详细总结（自动生成）

这是一份关于论文《Bitwise Systolic Array Architecture for Runtime-Reconfigurable Multi-precision Quantized Multiplication on Hardware Accelerators》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在硬件加速器上高效支持**运行时可重构的混合精度（Mixed-precision）**量化神经网络（QNN）推理。
*   **研究动机**：
    *   **精度与资源的权衡**：低比特量化（如1-bit）虽节省资源但精度损失大；混合精度量化（不同层使用不同比特数）能平衡两者。
    *   **硬件局限性**：现有的硬件乘法器（如Vivado IP）通常是固定宽度的（如8-bit）。在处理低比特层（1/2/4-bit）时，必须将数据填充至8-bit，导致计算资源浪费，无法获得推理加速。
    *   **灵活性需求**：需要一种能在运行时动态调整精度和通道数，且不损失吞吐量的乘法器架构。

### 2. 方法论
*   **核心思想**：将乘法运算分解为**位级（Bitwise）操作**，利用位级脉动阵列（Systolic Array）处理子部分积，并通过掩码（Mask）和移位实现多精度重构。
*   **关键技术细节**：
    *   **BitSys 架构**：由位级处理单元（PE）组成的脉动阵列。支持 1/2/4/8 通道的 8/4/2/1-bit 乘法。
    *   **位级 PE 设计**：设计了两种 PE。Type I 支持 XNOR（用于二值网络 BNN）和 AND 操作；Type II 支持 AND 和零输出（用于掩码过滤）。利用 Xilinx FPGA 的 **LUT6_2 原语**进行底层优化，降低延迟。
    *   **输出生成流水线**：包含移位器和加法器树，并引入“进位截断（Carry-cutter）”模块，以确保多通道模式下各通道结果互不干扰。
    *   **运行时重构**：通过状态机控制模式信号，仅需 3 个时钟周期即可完成层间的精度切换。

### 3. 实验设计
*   **硬件平台**：Ultra96-V2 FPGA (Zynq UltraScale+ ZU3EG)。
*   **数据集与模型**：使用 **MNIST** 数据集，训练了 Tiny MLP (TFC) 和 Tiny CNN (TCV) 模型。
*   **对比基准（Benchmark）**：
    *   **Vivado IP**：标准的 8-bit 整数乘法器。
    *   **Multiplier-Tree (MTree)**：基于子乘法器方案的先前工作。
    *   **Bitshifter**：作者先前基于位移和掩码的乘法器设计。
*   **评估指标**：资源消耗（LUT/FF/BRAM）、时钟频率、关键路径延迟、功耗、推理延迟（Latency）。

### 4. 资源与算力
*   **硬件资源**：在 FPGA 上实现，单个 BitSys-LUT 乘法器消耗约 350 个 LUT 和 525 个 FF。
*   **算力表现**：加速器包含 64 个乘法器。在 250MHz 频率下，推理混合精度 TFC 模型的延迟为 36.741 μs。
*   **训练说明**：论文侧重于推理硬件架构，模型训练使用 Brevitas 库完成，未详细说明训练所用的 GPU 型号或时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 6 种不同的乘法器/累加器实例（基础版与流水线版）。
    *   对比了 7 种加速器配置（单层架构 vs 脉动阵列架构）。
    *   进行了 16,000 次随机乘法测试以评估功耗。
*   **充分性评价**：实验在硬件指标（面积、速度、功耗）的对比上非常详尽，涵盖了从算子级到系统级的评估。但**模型覆盖面较窄**，仅限于 MNIST 上的小型网络，未在 ImageNet 等大型任务上验证。

### 6. 主要结论与发现
*   **性能提升**：BitSys 架构在推理混合精度模型时，比 Vivado IP 方案快 **3.5671 倍**，比先前的 MTree 和 Bitshifter 方案快约 **1.32 倍**。
*   **频率优势**：由于采用了位级 PE 和深度流水线，BitSys 支持高达 **250MHz** 的时钟频率，显著高于对比方案。
*   **能效比**：虽然 BitSys 的动态功耗略高，但其功耗延迟积（PDP）和面积延迟积（ADP）均优于对比方案，证明了其在性能与资源利用之间的平衡性。

### 7. 优点
*   **高度灵活**：真正实现了运行时（Runtime）的精度重构，且支持 BNN 的 XNOR 运算。
*   **底层优化**：针对 FPGA LUT 原语进行了手动优化，极大地压缩了关键路径延迟（低至 1.4ns 左右）。
*   **高吞吐量**：脉动阵列结构简化了数据流控制，充分利用了流水线并行性。

### 8. 不足与局限
*   **模型复杂度低**：实验仅使用了极小规模的网络（TFC/TCV）和简单数据集（MNIST），难以评估其在复杂视觉任务（如 ResNet/Transformer）中的实际表现。
*   **资源开销**：为了实现高频率和灵活性，其寄存器（FF）的消耗量显著高于传统方案（比单层架构高出约 18%）。
*   **应用限制**：该设计主要针对 FPGA 架构优化，若迁移至 ASIC，其基于 LUT 的优化优势可能无法直接体现。

（完）
