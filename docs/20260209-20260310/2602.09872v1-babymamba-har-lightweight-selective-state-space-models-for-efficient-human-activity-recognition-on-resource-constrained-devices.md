---
title: "BabyMamba-HAR: Lightweight Selective State Space Models for Efficient Human Activity Recognition on Resource Constrained Devices"
title_zh: BabyMamba-HAR：用于资源受限设备上高效人体活动识别的轻量级选择性状态空间模型
authors: Mridankan Mandal
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09872v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 资源受限设备的能效架构设计
tldr: 针对资源受限设备上的传感器人体活动识别（HAR），本文提出了BabyMamba-HAR框架。该框架包含两种基于Mamba的轻量级架构：CI-BabyMamba-HAR（通道独立）和Crossover-BiDir-BabyMamba-HAR（早期融合）。通过引入双向扫描和门控时间注意力池化，在保持极低参数量（约27K）和计算量（2.21M MACs）的同时，实现了与主流模型相当的准确率，显著提升了在TinyML环境下的运行效率。
selection_source: fresh_fetch
motivation: 现有的HAR模型在移动和可穿戴设备上面临内存和计算瓶颈，需要一种比传统注意力机制更高效的序列处理方案。
method: 提出了两种基于选择性状态空间模型（SSM）的轻量级架构，并结合了权重共享的双向扫描和门控时间注意力池化技术。
result: "在八个基准测试中，该模型以约27K参数实现了86.52%的F1分数，且在高通道数据集上的计算量比TinyHAR降低了11倍。"
conclusion: 研究证明了选择性状态空间模型作为TinyML骨干网络在HAR任务中的高效性，并为资源受限下的模型设计提供了实用准则。
---

## 摘要
穿戴式和移动设备上的人体活动识别（HAR）受限于内存占用和计算预算，但仍需在异构传感器配置下保持具有竞争力的准确率。选择性状态空间模型（SSMs）通过输入依赖的门控机制提供线性时间序列处理，为具有二次复杂度的注意力机制提供了一个极具吸引力的替代方案。然而，在 TinyML 领域部署 SSMs 的设计空间在很大程度上仍未被探索。本文介绍了 BabyMamba-HAR，这是一个包含两种新型轻量级 Mamba 启发式架构的框架，专门针对资源受限的 HAR 进行了优化：(1) CI-BabyMamba-HAR，采用通道独立（channel independent）的主干网络，通过共享权重但实例独立的变换处理每个传感器通道，以防止跨通道噪声传播；(2) Crossover-BiDir-BabyMamba-HAR，采用早期融合主干网络，实现了与通道数无关的计算复杂度。两种变体都结合了权重绑定的双向扫描和轻量级时间注意力池化。通过在八个不同基准测试上的评估，结果表明 Crossover-BiDir-BabyMamba-HAR 以约 2.7 万个参数和 221 万次 MACs 实现了 86.52% 的平均宏 F1 分数，在与 TinyHAR（86.16%）性能相当的同时，在高通道数据集上的 MACs 需求减少了 11 倍。系统性的消融研究表明，双向扫描贡献了高达 8.42% 的 F1 分数提升，而门控时间注意力相比均值池化提供了高达 8.94% 的 F1 分数增益。这些发现为将选择性状态空间模型部署为高效的 HAR TinyML 骨干网络确立了实用的设计原则。

## Abstract
Human activity recognition (HAR) on wearable and mobile devices is constrained by memory footprint and computational budget, yet competitive accuracy must be maintained across heterogeneous sensor configurations. Selective state space models (SSMs) offer linear time sequence processing with input dependent gating, presenting a compelling alternative to quadratic complexity attention mechanisms. However, the design space for deploying SSMs in the TinyML regime remains largely unexplored. In this paper, BabyMamba-HAR is introduced, a framework comprising two novel lightweight Mamba inspired architectures optimized for resource constrained HAR: (1) CI-BabyMamba-HAR, using a channel independent stem that processes each sensor channel through shared weight, but instance independent transformations to prevent cross channel noise propagation, and (2) Crossover-BiDir-BabyMamba-HAR, using an early fusion stem that achieves channel count independent computational complexity. Both variants incorporate weight tied bidirectional scanning and lightweight temporal attention pooling. Through evaluation across eight diverse benchmarks, it is demonstrated that Crossover-BiDir-BabyMamba-HAR achieves 86.52% average macro F1-score with approximately 27K parameters and 2.21M MACs, matching TinyHAR (86.16%) while requiring 11x fewer MACs on high channel datasets. Systematic ablation studies reveal that bidirectional scanning contributes up to 8.42% F1-score improvement, and gated temporal attention provides up to 8.94% F1-score gain over mean pooling. These findings establish practical design principles for deploying selective state space models as efficient TinyML backbones for HAR.

---

## 论文详细总结（自动生成）

以下是对论文《BabyMamba-HAR: Lightweight Selective State Space Models for Efficient Human Activity Recognition on Resource Constrained Devices》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限（如 Flash < 100KB，计算量 < 10M MACs）的可穿戴设备上，实现高效且高精度的人体活动识别（HAR）。
*   **研究背景**：
    *   传统的 CNN 和 RNN 模型在处理长序列时存在效率或表达能力瓶颈。
    *   Transformer 虽然强大，但其二次方复杂度（$O(N^2)$）不适合 TinyML 设备。
    *   选择性状态空间模型（SSM/Mamba）具有线性复杂度（$O(N)$），但在 HAR 领域的轻量级设计空间（TinyML 制度）尚未得到充分探索。
    *   现有 HAR 研究缺乏统一的评估协议，导致模型间的公平比较困难。

### 2. 提出的方法论
论文提出了 **BabyMamba-HAR** 框架，包含两种针对不同传感器配置优化的轻量级架构：
*   **核心思想**：利用 Mamba 的选择性扫描机制进行高效序列建模，并通过结构优化压缩参数量。
*   **关键技术细节**：
    1.  **CI-BabyMamba-HAR（通道独立型）**：每个传感器通道通过共享权重的卷积层独立处理，旨在防止异构传感器间的噪声传播，适合传感器类型差异大的场景。
    2.  **Crossover-BiDir-BabyMamba-HAR（早期融合型）**：通过单一卷积将所有通道投影到特征空间，计算复杂度与通道数无关，在处理高维传感器数据（如 79 通道的 Opportunity 数据集）时极具优势。
    3.  **权重绑定双向扫描（Weight Tied BiDir Scanning）**：在不增加参数的前提下，通过共享权重的正向和反向扫描，使有效感受野翻倍。
    4.  **门控时间注意力池化（Context Gated Temporal Attention Pooling）**：取代传统的全局平均池化，利用轻量级注意力机制聚焦于关键的动作转换瞬间。

### 3. 实验设计
*   **数据集**：涵盖了 8 个主流公开基准数据集，包括 UCI-HAR、MotionSense、WISDM、PAMAP2、Opportunity、UniMiB-SHAR、Skoda 和 Daphnet。
*   **评估协议**：采用统一的预处理流程、受试者独立（Subject-independent）的数据划分，并进行 5 次随机种子实验取平均值。
*   **对比方法（Baselines）**：
    *   **TinyHAR**（约 55K 参数）：基于 CNN 和注意力的轻量级模型。
    *   **TinierHAR**（约 33K 参数）：基于深度可分离卷积和 GRU 的超轻量级模型。
    *   **DeepConvLSTM**（约 136K 参数）：经典的 CNN-LSTM 架构。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练硬件（如 GPU 型号）或训练总时长。
*   **模型资源占用**：
    *   **参数量**：约 27K - 28K，远低于 100KB 的 Flash 限制。
    *   **计算量**：Crossover 变体平均仅需 2.21M MACs。在高通道数据集上，计算量比 TinyHAR 降低了 11 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个数据集上进行了详尽测试，每个模型运行 5 个随机种子。
*   **消融实验**：系统性地隔离了双向扫描、池化策略、主干架构（Stem）以及超参数（如 $d_{state}$、$d_{model}$）的影响。
*   **客观性与公平性**：
    *   使用了统一的代码库重新实现所有基准模型。
    *   特别针对 Skoda 单受试者数据集提出了“时间分割法”以防止数据泄露，体现了严谨的实验态度。

### 6. 主要结论与发现
*   **性能表现**：Crossover-BiDir-BabyMamba-HAR 实现了 86.52% 的平均宏 F1 分数，在参数量减半的情况下，性能与 TinyHAR 持平。
*   **效率优势**：在处理高通道数据时，早期融合架构的计算优势巨大（如 Opportunity 数据集上仅需 3.44M MACs）。
*   **关键组件贡献**：双向扫描对复杂动作识别贡献了高达 8.42% 的 F1 提升；门控注意力相比均值池化提升了约 8.94%。
*   **鲁棒性**：模型对超参数变化（如状态维度减半）不敏感，证明了其在轻量级 regime 下的稳定性。

### 7. 优点
*   **极高的能效比**：在极低参数量（<30K）下保持了 SOTA 级别的准确率。
*   **架构灵活性**：提供了 CI 和 Crossover 两种变体，分别应对“噪声隔离”和“高维计算效率”两种需求。
*   **设计创新**：权重绑定的双向扫描机制巧妙地解决了非因果序列处理中的参数冗余问题。
*   **评估严谨**：通过统一协议和针对性的数据分割修正了以往研究中可能存在的性能虚高问题。

### 8. 不足与局限
*   **实时推理限制**：由于采用了双向扫描（BiDir），该模型目前主要适用于窗口化分类，无法直接进行真正的流式（Streaming）实时推理。
*   **模态局限性**：实验仅限于惯性传感器（IMU），尚未验证其在压力传感器、肌电信号（EMG）等其他模态上的表现。
*   **硬件部署缺失**：虽然计算指标（MACs）很低，但尚未在真实的单片机（如 Cortex-M 系列）上进行实际的功耗和延迟测量，也未讨论 INT8 量化对 SSM 稳定性的影响。

（完）
