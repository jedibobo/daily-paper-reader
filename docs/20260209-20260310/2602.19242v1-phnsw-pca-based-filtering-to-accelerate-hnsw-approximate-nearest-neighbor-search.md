---
title: "pHNSW: PCA-Based Filtering to Accelerate HNSW Approximate Nearest Neighbor Search"
title_zh: pHNSW：基于 PCA 过滤的 HNSW 近似最近邻搜索加速
authors: "Zheng Li, Guangyi Zeng, Paul Delestrac, Enyi Yao, Simei Yang"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19242v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 算法-硬件协同优化方案及自定义指令设计
tldr: 针对高维近似最近邻搜索中HNSW算法计算量大且数据访问不规则的问题，本文提出pHNSW。该方案采用算法与硬件协同优化策略，在算法层利用PCA过滤降低数据维度以减少计算和访存开销，在硬件层设计了具备定制指令的专用处理器。实验表明，pHNSW显著提升了搜索吞吐量并大幅降低了能耗，是加速大规模向量检索的有效方案。
selection_source: fresh_fetch
motivation: HNSW算法在高维数据检索中面临巨大的计算负担和不规则的大规模数据访问挑战。
method: 提出pHNSW方案，通过PCA过滤降低维度以减少计算量，并设计了支持定制指令的专用硬件处理器。
result: "实验显示pHNSW在CPU和GPU上分别实现了最高21.37倍和8.46倍的吞吐量提升，并降低了57.4%的能耗。"
conclusion: 通过PCA降维与硬件定制化的协同设计，pHNSW有效解决了HNSW的性能瓶颈，显著提升了搜索效率和能效。
---

## 摘要
分层导航小世界（HNSW）在高维最近邻搜索中表现出卓越的准确性和低延迟。然而，其高计算需求以及不规则、大容量的数据访问模式对搜索效率提出了重大挑战。为了应对这些挑战，我们提出了 pHNSW，这是一种通过主成分分析（PCA）过滤来加速 HNSW 的算法-硬件协同优化方案。在算法方面，我们应用 PCA 过滤来降低数据集的维度，从而减少邻居访问量并降低距离计算的计算负载。在硬件方面，我们设计了具有自定义指令的 pHNSW 处理器，以优化搜索吞吐量和能效。在实验中，我们使用 65nm 工艺节点综合了 pHNSW 处理器 RTL 设计，并使用 DDR4 和 HBM1.0 DRAM 标准对其进行了评估。结果表明，与标准 HNSW 实现相比，pHNSW 在 CPU 上的每秒查询数（QPS）提升了 14.47 倍至 21.37 倍，在 GPU 上提升了 5.37 倍至 8.46 倍，同时能耗降低了高达 57.4%。

## Abstract
Hierarchical Navigable Small World (HNSW) has demonstrated impressive accuracy and low latency for high-dimensional nearest neighbor searches. However, its high computational demands and irregular, large-volume data access patterns present significant challenges to search efficiency. To address these challenges, we introduce pHNSW, an algorithm-hardware co-optimized solution that accelerates HNSW through Principal Component Analysis (PCA) filtering. On the algorithm side, we apply PCA filtering to reduce the dimensionality of the dataset, thereby lowering the volume of neighbor access and decreasing the computational load for distance calculations. On the hardware side, we design the pHNSW processor with custom instructions to optimize search throughput and energy efficiency. In the experiments, we synthesized the pHNSW processor RTL design with a 65nm technology node and evaluated it using DDR4 and HBM1.0 DRAM standards. The results show that pHNSW boosts Queries per Second (QPS) by 14.47x-21.37x on a CPU and 5.37x-8.46x on a GPU, while reducing energy consumption by up to 57.4% compared to standard HNSW implementation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **pHNSW** 的算法-硬件协同优化方案，旨在通过 PCA（主成分分析）过滤技术加速 HNSW（分层导航小世界）近似最近邻搜索。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：HNSW 是目前性能最优的图近似最近邻搜索（ANN）算法之一，广泛应用于推荐系统和信息检索。
*   **核心痛点**：
    1.  **计算负担重**：高维向量（如 128 维或更高）的距离计算消耗大量 CPU/GPU 算力。
    2.  **访存瓶颈**：图遍历过程产生大量不规则、非连续的内存访问，导致数据传输效率低下。
*   **研究动机**：通过降维（PCA）减少计算量，并结合专用硬件设计解决不规则访存问题，实现高吞吐、低能耗的向量检索。

### 2. 方法论
pHNSW 采用了**算法-硬件协同设计**的思路：
*   **算法层面（PCA 过滤）**：
    1.  **降维投影**：将高维向量投影到低维空间（如 128 维降至 15 维）。
    2.  **三步搜索法**：首先在低维空间计算邻居距离；然后筛选出距离最近的 $k$ 个候选者（Top-$k$）；最后仅对这 $k$ 个候选者进行高维空间的精确距离校验。
    3.  **分层 $k$ 值选择**：在 HNSW 的不同层级动态调整 $k$ 值（高层节点稀疏，$k$ 值小；底层节点密集，$k$ 值大），以平衡搜索精度和速度。
*   **硬件层面（专用处理器设计）**：
    1.  **自定义指令集（ISA）**：设计了专门支持数据移动、DMA 访问、并行排序（kSort.L）和距离计算的指令。
    2.  **数据库组织优化**：在 DRAM 中将邻居索引与低维原始数据连续存储，将原本随机的特征向量访问转变为顺序块访问。
    3.  **专用计算单元**：
        *   **Dist.L**：并行计算低维向量距离。
        *   **kSort.L**：采用全并行比较矩阵方案，将 16 个元素的排序延迟从 120 个周期缩短至 7 个周期。
        *   **Dist.H**：处理少量 Top-$k$ 候选者的高维距离计算。

### 3. 实验设计
*   **数据集**：标准 Benchmark 数据集 **SIFT1M**（100 万个 128 维向量）。
*   **对比基准（Benchmarks）**：
    *   **HNSW-CPU**：运行在 Intel i9-12900H 上的标准实现。
    *   **HNSW-GPU**：基于 NVIDIA 的 CAGRA 算法结果。
    *   **消融实验变体**：包括 HNSW-Std（仅硬件优化）、pHNSW-Sep（算法+硬件优化，但未做数据库布局优化）。
*   **内存标准**：分别在 DDR4 和 HBM 1.0 两种带宽环境下评估。

### 4. 资源与算力
*   **硬件实现**：使用 Verilog 编写，采用 **TSMC 65nm** 工艺节点进行 RTL 综合，主频设定为 **1GHz**。
*   **仿真工具**：使用 CACTI 7.0 建模片上 SRAM（128KB），使用 Ramulator 仿真 DRAM 行为。
*   **算力说明**：论文侧重于推理加速器的硬件设计与综合结果，未涉及大规模模型训练，因此未提及 GPU 训练时长。

### 5. 实验数量与充分性
*   **实验维度**：涵盖了 QPS（吞吐量）对比、能耗分解、芯片面积分析、以及针对参数 $k$ 的消融实验（分析 Recall 与 QPS 的权衡）。
*   **充分性**：实验对比了 CPU、GPU 以及多种硬件配置方案，数据较为详实。但实验目前仅限于 SIFT1M 数据集，缺乏在更大规模（如 SIFT1B）或其他维度分布数据集上的验证。

### 6. 主要结论与发现
*   **吞吐量提升**：在相同召回率（Recall@10 = 0.92）下，pHNSW 在 HBM 环境下的 QPS 比 CPU 提升了 **21.37 倍**，比 GPU 提升了 **8.46 倍**。
*   **能效显著**：相比标准 HNSW 的硬件实现，pHNSW 降低了高达 **57.4%** 的能耗。
*   **访存优化效果**：数据库布局优化（顺序存储低维数据）是性能提升的关键，相比不规则访问方案，QPS 提升了数倍。

### 7. 优点
*   **协同优化**：不只是单纯的硬件加速，而是从算法逻辑、数据存储结构到硬件指令的全栈优化。
*   **排序单元创新**：kSort.L 的全并行设计极大地解决了图搜索中频繁排序带来的延迟问题。
*   **实用性强**：在保证高召回率的前提下，显著降低了硬件部署的能耗成本。

### 8. 不足与局限
*   **存储空间开销**：为了实现顺序访问，额外存储低维数据导致数据库体积增加了约 **2.92 倍**（约 1.8GB），这在处理十亿级（Billion-scale）数据时可能会对内存容量造成压力。
*   **数据集单一**：仅在 SIFT1M 上进行了测试，不同类型的数据（如稀疏向量或更高维度的深度学习嵌入向量）可能对 PCA 降维的敏感度不同。
*   **应用限制**：目前主要针对单查询（Single-query）场景优化，对于高并发批处理（Batch processing）场景的扩展性有待进一步探讨。

（完）
