---
title: Improving Continual Learning for Gaussian Splatting based Environments Reconstruction on Commercial Off-the-Shelf Edge Devices
title_zh: 改进商用现货边缘设备上基于高斯泼溅的环境重建持续学习
authors: "Ivan Zaino, Matteo Risso, Daniele Jahier Pagliari, Miguel de Prado, Toon Van de Maele, Alessio Burrello"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08499v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对资源受限硬件的精度自适应优化
tldr: 针对边缘机器人对3D场景重建的实时性与低功耗需求，本研究优化了变分贝叶斯高斯泼溅（VBGS）算法。通过内核融合与混合精度搜索，显著降低了内存占用和训练延迟，首次在Jetson Orin Nano等商用嵌入式设备上实现了高效的持续学习与新视角合成，在保持重建质量的同时大幅提升了边缘端的实用性。
selection_source: fresh_fetch
motivation: 现有的VBGS算法因高精度计算和巨大的中间张量，难以在资源受限的边缘设备上进行实时在线训练。
method: 提出一种精度自适应优化框架，通过算子级内核融合减少中间变量，并利用有界相对误差的混合精度搜索自动分配计算精度。
result: "在A5000上将峰值内存从9.44GB降至1.11GB，训练时间缩短约74%，并在Jetson Orin Nano上实现了比3DGS快19倍的单帧延迟。"
conclusion: 该优化框架成功使VBGS能够在商用嵌入式平台上运行，为边缘侧的持续场景重建和SLAM应用提供了高效的技术支撑。
---

## 摘要
新视角合成 (NVS) 对于边缘机器人技术日益重要，在内存和延迟预算紧张的情况下，SLAM、导航和巡检需要紧凑且可增量更新的 3D 场景模型。变分贝叶斯高斯泼溅 (VBGS) 通过维护概率场景模型，实现了 3DGS 算法的无回放持续更新，但其高精度计算和大型中间张量使得设备端训练变得不切实际。我们提出了一个精度自适应优化框架，在不改变其变分公式的情况下，实现了在资源受限硬件上的 VBGS 训练。我们 (i) 对 VBGS 进行性能分析以识别内存/延迟热点，(ii) 融合内存主导的算子以减少实例化的中间张量，以及 (iii) 通过具有有界相对误差的混合精度搜索自动分配算子级精度。在 Blender、Habitat 和 Replica 数据集上，我们优化的流水线在 A5000 GPU 上将峰值内存从 9.44 GB 降低到 1.11 GB，训练时间从约 234 分钟缩短到约 61 分钟，同时保持（在某些情况下甚至提高了）最先进 VBGS 基线的重建质量。我们还首次在商用嵌入式平台 Jetson Orin Nano 上实现了 NVS 训练，与 3DGS 相比，每帧延迟降低了 19 倍。

## Abstract
Novel view synthesis (NVS) is increasingly relevant for edge robotics, where compact and incrementally updatable 3D scene models are needed for SLAM, navigation, and inspection under tight memory and latency budgets. Variational Bayesian Gaussian Splatting (VBGS) enables replay-free continual updates for the 3DGS algorithm by maintaining a probabilistic scene model, but its high-precision computations and large intermediate tensors make on-device training impractical. We present a precision-adaptive optimization framework that enables VBGS training on resource-constrained hardware without altering its variational formulation. We (i) profile VBGS to identify memory/latency hotspots, (ii) fuse memory-dominant kernels to reduce materialized intermediate tensors, and (iii) automatically assign operation-level precisions via a mixed-precision search with bounded relative error. Across the Blender, Habitat, and Replica datasets, our optimised pipeline reduces peak memory from 9.44 GB to 1.11 GB and training time from ~234 min to ~61 min on an A5000 GPU, while preserving (and in some cases improving) reconstruction quality of the state-of-the-art VBGS baseline. We also enable for the first time NVS training on a commercial embedded platform, the Jetson Orin Nano, reducing per-frame latency by 19x compared to 3DGS.

---

## 论文详细总结（自动生成）

这篇论文针对在商用边缘设备上实现高效、持续的 3D 环境重建提出了优化方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的边缘机器人设备（如 Jetson Orin Nano）上，实现既能防止“灾难性遗忘”又能高效运行的 3D 场景重建。
*   **研究动机**：
    *   传统的 **3D 高斯泼溅 (3DGS)** 在处理连续图像流时会遗忘旧场景，通常需要存储大量历史帧（Replay Buffer）进行重训练，这极大地消耗了边缘设备的存储和计算资源。
    *   **变分贝叶斯高斯泼溅 (VBGS)** 虽然通过概率建模实现了无回放的持续学习，但其原始实现依赖双精度浮点数（fp64）且会产生巨大的中间张量，导致内存占用过高（>9GB），无法在典型的边缘硬件上运行。

### 2. 方法论
论文提出了一个**精度自适应优化框架**，在不改变 VBGS 变分推断逻辑的前提下，从内存和计算效率两方面进行改进：
*   **内存驱动的内核融合 (Kernel Fusion)**：
    *   分析发现 `sum_stats_over_samples` 函数在计算加权统计量时，会先实例化一个巨大的中间张量（$O(B \times N \times K)$），导致内存峰值。
    *   **核心思想**：将“先实例化再归约”的模式改为“融合收缩（Fused Contraction）”，直接在计算过程中进行累加，将内存复杂度降低到 $O(N \times K)$，消除了 90% 以上的冗余内存分配。
*   **自动混合精度搜索 (Automatic Mixed-Precision Search)**：
    *   提出一种三阶段搜索算法：
        1.  **精度感知阶段**：基于 JAX 框架，通过贪心策略识别对数值稳定性敏感的算子，在保证相对误差 $\epsilon \le 10^{-6}$ 的前提下，尽可能下放精度（如 fp16, TF32）。
        2.  **结构感知阶段**：探索计算图邻居节点，进一步优化未被第一阶段覆盖的算子。
        3.  **延迟感知阶段**：剔除那些因类型转换（Cast）开销过大而无法带来实际加速的精度下放。

### 3. 实验设计
*   **数据集/场景**：
    *   **Blender**：8 个合成物体场景（前向视角）。
    *   **Habitat**：3 个照片级室内环境（如 Van Gogh Room）。
    *   **Replica**：4 个室内扫描场景。
*   **Benchmark 与对比方法**：
    *   以 **PSNR（峰值信噪比）** 作为重建质量指标。
    *   对比方法包括：原始 3DGS（带回放和不带回放版本）、原始 VBGS 基线。

### 4. 资源与算力
*   **硬件平台**：
    *   **工作站级**：NVIDIA RTX A5000 GPU (24GB VRAM)。
    *   **边缘级**：NVIDIA Jetson Orin Nano (8GB 统一内存)。
*   **训练时长与效率**：
    *   在 A5000 上，单帧拟合延迟从 70.12s 降至 **18.33s**（加速约 4 倍）；200 帧总训练时间从 234 分钟降至 **61 分钟**。
    *   在 Jetson Orin Nano 上，原始 VBGS 因显存溢出（OOM）无法运行，优化后实现了 **180s/帧** 的训练速度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流数据集共 15 个不同复杂度的场景。
*   **消融实验**：对混合精度搜索的容差（Tolerance）进行了详细的扫参实验（$10^{-8}$ 到 $10^{-4}$），验证了精度分配与重建质量之间的平衡。
*   **充分性评价**：实验设计较为充分，不仅对比了算法性能，还深入分析了内存随时间变化的曲线，客观展示了内核融合对消除内存峰值的有效性。

### 6. 主要结论与发现
*   **内存大幅下降**：通过内核融合，峰值内存从 9.44 GB 降至 **1.11 GB**（降幅达 9 倍），使算法能够适配 8GB 甚至更小内存的设备。
*   **质量保持**：混合精度优化后的 PSNR 与全 fp64 的基线基本持平，甚至在某些场景下略有提升，证明了局部降低精度不会破坏变分推断的收敛性。
*   **边缘端可行性**：首次证明了在商用现货（COTS）嵌入式平台上进行无回放持续学习 NVS 训练的可行性。

### 7. 优点
*   **通用性强**：提出的混合精度搜索算法基于 JAX 的中间表示（jaxpr），理论上可推广到其他复杂的科学计算或深度学习任务。
*   **工程落地性高**：不改变数学公式，仅通过底层计算优化解决硬件适配问题，非常适合机器人工程应用。
*   **显著的资源节省**：在不牺牲质量的前提下，极大地降低了对高精度硬件（fp64 算力）的依赖。

### 8. 不足与局限
*   **实时性仍有差距**：虽然在边缘设备上跑通了训练，但 180s/帧 的速度对于需要即时反馈的快速移动机器人来说仍然较慢。
*   **框架依赖**：优化深度依赖于 JAX 和 XLA 编译器的特性，在其他框架（如 PyTorch）上的移植可能需要额外工作。
*   **场景复杂度限制**：对于极其精细的场景（如 Skokloster Castle），精度下放仍会导致轻微的质量损失，说明在极端细节重建上仍需谨慎权衡。

（完）
