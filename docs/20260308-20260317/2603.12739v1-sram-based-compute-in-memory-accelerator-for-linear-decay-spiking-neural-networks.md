---
title: SRAM-Based Compute-in-Memory Accelerator for Linear-decay Spiking Neural Networks
title_zh: 基于 SRAM 的线性衰减脉冲神经网络存内计算加速器
authors: "Hongyang Shang, Shuai Dong, Yahan Yang, Junyi Yang, Peng Zhou, Arindam Basu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12739v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于高能效SNN的SRAM存算一体加速器
tldr: 针对脉冲神经网络（SNN）中神经元状态更新导致的计算瓶颈，本文提出了一种基于SRAM的存内计算（CIM）加速器。通过在算法层将指数衰减简化为线性衰减，并在架构层引入存内并行更新机制，消除了全局顺序更新的开销。该方案在保持高精度的同时，显著降低了能耗并提升了处理速度，为低功耗实时类脑计算提供了高效的硬件支持。
selection_source: fresh_fetch
motivation: 传统SNN加速器中神经元膜电位状态的串行更新已成为制约推理速度和能效的主要瓶颈。
method: 采用线性衰减LIF模型简化计算，并设计了支持原位并行状态更新的SRAM存内计算架构。
result: 在精度损失极小的情况下，实现了最高16.7倍的SOP能耗降低和69倍的能效提升。
conclusion: 优化存内计算架构中的状态更新动力学对于实现高性能、低功耗的神经形态处理至关重要。
---

## 摘要
脉冲神经网络 (SNN) 作为一种受生物启发的传统深度网络替代方案，提供了事件驱动和高能效的计算。然而，其吞吐量仍受限于神经元膜电位状态的串行更新。虽然许多硬件加速器和存内计算 (CIM) 架构能够高效地并行化突触操作 (W x I)，使矩阵-向量乘法达到 O(1) 复杂度，但随后的状态更新步骤仍需 O(N) 时间来刷新所有神经元膜电位。这种不匹配使得状态更新成为 SNN 推理中主要的延迟和功耗瓶颈。为了应对这一挑战，我们提出了一种针对线性衰减漏电积分发放 (LD-LIF) 神经元 SNN 的基于 SRAM 的 CIM，实现了算法与硬件的协同优化。在算法层面，我们用线性衰减近似取代了传统的指数膜电位衰减，将昂贵的乘法转换为简单的加法，而准确率仅下降约 1%。在架构层面，我们引入了一种存内并行更新方案，直接在 SRAM 阵列内执行原位衰减，消除了全局顺序更新的需求。在基准 SNN 工作负载上的评估结果显示，所提方法实现了 1.1 倍至 16.7 倍的 SOP 功耗降低，同时提供了 15.9 倍至 69 倍的能效提升，且相对于原始衰减模型，准确率损失可忽略不计。本研究强调，除了加速 (W x I) 计算外，优化 CIM 架构内的状态更新动力学对于实现可扩展、低功耗和实时的类脑处理至关重要。

## Abstract
Spiking Neural Networks (SNNs) have emerged as a biologically inspired alternative to conventional deep networks, offering event-driven and energy-efficient computation. However, their throughput remains constrained by the serial update of neuron membrane states. While many hardware accelerators and Compute-in-Memory (CIM) architectures efficiently parallelize the synaptic operation (W x I) achieving O(1) complexity for matrix-vector multiplication, the subsequent state update step still requires O(N) time to refresh all neuron membrane potentials. This mismatch makes state update the dominant latency and energy bottleneck in SNN inference. To address this challenge, we propose an SRAM-based CIM for SNN with Linear Decay Leaky Integrate-and-Fire (LD-LIF) Neuron that co-optimizes algorithm and hardware. At the algorithmic level, we replace the conventional exponential membrane decay with a linear decay approximation, converting costly multiplications into simple additions while accuracy drops only around 1%. At the architectural level, we introduce an in-memory parallel update scheme that performs in-place decay directly within the SRAM array, eliminating the need for global sequential updates. Evaluated on benchmark SNN workloads, the proposed method achieves a 1.1 x to 16.7 x reduction of SOP energy consumption, while providing 15.9 x to 69 x more energy efficiency, with negligible accuracy loss relative to original decay models. This work highlights that beyond accelerating the (W x I) computation, optimizing state-update dynamics within CIM architectures is essential for scalable, low-power, and real-time neuromorphic processing.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于 SRAM 的存内计算（CIM）加速器，专门用于优化脉冲神经网络（SNN）中的神经元状态更新过程。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 SNN 硬件加速中，突触权重与输入的乘加运算（$W \times I$）可以通过存内计算实现 $O(1)$ 的并行度，但神经元膜电位的**状态更新（State Update）**通常仍需串行处理，复杂度为 $O(N)$。
*   **研究动机**：这种计算密度的不匹配导致状态更新成为 SNN 推理中的主要延迟和能耗瓶颈。此外，传统漏电积分发放（LIF）模型采用指数衰减，涉及复杂的乘法运算，不利于硬件的高效实现。

### 2. 方法论
论文通过“算法-硬件协同设计”提出了 **LD-LIF（线性衰减 LIF）** 模型及其对应的 **SRAM-CIM 架构**：
*   **算法层面（线性衰减近似）**：
    *   将传统的指数衰减 $V_{mem}[t+1] = \alpha V_{mem}[t]$ 替换为线性衰减 $V_{mem}[t+1] = V_{mem}[t] - \beta$。
    *   核心思想是将昂贵的乘法转换为简单的减法。
    *   引入**可学习的衰减参数 $\beta$**，每一层共享一个 $\beta$，通过反向传播优化以弥补线性近似带来的精度损失。
*   **硬件架构层面（存内并行更新）**：
    *   **单元设计**：采用 8T SRAM 存储膜电位，并集成处理元件（PE），包含全加器（FA）和多路复用器（MUX）。
    *   **乒乓更新机制**：设计了两个 SRAM 块（VMEM A 和 B），膜电位在两者之间交替流动，实现原位更新。
    *   **三周期更新流程**：
        1.  第一周期：$V_{init} + MAC \rightarrow V_{mid}$（累加突触输入）。
        2.  第二周期：$V_{mid} - (DCY + TH) \rightarrow V'_{mid}$（同时减去衰减值和阈值，利用符号位判断是否发放脉冲，省去比较器）。
        3.  第三周期：根据脉冲状态重置为 0 或写回最终值 $V_{final}$。

### 3. 实验设计
*   **数据集**：
    *   **N-MNIST**：用于 MLP-1 网络（2312-256-128-10）。
    *   **SHD (Spiking Heidelberg Digits)**：用于 MLP-2 网络（140-128-128-128-10）。
    *   **DVS Gesture**：用于 CNN 网络（两层卷积 + 全连接）。
*   **Benchmark（基准）**：传统的指数衰减 LIF 模型（v-LIF）。
*   **对比维度**：分类准确率、量化影响（3-bit/4-bit 权重）、硬件延迟、SOP（突触操作）能耗及整体能效（TOPS/W）。

### 4. 资源与算力
*   **硬件仿真**：使用 **TSMC 65nm CMOS 工艺**进行电路仿真。
*   **电压设置**：LD-LIF 模块运行在 0.9V，MAC 和 Scaler 模块分别运行在 0.72V 和 1.2V。
*   **算力说明**：论文未详细说明训练这些模型所使用的具体 GPU 型号或训练时长，重点在于推理阶段的硬件电路表现。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了从简单的 MLP 到 CNN 架构，以及从静态图像转换的脉冲数据到原生事件相机数据（DVS）。
*   **充分性**：
    *   进行了**精度对比实验**，证明线性衰减与指数衰减精度差距极小（约 1%）。
    *   进行了**量化实验**，验证了在低比特（W3A5/W4A6）情况下的鲁棒性。
    *   进行了**硬件对比实验**，与三篇近年的顶级期刊/会议（JSSC, VLSI, TCASAI）成果进行了横向对比。
*   **评价**：实验设计较为全面，从算法有效性到硬件优越性均有数据支撑，对比具有客观性。

### 6. 主要结论与发现
*   **精度保持**：LD-LIF 在三个数据集上的准确率损失仅为 0.38% - 1.11%。
*   **能效飞跃**：相比传统数字实现的 v-LIF，该架构能效提升了 5.2 倍；相比现有最先进技术（SOTA），SOP 能耗降低了 **1.1x - 16.7x**，能效（TOPS/W）提升了 **15.9x - 69x**。
*   **延迟优化**：通过并行更新，将 32 个神经元的更新延迟缩短至 21ns，比传统串行架构快约 30 倍。
*   **学习特性**：发现不同任务学习到的 $\beta$ 差异显著，甚至在某些任务中 $\beta$ 为负值（增加膜电位），显示了线性衰减模型的灵活性。

### 7. 优点
*   **协同优化**：不只是单纯改进硬件，而是通过简化算法逻辑（线性化）来适配高效的 SRAM 电路。
*   **原位计算**：在存储单元旁直接完成状态更新，极大地减少了数据搬运能耗。
*   **硬件开销低**：巧妙利用加法器符号位代替比较器，进一步节省了面积和功耗。

### 8. 不足与局限
*   **灵活性限制**：为了硬件效率，每一层共享同一个衰减参数 $\beta$，这可能限制了神经元异构动力学的表达能力。
*   **工艺节点**：实验基于 65nm 仿真，虽然进行了等效缩放对比，但在更先进工艺（如 7nm/5nm）下的漏电和可靠性挑战未深入讨论。
*   **应用范围**：目前主要验证了中小型网络，在超大规模深度 SNN（如脉冲 ResNet 或 Transformer）上的扩展性有待验证。

（完）
