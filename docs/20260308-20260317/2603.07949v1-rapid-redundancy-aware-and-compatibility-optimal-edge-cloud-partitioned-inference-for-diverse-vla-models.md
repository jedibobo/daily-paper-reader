---
title: "RAPID: Redundancy-Aware and Compatibility-Optimal Edge-Cloud Partitioned Inference for Diverse VLA models"
title_zh: RAPID：面向多样化 VLA 模型的冗余感知与兼容性最优边云划分推理
authors: "Zihao Zheng, Sicheng Tian, Hangyu Cao, Chenyue Li, Jiayu Chen, Maoliang Li, Xinhao Sun, Hailong Zou, Guojie Luo, Xiang Chen"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07949v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 针对资源受限边缘设备的边云分区推理
tldr: 针对具身智能中视觉语言动作（VLA）模型推理成本高的问题，本文提出RAPID框架。该框架采用边云协同推理，解决了现有方法易受视觉噪声干扰及忽视步骤间冗余导致动作不连续的挑战。RAPID通过感知冗余和优化兼容性进行边云分区，在保持物理连续性的同时，实现了显著的推理加速，为实时具身智能任务提供了高效解决方案。
selection_source: fresh_fetch
motivation: 现有边云协同推理方法在处理VLA模型时易受视觉噪声干扰，且忽视了具身任务的步骤间冗余，导致动作不连续和效率低下。
method: 提出RAPID框架，一种针对多样化VLA模型设计的冗余感知且兼容性最优的边云分区推理方法。
result: "实验结果显示，RAPID在仅产生5%至7%额外开销的前提下，实现了高达1.73倍的推理加速。"
conclusion: RAPID框架通过创新的边云分区策略，有效解决了VLA模型的推理瓶颈，提升了具身智能系统的实时响应能力。
---

## 摘要
视觉-语言-动作 (VLA) 模型是具身智能领域的主流模型，但面临高昂的推理成本。边云协同 (ECC) 推理通过缓解边缘设备的计算压力以满足实时性需求，提供了一种有效的解决方案。然而，现有的 ECC 框架对于 VLA 模型而言并非最优，主要面临两个挑战：(1) 主流的面向环境的边云划分方法容易受到视觉噪声的干扰；(2) 现有的边云划分方法忽视了具身任务特有的步进式冗余，从而破坏了动作的物理连续性。为了解决这些问题，我们提出了一种名为 RAPID 的新型 ECC 推理框架。具体而言，我们针对该框架开发了定制化的实现。实验表明，该方法在仅产生 5%~7% 开销的情况下，实现了高达 1.73 倍的加速。

## Abstract
Vision Language Action (VLA) models are mainstream in embodied intelligence but face high inference costs. Edge-Cloud Collaborative (ECC) inference offers an effective fix by easing edge-device computing pressure to meet real-time needs. However, existing ECC frameworks are suboptimal for VLA models due to two challenges: (1) Mainstream environment-oriented edge-cloud partitioning methods are susceptible to interference from visual noise; (2) Existing edge-cloud partitioning methods overlook the step-wise redundancy unique to embodied tasks, thereby disrupting the physical continuity of motion. To address these issues, we propose a novel ECC inference framework, termed RAPID. Specifically, we developed an implementation tailored to the proposed framework. Experiments demonstrate this achieves a speedup of up to 1.73x with only 5%~7% overhead.

---

## 论文详细总结（自动生成）

以下是对论文《RAPID: Redundancy-Aware and Compatibility-Optimal Edge-Cloud Partitioned Inference for Diverse VLA models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型是具身智能的主流架构，但其庞大的参数量导致在边缘设备上推理缓慢，难以满足机器人控制的实时性要求。
*   **现有方案局限性**：目前的边云协同（ECC）推理框架主要依赖“面向环境”的动态划分（如基于视觉特征的置信度或熵）。这种方法存在两大痛点：
    1.  **兼容性差**：极易受视觉噪声（光照变化、遮挡、无关移动物体）干扰，导致错误的卸载决策。
    2.  **忽视冗余**：未考虑具身任务中特有的“步进式冗余”（即某些动作阶段重要性低），导致频繁的动作中断和系统效率低下。
*   **研究目标**：提出一种不依赖视觉、能感知动作冗余且具备高兼容性的边云划分框架。

### 2. 方法论：核心思想与关键技术
RAPID 的核心思想是**利用运动学（Kinematic）特征代替视觉特征**作为边云划分的触发器。
*   **兼容性最优划分机制**：利用瞬时关节加速度（$\ddot{q}_t$）识别非线性运动突变（如避障、任务切换）。通过滑动窗口统计计算归一化的异常得分，对视觉噪声具有天然的免疫力。
*   **冗余感知划分机制**：研究发现关节扭矩（Torque）与 VLA 内部注意力权重（代表动作重要性）高度相关。在平滑接近阶段（高冗余），扭矩变化小，由边缘设备执行缓存动作；在关键交互阶段（低冗余），扭矩剧增，触发云端推理。
*   **双阈值融合策略**：引入动态相位权重 $\omega(t)$。在高流速运动时侧重加速度监控，在低速精细操作时侧重扭矩监控，合成“动作重要性得分”来指导划分。
*   **系统级优化**：
    *   **异步多速率处理**：传感器轮询（500Hz）与控制循环（20Hz）解耦，降低监控开销。
    *   **动作抢占与冷却机制**：一旦触发云端卸载，立即中止边缘旧动作并进入冷却期，防止网络拥塞。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：LIBERO 基准测试，涵盖 Pick & Place（取放）、Drawer Opening（开抽屉）、Peg Insertion（插拔）等任务。
    *   **现实环境**：在物理机械臂上进行真实部署验证。
*   **环境设置**：分为标准环境、视觉噪声环境（光照变化）、干扰环境（移动遮挡）。
*   **Benchmark（对比方法）**：
    1.  **Edge-Only**：全量模型在边缘运行。
    2.  **Cloud-Only**：全量模型在云端运行。
    3.  **Vision-Based (SAFE/ISAR)**：基于视觉熵的动态划分策略。

### 4. 资源与算力
*   **硬件平台**：实验使用了 **NVIDIA A100 GPU** 配对 **Intel Xeon Silver 4410T CPU**。
*   **模型负载**：OpenVLA 模型（约 14.2GB-14.5GB 参数量）。
*   **算力说明**：文中未详细说明训练时长（因为该研究侧重于推理阶段的系统框架优化），但明确指出边缘侧内存占用被压缩至约 2.4GB，其余负载由云端承担。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真和实物两大场景，针对三种典型具身任务进行了多轮测试。
*   **消融实验**：专门设计了针对加速度触发器（$\theta_{comp}$）和扭矩触发器（$\theta_{red}$）的消融实验，验证了双阈值机制的必要性。
*   **客观性**：通过引入不同等级的视觉噪声和干扰物，客观地展示了传统视觉方法在复杂环境下的失效，对比过程公平且具有说服力。

### 6. 主要结论与发现
*   **性能提升**：相比视觉基准方法，RAPID 实现了高达 **1.73倍** 的端到端系统加速。
*   **精度与稳定性**：在噪声环境下，准确率提升高达 **15.8%**，且延迟波动极小（约 11-15ms）。
*   **极低开销**：系统级监控和调度产生的额外开销仅为 **5%~7%**。
*   **关键发现**：运动学特征（加速度和扭矩）是识别 VLA 推理冗余的极佳“轻量级代理指标”，能有效替代计算昂贵的注意力权重分析。

### 7. 优点
*   **鲁棒性强**：彻底摆脱了对视觉特征的依赖，解决了 ECC 领域长期存在的视觉噪声干扰问题。
*   **物理连续性好**：通过冗余感知减少了不必要的云端请求，保证了机器人动作的流畅性。
*   **系统实现优雅**：异步多速率架构和抢占机制非常贴合工业机器人控制的实际需求。

### 8. 不足与局限
*   **超参数敏感性**：双阈值（$\theta_{comp}, \theta_{red}$）的设定依赖经验，不同类型的机器人或任务可能需要重新调优。
*   **传感器依赖**：该方法高度依赖高精度的关节编码器和力矩传感器，对于缺乏高质量运动学反馈的低成本机器人可能效果受限。
*   **应用范围**：目前主要针对单臂操纵任务，在多机协作或更复杂的全身控制任务中的表现尚待验证。

（完）
