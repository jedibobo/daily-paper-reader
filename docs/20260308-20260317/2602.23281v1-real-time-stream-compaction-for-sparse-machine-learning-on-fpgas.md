---
title: Real-Time Stream Compaction for Sparse Machine Learning on FPGAs
title_zh: FPGA上稀疏机器学习的实时流压缩
authors: "Marc Neu, Isabel Haide, Torben Ferber, Jürgen Becker"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23281v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 基于FPGA的GNN硬件加速与延迟优化
tldr: 针对对撞机实验中图神经网络（GNN）在FPGA触发器上的高吞吐量和低延迟需求，本文提出了一种实时流压缩预处理方案。该方案通过分层稀疏压缩流水线，将大量传感器前端的稀疏数据重新排列并压缩至较少的接口，有效消除了动态输入稀疏性。该工具基于Chisel开发并开源，在Belle II探测器触发器中得到验证，显著提升了硬件加速效率。
selection_source: carryover_cache
motivation: 现有的FPGA触发器难以处理GNN带来的高计算需求，且传感器数据的动态稀疏性限制了硬件加速器的效率。
method: 开发了一种基于Chisel的分层稀疏压缩流水线，将大量输入FIFO的数据重组并压缩到少数输出接口中。
result: 在Belle II电磁量能器触发器中成功应用，并展示了在延迟、吞吐量和资源利用率方面的优异性能与可扩展性。
conclusion: 该开源硬件生成器为大规模科学实验中的稀疏数据预处理提供了高效、低延迟的硬件加速解决方案。
---

## 摘要
机器学习算法在对撞机实验的一级触发器中应用日益广泛，其中图神经网络（GNN）对基于FPGA的触发器的硬件要求已超出现有技术水平。为了满足高吞吐量和低延迟环境的严苛需求，我们提出了一种针对稀疏传感器数据进行延迟优化的预处理方案，通过消除动态输入稀疏性来实现高效的GNN硬件加速。我们的方法将来自大量先进先出（FIFO）接口（通常为传感器前端）的数据重新排列到连接至机器学习硬件加速器的较少数量的FIFO接口中。为了在最小化硬件资源占用的同时实现高吞吐量，我们开发了一种针对FPGA优化的分层稀疏压缩流水线。我们使用Chisel设计语言将该方案实现为一个开源硬件生成器。作为演示，我们将该模块的一个配置实现为Belle II探测器电磁量能器中基于GNN的一级触发器的预处理阶段。此外，我们评估了广泛参数下的延迟、吞吐量、资源利用率和可扩展性，以便在其他大规模科学实验中得到更广泛的应用。

## Abstract
Machine learning algorithms are being used more frequently in the first-level triggers in collider experiments, with Graph Neural Networks pushing the hardware requirements of FPGA-based triggers beyond the current state of the art. To meet the stringent demands of high-throughput and low-latency environments, we propose a concept for latency-optimized preprocessing of sparse sensor data, enabling efficient GNN hardware acceleration by removing dynamic input sparsity. Our approach rearranges data coming from a large number of First-In-First-Out interfaces, typically sensor frontends, to a smaller number of FIFO interfaces connected to a machine learning hardware accelerator. In order to achieve high throughput while minimizing the hardware utilization, we developed a hierarchical sparsity compression pipeline optimized for FPGAs. We implemented our concept in the Chisel design language as an open-source hardware generator. For demonstration, we implemented one configuration of our module as preprocessing stage in a GNN-based first-level trigger for the Electromagnetic Calorimeter inside the Belle II detector. Additionally we evaluate latency, throughput, resource utilization, and scalability for a wide range of parameters, to enable broader use for other large scale scientific experiments.

---

## 论文详细总结（自动生成）

以下是对论文《Real-Time Stream Compaction for Sparse Machine Learning on FPGAs》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在高能物理实验（如Belle II对撞机）的一级触发器中，图神经网络（GNN）因其 $O(N^2)$ 的计算复杂度，难以满足极低延迟（微秒级）和高吞吐量（每秒千万次快照）的硬件要求。
*   **研究背景**：探测器数据（如电磁量能器ECL）具有极高的**稀疏性**（大部分传感器信号为零）。如果直接处理原始稀疏数据，会浪费大量FPGA计算资源。
*   **整体含义**：本文提出了一种实时流压缩（Stream Compaction）预处理模块，旨在将大量稀疏的输入接口数据压缩到少数几个稠密的输出接口中，从而消除动态稀疏性，显著降低后续机器学习加速器的计算负担。

### 2. 方法论
*   **核心思想**：采用分层树状拓扑结构，通过多级“流压缩单元”将 $N_I$ 个输入端口的数据汇聚到 $N_O$ 个输出端口。
*   **关键技术细节**：
    *   **分层架构**：每个单元将 $2 \cdot N_O$ 个输入压缩为 $N_O$ 个输出，通过级联形成树状结构。
    *   **三级流水线设计**：
        1.  **预取阶段**：加载数据到寄存器。
        2.  **地址计算阶段**：利用有效信号生成位掩码，通过前缀和（Prefix Sum）和链式优先级编码器（Priority Encoder）确定非零元素的输出位置。
        3.  **转发阶段**：配置交叉开关（Crossbar）将非零数据定向到正确的输出端口。
*   **确定性延迟**：设计保证了处理延迟是确定的，公式为 $L = 3 \cdot \lceil \log_2(1 + N_I/N_O) \rceil / f_{sys}$，这对于硬实时触发系统至关重要。
*   **开发工具**：使用 Chisel 硬件描述语言开发，实现为一个可参数化的硬件生成器。

### 3. 实验设计
*   **应用场景**：Belle II 探测器的一级触发器系统中的 GNN-ETM 模块。
*   **数据集/输入**：Belle II 电磁量能器（ECL）的真实传感器数据（576个触发单元TC）。
*   **Benchmark/对比对象**：
    *   对比了**朴素方法**（不进行压缩直接处理所有输入）。
    *   评估了不同配置参数（输入端口 $N_I$ 为64/128/256，输出端口 $N_O$ 为2/4/8）下的性能。
*   **评估指标**：资源利用率（LUT、FF、CLB）、系统频率（MHz）、延迟（ns）。

### 4. 资源与算力
*   **硬件平台**：针对 **AMD UltraScale XCVU190 FPGA**（Belle II 通用触发板 UTB4 上的芯片）进行优化。
*   **算力说明**：文中未涉及 GPU 训练，重点在于 FPGA 上的推理预处理逻辑。
*   **资源占用**：资源消耗随输入端口数线性增长。例如，在 $N_I=256, N_O=8$ 的配置下，LUT 占用率约为 10-15%。
*   **频率**：最高可达 500 MHz（在简单配置下），复杂配置下频率降至约 277 MHz。

### 5. 实验数量与充分性
*   **实验规模**：共测试了 **9 组不同的硬件配置**，涵盖了不同的输入输出比例。
*   **充分性评价**：
    *   **功能验证**：通过 ModelSim 进行了 RTL 级仿真。
    *   **实战验证**：该模块已实际部署在 Belle II 的 GNN-ETM 触发器中，证明了其在真实物理实验环境中的可行性。
    *   **客观性**：提供了详细的资源扩展性曲线和频率变化图，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **计算负载降低**：与朴素方法相比，该压缩模块将后续 GNN 推理的计算负载降低了 **324 倍**。
*   **极低延迟**：在 Belle II 的实际部署中，该模块引入的延迟开销**低于 60 ns**，远低于 4.4 $\mu$s 的总延迟预算。
*   **可扩展性**：证明了该硬件生成器可以灵活适配不同规模的探测器数据需求。

### 7. 优点
*   **开源贡献**：提供了基于 Chisel 的开源硬件生成器，具有高度的可重用性。
*   **硬实时性**：流水线设计无停顿（Stall-free），且延迟完全可预测。
*   **显著提效**：通过简单的预处理逻辑极大地释放了后续复杂模型（如 GNN）的计算压力。

### 8. 不足与局限
*   **频率瓶颈**：随着输出端口 $N_O$ 的增加，交叉开关（Crossbar）的复杂度导致系统最高频率明显下降，这可能限制其在极高性能需求场景下的扩展。
*   **硬件平台局限**：实验主要针对 AMD UltraScale 系列，未讨论在其他厂商（如 Intel/Altera）或更现代的 FPGA 架构上的移植表现。
*   **数据依赖性**：虽然处理了稀疏性，但如果输入数据突然变得稠密（如背景噪声激增），输出端口可能会成为瓶颈导致数据丢失（尽管文中提到通过 FIFO 缓冲缓解）。

（完）
