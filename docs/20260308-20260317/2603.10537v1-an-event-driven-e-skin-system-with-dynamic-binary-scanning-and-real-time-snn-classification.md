---
title: An Event-Driven E-Skin System with Dynamic Binary Scanning and real time SNN Classification
title_zh: 一种具有动态二分扫描和实时 SNN 分类的事件驱动电子皮肤系统
authors: "Gaishan Li, Zhengnan Fu, Anubhab Tripathi, Junyi Yang, Arindam Basu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10537v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 集成感知与类脑计算的硬件系统
tldr: "该研究提出了一种集成感知与类脑计算的事件驱动型电子皮肤系统。通过16x16压阻式触觉阵列和动态二进制扫描策略，实现了极高的数据稀疏性和压缩率。结合FPGA上的卷积脉冲神经网络（Conv-SNN），在大幅降低计算量和存储需求的同时，实现了92.11%的实时手写数字识别准确率，为机器人感知提供了高效方案。"
selection_source: fresh_fetch
motivation: 旨在解决传统电子皮肤在高速采样中面临的数据采集开销大和计算资源消耗高的问题。
method: 采用事件驱动的二进制扫描搜索策略进行数据采集，并利用FPGA实现的卷积脉冲神经网络进行实时分类。
result: "实现了12.8倍的扫描次数减少、99%的数据稀疏度，且在手写数字识别任务中达到92.11%的准确率。"
conclusion: 该系统展示了从模拟感知到类脑分类的完整事件驱动流水线，为高效机器人感知和人机交互提供了有力支持。
---

## 摘要
本文提出了一种用于高速、基于事件稀疏采样的电子皮肤（e-skin）的新型硬件系统，该系统集成了传感与类脑计算。该系统围绕一个带有前端的 16x16 压阻式触觉阵列构建，并引入了一种基于事件的二分扫描搜索策略来对数字进行分类。这种事件驱动策略实现了扫描次数减少 12.8 倍、数据压缩率达 38.2 倍、等效动态范围提升 28.4 倍以及 99% 的数据稀疏度，从而大幅降低了数据采集开销。生成的稀疏数据流由在 FPGA 上实现的多层卷积脉冲神经网络（Conv-SNN）进行处理，与 CNN 相比，其仅需 65% 的计算量和 15.6% 的权重存储。尽管效率显著提升，该系统在实时手写数字识别中仍保持了 92.11% 的高分类准确率。此外，本文还构建了一个使用地址事件表示（AER）的真实类脑触觉数据集。这项工作展示了一个从模拟传感至类脑分类的完整集成、事件驱动流水线，为机器人感知和人机交互提供了一种高效的解决方案。

## Abstract
This paper presents a novel hardware system for high-speed, event-sparse sampling-based electronic skin (e-skin)that integrates sensing and neuromorphic computing. The system is built around a 16x16 piezoresistive tactile array with front end and introduces a event-based binary scan search strategy to classify the digits. This event-driven strategy achieves a 12.8x reduction in scan counts, a 38.2x data compression rate and a 28.4x equivalent dynamic range, a 99% data sparsity, drastically reducing the data acquisition overhead. The resulting sparse data stream is processed by a multi-layer convolutional spiking neural network (Conv-SNN) implemented on an FPGA, which requires only 65% of the computation and 15.6% of the weight storage relative to a CNN. Despite these significant efficiency gains, the system maintains a high classification accuracy of 92.11% for real-time handwritten digit recognition. Furthermore, a real neuromorphic tactile dataset using Address Event Representation (AER) is constructed. This work demonstrates a fully integrated, event-driven pipeline from analog sensing to neuromorphic classification, offering an efficient solution for robotic perception and human-computer interaction.

---

## 论文详细总结（自动生成）

以下是对论文《An Event-Driven E-Skin System with Dynamic Binary Scanning and real time SNN Classification》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：触觉感知对于机器人灵巧操作和人机交互至关重要。压阻式传感器阵列是主流技术，但传统基于帧（Frame-based）的逐点扫描方式存在严重瓶颈。
*   **核心问题**：
    1.  **冗余度高**：传统扫描不考虑触觉信号的空时稀疏性，产生大量无用数据，导致高功耗和传输开销。
    2.  **架构不匹配**：即便前端实现了事件驱动采样，后端往往仍需将其重构为密集帧供传统人工神经网络（ANN）处理，抵消了稀疏性带来的优势。
*   **研究动机**：开发一种从底层硬件扫描到顶层类脑计算完全集成的端到端“事件驱动”电子皮肤系统，以实现极低能耗和高效实时感知。

### 2. 方法论：核心思想与关键技术
*   **硬件架构**：系统由 16×16 压阻式传感器阵列、模拟前端（AFE）、FPGA 和 PC 组成。
*   **事件驱动二分扫描策略（核心创新）**：
    *   **监测模式**：平时所有行开关开启，监测列电流。若某列电流变化超过阈值，判定存在触觉事件。
    *   **二分查找**：一旦检测到事件，利用二分搜索算法快速排除非活跃区域，精确定位触点。
    *   **采样重分配**：锁定触点后，对该点周围 3×3 区域进行 120Hz 的高频采样，而非全阵列扫描。
*   **数据编码**：采用 **Delta 调制（Delta Modulation）** 将模拟电压信号转换为三值脉冲（-1, 0, +1），生成地址事件表示（AER）流。
*   **分类算法**：在 FPGA 上实现 **卷积脉冲神经网络（Conv-SNN）**。
    *   结构包含两层卷积层和一层全连接层。
    *   利用脉冲的稀疏性实现“零跳过”计算，仅在有脉冲时更新神经元状态（LIF 模型）。

### 3. 实验设计
*   **数据集**：自建了一个真实的类脑触觉数据集。邀请 13 名参与者在传感器上书写数字 1-9，共收集 760 个样本。
*   **场景**：实时手写数字识别。
*   **Benchmark 与对比方法**：
    *   **扫描策略对比**：对比了传统行列扫描（Row-Column Scan）和开关扫描（Switch Scan）。
    *   **网络架构对比**：对比了传统 CNN、普通 SNN 以及本文的 Conv-SNN。
    *   **消融实验**：研究了不同 Delta 阈值（压缩率）对识别准确率的影响。

### 4. 资源与算力
*   **硬件平台**：算法部署在 FPGA 上实现实时推理。
*   **算力细节**：文中未明确给出具体的 FPGA 型号或 GPU 训练时长，但提供了计算开销的量化对比：
    *   **计算量**：Conv-SNN 的前向传播计算量仅为 CNN 的 65%。
    *   **存储**：权重存储需求仅为 CNN 的 15.6%（得益于 5-bit 量化和脉冲架构）。

### 5. 实验数量与充分性
*   **实验规模**：使用了 760 个真实采集的触觉样本，按 4:1 划分训练集和测试集。
*   **充分性评价**：
    *   实验涵盖了从底层物理扫描效率到顶层分类准确率的全方位评估，逻辑闭环。
    *   **客观性**：通过对比不同网络在相同参数量下的表现，证明了 Conv-SNN 在处理稀疏触觉数据时的优越性。
    *   **局限性**：样本量（760）相对于深度学习领域较小，且任务仅限于数字识别，尚未测试更复杂的纹理或多点交互场景。

### 6. 主要结论与发现
*   **高效采集**：二分扫描策略使扫描次数减少了 12.8 倍，数据稀疏度达到 99%，压缩率达 38.2 倍。
*   **性能卓越**：在大幅压缩数据的情况下，Conv-SNN 实现了 **92.11%** 的识别准确率，优于 CNN（89%）和普通 SNN（86.18%）。
*   **动态范围**：该策略使等效动态范围（DR）提升了 28.4 倍。
*   **鲁棒性**：实验发现适当提高 Delta 阈值具有去噪效果，能进一步提升识别准确率（最高可达 93.4%）。

### 7. 优点
*   **端到端集成**：真正实现了从模拟感知、硬件扫描到类脑计算的深度融合，避免了数据格式转换的浪费。
*   **极高稀疏性**：99% 的数据稀疏度极大地降低了边缘设备的功耗和带宽压力。
*   **硬件友好**：Conv-SNN 对权重存储和计算量的极低要求，使其非常适合部署在资源受限的嵌入式机器人系统中。

### 8. 不足与局限
*   **任务单一**：目前仅验证了手写数字识别，对于更复杂的机器人操作任务（如物体抓取、滑动检测）尚需进一步验证。
*   **数据集限制**：自建数据集规模有限，且缺乏与其他公开触觉数据集的横向对比。
*   **硬件细节缺失**：论文未详细列出 FPGA 的资源占用率（如 LUT、BRAM、功耗具体数值），难以评估其在特定芯片上的移植性。

（完）
