---
title: "STELLA: Efficient Sensor-to-LLM Translation for On-Device Human Activity Recognition"
title_zh: STELLA：面向设备端人体活动识别的高效传感器到大语言模型翻译框架
authors: "Nirhoshan Sivaroopan, Albert Zomaya, Kanchana Thilakarathna"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03089v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 边缘设备上高效的端侧HAR与传感器标记化
tldr: 针对人体活动识别（HAR）在边缘设备上部署大语言模型（LLM）时面临的提示词过长、延迟高及隐私风险等问题，本文提出STELLA框架。该框架通过轻量级分层分词器将传感器数据压缩为紧凑标记，并输入至冻结的LLM中，实现了高效的传感器到语言的转换。STELLA支持端侧个性化适配，在保持实时推理的同时，显著提升了识别精度和隐私保护能力，为端侧大模型应用提供了新思路。
selection_source: fresh_fetch
motivation: 现有的基于大语言模型的人体活动识别方法在边缘设备上面临推理延迟高、隐私风险大以及模型微调导致通用性丧失等部署难题。
method: 提出STELLA框架，利用轻量级分层分词器将多通道惯性数据压缩为潜在标记，并将其投影到冻结的预训练LLM嵌入空间中进行标签评分。
result: "在七个公开数据集上取得SOTA性能，F1分数最高提升11.83%，且端侧个性化适配可使F1分数进一步提升21.91%。"
conclusion: STELLA证明了高效的传感器分词技术是实现准确、私密且可个性化的端侧大模型人体活动识别的有效路径。
---

## 摘要
人体活动识别（HAR）正日益被期望在边缘设备上持续运行，然而近期基于大语言模型（LLM）的方法仍难以部署：原始传感器提示词过长，云端推理增加了延迟和隐私风险，且微调后的 LLM 流水线将通用模型转变成了特定任务的分类器。我们提出了 STELLA，一种用于设备端 HAR 的高效传感器到 LLM 翻译框架，它将负担从 LLM 适配转移到了传感器分词（tokenization）上。一个轻量级的层次化分词器将整个多通道惯性窗口压缩为一组固定的紧凑潜传感器标记（latent sensor tokens），这些标记被投影到冻结的预训练 LLM 的嵌入空间中，并与自然语言提示词结合进行标签评分。这在保留与活动相关的时序和跨通道结构的同时，使 LLM 端的计算在不同传感器配置下保持可预测性。STELLA 还支持设备端个性化，仅在少量用户特定的标注数据上适配轻量级分词器，并通过本地检索上下文增强推理，从而将 LLM、用户数据和检索过程保留在设备上。在七个公开 HAR 数据集和八个基准设置中，STELLA 实现了新的最先进性能，比现有方法提升了高达 11.83% 的 F1 分数；随着部署后用户数据的积累，设备端个性化可进一步提升高达 21.91% 的 F1 分数。在相同的 LLM 流水线下，STELLA 也优于代表性的时间序列分词器，并在实际的移动和边缘预算下实现了实时推理，表明高效的传感器分词是通往边缘设备上准确、私密且个性化的基于 LLM 的 HAR 的切实路径。

## Abstract
HAR is increasingly expected to run continuously on edge devices, yet recent LLM-based methods remain hard to deploy: raw sensor prompts are long, cloud inference adds latency and privacy risk, and fine-tuned LLM pipelines turn general-purpose models into task-specific classifiers. We present STELLA, an efficient sensor-to-LLM translation framework for on-device HAR that shifts the burden from LLM adaptation to sensor tokenization. A lightweight hierarchical tokenizer compresses an entire multi-channel inertial window into a fixed set of compact latent sensor tokens, which are projected into the embedding space of a frozen pretrained LLM and combined with a natural-language prompt for label scoring. This preserves activity-relevant temporal and cross-channel structure while keeping LLM-side computation predictable across sensor configurations. STELLA also supports on-device personalization, adapting only the lightweight tokenizer on small amounts of user-specific labelled data and augmenting inference with a local retrieval context, keeping the LLM, user data, and retrieval on device. Across seven public HAR datasets and eight benchmark settings, STELLA achieves new state-of-the-art performance, improving over prior methods by up to 11.83% F1; on-device personalization yields up to a further 21.91% F1 as user data accumulates after deployment. STELLA also outperforms representative time-series tokenizers under the same LLM pipeline and achieves real-time inference under practical mobile and edge budgets, showing that efficient sensor tokenization is a practical path toward accurate, private, and personalized LLM-based HAR on edge devices.

---

## 论文详细总结（自动生成）

这是一份关于论文《STELLA: Efficient Sensor-to-LLM Translation for On-Device Human Activity Recognition》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：人体活动识别（HAR）在健康监测和健身追踪中至关重要。近期研究尝试引入大语言模型（LLM）以利用其语义推理能力，但面临三大挑战：
    1.  **表示失配**：传感器数据是连续浮点流，而 LLM 擅长处理离散文本标记，直接序列化会导致提示词过长，增加计算开销。
    2.  **部署困难**：微调 LLM 会使其变成特定任务的分类器，失去通用性，且边缘设备难以承载多个专用大模型。
    3.  **隐私与延迟**：云端推理存在隐私泄露风险且网络延迟不可控。
*   **核心问题**：如何在资源受限的边缘设备上，既保留预训练 LLM 的推理能力，又能高效、私密地处理长序列传感器数据？

### 2. 论文提出的方法论：STELLA 框架
*   **核心思想**：将负担从“LLM 适配”转移到“传感器分词（Tokenization）”。通过一个轻量级的传感器分词器将原始信号翻译为 LLM 可理解的紧凑标记，同时保持 LLM 冻结。
*   **关键技术细节**：
    1.  **STELLA-Tower（分词器）**：采用分层架构，包含卷积栈（提取局部特征）、RepMixer 块（高效的时间维度混合）和轻量级自注意力层（捕捉全局上下文）。
    2.  **多尺度标记聚合（PoolToN）**：从不同层级提取特征，通过线性插值将其压缩为固定数量（$N=16$）的潜在标记。
    3.  **投影与对齐**：使用两层 MLP 将传感器标记映射到 LLM 的嵌入空间，与自然语言提示词（Prompt）拼接。
    4.  **NLL 标签评分**：不使用自由文本生成，而是计算候选活动标签在 LLM 下的负对数似然（NLL）得分，选择得分最高者。
    5.  **端侧个性化**：部署后仅微调轻量级分词器（约占总参数 10%），并结合本地检索增强（RAG）来适配特定用户的动作风格。

### 3. 实验设计
*   **数据集**：涵盖 7 个公开 HAR 数据集（HHAR, PAMAP2, MHEALTH, GOTOV, SKODA, Opportunity, USC-HAD），涉及 6 到 60 个传感器通道。
*   **Benchmark 与对比方法**：
    *   **传统深度学习**：CNN, LSTM, Transformer, DeepConvLSTM 等。
    *   **LLM 相关方法**：HARGPT, SensorLLM, RAG-HAR, LLM4HAR 等。
    *   **时间序列分词器**：PatchTST, Chronos, TimesNet, iTransformer 等（在相同 LLM 流水线下对比）。
*   **评估协议**：采用留一法（LOSO）或特定数据集的训练/测试划分，确保评估的严谨性。

### 4. 资源与算力
*   **硬件环境**：
    *   **服务器端**：NVIDIA RTX 4090 GPU 用于训练和基准测试。
    *   **边缘端**：Jetson Xavier NX（边缘 AI 平台）和 Samsung S23 Ultra（旗舰智能手机）。
*   **算力开销**：
    *   模型总参数约 1.38 亿，其中 GPT-2 占 89.7%（冻结）。
    *   在 S23 Ultra 上，使用 GPU 加速（NNAPI）的推理延迟仅为 41.7ms。
    *   训练时长：文中提到在 USC-HAD 数据集上，每轮（epoch）训练约需 13.3 秒。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个基准设置上进行了全面测试，并针对 8 个设计轴（如分词器架构、LLM 训练策略、标记数量等）进行了深入的消融实验。
*   **充分性评价**：实验设计非常充分。作者不仅对比了最终精度，还详细分析了推理延迟、内存占用、量化影响以及采样率鲁棒性。通过跨多个不同传感器配置的数据集验证，证明了方法的通用性和客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：STELLA 在所有数据集上均达到 SOTA，F1 分数平均提升 3.6% 至 11.83%。
*   **配置无关性**：无论传感器通道多少，LLM 处理的标记数固定为 16，保证了推理成本的可预测性。
*   **个性化威力**：仅需少量用户数据进行端侧适配，最难识别受试者的 F1 分数可提升 21.91%。
*   **分词器关键性**：实验证明，专门设计的传感器分词器优于通用的时间序列编码器。

### 7. 优点
*   **架构优雅**：通过固定标记数量解耦了传感器复杂性与 LLM 计算成本。
*   **端侧友好**：支持冻结 LLM 运行，方便复用设备上已有的通用大模型服务。
*   **隐私保护**：个性化适配和检索过程完全在本地完成，数据不离端。
*   **实时性强**：在商用手机上即可实现实时推理，具有极高的实用价值。

### 8. 不足与局限
*   **LLM 依赖**：虽然使用了轻量级的 GPT-2，但对于极低功耗的嵌入式设备（如单片机），500MB+ 的模型体积依然过大。
*   **量化敏感性**：实验发现全模型 INT8 量化在某些数据集上会导致精度大幅下降，需要更精细的量化策略（如 GPTQ/AWQ）。
*   **标签依赖**：识别性能受限于预定义的候选标签集，对于完全未见过的活动类别，其零样本泛化能力仍有待进一步验证。

（完）
