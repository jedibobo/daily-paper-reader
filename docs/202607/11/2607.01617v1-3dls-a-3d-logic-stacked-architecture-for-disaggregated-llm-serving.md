---
title: "3DLS: A 3D Logic-Stacked Architecture for Disaggregated LLM Serving"
authors: "Jaehun Lee, In-Jun Jung, Joo-Young Kim"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01617v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 用于LLM服务的3D堆叠芯粒架构
tldr: 针对大语言模型推理中预填充与解码分离架构下的通信瓶颈，本文提出 3DLS 架构。该架构利用 3D 逻辑堆叠技术，将 KV 缓存传输与张量并行通信在物理上解耦，分别通过垂直和水平互连进行传输。实验表明，3DLS 有效消除了通信竞争，显著提升了吞吐量并降低了端到端延迟，为未来芯粒化 LLM 推理系统提供了物理隔离的设计准则。
selection_source: carryover_cache
motivation: 传统 2D/2.5D 架构中 KV 缓存传输与张量并行通信共享同一水平互连，导致严重的流量竞争并增加了解码延迟。
method: 提出一种 3D 逻辑堆叠芯粒架构 3DLS，通过垂直互连处理 KV 缓存传输，同时保留水平互连用于解码端的张量并行通信。
result: "与共享织网的平面基准相比，3DLS 实现了高达 1.49 倍的吞吐量提升和 60.2% 的端到端延迟降低。"
conclusion: 物理隔离通信流量是未来基于芯粒的 PD 分离式 LLM 推理系统设计中的重要原则。
---

## Abstract
Large language model (LLM) serving increasingly combines prefill-decode (PD) disaggregation with tensor parallelism (TP) to support large models and long contexts. In conventional 2D/2.5D chiplet architectures, layer-wise prefill-to-decode KV-cache transfer decode-side TP collectives share the same lateral die-to-die (D2D) interconnect, creating mixed-traffic contention on the decode critical path. This contention increases communication latency, prolongs token generation intervals, and degrades end-to-end serving performance. We propose 3DLS, a logic-on-logic 3D-stacked chiplet architecture that separates traffic classes by routing KV-cache transfers through vertical interconnects while preserving decode-side TP collectives on the lateral D2D fabric. 3DLS achieves up to 1.49$\times$ throughput and 60.2\% lower end-to-end (E2E) latency over the shared-fabric planar baseline, and still achieves up to 1.17$\times$ throughput and 31.4\% lower E2E latency over a workload-aware priority-managed planar baseline. These results highlight that physical isolation is an important design principle for future chiplet-based PD-disaggregated LLM serving systems.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **3DLS** 的 3D 逻辑堆叠架构，专门用于优化大语言模型（LLM）在“预填充-解码分离”（Prefill-Decode Disaggregation）场景下的推理性能。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：为了应对超大规模 LLM 和长上下文需求，业界常采用“预填充-解码分离”技术，并结合张量并行（TP）来提升效率。
*   **核心问题**：在传统的 2D/2.5D 芯粒（Chiplet）架构中，**KV 缓存传输**（从预填充池到解码池）与**解码端的张量并行集合通信**（如 AllReduce）共享相同的水平片间（D2D）互连。
*   **研究动机**：这种混合流量竞争会导致解码阶段的通信延迟增加，进而拉长 Token 生成间隔（TBT），降低端到端（E2E）服务性能。作者发现，单纯增加带宽或逻辑上的优先级管理无法从根本上消除物理链路上的排队冲突。

### 2. 方法论：3DLS 架构设计
*   **核心思想**：利用 3D 逻辑堆叠（Logic-on-Logic）实现**流量的物理隔离**。
*   **关键技术细节**：
    *   **垂直互连隔离**：将预填充池（Prefill Pools）置于顶层，解码池（Decode Pools）置于底层。层间的 KV 缓存传输通过垂直 3D 互连（如 TSV 或混合键合）完成。
    *   **水平互连保留**：解码端的张量并行集合通信（AllReduce）保留在底层的水平 2.5D D2D 织网（如 UCIe）上。
    *   **存储共享机制**：HBM 堆栈连接在底层，顶层预填充芯粒通过垂直链路读取权重。虽然增加了垂直跳数，但由于预填充是计算密集型，其延迟可通过流水线化掩盖。
    *   **非对称布局支持**：支持非对称的预填充与解码池比例（如 2:1 或 4:1），通过垂直链路精确对齐 TP 分片。

### 3. 实验设计
*   **数据集/场景**：
    *   使用 **Azure 公共数据集**中的对话（Conversation，解码主导）和代码（Code，预填充主导）追踪。
    *   测试模型包括：**LLaMA3-8B (TP=4)**、**LLaMA3-70B (TP=8)** 和 **OPT-175B (TP=16)**。
*   **对比基准（Benchmark）**：
    *   **Naive Planar**：传统的 2D/2.5D 共享织网架构。
    *   **PM-Planar**：具备优先级管理（QoS 感知调度和虚拟通道）的平面架构，尝试在逻辑上隔离流量。
*   **实验配置**：采用等带宽（Iso-bandwidth）配置，确保 3DLS 的性能提升源于路径隔离而非单纯的带宽增加。

### 4. 资源与算力
*   **算力说明**：论文基于**内部仿真器**进行评估，模拟了类似 Tesla Dojo D1 的硬件参数。
*   **具体参数**：峰值算力 261.12 TFLOPS，内存带宽 3.35 TB/s，D2D 带宽 512 GB/s。
*   **物理实现**：文中未提及实际的 GPU 训练时长，因为这是一篇关于硬件架构设计的论文，重点在于推理阶段的仿真模拟。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 种不同规模的模型（8B 到 175B）和 2 种典型工作负载进行了全面测试。
*   **维度覆盖**：涵盖了不同批处理大小（Batch Size）、上下文长度（1K 到 16K）以及输出 Token 长度（128 到 1024）的灵敏度分析。
*   **公平性**：通过等带宽对比和热包络（Thermal Envelope）初步检查（200 W/cm²），证明了实验设计的客观性和严谨性。

### 6. 主要结论与发现
*   **性能提升**：相比 Naive Planar，3DLS 实现了高达 **1.49 倍**的吞吐量提升和 **60.2%** 的端到端延迟降低。
*   **优于逻辑隔离**：即使对比经过优化的 PM-Planar，3DLS 仍能提升 **1.17 倍**吞吐量并降低 **31.4%** 的延迟。
*   **核心发现**：物理隔离是解决 PD 分离架构中流量竞争的最有效手段。逻辑上的优先级管理（PM-Planar）虽然有用，但受限于物理链路的共享，无法完全消除干扰。

### 7. 优点
*   **设计理念先进**：首次提出将 3D 堆叠用于 LLM 推理中的流量类型隔离，而非仅仅为了增加存储带宽。
*   **解决根源问题**：通过物理路径解耦，彻底消除了 KV 缓存传输对解码关键路径（AllReduce）的挤占。
*   **高扩展性**：架构设计考虑了高 TP 度数下的非对称资源分配，符合未来超大模型的发展趋势。

### 8. 不足与局限
*   **散热与功耗挑战**：虽然文中提到了热包络检查，但 3D 逻辑堆叠带来的热点堆叠和散热成本在实际量产中仍是巨大挑战。
*   **制造复杂性**：3D 堆叠涉及混合键合等先进封装技术，其良率（Yield）和测试成本（KGD 测试）在文中未做深入的经济性分析。
*   **仿真局限**：所有结论均基于仿真数据，缺乏在真实 3D 堆叠芯片上的流片验证。

（完）
