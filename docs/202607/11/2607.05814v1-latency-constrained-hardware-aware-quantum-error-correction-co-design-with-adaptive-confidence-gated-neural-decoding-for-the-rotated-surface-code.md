---
title: Latency-Constrained Hardware-Aware Quantum Error Correction Co-Design with Adaptive Confidence-Gated Neural Decoding for the Rotated Surface Code
title_zh: 针对旋转表面码的延迟受限、硬件感知量子纠错协同设计：采用自适应置信度门控神经解码
authors: Sumit Chongder
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05814v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 延迟约束的硬件感知协同设计
tldr: 针对量子纠错（QEC）中实时解码的延迟瓶颈，本文提出一种面向旋转表面码的自适应置信度门控解码框架。该框架采用两阶段推理：轻量级神经网络负责快速解码大部分综合征，仅将低置信度预测交给最小权重完美匹配（MWPM）算法精化。实验表明，该方法在保持高逻辑准确率的同时显著降低了平均解码成本，为实现容错量子计算提供了高效的硬件感知协同设计方案。
selection_source: fresh_fetch
motivation: 实时解码是量子纠错从近中尺度量子设备向容错量子计算扩展的主要瓶颈。
method: 提出一种两阶段解码框架，利用轻量级神经网络进行快速路径解码，并仅对低置信度结果使用MWPM进行精化。
result: "在仅需精化3.3%-6.2%综合征的情况下，逻辑准确率从99.21%提升至99.81%，且在普通CPU上实现了极高的解码吞吐量。"
conclusion: 该协同设计方案有效平衡了解码精度与延迟，为硬件受限下的实时量子纠错提供了可行的技术路径。
---

## 摘要
实时解码是从含噪声中等规模量子 (NISQ) 设备扩展到容错量子计算的主要瓶颈。我们提出了一种针对旋转表面码的自适应置信度门控解码框架，将解码视为一个两阶段推理问题。一个轻量级前馈神经网络对大多数伴随式测量执行快速路径解码，而只有低置信度的预测会被提交到最小权重完美匹配 (MWPM) 精炼阶段。我们使用 Stim 稳定器模拟器，在电路级去极化噪声下，对码距为 $d \in \{3,5,7,9,11\}$ 的旋转表面码进行了基准测试。评估表征了逻辑准确率、置信度控制的准确率-延迟权衡、解码吞吐量、单次延迟以及解码图资源扩展性。仅将 3.3%-6.2% 的伴随式路由到精炼阶段，在置信度阈值为 0.95 时，将逻辑准确率从纯神经基线的 99.21% 提高到 99.81%，而平均解码成本仅有界增加。在商用 CPU 硬件上，当批大小为 512 时，神经解码器吞吐量在 $4.6 \times 10^{5}$ 样本/秒附近达到饱和，这表明在码距 $d=7$ 之后，神经快速路径不再是主要的吞吐量瓶颈。我们发布了完整的基准测试流水线、训练好的模型、原始基准数据和源代码，并明确区分了经实验验证的贡献与更广泛的硬件感知 QEC 协同设计路线图，后者包括硬件受限的码发现、GPU 加速推理和多噪声优化，这些仍是未来的研究方向。

## Abstract
Real-time decoding is a major bottleneck in scaling quantum error correction (QEC) from noisy intermediate-scale quantum (NISQ) devices to fault-tolerant quantum computing. We present an adaptive confidence-gated decoding framework for the rotated surface code that treats decoding as a two-stage inference problem. A lightweight feed-forward neural network performs fast-path decoding for the majority of syndrome measurements, while only low-confidence predictions are escalated to a minimum-weight perfect matching (MWPM) refinement stage. We benchmark the framework on rotated surface codes with distances $d \in \{3,5,7,9,11\}$ under circuit-level depolarising noise using the Stim stabiliser simulator. The evaluation characterises logical accuracy, confidence-controlled accuracy-latency trade-offs, decoding throughput, per-shot latency, and decoding-graph resource scaling. Routing only 3.3%-6.2% of syndromes to the refinement stage improves logical accuracy from 99.21% for the neural-only baseline to 99.81% at a confidence threshold of 0.95 while incurring only a bounded increase in average decoding cost. Neural-decoder throughput saturates near $4.6 \times 10^{5}$ samples s$^{-1}$ at batch size 512 on commodity CPU hardware, indicating that the neural fast path is not the dominant throughput bottleneck beyond code distance $d=7$. We release the complete benchmarking pipeline, trained models, raw benchmark data, and source code, and explicitly distinguish the experimentally validated contributions from the broader hardware-aware QEC co-design roadmap, including hardware-constrained code discovery, GPU-accelerated inference, and multi-noise optimisation, which remain directions for future work.

---

## 论文详细总结（自动生成）

这篇论文针对量子纠错（QEC）中的实时解码瓶颈，提出了一种硬件感知的协同设计方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着量子计算规模扩大，如何在极短的量子相干时间内完成“伴随式（Syndrome）”测量到“错误修正”的实时解码。
*   **背景**：传统的最小权重完美匹配（MWPM）算法虽然准确但计算开销大；神经网络解码器推理速度快，但在处理罕见的、低置信度的错误模式时准确率不足。
*   **整体含义**：论文试图通过一种“两阶段”自适应架构，平衡解码的延迟（Latency）与逻辑准确率，为实现容错量子计算提供可落地的硬件协同设计思路。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用**自适应置信度门控解码（Adaptive Confidence-Gated Decoding）**。将解码视为一个两阶段推理任务：
    1.  **快速路径（Fast Path）**：使用轻量级前馈神经网络（FFNN）对大多数简单的伴随式进行极速解码。
    2.  **精化路径（Refinement Path）**：通过置信度阈值 $\tau$ 进行过滤。如果神经网络输出的最高概率（置信度）低于 $\tau$，则将该样本“升级”交给 MWPM 算法（使用 PyMatching 实现）进行精确处理。
*   **算法流程**：
    *   输入伴随式向量 $s$。
    *   神经网络预测逻辑错误概率分布，提取置信度 $c$。
    *   若 $c \ge \tau$，直接应用神经解码结果；否则，调用 MWPM 重新计算。
*   **协同设计框架**：提出了一个包含代码生成、噪声建模、电路综合和硬件约束优化的闭环路线图，旨在联合优化码结构、解码策略和硬件延迟。

### 3. 实验设计
*   **数据集/场景**：使用 **Stim** 模拟器生成的旋转表面码（Rotated Surface Code）存储实验数据。
*   **Benchmark**：以纯 MWPM 解码和纯神经解码作为基准。
*   **对比维度**：
    *   **码距（Distance）**：$d \in \{3, 5, 7, 9, 11\}$。
    *   **物理错误率（Physical Error Rate）**：$p \in [10^{-4}, 5 \times 10^{-3}]$。
    *   **置信度阈值**：$\tau \in \{0.60, 0.70, 0.80, 0.90, 0.95\}$。
    *   **推理批大小（Batch Size）**：1 到 512。

### 4. 资源与算力
*   **硬件环境**：实验明确指出是在**商用 CPU 硬件**（Commodity CPU）上运行的。
*   **GPU 使用情况**：论文**未涉及 GPU 加速推理**的实验结果，并将其列为未来的研究方向。
*   **训练细节**：使用了标准的交叉熵损失函数进行训练，但未详细列出具体的训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模**：每个（码距, 错误率）组合进行了 6 次独立试验，每次 5,000 次采样（共计 30,000 次测量/单元格）。
*   **充分性**：实验覆盖了从极低错误率到接近阈值的多个量级，并系统分析了吞吐量、延迟、准确率和资源扩展性。
*   **客观性**：通过多轴（码距、噪声、阈值、批大小）的联合表征，较为客观地展示了自适应路由的性能增益。

### 6. 主要结论与发现
*   **准确率提升**：在 $d=7$ 时，仅需将 3.3% - 6.2% 的困难样本路由至精化阶段，逻辑准确率即可从 99.21%（纯神经）提升至 99.81%（$\tau=0.95$）。
*   **吞吐量表现**：在 CPU 上，批处理大小为 512 时，神经解码器吞吐量可达 $4.6 \times 10^5$ 样本/秒。
*   **瓶颈转移**：当 $d > 7$ 时，神经快速路径不再是主要的吞吐量瓶颈，主要的延迟挑战转向了低置信度样本的尾部延迟。
*   **非单调性**：在极高噪声（$p=0.005$）下，观察到逻辑错误率随码距增加呈现非单调变化，这可能与固定容量神经网络的表达能力有关。

### 7. 优点
*   **实用性强**：提出的置信度门控机制非常适合硬件部署，能以极小的平均成本换取接近最优的解码精度。
*   **多维度表征**：不仅关注准确率，还深入分析了硬件部署关心的吞吐量、批处理效应和解码图资源扩展性。
*   **开源贡献**：提供了完整的基准测试流水线、模型和原始数据，具有很高的可重复性。

### 8. 不足与局限
*   **噪声模型单一**：仅使用了电路级去极化噪声，未测试相干噪声、泄漏错误或真实设备的关联噪声。
*   **码族限制**：实验仅限于旋转表面码，未验证其在 QLDPC 码或颜色码上的泛化性。
*   **硬件评估不全**：缺乏 GPU 或 FPGA 的实际加速数据，且未对精化阶段（MWPM）的单次延迟分布进行独立剖析。
*   **架构固定**：未进行神经网络架构搜索（NAS），当前的非单调性结果可能受限于特定的网络设计。

（完）
