---
title: "OmniPilot: An Uncertainty-Aware LLM Inference Advisor for Heterogeneous GPU Clusters"
title_zh: OmniPilot：一种用于异构 GPU 集群的不确定性感知 LLM 推理建议器
authors: "D. Balamurugan, Thomas W. Bush"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01579v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 异构集群下硬件感知的LLM推理配置
tldr: OmniPilot是一个针对异构GPU集群的LLM推理启动建议工具。它通过结合保形校准的分位数成本模型和离群值（OOD）弃权层，在考虑GPU类型、张量并行度和精度等复杂变量的情况下，准确预测推理成本并优化配置选择。该工具能有效应对硬件故障和集群波动，显著提升了资源利用率和配置决策的准确性。
selection_source: carryover_cache
motivation: 在异构GPU集群上部署LLM时，由于硬件性能、并行策略和量化效果的复杂交互，用户难以选择最优的推理配置。
method: 提出OmniPilot框架，利用保形校准的分位数成本模型预测多项指标，并引入OOD弃权层以识别低置信度的预测场景。
result: "在A100/H100/H200等硬件上的评估显示，吞吐量预测误差仅为6.2%，Top-1配置推荐准确率达到95%。"
conclusion: OmniPilot通过不确定性感知和经济效用度量，为异构集群中的LLM推理提供了高效且可靠的自动化配置建议。
---

## 摘要
在共享的异构 GPU 集群上部署大语言模型（LLM）服务，要求用户和运营商在投入宝贵的节点机时之前，预先选择 GPU 类型、张量并行度（tensor-parallel degree）和精度。做出这些选择具有挑战性，因为有效吞吐量、启动成功率以及集群需求和利用率在不断波动。此外，静态配置方案忽略了关键的相互作用：量化效果在很大程度上取决于模型系列，键值（KV）缓存压力导致了模型大小与精度之间的权衡，且不同张量并行度下的故障率差异可达两倍以上。此外，集群资源经常受到不可预测的硬件故障的限制。为了应对这些挑战，我们提出了 OmniPilot，这是一种启动建议器，它能够预测可行配置的服务成本，并在请求超出其测量的支持范围时选择弃权。OmniPilot 将保形校准的分位数成本模型（覆盖八个服务目标）与分布外（OOD）弃权层相结合。它使用根据运营商显性偏好校准的经济效用指标对配置进行排名。在 A100、H100 和 H200 硬件上跨四种精度的 460 次基准测试运行评估中，OmniPilot 预测的总吞吐量平均绝对百分比误差（MAPE）为 6.2%，对数空间 R^2 为 0.92。该建议器实现了 95% 的 top-1 准确率，平均效用遗憾（utility regret）仅为 0.003。在对不支持单元的 OOD 留出集进行测试时，预测误差攀升至 24-46%，且保形区间未能覆盖 5 个测试点中的任何一个；然而，弃权层成功地将所有 5 个点标记为低置信度。随着时间的推移，这些 OOD 场景将被整合到训练数据集中，以不断扩大建议器的支持范围。

## Abstract
Serving large language models (LLMs) on a shared, heterogeneous GPU cluster requires users and operators to select the GPU type, tensor-parallel degree, and precision before committing valuable node-hours. Making these choices is challenging because effective throughput, launch-success rates, and cluster demand and utilization continuously fluctuate. Furthermore, static configuration recipes miss critical interactions: quantization effects depend heavily on the model family, key-value cache pressure creates size-by-precision trade-offs, and failure rates vary by more than twofold across different tensor-parallel degrees. Additionally, cluster resources are frequently constrained by unpredictable hardware failures. To address these challenges, we present \textbf{OmniPilot}, a launch advisor that predicts serving costs for feasible configurations and abstains when requests fall outside its measured support envelope. OmniPilot pairs a conformally calibrated quantile cost model (spanning eight serving targets) with an out-of-distribution (OOD) abstention layer. It ranks configurations using an economic utility metric calibrated to an operator's revealed preferences. In evaluations across 460 benchmark runs on A100, H100, and H200 hardware across four precisions, OmniPilot predicts aggregate throughput with a 6.2\% mean absolute percentage error (MAPE) and a log-space $R^2=0.92$. The advisor achieves 95\% top-1 accuracy with a mean utility regret of just 0.003. When tested on an OOD holdout of unsupported cells, prediction error climbs to 24-46\% and conformal intervals cover 0 of 5 points; however, the abstention layer successfully flags all five as low-confidence. Over time, these OOD scenarios will be integrated into the training dataset to continuously expand the advisor's support envelope.

---

## 论文详细总结（自动生成）

### 论文总结：OmniPilot —— 异构 GPU 集群下的 LLM 推理不确定性感知建议器

#### 1. 核心问题与整体含义（研究动机和背景）
在共享且异构的 GPU 集群（如包含 A100、H100、H200 的环境）中，部署大语言模型（LLM）推理服务面临复杂的决策困境。用户必须在启动前确定 **GPU 类型、张量并行（TP）度、数值精度（量化）**。
*   **挑战：** 静态配置规则往往失效。例如，4-bit 量化在某些硬件上可能比 16-bit 更慢；KV 缓存压力会导致性能非线性下降；多 GPU 启动的失败率随 TP 度增加而剧增。
*   **后果：** 盲目选择配置会导致昂贵的节点机时浪费、内存溢出（OOM）或服务水平协议（SLO）违规。
*   **目标：** 开发一个“启动建议器”（Launch Advisor），在任务提交前预测成本、性能及风险，并提供带有置信区间的建议。

#### 2. 方法论
OmniPilot 的核心是一个**不确定性感知的预测框架**，主要包含以下技术组件：
*   **分位数成本模型（Quantile Cost Model）：** 采用梯度提升回归树（GBDT），针对 8 个目标（如总吞吐量、首字延迟 TTFT、峰值 KV 缓存使用率、平均功耗等）拟合 0.1、0.5 和 0.9 分位数。
*   **保形校准（Conformal Calibration）：** 使用保形分位数回归（CQR）技术，在留出的工作负载单元上进行校准，确保预测区间（Prediction Intervals）在统计上具有 80% 的覆盖可靠性。
*   **分布外（OOD）弃权层：** 建立了一个支持包络（Support Envelope）。当请求的参数（如模型大小、并发数）超出训练数据范围时，系统会主动“弃权”并标记为低置信度，而非给出不可靠的预测。
*   **经济效用度量（Economic Utility Metric）：** 将预测结果转化为统一的效用分数。公式综合了：`代币价值 - 计算成本 - 排队等待成本 - 预期失败成本 - SLO 违规惩罚 - KV 饱和惩罚`。

#### 3. 实验设计
*   **实验场景：** 哈佛大学 Kempner AI 集群，一个生产级的共享集群。
*   **硬件环境：** A100 (40GB)、H100 (80GB)、H200 (141GB)。
*   **数据集/模型：** 涵盖 9 个模型家族（Qwen2.5, Llama3, Mistral, Gemma2 等），参数量从 0.5B 到 46.7B。
*   **Benchmark：** 460 次受控的基准测试运行（后扩展至 561 次），以及对 50 万个历史 Slurm 作业的遥测数据挖掘。
*   **对比方法：** 
    *   **Cheapest-fit：** 选择能跑通的最便宜硬件。
    *   **Size-threshold TP rule：** 基于模型大小的静态并行规则。
    *   **H100/FP8 recipe：** 盲目追求最新硬件和精度。
    *   **Throughput-max：** 追求最高吞吐量。
    *   **Nearest-neighbor：** 最近邻搜索策略。

#### 4. 资源与算力
*   **硬件：** 实验使用了 A100、H100 和 H200 GPU。
*   **训练时长：** 论文未明确给出具体的训练时长，但指出成本模型是基于 GBDT 的轻量级回归器，且训练集规模仅为数百行，因此模型训练和更新的算力开销极低（秒级或分钟级）。
*   **推理引擎：** 统一使用 vLLM (v0.11.2) 作为底层推理后端。

#### 5. 实验数量与充分性
*   **实验规模：** 核心评估基于 460-476 组标记的推理实验数据。
*   **验证方法：** 采用了**无泄露的 5 折 GroupKFold 交叉验证**，确保模型从未在测试过的特定工作负载单元（模型×并发×上下文长度）上进行训练。
*   **消融实验：** 针对量化特征、KV 饱和项、成功率先验等关键组件进行了详细的消融分析。
*   **充分性评价：** 实验设计考虑了异构硬件交互、非单调的量化性能以及集群层面的失败率，覆盖面较广且具有生产环境的代表性。

#### 6. 主要结论与发现
*   **高精度预测：** 吞吐量预测的平均绝对百分比误差（MAPE）仅为 **6.2%**，对数空间 R² 达到 **0.92**。
*   **决策优越性：** 在联合配置选择（GPU+TP+精度）中，OmniPilot 达到了 **95% 的 Top-1 准确率**，其效用遗憾（Regret）比基准方法低一个数量级。
*   **量化陷阱：** 发现量化并不总是带来加速（如 AWQ 在某些路径上较慢），OmniPilot 成功捕捉到了这些非单调行为。
*   **安全保障：** 在 OOD 测试中，虽然预测误差升至 24-46%，但弃权层成功识别并标记了所有高风险请求。

#### 7. 优点
*   **不确定性感知：** 不仅给出预测值，还给出经过统计校准的置信区间，这对于生产环境的风险控制至关重要。
*   **经济驱动：** 将技术指标转化为经济效用，更符合集群运营商和用户的实际利益。
*   **闭环学习：** 设计了回归门控（Regression-gated）的自动更新循环，能够随新数据的加入持续进化。
*   **实用性强：** 能够识别 KV 缓存饱和导致的性能骤降，这是许多静态调度器忽略的细节。

#### 8. 不足与局限
*   **范围限制：** 目前仅专注于**单节点**推理服务，尚未覆盖多节点分布式推理、模型训练或故障恢复任务。
*   **集群依赖：** 某些阈值（如 KV 饱和点、硬件失败先验）具有集群特异性，迁移到其他环境可能需要重新校准。
*   **黑盒引擎：** 模型未考虑 vLLM 内部复杂的运行时参数调整（如 block size 等），仅将其视为黑盒。
*   **数据稀疏性：** 对于某些新型架构（如 MoE）或极端长文本场景，训练数据仍显不足。

（完）
