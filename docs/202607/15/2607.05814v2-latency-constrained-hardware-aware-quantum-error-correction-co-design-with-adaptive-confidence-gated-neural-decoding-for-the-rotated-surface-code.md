---
title: Latency-Constrained Hardware-Aware Quantum Error Correction Co-Design with Adaptive Confidence-Gated Neural Decoding for the Rotated Surface Code
title_zh: 针对旋转表面码的延迟受限、硬件感知量子纠错协同设计：采用自适应置信度门控神经解码
authors: Sumit Chongder
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05814v2"
tags: ["query:hwnas"]
score: 8.0
evidence: 神经解码的硬件感知协同设计
tldr: "针对容错量子计算中实时解码的延迟瓶颈，本文提出一种面向旋转表面码的自适应置信度门控解码框架。该方案采用两阶段推理：由轻量级神经网络快速处理大部分综合征，仅将低置信度预测交由MWPM算法精化。实验证明，该方法在仅精化约5%样本的情况下显著提升了逻辑准确率，并在通用硬件上实现了高吞吐量，为硬件感知的量子纠错协同设计提供了有效路径。"
selection_source: fresh_fetch
motivation: 实时解码是量子纠错从近临界噪声设备向容错量子计算扩展的主要瓶颈。
method: 提出一种两阶段解码框架，利用轻量级神经网络进行快速路径解码，并结合置信度门控机制将疑难样本交给MWPM算法精化。
result: "在仅需精化3.3%-6.2%样本的情况下，逻辑准确率从99.21%提升至99.81%，且在CPU上实现了每秒约46万次的解码吞吐量。"
conclusion: 该协同设计框架在保证低延迟的同时显著提升了解码精度，为未来硬件受限的量子纠错实现奠定了基础。
---

## 摘要
实时解码是将量子纠错（QEC）从含噪声的中等规模量子（NISQ）设备扩展到容错量子计算的主要瓶颈。我们提出了一种针对旋转表面码的自适应置信度门控解码框架，将解码视为一个两阶段推理问题。一个轻量级前馈神经网络对大多数伴随式测量执行快速路径解码，而只有低置信度的预测会被提交到最小权重完美匹配（MWPM）细化阶段。我们使用 Stim 稳定器模拟器，在电路级去极化噪声下，对码距为 $d \in \{3,5,7,9,11\}$ 的旋转表面码进行了基准测试。评估表征了逻辑准确率、置信度控制的准确率-延迟权衡、解码吞吐量、单次延迟以及解码图资源扩展性。仅将 3.3%-6.2% 的伴随式路由至细化阶段，在置信度阈值为 0.95 时，将逻辑准确率从纯神经基准的 99.21% 提高到 99.81%，而平均解码成本仅有界增加。在商用 CPU 硬件上，当批处理大小为 512 时，神经解码器吞吐量在 $4.6 \times 10^{5}$ 样本/秒附近达到饱和，这表明在码距 $d=7$ 之后，神经快速路径不再是主要的吞吐量瓶颈。我们发布了完整的基准测试流水线、训练好的模型、原始基准数据和源代码，并明确区分了经实验验证的贡献与更广泛的硬件感知 QEC 协同设计路线图，包括硬件受限的码发现、GPU 加速推理和多噪声优化，这些仍是未来的研究方向。

## Abstract
Real-time decoding is a major bottleneck in scaling quantum error correction (QEC) from noisy intermediate-scale quantum (NISQ) devices to fault-tolerant quantum computing. We present an adaptive confidence-gated decoding framework for the rotated surface code that treats decoding as a two-stage inference problem. A lightweight feed-forward neural network performs fast-path decoding for the majority of syndrome measurements, while only low-confidence predictions are escalated to a minimum-weight perfect matching (MWPM) refinement stage. We benchmark the framework on rotated surface codes with distances $d \in \{3,5,7,9,11\}$ under circuit-level depolarising noise using the Stim stabiliser simulator. The evaluation characterises logical accuracy, confidence-controlled accuracy-latency trade-offs, decoding throughput, per-shot latency, and decoding-graph resource scaling. Routing only 3.3%-6.2% of syndromes to the refinement stage improves logical accuracy from 99.21% for the neural-only baseline to 99.81% at a confidence threshold of 0.95 while incurring only a bounded increase in average decoding cost. Neural-decoder throughput saturates near $4.6 \times 10^{5}$ samples s$^{-1}$ at batch size 512 on commodity CPU hardware, indicating that the neural fast path is not the dominant throughput bottleneck beyond code distance $d=7$. We release the complete benchmarking pipeline, trained models, raw benchmark data, and source code, and explicitly distinguish the experimentally validated contributions from the broader hardware-aware QEC co-design roadmap, including hardware-constrained code discovery, GPU-accelerated inference, and multi-noise optimisation, which remain directions for future work.

---

## 论文详细总结（自动生成）

这篇论文针对容错量子计算中的实时解码瓶颈，提出了一种硬件感知的协同设计框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着量子纠错（QEC）规模的扩大，经典的解码算法（如 MWPM）在处理大规模表面码时面临严重的延迟挑战，而纯神经解码器虽然速度快，但在处理罕见的、低置信度的“困难”伴随式（Syndromes）时准确率不足。
*   **研究背景**：实时解码必须在量子比特的相干时间内完成。目前的神经解码器研究往往只关注平均准确率，忽略了少数低置信度错误对逻辑失败的成倍贡献。
*   **整体含义**：本文旨在通过“自适应置信度门控”机制，结合神经网络的快速推理能力和经典算法的稳健性，实现延迟与精度的最优权衡，并将其纳入一个更广泛的硬件感知协同设计路线图中。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将解码视为一个**两阶段推理问题**。
    *   **快速路径（Fast-path）**：使用轻量级前馈神经网络（FFNN）处理绝大多数简单的伴随式。
    *   **精化阶段（Refinement stage）**：仅当神经网络输出的置信度（预测类别的最大概率）低于设定阈值 $\tau$ 时，才将该样本“升级”交给 MWPM 算法处理。
*   **关键技术细节**：
    *   **置信度门控**：通过推理时可调的阈值 $\tau$ 来控制路由比例，无需重新训练模型。
    *   **协同设计框架**：提出了一个包含代码生成、噪声建模、电路综合、自适应解码和硬件感知评估的闭环系统。
    *   **多目标优化函数**：定义了结合逻辑错误率 ($P_L$)、运行时间 ($T$)、内存占用 ($M$) 和硬件成本 ($C$) 的加权目标函数 $L = w_1 P_L + w_2 T + w_3 M + w_4 C$。

### 3. 实验设计
*   **数据集与场景**：使用 **Stim** 稳定器模拟器生成旋转表面码（Rotated Surface Code）的伴随式数据。
*   **实验参数**：
    *   **码距 ($d$)**：$\{3, 5, 7, 9, 11\}$。
    *   **物理错误率 ($p$)**：电路级去极化噪声，范围从 $10^{-4}$ 到 $5 \times 10^{-3}$。
    *   **置信度阈值 ($\tau$)**：$\{0.60, 0.70, 0.80, 0.90, 0.95\}$。
*   **Benchmark（基准）**：
    *   对比了**纯神经解码器**（Baseline）与**自适应混合解码器**。
    *   精化阶段使用 **PyMatching**（基于 Sparse Blossom 算法的 MWPM 实现）。
*   **评估指标**：逻辑准确率、逻辑错误率缩放、解码吞吐量、单次延迟、解码图资源占用。

### 4. 资源与算力
*   **硬件环境**：实验明确指出是在**商用 CPU 硬件**（Commodity CPU）上运行的。
*   **算力细节**：
    *   使用了单核 CPU 进行延迟测量。
    *   批处理推理（Batch Inference）测试了从 1 到 512 的不同规模。
*   **缺失信息**：文中**未提供具体的 GPU 推理数据**，并将其列为未来研究的方向。训练时长等具体数值未详细列出，但提供了模型架构和超参数。

### 5. 实验数量与充分性
*   **实验规模**：每个 $(d, p)$ 组合进行了 30 次独立测量（6 轮独立试验，每轮 5,000 次采样），总计针对不同阈值和批次大小进行了数万次模拟。
*   **充分性评价**：
    *   **多维度覆盖**：实验涵盖了码距、噪声强度、阈值和批处理大小四个关键轴，数据量较为充分。
    *   **客观性**：作者明确区分了“已验证的贡献”与“架构提案（如代码发现、GPU 加速）”，这种诚实的报告方式增强了结论的可信度。
    *   **局限性**：实验仅限于旋转表面码和去极化噪声，未涉及相关噪声或 QLDPC 码，这在文中已作为 Limitation 明确指出。

### 6. 主要结论与发现
*   **精度提升**：在 $d=7$ 时，仅将 **3.3% – 6.2%** 的样本路由至精化阶段，逻辑准确率就从 99.21%（纯神经）提升至 **99.81%**（$\tau=0.95$）。
*   **吞吐量表现**：在 CPU 上，批处理大小为 512 时，神经解码器吞吐量达到约 **4.6 × 10⁵ 样本/秒**。
*   **瓶颈分析**：实验表明，对于 $d \le 7$ 的情况，神经快速路径并非主要的吞吐量瓶颈，真正的挑战在于精化阶段的尾部延迟。
*   **置信度校准**：发现解码器置信度与实际准确率高度相关，但在高噪声下存在“过度自信”现象，验证了门控机制的必要性。

### 7. 优点（亮点）
*   **实用性强**：提出的两阶段架构非常符合工业界对“快速路径/慢速路径”处理逻辑的需求。
*   **可复现性高**：作者开源了完整的流水线、训练模型和原始基准数据。
*   **系统性分析**：不仅关注准确率，还深入分析了解码图的资源扩展性（探测器数量、电路操作数等），为硬件实现提供了参考。

### 8. 不足与局限
*   **噪声模型单一**：仅使用了独立的电路级去极化噪声，未测试相干噪声、泄漏错误或串扰等更复杂的现实噪声。
*   **硬件评估不完整**：缺乏 GPU 加速的实测数据，且未对精化阶段（MWPM）的延迟分布进行单独的深度剖析。
*   **架构局限**：代码发现（Code Discovery）和闭环优化部分目前仅停留在架构提案阶段，尚未进行实证评估。
*   **非单调性问题**：在高噪声强度下，观察到逻辑准确率随码距变化的非单调性，这可能暗示了所选神经网络架构的容量限制。

（完）
