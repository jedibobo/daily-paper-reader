---
title: Low-Power License Plate Detection and Recognition on a RISC-V Multi-Core MCU-Based Vision System
title_zh: 基于 RISC-V 多核 MCU 视觉系统的低功耗车牌检测与识别
authors: "Lorenzo Lamberti, Manuele Rusci, Marco Fariselli, Francesco Paci, Luca Benini"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.09768v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于低功耗MCU的边缘设备视觉任务
tldr: 本研究展示了首个基于低功耗MCU（GAP8，9核RISC-V处理器）的自动车牌识别（ALPR）边缘设备。通过结合SSDlite-MobilenetV2检测模型和LPRNet识别模型，并应用压缩与优化策略，该系统在仅117mW的功耗下实现了1.09 FPS的吞吐量。其能效比树莓派3高出73倍，且在极低分辨率（30x5像素）下仍能保持高识别率，为低功耗边缘视觉应用提供了新方案。
selection_source: fresh_fetch
motivation: 旨在解决传统自动车牌识别系统功耗过高、难以在资源受限的边缘MCU设备上部署的问题。
method: 采用基于9核RISC-V处理器的GAP8芯片，构建了集成SSDlite-MobilenetV2检测和LPRNet识别的多模型推理流水线，并进行了针对性的压缩优化。
result: "系统在117mW功耗下达到1.09 FPS，车牌识别率超过99.13%，能效比树莓派3提升了73倍。"
conclusion: 该设计证明了在不依赖硬件加速器的情况下，利用多核MCU也能高效实现复杂的深度学习视觉任务，具有极高的灵活性和能效。
---

## 摘要
本文展示了（据我们所知）首个基于低功耗 MCU 的自动车牌识别（ALPR）边缘设备。该设计利用了 9 核 RISC-V 处理器 GAP8，并配备了 QVGA 超低功耗灰度成像仪。所提出的视觉处理流水线采用基于 SSDlite-MobilenetV2 的车牌检测和基于 LPRNet 的光学字符识别的多模型推理方法，在公开数据集上，前一任务的 mAP 分数达到 38.9%，后一任务的识别率超过 99.13%。在真实世界数据中，当车牌裁剪区域的大小仅为 30x5 像素时，该流水线仍能识别车牌号码。得益于所采用的压缩和优化策略，多模型推理（687 MMAC）在 GAP8 上运行时，在 117 mW 的功耗下实现了 1.09 FPS 的吞吐量。我们的解决方案是首个嵌入此类复杂度网络的 MCU 级设备，其能效比之前基于树莓派 3（Raspberry Pi3）的移动级 ALPR 系统高出 73 倍。所提出的设计不依赖任何硬连线加速引擎，从而为未来的算法改进保留了充分的灵活性。

## Abstract
In this paper, we present the first (to the best of our knowledge) demonstration of a low-power MCU-based edge device for Automatic License Plate Recognition (ALPR). The design leverages on a 9-core RISC-V processor, GAP8, coupled with a QVGA ultra-low-power greyscale imager. The proposed visual processing pipeline uses a multi-model inference approach based on SSDlite-MobilenetV2 for license plate detection and LPRNet for optical character recognition, reaching a 38.9% mAP score for the first task and a recognition rate of >99.13% for the latter on public datasets. On real-world data, the pipeline recognizes registration numbers when the size of LP crops is as small as 30x5 pixels. Thanks to the applied compression and optimization strategies, the multi-model inference (687 MMAC) achieves a throughput of 1.09 FPS at a power cost of 117 mW when running on GAP8. Our solution is the first MCU-class device embedding such a level of network complexity, resulting to be 73x more energy-efficient w.r.t. precedent mobile-class ALPR system featuring a Raspberry Pi3. The proposed design does not resort to any hardwired acceleration engines, thus retaining full flexibility for future algorithmic improvements.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种在超低功耗 RISC-V 多核微控制器（MCU）上实现自动车牌识别（ALPR）的完整视觉系统。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的自动车牌识别（ALPR）系统通常依赖高性能嵌入式平台（功耗 >5W），无法满足电池供电、长效在线（Always-on）的智能传感需求。而普通单核 MCU（如 ARM Cortex-M4）又因内存和算力极度受限，难以运行复杂的深度学习视觉流水线。
*   **研究动机**：旨在证明通过算法优化和并行架构，可以在百毫瓦（mW）级别的功耗预算内，实现包含检测与识别两个阶段的复杂视觉任务。

### 2. 方法论：核心思想与关键技术
论文提出了一个两阶段的深度学习流水线，并针对 GAP8 处理器进行了深度优化：
*   **硬件平台**：采用 **GAP8**（基于 RISC-V 的 9 核处理器），包含 1 个结构核心和 8 个计算簇核心，配合超低功耗 Himax 图像传感器（QVGA 分辨率）。
*   **车牌检测阶段**：使用 **SSDlite-MobilenetV2**。
    *   **优化**：将 SSD 预测头替换为深度可分离卷积（SSDLite），减少了 150 万个参数和 30 MMAC 的计算量。
    *   **输入**：采用 320x240 灰度图像，平衡了计算成本与检测精度。
*   **车牌识别阶段**：使用 **LPRNet**（全卷积、无分割架构）。
    *   **优化**：移除了 GAP8 不支持的 Tiling 操作，通过减半全连接层深度来抵消内存增长，最终模型仅 1MB。
*   **部署优化**：
    *   **量化**：采用量化感知训练（QAT）将模型压缩至 **8-bit 整型**，内存占用降低 4 倍。
    *   **并行化**：利用 GAP flow 工具将计算任务分配到 8 个 RISC-V 核心上，并利用 SIMD 指令加速。

### 3. 实验设计与 Benchmark
*   **数据集**：
    *   **检测**：OpenImagesV4（从中提取车牌样本）。
    *   **识别**：CCPD（中国城市停车数据集）、Synthetic Chinese LPs（合成数据集）、ReId Czech LPs（捷克车牌）。
    *   **实测**：自建 **Himax Test Dataset**，包含不同距离（0.3m 到 4m）下的真实拍摄图像。
*   **对比基准（Benchmark）**：
    *   **移动级平台**：Raspberry Pi 3 (树莓派3)。
    *   **加速器/FPGA**：Intel NCS2 (神经计算棒)、Xilinx Zynq FPGA。
    *   **智能相机**：Pixy2、HuskyLens。

### 4. 资源与算力
*   **处理器**：GAP8 (RISC-V 9-core)，运行频率 175MHz。
*   **算力消耗**：总计算量为 **687 MMAC**（检测 515M + 识别 172M）。
*   **内存占用**：总参数量 4.1M（约 4.1MB），符合 GAP8 的存储特性。
*   **训练细节**：文中未详细说明训练所用的具体 GPU 型号或训练时长，重点在于推理端的部署表现。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   进行了不同网络拓扑（SSD-MV1 vs MV2 vs SSDLite）的对比实验。
    *   进行了不同输入分辨率和量化位宽的消融实验。
    *   在三个不同国家/地区的车牌数据集上验证了识别率。
    *   **实地测试**：通过改变车辆距离（0.3m-4m）测试了系统对极小目标（30x5 像素）的鲁棒性。
*   **充分性评价**：实验设计较为全面，不仅有标准数据集的精度对比，还有功耗、延迟、能效比的量化分析，客观地展示了 MCU 方案与高性能方案的差距与优势。

### 6. 主要结论与发现
*   **性能指标**：在 GAP8 上实现了 **1.09 FPS** 的吞吐量。
*   **功耗与能效**：系统总功耗仅 **117 mW**（含传感器和内存），单次推理能耗为 108 mJ。
*   **能效对比**：比基于树莓派 3 的系统能效高出 **73 倍**。
*   **精度表现**：检测 mAP 为 38.9%，识别准确率 >99.13%。在车牌字符小至 8x16 像素时仍能保持 100% 识别率。

### 7. 优点
*   **极高能效**：首次在百毫瓦级 MCU 上跑通了完整的两阶段 ALPR 流程。
*   **纯软件灵活性**：不依赖硬连线加速器，算法可根据需求灵活更新。
*   **鲁棒性**：针对低功耗传感器的灰度特性进行了专门训练，对小尺寸目标支持良好。

### 8. 不足与局限
*   **帧率较低**：1.09 FPS 仅适用于静态或低速场景（如停车场出入口），无法处理高速公路等高速场景。
*   **识别距离受限**：受限于 QVGA 分辨率，当距离超过 1 米或字符小于 5x10 像素时，识别率大幅下降。
*   **内存压力**：4.1MB 的模型对于大多数低端单核 MCU 仍然过大，该方案高度依赖 GAP8 这种具有多级存储和多核架构的高端 MCU。

（完）
