---
title: "Collate: Collaborative Neural Network Learning for Latency-Critical Edge Systems"
title_zh: Collate：面向延迟敏感型边缘系统的协作神经网络学习
authors: "Shuo Huai, Di Liu, Hao Kong, Xiangzhong Luo, Weichen Liu, Ravi Subramaniam, Christian Makaya, Qian Lin"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08013v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对延迟敏感边缘系统的异构模型协作学习
tldr: 针对边缘系统中设备异构性导致的推理延迟问题，本文提出Collate框架。该框架通过动态归零-恢复方法灵活调整局部模型架构，并结合原型校正的联邦聚合方案，实现在单次训练过程中让满足不同延迟约束的异构模型协同学习。实验表明，Collate在满足延迟要求的同时显著提升了模型精度，且几乎不增加额外训练开销。
selection_source: fresh_fetch
motivation: 现有的联邦学习优化主要关注训练效率，忽略了边缘设备异构性对实时推理延迟约束的影响。
method: 设计了动态归零-恢复方法来调整局部模型架构，并引入原型校正聚合方案以统一处理异构模型。
result: "在延迟约束下，该方法使扩展模型和缩小模型的平均精度分别提升了1.96%和3.09%。"
conclusion: Collate框架通过协同学习异构模型，有效解决了边缘系统在严格延迟限制下的高精度模型部署难题。
---

## 摘要
联邦学习 (FL) 使多个客户端能够协作学习一个模型，在保护数据隐私的同时扩大了每个客户端的训练数据以实现高准确率。然而，在实时边缘系统中部署联邦学习时，系统间设备的异构性会对推理模型的性能产生严重影响。现有的联邦学习优化侧重于提高训练效率，但未能加速推理，特别是在存在延迟约束的情况下。在这项工作中，我们提出了 Collate，这是一个新颖的训练框架，它协作学习异构模型，以同时满足多个边缘系统的延迟约束。我们设计了一种动态归零-恢复 (dynamic zeroizing-recovering) 方法，用于调整每个本地模型的架构，从而在延迟约束下实现高准确率。此外，还引入了一种原型校正联邦聚合 (proto-corrected federated aggregation) 方案来聚合所有异构本地模型，仅通过一次训练过程即可满足不同系统的延迟约束并保持高准确率。大量实验表明，与最先进的方法相比，在延迟约束下，我们的扩展模型平均可提高 1.96% 的准确率，而我们的收缩模型平均也能获得 3.09% 的准确率提升，且几乎没有额外的训练开销。相关代码和数据将在 https://github.com/ntuliuteam/Collate 上发布。

## Abstract
Federated Learning (FL) empowers multiple clients to collaboratively learn a model, enlarging the training data of each client for high accuracy while protecting data privacy. However, when deploying FL in real-time edge systems, the heterogeneity of devices among systems has a severe impact on the performance of the inferred model. Existing optimizations on FL focus on improving the training efficiency but fail to speed up inference, especially when there is a latency constraint. In this work, we propose Collate, a novel training framework that collaboratively learns heterogeneous models to meet the latency constraints of multiple edge systems simultaneously. We design a dynamic zeroizing-recovering method to adjust each local model architecture for high accuracy under its latency constraint. A proto-corrected federated aggregation scheme is also introduced to aggregate all heterogeneous local models, satisfying the latency constraint of different systems with only one training process and maintaining high accuracy. Extensive experiments indicate that, compared to state-of-the-art methods and under a latency constraint, our extended models can improve the accuracy by 1.96% on average, and our shrunk models can also obtain a 3.09% accuracy improvement on average, with almost no extra training overhead. The related codes and data will be available at https://github.com/ntuliuteam/Collate

---

## 论文详细总结（自动生成）

### Collate: 面向延迟敏感型边缘系统的协作神经网络学习总结

#### 1. 核心问题与整体含义
在边缘计算环境下，联邦学习（FL）虽然能保护隐私并整合多方数据，但面临严重的**硬件异构性**挑战。传统的 FL 通常训练一个统一的模型，这导致高性能设备算力浪费，而低性能设备则无法满足实时推理的延迟要求。本文提出的 **Collate** 框架旨在通过一次协作训练过程，为具有不同延迟约束的多个边缘系统定制化地生成异构模型，在满足实时性要求的同时最大化推理准确率。

#### 2. 方法论
Collate 框架由两个核心组件和两个辅助技术组成：
*   **核心思想**：将模型架构调整集成到联邦学习的本地训练中，通过动态剪裁和扩展，使每个客户端的模型与其硬件延迟约束相匹配。
*   **动态归零-恢复方法（Dynamic Zeroizing-Recovering）**：
    *   **初始化**：先进行若干轮预训练，使参数具有代表性。
    *   **归零**：利用可学习的掩码层（Mask Layer）评估卷积核贡献度，根据延迟预测器的反馈，将贡献小的卷积核参数暂时置零，以满足延迟约束。
    *   **恢复**：利用优化器的动量机制，允许被置零的参数在后续迭代中更新并可能“复活”，从而探索更优的架构。
*   **原型校正聚合方案（Proto-corrected Aggregation）**：
    *   针对异构模型参数量不一致的问题，采用子集聚合策略。
    *   引入“原型学习”，提取各类别的高维特征向量（原型），通过在损失函数中加入原型惩罚项，纠正异构层在非独立同分布（Non-IID）数据下的偏差。
*   **辅助技术**：
    *   **延迟预测器**：基于 BP 神经网络构建，预先学习硬件特征，在训练中快速评估模型架构的推理延迟。
    *   **模型扩展**：对于算力盈余的设备，通过宽度乘数统一扩展模型，以提升精度。

#### 3. 实验设计
*   **数据集与场景**：使用了 MNIST、CIFAR-10、CIFAR-100（图像分类）以及 HAR（人类活动识别，传感器数据）。涵盖了 IID（独立同分布）和 Non-IID（非独立同分布）两种数据分布场景。
*   **硬件平台**：HP ProBook、NVIDIA Jetson TX2、Jetson Nano、Raspberry Pi 4B、Samsung Galaxy Note10。
*   **对比方法（Benchmarks）**：
    *   **ERFL**：通过随机选择子集减少资源消耗。
    *   **Helios**：动态压缩全局模型。
    *   **HeteroFL**：预定义不同复杂度的模型进行聚合。

#### 4. 资源与算力
*   **硬件环境**：论文详细列出了 5 种边缘设备的型号和配置（如 CPU、GPU、内存等）。
*   **算力消耗**：论文未明确说明服务器端用于聚合和协调的具体 GPU 型号及总训练时长。但强调了 Collate 的本地训练开销极低，增加的掩码层和排序操作复杂度远低于卷积运算。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上分别运行了 20 到 100 轮不等的通信迭代，每个本地训练周期为 6 个 Epoch。
*   **充分性**：实验涵盖了从 0.25x 到 1.5x 的多种复杂度水平，并在 5 种真实硬件上验证了延迟与精度的平衡。通过与三种前沿方法的对比，证明了其在不同数据分布下的稳健性。实验设计较为客观，考虑了实际部署中的延迟波动。

#### 6. 主要结论与发现
*   **精度提升**：在相同的延迟约束下，Collate 的扩展模型比基准方法平均精度提升 **1.96%**，缩小模型平均精度提升 **3.09%**。
*   **延迟满足**：Collate 能够确保所有参与设备在单次训练任务中均获得符合其延迟限制的模型。
*   **Non-IID 适应性**：原型校正机制有效缓解了异构模型在处理非均匀分布数据时的精度损失。

#### 7. 优点
*   **双向优化**：不仅关注低端设备的“减负”，还通过“模型扩展”充分利用了高端设备的算力。
*   **动态探索**：归零-恢复机制避免了结构剪枝的不可逆性，使模型在训练过程中能动态寻找最优子结构。
*   **低开销**：在不显著增加训练负担的前提下，解决了推理阶段的延迟痛点。

#### 8. 不足与局限
*   **预训练依赖**：延迟预测器需要针对每种新硬件进行预先的数据采集和训练，这在设备种类极度繁杂时存在初始化成本。
*   **架构限制**：目前主要针对卷积神经网络（CNN）的宽度进行调整，对于更复杂的算子融合或 Transformer 等新型架构的适用性有待验证。
*   **客户端规模**：实验主要展示了 5 个典型客户端的协作，在成千上万个超大规模客户端场景下的聚合效率和收敛性尚需进一步探讨。

（完）
