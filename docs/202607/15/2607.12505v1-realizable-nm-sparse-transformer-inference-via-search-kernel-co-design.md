---
title: "Realizable N:M Sparse Transformer Inference via Search-Kernel Co-Design"
title_zh: "基于搜索与算子协同设计的可实现 N:M 稀疏 Transformer 推理"
authors: "Yiming Liu, Wenqi Lou, Zhiguang Wang, Zhiwei Ke, Fengrui Zuo, Chao Wang, Xuehai Zhou"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12505v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 针对稀疏Transformer推理的软硬件协同设计
tldr: M稀疏化时理论加速与实际性能不匹配的问题，本文提出了一种软硬件协同设计框架。硬件上，设计了MD-SpMM内核以优化数据流和并行度；软件上，通过三阶段启发式搜索在延迟预算下优化层级稀疏度分配。该框架在保持精度的同时实现了超过2.2倍的端到端加速，为稀疏模型的高效部署提供了新方案。
selection_source: fresh_fetch
motivation: M稀疏化技术由于忽略了硬件执行的规律性和调度开销，导致理论计算量的减少难以转化为实际的推理加速。
method: 采用软硬件协同设计，包括开发支持Tensor Core对齐的MD-SpMM稀疏内核，以及在延迟预算下进行三阶段启发式层级稀疏度搜索。
result: 在多个ViT和Swin模型及GPU平台上实现了超过2.2倍的端到端加速，且在相同延迟约束下维持或提升了模型精度。
conclusion: 通过将底层算子优化与高层稀疏配置搜索相结合，可以显著提升稀疏Transformer在现代硬件上的实际运行效率。
---

## 摘要
视觉 Transformer (ViT) 取得了极高的准确率，但推理延迟也较高。半结构化 N:M 稀疏性可以降低计算成本，但其理论上的节省往往无法在现代 GPU 上转化为成比例的端到端加速。这种不匹配的原因在于，部署延迟不仅取决于计算量的减少，还取决于稀疏情况下的执行规则性和硬件调度。因此，实现实际加速需要对稀疏执行和稀疏配置进行协同设计。为此，我们提出了一个用于 N:M 稀疏 ViT 推理的软硬件协同设计框架。在硬件方面，我们设计了 MD-SpMM，这是一种 N:M 稀疏 CUDA 算子，它将稀疏 GEMM 重新组织为微密集（micro-dense）、Tensor-Core 对齐的数据流，并使用推理感知的自适应并行性来维持利用率。在软件方面，我们在明确的端到端延迟预算下，使用带有约束松弛的三阶段启发式搜索进行逐层稀疏度搜索，以避免过早收敛并实现部署感知的稀疏度分配。在多个 ViT/Swin 模型和 GPU 平台上的实验表明，该框架在保持相当准确率的同时实现了超过 2.2 倍的延迟加速，并在相同的延迟约束下提供了更高的准确率。源代码已在 https://github.com/liuganhuo/realizable-nm-sparse-transformer 公开。

## Abstract
Vision Transformers (ViTs) achieve strong accuracy but incur high inference latency. Semi-structured N:M sparsity can reduce arithmetic cost, yet its theoretical savings often fail to translate into proportional end-to-end speedups on modern GPUs. This mismatch arises because deployment latency depends not only on arithmetic reduction but also on execution regularity and hardware scheduling under sparsity. Achieving practical acceleration, therefore, requires coordinated design across sparse execution and sparsity configuration. To this end, we propose a hardware-software co-design framework for N:M sparse ViT inference. On the hardware side, we design MD-SpMM, an N:M sparse CUDA kernel that reorganizes sparse GEMM into micro-dense, Tensor-Core-aligned dataflow and uses inference-aware adaptive parallelism to sustain utilization. On the software side, we perform layer-wise sparsity search under explicit end-to-end latency budgets using a three-stage heuristic search with constraint relaxation to avoid premature convergence and enable deployment-aware sparsity allocation. Experiments on multiple ViT/Swin models and GPU platforms show that the framework achieves over 2.2x latency speedup while maintaining comparable accuracy and delivering superior accuracy under the same latency constraint. The source code is publicly available at https://github.com/liuganhuo/realizable-nm-sparse-transformer.

---

## 论文详细总结（自动生成）

以下是对论文《Realizable N:M Sparse Transformer Inference via Search-Kernel Co-Design》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：Vision Transformer (ViT) 虽然性能强大，但推理延迟高。半结构化 N:M 稀疏性理论上能减少计算量，但在现代 GPU（尤其是 INT8 推理）上，理论节省往往无法转化为实际的端到端加速。
*   **研究动机**：
    *   **执行不匹配**：稀疏算子的不规则访存和调度开销常抵消计算量的缩减。
    *   **配置盲目性**：现有的稀疏化方法多以 FLOPs 或稀疏率为目标，忽略了不同层在特定硬件上的真实延迟表现。
    *   **目标**：实现“可实现的加速”（Realizable Acceleration），即在真实部署栈上获得与理论匹配的性能提升。

### 2. 方法论
论文提出了一个**软硬件协同设计框架**，包含两个核心组件：

*   **硬件端：MD-SpMM 稀疏内核**
    *   **微密集重构 (Micro-Dense)**：将 N:M 稀疏计算重组为与 Tensor Core 形状对齐的密集块执行，将稀疏处理逻辑移至加载阶段，确保计算阶段完全利用 MMA（矩阵乘累加）指令。
    *   **权重打包 (Weight Packing)**：设计了 decode-friendly 的存储格式，将非零值与索引联合编码，减少访存次数并实现轻量化运行时解码。
    *   **自适应并行 (Adaptive Parallelism)**：采用规则驱动的 Split-K 策略，根据算子形状动态调整并行度，以在高稀疏度下维持 SM（流式多处理器）的占用率。

*   **软件端：延迟导向的层级搜索**
    *   **LUT 延迟预测器**：通过离线测量构建“形状-稀疏度”查找表，并利用线性模型拟合端到端延迟，预测误差控制在 5% 以内。
    *   **三阶段进化搜索算法**：
        1.  **Overshoot（过冲）**：随机增加部分层的密度，故意违反延迟约束以跳出局部最优。
        2.  **Repair（修复）**：随机增加稀疏度以恢复延迟可行性。
        3.  **Refine（精细化）**：在满足约束的前提下，逐步微调以提升准确率。

### 3. 实验设计
*   **数据集**：ImageNet-1K（完整验证），ImageNet-300（搜索阶段使用的子集）。
*   **模型架构**：DeiT-Small, DeiT-Base, Swin-Small。
*   **硬件平台**：NVIDIA A100, RTX 4090, RTX 4060。
*   **对比基准 (Benchmark)**：
    *   **算子级**：cuBLAS (密集基准), nmSPARSE (先进稀疏库)。
    *   **策略级**：Uniform N:M (均匀稀疏), Evo-Press (先进的层级稀疏搜索方法)。

### 4. 资源与算力
*   **算力消耗**：论文提到主要的计算成本在于 One-shot Supernet 的训练。
*   **搜索耗时**：在 **3 台 NVIDIA A100 GPU** 上，进行 200 代进化搜索大约需要 **10 小时**。
*   **内核测试**：在 A100 及 40 系列显卡上进行了广泛的算子性能实测。

### 5. 实验数量与充分性
*   **实验规模**：
    *   内核测试覆盖了数十种不同的矩阵形状 (M, N, K) 和三种稀疏水平 (50%, 75%, 87.5%)。
    *   端到端测试涵盖了 1.5x, 2.0x, 2.5x 等多个目标加速比。
*   **充分性**：实验不仅对比了最终结果，还展示了搜索轨迹、不同硬件平台的适配性以及消融实验（如自适应并行的有效性）。跨消费级 (RTX) 和企业级 (A100) GPU 的测试证明了方法的通用性，实验设计客观且公平。

### 6. 主要结论与发现
*   **显著加速**：该框架实现了超过 **2.2 倍** 的端到端延迟缩减。
*   **精度优势**：在相同的延迟约束下，该方法比现有技术提升了高达 **1.4%** 的 Top-1 准确率。
*   **内核性能**：MD-SpMM 内核相比 cuBLAS 密集算子平均提速 2.0x，相比 nmSPARSE 提速 1.6x-4.0x。
*   **协同效应**：证明了单纯优化算子或单纯优化搜索算法都无法达到最优，软硬协同是实现 N:M 稀疏化潜力的关键。

### 7. 优点
*   **落地导向**：直接解决“理论加速不等于实际加速”的痛点，对工业界部署具有极高参考价值。
*   **搜索策略创新**：三阶段搜索有效地解决了延迟约束导致的搜索空间破碎问题，收敛更稳定。
*   **高效内核**：MD-SpMM 成功将稀疏性带来的不规则性限制在解码阶段，保持了 Tensor Core 的高效能。

### 8. 不足与局限
*   **硬件局限性**：内核设计深度绑定 NVIDIA 的 Tensor Core 架构，迁移到 AMD GPU 或其他 AI 芯片（如华为 Ascend）需要重新设计底层算子。
*   **搜索成本**：虽然搜索过程比重新训练快，但仍依赖于预训练的 Supernet，对于没有预训练超网络的新模型，前期准备成本较高。
*   **离散空间限制**：N:M 稀疏模式受限于硬件支持（如 2:4, 4:8 等），搜索空间是离散且有限的，可能无法实现极细粒度的权衡。

（完）
