---
title: Toward Energy-Efficient and Low-Power Arrhythmia Detection for Wearable Devices
title_zh: 面向可穿戴设备的能效型低功耗心律失常检测研究
authors: "Floriaan Bulten, Yawar Rasheed, Arlene John, Vincenzo Stoico, Ghayoor Gillani"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14747v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 面向可穿戴设备的节能低功耗架构设计
tldr: "针对可穿戴设备在心律失常监测中面临的高功耗和计算复杂性挑战，本文提出了一种基于近似计算技术的深度学习硬件架构。通过降低数据精度和采用近似乘法器，在保持93.7%分类准确率的同时，将功耗降低了64.9%，显著延长了设备的电池寿命，为低功耗医疗监测提供了有效方案。"
selection_source: fresh_fetch
motivation: 现有的深度学习心律失常检测算法计算复杂度高且功耗大，难以在资源受限的可穿戴设备中长期部署。
method: 研究并应用了数据精度缩减和近似乘法技术，对先进的深度学习模型及其硬件架构进行优化。
result: "在MIT-BIH数据集上，该架构在12 kHz下功耗降低了64.9%，在100 MHz下能耗降低了61.5%，且保持了93.7%的准确率。"
conclusion: 近似计算技术能在保证分类性能的前提下大幅提升能效，是实现长效可穿戴心律监测的关键技术。
---

## 摘要
心血管疾病是全球范围内的首要死因，而心律失常等病症通常需要长期监测才能进行有效的检测和诊断。然而，目前的可穿戴监测设备体积庞大、佩戴不适，且通常依赖临床医生手动评估心电图（ECG）。虽然深度学习（DL）算法在心律失常检测和分类方面表现出卓越的性能，但其计算复杂性以及高功耗限制了其在可穿戴设备中的部署。为了应对这一挑战，本文研究了利用近似技术在保持可接受的分类性能的同时，降低深度学习架构的功耗和能耗。具体而言，本文在最先进的深度学习模型及其相应的硬件架构中研究了数据精度降低和近似乘法等技术。该模型使用 MIT-BIH 心律失常数据库进行训练和验证，并对采用各种近似乘法器的硬件实现进行了综合与评估。与最先进的 8.75 μW（以及 2.08 μJ）参考架构相比，我们提出的架构在 12 kHz 频率下的功耗为 3.07 μW（以及 2.17 μJ），功耗降低了 64.9%，同时提供了可接受的输出质量，即 93.7% 的分类准确率和 92.1% 的灵敏度。在 100 MHz 频率下，我们提出的架构功耗为 9.45 mW（以及 0.8 μJ），与最先进的架构相比，能耗降低了 61.5%。这些结果表明，我们提出的近似方法在保持所需的心律失常分类性能的同时，显著延长了可穿戴设备的电池寿命。

## Abstract
Cardiovascular diseases are the leading cause of death worldwide, and conditions such as arrhythmia often require long-term monitoring for effective detection and diagnosis. However, current wearable monitoring devices are bulky, uncomfortable, and typically rely on clinicians to manually evaluate electrocardiograms (ECGs). While Deep Learning (DL) algorithms have shown superior performance in arrhythmia detection and classification, their computational complexity coupled with high power consumption limit deployment in wearable devices. To address this challenge, this paper investigates the use of approximation techniques to reduce the power and energy consumption of DL architectures while maintaining acceptable classification performance. Specifically, techniques such as data precision reduction and approximate multiplication are investigated in a state-of-the-art DL model and its corresponding hardware architecture. The model is trained and validated using the MIT-BIH Arrhythmia Database, and hardware implementations employing various approximate multipliers are synthesized and evaluated. Compared with the state-of-the-art 8.75 μW (and 2.08 μJ) reference architecture, our proposed architecture consumes 3.07 μW (and 2.17 μJ) at 12 kHz, showing 64.9% reduction in power consumption while providing an acceptable output quality, i.e., 93.7% classification accuracy and 92.1% sensitivity. At 100 MHz, our proposed architecture consumes 9.45 mW (and 0.8 μJ), showing 61.5% reduction in energy consumption as compared to the state-of-the-art architecture. These results demonstrate that our proposed approximations significantly extend wearable device battery life while preserving the required arrhythmia classification performance.

---

## 论文详细总结（自动生成）

这篇论文针对可穿戴医疗设备在心律失常监测中面临的功耗瓶颈，提出了一种结合**近似计算（Approximate Computing）**与**深度学习（DL）**的跨层次优化方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：心血管疾病（CVD）是全球首要死因，心律失常的诊断依赖长期、连续的监测。然而，现有的深度学习模型虽然分类精度高，但计算复杂度大、功耗高，导致可穿戴设备电池寿命短、体积臃肿。
*   **研究动机**：如何在保证医疗级分类精度（需高于人类心脏病专家平均水平 78.4%）的前提下，通过硬件和算法的协同优化，大幅降低心律失常检测系统的功耗和能效。

### 2. 论文提出的方法论
论文采用了从软件算法到硬件电路的跨层次优化策略：
*   **数据精度缩减（Precision Scaling）**：将模型权重和激活值从 16 位定点数缩减至 8 位。
*   **近似乘法器应用**：在硬件实现中引入了多种近似乘法器（如 SSM4, CSSM4, N8-L2 等），利用深度神经网络对误差的天然容忍性，牺牲微小的计算精度来换取大幅的功耗下降。
*   **近似感知重训练（Approximation-aware Retraining）**：
    *   在训练的前向传播中使用近似乘法器，反向传播使用精确计算。
    *   采用分段训练策略（每 15 个 epoch 重置权重并保存最佳值），以应对低精度训练带来的梯度噪声。
*   **硬件架构优化**：
    *   **符号-数值表示法（Sign-Magnitude）**：相比补码，该表示法在处理近似乘法时只需翻转符号位，简化了电路。
    *   **峰值检测模块**：集成了自动 R 峰检测与分段逻辑，实现了端到端的片上处理。
    *   **高频能效优化**：通过将工作频率从 12 kHz 提升至 100 MHz，利用硬件闲置时间（Slack）降低单次分类的能耗。

### 3. 实验设计
*   **数据集**：使用标准的 **MIT-BIH 心律失常数据库**，包含 48 条半小时长的 ECG 记录，按照 AAMI 标准分为 N、S、V、F、Q 五类。
*   **Benchmark（基准）**：以 Janveja 等人提出的 16 位深度神经网络（DNN）架构为基准（功耗 8.75 μW，能效 2.08 μJ）。
*   **对比方法**：
    *   对比了 11 种不同的 8 位硬件架构，包括使用精确乘法器（Shift-Add, Wallace 等）和 5 种主流近似乘法器（SSM4, CSSM4 等）的组合。
    *   对比了重训练前后的模型性能。

### 4. 资源与算力
*   **硬件实现**：使用 **180nm UMC 工艺**进行 ASIC 综合评估。
*   **算力说明**：论文**未明确说明**具体使用的 GPU 型号、数量或训练时长。其研究重点在于推理阶段的硬件资源消耗（如面积 1.03 $mm^2$、功耗 μW 级别）。

### 5. 实验数量与充分性
*   **实验覆盖面**：作者评估了 11 种硬件变体，涵盖了准确率、灵敏度、功耗、能效、芯片面积和时序余量（Slack）等多个维度。
*   **充分性评价**：实验设计较为充分。不仅验证了分类精度，还专门测试了峰值检测算法的准确性，并探讨了近似误差在 MAC（乘累加）操作中的抵消效应。通过混淆矩阵分析了不平衡数据集下的分类表现，具有较强的客观性。

### 6. 主要结论与发现
*   **功耗大幅下降**：提出的 **S8GC(R)** 架构在 12 kHz 下功耗仅为 **3.07 μW**，较基准架构降低了 **64.9%**。
*   **能效显著提升**：在 100 MHz 频率下，单次分类能耗降至 **0.8 μJ**，较基准降低了 **61.5%**。
*   **性能达标**：优化后的 8 位模型实现了 **93.7% 的准确率**和 **92.1% 的灵敏度**，远超人类医生的平均水平（78.4%）。
*   **误差抵消效应**：具有对称误差分布的近似乘法器（如 CSSM4）在神经网络中表现更好，因为正负误差在累加过程中会相互抵消。

### 7. 优点（亮点）
*   **跨层次协同**：不只是单纯的硬件替换，而是结合了“近似感知重训练”算法，有效补偿了硬件近似带来的精度损失。
*   **极低功耗**：在 180nm 这一较老工艺下实现了微瓦（μW）级别的功耗，非常适合超长待机的可穿戴设备。
*   **正则化效果**：研究发现 8 位量化在某些情况下起到了类似正则化的作用，缓解了过拟合，使精度甚至略优于 16 位模型。

### 8. 不足与局限
*   **峰值检测缺陷**：实验显示该算法对 **F 类（融合搏动）**的检测灵敏度为 0%，说明峰值检测模块对某些特定形态的波形存在系统性失效。
*   **数据集单一**：仅在 MIT-BIH 数据集上验证，未在更复杂的真实世界噪声环境或跨数据集（Cross-dataset）场景下测试。
*   **应用限制**：虽然功耗极低，但 180nm 工艺的芯片面积（约 1 $mm^2$）在现代微型化需求下仍有优化空间。

（完）
