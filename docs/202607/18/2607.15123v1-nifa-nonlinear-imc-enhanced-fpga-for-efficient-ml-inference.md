---
title: "NIFA: Nonlinear IMC enhanced FPGA for efficient ML inference"
title_zh: NIFA：用于高效机器学习推理的非线性存内计算增强型 FPGA
authors: "Jiajun Hu, Ruthwik Reddy Sunketa, Lei Zhao, Archit Gajjar, Luca Buonanno, Aman Arora"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15123v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于 ReRAM 的存内计算 FPGA 推理软硬协同设计
tldr: 本研究针对传统FPGA中ReRAM存算一体（IMC）模块因ADC功耗高且仅支持静态权重计算，导致在处理Transformer等模型时效率受限的问题，提出了NIFA架构。NIFA通过引入无ADC的IMC设计，利用模拟内容寻址存储器（ACAM）在块内原生执行非线性操作和动态矩阵乘法。实验表明，该架构在CNN和Transformer任务中均实现了显著的能效与面积效率提升，为领域专用FPGA设计提供了新思路。
selection_source: fresh_fetch
motivation: 传统IMC架构因高功耗ADC及对非线性和动态矩阵乘法支持不足，限制了其在Transformer等复杂模型中的应用效率。
method: 提出一种集成无ADC存算一体块的FPGA架构，利用ACAM替代ADC以原生支持非线性运算，并优化了动态矩阵乘法的映射方案。
result: 与现有方案相比，NIFA在CNN和Transformer基准测试中分别实现了最高40倍和1.9倍的能效提升，以及4.1倍和2.5倍的面积效率提升。
conclusion: NIFA架构通过在IMC块内集成非线性处理能力，显著增强了FPGA处理多样化深度学习负载的通用性与效率。
---

## 摘要
近年来，FPGA 通过专用张量块和 BRAM 内计算提高了深度学习（DL）的推理效率。基于 ReRAM 的模拟存内计算（IMC）通过在 ReRAM 交叉阵列中直接执行向量-矩阵乘法（VMM），在计算密度和能效方面比传统数字逻辑提高了一个数量级；此前已有研究将此类 IMC 模块集成到 FPGA 中用于深度学习推理。然而，传统的 IMC 设计仅支持静态权重 VMM，而将非线性操作和动态矩阵-矩阵乘法（DIMM）留给 FPGA 逻辑阵列处理。因此，IMC 的优势主要局限于静态权重模型，而依赖频繁非线性操作和 DIMM 操作的 Transformer 模型所获得的提升有限。此外，每个 IMC 模块内的模数转换器（ADC）消耗了超过 70% 的面积和功耗，进一步限制了系统的效率和可扩展性。为了解决这些局限性，我们提出了一种新型 FPGA 架构，该架构集成了一个无 ADC 的 IMC 模块，使用模拟内容可寻址存储器（ACAM）取代传统的 ADC，从而在模块内部原生执行非线性操作。为了充分利用该模块，我们进行了感知 FPGA 的设计空间探索，在平衡 FPGA 面积、灵活性和深度学习性能的同时确定了最优的交叉阵列维度，并开发了一种高效映射方法，利用 ACAM 执行 DIMM 操作，将 IMC 的适用性扩展到注意力机制计算。在 CNN 和基于 Transformer 的基准测试中，所提架构的能效分别提高了 40 倍和 1.9 倍，面积效率分别提高了 4.1 倍和 2.5 倍。总之，该架构显著提高了 FPGA 的深度学习推理效率，并在长输入序列的 Transformer 工作负载上保持了稳健的增益，推动了领域专用 FPGA 设计的发展。

## Abstract
Recent FPGAs have improved deep learning (DL) inference efficiency through dedicated tensor blocks and in-BRAM computation. ReRAM-based analog in-memory computing (IMC) pushes efficiency further, offering an order-of-magnitude improvement in compute density and energy efficiency over conventional digital logic by performing vector-matrix multiplication (VMM) directly within the ReRAM crossbar; prior work has integrated such IMC blocks into FPGAs for DL inference. However, conventional IMC designs support only static-weight VMM, leaving nonlinear operations and dynamic matrix-matrix multiplication (DIMM) to the FPGA fabric. As a result, the benefits of IMC are largely confined to static-weight models, whereas Transformer-based models, which rely on frequent nonlinear and DIMM operations, gain only limited improvement. Moreover, the ADCs within each IMC block consume more than 70% of its area and power, further limiting system efficiency and scalability. To address these limitations, we propose a novel FPGA architecture that integrates an ADC-free IMC block, replacing the conventional ADC with analog content-addressable memories (ACAMs) that natively perform nonlinear operations inside the block. To fully exploit this block, we conduct an FPGA-aware design-space exploration that determines optimal crossbar dimensions while balancing FPGA area, flexibility, and DL performance, and we develop an efficient mapping that leverages ACAMs to carry out DIMM operations, extending the applicability of IMC to attention computation. On CNN and Transformer-based benchmarks, the proposed architecture achieves up to 40x and 1.9x higher energy efficiency and 4.1x and 2.5x higher area efficiency, respectively. Overall, it significantly improves FPGA DL inference efficiency and sustains robust gains on Transformer-based workloads across long input sequences, advancing domain-specialized FPGA design.

---

## 论文详细总结（自动生成）

这篇论文提出了 **NIFA**，一种集成非线性存内计算（IMC）模块的新型 FPGA 架构，旨在解决传统模拟存内计算在处理 Transformer 等现代深度学习模型时面临的效率瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然基于 ReRAM 的模拟存内计算（IMC）能显著提升向量-矩阵乘法（VMM）的效率，但在 FPGA 集成中存在三大痛点：
    1.  **ADC 开销巨大**：传统 IMC 模块中的模数转换器（ADC）占据了超过 70% 的面积和功耗。
    2.  **非线性算子瓶颈**：激活函数（如 ReLU、Softmax）通常需返回 FPGA 软逻辑处理，造成频繁的数据搬运。
    3.  **动态计算支持不足**：Transformer 中的动态矩阵乘法（DIMM，如 $QK^T$）无法直接利用静态权重的 IMC 模块，导致加速效果受限。
*   **整体含义**：NIFA 通过引入无 ADC 的非线性点积引擎（NL-DPE），利用模拟内容寻址存储器（ACAM）原生支持非线性运算和对数域计算，从而全面提升 CNN 和 Transformer 的推理效率。

### 2. 方法论
*   **核心思想**：用 **ACAM（模拟内容寻址存储器）** 替代传统的 ADC。ACAM 不仅能将模拟电流转换为数字信号，还能通过预编程的阈值直接实现非线性映射（如 ReLU、Exp、Log）。
*   **关键技术细节**：
    *   **NL-DPE 模块**：包含 ReRAM 交叉阵列（用于 VMM）和 ACAM 单元阵列。ACAM 充当可编程的查表机，执行分段线性近似的非线性函数。
    *   **对数域注意力机制映射**：针对 Transformer 的 DIMM 操作，NIFA 将乘法转换为对数域的加法。利用 ACAM 执行 $Log$ 和 $Exp$ 变换，将复杂的矩阵乘法简化为 FPGA 逻辑中的加法，从而复用 IMC 模块。
    *   **感知 FPGA 的设计空间探索（DSE）**：
        *   **第一轮**：筛选最优交叉阵列尺寸（如 $1024 \times 128$），平衡面积与计算粒度。
        *   **第二轮**：确定 IMC 模块在 FPGA 阵列中的面积占比（Area Budget），通过 **FlexScore** 指标权衡深度学习性能与通用 FPGA 逻辑的灵活性。
    *   **噪声感知微调（NAF）**：在软件端进行离线训练，补偿 ReRAM 器件的非理想特性（如电导漂移），确保 INT8 精度下的模型准确率。

### 3. 实验设计
*   **数据集/场景**：
    *   **CNN 任务**：ResNet-9, VGG-11。
    *   **Transformer 任务**：BERT-Tiny（涵盖不同序列长度 $N=128$ 到 $2048$）。
    *   **非 DL 基准（用于灵活性测试）**：bgm, LU8PEEng, stereovision1, arm_core。
*   **Benchmark（对比方法）**：
    *   **Azure-Lily**：目前最先进的（SOTA）集成模拟 IMC 的 FPGA 架构（基于 ADC 设计）。
*   **评估工具**：使用 VTR（Verilog-to-Routing）进行面积和频率评估，结合自定义循环级模拟器进行功耗和延迟分析。

### 4. 资源与算力
*   **算力说明**：论文主要关注**硬件架构设计与仿真**，未详细说明训练模型所用的具体 GPU 集群型号。
*   **硬件参数**：仿真基于 22nm 工艺节点。IMC 模块在 0.9GHz 频率下运行，单块提供高达 16.4~32.8 TOPS (INT8) 的峰值算力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 12 种不同的交叉阵列配置。
    *   针对 6 种不同尺寸的全连接层（FC）进行了微基准测试。
    *   进行了端到端的 CNN 和 Transformer 推理实验。
*   **充分性评价**：实验设计较为充分。作者不仅关注了深度学习的加速比，还通过 FlexScore 客观评估了引入专用硬核对 FPGA 通用路由和逻辑资源的影响，体现了架构设计的公平性。

### 6. 主要结论与发现
*   **能效飞跃**：在 CNN 任务中，NIFA 比 Azure-Lily 提升了最高 **40 倍**的能效；在 Transformer 任务中提升了 **1.9 倍**。
*   **面积效率**：面积效率提升了 **2.5x 至 4.1x**，这主要归功于去除了笨重的 ADC 阵列。
*   **Transformer 扩展性**：随着输入序列长度增加，注意力机制的计算复杂度呈平方增长，NIFA 的对数域加速方案显示出更强的稳健性，性能优势随序列长度增加而扩大。

### 7. 优点
*   **架构创新**：首次在 FPGA 领域提出利用 ACAM 实现无 ADC 的 IMC 架构，解决了模拟计算最核心的面积/功耗瓶颈。
*   **功能完备**：不仅支持线性 VMM，还通过硬核化支持了非线性激活和动态矩阵乘法，使 IMC 真正适用于 Transformer 模型。
*   **系统级考量**：通过两轮 DSE 科学地确定了硬核化的比例，避免了过度专用化导致的 FPGA 灵活性丧失。

### 8. 不足与局限
*   **精度限制**：目前主要针对 **INT8** 精度，对于需要更高精度（如 FP16/BF16）的科学计算或大模型微调场景支持不足。
*   **物理设计简化**：论文未深入讨论 ReRAM 编程所需的高压电源轨在传统低压 FPGA 逻辑中的布线挑战和可靠性风险。
*   **软件栈依赖**：该架构高度依赖“噪声感知微调”软件，这意味着模型部署前需要复杂的离线处理，无法直接运行现成的预训练权重。

（完）
