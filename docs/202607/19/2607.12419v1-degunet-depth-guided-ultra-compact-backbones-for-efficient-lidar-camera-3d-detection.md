---
title: "DeGuNet: Depth-Guided Ultra-Compact Backbones for Efficient LiDAR-Camera 3D Detection"
title_zh: DeGuNet：用于高效激光雷达-相机3D检测的深度引导超紧凑骨干网络
authors: "Haifa Zhang, Yijing Wang, Peixi Peng, Zhiqiang Zuo"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12419v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 专为高效 3D 检测设计的超紧凑骨干网络
tldr: "针对自动驾驶中多模态3D检测依赖庞大2D预训练骨干网络导致的参数冗余和结构失配问题，本文提出DeGuNet。这是一种超轻量、即插即用的深度引导图像骨干网络，通过稀疏感知特征提取机制实现多视图图像与LiDAR深度的有效对齐。在nuScenes数据集上，它显著降低了66.5%的显存占用，提升了推理速度，并大幅提高了检测精度，为高效多模态感知提供了新范式。"
selection_source: fresh_fetch
motivation: 现有的多模态框架依赖庞大的2D预训练骨干网络，存在严重的参数冗余且难以处理LiDAR投影的极端稀疏性。
method: 提出DeGuNet，一种超紧凑的即插即用骨干网络，利用稀疏感知机制将多视图图像与非结构化LiDAR深度进行深度引导对齐。
result: "在nuScenes上，该方法减少了高达66.5%的显存消耗，推理速度提升1.16倍，且mAP最高提升了6.20个百分点。"
conclusion: DeGuNet证明了通过深度引导的轻量化设计可以消除架构冗余，实现更高效且精准的多模态3D目标检测。
---

## 摘要
在自动驾驶感知中，激光雷达与相机模态的融合已成为3D目标检测的主流范式。然而，当前的多模态框架严重依赖于在2D语义任务上预训练的大规模视觉骨干网络。这种依赖引入了大量的参数冗余和结构失配，因为2D先验难以处理鸟瞰图（BEV）几何所需的激光雷达投影的极端稀疏性。为了解决这一问题，我们提出了DeGuNet，这是一种专为深度引导表征学习设计的超紧凑且即插即用的图像骨干网络。通过引入稀疏感知特征提取机制，DeGuNet有效地将多视图图像与非结构化激光雷达深度对齐，同时严格防止无效区域的污染。在nuScenes数据集上的广泛实验证明了DeGuNet广泛的即插即用适用性和卓越的效率。当集成到现有的基准模型中时，它从根本上消除了架构冗余，将GPU显存消耗降低了高达66.5%，并实现了1.16倍的推理加速。同时，DeGuNet实现了高达6.20的绝对mAP增益，为参数高效的多模态3D感知建立了新的范式。

## Abstract
In autonomous driving perception, the fusion of LiDAR and camera modalities has become the dominant paradigm for 3D object detection. However, current multi-modal frameworks heavily rely on massive visual backbones pretrained on 2D semantic tasks. This reliance introduces substantial parameter redundancy and a structural misalignment, as 2D priors are ill-equipped to handle the extreme sparsity of LiDAR projections required for Bird's-Eye-View geometry. To address this, we present DeGuNet, an ultra-compact and plug-and-play image backbone explicitly designed for depth-guided representation learning. By incorporating sparsity-aware feature extraction mechanisms, DeGuNet effectively aligns multi-view images with unstructured LiDAR depth while strictly preventing invalid-region contamination. Extensive experiments on the nuScenes dataset demonstrate DeGuNet's broad plug-and-play applicability and superior efficiency. When integrated into established baselines, it fundamentally eliminates architectural redundancy, reducing GPU memory consumption by up to 66.5% and achieving a 1.16x inference speedup. Concurrently, DeGuNet delivers up to a 6.20 absolute mAP gain, establishing a new paradigm for parameter-efficient multi-modal 3D perception.

---

## 论文详细总结（自动生成）

这是一份关于论文《DeGuNet: Depth-Guided Ultra-Compact Backbones for Efficient LiDAR-Camera 3D Detection》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前多模态（LiDAR+相机）3D目标检测框架过度依赖庞大的、在2D语义任务上预训练的视觉骨干网络（如 ResNet, Swin-T）。
*   **研究动机**：
    *   **参数冗余**：视觉分支通常占据模型总参数量的50%到86%，但在融合中仅起辅助作用。
    *   **结构失配**：2D预训练特征缺乏3D空间感知能力，且标准卷积在处理极端稀疏的LiDAR投影（98%以上像素为空白）时，会受到无效区域的“污染”，导致特征退化。
    *   **效率瓶颈**：巨大的显存占用和计算开销限制了在自动驾驶嵌入式设备上的部署。

### 2. 方法论
DeGuNet 提出了一种**超紧凑（0.31M参数）且即插即用**的图像骨干网络，核心思想是通过深度引导和稀疏感知机制实现特征对齐。
*   **两阶段生命周期**：
    1.  **几何引导预训练**：在深度补全任务上预训练，强制网络将2D纹理映射到3D几何空间。
    2.  **端到端检测集成**：丢弃深度解码器，将预训练的编码器作为图像分支集成到检测框架中。
*   **关键技术组件**：
    *   **MPIR（掩码部分反转残差块）**：在局部特征提取中引入“部分卷积（Partial Conv）”，仅在有效像素上更新权重，防止无效区域污染。
    *   **MMViT（掩码移动视觉Transformer）**：在全局建模中引入掩码注意力机制，确保全局上下文聚合仅限于有效几何节点。
    *   **渐进式引导模块（Guide Module）**：在多个尺度上将高分辨率RGB语义注入稀疏的几何流中，实现结构化增强。
    *   **LiteNeck**：轻量化颈部网络，将特征解耦为深度对数和上下文特征，适配主流的 LSS（Lift-Splat-Shoot）架构。

### 3. 实验设计
*   **数据集**：使用自动驾驶主流数据集 **nuScenes**（包含1000个场景，提供360° LiDAR和6路相机图像）。
*   **Benchmark 与对比方法**：
    *   **基准框架**：集成了 BEVFusion, GraphBEV, EA-LSS, IS-Fusion 等 SOTA 模型。
    *   **对比骨干网络**：对比了 ResNet, MobileViT-XS/XXS, Swin-T 等。
    *   **评估指标**：mAP（平均精度）、NDS（nuScenes检测得分）、参数量、FPS（推理速度）、显存占用。

### 4. 资源与算力
*   **硬件环境**：推理速度（FPS）和显存占用是在单张 **NVIDIA A100 GPU** 上测量的（Batch Size = 1）。
*   **训练细节**：论文提到遵循了各基准框架（如 MMDetection3D）的官方配置，包括学习率、优化器和训练计划。具体训练总时长未在文中明确给出，但强调了其预训练阶段是独立且高效的。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **跨基准验证**：在 4 个主流多模态框架上进行了即插即用实验。
    *   **消融实验**：详细分析了 MPIR、Guide 模块、MMViT、LiteNeck 等组件的贡献，以及不同融合阶段的影响。
    *   **性能对比**：与超过 15 种 SOTA 方法进行了参数效率和精度的综合对比。
*   **充分性与公平性**：实验设计较为客观，通过“参数效率（Eff.）”这一指标（即每百万参数带来的 mAP 增益）公平地衡量了不同规模骨干网络的实际贡献。

### 6. 主要结论与发现
*   **极高的参数效率**：DeGuNet 仅需 0.31M 参数，在 IS-Fusion 框架下实现了 20.00 的参数效率得分，远超传统骨干网络（通常低于 0.30）。
*   **显著的资源节省**：相比 BEVFusion 原版，集成 DeGuNet 后 **GPU 显存消耗降低了 66.5%**（从 20.46GB 降至 6.86GB）。
*   **性能不降反升**：在大幅减小模型的同时，mAP 普遍提升了 0.2 到 0.9 个百分点，在某些配置下绝对增益高达 6.20 mAP。
*   **稀疏感知是关键**：实验证明，单纯改变预训练任务而不改变架构（如用轻量级 ResNet 做深度补全）效果有限，必须使用稀疏感知架构。

### 7. 优点
*   **即插即用**：作为通用基础设施，能无缝替换现有 LSS 类检测器的重型图像分支。
*   **极致轻量化**：0.31M 的参数量极大地缓解了多模态系统的计算负担。
*   **结构对齐**：通过深度引导预训练，从根本上解决了 2D 语义与 3D 几何之间的表征失配问题。

### 8. 不足与局限
*   **语义抽象能力上限**：由于采用了极简设计，对于需要极高语义理解能力的“长尾稀少类别”，其特征表达能力可能存在上限。
*   **依赖 LiDAR 引导**：该方法在预训练和推理时均需要 LiDAR 投影引导，不适用于纯视觉（Camera-only）检测场景。
*   **实验覆盖范围**：虽然在 nuScenes 上表现优异，但未在 Waymo 或 Argoverse 等其他大型数据集上验证泛化性。

（完）
