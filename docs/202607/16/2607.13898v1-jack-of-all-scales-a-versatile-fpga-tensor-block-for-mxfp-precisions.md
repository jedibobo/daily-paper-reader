---
title: "Jack of All Scales: A Versatile FPGA Tensor Block for MXFP Precisions"
title_zh: 尺度全能：一种支持 MXFP 精度的通用 FPGA 张量块
authors: "Marwan Mekhemer, Ahmed Elsousy, Balaji Venkatesh, Raphael Rowley, Vaughn Betz, Nachiket Kapre, Andrew Boutros"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13898v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对低精度格式的FPGA张量块设计
tldr: 针对现代深度学习对低精度MXFP格式的需求，本文研究了FPGA中DSP块对MXFP的支持现状。针对现有架构在处理MXFP8和部分MXFP6格式时的局限性，提出了一种改进的FPGA张量块架构。该设计在保持向后兼容的同时，实现了对所有MXFP精度的原生支持，显著提升了算术密度和吞吐量，且面积开销极小，为未来AI加速器设计提供了高效的硬件支持方案。
selection_source: fresh_fetch
motivation: 现有的FPGA DSP块在张量模式下无法原生支持MXFP8和部分MXFP6格式，导致低精度推理效率受限且缺乏灵活性。
method: 通过对DSP块内部张量模式架构进行针对性修改，使其能够灵活适配多种MXFP精度，并利用ASAP7 PDK评估了面积成本与性能权衡。
result: "改进后的DSP块在仅增加1.8%总芯片面积的情况下，使所有支持的MXFP格式平均吞吐量提升了4.2倍。"
conclusion: 该研究证明了通过微调FPGA硬件架构，可以高效且低成本地支持多样化的低精度数值格式，显著增强了FPGA在深度学习推理中的竞争力。
---

## 摘要
现代深度学习工作负载日益依赖窄数值格式，以提高效率并减少内存占用。最近标准化的微缩放浮点（MXFP）系列格式（包括 MXFP8、MXFP6 和 MXFP4）为低精度推理提供了一种实用的方法，但当前 FPGA 架构中的数字信号处理（DSP）块对这些格式的原生支持有限。在这项工作中，我们首先对 Altera Agilex-5 FPGA 上的 MXFP 点积实现进行了全面表征，探索了涵盖纯软逻辑、定点、浮点和张量模式下的 DSP 块等一系列策略。我们的结果表明，虽然张量模式为 MXFP4 (E2M1) 和 MXFP6 (E2M3) 提供了最高的算术密度，但它无法实现 MXFP6 (E3M2) 或任何 MXFP8 精度，从而迫使设计人员退而求其次，使用密度较低的替代方案。针对这一差距，我们建议对 DSP 块的内部张量模式架构进行针对性修改，从而在保持向后兼容性的同时，实现对所有 MXFP 精度的原生支持。我们使用基于开源 ASAP7 PDK 实现的 Agilex-5 DSP 块核心的简化版本，估算了这些修改的面积成本。我们评估了多种改进的 DSP 块设计，这些设计在格式覆盖范围、算术密度和面积开销之间进行了权衡。我们首选的设计点将 DSP 块面积增加了 36%，仅相当于 FPGA 总芯片面积的 1.8%。我们通过比较所有 MXFP 精度下的脉动阵列矩阵乘法器实现，评估了增强型 DSP 块在器件层面的影响，并将现有架构上的最佳策略与利用我们改进的 DSP 块的设计进行了对比。我们的结果表明，在所有支持的 MXFP 格式中，平均吞吐量提高了 4.2 倍。

## Abstract
Modern deep learning workloads increasingly rely on narrow numerical formats to improve efficiency and reduce memory footprint. The recently standardized microscaling floating-point (MXFP) family of formats, including MXFP8, MXFP6, and MXFP4, offers a practical approach to low-precision inference, yet the digital signal processing (DSP) blocks in current FPGA architectures offer limited native support for these formats. In this work, we first present a comprehensive characterization of MXFP dot product implementations on Altera Agilex-5 FPGAs, exploring a range of strategies spanning pure soft logic, DSP blocks in fixed-point, floating-point, and tensor modes. Our results show that while the tensor mode delivers the highest arithmetic density for MXFP4 (E2M1) and MXFP6 (E2M3), it cannot implement MXFP6 (E3M2) or any MXFP8 precisions, forcing designers to fall back to lower-density alternatives. Motivated by this gap, we propose targeted modifications to the DSP block's internal tensor-mode architecture that enable native support for all MXFP precisions while retaining backward compatibility. We estimate the area cost of these modifications using a simplified version of the Agilex-5 DSP block core implemented using the open-source ASAP7 PDK. We evaluate a variety of modified DSP block designs that present a tradeoff between format coverage, arithmetic density, and area overhead. Our preferred design point increases the DSP tile area by 36%, corresponding to only 1.8\% of the total FPGA die area. We evaluate the device-level impact of our enhanced DSP block by comparing systolic array matrix multiplier implementations across all MXFP precisions, contrasting the best-available strategies on the existing architecture against designs leveraging our modified DSP block. Our results demonstrate an average throughput improvement of 4.2x across all supported MXFP formats.

---

## 论文详细总结（自动生成）

这篇论文针对现代深度学习中新兴的微缩放浮点（MXFP）格式，提出了一种改进的 FPGA DSP（数字信号处理）块架构设计。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着深度学习模型规模的扩大，低精度数值格式（如 MXFP8/6/4）成为提升推理效率的关键。然而，现有的 FPGA 硬件（如 Altera Agilex-5）虽然拥有“张量模式”的 DSP 块，但对 MXFP 格式的支持并不全面。
*   **研究动机**：
    *   **硬件鸿沟**：现有 DSP 张量模式仅能原生支持 MXFP4 和部分 MXFP6，对于精度更高的 MXFP8 或特定 MXFP6 格式，必须退回到效率极低的软逻辑（LUT）或普通定点模式。
    *   **效率瓶颈**：软逻辑实现占用大量面积且频率较低，限制了 FPGA 在高性能 AI 推理中的竞争力。
    *   **标准化趋势**：OCP 联盟已将 MXFP 标准化，硬件层面需要更通用的支持。

### 2. 方法论：核心思想与技术细节
论文通过四个迭代步骤改进了 DSP 块的张量模式架构：
*   **核心思想**：将 DSP 块从仅支持定点/标准浮点，改造为能够原生处理“块缩放（Block-scaled）”的窄浮点格式，同时保持向后兼容。
*   **关键技术细节**：
    *   **原生 MXFP 输入**：修改输入端，使其直接接收 MXFP 编码，而非预先在软逻辑中转换。
    *   **尾数乘法与对齐**：在 DSP 内部集成小型尾数乘法器，并引入由指数和控制的**桶形移位器（Barrel Shifters）**，实现乘积的动态对齐。
    *   **扩展 CSA 树**：为了支持 MXFP8 较大的动态范围，扩展了进位保留加法器（CSA）归约树的位宽。
    *   **点积长度优化（Iteration 4）**：将基准点积长度从 10 调整为 8（dot-8），这更符合 2 的幂次计算习惯，提高了硬件利用率并减少了 72b 输入引脚的需求。
    *   **面积优化**：利用 COFFE 工具优化全定制路由接口，确保修改不增加额外的 I/O 引脚负担。

### 3. 实验设计
*   **评估场景**：
    1.  **算术密度表征**：在 Agilex-5 FPGA 上对比软逻辑、定点打包、FP16 向量模式和原生张量模式的性能。
    2.  **ASIC 面积估算**：使用 **ASAP7 (7nm)** 开源 PDK 评估改进后 DSP 块的硅片面积。
    3.  **器件级评估**：构建**脉动阵列（Systolic Array）**矩阵乘法器，模拟填满整个 FPGA 芯片后的吞吐量。
*   **Benchmark**：以 Agilex-5 原始 DSP 架构作为基准（Baseline）。
*   **对比方法**：对比了纯软逻辑实现、基于定点打包的 DSP 实现、以及论文提出的多种改进迭代版本。

### 4. 资源与算力
*   **EDA 工具与 PDK**：使用了 Cadence Genus (合成) 和 Innovus (布局布线)，采用 ASAP7 7nm 预测性设计套件。
*   **硬件建模**：使用 COFFE 2 工具进行全定制电路建模。
*   **算力说明**：论文未涉及 GPU 训练，其研究对象是 FPGA 硬件架构。实验通过 Quartus Prime Pro 25.3 软件进行 FPGA 映射仿真，评估主频（约 400-500 MHz）和吞吐量（TFLOPS）。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了 MXFP 家族的所有 5 种主要子格式（E2M1, E2M3, E3M2, E4M3, E5M2）。
*   **消融与权衡研究**：论文详细对比了 4 个设计迭代版本，讨论了支持 MXFP8 带来的面积开销与性能收益之间的权衡。
*   **充分性评价**：实验设计较为充分，不仅有底层的 ASIC 面积分析，还有顶层的系统级脉动阵列仿真，客观地展示了硬件修改对实际应用（矩阵乘法）的增益。

### 6. 主要结论与发现
*   **性能提升**：改进后的 DSP 块使 MXFP 格式的平均吞吐量提升了 **4.2 倍**。对于原本不支持的 MXFP8，性能提升高达 **9.1-15.3 倍**。
*   **面积成本极低**：虽然单个 DSP 块面积增加了 36%，但由于 DSP 仅占 FPGA 总面积的一小部分，最终反映在**整片芯片面积上仅增加 1.8%**。
*   **资源释放**：原生支持减少了对软逻辑（ALM）的依赖，使更多资源可用于控制逻辑和数据搬运。

### 7. 优点
*   **实用性强**：设计考虑了向后兼容性，且不增加 I/O 引脚，易于集成到现有的 FPGA 架构中。
*   **开源贡献**：作者开源了所有 MXFP 点积单元、改进的 DSP 块 RTL 和脉动阵列基准测试代码。
*   **架构洞察**：通过将 dot-10 改为 dot-8，巧妙地平衡了利用率和面积。

### 8. 不足与局限
*   **模型简化**：为了便于 ASIC 评估，论文使用的是简化版的 DSP 核心，未包含商业 DSP 块中极其复杂的 SRAM 配置电路和所有工作模式。
*   **应用局限**：研究重点完全集中在**推理（Inference）**任务上，未深入探讨这些格式在 FPGA 上进行低精度训练（Training）的潜力。
*   **特殊编码支持**：在 MXFP8 模式下，为了节省面积，未完全支持无穷大（Infinity）和非数（NaN）的特殊处理逻辑。

（完）
