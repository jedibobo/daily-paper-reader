---
title: "No Attention, No Problem: DPU-Aware Attention Approximation in Modern YOLO on FPGA"
title_zh: 无需注意力，亦无问题：FPGA 上现代 YOLO 的 DPU 感知注意力近似
authors: "Suraj Karki, Qazi Arbab Ahmed, Thorsten Jungeblut"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.13106v1"
tags: ["query:hwnas"]
score: 9.0
evidence: FPGA上感知DPU的架构和硬件兼容的注意力近似
tldr: 本研究针对边缘端FPGA部署，提出了一种适用于AMD DPU的注意力机制近似架构，旨在优化现代YOLO变体（如YOLOv26和YOLOv11）的性能。通过替换不支持的激活函数、使用1x1卷积替代分割操作以及重构空间注意力机制，解决了硬件兼容性问题。在多个数据集上的实验表明，该方法在保持较高检测精度的同时，显著提升了推理速度并大幅降低了功耗，为嵌入式实时目标检测提供了高效方案。
selection_source: fresh_fetch
motivation: 现代YOLO模型中的注意力机制和特定算子与FPGA上的DPU硬件不兼容，导致部署困难且效率低下。
method: 通过替换不支持的激活函数、用1x1卷积代替分割操作以及实现DPU友好的空间注意力近似来重构模型架构。
result: YOLOv26n在ZCU104上实现了最高34.05 FPS的吞吐量，且功耗比现有技术降低了约3倍。
conclusion: 该研究证明了通过硬件感知的架构优化，可以在FPGA上高效运行复杂的注意力增强型目标检测模型。
---

## 摘要
边缘端人工智能（AI）加速近期推动了实时目标检测的进展。边缘设备上的目标检测需要在精度、速度和功耗效率之间取得平衡。本文针对部署在 AMD FPGA 上的基于注意力的 YOLO 变体，提出了一种定制的感知深度学习处理器单元（DPU）的架构。具体而言，我们在 Xilinx ZCU104 上针对标准和旋转目标检测任务，对 YOLOv26 和 YOLOv11 这两种现代基于注意力的 YOLO 变体进行了评估和基准测试。我们替换了不支持的激活函数，用 1x1 卷积替代了分割（split）操作，并以 DPU 兼容的方式近似了空间注意力机制。随后，所有模型在 COCO、Pascal VOC、KITTI、DOTA、DIOR-R 以及一个内部人体存在数据集等六个基准数据集上进行了训练和评估，并在所有八种 DPU 配置（从 B512 到 B4096）下，就 mAP、FPS、延迟、功耗和资源利用率进行了基准测试。值得注意的是，YOLOv26n 和 YOLOv26n-OBB 在标准和旋转检测中分别实现了 34.05 和 29.55 FPS 的最高端到端吞吐量；尽管由于量化导致精度平均绝对下降了 5%，但与现有技术相比，其功耗降低了约 3 倍。

## Abstract
Edge-based Artificial Intelligence (AI) acceleration has recently improved progress in real-time object detection. Object detection on edge devices requires a balance between accuracy, speed, and power efficiency. This paper proposes a customized Deep Learning Processor Unit (DPU)-aware architecture for attention-based YOLO variants deployed on AMD FPGAs. Specifically, we evaluate and benchmark YOLOv26 and YOLOv11, two modern attention-based YOLO variants, on the Xilinx ZCU104 across both standard and oriented object detection tasks. We replace unsupported activation functions, substitute split operations with 1x1 convolutions, and approximate the spatial attention mechanism in a DPU-compatible way. All models are then trained and evaluated across six benchmark datasets such as COCO, Pascal VOC, KITTI, DOTA, DIOR-R, and an in-house human presence dataset, and benchmarked across all eight DPU configurations (B512 to B4096) in terms of mAP, FPS, latency, power, and resource utilization. Notably, YOLOv26n and YOLOv26n-OBB deliver the highest end-to-end throughput at 34.05 and 29.55 FPS for standard and oriented detection, respectively, with an average of 5% absolute reduction in accuracy due to quantization while achieving up to approximately 3x lower power consumption compared with the state of the art.

---

## 论文详细总结（自动生成）

这篇论文针对现代 YOLO 模型在 FPGA 上的部署难题，提出了一套硬件感知的优化方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：现代 YOLO 变体（如 YOLOv11 和 YOLOv26）引入了复杂的注意力机制（如 C2PSA）和特定的算子（如 SiLU 激活函数、Split 分割操作），这些算子在 AMD FPGA 的 DPU（深度学习处理器单元）上缺乏原生硬件支持。
*   **研究动机**：若直接部署，不支持的算子会回退到 CPU 执行，导致严重的性能瓶颈。论文旨在通过“DPU 感知”的架构近似，在保持现代模型精度的同时，实现高效的 FPGA 端到端加速，涵盖标准目标检测和旋转框（OBB）检测任务。

### 2. 方法论：核心思想与关键技术
论文的核心思想是**“硬件软件协同设计”**，通过等效或近似的 DPU 友好算子替换原有的复杂算子：
*   **激活函数替换**：将 DPU 不支持的 **SiLU** 替换为硬件高效的 **ReLU**。
*   **Split 操作替代**：DPU 不支持特征图分割。作者使用两个 **1x1 卷积** 来模拟 Split 操作，通过将权重初始化为二值选择矩阵（对角线为 1，其余为 0），实现通道的精确提取。
*   **注意力机制近似（核心创新）**：
    *   原始 C2PSA 包含 Reshape、Transpose、矩阵乘法和 Softmax，这些在 DPU 上极慢。
    *   **近似方案**：使用并行 1x1 卷积提取 Q、K、V；使用逐元素乘法（$Q \odot K$）近似注意力权重；用 **Hard-Sigmoid** 替代 Softmax 进行归一化；最后通过 1x1 卷积进行投影。
*   **YOLOv26 优化**：移除分布焦点损失（DFL），改用直接边界框回归，并支持无 NMS（非极大值抑制）推理架构以减少后处理开销。

### 3. 实验设计
*   **数据集与场景**：
    *   **标准检测**：Pascal VOC、COCO、KITTI、自研人体检测数据集。
    *   **旋转框检测（OBB）**：DOTA、DIOR-R、自研人体检测数据集。
*   **Benchmark（基准）**：
    *   对比了 YOLOv8、YOLOv11、YOLOv26 的原始模型与修改后的模型。
    *   在 AMD **ZCU104** 开发板上测试了从 **B512 到 B4096** 全部 8 种 DPU 配置。
*   **对比指标**：mAP@0.5:0.95、FPS（DPU 纯算力 vs 端到端）、延迟、功耗、资源利用率。

### 4. 资源与算力
*   **训练阶段**：使用了高性能 **GPU 集群** 进行模型训练，开启了混合精度（FP16）以提高效率。文中未明确具体 GPU 型号和训练时长。
*   **部署阶段**：使用 AMD Vitis AI 工具链，将模型量化为 **INT8**。
*   **硬件平台**：Xilinx ZCU104（搭载 Zynq UltraScale+ MPSoC EV 芯片）。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 3 种 YOLO 版本、2 种检测任务（水平/旋转）、6 个主流数据集、8 种 DPU 架构配置。
*   **充分性**：实验设计非常全面。不仅验证了算法在不同任务上的通用性，还通过不同 DPU 配置展示了性能随硬件资源增加的缩放规律。
*   **客观性**：作者诚实地报告了量化带来的精度损失（平均约 5%-7%），并指出了后处理（NMS）是目前的性能瓶颈，体现了客观的学术态度。

### 6. 主要结论与发现
*   **性能卓越**：YOLOv26n 在 B4096 DPU 上实现了 **34.05 FPS**（标准）和 **29.55 FPS**（旋转）的端到端吞吐量。
*   **功耗优势**：相比现有技术，该方案的功耗降低了约 **3 倍**（在 4.8W 至 8.8W 之间）。
*   **精度权衡**：架构修改导致精度下降约 3%，INT8 量化进一步导致约 4% 的下降，总精度损失在可接受范围内。
*   **瓶颈发现**：尽管 DPU 算力随架构升级（B512 -> B4096）显著提升，但由于后处理（NMS）在 CPU 上运行，端到端 FPS 的提升并不成正比。

### 7. 优点
*   **实用性强**：直接解决了最新 YOLO 模型无法在商用 FPGA 工具链上高效运行的痛点。
*   **算子创新**：用 1x1 卷积巧妙替代 Split 操作，以及 DPU 友好的注意力近似方案，具有很高的工程参考价值。
*   **覆盖面广**：罕见地在 FPGA 上系统性地对比了旋转框（OBB）检测任务。

### 8. 不足与局限
*   **后处理瓶颈**：虽然模型推理很快，但 NMS 仍然在 ARM CPU 上运行，限制了整体系统的实时性。
*   **精度损失**：对于某些对精度极度敏感的应用，5%-7% 的 mAP 下降可能是一个障碍。
*   **量化策略**：仅使用了后训练量化（PTQ），未尝试量化感知训练（QAT），后者可能进一步缩小精度差距。

（完）
