---
title: "Realizable N:M Sparse Transformer Inference via Search-Kernel Co-Design"
title_zh: "通过搜索-算子协同设计实现可落地的 N:M 稀疏 Transformer 推理"
authors: "Yiming Liu, Wenqi Lou, Zhiguang Wang, Zhiwei Ke, Fengrui Zuo, Chao Wang, Xuehai Zhou"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12505v2"
tags: ["query:hwnas"]
score: 9.0
evidence: "针对 N:M 稀疏 ViT 推理的软硬件协同设计框架，包含自定义 CUDA 内核"
tldr: M稀疏推理框架。硬件层面，设计了MD-SpMM内核，通过微密集数据流和自适应并行提高GPU利用率；软件层面，采用三阶段启发式搜索在延迟预算下优化层级稀疏配置。该框架在保持精度的同时实现了超过2.2倍的端到端加速，解决了稀疏性理论节省与实际加速不匹配的难题。
selection_source: fresh_fetch
motivation: M稀疏技术在GPU上往往无法将理论上的计算量减少转化为实际的端到端推理加速。
method: 提出软硬件协同设计框架，包含对Tensor Core友好的MD-SpMM稀疏内核以及基于延迟约束的三阶段启发式稀疏搜索算法。
result: 在多种ViT模型和GPU平台上实现了超过2.2倍的延迟加速，且在相同延迟约束下表现出更优的精度。
conclusion: M稀疏Transformer的高效推理。
---

## 摘要
视觉 Transformer (ViT) 取得了极高的准确率，但推理延迟也较高。半结构化 N:M 稀疏可以降低计算成本，但其理论上的节省往往无法在现代 GPU 上转化为成比例的端到端加速。这种不匹配的原因在于，部署延迟不仅取决于计算量的减少，还取决于稀疏情况下的执行规整性和硬件调度。因此，要实现实际加速，需要对稀疏执行和稀疏配置进行协同设计。为此，我们提出了一个用于 N:M 稀疏 ViT 推理的软硬件协同设计框架。在硬件方面，我们设计了 MD-SpMM，这是一种 N:M 稀疏 CUDA 算子，它将稀疏 GEMM 重新组织为微密集（micro-dense）、Tensor Core 对齐的数据流，并使用推理感知的自适应并行性来维持利用率。在软件方面，我们在明确的端到端延迟预算下，使用带有约束松弛的三阶段启发式搜索进行逐层稀疏度搜索，以避免过早收敛并实现部署感知的稀疏度分配。在多个 ViT/Swin 模型和 GPU 平台上的实验表明，该框架在保持相当准确率的同时实现了超过 2.2 倍的延迟加速，并在相同的延迟约束下提供了更优的准确率。源代码已在 https://github.com/liuganhuo/realizable-nm-sparse-transformer 公开。

## Abstract
Vision Transformers (ViTs) achieve strong accuracy but incur high inference latency. Semi-structured N:M sparsity can reduce arithmetic cost, yet its theoretical savings often fail to translate into proportional end-to-end speedups on modern GPUs. This mismatch arises because deployment latency depends not only on arithmetic reduction but also on execution regularity and hardware scheduling under sparsity. Achieving practical acceleration, therefore, requires coordinated design across sparse execution and sparsity configuration. To this end, we propose a hardware-software co-design framework for N:M sparse ViT inference. On the hardware side, we design MD-SpMM, an N:M sparse CUDA kernel that reorganizes sparse GEMM into micro-dense, Tensor-Core-aligned dataflow and uses inference-aware adaptive parallelism to sustain utilization. On the software side, we perform layer-wise sparsity search under explicit end-to-end latency budgets using a three-stage heuristic search with constraint relaxation to avoid premature convergence and enable deployment-aware sparsity allocation. Experiments on multiple ViT/Swin models and GPU platforms show that the framework achieves over 2.2x latency speedup while maintaining comparable accuracy and delivering superior accuracy under the same latency constraint. The source code is publicly available at https://github.com/liuganhuo/realizable-nm-sparse-transformer.

---

## 论文详细总结（自动生成）

这篇论文针对 Vision Transformer (ViT) 在实际部署中“稀疏性不等于加速”的痛点，提出了一种软硬件协同设计的框架。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：半结构化 N:M 稀疏性（如 2:4 稀疏）理论上能减少 50% 的计算量，但在现代 GPU（尤其是 INT8 量化推理）上，由于执行不规整、内存访问开销以及硬件调度等因素，往往无法转化为实际的端到端延迟缩减。
*   **研究动机**：现有的研究通常只关注算法端的稀疏配置或单一的算子优化，导致“可实现加速（Realizable Acceleration）”缺失。论文旨在通过**搜索-算子协同设计**，确保稀疏配置既能保持模型精度，又能真正被底层硬件高效执行。

### 2. 方法论：核心思想与关键技术
论文提出了一个双层协同框架：

*   **硬件端：MD-SpMM 稀疏内核**
    *   **微密集重构（Micro-Dense Restructuring）**：将 N:M 稀疏矩阵重新组织为与 Tensor Core 形状对齐的微小密集块，使稀疏计算回归到高效的 MMA（矩阵乘累加）指令流中。
    *   **权重打包（Weight Packing）**：设计了一种解码友好的存储格式，将非零值及其索引合并存储，减少内存读取次数并实现解码与计算的重叠。
    *   **自适应并行（Adaptive Parallelism）**：引入 Split-K 策略，根据算子形状和硬件资源动态调整并行度，解决推理场景下小 Batch 导致的 GPU 利用率不足问题。
*   **软件端：延迟感知的层级稀疏搜索**
    *   **延迟预测器**：基于查表法（LUT）并结合线性模型校准（使用非负最小二乘法 NNLS），能够以低于 5% 的误差预测端到端推理延迟。
    *   **三阶段进化搜索（Overshoot-Repair-Refine）**：针对延迟约束导致的搜索空间碎片化问题，采用“先松弛再修复再精炼”的策略，避免搜索陷入局部最优，寻找最优的逐层稀疏比例。

### 3. 实验设计
*   **模型与数据集**：在 DeiT-Small、DeiT-Base 和 Swin-Small 模型上进行验证；使用 ImageNet-1K 数据集进行最终评估，搜索阶段使用 ImageNet-300 子集以提高效率。
*   **硬件平台**：涵盖了数据中心级（A100）和消费级（RTX 4090, RTX 4060）GPU。
*   **Benchmark 与对比方法**：
    *   **算子层级**：对比 NVIDIA 官方的 **cuBLAS**（稠密基准）和 SOTA 稀疏库 **nmSPARSE**。
    *   **系统层级**：对比均匀稀疏（Uniform Sparsity）和先进的进化搜索方法 **Evo-Press**。

### 4. 资源与算力
*   **算力设备**：实验使用了 NVIDIA A100、RTX 4090 和 RTX 4060。
*   **训练与搜索时长**：论文明确提到，在 3 台 A100 GPU 上，包含 Supernet 训练和 200 代进化搜索的完整流程大约需要 **10 小时**。
*   **离线开销**：算子延迟查表（LUT）的构建在数秒内即可完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对数十种不同的矩阵形状（M/N/K 组合）进行了算子性能测试。
    *   针对三种主流 ViT 变体在三种不同 GPU 架构上进行了端到端测试。
    *   设置了 1.5x、2.0x、2.5x 等多个目标加速梯度。
*   **充分性评价**：实验设计较为充分，不仅涵盖了不同性能等级的硬件，还通过消融实验验证了自适应并行策略和搜索算法各阶段的有效性。对比基准选择合理，包含了厂商优化库和学术界前沿方法。

### 6. 主要结论与发现
*   **显著加速**：MD-SpMM 算子相比 cuBLAS 稠密算子平均实现 2.0x 加速，相比 nmSPARSE 提升 1.6x–4.0x。
*   **端到端优势**：在保持相同精度的情况下，该框架实现了超过 **2.2x** 的端到端延迟缩减。
*   **精度提升**：在相同的延迟约束下，通过优化的层级稀疏分配，Top-1 准确率比传统方法最高提升了 **1.4%**。
*   **协同的重要性**：实验证明，单纯优化算子或单纯优化搜索都无法达到最优的加速比，软硬件协同是实现“可落地加速”的关键。

### 7. 优点（亮点）
*   **落地导向**：不追求虚高的理论 FLOPs 缩减，而是以真实的端到端延迟作为搜索约束。
*   **内核设计精巧**：MD-SpMM 成功解决了 INT8 稀疏推理在 Tensor Core 上难以加速的难题，尤其是对小规模推理任务的并行优化。
*   **搜索策略稳健**：三阶段搜索算法有效解决了带硬性延迟约束的非凸优化问题，搜索效率高且收敛性好。

### 8. 不足与局限
*   **硬件依赖性**：MD-SpMM 内核高度依赖 NVIDIA Tensor Core 架构，对于非 NVIDIA GPU（如 AMD 或国产 AI 芯片）的迁移性未做讨论。
*   **模型覆盖**：虽然涵盖了 ViT 和 Swin，但未测试更广泛的 CNN 或最新的大语言模型（LLM）中的 Transformer 结构。
*   **量化敏感性**：实验主要基于 INT8，未深入探讨在更低比特（如 INT4）或浮点（FP16）下的表现差异。

（完）
