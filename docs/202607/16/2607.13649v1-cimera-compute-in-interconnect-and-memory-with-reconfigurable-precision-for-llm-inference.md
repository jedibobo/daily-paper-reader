---
title: "CIMERA: Compute-in-Interconnect and Memory with Reconfigurable Precision for LLM Inference"
title_zh: CIMERA：面向大语言模型推理的具有可重构精度的互连与内存内计算
authors: "Yue Jiet Chong, Yimin Wang, Wei Zhang, Xuanyao Fong"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13649v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 具有可重构精度的LLM推理软硬件协同设计
tldr: CIMERA 是一款针对大语言模型（LLM）推理设计的可重构精度加速器。它通过集成互连计算与内存计算技术，有效缓解了“存储墙”瓶颈，并支持自适应精度执行以平衡推理精度与能效。实验表明，相比 Nvidia H100，CIMERA 在处理 1B 和 13B 模型时，能效分别提升了 25 倍和 10 倍，为从数据中心到边缘设备的 LLM 推理提供了高效能方案。
selection_source: fresh_fetch
motivation: 大语言模型推理面临严峻的计算与内存挑战，且不同负载对精度的需求具有异构性，需要更灵活的硬件支持。
method: 提出一种集成互连计算与内存计算的架构，支持可重构精度以实现精度感知的自适应执行。
result: 与 Nvidia H100 相比，CIMERA 在 1B 和 13B 模型上的能效分别提升了高达 25 倍和 10 倍。
conclusion: CIMERA 通过结合互连计算与可重构精度技术，有效解决了存储墙问题并显著提升了 LLM 推理的能效。
---

## 摘要
大语言模型（LLM）带来了巨大的计算和内存需求，为从数据中心到功耗受限的边缘设备等各类平台上的高效能推理带来了挑战。权重精度在平衡推理准确性、吞吐量和能耗方面起着至关重要的作用，而现代 LLM 工作负载表现出显著的异构性和容错性，这有利于自适应精度执行。本文提出了 CIMERA，一种可重构精度的 LLM 推理加速器，它集成了互连内计算和内存内计算，以缓解内存墙问题并实现精度感知执行。与 Nvidia H100 相比，CIMERA 在 1B 和 13B 模型上的能效分别提高了高达 25 倍和 10 倍。

## Abstract
LLM impose significant computational and memory demands, creating challenges for energy-efficient inference across platforms ranging from data centers to power-constrained edge devices. Weight precision plays a critical role in balancing inference accuracy, throughput, and energy consumption, while modern LLM workloads exhibit pronounced heterogeneity and tolerance that favors adaptive precision execution. This paper presents CIMERA, a reconfigurable-precision LLM inference accelerator that integrates compute-in-interconnect and memory to mitigate the memory wall and enable precision-aware execution. Compared to Nvidia H100, CIMERA delivers up to $25\times$ and $10\times$ higher energy efficiency for 1B and 13B models, respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《CIMERA: Compute-in-Interconnect and Memory with Reconfigurable Precision for LLM Inference》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）推理面临严重的“存储墙”瓶颈和巨大的计算能耗压力。
*   **研究动机**：
    *   LLM 在不同层、不同推理阶段（Prefill 与 Decode）对权重的精度需求具有异构性。
    *   传统的固定精度硬件无法有效利用 LLM 对低精度的容忍度来换取能效。
    *   现有的存内计算（CIM）方案往往难以兼顾静态权重计算（SMAC）与动态数据计算（DMAC，如 Attention 分数）。
*   **整体含义**：提出一种名为 **CIMERA** 的芯片组（Chiplet）架构，通过结合“存内计算”与“互连内计算”，并引入可重构精度技术，实现高效、灵活的 LLM 推理加速。

### 2. 论文提出的方法论
*   **核心思想**：将静态权重矩阵乘法（SMAC）放在存储单元内完成，将动态张量计算（DMAC）和数据调度放在互连网络（IPCN）中完成，并支持硬件级的精度可重构。
*   **关键技术细节**：
    *   **存内计算 PE（Processing Element）**：采用氧化物半导体铁电场效应晶体管（OS FeFET）非易失性存储阵列。每个 FeFET 单元可存储高达 4-bit 精度（QLC），通过空间位切片（Bit-slicing）支持更高精度。
    *   **互连内计算（IPCN）**：采用 2D-Mesh 结构的路由器网络。路由器不仅负责数据传输，还集成了计算单元（DMAC）用于处理 Attention 分数计算、部分和归约及激活函数。
    *   **可重构精度 ADC**：采用逐次逼近寄存器（SAR）ADC，通过截断转换循环实现分辨率可调，从而在低精度下降低功耗和延迟。
    *   **单片 3D 集成（M3D）**：利用 FeFET 的后端工艺（BEOL）兼容性，将存储阵列垂直堆叠在 CMOS 逻辑电路（如 ADC、控制逻辑）之上，大幅节省硅片面积。
    *   **数据流编排**：采用蛇形（Serpentine）执行拓扑，支持广播、归约和单播三种通信模式，优化层间流水线。

### 3. 实验设计
*   **测试模型**：LLaMA-3.2 (1B)、Mistral (7B)、LLaMA-2 (13B)。
*   **实验场景**：涵盖了不同的上下文长度（1024 和 2048 tokens）以及不同的权重精度（4-bit, 8-bit, 16-bit）。
*   **Benchmark（基准）**：对比了行业领先的 **Nvidia H100 GPU**。
*   **评估指标**：吞吐量（tokens/s）、平均功耗（W）、能效比（tokens/J）。

### 4. 资源与算力
*   **硬件实现/仿真**：
    *   数字硬件使用 Verilog HDL 实现，并在 **7nm 工艺节点**下进行逻辑综合（Synopsys Design Compiler）和布局布线（Cadence Innovus）。
    *   PE 部分制造并表征了 OS FeFET 存储阵列原型，并基于测量数据建立了紧凑模型。
    *   SRAM 功耗和面积通过 CACTI 工具获取。
*   **算力说明**：论文未提及训练算力（因其专注于推理加速），推理性能评估基于周期精确（Cycle-accurate）的指令级仿真器。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 种不同规模的模型，在 3 种精度和 2 种序列长度下进行了交叉实验，并给出了详细的功耗和面积拆解。
*   **充分性评价**：实验设计较为充分，涵盖了从底层器件表征到系统级仿真的全过程。通过与 H100 的对比，直观展示了架构在能效上的优势。但实验主要基于仿真数据，缺乏大规模物理芯片集群的实测数据。

### 6. 论文的主要结论与发现
*   **能效大幅提升**：在 1B 模型上，CIMERA 的能效比 H100 高出 **25 倍**；在 13B 模型上高出 **10 倍**。
*   **精度与性能的权衡**：降低权重精度可显著提高吞吐量并降低功耗。例如，4-bit 模式下的吞吐量和能效远高于 16-bit 模式。
*   **面积效率**：得益于 M3D 集成技术，外围电路（如 ADC）不占用平面面积，使得系统具有极高的集成度。
*   **可扩展性**：Chiplet 设计和层级化数据流使得架构能够灵活应对不同规模的 LLM。

### 7. 优点
*   **架构创新**：巧妙结合了 CIM（存内计算）和 Compute-in-Interconnect（互连计算），解决了 Attention 机制中动态计算的难题。
*   **精度灵活性**：硬件级支持可重构精度，能够完美适配混合精度量化算法。
*   **先进工艺应用**：利用 OS FeFET 和 M3D 技术，从底层器件层面解决了存储密度和互连延迟问题。

### 8. 不足与局限
*   **精度损失未量化**：论文重点讨论了硬件能效的提升，但未详细分析不同精度（如 4-bit）对特定 LLM 任务准确率（Accuracy/Perplexity）的具体影响。
*   **应用限制**：FeFET 作为新兴器件，其大规模量产的良率、循环寿命（Endurance）以及在复杂环境下的稳定性仍需进一步验证。
*   **软件栈依赖**：该架构高度依赖特定的指令集和编译器优化，现有的深度学习框架（如 PyTorch）可能需要复杂的适配工作。

（完）
