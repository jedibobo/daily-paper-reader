---
title: "Emulated Integrity Replica: Enabling Self-Healing on FPGA SoCs via Hierarchical Twins"
title_zh: 仿真完整性副本：通过分层孪生在 FPGA SoC 上实现自愈
authors: "Arsalan Ali Malik, Ali Suvizi, Guru Venkataramani, Aydin Aysu"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12298v1"
tags: ["query:hwnas"]
score: 9.0
evidence: FPGA SoC上的硬件加速推理和自愈边缘计算
tldr: 针对FPGA SoC上CNN加速器的容错挑战，本文提出了一种名为EIR的层次化数字孪生框架。该框架利用处理系统（PS）在加速器运行时的空闲周期，通过粗粒度的Rabbit模型进行快速故障检测，并利用细粒度的Tortoise模型实现精确的状态恢复。相比传统的硬件冗余方案，EIR在保证高故障覆盖率的同时，显著降低了资源开销和能耗，为资源受限的边缘AI部署提供了自愈能力。
selection_source: fresh_fetch
motivation: 传统的硬件冗余方案资源开销过大，而软件容错方法在延迟、能耗或故障覆盖率方面表现不足。
method: 提出EIR框架，利用PS端的空闲周期运行行为级模型进行检测和门级模型进行故障恢复。
result: 实验表明，EIR在保持与双模块冗余相当的故障覆盖率的同时，有效减少了芯片面积占用和能源消耗。
conclusion: EIR为在严格资源预算下实现具有自愈能力的边缘AI部署提供了一种高效且实用的技术路径。
---

## 摘要
卷积神经网络 (CNN) 正越来越多地部署在片上系统 (SoC) 平台上，硬件加速推理在这些平台上实现了低延迟边缘计算。在这些设备上实现容错仍具挑战性，因为传统的冗余技术（如双重/三重模块冗余，DMR/TMR）会产生高昂的资源成本，而以软件为中心的方法（如基于算法的容错 (ABFT)、检查点重启、指令级复制以及软件看门狗/断言）则会引入显著的延迟/能耗开销，降低模型精度，或对加速器引发的故障覆盖不足。在本文中，我们提出了仿真完整性副本 (EIR)，这是一种用于 FPGA SoC 的分层数字孪生框架，可提供自主故障检测与恢复。与复制硬件逻辑并产生相应面积和功耗开销的 DMR/TMR 不同，EIR 通过利用处理系统 (PS) 中的时间松弛来避免架构级的重复。在可编程逻辑 (PL) 中的加速器执行期间，PS 通常处于低利用率状态；EIR 利用这些空闲周期来承载两个互补的孪生模型：(i) Rabbit：用于快速故障检测的粗粒度行为模型；(ii) Tortoise：从检查点状态执行精确恢复的细粒度门级模型。加速器状态被定期捕获，利用加速器的执行速度分析来平衡性能开销与韧性。对代表性工作负载的实验表明，在评估的故障模型和工作负载假设下，EIR 相对于 DMR 基准实现了较高的经验故障覆盖率，同时减少了能耗和面积，这为在严格资源预算下实现韧性边缘 AI 部署提供了一条切实可行的路径。

## Abstract
Convolutional neural networks (CNNs) are increasingly being deployed on system-on-chip (SoC) platforms, where hardware-accelerated inference enables low-latency edge computing. Achieving fault tolerance on these devices remains challenging because conventional redundancy (dual/triple modular redundancy, DMR/TMR) incurs high resource cost, while software-centric methods (e.g., algorithm-based fault tolerance (ABFT), checkpoint-restart, instruction-level duplication, and software watchdogs/assertions) introduce nontrivial latency/energy overheads, reduce model accuracy, or provide inadequate coverage for accelerator-induced faults.   In this paper, we propose Emulated Integrity Replica (EIR), a hierarchical digital-twin framework for FPGA SoCs that provides autonomous fault detection and recovery. Unlike DMR/TMR, which replicates hardware logic and incurs proportional area and power overheads, EIR avoids fabric-level duplication by exploiting temporal slack in the processing system (PS). During accelerator execution in the programmable logic (PL), the PS typically remains underutilized; EIR capitalizes on these idle cycles to host two complementary twins: (i) Rabbit: a coarse-grained behavioral model for rapid fault detection and (ii) Tortoise: a fine-grained gate-level model that performs precise recovery from checkpointed states. The accelerator state is captured periodically, leveraging the accelerator's execution-speed profiling to balance performance overhead and resilience. Experiments on representative workloads show that EIR achieves high empirical fault coverage relative to a DMR baseline while reducing energy and area under the evaluated fault model and workload assumptions, indicating a practical path to resilient edge-AI deployments under strict resource budgets.

---

## 论文详细总结（自动生成）

### 论文总结：Emulated Integrity Replica (EIR)

#### 1. 核心问题与研究动机
在边缘计算场景下，部署在 FPGA SoC 上的卷积神经网络（CNN）加速器面临硬件故障（如单粒子翻转 SEU）的威胁。传统的容错方案存在明显局限：
*   **硬件冗余（DMR/TMR）：** 面积和功耗开销巨大（成倍增加），不适合资源受限的边缘设备。
*   **软件容错（ABFT、检查点）：** 往往带来显著的延迟、能耗开销，或无法提供精确的故障定位与恢复。
**研究动机：** 利用 FPGA SoC 中处理系统（PS，如 ARM 核）在加速器运行时的“空闲周期”，构建一种低成本、自愈式的分层数字孪生框架。

#### 2. 方法论
论文提出了 **EIR (Emulated Integrity Replica)** 框架，核心思想是“分层孪生”：
*   **Rabbit（兔子 - 粗粒度行为模型）：** 在 PS 端运行的高级 C/C++ 模型。它不模拟时钟周期，仅计算功能结果。其任务是快速与 PL（可编程逻辑）端的加速器输出进行比对，实现实时故障检测。
*   **Tortoise（乌龟 - 细粒度门级模型）：** 基于 Yosys CXXRTL 生成的周期精确（Cycle-accurate）模型。平时不运行，仅在 Rabbit 检测到故障时启动。它从上一个正确检查点开始模拟，重建精确的硬件状态。
*   **关键技术细节：**
    *   **状态捕获与恢复：** 利用 PCAP 接口读取/写入 PL 状态，无需修改加速器 RTL 逻辑。
    *   **选择性检查策略：** 针对神经网络，通过敏感度分析发现深层（全连接层）和高幅值 MAC 操作对故障更敏感。EIR 仅验证前 30% 的关键神经元，以平衡 PS 计算负载和检测覆盖率。
    *   **自愈流程：** 检测到故障 -> 启动 Tortoise 重建状态 -> 通过 PCAP 将正确状态写回 PL -> 恢复执行。

#### 3. 实验设计
*   **硬件平台：** AMD/Xilinx Zynq-7000 (XC7Z020) SoC。
*   **数据集/场景：** 使用 LeNet-5 模型在 CIFAR-10 数据集上进行推理验证。
*   **Benchmark（11 个代表性工作负载）：** 包括计数器、SHA-256、AES-128、Dense 层、Conv2D 层、Attention 层、FFT-64、GEMM、FIR、IIR 等。
*   **对比方法：** 以双模块冗余（DMR）作为主要基准（Baseline）。
*   **故障模型：** 模拟瞬态多比特翻转，通过修改读取的配置镜像（Readback-level fault injection）来实现。

#### 4. 资源与算力
*   **算力平台：** 实验在 Zynq-7000 SoC 上完成，PS 端为双核 ARM Cortex-A9。
*   **训练/开发：** 使用 Vivado 2024.2 进行综合，Vitis SDK 进行软件开发，Yosys CXXRTL 生成门级模型。
*   **论文未明确说明：** 未提及模型训练阶段的具体 GPU 型号或时长，因为研究重点在于推理阶段的容错。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从简单逻辑到复杂 AI 算子的 11 种不同类型的加速器。
*   **消融与敏感度分析：** 针对 LeNet-5 进行了详细的层级敏感度和幅值敏感度实验，验证了“选择性检查策略”的有效性。
*   **充分性评价：** 实验设计较为充分，不仅量化了面积和功耗的节省，还评估了检测延迟和恢复时间。对比 DMR 的实验展示了 EIR 在资源受限环境下的显著优势。

#### 6. 主要结论与发现
*   **资源优势：** 相比 DMR，EIR 保持了原始 PL 面积（DMR 增加 2.0-2.25 倍），平均降低了约 89.1% 的额外功耗开销。
*   **故障覆盖：** 在边缘 AI 负载中，通过审计前 30% 的神经元，可实现 ≥95% 的故障检测率。
*   **恢复性能：** 故障恢复过程（状态写回）仅需约 5.7ms，总停机时间在几十毫秒量级，符合大多数边缘控制系统的要求。

#### 7. 优点
*   **架构创新：** 首次将分层数字孪生（快慢模型结合）应用于 FPGA SoC 的自愈系统。
*   **非侵入性：** 利用 PCAP 和逻辑位置映射（.ll 文件），无需修改加速器的 RTL 代码或增加 PL 冗余逻辑。
*   **资源高效：** 巧妙利用了 PS 端的空闲算力，将硬件冗余转化为软件模拟，极大地节省了昂贵的 FPGA 布线资源。

#### 8. 不足与局限
*   **PS 负载依赖：** 如果 PS 端本身运行重型任务，Rabbit 模型的执行可能会受到干扰，导致检测延迟增加。
*   **故障类型限制：** 主要针对瞬态故障（如 SEU），无法处理永久性的硬件物理损坏。
*   **确定性要求：** 系统恢复依赖于加速器的确定性重放，对于包含异步接口或不可预测外部输入的系统，恢复逻辑会变得复杂。
*   **实时性挑战：** 虽然恢复时间为毫秒级，但对于极高实时性（微秒级控制环路）的应用，停机时间可能仍显过长。

（完）
