---
title: "FastTPS: An Optimized Method for LLM Token Phase for AI accelerators"
title_zh: FastTPS：一种针对 AI 加速器的 LLM Token 阶段优化方法
authors: "Wenzong Yang, Danyang Zhang, Kun Cao, Tejus Siddagangaiah, Rajeev Patwari, Zhanxing Pu, Siyin Kong, Zijiang Yang, Hao Zhu, Varun Sharma, Yue Gao, Tianping Li, Fan Yang, Jicheng Chen, Yushan Chen, Fennian Zhao, Aaron Ng, Elliott Delaye, Ashish Sirasao, Sudip Nag"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11211v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对AI加速器优化LLM令牌阶段
tldr: "FastTPS 针对大语言模型推理中的 Token 生成阶段，解决了 AI 加速器上并行度低和内存开销大的问题。该方法通过免重载 KV Cache 拼接、基于分块优化的 FLAT 注意力机制以及细粒度流水线调度的融合 MLP，显著提升了推理效率。在 AMD NPU 上实现了 6 倍加速，并达到 93% 的峰值内存带宽利用率，且保持了高精度。"
selection_source: fresh_fetch
motivation: 针对大语言模型 Token 生成阶段因串行处理导致的低并行度和高内存访问瓶颈问题。
method: 提出了免重载 KV Cache 拼接、分块优化的 RoPE 注意力机制以及细粒度流水线调度的全融合 MLP。
result: "在 AMD Ryzen AI 300 系列 NPU 上，相比非融合方案实现了 6 倍加速，内存带宽利用率高达 93%。"
conclusion: FastTPS 有效缓解了 Token 阶段的内存瓶颈，是提升通用 AI 加速器上 LLM 推理性能的高效方案。
---

## 摘要
大语言模型（LLM）的普及使得对高效推理的需求不断增长。然而，由于仅解码器（decoder-only）LLM 推理在 Token 阶段采用顺序处理方式，其固有的低并行性导致吞吐量降低，且人工智能（AI）加速器上的计算单元利用率不足，特别是在处理会带来显著内存开销的长序列输入时。近期，许多已报道的方法被开发为潜在解决方案，但它们往往伴随着数值偏差。本文提出了 FastTPS，这是一种用于在通用 AI 加速器上加速 LLM 推理 Token 阶段的高性能、低精度损失方法，包含三个关键组件：(1) 支持 AI 加速器的免重载 KV Cache 拼接，可减少内存访问开销并实现 Attention 的全融合；(2) 基于分块（tiling）优化 FLAT 的高效、高精度“RoPE”注意力机制；(3) 具有细粒度流水线调度的紧密融合 MLP。我们的结果证实，FastTPS 显著缓解了 Token 阶段的内存瓶颈，在 BF16 精度下，在 AMD Ryzen AI 300 系列 NPU 上实现了 6 倍的速度提升（与未融合相比），同时在 Phi3-mini-4k-instruct 推理过程中保持了 93% 的峰值内存带宽利用率。

## Abstract
The popularity of large language models (LLMs) escalates an ongoing demand for effective inference. However, due to the sequential processing of tokens during the token phase in decoder-only LLMs inference, the inherent low parallelism leads to reduced throughput and suboptimal utilization of the computing units on artificial intelligence (AI) accelerators, particularly when handling long-sequence inputs that impose significant memory overhead. Recently, many reported methods have been developed as potential solutions, since they emerge with numeric deviation. This paper presents FastTPS, a high performance and low-precision loss method for accelerating the token-phase in LLM inference on general AI accelerators which includes three key components: (1) AI accelerator-enabled reloading-free KV Cache concatenation which decreases memory access overhead as well as enables full fusion of Attention, (2) high-efficiency and high-accuracy 'RoPE' attention based on the tiling optimized FLAT, and (3) highly-fused MLP with fine-grain pipeline scheduling. Our results confirm that FastTPS significantly alleviates memory bottlenecks in the token phase, delivering a 6x speed improvement (compared to none-fusion) on an AMD Ryzen AI 300 series NPU with BF16 precision while sustaining 93% peak memory bandwidth utilization during Phi3-mini-4k-instruct inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《FastTPS: An Optimized Method for LLM Token Phase for AI accelerators》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）推理分为预填充（Prefill）和 Token 生成（Token Phase）两个阶段。Token 阶段由于自回归特性，每次仅处理一个 Token，导致计算并行度极低，属于典型的**内存受限（Memory-bound）**任务。
*   **核心挑战**：在 AI 加速器上，频繁的 KV Cache 加载与拼接、算子间的内存同步以及 MLP 层的权重读取，导致硬件计算单元利用率低下，且长序列下的内存开销巨大。
*   **整体含义**：本文提出 FastTPS 方案，旨在通过软硬件协同优化，在不损失精度的情况下，最大化 AI 加速器在 Token 生成阶段的吞吐量和带宽利用率。

### 2. 方法论：核心思想与关键技术
FastTPS 包含三个核心组件，针对 Attention 和 MLP 块进行了深度重构：
*   **全局 KV Cache 管理 (GKVC)**：
    *   采用 3D 静态内存管理策略，预先分配内存空间。
    *   **核心思想**：通过地址跳转策略直接将新生成的 KV 向量写入预留位置，消除了传统方法中由 CPU 处理的 `Concat`（拼接）算子，使整个数据流完全保留在 AI 加速器上，减少了数据搬运。
*   **针对 Token 阶段的分块优化注意力机制 (TPSFLAT)**：
    *   基于 FLAT 思想，针对 Token 阶段 $B=1, N=1$ 的特性进行简化。
    *   **技术细节**：将 RoPE（旋转位置编码）、$QK^T$、Softmax 和 $PV$ 算子进行全融合。利用三级缓存（L1/L2/L3）进行数据分块（Tiling），确保中间结果留在片上 L1 缓存中，显著提升了操作强度（OI）。
*   **融合 MLP 与交错布局 (Fusion MLP)**：
    *   **交错布局**：将 Gate 投影和 Up 投影的权重矩阵进行细粒度拆分并交错排列，构建一个更大的融合 Matmul。
    *   **算子融合**：将 Matmul 与 SiLU 激活、元素级乘法（Mul）融合。
    *   **流水线调度**：通过细粒度的矩阵划分，实现权重加载与计算的重叠（Overlap），掩盖内存延迟。

### 3. 实验设计
*   **测试模型**：涵盖了主流的开源 LLM，包括 ChatGLM3-6B、Llama2-7B、Llama3-8B-instruct、Llama3.2-1B 和 Phi3-mini-4k-instruct。
*   **Benchmark（基准）**：
    *   HuggingFace 标准实现（未融合方案）。
    *   FlashAttention（用于精度对比）。
    *   行业竞品：Intel Lunar Lake NPU、Nvidia Jetson Nano。
*   **评估指标**：延迟（Latency）、每秒 Token 数（TPS）、内存带宽利用率、数值精度（BF16）。

### 4. 资源与算力
*   **硬件平台**：主要基于 **AMD Ryzen AI 300 系列 NPU**（采用 XDNA 架构）。
*   **对比硬件**：Intel Lunar Lake NPU（带宽 102 GBPs）、Nvidia Nano（带宽 102 GBPs）。
*   **算力细节**：文中提到 AMD NPU 的峰值性能约为 2.84 GFLOPs（针对特定配置），实验主要关注推理阶段的带宽利用率（最高达到 93%）。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 5 种不同架构的模型进行了测试，并对比了不同序列长度（从 1k 到 4k）下的性能表现。
*   **充分性**：
    *   **消融分析**：分别对 Attention 块和 MLP 块的优化效果进行了独立验证。
    *   **精度验证**：对比了 FastTPS 与 FlashAttention 的误差曲线，证明了其高精度特性。
    *   **横向对比**：与 Intel 和 Nvidia 的硬件方案进行了 TPS 对比。
*   **客观性**：实验涵盖了多种主流模型和实际应用场景（如内容生成、摘要、代码生成），数据展示较为全面。

### 6. 主要结论与发现
*   **性能提升**：在 Phi3 模型上实现了 **6 倍** 的速度提升（相比非融合方案）；在 Llama3 上注意力机制延迟降低了 93%。
*   **带宽利用率**：FastTPS 使硬件接近理论性能极限，在 Phi3 推理中实现了 **93% 的峰值内存带宽利用率**。
*   **精度优势**：FastTPS 的数值精度比 FlashAttention 高出约一个数量级（误差约 1e-3 vs 1e-2），且误差随序列长度增加而减小。
*   **算子强度**：注意力机制的操作强度（OI）提升了 3 倍。

### 7. 优点
*   **全融合设计**：通过 GKVC 彻底消除了 Token 阶段最耗时的内存重组操作（Concat）。
*   **硬件感知**：深度利用了 AI 加速器的三级缓存架构进行分块优化，具有极高的执行效率。
*   **高精度保留**：与许多通过牺牲精度换取速度的量化/剪枝方法不同，FastTPS 在加速的同时保持了极高的数值稳定性。
*   **通用性**：该方法论可推广至 GPU、TPU 等其他具有多级缓存结构的 AI 加速器。

### 8. 不足与局限
*   **硬件依赖性**：虽然理论上通用，但文中核心优化（如交错权重布局和特定的流水线调度）是针对 AMD NPU 架构深度定制的，在其他架构上可能需要重新调优。
*   **阶段局限性**：该方法专注于 Token 生成阶段，未详细讨论预填充（Prefill）阶段的优化，而预填充在长文本输入时同样重要。
*   **模型结构限制**：主要针对 Decoder-only 架构，对于 Encoder-Decoder 或其他变体结构的适用性未做深入探讨。

（完）
