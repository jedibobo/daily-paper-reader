---
title: "BitFair: A 12nm Bit-Serial CNN Accelerator with Learnable Early Termination and Adaptive Bit Ordering for Ultra-Low-Power XR Vision"
title_zh: BitFair：一种具有可学习提前终止和自适应位排序的 12nm 位串行 CNN 加速器，用于超低功耗 XR 视觉
authors: "Ang Li, Chang Gao"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.05445v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 软硬件协同设计的位串行CNN加速器
tldr: 针对XR设备超低功耗和低延迟需求，本文提出BitFair，一种12nm位串行CNN加速器。通过软硬件协同设计，引入可学习的位级提前终止机制和自适应位排序，利用ReLU激活后的稀疏性减少冗余计算。在保持高精度的同时，实现了亚毫秒级延迟和极高的能效比，显著优于现有XR视觉加速器。
selection_source: fresh_fetch
motivation: 现有的位串行计算架构在处理最终输出为零的ReLU激活时仍会处理所有位，导致XR视觉应用中存在严重的能量浪费。
method: 提出可学习的层级阈值以触发提前终止，并搜索最优位排序以优先处理关键信息位，从而在不损失精度的情况下最大化节能。
result: 在12nm工艺下实现了117.0 BTOPS/W的能效和亚毫秒级延迟，相比先前加速器能效提升了4.0-22.1倍。
conclusion: BitFair通过位级动态稀疏性优化，为超低功耗XR视觉感知提供了一种高效的软硬件协同加速方案。
---

## 摘要
扩展现实 (XR) 可穿戴设备需要在几瓦的严苛功耗限制和低于 20 毫秒的运动到光子延迟预算内实现全时感知，这仅为神经网络推理留下了几毫秒的时间。位串行计算对于此类高能效神经网络加速具有吸引力，但许多现有架构即使在 ReLU 将最终输出设为零时，仍会处理所有位。本文提出了 BitFair，这是一种软硬件协同设计的位串行 CNN 加速器，具有可学习的位级提前终止和自适应位排序，适用于 XR 应用的超低功耗和严格延迟要求。BitFair 通过学习逐层阈值来利用动态位级稀疏性，当部分和能够可靠地预测最终 ReLU 输出为零时，这些阈值会触发提前终止。此外，它搜索逐层位顺序以优先处理信息量大的位，在不牺牲准确性的情况下最大限度地实现提前终止。采用 GlobalFoundries 12nm FinFET 工艺实现，核心面积为 0.34 mm²，片上存储器为 104 KB，电压缩放范围为 0.55 至 0.70 V，实现了亚毫秒级延迟、高达 117.0 BTOPS/W 的能效以及 0.07 pJ/SOP。在 IBM DVS128 Gesture 和 N-MNIST 数据集上，BitFair 分别达到了 96.5% 和 97.7% 的准确率，同时与之前制造的 XR 视觉加速器相比，有效能效提高了 4.0-22.1 倍，准确率提高了高达 9.2%。

## Abstract
Extended Reality (XR) wearables require always-on perception within tight power envelopes of a few watts and motion-to-photon latency budgets below 20 ms, leaving only a few milliseconds for neural-network inference. Bit-serial computing is attractive for such energy-efficient neural network acceleration, but many existing architectures still process all bits even when ReLU sets the final output to zero. This paper presents BitFair, a software-hardware co-designed bit-serial CNN accelerator with learnable bit-level early termination and adaptive bit ordering, working under the ultra-low-power and strict latency requirements of XR applications. BitFair exploits dynamic bit-level sparsity by learning per-layer thresholds that trigger early termination when partial sums reliably predict that the final ReLU output will be zero. Furthermore, it searches for layer-wise bit orders that prioritize informative bits, maximizing early termination without sacrificing accuracy. A GlobalFoundries 12nm FinFET implementation with a core area of 0.34 mm^2, 104 KB on-chip memory, and voltage scaling from 0.55 to 0.70 V achieves sub-millisecond latency, up to 117.0 BTOPS/W, and 0.07 pJ/SOP. On IBM DVS128 Gesture and N-MNIST, BitFair achieves 96.5% and 97.7% accuracy, respectively, while improving effective energy efficiency by 4.0-22.1x and accuracy by up to 9.2% over prior fabricated XR vision accelerators.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《BitFair: A 12nm Bit-Serial CNN Accelerator with Learnable Early Termination and Adaptive Bit Ordering for Ultra-Low-Power XR Vision》** 的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：扩展现实（XR）设备（如 AR/VR 头显）要求“全时开启”的视觉感知，但其功耗预算极低（仅数十毫瓦），且“运动到光子”的延迟必须控制在 20ms 以内，留给神经网络推理的时间仅有几毫秒。
*   **核心问题**：传统的位串行（Bit-serial）计算虽然能效高，但存在计算冗余。即使 ReLU 激活函数最终会将输出归零，现有架构仍会处理权重的所有位。
*   **研究动机**：如何利用 ReLU 诱导的动态稀疏性，在位级层面提前终止不必要的计算，以满足 XR 应用对超低功耗和极低延迟的苛刻要求。

### 2. 方法论
BitFair 采用软硬件协同设计，核心思想是“预测并跳过”对最终结果无贡献的计算：
*   **可学习的位级提前终止（Learnable Early Termination）**：
    *   为每一层神经网络引入可学习的阈值 $\theta_l$。
    *   **核心技术**：使用温度退火（Temperature Annealing）和 Sigmoid 门控函数，将不可微的提前终止决策转化为可微过程，从而允许通过梯度下降在训练阶段自动优化阈值，平衡精度与能效。
    *   **逻辑**：当累加的部分和（Partial Sum）低于阈值时，硬件判定该输出最终必为零，立即停止后续位的处理。
*   **自适应位排序（Adaptive Bit Ordering, ABO）**：
    *   打破传统的“从高位到低位（MSB-first）”处理顺序。
    *   **算法流程**：采用贪心搜索策略，针对每一层的权重分布寻找最优处理顺序。优先处理对结果预测贡献最大的位，从而让提前终止发生得更早。
*   **硬件架构**：
    *   基于 12nm FinFET 工艺，设计了 16×16 的处理单元（PE）阵列。
    *   采用输出平稳（Output-stationary）数据流，每个 PE 拥有独立的终止控制器，支持细粒度的时钟门控以节省功耗。

### 3. 实验设计
*   **数据集与场景**：
    *   **事件驱动视觉**：N-MNIST（手写数字）、DVSGesture（手势识别）。
    *   **传统帧视觉**：MNIST、SVHN（街景数字）、VWW（视觉唤醒词/人体检测）。
*   **Benchmark（基准测试）**：
    *   对比了近年来在 ISSCC、CICC、VLSI 等顶级会议上发表的 SOTA 边缘视觉加速器（主要是基于 SNN 架构的芯片，如 ReckOn, ANP-I 等）。
    *   对比了现有的 CNN 提前终止方法（如 BitSET）。
*   **消融实验**：分别验证了“可学习阈值”和“自适应位排序”对加速比和精度的具体贡献。

### 4. 资源与算力
*   **算力设备**：论文明确指出离线训练和位排序搜索是在单张 **NVIDIA RTX 4090 GPU** 上完成的。
*   **训练时长**：论文提供了详细的墙钟时间（Wall-clock seconds）。例如，在 DVSGesture 数据集上，BitFair 的训练流程约耗时 7535 秒（约 2.1 小时），比基准方法 BitSET 节省了约 26% 的训练时间。
*   **硬件规格**：12nm 工艺，核心面积 0.34 $mm^2$，片上存储 104 KB。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流数据集，跨越了事件相机和标准相机两种模态，实验设置较为全面。
*   **充分性**：
    *   进行了详尽的电压/频率缩放实验（0.55V-0.80V），展示了功耗从 0.5mW 到 18.2mW 的动态范围。
    *   通过消融实验展示了阈值变化对精度-效率曲线的影响，证明了自动学习阈值的必要性。
*   **客观性**：对比数据均来自已发表的流片结果或后仿真数据，评价指标包括能效（BTOPS/W）、能量延迟积（EDP）和精度，对比维度客观。

### 6 主要结论与发现
*   **极高能效**：在 0.55V 下达到 **117.0 BTOPS/W**，比同类 SNN 加速器能效提升了 4.0 至 22.1 倍。
*   **极低延迟**：推理延迟在 **0.12ms 至 1.55ms** 之间，完全满足 XR 亚毫秒级的需求。
*   **精度保持**：在大幅节能的同时，相比浮点模型精度损失小于 1.6%，且在 DVSGesture 等数据集上准确率优于 SNN 方案。
*   **发现**：MSB-first 并非总是最优排序，通过 ABO 调整位顺序可进一步提升约 10% 的加速比。

### 7. 优点
*   **软硬协同创新**：将 SNN 的“阈值触发”灵感巧妙引入位串行 CNN，解决了 CNN 动态稀疏性难以在位级利用的问题。
*   **训练效率**：通过可微门控设计，避免了复杂的非标准编码重训练，降低了离线部署的成本。
*   **工艺先进**：采用 12nm FinFET 工艺验证，数据具有很强的工程参考价值。

### 8. 不足与局限
*   **激活函数限制**：该方法高度依赖于具有“硬零区域”的激活函数（如 ReLU）。如果网络使用 GELU 或 Swish 等平滑激活函数，提前终止的概率将大幅降低。
*   **稀疏度依赖**：在激活稀疏度较低的任务（如某些极其密集的 RGB 场景）中，加速效果会退化至普通位串行水平。
*   **模型规模**：实验主要集中在轻量级模型（如 VGG-like），对于参数量巨大的 Transformer 或大型 ResNet 的适配性尚未在文中深入探讨。

（完）
