---
title: Is Your NPU Ready for LLMs? Dissecting the Hidden Efficiency Bottlenecks in Mobile LLM Inference
title_zh: 你的 NPU 为大语言模型（LLM）准备好了吗？剖析移动端 LLM 推理中隐藏的效率瓶颈
authors: "Guanyu Cai, Ruiming Tian, Lang Yang, Zhouhong Ren, Jinliang Yuan, Lingkun Li, Jiliang Wang"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05475v1"
tags: ["query:hwnas"]
score: 9.0
evidence: NPU效率瓶颈和特定后端的能量归因
tldr: 本研究针对移动端大语言模型（LLM）推理的硬件效率瓶颈，首次对5种主流框架和CPU、GPU、NPU三种后端进行了全面的跨层测量。通过开发细粒度分析工具PowerBench，揭示了NPU在Prefilling阶段的优势与在Decoding阶段的局限，并识别出由于调度不当导致的能耗浪费。研究最终提出了一套能耗优化的最佳实践配置，显著提升了移动端LLM的推理效率。
selection_source: fresh_fetch
motivation: 移动端LLM推理面临严重的硬件效率瓶颈，且现有研究缺乏对NPU等后端细粒度能耗与性能瓶颈的深入剖析。
method: 开发了PowerBench工具，对多种框架和硬件后端进行跨层测量，重点分析了算子卸载、量化策略及调度开销。
result: "揭示了NPU在计算密集型预填充阶段领先，而CPU在访存密集型解码阶段更优，并发现调度不当会导致40%的能量浪费。"
conclusion: "提出的能耗导向最佳实践配置在NPU后端上最高可减少54.8%的推理能耗。"
---

## 摘要
在移动设备上部署大语言模型（LLM）可以增强隐私并降低延迟，但受到硬件效率低下的严重制约。我们提出了首个针对移动端 LLM 推理的全面、跨层测量研究，涵盖了五个主流框架（如 llama.cpp、GENIE）和三个硬件后端（CPU、GPU、NPU）。为了实现这一分析，我们开发了 PowerBench，这是一种细粒度的性能分析工具，首次提供了针对特定后端的能量归因，超越了传统的设备级测量。我们的研究得出了三个关键见解：（1）框架引起的性能差异在 NPU 上被显著放大，由于不同的卸载和量化策略，使用自定义算子时的差异可达 10 倍。（2）我们发现了一个明显的阶段划分：NPU 擅长计算受限的预填充（prefilling）阶段，而 CPU 在内存受限的解码（decoding）阶段表现优于所有其他后端。这是由于 NPU 偏好大型、固定形状的工作负载，这与解码阶段小算子、动态的特性相冲突。（3）针对特定后端的性能分析揭示了先前工作忽略的大量调度优化空间。次优的线程配置、不协调的 NPU 休眠延迟以及 CPU 轮询间隔导致了高达 40% 的能量浪费。利用这些发现，我们提出了一种面向能源优化的移动端 LLM 推理最佳实践配置。我们估计，在三个数据集上，该配置在 NPU 后端可降低高达 54.8% 的能耗。

## Abstract
Deploying Large Language Models (LLMs) on mobile devices enhances privacy and reduces latency, but is severely bottlenecked by hardware inefficiency. We present the first comprehensive, cross-layer measurement study of mobile LLM inference, uniquely spanning five mainstream frameworks (e.g., llama.cpp, GENIE) and three hardware backends (CPU, GPU, NPU). To enable this analysis, we develop PowerBench, a fine-grained profiling tool that provides the first backend-specific energy attribution, moving beyond traditional device-level measurements. Our study yields three critical insights: (1) Framework-induced performance gaps are substantially amplified on NPUs, reaching up to 10x using custom operators due to divergent offloading and quantization strategies. (2) We identify a distinct phase split where NPUs excel at compute-bound prefilling, while CPUs outperform all other backends in memory-bound decoding. This is driven by the NPU's preference for large, fixed-shape workloads, which conflicts with the small-kernel, dynamic nature of decoding. (3) Backend-specific profiling uncovers substantial scheduling headroom missed by prior work. Suboptimal thread configurations, uncoordinated NPU sleep latencies, and CPU polling intervals result in up to 40% energy waste. Leveraging these findings, we present an energy-oriented best-practice configuration for mobile LLM inference. We estimate that this configuration could reduce energy consumption by up to 54.8% on the NPU backend across three datasets.

---

## 论文详细总结（自动生成）

### 论文总结：你的 NPU 为大语言模型准备好了吗？剖析移动端 LLM 推理中隐藏的效率瓶颈

#### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）向移动端迁移，隐私保护和低延迟成为核心诉求。然而，移动端硬件（尤其是被寄予厚望的 NPU）在实际推理中的效率表现并不理想。
*   **研究动机**：现有的移动端 LLM 优化研究往往孤立地关注框架、后端或调度，缺乏全局视角。
*   **核心问题**：NPU 在不同推理框架下的表现差异为何如此巨大？为什么 NPU 在某些阶段反而不如 CPU？如何通过系统级调度解决隐藏的能效瓶颈？

#### 2. 论文提出的方法论
论文提出了一套跨层的测量与分析框架，核心技术包括：
*   **PowerBench 工具**：开发了一个轻量级、框架无关的 C++ 插桩库。它利用高通电源遥测（QPT）驱动，通过 Linux `powercap` 接口捕获 SoC、CPU、GPU 和 NPU 各个计算单元的细粒度功耗数据，实现了从“设备级”到“后端级”的能量归因。
*   **标准化测量协议**：针对不同框架（如 llama.cpp 和 GENIE）的接口差异，构建了统一的 Benchmark 载体，精确控制 Prompt Token 注入和生成长度，确保 Prefill（预填充）和 Decode（解码）阶段的吞吐量测量具有可比性。
*   **跨层瓶颈剖析**：从算子缩放（Operator Scaling）、卸载策略（Offloading Strategy）、激活量化（Activation Quantization）以及系统调度（RPC 轮询、休眠延迟、DVFS）四个维度进行深度拆解。

#### 3. 实验设计
*   **硬件平台**：选取了四款搭载高通骁龙 SoC 的旗舰手机（包括最新的骁龙 8 Elite、8 Gen 3 等），涵盖了 V75 到 V81 不同世代的 NPU。
*   **软件框架**：对比了 5 种主流推理框架：`llama.cpp`、`MNN`、`MLC-LLM`、`MLLM` 和高通官方的 `GENIE`。
*   **模型与量化**：测试了 Llama 3.2 (1B/3B)、Qwen 2.5 (1.5B/7B) 和 Phi 3.5 (3.8B)；量化策略包括 W4（4位权重）和 W4A16（4位权重+16位激活）。
*   **数据集**：在 MathQA、RoleBench 和 LongBench 三个真实数据集上验证了优化配置的有效性。
*   **Benchmark 场景**：涵盖了从 16 到 4096 不同上下文长度的推理任务。

#### 4. 资源与算力
*   **推理算力**：实验主要在移动端 SoC 上进行，文中列出了各 SoC 的理论峰值（如 NPU > 50 TOPS INT8，GPU 约 5 TFLOPS FP32）。
*   **训练算力**：本论文侧重于**推理性能测量**，未涉及模型训练过程，因此未提及训练所用的 GPU 集群或时长。
*   **内存资源**：所有测试设备均配备 16GB DRAM，以消除内存容量对实验结果的干扰。

#### 5. 实验数量与充分性
*   **实验规模**：研究团队执行了超过 **400 种配置组合**（模型 × 设备 × 框架 × 后端 × 推理阶段）。
*   **充分性**：实验覆盖了当前移动端最主流的开源和闭源框架，且针对 NPU 的电压域（Voltage Corner）和总线频率进行了详尽的网格搜索（Heatmap 分析）。
*   **客观性**：通过多次重复实验（至少 3 次取均值）并严格控制设备温度（低于 28°C）以避免热降频，确保了数据的客观性和公平性。

#### 6. 主要结论与发现
*   **框架鸿沟**：在 NPU 上，不同框架的性能差距可达 10 倍以上。这主要源于算子实现的扩展性差异（如 QNN 的 MatMul 在大尺寸下比 llama.cpp 快 20 倍）以及卸载策略导致的通信开销。
*   **“阶段分裂”现象**：NPU 在计算受限的 **Prefill 阶段**具有绝对优势（吞吐量 > 1400 tokens/s）；但在访存受限的 **Decode 阶段**，CPU 反而表现更好，原因是 NPU 的静态图执行模式与动态增长的上下文不匹配。
*   **调度浪费**：默认的系统调度存在严重缺陷。例如，NPU 推理时 CPU 的过度轮询和不合理的 NPU 休眠延迟导致了大量能耗浪费。
*   **优化潜力**：通过调整 RPC 轮询间隔、NPU 休眠延迟和 CPU 频率，在不损失（甚至提升）吞吐量的前提下，**NPU 后端的能耗最高可降低 54.8%**。

#### 7. 优点
*   **细粒度归因**：打破了以往只能测量整机功耗的局限，首次清晰展示了 NPU 推理时 CPU 到底消耗了多少电量。
*   **揭示非直观规律**：指出了“NPU 并不总是最节能”的客观事实，特别是在 Decode 阶段。
*   **实用性强**：提出的最佳实践配置（Best-practice Configuration）直接为开发者提供了可落地的优化指南。

#### 8. 不足与局限
*   **硬件覆盖面**：研究完全集中在高通（Qualcomm）平台上，未涵盖联发科（MediaTek）或苹果（Apple）的 NPU 表现，其结论的普适性有待在其他架构上验证。
*   **量化精度损失**：虽然 W4A16 量化能显著提升 NPU 性能，但论文也指出其在小模型（如 1B）上会导致严重的困惑度（Perplexity）上升，论文未深入探讨如何平衡这种精度损失。
*   **长文本挑战**：实验虽然涉及了 4096 上下文，但对于超长文本（如 32k+）下的内存管理和 KV Cache 优化讨论较少。

（完）
