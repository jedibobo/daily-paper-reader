---
title: "Do Newer Lightweight CNNs Perform Better Under Resource Constraints? A Controlled Multigenerational Study of Architecture, Initialization, Training Budget, and Efficiency"
title_zh: 较新的轻量级卷积神经网络在资源受限下表现更好吗？一项关于架构、初始化、训练预算和效率的受控多代研究
authors: Tasnim Shahriar
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01984v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 精度、延迟和内存在硬件上的帕累托前沿分析
tldr: 本研究对九种轻量级卷积神经网络（CNN）在不同资源约束下的表现进行了受控的多代对比研究。通过在CIFAR和Tiny ImageNet数据集上统一评估准确率、参数量、计算量及在不同硬件上的推理延迟，发现较新的模型设计并非在所有指标上都优于旧模型。研究强调了预训练的重要性，并指出EfficientNet-B0在性能与效率平衡上依然极具竞争力，而硬件环境对延迟排名有显著影响，为模型选择提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在验证较新的轻量级CNN是否真的在预测性能和部署效率上全面超越了早期模型。
method: 在统一的下游协议下，对比了九种轻量级CNN在多个数据集上的准确率、资源消耗及在GPU和CPU上的推理表现。
result: EfficientNet-B0在帕累托前沿表现最稳定，而MobileNetV3-Small在某些任务上甚至优于更新的MobileNetV4。
conclusion: 较新的模型设计仅提供选择性的改进而非普遍提升，且计算量（GMACs）并不能完全预测实际推理延迟。
---

## 摘要
较新的轻量级卷积神经网络（CNN）通常被认为能提高预测性能和部署效率，但此类主张需要经过受控评估。本研究在共享的下游协议下，对比了 CIFAR-10、CIFAR-100 和 Tiny ImageNet 上的九种轻量级 CNN 模型包。我们报告了 Top-1 准确率、宏 F1 分数、Top-5 准确率、参数量、FP32 存储空间、GMACs、在 NVIDIA L4 和 AMD Ryzen 5 5500U CPU 上的 Batch-size-1 延迟、PyTorch CUDA 峰值分配张量内存以及点估计帕累托前沿（Pareto frontiers）。EfficientNetV2-S 在 CIFAR-10 和 CIFAR-100 上实现了最高的 Top-1 准确率，分别为 97.57% 和 86.98%，而 RepViT-M1.0 在 Tiny ImageNet 上以 79.87% 领先。EfficientNet-B0 与这三个数据集上的最佳结果差距分别在 0.22、0.85 和 1.79 个百分点以内，而其参数量和 GMACs 分别比 EfficientNetV2-S 少约 79% 和 86%。它还出现在每一个评估的准确率与资源帕累托前沿上，使其成为最具有持续竞争力的中等预算选项。MobileNetV3-Small 具有最低的 GMAC 计数，在两种 CPU 线程设置下都是最快的模型，并且在所有三个数据集上的观测准确率均高于 MobileNetV4-Conv-S。在随机初始化下，它领先 MobileNetV4-Conv-S 分别达 2.55、1.76 和 0.99 个百分点，且固定训练模型的配对测试集区间不包含零。尽管训练时间约为记录的五倍，但在经过 100 个 epoch 的从零训练（scratch training）后，EfficientNet-B0 仍比其预训练版本低 3.29、10.10 和 17.54 个百分点。SqueezeNet1.1 拥有最少的参数和最低的峰值 CUDA 分配，但准确率大幅落后。L4 和 CPU 环境下的延迟排名差异显著，表明仅凭 GMACs 无法预测实测的推理性能。总体而言，较新的设计提供的是选择性的而非普遍的提升。

## Abstract
Newer lightweight convolutional neural networks are often presented as improving predictive performance and deployment efficiency, but such claims require controlled evaluation. This study compares nine lightweight CNN model packages across CIFAR-10, CIFAR-100, and Tiny ImageNet under a shared downstream protocol. We report top-1 accuracy, macro F1, top-5 accuracy, parameter count, FP32 storage, GMACs, batch-size-1 latency on an NVIDIA L4 and AMD Ryzen 5 5500U CPU, peak PyTorch CUDA allocated tensor memory, and point estimate Pareto frontiers. EfficientNetV2-S achieves the highest observed top-1 accuracy on CIFAR-10 and CIFAR-100 at 97.57% and 86.98%, while RepViT-M1.0 leads Tiny ImageNet at 79.87%. EfficientNet-B0 remains within 0.22, 0.85, and 1.79 percentage points of the best result on the three datasets while using approximately 79% fewer parameters and 86% fewer GMACs than EfficientNetV2-S. It also appears on every evaluated accuracy and resource Pareto frontier, making it the most consistently competitive intermediate-budget option. MobileNetV3-Small has the lowest GMAC count, is the fastest model under both CPU thread settings, and records higher observed accuracy than MobileNetV4-Conv-S on all three datasets. Under random initialization, it leads MobileNetV4-Conv-S by 2.55, 1.76, and 0.99 points, with paired test-set intervals excluding zero for the fixed trained models. EfficientNet-B0 remains 3.29, 10.10, and 17.54 points below its pretrained counterpart after 100 epochs of scratch training, despite requiring about five times the recorded training time. SqueezeNet1.1 has the fewest parameters and lowest peak CUDA allocation, but substantially weaker accuracy. Latency rankings differ sharply between the L4 and CPU environments, showing that GMACs alone do not predict measured inference performance. Overall, newer designs provide selective rather than universal gains

---

## 论文详细总结（自动生成）

这是一份关于论文《较新的轻量级卷积神经网络在资源受限下表现更好吗？一项关于架构、初始化、训练预算和效率的受控多代研究》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着轻量级卷积神经网络（CNN）架构的快速迭代，新模型（如 2024 年的 MobileNetV4、RepViT）是否在实际的下游任务和资源受限的部署环境下，全面优于早期模型（如 2019 年的 EfficientNet、MobileNetV3）？
*   **研究动机**：新模型发布时通常带有特定的预训练技巧、增强方案和硬件目标，这使得单纯看论文数据难以在统一标准下评估“架构”本身的优劣。本研究旨在通过受控实验，揭示不同代际轻量级 CNN 在准确率、计算量、内存占用及多平台延迟之间的真实权衡。

### 2. 论文提出的方法论
*   **核心思想**：采用“受控下游协议”对 9 种代表性轻量级 CNN 模型包（架构+公开权重）进行基准测试。
*   **关键技术细节**：
    *   **统一协议**：所有模型在下游任务中使用相同的预处理（224x224 分辨率）、优化器（AdamW）、学习率调度（Cosine Annealing）和训练预算（20 epoch 微调）。
    *   **多维资源度量**：不仅关注参数量和 GMACs（计算量），还测量了 FP32 存储、NVIDIA L4 GPU 延迟、AMD Ryzen CPU 延迟（单线程/多线程）以及 PyTorch CUDA 峰值分配内存。
    *   **帕累托分析**：独立计算每个资源维度的点估计帕累托前沿（Pareto Frontier），以识别在特定约束下的最优模型。
    *   **初始化研究**：对比了预训练权重微调与从零训练（Scratch）的差异，并探讨了训练时长（20 vs 100 epoch）对性能的影响。

### 3. 实验设计
*   **数据集**：CIFAR-10、CIFAR-100、Tiny ImageNet。
*   **对比方法（9 种模型）**：
    *   早期/经典：SqueezeNet 1.1 (2016), ResNet18 (2016), MobileNetV2 (2018), ShuffleNetV2 (2018)。
    *   中期：EfficientNet-B0 (2019), MobileNetV3-Small (2019), EfficientNetV2-S (2021)。
    *   近期（2024）：RepViT-M1.0, MobileNetV4-Conv-S。
*   **Benchmark 标准**：以 Top-1 准确率为核心指标，结合多维资源消耗构建评估坐标系。

### 4. 资源与算力
*   **硬件环境**：
    *   **GPU**：NVIDIA L4（用于测量推理延迟、峰值内存及记录训练时长）。
    *   **CPU**：AMD Ryzen 5 5500U（用于测量单线程和四线程推理延迟）。
*   **训练时长示例**：EfficientNet-B0 在 CIFAR-10 上 20 epoch 微调约需 31.6 分钟，100 epoch 从零训练约需 159.7 分钟。
*   **软件栈**：Python 3.12, PyTorch 2.11/2.12, torchvision, timm。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 种模型在 3 个数据集上的完整微调实验（27 组核心实验），以及针对 EfficientNet-B0 和 MobileNet 系列的专项从零训练对比实验。
*   **充分性与公平性**：
    *   **公平性**：通过统一超参数消除了下游训练差异。
    *   **客观性**：引入了配对测试集不确定性分析（Bootstrap）和 McNemar 检验，确保准确率差异的统计显著性。
    *   **局限性**：由于算力限制，主要实验基于单次随机种子，未进行多次重复实验以评估训练方差。

### 6. 主要结论与发现
*   **新不一定比旧好**：MobileNetV4-Conv-S 在本研究的设置下，准确率和效率均未超过 2019 年的 MobileNetV3-Small。
*   **全能选手**：**EfficientNet-B0** 是最稳定的中等预算选择，它出现在所有资源维度的帕累托前沿上。
*   **任务相关性**：EfficientNetV2-S 在 CIFAR 任务上表现最佳，而 RepViT-M1.0 在更复杂的 Tiny ImageNet 上领先。
*   **理论与实际脱节**：GMACs 与 CPU 延迟高度相关（ρ=0.95），但与 GPU 延迟相关性极低（ρ=0.27）。ResNet18 虽然计算量大，但在 GPU 上推理极快。
*   **预训练价值**：即使经过 100 epoch 的从零训练，模型仍无法弥补与预训练权重微调之间的巨大准确率差距（差距高达 10-17%）。

### 7. 优点
*   **多维度评估**：跳出了单一的“准确率 vs 参数量”框架，深入探讨了内存分配和跨平台延迟。
*   **实战导向**：直接使用公开的 Checkpoint，模拟了开发者在实际项目中的模型选择过程。
*   **严谨的统计分析**：使用帕累托前沿和配对区间分析，避免了对微小精度差异的过度解读。

### 8. 不足与局限
*   **权重来源偏差**：不同模型的公开权重（如 timm vs torchvision）其原始训练方案（蒸馏、数据增强等）不同，这在一定程度上干扰了对“纯架构”的评估。
*   **超参数敏感性**：统一的下游协议可能无法发挥某些特定架构的最优性能（例如某些模型可能需要更高的学习率或更长的 Warmup）。
*   **硬件覆盖有限**：未在移动端 SoC（如 ARM NPU、DSP）上进行测试，而这些正是轻量级模型的核心战场。
*   **样本量**：未包含所有最新的轻量级 CNN（如 FasterNet, StarNet 等）。

（完）
