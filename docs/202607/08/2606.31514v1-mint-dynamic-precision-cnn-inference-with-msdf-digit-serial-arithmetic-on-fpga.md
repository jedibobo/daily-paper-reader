---
title: "MINT: Dynamic-Precision CNN Inference with MSDF Digit-Serial Arithmetic on FPGA"
title_zh: MINT：基于 FPGA 上 MSDF 位串行算术的动态精度 CNN 推理
authors: "Muhammad Usman, Malik Zohaib Nisar, Florian Aschauer, Dorit Merhof"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31514v1"
tags: ["query:hwnas"]
score: 9.0
evidence: FPGA上的CNN推理加速器及精度搜索
tldr: "MINT是一个基于FPGA的动态精度CNN推理加速器，采用最高有效位优先（MSDF）的串行算术。该架构允许在达到所需精度时提前终止计算，通过贪心搜索算法为卷积层分配INT2至INT7的变长精度。在Zynq-7020平台上，MINT在保持精度损失低于2%的同时，显著提升了VGG-16和ResNet-18的吞吐量与能效，是目前该平台上能效最高的方案之一。"
selection_source: fresh_fetch
motivation: 旨在解决传统固定精度CNN推理中存在的计算冗余问题，通过动态调整计算精度来优化资源利用率。
method: 采用基于冗余符号数字表示的MSDF串行并行内积单元，并利用预算约束的贪心搜索进行逐层精度配置。
result: "相比INT8基准，MINT在VGG-16和ResNet-18上分别实现了32.6%和26.0%的吞吐量提升，能效分别提高了82.1%和62.9%。"
conclusion: MINT证明了MSDF算术在实现高效动态精度FPGA加速器方面的潜力，在保证精度的前提下大幅提升了推理性能。
---

## 摘要
我们提出了 MINT，一种基于从左至右（LR）算术的动态精度 CNN 推理加速器。LR 算术以最高有效位优先（MSDF）的方式进行计算，并能尽早提供有用的中间结果，从而在达到所需精度时即可终止计算。其核心是一个 MSDF 串并联内积单元，该单元使用冗余有符号数字表示法来计算每个卷积窗口。一种受预算约束的贪心搜索算法对从 INT2 到 INT7 的所有卷积层进行分析，并为每层选择最低精度，同时将 VGG-16 和 ResNet-18 网络的总精度损失限制在 INT8 基准的 2% 以内。该设计在 Xilinx Zynq-7020 上以 200 MHz 的频率进行综合，VGG-16 的平均位宽为 5.64 位，ResNet-18 为 6.04 位；在 VGG-16 上实现了 19.86 GOPS 的吞吐量和 29.51 GOPS/W 的能效，在 ResNet-18 上实现了 18.86 GOPS 和 26.40 GOPS/W。与 INT8 相比，其吞吐量分别提高了 32.6% 和 26.0%，能效分别提高了 82.10% 和 62.90%，而相对于 INT8 基准的精度下降仅为 1.81% 和 1.96%。与本研究中考虑的代表性先前 FPGA CNN 加速器相比，MINT 在 Zynq-7020 平台上的 VGG-16 和 ResNet-18 设计中表现出最高的能效。

## Abstract
We present MINT, a dynamic-precision CNN inference accelerator based on left-to-right (LR) arithmetic. LR arithmetic computes in most-significant-digit-first manner and exposes useful partial results early so that the computation can be terminated once the desired precision is achieved. At the core, there is a MSDF serial-parallel inner-product unit, which uses redundant signed-digit representation to compute each convolution window. A budget-constrained greedy search profiles all convolution layers from INT2 to INT7 and selects the lowest precision per layer while constraining total accuracy loss to within 2\% of the INT8 baseline for VGG-16 and ResNet-18 networks. The design is synthesized on a Xilinx Zynq-7020 at \SI{200}{\mega\hertz}, and uses 5.64 average bits for VGG-16 and 6.04 for ResNet-18, while achieving 19.86 GOPS and 29.51 GOPS/W on VGG-16, and 18.86 GOPS and 26.40 GOPS/W on ResNet-18. This corresponds to 32.6\% and 26.0\% higher throughput and 82.10\% and 62.90\% higher energy efficiency than INT8 with only 1.81\% and 1.96\% drops relative to the INT8 baseline. Compared with representative prior FPGA CNN accelerators considered in this study, MINT delivers the highest energy efficiency among the listed VGG-16 and ResNet-18 designs on Zynq-7020 platform.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MINT**，一种专为 FPGA 设计的动态精度 CNN 推理加速器。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：卷积神经网络（CNN）在推理时通常使用固定的 8 位定点数（INT8），但不同层对精度的敏感度不同。传统的“从右至左”（LSDF）算术逻辑若提前终止计算会丢失高有效位，导致巨大误差。
*   **核心问题**：如何在不进行硬件重构、不增加控制开销的前提下，实现逐层动态精度调整，以降低功耗并提升吞吐量。
*   **整体含义**：MINT 利用“从左至右”（MSDF）算术的特性，通过控制时钟周期实现“计算提前终止”，从而在保证精度的前提下优化能效。

### 2. 方法论：核心思想与关键技术
*   **MSDF 算术逻辑**：采用最高有效位优先（MSDF）计算方式。使用冗余有符号数字（SD）表示法（基数为 2，数字集为 $\{-1, 0, 1\}$），使得输出位从最高位开始产生。
*   **硬件设计（PE 单元）**：
    *   **串并联乘法器**：一个操作数串行输入，另一个并行存储。
    *   **在线加法器树**：9 路输入（对应 $3 \times 3$ 卷积），通过 4 级加法器实现。
    *   **计算周期公式**：$P$ 位精度的计算延迟为 $C(P) = 2P + 10$ 个周期。精度越低，所需周期越少。
*   **预算约束贪心搜索算法**：
    *   **敏感度分析**：逐层测试从 INT2 到 INT7 的精度对整体准确率的影响。
    *   **效率评分**：结合周期节省量、层 MAC 占比和准确率损失进行排序。
    *   **贪心分配**：在总准确率下降不超过 2%（相对于 INT8 基准）的约束下，为每一层分配最低可行精度。

### 3. 实验设计
*   **数据集**：使用 **Imagenette-320**（ImageNet 的一个包含 10 类的子集）。
*   **模型**：**VGG-16** 和 **ResNet-18**。
*   **Benchmark（基准）**：
    *   **内部基准**：INT8 固定精度实现。
    *   **外部对比**：与 OPU、Caffeine、NEURAghe、ECHO 等多个已发表的 FPGA CNN 加速器进行对比。
*   **实验流程**：软件端进行精度搜索和模拟，硬件端进行 Vivado 综合、仿真及基于 SAIF 的功耗分析。

### 4. 资源与算力
*   **硬件平台**：Xilinx **Zynq-7020** FPGA（28nm 工艺）。
*   **算力参数**：
    *   运行频率：**200 MHz**。
    *   资源占用：单个 PE 占用 732 个 LUT 和 548 个 FF；128 个 PE 阵列预计占用约 94K LUT。
    *   吞吐量：VGG-16 为 19.86 GOPS，ResNet-18 为 18.86 GOPS。
*   **训练/模拟时长**：文中未明确说明软件搜索的具体时长，但指出使用了 PyTorch 进行后训练量化（PTQ）。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   完成了从 INT2 到 INT8 的全网络统一精度消融实验。
    *   进行了逐层敏感度热图分析。
    *   对比了动态精度与固定精度的吞吐量、能效（GOPS/W）和功耗延迟积（PDP）。
*   **充分性评价**：实验设计较为充分，涵盖了从底层硬件参数到高层模型准确率的完整链路。对比实验选择了同类平台（Zynq-7020 或类似量级）的代表性工作，具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **精度与性能平衡**：在准确率损失控制在 2% 以内时，VGG-16 的平均位宽降至 **5.64 位**，ResNet-18 降至 **6.04 位**。
*   **能效提升显著**：相比 INT8 基准，MINT 在 VGG-16 上实现了 **82.1%** 的能效提升，在 ResNet-18 上提升了 **62.9%**。
*   **吞吐量增长**：由于低精度层计算周期缩短，吞吐量分别提升了 **32.6%** 和 **26.0%**。
*   **结论**：MINT 是目前 Zynq-7020 平台上针对 VGG 和 ResNet 能效最高的方案之一。

### 7. 优点
*   **零开销切换**：无需重新配置 FPGA 逻辑或更改数据通路，仅通过控制时钟周期即可实现精度切换。
*   **硬件利用率高**：MSDF 结构天然支持提前终止，且冗余表示法消除了长进位链，有利于提升频率。
*   **硬件感知优化**：搜索算法直接将精度决策映射到周期级的硬件节省上，优化目标明确。

### 8. 不足与局限
*   **串行延迟**：虽然能效高，但位串行计算导致单个操作的绝对延迟（Latency）高于位并行架构。
*   **准确率上限**：实验中累加操作在软件模拟时使用 FP32，硬件实现可能存在微小的精度偏差。
*   **应用范围**：目前仅针对卷积层进行了优化，全连接层和非线性层（如 ReLU、Pooling）的开销在模型中被简化或忽略。
*   **数据集规模**：主要在 Imagenette 上验证，在完整 ImageNet 或更复杂的任务（如目标检测）上的表现尚待观察。

（完）
