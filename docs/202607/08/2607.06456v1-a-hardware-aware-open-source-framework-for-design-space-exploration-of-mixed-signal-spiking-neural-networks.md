---
title: A Hardware-Aware Open-Source Framework for Design Space Exploration of Mixed-Signal Spiking Neural Networks
title_zh: 一种用于混合信号脉冲神经网络设计空间探索的硬件感知开源框架
authors: "Sayma Nowshin Chowdhury, Vineeta Nair, Taseen Forhad, Aishwarya Natarajan, Corey Hart, Sahil Shah"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06456v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 用于SNN设计空间探索的硬件感知仿真框架
tldr: 本研究针对边缘端神经形态计算需求，开发了一个开源的硬件感知混合信号脉冲神经网络（SNN）设计空间探索框架。该框架集成了多种神经元模型（如LIF、HH）和非易失性模拟突触（如ReRAM），通过在PyTorch中直接引入器件级非线性特性，实现了对物理参数的优化。它不仅能评估分类准确率，还能提供芯片面积、功耗等硬件指标，为跨层协同设计提供了有力工具。
selection_source: fresh_fetch
motivation: 边缘端神经形态硬件设计需要能够模拟混合信号非理想特性并支持系统级探索的仿真工具。
method: 开发了一个基于PyTorch的开源框架，将多种神经元模型和模拟突触的物理非线性特性直接融入训练与推理过程。
result: 在N-MNIST和DVS Gesture等基准测试中，该框架成功评估了不同配置下的准确率、功耗、面积及量化敏感度。
conclusion: 该框架通过跨层设计空间探索，有效帮助开发者在精度、能效和硬件保真度之间找到最优的神经元-突触配置。
---

## 摘要
边缘端的高能效类脑计算需要能够捕捉混合信号脉冲神经网络（SNN）硬件非理想行为，同时支持系统级设计探索的仿真工具。本研究提出了一种用于混合信号 SNN 的开源硬件感知仿真框架，能够对神经元、突触和架构选择进行对比分析。该框架支持多种神经元模型，包括漏电积分发放（LIF）、Hodgkin-Huxley (HH) 和 Axon-Hillock (AH)，以及基于浮栅晶体管和阻变存储器（ReRAM）器件的非易失性模拟突触。通过将器件级非线性直接纳入基于 PyTorch 的训练和推理中，该工具实现了对物理突触参数而非理想化抽象权重的优化。该框架在标准的类脑基准测试集上进行了评估，包括 N-MNIST、DVS Gesture 和 Spiking Heidelberg Digits (SHD)。对于每种模型数据集配置，它会报告分类准确率以及面向硬件的指标，如芯片面积、功耗和量化敏感性。这些功能实现了跨层设计空间探索，并有助于确定最能满足特定应用在准确率、能效、面积和硬件保真度方面约束的神经元-突触配置。

## Abstract
Energy-efficient neuromorphic computing at the edge requires simulation tools that can capture the non-ideal behavior of mixed-signal spiking neural network (SNN) hardware while supporting system-level design exploration. This work presents an open-source hardware-aware simulation framework for mixed-signal SNNs that enables comparative analysis across neuron, synapse and architecture choices. The framework supports multiple neuron models, including Leaky Integrate-and-Fire (LIF), Hodgkin-Huxley (HH) and Axon-Hillock (AH), together with non-volatile analog synapses based on floating-gate transistors and ReRAM devices. By incorporating device-level nonlinearities directly into PyTorch-based training and inference, the tool enables optimization of physical synaptic parameters rather than idealized abstract weights. The framework is evaluated on standard neuromorphic benchmarks, including N-MNIST, DVS Gesture and Spiking Heidelberg Digits (SHD). For each model dataset configuration, it reports classification accuracy together with hardware-oriented metrics such as silicon area, power consumption and quantization sensitivity. These capabilities enable cross-layer design space exploration and help identify neuron-synapse configurations that best satisfy application-specific constraints on accuracy, energy efficiency, area and hardware fidelity.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Hardware-Aware Open-Source Framework for Design Space Exploration of Mixed-Signal Spiking Neural Networks》的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的脉冲神经网络（SNN）算法在软件仿真时表现优异，但直接部署到混合信号（模拟/数字）神经形态硬件时，由于硬件的**非理想特性**（如非线性、器件失配、工艺偏差），会导致严重的准确率下降（实验显示降幅可达 56%）。
*   **研究动机**：边缘计算设备对功耗、面积和重量（SWAP）有极高要求。混合信号电路虽高效，但缺乏能同时捕捉底层器件物理特性和高层系统学习动态的仿真工具。
*   **整体含义**：本文开发了一个开源框架，旨在弥合“算法开发”与“硬件实现”之间的鸿沟，支持在设计初期进行跨层的设计空间探索（DSE）。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将底层硬件的物理参数（如浮栅电压、ReRAM 细丝间隙）直接嵌入到基于 PyTorch 的训练循环中，使网络在训练阶段就“感知”到硬件的非线性，从而优化物理参数而非抽象权重。
*   **关键技术细节**：
    *   **硬件感知突触模型**：集成了 65nm 浮栅（FG）晶体管和 130nm 阻变存储器（ReRAM）模型。使用物理方程（如 EKV 模型）描述电流-电压关系。
    *   **多样化神经元模型**：支持 Axon-Hillock (AH)、LIF、自适应 LIF、施密特触发 LIF 以及生物物理特性更强的 Hodgkin-Huxley (HH) 模型。
    *   **训练算法**：采用基于**代理梯度（Surrogate Gradient）**的时间反向传播（BPTT）。通过链式法则将损失函数对权重的梯度转化为对物理器件参数（$\theta$）的梯度：$\frac{\partial L}{\partial \theta} = \frac{\partial L}{\partial W} \cdot \frac{\partial W}{\partial \theta}$。
    *   **硬件指标评估**：基于 Cadence 仿真提取的面积和功耗数据，框架可自动估算整个架构的硅片面积和平均功耗。

### 3. 实验设计
*   **数据集**：
    *   **N-MNIST**：神经形态视觉数据集（手写数字）。
    *   **DVS Gesture**：动态手势识别（高时空复杂度）。
    *   **SHD (Spiking Heidelberg Digits)**：语音数字数据集（强调时间依赖性）。
*   **Benchmark（基准）**：使用 `snnTorch` 训练的理想化数字 SNN 作为性能上限基准。
*   **对比维度**：对比了不同神经元模型（LIF vs HH vs AH）、不同突触技术（FG vs ReRAM）以及不同架构（全连接 FC vs 循环神经网络 RNN）在准确率、面积和功耗上的表现。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或数量。
*   **仿真效率**：文中对比了仿真耗时。以 XOR 任务为例，Cadence 晶体管级仿真耗时 **41.38 秒**，而该 Python 框架仅需 **268 毫秒**，速度提升了两个数量级以上，证明了其在大规模网络探索中的实用性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流神经形态数据集，测试了 5 种神经元模型和 2 种非易失性存储突触。
*   **充分性评估**：
    *   **逻辑验证**：首先通过 XOR 逻辑验证了 Python 模型与 Cadence SPICE 仿真的一致性。
    *   **量化分析**：进行了 3-bit（ReRAM）和 8-bit（FG）的量化敏感度实验。
    *   **客观性**：实验对比了理想软件模型与硬件感知模型之间的性能差距，客观展示了硬件非理想性带来的挑战。

### 6. 主要结论与发现
*   **硬件感知的必要性**：直接映射理想权重会导致准确率大幅崩溃，而通过硬件感知训练，即使存在非线性，网络仍能收敛并保持较高准确率（如 N-MNIST 可达 86.79%）。
*   **技术权衡**：
    *   **ReRAM**：面积极小（比 FG 小 100 倍以上），功耗低，但由于量化精度低（3-bit），准确率略低于浮栅。
    *   **FG（浮栅）**：精度高（8-bit），准确率好，但面积和功耗开销较大。
*   **架构影响**：循环架构（RNN）在处理 SHD 等时间序列数据时，比全连接架构更节省面积和功耗。

### 7. 优点
*   **跨层协同**：打破了器件物理与系统算法之间的壁垒，支持直接优化物理参数。
*   **开源与模块化**：框架基于 PyTorch，易于扩展新的神经元或器件模型。
*   **多指标评估**：不仅关注准确率，还提供了芯片设计者关心的面积和功耗量化指标。
*   **多工艺支持**：参数提取涵盖了 28nm、65nm 和 130nm 等多种 CMOS 工艺节点。

### 8. 不足与局限
*   **非理想特性覆盖有限**：目前主要模拟了静态非线性，对于器件的**随机噪声（Noise）**、**工艺偏差（Variation）**和**时间漂移（Drift）**的建模尚不完善。
*   **系统级建模缺失**：尚未考虑神经形态芯片中的地址事件表示（AER）路由延迟、仲裁开销等系统级通信约束。
*   **应用限制**：目前仅支持点神经元模型，未涉及更复杂的树突计算或区室模型。

（完）
