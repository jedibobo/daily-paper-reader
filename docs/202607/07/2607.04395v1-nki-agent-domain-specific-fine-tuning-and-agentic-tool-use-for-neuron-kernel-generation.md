---
title: "NKI-Agent: Domain-Specific Fine-Tuning and Agentic Tool Use for Neuron Kernel Generation"
title_zh: NKI-Agent：面向 Neuron 算子生成的领域特定微调与智能体工具调用
authors: "Junjie Tang, Jun Huan, Hao Zhou, Yuhao Zhang, Lin Wang"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04395v1"
tags: ["query:hwnas"]
score: 9.0
evidence: AI加速器的自动算子生成
tldr: NKI-Agent是首个针对AWS Neuron硬件（Trainium/Inferentia）的内核生成系统。针对NKI编程中多引擎架构和显式内存管理的挑战，该研究结合了领域特定微调（SFT）与“编译-验证-修复”的智能体循环。通过构建包含6000个任务的训练集和NKIBench基准测试，NKI-Agent显著提升了内核生成成功率，证明了工具使用和SFT在特定硬件编程领域的关键作用。
selection_source: fresh_fetch
motivation: 针对AWS Neuron硬件的NKI内核生成面临架构复杂、缺乏训练数据和基准测试的挑战。
method: 结合领域特定监督微调（SFT）与基于“编译-验证-修复”循环的智能体框架，并构建了大规模训练集和基准测试集。
result: "NKI-Agent配合Claude Opus在NKIBench上达到77.3%的通过率，且微调后的开源模型在极低成本下表现优于部分闭源模型。"
conclusion: 智能体工具链和领域微调是解决特定硬件内核生成难题的关键，而强化学习中的奖励设计仍需进一步优化。
---

## 摘要
最近，基于大语言模型（LLM）的算子生成智能体方法在 CUDA 上取得了显著成果。然而，对于 AWS Trainium 和 Inferentia 等新兴 AI 加速器，自动化的算子生成与优化在很大程度上仍未得到解决。通过 Neuron Kernel Interface (NKI) 为这些芯片编写算子尤其具有挑战性：开发者必须应对多引擎架构、基于分块（tile-based）的编程以及跨多级内存层次结构的显式数据移动。此外，该领域尚不存在公开可用的训练数据、基准测试或工具增强型智能体。我们推出了 NKI-Agent，这是首个将领域特定监督微调（SFT）与“编译-验证-修复”智能体循环相结合的 NKI 算子生成系统。我们将现有的 CUDA-Agent 框架适配到 Neuron 硬件，策划了 6,000 个 NKI 算子生成任务用于训练，并构建了包含三个难度级别的 250 个任务的基准测试 NKIBench。在真实的 Trn1 硬件上进行评估，结合 Claude Opus 4.8 和秩感知（rank-aware）系统提示词的 NKI-Agent 在 150 个任务的 NKIBench 上实现了 77.3% 的通过率。我们证明了工具调用至关重要：在没有智能体工具的情况下，Opus 4.8 在单次尝试（single-shot）模式下的得分仅为 6%。在 60 个任务的子集上，我们展示了经过 SFT 训练的 Qwen3-Coder-30B-A3B 以 1/100 的成本实现了 25.0% 的通过率，优于 Claude Sonnet 4 (15.0%)。我们还报告了使用二元编译奖励的群体相对策略优化（GRPO）未能优于 SFT，这为基于强化学习（RL）的算子生成的奖励设计提供了指导。

## Abstract
Recent agentic approaches to LLM-based kernel generation have achieved impressive results on CUDA. For emerging AI accelerators such as AWS Trainium and Inferentia, automated kernel generation and optimization remain largely unaddressed. Writing kernels for these chips via the Neuron Kernel Interface (NKI) is particularly challenging: developers must navigate a multi-engine architecture, tile-based programming, and explicit data movement across multi-level memory hierarchy. Moreover, no publicly-available training data, benchmarks, or tool-augmented agents exist for this domain. We introduce NKI-Agent, the first system combining domain-specific supervised fine-tuning (SFT) with a compile-verify-fix agent loop for NKI kernel generation. We adapt the existing CUDA-Agent framework to Neuron hardware, curate 6,000 NKI kernel generation tasks for training, and construct NKIBench, a 250-task benchmark across three difficulty levels. Evaluated on real Trn1 hardware, NKI-Agent with Claude Opus 4.8 and a rank-aware system prompt achieves a 77.3% pass rate on the 150-task NKIBench. We show that tool use is critical: Opus 4.8 scores 6% in single-shot mode without agent tools. On a 60-task subset, we show that an SFT-trained Qwen3-Coder-30B-A3B achieves 25.0% pass rate at 1/100th the cost, outperforming Claude Sonnet 4 (15.0%). We also report that Group Relative Policy Optimization (GRPO) with binary compilation reward fails to improve over SFT, providing guidance on reward design for RL-based kernel generation.

---

## 论文详细总结（自动生成）

### NKI-Agent: 面向 Neuron 算子生成的领域特定微调与智能体工具调用

#### 1. 核心问题与研究动机
*   **核心问题**：随着 AI 加速器（如 AWS Trainium 和 Inferentia）的兴起，如何自动化地为这些非 CUDA 硬件生成高性能且正确的底层算子（Kernel）？
*   **研究背景**：
    *   **编程门槛高**：AWS 的 Neuron 算子接口（NKI）要求开发者处理多引擎架构（Tensor, Vector 等）、固定的 2D 分块（Tile）逻辑以及显式的内存层级移动（HBM 到 SBUF/PSUM），复杂度远超 CUDA。
    *   **资源匮乏**：该领域缺乏公开的训练数据、基准测试集（Benchmark）以及能够利用硬件反馈进行自我修复的智能体框架。

#### 2. 方法论
NKI-Agent 系统由四个核心组件构成：
*   **数据合成流水线**：
    *   从 PyTorch 算子和 NKI 示例中提取种子，通过形状、数据类型和算子融合进行组合扩张。
    *   经过编译、执行验证和稳定性过滤，构建了包含 6,000 个任务的 **NKI-Agent-Ops-6K** 数据集。
*   **多轮智能体循环（Agentic Loop）**：
    *   采用“生成-编译-验证-修复”模式，最多迭代 10 轮。
    *   **工具调用**：智能体可调用 `compile`（使用 `neuronx-cc` 编译器）和 `verify`（在真实 Trn1 硬件上运行并与 PyTorch 参考结果对比）。
*   **秩感知系统提示词（Rank-aware System Prompt）**：
    *   针对不同维度的张量（2D/3D/4D）提供特定的代码模板和约束提醒，显著提升了模型处理复杂布局的能力。
*   **训练流水线**：
    *   **阶段 1 (SFT)**：在 647 条高质量专家轨迹上对 Qwen3-Coder-30B-A3B 进行 LoRA 微调。
    *   **阶段 2 (GRPO)**：尝试使用群体相对策略优化，利用二元奖励（编译通过与否）进行强化学习。

#### 3. 实验设计
*   **基准测试集**：构建了 **NKIGen-Bench**，包含 250 个任务，分为三个难度：
    *   **Level 1**：基础单算子（如 Matmul, Reductions）。
    *   **Level 2**：融合算子（如 Conv+ReLU+Bias）。
    *   **Level 3**：完整模型组件（如 Transformer 层、Mamba 块）。
*   **对比方法**：
    *   **模式对比**：单次生成（Single-shot）、无工具多轮对话（MT-5）、智能体工具模式（NKI-Agent）。
    *   **模型对比**：Qwen3-Coder-30B-A3B（基础版/SFT版/GRPO版）、Claude Sonnet 4、Claude Opus 4.8。
*   **评估指标**：通过率（Pass Rate），要求必须通过编译器校验且数值误差在允许范围内。

#### 4. 资源与算力
*   **训练算力**：SFT 阶段使用了 **8 块 NVIDIA A100 40GB GPU**，采用 BF16 精度，训练时长未详细说明，但提到批大小为 4，迭代 2 个 Epoch。
*   **评估硬件**：所有算子的编译和验证均在**真实的 AWS Trn1 硬件**上完成。
*   **评估耗时**：一次完整的 150 任务基准测试（10 轮迭代）在 Trn1 上约需 **20 小时**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 150 个任务的标准评估和 60 个任务的消融研究。
*   **消融实验**：详细分析了数据质量演进（四个版本）、工具调用的增益、不同难度级别的表现差异。
*   **充分性**：实验设计较为客观，通过对比闭源最强模型（Opus 4.8）确立了框架的性能上限。但作者也承认，由于计算资源限制，部分实验为单次运行，缺乏多次重复的误差棒分析。

#### 6. 主要结论与发现
*   **工具调用至关重要**：即使是顶级模型 Opus 4.8，在单次模式下通过率仅 6%，但在智能体循环下飙升至 **77.3%**。
*   **SFT 效能显著**：微调后的 30B 开源模型以 **1/100 的成本** 达到了 25% 的通过率，表现优于 Claude Sonnet 4。
*   **数据质量胜过调参**：SFT 过程中，修复数据中的函数名错误和维度布局问题带来的提升远超超参数优化。
*   **强化学习负面结果**：使用二元奖励（对/错）的 GRPO 效果不如 SFT，说明在算子生成领域，稀疏奖励难以提供足够的学习信号。

#### 7. 优点
*   **首创性**：填补了非 CUDA 硬件自动算子生成领域的空白，提供了首个 NKI 智能体框架和基准测试。
*   **闭环验证**：实验完全基于真实硬件反馈，而非模拟器，保证了结果的真实性。
*   **领域洞察**：通过对 GRPO 失败的分析，为后续研究指明了“分级奖励（Graded Reward）”的重要性。

#### 8. 不足与局限
*   **性能优化缺失**：目前仅关注算子的“正确性”，尚未涉及算子运行效率（Speedup）的优化。
*   **奖励设计单一**：GRPO 实验中使用的二元奖励过于简单，限制了强化学习的潜力。
*   **SDK 依赖性**：结果高度依赖于特定的 Neuron SDK 版本（v2.24），随着 API 演进，模型可能需要重新微调。

（完）
