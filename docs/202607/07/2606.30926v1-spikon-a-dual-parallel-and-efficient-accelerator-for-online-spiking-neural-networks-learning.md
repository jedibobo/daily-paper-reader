---
title: "SpikON: A Dual-Parallel and Efficient Accelerator for Online Spiking Neural Networks Learning"
title_zh: SpikON：一种用于在线脉冲神经网络学习的双并行高效加速器
authors: "Peilin Chen, Xiaoxuan Yang"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30926v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 面向高效边缘部署的算法硬件协同设计
tldr: SpikON是一个针对在线监督脉冲神经网络（SNN）学习的算法-硬件协同设计框架。针对现有在线学习算法硬件部署效率低的问题，提出了可学习阈值和缩放权重中心化技术，并设计了支持前向-后向并发计算及时间步复用的双并行加速器架构。该方案在保持高精度的同时，显著降低了训练延迟和能耗，在吞吐量和能效比上远超通用边缘计算设备。
selection_source: fresh_fetch
motivation: 现有的在线监督SNN学习算法因操作不符合硬件特性，导致在边缘设备上部署时效率低下且难以扩展。
method: 提出了可学习阈值与权重中心化技术，并设计了支持双并行引擎和级联计算复用的专用SNN硬件加速器。
result: "相比基准算法，训练延迟和能耗分别降低了32.2%和35.0%，且能效比显著优于Apple M4 GPU和类TPU加速器。"
conclusion: SpikON通过算法与硬件的深度协同，实现了高效、可扩展的端到端在线SNN监督学习，为边缘智能计算提供了新方案。
---

## 摘要
脉冲神经网络（SNNs）已成为一种极具前景的高能效类脑计算范式。然而，现有的在线无监督 SNN 学习面临训练准确率低和可扩展性差的问题。尽管目前的在线有监督学习算法在大规模数据集和网络上表现良好，但其非硬件友好的操作阻碍了高效的边缘部署。在这项工作中，我们提出了 SpikON，这是首个用于高效且可扩展的端到端在线有监督 SNN 学习的算法-硬件协同设计框架。我们首先提出了随时间变化的可学习阈值（learnable threshold through time）和随时间变化的缩放权重中心化（scaled weight centralization through time）技术，以解决传统算法的低效问题。此外，为了降低延迟和功耗，我们为 SNN 引入了新型训练数据流和级联计算复用方案，实现了前向-后向并发计算以及跨时间步的时间复用。我们进一步设计了具有双并行引擎和定制化基于 SIMD 的 SNN 核心的专用 SNN 加速器，以实现高效的端到端在线学习。实验表明，SpikON 算法在不牺牲准确率的情况下，相比基准方案分别降低了 32.2% 的训练延迟和 35.0% 的能耗。此外，与边缘 Apple M4 GPU 和类 TPU 加速器相比，SpikON 协同设计分别实现了 7.2 倍（11.5 倍）和 26.8 倍（15.8 倍）的训练吞吐量（能效）。代码可在 https://github.com/peilin-chen/SpikON 获取。

## Abstract
Spiking neural networks (SNNs) have emerged as a promising paradigm for energy-efficient brain-inspired computing. However, existing online unsupervised SNN learning suffers from low training accuracy and poor scalability. Although current online supervised learning algorithms perform well on large-scale datasets and networks, the non-hardware-friendly operations hinder efficient edge deployment. In this work, we propose SpikON, the first algorithm-hardware co-design framework for efficient and scalable end-to-end online supervised SNN learning. We first propose the learnable threshold through time and scaled weight centralization through time techniques to address the inefficiency of traditional algorithms. Moreover, to reduce latency and energy consumption, we introduce the novel training dataflow and cascade computation reuse scheme for SNNs that allows concurrent forward-backward computation and temporal reuse across timesteps. We further design the dedicated SNN accelerator with a dual-parallel engine and customized SIMD-based SNN core for efficient end-to-end online learning. Experiments show that the SpikON algorithm achieves 32.2% and 35.0% reductions in training latency and energy consumption over the baseline, without sacrificing accuracy. Moreover, the SpikON co-design achieves 7.2x (11.5x) and 26.8x (15.8x) training throughput (energy efficiency) compared with the edge Apple M4 GPU and TPU-like accelerator, respectively. The code is available at https://github.com/peilin-chen/SpikON.

---

## 论文详细总结（自动生成）

这篇论文提出了 **SpikON**，一个针对在线监督脉冲神经网络（SNN）学习的算法-硬件协同设计框架，旨在解决 SNN 在边缘设备上进行在线训练时效率低下、内存开销大以及硬件不友好等核心痛点。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然 SNN 具有高能效潜力，但其训练面临挑战。传统的 BPTT（随时间反向传播）算法内存开销巨大且不符合在线学习（即时更新）的特性。现有的在线监督学习算法（如 SLTT）虽然解决了部分问题，但依赖于硬件不友好的归一化技术（如 Batch Normalization 或 Scaled Weight Standardization），导致在边缘设备上的延迟和能耗依然很高。
*   **核心问题**：如何在不牺牲准确率的前提下，实现高效、可扩展且硬件友好的端到端在线 SNN 监督学习？

### 2. 核心方法论
SpikON 采用了算法与硬件深度协同的设计思路：
*   **算法层面（Normalization-free 技术）**：
    *   **LTTT（随时间可学习阈值）**：为每个时间步分配独立的学习阈值，通过控制脉冲发放频率来模拟激活归一化的效果。
    *   **sWCTT（随时间缩放权重中心化）**：将权重中心化（均值为零）并引入随时间步变化的可学习缩放因子 $\alpha$，替代了计算复杂的方差归一化，极大地简化了硬件实现。
*   **硬件层面（高效加速器架构）**：
    *   **BTP（双向时间并行数据流）**：允许不同时间步的前向和后向计算并发执行，通过交错执行流水线打破了时间步的串行限制。
    *   **CTCR（级联时间计算复用）**：利用相邻时间步之间脉冲激活的高度相似性，仅计算脉冲的变化部分（Delta），从而减少冗余计算。
    *   **双并行引擎（BTPE）**：设计了包含 24 个并行 Lane 的引擎，支持稀疏和稠密计算模式，专门处理向量-矩阵乘法。
    *   **定制 SIMD SNN 核心**：处理神经元动力学更新（如 LIF 模型）和特定的 SNN 指令。

### 3. 实验设计
*   **数据集**：涵盖了静态图像数据集（CIFAR-10, CIFAR-100）和神经形态（事件驱动）数据集（DVS-CIFAR10, DVS128-Gesture）。
*   **模型架构**：统一使用 VGG11 脉冲神经网络。
*   **对比算法（Benchmark）**：OTTT（Online Training Through Time）和 SLTT（Spatial Learning Through Time）。
*   **对比硬件**：边缘端 Apple M4 GPU（10核）、服务器端 Nvidia A40 GPU，以及一个自定义的类 TPU（TPU-like）脉冲训练加速器。

### 4. 资源与算力
*   **算法评估**：使用 **Nvidia A40 GPU** 进行训练测试。
*   **硬件实现**：使用 Verilog 编写 RTL，并在 **TSMC 28nm** 工艺下进行综合，主频设定为 1.5 GHz。
*   **能耗评估**：片上 SRAM 使用 TSMC 存储器编译器生成，片外内存（HBM2）能耗参考值为 5.7 pJ/b。
*   **算力细节**：加速器总面积为 35.096 $mm^2$，总功耗为 11.861 W。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流数据集上进行了完整的准确率、延迟和能耗对比。
*   **消融实验**：针对 BTP 数据流、稀疏感知 PU 以及 CTCR 复用策略分别进行了消融实验，验证了各模块对吞吐量和能效的贡献。
*   **充分性**：实验设计较为全面，不仅对比了算法性能，还通过硬件仿真对比了实际部署中的吞吐量和能效比，涵盖了从算法理论到硬件落地的全过程，论证较为客观。

### 6. 主要结论与发现
*   **算法性能**：SpikON 算法在不损失精度的情况下，比 SLTT 基准降低了 **32.2% 的延迟**和 **35.0% 的能耗**。
*   **硬件性能**：SpikON 加速器在训练吞吐量上是 Apple M4 GPU 的 **7.2 倍**，能效比是其 **11.5 倍**。
*   **对比类 TPU 架构**：相比于传统的类 TPU 脉冲加速器，SpikON 实现了 **26.8 倍的吞吐量提升**和 **15.8 倍的能效提升**。
*   **稀疏性利用**：CTCR 技术在静态数据集上表现尤为突出，能显著提高计算的稀疏度。

### 7. 优点
*   **首创性**：这是首个针对端到端在线监督 SNN 学习的算法-硬件协同设计框架。
*   **硬件友好性**：通过 LTTT 和 sWCTT 巧妙避开了传统归一化中复杂的方差计算，非常适合 ASIC 实现。
*   **并行度高**：BTP 数据流有效解决了 SNN 时间步依赖导致的流水线空转问题。

### 8. 不足与局限
*   **特定数据集限制**：在处理大尺寸输入（如 DVS128-Gesture 的 128x128 图像）时，受限于硬件 Lane 的并行规模，其吞吐量提升幅度不如在小尺寸数据集上明显（虽然能效依然领先）。
*   **硬件开销**：为了支持 24 个时间步并行，BTPE 引擎占用了超过 60% 的芯片面积，对于极度资源受限的微型边缘设备可能仍显沉重。
*   **应用范围**：目前仅在 VGG 结构上验证，对于更复杂的 Transformer 或残差结构 SNN 的适用性有待进一步研究。

（完）
