---
title: A Spiking Sequence Generator for Polar Trajectories on Neuromorphic Hardware
title_zh: 一种用于神经形态硬件上极坐标轨迹的脉冲序列生成器
authors: "William R. P. Nourse, Roger D. Quinn"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02753v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对类脑硬件的能效神经架构
tldr: 针对受限环境下的神经形态控制器，本文提出一种用于生成极坐标轨迹的脉冲神经网络（SNN）架构。该架构利用赢者通吃（WTA）机制和辅助神经元群实现可控的活动转换，并通过分流抑制技术实现对方向、速度和半径的独立控制。在SpiNNaker2硬件上的实验表明，该方法在运行速度和能效上比传统计算平台提升了数个数量级，兼具高效性与可解释性。
selection_source: fresh_fetch
motivation: 现有的神经形态控制器在端到端训练中缺乏可解释性，或未能充分利用神经形态动力学的优势。
method: 提出一种基于WTA架构和辅助神经元群的SNN，利用分流抑制技术实现对极坐标轨迹参数的独立调节。
result: 在SpiNNaker2处理器上实现，相比传统平台，运行步时缩短了2-3个数量级，能耗降低了3-4个数量级。
conclusion: 该架构为资源受限系统提供了一种高效、可解释且具备高性能的神经形态轨迹生成方案。
---

## 摘要
针对尺寸、重量和功耗受限系统的神经形态控制器，需要既能效高又在系统动力学层面具有可解释性的神经架构。然而，现有方法要么依赖于可解释性有限的端到端训练脉冲网络，要么依赖于无法充分利用神经形态动力学的转换后的经典控制器。我们提出了一种用于生成极坐标轨迹的脉冲神经网络（SNN）架构，该架构采用赢者通吃（WTA）结构，并结合辅助种群来诱导神经活动的受控转换。我们展示了这些种群动力学的调节规则，并利用一种分流抑制（shunting inhibition）形式，实现了对生成的极坐标轨迹的方向、速度和半径的独立控制。我们在 SpiNNaker2 神经形态处理器上实现了该网络，并证明与传统计算平台相比，其墙钟步进时间减少了两到三个数量级，能耗降低了三到四个数量级。

## Abstract
Neuromorphic controllers for size, weight, and power-constrained systems require neural architectures that are both energy-efficient and interpretable at the level of system dynamics. However, existing approaches either rely on end-to-end trained spiking networks with limited interpretability, or on converted classical controllers that fail to fully exploit neuromorphic dynamics. We present a spiking neural network (SNN) architecture for generating polar trajectories, using a winner-take-all (WTA) architecture with accessory populations that induce controlled transitions in neural activity. We demonstrate tuning rules for these population dynamics, and utilize a form of shunting inhibition to enable independent control of direction, speed, and radius of the resulting polar trajectories. We implement the network on the SpiNNaker2 neuromorphic processor, and demonstrate a two to three orders of magnitude reduction in wall-clock step time and three to four orders of magnitude reduction in energy expenditure when compared to conventional computing platforms.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种在神经形态硬件（SpiNNaker2）上实现的可控脉冲序列生成器，用于生成极坐标轨迹。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在资源受限（尺寸、重量和功耗，即 SWaP）的机器人平台上，传统的深度脉冲神经网络（SNN）通常表现为“黑盒”，缺乏可解释性和稳定性保障；而将经典控制器直接转换为 SNN 的方法又往往无法充分利用神经形态硬件的动态特性。
**核心问题：** 如何设计一种既能发挥神经形态硬件高能效优势，又具备低维可解释性，且能精确控制运动轨迹（如方向、速度、半径）的神经架构？
**研究背景：** 借鉴生物学中的“神经流形”（Neural Manifolds）概念，许多生物行为（如定向、步态生成）可简化为极坐标下的低维轨迹。

### 2. 方法论
论文提出了一种基于**赢者通吃（WTA）**机制的脉冲序列生成器，核心组件包括：
*   **核心 WTA 种群：** 使用漏电整合发放（LIF）神经元，通过自兴奋（维持活动）和全局抑制（确保唯一活动节点）形成稳定的吸引子动力学。
*   **辅助旋转种群（Accessory Populations）：** 引入“上升”和“下降”两组辅助神经元，通过不对称的兴奋性反馈诱导 WTA 状态在环形结构中移动，从而实现相位（角度）的旋转。
*   **增益调制突触（Shunting Inhibition 启发）：** 开发了一种乘法突触模型（$S_{gain} = S_A \cdot S_B$），通过外部脉冲的间隔（ISI）来调节神经元增益，从而独立控制旋转速度和输出半径。
*   **读取层（Readout Population）：** 将相位与振幅解耦，通过外部调制信号控制读取层的发放率，实现对极坐标轨迹半径的精确塑造。
*   **优化策略：** 利用硬件的时间离散特性，在 PyTorch 中通过暴力搜索（Grid Search）快速找到生成特定形状（如玫瑰线）所需的最佳输入脉冲频率。

### 3. 实验设计
*   **功能验证：** 演示了网络在不同刺激下的旋转方向切换、速度调节以及复杂极坐标曲线（如谐波调制曲线、五瓣玫瑰线）的生成能力。
*   **硬件基准测试（Benchmark）：**
    *   **对比平台：** SpiNNaker2（神经形态芯片）、NVIDIA RTX 4070 GPU、Intel i5-3570K CPU（单线程与多线程）。
    *   **测试指标：** 每步模拟的墙钟时间（Wall-clock time）、总能耗、最大网络规模、接口通信开销。
    *   **场景：** 改变网络规模 $N$（从 4 到 512 个节点），评估不同硬件的扩展性。

### 4. 资源与算力
*   **神经形态硬件：** 使用了单片 SpiNNaker2 开发板，包含 152 个 ARM 架构的处理单元（PE）。
*   **传统硬件：** NVIDIA RTX 4070 GPU 和 Intel i5-3570K CPU。
*   **算力使用：** 实验主要关注推理性能。在 GPU 上利用 PyTorch 进行并行化参数优化（Grid Search），由于搜索空间较小且维度低，可在极短时间内完成。
*   **训练/优化时长：** 文中未给出具体训练时长，但强调了该方法不需要迭代训练，而是“单次（single-shot）”优化。

### 5. 实验数量与充分性
*   **实验规模：** 进行了从 $N=4$ 到 $N=512$ 的多组规模扩展实验，并针对每种配置进行了多次重复采样（样本量见表 1，部分配置重复达 500 次以上），以确保统计显著性（置信区间精度 $\rho = 5\%$）。
*   **充分性：** 实验覆盖了功能性验证（动力学控制）和性能指标对比，客观地展示了神经形态硬件在推理阶段的巨大优势，同时也诚实地记录了接口通信（Loading/Interfacing）带来的额外开销。

### 6. 主要结论与发现
*   **性能飞跃：** 在 SpiNNaker2 上运行该网络，每步模拟时间比 CPU/GPU 快 **2-3 个数量级**（微秒级 vs 毫秒级）。
*   **能效极高：** 推理能耗比传统平台低 **3-4 个数量级**（毫焦级 vs 焦耳级）。
*   **可控性：** 证明了通过脉冲频率调制（PFM）可以实现对轨迹参数的线性或指数级独立控制。
*   **瓶颈发现：** 硬件接口的初始化和数据传输开销是短时间模拟的主要负担，但在持续运行的嵌入式部署中，神经形态的优势将占主导。

### 7. 优点
*   **高度可解释：** 每一个神经元和突触连接都有明确的物理/动力学含义，非“黑盒”模型。
*   **参数解耦：** 成功实现了方向、速度和半径的独立控制，这对于机器人运动控制至关重要。
*   **硬件友好：** 针对 SpiNNaker2 的内存和通信限制优化了映射算法（分布式部署 WTA 节点）。

### 8. 不足与局限
*   **角度分辨率受限：** 由于采用 WTA 离散结构而非连续环形吸引子，轨迹的角度分辨率受限于神经元数量 $N$。
*   **内存瓶颈：** 目前受限于预存储脉冲序列的内存占用，限制了超大规模网络的长时间运行（尽管闭环生成可解决此问题）。
*   **突触通道限制：** SpiNNaker2 当前 API 限制了突触通道数量，导致具有增益控制的神经元无法同时接收复杂的兴奋和抑制组合。
*   **应用范围：** 目前仅限于极坐标轨迹生成，尚未扩展到更复杂的 3D 空间或非周期性轨迹。

（完）
