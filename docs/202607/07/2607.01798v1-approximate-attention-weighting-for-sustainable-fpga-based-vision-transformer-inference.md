---
title: Approximate Attention Weighting for Sustainable FPGA-Based Vision Transformer Inference
title_zh: 基于 FPGA 的可持续视觉 Transformer 推理的近似注意力加权
authors: "Muhammad Usman, Shujaat Khan, Dorit Merhof"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01798v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于FPGA的ViT推理，采用硬件高效的注意力加权
tldr: 针对视觉Transformer（ViT）在边缘FPGA部署时Softmax计算开销大的问题，本文提出一种无需BRAM的近似注意力权重单元。该设计利用16段分段线性函数在分布式LUT中实现自然指数运算，避免了模型重校准。在Zynq-7020上的实验表明，该方案在极低资源消耗下实现了与精确Softmax接近的推理精度，为资源受限平台的节能ViT推理提供了新思路。
selection_source: fresh_fetch
motivation: 视觉Transformer中的Softmax层涉及复杂的指数和归一化运算，在小型FPGA上部署时面临高面积和高功耗的挑战。
method: 提出一种基于16段分段线性函数的近似注意力权重单元，完全使用分布式LUT实现，无需BRAM且保留了自然指数形式以避免重校准。
result: "在Xilinx Zynq-7020上实现，仅消耗1444个LUT和77个DSP，且在ViT模型上的Top-1准确率损失低于0.20%。"
conclusion: 该研究证明了通过硬件友好的近似计算可以在资源受限的边缘AI平台上实现高效且高精度的ViT推理。
---

## 摘要
视觉 Transformer (ViT) 通过利用自注意力机制捕获图像区域间的全局上下文，重塑了计算机视觉领域。这使得它们在可再生能源基础设施、工业质量控制、医学成像和自主系统传感等应用的边缘视觉检测与监控中极具吸引力。然而，在小型 FPGA 上部署 ViT 仍然具有挑战性，因为自注意力中的 softmax 阶段需要指数运算和归一化，这在硬件实现上成本很高。现有的实现通常依赖 CORDIC 流水线或基于 BRAM 的查找表，从而增加了面积和功耗。本文提出了一种用于基于 FPGA 的 ViT 推理的无 BRAM 近似注意力加权单元。所设计的方案使用完全由分布式 LUT 结构实现的 16 段分段线性函数来近似 softmax 中的自然指数。与以 2 为底的近似方法不同，自然指数公式保留了预训练的注意力温度，并避免了针对特定模型的重新校准。在 Xilinx Zynq-7020 上的实现结果显示，完整的注意力行核心使用了 1444 个 LUT、77 个 DSP 且不占用 BRAM；同时，硬件精确仿真表明，在 ViT 系列模型上，其 Top-1 准确率与精确 softmax 参考值相比，绝对差异在 0.20% 以内。这些结果证明了所提核心在资源受限的边缘 AI 平台上进行高效能 ViT 推理的潜力。

## Abstract
Vision Transformers have reshaped computer vision by using self-attention to capture global context across image regions. This makes them attractive for edge visual inspection and monitoring in applications such as renewable-energy infrastructure, industrial quality control, medical imaging, and autonomous-system sensing. However, deploying ViTs on small FPGAs remains challenging because the softmax stage in self-attention requires exponential evaluation and normalization, which are costly in hardware. Existing implementations often rely on CORDIC pipelines or BRAM-based look-up tables, increasing area and power consumption. This paper presents a BRAM-free approximate attention-weighting unit for FPGA-based ViT inference. The proposed design approximates the natural exponential in softmax using a 16-segment piecewise-linear function implemented entirely with distributed LUT fabric. Unlike base-2 approximations, the natural-exponential formulation preserves the pre-trained attention temperature and avoids model-specific recalibration. Implemented on a Xilinx Zynq-7020, the complete attention-row core uses 1444 LUTs, 77 DSPs, and no BRAM, while hardware-accurate emulation shows accuracy within a \(0.20\%\) absolute top-1 difference from the exact-softmax reference on ViT-family models. These results demonstrate the potential of the proposed core for energy-efficient ViT inference on resource-constrained edge-AI platforms.

---

## 论文详细总结（自动生成）

这是一份关于论文《Approximate Attention Weighting for Sustainable FPGA-Based Vision Transformer Inference》的结构化技术总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：视觉 Transformer (ViT) 在工业检测、医疗影像等边缘 AI 领域表现卓越，但其核心的“自注意力机制”在硬件实现上面临巨大挑战。
*   **核心问题**：自注意力中的 Softmax 运算涉及复杂的指数计算和归一化，传统 FPGA 实现通常依赖昂贵的 CORDIC 算法或占用大量 BRAM（块随机存储器）的查找表，这导致小型 FPGA 设备的面积和功耗过高，不利于可持续的边缘部署。
*   **整体含义**：本文旨在通过一种“无 BRAM”的近似计算方法，降低 ViT 在资源受限 FPGA 上的推理开销，同时保持模型精度，推动低功耗、可持续的边缘智能发展。

### 2. 论文提出的方法论
*   **核心思想**：采用 **16 段均匀分段线性 (PWL) 函数** 来直接近似自然指数 $e^x$，从而替代复杂的 Softmax 运算。
*   **关键技术细节**：
    *   **数值稳定性**：利用 Softmax 的最大值减法特性，将指数输入限制在 $[ -8, 0 ]$ 区间内。
    *   **无 BRAM 设计**：将 16 段线性函数的 17 个端点值（共 272 位）存储在分布式 LUT（查找表）逻辑中，完全不占用 FPGA 的 BRAM 资源。
    *   **保留自然指数语义**：与常见的“以 2 为底”的近似方法不同，该方法直接近似 $e^x$，避免了因底数改变导致的“注意力温度”偏移，因此无需对预训练模型进行重新校准或微调。
*   **算法流程**：硬件架构分为五个流水线阶段：得分计算（Score MAC）、PWL 指数加权、分母累加、分子累加以及最后的归一化除法。

### 3. 实验设计
*   **数据集**：主要使用 **Imagenette**（ImageNet 的一个包含 10 个类别的精简子集，共 3550 张验证图像）。
*   **Benchmark（基准）**：
    *   FP32 精确 Softmax 参考值。
    *   INT8 和 INT16 量化下的精确 Softmax 实现。
*   **对比方法**：与现有的多种 FPGA Softmax 加速方案（如基于 CORDIC、base-2 近似、在线 Softmax 等）在资源消耗、功耗和精度损失方面进行了对比。

### 4. 资源与算力
*   **硬件平台**：Xilinx Zynq-7020 (xc7z020) SoC FPGA。
*   **算力与资源消耗**：
    *   **逻辑资源**：1444 个 LUTs (2.71%)，1899 个寄存器 (1.78%)。
    *   **计算资源**：77 个 DSP48E1 单元 (35.0%)。
    *   **存储资源**：**0 个 BRAM**。
    *   **性能**：在 100 MHz 频率下，处理一个 197 标记（token）的注意力行需 79.2 $\mu s$。
*   **训练说明**：论文未提及模型训练的算力，因为其核心贡献在于推理阶段的硬件优化，模型权重直接采用预训练好的开源版本。

### 5. 实验数量与充分性
*   **实验规模**：针对 ViT 家族的多个模型（ViT-S/16, ViT-B/16, ViT-L/16 以及 DeiT-S/16）进行了测试。
*   **充分性评估**：
    *   **精度验证**：涵盖了 INT8 和 INT16 两种量化模式，实验结果显示精度损失极小。
    *   **功耗分析**：使用了基于 SAIF（开关活动交换格式）的后布线功耗仿真，结果具有较高的可信度。
    *   **场景模拟**：额外增加了一个 500 节点部署的能源缺口分析，从宏观角度论证了可持续性。
    *   **局限性**：实验主要集中在图像分类任务，且数据集规模较小（Imagenette），未在更大规模的 ImageNet-1K 上进行全量验证。

### 6. 论文的主要结论与发现
*   **精度保持**：在 INT16 模式下，该近似方法与精确 Softmax 相比，Top-1 准确率差异在 **0.20%** 以内。
*   **能效显著**：动态功耗仅为 **21 mW**，动态能效达到 601.3 krows/s/W。
*   **可持续性潜力**：通过 500 个监测节点的模拟，该方案相比 10W 的嵌入式 GPU 参考平台，每年可节省约 43.26 MWh 的电能，显著降低了碳足迹。

### 7. 优点
*   **极度节省资源**：通过分布式逻辑实现指数运算，释放了宝贵的 BRAM 资源供其他任务使用。
*   **部署友好**：无需针对特定模型进行温度校准，实现了“即插即用”的硬件替换。
*   **数值稳定**：结合了最大值减法和受限定义域，确保了在低位宽量化下的鲁棒性。

### 8. 不足与局限
*   **实验覆盖面**：精度评估仅限于分类任务，未探讨该近似方法在目标检测或语义分割等对注意力分布更敏感的任务中的表现。
*   **系统级验证不足**：目前的测试集中在“注意力行核心”，未包含完整的端到端系统（如 DDR 内存访问、控制器开销等）的实测功耗。
*   **数据集偏差**：Imagenette 数据集相对简单，可能无法完全反映模型在处理极度复杂场景时的数值敏感度。

（完）
