---
title: A Reconfigurable and Representation-Adaptive ISA-Based Architecture for Efficient DNN Acceleration
title_zh: 一种用于高效DNN加速的可重构且表示自适应的基于ISA的架构
authors: "Vasilis Sakellariou, Vassilis Paliouras, Ioannis Kouretas, Hani Saleh, Thanos Stouraitis"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04475v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于高效DNN加速的可重构硬件和指令集架构
tldr: 针对深度神经网络加速器在效率与灵活性之间的权衡问题，本文提出了一种面向机器学习的指令集架构（ISA）及可重构硬件平台。该架构通过解耦执行、细粒度数据控制和动态精度支持，实现了高处理单元利用率。基于余数系统（RNS）的22nm芯片实现证明，该设计在保持编程灵活性的同时，能效优于传统定点数加速器及现有先进设计。
selection_source: fresh_fetch
motivation: 旨在解决专用硬件加速器缺乏灵活性与通用ISA加速器效率低下之间的矛盾。
method: 提出一种支持动态精度和解耦执行的机器学习专用ISA，并结合轻量级可重构硬件架构实现高效计算。
result: 在22nm工艺下实现了5.12-10.47 TOPS/W的能效，比定点数方案提升了1.2倍，且优于现有混合精度加速器。
conclusion: 该设计成功弥合了现代DNN加速器在效率与可编程性之间的鸿沟，为高效灵活的AI计算提供了新思路。
---

## 摘要
领域专用硬件加速器在深度神经网络（DNN）工作负载方面比通用处理器具有显著更高的性能和能效，但往往缺乏对不断演进的模型架构的适应性。相比之下，基于通用指令集架构（ISA）的解决方案（如基于RISC-V的加速器）以效率为代价提高了可编程性。本研究通过引入一种面向机器学习的指令集架构（ISA）和一种兼具高效率与灵活性的可重构硬件平台，解决了这一权衡问题。所提出的ISA实现了对数据移动、动态精度以及跨数据获取、张量处理和后处理领域的解耦执行的细粒度控制。相应的架构采用轻量级可编程核心和SIMD单元，在保持低控制开销的同时实现了高处理单元利用率，且与底层数值表示无关。我们使用支持3-8位动态精度的余数系统（RNS）实例化演示了该方法。在22纳米工艺下的实现对于典型工作负载达到了5.12-10.47 TOPS/W的能效，在保持模型准确性的同时，比定点运算对应版本的能效高出多达1.2倍。它还优于目前最先进的加速器和混合精度加速器。这些结果表明，所提出的设计有效地弥合了现代DNN加速器在效率和可编程性之间的差距。

## Abstract
Domain-specific hardware accelerators provide significantly higher performance and energy efficiency for deep neural network (DNN) workloads than general-purpose processors, but often lack adaptability to evolving model architectures. In contrast, general-purpose ISA-based solutions, such as RISC-V-based accelerators, improve programmability at the cost of efficiency. This work addresses this tradeoff by introducing a machine-learning-oriented instruction set architecture (ISA) and a reconfigurable hardware platform that combine high efficiency with flexibility. The proposed ISA enables fine-grained control over data movement, dynamic precision, and decoupled execution across data-fetching, tensor processing, and post-processing domains. The corresponding architecture employs lightweight programmable cores and SIMD units to maintain high processing-element utilization with low control overhead, while remaining independent of the underlying numerical representation. We demonstrate the approach using a Residue Number System (RNS) instantiation supporting 3-8-bit dynamic precision. A 22-nm implementation achieves 5.12-10.47 TOPS/W for a typical workload and up to 1.2x higher energy efficiency than its fixed-point counterpart, while preserving model accuracy. It also outperforms state-of-the-art and mixed-precision accelerators. These results show that the proposed design effectively bridges the gap between efficiency and programmability in modern DNN accelerators.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“可重构且表示自适应的基于ISA的架构”，旨在解决深度神经网络（DNN）加速器在“高能效”与“编程灵活性”之间的长期矛盾。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心矛盾**：传统的专用集成电路（ASIC）加速器虽然能效极高（10-50 TOPS/W），但其硬件逻辑固定，难以适应快速演进的AI模型（如从CNN转向Transformer）。而基于通用指令集（如RISC-V）的方案虽然灵活，但由于通用的取指、译码和控制开销，能效远低于专用硬件。
*   **研究动机**：开发一种面向机器学习的自定义指令集架构（ISA），通过轻量级可编程核心（mCores）和解耦的执行域，在保持接近ASIC效率的同时，提供支持多种算子、数据流和数值表示（如RNS、定点数）的灵活性。

### 2. 方法论
*   **核心思想**：将硬件划分为四个独立的**处理域**（数据获取、预处理、张量处理、后处理），并由轻量级的**mCores**进行编排。
*   **关键技术细节**：
    *   **解耦执行架构**：控制流、数据移动、张量计算和非线性后处理相互分离。这种设计允许在进行张量计算的同时，并行处理数据的预取和激活函数的计算，从而最大化处理单元（PE）的利用率。
    *   **自定义ISA**：包含五类指令（访存、算术、分支、SIMD、中断）。特别设计了如`pproc`（触发后处理）和`qfunc`（融合激活与量化）等专用指令。
    *   **动态精度支持**：支持权重（3-8位细粒度）和激活值（4/8位粗粒度）的动态调整。
    *   **RNS（余数系统）实例化**：作为案例，论文使用了RNS表示法。RNS将大位宽运算分解为多个小模数通道的并行运算，消除了长进位链，显著提升了频率和能效。
    *   **层融合（Layer Fusion）**：针对深度可分离卷积，通过硬件缓冲区和PE划分，实现了DW和PW卷积的流水线融合执行。

### 3. 实验设计
*   **数据集与模型**：
    *   **图像分类**：ResNet-50、ViT-16（ImageNet数据集）。
    *   **目标检测/分割**：Yolov5-m（COCO数据集）。
    *   **自然语言处理**：Bert-base（SQuAD数据集）。
*   **Benchmark与对比方法**：
    *   **基准线**：该架构自身的定点数（FXP）实现版本。
    *   **对比对象**：包括基于RISC-V的加速器（MARSELLUS, Flex-v, Spatz, Quadrilatero）以及先进的专用加速器（DIANA, Bit-Blade, DataMaestro）。
*   **实现工艺**：采用22nm FDX工艺进行全流程设计（Place-and-Route）。

### 4. 资源与算力
*   **硬件实现**：在22nm工艺下，核心面积为**2.25 $mm^2$**。
*   **运行参数**：在0.8V下主频为**800 MHz**；在0.65V低功耗模式下主频为**500 MHz**。
*   **算力消耗**：论文未提及模型训练阶段的算力（如GPU型号），重点在于推理阶段的硬件效率。其峰值能效达到了**10.47 TOPS/W**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括：
    *   不同模型（CNN, Transformer, BERT）的端到端精度与能效权衡实验。
    *   PE利用率分析（针对不同矩阵规模和卷积核）。
    *   功耗分解实验（分析控制、存储、计算各部分的占比）。
    *   RNS模数基（Moduli Base）的最优性搜索实验。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了能效数字，还深入探讨了在不同精度损失约束下（如1%精度下降）的实际收益，并涵盖了视觉和语言两大主流AI领域。

### 6. 主要结论与发现
*   **高能效**：在22nm工艺下，该架构实现了**5.14 - 10.47 TOPS/W**的能效。
*   **RNS优势**：在相同精度下，RNS版本比定点数（FXP）版本能效提升了约**1.2倍**。
*   **低控制开销**：尽管引入了可编程性，但控制逻辑（mCores）仅占总面积的**5%**，功耗占比仅为**4.1% - 8.9%**。
*   **高利用率**：对于典型的矩阵乘法和卷积任务，PE利用率可稳定在**90% - 99%**。
*   **精度保持**：通过近似Softmax和GeLU算法，在8位量化下几乎不损失模型准确率（如BERT在SQuAD上的F1分数仅下降极小）。

### 7. 优点
*   **极佳的平衡性**：成功在ISA的灵活性与ASIC的效率之间找到了平衡点。
*   **表示无关性**：ISA设计不绑定特定的数值格式，这意味着未来可以轻松扩展到浮点数或其他新型数值系统。
*   **细粒度控制**：通过mCores实现的细粒度数据流控制，解决了传统加速器在处理深度可分离卷积等非规则算子时利用率低的问题。

### 8. 不足与局限
*   **编译器依赖**：目前主要依赖预定义的程序模板，尚未实现全自动的端到端模型编译器，这可能限制普通开发者的上手速度。
*   **RNS转换成本**：虽然RNS计算高效，但输入输出时的FXP与RNS转换逻辑仍占据了约7%的面积和4%的功耗。
*   **应用场景限制**：该研究主要聚焦于推理加速，对于需要高动态范围的训练（Training）场景，该架构的适用性尚未得到验证。

（完）
