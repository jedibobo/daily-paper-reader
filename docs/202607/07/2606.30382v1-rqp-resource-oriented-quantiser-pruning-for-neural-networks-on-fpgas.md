---
title: "RQP: Resource-Oriented Quantiser Pruning for Neural Networks on FPGAs"
title_zh: RQP：面向 FPGA 神经网络的资源导向量化器剪枝
authors: "Changhong Li, Biswajit Basu, Shreejith Shanker"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30382v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 面向FPGA边缘神经网络应用的资源导向型量化器剪枝
tldr: 本研究针对FPGA边缘神经网络应用，提出了一种资源导向的量化器剪枝方法（RQP）。针对传统高粒度量化（HGQ）流程计算量大且易错过最优子网络的问题，RQP通过一次性剪枝快速接近目标搜索空间，并结合双向Beta调度进行微调。在喷注子结构分类任务中，该方法在保持竞争力的帕累托前沿的同时，将搜索成本降低了高达20.58倍，显著提升了硬件受限下的模型优化效率。
selection_source: fresh_fetch
motivation: 传统的高粒度量化流程计算成本高且具有单向不可逆性，容易在资源受限的情况下错过最优的网络配置。
method: 提出一种资源导向的一次性量化器剪枝方法，并利用双向Beta调度进行微调，以实现对帕累托前沿的精细扫描。
result: 在JSC任务上的实验表明，该方法在保证模型性能的同时，比标准HGQ流程的搜索成本降低了多达20.58倍。
conclusion: RQP方法通过优化搜索策略，为FPGA上的神经网络部署提供了一种高效且资源敏感的量化剪枝方案。
---

## 摘要
高粒度量化 (HGQ) 利用权重级量化和剪枝来设计资源高效的神经网络加速器，在准确率和硬件利用率之间实现了极具吸引力的权衡。HGQ 特别适用于基于 FPGA 的边缘神经网络应用。标准的 HGQ 工作流从高精度模型开始，在基于梯度的优化引导下逐步降低位宽，以描绘帕累托前沿。这种单调且不可逆的剪枝过程计算开销巨大，且可能忽略给定资源水平下的最优子网络。我们提出了一种面向资源的单次量化器剪枝方法，使网络直接接近目标搜索空间，随后使用双向 beta 调度进行微调，以实现对帕累托前沿更精细的扫描。在射流子结构分类 (JSC) 任务上的验证表明，与标准 HGQ 工作流中的单调资源缩减相比，我们的方法将搜索成本降低了高达 20.58 倍，同时实现了具有竞争力的帕累托前沿和最终网络配置。

## Abstract
High granularity quantisation (HGQ) exploits weight-level quantisation and pruning to design resource-efficient neural network accelerators, achieving an attractive trade-off between accuracy and hardware utilisation. HGQ is particularly well suited to FPGA-based edge neural network applications. Standard HGQ workflow starts from a high-precision model and progressively reduces bit width, guided by gradient-based optimisation to outline the Pareto frontier. This monotonic and irreversible pruning process is computationally intensive and can overlook the optimal subnetwork for a given resource level. We propose a resource-oriented one-shot quantiser pruning method that brings the network directly close to the target search space, and then use bidirectional beta scheduling for fine-tuning to enable a more refined scan of the Pareto frontier. Validated on the jet substructure classification, JSC, task, our method reduces the search cost by up to 20.58x compared with monotonic resource reduction in standard HGQ workflows, while achieving a competitive Pareto frontier and final network configuration.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RQP（Resource-Oriented Quantiser Pruning，资源导向量化器剪枝）** 的新方法，旨在优化神经网络在 FPGA 上的部署效率。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 FPGA 等资源受限的边缘设备上，实现微秒级超低延迟推理需要对神经网络进行极度压缩。
*   **研究背景**：现有的**高粒度量化（HGQ）**技术虽然能同时优化准确率和硬件资源（通过位宽级剪枝），但其标准流程采用“渐进式单调减枝”。
*   **痛点**：
    1.  **计算开销大**：从高精度模型缓慢压缩到目标资源水平需要极长的训练时间。
    2.  **路径依赖**：单向不可逆的剪枝过程容易陷入局部最优，错过性能更好的子网络。
    3.  **搜索效率低**：大量计算资源浪费在中间过渡阶段，而非目标资源区间。

### 2. 核心方法论
RQP 放弃了渐进式压缩，转而采用“一次性剪枝 + 目标区间精细搜索”的策略：

*   **一次性量化器剪枝（One-shot Quantiser Pruning）**：
    *   **位宽重分配**：根据预训练高精度模型的统计特性，直接将各层位宽缩放到目标资源预算（EBOPs）附近。
    *   **谱约束拓扑补偿（Spectral-constrained Topology Compensation）**：在剪枝后，通过检查权重的条件数（Condition Number）来确保稀疏子网络具有良好的“可训练性”，避免梯度消失或信号坍塌。
*   **双向 $\beta$ 调度（Bidirectional $\beta$ Scheduling）**：
    *   **内环控制**：根据实时观测到的资源消耗与目标的偏差，动态调整资源压力系数 $\beta$（超预算则增加压力，低于预算则减小）。
    *   **外环松弛（退火机制）**：当验证集性能停滞时，主动降低 $\beta$ 以释放压缩压力，允许网络重新激活部分连接，从而跳出“$\beta$ 死锁”并探索更优的帕累托前沿。

### 3. 实验设计
*   **任务场景**：高能物理中的**射流子结构分类（JSC）**，这是典型的超低延迟 FPGA 应用场景。
*   **数据集**：OpenML（hls4ml LHC Jets）和 CERNBox 数据集。
*   **Benchmark 与对比方法**：
    *   **基准**：标准 HGQ 流程（不同训练时长：6k 到 200k epochs）。
    *   **对比剪枝算法**：SNIP、GraSP、Magnitude（量级剪枝）、Random（随机剪枝）以及不带谱约束的重分配方法。
    *   **硬件对比**：与 LogicNets、PolyLUT、NeuraLUT 等多种最先进的 FPGA 专用神经网络架构进行帕累托前沿对比。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX A4000 GPU** 和 Intel U7 265K CPU。
*   **训练时长**：
    *   标准 HGQ 达到最优性能需要 200,000 个 epoch（约 373 分钟）。
    *   **RQP 仅需 6,000 个 epoch（约 18 分钟）**。
*   **算力节省**：在达到相同或更高准确率的情况下，搜索成本降低了 **20.58 倍**。

### 5. 实验数量与充分性
*   **实验规模**：针对 400 到 11,000+ EBOPs 的多个资源预算点进行了实验。
*   **消融实验**：通过对比“Reallocation”变体，验证了谱约束对提高子网络恢复能力的有效性。
*   **充分性评价**：实验覆盖了从算法训练轨迹到 FPGA 最终布局布线（Place-and-Route）的全流程。使用了两个不同的物理数据集，证明了方法的通用性。实验设计客观，对比了多种经典剪枝策略，具有较强的说服力。

### 6. 主要结论与发现
*   **效率提升**：RQP 能在极短时间内直接切入目标资源区间，显著缩短了硬件感知神经架构搜索的时间。
*   **性能优越**：在相同的 LUT 资源消耗下，RQP 发现的模型准确率高于标准 HGQ。
*   **硬件友好**：由于 RQP 缓解了 $\beta$ 死锁，生成的模型位宽分布更优化，减少了关键路径延迟，使得 FPGA 上的**最高工作频率（Fmax）更高**，从而补偿了流水线深度的增加，实现了更低的端到端延迟（ns）。

### 7. 优点与亮点
*   **搜索策略创新**：将“一次性跳跃”与“双向微调”结合，打破了传统剪枝的单向限制。
*   **可训练性保障**：引入谱约束作为代理指标，解决了极度稀疏化后模型难以收敛的问题。
*   **实用性强**：直接产出可部署的 Vivado 硬件结果，而非仅停留在理论精度。

### 8. 不足与局限
*   **模型复杂度**：目前主要在全连接网络（MLP）上验证，尚未在更复杂的 CNN 或 Transformer 架构上大规模测试（尽管作者表示这是未来工作）。
*   **超参数敏感性**：双向调度中的 $\beta$ 调整步长和松弛窗口可能需要针对不同任务进行微调。
*   **应用领域单一**：实验集中在高能物理分类任务，对于计算机视觉或自然语言处理等通用领域的适用性有待进一步验证。

（完）
