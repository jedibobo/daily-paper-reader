---
title: "Optimus: A Generic Operator-Level PyTorch Model Transformation Framework"
title_zh: Optimus：一个通用的算子级 PyTorch 模型转换框架
authors: "Menglu Yu, Jiaqi Xu, Yuzhen Huang, Yanbo Liang, Jia Liu, Shuai Yang, Jason Ansel, Elias Ellison, Edward Yang, Brian Hirsh, Jia Chen Ren, Will Feng, Oguz Ulgen, Xu Zhao, Daohang Shi, Huaqing Xiong, Quanyu Zhu, Mingming Ding, Junqing Zhou, Ruilin Chen, Yuhang Yang, Chi-Keung Luk"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.02945v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 算子级机器学习编译器与模型转换
tldr: Optimus是一个集成在PyTorch 2.x中的通用算子级模型转换框架，旨在解决大规模工业推荐模型中手动优化规则难以扩展的问题。它通过预定义模式和高效的贪婪搜索算法实现自动化的图优化，在保持模型语义一致性的同时，显著提升了推理性能并降低了编译开销，为复杂深度学习模型提供了一种可扩展且高度定制的优化方案。
selection_source: fresh_fetch
motivation: 传统的基于PyTorch FX的手动模块级重写规则难以应对日益复杂且多样化的工业级模型架构。
method: 提出了一种基于算子级的通用转换框架，利用预定义模式和贪婪搜索算法在PyTorch 2.x编译栈中进行自动化的图匹配与替换。
result: "在工业级推荐模型上实现了最高63%的加速、6%的显存降低以及超过400秒的编译时间缩减。"
conclusion: Optimus作为PyTorch 2.x的可定制模型转换层，为大规模深度学习模型的自动化性能优化提供了一个高效且通用的工具。
---

## 摘要
在大规模工业应用中，驱动推荐和排序的深度学习模型具有复杂且多样的模型架构。这些模型由庞大的机器学习工程师团队不断开发和改进，使得手动优化变得不可行。因此，基于图的优化技术已成为提升性能的行业标准，其中 PyTorch FX 转换处于领先地位。这些转换通常依赖于一组人工设计的模块级重写规则，这些规则无法扩展到多样的模型架构。为了解决这一局限性，我们推出了 Optimus，这是一个构建在 PyTorch 2.x (PT2) 机器学习编译器中的通用模型转换框架。通过一组简洁的预定义模式，Optimus 应用高效的贪心搜索算法进行模式匹配和替换，同时保持模型语义。它被设计并实现为一个高度可定制且可扩展的框架，并集成到 PT2 技术栈中。我们的评估表明，与基准相比，该框架在工业级推荐模型上可实现高达 63% 的加速、6% 的峰值内存减少以及超过 400 秒的编译时间缩减。Optimus 已随 PyTorch 2.x 一同开源，作为一个可定制的模型转换层。

## Abstract
In large-scale industrial applications, deep learning models that power recommendation and ranking have complex and diverse model architectures. These models are continuously developed and refined by large teams of machine learning engineers, rendering manual optimization infeasible. Consequently, graph-based optimization techniques have become an industry standard for boosting performance, with PyTorch FX transformations leading the charge. These transformations typically rely on a set of human-engineered module-level rewrite rules which are not scalable to diverse model architectures. To address this limitation, we introduce Optimus, a general-purpose model transformation framework built in the PyTorch 2.x (PT2) machine learning compiler. With a concise set of predefined patterns, Optimus applies an efficient greedy search algorithm for pattern matching and replacement, while preserving model semantic. It is designed and implemented as a highly customizable and extensible framework integrated into the PT2 stack. Our evaluation shows that the framework can achieve up to 63% speedup, 6% peak memory reduction, and over 400 second compile time decrease for our industry-scale recommendation models compared to baselines. Optimus is open-sourced together with PyTorch 2.x as a customizable model transformation layer.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Optimus** 的通用算子级模型转换框架，旨在优化大规模工业级深度学习模型（特别是推荐系统）的性能。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在工业界，推荐和排序模型架构极其复杂且迭代迅速。传统的优化方法（如 PyTorch FX 转换）主要依赖人工编写的“模块级”重写规则，这种方式难以扩展到多样化的架构，且无法捕捉细粒度的算子融合机会。
*   **核心问题**：现有的自动化图优化工具（如 TASO、TENSAT）虽然能发现优化机会，但搜索空间巨大，导致编译时间过长（动辄数小时），无法满足生产环境的实时训练和快速部署需求。
*   **整体含义**：Optimus 通过在 PyTorch 2.x (PT2) 编译栈中引入一套基于“原子算子”的通用转换框架，平衡了优化深度与编译效率。

### 2. 方法论
*   **核心思想**：将复杂的启发式优化分解为模块化的原子算子转换任务，利用贪婪搜索算法在计算图中快速定位并替换低效子图。
*   **关键技术细节**：
    *   **集成点**：深度集成于 PT2 栈，支持在 Torch-IR、ATen-IR 和 Inductor-IR 多个层面进行转换。
    *   **水平优化（Horizontal Optimization）**：包括批处理融合（Batch Fusion）和组融合（Group Fusion），将无数据依赖的相同或不同形状的算子（如多个 Linear 或 LayerNorm）合并为一个内核启动。
    *   **垂直优化（Vertical Optimization）**：专注于消除冗余操作，特别是针对推荐模型中常见的 `split-concat` 模式进行精简。
    *   **搜索算法**：采用**基于锚点（Anchor-based）的贪婪搜索**。算法从特定算子（如 `split`）开始，通过广度优先搜索（BFS）在限定深度（`max_depth`）内匹配预定义模式，避免了全图搜索的指数级复杂度。
    *   **语义保持**：通过符号表达式定义源模式和目标模式，确保转换前后的输入输出一致，并提供双向调试和数值检查机制。

### 3. 实验设计
*   **场景与模型**：
    *   **工业级模型**：评估了 5 种 Meta 内部的大规模推荐模型架构（包括 DHEN-like, Wukong-like, COFFEE-like, Interformer-like 等）。
    *   **开源模型**：在 TorchBench 中添加了一个示例模型以供复现。
*   **Benchmark（基准）**：
    *   主要对比对象是 **TorchInductor**（PyTorch 2.x 的默认后端）。
    *   在特定实验中对比了自动化框架 **TASO** 和 **TENSAT**。
*   **评估指标**：每秒查询数（QPS）增益、峰值内存减少量、编译时间缩减。

### 4. 资源与算力
*   **硬件设备**：使用了 NVIDIA **A100、H100 和 B200** GPU。
*   **算力规模**：实验涵盖了从单卡到大规模集群（**64 到 256 张 GPU**）的配置。
*   **训练/编译时长**：论文提到通过优化，编译时间可减少 400 秒以上；在某些模型上，将编译预算从 5 分钟增加到 10 分钟可以获得更高的 QPS 收益。

### 5. 实验数量与充分性
*   **实验规模**：针对 5 种不同类型的工业模型进行了详细的 QPS 增益分析，并对基础模型（Foundation Model）进行了显存和编译时间的消融实验。
*   **充分性**：实验覆盖了推理和训练两个阶段，并考虑了不同硬件（H100 vs B200）的影响。
*   **客观性**：每个实验重复 100 次以消除测量噪声，并使用了标准的工业级架构作为测试基准，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：在工业推荐模型上实现了高达 **63% 的 QPS 提升**。
*   **显存优化**：通过激活值量化模式（FP8），峰值内存减少了 **2%–6%**。
*   **编译效率**：相比于基准，编译时间显著降低（减少 >400s），且在处理小算子堆叠时效果尤为明显（内核数量从上万个压缩至几十个）。
*   **协同效应**：水平优化和垂直优化结合使用时，效果远好于单一优化，因为垂直优化可以消除水平优化引入的辅助节点（如 `stack/unbind`）。

### 7. 优点
*   **通用性强**：不绑定特定模型，基于原子算子的规则可以复用到各种新架构中。
*   **生产环境友好**：搜索算法高效，编译开销可控，适合大规模工业部署。
*   **高度集成**：作为 PT2 的插件，充分利用了 PyTorch 2.x 的符号追踪和自动微分能力。
*   **开源贡献**：代码已集成进 PyTorch 官方库，具有很高的实用价值。

### 8. 不足与局限
*   **模式依赖**：虽然是算子级，但仍需要人工预定义转换模式（Pattern），未能实现完全脱离人工干预的“超优化（Superoptimization）”。
*   **模型偏差**：实验主要集中在推荐系统（DLRM 变体），对于视觉（CV）或纯语言模型（LLM）的泛化效果在文中虽有提及（如 LLaMA 显存优化），但数据支持相对较少。
*   **硬件特定性**：部分优化（如 FP8 量化、特定算子填充）高度依赖 NVIDIA 的硬件特性（如 CUDA Core/Tensor Core 的对齐要求），在其他硬件平台上的表现未知。

（完）
