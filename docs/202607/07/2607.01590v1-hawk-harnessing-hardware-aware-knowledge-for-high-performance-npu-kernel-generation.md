---
title: "Hawk: Harnessing Hardware-Aware Knowledge for High-Performance NPU Kernel Generation"
title_zh: Hawk：利用硬件感知知识实现高性能 NPU 算子生成
authors: "Junyi Wen, Ruiyan Zhuang, Yongjia Xu, Pengtu Li, Rui Zou, Hongyi Chen, Chingman Wan, Puxu Yang, Wuhui Chen, Yanlin Wang"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01590v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于NPU算子生成的硬件感知知识
tldr: "针对NPU内核开发中硬件约束复杂、LLM缺乏硬件先验导致生成代码易崩溃的问题，本文提出Hawk框架。Hawk通过运行时知识合成、瓶颈感知检索和效果驱动蒸馏三大模块，将硬件感知知识融入生成过程。实验表明，Hawk显著提升了内核生成准确率（从49.4%至80.0%）并实现了最高2.2倍的性能加速。"
selection_source: fresh_fetch
motivation: NPU内核开发受限于复杂的硬件约束和内存层级，现有LLM因缺乏硬件先验知识常生成低效或无法运行的代码。
method: 提出包含运行时知识合成、2D检索范式和基于执行反馈的知识蒸馏三大模块的免训练框架Hawk。
result: "在真实NPU工作负载上，Hawk将生成准确率提升至80.0%，并比现有基准方法实现高达2.2倍的执行加速。"
conclusion: Hawk证明了通过显式引入硬件感知知识和执行反馈，可以有效解决LLM在高性能NPU内核生成中的局限性。
---

## 摘要
为神经网络处理器（NPU）开发高性能算子是当前工业界的一个关键瓶颈，要求开发者手动处理隐式的硬件约束和严格的内存层级。虽然大语言模型展现了巨大的自动化潜力，但由于根本上缺乏特定于硬件的先验知识，它们在 NPU 上的表现极差。盲目地从相似的 NPU 算子中移植代码片段虽然可能通过编译器检查，但往往会因为违反底层硬件约束而导致运行时崩溃和性能下降。为了克服这一问题，我们提出了 Hawk，这是一个无需训练的框架，通过三个核心模块利用硬件感知知识：(1) 运行时知识综合模块，采用三部分可执行知识表示，将错误上下文与可执行语义内在耦合；(2) 瓶颈感知知识检索模块，实现了一种二维检索范式，将查询投影到正交的语法空间和硬件对齐的语义空间中；(3) 效果驱动的知识蒸馏模块，利用大模型驱动的语义仲裁，根据经验执行反馈，通过剪枝错误和整合冗余来持续蒸馏知识。在真实 NPU 工作负载上的广泛评估表明，Hawk 将生成准确率从 49.4% 提升至 80.0%，同时相比最先进的基准方法实现了高达 2.2 倍的执行加速。

## Abstract
Developing high-performance kernels for Neural Processing Units (NPUs) is a critical industry bottleneck, requiring developers to manually navigate implicit hardware constraints and strict memory hierarchies. While large language models offer immense automation potential, they fail catastrophically on NPUs due to a fundamental lack of hardware-specific priors. Naively transplanting code snippets from similar NPU kernels may pass the compiler, but it consistently triggers runtime crashes and performance degradation by blindly violating underlying hardware constraints. To overcome this, we introduce Hawk, a training-free framework that harnesses hardware-aware knowledge through three core modules: (1) Run-Time Knowledge Synthesis Module, which employs a Triple-Part Executable Knowledge Representation to inherently couple the error context with executable semantics; (2) Bottleneck-Aware Knowledge Retrieval Module, which implements a 2D-Retrieval paradigm to project queries into orthogonal syntactic and hardware-aligned semantic spaces; and (3) Effect-Driven Knowledge Distillation Module, which leverages LLM-driven semantic arbitration to continuously distill the knowledge by pruning errors and consolidating redundancies based on the empirical execution feedback. Extensive evaluations on real-world NPU workloads demonstrate that Hawk elevates generation accuracy from 49.4% to 80.0%, while achieving up to a 2.2x execution speedup over state-of-the-art baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Hawk** 的免训练框架，旨在解决大语言模型（LLM）在生成高性能神经网络处理器（NPU）算子（Kernel）时因缺乏硬件先验知识而导致的准确率低、性能差的问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机：** NPU（如华为昇腾 Ascend）采用异构架构，具有复杂的显式内存层级（Global Memory, L1, UB, L0）和指令流水线。开发者必须手动管理数据搬运和分块（Tiling）。
*   **核心挑战：** 
    *   **LLM 缺乏硬件先验：** 即使是强大的模型（如 DeepSeek-V4），在 NPU 算子生成上的编译成功率和正确率也远低于 GPU（CUDA）。
    *   **硬件约束严格：** 简单的代码移植常因违反统一缓冲区（UB）容量限制而导致运行时崩溃。
    *   **性能瓶颈：** 即使功能正确的代码，若未利用硬件加速特性（如 HF32 模式、多核并行），其执行速度可能比优化后的代码慢 20 倍以上。
*   **整体含义：** Hawk 通过构建一个动态演进的“硬件感知知识库”，引导 LLM 在生成代码时遵循物理硬件约束，从而在无需微调模型的情况下实现高性能算子生成。

### 2. 论文提出的方法论
Hawk 框架包含三个核心模块，形成一个“合成-检索-蒸馏”的闭环：

*   **运行时知识合成模块 (Run-Time Knowledge Synthesis)：**
    *   **三部分可执行知识表示：** 将知识结构化为：(1) **元数据层**（索引触发器，如算子类型、错误码）；(2) **理由层**（自然语言解释硬件约束和优化策略）；(3) **构件层**（经过验证的可执行代码片段）。
    *   **触发机制：** 仅在解决复杂 Bug、修正官方文档错误或获得显著性能提升时才合成新知识，确保知识库的高价值。
*   **瓶颈感知知识检索模块 (Bottleneck-Aware Retrieval)：**
    *   **2D 检索范式：** 同时在**语法空间**（利用 BM25 算法匹配 API 签名和错误码）和**硬件对齐语义空间**（利用 Embedding 匹配硬件瓶颈描述）进行检索。
    *   **RRF 融合：** 使用倒数排名融合（Reciprocal Rank Fusion, RRF）公式 $S(K_i, Q) = \sum_{r \in \{syn, sem\}} \frac{1}{k + rank_r(K_i)}$ 来平衡两个维度的排名，确保检索结果既能编译通过又符合硬件逻辑。
*   **效果驱动知识蒸馏模块 (Effect-Driven Knowledge Distillation)：**
    *   **语义仲裁：** 利用 LLM 充当仲裁者，根据实际的编译和执行反馈，自动剔除导致崩溃的错误知识，并合并功能冗余的条目，防止上下文窗口被无效信息占据。

### 3. 实验设计
*   **数据集/场景：** 
    *   **L1 算子：** 基础激活函数和数学运算（如 Exp, Gelu, Sigmoid 等）。
    *   **L2 算子：** 更复杂的组合算子（如 AdamW, Softmax, CrossEntropyLoss 等），用于测试框架的扩展性。
*   **Benchmark：** 使用了 **CANNBench**（包含 20 个测试用例/算子）和 **MultiKernelBench**。
*   **对比方法：** 
    *   **Vanilla LLM：** 原始大模型。
    *   **Few-shot：** 提供简单示例的提示词工程。
    *   **CANNBot：** 现有的基于官方文档和工具的 Agent 基准。
*   **评价指标：** 编译成功率 (CS)、准确率 (Acc)、加速比 (Speedup) 以及综合评分 (Score)。

### 4. 资源与算力
*   **硬件环境：** 实验在配备 **8 台华为昇腾 910B2 NPU** 的服务器上运行。
*   **软件环境：** 使用 CANN 8.5.1 版本。
*   **模型：** 使用了 GLM-5.1 作为主要后端，并使用 DeepSeek-V4-Flash 验证泛化性。
*   **训练时长：** 论文强调 Hawk 是 **Training-free（免训练）** 的，因此不涉及模型训练时长，主要开销在于推理和迭代优化过程。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 8 个 L1 算子和 16 个 L2 算子的详细评测。
*   **消融实验：** 针对知识表示结构、2D 检索机制、蒸馏模块分别做了消融，证明了每个模块的必要性。
*   **充分性评价：** 实验设计较为充分。不仅对比了最终结果，还展示了**迭代轨迹（Score Trajectory）**，证明 Hawk 能更快收敛到高性能代码。同时，通过 L1 到 L2 的迁移实验验证了知识的通用性，实验结果具有统计意义。

### 6. 主要结论与发现
*   **准确率提升：** Hawk 将 NPU 算子生成的平均准确率从 49.4% 提升至 **80.0%**。
*   **性能加速：** 相比于最强的基准方法，Hawk 生成的代码实现了最高 **2.2 倍**（部分复杂算子达 3.5 倍）的执行加速。
*   **知识的重要性：** 实验发现，即使只有 4.4% 的错误知识混入，也会导致生成准确率从 84% 暴跌至 28%，证明了“知识蒸馏”在闭环系统中的关键作用。
*   **跨模型泛化：** Hawk 在较弱的模型（如 DeepSeek-V4-Flash）上同样有效，显著提升了其处理 NPU 任务的能力。

### 7. 优点
*   **硬件深度耦合：** 不同于通用的代码生成框架，Hawk 深入到了 NPU 的内存管理和指令流水线层面。
*   **免训练且易扩展：** 无需昂贵的模型微调，通过更新外部知识库即可适应 API 的频繁更迭。
*   **闭环自进化：** 能够从失败的尝试中学习并固化为知识，具有很强的工程实用性。

### 8. 不足与局限
*   **初始知识依赖：** 框架的“冷启动”依赖于从现有专家代码中提取的初始知识库。
*   **生态局限性：** 目前所有的实验和知识提取均基于华为昇腾（Ascend）生态，虽然理论上可迁移至其他 NPU（如寒武纪、TPU），但尚未在其他架构上验证。
*   **搜索空间限制：** 对于极其复杂的融合算子（Fused Ops），目前的检索和合成机制可能面临搜索空间过大的挑战。

（完）
