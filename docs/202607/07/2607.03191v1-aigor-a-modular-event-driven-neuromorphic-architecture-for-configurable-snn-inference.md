---
title: "AIGOR: A Modular, Event-Driven Neuromorphic Architecture for Configurable SNN Inference"
title_zh: AIGOR：一种用于可配置 SNN 推理的模块化、事件驱动类脑架构
authors: "Pierpaolo Perticaroli, Roberto Ammendola, Andrea Biagioni, Ottorino Frezza, Francesca Lo Cicero, Michele Martinelli, Pier Stanislao Paolucci, Elena Pastorelli, Luca Pontisso, Cristian Rossi, Francesco Simula, Piero Vicini, Alessandro Lonardo"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03191v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 具有参数化计算、存储和通信IP模块的模块化类脑架构
tldr: AIGOR 是一种模块化、事件驱动的类脑架构，旨在解决脉冲神经网络（SNN）硬件平台因固定模型和策略导致的碎片化问题。它采用参数化 IP 库构建，支持神经元模型、数值精度及硬件映射的灵活配置。通过时间步同步的处理核心和分组交换通信，AIGOR 在 FPGA 上实现了从前馈分类器到递归平衡网络的多种负载，并保持了极高的精度与可扩展性，为 SNN 推理提供了通用的硬件解决方案。
selection_source: fresh_fetch
motivation: 现有的 SNN 硬件通常受限于固定的神经元模型或特定的执行策略，缺乏适配多样化工作负载的灵活性。
method: 提出一种基于参数化 IP 模块的模块化架构，允许在部署时动态配置神经元模型、计算精度和硬件映射方案。
result: 在 AMD Versal FPGA 上的实验证明，该架构在图像分类和递归网络任务中均达到了参考模型的精度，并验证了千核规模的扩展性。
conclusion: AIGOR 提供了一个高度可配置且可扩展的 SNN 推理框架，其模块化设计能够有效定位并优化硬件性能瓶颈。
---

## 摘要
脉冲神经网络 (SNN) 目前运行在碎片化的硬件平台上：专用类脑处理器、特定应用的 FPGA 加速器以及大规模神经科学模拟器，这些平台通常围绕固定的神经元模型、执行策略或工作负载类别构建。我们提出了 AIGOR，这是一种用于脉冲神经网络推理的模块化、事件驱动类脑架构。AIGOR 将神经元组织成时间步同步的处理核心，这些核心通过分组交换通信层以数据包的形式交换脉冲；它由参数化的计算、存储和通信 IP 模块库组装而成，而非针对单一网络的针对性设计。神经元模型、数值精度、神经元到硬件的折叠（folding）以及跨核心的分区均按实例进行配置，而非在设计时固定；随后，通过单一的声明式规范生成实现给定网络的核心、神经元内核和突触存储镜像。我们在 AMD Versal VPK180 上验证了首个原型，涵盖了映射到相同核心的两种截然不同的工作负载：一个在 snnTorch 中训练的前馈图像分类器，以及一个在 NEST 中建模的循环平衡随机网络。分类器重现了其 snnTorch 参考精度，而循环网络在跨越两个 FPGA 的多个核心上实现了与 NEST 参考一致的脉冲级精度。我们报告了实现后的资源利用率，并在仿真中验证了在三维环面（torus）上扩展至一千个核心的多节点同步方案。原型的测量极限确定了突触传递数据通路和全局时间步屏障中的吞吐量瓶颈，并促使了一系列数据通路的改进（目前正在开发中），该架构的可配置结构允许在相同核心上进行这些更改。

## Abstract
Spiking neural networks (SNNs) run today on a fragmented landscape of hardware: dedicated neuromorphic processors, application-specific FPGA accelerators, and large-scale neuroscience simulators, each typically built around a fixed neuron model, execution strategy, or workload class. We present AIGOR, a modular, event-driven neuromorphic architecture for spiking neural network inference. AIGOR organizes neurons into timestep-synchronized processing cores that exchange spikes as packets over a packet-switched communication layer, and it is assembled from a library of parameterized compute, memory, and communication IP blocks rather than as a one-off design for a single network. The neuron model, numeric precision, the folding of neurons onto hardware, and the partitioning across cores are configured per instance rather than committed at design time; a single declarative specification then generates the cores, neuron kernels, and synaptic-memory images that realize a given network. We validate a first prototype on the AMD Versal VPK180 across two deliberately different workloads mapped onto the same cores: a feedforward image classifier trained in snnTorch and a recurrent bal anced random network modeled in NEST. The classifier reproduces its snnTorch reference accuracy, and the recurrent network matches its NEST reference at spike-level precision across multiple cores spanning two FPGAs. We report post-implementation resource utilization and validate the multi-node synchronization scheme in simulation up to one thousand cores on a three-dimensional torus. The prototype's measured limits localize the throughput bottleneck in the synaptic-delivery datapath and the global timestep barrier, and motivate a set of datapath refinements, now in development, that the configurable structure of the architecture admits as changes to the same cores.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AIGOR** 的模块化、事件驱动类脑（Neuromorphic）架构，旨在为脉冲神经网络（SNN）推理提供高度可配置的硬件解决方案。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **硬件碎片化：** 现有的 SNN 硬件平台（如专用 ASIC、FPGA 加速器、神经科学模拟器）通常在设计阶段就固定了神经元模型、数值精度和执行策略，导致难以适配多样化的工作负载。
*   **应用领域差异：** 机器学习社区关注高能效的前馈 SNN 推理，而计算神经科学社区则需要大规模递归网络的精确模拟。
*   **研究目标：** 开发一种通用的、基于 FPGA 的模块化架构，能够通过单一的声明式规范（YAML）生成针对特定网络优化的硬件实例，弥合机器学习与神经科学应用之间的鸿沟。

### 2. 方法论
*   **核心思想：** AIGOR 采用参数化 IP 模块库（计算、存储、通信）构建。它将神经元组织成时间步同步的处理核心，通过分组交换网络（Apeiron 框架）交换脉冲数据包。
*   **关键技术细节：**
    *   **分层配置：** 用户通过 YAML 文件定义神经元模型（LIF/IAF 等）、数值精度（浮点/定点）、空间与时间折叠比例（并行度）以及核心间的分区。
    *   **事件驱动通信：** 仅在脉冲发生时触发突触存储读取和累加，降低功耗。
    *   **循环延迟缓冲（Circular Delay Buffers）：** 在每个工作单元（Worker）中实现，用于处理具有不同突触延迟的输入。
    *   **自动化流程：** 工具链自动将 NEST（递归网络）或 snnTorch（前馈网络）模型转换为硬件 RTL、神经元内核和初始化的突触存储镜像。
    *   **同步机制：** 采用全局时间步屏障（Barrier）确保多核心、多 FPGA 之间的脉冲顺序一致性。

### 3. 实验设计
*   **数据集与场景：**
    1.  **前馈分类器：** 使用 MNIST 数据集，采用 256→128→10 的全连接 LIF 网络，在 snnTorch 中训练。
    2.  **递归平衡随机网络：** 经典的 Brunel 风格网络（约 2048 个神经元，含兴奋性和抑制性），在 NEST 中建模。
*   **Benchmark（基准）：**
    *   分类器以 **snnTorch** 的软件推理精度为基准。
    *   递归网络以 **NEST** 模拟器的脉冲级（Spike-level）定时精度为基准。
*   **对比方案：** 主要对比了硬件实现与软件参考模型在功能上的一致性，并验证了从单芯片到双芯片 FPGA 的扩展性。

### 4. 资源与算力
*   **硬件平台：** 使用了 **AMD Versal Premium VPK180** FPGA 开发板（单板或双板级联）。
*   **算力细节：**
    *   核心逻辑由 Catapult HLS 和 Vitis HLS 生成。
    *   **资源占用：** 一个包含 2048 个神经元的核心约占用 VPK180 22.7% 的 LUT 和 1.94% 的 BRAM（突触存储主要使用 URAM）。
    *   **吞吐量：** 在 MNIST 分类任务中，单核实例达到 568 samples/s。
    *   **仿真规模：** 在 SystemC 仿真中验证了多达 1000 个核心在 3D Torus 拓扑下的同步。

### 5. 实验数量与充分性
*   **实验覆盖：** 论文针对两种截然不同的 SNN 范式（前馈 vs 递归）进行了硬件部署验证。
*   **充分性评价：** 实验设计较为充分，不仅涵盖了功能验证（精度匹配），还包括了详细的资源利用率分析和扩展性仿真。通过跨越两个 FPGA 运行递归网络，证明了其分布式处理能力。实验过程客观，明确指出了定点化带来的精度漂移时间点。

### 6. 主要结论与发现
*   **通用性验证：** AIGOR 能够成功运行来自不同生态系统（ML 和神经科学）的模型，并保持高精度。
*   **精度表现：** 在 32 位定点数配置下，硬件在模拟前 2ms 内能与 NEST 实现完全一致的脉冲定时。
*   **性能瓶颈：** 识别出当前的性能瓶颈在于突触传递的数据通路（密集连接下的存储读取）以及全局时间步同步的开销。
*   **可扩展性：** 证明了模块化设计允许在不改变核心设计的情况下，通过调整配置来优化面积或延迟。

### 7. 优点
*   **高度灵活性：** 极少数能同时支持 snnTorch 和 NEST 模型的硬件架构，且支持运行时/编译前的深度参数化。
*   **模块化设计：** 计算与通信解耦，方便未来集成更复杂的神经元模型或改进通信协议。
*   **工具链完整：** 提供了从高层描述到硬件比特流的自动化生成路径。

### 8. 不足与局限
*   **吞吐量受限：** 在处理高密度连接网络时，突触存储的串行读取成为瓶颈。
*   **同步开销：** 全局屏障同步机制在核心数量极大时可能导致扩展性下降（论文已提出 GALS 改进方案作为未来工作）。
*   **能效对比缺失：** 论文主要关注功能和灵活性，未提供与专用类脑芯片（如 Loihi 或 TrueNorth）在能效比（pJ/SOP）上的直接对比数据。

（完）
