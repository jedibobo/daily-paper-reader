---
title: "ArchEval: Measuring AI Agents as Computer Architects"
title_zh: ArchEval：衡量作为计算机架构师的 AI 智能体
authors: "Chenyu Wang, Zishen Wan, Jeffrey Ma, Shvetank Prakash, Zhenting Qi, Haebin Do, Andy Cheng, Arya Tschand, Jiahe Shi, Yilun Du, Vijay Janapa Reddi"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03601v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于计算机架构和加速器设计的AI智能体基准测试
tldr: 本研究针对大语言模型（LLM）智能体在计算机体系结构设计中的评估难题，提出了ArchEval基准测试平台。该平台包含20项涵盖CPU、内存和加速器等领域的挑战，并结合8种模拟器，设置了从全反馈到无反馈的三种评估等级。研究发现，当前智能体在有反馈支持时表现优异，但在自主构建工作流和性能预测方面仍存在显著短板，为未来智能体在架构设计领域的发展指明了方向。
selection_source: fresh_fetch
motivation: 传统的基准测试难以全面衡量LLM智能体在处理复杂计算机体系结构设计任务（如负载分析、模拟器使用和性能预测）时的综合能力。
method: 提出了ArchEval基准平台，通过20个跨领域的架构挑战和三种不同反馈强度的评估等级（L1-L3），全面测试智能体的设计与优化能力。
result: 实验显示智能体在L1模式下表现出色，但在缺乏反馈的L3模式下，仅GPT-5.5+Codex能维持基准线以上的性能，且普遍存在性能建模准确率低的问题。
conclusion: 当前LLM智能体更适合作为架构优化的辅助工具而非完全自主的架构师，未来需重点提升其模拟器工具调用、校准预测及自主发现机制的能力。
---

## 摘要
计算机架构长期以来一直使用基准测试来使进展可衡量。大语言模型（LLM）智能体带来了一个不同的衡量问题：成功不仅仅是编写代码或调整参数。智能体必须解释工作负载、选择机制、使用模拟器、预测性能、满足硬约束，并决定哪些可行设计值得评估。本文介绍了 ArchEval，这是一个用于评估 LLM 智能体在计算机架构设计和优化方面表现的基准测试和平台。它包含 20 个挑战，涵盖 CPU 核心机制、系统架构、内存系统、加速器和存内计算，并由八个模拟器提供支持。每个挑战都在三种设置下提出：L1 全套件（full harness），具有重复的模拟器反馈；L2 模拟器代码容器，提供模拟器源码但智能体必须自行组建工作流；以及 L3 仅智能体，在提交前没有可运行的反馈。每次运行都会报告基准归一化的验证器性能，并记录完整轨迹，将结果与工作负载分析、模拟器工具使用、预测、约束处理和工件完整性联系起来。初步结果显示当前智能体存在明显的界限。在 L1 支持下，所有四个评估的智能体都达到或超过了基准，并在多种模拟器上改进了实际设计。移除支持后暴露了弱点：许多智能体无法将模拟器源码转化为有用的实验，且 L3 预测往往与验证器结果不一致。在 L3 中，只有 GPT-5.5 + Codex 保持在基准之上，达到 1.21 倍的几何平均性能和 65% 的胜率；其他三个则低于基准。即使是 GPT-5.5 + Codex，其性能建模通过率也仅为 15%。ArchEval 将当今的智能体定位为有用的优化助手而非自主架构师，并确定了下一步所需的能力：模拟器工具使用、校准预测、反馈前判断以及有用机制的发现。

## Abstract
Computer architecture has long used benchmarks to make progress measurable. LLM agents create a different measurement problem: success is not merely writing code or tuning parameters. The agent must interpret workloads, choose mechanisms, use simulators, predict performance, satisfy hard constraints, and decide which feasible design is worth evaluating. This paper introduces ArchEval, a benchmark and platform for evaluating LLM agents on computer architecture design and optimization. It contains 20 challenges across CPU core mechanisms, system architecture, memory systems, accelerators, and compute-in-memory, backed by eight simulators. Each challenge is posed under three settings: L1 full harness, with repeated simulator feedback; L2 simulator-code container, where simulator source is available but the agent must assemble its own workflow; and L3 agent-only, with no runnable feedback before submission. Each run reports baseline-normalized verifier performance and records the full trajectory, connecting results to workload analysis, simulator-tool use, prediction, constraint handling, and artifact integrity. Initial results show a sharp boundary in current agents. With L1 support, all four evaluated agents reach or exceed baseline and improve real designs across diverse simulators. Removing support exposes weaknesses: many agents fail to turn simulator source into useful experiments, and L3 predictions often disagree with verifier results. In L3, only GPT-5.5 + Codex remains above baseline, reaching 1.21x geomean performance and a 65% win rate; the other three fall below baseline. Even GPT-5.5 + Codex has only a 15% performance-modeling pass rate. ArchEval frames today's agents as useful optimization assistants rather than autonomous architects, and identifies capabilities needed next: simulator-tool use, calibrated prediction, pre-feedback judgment, and useful mechanism discovery.

---

## 论文详细总结（自动生成）

### ArchEval：衡量作为计算机架构师的 AI 智能体

#### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的计算机架构基准测试（如 SPEC, MLPerf）旨在衡量硬件设计的性能，而非衡量“架构师”本身的设计决策过程。随着 LLM 智能体开始参与架构设计（如编写代码、调优参数），行业缺乏一个系统性的基准来评估智能体是否具备架构师的核心能力：分析负载、建立性能模型、使用复杂模拟器工具以及在硬约束下进行权衡。
*   **核心问题**：AI 智能体能否真正胜任计算机架构师的工作？在缺乏人类预设的优化框架和实时反馈时，它们的自主设计能力究竟如何？

#### 2. 论文提出的方法论
*   **核心思想**：提出 **ArchEval** 平台，将架构设计任务抽象为“预测（Prediction）”、“优化（Optimization）”和“生成（Generation）”三种角色，并通过不同等级的实验支持来测试智能体的自主性。
*   **关键技术细节**：
    *   **统一接口（Connector）**：将 8 种异构模拟器（如 ChampSim, gem5, Timeloop 等）封装在统一接口下，使智能体能以相同方式提交设计、获取反馈。
    *   **三级评估设置（L1-L3）**：
        *   **L1 (Full Harness)**：提供完整的优化框架和重复的模拟器反馈，测试智能体作为“优化助手”的能力。
        *   **L2 (Sim-code Container)**：提供模拟器源码但无预设框架，要求智能体自行构建实验流。
        *   **L3 (Agent-Only)**：仅提供静态负载证据和约束，无模拟器反馈，要求智能体在提交前自行预测性能，测试其“设计判断力”。
    *   **轨迹分析（Trajectory Analysis）**：不仅看最终性能，还记录并审计智能体的思考过程、工具调用和约束遵守情况。

#### 3. 实验设计
*   **数据集与场景**：包含 20 个挑战任务，涵盖 5 大领域：
    *   **CPU 核心**：分支预测、缓存替换、预取算法。
    *   **系统架构**：缓存层次配置、分布式训练集体通信优化。
    *   **内存系统**：DRAM 控制器调优、RowHammer 防御。
    *   **加速器**：脉动阵列映射、数据流优化。
    *   **存内计算 (CIM)**：软硬件协同设计。
*   **对比方法**：评估了四种智能体配置：GPT-5.5 + Codex、Gemini 3.5 Flash、Gemini 3.1 Flash-Lite、Gemma 4 31B。
*   **Benchmark 基准**：每个挑战都设有一个可信的 Baseline（如 LRU 算法或已发表的论文设计），性能以基准归一化值表示。

#### 4. 资源与算力
*   **算力说明**：论文未详细列出训练这些 LLM 所需的 GPU 资源（因为是评估现成模型），但详细记录了评估过程中的消耗。
*   **运行统计**：记录了每个智能体在不同等级下的平均 Token 消耗、交互轮数和运行时间（如 L1 模式下平均运行约 15-18 分钟，L3 模式下约 1-7 分钟）。

#### 5. 实验数量与充分性
*   **实验规模**：20 个挑战 × 3 种设置 × 4 个智能体，共计数百组实验轨迹。
*   **充分性与公平性**：
    *   **充分性**：覆盖了从微架构到分布式系统的广泛领域，实验设计较为全面。
    *   **客观性**：通过隔离的验证器容器确保评估环境一致，防止智能体篡改评估脚本。
    *   **局限性**：目前主要基于单次随机种子（Single-seed）的初步研究，样本量在统计严谨性上仍有提升空间。

#### 6. 论文的主要结论与发现
*   **能力边界清晰**：在 L1（有反馈）模式下，所有智能体都能达到或超过基准线，表现出优秀的优化能力。
*   **自主性缺失**：在 L3（无反馈）模式下，性能大幅下滑。除 GPT-5.5 外，其他智能体均跌破基准线。
*   **性能建模极弱**：智能体普遍无法准确预测其设计的性能，L3 模式下的性能建模通过率极低（最高仅 15%），且倾向于过度自信（高估自己的设计）。
*   **工具使用障碍**：许多智能体在 L2 模式下无法将模拟器源码转化为有效的本地实验，更多是将其视为静态参考文本。

#### 7. 优点
*   **多维度评估**：不只看最终分数，通过 L1-L3 的设置区分了“盲目搜索”与“理性设计”。
*   **真实环境集成**：集成了 8 种真实的工业/学术级模拟器，而非简化的玩具模型。
*   **轨迹审计**：引入了详细的评估准则（Rubric），能够识别出智能体是否通过“走捷径”或“硬编码”获胜。

#### 8. 不足与局限
*   **模型覆盖有限**：主要测试了闭源领先模型，对开源社区的小模型覆盖不足。
*   **创新性评估难**：论文发现智能体大多在组合现有机制，很难客观衡量其是否提出了真正“原创”的架构思想。
*   **静态负载局限**：L3 模式提供的静态证据可能不足以让智能体进行深度推理，与现实中架构师查阅详细 Trace 的过程仍有差距。

（完）
