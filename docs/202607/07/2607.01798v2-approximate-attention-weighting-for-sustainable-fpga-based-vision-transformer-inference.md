---
title: Approximate Attention Weighting for Sustainable FPGA-Based Vision Transformer Inference
title_zh: 面向可持续基于 FPGA 的视觉 Transformer 推理的近似注意力加权
authors: "Muhammad Usman, Muhammad Akmal Shafique, Shujaat Khan, Dorit Merhof"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01798v2"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于FPGA的视觉Transformer推理硬件感知设计
tldr: 针对视觉Transformer（ViT）在资源受限FPGA上部署时Softmax计算开销大的问题，本文提出一种无需BRAM的近似注意力权重单元。该设计利用分布式查找表实现16段分段线性函数来近似自然指数，在保留预训练模型参数的同时显著降低了硬件资源消耗。在Zynq-7020平台上的实验表明，该方法在几乎不损失精度的情况下实现了高效的ViT推理。
selection_source: fresh_fetch
motivation: 传统的Softmax硬件实现依赖CORDIC或BRAM查找表，导致在小型FPGA上部署ViT时面积和功耗过高。
method: 提出一种基于分布式查找表实现的16段分段线性函数，用于近似Softmax中的自然指数运算，且无需模型重校准。
result: "在Zynq-7020上实现的注意力核心仅消耗1444个LUT和77个DSP，且Top-1精度损失控制在0.20%以内。"
conclusion: 该方案为资源受限的边缘AI平台提供了一种低功耗、高效率的ViT推理加速新思路。
---

## 摘要
视觉 Transformer (ViT) 通过利用自注意力机制捕获图像区域间的全局上下文，重塑了计算机视觉领域。这使得它们在可再生能源基础设施、工业质量控制、医学成像和自主系统传感等应用的边缘视觉检测与监控中极具吸引力。然而，在小型 FPGA 上部署 ViT 仍然具有挑战性，因为自注意力中的 softmax 阶段需要指数计算和归一化，这在硬件实现上成本很高。现有的实现通常依赖于 CORDIC 流水线或基于 BRAM 的查找表，这增加了面积和功耗。本文提出了一种用于基于 FPGA 的 ViT 推理的无 BRAM 近似注意力加权单元。所设计的方案使用完全由分布式 LUT 结构实现的 16 段分段线性函数来近似 softmax 中的自然指数。与以 2 为底的近似方法不同，自然指数公式保留了预训练的注意力温度，并避免了针对特定模型的重新校准。在 Xilinx Zynq-7020 上的实现结果显示，完整的注意力行核心使用了 1444 个 LUT、77 个 DSP 且不占用 BRAM；同时，硬件精确仿真表明，在 ViT 系列模型上，其 Top-1 准确率与精确 softmax 基准相比，绝对差异在 0.20% 以内。这些结果证明了所提核心在资源受限的边缘人工智能平台上进行高效能 ViT 推理的潜力。

## Abstract
Vision Transformers have reshaped computer vision by using self-attention to capture global context across image regions. This makes them attractive for edge visual inspection and monitoring in applications such as renewable-energy infrastructure, industrial quality control, medical imaging, and autonomous-system sensing. However, deploying ViTs on small FPGAs remains challenging because the softmax stage in self-attention requires exponential evaluation and normalization, which are costly in hardware. Existing implementations often rely on CORDIC pipelines or BRAM-based look-up tables, increasing area and power consumption. This paper presents a BRAM-free approximate attention-weighting unit for FPGA-based ViT inference. The proposed design approximates the natural exponential in softmax using a 16-segment piecewise-linear function implemented entirely with distributed LUT fabric. Unlike base-2 approximations, the natural-exponential formulation preserves the pre-trained attention temperature and avoids model-specific recalibration. Implemented on a Xilinx Zynq-7020, the complete attention-row core uses 1444 LUTs, 77 DSPs, and no BRAM, while hardware-accurate emulation shows accuracy within a \(0.20\%\) absolute top-1 difference from the exact-softmax reference on ViT-family models. These results demonstrate the potential of the proposed core for energy-efficient ViT inference on resource-constrained edge-AI platforms.

---

## 论文详细总结（自动生成）

以下是对论文《Approximate Attention Weighting for Sustainable FPGA-Based Vision Transformer Inference》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：视觉 Transformer (ViT) 在边缘计算（如光伏面板检测、工业质检）中表现卓越，但其核心的自注意力机制（Self-Attention）包含复杂的 Softmax 运算。
*   **核心问题**：在资源受限的边缘 FPGA 上，Softmax 中的指数运算（$e^x$）和归一化操作成本极高。传统方法依赖 CORDIC 算法或 BRAM 查找表，会消耗大量面积和功耗；而常见的以 2 为底（Base-2）的近似方法会改变注意力温度，导致模型需要重新校准或微调。
*   **研究目标**：开发一种**无 BRAM（BRAM-free）**、**无需校准**且**高能效**的近似注意力加权单元，以支持可持续的边缘 AI 推理。

### 2. 方法论
*   **核心思想**：直接利用 FPGA 的分布式查找表（LUTRAM）实现一个 **16 段均匀分段线性（PWL）函数**来近似自然指数 $e^x$，从而保留预训练模型的注意力语义。
*   **关键技术细节**：
    *   **数值稳定范围**：利用 Softmax 的最大值减法特性，将输入 $u_j$ 限制在 $[ -8, 0 ]$ 区间。在此区间内，$e^x$ 的贡献显著，超出此范围则饱和为 0。
    *   **PWL 近似**：将 $[-8, 0]$ 划分为 16 个均匀段，每段长度为 0.5。硬件仅需存储 17 个端点值（共 272 bits），完全由分布式 LUT 实现。
    *   **硬件架构**：设计了一个五阶段流水线核心：
        1.  **Score MAC**：计算查询（Query）与键（Key）的点积得分。
        2.  **Natural PWL Weight**：执行分段线性插值计算近似权重。
        3.  **Denominator Accumulator**：累加权重和（分母）。
        4.  **Numerator Accumulator**：计算权重与值（Value）的乘积累加（分子）。
        5.  **Restoring Divider**：执行最终的归一化除法。
*   **算法流程**：通过两遍扫描（Two-pass）调度实现：第一遍计算得分并寻找行最大值；第二遍计算近似指数权重并完成加权求和。

### 3. 实验设计
*   **数据集**：使用 **Imagenette**（ImageNet 的 10 类精简子集，包含 3550 张验证图像）。
*   **模型对象**：ViT-S/16, ViT-B/16, ViT-L/16 以及 DeiT-S/16。
*   **Benchmark（基准）**：
    *   FP32 精确 Softmax（软件参考）。
    *   INT8/INT16 精确 Softmax。
*   **对比方法**：与现有的 FPGA Softmax 加速器（如 Li et al. 的 Base-2 方法、Hirayae et al. 的位移近似方法、ViTA 的 CORDIC 方法等）进行资源和精度对比。

### 4. 资源与算力
*   **硬件平台**：Xilinx **Zynq-7020** (xc7z020clg484-1) SoC FPGA。
*   **资源消耗**：
    *   **逻辑资源**：1444 LUTs (2.71%), 1899 FFs (1.78%)。
    *   **计算资源**：77 DSPs (35.0%)。
    *   **存储资源**：**0 BRAM**（完全不占用块 RAM）。
*   **功耗与频率**：运行频率 100 MHz；动态功耗 **21 mW**，总芯片功耗 124 mW。
*   **训练时长**：未提及训练时长，因为该方法旨在直接使用预训练权重，**无需针对特定模型进行微调或重训练**。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种不同规模的模型进行了精度验证，并提供了详细的模块级资源和功耗拆解。
*   **充分性**：
    *   通过硬件精确仿真（Hardware-accurate emulation）确保了软件评估与硬件行为的一致性。
    *   使用了 SAIF（切换活动交换格式）文件驱动功耗分析，结果具有较高的可信度。
*   **客观性**：实验对比了 INT8 和 INT16 两种量化设置，并与 SOTA 论文在相同精度协议下进行了横向对比，评估较为客观。

### 6. 主要结论与发现
*   **精度保持**：在 INT16 匹配设置下，Top-1 准确率与精确 Softmax 相比，最大差异仅为 **0.20%**，证明了自然指数近似的有效性。
*   **极高能效**：动态能效达到 **601.3 krows/s/W**，处理一个 197 标记（Token）的注意力行仅需 1.66 μJ 动态能量。
*   **可持续性潜力**：在 500 个节点的部署场景中，相比 10W 的嵌入式 GPU 参考方案，该 FPGA 核心每年理论上可节省约 **43.26 MWh** 的电能。

### 7. 优点
*   **资源极简**：通过分布式 LUT 替代 BRAM，为小型 FPGA 腾出了宝贵的存储资源用于缓存特征图。
*   **即插即用**：由于直接近似 $e^x$ 而非 $2^x$，保留了原始模型的“注意力温度”，无需复杂的后训练校准。
*   **数值稳定**：结合了 Max-centering 技术，确保了在有限位宽下的计算稳定性。

### 8. 不足与局限
*   **实验覆盖面**：精度验证仅限于 Imagenette 数据集，尚未在完整的 ImageNet-1K 或更复杂的检测/分割任务上验证其鲁棒性。
*   **系统完整性**：目前仅实现了注意力行的核心算术单元，未包含完整的 ViT 管道（如 LayerNorm、MLP 或外部存储 DMA 接口）。
*   **应用限制**：PWL 近似区间固定在 $[-8, 0]$，虽然适用于大多数 ViT，但对于某些具有极端得分分布的特殊模型，可能需要调整段数或区间。

（完）
