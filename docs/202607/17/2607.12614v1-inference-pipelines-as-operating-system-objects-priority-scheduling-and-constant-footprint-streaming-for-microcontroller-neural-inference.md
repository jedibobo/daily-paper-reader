---
title: "Inference Pipelines as Operating-System Objects: Priority Scheduling and Constant-Footprint Streaming for Microcontroller Neural Inference"
title_zh: 推理流水线作为操作系统对象：微控制器神经推理的优先级调度与恒定占用流式处理
authors: Dimitrios Kafetzis
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12614v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 微控制器神经推理与优先级调度
tldr: 针对微控制器推理流水线常被视为应用代码导致重复实现的问题，本文提出将推理流水线作为操作系统（SynapticOS）的一等公民。通过引入优先级调度器、无堆内存管理和恒定内存占用的流式处理机制，实现了高效、确定性的推理执行。在NXP开发板上的评估显示，该引擎在保持极低调度开销的同时，显著提升了DSP任务性能，并确保了内存使用的稳定性。
selection_source: fresh_fetch
motivation: 现有的微控制器运行时将推理流水线视为应用逻辑，导致开发者需重复实现阶段排序、缓冲区管理和信号处理等底层逻辑。
method: 将流水线作为OS对象，采用优先级作业调度、静态验证、无堆推理路径以及基于临时区域的恒定内存流式处理机制。
result: "在NXP FRDM-MCXN947上，调度开销仅为92微秒，人脸检测流水线达到215.8 FPS，且内存峰值恒定在2,784字节。"
conclusion: 将推理流水线集成到操作系统内核中，能有效降低微控制器AI应用的开发复杂度，并显著提高执行效率与内存确定性。
---

## 摘要
微控制器运行时将推理流水线（预处理、加速器调用、后处理）视为应用程序代码：每个项目都围绕库调用重新实现阶段排序、缓冲区大小调整和完成信号。我们认为这些属于操作系统层面的关注点，并展示了 SynapticOS 的第二阶段推理引擎。SynapticOS 是一个基于 Zephyr 的开源运行时，它将流水线作为操作系统的一等对象。流水线从静态池中提取，根据规范的阶段顺序进行验证，并由优先级作业调度器（实时 > 普通 > 尽力而为，每类按先进先出）执行，支持取消操作和有界作业表；推理路径上不使用堆内存。针对九个内置处理器，阶段缓冲区的大小根据配置和张量几何形状精确确定（用户阶段具有 4 倍的有界回退）；所有中间结果都存储在每帧重置的临时区域（ephemeral arena）中，因此流式处理的内存占用是恒定的。我们在 NXP FRDM-MCXN947 (Cortex-M33, 150 MHz) 和 qemu_cortex_m3 CI 目标上进行了评估，两者均运行确定性的存根 NPU 内核：旨在测量引擎开销基准，而非芯片吞吐量。在开发板上，调度器相比第一阶段的直接硬件抽象层（HAL）封装增加了 92 微秒（1,130 对比 1,038 微秒；分发耗时 1 微秒）；一个包含 30 帧、六阶段的人脸检测流水线平均每帧耗时 4.63 毫秒（215.8 FPS，包含存根模型），而在 QEMU 软浮点环境下为 31.1 毫秒，且每帧的恒定内存区域峰值为 2,784 字节，并在每帧结束时归零。PowerQuad DSP 针对 FFT 和 Q15 矩阵乘法进行了路由和自校准；端到端加速比分别为 5.51 倍（256 点 FFT）和 1.66 倍（16x16 矩阵乘法），未达到计划的 10 倍目标——报告为未达标，而非重新调整范围。阶段边界分析现在可以在开发板上实时运行，弥补了第一阶段的空白。该引擎在 QEMU 上增加了 3.8 KB 的闪存占用，在 FRDM 上增加了 20.7 KB。跨 13 个 ZTEST 测试套件的 99 个测试在仿真环境下 100% 通过。该项目以 Apache 2.0 协议发布于 https://github.com/Dimitrios-Kafetzis/SynapticOS。

## Abstract
Microcontroller runtimes treat the inference pipeline -- pre-processing, accelerator invocation, post-processing -- as application code: every project re-implements stage sequencing, buffer sizing, and completion signalling around a library call. We argue these are operating-system concerns and present the Phase 2 inference engine of SynapticOS, an open-source Zephyr-based runtime that makes the pipeline a first-class OS object. A pipeline is drawn from a static pool, validated against a canonical stage order, and executed by a priority job scheduler (realtime > normal > best-effort, FIFO per class) with cancellation and a bounded job table; no heap on the inference path. Stage buffers are sized exactly from configuration and tensor geometry for the nine built-in processors (bounded 4x fallback for user stages); all intermediates live in an ephemeral arena reset per frame, so streaming footprint is constant. We evaluate on the NXP FRDM-MCXN947 (Cortex-M33, 150 MHz) and the qemu_cortex_m3 CI target, both running a deterministic stub NPU kernel: engine-overhead baselines, not silicon throughput. On the board the scheduler adds 92 us over the Phase 1 direct-HAL bracket (1,130 vs 1,038 us; dispatch 1 us); a 30-frame, six-stage face-detection pipeline averages 4.63 ms/frame (215.8 FPS, stub model included) vs 31.1 ms under QEMU soft-float, at a constant 2,784-byte arena peak returning to zero each frame. The PowerQuad DSP is routed and self-calibrated for FFT and Q15 matmul; end-to-end speedups are 5.51x (256-point FFT) and 1.66x (16x16 matmul), short of the plan's 10x target -- reported as missed, not re-scoped. Stage-boundary profiling now runs live on the board, closing a Phase 1 gap. The engine adds 3.8 KB flash on QEMU and 20.7 KB on FRDM. 99 tests across 13 ZTEST suites pass 100% under emulation. Released under Apache 2.0 at https://github.com/Dimitrios-Kafetzis/SynapticOS

---

## 论文详细总结（自动生成）

这是一份关于论文《Inference Pipelines as Operating-System Objects: Priority Scheduling and Constant-Footprint Streaming for Microcontroller Neural Inference》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在当前的微控制器（MCU）开发中，神经推理流水线（预处理、模型调用、后处理）通常被视为“应用程序代码”。开发者必须手动管理阶段排序、中间缓冲区大小以及并发信号。这种做法导致了代码重复、内存管理风险（如缓冲区溢出）以及难以调试的并发错误。
*   **核心理念**：论文提出将推理流水线提升为**操作系统（OS）的一等对象**（类似于文件或套件）。通过在 SynapticOS（基于 Zephyr RTOS）中实现这一抽象，OS 可以自动验证流水线结构、精确规划内存并进行优先级调度，从而降低开发复杂度并提高系统可靠性。

### 2. 论文提出的方法论
*   **流水线作为 OS 对象**：
    *   **静态池管理**：流水线对象从静态池中分配，完全避免堆内存（Heap）分配。
    *   **规范排序验证**：强制执行 `预处理* -> 模型 -> 后处理*` 的顺序，在构建时拦截错误。
*   **尺寸感知的缓冲区规划**：
    *   **精确计算**：针对 9 种内置处理器（如缩放、归一化、量化等），引擎根据输入几何形状和配置计算精确的输出大小。
    *   **有界回退**：对于自定义阶段，采用 4 倍输入大小的保守边界，确保内存安全。
*   **优先级作业调度器**：
    *   **三级优先级**：REALTIME（实时）、NORMAL（普通）、BEST-EFFORT（尽力而为），同级内采用 FIFO。
    *   **非抢占式执行**：当前阶段采用非抢占式调度，以降低上下文切换开销，但预留了未来实现层级抢占的接口。
*   **恒定占用流式处理**：
    *   **临时区域（Ephemeral Arena）**：所有中间张量存放在临时内存区，每帧结束后重置。这种“锯齿状”内存模式确保了长时间运行下的零碎片化。

### 3. 实验设计
*   **硬件平台**：
    *   **NXP FRDM-MCXN947**：搭载 Cortex-M33 (150 MHz) 和 PowerQuad DSP 加速器。
    *   **QEMU (Cortex-M3)**：用于持续集成（CI）和确定性仿真测试。
*   **Benchmark 与对比**：
    *   **基准对比**：对比 SynapticOS Phase 1 的直接硬件抽象层（HAL）调用开销。
    *   **场景测试**：一个包含 30 帧的人脸检测流水线（缩放、归一化、量化、模型、解码、非极大值抑制）。
    *   **DSP 性能**：对比软件内核与 PowerQuad 硬件加速的 FFT 和矩阵乘法性能。

### 4. 资源与算力
*   **算力设备**：实验主要在嵌入式 MCU 上完成，不涉及大规模 GPU 训练。
*   **内存与存储占用**：
    *   **Flash 占用**：引擎在 QEMU 上增加 3.8 KB，在 FRDM 开发板上增加 20.7 KB（含 Shell 和驱动）。
    *   **RAM 占用**：QEMU 增加 2.6 KB，FRDM 增加 16.5 KB（主要为 PowerQuad 的静态工作缓冲区）。
*   **训练时长**：文中未提及模型训练，重点在于推理引擎的运行时开销。

### 5. 实验数量与充分性
*   **测试套件**：包含 13 个 ZTEST 套件，共 **99 个测试用例**，在仿真环境下达到 100% 通过率。
*   **流式实验**：通过 30 帧连续的人脸检测任务验证了内存的稳定性。
*   **客观性**：作者采用了“诚实基准”方法，明确区分了引擎开销与模型延迟，并使用了确定性的存根（Stub）内核来隔离变量。实验覆盖了并发调度、内存重置、DSP 加速等多个维度，设计较为充分。

### 6. 论文的主要结论与发现
*   **极低的调度开销**：在硬件上，整个调度路径的额外开销仅为 **92 μs**，其中分发逻辑仅占 **1 μs**。
*   **高效的流式处理**：人脸检测流水线在开发板上达到 **215.8 FPS**（含存根模型），内存峰值恒定在 **2,784 字节**，且每帧后均能归零。
*   **DSP 加速效果**：PowerQuad 硬件加速相比软件内核，在 256 点 FFT 上实现了 **5.51 倍**加速，在 16x16 矩阵乘法上实现了 **1.66 倍**加速。
*   **并发安全性**：通过协作式线程仿真，成功捕获并修复了一个在抢占式硬件上难以发现的完成信号竞态条件（Race Condition）。

### 7. 优点
*   **架构创新**：将推理逻辑从应用层下沉到 OS 层，提供了更强的确定性和安全性。
*   **零堆内存设计**：完全符合嵌入式系统对内存确定性的严苛要求，消除了碎片化风险。
*   **透明的性能分析**：内置阶段级分析器，使开发者能实时看到每一阶段的耗时和内存占用。
*   **诚实的科学态度**：作者主动报告并分析了未达到预期目标（10 倍 DSP 加速）的实验结果，而非掩盖或重新调整指标。

### 8. 不足与局限
*   **模型阶段局限**：目前模型阶段使用的是存根（Stub）内核，尚未集成真实的 NXP eIQ Neutron NPU 硬件加速，因此 FPS 数据不代表最终硅片吞吐量。
*   **调度功能尚不完全**：虽然预留了截止日期（Deadline）和抢占（Preemption）参数，但在 Phase 2 中尚未实际生效。
*   **DSP 性能未达标**：由于封装开销（如浮点/定点转换、互斥锁），DSP 加速比未达到计划的 10 倍目标。
*   **流水线限制**：目前每个流水线仅支持单个模型阶段，不支持复杂的多模型级联（需在应用层手动组合）。

（完）
