---
title: "Seeing is Free, Speaking is Not: Uncovering the True Energy Bottleneck in Edge VLM Inference"
title_zh: 视觉是免费的，说话则不然：揭示边缘端 VLM 推理中的真实能量瓶颈
authors: "Junfei Zhan, Haoxun Shen, Mingang Guo, Zixuan Huang, Tengjiao He"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09520v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 边缘硬件上VLM推理的能效分析
tldr: 本研究针对边缘设备上视觉语言模型（VLM）的能耗瓶颈进行了首次系统性分析。通过在不同硬件平台上测试多种模型，研究发现推理功耗基本恒定，而能耗差异主要源于推理时间。实验揭示了输出解码阶段的能耗远高于输入预填充阶段，输出长度才是决定能耗的关键因素。这一发现挑战了当前侧重于减少视觉标记的优化方向，证明控制输出长度比视觉剪枝更能显著降低能耗。
selection_source: fresh_fetch
motivation: 现有的VLM能效优化大多集中在减少视觉标记上，但边缘硬件上真实的能耗瓶颈尚未得到系统性研究。
method: 研究者在NVIDIA RTX 3070和Jetson Orin NX平台上，对涵盖三种架构的五种VLM模型进行了跨分辨率、跨复杂度的系统性能耗剖析。
result: "实验表明输出标记的时间成本是输入标记的11至39倍，且控制输出长度最高可节省97%的推理能耗。"
conclusion: 边缘VLM推理的真正能耗瓶颈在于模型生成的文本长度而非视觉处理过程，这为未来的模型优化提供了新方向。
---

## 摘要
视觉语言模型（VLM）是具身智能的感知支柱，但它们在边缘硬件上的能耗足迹仍未得到充分理解。现有的效率优化工作主要集中在减少视觉 token，隐含地将视觉处理视为主要的能量消耗。我们通过对设备端 VLM 推理进行的首次系统性能耗分析，推翻了这一隐含假设。该分析涵盖了三个架构家族的五个模型、四种输入分辨率以及两个硬件平台（NVIDIA RTX 3070 和 Jetson Orin NX）。我们的分析得出了三个发现：首先，平均推理功率是模型固有的常数，不随输入分辨率、图像复杂度和提示词类型而变化，在所有条件下的波动小于 5%。这意味着不同输入之间的所有能量差异必然源于推理时间的变化，而非功率消耗的变化。其次，由于预填充（prefill）和解码（decode）之间计算受限与内存受限的不对称性，每个输出 token 消耗的实际时间比每个输入 token 高出 11 到 39 倍，使得输出 token 数量成为延迟和能量消耗的主要驱动因素。第三，以图像中物体数量衡量的图像复杂度，在相同分辨率下会导致高达 4.1 倍的能量差异。这种差异并非源于视觉处理成本的增加，而是源于输出长度的不同。这些发现揭示了视觉 token 剪枝的一个根本局限性：对于固定 token 模型，即使移除所有视觉 token，最多也只能节省 10% 的总能量。在参数量从 10 亿到 80 亿的模型中，控制输出长度可节省高达 97% 的总能量，且随着模型规模的增大，解码在能量消耗中的主导地位变得更加显著。简而言之，边缘端 VLM 推理的真实能量瓶颈不在于模型“看到”了什么，而在于它“说”了多少。

## Abstract
Vision-Language Models (VLMs) are the perceptual backbone of embodied AI, but their energy footprint on edge hardware remains poorly understood. Existing efficiency efforts focus predominantly on reducing visual tokens, implicitly treating visual processing as the dominant energy cost. We overturn this implicit assumption through the first systematic energy profiling of on-device VLM inference, spanning five models across three architecture families, four input resolutions, and two hardware platforms (NVIDIA RTX 3070 and Jetson Orin NX). Our analysis yields three findings. First, average inference power is a model-intrinsic constant, invariant to input resolution, image complexity, and prompt type, with less than 5% variation across all conditions. This means that all energy variation across inputs must arise from variation in inference time, not from variation in power draw. Second, each output token costs 11 to 39x more wall-clock time than each input token due to the compute-bound and memory-bound asymmetry between prefill and decode, making output token count the dominant driver of both latency and energy. Third, image complexity, measured by the number of objects in an image, induces up to 4.1x energy differences at identical resolution. This variation arises not from increased visual processing cost, but from differences in output length. These findings expose a fundamental limitation of visual token pruning: even removing all visual tokens saves at most 10% of total energy for fixed-token models. Across models spanning 1 billion to 8 billion parameters, controlling output length saves up to 97% of total energy, with the energy dominance of decoding growing stronger at larger model scale. In short, the true energy bottleneck in edge VLM inference is not what the model sees, but how much it says.

---

## 论文详细总结（自动生成）

这是一份关于论文《Seeing is Free, Speaking is Not: Uncovering the True Energy Bottleneck in Edge VLM Inference》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着具身智能（Embodied AI）的发展，视觉语言模型（VLM）被广泛部署在资源受限的边缘设备（如机器人、无人机、AR眼镜）上。
*   **核心矛盾**：现有的效率优化研究（如视觉标记剪枝）大多隐含地假设“视觉处理”是能耗的主要来源。然而，这一假设从未在边缘硬件上得到系统性的实证验证。
*   **整体含义**：本文通过首次系统性的边缘端 VLM 推理能耗剖析，揭示了一个反直觉的结论：**视觉处理（Seeing）的能耗极低，而文本生成（Speaking/Decoding）才是真正的能量瓶颈。**

### 2. 方法论
论文提出了一套分解和预测 VLM 推理能耗的框架：
*   **功率指纹（Power Fingerprint）**：发现平均推理功率 $\bar{P}$ 是模型固有的常数，与输入分辨率、图像内容复杂度、提示词类型无关（波动 < 5%）。
*   **线性延迟模型**：将推理时间 $t$ 分解为预填充（Prefill）和解码（Decode）两个阶段：
    $$t_{wall} \approx \alpha_p \cdot N_{in} + \alpha_d \cdot N_{out} + \beta$$
    其中 $\alpha_p$ 和 $\alpha_d$ 分别是输入和输出 token 的边际时间成本。
*   **能耗分解公式**：由于功率恒定，能耗 $E = \bar{P} \times t$。研究者据此计算出“输出长度敏感度”（OLS），即每增加一个输出 token 所需的焦耳数。
*   **通用预测器**：构建了一个基于模型参数量 $S$、输入 token 数 $N_{in}$ 和输出 token 数 $N_{out}$ 的线性回归模型，用于在不进行实际测量的情况下预测能耗。

### 3. 实验设计
*   **硬件平台**：NVIDIA RTX 3070 Laptop GPU（代表高性能边缘端）和 NVIDIA Jetson Orin NX（代表嵌入式低功耗端）。
*   **测试模型**：涵盖 3 个架构家族的 5 种模型（InternVL3-1B/2B、Qwen2-VL-2B、Qwen2.5-VL-3B、Gemma-3-4B），并扩展测试了 7B 和 8B 模型。
*   **数据集与变量**：
    *   使用 **COCO 2017** 验证集，根据物体数量划分 6 个图像复杂度等级。
    *   **分辨率**：224²、448²、672²、896²。
    *   **提示词**：对比“详细描述”（长输出）与“简短回答”（极短输出）。
*   **Benchmark**：以 llama.cpp 作为推理后端，测量实际的墙钟时间（Wall-clock time）和硬件传感器采集的实时功率。

### 4. 资源与算力
*   **推理设备**：
    *   RTX 3070 Laptop (8GB VRAM)，频率锁定在 1500MHz。
    *   Jetson Orin NX (15W 功率模式)，使用 `jetson_clocks` 锁定频率。
*   **测量工具**：HWiNFO64 Pro（PC端）和板载 INA3221 传感器（Jetson端），采样间隔为 100ms。
*   **训练算力**：本文专注于推理性能分析，未涉及模型训练过程。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **1680 次** 多模态推理实验。
*   **充分性**：实验覆盖了从 1B 到 8B 的模型参数梯度，对比了“固定 token”和“动态分辨率”两种主流 VLM 架构，并在两种完全不同的硬件架构上验证了结论的一致性。
*   **客观性**：通过控制变量法（如固定分辨率改变内容、固定内容改变提示词）排除了干扰因素，实验设计严谨，数据具有统计学说服力（$R^2$ 达到 0.986）。

### 6. 主要结论与发现
1.  **功率是模型指纹**：平均功率由模型大小决定，每增加 1B 参数约增加 12W 功率，但与输入图像的具体特征无关。
2.  **解码主导能耗**：自回归解码阶段占总能耗的 **86% 到 97%**。
3.  **成本极度不对称**：单个输出 token 的时间/能量成本是单个输入 token（含视觉 token）的 **11 到 39 倍**。
4.  **视觉剪枝的局限**：对于固定 token 模型，即使剪掉 100% 的视觉 token，节能上限也仅为 10% 左右。
5.  **输出长度是关键**：控制输出长度（如通过提示词或 `max_tokens`）最高可节省 **97%** 的能量。

### 7. 优点
*   **颠覆性认知**：有力地挑战了当前 VLM 效率优化领域过度关注“视觉 token 减少”的现状。
*   **高泛化性预测**：提出的通用能耗预测器无需针对特定模型校准，即可达到约 90% 的预测准确率。
*   **部署指导意义**：为具身智能系统提供了明确的节能策略（如：预算输出而非输入、根据电池电量动态调整输出长度）。

### 8. 不足与局限
*   **批处理场景缺失**：实验主要针对 Batch Size = 1（单智能体典型场景），未探讨多请求并发时的能耗动态。
*   **硬件多样性有限**：虽然对比了 PC 和 Jetson，但尚未覆盖手机 SoC（如骁龙 NPU）或专用 AI 加速器。
*   **任务类型单一**：主要基于图像描述任务，对于需要长文本上下文的多轮对话或复杂逻辑推理任务，KV 缓存增长对能耗的非线性影响讨论较少。

（完）
