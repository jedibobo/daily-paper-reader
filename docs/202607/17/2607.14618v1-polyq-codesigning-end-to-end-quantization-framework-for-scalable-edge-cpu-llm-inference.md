---
title: "PolyQ: Codesigning End-to-End Quantization Framework for Scalable Edge CPU LLM Inference"
title_zh: PolyQ：面向可扩展边缘 CPU 大语言模型推理的端到端量化框架协同设计
authors: "Hyunwoo Oh, Suyeon Jang, Hanning Chen, KyungIn Nam, Sanggeon Yun, Ryozo Masukawa, Mohsen Imani"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14618v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 面向CPU的LLM推理编译器与量化协同设计
tldr: 针对边缘端CPU上大语言模型推理效率与精度的权衡问题，本文提出PolyQ框架。该框架采用软硬件协同设计，通过感知激活的通道级位宽分配（2-16位）实现细粒度量化。利用编译器在编译阶段对通道进行重排与聚类，生成高效的SIMD和LUT内核，有效解决了混合精度在CPU上难以高效执行的难题。实验表明，PolyQ在多种模型和硬件上实现了精度与性能的平滑扩展，显著提升了推理效率。
selection_source: fresh_fetch
motivation: 现有低比特量化方法在CPU上要么粒度太粗影响精度，要么混合精度执行效率低下，难以满足边缘端LLM推理的需求。
method: 提出一种编译器与量化协同设计方案，通过通道级位宽分配、编译时布局正则化及高效内核生成，实现实用的分数比特部署。
result: "在多个模型上相比现有方法显著降低了困惑度，并减少了高达70.8%的激活重排开销，推理性能随比特预算近乎线性扩展。"
conclusion: PolyQ证明了在多样化的边缘CPU上实现细粒度、可扩展且高能效的分数比特LLM部署是切实可行的。
---

## 摘要
CPU 是设备端大语言模型（LLM）推理最通用的目标平台，但现有的低比特量化方法要么提供粗粒度的操作点，要么提供难以在 CPU 上高效执行的细粒度混合精度。我们提出了 PolyQ，这是一种面向 CPU 的编译器/量化协同设计方案，用于在用户指定的平均比特预算下进行激活感知的通道级比特分配。PolyQ 从 {2, 3, 4, 8, 16} 中分配每个通道的比特宽度，然后使用编译时模型编译器将通道置换并聚类为比特同质块，生成兼容 SIMD 和查找表（LUT）的算子内核，并合并跨算子的兼容置换，从而将布局正则化移出运行时路径。这使得细粒度的预算拟合转变为一种适用于纯 CPU 推理的实用分数比特（fractional-bit）部署方法。在 WikiText-2 数据集上针对 Falcon-H1-3B、Llama2-13B 和 Qwen3-32B 的测试表明，PolyQ 在 3-6 比特范围内提供了稳定的质量扩展，并在 3 比特目标下比现有方法将困惑度（perplexity）降低了 2.4-32.1%。在工作站、笔记本电脑和移动设备这三种代表性 CPU 上的端到端测量显示，编译器布局正则化将激活重排流量减少了高达 70.8%，预填充延迟和解码吞吐量几乎与配置的比特预算成比例缩放，且相对于优化的基于 LUT 的后端，每 Token 的能量开销保持在 2% 以下。这些结果表明，分数比特 CPU 部署在各种边缘目标上都是实用、可预测且节能的。

## Abstract
CPUs are the most universal target for on-device LLM inference, but existing low-bit quantization methods offer either coarse operating points or fine-grained mixed precision that is difficult to execute efficiently on CPUs. We present PolyQ, a CPU-oriented compiler/quantization co-design for activation-aware channel-wise bit allocation under a user-specified average-bit budget. PolyQ assigns per-channel bit-widths from $\{2,3,4,8,16\}$, then uses a compile-time model compiler to permute and cluster channels into bit-homogeneous blocks, generate SIMD- and LUT-compatible kernels, and merge compatible permutations across operators to keep layout regularization off the runtime path. This turns fine-grained budget fitting into a practical fractional-bit deployment method for CPU-only inference. Across Falcon-H1-3B, Llama2-13B, and Qwen3-32B on WikiText-2, PolyQ provides stable quality scaling from 3--6\,b and improves perplexity by 2.4--32.1\% over prior methods at a 3\,b target. End-to-end measurements on three representative CPUs -- workstation, laptop, and mobile -- show that compiler layout regularization reduces activation reorder traffic by up to 70.8\%, prefill latency and decode throughput scale nearly proportionally with the configured bit budget, and energy/token overhead stays below 2\% relative to an optimized LUT-based back-end. These results show that fractional-bit CPU deployment is practical, predictable, and energy-efficient across diverse edge targets.

---

## 论文详细总结（自动生成）

以下是对论文《PolyQ: Codesigning End-to-End Quantization Framework for Scalable Edge CPU LLM Inference》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的边缘 CPU 上实现高效且精度可控的大语言模型（LLM）推理。
*   **研究动机**：
    *   **CPU 的普适性**：尽管 GPU 吞吐量高，但 CPU 在边缘设备（笔记本、嵌入式控制器）中更通用且成本更低。
    *   **现有量化方案的局限**：传统的后训练量化（PTQ）通常只提供粗粒度的整数比特（如 W3 或 W4），无法精确匹配碎片化的内存预算。
    *   **执行效率瓶颈**：现有的细粒度混合精度方法虽然精度高，但在 CPU 上执行时会产生巨大的数据重排（Reorder）开销，且不兼容 SIMD 或查找表（LUT）加速内核。

### 2. 方法论
PolyQ 提出了量化算法与编译器的协同设计方案，核心技术包括：
*   **多精度量化（Poly-Precision Quantization）**：
    *   **CPU 友好型调色板**：允许每个通道从 {2, 3, 4, 8, 16} 比特中选择位宽，这组位宽直接对应 CPU 的 LUT 和 SIMD 处理能力。
    *   **激活感知误差代理**：通过校准集收集激活能量（$E[x^2]$），计算每个通道在不同比特下的重构误差。
    *   **注水算法（Waterfilling）**：在给定的分数比特预算（如 3.7-bit）下，贪婪地为高显著性通道分配更多比特，直到耗尽预算。
    *   **ISA 感知量子匹配**：将通道数调整为符合硬件 SIMD 向量长度或 LUT 块大小的整数倍。
*   **模型编译器架构**：
    *   **通道聚类与置换**：在编译阶段将相同比特的通道重新排列并聚类，形成连续的同质块。
    *   **跨层置换合并**：通过静态分析算子图（DAG），将下游所需的激活重排操作吸收进上游权重的编译时置换中，从而消除运行时的激活重排开销。
    *   **自动内核生成**：为不同的比特块生成特定的 C++ 内核（如 2-4 bit 使用 LUT，8-16 bit 使用 SIMD MAC）。

### 3. 实验设计
*   **模型选择**：Falcon-H1-3B、Llama2-13B、Qwen3-32B。
*   **数据集**：
    *   **语言建模**：WikiText-2（衡量困惑度 PPL）。
    *   **下游任务**：MMLU、WinoGrande、ARC-Easy/Challenge、HellaSwag（衡量零样本准确率）。
*   **基准对比（Benchmarks）**：AWQ、GPTQ、Slim-LLM、AMQ。
*   **硬件平台**：
    *   工作站：AMD Ryzen 9 9950X (16核)。
    *   笔记本：AMD Ryzen 7 7840U (8核)。
    *   移动端：Intel N250 (4核)。

### 4. 资源与算力
*   **校准算力**：使用了 128 条随机采样的 WikiText-2 序列进行校准。论文未详细列出量化过程的具体 GPU 型号，但指出该过程是轻量级的（PTQ 模式，无需重训练）。
*   **推理算力**：实验重点在于 CPU 推理性能，使用了上述三种不同等级的 CPU 进行端到端测量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3.0b 到 6.0b 的细粒度比特扫描（步长 0.1b），并在三种不同架构的模型和三种不同性能等级的硬件上进行了验证。
*   **充分性评价**：实验设计较为充分。不仅对比了模型精度（PPL 和 Accuracy），还深入分析了系统级指标，如内存占用、激活重排流量、预填充延迟、解码吞吐量以及每 Token 能耗。对比方法包含了目前主流的量化框架，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **精度提升**：在 3-bit 目标下，PolyQ 比现有方法降低了 2.4%–32.1% 的困惑度。
*   **性能扩展**：推理延迟和吞吐量随比特预算呈近乎线性的比例缩放，证明了分数比特控制的有效性。
*   **开销极低**：编译器优化减少了高达 70.8% 的激活重排流量；相比优化的 LUT 后端，PolyQ 的额外能耗开销低于 2%，延迟开销低于 5.8%。
*   **内存优化**：能够精确匹配内存预算，避免了整数比特量化带来的内存浪费。

### 7. 优点
*   **软硬协同**：不只是改进量化算法，还通过编译器解决了混合精度在 CPU 上的执行难题。
*   **细粒度控制**：支持分数比特（Fractional-bit）部署，使模型能完美契合边缘设备碎片化的内存环境。
*   **零运行时负担**：通过编译时置换合并，将复杂的布局调整移出推理路径，保持了极高的执行效率。

### 8. 不足与局限
*   **校准集依赖**：作为 PTQ 方法，其精度高度依赖于校准数据集的质量和代表性。
*   **硬件特定优化**：虽然理论上可移植，但目前的内核生成和量子匹配主要针对支持 SIMD 和特定指令集的现代 CPU（如 AVX2, NEON），在极老旧硬件上可能受限。
*   **任务覆盖**：虽然测试了多个下游任务，但主要集中在选择题类任务，对于长文本生成或复杂推理任务的评估相对较少。

（完）
