---
title: Pattern-Guided Design Space Exploration for FPGA Accelerator Design
title_zh: 面向 FPGA 加速器设计的模式引导设计空间探索
authors: "Jialiang Zhang, Weiman Yan, Yuelin Zou"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15068v1"
tags: ["query:hwnas"]
score: 9.0
evidence: FPGA加速器设计的搜索空间探索
tldr: 本研究针对FPGA加速器设计中高级综合（HLS）调度决策空间过大的问题，提出了基于模式引导的设计空间探索框架PATTERNDSE。该框架针对Allo编程系统，将常见的计算模式（如映射、规约、矩阵运算等）映射到紧凑的调度空间，并通过功能验证和模式感知评估器对候选方案进行排序。实验表明，该方法在保持最优性能的同时，将搜索空间平均缩减了4.83倍，显著提升了设计效率。
selection_source: fresh_fetch
motivation: 传统的HLS设计依赖复杂的调度决策，导致组合爆炸的设计空间，难以高效寻找最优硬件实现。
method: 开发了PATTERNDSE框架，通过识别计算模式来约束调度空间，并利用轻量级评估器在综合前对候选方案进行筛选。
result: 在vecadd、gemm等六个内核上的测试显示，该方法在减少高达12倍搜索次数的情况下，仍能获得与穷举法一致的最优延迟。
conclusion: 结合计算模式信息可以有效剪枝低效的调度组合，是优化FPGA HLS设计空间探索的关键途径。
---

## 摘要
高级综合 (HLS) 将 FPGA 加速器设计的抽象层级从硬件描述语言提升到了 C/C++，但高质量的结果仍取决于调度决策，如流水线化 (pipelining)、展开 (unrolling)、分块 (tiling)、重排序 (reordering) 和缓冲 (buffering)。这些决策创造了一个组合设计空间，而许多数值算子表现出重复出现的计算模式，这些模式暗示了不同的优化策略。本文提出了 PATTERNDSE，这是一个轻量级的模式引导设计空间探索 (DSE) 框架，适用于使用面向调度的 HLS 编程系统 Allo 编写的 FPGA 算子。PATTERNDSE 将重复出现的计算模式（包括逐元素映射、归约、矩阵-向量运算、矩阵-矩阵运算和类 stencil 更新）映射到紧凑的调度空间。随后，它应用候选调度，通过 LLVM 执行验证功能正确性，检查 HLS C 代码生成，并在 Vitis HLS 综合之前使用简单的模式感知估计器对候选方案进行排序。我们在六个代表性算子上评估了 PATTERNDSE：vecadd、axpy、dot、matvec、gemm 和 jacobi2d。与轻量级穷举 (exhaustive-lite) 基准相比，模式引导的 DSE 将 HLS 评估的候选方案数量从 140 个减少到 29 个，实现了 4.83 倍的总体搜索缩减，单个算子的缩减倍数高达 12.0 倍。在所有评估的算子中，PATTERNDSE 获得了与轻量级穷举基准相同的最佳有效 Vitis HLS 延迟，证明了计算模式信息可以在保留高质量 HLS 结果的同时，剪枝掉无意义的调度组合。

## Abstract
High-level synthesis (HLS) raises the abstraction level of FPGA accelerator design from hardware description languages to C/C++, but high-quality results still depend on schedule decisions such as pipelining, unrolling, tiling, reordering, and buffering. These decisions create a combinatorial design space, while many numerical kernels exhibit recurring computation patterns that suggest different optimization strategies. This paper presents PATTERNDSE, a lightweight pattern-guided design space exploration (DSE) framework for FPGA kernels written in Allo, a scheduling-oriented HLS programming system. PATTERNDSE maps recurring computation patterns, including elementwise maps, reductions, matrix-vector operations, matrix-matrix operations, and stencil-like updates, to compact schedule spaces. It then applies candidate schedules, validates functional correctness through LLVM execution, checks HLS C code generation, and uses a simple pattern-aware estimator to rank candidates before Vitis HLS synthesis. We evaluate PATTERNDSE on six representative kernels: vecadd, axpy, dot, matvec, gemm, and jacobi2d. Compared with an exhaustive-lite baseline, pattern-guided DSE reduces the number of HLS-evaluated candidates from 140 to 29, achieving a 4.83x overall search reduction and up to 12.0x reduction for individual kernels. Across all evaluated kernels, PATTERNDSE recovers the same best valid Vitis HLS latency as the exhaustive-lite baseline, demonstrating that computation-pattern information can prune unproductive schedule combinations while preserving high-quality HLS outcomes.

---

## 论文详细总结（自动生成）

这是一份关于论文《Pattern-Guided Design Space Exploration for FPGA Accelerator Design》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：高级综合（HLS）虽然降低了 FPGA 设计门槛，但其性能高度依赖于调度决策（如流水线、展开、分块等）。这些决策构成了庞大的组合搜索空间，导致传统的设计空间探索（DSE）效率低下。
*   **研究动机**：数值计算内核并非随机程序，而是具有显著的重复模式（如矩阵乘法、规约、卷积等）。作者认为，利用这些计算模式的结构化信息，可以大幅剪枝无效的调度组合，从而在不损失硬件实现质量的前提下，加速 DSE 过程。

### 2. 方法论
核心思想是开发一个名为 **PATTERN DSE** 的轻量级框架，作为 Allo（一种面向调度的 HLS 编程系统）的前端剪枝层。
*   **模式模板（Pattern Templates）**：预定义了五类模式（Elementwise, Reduction, MatVec, GEMM, Stencil），每种模式对应特定的调度优先级（例如，GEMM 侧重于循环顺序和乘累加并行度）。
*   **算法流程**：
    1.  **候选生成**：根据内核所属模式，从规则库中实例化受限的调度参数。
    2.  **后端验证**：通过 LLVM 执行验证功能正确性，并检查是否能成功生成 HLS C/C++ 代码。
    3.  **评估与排序**：使用启发式评分公式 $S(d) = \hat{L}(d) \cdot (1 + \alpha \hat{R}(d))$ 对候选方案排序，其中 $\hat{L}$ 是估计延迟，$\hat{R}$ 是资源压力，$\alpha$ 是惩罚系数。
    4.  **综合评估**：仅将排名前 $k$ 的候选方案提交给昂贵的 Vitis HLS 进行最终综合。

### 3. 实验设计
*   **实验场景**：针对六个代表性数值内核进行 FPGA 加速器设计优化。
*   **测试内核（Benchmark）**：`vecadd` (向量加法), `axpy` (标量乘加), `dot` (点积), `matvec` (矩阵-向量乘), `gemm` (通用矩阵乘), `jacobi2d` (2D 模板计算)。
*   **对比方法**：**Exhaustive-lite**（轻量级穷举法）。这是一种受限的枚举策略，在相同的搜索边界内应用通用调度模板，不利用模式信息。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**运行 DSE 脚本的具体硬件配置（如 CPU 型号、内存等）。
*   **工具链**：使用了 Allo 编程框架、LLVM 验证环境以及 Xilinx Vitis HLS 综合工具。实验结果主要关注搜索次数的减少和 HLS 报告的周期数（Latency）。

### 5. 实验数量与充分性
*   **实验规模**：共对 6 个内核进行了测试。在基准测试中，穷举法评估了 140 个候选方案，而 PATTERN DSE 仅评估了 29 个。
*   **充分性评价**：
    *   **覆盖度**：涵盖了从简单的逐元素运算到复杂的矩阵运算和模板计算，具有一定的代表性。
    *   **客观性**：通过对比“最佳有效延迟”来验证剪枝是否误删了最优解，实验设计逻辑严密。
    *   **局限性**：实验样本量（6 个内核）相对较小，且模式识别目前依赖手动标注而非自动提取。

### 6. 主要结论与发现
*   **搜索效率提升**：PATTERN DSE 将 HLS 评估的候选方案数量减少了 **4.83 倍**（从 140 降至 29），在 `gemm` 等复杂内核上缩减达 6 倍，`jacobi2d` 甚至达到 12 倍。
*   **性能保持**：在所有测试用例中，PATTERN DSE 找到的最优延迟与穷举法完全一致，证明了模式引导的剪枝策略在剔除无效空间的同时保留了高质量解。
*   **模式价值**：计算模式信息是 HLS 优化中极强的先验知识，能有效解决调度决策的组合爆炸问题。

### 7. 优点
*   **高效剪枝**：显著降低了昂贵的 HLS 综合工具的调用次数，极大地缩短了设计周期。
*   **可解释性强**：每个生成的候选调度都与特定的计算模式规则相关联，比黑盒搜索（如遗传算法）更易于理解。
*   **兼容性**：作为 Allo 系统的插件，不改变原有的编程模型，且能与现有的分析模型或学习方法结合。

### 8. 不足与局限
*   **模式库有限**：目前仅支持 5 种模式，尚未覆盖卷积、张量收缩、稀疏算子或流式数据流等更复杂的模式。
*   **自动化程度**：模式匹配目前需要手动元数据支持，缺乏自动化的模式提取/识别机制。
*   **评估器简单**：使用的启发式评分公式较为基础，在处理更大规模或更复杂的调度空间时，可能需要更精确的分析模型或机器学习模型来辅助排序。
*   **基准对比单一**：主要对比了受限穷举法，未与贝叶斯优化或强化学习等先进 DSE 工具进行横向对比。

（完）
