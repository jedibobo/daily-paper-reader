---
title: "ExaGEMM: Exploration Framework for CPU-Driven ML Inference via Associative In-Register Computing for Low-Bit GEMM"
title_zh: ExaGEMM：通过用于低比特 GEMM 的关联式寄存器内计算实现 CPU 驱动机器学习推理的探索框架
authors: "Hyunwoo Oh, Suyeon Jang, Hanning Chen, Sanggeon Yun, Ryozo Masukawa, Mohsen Imani"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14622v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对CPU原生低比特GEMM执行的工作负载感知协同设计
tldr: 针对低比特GEMM在传统CPU上执行效率低的问题，本文提出ExaGEMM框架。该框架利用寄存器内查找表（LUT）执行机制，通过协同探索参数化算子和轻量级SIMD指令集扩展，实现了高效的CPU原生低比特推理。ExaGEMM利用分析模型大幅缩减设计空间，在多种ML模型上相比纯软件方案实现了13.29倍的延迟优化，为混合精度LLM负载提供了高效的硬件支持方案。
selection_source: fresh_fetch
motivation: 传统CPU在处理极低比特GEMM时效率低下，且不同精度的硬件支持成本与复用机会差异巨大，亟需轻量级的硬件协同设计方案。
method: 提出基于寄存器内查找表执行的ExaGEMM框架，通过分析模型协同探索内核参数与ISA支持，并引入轻量级的寄存器选择与馈送硬件机制。
result: "该框架成功将候选设计空间缩减了99.2%，并在代表性ML模型上实现了比纯软件基准高出13.29倍的推理延迟改进。"
conclusion: ExaGEMM证明了结合工作负载感知的硬件指令集扩展与寄存器计算，是提升CPU低比特ML推理性能的有效途径。
---

## 摘要
低比特 GEMM 在高效机器学习推理中日益重要，但极低比特执行在传统 CPU 上仍然表现不佳。实际部署涵盖了碎片化的方案——从 1/2/4 比特权重到不同的激活精度——在固定的 SIMD 和寄存器堆预算下，这些方案的可行性、复用机会和支持成本各不相同，使得轻量级 CPU 支持的选择成为一个首要的设计问题。我们提出了 ExaGEMM，这是一个通过寄存器驻留查找表（LUT）执行实现 CPU 原生低比特 GEMM 的工作负载感知协同设计与探索框架。核心见解是现有的 SIMD 数据通路已经涵盖了表格生成和累加；唯一新增的硬件是具有显式建模成本的寄存器内选择/馈送机制。ExaGEMM 利用寄存器可行性、计算成本、内存流量和硬件开销的分析模型，对参数化内核和轻量级 SIMD 指令集架构（ISA）支持进行协同探索，在仿真前将候选空间缩减了 99.2%。随后，它识别出非支配支持点，并生成 ISA 规范、gem5 补丁和 GEMM 内核以进行验证。在代表性的机器学习模型和 CPU 目标上，ExaGEMM 相比纯软件基准将延迟降低了 13.29 倍，同时表明工作负载感知的边界选择对于混合精度大语言模型（LLM）工作负载尤为重要。

## Abstract
Low-bit GEMM is increasingly central to efficient ML inference, yet very-low-bit execution remains a poor fit for conventional CPUs. Practical deployment spans fragmented regimes-from 1/2/4-bit weights to varying activation precision-whose feasibility, reuse opportunity, and support cost differ under fixed SIMD and register-file budgets, making lightweight CPU support selection a first-class design problem. We present ExaGEMM, a workload-aware codesign and exploration framework for CPU-native low-bit GEMM via register-resident LUT execution. The key insight is that existing SIMD datapaths already cover table generation and accumulation; the only new hardware is an in-register select/feed mechanism with explicitly modeled cost. ExaGEMM co-explores parameterized kernels and lightweight SIMD ISA support using analytical models of register feasibility, compute cost, memory traffic, and hardware overhead, pruning the candidate space by 99.2% before simulation. It then identifies non-dominated support points and generates ISA specs, gem5 patches, and GEMM kernels for validation. Across representative ML models and CPU targets, ExaGEMM improves latency by 13.29x over software-only baselines, while showing that workload-aware frontier selection is especially important for mixed-precision LLM workloads.

---

## 论文详细总结（自动生成）

以下是对论文《ExaGEMM: Exploration Framework for CPU-Driven ML Inference via Associative In-Register Computing for Low-Bit GEMM》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着机器学习（ML）推理向边缘侧和私有化发展，低比特（1/2/4-bit）矩阵乘法（GEMM）成为减少模型体积和内存流量的关键。
*   **核心痛点**：
    1.  **硬件不匹配**：传统 CPU 的 SIMD 指令集难以高效处理极低比特运算，软件查表法（LUT）虽能减少计算量，但受限于表格构建和内存移动开销。
    2.  **方案碎片化**：实际应用中存在多种量化组合（如 W1A8, W4A16 等），单一的固定硬件设计或软件内核难以覆盖所有最优性能点。
    3.  **设计空间巨大**：在有限的寄存器和 SIMD 预算下，如何选择最优的硬件支持点（ISA 扩展）与内核参数是一个复杂的协同设计问题。
*   **研究目的**：开发一个工作负载感知的协同设计框架，通过轻量级硬件扩展实现 CPU 原生的低比特 GEMM 加速。

### 2. 方法论：ExaGEMM 框架
*   **核心思想**：利用 CPU 现有的 SIMD 数据通路进行表格生成和累加，仅新增一个轻量级的**寄存器内选择/馈送机制（Select Network）**。通过将查找表（LUT）驻留在寄存器中，消除内存访问瓶颈。
*   **关键技术细节**：
    *   **参数化执行空间**：定义内核参数 $k = (e, b, r, c, u)$，其中 $e$ 为打包因子，$b$ 为扇入大小，$r$ 和 $c$ 为行列分块大小，$u$ 为指令级合并因子。
    *   **轻量级 ISA 支持**：引入两个逻辑原语：`TBL_r×be`（生成寄存器内查找表）和 `GEMM_r×be×c`（执行查表计算并更新输出）。
    *   **分析探索器（Analytical Explorer）**：建立数学模型评估寄存器可行性、指令计数、内存流量、计算成本和硬件开销。
    *   **指令级合并（ILM）**：通过合并多个 K 维度子块，减少冗余的输出加载/存储操作，提高寄存器复用率。
*   **算法流程**：
    1.  输入量化模型参数和目标 CPU 规格。
    2.  枚举候选内核参数，利用分析模型进行初步筛选。
    3.  **大幅剪枝**：在仿真前剔除不满足寄存器约束或性能较差的方案（剪枝率达 99.2%）。
    4.  识别帕累托最优（Non-dominated）的支持点，生成 ISA 规范、gem5 仿真补丁和优化内核。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **视觉任务**：DeiT-B 模型，采用 W1A8 均匀量化。
    *   **语言任务**：Llama2-7B 模型，涵盖 W2A16 均匀量化及 W1/2/4A16 混合精度量化。
*   **硬件目标（仿真）**：
    *   **工作站级**：x86-64 (AVX-512), 32 个 512-bit 寄存器。
    *   **笔记本级**：x86-64 (AVX2), 16 个 256-bit 寄存器。
    *   **移动级**：ARMv8-A (NEON), 16 个 128-bit 寄存器。
*   **对比基准（Benchmarks）**：
    *   **T-MAC**：先进的纯软件 SIMD 查表内核。
    *   **llama.cpp**：工业界常用的 LLM 推理框架。
    *   **VAQF**：针对 FPGA 的低比特视觉 Transformer 协同设计方案（作为参考）。

### 4. 资源与算力
*   **仿真工具**：使用 **gem5** 模拟器进行微架构级验证。
*   **硬件成本评估**：使用 **TSMC 28nm** 工艺库，通过 Cadence Genus 进行 ASIC 综合，评估新增选择网络的面积和功耗开销。
*   **算力说明**：文中未提及具体的 GPU 训练时长，因为该研究聚焦于**推理阶段的 CPU 架构扩展**。实验主要消耗的是 CPU 仿真算力和 EDA 工具的综合算力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对数以亿计的潜在设计组合进行了分析建模。
    *   在 gem5 中对筛选出的约 30 个代表性支持点（包括帕累托最优和随机采样点）进行了详细仿真。
    *   覆盖了从 128-bit 到 512-bit 的主流 CPU 向量宽度。
*   **充分性评价**：实验设计较为充分。通过 Spearman 相关系数（0.889-0.982）验证了分析模型与实际仿真结果的高度一致性。对比了不同量化制度（均匀 vs 混合精度）和不同硬件平台，证明了框架的通用性。

### 6. 主要结论与发现
*   **性能提升**：ExaGEMM 相比纯软件基准（T-MAC）实现了 **1.92× 至 13.29×** 的延迟改进。
*   **剪枝效率**：分析模型成功将搜索空间缩减了 **99.2%** 以上，极大地降低了架构探索的成本。
*   **工作负载感知的重要性**：对于均匀量化（如 DeiT），单一强力硬件点即可满足需求；但对于混合精度 LLM，工作负载感知的边界选择能显著平衡硬件成本与执行效率。
*   **硬件开销**：新增的硬件支持非常轻量，在 128/256-bit 平台上，面积开销通常小于 4%。

### 7. 优点与亮点
*   **协同设计深度**：不仅优化了软件内核，还深入到 ISA 指令定义和微架构电路设计。
*   **高效率探索**：提出的分析模型能够精准预测性能趋势，避免了昂贵的穷举仿真。
*   **原生性**：方案不依赖外部加速器（如 NPU/GPU），而是增强 CPU 自身的 SIMD 能力，适合对成本和集成度敏感的边缘设备。
*   **跨平台验证**：同时支持 x86 和 ARM 架构，具有较强的工业应用潜力。

### 8. 不足与局限
*   **仿真依赖**：主要结果基于 gem5 仿真而非真实硅片，实际硬件中的时钟频率、布线延迟等物理因素可能存在偏差。
*   **量化类型限制**：主要针对基于查找表的线性量化，对于非线性量化或更复杂的动态量化支持尚不明确。
*   **硬件修改门槛**：虽然硬件改动轻量，但仍需修改 CPU 的 ISA 和数据通路，这对于现有的商用通用 CPU 来说难以直接应用，更适合定制化的 SoC 或 RISC-V 架构。

（完）
