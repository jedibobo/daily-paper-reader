---
title: "CODA: Algorithm-Hardware Co-design for Edge Video Diffusion via NMP-Enabled Compute-Cache Operator Disaggregation"
title_zh: CODA：通过支持近内存处理的计算-缓存算子解耦实现边缘视频扩散的算法-硬件协同设计
authors: "Yuanpeng Zhang, YuXuan Wu, Yitong Xiao, Chenhao Xue, Yi Ren, Cong Li, Yihan Yin, Dimin Niu, Guangyu Sun"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14908v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对边缘视频扩散模型的算法-硬件协同设计，采用计算-缓存算子解耦技术
tldr: 针对边缘设备上视频扩散模型（VDM）推理速度慢和内存受限的问题，本文提出CODA架构。通过算法与硬件协同设计，CODA实现了计算与缓存算子的解耦，将内存受限的缓存路径迁移至近内存处理（NMP）引擎，并利用分类器自由引导（CFG）的并行性优化执行流。该方案在保持生成质量的同时，显著提升了推理速度和能效。
selection_source: fresh_fetch
motivation: 视频扩散模型的跨时间步缓存技术虽能减少计算，但巨大的缓存开销导致边缘GPU显存溢出，且频繁的PCIe通信成为了性能瓶颈。
method: 提出计算-缓存算子解耦架构，将密集计算留在xPU，而将内存受限的缓存操作移至近内存引擎，并结合缓存段合并与CFG分支重叠执行。
result: 实验表明，CODA相比现有最先进的缓存算法，实现了高达1.80倍的端到端加速和1.74倍的能效提升。
conclusion: CODA通过软硬件协同优化，有效解决了边缘端视频生成任务中的内存与通信瓶颈，为高效本地化视频扩散推理提供了可行方案。
---

## 摘要
在边缘设备上部署视频扩散模型 (VDM) 对于实现本地化和隐私保护生成具有吸引力，但其基于 Transformer 的迭代去噪过程对于实际的本地推理来说仍然太慢。跨时间步缓存 (CTC) 已成为减少冗余计算的一个有前景的方向，它通过在相邻去噪步骤中重用激活值而非修改模型权重，同时在很大程度上保持了生成保真度。然而，在内存受限的边缘 GPU 上，CTC 需要巨大的缓存占用，这会迅速超过设备显存 (VRAM) 并迫使缓存进入主机内存。更根本的是，缓存算子与原生计算算子仍然紧密交织且存在链式依赖，因此简单的近内存卸载仍会因残差和融合计算产生重复的 PCIe 交换，使缓存重用变成受通信和串行化限制的执行流。因此，我们提出了 CODA，这是一种以计算-缓存算子解耦为核心的算法-硬件协同设计架构。CODA 将密集计算路径和内存受限的缓存路径分别在 xPU 和轻量级 DIMM 侧近内存引擎上进行分离，将碎片化的缓存活动重组为硬件友好的合并段，并利用无分类器指导 (CFG) 分支的独立性，使 xPU 计算与缓存侧执行相重叠。实验表明，与最先进的缓存算法相比，CODA 实现了高达 1.80 倍的端到端加速和 1.74 倍的能效提升，同时保持了具有竞争力的生成质量。

## Abstract
Deploying Video Diffusion Models (VDMs) on edge devices is appealing for localized and privacy-preserving generation, but their iterative Transformer-based denoising remains too slow for practical local inference. Cross-Timestep Caching (CTC) has emerged as a promising direction for reducing redundant computation, reusing activations across adjacent denoising steps rather than modifying model weights, while largely preserving generation fidelity. However, on memory-constrained edge GPUs, CTC requires a massive cache footprint that quickly exceeds on-device VRAM and forces the cache into host memory. More fundamentally, cache operators remain tightly interleaved and chain-dependent with native compute operators, so naive near-memory offloading still incurs repeated PCIe exchanges for residual and fusion computations, turning cache reuse into a communication- and serialization-bound execution flow. We therefore propose CODA, an algorithm-hardware co-designed architecture centered on Compute-Cache Operator Disaggregation. CODA separates dense compute paths and memory-bound cache paths across the xPU and a lightweight DIMM-side near-memory engine, reorganizes fragmented cache activity into hardware-friendly coalesced segments, and exploits Classifier-Free Guidance (CFG) branch independence to overlap xPU compute with cache-side execution. Experiments show that CODA achieves up to 1.80x end-to-end speedup and 1.74x higher energy efficiency, while preserving competitive generation quality compared with a state-of-the-art caching algorithm.

---

## 论文详细总结（自动生成）

这是一份关于论文《CODA: Algorithm-Hardware Co-design for Edge Video Diffusion via NMP-Enabled Compute-Cache Operator Disaggregation》的结构化深入分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的边缘设备（如工作站、边缘服务器）上高效运行视频扩散模型（VDM）。
*   **研究背景**：
    *   **计算瓶颈**：VDM（如Sora、HunyuanVideo）依赖复杂的Transformer架构，迭代去噪过程计算量巨大。
    *   **缓存困境**：现有的“跨时间步缓存”（CTC）技术虽能减少计算，但会产生海量中间特征缓存（如4秒720p视频需40GB+缓存），远超边缘GPU显存（通常24GB）。
    *   **通信墙**：当缓存被迫存放在主机内存（DRAM）时，频繁通过PCIe总线读取低算术强度的缓存算子，导致PCIe带宽成为严重瓶颈，甚至抵消了缓存带来的加速效果。
    *   **串行依赖**：计算算子与缓存算子紧密交织，导致系统执行流高度碎片化且存在严重的串行等待。

### 2. 方法论：CODA 架构设计
CODA 提出了**计算-缓存算子解耦**的核心思想，通过算法与硬件的协同设计解决上述问题：
*   **硬件层：轻量级 DIMM-NMP 子系统**：
    *   在内存条（DIMM）侧设计近内存引擎（NME），专门负责处理内存密集型的缓存操作（如特征检索、残差融合、缩放）。
    *   将密集计算（GEMM等）保留在高性能 xPU（如GPU）上，实现物理层面的算子解耦。
*   **软件层：混合硬件感知缓存调度器**：
    *   **静态分析**：通过离线分析算子的敏感度，将零散的缓存活动重组为“合并缓存段”（Coalesced Segments），减少PCIe往返次数。
    *   **动态调整（DRA）**：引入异步影子监控机制，在运行时检测特征演化，若发现缓存可能导致质量下降，则自动回退到原生计算模式。
*   **执行层：CFG 交错流水线（CFG-Interleaved Pipelining）**：
    *   利用无分类器指导（CFG）中“条件分支”与“无条件分支”相互独立的特性。
    *   当 GPU 处理一个分支的密集计算时，NMP 同时处理另一个分支的缓存段，从而在时间上掩盖 PCIe 通信和近内存处理的开销。

### 3. 实验设计
*   **测试模型**：涵盖了主流的 7 种 VDM 模型，包括 Latte、Open-Sora、Open-Sora Plan、Wan 2.1、Hunyuan-Video、Vchitect-2.0 和 CogVideoX-5B。
*   **场景设置**：涵盖不同分辨率（360p 到 1080p）和视频长度（2秒到 6秒）。
*   **对比基准（Baselines）**：
    1.  **Vanilla-GPU**：无缓存的原生执行。
    2.  **Cache-HostOffload**：传统的缓存卸载至主机内存方案。
    3.  **Cache-NaiveNMP**：仅简单使用 NMP 卸载，无调度和流水线优化。
    4.  **PAB (SOTA)**：目前最先进的跨时间步缓存算法。
*   **评估指标**：端到端延迟、能效比、视频质量（VBench 评分、PSNR、SSIM、LPIPS）。

### 4. 资源与算力说明
*   **硬件环境**：
    *   **xPU**：单张 NVIDIA GeForce RTX 4090 GPU (24 GB GDDR6X)。
    *   **主机**：Intel Core i9-13900K CPU，128 GB DDR4 DRAM。
    *   **接口**：PCIe 4.0 x16（双向带宽约 64 GB/s）。
*   **仿真与实现**：
    *   使用 **Ramulator 2.0** 扩展进行周期精确的 NMP 性能建模。
    *   NME 硬件使用 **TSMC 7nm** 工艺库进行 RTL 综合，以估算面积和功耗。
    *   GPU 延迟通过 NVIDIA Nsight Compute 实际测量。

### 5. 实验数量与充分性
*   **实验规模**：针对 7 种模型进行了详尽测试，每组实验重复 5 次取平均值，使用了 VBench 中的 100 个提示词（Prompts）。
*   **消融实验**：论文分别验证了 NMP 硬件、调度器优化、CFG 流水线对性能的贡献，并进行了架构参数（如 PE 数量、缓冲区大小）的搜索。
*   **客观性**：实验不仅关注速度，还通过多维度质量指标验证了算法的保真度，对比了 SOTA 算法在不同激进程度下的表现，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：CODA 实现了高达 **1.80倍** 的端到端加速。
*   **能效优化**：相比原生 GPU 执行，能效提升了 **1.74倍**。
*   **质量保持**：在大幅加速的同时，生成视频的 VBench 评分与最先进的缓存算法持平，肉眼几乎无法察觉质量损失。
*   **瓶颈转移**：实验证明，单纯增加 NMP 硬件而不进行调度优化（NaiveNMP）甚至可能因 PCIe 往返开销导致性能倒退，证明了协同设计的必要性。

### 7. 论文优点
*   **软硬协同深度高**：不只是简单的硬件加速，而是深入挖掘了扩散模型特有的 CFG 结构和时间步相似性。
*   **解决实际痛点**：直接针对边缘端显存不足和 PCIe 带宽受限这两个最现实的部署难题。
*   **硬件开销极低**：设计的 NME 引擎面积和功耗增加不到 DIMM 的 0.1%，具有很高的工程可行性。

### 8. 不足与局限
*   **模型结构依赖**：CFG 交错流水线高度依赖于模型必须包含两个独立分支，对于不使用 CFG 或使用其他引导技术的模型，加速效果可能受限。
*   **硬件落地门槛**：虽然 NME 设计轻量，但仍需要定制化的 DIMM 缓冲芯片，这在当前商用通用内存市场中尚需生态支持。
*   **应用范围**：目前主要针对视频扩散模型，虽然讨论中提到了扩散语言模型（DLLM），但尚未在文本生成等其他领域进行验证。

（完）
