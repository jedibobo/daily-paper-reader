---
title: "Smart Scissor: Coupling Spatial Redundancy Reduction and CNN Compression for Embedded Hardware"
title_zh: Smart Scissor：面向嵌入式硬件的耦合空间冗余消除与CNN压缩框架
authors: "Hao Kong, Di Liu, Shuo Huai, Xiangzhong Luo, Weichen Liu, Ravi Subramaniam, Christian Makaya, Qian Lin"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.06915v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对嵌入式硬件的CNN压缩与空间冗余减少
tldr: 针对嵌入式硬件上CNN计算开销大的问题，本文提出Smart Scissor框架。该框架结合了动态图像裁剪和复合缩减策略，通过轻量级预测器精准提取前景以减少空间冗余，并协同压缩模型的深度、宽度和分辨率。实验表明，该方法在显著降低计算量的同时，甚至能提升ImageNet分类精度，性能优于现有主流压缩方案。
selection_source: fresh_fetch
motivation: 传统的直接缩小图像分辨率会丢失关键特征导致精度下降，且CNN架构本身也存在计算冗余。
method: 提出一种包含轻量级前景预测器的动态裁剪框架，并结合深度、宽度、分辨率三维度的复合缩减策略。
result: "在ImageNet上使ResNet50计算量减少41.5%且精度提升0.3%，性能显著优于SOTA方法HRank。"
conclusion: Smart Scissor通过耦合空间冗余消除与模型压缩，为资源受限设备上的高效深度学习推理提供了有效方案。
---

## 摘要
降低输入图像的分辨率可以显著减少卷积神经网络（CNN）的计算开销，这对于边缘人工智能（Edge AI）具有广阔的前景。然而，由于图像通常包含大量的空间冗余（例如背景像素），直接缩小整张图像会丢失前景目标的重要特征，从而导致准确率严重下降。在本文中，我们提出了一种动态图像裁剪框架，通过从图像中精确裁剪前景目标来减少空间冗余。为了实现实例感知的精细裁剪，我们引入了一个轻量级前景预测器，以高效地定位并裁剪图像的前景。即使在较低的分辨率下，经过精细裁剪的图像也能被正确识别。同时，CNN架构中也存在计算冗余。为了在资源受限的嵌入式设备上追求更高的执行效率，我们还提出了一种复合收缩策略，协同压缩CNN的三个维度（深度、宽度、分辨率）。最终，我们将所提出的动态图像裁剪和复合收缩无缝结合到一个统一的压缩框架 Smart Scissor 中，旨在显著降低 CNN 的计算开销，同时保持高准确率。在 ImageNet-1K 上的实验表明，我们的方法在将 ResNet50 的计算成本降低 41.5% 的同时，将 Top-1 准确率提高了 0.3%。此外，与最先进的 CNN 压缩框架 HRank 相比，我们的方法在相同计算成本下实现了高出 4.1% 的 Top-1 准确率。代码和数据可在 https://github.com/ntuliuteam/smart-scissor 获取。

## Abstract
Scaling down the resolution of input images can greatly reduce the computational overhead of convolutional neural networks (CNNs), which is promising for edge AI. However, as an image usually contains much spatial redundancy, e.g., background pixels, directly shrinking the whole image will lose important features of the foreground object and lead to severe accuracy degradation. In this paper, we propose a dynamic image cropping framework to reduce the spatial redundancy by accurately cropping the foreground object from images. To achieve the instance-aware fine cropping, we introduce a lightweight foreground predictor to efficiently localize and crop the foreground of an image. The finely cropped images can be correctly recognized even at a small resolution. Meanwhile, computational redundancy also exists in CNN architectures. To pursue higher execution efficiency on resource-constrained embedded devices, we also propose a compound shrinking strategy to coordinately compress the three dimensions (depth, width, resolution) of CNNs. Eventually, we seamlessly combine the proposed dynamic image cropping and compound shrinking into a unified compression framework, Smart Scissor, which is expected to significantly reduce the computational overhead of CNNs while still maintaining high accuracy. Experiments on ImageNet-1K demonstrate that our method reduces the computational cost of ResNet50 by 41.5% while improving the top-1 accuracy by 0.3%. Moreover, compared to HRank, the state-of-the-art CNN compression framework, our method achieves 4.1% higher top-1 accuracy at the same computational cost. The codes and data are available at https://github.com/ntuliuteam/smart-scissor

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Smart Scissor** 的深度压缩框架，旨在通过耦合“空间冗余消除”与“网络架构压缩”来提升卷积神经网络（CNN）在嵌入式硬件上的运行效率。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在边缘设备上部署高性能 CNN 面临巨大的计算开销。虽然降低输入分辨率能显著减少计算量（MACs），但直接缩放整张图像会丢失关键的前景特征，导致精度大幅下降。
*   **研究动机**：图像中存在大量**空间冗余**（如背景），而 CNN 架构本身存在**结构冗余**（深度和宽度）。现有的压缩方法通常只关注其中一个维度，缺乏对图像内容和网络结构的协同优化。

### 2. 方法论
Smart Scissor 由两个核心模块组成：
*   **动态图像裁剪（DIC）**：
    *   **边界框生成（BBG）**：利用 Grad-CAM 技术生成训练集图像的显著性图，自动标注出前景目标的边界框（Bounding Box）。
    *   **前景预测器**：设计了一个极轻量级的 CNN（仅 0.27M 参数）作为回归器。在推理时，该预测器先快速定位前景，裁剪掉冗余背景，再将精细裁剪后的图像送入主网络。
*   **复合缩减策略（CS）**：
    *   **三维协同优化**：同时调整网络的深度（Depth）、宽度（Width）和分辨率（Resolution）。
    *   **精度估计器**：为了避免穷举训练的巨大开销，论文提出了一个基于二次多项式的精度估计模型，通过少量采样点拟合不同维度缩减对精度的影响，从而计算出给定计算预算（MACs）下的最优缩减系数。

### 3. 实验设计
*   **数据集**：ImageNet-1K（大规模分类基准）和 ImageNet-100（子集）。
*   **基准模型（Backbones）**：ResNet50 和 RegNet-X。
*   **对比方法**：
    *   **图像处理类**：ResizedCenter-Crop (RCC)、RandomCrop。
    *   **模型压缩类**：HRank、GAL、DR-ResNet、SSS、Versatile 等 SOTA 方法。
*   **硬件平台**：NVIDIA AGX Xavier、NVIDIA Jetson Nano（嵌入式 GPU）以及 Intel i7-9750H CPU。

### 4. 资源与算力
*   **训练细节**：使用 SGD 优化器，Batch Size 为 1024。主网络预训练 100 轮，加入 DIC 后微调 20 轮。
*   **算力说明**：文中**未明确说明**具体使用的 GPU 型号和数量（如 A100 或 V100 的具体个数），但从 1024 的 Batch Size 可以推断使用了多 GPU 并行训练。
*   **推理开销**：详细记录了在 Xavier 和 Nano 上的延迟（Latency）和吞吐量（Throughput）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从高 MACs 到低 MACs 的多个区间，验证了框架在不同资源约束下的泛化能力。
*   **消融实验**：分别验证了 DIC 模块和 CS 模块的独立贡献，证明了两者结合的优越性。
*   **充分性评价**：实验设计较为充分，不仅有理论上的 MACs 减少，还有真实硬件上的性能实测。对比了多种不同类型的 SOTA 方法，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **精度与效率双赢**：在 ImageNet-1K 上，Smart Scissor 使 ResNet50 的计算量减少 41.5%，同时 Top-1 准确率反而提升了 0.3%。
*   **显著优于 SOTA**：在相同计算预算下，比主流压缩框架 HRank 的准确率高出 4.1%。
*   **硬件友好**：在 Jetson Nano 等低功耗设备上，该方法在提升精度的同时，显著降低了推理延迟并提高了吞吐量。

### 7. 优点（亮点）
*   **实例感知（Instance-aware）**：不同于传统的静态裁剪，DIC 能根据图像内容动态调整裁剪区域，保留关键特征。
*   **多维度耦合**：首次将图像空间冗余消除与模型三维（深、宽、分辨率）缩减结合，寻找全局最优解。
*   **低开销预测**：前景预测器的开销极小（0.09B MACs），其带来的计算负担远低于通过裁剪图像节省下来的计算量。

### 8. 不足与局限
*   **训练流程复杂**：需要先运行 Grad-CAM 生成标签，再训练预测器，最后微调主网络，步骤较多。
*   **任务局限性**：目前主要针对图像分类任务。对于目标检测或分割任务，裁剪背景可能会误删其他目标或上下文信息，适用性有待验证。
*   **超参数依赖**：显著性阈值 $t$ 的选择对最终效果有影响，虽然文中给出了经验值 0.5，但在不同领域的数据集上可能需要重新搜索。

（完）
