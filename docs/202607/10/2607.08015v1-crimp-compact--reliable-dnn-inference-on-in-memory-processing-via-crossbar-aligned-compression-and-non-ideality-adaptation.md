---
title: "CRIMP: Compact & Reliable DNN Inference on In-Memory Processing via Crossbar-Aligned Compression and Non-ideality Adaptation"
title_zh: CRIMP：通过交叉棒对齐压缩与非理想性自适应实现存内计算上紧凑且可靠的 DNN 推理
authors: "Shuo Huai, Hao Kong, Xiangzhong Luo, Shiqing Li, Ravi Subramaniam, Christian Makaya, Qian Lin, Weichen Liu"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08015v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对内存处理的硬件友好型DNN自动设计
tldr: 针对存内计算（IMP）加速器面临的浮点运算不兼容、剪枝对齐开销大及器件非理想性导致精度下降等挑战，本文提出CRIMP框架。该框架通过复用移位单元实现纯整数推理，采用跨阵列对齐的卷积核组与阵列剪枝减少硬件开销，并引入运行时感知的非理想性自适应训练。实验表明，CRIMP在无额外硬件开销下，实现了高压缩率与高可靠性的DNN部署。
selection_source: fresh_fetch
motivation: 旨在解决存内计算加速器在浮点缩放因子处理、剪枝数据对齐以及器件非理想性方面存在的硬件开销与精度损失问题。
method: 提出一种集成纯整数化量化、跨阵列对齐剪枝和非理想性自适应训练的统一学习框架。
result: "在CIFAR-10数据集上，VGG-16和ResNet-56模型的精度损失分别仅为2.19%和1.26%，且实现了更高的稀疏度。"
conclusion: CRIMP框架通过协同优化压缩与鲁棒性，为在存内计算架构上实现高效、可靠的纯整数DNN推理提供了有效方案。
---

## 摘要
基于交叉棒（Crossbar）的存内计算（IMP）加速器为深度神经网络（DNN）实现了高速、低功耗计算，但面临三个障碍。首先，浮点（FP）运算与交叉棒不兼容，现有的量化方案仍需要浮点处理器来处理缩放因子，从而产生硬件开销。其次，冗余的 DNN 参数占用了过多的交叉棒，而目前的 IMP 感知剪枝方法需要跨交叉棒的数据对齐，引入了显著的存储和计算开销。第三，交叉棒的非理想行为（如写入波动）会降低部署模型的准确率，而现有的补偿方法会增加大量开销。在本文中，我们在单一训练过程中解决了这三个问题。我们复用了交叉棒中的位移单元来近似乘以缩放因子，从而避免了浮点处理器。我们应用卷积核组剪枝和交叉棒剪枝来移除数据对齐所需的硬件单元。我们采用运行时感知的非理想性自适应，通过利用交叉棒特性在训练阶段减轻器件非理想性的影响。将这三项优化整合到一个综合学习框架中，减少了训练开销并提高了准确率。实验表明，我们的量化带来的准确率下降可以忽略不计，且我们的剪枝方法比现有最先进方法实现了更高的稀疏度和准确率。我们的框架在 IMP 加速器上为 CIFAR-10 生成了纯整数、已剪枝且可靠的 VGG-16 和 ResNet-56 模型，在没有硬件开销的情况下，准确率下降分别仅为 2.19% 和 1.26%。

## Abstract
Crossbar-based In-Memory Processing (IMP) accelerators achieve high-speed, low-power computing for deep neural networks (DNNs), but face three obstacles. First, floating-point (FP) arithmetic is incompatible with crossbars, and existing quantization schemes still require FP processors for scaling factors, incurring hardware overhead. Second, redundant DNN parameters occupy too many crossbars, and current IMP-aware pruning methods require data aligning across crossbars, introducing significant memory and computing overhead. Third, non-ideal crossbar behaviors such as write variations degrade the accuracy of deployed models, and existing compensation methods add substantial overhead. In this paper, we address all three problems within a single training process. We reuse bit-shift units in crossbars to approximately multiply scaling factors, avoiding FP processors. We apply kernel-group pruning and crossbar pruning to remove the hardware units needed for data aligning. We adopt runtime-aware non-ideality adaptation to relieve the impact of device non-ideality from the training stage by exploiting crossbar features. Integrating these three optimizations into one comprehensive learning framework reduces training overhead and improves accuracy. Experiments show that our quantization incurs a negligible accuracy drop, and our pruning achieves higher sparsity and accuracy than state-of-the-art methods. Our framework produces integer-only, pruned, and reliable VGG-16 and ResNet-56 models for CIFAR-10 on IMP accelerators, with accuracy drops of only 2.19% and 1.26%, respectively, without hardware overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《CRIMP: Compact & Reliable DNN Inference on In-Memory Processing via Crossbar-Aligned Compression and Non-ideality Adaptation》** 的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文旨在解决深度神经网络（DNN）在基于 ReRAM（阻变存储器）的**存内计算（IMP）加速器**上部署时面临的三大瓶颈：
*   **硬件不兼容性**：传统的量化方法虽然减少了位宽，但仍需浮点（FP）处理器来处理缩放因子，这在交叉棒（Crossbar）架构中会产生约 7% 的功耗和 9% 的面积开销。
*   **剪枝对齐开销**：现有的 IMP 感知剪枝（如细粒度剪枝）需要额外的硬件单元（如稀疏表 ST）来对齐数据，这严重降低了硬件的集成密度。
*   **器件非理想性**：ReRAM 器件存在写入波动（WV）和粘滞故障（SAF），导致预训练模型在推理时精度大幅下降。
**核心目标**：在不引入额外硬件开销的前提下，实现一个紧凑、纯整数运算且对器件误差具有鲁棒性的 DNN 部署框架。

### 2. 方法论：核心思想与关键技术
CRIMP 提出了一个集成的学习框架，包含以下三个核心技术：
*   **纯整数化量化（Integer-Only Quantization）**：
    *   核心思想是消除浮点缩放因子。通过将缩放因子强制约束为 **2 的幂次（$2^n$）**，从而在硬件端直接复用交叉棒现有的“移位累加（Shift-and-Add）”单元来完成乘法操作，彻底移除浮点乘法器。
*   **跨阵列对齐剪枝（Crossbar-Aligned Pruning）**：
    *   采用多粒度剪枝策略：**卷积核组剪枝**（确保剩余卷积核数量是交叉棒宽度的整数倍）和**交叉棒块剪枝**（以整个交叉棒阵列为单位进行剔除）。这种设计保证了剪枝后的模型结构与硬件物理阵列天然对齐，无需任何数据对齐硬件。
*   **运行时感知非理想性自适应（Runtime-aware Non-ideality Adaptation）**：
    *   开发了一个真实的运行时模拟器，将 ReRAM 的非理想行为（如 lognormal 分布的写入波动）引入训练的向前传播中。
    *   **关键发现**：推导出交叉棒高度 $H$ 与误差期望的关系。发现当 $H$ 较小时，ADC 的舍入（Round）功能可以自动抵消部分物理误差。
*   **综合学习框架与动态零恢复（Zero-recovery）**：
    *   将量化、剪枝和误差自适应整合进同一个训练流程。引入“零恢复”机制，允许在训练过程中被暂时屏蔽的权重通过动量更新重新激活，从而探索更优的模型架构和权重分布。

### 3. 实验设计
*   **数据集**：MNIST、CIFAR-10、ImageNet。
*   **模型架构**：LeNet-5、VGG-16、ResNet-56、ResNet-18。
*   **Benchmark（对比方法）**：
    *   **量化对比**：IAO（Google 的纯整数推理方案）。
    *   **剪枝对比**：LSRR、CSAO、SPRC、XBA、PIM-Prune 等 SOTA 存内计算剪枝方法。
    *   **可靠性对比**：R-V-W（读-校验-写）、AIIR、CorrectNet、DRD、CTSF 等针对 ReRAM 误差的补偿方案。
*   **硬件模拟**：基于 ISAAC 架构，使用 NeuroSim 进行电路级仿真，CACTI 建模存储开销。

### 4. 资源与算力
*   **软件环境**：使用 PyTorch 框架实现。
*   **硬件模拟参数**：设定交叉棒尺寸为 $128 \times 128$，每个单元存储 2-bit。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。但提到该框架将三种优化整合在一次训练过程中，旨在降低整体的训练开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单（MNIST）到复杂（ImageNet）的多个任务，实验设计较为全面。
*   **消融实验**：详细探讨了交叉棒高度 $H$、剪枝比例、非理想性程度（$\epsilon$）对精度的交叉影响，验证了各组件的有效性。
*   **客观性**：通过 20 次推理取平均值的方式处理随机的物理波动误差，确保了结果的统计客观性。对比实验中考虑了硬件面积和功耗的实际物理参数，而非仅对比算法精度。

### 6. 主要结论与发现
*   **硬件效率**：相比原始模型，CRIMP 平均减少了 **122.38倍** 的计算功耗和 **19.51倍** 的计算面积。
*   **精度表现**：在 CIFAR-10 上，VGG-16 和 ResNet-56 的精度下降仅为 2.19% 和 1.26%，且实现了极高的稀疏度。
*   **可靠性**：通过运行时感知训练，模型在存在严重写入波动（$\epsilon=0.5$）的情况下仍能保持较高的推理精度，远优于未适配的模型。
*   **硬件发现**：理论证明了减小交叉棒高度或利用 ADC 舍入特性可以增强模型对物理噪声的容忍度。

### 7. 优点（亮点）
*   **真正的零硬件开销**：通过算法层面的约束（2 的幂次缩放、对齐剪枝），完全消除了对浮点处理器和对齐电路的需求。
*   **协同优化**：将压缩与鲁棒性训练结合，利用剪枝减少受误差影响的权重数量，从而间接提升了可靠性。
*   **理论支撑**：对交叉棒物理特性与数学误差模型进行了深入的理论推导，而非单纯的经验性实验。

### 8. 不足与局限
*   **ImageNet 表现**：在 ImageNet 等大规模数据集上，虽然优于 SOTA，但 50.5% 的稀疏度相比小数据集仍有提升空间，且精度损失（0.99%）在严苛场景下仍需关注。
*   **应用限制**：该方法高度依赖于对 ReRAM 物理特性的准确建模，若实际硬件的误差分布偏离 lognormal 模型，自适应效果可能会打折扣。
*   **训练复杂度**：虽然减少了硬件开销，但将多种约束引入训练过程（尤其是动态零恢复机制）可能会增加训练的收敛难度和超参数调优成本。

（完）
