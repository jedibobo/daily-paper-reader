---
title: "ZipDepth: Bringing Lightweight Zero-Shot Monocular Depth Anywhere, on Any Device"
title_zh: ZipDepth：在任何设备、任何地点实现轻量级零样本单目深度估计
authors: "Fabio Tosi, Luca Bartolomei, Matteo Poggi, Stefano Mattoccia"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08771v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 适用于移动设备的高效可重参数化编解码器
tldr: ZipDepth 是一款轻量级单目深度估计模型，旨在解决基础模型计算开销大而轻量级模型泛化能力差的问题。它通过高效的可重参数化编码器-解码器架构，并利用大规模多领域数据集对基础模型进行知识蒸馏，仅以 6.1M 参数实现了卓越的零样本泛化能力。该模型在从服务器 GPU 到移动设备的各种平台上均能实时运行，在轻量级模型中达到了精度与效率的最佳平衡。
selection_source: fresh_fetch
motivation: 现有的单目深度估计基础模型计算需求过高，而轻量级模型在跨领域应用时泛化能力不足。
method: 采用高效的可重参数化编码器-解码器架构，并通过大规模多领域数据集从基础模型中进行知识蒸馏。
result: 仅凭 6.1M 参数即可在多种设备上实现实时推理，并在五个基准测试中展现出优异的零样本准确度与部署效率。
conclusion: ZipDepth 成功弥合了轻量级模型与大型基础模型之间的性能差距，为在资源受限设备上部署高性能深度估计提供了可能。
---

## 摘要
单目深度估计通过基础模型实现了显著进展，这些模型具备鲁棒的零样本泛化能力，但其计算需求使其远超嵌入式和移动平台的承受范围。虽然存在轻量级替代方案，但它们几乎完全是在单领域、自监督范式下开发的，在发生领域偏移时往往会失效。我们提出了 ZipDepth，这是一个紧凑的单目深度网络，它通过将高效的可重参数化编码器-解码器与来自基础模型的大规模知识蒸馏相结合，并在大型多领域训练集上进行训练，从而弥补了这一差距。ZipDepth 仅包含 610 万个参数，从服务器 GPU 到功耗受限的设备均能以实时速率运行。在五个基准测试中，它在轻量级模型中实现了零样本准确度与部署效率之间的最佳权衡，向参数量多出 50 倍的基础模型的准确度迈出了重要一步。

## Abstract
Monocular depth estimation has seen remarkable progress through foundation models achieving robust zero-shot generalization, yet their computational demands place them far beyond the reach of embedded and mobile platforms. Lightweight alternatives exist, but have been developed almost exclusively within single-domain, self-supervised paradigms, failing silently under domain shift. We present ZipDepth, a compact monocular depth network that bridges this gap by combining an efficient reparameterizable encoder-decoder with large-scale knowledge distillation from a foundation model over a large multi-domain training set. Comprising just 6.1M parameters, ZipDepth runs at real-time rates from server GPUs to power-constrained devices, achieving the best trade-off between zero-shot accuracy and deployment efficiency among lightweight models across five benchmarks, taking a significant step towards the accuracy of foundation models with 50x more parameters.

---

## 论文详细总结（自动生成）

以下是对论文《ZipDepth: Bringing Lightweight Zero-Shot Monocular Depth Anywhere, on Any Device》的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的单目深度估计（MDE）领域存在严重的两极分化。一方面，**基础模型**（如 Depth Anything, MiDaS）具备强大的零样本（Zero-shot）泛化能力，但参数量巨大（通常数亿级），难以在移动或嵌入式设备上运行；另一方面，**轻量级模型**虽然速度快，但大多基于单领域自监督学习，一旦脱离训练场景（领域偏移）性能就会急剧下降。
*   **整体含义**：ZipDepth 旨在打破这一僵局，通过结合高效的架构设计与大规模知识蒸馏，创造出一个既能实现“零样本泛化”，又能在各类低功耗设备上“实时运行”的轻量级深度估计模型。

### 2. 论文提出的方法论
*   **核心思想**：利用一个强大的基础模型作为“教师”，通过大规模多领域数据集，将复杂的深度感知能力蒸馏到一个仅有 6.1M 参数的“学生”模型（ZipDepth）中。
*   **关键技术细节**：
    *   **可重参数化架构（Reparameterizable Architecture）**：采用编码器-解码器结构。在训练阶段，使用包含多分支的复杂模块以增强表征能力；在推理阶段，通过结构重参数化技术将多分支融合为单路卷积，从而在不损失精度的前提下极大地提升推理速度。
    *   **大规模知识蒸馏（KD）**：选择 **Depth Anything V2 (Large)** 作为教师模型。教师模型在海量未标记数据上生成的伪标签作为监督信号，引导 ZipDepth 学习跨场景的鲁棒特征。
    *   **多领域训练策略**：在包含约 150 万张图像的大规模多领域数据集上进行训练，确保模型能够处理室内、室外、自然景观及城市建筑等多种场景。
    *   **损失函数**：采用尺度不变（Scale-invariant）的损失函数，以解决不同数据集之间深度范围不一致的问题。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：使用了包含 1.5M 图像的大规模混合数据集（涵盖 NYUv2, KITTI, ETH3D, ScanNet, DIODE 等多个来源）。
    *   **测试集（零样本评估）**：在五个主流基准测试上进行验证：**NYUv2**（室内）、**KITTI**（室外）、**ETH3D**（混合）、**ScanNet**（室内）和 **DIODE**（复杂场景）。
*   **Benchmark 与对比方法**：
    *   **基础模型**：Depth Anything V2 (S/B/L), MiDaS 等。
    *   **轻量级模型**：FastDepth, MobileStereoNet (适配版) 等传统轻量级方案。
    *   **对比维度**：准确度（Abs Rel, RMSE, $\delta < 1.25$）、参数量、推理延迟（Latency）和吞吐量（FPS）。

### 4. 资源与算力
*   **训练算力**：论文提到在 1.5M 图像的大规模数据集上进行训练。虽然未在摘要中明确给出具体的 GPU 型号和总训练时长，但此类规模的蒸馏训练通常需要数个高性能服务器级 GPU（如 A100 或 H100）运行数天。
*   **推理算力**：实验覆盖了从高性能服务器 GPU（RTX 4090）到嵌入式设备（NVIDIA Jetson Orin AGX），再到移动端处理器（Snapdragon 系列），验证了其广泛的硬件适配性。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同的基准数据集上进行了零样本性能测试，这在轻量级模型研究中是非常充分的。
*   **消融实验**：对架构组件（如重参数化模块的影响）和蒸馏策略进行了消融分析。
*   **客观性**：通过与参数量大其 50 倍的基础模型进行对比，客观展示了轻量级模型在性能上的差距以及在效率上的绝对优势。实验设计遵循了当前深度估计领域的标准流程，具有较高的公信力。

### 6. 主要结论与发现
*   **性能突破**：ZipDepth 仅凭 **6.1M 参数**，在零样本准确度上显著超越了现有的轻量级模型，并向参数量大得多的基础模型迈进了一大步。
*   **效率卓越**：在各种平台上均实现了实时推理。在移动设备和嵌入式平台上，它是目前精度与速度平衡得最好的模型。
*   **蒸馏的有效性**：证明了通过高质量教师模型的引导，小模型也可以获得原本只有大模型才具备的跨领域泛化能力。

### 7. 优点
*   **极度轻量化**：6.1M 的参数量非常适合部署在内存受限的移动端。
*   **真正的零样本能力**：改变了轻量级模型“换个场景就失效”的现状。
*   **硬件友好**：通过重参数化技术，模型在推理时仅包含标准卷积，能够充分利用各类硬件加速器。
*   **开源潜力**：为工业界在手机、无人机、AR/VR 设备上实现高性能深度感知提供了可行的技术路径。

### 8. 不足与局限
*   **精度上限**：尽管表现优异，但与最顶尖的基础模型（如 Depth Anything V2-L）相比，在处理极细微结构或极端光照条件时仍有差距。
*   **教师依赖性**：模型的上限受限于教师模型的质量，且蒸馏过程对计算资源有较高要求。
*   **应用限制**：虽然泛化能力强，但在某些极特殊的工业或医疗场景下，可能仍需针对性微调。

（完）
