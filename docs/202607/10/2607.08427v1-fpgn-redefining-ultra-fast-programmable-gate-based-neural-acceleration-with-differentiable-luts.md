---
title: "FPGN: Redefining Ultra-Fast Programmable Gate-based Neural Acceleration with Differentiable LUTs"
title_zh: FPGN：利用可微分查找表重新定义超快速可编程门级神经加速
authors: "Jiawei Liang, Haotong Qin, Linfeng Du, Xingyu Liu, Shangkun Li, Hui Yu, Michele Magno, Xinyu Chen, Jiang Xu, Wei Zhang"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08427v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对FPGA加速器的可微分查找表神经架构搜索
tldr: 本研究针对超低延迟应用，提出了FPGN框架，旨在解决LUT原生神经网络在FPGA实现中的硬件不匹配和布线难题。FPGN通过硬件对齐的可微分LUT神经元训练、结构化的流式硬件架构以及延迟驱动的自动化编译流程，实现了从算法到硬件的高效映射。该框架填补了LUT原生学习与高性能FPGA部署之间的空白，在保持准确率的同时显著提升了推理速度和资源利用率。
selection_source: fresh_fetch
motivation: 现有LUT原生神经网络在算法优化与FPGA硬件实现之间存在鸿沟，导致难以发挥其在超低延迟推理中的潜力。
method: 提出FPGN框架，包含硬件对齐的可微分LUT神经元训练、结构化流式硬件架构以及延迟驱动的自动化编译流程。
result: 相比代表性BNN加速器，FPGN实现了高达205倍的延迟降低，且LUT效率比以往LUT原生网络提高30倍。
conclusion: FPGN通过软硬件协同设计，成功将LUT原生学习转化为高性能FPGA加速器，为超低延迟神经网络推理提供了新范式。
---

## 摘要
为深度神经网络（DNN）实现纳秒级推理延迟已成为延迟敏感型应用的主要架构关注点。虽然现场可编程门阵列（FPGA）为低延迟推理提供了极具前景的底层架构，但传统的 FPGA 加速器仍以算术运算为中心，主要将查找表（LUT）用作数值算子和外围逻辑的构建模块。相比之下，近期的原生 LUT 神经网络将 LUT 视为可学习的神经元，展现出利用其内在逻辑表达能力的巨大理论潜力。然而，现有方法大多局限于算法优化，未能将这种理论潜力转化为高性能的 FPGA 加速器。具体而言，它们的可微分公式无法忠实匹配 FPGA 的 LUT 原语，其物理无关的拓扑结构损害了可布线性和时序收敛，且缺乏自动化的优化流程，阻碍了系统的设计空间探索（DSE）和高效的硬件实现。在本文中，我们提出了 FPGN，这是一个端到端的物理感知框架，旨在弥合原生 LUT 学习与延迟优化的 FPGA 实现之间的差距。FPGN 通过以下方式解决这些挑战：(i) 一种用于训练 FPGA 原生 LUT 神经元的硬件对齐可微分公式；(ii) 一种具有流式硬件架构的结构化原生 LUT 拓扑，以提高布线局部性和时序收敛；(iii) 一个延迟驱动的编译器，利用高保真分析结果质量（QoR）模型来自动化 DSE 和硬件生成。实验表明，与代表性的基于 FPGA 的 BNN 加速器相比，FPGN 实现了高达 205 倍的延迟降低，且与之前的可微分原生 LUT 网络相比，LUT 效率提高了 30 倍，同时保持了具有竞争力的推理准确率。

## Abstract
Achieving nanosecond-scale inference latency for deep neural networks (DNNs) has become a primary architectural concern for latency-critical applications. While Field-Programmable Gate Arrays (FPGAs) offer a promising substrate for low-latency inference, conventional FPGA accelerators remain arithmetic-centric, using LUTs primarily as building blocks for numerical operators and peripheral logic. In contrast, recent LUT-native neural networks treat LUTs as learnable neurons, revealing promising theoretical potential to exploit their intrinsic logic expressivity. However, existing methods are largely confined to algorithmic optimizations, failing to translate this theoretical potential into high-performance FPGA accelerators. Specifically, their differentiable formulations do not faithfully match FPGA LUT primitives, their physically-unaware topologies compromise routability and timing closure, and their lack of automated optimization flow hinders systematic design space exploration (DSE) and efficient hardware implementation.   In this paper, we propose FPGN, an end-to-end physically-aware framework that closes the gap between LUT-native learning and latency-optimized FPGA implementation. FPGN addresses these challenges through (i) a hardware-aligned differentiable formulation for training FPGA-native LUT neurons, (ii) a structured LUT-native topology with a streaming hardware architecture to improve routing locality and timing closure, and (iii) a latency-driven compiler that leverages high-fidelity analytical Quality of Results models to automate DSE and hardware generation. Experiments show that FPGN achieves up to 205$\times$ latency reduction compared to representative FPGA-based BNN accelerators and up to 30$\times$ higher LUT efficiency than prior differentiable LUT-native networks, while maintaining competitive inference accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：FPGN——利用可微分查找表重新定义超快速可编程门级神经加速

#### 1. 核心问题与整体含义（研究动机和背景）
在诸如高能物理触发器、高频交易和高速网络数据包分类等应用中，深度神经网络（DNN）的推理延迟需要达到**纳秒级**。虽然 FPGA 具有极高的灵活性，但传统加速器（如 BNN）仍以“算术运算为中心”，将查找表（LUT）仅视为构建加法器或 XNOR 门的工具，未能充分发挥 LUT 作为通用布尔逻辑单元的表达能力。
**核心挑战：**
*   **训练与硬件不匹配：** 离散的 LUT 逻辑难以直接进行梯度下降训练。
*   **物理实现难题：** 现有的 LUT 原生网络拓扑结构随机，导致 FPGA 内部布线拥塞、时序难以收敛。
*   **缺乏自动化流程：** 缺乏系统性的设计空间探索（DSE）工具来平衡资源与延迟。

#### 2. 论文提出的方法论
FPGN 提出了一个从算法训练到硬件生成的全栈协同设计框架：
*   **硬件对齐的可微分训练：**
    *   **可微分 LUT 公式：** 引入“可微分相等指示器（Differentiable Equality Indicator）”，将 LUT 的离散寻址逻辑松弛为连续的加权组合，使梯度能流向所有 LUT 条目。
    *   **三阶段优化策略：** 采用“连续松弛训练 -> 渐进二值化 -> 二值微调”的策略，并配合双峰高斯初始化，解决梯度稀疏和量化误差问题。
*   **物理感知拓扑与流式架构：**
    *   **结构化微拓扑：** 定义了 LUT-Vector（并行处理）和 LUT-Tree（对数级深度归约）单元，通过“顺序扁平化”和“局部感知填充”确保布线的局部性。
    *   **全流式硬件：** 采用可配置的并行展开（Unrolling）和自适应流水线，利用“平稳窗口循环行缓存”消除复杂的选择逻辑（MUX），降低时序压力。
*   **延迟驱动的自动化编译器：**
    *   建立高保真分析模型（QoR），通过启发式搜索与混合整数线性规划（MILP）自动寻找最优的展开因子，生成 RTL 代码。

#### 3. 实验设计
*   **数据集/场景：** 图像分类（CIFAR-10, SVHN）、语音关键词识别（KWS）、高能物理喷注分类（JSC-OpenML, JSC-CERNBox）。
*   **Benchmark（基准方法）：**
    *   **算术中心类：** AMD 官方 BNN 框架 FINN。
    *   **LUT 算子类：** LUTNet。
    *   **LUT 神经元类：** DWN, PolyLUT, NeuraLUT, AmigoLUT 等。
*   **硬件平台：** AMD Versal Premium VP1902 和 Virtex UltraScale+ VU9P。

#### 4. 资源与算力
*   **训练算力：** 使用了 **NVIDIA A30 GPU** 进行模型训练。
*   **训练细节：** 论文提到模型是从头开始训练的（Train from scratch），但未明确说明每个模型的具体训练时长（小时数）。
*   **硬件资源：** 实验详细记录了 FPGA 上的 LUT、寄存器（FF）、BRAM 和功耗数据。

#### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了从小型 MLP 到中型 CNN（FPGN-3/6）的多种架构，并在 5 个不同领域的数据集上进行了验证。
*   **消融实验：** 针对训练策略（三阶段 vs. 直接二值化）、初始化方法（不同均值 $\mu$）、拓扑结构（结构化 vs. 随机布线）以及编译器保真度做了深入的对比实验。
*   **客观性：** 实验通过在相同硬件平台上重新实现 FINN 等方法进行归一化对比，确保了对比的公平性。

#### 6. 主要结论与发现
*   **极致延迟：** 在 CIFAR-10 上实现了 **658 ns** 的推理延迟，比 FINN 快 **205 倍**。
*   **高效率：** 相比 LUTNet，LUT 利用效率提升了 **30 倍**；相比 FINN，能量效率（kFPS/W）提升了 **7.31 倍**。
*   **物理优势：** 结构化拓扑使时钟频率（$F_{max}$）显著提升（例如从 298MHz 提升至 526MHz），且布线长度减少了 3.5 倍。
*   **零 DSP/BRAM：** 该架构完全不使用 DSP 和 BRAM 存储权重，参数直接固化在 LUT 配置中。

#### 7. 优点
*   **全栈协同：** 真正打通了“可微分逻辑算法”与“FPGA 物理布线约束”之间的鸿沟。
*   **物理感知：** 意识到随机连接在硬件上的不可行性，通过结构化设计保证了高频运行。
*   **自动化：** 提供的编译器能自动处理复杂的资源-延迟权衡，降低了使用门槛。

#### 8. 不足与局限
*   **准确率权衡：** 虽然比 BNN 强，但与全精度（FP32）模型相比仍有一定准确率差距（如 CIFAR-10 约为 83% vs 91%）。
*   **模型规模限制：** 目前主要针对中小型网络，对于超大规模模型（如 Transformer）的适用性尚待进一步研究（文中提到这是未来方向）。
*   **任务局限：** 实验主要集中在分类任务，尚未在回归或生成任务上广泛验证。

（完）
