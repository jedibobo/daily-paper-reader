---
title: "EdgeCompress: Coupling Multidimensional Model Compression and Dynamic Inference for EdgeAI"
title_zh: EdgeCompress：面向边缘人工智能的多维模型压缩与动态推理耦合框架
authors: "Hao Kong, Di Liu, Shuo Huai, Xiangzhong Luo, Ravi Subramaniam, Christian Makaya, Qian Lin, Weichen Liu"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.06982v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对资源受限嵌入式设备的多维模型压缩与动态推理
tldr: 针对嵌入式设备上CNN计算开销大的挑战，本文提出EdgeCompress框架。该框架通过动态图像裁剪（DIC）去除背景冗余，并利用复合缩减（CS）从深度、宽度和分辨率多维度压缩模型。此外，引入动态推理机制，根据图像识别难度级联不同复杂度的模型。实验表明，该方法在显著降低ResNet-50计算量的同时提升了精度，性能优于现有主流压缩框架，有效推动了先进CNN在边缘硬件上的部署。
selection_source: fresh_fetch
motivation: 卷积神经网络高昂的计算成本阻碍了其在资源受限的嵌入式设备上的广泛应用。
method: 耦合了动态图像裁剪、多维度复合缩减以及基于模型级联的动态推理框架，从输入数据和网络结构两方面消除冗余。
result: "在ImageNet-1K上，ResNet-50的计算量减少了48.8%且Top-1准确率提升了0.8%，在同等计算量下比HRank准确率高出4.1%。"
conclusion: EdgeCompress通过多维度压缩与动态推理的协同优化，显著提升了CNN在边缘AI场景下的推理效率与模型性能。
---

## 摘要
卷积神经网络（CNN）在图像分类任务中取得了令人鼓舞的结果。然而，CNN 极高的计算成本阻碍了其在资源受限的嵌入式设备上的部署。为了解决这一问题，我们提出了 EdgeCompress，一个旨在降低 CNN 计算开销的综合压缩框架。在 EdgeCompress 中，我们首先引入了动态图像裁剪（DIC），设计了一个轻量级的前景预测器，以准确裁剪输入图像中信息最丰富的前景对象进行推理，从而避免了背景区域的冗余计算。随后，我们提出了复合收缩（CS），根据 CNN 的三个维度（深度、宽度和分辨率）对准确率和模型计算量的贡献，协同压缩这些维度。DIC 和 CS 共同构成了一个多维 CNN 压缩框架，能够全面减少输入图像和神经网络架构中的计算冗余，从而提高 CNN 的推理效率。此外，我们提出了一个动态推理框架，以高效处理具有不同识别难度的输入图像。在该框架中，我们级联了来自压缩框架的多个不同复杂度的模型，并针对不同的输入图像动态采用不同的模型，这进一步压缩了计算冗余并提高了 CNN 的推理效率，促进了先进 CNN 在嵌入式硬件上的部署。在 ImageNet-1K 上的实验表明，EdgeCompress 在将 ResNet-50 的计算量减少 48.8% 的同时，将 Top-1 准确率提高了 0.8%。同时，与最先进的压缩框架 HRank 相比，在计算量相似的情况下，我们将准确率提高了 4.1%。源代码和模型可在 https://github.com/ntuliuteam/edge-compress 获取。

## Abstract
Convolutional neural networks (CNNs) have demonstrated encouraging results in image classification tasks. However, the prohibitive computational cost of CNNs hinders the deployment of CNNs onto resource-constrained embedded devices. To address this issue, we propose EdgeCompress, a comprehensive compression framework to reduce the computational overhead of CNNs. In EdgeCompress, we first introduce dynamic image cropping (DIC), where we design a lightweight foreground predictor to accurately crop the most informative foreground object of input images for inference, which avoids redundant computation on background regions. Subsequently, we present compound shrinking (CS) to collaboratively compress the three dimensions (depth, width, and resolution) of CNNs according to their contribution to accuracy and model computation. DIC and CS together constitute a multidimensional CNN compression framework, which is able to comprehensively reduce the computational redundancy in both input images and neural network architectures, thereby improving the inference efficiency of CNNs. Further, we present a dynamic inference framework to efficiently process input images with different recognition difficulties, where we cascade multiple models with different complexities from our compression framework and dynamically adopt different models for different input images, which further compresses the computational redundancy and improves the inference efficiency of CNNs, facilitating the deployment of advanced CNNs onto embedded hardware. Experiments on ImageNet-1K demonstrate that EdgeCompress reduces the computation of ResNet-50 by 48.8% while improving the top-1 accuracy by 0.8%. Meanwhile, we improve the accuracy by 4.1% with similar computation compared to HRank, the state-of-the-art compression framework. The source code and models are available at https://github.com/ntuliuteam/edge-compress

---

## 论文详细总结（自动生成）

### 论文总结：EdgeCompress - 面向边缘人工智能的多维模型压缩与动态推理耦合框架

#### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何在资源受限的嵌入式设备（EdgeAI）上高效部署高精度的卷积神经网络（CNN）。
*   **研究背景**：现代 CNN 精度虽高，但计算开销（MACs）巨大。
*   **研究动机**：
    *   **空间冗余**：直接缩小图像分辨率会丢失小目标的特征，导致精度下降。
    *   **维度局限**：现有的剪枝方法通常只针对单一维度（如宽度或深度），压缩率有限。
    *   **静态推理**：传统方法对所有图像使用相同的模型，忽略了图像识别难度的差异（简单样本 vs. 困难样本）。

#### 2. 方法论：核心思想与关键技术
EdgeCompress 提出了一个两阶段的多维压缩与动态推理框架：

*   **动态图像裁剪 (DIC)**：
    *   **核心思想**：仅保留图像中最具信息量的前景区域，剔除背景冗余。
    *   **技术细节**：首先利用 Grad-CAM 在训练集上生成伪标签（Bounding Box）；随后训练一个极轻量级的前景预测器（仅 0.27M 参数），在推理时快速定位前景并进行裁剪。
*   **复合缩减 (CS)**：
    *   **核心思想**：协同压缩深度（Depth）、宽度（Width）和分辨率（Resolution）。
    *   **技术细节**：引入“维度精度估计器”，通过二次多项式拟合不同维度缩减对精度的影响，自动计算最优缩减系数 $C_s$，从而在给定计算预算下实现最优的维度组合。
*   **动态推理 (DI)**：
    *   **核心思想**：级联多个不同复杂度的子网络，实现样本自适应推理。
    *   **关键技术**：
        *   **终止指标**：提出“概率差值”（最高概率与次高概率之差 $D_N = I_N - I'_N$）作为置信度度量，比传统的最高概率指标更能区分分类正确性。
        *   **预测累加**：通过累加已执行模型的 Logits（输出值）来融合信息，在不增加计算量的前提下提升精度。

#### 3. 实验设计
*   **数据集**：CIFAR-10、CIFAR-100、ImageNet-100、ImageNet-1K。
*   **基准模型 (Backbones)**：ResNet-50、VGG-16 BN、RegNet-X。
*   **对比方法**：
    *   **静态压缩**：HRank、GAL、Taylor 剪枝、AutoPruner 等。
    *   **动态推理**：DR-ResNet、RANet、MSDNet、SkipNet 等。
*   **评估指标**：Top-1 准确率、MACs（计算量）、参数量、端到端延迟（Latency）、吞吐量（Throughput）。

#### 4. 资源与算力
*   **推理硬件**：NVIDIA AGX Xavier（11 TOPS）、Jetson Nano（0.5 TOPS）、Intel i7-9750H CPU。
*   **训练细节**：
    *   使用 SGD 优化器，Momentum 为 0.9。
    *   ImageNet 训练：Batch size 1024，初始学习率 2.0，训练 100 轮（Warmup 5 轮），随后使用 DIC 微调 20 轮。
    *   **算力说明**：文中未明确提及训练所用的具体 GPU 型号和数量，但从 Batch size 1024 推断，训练过程使用了高性能 GPU 集群。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从小型（CIFAR）到大型（ImageNet-1K）的数据集，实验设计非常全面。
*   **消融实验**：详细分析了 DIC、CS、DI 三个组件各自的贡献，验证了“概率差值”指标和“Logits 累加”策略的有效性。
*   **客观性**：在三种不同的硬件平台（高/低功耗 GPU 及 CPU）上测试了真实延迟和吞吐量，而非仅停留在理论计算量（FLOPs）的对比，实验结果具有较强的说服力和客观性。

#### 6. 论文的主要结论与发现
*   **精度与效率双赢**：在 ImageNet-1K 上，EdgeCompress 将 ResNet-50 的计算量减少了 48.8%，同时 Top-1 准确率反而提升了 0.8%。
*   **超越 SOTA**：在相同计算量下，比主流压缩框架 HRank 准确率高出 4.1%。
*   **动态优势**：动态推理机制使得大部分简单样本能在极小的子网络中快速退出，显著降低了平均推理延迟。

#### 7. 优点（亮点）
*   **多维度协同**：打破了单一维度压缩的限制，通过自动化的系数计算实现了深度、宽度、分辨率的最优权衡。
*   **轻量级预测器**：设计的前景预测器开销极低（0.09B MACs），使得图像裁剪带来的收益远大于其自身的计算成本。
*   **信息无损融合**：提出的 Logits 累加方法巧妙地利用了早期模型的计算结果，避免了特征图融合带来的额外开销。

#### 8. 不足与局限
*   **训练成本**：虽然推理高效，但该框架需要训练多个子网络，且需要先通过 Grad-CAM 生成伪标签，整体训练流程较为复杂且耗时。
*   **场景限制**：目前主要针对图像分类任务。在目标检测或分割任务中，前景可能包含多个目标，该轻量级预测器的有效性尚待验证。
*   **超参数依赖**：动态推理中的终止阈值 $D_0$ 和累加系数 $\alpha$ 需要根据具体应用场景进行手动或经验性调节。

（完）
