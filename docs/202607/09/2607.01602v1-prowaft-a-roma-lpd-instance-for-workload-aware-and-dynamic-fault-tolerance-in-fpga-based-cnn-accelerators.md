---
title: "ProWAFT: A ROMA-LPD Instance for Workload-Aware and Dynamic Fault Tolerance in FPGA-Based CNN Accelerators"
title_zh: ProWAFT：一种用于基于 FPGA 的 CNN 加速器中工作负载感知和动态容错的 ROMA-LPD 实例
authors: "Xinxin Chen, Haoran Qiao, Yiming Guo, Kecheng Luo, Siyuan Feng, Jingwen Ma"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01602v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对FPGA端CNN加速器的负载感知容错技术
tldr: ProWAFT是一个针对FPGA卷积神经网络加速器的前瞻性工作负载感知容错框架。针对SRAM型FPGA易受瞬态故障影响且传统冗余方案开销大的问题，该框架利用部分重构技术，根据工作负载重要性动态选择TMR应用范围。通过建模故障传播和重构开销，在延迟、能耗和可靠性之间取得平衡，显著降低了综合成本并保持了高吞吐量。
selection_source: fresh_fetch
motivation: 传统的全冗余方案在FPGA上能耗和性能开销巨大，而反应式恢复又会带来不可接受的延迟。
method: 提出ProWAFT框架，利用部分重构在不同分区选择性应用TMR，并基于工作负载关键性、故障传播和重构开销进行多目标优化决策。
result: 在ZCU104平台上的多种模型测试表明，ProWAFT在保持高任务成功率的同时，综合成本低于静态TMR和反应式重构。
conclusion: 该研究证明了结合工作负载感知与动态部分重构能有效提升FPGA加速器在资源受限环境下的可靠性与效率。
---

## 摘要
基于 SRAM 的 FPGA 为网络边缘受能耗和延迟限制的 CNN 推理提供了一个极具吸引力的平台，但瞬态故障可能导致静默错误，从而损害可靠性。常开冗余（例如全三模冗余 TMR）提高了正确性，但会产生巨大的性能和能耗开销，而反应式恢复可能会在关键路径上引入不可接受的延迟。我们提出了 ProWAFT，这是一种针对基于 FPGA 的 CNN 加速器的主动式工作负载感知容错框架，它利用局部重构在可重构分区中选择性地应用 TMR。ProWAFT 量化了工作负载的关键性，对故障传播和重构开销进行建模，并选择能够最小化延迟、能耗和可靠性风险综合目标的配置。在具有六个可重构区域的 Xilinx Zynq UltraScale+ ZCU104 平台上实现，并在源自 ResNet-18、MobileNetV2 和 EfficientNet-Lite 的 500 个任务轨迹（在随时间变化的单粒子翻转 SEU 注入下）上进行评估，ProWAFT 实现了比静态 TMR 和反应式重构更低的综合成本，同时保持了高任务成功率和接近基准的吞吐量，且在线决策开销较低。

## Abstract
SRAM-based FPGAs provide an attractive platform for energy- and latency-constrained CNN inference at the network edge, yet transient faults can lead to silent errors that compromise reliability. Always-on redundancy (e.g., full TMR) improves correctness but incurs substantial performance and energy overhead, while reactive recovery may introduce unacceptable latency on the critical path. We propose \textbf{ProWAFT}, a proactive workload-aware fault-tolerance framework for FPGA-based CNN accelerators that uses partial reconfiguration to selectively apply TMR across reconfigurable partitions. ProWAFT quantifies workload criticality, models fault propagation and reconfiguration overhead, and selects configurations that minimize a composite objective over latency, energy, and reliability risk. Implemented on a Xilinx Zynq UltraScale+ ZCU104 platform with six reconfigurable regions and evaluated on a 500-task trace derived from ResNet-18, MobileNetV2, and EfficientNet-Lite under time-varying SEU injection, ProWAFT achieves lower composite cost than static TMR and reactive reconfiguration while maintaining high task success rate and near-baseline throughput with low online decision overhead.

---

## 论文详细总结（自动生成）

以下是对论文《ProWAFT: A ROMA-LPD Instance for Workload-Aware and Dynamic Fault Tolerance in FPGA-Based CNN Accelerators》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在边缘计算场景下，基于 SRAM 的 FPGA 极易受到单粒子翻转（SEU）等瞬态故障的影响，导致静默数据损坏（SDC）。
*   **研究背景**：
    *   **传统方案的局限**：全三模冗余（Full TMR）虽然可靠，但会带来 3 倍以上的面积和功耗开销；而反应式恢复（发现错误再重构）会产生巨大的停机延迟，无法满足实时性要求。
    *   **需求**：需要一种能在资源受限（功耗、延迟）与高可靠性要求之间取得动态平衡的容错机制。

### 2. 方法论：ProWAFT 框架
ProWAFT 提出了一种**主动式、工作负载感知**的动态容错方案，核心思想是利用 FPGA 的**部分重构（Partial Reconfiguration, PR）**技术，根据当前任务的重要性动态调整冗余级别。
*   **核心思想**：将 FPGA 资源划分为多个可重构分区（RP），每个分区可以独立配置为“单模块（Simplex）”或“三模冗余（TMR）”模式。
*   **关键技术细节**：
    1.  **工作负载关键性量化**：通过分析 CNN 不同层对最终分类准确率的影响，量化各任务阶段的“关键性”。
    2.  **故障传播建模**：建立数学模型预测 SEU 故障在不同配置下的传播概率及其对系统可靠性的风险。
    3.  **多目标优化决策**：构建一个综合成本函数（Composite Objective），综合考虑**延迟（Latency）**、**能耗（Energy）**和**可靠性风险（Reliability Risk）**。
    4.  **动态调度**：在运行时，决策引擎根据预估的故障率和任务特征，选择最优的 PR 配置组合，以最小化综合成本。

### 3. 实验设计
*   **硬件平台**：Xilinx Zynq UltraScale+ MPSoC ZCU104 开发板。
*   **模型对象**：ResNet-18、MobileNetV2 和 EfficientNet-Lite。
*   **实验场景**：模拟了包含 500 个任务的轨迹，并在运行过程中注入随时间变化的 SEU 故障。
*   **对比基准（Benchmarks）**：
    *   **Baseline**：无保护模式。
    *   **Static TMR**：全时开启三模冗余。
    *   **Reactive Reconfiguration**：仅在检测到错误后才进行重构恢复。
    *   **ProWAFT**：本文提出的主动式动态方案。

### 4. 资源与算力
*   **硬件资源**：实验在 **ZCU104 FPGA** 上实现，划分为 6 个可重构区域。
*   **算力说明**：论文重点在于 FPGA 的推理阶段容错，未详细说明模型训练阶段的 GPU 算力（如型号、时长），因为其核心贡献在于推理侧的硬件架构与调度算法。在线决策引擎的开销被证明是极低的，不会成为性能瓶颈。

### 5. 实验数量与充分性
*   **实验规模**：使用了 500 个连续任务组成的测试集，涵盖了三种主流的轻量级与中等规模 CNN 模型。
*   **充分性评价**：实验设计较为充分。作者不仅对比了吞吐量和成功率，还通过消融实验验证了不同故障率下框架的适应性。通过在真实 FPGA 硬件上进行故障注入实验，保证了结果的客观性和说服力。

### 6. 主要结论与发现
*   **综合成本更低**：ProWAFT 在综合成本（延迟+能耗+风险）上显著优于静态 TMR 和反应式重构。
*   **高可靠性**：在保持高任务成功率的同时，其吞吐量接近无保护的基准测试水平。
*   **动态适应性**：实验证明 ProWAFT 能够根据环境故障率的变化，自动在“高性能/低功耗”和“高可靠性”模式之间平滑切换。

### 7. 优点与亮点
*   **主动性（Proactive）**：不同于传统的被动修复，ProWAFT 预判风险并提前调整配置，避免了关键路径上的突发延迟。
*   **细粒度控制**：利用 PR 技术实现分区级别的冗余控制，比粗粒度的系统级冗余更灵活。
*   **软硬协同**：结合了高层算法的关键性分析与底层硬件的重构特性，实现了跨层优化。

### 8. 不足与局限
*   **重构开销限制**：虽然是部分重构，但 PR 过程本身仍存在毫秒级的配置延迟，如果任务切换过于频繁，重构开销可能会抵消性能收益。
*   **模型依赖性**：关键性量化需要针对特定模型进行预分析，对于全新或动态生成的网络结构，可能需要额外的离线处理时间。
*   **硬件平台特定性**：该方案高度依赖支持 PR 特性的 Xilinx FPGA，在其他架构（如某些国产 FPGA 或 ASIC）上的迁移成本较高。

（完）
