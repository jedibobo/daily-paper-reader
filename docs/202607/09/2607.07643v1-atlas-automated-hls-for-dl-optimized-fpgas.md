---
title: "ATLAS: Automated HLS for DL-Optimized FPGAs"
title_zh: ATLAS：面向深度学习优化 FPGA 的自动化高层次综合
authors: "Ruthwik Reddy Sunketa, Aman Arora"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07643v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对深度学习优化FPGA的自动高层次综合及硬件微架构感知
tldr: ATLAS是一个全自动的高级综合（HLS）流程，旨在将深度学习模型直接部署到具有领域特定硬核（如Tensor Slices）的FPGA上。它解决了传统HLS工具无法原生支持自定义硬核的问题，通过将GEMM作为通用抽象层，实现了从PyTorch/Keras模型到硬件实现的自动化转换。该流程包含hls4ml-GEMM编译器前端和GEMM IP生成器后端，在显著提升开发效率的同时，充分发挥了底层硬件的加速性能。
selection_source: fresh_fetch
motivation: 现有的HLS工具无法原生支持FPGA中针对深度学习优化的自定义硬核，导致开发者仍需手动编写复杂的RTL代码或HLS黑盒调用，严重限制了生产力。
method: 提出ATLAS流程，利用GEMM作为抽象层，通过hls4ml-GEMM前端生成架构无关的C代码，并配合架构感知的GEMM IP生成器自动产生RTL封装、分块逻辑和控制状态机。
result: 在包括CNN、MLP和Transformer在内的11个深度学习设计上进行了验证，成功实现了从高层描述到集成Tensor Slices硬核的FPGA硬件自动化映射。
conclusion: ATLAS通过自动化集成领域特定硬核，消除了手动RTL设计的负担，为在新型FPGA架构上高效部署深度学习模型提供了端到端的解决方案。
---

## 摘要
FPGA 架构越来越多地集成领域特定的织物内硬核（in-fabric hardblocks）以加速深度学习（DL）推理，特别是占据 DL 计算主导地位的通用矩阵乘法（GEMM）。为了实现这些硬核带来的性能提升，通常需要手动进行 RTL 设计：程序员必须理解硬核的微架构，在 RTL 中实例化它们，并管理分块（tiling）和控制逻辑。虽然使用 C/C++ 编程和高层次综合（HLS）工具提高了 FPGA 工程师的抽象水平和生产力，但 HLS 工具原生并不支持为自定义硬核生成代码。先前的工作表明，HLS 工具中的黑盒机制可用于针对自定义硬核，但这仍需要在用户编写的 HLS C 代码中进行显式函数调用，并手动创建 RTL IP 库，这对于 DL 模型中的每一层都需要重复进行大量工作。此外，对于 DL 而言，为了提高可编程性和用户采纳度，更高级别的编程接口（例如 Pytorch/Keras 而非 C/C++）是更理想的。我们提出了 ATLAS，这是一个从高级 DL 模型描述到具有自定义织物内 DL 优化硬核的 FPGA 硬件实现的自动化流程，无需最终用户进行手动 RTL 设计或显式硬核实例化。我们的方法将 GEMM 作为通用抽象层，包含两个组件：(1) hls4ml-GEMM，一个将 DL 层转换为带有架构无关 GEMM 函数调用的 HLS C 代码的编译器前端；(2) GEMM IP 生成器，一个架构感知的后端，用于生成带有分块逻辑、控制状态机（FSM）和调度元数据的基于硬核的 RTL 封装。我们在 11 个 DL 设计上评估了该流程，包括独立的全连接层、卷积层和注意力层，以及完整的 CNN、MLP 和 Transformer 模型，目标是具有 Tensor Slices 的 FPGA 架构，并使用 Catapult 进行 HLS，使用 VTR 进行实现。

## Abstract
FPGA architectures increasingly incorporate domain-specific in-fabric hardblocks to accelerate DL inference, particularly GEMM, which dominates DL computation. To realize the performance gains of these hardblocks, manual RTL design is required: the programmer must understand the hardblock microarchitecture, instantiate them in RTL, and manage tiling and control logic. While programming in C/C++ and using HLS tools has increased the abstraction level and productivity of FPGA engineers, HLS tools do not support code generation for custom hardblocks natively. Prior work has demonstrated that blackbox mechanisms in HLS tools can be used to target custom hardblocks, but this still requires explicit function calls in user-written HLS C and manual creation of RTL IP libraries, significant effort that must be repeated for every layer in a DL model. Furthermore, for DL, an even high-level programming interface, e.g., Pytorch/Keras instead of C/C++, is desirable for improved programmability and user adoption.   We present ATLAS, a fully automated flow from a high-level DL model description to a hardware implementation on an FPGA with custom in-fabric DL-optimized hardblocks, requiring no manual RTL design or explicit hardblock instantiation from the end user. Our approach uses GEMM as a universal abstraction layer and comprises two components: (1) hls4ml-GEMM, a compiler frontend that transforms DL layers into HLS C code with architecture-agnostic GEMM function calls, and (2) a GEMM IP Generator, an architecture-aware backend that produces hardblock-based RTL wrappers with tiling logic, control FSMs, and scheduling metadata. We evaluate the flow across 11 DL designs, including individual fully connected, convolution, and attention layers, as well as full CNN, MLP, and Transformer models targeting an FPGA architecture with Tensor Slices using Catapult for HLS and VTR for implementation.

---

## 论文详细总结（自动生成）

### ATLAS：面向深度学习优化 FPGA 的自动化高层次综合总结

#### 1. 核心问题与整体含义（研究动机和背景）
现代 FPGA 架构（如 Intel Agilex、Tensor Slices 等）越来越多地集成专门用于加速深度学习（DL）的织物内硬核（Hardblocks），以解决传统 DSP 和 LUT 在处理矩阵乘法（GEMM）时的效率瓶颈。然而，现有的高层次综合（HLS）工具（如 Vitis HLS、Catapult）无法原生支持这些自定义硬核，导致开发者必须手动编写复杂的 RTL 代码或在 HLS 中手动插入黑盒调用。这不仅要求开发者具备深厚的硬件底层知识，且在处理复杂模型时极易出错，严重阻碍了新型 FPGA 架构的普及。

#### 2. 方法论：核心思想与关键技术
ATLAS 提出了一个全自动流程，将 GEMM 作为编译器前端与硬件后端之间的通用抽象层。其核心组件包括：
*   **hls4ml-GEMM（前端）：** 架构无关的编译器。它扩展了 `hls4ml` 框架，将 Keras 模型转换为 HLS C++ 代码。它会自动识别 Dense、Conv 和 Attention 层，并将其映射为标准化的 GEMM 函数调用。关键技术包括：
    *   **权重预打包：** 在编译时重新组织权重布局，以匹配硬核的流式或内存访问模式。
    *   **卷积映射：** 通过 `im2col` 变换将卷积转换为 GEMM。
    *   **注意力映射：** 解析 Einsum 表达式，将注意力机制中的矩阵运算分解为 GEMM。
*   **GEMM IP 生成器（后端）：** 架构感知的生成器。它接收前端生成的 GEMM 规格（维度 M/N/K、精度、接口类型），自动生成包含分块（Tiling）逻辑、控制状态机（FSM）和调度元数据的 RTL 封装。
*   **HLS 黑盒集成：** 利用 HLS 的黑盒机制，通过一个 C++ 包装器（Shim）向 HLS 工具隐藏硬核的复杂时序，确保 HLS 调度器能正确集成多周期的硬核 IP。

#### 3. 实验设计
*   **场景与数据集：** 评估涵盖了 11 个设计，分为两类：
    *   **层级微基准（Layer-wise）：** 包括 FC（全连接）、Conv1D、Conv2D、Attention 的小型和大型配置。
    *   **完整模型（Full designs）：** 包括 MLP（3 层）、CNN（2 卷积 + 2 全连接）、Transformer（多头注意力 + 前馈网络）。
*   **Benchmark 与对比方法：**
    *   **hls4ml Baseline：** 仅使用软逻辑（LUT/DSP/BRAM）的标准自动化流程。
    *   **Hand-written RTL：** 手动编写的、直接实例化硬核的 RTL 基准（代表性能上限）。
    *   **ATLAS（Proposed）：** 本文提出的全自动流程。
*   **工具链：** 使用 Catapult 2026.1 进行 HLS，使用 VTR 9 进行逻辑综合、布局布线。

#### 4. 资源与算力
*   **硬件建模：** 论文使用了基于 22nm 工艺的 Tensor Slices 硬核模型，其面积和延迟参数通过 COFFE 工具建模。
*   **算力说明：** 文中未提及深度学习模型的训练算力（如 GPU 型号或时长），因为研究重点在于**推理阶段的硬件合成与部署效率**。实验主要在 CPU 服务器上运行 HLS 和 VTR 编译流程。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 11 组核心实验，涵盖了从单层算子到端到端完整模型的映射。
*   **充分性评价：** 实验设计较为充分。它不仅验证了对齐维度的情况，还测试了非对齐维度（需要分块和填充逻辑）的情况。通过对比软逻辑基准和手动 RTL 基准，客观地展示了自动化带来的开销（Area Overhead）与性能增益。

#### 6. 主要结论与发现
*   **效率提升：** 在层级设计中，ATLAS 的计算-面积效率达到了手动 RTL 的 89%，比纯软逻辑流程高出 24%。
*   **模型表现：** 在完整模型上，ATLAS 的效率是软逻辑流程的 1.42 倍，达到了手动 RTL 的 63%。
*   **生产力飞跃：** 将设计周期从**数天（手动 RTL）缩短至数小时（自动化编译）**，且用户无需编写任何 RTL 代码。
*   **频率与延迟：** 自动化生成的 IP 在时钟频率（Fmax）上接近手动 RTL，但在延迟上由于增加了通用的封装和 FIFO 逻辑，略高于手动优化版本。

#### 7. 优点
*   **高度自动化：** 实现了从 Keras 模型到自定义硬核 FPGA 实现的端到端闭环，极大地降低了门槛。
*   **抽象层设计：** 使用 GEMM 作为抽象层具有很强的通用性，可以轻松适配其他类型的矩阵加速硬核。
*   **调度创新：** 巧妙利用 HLS 黑盒状态机标记，解决了复杂硬核在 HLS 工具中的调度冲突问题。

#### 8. 不足与局限
*   **开销问题：** 自动化生成的通用封装逻辑（如填充、掩码、FIFO）相比精细优化的手动 RTL 仍存在一定的面积和延迟开销。
*   **缺乏反馈闭环：** 目前是单向编译，前端在选择分块策略时无法根据后端的布局布线结果进行动态调整（Open-loop）。
*   **精度限制：** 目前仅支持硬核原生支持的精度，若模型精度超过硬核能力，尚无自动拆分处理机制。
*   **Transformer 效率：** 在处理 Transformer 等复杂动态计算时，由于软逻辑封装开销较大，效率提升不如 CNN 显著。

（完）
