---
title: Hardware-aware Graph Neural Networks prunning for embedded event-based vision
title_zh: 面向嵌入式事件视觉的硬件感知图神经网络剪枝
authors: "Piotr Wzorek, Kamil Jeziorek, Tomasz Kryjak"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06739v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 嵌入式FPGA平台上GNN的硬件感知剪枝与量化
tldr: "针对移动机器人中事件相机数据处理的高实时性与低功耗需求，本文提出了一种面向嵌入式FPGA平台的图卷积神经网络（GNN）硬件感知优化策略。该方法结合了剪枝与量化技术，通过细粒度网格搜索和贪婪逐层迭代加深搜索，在保持推理精度的同时显著降低了片上内存占用。实验表明，该方案在多个数据集上实现了约30%的BRAM资源节省，为嵌入式事件视觉应用提供了高效的硬件加速方案。"
selection_source: fresh_fetch
motivation: 事件相机在移动机器人中应用广泛，但其数据处理对嵌入式平台的功耗和延迟提出了严峻挑战。
method: 提出一种结合硬件感知剪枝与量化的优化策略，并利用细粒度网格搜索和贪婪搜索算法进行设计空间探索。
result: "在CIFAR-10、MNIST-DVS和N-Caltech101数据集上，分别实现了28.8%、31.4%和26.5%的BRAM内存缩减。"
conclusion: 该硬件感知优化方法能有效平衡模型精度与资源消耗，显著提升了GNN在受限嵌入式硬件上的部署效率。
---

## 摘要
由于在动态环境中的高性能表现，事件相机正逐渐成为移动机器人领域的首选传感器。然而，这些应用需要具有低延迟和低功耗的高效实时数据处理。满足这些严苛要求的一种策略是，对能够保留事件数据时间稀疏性的高效算法进行硬件加速。在这项工作中，我们提出了一种针对图卷积神经网络（GCNN）模型的优化策略，旨在使其架构适应嵌入式异构 FPGA 平台的有限资源。我们的方法结合了硬件感知剪枝和量化，并考虑了片上存储节省与推理准确率之间的权衡。通过细粒度网格搜索（Fine Grid Search）和贪婪逐层迭代加深搜索（Greedy layer-wise Iterative Deepening Search）方法对设计空间进行策略性探索，使模型架构能够灵活地适应目标平台。我们的方法在多种网络配置和多个数据集上进行了评估，结果显示：CIFAR-10 的 BRAM 内存减少了 28.8%（准确率下降 1.65%），MNIST-DVS 减少了 31.4%（准确率下降 3.55%），N-Caltech101 减少了 26.5%（准确率下降 5.18%）。

## Abstract
Event-based cameras are gaining popularity as the sensor of choice for mobile robotics, due to their high performance in dynamic environments. However, these applications require efficient real-time data processing with low latency and power consumption. One strategy to meet these stringent requirements is hardware acceleration of efficient algorithms that preserve the temporal sparsity of event data. In this work, we propose an optimization strategy for Graph Convolutional Neural Networks models aimed at adapting their architecture to the limited resources of embedded heterogeneous FPGA platforms. Our method incorporates hardware-aware pruning and quantization, taking into account the trade-off between on-chip memory savings and inference accuracy. Strategic exploration of the design space with Fine Grid Search and Greedy layer-wise Iterative Deepening Search methods enables flexible adaptation of the model architecture to the target platform. Our approach was evaluated across various network configurations and multiple datasets, resulting in BRAM memory reductions of 28.8% for CIFAR-10 (with a 1.65% decrease in accuracy), 31.4% for MNIST-DVS (accuracy drop of 3.55%), and 26.5% for N-Caltech101 (with a 5.18% accuracy reduction).

---

## 论文详细总结（自动生成）

这篇论文针对嵌入式平台（特别是 FPGA）上处理事件相机（Event-based Camera）数据的图神经网络（GNN）优化问题进行了深入研究。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：事件相机具有高动态范围、低延迟和低功耗的优势，非常适合移动机器人。然而，处理这些稀疏数据的图卷积神经网络（GCNN）在嵌入式 FPGA 部署时面临巨大的**片上内存（BRAM/URAM）瓶颈**。
*   **研究动机**：现有的剪枝方法大多只关注减少计算量（FLOPs）或参数量，而忽略了硬件底层的存储结构（如 FPGA 内存块的固定位宽）。作者旨在提出一种“硬件感知”的优化策略，通过协同优化剪枝和量化，使模型在资源受限的异构 FPGA 上实现精度与存储的最优权衡。

### 2. 方法论
核心思想是根据 FPGA 内存块（BRAM 为 18kb，URAM 为 72kb）的物理特性来约束通道数和量化位宽。
*   **硬件感知约束**：
    *   推导了内存利用率公式，确保通道数 $C$ 和量化精度 $q$ 的乘积加上边缘信息位宽后，能够恰好填满 18-bit（BRAM）或 72-bit（URAM）的整数倍，从而避免存储位浪费。
*   **两阶段搜索策略**：
    1.  **细粒度网格搜索 (FG)**：在预定义的剪枝步长和量化水平（如 6-bit, 8-bit）空间内进行初步探索，通过帕累托前沿（Pareto frontier）筛选出初步候选配置。
    2.  **贪婪逐层迭代加深搜索 (GLID)**：从 FG 选出的配置出发，逐层尝试进一步减少资源占用，选择精度下降最少的修改方案，直到达到预设的精度损失阈值。
*   **微调 (Fine-tuning)**：对最终确定的压缩模型进行短时间的重训练，以补偿精度损失。

### 3. 实验设计
*   **数据集**：使用了三个主流的神经形态视觉数据集：**MNIST-DVS**、**CIFAR10-DVS** 和 **N-Caltech101**。
*   **Benchmark**：以原始的 **EFGCN**（Event-based FPGA-accelerated GCN）架构为基准，对比了浮点模型、未剪枝的 8-bit 量化模型以及经过不同搜索阶段优化后的模型。
*   **硬件验证**：在 **Xilinx ZCU104** 异构 FPGA 平台上实现了硬件原型，验证实际资源消耗。

### 4. 资源与算力
*   **训练算力**：文中提到使用了波兰高性能计算基础设施 PLGrid 进行实验，但未明确给出具体的 GPU 型号和数量。
*   **训练参数**：初始训练 100 个 epoch（Adam 优化器），微调阶段训练 10 个 epoch。
*   **硬件部署**：目标平台为 ZCU104，运行频率为 200MHz。

### 5. 实验数量与充分性
*   **实验规模**：FG 阶段探索了多达 1024 种配置。针对三个不同复杂度的任务（10 类到 101 类分类）进行了完整测试。
*   **充分性评价**：实验设计较为充分，涵盖了从算法仿真到硬件实现的完整链路。通过帕累托曲线展示了不同资源约束下的表现，证明了搜索算法的有效性。对比实验客观地展示了精度下降与资源节省之间的负相关关系。

### 6. 主要结论与发现
*   **显著的资源节省**：在三个数据集上分别实现了 **28.8% (CIFAR-10)**、**31.4% (MNIST-DVS)** 和 **26.5% (N-Caltech101)** 的 BRAM 内存缩减。
*   **精度损失可控**：对应的准确率下降分别仅为 1.65%、3.55% 和 5.18%。
*   **硬件一致性**：硬件实现结果显示，整体 BRAM 消耗降低了 30.6%，同时由于位宽减小，逻辑资源（LUT 和 FF）也分别减少了约 34.4% 和 28.9%。

### 7. 优点
*   **硬件感知度高**：不同于通用的剪枝算法，该方法直接将 FPGA 的 BRAM/URAM 物理位宽作为约束条件，具有极强的工程落地价值。
*   **灵活性强**：两阶段搜索允许开发者根据目标平台的剩余资源，灵活选择帕累托前沿上的不同平衡点。
*   **首创性**：这是首个针对事件视觉 GNN 的硬件感知剪枝与量化协同优化方案。

### 8. 不足与局限
*   **搜索算法优化空间**：作者提到目前使用的贪婪搜索可能会陷入局部最优，且尝试过贝叶斯优化和遗传算法（NSGA II/III）但未能在该任务上收敛，说明设计空间的搜索效率仍有提升空间。
*   **架构依赖性**：该方法目前主要针对 EFGCN 架构及其特定的 PointNetConv 算子，对于其他类型的图神经网络（如 GAT 或更复杂的动态图网络）的普适性有待验证。
*   **精度权衡**：在类别较多的数据集（如 N-Caltech101）上，精度下降超过 5%，对于某些高精度要求的机器人任务可能仍显过大。

（完）
