---
title: Latency-Constrained DNN Architecture Learning for Edge Systems using Zerorized Batch Normalization
title_zh: 基于归零化批归一化的边缘系统延迟约束 DNN 架构学习
authors: "Shuo Huai, Di Liu, Hao Kong, Weichen Liu, Ravi Subramaniam, Christian Makaya, Qian Lin"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.06922v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 面向延迟的神经网络学习，结合硬件定制的延迟预测器
tldr: 针对边缘设备对实时性的严苛要求，本文提出一种基于零化批归一化（ZeroBN）的延迟约束DNN架构学习方法。该方案引入硬件定制的延迟预测器，通过单次训练过程自动调整神经元数量，确保模型在满足硬性延迟限制的同时最大化准确率。实验表明，该方法在NVIDIA Jetson平台上能有效平衡性能与速度，甚至在大幅降低延迟的情况下提升模型精度。
selection_source: fresh_fetch
motivation: 边缘计算场景需要严格满足实时延迟约束，而传统优化方法难以直接将推理时间成本纳入模型架构的自动优化过程。
method: 提出一种利用零化批归一化进行架构学习的方法，并结合硬件定制的延迟预测器，在单次训练中实现延迟感知的模型优化。
result: 在Jetson系列平台上，该方法能将模型精确压缩或扩展至目标延迟，且在GoogLeNet和VGG-19上实现了极低的精度损失或性能提升。
conclusion: 本研究证明了通过ZeroBN和延迟预测器可以高效地在边缘设备上构建满足硬性时间约束的高性能深度学习模型。
---

## 摘要
深度学习应用已在边缘设备上广泛采用，以缓解访问云服务器带来的隐私和延迟问题。在设计深度神经网络时，确定神经元数量以最大化性能并不直观。特别是，许多应用场景具有实时性并伴有严格的延迟约束，而传统的神经网络优化方法并不能直接改变延迟敏感型边缘系统模型推理的时间成本。在这项工作中，我们提出了一种面向延迟的神经网络学习方法，旨在满足延迟约束的同时优化模型以获得高准确率。为了提高效率，我们还引入了一种通用的硬件定制延迟预测器，通过单次（one-shot）训练过程来优化该流程，从而学习到满足延迟约束的模型。实验结果表明，与现有最先进的方法相比，我们的方法能够很好地拟合“硬”延迟约束并实现高准确率。在与原始模型相同的训练设置下，并在 ImageNet-100 数据集上满足 34 ms 延迟约束的情况下，我们在 NVIDIA Jetson Nano 上将 GoogLeNet 的延迟从 40.32 ms 降低到 34 ms，准确率仅下降了 0.14%。当结合量化技术时，我们的方法可以进一步改进，使 GoogLeNet 的准确率下降仅为 0.04%。在 NVIDIA Jetson TX2 上，我们将 VGG-19 从 119.98 ms 压缩到 34 ms，甚至将其准确率提高了 0.5%；同时我们将 GoogLeNet 从 20.27 ms 扩展到 34 ms，并实现了 0.78% 的准确率提升。我们还在 https://github.com/ntuliuteam/ZeroBN 开源了该框架。

## Abstract
Deep learning applications have been widely adopted on edge devices, to mitigate the privacy and latency issues of accessing cloud servers. Deciding the number of neurons during the design of a deep neural network to maximize performance is not intuitive. Particularly, many application scenarios are real-time and have a strict latency constraint, while conventional neural network optimization methods do not directly change the temporal cost of model inference for latency-critical edge systems. In this work, we propose a latency-oriented neural network learning method to optimize models for high accuracy while fulfilling the latency constraint. For efficiency, we also introduce a universal hardware-customized latency predictor to optimize this procedure to learn a model that satisfies the latency constraint by only a one-shot training process. The experiment results reveal that, compared to state-of-the-art methods, our approach can well-fit the 'hard' latency constraint and achieve high accuracy. Under the same training settings as the original model and satisfying a 34 ms latency constraint on the ImageNet-100 dataset, we reduce GoogLeNet's latency from 40.32 ms to 34 ms with a 0.14% accuracy reduction on the NVIDIA Jetson Nano. When coupled with quantization, our method can be further improved to only 0.04% drop for GoogLeNet. On the NVIDIA Jetson TX2, we compress VGG-19 from 119.98 ms to 34 ms and even improve its accuracy by 0.5%, and we scale GoogLeNet up from 20.27 ms to 34 ms and achieve higher accuracy by 0.78%. We also open source this framework at https://github.com/ntuliuteam/ZeroBN

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ZeroBN** 的深度神经网络（DNN）优化框架，旨在解决边缘设备在实时应用中面临的严苛延迟约束问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：边缘设备（如自动驾驶、无人机）对推理延迟有极高要求。然而，现有的模型优化方法（如剪枝、NAS）通常使用 FLOPs 或参数量等间接指标，这些指标无法准确反映特定硬件上的真实延迟。
*   **核心挑战**：
    1.  **测量耗时**：在优化过程中频繁在真实硬件上测量延迟非常低效。
    2.  **流程复杂**：传统的“预训练-剪枝-微调”三阶段流程耗时巨大。
    3.  **缺乏灵活性**：现有方法难以在单一框架内同时处理复杂模型的压缩和简单模型的扩展。
*   **整体含义**：提出一种**单次（One-shot）训练**框架，通过硬件感知的延迟预测器，直接以“硬延迟”为约束，自动学习最优的模型架构。

### 2. 方法论
*   **核心思想**：利用批归一化（Batch Normalization, BN）层的缩放因子 $\gamma$ 作为通道重要性的度量，通过动态“归零”和“恢复”机制在训练过程中演化出最优架构。
*   **关键技术细节**：
    *   **紧凑学习（Compact Learning）**：分为三个阶段：
        1.  **初始训练**：进行稀疏化训练，使 $\gamma$ 能够反映通道重要性。
        2.  **零化训练（Zero Training）**：根据延迟预测器给出的压缩比，将不重要通道的 $\gamma$ 和 $\beta$ 置零。
        3.  **恢复训练（Recovery Training）**：利用优化器的动量（Momentum）机制，允许被置零但潜在重要的通道重新获得非零值，从而探索更优架构。
    *   **延迟预测器**：构建基于 BP 神经网络的硬件定制预测器。它通过对单层算子进行采样测量，预测整个模型在目标硬件上的延迟，避免了训练过程中的实机测量。
    *   **模型扩展（Scaling）**：对于性能冗余的设备，先对简单模型进行统一缩放（增加深度和宽度），再通过紧凑学习将其“修剪”至刚好满足延迟约束，以最大化准确率。
    *   **层剪枝**：通过引入分支结构（Branch Layer），将层的重要性与通道重要性关联，实现层级和通道级的联合优化。

### 3. 实验设计
*   **数据集**：CIFAR-10 和 ImageNet-100（ImageNet 的 100 类子集）。
*   **硬件平台**：NVIDIA Jetson TX2 和 NVIDIA Jetson Nano。
*   **Benchmark 模型**：VGG-19, ResNet-50/164, DenseNet-40, GoogLeNet。
*   **对比方法**：包括 SFP, FPGM, NS (Network Slimming), PGMPF, OTO 等剪枝方法，以及 EfficientNet 和 HACScale 等缩放方法。

### 4. 资源与算力
*   **训练设备**：使用 NVIDIA Quadro GV100 GPU 进行模型训练。
*   **算力开销**：
    *   **训练效率**：ZeroBN 的单次训练开销极低，每轮（Epoch）额外开销仅约 **1.12%**。
    *   **总时长**：在 ImageNet-100 上训练 90 轮约需 20.4 小时，远低于需要预训练和多次微调的传统方法（如 NS 需要约 39.5 小时）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流架构、2 个数据集、2 种硬件平台。
*   **消融实验**：对超参数 $s$（启动轮数）和 $k$（间隔轮数）进行了敏感性分析，验证了自动参数设置的合理性。
*   **公平性**：在相同的训练轮数和相同的“硬延迟约束”（如 34ms）下进行对比，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **高精度与硬约束**：在满足 34ms 延迟约束的前提下，ZeroBN 显著优于 SOTA 方法。例如，在 Jetson TX2 上将 VGG-19 延迟从 119.98ms 压缩至 34ms，准确率反而提升了 0.5%。
*   **单次训练优势**：证明了不需要复杂的预训练和微调，通过动态的零化-恢复机制即可在一次训练中获得高性能的紧凑架构。
*   **预测器精度**：提出的 BP 神经网络预测器平均误差仅为 **6.12%**，足以指导架构搜索。
*   **量化协同**：结合 FP16 量化后，模型可以在相同的延迟预算下保留更多参数，进一步提升准确率。

### 7. 优点
*   **硬件感知**：直接优化真实延迟而非 FLOPs，更具实用价值。
*   **极高效率**：单次训练流程极大缩短了模型部署周期。
*   **双向优化**：既能压缩大模型，也能扩展小模型，适应性强。
*   **开源贡献**：提供了开源代码，增加了研究的可信度和可复现性。

### 8. 不足与局限
*   **数据集局限**：主要在 ImageNet-100 上验证，未在完整的 ImageNet-1K 上展示超大规模实验结果。
*   **硬件覆盖**：实验仅限于 NVIDIA Jetson 系列 GPU 边缘设备，未测试在 CPU、DSP 或专用 NPU 上的表现。
*   **架构依赖**：该方法高度依赖 BN 层。虽然文中讨论了无 BN 模型的处理方案，但其在 Transformer 等非典型 CNN 架构上的效果尚待验证。

（完）
