---
title: KVpop -- Key-Value Cache Compression with Predictive Online Pruning
title_zh: KVpop —— 基于预测性在线剪枝的键值缓存压缩
authors: "Lukas Hauzenberger, Niklas Schmidinger, Anamaria-Roberta Hartl, David Stap, Thomas Schmied, Sebastian Böck, Günter Klambauer, Sepp Hochreiter"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05061v1"
tags: ["query:hwnas"]
score: 7.0
evidence: 自回归解码中KV缓存的内存与带宽优化
tldr: "KVpop 针对大模型推理中 KV Cache 增长带来的内存瓶颈，提出了一种基于预测性在线剪枝的压缩方法。它通过直接监督“保留或丢弃”决策，并利用高效计算的未来注意力目标训练评分器，结合延迟评分机制捕捉近未来上下文。在数学推理任务上，该方法在 88% 的压缩率下仍能保持 97% 的性能，显著优于现有基准。"
selection_source: fresh_fetch
motivation: 现有的 KV 缓存剔除方法多依赖静态启发式或代理分数，难以准确预测未来 Token 的效用且在相关性偏移时表现脆弱。
method: 引入 KVpop 学习固定预算的剔除策略，通过未来注意力信号进行监督，并采用延迟评分器以利用近未来上下文信息。
result: "在 Qwen3 模型上，KVpop 在 75% 和 88% 的压缩率下分别保留了 98% 和 97% 的全注意力性能，优于主流基准。"
conclusion: 通过未来注意力信号监督剔除决策，可以在大幅降低内存成本的同时，有效维持大语言模型的生成质量。
---

## 摘要
键值（KV）缓存的增长是自回归解码中的主要瓶颈，因为内存和带宽随上下文长度线性扩展。现有的 KV 逐出方法通常依赖于静态启发式方法或代理评分，这些方法难以追踪未来 Token 的效用，并在相关性发生偏移时导致脆弱的逐出决策。为了解决这一问题，我们引入了 KVpop，它通过直接监督“保留或丢弃”决策来学习固定预算的 KV 逐出策略。该评分器针对一种新颖的未来注意力（future-attention）目标进行训练，该目标无需实例化稠密注意力图即可高效计算。我们进一步引入了一种基于延迟记忆的评分器，这在学习型逐出方法中独树一帜，它通过将评分延迟固定步数来利用近未来上下文。在 AIME 和 HMMT 数学推理任务上，KVpop 在 Qwen3-4B 模型上实现 75% 的 KV 缓存压缩时保留了 98% 的全注意力性能，在 88% 的压缩率下保留了 97%，持续优于现有的逐出基准。Qwen3-8B 表现出更强的结果，达到了接近全量教师模型的性能。这些结果表明，利用未来注意力信号监督逐出过程可以在保持质量的同时降低内存成本。

## Abstract
Key-value (KV) cache growth is a major bottleneck in autoregressive decoding, as memory and bandwidth scale linearly with context length. Existing KV eviction methods often rely on static heuristics or proxy scores, which poorly track future token utility and cause brittle eviction as relevance shifts. To address this, we introduce KVpop, which learns a fixed-budget KV eviction policy by directly supervising the keep-or-drop decision. The scorer is trained against a novel future-attention target, computed efficiently without materializing dense attention maps. We further introduce a delayed memory-based scorer that, uniquely among learned eviction methods, defers scoring for a fixed number of steps to exploit near-future context. On AIME and HMMT mathematical reasoning, KVpop retains 98% of full-attention performance on Qwen3-4B at 75% KV cache compression and 97% at 88% compression, consistently outperforming established eviction baselines. Qwen3-8B shows even stronger results, reaching near-full teacher performance. These results show that supervising eviction with future-attention signals cuts memory costs while maintaining quality.