---
title: "EvoLP: Self-Evolving Latency Predictor for Model Compression in Real-Time Edge Systems"
title_zh: EvoLP：面向实时边缘系统模型压缩的自进化延迟预测器
authors: "Shuo Huai, Hao Kong, Shiqing Li, Xiangzhong Luo, Ravi Subramaniam, Christian Makaya, Qian Lin, Weichen Liu"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09063v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 实时边缘系统中用于模型压缩的延迟预测器
tldr: EvoLP是一个专为边缘设备设计的自进化延迟预测框架，旨在解决模型压缩过程中实时延迟测量困难且成本高昂的问题。该框架通过在压缩过程中不断进化以提高预测精度，从而在满足严格延迟约束的同时，有效引导模型压缩以获得更高的准确率。实验证明其在多种设备和模型上均优于现有技术，并已开源。
selection_source: fresh_fetch
motivation: 边缘设备资源受限且实时性要求高，但在实际设备上频繁测量模型推理延迟既困难又耗时。
method: 提出了一种自进化的延迟预测器EvoLP，该预测器能在模型压缩过程中通过自我演化不断提升预测精度。
result: 在三种边缘设备和四种模型变体上的评估结果显示，EvoLP的预测性能优于目前最先进的方法。
conclusion: EvoLP能有效指导模型压缩，在满足严格延迟约束的前提下实现更高的模型准确率，是边缘系统模型优化的有力工具。
---

## 摘要
边缘设备越来越多地被用于在嵌入式系统上部署深度学习应用。许多应用的实时性要求以及边缘设备有限的资源，使得针对延迟的神经网络压缩变得必不可少。然而，在真实设备上测量延迟既具有挑战性且成本高昂。因此，本研究提出了一种名为 EvoLP 的新型高效框架，用于准确预测模型在边缘设备上的推理延迟。该预测器可以在网络压缩过程中不断进化，以实现更高的延迟预测精度。实验结果表明，通过在三种边缘设备和四种模型变体上的评估，EvoLP 的性能优于以往的最先进方法。此外，当集成到模型压缩框架中时，它能有效引导压缩过程，在满足严格延迟约束的同时获得更高的模型准确率。我们在 https://github.com/ntuliuteam/EvoLP 开源了 EvoLP。

## Abstract
Edge devices are increasingly utilized for deploying deep learning applications on embedded systems. The real-time nature of many applications and the limited resources of edge devices necessitate latency-targeted neural network compression. However, measuring latency on real devices is challenging and expensive. Therefore, this letter presents a novel and efficient framework, named EvoLP, to accurately predict the inference latency of models on edge devices. This predictor can evolve to achieve higher latency prediction precision during the network compression process. Experimental results demonstrate that EvoLP outperforms previous state-of-the-art approaches by being evaluated on three edge devices and four model variants. Moreover, when incorporated into a model compression framework, it effectively guides the compression process for higher model accuracy while satisfying strict latency constraints. We open source EvoLP at https://github.com/ntuliuteam/EvoLP.

---

## 论文详细总结（自动生成）

这是一份关于论文《EvoLP: Self-Evolving Latency Predictor for Model Compression in Real-Time Edge Systems》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在实时边缘系统中，深度神经网络（DNN）的部署受限于有限的计算资源和严格的延迟要求。为了满足这些约束，通常需要进行模型压缩。然而，**获取准确的推理延迟**是一个重大挑战：
*   **直接测量成本高**：在真实硬件上测量每个候选模型的延迟非常耗时（每次可能需数分钟）。
*   **现有预测器局限性**：查表法（LUT）无法处理未记录的新配置；硬件模拟器难以构建且不透明；基于学习的方法（如 nn-Meter）虽然准确，但需要海量的离线采样数据（耗时数天），且在模型压缩导致通道数频繁变化时，搜索空间过大，难以负担。
*   **研究动机**：开发一种既能减少采样成本，又能随压缩过程动态进化以提高精度的延迟预测框架。

### 2. 方法论：核心思想与关键技术
EvoLP 的核心思想是**针对特定模型进行采样**并引入**自进化机制**。其流程分为三个阶段：
*   **模型特定采样（Model Sampling）**：
    *   将模型层分为“常量层”（压缩中延迟不变）和“变量层”（延迟随压缩改变）。
    *   仅针对变量层在特定配置范围内进行少量采样，构建层级池（Layer Pool），显著缩小了搜索空间。
*   **预测器生成（Predictor Generation）**：
    *   利用采样数据训练一个轻量级的**三层多层感知机（MLP）**，作为层级延迟预测器（LWLP）。
    *   MLP 能够学习层配置与延迟之间的非线性关系，前向计算开销极低。
*   **自进化方案（Evolution Scheme）**：
    *   引入**进化矩阵（EM）**来修正整体延迟预测。公式类似于泰勒展开：$P = \sum ReLU(T_i + \sum a_{im} * T_i^m)$，其中 $T_i$ 是 LWLP 预测的层延迟，$a_{im}$ 是可学习参数。
    *   **并行演化**：在模型压缩训练过程中，云端进行训练，边缘设备并行测量真实延迟。测量结果反馈给预测器，通过随机梯度下降（SGD）更新 EM，使预测器在压缩约束边界附近越来越准。

### 3. 实验设计
*   **硬件平台**：涵盖了三种典型的边缘设备：
    *   CPU 平台：ARMv8 rev 3。
    *   GPU 平台：NVIDIA Pascal GPU 和 NVIDIA Maxwell GPU。
*   **模型对象**：VGG、ResNet、Inception、MobileNet。
*   **数据集**：ImageNet-100（用于验证压缩后的模型准确率）。
*   **对比基准（Benchmarks）**：
    *   传统指标：FLOPs（浮点运算量）。
    *   改进指标：FLOPsE（带进化机制的 FLOPs）。
    *   先进预测器：ZBNLP（基于 MLP）、nn-Meter、TALM。
*   **评估指标**：均方根误差（RMSE）、相对均方根百分比误差（RMSPE）、±5% 准确率（预测误差在 5% 以内的样本占比）。

### 4. 资源与算力
*   **采样成本**：论文指出，构建层级池的测量过程在**几小时内**即可完成，远低于 nn-Meter 的 4.4 天。
*   **训练与推理**：预测器本身是轻量级 MLP，仅需几百次加乘运算。
*   **算力说明**：文中未详细列出训练预测器所用的具体服务器 GPU 型号，但强调了测量是在上述边缘设备上进行的，且测量与模型压缩训练是**异步并行**的，不会增加额外的训练停顿时间。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种硬件、4 种主流架构、多种压缩率下进行了广泛测试。
*   **充分性**：实验不仅对比了预测精度（RMSE 等），还展示了预测器在实际模型压缩任务中的表现（准确率 vs. 延迟约束）。
*   **客观性**：通过引入 FLOPsE 和 ZBNLP 作为消融对比，证明了 MLP 结构和进化机制各自的贡献，实验设计较为严谨。

### 6. 主要结论与发现
*   **精度领先**：EvoLP 在所有测试设备和模型上的 ±5% 预测准确率均显著优于 nn-Meter 和 TALM。
*   **进化有效性**：随着训练轮次的增加，进化机制使预测误差曲线明显下降，尤其在延迟约束线附近表现更佳。
*   **压缩优化**：在 EvoLP 的指导下，模型压缩框架能够更贴近延迟硬约束，从而在相同延迟限制下获得比基准方法更高的模型准确率。

### 7. 优点
*   **高效性**：通过模型特定采样，将原本“无限”的搜索空间缩小到实用范围。
*   **动态适应**：自进化机制解决了硬件运行时优化（如并行处理、数据传输开销）难以建模的问题。
*   **实用性强**：开源了代码，且预测器极其轻量，适合集成到各种 NAS 或模型压缩流程中。

### 8. 不足与局限
*   **硬件依赖**：虽然采样量小，但仍需在目标硬件上进行实测，对于完全无法获取物理设备的场景不适用。
*   **模型结构限制**：实验主要集中在 CNN 架构，对于 Transformer 等新兴架构在边缘端的延迟预测表现尚未讨论。
*   **冷启动问题**：在压缩初期，进化矩阵尚未收敛时，预测精度可能存在波动。

（完）
