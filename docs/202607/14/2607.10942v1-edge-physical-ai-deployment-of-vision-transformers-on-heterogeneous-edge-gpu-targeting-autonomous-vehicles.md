---
title: Edge Physical AI Deployment of Vision Transformers on Heterogeneous Edge GPU Targeting Autonomous Vehicles
title_zh: 面向自动驾驶汽车的异构边缘 GPU 视觉 Transformer 边缘物理人工智能部署
authors: "Ashiyana Abdul Majeed, Mahmoud Meribout, Neethu Joseph, Abel Kidane Haile, Mohammad Abdullah Al Faruque"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10942v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 硬件感知调度以提高边缘GPU的利用率和每瓦吞吐量
tldr: 针对自动驾驶中Vision Transformer在边缘GPU部署时硬件利用率低和算子不兼容的问题，本文提出了一种硬件感知的异构帧调度方法H-FraDS。通过对模型算子进行DLA适配（如近似ERF和LayerNorm）并利用GPU、DLA和OFA进行协同调度，实现了高吞吐量和低功耗的推理，在保持高精度的同时显著提升了处理速度。
selection_source: fresh_fetch
motivation: 自动驾驶系统需要高效部署Transformer模型，但边缘异构硬件常因算子不兼容和调度不当导致性能受限。
method: 提出H-FraDS调度策略，并对Transformer组件进行重塑和近似处理以适配深度学习加速器（DLA）。
result: 在Swin Transformer上实现了125.93 FPS的吞吐量，较独立DLA执行提升2.36倍，且能效比达4.0 FPS/W。
conclusion: 该方案成功解决了边缘异构硬件上的Transformer部署难题，满足了自动驾驶对实时性和低功耗的严苛要求。
---

## 摘要
物理人工智能系统（如自动驾驶汽车和智能机器）需要满足严格边缘延迟和能耗约束的基于 Transformer 的感知模型。然而，异构边缘 GPU 的部署仍受限于硬件引擎利用不足以及与加速器不兼容的算子，导致执行碎片化和较低的单位功耗吞吐量。本文提出了异构帧分发调度（H-FraDS），这是一种针对最新 NVIDIA 边缘 GPU 上 Transformer 推理的硬件感知帧调度方法。H-FraDS 使用固定分发比例在 GPU 和双深度学习加速器（DLA）核心之间路由帧，以在延迟和功耗约束下提高利用率。为了实现调度，通过重塑张量、使用 tanh 近似误差函数（ERF）以及用有界 tanh 替换层归一化（layer normalization），使不兼容的 Transformer 组件适配 DLA 执行。适配后的模型保持了 92% 的 F1 分数，仅比原始模型降低了 2%。光流加速器（OFA）被进一步用于推理侧的光流估计。据作者所知，先前的研究尚未解决这些综合问题。在使用 Swin Transformer 进行自动驾驶感知时，H-FraDS 平衡分发（1:2）达到了 125.93 FPS，相比独立适配的 DLA 执行实现了 2.36 倍的加速，达到 4.0 FPS/W，且 DLA 延迟约为 24 ms，满足 30 FPS 的实时运行要求；GPU-DLA-OFA 案例实现了 2.02 倍的 DLA 吞吐量加速。

## Abstract
Physical AI systems, such as autonomous vehicles and intelligent machines, require transformer-based perception models that satisfy stringent edge latency and energy constraints. However, heterogeneous edge-GPU deployment remains limited by underutilized hardware engines and accelerator-incompatible operators, causing fragmented execution and lower throughput per watt. This paper presents Heterogeneous Frame Dispatch Scheduling (H-FraDS), a hardware-aware frame scheduling methodology for transformer inference on a recent NVIDIA edge GPU. H-FraDS routes frames across the GPU and dual deep learning accelerator (DLA) cores using fixed dispatch ratios to improve utilization under latency and power constraints. To enable scheduling, incompatible transformer components are adapted for DLA execution by reshaping tensors, approximating error function (ERF) with tanh, and replacing layer normalization with bounded tanh. The adapted model maintains a 92% F1 score, with only a 2% reduction from the original. Optical flow accelerator (OFA) is further used for inference-side optical-flow estimation. To the best of the authors' knowledge, prior work has not addressed these combined issues. Using Swin Transformer for autonomous-driving perception, H-FraDS Balanced Dispatch (1:2) achieves 125.93 FPS, a 2.36x speedup over standalone adapted-DLA execution, 4.0 FPS/W, and approximately 24 ms DLA latency, satisfying 30 FPS real-time operation; the GPU-DLA-OFA case achieves a 2.02x DLA throughput speedup.

---

## 论文详细总结（自动生成）

这篇论文针对自动驾驶领域中 Vision Transformer (ViT) 模型在边缘异构硬件上部署效率低下的问题，提出了系统性的优化方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：Transformer 模型（如 Swin Transformer）虽然在自动驾驶感知任务中表现优异，但在边缘异构 GPU 平台（如 NVIDIA Jetson）上部署时，面临**硬件利用率低**和**算子不兼容**的双重挑战。
*   **研究动机**：
    *   **执行碎片化**：Transformer 中的某些算子（如 LayerNorm、GELU）不被深度学习加速器（DLA）原生支持，导致频繁切换回 GPU 执行（GPU fallback），产生大量开销。
    *   **资源闲置**：边缘 SoC 包含 GPU、DLA 和光流加速器（OFA）等多个引擎，但现有框架往往无法让这些引擎高效协同工作。
    *   **实时性要求**：自动驾驶需要极低的延迟和高能效比，传统的部署方式难以在有限功耗下达到 30 FPS 以上的实时性能。

### 2. 方法论：H-FraDS 与 DLA 适配技术
论文提出了**异构帧分发调度（H-FraDS）**框架，结合了模型架构的硬件感知适配：
*   **DLA 算子适配（消除不兼容性）**：
    *   **维度重塑**：将 Transformer 内部的 3D 张量（Batch, Sequence, Channel）重塑为 DLA 要求的 4D 格式。
    *   **GELU 近似**：使用 DLA 支持的 `tanh` 函数近似替代复杂的误差函数（ERF），公式简化为：$GELU(x) \approx \frac{x}{2} [1 + tanh(\frac{x}{\sqrt{2}})]$。
    *   **LayerNorm 替代**：由于 DLA 不支持动态均值/方差计算，使用**有界 tanh（bounded tanh）**作为替代方案，以稳定激活值范围。
*   **H-FraDS 调度策略**：
    *   **帧级分发**：根据 GPU 和 DLA 的延迟差异，按固定比例（如 1:2）将连续的视频帧分发到不同的计算引擎。
    *   **多引擎协同**：首次将 **OFA（光流加速器）** 整合进推理流水线，与 GPU 和双 DLA 核心并行工作，处理运动估计任务。
    *   **平衡负载**：通过实验确定最佳分发比例，使各引擎同时处于饱和状态，减少空闲等待。

### 3. 实验设计
*   **硬件平台**：NVIDIA Jetson AGX Orin（包含 Ampere GPU, 2x DLA, OFA）。
*   **基准模型**：Swin Transformer (Swin-T)。
*   **数据集**：
    *   **Roboflow Udacity**：主要用于目标分类和 F1 分数评估。
    *   **KITTI**：用于道路/车道检测验证。
    *   **Cityscapes**：用于语义分割任务的泛化性测试。
*   **对比方法**：对比了原始 Swin 模型与适配后的模型在独立 DLA 执行、独立 GPU 执行以及 9 种不同调度策略（如 1:1, 1:2, 1:5 等比例）下的表现。

### 4. 资源与算力
*   **算力设备**：使用了单台 **NVIDIA Jetson AGX Orin** 边缘 SoC。
*   **频率锁定**：为了实验公平性，锁定了 CPU (2201 MHz)、GPU (1293-1296 MHz) 和 DLA (1600 MHz) 的频率，避免 DVFS（动态电压频率调整）干扰。
*   **训练细节**：文中未详细说明模型训练的具体时长，重点在于推理侧的部署优化。模型在适配后进行了微调以恢复精度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   设计了 **9 种异构调度实验**，涵盖了单 DLA、双 DLA 并行、GPU-DLA 协同及加入 OFA 的全负载场景。
    *   在 **3 个主流自动驾驶数据集**上进行了精度验证。
*   **充分性评价**：实验设计非常充分且具有针对性。通过消融实验对比了算子替换前后的性能，并详细记录了功耗、延迟、利用率和 FPS/W，客观地展示了硬件感知优化的收益。

### 6. 主要结论与发现
*   **性能提升**：H-FraDS 的“平衡分发（1:2）”策略达到了 **125.93 FPS**，比单纯在 DLA 上执行提升了 **2.36 倍**。
*   **能效比**：系统能效达到 **4.0 FPS/W**，在多任务并行模式下表现更优。
*   **延迟达标**：适配后的模型在 DLA 上的推理延迟约为 **24 ms**，远低于自动驾驶 30 FPS（33.3 ms）的实时性阈值。
*   **精度损失可控**：通过算子近似，F1 分数从 94.91% 降至 92.26%，仅损失约 **2.65%**，但在速度和能效上获得了巨大回报。

### 7. 优点
*   **实战导向**：直接解决了 Transformer 在现有商用边缘芯片上“跑不动、跑不快”的痛点。
*   **全栈优化**：不仅做了软件层面的调度（H-FraDS），还深入到算子层面的硬件适配。
*   **资源挖掘**：充分利用了 SoC 中常被忽视的 OFA 引擎，实现了真正的异构并行。

### 8. 不足与局限
*   **精度权衡**：LayerNorm 的替代方案（bounded tanh）并非数学等价，虽然在测试集上表现良好，但在极端或未见过的场景下可能存在鲁棒性风险。
*   **静态调度**：H-FraDS 目前采用固定的分发比例，无法根据系统实时负载或热节流（Thermal Throttling）状态动态调整比例。
*   **应用限制**：该方法高度依赖 NVIDIA Jetson 的硬件特性（如 DLA 的特定限制），迁移到其他边缘平台（如华为昇腾、高通 Snapdragon）时需要重新设计适配逻辑。

（完）
