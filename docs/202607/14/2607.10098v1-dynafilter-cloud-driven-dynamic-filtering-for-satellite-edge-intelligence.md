---
title: "DynaFilter: Cloud-driven Dynamic Filtering for Satellite Edge Intelligence"
title_zh: DynaFilter：云驱动的卫星边缘智能动态过滤技术
authors: "Ziyang Zhang, Jie Liu, Luca Mottola"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10098v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 卫星边缘智能与遥感的动态过滤技术
tldr: 针对卫星边缘计算中带宽受限和连接不稳定的挑战，本文提出DynaFilter。该技术利用压缩域特征（如JPEG系数和运动矢量）与语义查询的关联，在无需完全解压的情况下实现感兴趣区域（RoI）的动态过滤。通过云端驱动的语义映射，边缘设备仅传输相关数据，显著降低了带宽消耗、能耗并提升了推理速度。
selection_source: fresh_fetch
motivation: 卫星边缘系统带宽极度受限且连接不稳定，传统方法在传输前需完全解压或传输冗余数据，导致效率低下。
method: 建立云端查询语义与压缩域多模态特征（如DC/AC系数和运动矢量）的精确映射，在边缘侧直接进行压缩域RoI过滤。
result: "图像解码数据量减少1.6-7.1倍，视频带宽节省92%，能耗降低43.1-88.6%，推理速度提升1.6-3.0倍。"
conclusion: DynaFilter通过压缩域动态过滤有效解决了卫星边缘智能的传输与计算瓶颈，实现了高效的云边协同。
---

## 摘要
现代卫星边缘系统（包括执行目标检测和跟踪等遥感任务的系统）具有带宽严重受限和连接断续的特点，这使得向云端进行持续数据传输变得不切实际。然而，现有的边缘-云系统要么在分析前需要繁重的预处理（例如，对图像数据进行完全解压缩），要么不加区分地传输所有压缩数据。为了应对这些挑战，我们设计了 DynaFilter，这是一种动态过滤技术，使卫星边缘设备能够直接在压缩域中执行选择性的感兴趣区域（RoI）推理，而无需完全解压缩。我们的核心见解是，低级压缩语法（特别是 JPEG 图像中的 DC 系数/AC 能量以及视频流中的运动矢量）与高级语义查询表现出强相关性。通过在云查询语义与多模态压缩域特征之间建立精确映射，DynaFilter 使边缘端能够识别并仅传输与 RoI 相关的有效数据。广泛的评估表明，与最先进的基准方案相比，DynaFilter 将用于解码和后续推理的图像像素数据总量减少了 1.6 倍至 7.1 倍，并为视频流节省了 92.0% 的带宽。此外，它在目标设备上降低了 43.1-88.6% 的能耗，并在推理延迟方面实现了 1.6 倍至 3.0 倍的加速。

## Abstract
Modern satellite edge systems, including those performing remote sensing tasks such object detection and tracking, are characterized by severely limited bandwidth and intermittent connections, making continuous data transmission to the cloud impractical. Existing edge-cloud systems, however, either require heavy pre-processing before analysis, for instance, full decompression of imagery data, or transmit all compressed data regardless of relevance. To address these challenges, we design DynaFilter, a dynamic filtering technique that enables satellite edge devices to perform selective region-of-interest (RoI) inference directly in the compressed-domain, without full decompression. Our key insight is that low-level compression syntax, specifically DC coefficients/AC energy in JPEG images and motion vectors in video streams, exhibits strong correlations with high-level semantic queries. By establishing a precise mapping between cloud query semantics and multimodal compressed-domain features, DynaFilter enables the edge to identify and transmit only relevant data associated to RoIs. Extensive evaluations show that DynaFilter reduces the total volume of pixel data for decoding and subsequent inference by 1.6x-7.1x for images, and achieves 92.0% bandwidth savings for video streams compared to state-of-the-art baselines. Furthermore, it decreases energy consumption by 43.1-88.6% on target devices and achieves a 1.6x-3.0x speedup in inference latency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DynaFilter** 的系统，旨在解决卫星边缘计算中带宽极度受限、连接断续以及计算资源匮乏带来的挑战。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代卫星遥感任务（如目标检测、跟踪）产生海量数据，但卫星与地面间的带宽极低（通常仅为 kbps 级别）且连接不稳定。
*   **研究动机**：
    *   **数据漂移（Data Drift）**：云端查询需求（如寻找特定类别的目标）动态变化，而边缘端存储的压缩数据无法预知哪些是相关的。
    *   **DIR 瓶颈**：传统的“解压-推理-再压缩（DIR）”流程在边缘端极其耗能且延迟高。实验显示，解压高分辨率图像可占总处理时间的 35% 和能耗的 42%。
    *   **资源错配**：现有系统要么传输所有数据（浪费带宽），要么在边缘进行全量推理（浪费算力）。

### 2. 方法论：核心思想与关键技术
DynaFilter 的核心思想是**利用压缩域特征与高级语义之间的强相关性**，在不完全解压的情况下识别感兴趣区域（RoI）。
*   **核心思想**：JPEG 的 DC 系数（平均亮度）/ AC 能量（纹理丰富度）以及视频的运动矢量（MV）能有效反映目标的存在。
*   **关键技术组件**：
    1.  **压缩域特征映射器（Cloud-side）**：使用轻量级神经网络（NN），输入用户查询（目标类别、置信度）和历史统计数据，输出压缩域特征的过滤阈值（如 $T_{ac}, T_{dc}, T_{mv}$）。
    2.  **多模态特征融合滤波器（Cloud-side）**：基于 Transformer 架构，融合空间（纹理）和时间（运动）特征，生成适应动态场景（如遮挡、小目标）的增强配置。
    3.  **编解码器分析器（Edge-side）**：直接解析 JPEG 或 H.264/HEVC 位流，提取 DCT 系数和运动矢量，跳过耗时的逆量化、IDCT 和环路滤波。
    4.  **RoI 定位器（Edge-side）**：根据云端下发的阈值生成二值掩码，通过连通域分析合并碎片块，最终仅对识别出的 RoI 进行局部解压和推理。

### 3. 实验设计
*   **数据集**：
    *   **DOTA-v1.0**：大规模卫星遥感图像数据集（15 类目标），用于评估目标检测。
    *   **VisDrone 2019**：无人机航拍视频数据集（10 类目标），用于评估多目标跟踪。
*   **Benchmark 与对比方法**：
    *   **Full Inference**：全量解压并推理。
    *   **DeepCOD / LimitNet**：先进的边缘-云协同/压缩卸载框架。
    *   **EFilter**：DynaFilter 的变体（不带 RoI 定位，仅做压缩域特征提取）。
    *   **DIR / Full Streaming**：视频场景下的传统处理方案。
*   **硬件平台**：边缘端使用 NVIDIA Jetson Orin Nano 和 AGX Orin；云端使用配备 GTX 3080 GPU 的服务器。

### 4. 资源与算力
*   **训练阶段**：在 NVIDIA GeForce GTX 3080 GPU 上训练映射器模型，使用 Adam 优化器，学习率 0.001，Batch Size 64，训练约 **150 个 Epoch** 即可收敛。
*   **推理阶段（边缘）**：在 Jetson Orin Nano 上运行。映射器推理时间约为 **12.6~13.1ms**，内存占用极低（约 0.8~0.9MB）。
*   **系统开销**：分析器和 RoI 定位器仅占总处理时间的 20% 左右，远低于全量解压和推理的开销。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的评估，包括端到端延迟、推理准确度（mAP/MOTA）、能耗、带宽节省、动态查询响应速度等。
*   **消融实验**：针对多模态特征（AC/DC/Temporal/Spatial）对跟踪准确度的贡献进行了详细分析。
*   **充分性评价**：实验覆盖了静态图像和动态视频两种模态，对比了多种 SOTA 方法，并考虑了小目标、遮挡、不同 JPEG 质量因子等极端情况，实验设计客观且公平。

### 6. 主要结论与发现
*   **带宽与数据量**：图像解码数据量减少了 **1.6x~7.1x**，视频流带宽节省高达 **92.0%**。
*   **能耗与延迟**：边缘端能耗降低了 **43.1%~88.6%**，推理延迟实现了 **1.6x~3.0x** 的加速。
*   **准确度保持**：在大幅节省资源的前提下，检测准确度损失控制在 **2%** 以内，跟踪准确度（MOTA）与全量流传输相当。
*   **动态适应**：DynaFilter 能在毫秒级内响应云端查询的变化（如从检测“飞机”切换到“油罐”），有效解决了数据漂移问题。

### 7. 优点与亮点
*   **压缩域处理**：巧妙避开了边缘端最沉重的解压负担，直接在位流层面做决策。
*   **云驱动架构**：将复杂的阈值计算留在云端，边缘端仅执行简单的阈值匹配，符合卫星边缘计算的资源分布特征。
*   **多模态融合**：结合了空间纹理和时间运动特征，显著提升了在复杂环境（如遮挡）下的鲁棒性。

### 8. 不足与局限
*   **依赖历史数据**：映射器的阈值预测依赖于历史统计特征，在环境发生剧烈突变（如光照极端变化或进入全新地貌）时，初始阶段的过滤精度可能受影响。
*   **编解码器限制**：目前主要针对 JPEG 和 H.264/HEVC。虽然论文讨论了扩展到 WebP 或神经编解码器的可能性，但尚未在文中实现和验证。
*   **任务类型**：主要集中在检测和跟踪任务，对于需要全局像素信息的任务（如超分辨率重建或精细分割）可能不适用。

（完）
