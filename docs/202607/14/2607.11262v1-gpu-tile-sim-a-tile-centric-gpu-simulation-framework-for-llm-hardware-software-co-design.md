---
title: "GPU-Tile-Sim: A Tile-Centric GPU Simulation Framework for LLM Hardware-Software Co-Design"
title_zh: GPU-Tile-Sim：一种面向大语言模型软硬件协同设计的以 Tile 为中心的 GPU 仿真框架
authors: "Yitong Ding, Jiawei Huang, Renyang Guan, Yangjie Zhou, Zihan Liu, Yu Feng, Shixuan Sun, Mingyi Guo, Jingwen Leng, Jian Weng"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11262v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 大模型软硬件协同设计的仿真框架
tldr: 针对现代大语言模型（LLM）工作负载中高度优化的GPU算子，传统的指令级模拟器和分析模型难以兼顾效率与精度。本文提出GPU-Tile-Sim，一个以Tile为中心的GPU模拟框架。该框架将内核执行表示为Warp级的Tile图，通过捕捉Tile间的依赖关系和计算存储重叠来建模性能。实验表明，该框架在A100、H100及Blackwell架构上对GEMM和注意力机制等算子具有极高的预测精度，是LLM软硬件协同设计的有效工具。
selection_source: fresh_fetch
motivation: 现有的GPU性能模型难以在保持低开发成本的同时，准确捕捉现代LLM优化算子中复杂的细粒度依赖和计算存储重叠特性。
method: 提出一种以Tile为中心的模拟框架，将内核执行抽象为Warp级Tile图，利用自动前端生成图结构并由图驱动后端进行模拟。
result: "在A100和H100上对多种算子进行评估，平均绝对百分比误差（MAPE）仅为1.22%至8.71%，并成功扩展至Blackwell架构。"
conclusion: GPU-Tile-Sim为LLM软硬件协同设计提供了一个高精度且灵活的性能分析工具，能有效指导软件优化和架构设计选择。
---

## 摘要
现代大语言模型（LLM）工作负载日益依赖通过软硬件协同设计优化的 GPU 核函数。这些核函数通过细粒度的依赖调度以及计算与内存的重叠来实现高性能。因此，它们对现有的 GPU 性能模型提出了新的挑战。指令驱动的仿真器难以适应不断演进的架构且成本高昂，而分析模型又过于粗糙，无法捕捉核函数的特性。我们提出了 GPU-Tile-Sim，这是一个面向 LLM 软硬件协同设计的以 Tile 为中心的 GPU 仿真框架。其核心见解在于，现代 LLM 核函数的性能受单个指令延迟的影响较小，而更多地取决于控制执行顺序和重叠的依赖结构。因此，GTSim 将核函数的执行表示为 Warp 级的 Tile 图，其节点捕捉 Tile 级的操作，边则编码数据和顺序约束。利用这种表示，我们设计了一个自动 Tile 图前端和一个图驱动的仿真后端。我们在代表性的 GEMM、注意力机制和端到端 LLM 推理工作负载上评估了 GTSim。在 A100 和 H100 上，针对传统和高度优化的核函数，GTSim 均实现了极高的性能建模准确度（平均绝对百分比误差 MAPE 为 1.22%--8.71%）。我们进一步将 GTSim 扩展到 Blackwell 架构并进行了初步验证，展示了其在分析软件和架构设计选择方面的有效性。

## Abstract
Modern LLM (large language model) workloads increasingly rely on optimized GPU kernels through hardware-software co-design. These kernels achieve high-performance through fine-grained dependency scheduling and computation-memory overlap. As such, they incur new challenges on existing GPU performance models. Instruction-driven simulators are costly to adapt to evolving architectures, while analytical models are too coarse to capture kernels' characteristics. We propose GPU-Tile-Sim, a tile-centric GPU simulation framework for LLM hardware-software co-design. The key insight is that modern LLM kernel performance is governed less by individual instruction latency than by the dependency structure that controls execution order and overlap. Accordingly, GTSim represents kernel execution as a warp-level tile graph whose nodes capture tile-level operations and whose edges encode data and ordering constraints. Using this representation, we design an automatic tile-graph frontend and a graph-driven simulation backend. We evaluate GTSim on representative GEMM, attention, and end-to-end LLM inference workloads. On A100 and H100 across both conventional and highly optimized kernels, GTSim achieves high performance-modeling accuracy (MAPE, Mean Absolute Percentage Error, 1.22%--8.71%). We further extend GTSim to Blackwell with preliminary validation, and demonstrate its effectiveness in analyzing software and architectural design choices.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **GPU-Tile-Sim (GTSim)** 的仿真框架，旨在解决现代大语言模型（LLM）在 GPU 上进行软硬件协同设计时的性能建模难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：现代 LLM 工作负载高度依赖优化的 GPU 核函数（Kernels），这些核函数利用了如 Tensor Memory Accelerator (TMA)、分布式共享内存 (DSMEM) 和 Warp Specialization 等硬件特性。
*   **现有模型的局限性**：
    *   **指令驱动模拟器**（如 GPGPU-Sim）：虽然精度高，但模拟速度慢，且难以快速适配频繁迭代的新硬件架构（如 Hopper, Blackwell）。
    *   **分析模型**（如 TileFlow, LLMCompass）：通常过于粗糙，无法捕捉细粒度的异步执行、计算与内存重叠以及复杂的 Warp 协作模式。
*   **核心挑战**：如何在保持高仿真效率的同时，准确建模现代 GPU 核函数中由依赖关系驱动的执行逻辑。

### 2. 方法论
GTSim 的核心思想是：**现代 GPU 核函数的性能主要受控于 Tile 级别的依赖结构和重叠模式，而非单个指令的延迟。**
*   **以 Warp 为中心的 Tile 图 (Warp-Centric Tile Graph)**：
    *   **节点 (Nodes)**：代表 Warp 或 Warp 组执行的 Tile 级操作（如矩阵乘法 MMA、数据加载 Load）。包含操作类型、执行者信息和 Tile 描述符（存储位置、形状、布局）。
    *   **边 (Edges)**：分为**数据边**（编码数据流依赖）和**顺序边**（编码同步、缓冲区重用等非数据流约束）。
*   **自动前端 (Frontend)**：从 TileLang IR（一种领域特定语言的中间表示）中自动提取 Tile 图，保留了循环结构、Warp 角色分配和同步原语。
*   **图驱动后端 (Backend)**：
    *   **执行流**：直接调度 Tile 图中就绪的节点，而非逐条执行指令。
    *   **硬件建模**：采用基于吞吐量的抽象模型，涵盖计算流水线（Tensor Core, CUDA Core）、内存系统（L2, DRAM, TMA）和片上网络（NoC/DSMEM）。
    *   **依赖驱动运行时**：通过跟踪节点的入度来动态激活就绪节点，支持 Warp 间的并发执行建模。

### 3. 实验设计
*   **测试场景与算子**：
    *   **GEMM**：常规 GEMM、融合了 SiLU 激活函数的 GEMM、FP8 精度 GEMM。
    *   **注意力机制 (Attention)**：FlashAttention-3、Flash-Decoding、FlashMLA。
    *   **端到端推理**：Llama-3-8B 模型的完整解码层。
*   **基准测试 (Benchmarks)**：
    *   **硬件平台**：NVIDIA A100 和 H100 SXM。
    *   **对比方法**：TileFlow（基于映射的分析模型）、LLMCompass（分析模型）、Accel-Sim（指令级模拟器）。

### 4. 资源与算力
*   **硬件型号**：实验主要在 A100 和 H100 GPU 上进行验证。
*   **仿真效率**：在 A100 上的 GEMM 实验中，GTSim 的仿真速度比指令级模拟器 Accel-Sim 快约 **3.48x 至 4.60x**。
*   **算力消耗**：论文未明确提及运行仿真器本身的服务器具体配置或总训练时长，但强调了其轻量级特性，图构建耗时仅在 0.01s-0.08s 级别。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 64 种不同形状的 GEMM 进行了参数化扫描。
    *   涵盖了从单算子到端到端全模型推理的多个维度。
    *   进行了消融实验，验证了“顺序边”和“同步机制”对建模精度的必要性。
*   **充分性评价**：实验设计较为充分，不仅对比了多种现有模型，还跨越了两个硬件世代（Ampere 和 Hopper），并对最新的 Blackwell 架构进行了初步探索，证明了模型的泛化能力和客观性。

### 6. 主要结论与发现
*   **高精度**：在 A100/H100 上，GTSim 的平均绝对百分比误差 (MAPE) 仅为 **1.22%–8.71%**，显著优于 TileFlow 和 LLMCompass。
*   **架构适配性**：通过简单的硬件参数更新和少量节点语义扩展，GTSim 能够快速适配 Blackwell (B200) 架构，并准确预测 FlashAttention-4 的性能趋势。
*   **设计指导**：案例研究表明，GTSim 能有效分析软件流水线深度、NoC 拓扑映射对性能的影响，为软硬件协同设计提供量化依据。

### 7. 优点
*   **抽象层次适中**：避开了复杂的指令级模拟，通过 Tile 图捕捉到了影响性能的关键因素（重叠与依赖）。
*   **自动化程度高**：支持从 DSL 直接生成仿真输入，降低了使用门槛。
*   **扩展性强**：模块化的硬件建模使其能迅速跟进 GPU 架构的快速演进。

### 8. 不足与局限
*   **单卡限制**：目前主要针对单 GPU 内部的核函数执行进行建模，尚未扩展到多卡、多节点的大规模分布式通信仿真。
*   **依赖特定前端**：虽然理论上通用，但目前的自动化流程高度依赖 TileLang IR，对于手写的复杂 CUDA 代码可能需要额外的转换工具。
*   **Blackwell 验证尚浅**：由于 Blackwell 硬件获取受限，该部分的验证主要基于已发表的论文数据，缺乏大规模实测对比。

（完）
