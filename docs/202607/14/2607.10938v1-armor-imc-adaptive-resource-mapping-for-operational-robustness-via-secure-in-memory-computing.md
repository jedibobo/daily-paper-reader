---
title: "ARMOR-IMC: Adaptive Resource Mapping for Operational Robustness via Secure In-Memory Computing"
title_zh: ARMOR-IMC：基于安全存内计算的运行鲁棒性自适应资源映射
authors: "Muhtasim Alam Chowdhury, Ramtin Zand, Soheil Salehi"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10938v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 安全内存计算的硬件和软件协同设计
tldr: 针对存内计算（IMC）架构在深度学习处理中面临的工艺偏差（PV）和侧信道攻击（SCA）双重威胁，本文提出ARMOR-IMC框架。该框架无需重新训练模型，通过变异影响评分（VIS）指导故障观测窗口映射，并利用推理泄漏（LPI）指标量化并降低功耗变动，从而在提升硬件可靠性的同时增强安全性，实现了鲁棒性与安全性的协同优化。
selection_source: fresh_fetch
motivation: 存内计算架构虽能解决数据搬运瓶颈，但面临制造工艺偏差导致的可靠性下降及侧信道攻击带来的安全风险。
method: 提出一种无需重训的后训练框架，利用变异影响评分（VIS）优化资源映射，并引入LPI指标通过随机注入来抵御功耗分析攻击。
result: 实验表明该方法能将受工艺偏差影响而大幅下降的准确率恢复至接近基准水平，并有效防御相关性功耗分析攻击。
conclusion: ARMOR-IMC为模拟存内计算加速器提供了一种兼顾运行鲁棒性与安全性的高效映射方案，证明了在不重训模型的情况下提升硬件防御能力的可行性。
---

## 摘要
传统架构中巨大的数据迁移开销促使人们采用存内计算（IMC）来实现高效能的深度神经网络（DNN）处理。通过利用自旋轨道转矩磁隧道结（SOT-MTJs）等新兴器件，IMC 绕过了“存储墙”并降低了传统 CMOS 固有的漏电功耗。然而，这种转变引入了双重硬件威胁：制造过程中的工艺偏差（PV）降低了可靠性并增加了对故障注入的脆弱性，而功耗侧信道攻击（SCAs）则危及安全性。现有的防御措施通常孤立地处理这些威胁。本研究提出了一种训练后框架，在无需重新训练模型的情况下，同时增强模拟 IMC 加速器抵御这两种威胁的能力。该方法在 IMAC-Sim 仿真器中实现，利用提出的偏差影响评分（VIS）来指导故障观测窗口（FOWs）的映射，并引入了单次推理泄漏（LPI）指标，以量化随机注入下与输入相关的功耗变异性以及由此导致的有效信噪比下降。实验表明，工艺偏差诱发的故障会使准确率下降超过 50%，而我们的方法能够恢复至接近基准的准确率，并减轻基于相关性的功耗分析攻击的威胁。

## Abstract
The massive data-movement overhead in traditional architectures has led to the adoption of In-Memory Computing (IMC) for energy-efficient Deep Neural Network (DNN) processing. By leveraging emerging devices like Spin-Orbit Torque Magnetic Tunnel Junctions (SOT-MTJs), IMC bypasses the "memory wall" and reduces leakage power inherent in traditional CMOS. However, this shift introduces dual hardware threats: manufacturing Process Variation (PV) degrades reliability and increases vulnerability to fault injection, while power Side-Channel Attacks (SCAs) compromise security. Existing defenses address these threats in isolation. This work presents a posttraining framework that simultaneously hardens analog IMC accelerators against both threats without retraining the model. Implemented in the IMAC-Sim simulator, our approach uses the proposed Variation Impact Score (VIS) to guide the mapping of Fault Observation Windows (FOWs) and introduces the Leakage Per Inference (LPI) metric to quantify input-dependent power variability under stochastic injection and the resulting reduction in effective signal-to-noise ratio. Experiments show that PV-induced faults can degrade accuracy by over 50%, while our method restores near-baseline accuracy and mitigates the threat of correlation-based power analysis attacks.

---

## 论文详细总结（自动生成）

### 论文总结：ARMOR-IMC —— 基于安全存内计算的运行鲁棒性自适应资源映射

#### 1. 核心问题与整体含义
*   **研究背景**：随着深度学习对算力的需求激增，存内计算（IMC）架构因能解决传统冯·诺依曼架构的“存储墙”问题而备受关注。特别是基于自旋轨道转矩磁隧道结（SOT-MTJ）的非易失性存储器，在能效方面具有显著优势。
*   **核心问题**：IMC 架构在实际应用中面临双重硬件威胁：
    1.  **可靠性威胁**：制造过程中的工艺偏差（PV）会导致器件电阻状态不稳定，引发比特翻转（Bit-flip），严重降低模型推理准确率。
    2.  **安全性威胁**：功耗侧信道攻击（SCA）利用功耗与处理数据之间的相关性，通过相关功耗分析（CPA）等手段窃取模型参数或输入隐私。
*   **研究动机**：现有的防御方案通常孤立地处理可靠性或安全性，且往往需要昂贵的模型重新训练。本文旨在提出一种**无需重训**的后训练框架，同时加固模拟 IMC 加速器的鲁棒性与安全性。

#### 2. 方法论
ARMOR-IMC 框架结合了架构级映射策略和随机功耗注入机制，核心步骤如下：
*   **故障观测窗口（FOW）抽象**：将神经网络层映射的交叉阵列划分为固定大小的切片（如 32×32），称为 FOW，作为故障分析的基本单位。
*   **变异影响评分（VIS）**：
    *   通过在特定 FOW 中注入高比例（50%-90%）的随机比特翻转，测量其对整体推理准确率的影响。
    *   根据准确率下降程度对 FOW 进行排名，识别出对故障最敏感的“关键区域”。
*   **动态权重映射策略**：
    *   利用 VIS 评分指导权重重定位。计算受损矩阵与参考矩阵的平均绝对误差（MAE），将关键权重从高风险（易受 PV 影响）的 FOW 交换到低风险（更可靠）的 FOW。
*   **单次推理泄漏（LPI）指标**：
    *   定义为不同输入推理间平均功耗的标准差，用于量化功耗对输入的依赖性。
*   **随机噪声注入（Stochastic Noise Injection）**：
    *   在推理窗口内引入随机采样的辅助功耗（通过时间门控电阻负载实现），使总功耗表现为非平稳随机过程，从而掩盖数据特征，降低侧信道的信噪比（SNR）。

#### 3. 实验设计
*   **仿真工具**：使用 **IMAC-Sim**（一种基于 Python 的电路级仿真框架，可生成 SPICE 网表，精确模拟互连寄生效应和功耗）。
*   **数据集**：MNIST 手写数字识别。
*   **模型架构**：三层全连接 DNN（400 × 120 × 84 × 10）。
*   **Benchmark（基准）**：
    *   无故障、无防御的原始 IMC 映射。
    *   固定故障率（5%-25%）下的未优化映射。
*   **对比实验**：
    *   针对可靠性：对比“有无 VIS 映射优化”在不同局部故障率下的准确率。
    *   针对安全性：对比“原始功耗”、“确定性功耗平衡”与“随机噪声注入”的 LPI 指标和功耗分布重叠度。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或具体的训练/仿真耗时。
*   **仿真细节**：实验基于电路级仿真，提到针对每个配置运行了 **100 次蒙特卡洛模拟**以评估工艺偏差的影响。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对 DNN 的所有三层进行了细粒度的 FOW 划分（如第一层划分为 52 个 FOW）。
    *   故障注入实验覆盖了从 50% 到 90% 的极端局部故障率。
    *   侧信道分析针对 MNIST 的 10 个数字类别，每个类别抽取 400 个推理样本进行统计。
*   **充分性评价**：实验在电路级仿真层面较为深入，通过蒙特卡洛模拟保证了统计上的客观性。然而，由于仅使用了 MNIST 数据集和较简单的全连接网络，其在复杂视觉任务（如 ImageNet）或更深层架构（如 ResNet、Transformer）上的泛化能力尚待验证。

#### 6. 主要结论与发现
*   **可靠性提升**：工艺偏差可导致准确率下降 50% 以上。通过 VIS 映射，在 Layer 3 极端故障（90%）情况下，准确率从 17% 成功恢复至 34%（接近翻倍），在其他层也实现了显著的准确率修复。
*   **安全性增强**：
    *   确定性平衡可消除 96% 以上的 LPI，但易受建模攻击。
    *   随机噪声注入（0.2-0.4W 范围）使不同数字类别的功耗分布产生显著重叠（如数字 0 和 1 的重叠达 34mW），有效掩盖了 140mW 的原始特征信号。
*   **开销**：该防御机制带来了约 **6.5% 的额外功耗开销**，但在安全性提升面前被认为是可接受的。

#### 7. 优点
*   **协同防御**：首次在一个框架内同时解决了 IMC 的可靠性（PV）和安全性（SCA）问题。
*   **部署便捷**：作为一种“后训练”方法，不需要修改模型权重或重新训练，适合已训练好的模型直接部署到硬件。
*   **细粒度评估**：提出的 VIS 指标能够精准定位硬件阵列中的脆弱物理区域。

#### 8. 不足与局限
*   **实验覆盖面有限**：仅在 MNIST 和小型 DNN 上验证，缺乏在大规模数据集和复杂模型上的表现数据。
*   **映射约束**：动态映射受限于 FOW 的尺寸兼容性（必须交换相同尺寸的块），这在处理非规则边缘切片时可能导致次优的资源利用。
*   **功耗开销**：虽然 6.5% 的开销不算极高，但对于极度追求低功耗的边缘设备，仍需进一步优化噪声注入的效率。

（完）
