---
title: "Terastate-per-second QUBO Brute-Force on a Single GPU: A Matrix Prefix-Suffix Decomposition"
title_zh: 单 GPU 上每秒万亿状态的 QUBO 暴力搜索：一种矩阵前缀-后缀分解方法
authors: "Aleksandr Maltsev, Mikhail Remnev, Alexey Kapranov, Ekaterina Krivtsova"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04857v1"
tags: ["query:hwnas"]
score: 7.0
evidence: 将问题从内存受限转为计算受限以最大化利用率
tldr: 本研究针对稠密矩阵的QUBO穷举搜索，提出了一种基于前缀-后缀分解和格雷码排序的并行算法。该算法实现了每状态O(1)的计算复杂度，并通过可调节的能量分量缓存机制将问题从内存受限转为计算受限。在单GPU上达到了每秒7.5万亿次状态评估的性能，为精确求解器的全空间搜索子类树立了新的性能基准。
selection_source: fresh_fetch
motivation: 旨在提高稠密矩阵QUBO穷举搜索的效率，解决传统方法中内存带宽受限的问题。
method: 采用前缀-后缀分解与格雷码排序相结合的策略，并利用可调节缓存优化内存访问。
result: 在单块GPU上实现了每秒7.5万亿次状态的评估速率，显著提升了计算吞吐量。
conclusion: 该算法成功将QUBO暴力破解从内存受限转变为计算受限任务，为精确求解器提供了新的性能标杆。
---

## 摘要
本文提出了一种基于前缀-后缀分解和格雷码排序的稠密矩阵并行 QUBO 穷举搜索算法。该算法实现了 O(1) 的单状态复杂度：在进行 QUBO 目标函数计算时，每个状态仅需执行一次算术运算。通过可调节的能量分量缓存大小，算法能够利用最快的可用内存层级。这显著降低了内存带宽需求至可忽略的水平，并将该问题从内存受限型转变为计算受限型。我们基于 CUDA 的实现在单个 GPU 上达到了每秒 $7.5\times10^{12}$ 个状态的尖端评估速率，为精确求解器中的全空间搜索子类树立了新的性能基准。

## Abstract
This paper presents a parallel QUBO exhaustive search algorithm for dense matrices, based on a prefix-suffix decomposition and Gray code ordering. The algorithm achieves O(1) per-state complexity: for the QUBO objective function computation only one arithmetic operation per state is performed. An adjustable energy components cache size enables placement in the fastest available memory tier. This reduces memory bandwidth requirements to a negligible level and transforms the problem from memory-bound to compute-bound. Our CUDA-based implementation achieves a state-of-the-art evaluation rate of $7.5\times10^{12}$ states per second on a single GPU, setting a new performance benchmark for the full-space-search subclass of exact solvers.