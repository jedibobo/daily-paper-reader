---
title: "MOSAIC: Adaptive Inter-layer Composition for Efficient Heterogeneous Vision-Language Models"
title_zh: MOSAIC：用于高效异构视觉语言模型的自适应层间组合
authors: "Yuncheng Yang, Feiyang Ye, Shixian Luo, Yinna Zhu, Lianlei Shan, Wangcai Zhao, Kuo Zhang, Yan Chen, Yong Wu, Yan Xie"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09029v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 针对优化异构架构的硬件感知搜索方法
tldr: 针对视觉语言模型（VLM）中同构Transformer架构效率不足的问题，本文提出MOSAIC框架。该方法通过硬件感知的多目标搜索，将同构模型自动转换为集成线性、稀疏和低秩算子的异构架构。利用混合整数规划在延迟约束下优化配置，并结合两阶段蒸馏技术快速恢复性能。实验表明，MOSAIC-4B在极低训练成本下显著提升了推理速度，为高效VLM设计提供了新范式。
selection_source: fresh_fetch
motivation: 现有的异构视觉语言模型多依赖手工设计的静态混合模式，难以针对特定硬件实现最优的性能与延迟平衡。
method: 提出一种基于混合整数规划（MIP）的硬件感知搜索方法，自动组合多种高效算子，并采用两阶段蒸馏策略进行参数恢复。
result: "MOSAIC-4B在仅消耗不到2%原始训练成本的情况下，实现了1.76倍的预填充加速和2.54倍的解码加速，且性能保持不变。"
conclusion: 通过自动化搜索异构架构并配合高效蒸馏，可以在极低成本下大幅提升视觉语言模型的硬件推理效率。
---

## 摘要
视觉语言模型 (VLM) 通过使用同构 Transformer 处理多媒体数据取得了成功。最近的研究表明，交错使用线性注意力等高效机制的异构结构，在性能和推理延迟方面均优于同构设计。然而，这些工作依赖于手工设计的静态混合模式，这并非最优解，且难以适应特定硬件。为了弥补这一差距，我们提出了 MOSAIC（自适应层间组合的多目标搜索），这是一种硬件感知的搜索方法，能够自动将同构模型转换为优化的异构架构。MOSAIC 将多种高效机制（包括线性、稀疏和低秩算子）集成到一个统一的搜索空间中。通过将选择过程建模为多目标混合整数规划 (MIP) 问题，我们的方法能够在严格的硬件延迟约束下，识别出最大化下游性能的最优配置。为了缓解结构转换带来的性能下降，我们引入了一个两阶段参数恢复过程：首先是全局离策 (off-policy) 蒸馏以稳定内部表示，随后是利用 235B 预言机进行知识扩展并利用原始 4B 教师模型维持分布稳定性的双教师就策 (on-policy) 蒸馏。我们通过源自 Qwen3-VL-4B-Instruct 的 MOSAIC-4B 验证了 MOSAIC。结果表明，MOSAIC-4B 在多个基准测试中达到了基线模型的性能，而训练成本不足原始成本的 2%。此外，它显著提高了推理效率，实现了 1.76 倍的预填充加速和 2.54 倍的解码加速。

## Abstract
Vision-Language Models (VLMs) have achieved success using homogeneous Transformers to process multimedia data. Recent studies show that heterogeneous structures interleaving efficient mechanisms, like linear attention, improve both performance and inference latency over homogeneous designs. However, these efforts rely on handcrafted static mixing patterns, which are sub-optimal and difficult to adapt to specific hardware. To bridge this gap, we propose Multi-Objective Search for Adaptive Inter-layer Composition (MOSAIC), a hardware-aware search method that automatically transforms homogeneous models into optimized heterogeneous architectures. MOSAIC integrates diverse efficiency mechanisms--including linear, sparse, and low-rank operators--into a unified search space. By formulating the selection as a multi-objective Mixed Integer Programming (MIP) problem, our method identifies optimal configurations that maximize downstream performance under strict hardware latency constraints. To mitigate performance degradation from structural transitions, we introduce a two-stage parameter recovery process: global off-policy distillation to stabilize internal representations, followed by a dual-teacher on-policy distillation leveraging a 235B oracle for knowledge expansion and the original 4B teacher for distributional stability. We validate MOSAIC through MOSAIC-4B, derived from Qwen3-VL-4B-Instruct. Results demonstrate that MOSAIC-4B matches the baseline's performance across multiple benchmarks while requiring less than 2% of the original training cost. Furthermore, it substantially improves inference efficiency, achieving 1.76x prefilling and 2.54x decoding speedups.

---

## 论文详细总结（自动生成）

这是一份关于论文《MOSAIC: Adaptive Inter-layer Composition for Efficient Heterogeneous Vision-Language Models》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的视觉语言模型（VLM）通常采用同构的 Transformer 架构（全注意力机制），在处理长文本或在资源受限硬件上部署时，面临计算复杂度高（二次方复杂度）和推理延迟大的挑战。
*   **研究动机**：虽然近期有研究尝试引入线性注意力、Mamba 等高效算子构建异构模型，但这些设计大多依赖于**手工设计的静态混合模式**（如每 3 层放一个全注意力层），这在特定硬件上并非最优，且缺乏灵活性。本文旨在实现一种**硬件感知、自动化的异构架构搜索与转换方法**。

### 2. 方法论
MOSAIC 框架包含三个核心阶段：
*   **统一的层级搜索空间**：为每一层提供多种候选算子，包括标准 GQA、线性注意力（KDA, GDN）、低秩压缩（MLA）和稀疏注意力（SWA），同时对 FFN 层进行不同比例的结构化剪枝（0%-100%）。
*   **多目标架构搜索**：
    *   **块级局部蒸馏 (BLD)**：通过对齐候选块与教师层输出的激活值，快速初始化候选池。
    *   **多能力评分**：基于 KL 散度、困惑度（PPL）及下游任务得分评估每个候选块。
    *   **混合整数规划 (MIP)**：将架构选择建模为受硬件延迟约束的多目标优化问题，利用 Tchebycheff 标量化方法求解 Pareto 最优配置。
*   **两阶段参数恢复**：
    *   **第一阶段（全局离策蒸馏）**：利用大规模文本和多模态数据稳定内部表示。
    *   **第二阶段（双教师就策蒸馏）**：同时利用 235B 预言机教师进行知识扩展，并利用原始 4B 教师维持分布稳定性。

### 3. 实验设计
*   **基准模型**：以 Qwen3-VL-4B-Instruct 为教师模型，衍生出 MOSAIC-4B。
*   **数据集与场景**：涵盖图像理解（13 个基准，如 MME, MMMU, DocVQA）、视频理解（3 个基准，如 Video-MME）和纯文本理解（3 个基准）。
*   **对比方法**：
    *   **同构模型**：Qwen3-VL-4B/2B, InternVL3.5-4B/2B, SmolVLM2-2.2B, Molmo2-4B。
    *   **异构模型**：Cobra, MatVLM, InfiniteVL。
    *   **消融实验**：对比了手工设计的固定比例架构、不同搜索目标的影响以及搜索空间的完整性。

### 4. 资源与算力
*   **硬件设备**：使用了 **64 台 NVIDIA H800-80GB GPU**。
*   **训练时长**：总计 **172 小时**（约 1.1 万 GPU 小时）。
*   **成本优势**：论文强调其训练成本**不足原始模型训练成本的 2%**（仅使用约 32M 样本进行恢复训练）。

### 5. 实验数量与充分性
*   **实验规模**：在 **19 个主流基准测试**上进行了验证，涵盖了图像、视频、文本三大模态。
*   **消融实验**：设计了多组详尽的消融实验，包括搜索空间组件（去掉线性/稀疏算子）、搜索目标（仅 KL vs. 多目标）以及恢复策略的有效性。
*   **客观性**：通过在统一的 vLLM 推理框架下测量 TTFT（首字延迟）和 TPOT（每 Token 延迟），确保了效率评估的客观性。实验覆盖了从 1.2x 到 2.5x 的多种加速约束场景。

### 6. 主要结论与发现
*   **性能持平**：MOSAIC-4B 在 19 个基准测试上的平均表现与 4B 教师模型基本持平，显著优于同等参数量的 2B 模型。
*   **效率大幅提升**：实现了 **1.76 倍的预填充加速**和 **2.54 倍的解码加速**（在长序列下加速效果更明显）。
*   **架构规律**：搜索发现，模型浅层倾向于使用稀疏注意力（SWA），中层倾向于线性注意力（KDA/GDN），而深层则保留低秩（MLA）或全注意力以维持全局聚合能力。

### 7. 优点（亮点）
*   **自动化与硬件感知**：首次实现了针对异构 VLM 的自动化硬件感知搜索，摆脱了人工设计的局限。
*   **极高性价比**：通过两阶段蒸馏技术，在极小的训练代价下完成了从同构到异构的结构转换。
*   **部署友好**：直接在 vLLM 等主流推理引擎上验证了实际加速效果，具有很强的实用价值。

### 8. 不足与局限
*   **教师模型依赖**：性能上限受限于教师模型的能力，且需要一个极大规模（235B）的预言机模型辅助蒸馏。
*   **搜索空间离散性**：搜索空间基于预定义的算子库，可能遗漏了某些更细粒度的混合算子设计。
*   **硬件环境特定性**：延迟分析基于特定的 H800 环境，在其他异构硬件（如边缘端芯片）上的表现规律可能需要重新评估。

（完）
