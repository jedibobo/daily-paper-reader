---
title: "StreamDQ: Near-Memory Weight DeQuantization in Custom HBM for Scalable AI Inference Acceleration"
title_zh: StreamDQ：定制 HBM 中的近内存权重反量化，用于可扩展的 AI 推理加速
authors: "Minki Jeong, Daegun Yoon, Soohong Ahn, Seungyong Lee, Nameun Kang, Hyeonseok Ju, Ieryung Park, Joonseop Sim, Youngpyo Joo, Hoshik Kim"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08993v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 近内存去量化的硬件软件协同设计
tldr: 针对大语言模型推理中权重量化带来的计算开销，本文提出StreamDQ架构。该方案通过在HBM内存基底中集成轻量级反量化模块（DQB），实现了近内存的内联反量化处理。StreamDQ利用侧边栏标签控制反量化模式，有效消除了GPU侧CUDA核心的指令负担和片上通信压力。实验表明，该方案在显著提升GEMM性能和能效的同时，大幅降低了端到端推理延迟并提高了吞吐量，为可扩展AI推理加速提供了高效的硬件支持。
selection_source: fresh_fetch
motivation: 现有GPU在处理大模型量化权重时，CUDA核心执行的反量化操作带来了显著的指令开销、片上流量瓶颈和流水线停顿。
method: 在HBM基底中集成轻量级反量化模块（DQB），通过内存侧的内联处理和侧边栏标签控制实现即时反量化。
result: "混合精度GEMM性能提升达7.08倍且能耗降低90.23%，端到端推理延迟最高降低54.68%，解码吞吐量提升2.20倍。"
conclusion: 将反量化任务从GPU核心下沉至内存子系统，能有效消除计算负担并优化数据传输，是提升大规模AI推理效率的关键。
---

## 摘要
随着大语言模型（LLM）规模的扩大，其内存和计算需求大幅增长，使得仅权重（weight-only）量化成为一种在保持极小精度损失的同时减小模型规模的广泛采用的技术。然而，在当前的 GPU 上，基于 CUDA 核心的反量化引入了大量的指令开销、片上流量和流水线停顿，使其成为高吞吐量、云级 LLM 服务的瓶颈。为了解决这些限制，我们提出了 StreamDQ，这是一种轻量级的架构增强方案，能够在内存子系统中实现即时（on-the-fly）反量化，以支持高吞吐量、大批次的 LLM 推理。StreamDQ 将紧凑的反量化模块（DQB）集成到高带宽内存（HBM）的基础层芯片（base die）中，并在标准内存加载时执行内联反量化。每个内存读取请求上的轻量级边带标签（sideband tag）在保留传统加载语义的同时选择反量化模式。通过将反量化迁移到内存端，StreamDQ 消除了 GPU 侧基于 CUDA 核心的反量化，从而减少了 GPU 上的片上流量，并避免了大批次情况下反量化权重的额外 HBM 写回和重新加载。我们的评估表明，在 12nm CMOS 工艺下，StreamDQ 在混合精度 GEMM 中实现了高达 7.08 倍的加速和 90.23% 的能耗降低，且每个 DQB 仅产生 0.127 mm² 的面积和 0.355 W 的功耗开销。对于端到端 LLM 推理，StreamDQ 将延迟降低了高达 54.68%，并将解码吞吐量提高了多达 2.20 倍。

## Abstract
As large language models (LLMs) scale, their memory and computation demands have grown substantially, making weight-only quantization a widely adopted technique for reducing model size with minimal accuracy loss. However, on current GPUs, CUDA-core-based dequantization introduces substantial instruction overhead, on-chip traffic, and pipeline stalls, making it a major bottleneck for high-throughput, cloud-scale LLM serving. To address these limitations, we propose StreamDQ, a lightweight architectural enhancement that enables on-the-fly dequantization in the memory subsystem for high-throughput, large-batch LLM inference. StreamDQ integrates compact DeQuantization Blocks (DQBs) into the base die of high-bandwidth memory (HBM) and performs inline dequantization on standard memory loads. A lightweight sideband tag on each memory read request selects the dequantization mode while preserving conventional load semantics. By relocating dequantization to the memory side, StreamDQ eliminates GPU-side CUDA-core-based dequantization, thereby reducing on-chip traffic on the GPU and avoiding extra HBM write-back and reload of dequantized weights at large batch sizes. Our evaluation shows that StreamDQ achieves up to 7.08$\times$ speedup and 90.23\% lower energy for mixed-precision GEMM, with only 0.127\,mm$^2$ area and 0.355\,W power overhead per DQB in a 12\,nm CMOS process. For end-to-end LLM inference, StreamDQ reduces latency by up to 54.68\% and improves decode throughput by up to 2.20$\times$.

---

## 论文详细总结（自动生成）

以下是对论文《StreamDQ: Near-Memory Weight DeQuantization in Custom HBM for Scalable AI Inference Acceleration》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在大语言模型（LLM）推理中，为了节省内存，通常采用“仅权重（Weight-only）”量化（如 W4A16）。然而，现有 GPU 架构在执行此类推理时，必须先由 **CUDA 核心**进行反量化（Dequantization），再交给 **Tensor 核心**计算。
*   **瓶颈分析**：
    *   **指令开销**：反量化操作占用了大量 CUDA 核心资源，导致 Tensor 核心空转。
    *   **数据搬运**：在大批次（Large Batch）推理时，反量化后的权重体积膨胀，无法完全存放在片上缓存，被迫写回 HBM 再重新读取，造成巨大的额外带宽消耗。
    *   **性能停顿**：CUDA 核心与 Tensor 核心之间的同步和片上流量拥塞成为高吞吐推理的主要障碍。

### 2. 方法论：核心思想与关键技术
StreamDQ 的核心思想是将反量化逻辑下沉到 **HBM 的基础层芯片（Base Die）** 中，实现“近内存处理（NMP）”。
*   **关键技术细节**：
    *   **反量化模块（DQB）**：在 HBM 的每个伪通道（Pseudo-channel）路径上集成轻量级的硬件模块，支持 INT4/INT8/FP8 到 FP16/BF16 的即时转换。
    *   **侧边栏标签（Sideband Tagging）**：通过内存请求中未使用的元数据位发送 3-bit 标签，指示 DQB 执行特定的转换模式，无需修改指令集架构（ISA）或地址映射。
    *   **伪通道感知布局（PC-Aware Layout）**：在模型部署时重新排列权重及其缩放因子（Scale/Zero-point），确保反量化所需的所有元数据都位于同一个伪通道内，避免了复杂的跨通道通信。
    *   **高效硬件实现**：
        *   **FP-to-FP 转换**：采用连线映射（Wire-mapping）而非算术电路，极大地降低了面积和功耗。
        *   **INT-to-FP 转换**：使用共享查找表（LUT）和零填充优化。
*   **算法流程**：GPU 发出带标签的读取请求 -> HBM 内部 DQB 解析标签 -> DQB 从本地缓存或内存获取 S/Z 元数据 -> 执行内联反量化 -> 返回 FP16/BF16 数据给 GPU。

### 3. 实验设计
*   **测试场景**：混合精度 GEMM 算子性能测试及端到端 LLM 推理测试。
*   **数据集/模型**：LLaMA-3.1-8B-Instruct、Qwen3-8B、Mistral-7B-Instruct-v0.3。
*   **Benchmark 与对比方法**：
    *   **软件基准**：vLLM 框架。
    *   **对比算法**：GPTQ、AWQ (v1 & v2)、TorchAO。
    *   **精度格式**：W4A16、W8A16（目标格式为 FP16 或 BF16）。

### 4. 资源与算力
*   **硬件平台**：实验基于 **NVIDIA A100 PCIe 40GB GPU**（配备 5 个 HBM2 堆栈）进行基准测量。
*   **仿真工具**：开发了自定义模拟器 **StreamDQ-Sim**（基于 Accel-Sim 和 AccelWattch），并使用真实 GPU 的测量数据进行了校准（PCC 相关系数 > 0.83）。
*   **算力说明**：论文未提及具体的训练时长（因为是推理加速研究），但详细说明了 DQB 模块在 12nm 工艺下的合成结果（面积 0.127 $mm^2$，功耗 0.355 W）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种主流开源 LLM。
    *   对比了 4 种最先进的软件优化内核。
    *   测试了从 Batch Size 1 到 128 的多种负载情况。
    *   进行了热力学仿真（FloTHERM）以验证 HBM 基底集成的散热可行性。
*   **充分性评价**：实验设计较为全面，不仅关注算子级（GEMM）的提升，还通过端到端推理延迟和吞吐量验证了实际应用价值。通过热力学和面积开销分析，证明了该硬件方案的工程可行性。

### 6. 主要结论与发现
*   **算子级提升**：相比于最先进的融合内核（Fused Kernels），StreamDQ 在混合精度 GEMM 中实现了高达 **7.08 倍** 的加速，并降低了 **90.23%** 的能耗。
*   **系统级提升**：端到端推理延迟最高降低 **54.68%**，解码吞吐量提升达 **2.20 倍**。
*   **大批次优势**：在计算密集型（大 Batch）场景下，StreamDQ 避免了反量化权重的写回/重读，优势比小 Batch 场景更显著。
*   **硬件可行性**：DQB 模块仅占用 HBM 基底面积的约 3.36%，且热增加量在安全范围内，不会导致 DRAM 数据保持失效。

### 7. 优点
*   **架构透明性**：通过侧边栏标签实现，无需修改 GPU 核心架构或 ISA，具有良好的向后兼容性。
*   **解决 NMP 痛点**：巧妙地利用伪通道感知布局规避了传统近内存计算中棘手的地址转换（VA-to-PA）和跨通道数据一致性问题。
*   **高能效**：将数据转换移至数据源头，极大地减少了片上网络（NoC）的无效流量。

### 8. 不足与局限
*   **仿真依赖**：由于涉及定制化 HBM 硬件，所有核心结论均基于模拟器预测，而非真实硅片测试。
*   **布局依赖**：需要离线对模型权重进行特定的伪通道对齐排列，增加了模型部署的预处理复杂性。
*   **应用限制**：该方案主要针对“仅权重”量化，对于权重和激活同时量化（W8A8）的场景，其收益可能不如专门的低比特 Tensor 核心显著。
*   **硬件定制成本**：需要 HBM 厂商（如 SK Hynix）深度参与定制 Base Die，增加了供应链的垂直整合难度。

（完）
