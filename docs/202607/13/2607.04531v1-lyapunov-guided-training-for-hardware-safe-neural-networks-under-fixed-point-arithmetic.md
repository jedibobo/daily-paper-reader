---
title: Lyapunov-Guided Training for Hardware-Safe Neural Networks Under Fixed-Point Arithmetic
title_zh: 定点运算下硬件安全神经网络的李雅普诺夫引导训练
authors: "Anis Hamadouche, Amir Hussain"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04531v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对定点运算和溢出处理的硬件安全训练
tldr: 针对低功耗硬件中定点运算导致的溢出不稳定问题，本文提出了一种基于李雅普诺夫（Lyapunov）稳定性的量化框架。该方法通过层级李雅普诺夫函数监控隐藏状态能量，并利用单调投影强制约束状态演化，有效抑制了补码溢出带来的数值错误。实验表明，该方法在极低位宽下仍能保持模型稳定性，显著提升了定点神经网络在硬件部署时的推理和训练可靠性。
selection_source: fresh_fetch
motivation: 定点运算中的补码溢出回绕会严重破坏激活值的数值稳定性，导致低比特神经网络在实际硬件部署时精度大幅下降。
method: 提出一种李雅普诺夫引导的量化框架，通过层级能量监控和单调投影算子来约束隐藏状态的演化范围。
result: "在MNIST任务上，该方法将激活溢出率降至0.012%以下，并在12位定点精度下实现了86.55%的准确率，远优于传统量化训练。"
conclusion: 基于李雅普诺夫的状态控制可作为一种硬件感知的稳定机制，确保定点神经网络在资源受限环境下的可靠运行。
---

## 摘要
低精度神经网络对于资源受限的硬件具有吸引力，但定点运算引入的失效模式通常被理想化的量化模型所掩盖。特别是，补码溢出回绕（two's-complement overflow wrapping）会通过改变隐藏层激活值的幅度和符号来破坏它们，从而导致不稳定的数值误差传播和严重的精度下降。本文提出了一种针对在硬件风格回绕运算下运行的低精度神经网络的李雅普诺夫稳定量化框架。通过逐层李雅普诺夫函数监测隐藏状态能量，并应用单调投影以强制状态在深度方向上进行有界且非递增的演化。该方法在 MNIST 数据集上使用紧凑的基于 patch 的 Transformer 进行了评估，涵盖了 4 到 16 位定点位宽下的训练后量化和量化感知训练。蒙特卡洛结果显示，无约束的回绕量化感知训练在 6-16 位下会崩溃至接近随机猜测的精度，激活溢出率超过 11%。相比之下，所提出的单调李雅普诺夫投影将激活溢出抑制到 0.012% 以下，并恢复了稳定的低精度学习，在 12 位下实现了 86.55% 的准确率。这些结果表明，基于李雅普诺夫的状态控制可以作为一种硬件感知的稳定机制，用于可靠的定点神经推理和训练。

## Abstract
Low-precision neural networks are attractive for resource-constrained hardware, but fixed-point arithmetic introduces failure modes that are often hidden by idealised quantisation models. In particular, two's-complement overflow wrapping can corrupt hidden activations by changing both their magnitude and sign, leading to unstable numerical error propagation and severe accuracy degradation. This paper proposes a Lyapunov-stabilised quantisation framework for low-precision neural networks operating under hardware-style wrapping arithmetic. The hidden-state energy is monitored through a layerwise Lyapunov function, and a monotone projection is applied to enforce bounded and non-increasing state evolution across depth. The method is evaluated on MNIST using a compact patch-based transformer under post-training quantisation and quantisation-aware training with fixed-point bit-widths from 4 to 16 bits. Monte Carlo results show that unconstrained wrapped quantisation-aware training collapses to near-chance accuracy across 6-16 bits, with activation overflow rates exceeding 11%. In contrast, the proposed monotone Lyapunov projection suppresses activation overflow to below 0.012% and restores stable low-precision learning, achieving 86.55% accuracy at 12 bits. These results demonstrate that Lyapunov-based state control can act as a hardware-aware stabilisation mechanism for reliable fixed-point neural inference and training.

---

## 论文详细总结（自动生成）

### 论文总结：定点运算下硬件安全神经网络的李雅普诺夫引导训练

#### 1. 核心问题与研究动机
*   **核心问题**：在资源受限的硬件（如 FPGA、ASIC）上部署深度学习模型时，通常使用定点运算（Fixed-Point Arithmetic）。传统的量化研究多假设溢出时进行“饱和截断”（Clipping），但实际硬件逻辑（如补码运算）往往表现为“溢出回绕”（Wrapping）。
*   **研究动机**：溢出回绕会导致数值的符号和幅度发生剧烈跳变（例如，最大正数加 1 变成最小负数）。这种不连续的数值错误在残差网络或 Transformer 等深层架构中会随深度传播，导致隐藏状态动力学失稳，使模型精度彻底崩溃。本文旨在通过控制理论中的李雅普诺夫（Lyapunov）稳定性方法，约束网络内部状态，防止其进入溢出高风险区域。

#### 2. 方法论
*   **核心思想**：将神经网络的层级演化视为离散动力系统，通过定义层级李雅普诺夫能量函数来监控隐藏状态，并利用单调投影算子强制执行能量约束。
*   **关键技术细节**：
    *   **能量定义**：定义层级李雅普诺夫函数 $V(h_\ell)$ 为隐藏状态 $h_\ell$ 的归一化 Frobenius 范数平方，衡量状态的平均能量。
    *   **安全阈值**：根据训练集初始输入的能量分布，设定一个安全能量上限 $V_{max}$。
    *   **单调投影（Monotone Projection）**：在每一层更新后，应用投影算子 $\Pi$，强制要求当前层能量 $V(h_{\ell+1})$ 不超过前一层能量 $V(h_\ell)$ 且不超过 $V_{max}$。
    *   **硬件感知流程**：在量化感知训练（QAT）中，投影操作发生在定点数“写回”内存之后。如果写回过程再次触发溢出，则执行“修复投影”（Repair Projection），确保最终存储的状态符合稳定性约束。
*   **算法流程**：计算层更新 -> 定点回绕处理 -> 李雅普诺夫投影 -> 定点写回 -> 修复投影。

#### 3. 实验设计
*   **数据集与场景**：使用 **MNIST** 手写数字识别任务。
*   **模型架构**：采用紧凑的 **Patch-based Transformer**（4层，64维嵌入，16个 token）。
*   **Benchmark 与对比方法**：
    *   **FP32 基准**：全精度模型。
    *   **PTQ Wrap**：带溢出回绕的训练后量化。
    *   **QAT Wrap**：带溢出回绕的量化感知训练（无约束）。
    *   **Lyapunov-Stabilized 版本**：在上述 PTQ 和 QAT 基础上加入单调投影约束。
*   **位宽范围**：测试了 4, 6, 8, 10, 12, 16 位定点位宽。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：提到了使用 AdamW 优化器，全精度训练 5 个 epoch（学习率 $2 \times 10^{-3}$），量化微调学习率为 $5 \times 10^{-4}$。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多组位宽扫描（4-16 bits）。
    *   每组实验均采用 **3 次蒙特卡洛运行**（不同随机种子），并报告了均值和标准差。
*   **充分性评价**：实验设计较为客观，通过对比“无约束”与“李雅普诺夫约束”在相同硬件模拟环境下的表现，清晰地展示了溢出回绕的影响。然而，实验仅限于 MNIST 这一简单数据集，对于大规模数据集（如 ImageNet）或更深的模型（如 BERT/GPT）的泛化性尚待验证。

#### 6. 主要结论与发现
*   **QAT 崩溃现象**：在存在溢出回绕的情况下，标准的 QAT 在 6-16 位宽下均告失败，准确率降至随机水平（约 10%），激活溢出率高达 11%-14%。
*   **稳定性恢复**：引入李雅普诺夫投影后，激活溢出率被抑制到 **0.012% 以下**。
*   **精度表现**：在 12 位定点下，该方法实现了 **86.55%** 的准确率，接近全精度投影模型的水平（86.95%），证明了该约束能有效引导模型在安全的数值空间内学习。
*   **位宽极限**：在 4 位极低精度下，无论是否使用该方法，模型均失效，这归因于表示能力坍缩而非溢出不稳定。

#### 7. 优点
*   **硬件真实性**：不同于大多数模拟“饱和截断”的量化论文，本文直面硬件中真实的“补码回绕”问题，具有极强的工程参考价值。
*   **理论结合紧密**：将经典的李雅普诺夫稳定性理论引入深度学习量化领域，为解决数值不稳定性提供了严谨的数学框架。
*   **效果显著**：在不改变硬件位宽的前提下，仅通过修改训练/推理逻辑就解决了 QAT 崩溃的问题。

#### 8. 不足与局限
*   **任务复杂度低**：仅在 MNIST 上验证，缺乏在复杂视觉或自然语言处理任务上的表现数据。
*   **计算开销**：虽然投影操作在数学上简单，但在硬件推理中增加层级范数计算和投影缩放会带来额外的延迟和面积开销，论文未对此进行量化评估。
*   **超参数依赖**：安全阈值 $V_{max}$ 的设定依赖于对初始能量分布的估计，可能需要针对不同任务进行精细调优。

（完）
