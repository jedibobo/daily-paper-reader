---
title: Commissioning and Low Latency Operation of the Graph Neural Network Electromagnetic Calorimeter Trigger at the Belle II Experiment
title_zh: Belle II 实验中图神经网络电磁量能器触发器的调试与低延迟运行
authors: "M. Neu, F. Baptist, I. Haide, Y. Unno, J. Becker, T. Ferber, K. Arai, Y. -T. Lai, T. Koga, M. Maushart, H. Nakazawa, V. Savinov, K. Unger"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09347v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于FPGA的GNN硬件算法协同设计
tldr: 本研究介绍了Belle II实验中基于图神经网络的电磁量能器触发模块（GNN-ETM）的调试与运行。该系统将量能器单元视为图节点进行聚类和特征提取，通过FPGA硬件与算法的协同设计，实现了仅1.053微秒的超低延迟。研究完成了系统与一级触发器的集成，并开发了慢控制驱动和在线监测功能，验证了其在Belle II一级触发系统中的可靠性与高效性。
selection_source: fresh_fetch
motivation: 旨在为Belle II实验开发一种能够满足一级触发系统严格延迟要求的、基于图神经网络的高效电磁量能器触发模块。
method: 采用硬件-算法协同设计方法，在FPGA上实现GNN架构，将量能器触发单元作为图节点进行处理，并集成慢控制与在线监测系统。
result: 成功实现了1.053微秒的系统总延迟，并通过RTL仿真验证了硬件实现与离线参考模型在位精度上的一致性。
conclusion: GNN-ETM已成为一个功能完备、低延迟且具备在线监控能力的触发模块，完全符合Belle II一级触发系统的运行要求。
---

## 摘要
我们介绍了在 SuperKEKB 对撞机上的 Belle II 实验中，图神经网络电磁量能器触发模块（GNN-ETM）的调试与运行。GNN-ETM 将量能器触发单元作为图节点进行处理，以执行聚类和特征提取。我们将该系统与一级触发器的后续阶段完全集成，开发了慢控制驱动程序，并增加了在线监测功能。通过硬件-算法协同设计，我们优化了现有的基于 FPGA 的架构，实现了 1.053 μs 的系统总延迟。我们的硬件实现通过寄存器传输级仿真得到了验证，实现了与离线参考模型的位准确一致。在线监测实现了瞬时触发率的测量，为触发级性能研究提供了定量依据。总之，我们报告了 GNN-ETM 作为一个功能完备、具有在线控制和监测能力的低延迟触发模块，其性能符合 Belle II 一级触发系统的延迟要求。

## Abstract
We present the commissioning and operation of the Graph Neural Network Electromagnetic Calorimeter Trigger Module (GNN-ETM) of the Belle II experiment at the SuperKEKB collider. The GNN-ETM processes calorimeter trigger cells as graph nodes to perform clustering and feature extraction. We fully integrate the system with the successive stages of the first-level trigger, develop slow-control drivers, and add online monitoring capabilities. We optimise the existing FPGA-based architecture through hardware-algorithm co-design, achieving an overall system latency of 1.053 us. Our hardware implementation is validated through register-transfer-level simulations, achieving bit-accurate agreement with the offline reference model. Online monitoring enables the measurement of instantaneous trigger rates, providing a quantitative basis for trigger-level performance studies. In summary, we report on the GNN-ETM as a fully operational, low-latency trigger module with online control and monitoring capabilities, compatible with the latency requirements of the Belle II first-level trigger system.

---

## 论文详细总结（自动生成）

### Belle II 实验中图神经网络电磁量能器触发器的调试与低延迟运行总结

#### 1. 核心问题与整体含义
本论文的核心问题是如何在 Belle II 实验的一级（L1）触发系统中，实现基于图神经网络（GNN）的电磁量能器（ECL）实时聚类算法，并满足极其严苛的**硬实时延迟要求**。
*   **背景**：Belle II 实验产生的数据量巨大，必须通过 L1 触发器进行在线过滤。
*   **动机**：早期的 GNN-ETM 原型虽然实现了 FPGA 部署，但其 3.168 μs 的延迟超过了 L1 触发器 1.067 μs 的截止时间，且缺乏与后续触发阶段（如全局决策逻辑 GDL）的集成及在线监控能力。本研究旨在通过硬件-算法协同设计，使 GNN 触发器达到生产环境可用的状态。

#### 2. 方法论
论文提出了通过**硬件-算法协同设计（Hardware-Algorithm Co-design）**来优化系统：
*   **模型压缩（Sunset CaloClusterNet）**：
    *   将 GravNet 块从两个减少到一个，降低网络复杂度。
    *   采用层间异构定点量化（QKERAS），强制限制为 8 位精度，并使用 2 的幂次量化以简化硬件重标定。
    *   引入随机舍入（Stochastic Rounding）和量化噪声训练，以弥补精度损失。
*   **硬件架构优化**：
    *   **手动布局布线（Floorplanning）**：针对 AMD Ultrascale XCVU190 FPGA 的三 SLR 结构进行优化，将 GNN 加速器限制在单个 SLR 内，提高运行频率至 254 MHz。
    *   **DSP 映射优化**：禁用硬核 DSP 模块，改用分布式逻辑实现乘法，以减少布线拥塞并降低流水线延迟。
*   **系统集成**：开发了后处理模块（生成触发位 Trigger Bits）、慢控制驱动（基于 VME 总线）和在线监测系统（基于 NSM2 和 EPICS 数据库）。

#### 3. 实验设计
*   **数据集/场景**：实验在 Belle II 探测器的真实运行环境中进行，包括**宇宙线运行（Cosmic Runs）**和**束流运行（Beam Runs）**。
*   **Benchmark（基准）**：现有的 **ICN-ETM**（基于传统孤立聚类逻辑的触发模块）。
*   **对比方法**：论文对比了四个设计迭代版本（从原始的 Armadillo 模型到最终优化的 Sunset 4 模型），评估其在延迟、资源占用和触发率上的表现。
*   **验证方式**：通过周期精确的 RTL 仿真验证位准确度（Bit-accurate），并在 GDL 模块上实测端到端延迟。

#### 4. 资源与算力
*   **训练算力**：论文提到 GNN 模型的训练是在卡尔斯鲁厄理工学院（KIT）科学计算中心的 **TOpAS GPU 集群**上完成的。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量以及训练所需的总时长。
*   **硬件平台**：部署平台为第四代通用触发板（UT4），搭载 AMD Ultrascale XCVU190 FPGA。

#### 5. 实验数量与充分性
*   **实验组数**：论文详细记录了 4 个设计迭代版本的性能演进，并展示了 2026 年 5 月至 6 月期间多个代表性运行周期的在线监测数据。
*   **充分性与客观性**：实验涵盖了无背景噪声（宇宙线）和高背景噪声（束流）两种极端情况，对比了开启和关闭信号分类器（Signal Classifier）对触发率的影响。实验结果通过与现有成熟系统的实时对比，证明了其有效性，数据展示较为客观、透明。

#### 6. 主要结论与发现
*   **延迟大幅降低**：通过优化，端到端延迟从 3168 ns 降至 **1053 ns**（缩减了 3.01 倍），成功进入了 1067 ns 的 L1 触发预算内。
*   **资源优化**：模型压缩使 FPGA 的 LUT 和 FF 资源占用减少了约 50%。
*   **性能验证**：在线监测显示，GNN-ETM 在宇宙线运行中表现与传统系统一致；在束流运行中，通过信号分类器能显著降低背景触发率，展示了 GNN 在背景抑制方面的潜力。
*   **集成成功**：系统已完全集成到 Belle II 的慢控制和数据采集链中，具备了参与实际触发决策的能力。

#### 7. 优点
*   **极低延迟实现**：在复杂的 GNN 模型上实现了微秒级的推理延迟，是高能物理实时触发领域的重大突破。
*   **全栈集成**：不仅关注算法，还解决了慢控制、在线监控、硬件同步等实际部署中的“最后一公里”问题。
*   **硬件协同优化**：通过禁用 DSP 和手动布局等底层手段压榨硬件性能，具有很强的工程参考价值。

#### 8. 不足与局限
*   **物理性能分析尚浅**：虽然展示了触发率的趋势，但尚未进行深入的物理分析（如效率曲线、能量分辨率等）来定量证明 GNN 优于传统算法。
*   **应用限制**：目前 GNN-ETM 虽已部署，但仍处于并行监测阶段，尚未正式接管 Belle II 的最终触发决策（GDL 仅做记录，不参与逻辑）。
*   **硬件依赖**：高度依赖特定的 FPGA 架构和手动优化，算法的可移植性（如迁移到不同型号的 FPGA）可能受限。

（完）
