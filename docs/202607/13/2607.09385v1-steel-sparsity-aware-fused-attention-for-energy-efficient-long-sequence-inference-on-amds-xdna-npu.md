---
title: "STEEL: Sparsity-Aware Fused Attention for Energy-Efficient Long-Sequence Inference on AMD's XDNA NPU"
title_zh: STEEL：针对 AMD XDNA NPU 的能效型长序列推理的稀疏感知融合注意力机制
authors: "Victor J. B. Jung, Gagandeep Singh, Joseph Melber, Kristof Denolf, Francesco Conti, Luca Benini"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09385v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 将注意力机制映射到NPU以实现能效推理
tldr: 针对笔记本SoC上大语言模型推理的能效挑战，本文提出了STEEL，这是首个针对AMD XDNA架构NPU的开源FlashAttention实现。STEEL通过数据流公式化预填充注意力机制，并利用稀疏感知流水线布局解决因果掩码导致的负载不均衡问题。实验表明，STEEL在AMD Ryzen AI 9 HX 370上显著降低了能耗，且在延迟表现上大幅优于现有的CPU、GPU及NPU基准实现。
selection_source: fresh_fetch
motivation: 在笔记本SoC上，由于NPU架构的复杂性和数据移动编程的困难，难以高效实现兼顾隐私与能效的注意力机制。
method: 提出了一种针对XDNA NPU的数据流公式化预填充注意力机制，并结合稀疏感知流水线布局来优化空间并行度和内存利用率。
result: 实验显示STEEL的能耗比CPU和GPU分别降低了9.17倍和1.75倍，且在XDNA 2架构上比层级实现快22.8倍。
conclusion: STEEL通过针对NPU架构的深度优化，为移动端设备实现长序列、高能效的大模型推理提供了高效的开源解决方案。
---

## 摘要
基于大语言模型的智能体在操作系统工作流中的日益普及，提升了在笔记本电脑级片上系统（SoC）上进行能效型推理的重要性。虽然云端卸载仍然常见，但它引入了可靠性和隐私问题，这对于智能体工作负载而言尤为突出。因此，近期的笔记本 SoC 集成了针对能效优化的神经网络处理单元（NPU）；然而，由于架构多样性和显式数据移动编程模型，将注意力机制有效映射到 NPU 上仍然具有挑战性。在这项工作中，我们提出了 STEEL，这是首个针对类 XDNA NPU 的 FlashAttention 开源实现。STEEL 引入了预填充注意力的算子流公式，能够高效利用空间并行性和片上存储。此外，STEEL 通过在 NPU 阵列上采用稀疏感知流水线放置，解决了由因果掩码引起的负载不均衡问题，从而减少了同步开销并提升了利用率。我们在 AMD Ryzen AI 9 HX 370 SoC 上评估了 STEEL，并将其性能与优化的 CPU 和 GPU 实现进行了对比。实验结果表明，与 CPU 和 GPU 基准相比，STEEL 的能耗平均分别降低了 9.17 倍和 1.75 倍。在 XDNA 1 上，STEEL 相比现有技术实现了平均 9.6 倍的延迟降低；在 XDNA 2 上，与逐层注意力实现相比，它实现了平均 22.8 倍的加速。

## Abstract
The growing adoption of large language model-based agents within operating system workflows has increased the importance of energy-efficient inference on laptop-class systems-on-chip (SoCs). While cloud offloading remains common, it introduces reliability and privacy concerns that are particularly problematic for agentic workloads. Recent laptop SoCs, therefore, incorporate neural processing engines (NPUs) optimized for energy efficiency; however, effectively mapping attention mechanisms onto NPUs remains challenging due to architectural diversity and explicit data-movement programming models. In this work, we present STEEL, the first open-source implementation of FlashAttention targeting XDNA-like NPUs. STEEL introduces a dataflow formulation of prefill attention, enabling efficient exploitation of spatial parallelism and on-chip memory. Furthermore, STEEL addresses the load imbalance induced by the causal mask by leveraging a sparsity-aware pipeline placement onto the NPU array, reducing synchronization overhead and improving utilization. We evaluate STEEL on the AMD Ryzen AI 9 HX 370 SoC and compare its performance against optimized CPU and GPU implementations. Experimental results show that STEEL reduces energy consumption by an average of 9.17x and 1.75x relative to CPU and GPU baselines, respectively. On XDNA 1, STEEL achieves an average 9.6x latency reduction over the prior state of the art, and delivers a 22.8x speedup on average compared to a layer-by-layer attention implementation on XDNA 2.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **STEEL**，这是针对 AMD XDNA 架构 NPU（神经网络处理单元）优化的首个开源 FlashAttention 实现，旨在提升笔记本电脑端大语言模型（LLM）长序列推理的能效。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着 AI 智能体集成到操作系统工作流，笔记本 SoC 需要在本地实现高能效推理，以解决云端处理带来的隐私、延迟和可靠性问题。
*   **核心挑战**：
    *   **NPU 编程困难**：NPU 采用显式数据移动编程模型和空间数据流架构，将复杂的注意力机制（Attention）映射到其上极具挑战。
    *   **预填充阶段瓶颈**：在长序列推理中，注意力机制的预填充（Prefill）阶段是计算和能耗的主要来源。
    *   **负载不均衡**：因果掩码（Causal Mask）会导致计算任务在 NPU 阵列中分布不均，降低硬件利用率。

### 2. 方法论
STEEL 提出了一种针对类 XDNA 架构的算子流（Dataflow）公式化 FlashAttention 实现：
*   **三阶段流水线分解**：将 FlashAttention-2 算法分解为三个阶段，分别映射到不同的 AIE（AI 引擎）核心上：
    1.  **第一阶段**：计算注意力分数 $A_{ij} = Q_i K_j^T$。
    2.  **第二阶段**：应用在线 Softmax，计算指数和统计量。
    3.  **第三阶段**：进行加权累加得到输出 $O_i$。
*   **稀疏感知流水线布局（Sparsity-Aware Placement）**：针对因果掩码导致的稀疏性，STEEL 不再采用均匀的行分配，而是通过重新分配行索引，使每个流水线组处理的非掩码块数量趋于一致，从而减少同步等待。
*   **高效数据移动**：利用 XDNA 2 的 Mem Tile DMA 支持 4-D 传输的特性，在片上完成布局转换（Swizzling），避免了中间张量写回 DRAM 的开销。

### 3. 实验设计
*   **硬件平台**：AMD Ryzen™ AI 9 HX 370 SoC（包含 12 核 Zen5 CPU、RDNA 3.5 GPU 和 XDNA 2 NPU）。
*   **对比基准（Benchmarks）**：
    *   **CPU**：使用 PyTorch 2.1 优化的 FlashAttention 后端。
    *   **GPU**：使用 ROCm 6.4 优化的 FlashAttention 实现。
    *   **NPU 现有技术**：与 XDNA 1 上的 SOTA 实现 DATO 以及 XDNA 2 上的逐层（Layer-by-layer）实现进行对比。
*   **测试场景**：
    *   **BERT 配置**：12 头，头维度 $d=64$。
    *   **Llama 3.1-1B 配置**：32 头，头维度 $d=64$，序列长度从 2048 扫频至 32768。

### 4. 资源与算力
*   **算力设备**：实验直接在 **AMD Ryzen™ AI 9 HX 370** 零售级 SoC 上运行。
*   **NPU 配置**：XDNA 2 NPU 运行在 Turbo 模式（1.8 GHz），峰值性能约为 50 TOPS。
*   **训练/推理时长**：论文侧重于推理性能，延迟结果取 50 次迭代的平均值，功耗测量采用 AMD AGT 工具以 50ms 采样率获取。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2048 到 32768 的多种序列长度，对比了 CPU、GPU、NPU 三种不同的计算引擎。
*   **消融与对比**：进行了稀疏感知布局与均匀布局的对比实验（提升 38% 性能），以及跨代架构（XDNA 1 vs XDNA 2）的移植性验证。
*   **评价**：实验设计较为充分，对比了当前主流的开源优化库，且在真实硬件上进行了端到端的能效测量，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **能效显著提升**：在 Llama 3.1 负载下，STEEL 的能耗比 CPU 平均降低 **9.17 倍**，比 GPU 平均降低 **1.75 倍**。
*   **延迟大幅优化**：
    *   在 XDNA 1 上，比之前的 SOTA 实现（DATO）快 **9.6 倍**。
    *   在 XDNA 2 上，比非融合的逐层实现快 **22.8 倍**。
*   **稀疏优化有效性**：稀疏感知布局成功解决了因果掩码带来的失速问题，相比传统布局减少了 38% 的延迟。

### 7. 优点
*   **开源贡献**：作为首个针对 XDNA NPU 的开源 FlashAttention 实现，填补了该领域空白。
*   **架构深度适配**：充分利用了 AIE 核心的 VLIW 指令集和 Mem Tile 的 4-D DMA 能力，而非简单的算子移植。
*   **解决负载均衡**：创新性地提出了稀疏感知布局，这在针对 NPU 的注意力机制优化中较为少见。

### 8. 不足与局限
*   **算子覆盖范围**：目前主要针对 **Prefill（预填充）** 阶段进行优化，未详细讨论 Decoding（解码）阶段的 KV Cache 管理优化。
*   **维度限制**：实验主要集中在头维度 $d=64$ 的模型上，对于更大维度（如 $d=128$）的适配性和性能表现未做深入探讨。
*   **硬件依赖**：虽然声称具有移植性，但其核心优化高度依赖于类 XDNA 的空间数据流架构，在其他类型的 NPU（如基于脉动阵列的架构）上可能无法直接应用。

（完）
