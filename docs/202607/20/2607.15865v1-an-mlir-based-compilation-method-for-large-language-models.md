---
title: An MLIR-Based Compilation Method for Large Language Models
title_zh: 一种基于 MLIR 的大语言模型编译方法
authors: "Pengchao Hu, Zhibin Xin, Yifan Chen, Yangyang Zhou, Liang Wang"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15865v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 基于 MLIR 的编译方法，针对大模型进行硬件相关的算子映射
tldr: 针对大语言模型在AI加速器上部署时面临的模型导入与推理调度难题，本文提出了一种基于MLIR的编译方法。该方法利用TopOp和TpuOp两级方言实现从模型语义到硬件指令的逐层转换，并将Transformer层拆分为prefill、prefill_kv和decode三个阶段进行静态编译优化。该方案已在TPU-MLIR编译器中实现，支持Qwen、Llama等多种主流模型及GPTQ、AWQ等量化方式，有效提升了LLM在专用硬件上的部署效率。
selection_source: fresh_fetch
motivation: 旨在解决LLM在专用AI加速器上部署时，模型导入编译器困难以及有限片上内存下自回归推理调度效率低下的核心挑战。
method: 提出基于MLIR的双层方言架构（TopOp与TpuOp），并通过将推理过程划分为三个静态编译阶段来优化Transformer层的计算特性。
result: 该方法已成功应用于TPU-MLIR和LLM-TPU项目，广泛支持了包括Qwen、Llama在内的多种生成式模型及多种主流模型量化技术。
conclusion: 本文提供了一套完整的基于MLIR的LLM编译与部署流程，为大模型在特定硬件上的高效落地提供了系统化的技术支撑。
---

## 摘要
大语言模型（LLMs）已成为现代 AI 加速器上的主要工作负载，但在专用硬件上部署它们仍面临两个核心挑战：如何将训练好的模型导入编译器友好的中间表示，以及如何在有限的片上内存下高效调度自回归推理循环。本文提出了一种基于 MLIR（多级中间表示）的大语言模型编译方法，通过 TopOp 和 TpuOp 两种算子方言（dialects）进行说明。TopOp 作为一种独立于源框架和目标芯片的高级图方言，负责表达模型语义；TpuOp 作为目标硬件方言，承载量化、层组（layer groups）和内存布局等芯片相关决策。模型首先表示为 TopOp，然后逐层降低（lowered）为 TpuOp，最后生成可部署的二进制文件。此外，每个 Transformer 层被划分为三个静态编译阶段：prefill（预填充）、prefill_kv（带有历史键值缓存的预填充）和 decode（解码），以适应 Prompt 并行处理和逐 Token 生成的不同计算特性。该方法已在 TPU-MLIR 编译器和 LLM-TPU 部署项目中实现，支持包括 Qwen、Llama、InternVL 和 MiniCPM-V 系列在内的多种生成式模型，以及 GPTQ、AWQ 和 AutoRound 等多种量化和部署形式。

## Abstract
Large Language Models (LLMs) have become the dominant workload on modern AI accelerators, yet deploying them on specialized hardware still faces two core challenges: how to import a trained model into a compiler-friendly intermediate representation, and how to efficiently schedule the autoregressive inference loop under limited on-chip memory. This paper presents an MLIR (Multi-Level Intermediate Representation) based compilation method for large language models, illustrated using two dialects of operators, TopOp and TpuOp. TopOp serves as a high-level graph dialect that is independent of both the source framework and the target chip, and is responsible for expressing model semantics; TpuOp serves as the target hardware dialect, carrying chip-related decisions such as quantization, layer groups, and memory layout. A model is first represented as TopOp, then lowered layer by layer to TpuOp, and finally a deployable binary is generated. In addition, each Transformer layer is split into three stages for static compilation: prefill, prefill_kv (prefill with historical key-value cache), and decode, so as to accommodate the different computational characteristics of prompt-parallel processing and per-token generation. The method has been implemented in the TPU-MLIR compiler{https://github.com/sophgo/tpu-mlir} and the LLM-TPU deployment project\footnote{https://github.com/sophgo/LLM-TPU}, supporting a variety of generative models including the Qwen, Llama, InternVL, and MiniCPM-V series, as well as multiple quantization and deployment forms such as GPTQ, AWQ, and AutoRound.

---

## 论文详细总结（自动生成）

这篇论文由算能（Sophgo）团队撰写，介绍了一种基于 MLIR（多级中间表示）的大语言模型（LLM）编译方法，旨在解决 LLM 在专用 AI 加速器上部署时的效率与兼容性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：LLM 在专用 AI 加速器（如 TPU）上部署面临两大难题：
    1.  **模型导入困难**：如何将训练框架（如 PyTorch）中的模型高效转换为编译器友好的中间表示。
    2.  **推理调度复杂**：自回归推理循环在有限的片上内存下，难以高效调度 Prompt 处理（Prefill）与逐 Token 生成（Decode）这两个计算特性迥异的阶段。
*   **研究动机**：传统的为每个硬件手写算子库的方法成本高且难以跟上模型演进。本文旨在通过 MLIR 基础设施，实现一种自动化、模块化且硬件无关的编译流程。

### 2. 论文提出的方法论
核心思想是利用 MLIR 的多级方言（Dialect）架构，将模型逐层抽象并针对推理阶段进行静态拆分。
*   **双层方言架构**：
    *   **TopOp（高级图方言）**：独立于框架和芯片，表达模型语义（如 `MatMul`, `RMSNorm`, `FAttention`）。它保留了高层融合边界（如 MLP 融合），便于后续优化。
    *   **TpuOp（目标硬件方言）**：承载芯片相关决策，包括量化（INT8/BF16/F16）、权重压缩格式（AWQ/GPTQ）、内存布局（Layer Group）和物理地址分配。
*   **三阶段静态编译拆分**：
    *   **Prefill（预填充）**：并行处理输入 Prompt，生成初始 KV Cache。
    *   **Prefill_kv（带历史缓存的预填充）**：用于多轮对话，将历史 KV Cache 与新 Prompt 拼接。
    *   **Decode（解码）**：逐个生成 Token，Query 长度为 1，Key/Value 长度随生成序列增长。
*   **关键技术细节**：
    *   **模块化编译**：将模型按层和阶段拆分为多个小模块并行编译，最后合并为单一二进制文件。
    *   **性能优化**：使用固定大小的 Causal Mask 减少带宽；将 RoPE 的正余弦计算预处理为静态表，通过 `Gather` 算子查表以减少运行时计算；通过地址复用实现 KV Cache 的原地更新。

### 3. 实验设计
*   **应用场景**：该方法已集成于开源项目 `TPU-MLIR` 和 `LLM-TPU` 中。
*   **支持模型**：涵盖了 Qwen、Llama、InternVL、MiniCPM-V 等主流生成式模型系列。
*   **支持技术**：支持多种量化方案，包括 GPTQ、AWQ 和 AutoRound，以及 F32/BF16/F16/INT8 等多种数据类型。
*   **对比对象**：虽然文中未给出详细的数值对比图表，但其设计目标是对比传统的“手写算子库”模式和“全动态编译”模式，强调静态编译带来的确定性和低运行时开销。

### 4. 资源与算力
*   **算力说明**：论文未明确列出具体的训练时长、GPU 数量或具体的硬件型号（如具体的 TPU 版本）。
*   **实现背景**：该方法是为算能（Sophgo）的专用 AI 加速器设计的，并在其开源编译器框架中落地。

### 5. 实验数量与充分性
*   **实验覆盖面**：论文通过支持多种不同架构（Dense Decoder-only, GQA, MoE, 多模态）的模型，证明了该方法的**通用性**。
*   **充分性评价**：作为一篇方法论论文，它侧重于架构设计和编译流程的完整性。虽然缺乏具体的性能 Benchmark（如每秒 Token 数、延迟数据），但其在多个主流模型上的成功部署证明了方案的可行性和实用性。

### 6. 论文的主要结论与发现
*   **结论**：基于 MLIR 的双层方言转换和三阶段静态拆分方法，能够有效解决 LLM 在专用硬件上的部署难题。
*   **发现**：将 Transformer 层拆分为 Prefill、Prefill_kv 和 Decode 三个静态模块，可以显著降低运行时动态调度的开销，并提高内存分配的确定性。

### 7. 优点
*   **框架无关性**：TopOp 的设计使得编译器可以轻松对接不同的深度学习框架。
*   **静态编译优势**：通过静态形状（Static Shape）优化，减少了专用硬件上的指令复杂度，提升了硬件利用率。
*   **高度模块化**：支持并行编译，缩短了大型模型的编译时间，且方便运行时灵活组合。

### 8. 不足与局限
*   **缺乏量化性能数据**：文中未提供不同量化模式（如 INT8 vs FP16）下的精度损失或实际加速比的具体数据。
*   **静态形状限制**：虽然通过拆分阶段解决了部分动态性问题，但对于极长序列或极其复杂的动态生成场景，静态编译可能仍需生成多个变体（Chunk List），增加了存储负担。
*   **硬件绑定**：虽然 TopOp 是通用的，但 TpuOp 及其后续优化逻辑高度依赖于特定的 TPU 架构。

（完）
