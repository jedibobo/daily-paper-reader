---
title: "DSTAR: Accelerating Diffusion Transformers via Spatial and Temporal Redundancy Reduction"
title_zh: DSTAR：通过空间和时间冗余缩减加速扩散 Transformer
authors: "Chi Zhang, Jieru Zhao, Yu Feng, Chen Zhang, Quan Chen, Minyi Guo"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15846v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于加速扩散Transformer的软硬件协同设计框架
tldr: Diffusion Transformers (DiTs) 在图像和视频生成中表现优异，但多轮迭代推理导致效率低且能耗高。本文提出 DSTAR 软硬件协同设计框架，通过减少时空冗余加速推理。算法上引入细粒度混合精度量化和稀疏注意力复用；硬件上设计专用加速器。实验表明，DSTAR 在保持精度的前提下，相比 A100 GPU 实现了显著的加速和节能。
selection_source: fresh_fetch
motivation: 现有加速方法主要关注相邻时间步的冗余，忽略了 DiT 特有的时空特征，导致推理效率低或精度大幅下降。
method: 采用软硬件协同设计，结合了针对差分激活的细粒度混合精度量化、稀疏注意力复用机制以及专用的硬件加速架构。
result: 在七种典型 DiT 模型上，DSTAR 相比 A100 GPU 实现了最高 7.33 倍的加速和 41.89 倍的节能，且优于现有 SOTA 加速器。
conclusion: DSTAR 通过有效利用 DiT 的时空冗余，在不损失精度的情况下大幅提升了推理性能，为高效生成式模型部署提供了有力支持。
---

## 摘要
扩散 Transformer (DiT) 已广泛应用于图像合成、视频生成和内容编辑等多种任务。然而，其多迭代推理过程导致了性能效率低下和高能耗。现有的加速方法主要集中在减少相邻时间步之间的时间冗余，但往往忽略了 DiT 的特定特征。因此，这些方法要么面临严重的精度下降，要么无法实现高效率。我们提出了 DSTAR，这是一个通过减少空间和时间冗余来加速 DiT 推理的软硬件协同设计框架。在算法层面，DSTAR 为线性操作中的差分激活引入了一种细粒度的混合精度量化方法，显著增加了低比特计算的比例。此外，DSTAR 结合了稀疏注意力复用机制，以最大限度地减少注意力层中的冗余计算。在架构支持方面，我们设计了一个专门的硬件加速器，在延迟和能耗方面均实现了高效率。对七种典型 DiT 的评估表明，与 NVIDIA A100 GPU 相比，DSTAR 实现了高达 7.33 倍的延迟加速和 41.89 倍的节能；与 SOTA 加速器相比，实现了高达 2.54 倍的延迟加速和 3.68 倍的节能，且没有精度损失。

## Abstract
Diffusion Transformers (DiTs) have been widely used in many tasks, including image synthesis, video generation, and content editing. However, their multi-iteration inference process leads to performance inefficiency and high energy consumption. Existing acceleration methods primarily focus on reducing temporal redundancy between adjacent timesteps, but often overlook the specific features of DiTs. As a result, these approaches either suffer from great accuracy degradation or fail to achieve high efficiency.   We present DSTAR, a software-hardware co-design framework that accelerates DiT inference by reducing spatial and temporal redundancy. At the algorithmic level, DSTAR introduces a fine-grained mixed-precision quantization method for differential activations in linear operations, significantly increasing the proportion of low-bit computations. Additionally, DSTAR incorporates a sparse attention reuse mechanism to minimize redundant computation in attention layers. For architectural support, we design a specialized hardware accelerator which achieves high efficiency in both latency and energy consumption. Evaluation on seven typical DiTs demonstrates that DSTAR achieves up to 7.33x latency speedup and 41.89x energy savings compared to an NVIDIA A100 GPU, and achieves up to 2.54x latency speedup and 3.68x energy savings compared to SOTA accelerators, without accuracy degradation.

---

## 论文详细总结（自动生成）

### DSTAR：通过空间和时间冗余缩减加速扩散 Transformer 总结

#### 1. 核心问题与整体含义（研究动机和背景）
扩散 Transformer (DiT) 在图像和视频生成领域已成为主流架构（如 Flux.1, SD3.5），但其面临严重的推理效率问题。核心挑战在于：
*   **计算量巨大**：DiT 需要多轮迭代去噪，且模型参数量和分辨率不断攀升。
*   **现有方法局限**：传统的加速技术（如针对 UNet 的方法）直接应用到 DiT 时，要么导致生成质量大幅下降（如 FID 升高），要么无法有效利用 DiT 特有的时空冗余特性。
*   **冗余性未充分挖掘**：DiT 在相邻时间步之间存在**时间冗余**（激活值变化小），在注意力机制中存在**空间冗余**（注意力矩阵稀疏）。

#### 2. 论文提出的方法论：核心思想与关键技术
DSTAR 提出了一个**软硬件协同设计框架**，通过减少时空冗余来加速推理：

*   **FMDQ（细粒度混合精度差分量化）**：
    *   针对线性层（如 FFN），计算相邻步激活值的差分（$\Delta x$）。
    *   **核心发现**：离群值（Outliers）在通道内具有局部聚集性。
    *   **技术细节**：将张量沿 Token 维度分块（Tile），在子通道级别应用混合精度（支持 2/4/6/8 比特）。采用基于 2 的幂次（PoT）的量化缩放，以便硬件通过位移操作实现快速重构。
    *   **重排序优化**：在硬件执行前对子通道按精度重排序，减少流水线停顿。
*   **SAR（稀疏注意力复用）**：
    *   针对注意力层，利用 DiT 稳定的块状稀疏模式。
    *   **静态分析**：通过预分析确定重要的注意力块，生成静态掩码进行块级剪枝。
    *   **跨步复用**：在相邻步间直接复用注意力分数（Attention Score），跳过 $Q \times K$ 和 Softmax 计算。
*   **硬件架构设计**：
    *   设计了专用加速器，包含混合精度矩阵单元（MPU）、量化处理单元（QPU）和向量处理单元（VPU）。
    *   MPU 支持 SIMD-4 运算，能够高效处理 2-bit 激活切片与 8-bit 权重的乘法。

#### 3. 实验设计
*   **数据集/场景**：
    *   图像生成：COCO 数据集。
    *   视频生成：FETV 数据集。
*   **评估模型（7 种典型 DiT）**：DiT-XL-512, PixArt-Sigma, SD3.5-Medium/Large, Flux.1-Dev/Schnell, Latte。
*   **对比基准（Benchmark）**：
    *   **硬件**：NVIDIA A100 GPU。
    *   **SOTA 加速器**：S-DMA, Cambricon-D, DITTO, EXION。
*   **评估指标**：FID（图像质量）、IS（多样性）、CLIP Score（图文对齐度）、延迟（Latency）、能效（Energy）。

#### 4. 资源与算力
*   **算法评估**：在 **NVIDIA A100 GPU** 上实现并测试算法对精度的影响。
*   **硬件评估**：
    *   使用 **SimPy** 和 **Ramulator** 构建离散事件模拟器。
    *   硬件逻辑使用 **Synopsys Design Compiler** 在 **45nm** 工艺下进行综合（1GHz 频率）。
    *   SRAM 功耗通过 **CACTI** 估算。
*   **算力规模**：模拟配置了 32 个核心（MPC），总算力约为 262 TOPS (A8W8)，与 A100 的理论 FP16 算力相当。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.6B 到 12B 参数量的多种模型，覆盖了图像和视频生成任务。
*   **消融实验**：详细分析了 FMDQ 和 SAR 各自的贡献，以及时间步长（Timestep）对加速比和精度的影响。
*   **敏感性分析**：通过不同随机种子测试了静态分析的泛化能力。
*   **客观性**：对比了多种最新的专用加速器，并使用了标准的生成质量评估工具，实验设计较为全面、客观。

#### 6. 论文的主要结论与发现
*   **性能提升**：相比 A100 GPU，DSTAR 实现了最高 **7.33 倍**的延迟加速和 **41.89 倍**的节能。
*   **超越 SOTA**：相比 DITTO 和 EXION 等加速器，DSTAR 实现了最高 **2.54 倍**的加速和 **3.68 倍**的节能。
*   **精度保持**：在大幅压缩计算量的同时，FID 和 IS 等指标的下降微乎其微（通常小于 5%），视觉质量保持良好。
*   **冗余利用率**：FMDQ 将平均比特数降低至 4.3 bit，SAR 减少了约 50% 的注意力层计算量。

#### 7. 优点
*   **软硬件深度协同**：不仅有算法创新（细粒度量化），还设计了能高效处理这些算法的硬件单元（如支持 PoT 位移的 PE）。
*   **普适性强**：在不同规模（小到 DiT-XL，大到 Flux.1）和不同任务（图、影）上均表现稳健。
*   **高效处理离群值**：通过子通道分块和重排序，解决了传统量化方法在处理 DiT 离群值时效率低下的问题。

#### 8. 不足与局限
*   **静态分析依赖**：SAR 依赖于预先的静态分析（Profiling），如果模型权重或推理步数发生变化，可能需要重新分析。
*   **工艺节点较老**：硬件综合使用的是 45nm 工艺，虽然通过工具进行了等效缩放对比，但与现代 7nm/5nm 工艺下的实际表现可能存在偏差。
*   **应用限制**：目前主要针对推理阶段，未讨论该架构在训练或微调阶段的适用性。

（完）
