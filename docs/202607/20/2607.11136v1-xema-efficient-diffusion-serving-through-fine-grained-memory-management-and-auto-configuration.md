---
title: "Xema: Efficient Diffusion Serving through Fine-Grained Memory Management and Auto-Configuration"
title_zh: Xema：通过细粒度内存管理和自动配置实现高效的扩散模型推理服务
authors: "Xueze Kang, Guangyu Xiang, Suyi Li, Yuxin Wang, Shaohuai Shi, Lin Zhang, Xiaowen Chu"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11136v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 扩散模型的细粒度内存管理和自动配置
tldr: Xema是一个针对扩散模型推理服务的高效内存管理系统。针对高分辨率图像和长视频生成中GPU显存受限的问题，Xema利用张量生命周期的可预测性，通过离线追踪引导的细粒度内存优化，仅在显存压力峰值期间应用缓解策略。结合静态内存布局和自动配置规划器，Xema在满足SLO约束的同时显著提升了并发处理能力和规划效率。
selection_source: fresh_fetch
motivation: 现有的扩散模型显存节省技术（如权重卸载和分片）往往会引入巨大的性能开销，难以满足生产环境下的高分辨率生成需求。
method: Xema通过离线追踪识别显存压力区间，实施按需内存缓解，并利用静态内存布局减少碎片化，同时配合自动规划器优化并行与并发配置。
result: 在Flux.2和CogVideoX等模型上的评估显示，Xema将SLO达成率提升了高达3.7倍，并将配置规划时间从6.3小时缩短至197秒。
conclusion: Xema证明了通过细粒度的内存管理和自动化的离线规划，可以在不牺牲性能的前提下显著提升大规模扩散模型的推理服务效率。
---

## 摘要
扩散模型正越来越多地被部署为生产级视觉生成服务，但在提供高分辨率图像和长视频生成服务时，往往受限于 GPU 内存。常见的内存节省技术（如权重卸载、分片和 VAE 切片）通常并不实用，因为它们往往会引入显著的性能开销。在本文中，我们提出了 Xema，这是一个内存高效的扩散模型推理服务系统，它利用可预测的张量生命周期进行基于追踪（trace-guided）的内存优化。对于每个请求模板，Xema 导出离线内存追踪以识别短时间的内存压力区间，并仅在这些区间内、仅按满足目标 GPU 预算所需的量应用内存缓解措施。Xema 进一步为具有可预测生命周期的张量构建静态内存布局，减少了因碎片化导致的预留内存，并使离线内存推理在运行时变得可靠。基于该内存优化层，Xema 引入了一个离线规划器，在 GPU 内存和 SLO 约束下联合选择并行度、并发度和内存控制策略。所选方案存储在规划表中，并由在线推理运行时直接使用。我们在生产级扩散流水线上实现了 Xema，并使用 Flux.2、CogVideoX-5B 和 LTX-2 对其进行了评估。与现有的推理配置相比，Xema 将 SLO 达成率提高了多达 3.7 倍，并将规划成本从网格搜索的 6.3 小时降低到 197 秒。

## Abstract
Diffusion models are increasingly deployed as production visual-generation services, where serving high-resolution image and long video generation is often limited by GPU memory. Popular memory-saving techniques such as weight offloading, sharding, and VAE slicing are often not practical because they tend to introduce significant performance overhead. In this paper, we present Xema, a memory-efficient diffusion serving system that exploits predictable tensor lifetimes for trace-guided memory optimization. For each request template, Xema derives an offline memory trace to identify short memory-pressure intervals and applies memory mitigation only within these intervals and only by the amount needed to fit the target GPU budget. Xema further constructs a static memory layout for tensors with predictable lifetimes, reducing fragmentation-induced reserved memory and making offline memory reasoning reliable at runtime. Built on this memory optimization layer, Xema introduces an offline planner that jointly selects parallelism, concurrency, and memory control under GPU memory and SLO constraints. The selected plan is stored in a plan table and directly used by the online serving runtime. We implement Xema on production diffusion pipelines and evaluate it with Flux.2, CogVideoX-5B, and LTX-2. Compared with existing serving configurations, Xema improves SLO attainment by up to 3.7x and reduces planning cost from 6.3 hours to 197 seconds compared with grid search.

---

## 论文详细总结（自动生成）

这是一份关于论文《Xema: Efficient Diffusion Serving through Fine-Grained Memory Management and Auto-Configuration》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着扩散模型（如 Flux.2, CogVideoX）向高分辨率图像和长视频生成演进，**GPU 显存容量**成为了推理服务的核心瓶颈。现有的显存节省技术（如权重卸载、分片、VAE 切片）存在两个主要问题：
*   **粒度过粗**：通常是“全开”或“全关”，导致在显存充足的区间也引入了不必要的通信或传输开销，严重拖慢延迟。
*   **缺乏协同规划**：并行策略（SP/DP）、并发度（Batch Size）与显存控制手段之间存在复杂的权衡，手动调优难以在满足服务等级协议（SLO）的同时最大化吞吐量。

Xema 的核心目标是利用扩散模型推理的**模板静态性**（即特定分辨率和帧数下，张量生命周期可预测），实现“按需、局部”的精细化显存管理。

### 2. 方法论：核心思想与关键技术
Xema 提出了一个内存高效的推理服务框架，包含以下三个核心组件：
*   **追踪引导的细粒度内存缓解（Trace-guided Mitigation）**：
    *   **局部应用**：通过离线分析识别显存需求超过预算的“压力区间”，仅在这些短时间内应用卸载或序列化。
    *   **按需强度**：不进行全量卸载，而是计算出刚好能塞进显存所需的最小块数量，以最大化计算与传输的重叠。
    *   **手段扩展**：支持激活值卸载、算子分块（Chunking）、算子融合以及临时序列化（在峰值处降低并发）。
*   **静态内存布局（Static Memory Layout）**：
    *   将显存分配视为“矩形装箱问题”（Rectangle Packing），在地址-时间平面上规划张量位置。
    *   通过离线预分配消除运行时碎片，使预留显存（Reserved Memory）紧贴活跃显存（Active Memory），显存利用率可从 60% 提升至 95% 以上。
*   **自动配置规划器（Offline Planner）**：
    *   采用四阶段工作流：1. 搜索并行与批处理组合；2. 定位显存超标区间；3. 针对性搜索显存缓解组合；4. 选择 Goodput（满足 SLO 的吞吐量）最高的方案。

### 3. 实验设计
*   **模型与场景**：评估了三种生产级模型：**Flux.2**（32B 图像生成）、**CogVideoX-5B**（视频生成）、**LTX-2**（13B 视频生成）。
*   **Benchmark**：涵盖了从 256p 到 4K 的多种分辨率，以及不同帧数的视频请求。
*   **对比方法**：
    *   **vLLM-Omni**：最先进的扩散模型推理系统。
    *   **vLLM-Omni + ExtendedLib**：在 vLLM 基础上增加 Xema 的算子库，但维持全局粗粒度控制。
    *   **Heuristic**：使用细粒度控制但采用贪心搜索策略。

### 4. 资源与算力
*   **硬件环境**：单台服务器，配备 **8 张 NVIDIA RTX A6000 GPU**（每张 48GB 显存），通过 PCIe 4.0 和 NVLink 连接。
*   **算力消耗**：论文重点在于推理服务优化，未涉及模型训练。规划器的搜索效率极高，单模板规划时间仅需约 197 秒。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 21 种不同的请求模板（分辨率/帧数组合），进行了端到端 SLO 达成率、显存峰值测试、消融实验、规划成本分析等。
*   **充分性**：实验对比了不同到达率（Poisson 过程）和不同 GPU 数量下的表现，并针对视频模型做了极端负载（如 4K 视频）的压力测试。
*   **客观性**：通过消融实验分别验证了内存控制、并行策略和并发规划对最终性能的贡献，证明了联合优化的必要性。

### 6. 主要结论与发现
*   **性能提升**：在 Flux.2 上，Xema 将 SLO 达成率提升了多达 **3.7 倍**。
*   **可行性扩展**：Xema 使得单张 48GB 显卡能够处理原本会 OOM 的 4K 图像或长视频请求。
*   **规划效率**：相比于穷举网格搜索（需 6.3 小时），Xema 的规划器仅需 **197 秒**即可找到最优配置，效率提升两个数量级。
*   **显存利用率**：静态内存分配器显著减少了碎片，使显存利用率接近理论极限。

### 7. 优点
*   **精细化控制**：打破了传统“全局卸载”的低效模式，通过局部缓解平衡了显存与延迟。
*   **确定性**：静态内存布局消除了 PyTorch 缓存分配器的随机性和碎片化问题，使离线规划结果在运行时高度可靠。
*   **高效搜索**：利用显存追踪进行剪枝，极大地压缩了配置搜索空间。

### 8. 不足与局限
*   **模板依赖**：系统高度依赖于“请求模板”的静态性。如果输入分辨率是完全随机且不可预知的，离线规划的优势将减弱。
*   **动态开销**：虽然静态区域管理得很好，但仍需预留 5% 左右的空间给 CUDA 上下文等动态部分，这部分仍存在微小浪费。
*   **应用限制**：主要针对扩散模型，对于具有高度动态分支或输入依赖型长度的模型（如某些 LLM 场景），该方法的适用性有待验证。

（完）
