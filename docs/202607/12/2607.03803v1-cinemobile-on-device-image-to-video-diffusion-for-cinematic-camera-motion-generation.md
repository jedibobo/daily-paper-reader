---
title: "CineMobile: On-Device Image-to-Video Diffusion for Cinematic Camera Motion Generation"
title_zh: CineMobile：面向电影级镜头运动生成的端侧图生视频扩散模型
authors: "Xuyao Huang, Zelai Deng, Xu Wang, Xizhong Xiao, Zhijie Deng"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03803v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 移动端高效设备端图生视频扩散模型
tldr: CineMobile 是一款专为移动端设计的图像转视频扩散模型，旨在生成电影级镜头效果。针对 DiT 模型计算量大、推理慢的问题，该研究通过蒸馏引导剪枝、4步快速生成算法及混合量化技术，实现了模型的大幅瘦身与加速。在保持视觉质量的同时，CineMobile 在移动平台上实现了 40 倍的加速，显著降低了内存占用，为移动端实时视频创作提供了可行方案。
selection_source: fresh_fetch
motivation: 现有的视频生成模型因参数量巨大和迭代步数多，难以在资源受限的移动设备上高效实现电影级镜头的实时生成。
method: 采用蒸馏引导剪枝获取紧凑模型，结合扩散蒸馏与强化学习优化为 4 步生成器，并利用混合后训练量化压缩模型体积。
result: 在天玑 8400 平台上生成 49 帧 480p 视频仅需 20 秒，相比基准模型实现 40 倍加速，且模型占用空间小于 1GB。
conclusion: CineMobile 证明了通过深度优化策略，可以在移动端设备上高效运行高质量的图像转视频扩散模型。
---

## 摘要
移动设备上日益增长的图生视频需求正越来越多地关注电影级运动效果，如子弹时间、希区柯克变焦（dolly zoom）、慢动作等。尽管扩散 Transformer (DiT) 在视频生成方面表现出强大的性能，但其庞大的参数量和多步迭代去噪过程导致了巨大的计算开销，使得在移动设备上进行高效生成面临挑战。我们提出了 CineMobile 来填补这一空白。具体而言，CineMobile 采用了三重优化策略：(1) 利用蒸馏引导的剪枝方法推导出一个紧凑且高效的模型，保留了电影效果所需的关键视频生成能力；(2) 通过结合扩散蒸馏和强化学习，将压缩后的模型优化为 4 步生成器；(3) 采用混合训练后量化策略，将模型占用空间压缩至 1 GB 以下。实验结果表明，与采用 Wan 2.1 架构的教师模型相比，CineMobile 在保持相当视觉质量的同时，实现了 40 倍的生成加速。具体来说，CineMobile 在 NVIDIA H200 GPU 上的单步去噪延迟为 0.6 秒，在联发科天玑 8400 Ultimate 5G 平台上为 20 秒，可生成 49 帧 480p 视频，峰值内存占用为 1.8 GB，证明了其在基于移动端的图生视频创作中的实际应用价值。

## Abstract
The growing demand for image-to-video creation on mobile devices has increasingly focused on cinematic motion effects like bullet time, dolly zoom, slow motion, etc. While Diffusion Transformers (DiTs) exhibit strong performance in video generation, their large parameter sizes and multi-step iterative denoising processes lead to substantial computational overhead, making efficient generation on mobile devices challenging. We propose CineMobile to bridge the gap. In particular, CineMobile adopts a three-fold optimization strategy: (1) leveraging a distillation-guided pruning approach to derive a compact yet efficient model that retains the essential video generation capabilities required for cinematic effects; (2) optimizing the compressed model into a 4-step generator via a combination of diffusion distillation and reinforcement learning; (3) employing a hybrid post-training quantization strategy to compress the model footprint to under 1 GB. Experimental results show that compared to the teacher model with the Wan 2.1 architecture, CineMobile achieves a 40x speedup in generation while maintaining comparable visual quality. Specifically, CineMobile generates 49-frame 480p videos with a per-step denoising latency of 0.6s on an NVIDIA H200 GPU and 20s on the MediaTek Dimensity 8400 Ultimate 5G platform, with a peak memory usage of 1.8 GB, demonstrating its practical applicability for mobile-based image-to-video creation.

---

## 论文详细总结（自动生成）

以下是对论文《CineMobile: On-Device Image-to-Video Diffusion for Cinematic Camera Motion Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着移动端视觉内容创作的普及，用户对生成“电影级镜头效果”（如子弹时间、希区柯克变焦、慢动作）的需求日益增长。然而，目前主流的视频生成模型（如 Wan 2.1、Kling 等）多基于扩散 Transformer (DiT) 架构，面临以下挑战：
*   **计算开销巨大**：参数量动辄数十亿甚至上百亿，推理步数多（通常 40 步以上）。
*   **端侧部署困难**：移动设备内存有限且算力远低于服务器级 GPU，导致生成速度极慢或无法运行。
*   **控制力不足**：在大幅度加速（减少步数）时，模型往往难以保持语义一致性和复杂的镜头轨迹。
**CineMobile** 的目标是填补这一空白，通过模型压缩和加速技术，在移动端实现高效、高质量的电影级图生视频生成。

### 2. 方法论
CineMobile 采用了三重优化策略：
*   **蒸馏引导的结构化深度剪枝**：
    *   使用线性探针和 CKA 相似度检测 DiT 块中的冗余层。
    *   采用“深度剪枝”而非“宽度剪枝”，以保持隐藏层维度不变，从而更好地保留预训练的临时表示和人像细节。
    *   通过替代块（Surrogate Block）进行隐藏状态对齐训练，替换冗余层区间。
*   **两阶段步数蒸馏（Step Distillation）**：
    *   **SFT 热身阶段**：使用流匹配（Flow-matching）目标在教师模型生成的视频上进行微调，恢复基础生成能力。
    *   **对抗蒸馏与强化学习阶段**：结合 AdvDMD 框架，利用 LoRA 适配器将模型蒸馏为 4 步生成器。引入 **GRPO（群体相对策略优化）**，利用轻量级判别器提供的奖励信号进一步提升视觉真实感。
*   **混合后训练量化（Hybrid PTQ）**：
    *   将 FFN（前馈网络）权重压缩至 4-bit，其余线性层权重压缩至 8-bit，激活值保持 16-bit。
    *   该策略将模型 footprint 压缩至 1GB 以下，显著降低了端侧内存占用。

### 3. 实验设计
*   **场景与效果**：针对三种典型电影镜头：**子弹时间（Bullet Time）**、**希区柯克变焦（Dolly Zoom）**和**慢动作（Slow Motion）**。
*   **数据集**：使用 PPR10K（高质量人像）、Pexels 和 Pixabay（运动视频）作为图像源，通过教师模型生成对应的电影效果视频作为训练集。
*   **Benchmark 与对比方法**：
    *   **自动评估**：使用 **VBench** 协议，对比了 HunyuanVideo-1.5、LTX2.3、Open-Sora2.0 等开源模型。
    *   **人工评估**：与 **Kling（快影）、Hailuo（海螺）、Vidu** 等商业模型在伪影控制、畸变、一致性、镜头控制等维度进行对比。
    *   **消融实验**：验证了 GRPO 的有效性，并与传统插帧方法（FILM）进行了对比。

### 4. 资源与算力
*   **训练算力**：所有实验均在 **8 台 NVIDIA H200 GPU** 上完成。
*   **训练时长**：剪枝阶段使用 5000+ 样本；SFT 热身阶段训练 5000 步；蒸馏阶段使用 10,000+ 样本进行优化。
*   **部署平台**：在 **Infinix NOTE 60 Ultra**（搭载 **联发科天玑 8400 Ultimate 5G** 平台）上进行实测。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 14B 教师模型到 1.2B 压缩模型的完整性能演变过程。针对每种电影效果，均提供了定量（VBench）和定性（视觉对比）分析。
*   **充分性**：实验设计较为全面，不仅对比了开源模型，还通过人工评测挑战了闭源商业模型。消融实验证明了深度剪枝优于宽度剪枝，以及 GRPO 对美学质量的提升。
*   **客观性**：使用了标准化的 VBench 协议，并在端侧设备上给出了真实的延迟和内存占用数据，具有较高的参考价值。

### 6. 主要结论与发现
*   **性能飞跃**：相比 14B 的 Wan 2.1 教师模型，CineMobile 实现了 **40 倍的去噪加速**和 **71.89 倍的 FLOPs 降低**。
*   **端侧可行性**：在天玑 8400 平台上，单步去噪延迟约 20 秒（4 步生成约 80 秒），峰值内存仅 1.8 GB，模型文件小于 1 GB。
*   **质量保持**：尽管参数量和步数大幅减少，CineMobile 在 VBench 上的总分与教师模型非常接近（差距小于 1 分），且在镜头控制力上优于部分商业模型。

### 7. 优点
*   **针对性强**：专注于电影级镜头运动，解决了通用模型在特定轨迹控制上的不足。
*   **全栈优化**：整合了剪枝、蒸馏、强化学习和量化，是一套完整的端侧部署方案。
*   **创新的强化学习应用**：将 GRPO 引入视频蒸馏过程，有效提升了 4 步生成下的图像美学和细节。

### 8. 不足与局限
*   **端侧延迟仍有提升空间**：虽然实现了 40 倍加速，但在移动端生成一个视频仍需一分钟以上（4 步 × 20s），距离“秒级生成”尚有距离。
*   **场景局限性**：实验主要集中在人像摄影场景，对于更复杂的通用场景（如大规模风景、多主体交互）的泛化能力有待进一步验证。
*   **依赖教师模型**：模型的最终上限受限于教师模型生成的伪标签数据质量。

（完）
