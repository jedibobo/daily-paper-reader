---
title: Task-Oriented Multimodal Edge Intelligence via Integrated Sensing-Communication-Computation
title_zh: 面向任务的感知-通信-计算集成多模态边缘智能
authors: "Weiwei Chen, Yinghui He, Zhong Ye, Dingzhu Wen, Guanding Yu"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03907v1"
tags: ["query:hwnas"]
score: 8.0
evidence: 边缘智能的感算通一体化
tldr: 本文针对边缘智能中单模态感知鲁棒性不足的问题，提出了一个面向任务的多模态感知-通信-计算集成（ISCC）框架。该框架结合了设备端特征提取与边缘端联合推理，并引入最大编码率削减（MCR^2）准则来学习紧凑且具辨别力的任务相关特征。通过解决时延和资源约束下的感知准确度最大化问题，该方案在人体活动识别任务中显著提升了受限资源下的系统性能。
selection_source: fresh_fetch
motivation: 现有的单模态ISCC设计在复杂环境下鲁棒性差，且多模态设计面临高维数据处理与资源受限的挑战。
method: 提出基于MCR^2准则的任务导向型多模态ISCC框架，并利用块坐标下降（BCD）算法优化资源分配与感知精度。
result: 实验结果表明，该框架在人体活动识别任务中，在资源受限的情况下性能优于三种基准方案。
conclusion: 该研究证明了多模态集成感知与通信计算在提升边缘智能系统鲁棒性和效率方面的有效性。
---

## 摘要
感知、通信与计算集成（ISCC）近期已成为实现边缘智能的统一框架。然而，现有的 ISCC 设计主要依赖单模态感知，这在本质上容易受到遮挡、环境不确定性和特定模态故障的影响，导致在实际部署中鲁棒性下降。这激发了对多模态 ISCC 的需求，但其设计仍未得到充分探索。与单模态情况相比，多模态 ISCC 更具挑战性，因为异构模态扩大了数据维度并收紧了通信、计算和能量预算，而模态间的相关性进一步增加了性能表征的复杂性。为了应对这些挑战，我们提出了一种面向任务的多模态 ISCC 框架，该框架集成了设备端特征提取与边缘端联合多模态推理。我们方法的核心组件是最大编码率削减（MCR^2）准则，它使每个设备能够学习紧凑且具有辨别力的任务相关特征，与传统的基于交叉熵的提取器相比具有明显优势。我们进一步利用 MCR^2 作为边缘端感知评估的原则性指标。在此基础上，我们构建了延迟和资源约束下的感知准确率最大化问题，并在将该问题转化为更易处理的等效形式后，开发了一种高效的块坐标下降（BCD）算法。针对人体活动识别任务，我们在公开数据集上进行了广泛的实验，以评估所提 ISCC 框架的性能。结果表明，在有限资源条件下，我们的方法始终优于三种基准方案。

## Abstract
Integrated sensing, communication, and computation (ISCC) has recently emerged as a unified framework for enabling edge intelligence. However, existing ISCC designs predominantly rely on single-modal sensing, which is inherently vulnerable to occlusions, environmental uncertainties, and modality-specific failures, leading to degraded robustness in real-world deployments. This motivates the need for multi-modal ISCC, yet its design remains insufficiently explored. Compared with the single-modal case, multi-modal ISCC is more challenging because heterogeneous modalities enlarge data dimensionality and tighten communication/computation/energy budgets, while inter-modal correlations further complicate performance characterization. To address these challenges, we propose a task-oriented multi-modal ISCC framework that integrates device-side feature extraction with edge-side joint multi-modal inference. A central component of our approach is the maximal coding rate reduction (MCR^2) criterion, which enables each device to learn compact and discriminative task-relevant features, offering clear advantages over conventional cross-entropy-based extractors. We further leverage MCR^2 as a principled metric for edge-side sensing evaluation. On this basis, we formulate a sensing accuracy maximization problem under delay and resource constraints and develop an efficient block coordinate descent (BCD) algorithm after transforming the problem into a more tractable equivalent form. Focusing on a human activity recognition task, we conduct extensive experiments on publicly available datasets to evaluate the performance of the proposed ISCC framework. The results demonstrate that our approach consistently outperforms three baseline schemes under limited resource conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Task-Oriented Multimodal Edge Intelligence via Integrated Sensing-Communication-Computation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着6G网络的发展，感知-通信-计算集成（ISCC）成为实现边缘智能的关键。现有的ISCC研究多集中于单模态感知（如仅雷达或仅Wi-Fi）。
*   **核心痛点**：
    *   **鲁棒性不足**：单模态感知在面临遮挡、环境噪声或传感器故障时性能会大幅下降。
    *   **资源瓶颈**：多模态数据维度高，对无线带宽、计算能力和设备能耗提出了严苛挑战。
    *   **性能量化难**：基于深度学习的AI模型具有“黑盒”属性，难以推导出可用于系统资源优化的解析式性能指标。
*   **研究目的**：提出一个多模态ISCC框架，通过联合优化感知、通信和计算资源，在资源受限的边缘网络中实现高精度、高鲁棒性的智能任务执行。

### 2. 核心方法论
*   **核心思想**：采用“设备端特征提取+边缘端联合推理”的架构，并引入**最大编码率削减（MCR²）**准则作为特征学习和性能评估的核心。
*   **关键技术细节**：
    *   **MCR² 准则**：不同于传统的交叉熵损失，MCR² 旨在使不同类别的特征占据不同的子空间（最大化类间辨别力），同时使同类特征在子空间内尽可能紧凑（最小化类内体积）。
    *   **性能指标转化**：将 MCR² 目标函数转化为边缘服务器端的感知准确度度量，解决了AI任务性能不可导、不透明的问题。
    *   **资源分配建模**：构建了一个在总时延、设备能量和传输成功率约束下，最大化感知准确率（即最大化编码率削减值）的非凸优化问题。
*   **算法流程**：
    *   **问题转换**：利用辅助矩阵将非凸目标函数转化为等效的易处理形式。
    *   **BCD（块坐标下降）算法**：将原问题分解为两个子块：1) 更新辅助矩阵；2) 联合优化量化比特分配与通信时间分配。
    *   **交替优化（AO）**：在子块内部通过二分搜索和凸优化技术交替更新变量，直至收敛。

### 3. 实验设计
*   **任务场景**：人体活动识别（Human Activity Recognition, HAR）。
*   **数据集**：使用公开的多模态数据集 **XRF55**，包含 Wi-Fi、RFID 和毫米波雷达（mmWave）三种模态。
*   **实验设置**：选取8类代表性动作（如挥手、摔倒、行走等），共4800个样本（70%训练，30%测试）。
*   **对比方法（Benchmarks）**：
    1.  **设备级量化方案**：所有特征维度使用相同的量化比特。
    2.  **平均时间分配方案**：各设备平分通信时间。
    3.  **单模态方案**：仅使用 Wi-Fi 模态进行感知。
    4.  **语义通信基准（Semantic JSCC）**：与现有的深度联合源信道编码方案进行对比。
*   **分类器**：在边缘端分别使用了支持向量机（SVM）和多层感知器（MLP）进行验证。

### 4. 资源与算力
*   **硬件环境**：实验在 Linux 服务器上运行，配备了 **2 个 NVIDIA GeForce GTX 4090 GPU** 用于离线训练特征提取网络（ResNet18）和分类器。
*   **计算复杂度**：文中分析了算法的理论复杂度，并指出在典型设置下，算法运行约需 74 MFLOPs。
*   **时延估算**：在 Intel Xeon Bronze 级别 CPU 上，估计计算时延约为 3.70 ms，证明了其在线部署的可行性。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度的消融实验和参数敏感性分析，包括：
    *   不同时延预算（0.03s - 0.13s）下的准确率对比。
    *   不同通信带宽（50kHz - 200kHz）的影响。
    *   特征维度（8 - 32）对性能的权衡分析。
    *   能量阈值和分类任务难度（类别数量 4 - 20）的压力测试。
*   **充分性评价**：实验设计较为全面，涵盖了通信、计算、感知三个维度的核心变量。通过对比不同分类器（SVM/MLP）证明了 MCR² 指标的普适性，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **多模态优势**：多模态 ISCC 在所有测试场景下均显著优于单模态方案，尤其在复杂任务中表现出更强的鲁棒性。
*   **MCR² 的有效性**：实验证实了编码率削减值（ΔR）与实际分类准确率之间存在强正相关性，证明其作为优化目标的合理性。
*   **资源优化效果**：所提 BCD 算法在有限资源（如低带宽、短时延）下，比平均分配方案提升了约 10%-20% 的准确率。
*   **特征维度权衡**：发现特征维度并非越高越好，过高的维度在资源受限时会导致严重的量化噪声，从而降低精度。

### 7. 优点与亮点
*   **理论创新**：首次将 MCR² 这一几何深度学习准则引入 ISCC 资源分配领域，解决了 AI 任务性能难以解析建模的难题。
*   **系统性强**：完整考虑了从感知信号获取、本地特征压缩、无线传输到边缘推理的全流程。
*   **跨模态感知**：算法能够根据信道质量和模态贡献度自动调整资源，例如在毫米波模态信息量更大时为其分配更多资源。

### 8. 不足与局限
*   **感知功率预设**：文中假设感知功率 $p_s^k$ 是预先配置的，未将其纳入联合优化变量，这可能限制了整体能效的进一步提升。
*   **场景单一性**：实验主要基于人体活动识别，对于更复杂的动态场景（如自动驾驶中的多目标追踪）是否具有同样的增益尚待验证。
*   **移动性考虑不足**：实验假设信道在传输周期内相对稳定，未深入探讨高速移动场景下多模态同步和信道剧烈波动对 ISCC 的影响。

（完）
