---
title: "InstMeter: An Instruction-Level Method to Predict Energy and Latency of DL Model Inference on MCUs"
title_zh: InstMeter：一种用于预测微控制器（MCU）上深度学习模型推理能耗和延迟的指令级方法
authors: "Hao Liu, Qing Wang, Marco Zuniga"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04134v1"
tags: ["query:hwnas"]
score: 10.0
evidence: 用于MCU上NAS的指令级能耗和延迟预测
tldr: 针对微控制器（MCU）上深度学习模型推理的能耗和延迟预测，现有方法（如MACs）精度不足或数据需求大。本文提出InstMeter，一种基于指令级时钟周期的预测方法。利用时钟周期与能耗、延迟之间的强线性关系，InstMeter在显著减少训练数据需求的同时，大幅提升了预测精度，并在神经架构搜索（NAS）中表现优异，具有极强的跨硬件和跨软件环境的泛化能力。
selection_source: fresh_fetch
motivation: 现有的MCU推理性能预测指标（如MACs）不够精确，且传统建模方法往往需要海量的训练数据。
method: 提出基于指令级时钟周期的预测框架InstMeter，利用时钟周期与能耗及延迟的强线性关系进行建模。
result: 相比现有技术，InstMeter将能耗和延迟预测误差分别降低了3倍和6.5倍，且所需训练数据量减少了10到100倍。
conclusion: InstMeter是一种高效、准确且泛化性强的MCU推理性能预测工具，能有效辅助资源受限环境下的模型优化与搜索。
---

## 摘要
深度学习（DL）模型现在已能在微控制器（MCU）上运行。通过神经架构搜索（NAS），我们可以搜索满足 MCU 约束的深度学习模型。在各种约束中，模型推理的能耗和延迟成本是关键指标。为了预测这些指标，现有研究依赖于粗粒度的代理指标，如乘加运算次数（MACs）和模型的输入参数，这往往导致预测不准确或需要大量的数据收集。在本文中，我们提出了 InstMeter，这是一种利用 MCU 时钟周期来准确估计深度学习模型能耗和延迟的预测器。时钟周期是反映 MCU 操作的基础指标，直接决定了能耗和延迟成本。此外，该预测器的一个独特属性是其强大的线性，使其兼具简洁性与准确性。我们在不同的场景、MCU 和软件设置下对 InstMeter 进行了全面评估。与最先进的研究相比，InstMeter 可以分别将能耗和延迟预测误差降低 3 倍和 6.5 倍，同时所需的训练数据减少 100 倍和 10 倍。在 NAS 场景中，InstMeter 可以充分利用能耗预算，识别出具有更高推理准确率的最优深度学习模型。我们还通过在三个 ARM MCU（Cortex-M4、M7、M33）和一个基于 RISC-V 的 MCU（ESP32-C3）、不同的编译选项（-Os、-O2）、GCC 版本（v7.3、v10.3）、应用场景（关键词识别、图像识别）、动态电压频率调节、温度（21°C、43°C）以及软件设置（TFLMv2.4、TFLMvCI）上的各种实验，评估了 InstMeter 的泛化性能。我们将开源源代码和针对 MCU 的基准测试数据集。

## Abstract
Deep learning (DL) models can now run on microcontrollers (MCUs). Through neural architecture search (NAS), we can search DL models that meet the constraints of MCUs. Among various constraints, energy and latency costs of the model inference are critical metrics. To predict them, existing research relies on coarse proxies such as multiply-accumulations (MACs) and model's input parameters, often resulting in inaccurate predictions or requiring extensive data collection. In this paper, we propose InstMeter, a predictor leveraging MCUs' clock cycles to accurately estimate the energy and latency of DL models. Clock cycles are fundamental metrics reflecting MCU operations, directly determining energy and latency costs. Furthermore, a unique property of our predictor is its strong linearity, allowing it to be simple and accurate. We thoroughly evaluate InstMeter under different scenarios, MCUs, and software settings. Compared with state-of-the-art studies, InstMeter can reduce the energy and latency prediction errors by $3\times$ and $6.5\times$, respectively, while requiring $100\times$ and $10\times$ less training data. In the NAS scenario, InstMeter can fully exploit the energy budget, identifying optimal DL models with higher inference accuracy. We also evaluate InstMeter's generalization performance through various experiments on three ARM MCUs (Cortex-M4, M7, M33) and one RISC-V-based MCU (ESP32-C3), different compilation options (-Os, -O2), GCC versions (v7.3, v10.3), application scenarios (keyword spotting, image recognition), dynamic voltage and frequency scaling, temperatures (21°C, 43°C), and software settings (TFLMv2.4, TFLMvCI). We will open our source codes and the MCU-specific benchmark datasets.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **InstMeter** 的指令级预测方法，旨在解决微控制器（MCU）上深度学习（DL）模型推理能耗和延迟预测不准确及训练数据需求量大的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在资源受限的 MCU 上，如何快速且准确地预测深度学习模型的推理能耗和延迟，以支持神经架构搜索（NAS）。
*   **研究动机**：
    *   **现有指标局限**：常用的乘加运算次数（MACs）或模型参数与实际性能之间存在非线性关系，导致预测误差大。
    *   **数据成本高**：现有的高精度预测器（如 nn-Meter）通常需要数千个训练样本，而 MCU 上的数据采集极其缓慢。
    *   **底层忽略**：编译器优化、指令调度和内核函数选择等底层细节对性能影响巨大，但现有方法往往将其视为“黑盒”。

### 2. 方法论
*   **核心思想**：将预测问题从“非线性”转变为“线性”。论文发现 **MCU 时钟周期（Clock Cycles）** 与能耗/延迟具有极强的线性关系：$E (或 L) = a \cdot Cycles + b$。
*   **关键技术细节**：
    *   **指令分析（Instruction Profiling）**：通过建立指令库，将 DL 模型的算子映射到具体的 TFLM（TensorFlow Lite for Microcontrollers）内核函数。
    *   **循环级源码-反汇编映射（Loop-Level Source-disasm Mapping）**：
        *   **结构特征**：利用控制流图（CFG）和 VF2 图匹配算法，识别源码与反汇编代码中循环结构的对应关系。
        *   **语义特征**：除了传统的变量名，还引入了**比较运算符**（如 `<, >, ==`）作为特征，解决了编译器重命名变量导致的匹配难题。
    *   **周期估算**：结合源码中的循环次数和反汇编中的指令类型及每条指令的周期（CPI），计算总时钟周期。
*   **流程**：仅需一次性构建指令库，之后针对特定 MCU 只需约 5 个实测样本进行线性回归，即可确定参数 $a$ 和 $b$。

### 3. 实验设计
*   **数据集与场景**：
    *   **ML-Data & SL-Data**：包含混合层模型和单层模型，共 505 个样本。
    *   **兼容性数据集**：涵盖 4 种 MCU、2 种编译器优化、2 种 GCC 版本、2 种温度、2 种应用（KWS 语音唤醒、CIFAR-10 图像识别）等，总计 2125 个数据点。
*   **硬件平台**：ARM Cortex-M4, M7, M33 以及 RISC-V (ESP32-C3)。
*   **对比方法**：基于 MACs 的预测器、nn-Meter（最先进的非线性预测器）。

### 4. 资源与算力
*   **算力说明**：文中未提及使用高性能 GPU 进行模型训练（因为 InstMeter 本身是简单的线性回归）。
*   **数据采集开销**：使用自动化流水线和 OTII ACE PRO 功率计进行实测。单个样本采集需 1-5 分钟，总采集时间超过 **5000 分钟**。
*   **预测开销**：指令库查询延迟极低（约 84-154 毫秒/查询），适合集成到 NAS 流程中。

### 5. 实验数量与充分性
*   **实验规模**：总计 2125 组实验数据，涵盖了硬件、软件、环境（温度、电压）等多个维度。
*   **充分性**：实验设计非常全面。不仅对比了预测精度，还将其应用于实际的 NAS 搜索场景，验证了在给定能耗预算下寻找更高精度模型的能力。跨架构（ARM vs RISC-V）的测试增强了结果的说服力。

### 6. 主要结论与发现
*   **高精度与低数据需求**：InstMeter 仅需 **5 个训练样本** 即可达到极高精度。相比 nn-Meter，误差降低 3 倍，数据需求减少 100 倍；相比 MACs 方法，误差降低 6.5 倍。
*   **线性代理的优越性**：时钟周期是比 MACs 更本质、更线性的性能代理指标。
*   **NAS 增益**：在 NAS 过程中，InstMeter 能更精准地贴合能耗预算，从而搜索到准确率更高的模型。

### 7. 优点
*   **极简建模**：利用底层物理特性将复杂的非线性估计简化为线性回归。
*   **泛化性强**：在不同编译器版本、优化等级、甚至环境温度变化下均表现稳健。
*   **指令级洞察**：通过源码-反汇编映射，捕捉到了编译器优化带来的细微性能变化。

### 8. 不足与局限
*   **源码依赖**：需要访问算子库的源码和反汇编代码，对于完全闭源的硬件加速器（如某些专用 AI 芯片）难以直接应用。
*   **极端优化挑战**：在 `-O2` 等激进优化下，循环结构可能发生剧烈变化（如循环融合/拆分），导致映射算法误差增大。
*   **动态因素**：目前主要针对静态分析，尚未动态建模热节流（Thermal Throttling）等瞬态物理现象。

（完）
