---
title: Search Multilayer Perceptron-Based Fusion for Efficient and Accurate Siamese Tracking
title_zh: 搜索基于多层感知机融合的高效准确孪生跟踪
authors: "Tianqi Shen, Huakao Lin, Ning An"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01706v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 用于平衡准确性和效率的MLP模块分层搜索空间
tldr: 针对Siamese视觉跟踪器在资源受限硬件上难以平衡像素级交互效率与准确性的问题，本文提出一种基于多层感知机（MLP）的融合模块。通过构建分层搜索空间并引入定制的松弛策略，利用可微分神经架构搜索（DNAS）自动优化通道宽度与深度，实现了低复杂度的架构。该跟踪器在多个通用及航拍基准测试中达到SOTA性能，并在GPU和NPU上实现实时运行。
selection_source: fresh_fetch
motivation: 现有的卷积或Transformer融合机制在资源受限硬件上难以高效实现像素级交互，导致准确性与效率失衡。
method: 重新设计Siamese颈部，采用MLP融合模块，并通过DNAS在分层搜索空间中自动平衡通道宽度与深度。
result: 在四个通用和三个航拍跟踪基准上表现优异，同时在GPU和NPU上保持实时性能。
conclusion: 该研究证明了通过神经架构搜索优化的MLP融合机制能有效提升Siamese跟踪器的效率与准确性平衡。
---

## 摘要
孪生视觉跟踪器近期通过基于卷积或 Transformer 架构的日益复杂的融合机制取得了显著进展。然而，两者都难以在资源受限的硬件上高效地实现像素级交互，导致准确率与效率之间存在持久的失衡。受此限制启发，我们使用一种简单而有效的基于多层感知机（MLP）的融合模块重新设计了孪生网络颈部（Siamese neck），该模块能以极小的结构开销实现像素级交互。尽管如此，简单地堆叠 MLP 块会引入新的挑战：计算成本会随通道宽度呈二次方增长。为克服这一问题，我们构建了一个由精心设计的 MLP 模块组成的层级搜索空间，并引入了一种定制的松弛策略，使微分神经架构搜索（DNAS）能够将通道宽度优化与其他架构选择解耦。这种有针对性的解耦自动平衡了通道的宽度和深度，从而产生了一种低复杂度的架构。最终得到的跟踪器实现了最先进的准确率-效率权衡。它在四个通用和三个航空跟踪基准测试中名列前茅，同时在资源受限的图形处理单元（GPU）和神经网络处理单元（NPU）上均保持了实时性能。

## Abstract
Siamese visual trackers have recently advanced through increasingly sophisticated fusion mechanisms built on convolutional or Transformer architectures. However, both struggle to deliver pixel-level interactions efficiently on resource-constrained hardware, leading to a persistent accuracy-efficiency imbalance. Motivated by this limitation, we redesign the Siamese neck with a simple yet effective Multilayer Perception (MLP)-based fusion module that enables pixel-level interaction with minimal structural overhead. Nevertheless, naively stacking MLP blocks introduces a new challenge: computational cost can scale quadratically with channel width. To overcome this, we construct a hierarchical search space of carefully designed MLP modules and introduce a customized relaxation strategy that enables differentiable neural architecture search (DNAS) to decouple channel-width optimization from other architectural choices. This targeted decoupling automatically balances channel width and depth, yielding a low-complexity architecture. The resulting tracker achieves state-of-the-art accuracy-efficiency trade-offs. It ranks among the top performers on four general-purpose and three aerial tracking benchmarks, while maintaining real-time performance on both resource-constrained Graphics Processing Units (GPUs) and Neural Processing Units (NPUs).

---

## 论文详细总结（自动生成）

### 论文总结：基于搜索的多层感知机融合高效准确孪生跟踪（SEAT）

#### 1. 核心问题与整体含义（研究动机和背景）
孪生网络（Siamese Networks）在视觉目标跟踪领域表现优异，其核心在于模板与搜索区域的**特征融合（Neck部分）**。现有的融合机制存在两难境地：
*   **卷积（CNN）融合**：虽然硬件友好，但属于补丁级（Patch-level）交互，难以捕捉精细的像素级相似性。
*   **Transformer 融合**：虽能实现像素级（Pixel-level）交互，但计算开销巨大，在资源受限的边缘设备（如无人机载 NPU）上难以达到实时性。
*   **MLP 的挑战**：虽然 MLP 结构简单且具备像素级交互潜力，但其计算复杂度随通道宽度呈二次方增长。
**研究动机**：设计一种既能实现像素级交互，又能通过自动化架构搜索（NAS）在资源受限硬件上实现最优效率平衡的 MLP 融合架构。

#### 2. 论文提出的方法论
论文提出了 **SEAT** 跟踪器，核心技术包括：
*   **粗到精的融合框架**：
    *   **CFM (Coarse Fusion MLP)**：初步融合模块。通过矩阵乘法实现类似“交叉注意力”的效果，增强目标区域并抑制背景。
    *   **RFM (Refine Fusion MLP)**：精细融合模块。利用 **Wave-MLP** 块（将 Token 建模为振幅和相位）进一步提取特征。
*   **MCAS (MLP-based Correlation Architecture Space)**：
    *   构建了一个层级的 MLP 搜索空间，包含多个“协调块”（Harmonization Blocks）。
*   **Harmony-Relaxation 搜索策略**：
    *   **解耦优化**：将通道宽度（决定计算量的主因）与其他架构参数（如卷积核大小、扩张率）解耦。
    *   使用不同的优化器：Adam 优化通道连接参数 $\beta$，SGD 优化操作选择参数 $\alpha$。
*   **三阶段优化算法**：先预热浅层权重，再预热深层权重，最后进行双层（Bi-level）架构参数与模型权重的交替优化。

#### 3. 实验设计
*   **数据集**：
    *   **通用基准**：GOT-10k, OTB2015, VOT2019, NFS30。
    *   **航拍/无人机基准**：UAV123 (含 UAV10FPS, UAV20L), UAVDT, VisDrone。
*   **对比方法**：包括轻量级跟踪器（LightTrack, FEAR, E.T.Track）和高性能跟踪器（TransT, AutoMatch, SiamAPN++ 等）。
*   **变体模型**：
    *   **SEAT_LT**：针对受限 GPU 优化（使用 LightTrack_M 主干）。
    *   **SEAT_AL**：针对边缘 NPU 优化（使用 AlexNet 主干）。

#### 4. 资源与算力
*   **硬件环境**：搜索和重训练均在 **4 台 NVIDIA RTX 3090 GPU** 上完成。
*   **训练时长**：搜索阶段 48 个 Epoch，重训练阶段 48 个 Epoch。
*   **部署测试**：在 **地平线（Horizon）X3 芯片**（嵌入式 NPU 平台）上进行了实测，验证了在 5 TOPS 算力下的实时表现。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个主流公开数据集，进行了大量的定量对比。
*   **消融实验**：对比了不同预融合方式（CNN vs Transformer vs MLP）和后融合方式的组合，验证了 CFM+RFM 的有效性。
*   **搜索算法对比**：对比了 DNAS（可微分搜索）与 ENAS（进化算法搜索）的收敛性和稳定性。
*   **充分性评价**：实验设计非常充分，不仅有标准数据集的指标对比，还有针对无人机场景的 12 种属性分析（如遮挡、快速运动等）以及真实的芯片部署测试，客观地证明了其在实际应用中的价值。

#### 6. 主要结论与发现
*   **性能领先**：SEAT_LT 在 OTB2015 (69.7% AUC) 和 VOT2019 (0.340 EAO) 上超越了现有的轻量级 SOTA 方法。
*   **硬件友好**：SEAT_AL 在地平线 X3 芯片上达到了 20.41 FPS，且延迟显著低于基于 ViT 的跟踪器（如 TCTrack）。
*   **MLP 潜力**：证明了经过 NAS 优化的 MLP 架构在特征融合任务上可以替代复杂的 Transformer，同时保持极高的推理效率。

#### 7. 优点
*   **首创性**：这是首个专门为孪生跟踪 Neck 部分设计的 MLP 搜索空间。
*   **解耦搜索策略**：巧妙地解决了 MLP 计算量随通道数爆炸的问题，使 DNAS 能够高效处理离散的通道选择。
*   **端到端部署**：不仅停留在仿真，还深入到了边缘芯片的子图拆分和 DDR 延迟优化层面。

#### 8. 不足与局限
*   **光照敏感性**：消融实验显示，由于 Wave-MLP 的振幅分支对亮度敏感，该方法在极端光照变化下的表现略逊于某些 CNN 方法。
*   **两阶段限制**：目前 Backbone 和 Neck 是分开设计的，未来若能实现全网络的一阶段联合搜索，性能可能还有提升空间。
*   **应用场景**：主要针对轻量级实时场景，在算力极其充沛的服务器端，其绝对精度可能仍稍逊于超大规模的 Transformer 模型。

（完）
