---
title: "ARMOR: Robust and Efficient CNN-Based SAR ATR through Model-Hardware Co-Design"
title_zh: ARMOR：通过模型-硬件协同设计实现鲁棒且高效的基于 CNN 的 SAR ATR
authors: "Sachini Wickramasinghe, Tian Ye, Cauligi Raghavendra, Viktor Prasanna"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03598v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对CNN模型压缩与FPGA设计的模型-硬件协同设计
tldr: 针对合成孔径雷达（SAR）自动目标识别（ATR）在资源受限平台部署时面临的计算开销大及对抗鲁棒性不足的问题，本文提出了ARMOR框架。该框架通过模型与硬件协同设计，结合硬件引导的结构化剪枝和高效FPGA加速器设计，在保持对抗鲁棒性的同时显著降低了模型规模和推理延迟，实现了高效且稳健的SAR ATR部署。
selection_source: fresh_fetch
motivation: 现有的SAR ATR研究往往忽略了在资源受限平台上部署时，模型推理效率与对抗鲁棒性之间的平衡。
method: 提出一种模型-硬件协同设计框架，利用硬件性能模型引导结构化剪枝，并设计了支持通道感知处理单元分配的FPGA加速器。
result: 在MSTAR和FUSAR-Ship数据集上，模型体积缩小达18.3倍，FPGA推理延迟比CPU降低68.1倍，能效提升169.7倍。
conclusion: 该协同设计框架证明了在不牺牲对抗鲁棒性的前提下，能够显著提升CNN在FPGA平台上的SAR ATR性能和能效。
---

## 摘要
卷积神经网络（CNN）在合成孔径雷达（SAR）自动目标识别（ATR）中取得了最先进的准确率。然而，其高昂的计算成本、延迟和内存占用使得在小卫星等资源受限平台上进行部署面临挑战。虽然对抗鲁棒性对于现实世界的 SAR ATR 至关重要，但在系统级优化中往往被忽视。实现鲁棒性和推理效率的双重目标需要一个统一的框架，该框架应同时考虑经过对抗训练的模型与硬件约束。我们提出了一种用于基于 CNN 的 SAR ATR 的模型-硬件协同设计框架，该框架将保持鲁棒性的模型压缩与 FPGA 加速器设计相结合。压缩阶段包括硬件引导的结构化剪枝，其中源自 FPGA 设计的硬件性能模型可预测剪枝对延迟和资源使用的影响。这使得能够生成在用户定义目标下提高硬件效率的帕累托最优模型，同时将对抗鲁棒性保持在预定义的容差范围内。我们设计了一种具有通道感知处理单元（PE）分配的 FPGA 加速器，支持全流水线流式传输和时间资源复用架构。自动化设计生成流程可将压缩后的模型高效映射到优化的 FPGA 实现中。在广泛使用的 MSTAR 和 FUSAR-Ship 数据集上针对三种 CNN 架构进行的实验表明，我们的框架生成的模型体积缩小了高达 18.3 倍，MACs 减少了 3.1 倍，同时保持了鲁棒性。与 CPU（GPU）基准相比，我们的 FPGA 实现实现了高达 68.1 倍（6.4 倍）的推理延迟降低和高达 169.7 倍（33.2 倍）的能效提升，证明了所提出的协同设计框架在 FPGA 平台上实现鲁棒且高效的 SAR ATR 的有效性。

## Abstract
Convolutional Neural Networks (CNNs) have achieved state-of-the-art accuracy in Synthetic Aperture Radar (SAR) Automatic Target Recognition (ATR). However, their high computational cost, latency, and memory footprint make its deployment challenging on resource-constrained platforms such as small satellites. While adversarial robustness is critical for real-world SAR ATR, it is often overlooked in system-level optimizations. Achieving both robustness and inference efficiency requires a unified framework that considers adversarially trained models together with hardware constraints. We present a model-hardware co-design framework for CNN-based SAR ATR that integrates robustness-preserving model compression with FPGA accelerator design. The compression stage includes hardware-guided structured pruning, where a hardware performance model derived from the FPGA design predicts the pruning impact on latency and resource usage. This enables the generation of Pareto-optimal models that improve hardware efficiency under user-defined objectives, while maintaining adversarial robustness within a predefined tolerance. We design an FPGA accelerator with channel-aware Processing Element (PE) allocation that supports both fully pipelined streaming and temporal resource-reuse architectures. An automated design generation flow efficiently maps the compressed models to optimized FPGA implementations. Experiments on the widely used MSTAR and FUSAR-Ship datasets across three CNN architectures show that our framework produces models up to 18.3x smaller with 3.1x fewer MACs while preserving robustness. Our FPGA implementation achieves up to 68.1x (6.4x) lower inference latency and up to 169.7x (33.2x) better energy efficiency compared to CPU (GPU) baselines, demonstrating the effectiveness of the proposed co-design framework for robust and efficient SAR ATR on FPGA platforms.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ARMOR** 的模型-硬件协同设计框架，旨在解决合成孔径雷达（SAR）自动目标识别（ATR）在实际部署中面临的对抗鲁棒性与硬件效率之间的矛盾。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：SAR ATR 在国防和遥感领域至关重要，但其成像特性（如相干斑噪声）使其极易受到对抗样本攻击。
*   **核心矛盾**：
    *   **鲁棒性 vs. 复杂度**：对抗训练通常需要更深、更宽的网络，增加了计算开销。
    *   **效率 vs. 部署**：边缘平台（如小卫星）资源极度受限，现有的模型压缩方法（如剪枝）往往只关注 FLOPs，而忽略了实际硬件上的延迟和资源占用。
*   **研究目标**：开发一个统一的协同设计框架，在保证对抗鲁棒性的前提下，实现 CNN 模型在 FPGA 上的高效推理。

### 2. 方法论：核心思想与关键技术
ARMOR 框架由**鲁棒模型压缩**和**硬件加速器设计**两个紧密耦合的部分组成：
*   **硬件引导的结构化剪枝**：
    *   **核心思想**：不仅根据权重的重要性（显著性）剪枝，还引入“硬件增益”概念。
    *   **优先级评分公式**：$P_{l,c} = g_{l,c} / (S_{l,c} + \epsilon)$。其中 $g_{l,c}$ 是通过硬件性能模型预测的移除该通道后带来的硬件成本（延迟、DSP、BRAM）降低值，$S_{l,c}$ 是基于一阶泰勒展开或范数的鲁棒性显著性得分。
    *   **流程**：迭代剪枝优先级最高的通道，直到鲁棒性下降超过预设阈值（如 5%），生成帕累托最优模型候选集。
*   **硬件性能模型**：
    *   建立了一套解析方程，无需经过耗时的逻辑综合即可准确预测卷积层和池化层的延迟及资源消耗。
*   **参数化 FPGA 加速器设计**：
    *   **通道感知 PE 分配**：根据剪枝后的实际通道数动态配置处理单元（PE），避免硬件资源闲置。
    *   **双模式架构**：支持**全流水线流式（Streaming）**架构（适用于大型 FPGA）和**时间资源复用（Temporal）**架构（适用于资源受限的小型 FPGA）。
    *   **混合精度**：权重和激活值采用 INT8 量化，累加采用 32 位，关键层保留 FP32 以平衡精度。

### 3. 实验设计
*   **数据集**：
    *   **MSTAR**：经典的 10 类地面车辆 SAR 数据集。
    *   **FUSAR-Ship**：具有挑战性的 5 类海面舰船数据集。
*   **基准模型（Benchmark）**：
    *   **Attn-CNN**：带有注意力机制的最先进轻量化 SAR 模型。
    *   **AlexNet**：经典的通用 CNN 架构。
    *   **Two-Stream**：专门针对 SAR 特征设计的双流网络。
*   **对比方法**：
    *   **硬件平台对比**：与高性能 CPU（AMD EPYC 9754）和 GPU（NVIDIA RTX 6000 Ada）进行延迟和能效对比。
    *   **加速器对比**：与现有的 FPGA 加速器（如 SMART, fpgaConvNet）进行性能对比。
    *   **消融实验**：对比了有无硬件引导的剪枝效果，以及不同显著性函数（L1/L2 范数、泰勒显著性等）的影响。

### 4. 资源与算力
*   **硬件设备**：
    *   **训练/推理基准**：AMD EPYC 9754 CPU (128核), NVIDIA RTX 6000 Ada GPU (48GB 显存)。
    *   **FPGA 平台**：高性能平台 AMD Alveo U280，资源受限平台 ZCU104（模拟空间级 FPGA）。
*   **算力细节**：论文未明确给出总训练时长，但提到对抗训练使用 PGD-10 算法，评估使用更强的 PGD-20 攻击。量化采用对称周转量化（Symmetric per-tensor quantization）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个数据集、3 种主流架构、4 种优化目标（MACs、延迟、DSP、BRAM）以及 3 种硬件平台的交叉验证。
*   **充分性评价**：实验设计非常充分。作者不仅验证了最终的加速效果，还通过帕累托前沿曲线展示了鲁棒性与效率的权衡，并验证了硬件性能模型的预测准确度（误差小于 2.5%），证明了协同设计的科学性。

### 6. 主要结论与发现
*   **压缩效果**：在保持鲁棒性的前提下，模型体积缩小高达 **18.3 倍**，计算量（MACs）减少 **3.1 倍**。
*   **硬件性能**：
    *   相比 CPU，FPGA 推理延迟降低高达 **68.1 倍**，能效提升 **169.7 倍**。
    *   相比 GPU，FPGA 在单样本推理场景下延迟降低 **6.4 倍**，能效提升 **33.2 倍**。
*   **协同设计价值**：硬件引导的剪枝在相同延迟水平下，比单纯依赖算法显著性的剪枝能保留更高的对抗鲁棒性。

### 7. 优点（亮点）
*   **首创性**：这是首个系统性地将**对抗鲁棒性**纳入 SAR ATR 模型-硬件协同设计流程的工作。
*   **实用性强**：提供了自动化的设计生成流程，能够根据剪枝结果自动配置 HLS 模板，降低了 FPGA 开发门槛。
*   **高移植性**：通过参数化设计，ARMOR 能够适配从数据中心级（U280）到卫星级（ZCU104/KU60）的不同 FPGA 平台。

### 8. 不足与局限
*   **模型局限性**：研究重点在于 CNN，虽然提及了 Transformer 和 GNN，但未在框架中深入探讨这些新兴架构的鲁棒剪枝。
*   **量化策略**：主要采用后训练量化（PTQ），对于极度压缩的需求，可能需要量化感知训练（QAT）来进一步减少精度损失。
*   **对抗攻击多样性**：实验主要集中在 PGD 攻击（$L_\infty$ 范数），未广泛测试物理世界攻击或其他类型的对抗扰动。

（完）
