---
title: Hardware-Software Co-design for 3D-DRAM-based LLM Serving Accelerator
title_zh: 基于 3D-DRAM 的大语言模型推理服务加速器的软硬件协同设计
authors: "Cong Li, Yihan Yin, Chenhao Xue, Zhao Wang, Fujun Bai, Yixin Guo, Xiping Jiang, Qiang Wu, Yuan Xie, Guangyu Sun"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04797v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 大语言模型推理加速器的软硬件协同设计
tldr: 针对大语言模型（LLM）在线服务中动态负载和内存瓶颈挑战，本文提出Helios加速器。Helios采用基于3D-DRAM的近存计算（NMP）架构，通过软硬件协同设计，解决了现有NMP在KV缓存管理和注意力执行流上的局限性。其核心贡献包括分布式分块注意力执行流、高效通信原语及空间感知KV缓存分配机制，显著提升了动态负载下的推理性能与能效。
selection_source: fresh_fetch
motivation: 现有近存计算设计因KV缓存管理粒度粗且执行流僵化，难以高效应对LLM服务中高度动态的请求负载。
method: 提出Helios加速器，设计了分布式分块注意力执行流与通信原语，并引入空间感知KV缓存分配机制以平衡负载并减少数据传输。
result: 实验表明，Helios相比现有GPU和NMP方案实现了3.25倍的加速和3.36倍的能效提升，并显著降低了P50/P99逐字生成延迟。
conclusion: Helios通过优化内存抽象与计算引擎间的交互，有效解决了3D-DRAM架构在处理高度动态LLM推理任务时的性能瓶颈。
---

## 摘要
大语言模型（LLM）已广泛部署于在线生成式服务，其中众多 LLM 实例共同处理具有波动请求到达率和可变请求长度的工作负载。为了高效执行共存的计算密集型和内存密集型算子，基于近存计算（NMP）的计算范式已被广泛提出。然而，现有的 NMP 设计采用了粗粒度的 KV 缓存管理和僵化的注意力执行流。这些局限性阻碍了此类方案高效处理高度动态的 LLM 推理服务工作负载，限制了它们加速 LLM 推理服务的能力。为了解决这些问题，我们提出了 Helios，一种基于混合键合（Hybrid-bonding）的 LLM 推理服务加速器。Helios 旨在弥合 LLM 推理服务中 KV 缓存管理的动态特性与 NMP 处理引擎（PE）之间分布式、非均匀内存抽象之间的根本鸿沟。为此，我们为分布式分块注意力执行设计了 PE 内执行流和 PE 间通信原语。我们进一步提出了一种空间感知的 KV 缓存分配机制，以平衡注意力工作负载分布，同时最小化 PE 间的数据传输开销。与现有的 GPU/NMP 设计相比，Helios 实现了 3.25 倍（几何平均）的加速和 3.36 倍（几何平均）的能效提升，同时将 P50/P99 逐词生成间隔（time-between-tokens）缩短了高达 72%/76%。

## Abstract
Large language models (LLMs) have been widely deployed for online generative services, where numerous LLM instances jointly handle workloads with fluctuating request arrival rates and variable request lengths. To efficiently execute coexisting compute-intensive and memory-intensive operators, near-memory processing (NMP) based computing paradigm has been extensively proposed. However, existing NMP designs adopt coarse-grained KV cache management and inflexible attention execution flow. Such limitations hinder these proposals from efficiently handling \textit{highly dynamic} LLM serving workloads, limiting their ability to accelerate LLM serving.   To tackle these problems, we propose Helios, a Hybrid-bonding-based \uline{L}LM \uline{S}erving accelerator. Helios aims to bridge the fundamental gap between the dynamic nature of KV cache management in LLM serving and the distributed, non-uniform memory abstraction among NMP processing engines (PEs). To this end, we design both the intra-PE execution flow and the inter-PE communication primitives for distributed tiled attention execution. We further propose \textit{spatially-aware} KV cache allocation mechanism to balance the attention workload distribution while minimizing the inter-PE data transfer overhead. Compared with existing GPU/NMP designs, Helios achieves 3.25 times (geomean) speedup and 3.36 times (geomean) better energy efficiency, along with up to 72%/76% P50/P99 time-between-tokens degradation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hardware-Software Co-design for 3D-DRAM-based LLM Serving Accelerator》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）推理服务面临高度动态的挑战，包括波动的请求到达率和差异巨大的序列长度（从几百到上万 token）。
*   **核心矛盾**：
    *   **内存瓶颈**：LLM 的解码阶段（Decoding）是内存受限的，传统的 GPU 带宽不足。
    *   **现有 NMP 局限性**：现有的近存计算（NMP）方案通常采用静态、粗粒度的 KV 缓存管理，将注意力头固定在特定内存模块。这导致了严重的**计算负载不均衡**和**内存碎片化**，无法应对动态变化的 KV 缓存需求。
*   **整体含义**：本文旨在通过软硬件协同设计，打破 NMP 架构中“非均匀内存抽象”与 LLM 服务“动态特性”之间的隔阂，提出一种灵活、高效的加速器架构。

### 2. 方法论：核心思想与关键技术
Helios 提出了一个基于**混合键合（Hybrid-bonding）**技术的软硬件协同设计方案：
*   **硬件架构（HB-Device）**：
    *   利用混合键合技术将 4 层 DRAM 堆叠在逻辑芯片上，提供极高的 I/O 密度和带宽。
    *   逻辑芯片包含一个 4×4 的处理引擎（PE）阵列，每个 PE 拥有独立的矩阵单元、向量单元和在线 Softmax 单元。
*   **分布式分块注意力（Distributed Tiled Attention）**：
    *   **核心思想**：将 KV 缓存划分为细粒度的块（Blocks），并将其分布在所有 PE 上。
    *   **执行流**：设计了 PE 内的流水线执行流，通过双缓冲技术隐藏向量操作（如 Softmax）的延迟。
*   **空间感知 KV 缓存分配（Spatially-aware Allocation）**：
    *   **算法流程**：根据 PE 的实时负载（Token 数量）和物理位置（曼哈顿距离）动态分配 KV 块。
    *   **优化目标**：优先平衡计算负载，同时将“尾块”（Last Block）放置在靠近中心 PE 的位置，以最小化 PE 间通信的跳数和数据传输开销。
*   **通信原语**：针对 LLM 的不同算子（MHA、FFN、MoE、MLA）设计了基于 Mesh 拓扑的高效通信模式（如 2D Reduce-Scatter, 1D All-Reduce）。

### 3. 实验设计
*   **数据集/场景**：采用生产环境轨迹（MoonCake 轨迹）进行采样，模拟真实的请求到达（泊松分布）和长度分布。
*   **测试模型**：涵盖了多种主流架构：
    *   Dense 模型：OPT 66B, LLaMA3 70B。
    *   MoE 模型：Mixtral 8x22B, Qwen3 30B-A3B。
    *   MLA 架构：DeepSeek 236B（V2/V3 架构）。
*   **对比基准（Benchmarks）**：
    *   **GPU**：A100-80GB（运行 SGLang + FlashInfer）。
    *   **In-die NMP**：类似 AttAcc 的设计（PE 集成在 DRAM 芯片内部）。
    *   **Duplex**：基于 Buffer-die 的 NMP 设计。
    *   **HB-non-Tiled**：基于混合键合但采用静态非分块注意力的方案（类似 Stratum）。

### 4. 资源与算力
*   **算力平台**：论文基于仿真实验。
    *   **逻辑芯片**：采用 12nm 工艺，频率 1GHz。
    *   **内存**：80GB 总容量，通过 4 层 3D 堆叠实现。
    *   **仿真工具**：Ramulator2（DRAM 仿真）、BookSim（NoC 仿真）、Hotspot 7.0（热力学仿真）。
*   **训练时长**：未提及训练时长，因为本文聚焦于**推理加速器**的硬件设计与评估，不涉及模型训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 5 种不同规模和架构的模型。
    *   涵盖了从低负载到高负载（Stress Test）的多种请求到达率。
    *   进行了详细的消融实验和敏感性分析（如 KV 块大小、计算-带宽分配比例、PE 阵列粒度）。
*   **充分性与客观性**：实验考虑了**热力学约束**（确保芯片温度低于 75°C），并对比了最新的工业级推理框架（SGLang），实验设计较为严谨、客观，充分考虑了硬件实现的物理限制。

### 6. 主要结论与发现
*   **性能提升**：Helios 相比现有最优的 GPU/NMP 方案，实现了 **3.25 倍**的几何平均加速。
*   **能效比**：能效提升了 **3.36 倍**。
*   **服务质量（QoS）**：在动态负载下，P50 和 P99 的逐字生成延迟（TBT）分别降低了高达 72% 和 76%。
*   **负载均衡**：空间感知分配机制将计算不平衡率控制在 13.3% 以内，远优于传统 NMP 方案。
*   **内存利用率**：通过细粒度管理，显著消除了内存碎片，吞吐量提升了 1.19x-7.33x。

### 7. 优点
*   **软硬协同**：不仅改进了硬件架构（混合键合），还针对性地重构了软件调度算法（空间感知分配）。
*   **架构灵活性**：成功支持了复杂的 MLA（多潜变量注意力）和 MoE 架构，证明了其通用性。
*   **物理可行性**：详细分析了面积、功耗和散热，证明了 3D 堆叠方案在工程上的可行性。

### 8. 不足与局限
*   **异构依赖**：Helios 主要针对解码阶段（Decoding），预填充阶段（Prefill）仍需依赖 GPU 或其他计算密集型设备，这增加了系统集成的复杂性。
*   **成本与工艺**：混合键合（Hybrid Bonding）属于前沿封装技术，目前的制造成本和良率挑战可能限制其大规模商用。
*   **仿真局限**：所有结论均基于仿真器，虽然使用了业界公认的工具，但与真实流片后的物理表现可能存在细微偏差。

（完）
