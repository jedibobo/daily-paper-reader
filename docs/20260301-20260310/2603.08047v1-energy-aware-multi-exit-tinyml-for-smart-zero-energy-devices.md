---
title: Energy-Aware Multi-Exit TinyML for Smart Zero-Energy Devices
title_zh: 面向智能零能耗设备的能量感知多出口 TinyML
authors: "Shahab Jahanbazi, Mateen Ashraf, Lieven De Strycker, Jeroen Famaey, Onel L. A. Lopez"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08047v1"
tags: ["query:hwnas"]
score: 9.0
evidence: 针对零能耗设备的能效感知多出口TinyML架构
tldr: 针对零能耗设备（ZED）在内存、能量和精度方面的严苛限制，本文提出了一种能量感知的多退出点TinyML框架。通过在单个模型中设置多个推理出口，实现根据输入难度动态调整计算量，并结合辅助能量感知电路调节系统运行。该方法在保持检测精度的同时，显著降低了能耗，为实现高效、自主的边缘智能提供了新方案。
selection_source: fresh_fetch
motivation: 零能耗设备在执行边缘智能任务时面临内存受限、能量供应不稳定以及推理精度要求之间的矛盾。
method: 设计并部署了一个具有多个退出点的多分支TinyML模型，并引入辅助能量感知电路以根据可用能量动态调节系统操作。
result: "与最先进的能量感知单退出点TinyML方法相比，该方法在保证精度的前提下实现了约29.6%的能耗降低。"
conclusion: 该框架通过多退出点架构和能量感知电路的结合，有效提升了零能耗设备在边缘智能任务中的能效和自主性。
---

## 摘要
智能与自主系统的普及推动了将智能直接在边缘设备上执行的转变。这种转变对于零能耗设备 (ZEDs) 而言尤其具有挑战性，因为必须同时解决内存、能量可用性和推理准确性方面的严苛约束。在本文中，我们提出了一种为智能 ZEDs 管理这些约束的统一方法。具体而言，我们设计、训练并在 ZED 上部署了一个用于行人检测的微型机器学习 (TinyML) 模型。所提出的架构在内存中存储单个模型，同时通过多个出口点实现自适应推理，使计算开销能够随输入难度而缩放。因此，对于简单实例执行低能耗推理，而对于较难案例则选择性地采用高精度推理。该策略在不牺牲检测准确性的情况下显著降低了能耗。此外，为了增强设备的自主性并防止电源故障，我们引入了辅助能量感知电路，根据可用能量动态调节系统运行。与最先进的能量感知单出口 TinyML 方法相比，所提方法实现了约 29.6% 的能耗降低。总之，所提出的框架对于在 ZED 平台上实现准确且能效高的智能具有重要意义。

## Abstract
The proliferation of smart and autonomous systems has motivated a shift toward executing intelligence directly on edge devices. This shift becomes particularly challenging for zero-energy devices (ZEDs), where severe constraints on memory, energy availability, and inference accuracy must be addressed simultaneously. In this paper, we present a unified approach to managing these constraints for smart ZEDs. Specifically, we design, train, and deploy a tiny machine learning (TinyML) model for person detection on a ZED. The proposed architecture stores a single model in memory while enabling adaptive inference through multiple exit points, allowing computational effort to scale with input difficulty. As a result, low-energy inference is performed for easy instances, while higher-precision inference is selectively employed for harder cases. This strategy significantly reduces energy consumption without sacrificing detection accuracy. Furthermore, to enhance device autonomy and prevent power failures, we introduce auxiliary energy-aware circuits that dynamically regulate system operation based on available energy. Compared with a state-of-the-art energy-aware single-exit TinyML approach, the proposed method achieves an energy consumption reduction of approximately $29.6\%$. Overall, the proposed framework is appealing for enabling accurate and energy-efficient intelligence on ZED platforms.

---

## 论文详细总结（自动生成）

这篇论文《Energy-Aware Multi-Exit TinyML for Smart Zero-Energy Devices》针对零能耗设备（ZED）在执行边缘智能任务时面临的资源瓶颈，提出了一种软硬件协同设计的优化方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：零能耗设备（ZED）通过环境能量采集（如太阳能）驱动，无需电池，但面临三大严苛约束：**极小的内存（KB级）**、**不稳定的能量供应（间歇性工作）**以及**推理精度的要求**。
*   **核心矛盾**：传统的深度学习模型能耗固定且内存占用大；现有的多模型切换方案虽能节省能量，但会成倍增加内存负担。
*   **整体含义**：本文旨在通过“多出口（Multi-Exit）”架构，在单个模型内实现计算量的动态缩放，结合能量感知的硬件电路，使设备能在极低能耗下维持高精度检测。

### 2. 方法论
*   **核心思想**：采用**分支网络（BranchyNet）**架构。将模型分为“早退出口（EX1）”和“深层出口（EX2）”。简单样本在浅层即可完成推理以省电，复杂样本则进入深层以保精度。
*   **关键技术细节**：
    *   **多出口模型设计**：基于 MobileNet V1，在中间层插入轻量级分类器。通过加权损失函数 $L = \sum \lambda_j L_j$ 进行联合训练。
    *   **模型压缩**：依次应用**结构化剪枝**和**量化感知训练（PQAT）**，将模型转化为 8-bit 整数格式，以适配 MCU 的 Flash 和 RAM。
    *   **能量感知执行策略**：在推理前测量电容电压。若能量充足且 EX1 输出置信度低（处于阈值区间 $[\gamma_1, \gamma_2]$ 内），则追加测量能量并决定是否激活 EX2。
    *   **硬件优化**：设计了基于 **MOSFET 的电源门控电路**，用于在空闲时彻底切断摄像头模块的供电，比传统的负载开关更高效。

### 3. 实验设计
*   **任务与数据集**：行人检测（Person Detection）。使用 **COCO (Visual Wake Words - VWW)** 数据集进行训练和验证。
*   **硬件平台**：Arduino Nano 33 BLE Sense (nRF52840 芯片，64MHz Cortex-M4F)，Arducam OV7675 摄像头，e-peas PMU 能量管理单元。
*   **Benchmark（基准）**：对比了 Sabovic 等人提出的 SOTA（最先进）能量感知单出口 TinyML 方法（2023/2025年研究）。
*   **对比维度**：能耗、推理准确率、内存占用、任务完成数量。

### 4. 资源与算力
*   **训练端**：使用 TensorFlow/Keras 框架。文中未明确说明具体的 GPU 型号和训练时长，但提到了 Batch Size 为 64，采用了指数衰减的学习率策略。
*   **部署端（算力受限）**：部署在仅有 1MB Flash 和 256KB SRAM 的单片机上。模型通过 TFLite 转换器优化，确保在毫瓦级功耗下运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对不同宽度乘数（$\alpha=0.25$ 到 $1.0$）的模型进行了性能评估。
    *   对 5000 张验证集图像进行了置信度阈值（$\gamma_1, \gamma_2$）的消融实验，分析了精度与能耗的权衡。
    *   在不同采集电流（1mA 到 5mA）和不同电容容量下进行了长时间运行测试。
*   **充分性评价**：实验设计较为充分，涵盖了从模型训练、硬件电路功耗剖析到真实环境下的动态能量采集模拟，对比实验考虑了统计显著性（多次实验取平均值）。

### 6. 主要结论与发现
*   **能耗降低**：与单出口 SOTA 方法相比，所提方案在保持相当精度的情况下，**总能耗降低了约 29.6%**。
*   **硬件效率**：提出的 MOSFET 电源门控方案比现有的负载开关方案在图像采集阶段**节能约 34%**。
*   **自适应优势**：多出口模型能有效处理简单样本，在能量极度匮乏时，通过“早退”机制避免了系统因能量耗尽而崩溃（Brownout），提升了任务完成率。

### 7. 优点
*   **软硬协同**：不仅优化了算法架构，还改进了底层监控电路，解决了 ZED 设备的实际痛点。
*   **内存友好**：通过单模型多出口设计，避免了存储多个模型带来的内存溢出问题。
*   **高实用性**：在真实的 MCU 硬件上完成了全流程部署验证，而非仅停留在仿真阶段。

### 8. 不足与局限
*   **任务单一性**：实验仅限于二分类（行人检测），对于多分类或更复杂的视觉任务，多出口的置信度阈值调节可能更加复杂。
*   **手动调参风险**：置信度阈值 $\gamma$ 的设定依赖于离线分析，若实际应用场景与训练集偏差较大，可能导致推理策略失效。
*   **测量开销**：虽然文中称能量测量开销可忽略，但在极高频率的推理场景下，频繁的 ADC 采样仍可能累积一定的额外负担。

（完）
